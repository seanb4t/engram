---
phase: 03.1
reviewers: [codex]
reviewed_at: 2026-08-11T01:46:00Z
plans_reviewed: [03.1-00-PLAN.md, 03.1-01-PLAN.md, 03.1-02-PLAN.md, 03.1-03-PLAN.md, 03.1-04-PLAN.md, 03.1-05-PLAN.md]
---

# Cross-AI Plan Review — Phase 03.1

> **Convergence loop.** This file is append-only across cycles. Cycle 1 reviewed the 5-plan set;
> Cycle 2 (below, after the horizontal rule near the end) reviews the revised 6-plan set. Read both.

## Cycle 1 — 5-plan set (2026-08-10T21:45:58Z)

Single-reviewer cycle (`--codex`). Codex received the full source-grounding prompt plus an
explicit correction brief: D-06 was falsified mid-flight, D-15's reconciliation pass is the
correctness core, and SC3/REQ-merge-atomicity now read "not observable" rather than
"unreachable". Its review is against the corrected design.

## Codex Review

## Summary

The five-plan decomposition is strong: tracer-first pluralization, failure reconciliation, authorization/error semantics, idempotency, then published-contract drift gates. It correctly abandons the falsified “multi-ID `SetPayload` is atomic” premise and recognizes that dangling `superseded_by` values are especially damaging because four recall paths filter on key presence.

However, the plans are not execution-ready. Plan 01 omits required interface/fake migrations. Plan 02 lacks a workable seam for forcing the actual mid-operation failure or compensation failure it claims to prove, and compensation cannot make partial state literally unobservable to lock-free readers. Plan 03’s proposed resolution algorithm contains an impossible step and retains an owner-agnostic ambiguous-short-ID disclosure. Plan 04 leaves its most load-bearing staging refactor undecided during execution. The invented 256-target cap is implemented three waves before human approval.

Overall risk: **HIGH** until these blockers are corrected.

## Strengths

- The revised atomicity analysis matches the real damage mechanism. `Search`, `SearchDiscovery`, `List`, and `ListScheduled` all suppress any record carrying the key, regardless of whether its successor exists: [store.go:954](/Volumes/Code/github.com/seanb4t/engram/internal/store/store.go:954), [store.go:1054](/Volumes/Code/github.com/seanb4t/engram/internal/store/store.go:1054), [store.go:1191](/Volumes/Code/github.com/seanb4t/engram/internal/store/store.go:1191), and [store.go:1426](/Volumes/Code/github.com/seanb4t/engram/internal/store/store.go:1426). Plan 02 is right that merely deleting the survivor would permanently hide stamped predecessors.

- Plan 01 correctly treats pluralization as a full persistence seam. The current type, writer, and decoder are all scalar at [store.go:177](/Volumes/Code/github.com/seanb4t/engram/internal/store/store.go:177), [store.go:498](/Volumes/Code/github.com/seanb4t/engram/internal/store/store.go:498), and [store.go:592](/Volumes/Code/github.com/seanb4t/engram/internal/store/store.go:592). A tolerant decoder is necessary because the current unconditional `GetStringValue` path would silently lose list data.

- Sorted, deduplicated lock acquisition is the right local concurrency strategy. The current operation holds one keyed lock through its check/write sequence at [store.go:2009](/Volumes/Code/github.com/seanb4t/engram/internal/store/store.go:2009), while the locker is a keyed, non-reentrant mutex abstraction. Preserving that interface while acquiring canonical IDs in sorted order avoids overlapping-set deadlocks.

- The plans preserve the store-tier authorization recheck rather than trusting handler preflight. The actual write gate is `getWritable`, which deliberately collapses nonexistent and unauthorized records to `ErrNotFound`: [store.go:1621](/Volumes/Code/github.com/seanb4t/engram/internal/store/store.go:1621). The current handler also gates before embedding at [tools.go:1746](/Volumes/Code/github.com/seanb4t/engram/internal/server/tools.go:1746), so retaining both layers is correct.

- Plan 04 correctly preserves `idempotencyPointID`. The existing point ID is derived from owner, scope, and key at [tools.go:977](/Volumes/Code/github.com/seanb4t/engram/internal/server/tools.go:977), while request differences are detected through the stored fingerprint at [tools.go:985](/Volumes/Code/github.com/seanb4t/engram/internal/server/tools.go:985). Target-set identity belongs in the fingerprint, not in a changed lookup ID.

- The documentation plan explicitly recognizes that `docs-site/**` is excluded from markdown lint and adds a semantic drift test instead. That is substantially stronger than relying on `task lint:markdown`.

- No `must_haves.pattern` entry contains a backslash escape. The patterns consistently use character classes such as `[(]` and `[.]`, so they avoid the known parser hazard.

## Concerns

### Plan 03.1-01

- **HIGH — Plan 01 omits required production and test-interface files.** Task 1 changes `Store.Supersede` from a scalar target to a slice, but `files_modified` omits both `internal/server/store_iface.go` and `internal/server/fakestore_test.go`. The production interface still declares the scalar signature at [store_iface.go:39](/Volumes/Code/github.com/seanb4t/engram/internal/server/store_iface.go:39), and the compile-time assertion at [store_iface.go:47](/Volumes/Code/github.com/seanb4t/engram/internal/server/store_iface.go:47) will immediately fail. The spy implementation also has the scalar signature at [fakestore_test.go:214](/Volumes/Code/github.com/seanb4t/engram/internal/server/fakestore_test.go:214). “Call-site sweep (both test files)” is therefore incomplete.

- **HIGH — The spy’s behavior must be pluralized, not merely its signature.** The spy currently checks and stamps one target at [fakestore_test.go:219](/Volumes/Code/github.com/seanb4t/engram/internal/server/fakestore_test.go:219). Handler tests using it cannot prove multi-target authorization, all-target preflight, or multi-target stamping unless its implementation is updated consistently.

- **MEDIUM — Plan 01 knowingly lands an unsafe intermediate implementation.** It changes production to multi-ID `SetPayload` while deferring reconciliation to Plan 02. The current method simply returns the stamp error at [store.go:2037](/Volumes/Code/github.com/seanb4t/engram/internal/store/store.go:2037). After Plan 01, the branch can leave dangling predecessor links—the exact correctness failure this phase exists to prevent. If each plan is committed independently, Wave 1 produces a knowingly unsafe repository state.

- **LOW — The proposed grep backstop is brittle.** “Every non-test Go read routes through the decoder” is worthwhile, but exact hit counts will drift with comments and documentation. An AST-level or focused codec test is more durable than a global textual count.

### Plan 03.1-02

- **HIGH — The proposed TOCTOU trigger does not exercise the claimed window.** Task 2 suggests deleting the target “as a side effect of releasing the last acquired lock.” But `Store.Supersede` defers unlock until the method returns; release occurs after `SetPayload`, too late to create the target race. The alternative—deleting between an explicit `getWritable` and a subsequent `Supersede` call—also fails, because `Supersede` performs its own under-lock `getWritable` at [store.go:2018](/Volumes/Code/github.com/seanb4t/engram/internal/store/store.go:2018) and will reject before `Upsert`. The existing test’s “end-to-end” case has this same limitation: it deletes before entry and only proves the initial gate at [store_test.go:3581](/Volumes/Code/github.com/seanb4t/engram/internal/store/store_test.go:3581).

- **HIGH — A deliberate production seam is required.** To prove failure after preflight and after survivor creation, the plan needs a hook between `Upsert` and the multi-ID `SetPayload`, or an injectable points client whose `SetPayload` can execute a partial mutation then return an error. The current plan explicitly claims “no new fault-injection seam needed,” but the source contradicts that.

- **HIGH — Compensation failure logging is not testable with the current design.** The plan requires tests for survivor-delete failure and reconciliation failure while preserving the original stamp error. The current store owns its Qdrant client directly and exposes no described mutation fault script. A naturally missing target can trigger stamp failure, but Qdrant will normally be healthy enough for compensation to succeed. An outage severe enough to break cleanup also prevents deterministic setup and observation.

- **HIGH — “Not observable” is stronger than the proposed mechanism.** Store locks serialize writers, not readers. `Get` directly calls Qdrant without acquiring a target lock at [store.go:1501](/Volumes/Code/github.com/seanb4t/engram/internal/store/store.go:1501), and all recall queries are likewise lock-free. After Qdrant partially applies `SetPayload` and before reconciliation deletes the keys, a concurrent reader can observe hidden predecessors. The proposed tests check terminal state only. The plan can prove “no partial state remains after the call returns when compensation succeeds,” not literal non-observability.

- **MEDIUM — `reconcileSupersedeFailure`’s return shape is underspecified.** Returning only `dangling []string` must distinguish not-found from read failure and clear failure. The plan says not-found is harmless, but transport errors must cause the target to be reported as potentially dangling. That exact classification should be specified and tested.

- **MEDIUM — `Store.Delete` adds another authorization decision during rollback.** `Delete` re-runs `getWritable` using `ActionDelete` at [store.go:2095](/Volumes/Code/github.com/seanb4t/engram/internal/store/store.go:2095). A policy that permits creation/write but denies delete could prevent compensation. A private internal raw-delete primitive scoped to the freshly-created survivor may be more correct, or the plan must explicitly accept and test this policy interaction.

- **MEDIUM — Vacuous test gates remain.** Plan 02 accepts `go test ./internal/store/... -count=1` by exit status alone. Because Go returns success when a `-run` pattern matches nothing, every named-test gate should count `--- PASS`; the broad suite command can remain a supplemental gate, not evidence that a specific proof ran.

### Plan 03.1-03

- **HIGH — Class C contains an impossible operation.** The plan says inputs whose `ResolvePointID` returned not-found should “carry forward” and then be passed to `FetchForUpdate`. But no canonical ID exists for those inputs. Source confirms missing short IDs return `ErrNotFound` without an ID at [store.go:1564](/Volumes/Code/github.com/seanb4t/engram/internal/store/store.go:1564). The algorithm must instead collect resolution-not-found inputs directly into the same Class C bucket while calling `FetchForUpdate` only for successfully resolved IDs.

- **HIGH — Ambiguous short IDs remain an unauthenticated existence signal.** `ResolvePointID` is explicitly owner-agnostic at [store.go:1530](/Volumes/Code/github.com/seanb4t/engram/internal/store/store.go:1530), scrolls the global collection by short ID at [store.go:1555](/Volumes/Code/github.com/seanb4t/engram/internal/store/store.go:1555), and returns `ErrAmbiguousShortID` when at least two records match at [store.go:1569](/Volumes/Code/github.com/seanb4t/engram/internal/store/store.go:1569). Reporting Class B before ownership therefore reveals that a guessed handle has multiple global matches. The plan preserves full indistinguishability only for Class C, not across the entire target resolution process.

- **HIGH — PD-07 is implemented before approval.** The 256-target cap is a new externally-visible rejection for calls that are currently unbounded. Plan 03 is autonomous in Wave 2; human review occurs only in Plan 05 after implementation and documentation. That checkpoint is too late for a costly published contract decision.

- **MEDIUM — The cap is not tied to generated schema constraints.** Runtime validation and documentation are specified, but the JSON schema is not required to advertise `maxItems: 256` or `minItems: 1`. Agents relying on the advertised MCP schema will not learn the restriction until runtime.

- **MEDIUM — The cap does not address the Qdrant risk used to motivate it.** It still permits eight server chunks at the documented size of 32. Its only defensible purpose is request-amplification control, but 256 sequential resolve/fetch operations plus 256 held locks may itself be excessive. The plan should justify the number with a latency or resource budget.

- **MEDIUM — Store-tier offender remapping is fragile.** Plan 03 asks the handler to parse store error contents containing resolved IDs and map them back to inputs. Sentinel errors are not structured offender lists today; the existing store returns formatted prose at [store.go:2026](/Volumes/Code/github.com/seanb4t/engram/internal/store/store.go:2026). Parsing or substring matching would be brittle. Introduce a typed multi-target error carrying canonical IDs internally.

- **LOW — One acceptance gate relies on bare suite exit status.** `go test ./internal/server/... -count=1 exits 0` is useful as a broad regression check but not as evidence that the named rejection tests ran.

### Plan 03.1-04

- **HIGH — The load-bearing staging API is left undecided during execution.** Task 2 says to either add a hook to `resolveSupersedeTargets` or split it into two functions. This determines whether replay truly runs between ownership and already-superseded validation and should be fixed in the plan, not delegated to implementation judgment.

- **MEDIUM — PD-09 satisfies D-14 narrowly, but the contract needs explicit wording.** Resolving and ownership-gating before replay does not violate “before already-superseded,” and a normal retry after success still works because supersession does not delete its targets. However, a retry after targets are later deleted or ownership changes will not replay. That is a deliberate weakening of ordinary idempotent replay semantics and must be surfaced in the public contract, not only in a test.

- **MEDIUM — The proposed fingerprint duplicates the full content encoding.** `contentFingerprint` already owns the canonical field list at [idempotency.go:64](/Volumes/Code/github.com/seanb4t/engram/internal/server/idempotency.go:64). Copying its “exact field list and order” into `mergeFingerprint` creates two implementations that can drift. Prefer composing the existing content fingerprint with a length-prefixed canonical target encoding.

- **MEDIUM — Concurrent identical keyed merges are insufficiently specified.** Existing store-memory idempotency relies on deterministic IDs and post-write rereads, but multi-target calls also race on target locks and already-superseded state. The plan tests sequential replay, not two simultaneous identical keyed calls. One caller could pass the initial replay lookup, wait on locks, then hit already-superseded. A concurrency test should require both callers to return the same persisted result.

- **LOW — Two acceptance gates rely on broad bare exit status.** The package-wide `go test ... -count=1` checks are fine as supplemental regression gates, but named tests should remain the proof.

### Plan 03.1-05

- **HIGH — Documentation overstates terminal compensation if Plan 02 remains best-effort.** Saying a failed merge is “usually observably a no-op” is acceptable only if the immediately following sentence explains that cleanup failure can leave a survivor, dangling links, or both. The source has no durable retry/recovery queue, so operator logging is the only remediation.

- **MEDIUM — `task lint:markdown` is not evidence for docs-site correctness.** The plan acknowledges this at Plan 05 Task 1, but still includes `task lint:markdown` as a docs acceptance criterion. Keep it as a general repository gate, while making the semantic drift test and explicit content assertions the only evidence for `docs-site/**`.

- **MEDIUM — The drift test risks duplicating rather than invoking rendering logic.** “Calling the same wrapping the handler uses” is underspecified because current handler errors are assembled inline with `fmt.Errorf`, for example at [tools.go:1755](/Volumes/Code/github.com/seanb4t/engram/internal/server/tools.go:1755). A test that repeats the same `fmt.Errorf` expression can drift in parallel. Extract production rendering helpers and call those from both handler and test.

- **MEDIUM — Human approval arrives after all three disputed choices have shipped in code.** The checkpoint should precede Plan 03, especially for the cap and error class order. Plan 05 should verify approved decisions, not request first approval after implementation.

- **LOW — The stale-claim sweep is phrase-specific.** Testing only historical fragments can miss semantically equivalent stale wording. Retain it, but also positively assert the current key-support statement in every relevant document.

## Suggestions

1. **Amend Plan 01 file ownership and tasks.**
   - Add `internal/server/store_iface.go` and `internal/server/fakestore_test.go` to `files_modified`.
   - Explicitly pluralize `memStore.Supersede` and update the spy to preflight and stamp every target.
   - Add compile-time and spy multi-target tests.
   - Prefer landing Plan 01 and Plan 02 in the same atomic commit, or include minimal reconciliation in Plan 01 so no committed state introduces dangling-link behavior.

2. **Add a deterministic mutation fault seam in Plan 02.**
   - Introduce a narrowly-scoped injectable points-mutation wrapper or a test hook immediately after survivor `Upsert` and before back-stamp.
   - Script: partially stamp A/B, report C missing, optionally fail survivor delete, optionally fail selected `DeletePayload` calls.
   - Use it to prove original-error preservation and logging.
   - Keep the real-Qdrant test to prove Qdrant’s partial mutation semantics, but use the injected seam for complete production-flow coverage.

3. **Correct the atomicity claim.**
   - Replace “partially-applied merge is not observable” with:
     “After `Supersede` returns, successful reconciliation leaves no partial merge state; concurrent readers may observe the transient Qdrant state, and failed reconciliation is logged.”
   - If literal non-observability is mandatory, compensation is insufficient; the design needs a visibility/commit marker or staging protocol that recall filters understand.

4. **Specify reconciliation outcomes structurally.**
   - Return something like `{deleteErr error, readFailures []string, clearFailures []string, dangling []string}` internally.
   - Log all unresolved targets.
   - Treat `ErrNotFound` as resolved, transport errors as “possibly dangling.”
   - Consider an internal raw delete for the freshly-created survivor so rollback is not blocked by a second authorization decision.

5. **Rewrite Plan 03’s stage algorithm.**
   - Resolution stage should yield three buckets: resolved pairs, resolution-not-found inputs, and ambiguous inputs.
   - Class C begins with resolution-not-found inputs and appends resolved inputs whose `FetchForUpdate` returns not-found.
   - Never call `FetchForUpdate` without a canonical ID.

6. **Resolve ambiguous short-ID disclosure.**
   - Either perform owner-filtered resolution for this write path, or collapse ambiguous and not-found into the same caller-visible class.
   - Add a cross-owner collision test proving no global handle-count disclosure.

7. **Move the cap decision before Wave 2.**
   - Add a blocking checkpoint before Plan 03.
   - If approved, add `minItems: 1` and `maxItems: 256` to the generated MCP schema and test them.
   - Justify 256 with an explicit request-cost budget; otherwise omit the cap.

8. **Use typed internal multi-target errors.**
   - Have the store return a typed error carrying canonical offender IDs.
   - Map those IDs to original caller inputs in the handler without parsing prose.
   - Keep caller-facing sentinel identity unchanged.

9. **Fix Plan 04’s staging decision now.**
   - Split preflight into explicit functions:
     `resolveAndAuthorizeSupersedeTargets` → replay check → `validateSupersedeTargetState`.
   - This makes D-14 obvious and testable.
   - Add a concurrent identical-key merge test requiring both callers to return the same survivor.

10. **Compose fingerprints rather than duplicate field lists.**
    - Hash a length-prefixed `contentFingerprint(a)` plus a canonical target-set encoding.
    - Preserve `idempotencyPointID` and its namespace byte-for-byte.

11. **Strengthen Plan 05’s drift gate.**
    - Extract production rejection-rendering helpers and invoke them from both the handler and documentation test.
    - Treat `task lint:markdown` only as a general gate.
    - Positively assert current key-support language in addition to rejecting historical phrases.

12. **Replace remaining vacuous named-test risks.**
    - Every specific behavior gate should use a named `-run` expression and count `^--- PASS`.
    - Keep bare full-package exit status only as a supplemental regression gate.

## Risk Assessment

**Overall risk: HIGH.**

The architecture is directionally sound and the revised Qdrant analysis is correctly incorporated. The main problem is proof quality: Plan 02’s proposed test cannot reach the failure window it claims to test, compensation is transiently observable to readers, and cleanup-failure behavior lacks an injectable seam. Separately, Plan 01 will not compile as scoped because it misses the store interface and spy implementation, while Plan 03 contains an impossible resolution step and introduces a public cap before approval. These are execution blockers, not polish issues. Once the fault seam, interface migration, staging algorithm, ambiguity handling, and early cap decision are fixed, the plan set should fall to medium risk.

---

## Consensus Summary

Only one reviewer ran this cycle, so there is no cross-model consensus to synthesize. In its
place, every load-bearing Codex finding below was independently re-verified against the working
tree by the orchestrating agent; see **Verification coverage** for what was checked and what the
check found. Findings marked VERIFIED are grounded in source read this cycle, not in the
reviewer's assertion alone.

### Agreed Strengths

Single-reviewer cycle — strengths below are Codex's, orchestrator-verified where marked.

- The revised atomicity analysis matches the real damage mechanism. VERIFIED: exactly four
  recall gates filter on key *presence* (`internal/store/store.go:954`, `:1054`, `:1191`,
  `:1426`), all `qdrant.NewIsEmpty("superseded_by")`. A dangling stamp therefore soft-hides a
  live record permanently, independent of whether the successor exists. D-15's clause (b) is
  correctly identified as load-bearing, not belt-and-braces.
- Pluralization treated as a full persistence seam, with a tolerant decoder — necessary, since
  the current read path is unconditionally scalar.
- Sorted, deduplicated lock acquisition over *resolved* UUIDs is the right local strategy given
  a non-reentrant keyed locker, and it preserves the `TargetLocker` interface.
- Store-tier authz recheck retained rather than trusting handler preflight; `getWritable`
  collapses nonexistent and unauthorized to `ErrNotFound` and remains the real gate.
- Plan 04 correctly preserves `idempotencyPointID` and puts target-set identity in the
  fingerprint rather than in the lookup ID.
- Plan 05 explicitly recognizes that `docs-site/**` is excluded from rumdl. VERIFIED at
  `03.1-05-PLAN.md:145-146` — the plan says so in prose.
- No `must_haves` `pattern:` field contains a backslash escape. VERIFIED independently:
  `rg -n 'pattern:.*\\' .planning/phases/03.1-*/*-PLAN.md` returns zero hits across all 11
  `pattern:` entries in the five plans. The repo's known `parseMustHavesBlock` hazard is clear.

### Agreed Concerns

Highest-priority items, ordered by blocking weight. All are single-reviewer findings;
orchestrator verification status is noted per item.

1. **Plan 01 will not compile as scoped.** VERIFIED: `files_modified` (`03.1-01-PLAN.md:7-11`)
   lists only `internal/store/store.go`, `store_test.go`, `internal/server/tools.go`,
   `tools_test.go`. It omits `internal/server/store_iface.go`, whose `Supersede` is scalar at
   `store_iface.go:39`, and `internal/server/fakestore_test.go`, whose spy is scalar at
   `fakestore_test.go:214`. The compile-time interface assertion breaks on the first task.
2. **Plan 02's TOCTOU trigger cannot reach the window it claims to prove.** VERIFIED:
   `Store.Supersede` takes the lock and `defer unlock()`s (`store.go:2012-2016`), so release
   happens *after* `SetPayload` — deleting "as a side effect of releasing the last lock" is
   too late. The alternative pre-call delete is rejected by the method's own under-lock
   `getWritable` at `store.go:2020`. A deliberate fault seam between `Upsert` and the
   back-stamp is required; the plan explicitly claims none is needed.
3. **"Not observable" is still stronger than the mechanism delivers.** Store locks serialize
   writers only; `Get` and every recall query hit Qdrant lock-free. Between Qdrant's partial
   `SetPayload` and reconciliation, a concurrent reader *can* observe hidden predecessors. The
   plans' tests assert terminal state. The provable claim is "no partial state remains after
   the call returns when reconciliation succeeds, and reconciliation failure is logged".
4. **Plan 03's Class C staging contains an impossible step.** VERIFIED: inputs whose
   `ResolvePointID` returned not-found have no canonical ID — `store.go:1564` returns
   `ErrNotFound` with no ID — so they cannot be "carried forward" into `FetchForUpdate`.
5. **Ambiguous short IDs remain an unauthenticated existence signal.** VERIFIED:
   `ResolvePointID` is documented owner-agnostic (`store.go:1530-1534`), scrolls the global
   collection (`store.go:1555`), and returns `ErrAmbiguousShortID` on ≥2 matches
   (`store.go:1569`). Reporting Class B before ownership tells an unauthorized caller that a
   guessed handle has multiple global matches — 404-indistinguishability holds only for Class C.
6. **PD-07's 256 cap ships three waves before its approval checkpoint.** The cap is a new
   externally-visible rejection on a currently-unbounded input, implemented autonomously in
   Wave 2 (Plan 03) with human review only in Plan 05.
7. **Plan 04 leaves its load-bearing staging refactor undecided at execution time** ("either
   add a hook to `resolveSupersedeTargets` or split it"), which is precisely what determines
   whether D-14's ordering actually holds.
8. **Compensation-failure paths are asserted but not testable as designed** — the store owns
   its Qdrant client directly and no described seam can script a survivor-delete failure or a
   selective `DeletePayload` failure.

### Divergent Views

None — single reviewer. Two places where the orchestrator's verification *sharpened* rather
than contradicted Codex are recorded under Verification coverage.

---

## Verification coverage

Orchestrator re-checks performed this cycle against the working tree
(`/Volumes/Code/github.com/seanb4t/engram`, branch `feat/v0.13`):

| Claim | Method | Result |
|---|---|---|
| Four `IsEmpty("superseded_by")` recall gates | `rg -n 'IsEmpty\("superseded_by"\)' internal/store/store.go` | CONFIRMED — `:954`, `:1054`, `:1191`, `:1426` |
| Plan 01 `files_modified` omits `store_iface.go` / `fakestore_test.go` | read `03.1-01-PLAN.md:7-11` | CONFIRMED |
| `StoreIface.Supersede` is scalar | `store_iface.go:39` | CONFIRMED |
| `spyStore.Supersede` is scalar | `fakestore_test.go:214` | CONFIRMED |
| `Supersede` defers unlock past `SetPayload` | `store.go:2012-2041` | CONFIRMED — `defer unlock()` at `:2016`, `SetPayload` at `:2035` |
| Under-lock `getWritable` rejects pre-deleted target | `store.go:2020` | CONFIRMED |
| `ResolvePointID` owner-agnostic; no ID on not-found; ambiguous on ≥2 | `store.go:1530-1571` | CONFIRMED |
| No backslash escapes in any `must_haves` `pattern:` | `rg -n 'pattern:.*\\' .planning/phases/03.1-*/*-PLAN.md` | CONFIRMED — 0 hits / 11 `pattern:` entries |
| Plan 05 acknowledges rumdl excludes `docs-site` | `03.1-05-PLAN.md:145-146` | CONFIRMED — but `task lint:markdown exits 0` still stands as an acceptance criterion at `03.1-05-PLAN.md:160` and `:220` |
| Bare-exit-status test gates | `rg -n 'exits 0' .planning/phases/03.1-*/*-PLAN.md` | CONFIRMED — `03.1-02-PLAN.md:290`, `03.1-03-PLAN.md:323`, `03.1-04-PLAN.md:337`, `03.1-04-PLAN.md:366` gate on `go test ... -count=1` exit status; only `:366` adds a `--- FAIL`/`--- SKIP` count. None counts `--- PASS`. |

Two orchestrator additions beyond Codex's review:

- **`rg -c` counts matching *lines*, not occurrences.** Plan 05's automated gates
  (`03.1-05-PLAN.md:150-155`) assert `rg -c '<phrase>' <file>` **equals 1**. If the phrase ever
  appears twice on one line, or the intended assertion is "exactly one occurrence", `-c` is the
  wrong instrument — the repo convention is `rg -o '<pat>' <file> | wc -l`. This is the same
  class of vacuous-gate defect as the `go test` exit-status hazard and should be fixed in the
  same pass.
- **Codex's "no `must_haves` pattern escapes" strength is independently confirmed**, so that
  known repo hazard is genuinely clear this cycle and needs no plan edit.

---

# Cross-AI Plan Review — Phase 03.1 — Cycle 2

Second convergence cycle (`--codex`), against the revised **6-plan** set (commit `24c654b6`,
+1,274 insertions over the 5-plan set). Codex received the full source-grounding prompt plus a
change brief naming what the revision did, which two cycle-1 findings were deliberately REJECTED
and where the rejection reasoning is recorded, and the five focus areas for this cycle.

**Headline:** every one of cycle 1's 11 HIGH findings is RESOLVED. Three **new** HIGH defects were
introduced by the revision itself, all of them plan-level correctness contradictions rather than
implementation detail. Verdict remains **NOT execution-ready** — but for a different, smaller, and
fully bounded set of reasons.

## Codex Review

## Summary

The revision resolves most cycle-1 blockers: the approval gate now precedes implementation, interface/fake migrations are scoped, the Qdrant failure proof has a credible injection seam, ambiguity is collapsed behind the indistinguishable addressability/access class, and the atomicity claim is correctly limited to reconciled terminal state.

However, the plans are still **not execution-ready**. Three new implementation blockers remain:

1. Plan 01’s compensation test cannot reach the back-stamp failure branch.
2. Plan 02 specifies nil function fields but calls them directly, which would panic in production.
3. Plan 04’s simultaneous keyed-merge guarantee is impossible with the described ordering; the losing call reaches `ErrAlreadySuperseded` before any post-write reread.

**Execution-readiness verdict: NOT READY.** The architecture is close, but these are plan-level correctness defects rather than implementation details.

## Cycle 1 disposition

| Cycle-1 HIGH finding | Disposition | Evidence |
|---|---|---|
| Plan 01 omitted `store_iface.go` and `fakestore_test.go` | RESOLVED | Both are now in Plan 01’s `files_modified`; this covers the scalar interface at [store_iface.go:39](/Volumes/Code/github.com/seanb4t/engram/internal/server/store_iface.go:39) and compile-time assertion at [store_iface.go:47](/Volumes/Code/github.com/seanb4t/engram/internal/server/store_iface.go:47). |
| Spy behavior remained single-target | RESOLVED | Plan 01 explicitly rewrites the implementation currently checking/stamping only one target at [fakestore_test.go:214](/Volumes/Code/github.com/seanb4t/engram/internal/server/fakestore_test.go:214)–[232](/Volumes/Code/github.com/seanb4t/engram/internal/server/fakestore_test.go:232), with a second-member rejection test. |
| Proposed TOCTOU trigger could not reach the window | RESOLVED | Plan 02 now injects the mutation itself instead of relying on lock release. This correctly accounts for the lock spanning the existing check/write sequence and the under-lock `getWritable` in [store.go:2018](/Volumes/Code/github.com/seanb4t/engram/internal/store/store.go:2018). |
| A deliberate production fault seam was required | RESOLVED | Plan 02 introduces function-field seams following the existing pattern at [store.go:341](/Volumes/Code/github.com/seanb4t/engram/internal/store/store.go:341)–[348](/Volumes/Code/github.com/seanb4t/engram/internal/store/store.go:348). The concept is sound, although its nil-default invocation is currently defective; see Concerns. |
| Compensation-failure logging was not testable | RESOLVED | The proposed seams independently script partial stamp, survivor-removal failure, and selective clear failure while keeping Qdrant otherwise healthy. |
| “Not observable” exceeded the mechanism | RESOLVED | The claim is now terminal-state-only. This matches lock-free `Get` at [store.go:1501](/Volumes/Code/github.com/seanb4t/engram/internal/store/store.go:1501) and the documented writer-only, single-process locker at [locker.go:18](/Volumes/Code/github.com/seanb4t/engram/internal/store/locker.go:18)–[24](/Volumes/Code/github.com/seanb4t/engram/internal/store/locker.go:24). |
| Resolution-not-found was incorrectly carried into `FetchForUpdate` | RESOLVED | Plan 03 now keeps unresolvable inputs in a separate offender bucket and calls `FetchForUpdate` only for canonical IDs. This matches `ResolvePointID` returning no ID on absence at [store.go:1564](/Volumes/Code/github.com/seanb4t/engram/internal/store/store.go:1564)–[1567](/Volumes/Code/github.com/seanb4t/engram/internal/store/store.go:1567). |
| Ambiguous short IDs leaked global existence | RESOLVED | Plan 03 collapses `ErrAmbiguousShortID` into the same not-found class. That closes the oracle created by owner-agnostic global resolution at [store.go:1530](/Volumes/Code/github.com/seanb4t/engram/internal/store/store.go:1530)–[1570](/Volumes/Code/github.com/seanb4t/engram/internal/store/store.go:1570). |
| Target cap shipped before approval | RESOLVED | Plan 00 is Wave 1; Plans 01 and 03 both depend on `03.1-00`. Plan 03 conditionally implements and advertises the ruling. |
| Idempotency staging API was left undecided | RESOLVED | Plan 03 now mandates two functions, with Plan 04’s replay check structurally between authorization and state validation. |
| Documentation overstated best-effort compensation | RESOLVED | Plan 05 requires the failure paragraph to name orphan survivor, dangling links, absence of a recovery queue, operator logging, and transient reader observability. |

## Strengths

- The revised atomicity claim is meaningful, not vacuous. It promises a concrete postcondition after successful reconciliation: survivor absent, no requested predecessor still pointing at it, and recall restored. Those assertions directly cover the four soft-hide filters discussed by the plans.

- The partial-stamp seam is the right kind of seam. A wrapper can use the real Qdrant mutation on a subset and then return the scripted error. Coupled with the raw multi-ID falsification test, this proves both facts needed: Qdrant really can partially mutate, and the production reconciliation path repairs that state.

- Rejecting the use of authorization-gated `Store.Delete` for rollback is justified. The current delete path applies a distinct delete authorization decision at [store.go:2082](/Volumes/Code/github.com/seanb4t/engram/internal/store/store.go:2082). Restricting an ungated primitive to the freshly minted survivor avoids making rollback correctness depend on future policy changes.

- The rejection order now protects the full resolution path, not merely the later ownership fetch. Set-shape validation is the only pre-record class; ambiguity, absence, and non-ownership share one caller-visible sentinel and original-input rendering.

- Plan 00 genuinely gates the disputed contracts. Plans 01 and 03 depend on it, Plan 05 verifies rather than first approves it, and the conditional schema/docs obligations are explicit.

- The repository-specific gate hazards are handled well:

  - `must_haves.pattern` entries are backslash-free.
  - Named behavioral tests count `^--- PASS`.
  - Occurrence assertions use `rg -o … | wc -l`.
  - Plan 05 explicitly rejects `task lint:markdown` as evidence for `docs-site/**`.

## Concerns

### HIGH — NEW: Plan 01’s compensation test cannot reach its claimed branch

Plan 01 instructs `TestSupersedeMultiCompensatesInline` to force a back-stamp failure by supplying “a target id that does not exist alongside two that do” ([03.1-01-PLAN.md:389](/Volumes/Code/github.com/seanb4t/engram/.planning/phases/03.1-merge-supersession-supersede-memory-accepts-multiple-targets/03.1-01-PLAN.md:389)).

But the same task requires `Store.Supersede` to run `getWritable` for every target before `Upsert`. The existing gate calls `Get` and returns immediately on absence at [store.go:1621](/Volumes/Code/github.com/seanb4t/engram/internal/store/store.go:1621)–[1625](/Volumes/Code/github.com/seanb4t/engram/internal/store/store.go:1625). Therefore:

1. The nonexistent target fails preflight.
2. The survivor is never written.
3. The multi-ID back-stamp is never called.
4. The inline compensation branch is not exercised.

This is the same unreachable-window class cycle 1 identified, reintroduced one wave earlier. The required three-test PASS gate cannot prove what the plan says and may be impossible to implement honestly.

### HIGH — NEW: Plan 02’s nil-default seam calls would panic in production

Plan 02 declares optional fields:

- `setPayloadKeys func(...) error`
- `deletePoint func(...) error`

and says each “nil defaults” to a production method ([03.1-02-PLAN.md:194](/Volumes/Code/github.com/seanb4t/engram/.planning/phases/03.1-merge-supersession-supersede-memory-accepts-multiple-targets/03.1-02-PLAN.md:194)–[200](/Volumes/Code/github.com/seanb4t/engram/.planning/phases/03.1-merge-supersession-supersede-memory-accepts-multiple-targets/03.1-02-PLAN.md:200)). It then directs production code to call `s.setPayloadKeys(...)` and `s.deletePoint(...)` directly at [03.1-02-PLAN.md:214](/Volumes/Code/github.com/seanb4t/engram/.planning/phases/03.1-merge-supersession-supersede-memory-accepts-multiple-targets/03.1-02-PLAN.md:214)–[215](/Volumes/Code/github.com/seanb4t/engram/.planning/phases/03.1-merge-supersession-supersede-memory-accepts-multiple-targets/03.1-02-PLAN.md:215).

The precedent does not work that way. `deletePayloadKeys` is nil by default, and its caller explicitly resolves the fallback into a local variable before invocation at [store.go:1912](/Volumes/Code/github.com/seanb4t/engram/internal/store/store.go:1912)–[1916](/Volumes/Code/github.com/seanb4t/engram/internal/store/store.go:1916). `New` does not populate function fields at [store.go:383](/Volumes/Code/github.com/seanb4t/engram/internal/store/store.go:383)–[392](/Volumes/Code/github.com/seanb4t/engram/internal/store/store.go:392).

As written, a normal production `Store` would call nil function fields and panic. The plan mentions “accessors,” but specifies no accessor names and explicitly requires direct field calls.

### HIGH — NEW: simultaneous keyed merges cannot both succeed under the planned ordering

Plan 04 requires two simultaneous identical keyed merges to return the same stored survivor and attributes this to a post-write reread ([03.1-04-PLAN.md:374](/Volumes/Code/github.com/seanb4t/engram/.planning/phases/03.1-merge-supersession-supersede-memory-accepts-multiple-targets/03.1-04-PLAN.md:374)–[382](/Volumes/Code/github.com/seanb4t/engram/.planning/phases/03.1-merge-supersession-supersede-memory-accepts-multiple-targets/03.1-04-PLAN.md:382)).

The described call sequence makes that result impossible:

1. Both calls complete handler authorization and see no deterministic survivor during replay lookup.
2. Both pass handler state validation before either writes.
3. Call A acquires target locks, writes the survivor, and stamps targets.
4. Call B acquires the locks afterward.
5. Store-level preflight sees the targets already superseded and returns `ErrAlreadySuperseded`.
6. Call B never writes, so Plan 04’s post-write reread cannot help it.

The current store places the already-superseded check inside the locked operation at [store.go:2018](/Volumes/Code/github.com/seanb4t/engram/internal/store/store.go:2018)–[2028](/Volumes/Code/github.com/seanb4t/engram/internal/store/store.go:2028). The existing post-write reread pattern at [tools.go:1036](/Volumes/Code/github.com/seanb4t/engram/internal/server/tools.go:1036)–[1054](/Volumes/Code/github.com/seanb4t/engram/internal/server/tools.go:1054) only reconciles two writes that both reached `Upsert`; it cannot turn an earlier store rejection into replay success.

Plan 04 needs an explicit losing-racer recovery path, such as rechecking the deterministic idempotency record when store preflight returns already-superseded and returning it only when its fingerprint matches. Without that, the required concurrency test should fail.

### MEDIUM — NEW: reconciliation result ownership is internally contradictory

Plan 02 declares:

```go
type supersedeReconcileResult struct {
    RemoveErr error
    ReadFailures []string
    ClearFailures []string
    Dangling []string
}
```

and a `reconcileSupersedeFailure(ctx, survivorID, targets)` function at [03.1-02-PLAN.md:223](/Volumes/Code/github.com/seanb4t/engram/.planning/phases/03.1-merge-supersession-supersede-memory-accepts-multiple-targets/03.1-02-PLAN.md:223)–[225](/Volumes/Code/github.com/seanb4t/engram/.planning/phases/03.1-merge-supersession-supersede-memory-accepts-multiple-targets/03.1-02-PLAN.md:225). But the caller is separately instructed to delete the survivor, “recording the outcome in the result’s `RemoveErr`,” and only afterward call the reconciliation function at [03.1-02-PLAN.md:246](/Volumes/Code/github.com/seanb4t/engram/.planning/phases/03.1-merge-supersession-supersede-memory-accepts-multiple-targets/03.1-02-PLAN.md:246)–[247](/Volumes/Code/github.com/seanb4t/engram/.planning/phases/03.1-merge-supersession-supersede-memory-accepts-multiple-targets/03.1-02-PLAN.md:247).

A returned value from the later function cannot naturally contain an error produced before that function was called. The executor must either merge two values ad hoc or reinterpret which function owns survivor removal.

### MEDIUM — NEW: the docs gate appears to expect one shared schema test but splits ownership

Plan 03 owns `TestSupersedeMemorySchemaAdvertisesTargetBounds`; Plan 04 separately inverts the schema test for `idempotency_key`; Plan 05 says its new drift test obtains the generated schema and checks both shape/bounds and key support. This is workable, but the test construction remains transcription-heavy: it parses the maximum from prose while other canonical behavior comes from the schema.

A clearer contract would make the generated schema authoritative for type/min/max and require the docs to contain a generated or anchored representation of those exact values. This is not blocking, but it would reduce future prose-parsing brittleness.

## Suggestions

1. Move the mutation seams into Plan 01, or add a minimal Plan-01-local seam.

   - Plan 01’s inline compensation test should partially stamp a real subset and then return a scripted error.
   - Plan 02 can extend the same seam with classified failure reporting and structured logging.
   - Do not use a missing target as the trigger; preflight correctly prevents that path.

2. Specify nil-default invocation exactly.

   Either:

   ```go
   setPayload := s.setPayloadKeys
   if setPayload == nil {
       setPayload = s.defaultSetPayloadKeys
   }
   ```

   and similarly for `deletePoint`, or initialize both fields in `New`. The local fallback idiom is closer to [store.go:1912](/Volumes/Code/github.com/seanb4t/engram/internal/store/store.go:1912).

3. Give reconciliation one owner.

   Prefer:

   ```go
   func (s *Store) reconcileSupersedeFailure(...) supersedeReconcileResult
   ```

   where the function performs both survivor removal and target reconciliation, populating all four result fields. That aligns the name, signature, logging inputs, and direct unit test.

4. Add a losing-racer replay path for concurrent keyed merges.

   When `Store.Supersede` returns `ErrAlreadySuperseded` on a keyed request:

   - reread the deterministic point ID;
   - compare its stored fingerprint with `mergeFingerprint`;
   - return its persisted ID/short ID on a match;
   - preserve the conflict/rejection on mismatch or absence.

   Then the simultaneous-key test becomes achievable and directly proves the recovery path.

5. Add an explicit regression check for the Plan 01 compensation trigger.

   The test should assert the scripted mutation seam was called exactly once. That prevents a false green where the operation failed during preflight and never reached compensation.

6. Clarify the approved cap dependency in Plan 05’s prose instructions.

   It already depends on Plan 00, but the drift test should consume a machine-readable chosen value from code/schema rather than infer the number solely from free prose.

## Risk Assessment

**Overall risk: HIGH until the three HIGH concerns are repaired; MEDIUM afterward.**

The fundamental design is now sound: plural links, sorted deduped locks, all-target authorization, source-verified Qdrant reconciliation, terminal-state honesty, and content-plus-target-set idempotency all fit the existing architecture. The remaining risk comes from execution contradictions:

- one mandatory test cannot reach its branch;
- the production seam would panic if implemented literally;
- the concurrency guarantee lacks a path capable of producing the required result.

These are bounded fixes. Once the plans specify reachable Plan 01 compensation, safe seam defaulting, and losing-racer idempotent replay, the set should be ready for another convergence pass.

---

## Consensus Summary — Cycle 2

Single reviewer, so "consensus" is Codex plus the orchestrator's independent re-verification
against the working tree. Every Codex claim below was re-checked; two were sharpened and one
orchestrator-only finding was added.

### Agreed Strengths

- **The wave-1 decision gate does its job.** `03.1-00-PLAN.md` is `wave: 1`, `depends_on: []`,
  `autonomous: false`, with a single `checkpoint:decision gate="blocking"` covering PD-01, PD-02,
  PD-06 and PD-07. Plans 01, 03 and 05 declare `depends_on: ["03.1-00", …]` and cite
  `03.1-00-SUMMARY.md` by name (4, 9 and 9 references respectively). Plan 05's checkpoint is
  correctly demoted to verifying what shipped against what was ruled.
- **The `setPayloadKeys`/`deletePoint` seam reaches the window the old trigger could not.** The old
  trigger was unreachable because `Supersede` holds the lock via `defer unlock()`
  (`internal/store/store.go:2016`) through the `SetPayload` at `:2035`, and a pre-call delete is
  rejected by the under-lock `getWritable` at `:2020`. Scripting the mutation itself sidesteps both.
  The idiom matches the in-repo precedent — `deletePayloadKeys`/`mintCandidate` function-var fields
  at `internal/store/store.go:337-348`. Production ordering is unchanged and nothing is build-tagged.
  (One defect in *how* the seam is invoked — see HIGH-2.)
- **The rejection-class reorder genuinely closes the oracle rather than relocating it.** After the
  collapse, the only class that precedes the ownership stage is set shape, which names the argument
  and never a value (`03.1-03-PLAN.md:22-25`). Resolution-not-found, resolution-ambiguous,
  fetch-not-found and not-owned all render through one sentinel and one helper
  (`renderTargetRejection`), echoing the caller's own input strings. Rule immutability and
  already-superseded are evaluated only over records the caller provably owns, so neither can report
  on a record the caller cannot read. `03.1-03-PLAN.md:489` adds an observed-red regression check
  that reinstating the early ambiguity class must FAIL the indistinguishability test — the
  reordering is load-bearing, not cosmetic.
- **The narrowed atomicity claim is correct and still non-vacuous.** `<claim_scope>` at
  `03.1-02-PLAN.md:83-102` matches the tree: `TargetLocker` serializes writers only, its call sites
  are `Update` (`store.go:1749`) and `Supersede` (`store.go:2012`), and `Store.Get` (`store.go:1500`)
  plus every recall query take no lock. The retained claim — *terminal state clean once
  reconciliation succeeds: survivor gone, no target left pointing at it* — is a concrete, testable
  postcondition covering the four `IsEmpty("superseded_by")` recall gates, and it is stated
  identically in ROADMAP SC3 and REQ-merge-atomicity. It is narrowed, not hollowed out.
- **Both deliberate rejections hold.** Landing 01+02 in one commit is genuinely not a property a
  single plan can enforce across waves and agents, and the minimal-compensation fold into Plan 01
  is the right compensating control. Routing rollback around `Store.Delete`'s second `ActionDelete`
  decision (`store.go:2095`) is correctly justified: rollback correctness should not depend on a
  policy file, and the ungated primitive is bounded to one call site with one argument minted inside
  the call, with a comment-stripped call-site count as the gate.
- **All four repo-specific gate hazards are held.** Zero backslashes across all 19 `must_haves`
  `pattern:` entries; every behavior gate counts `^--- PASS` per named test with package-wide runs
  explicitly labelled supplemental; every occurrence assertion is `rg -o … | wc -l`; and Plan 05
  names `task lint:markdown` as a **prohibition** for `docs-site/**` evidence
  (`03.1-05-PLAN.md:38`, `:203`) rather than merely acknowledging the exclusion — a strict
  improvement on cycle 1, where it still stood as an acceptance criterion.

### Agreed Concerns

1. **HIGH — NEW — Plan 01's `TestSupersedeMultiCompensatesInline` cannot reach its branch.**
   `03.1-01-PLAN.md:389` says to force back-stamp failure "by naming a target id that does not exist
   alongside two that do". The same task requires `getWritable` for every target before `Upsert`,
   and `getWritable` calls `s.Get` and returns immediately on absence (`store.go:1621-1625`). So the
   nonexistent target fails preflight, the survivor is never written, the multi-ID back-stamp never
   runs, and the compensation branch is never exercised. This is the *same* unreachable-window class
   cycle 1 identified in Plan 02, reintroduced one wave earlier. The three-test `^--- PASS` gate at
   `03.1-01-PLAN.md:414` cannot be satisfied honestly.
2. **HIGH — NEW — Plan 02's seam invocation would panic in production.** `03.1-02-PLAN.md:191-200`
   says both fields nil-default "through an accessor exactly as `deletePayloadKeys` does" — but the
   in-repo precedent is not an accessor: it is a local-variable fallback resolved at the call site
   (`store.go:1912-1916`), and `New` never populates function fields. `03.1-02-PLAN.md:214-215` then
   mandates the direct field calls `s.setPayloadKeys(ctx, …)` and `s.deletePoint(ctx, newMem.ID)`,
   and the `must_haves` `key_links` patterns at `03.1-02-PLAN.md:55` and `:59` **require exactly
   those literal forms in the source**. A production `Store` would call a nil function value and
   panic — and an executor who writes the safe accessor form fails the `verify key-links` gate. The
   gate and the safe implementation are mutually exclusive as written.
   *Orchestrator addition:* the same plan compounds this at `03.1-02-PLAN.md:236`, which instructs
   the reconciliation pass to clear via `s.deletePayloadKeys(ctx, id, …)` — a direct call on the
   **existing** nil-defaulted field, which the current code at `store.go:1912` never does.
3. **HIGH — NEW — Plan 04's simultaneous-keyed-merge guarantee is unachievable under its own
   ordering.** `TestSupersedeMemoryConcurrentIdenticalKeyReturnsSameSurvivor`
   (`03.1-04-PLAN.md:374-382`) requires both racers to return the same persisted id and short id,
   and attributes that to the keyed-path post-write re-read wired in Task 2. But Task 2's re-read
   fires only "after a successful `Supersede`" (`03.1-04-PLAN.md:296-300`), and the losing racer
   never gets one: it clears the replay lookup, blocks on the target locks, and is then rejected by
   the under-lock already-superseded check (`store.go:2024-2027`) before reaching `Upsert`. The
   existing precedent at `internal/server/tools.go:1036-1054` only reconciles two writes that both
   reached `Upsert`; it cannot convert a store-tier rejection into a replay. The plan's own text at
   `:379-381` describes this exact sequence and then asserts the re-read resolves it — an internal
   contradiction. A losing-racer recovery path is needed: on `ErrAlreadySuperseded` for a keyed
   request, re-`Get` the deterministic point id, compare `mergeFingerprint`, and return the persisted
   answer on a match while preserving the rejection on mismatch or absence.
4. **MEDIUM — NEW — Plan 02's reconciliation result has contradictory ownership.**
   `03.1-02-PLAN.md:223-225` declares `reconcileSupersedeFailure(ctx, survivorID, targets)` returning
   `supersedeReconcileResult{RemoveErr, …}`, but `:246-247` tells the *caller* to remove the survivor
   first, "recording the outcome in the result's `RemoveErr`", and only then call the function. A
   value returned by a later call cannot contain an error produced before it. Give the function one
   owner: let it perform both survivor removal and target reconciliation and populate all four
   fields.
5. **MEDIUM — NEW (orchestrator) — Plan 04 is not gated by the wave-1 checkpoint, but depends on one
   of its rulings.** `03.1-04-PLAN.md:6` declares `depends_on: ["03.1-03"]` and the file contains
   **zero** references to `03.1-00` (compare: 4 in plan 01, 9 each in plans 03 and 05). Yet its
   `must_haves` truth at `:26` — "the same target set supplied … with a duplicate, replays
   identically" — and its `TestSupersedeMemoryIdempotencyReorderedSetReplays` (`:360`) hard-assume
   PD-01 resolved as `dedupe-silently`. If the developer rules `reject-duplicates`, that `must_have`
   becomes unsatisfiable and the test asserts behavior the phase does not have. Plans 03 and 05 both
   handle their conditional dependency explicitly ("If plan 00 adopted a cap, additionally …" at
   `03.1-03-PLAN.md:485`, `03.1-05-PLAN.md:196`); plan 04 has no equivalent. Related and weaker:
   `03.1-02-PLAN.md` also never cites `03.1-00`, and its cross-chunk framing around
   `qdrantPayloadOpBatchSize = 32` presumes no cap at or below 32 was adopted.
   **Fix:** add `"03.1-00"` to plan 04's `depends_on`, and make the duplicate-independence truth and
   its test conditional on the recorded PD-01 ruling in the same style plans 03 and 05 already use.
6. **MEDIUM — NEW — the cap's published value is parsed out of prose in three places.** Plan 03 owns
   `TestSupersedeMemorySchemaAdvertisesTargetBounds`, plan 04 separately inverts the schema test for
   `idempotency_key`, and plan 05's drift gate reads the maximum out of markdown
   (`03.1-05-PLAN.md:196`). Codex rates this non-blocking; it is still transcription-heavy. Making
   the generated JSON schema authoritative for type/`minItems`/`maxItems` and requiring the docs to
   carry an anchored representation of those exact values would remove the prose-parsing brittleness
   before it ships.

### Divergent Views

None — single reviewer. Where the orchestrator's re-verification differed from Codex it *sharpened*
the finding (concerns 2 and 5 above); nothing Codex asserted was contradicted.

### Cycle 1 → Cycle 2 disposition (all RESOLVED, not re-counted)

| Cycle-1 HIGH | Status | Evidence |
|---|---|---|
| Plan 01 omits `store_iface.go` / `fakestore_test.go` | RESOLVED | both in `03.1-01-PLAN.md:7-12`; compile-time assertion at `store_iface.go:47` now covered |
| Spy behavior single-target | RESOLVED | `fakestore_test.go:214-232` rewrite plus `TestSpyStoreSupersedeGatesEverySetMember` with an observed-red check |
| TOCTOU trigger unreachable | RESOLVED | replaced by the declared mutation seam; lock analysis at `store.go:2016`/`:2020`/`:2035` accepted |
| A production fault seam is required | RESOLVED | `setPayloadKeys`/`deletePoint` declared in the `store.go:337-348` idiom (invocation defect tracked separately as HIGH-2) |
| Compensation-failure logging untestable | RESOLVED | the same seam scripts survivor-removal and selective-clear failures independently |
| "Not observable" overstates the mechanism | RESOLVED | `<claim_scope>` at `03.1-02-PLAN.md:83-102`; ROADMAP SC3 and REQ-merge-atomicity reworded to match |
| Class C calls `FetchForUpdate` on unresolvable ids | RESOLVED | three-bucket resolution; `FetchForUpdate` only for canonical ids (`store.go:1564-1567` behavior respected) |
| Ambiguous short id leaks existence | RESOLVED | collapsed into the not-found class; cross-owner collision test plus observed-red regression check at `03.1-03-PLAN.md:489` |
| Cap ships three waves before approval | RESOLVED | `03.1-00` is wave 1 and blocking; plans 01/03/05 depend on it |
| Idempotency staging API undecided at execution time | RESOLVED | two named functions decided in plan 03; the replay check is a statement between them; callbacks forbidden |
| Docs overstate best-effort compensation | RESOLVED | three-sentence failure paragraph required and gated (`03.1-05-PLAN.md:199`, `:388`) |

---

## Verification coverage — Cycle 2

Orchestrator re-checks against the working tree (`/Volumes/Code/github.com/seanb4t/engram`,
branch `feat/v0.13`, 6-plan set at `24c654b6`):

| Claim | Method | Result |
|---|---|---|
| Wave/dependency graph is 00(1) → 01(2) → {02,03}(3) → 04(4) → 05(5) | read frontmatter of all six plans | CONFIRMED |
| Wave-3 plans touch disjoint files | compared `files_modified` of 02 (`internal/store/*`) and 03 (`internal/server/tools*`) | CONFIRMED — no overlap |
| Plans citing `03.1-00` | `rg -c '03.1-00' 03.1-0*-PLAN.md` | 01:4, 03:9, 05:9, **02:0, 04:0** — basis for concern 5 |
| No backslash in any `must_haves` `pattern:` | `rg -n 'pattern:' *.md \| rg '\\\\'` | CONFIRMED — 0 hits across 19 `pattern:` entries (01:4, 02:4, 03:5, 04:4, 05:2) |
| Behavior gates count `^--- PASS` per named test | `rg -n -- '--- PASS' 03.1-0*-PLAN.md` | CONFIRMED — 27 PASS-counting gates; every remaining `^--- FAIL`-only gate is explicitly labelled "Supplemental regression gate only, never the proof" |
| No `rg -c` used for file-occurrence counting | `rg -n 'rg -c' 03.1-0*-PLAN.md \| rg -v 'PASS\|FAIL\|SKIP'` | CONFIRMED — only remaining hit (`03.1-02-PLAN.md:389`) is a presence check on command output, not an equality count |
| `docs-site` excluded from rumdl is a prohibition, not a note | `03.1-05-PLAN.md:38`, `:203` | CONFIRMED — stronger than cycle 1, where `task lint:markdown exits 0` was still an acceptance criterion |
| `deletePayloadKeys` precedent uses a local-variable fallback, not an accessor | `store.go:1912-1916` | CONFIRMED — `del := s.deletePayloadKeys; if del == nil { del = s.defaultDeletePayloadKeys }` |
| `getWritable` returns on absence before any write | `store.go:1621-1625` | CONFIRMED — `m, err := s.Get(...)`, immediate return on error |
| Keyed post-write re-read only fires after a successful `Upsert` | `internal/server/tools.go:1036-1054` | CONFIRMED — gated on `m.IdempotencyFingerprint != ""` *after* `d.st.Upsert` succeeded |
| Under-lock already-superseded check precedes `Upsert` | `store.go:2024-2032` | CONFIRMED — rejection at `:2026` is before the `Upsert` at `:2032` |
| Narrowed atomicity claim is consistent across artifacts | `03.1-02-PLAN.md:83-102`, `ROADMAP.md` SC3, `REQUIREMENTS.md:129-135` | CONFIRMED — identical scoping in all three |
