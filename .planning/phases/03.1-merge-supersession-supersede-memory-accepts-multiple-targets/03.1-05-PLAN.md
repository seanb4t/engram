---
phase: 03.1
plan: 05
type: execute
wave: 5
depends_on: ["03.1-00", "03.1-02", "03.1-03", "03.1-04"]
files_modified:
  - docs-site/src/content/docs/reference/errors.md
  - docs-site/src/content/docs/reference/tools.md
  - docs-site/src/content/docs/reference/memory-record.md
  - CLAUDE.md
  - skill/engram/skills/curating-memory/SKILL.md
  - internal/server/supersededocs_test.go
autonomous: false
requirements: [REQ-merge-supersession, REQ-merge-atomicity, REQ-merge-idempotency]

estimate:
  tokens: 65000
  raw_tokens: 60000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "The published error reference documents the multi-target rejection shape: one failure class per response, every offending target named, echoed as the caller supplied it"
    - "The published error reference states that a target you do not own and a target that does not exist produce the same rejection"
    - "The published tool reference declares supersedes as an array and states that a one-element array is the single-target case"
    - "The published tool reference states that idempotency_key is accepted on this verb and that the fingerprint covers content and the target set"
    - "No shipped document still states that idempotency_key is unsupported on this verb, and every one of them positively asserts the shipped key-support sentence — a stale-phrase sweep alone cannot catch a document that simply says nothing"
    - "The published documentation describes the failed-merge no-op as the usual outcome, never as a guarantee, and the very next sentence states what a cleanup failure can leave behind: an orphaned survivor, dangling links, or both, with operator logging as the only remediation"
    - "The published documentation states that a target you do not own, one that does not exist, and one whose short id is ambiguous are all the same rejection"
    - "The published documentation states that a keyed retry whose targets were deleted or whose ownership changed does not replay"
    - "The documented multi-target rejection examples are produced by the SAME production rendering helper the handler uses, so the test cannot drift in parallel with the handler by re-typing its format string"
    - "The documented supersedes type agrees with the generated MCP JSON schema, enforced by the same test"
  prohibitions:
    - "Documentation MUST NOT advertise 'a failed merge is a no-op' as a guarantee — compensation and reconciliation talk to the same Qdrant that just failed and degrade in exactly the outage that triggers them (D-08)"
    - "Documentation MUST NOT claim a partially-applied merge is never observable. The provable claim is that the terminal state is clean once a merge attempt returns and its reconciliation succeeds; readers take no lock and can observe the transient state"
    - "task lint:markdown MUST NOT be presented as evidence for anything under docs-site — that tree is excluded from rumdl, so a green run there proves nothing about these files"
    - "The agent-facing skill MUST NOT be edited into suggesting supersession on a similarity hunch or as a write-through side effect; the merge verb stays explicitly invoked on user-blessed judgment (INV-04)"
  artifacts:
    - path: "docs-site/src/content/docs/reference/errors.md"
      provides: "the multi-target rejection section documenting the widened envelope"
      contains: "Multi-target rejections"
    - path: "internal/server/supersededocs_test.go"
      provides: "the drift gate binding the published docs to the shipped contract"
      contains: "func TestSupersedeDocsMatchShippedContract"
  key_links:
    - from: "internal/server/supersededocs_test.go"
      to: "docs-site/src/content/docs/reference/errors.md"
      via: "rendered rejection strings asserted verbatim against the published examples"
      pattern: "reference/errors[.]md"
    - from: "internal/server/supersededocs_test.go"
      to: "internal/server/tools.go"
      via: "the generated supersede_memory schema is compared against the documented argument types"
      pattern: "supersede_memory"
---

<objective>
Bring every shipped surface into agreement with what the code now does, and bind the two together with
a test so they cannot drift apart silently.

Purpose: this repo already has the failure mode. Constants get pinned in tests and mirrored by hand
into `docs-site`, and the mirror goes stale with the tests still green. The widened error envelope is
a machine-stable contract an agent parses; a documented example that no longer matches what the server
renders is worse than no example. And `CLAUDE.md` plus the agent-facing skill both currently assert
that this verb rejects idempotency keys, which stopped being true in plan 03.1-04.

Output: a multi-target rejection section in the error reference, corrected tool and record references,
corrected project and skill contracts, and one test that fails when any of them drifts.
</objective>

<review_dispositions>
Cycle-1 cross-AI review findings against this plan (`03.1-REVIEWS.md`) and what happened to each.

| Finding | Severity | Disposition |
|---|---|---|
| Documentation overstates terminal compensation while plan 02 remains best-effort | HIGH | INCORPORATED — the "usually observably a no-op" sentence now REQUIRES an immediately following sentence naming what a cleanup failure can leave (orphaned survivor, dangling links, or both) and stating that operator logging is the only remediation, because the source has no durable retry or recovery queue. An acceptance criterion reads the paragraph for it |
| `task lint:markdown` is not evidence for docs-site correctness | MEDIUM | INCORPORATED — demoted to a general repository gate in both tasks and named as a prohibition. The semantic drift test and explicit content assertions are the only evidence for `docs-site/**` |
| The drift test risks duplicating rather than invoking rendering logic (`tools.go:1755` assembles errors inline with `fmt.Errorf`) | MEDIUM | INCORPORATED — plan 03 now extracts `renderTargetRejection` as the single production rendering helper used by every rejection in the verb, and this plan's drift test calls it. Nothing in the test re-types a format string |
| Human approval arrives after all three disputed choices have shipped | MEDIUM | INCORPORATED — plan `03.1-00` is a blocking decision gate in wave 1. This plan's checkpoint is demoted to VERIFYING that what shipped matches what was ruled |
| The stale-claim sweep is phrase-specific and can miss equivalent stale wording | LOW | INCORPORATED — every document must also POSITIVELY assert the current key-support statement, so a document that drops the stale phrase without gaining the true one still fails |
| `rg -c` used for "equals 1" occurrence gates | MEDIUM (orchestrator addition) | INCORPORATED — every occurrence gate in this plan is `rg -o '<pat>' <file> | wc -l`. `rg -c` counts matching LINES, so two hits on one line read as 1 |
</review_dispositions>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-merge-supersession-supersede-memory-accepts-multiple-targets/03.1-CONTEXT.md
@.planning/phases/03.1-merge-supersession-supersede-memory-accepts-multiple-targets/03.1-00-SUMMARY.md
@.planning/phases/03.1-merge-supersession-supersede-memory-accepts-multiple-targets/03.1-02-SUMMARY.md
@.planning/phases/03.1-merge-supersession-supersede-memory-accepts-multiple-targets/03.1-03-SUMMARY.md
@.planning/phases/03.1-merge-supersession-supersede-memory-accepts-multiple-targets/03.1-04-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document the multi-target rejection envelope and the array wire shape</name>
  <reversibility rating="costly">Publishing the multi-fault rejection shape and the always-array `supersedes` form makes both machine-stable contracts agents will parse; narrowing either later breaks those callers (CONTEXT.md D-03/D-11).</reversibility>
  <files>docs-site/src/content/docs/reference/errors.md, docs-site/src/content/docs/reference/tools.md, docs-site/src/content/docs/reference/memory-record.md</files>
  <read_first>
    - docs-site/src/content/docs/reference/errors.md lines 1-60 (the envelope grammar section, the single-field and relational examples, and the ten-code hint table — match the page's existing voice and heading depth)
    - docs-site/src/content/docs/reference/tools.md lines 245-295 (the `supersede_memory` argument table, the "What changes" prose, and the chain/never-automatic bullets)
    - docs-site/src/content/docs/reference/memory-record.md lines 25-80 (the field table row for the forward link, the Supersession section, and the note about which fields the proto message omits)
    - internal/server/tools.go — the two staged preflight functions, `renderTargetRejection`, and the `supersede_memory` tool Description as plan 03.1-03 left them, for the exact class order and wording
    - .planning/phases/03.1-merge-supersession-supersede-memory-accepts-multiple-targets/03.1-00-SUMMARY.md (the cap ruling — it decides whether a maximum is published at all)
    - .planning/phases/03.1-merge-supersession-supersede-memory-accepts-multiple-targets/03.1-03-SUMMARY.md (the shipped class order and the rendered rejection strings)
    - .planning/phases/03.1-merge-supersession-supersede-memory-accepts-multiple-targets/03.1-04-SUMMARY.md (the shipped idempotency contract)
  </read_first>
  <action>
Add a "Multi-target rejections" section to `docs-site/src/content/docs/reference/errors.md`, placed
after the relational example so it reads as a continuation of the envelope grammar rather than a new
concept. It must state, in the page's existing register:

- Which verb this applies to and that the whole call is rejected — a merge never partially applies a
  valid subset.
- That a response carries exactly one failure class, and the fixed class order as plan 03.1-03
  shipped it: set shape, then addressability-and-access, then rule target, then already superseded.
  Take the order from `03.1-03-SUMMARY.md`, not from memory.
- That every offending target of the failing class is named in one response, comma-separated, in the
  order the caller supplied them.
- That each target is echoed exactly as the caller wrote it, so a target sent as a short id comes back
  as that short id and never as a resolved UUID.
- That three cases collapse into one rejection — a target you do not own, a target that does not
  exist, and a short id that matches more than one record — and that this is deliberate. Say what the
  reader must NOT conclude: a not-found is not proof the id is unused, and it is not proof the handle
  is unique. Add the practical consequence: if a short id will not resolve, name the target by its
  full UUID.
- A worked example for the addressability class and one for the already-superseded class, copied
  verbatim from what the server actually renders. Take these strings from plan 03.1-03's summary; the
  drift test in Task 3 asserts them against `renderTargetRejection`'s output byte for byte, so an
  invented example will fail the build.

Note in the same section that these rejections are sentinel-shaped rather than field-and-hint shaped,
and that the ten-code hint vocabulary is unchanged by this phase — no new hint code was added. Set
shape rejections (empty array, oversized array, blank entry) DO use the field-and-hint grammar and
should be shown that way, naming the argument rather than the values.

In `docs-site/src/content/docs/reference/tools.md`, update the `supersede_memory` section: the
argument row's type becomes an array of strings, still required, with the description saying one or
more ids, each a full UUID or a short id, and that a one-element array is the single-target case. Add
that duplicates and two spellings of the same record collapse to one target. State the maximum
accepted count ONLY if plan 00 adopted a cap — read `03.1-00-SUMMARY.md` and `03.1-03-SUMMARY.md`
first, and if it was dropped, publish no maximum and say the set is unbounded. Add that
`idempotency_key` is accepted on this verb, that the replay fingerprint covers the content and the
target set so the same key against a different set is a conflict, and that a retry after an ambiguous
failure returns the original result. Add the narrowing PD-09 requires in the contract, not in a
footnote: because the replay check runs after the target ownership check, a retry whose targets were
deleted or whose ownership changed does NOT replay — it is rejected as not found. Correct the
sentence in the general argument prose that currently groups this verb with the ones that do not take
a key. Keep the existing never-automatic and single-live-head bullets and extend them to the set: the
rule applies per target, and a rejection names every offending target.

Add one paragraph on failure behaviour, worded exactly as honestly as the code comment. It has three
sentences and none of them may be dropped:
1. When the back-stamp fails, the server compensates by removing the new record and clearing any link
   it left behind, so a failed merge is *usually* observably a no-op.
2. Compensation talks to the same store that just failed, so when it cannot complete, the call can
   leave an orphaned new record, one or more predecessors still carrying a link to it, or both. There
   is no retry queue and no recovery command; the server logs the affected ids for the operator, and
   that log is the only remediation path.
3. Once a merge attempt has returned and its cleanup succeeded, no partial state remains — but that
   is a statement about the state after the call, not about every instant during it. A concurrent
   read can briefly see a predecessor hidden while the merge is in flight.

Do not write any of this as a promise, and do not compress sentence 2 away — review found the
original wording advertised the good outcome without the cost.

In `docs-site/src/content/docs/reference/memory-record.md`, change the forward-link field row's type
to a list and update the Supersession section: one correcting record may replace several predecessors,
each predecessor still has exactly one successor, and chains still run forward with one live head.
Leave the note about which fields the proto message omits exactly as it is — this phase adds no proto
field.

Run `task fmt` after editing. Do not add an SPDX header to any file in `docs-site` — that tree is
excluded by `.licenserc.yaml` and a header above the frontmatter breaks it. `docs-site` is ALSO
excluded from `rumdl` in `.rumdl.toml`, so `task lint:markdown` is a general repository gate and is
not evidence about these three files. It appears nowhere in this task's acceptance criteria for that
reason. The content assertions below and the drift gate in Task 3 are the only evidence for
`docs-site/**`.
  </action>
  <verify>
    <automated>rg -o 'Multi-target rejections' docs-site/src/content/docs/reference/errors.md | wc -l &amp;&amp; rg -o 'array of string' docs-site/src/content/docs/reference/tools.md | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `rg -o 'Multi-target rejections' docs-site/src/content/docs/reference/errors.md | wc -l` equals 1.
    - `rg -o 'already superseded' docs-site/src/content/docs/reference/errors.md | wc -l` is at least 1.
    - `rg -o 'array of string' docs-site/src/content/docs/reference/tools.md | wc -l` is at least 1 and the hit sits in the `supersedes` argument row — confirm by reading it.
    - The published maximum matches plan 00's ruling: if a cap was adopted, `rg -o '<the ruled value>' docs-site/src/content/docs/reference/tools.md | wc -l` is at least 1; if it was dropped, no maximum appears anywhere in the `supersede_memory` section and the summary records that.
    - `rg -o 'The ten hint codes' docs-site/src/content/docs/reference/errors.md | wc -l` equals 1 — the hint vocabulary count is unchanged.
    - `rg -o 'idempotency_key' docs-site/src/content/docs/reference/tools.md | wc -l` is at least 2 (the general prose correction and the supersede section).
    - The three-sentence failure paragraph is complete: read it and confirm sentence 2 names the orphaned-survivor / dangling-link outcome and the operator log as the only remediation, and that sentence 3 scopes the clean-state claim to after the call returns. A paragraph carrying only sentences 1 and 3 fails this criterion.
    - `rg -n 'guarantee' docs-site/src/content/docs/reference/tools.md` — read every hit and confirm none claims a failed merge is guaranteed to be a no-op.
    - No overstated atomicity language survives: `rg -n 'never observable|cannot be observed|no reader' docs-site/src/content/docs/reference/tools.md` returns nothing. <!-- planner-discipline-allow: never observable, cannot be observed, no reader — these literals appear in Task 3's action because that task encodes them as historical-string constants in a Go test; the grep above scans only the markdown document, so there is no self-invalidation path -->
    - The generated schema and the published maximum agree, or both are absent — Task 3's drift gate is what enforces it; this criterion is the reading check.
    - NOT an acceptance criterion for this task, stated so nobody re-adds it: `task lint:markdown`. It is a general repository gate run in Task 2 and cannot say anything about `docs-site/**`.
  </acceptance_criteria>
  <done>The error reference documents the multi-target envelope with verbatim server examples, and the tool and record references describe the array shape, the accepted maximum, key support, and the honest failure behaviour.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the project contract and the agent-facing skill</name>
  <files>CLAUDE.md, skill/engram/skills/curating-memory/SKILL.md</files>
  <read_first>
    - CLAUDE.md lines 112-128 (the Supersession paragraph in the "Memory contract (stable)" section — the exact sentences that go stale)
    - skill/engram/skills/curating-memory/SKILL.md lines 400-430 (the supersession rules block and the three-way verb table it sits in)
    - .planning/phases/03.1-merge-supersession-supersede-memory-accepts-multiple-targets/03.1-CONTEXT.md (the phase boundary paragraph — the one-paragraph statement of what this verb now does)
    - .planning/phases/03.1-merge-supersession-supersede-memory-accepts-multiple-targets/03.1-03-SUMMARY.md and 03.1-04-SUMMARY.md (the shipped rejection and replay contracts)
  </read_first>
  <action>
Update `CLAUDE.md`'s Supersession paragraph in the "Memory contract (stable)" section so it describes
the shipped verb:

- The `supersedes` argument is a set of one or more target ids, so one call collapses a duplicate set
  into a single surviving record with history preserved for every predecessor.
- The link is one-directional: the forward link is a list, the backward link stays a single id, and
  each predecessor has exactly one successor.
- The single-live-head rule is enforced per target, an invalid set rejects the whole call, and the
  rejection names every offending target of one failure class.
- A non-owned target stays indistinguishable from a nonexistent one across the whole set.
- Replace the clause asserting this verb rejects idempotency keys with the shipped behaviour: the key
  is accepted, the fingerprint covers content and the target set, and a retry after an ambiguous
  failure replays instead of duplicating.
- Keep every other property of that paragraph unchanged — soft-hide on recall, fetchable by id,
  never automatic, rules not supersedable, and the guidance that this verb is for reversals while
  in-place refinement belongs to the update verb.

Update the supersession rules block in `skill/engram/skills/curating-memory/SKILL.md` for the agent
audience:

- Add a rule stating that several targets may be corrected in one call, with a concrete one-line
  example of the array form, and that a one-element array is the ordinary single-target case.
- Extend the own-the-target rule and the rules-are-immutable rule to say they apply to every target in
  the set, and that a rejection names every offender at once so the agent can fix the whole set in one
  round trip rather than discovering offenders one at a time.
- Replace the bullet asserting that keys are unsupported on this verb with the shipped behaviour and a
  usage note: pass a key when retrying a merge after an ambiguous failure, and expect a conflict rather
  than a silent second application if the target set changed between attempts.
- Leave the never-automatic rule strengthened rather than softened: a merge is still an explicit
  correction of specific records the agent identified and the user blessed — a larger target set is
  not a licence to guess.

Do not add an SPDX header to `SKILL.md` — files whose first line must be YAML frontmatter are excluded
by `.licenserc.yaml`. Run `task lint:markdown` and `task license:check` after editing. Both are
general repository gates here: `CLAUDE.md` and `SKILL.md` ARE covered by rumdl, unlike `docs-site`, so
`task lint:markdown` is meaningful for these two files and for nothing in Task 1.
  </action>
  <verify>
    <automated>task lint:markdown &amp;&amp; task license:check</automated>
  </verify>
  <acceptance_criteria>
    - `rg -o 'is not supported on this verb' CLAUDE.md | wc -l` equals 0.
    - `rg -o 'not.{0,4} supported here' skill/engram/skills/curating-memory/SKILL.md | wc -l` equals 0.
    - Positive assertion, not just the sweep: `rg -o 'idempotency_key' CLAUDE.md | wc -l` is at least 1 AND the surrounding sentence states the key is accepted — confirm by reading the hit. A document that merely dropped the stale phrase without gaining the true one fails here, which is the gap a phrase-only sweep leaves.
    - Same for the skill: `rg -o 'idempotency_key' skill/engram/skills/curating-memory/SKILL.md | wc -l` is at least 1 and the surrounding bullet states the key is accepted.
    - `rg -o 'never automatic' CLAUDE.md | wc -l` is at least 1 — the invariant survives the edit.
    - `rg -o 'Never automatic' skill/engram/skills/curating-memory/SKILL.md | wc -l` is at least 1.
    - `rg -o 'supersedes' skill/engram/skills/curating-memory/SKILL.md | wc -l` is at least 2, and at least one hit shows the array form.
    - `CLAUDE.md`'s supersession paragraph states that a non-owned target, a nonexistent target, and an ambiguous short id are one rejection — confirm by reading it.
    - `task lint:markdown` exits 0 and `task license:check` exits 0 — general repository gates, covering these two files but explicitly not `docs-site`.
  </acceptance_criteria>
  <done>The project contract and the agent-facing skill both describe the set-valued verb, key support, and the per-target rejection behaviour, with the never-automatic invariant intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Bind the docs to the shipped contract with a drift gate</name>
  <files>internal/server/supersededocs_test.go</files>
  <read_first>
    - internal/surfaces/conformance_test.go lines 1-80 (the precedent for a Go test reading docs-site and skill files by relative path, and the "a file that never mentions a field cannot be documenting it" reasoning)
    - internal/server/tools_test.go — the renamed schema test from plan 03.1-04, for how the generated `supersede_memory` schema is obtained in a test
    - internal/server/tools.go — `renderTargetRejection` (plan 03.1-03's single production rendering helper) and the two staged preflight functions, for the exact rendered rejection strings and the class order
    - docs-site/src/content/docs/reference/errors.md and reference/tools.md as Task 1 left them
    - CLAUDE.md and skill/engram/skills/curating-memory/SKILL.md as Task 2 left them
  </read_first>
  <behavior>
    - The rendered not-found rejection for a fixed two-target input appears verbatim in the error reference
    - The rendered already-superseded rejection for a fixed two-target input appears verbatim in the error reference
    - The generated schema declares the target argument as an array and the tool reference declares the same type
    - The generated schema advertises the idempotency key argument
    - No shipped document asserts that this verb rejects idempotency keys
  </behavior>
  <action>
Create `internal/server/supersededocs_test.go` with `TestSupersedeDocsMatchShippedContract`, following
the relative-path convention the surfaces conformance test already uses to reach the repository root
from a package test.

Sub-test one, rendered examples. Call `renderTargetRejection` — the production helper, by name — with
a fixed, hard-coded pair of target inputs, once for the addressability class sentinel and once for the
already-superseded sentinel. Read the error reference and assert each returned `Error()` string is
present verbatim.

The test MUST NOT contain a `fmt.Errorf` that reproduces the helper's format string. Review found the
original instruction ("calling the same wrapping the handler uses") underspecified against a handler
that assembled its errors inline at `tools.go:1755`: a test that re-types the same expression drifts
in lockstep with the handler and asserts nothing. Plan 03.1-03 extracted the helper precisely so this
test has one thing to call. If the helper does not exist when this task runs, that is a plan-03 defect
to fix there, not to work around here.

On failure the message must print both the rendered string and a pointer to the documentation file,
so the fix is obvious.

Sub-test two, schema versus documented type. Obtain the generated `supersede_memory` schema the way
the renamed schema test does, assert the target argument's declared type is an array, and assert the
tool reference's argument table declares the same type. Assert the schema advertises the idempotency
key argument. Assert the schema's `minItems` is 1, and — only if plan 00 adopted a cap — that its
`maxItems` equals the value published in the tool reference, read out of the document rather than
hard-coded, so the two cannot drift apart.

Sub-test three, stale-claim sweep AND positive assertion. For the tool reference, the project contract
file, and the curating-memory skill file: assert none still carries a sentence claiming this verb
rejects idempotency keys — encode the stale sentence fragments as named constants with a comment
explaining they are historical strings kept only so their reappearance fails — AND assert each file
positively carries the current key-support statement. The sweep alone is one-directional: a document
that deletes the stale sentence and says nothing in its place passes a phrase-only test while leaving
an agent with no statement at all. Assert both directions per file.

Sub-test four, the honesty of the failure paragraph. Assert the tool reference contains the
cleanup-failure sentence — the one naming the orphaned survivor and dangling links — and assert it
contains none of the overstated forms (`never observable`, `cannot be observed`, `no reader`). This
binds plan 02's narrowed claim to the published text so a future edit cannot quietly re-inflate it.

Give the file a doc comment naming the failure class it closes: this repository has previously pinned
a constant in a test and mirrored it by hand into the documentation site, and the mirror went stale
with the test suite green. This gate makes the documented example the rendered output rather than a
transcription of it.

Run `task license:add` so the new Go file gets its SPDX header, then `task fmt` and `task lint`.
  </action>
  <verify>
    <automated>go test ./internal/server/... -run 'TestSupersedeDocsMatchShippedContract' -v 2>&amp;1 | rg -c '^--- PASS'</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/server/... -run 'TestSupersedeDocsMatchShippedContract' -v 2>&1 | rg -c '^--- PASS'` is at least 1 and the sub-test lines show all four sub-tests running: `... 2>&1 | rg -c '^    --- PASS'` equals 4.
    - The same command reports zero `^--- FAIL` and zero `^--- SKIP`.
    - The test invokes the production renderer and does not reimplement it: `rg -o 'renderTargetRejection\(' internal/server/supersededocs_test.go | wc -l` is at least 2, and `rg -o 'fmt\.Errorf\("%w: %s"' internal/server/supersededocs_test.go | wc -l` equals 0.
    - `rg -o 'SPDX-License-Identifier: Apache-2.0' internal/server/supersededocs_test.go | wc -l` equals 1.
    - Deliberate regression check, run manually and NOT committed: change one character inside the documented addressability example in the error reference; the test must FAIL naming that file. Revert before committing and record the observed failure line in the summary.
    - Deliberate regression check, run manually and NOT committed: reinstate one stale sentence fragment in the project contract file; the stale-claim sub-test must FAIL. Revert and record the observed line.
    - Deliberate regression check, run manually and NOT committed: DELETE the key-support sentence from the project contract file without reinstating any stale phrase; the positive half of sub-test three must FAIL. Revert and record the observed line — this is the check that proves the sweep is two-directional.
    - Deliberate regression check, run manually and NOT committed: replace the cleanup-failure sentence in the tool reference with an unqualified no-op claim; sub-test four must FAIL. Revert and record the observed line.
    - `task` (lint + test) exits 0.
  </acceptance_criteria>
  <done>The published examples are the server's rendered output, the documented argument type is the generated schema's type, and a reintroduced stale claim fails the build.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify the shipped contract matches what was ruled in plan 00</name>
  <what-built>
    The complete merge-supersession contract as it will ship, across all six plans: `supersedes` as an
    always-array argument, one survivor per merge with every predecessor back-stamped, detection and
    reconciliation of a partially-applied back-stamp, a staged-by-class rejection naming every offender,
    and `idempotency_key` support with a content-plus-target-set fingerprint.

    **This checkpoint no longer asks for first approval.** Cycle-1 review found the discretionary
    decisions shipping in waves 1-2 with their approval here in wave 4 — after implementation and after
    publication. They were moved to plan `03.1-00`, a blocking gate in wave 1. This checkpoint verifies
    that what shipped is what was ruled, and reviews the two decisions that were always the planner's
    to make:

    - **Ruled at plan 00, verified here** — the target-set cap (PD-07: adopted at a value, or dropped),
      the rejection class order and the ambiguous-short-id collapse (PD-06), duplicate handling
      (PD-01), and cross-scope merges (PD-02). Read `03.1-00-SUMMARY.md` alongside the shipped
      documentation and confirm they agree.
    - **Planner's, not disputed** — PD-05: the multi-fault rejection keeps the existing error sentinels
      and widens only the message, one failure class per response. No new hint code, no new error type,
      no change to the Connect code mapping. PD-10: the store returns a typed multi-target error the
      handler reads with `errors.As`, with sentinel identity and rendered text unchanged.
  </what-built>
  <how-to-verify>
    1. Open `03.1-00-SUMMARY.md` and keep it beside you. Everything below is a comparison against it.
    2. Read the new "Multi-target rejections" section in
       `docs-site/src/content/docs/reference/errors.md`. Confirm the published class order is the one
       you ruled, that the worked examples read the way you want an agent to encounter them, and that
       the addressability wording does not hint that a target might belong to someone else or that a
       handle might be ambiguous.
    3. Read the `supersede_memory` section of `docs-site/src/content/docs/reference/tools.md`. Confirm
       the cap matches your ruling — the published number if you adopted one, or no maximum at all if
       you dropped it — and that the generated schema agrees (Task 3's drift gate asserts this, but
       read it yourself once).
    4. Read the three-sentence failure paragraph in the same section. Confirm it tells you what a
       cleanup failure can leave behind and that the operator log is the only remediation. If it reads
       like a promise, it is wrong.
    5. Read the updated Supersession paragraph in `CLAUDE.md` and the supersession rules block in
       `skill/engram/skills/curating-memory/SKILL.md`. Confirm the never-automatic invariant still
       reads as strongly as before.
    6. Run `task` and confirm lint and the full suite are green.
    7. Run `go test ./internal/store/... -run TestSupersedeMultiTOCTOU -v` and confirm it RUNS rather
       than skipping — the output must show a pass, not a skip. This is the one test that proves
       REQ-merge-atomicity, and its claim is about the terminal state after the call returns.
  </how-to-verify>
  <resume-signal>Type "approved", or name the divergence — in particular any place where the shipped contract does not match your plan-00 ruling.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published error reference → agent parser | Documented examples are treated as a machine-stable contract; a stale example misroutes an agent's error handling |
| published tool reference → agent behaviour | A documented argument type that disagrees with the schema produces requests the server rejects |
| skill file → agent judgment | The skill is what an agent reads before mutating a user's memory; a weakened invariant here becomes unwanted writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.1-01 | Information Disclosure | published rejection examples | high | mitigate | The documented not-found example is the rendered output, asserted verbatim by the drift gate, so the published text cannot drift into distinguishing "not yours" from "does not exist". |
| T-03.1-17 | Repudiation | documentation drift from the shipped contract | high | mitigate | `TestSupersedeDocsMatchShippedContract` binds the rendered rejections — produced by the production `renderTargetRejection`, never re-typed — and the generated schema type and bounds to the published files, sweeps for reintroduced stale claims, AND positively asserts the current statements. Four deliberate drift regressions were observed red before being reverted. `task lint:markdown` is explicitly NOT part of this mitigation: `docs-site` is excluded from rumdl. |
| T-03.1-27 | Repudiation | published documentation overstating what compensation guarantees | high | mitigate | The failure paragraph is a required three sentences: the usual no-op, what a cleanup failure can leave with operator logging as the only remediation, and the terminal-state scope of the clean-state claim. Sub-test four asserts the cost sentence is present and the overstated forms are absent, so re-inflating the claim fails the build. |
| T-03.1-18 | Tampering | agent-facing skill weakened into automatic supersession | medium | mitigate | The never-automatic rule is asserted still present by an acceptance criterion, and the checkpoint asks the developer to confirm it reads as strongly as before. |
| T-03.1-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this phase; `03.1-RESEARCH.md` §Package Legitimacy Audit records "Not applicable". |
</threat_model>

<verification>
- `task lint:markdown`, `task license:check`, and `task` all exit 0 — as general repository gates, not as evidence about `docs-site/**`.
- `TestSupersedeDocsMatchShippedContract` passes with all four sub-tests running, and all four deliberate drift regressions were observed red and reverted.
- `TestSupersedeMultiTOCTOU` was observed to run rather than skip during the checkpoint.
- The shipped contract was compared against `03.1-00-SUMMARY.md` at the checkpoint and the developer confirmed they agree.
</verification>

<success_criteria>
- Every shipped surface describes the set-valued verb, the multi-fault rejection, the cap ruling as ruled, and key support.
- No shipped document claims this verb rejects idempotency keys, and each one positively states that it accepts them.
- The failed-merge no-op is documented as the usual outcome, never as a guarantee, with the cleanup-failure cost stated in the next sentence.
- No shipped document claims a partially-applied merge is never observable.
- The published examples are the production renderer's output, enforced by a test that calls it rather than reproducing it.
- What shipped matches what plan 00 ruled, confirmed by the developer.
</success_criteria>

<artifacts_produced>
## Artifacts this plan produces

**docs-site/src/content/docs/reference/errors.md** — "Multi-target rejections" section: the four-class order, all-offenders-named rule, original-input echo, the three-way indistinguishability statement (not-owned / nonexistent / ambiguous handle), two verbatim worked examples

**docs-site/src/content/docs/reference/tools.md** — `supersede_memory` argument table row retyped to an array; the cap published only if plan 00 adopted one; duplicate collapsing documented; `idempotency_key` support documented, the general prose corrected, and PD-09's replay narrowing stated; the three-sentence honest failure-behaviour paragraph including the cleanup-failure cost and the terminal-state scope

**docs-site/src/content/docs/reference/memory-record.md** — forward-link field row retyped to a list; Supersession section generalized to several predecessors per correcting record

**CLAUDE.md** — Supersession paragraph in "Memory contract (stable)" rewritten for the set-valued verb and key support

**skill/engram/skills/curating-memory/SKILL.md** — supersession rules block extended to the set, key usage note added, never-automatic rule preserved

**internal/server/supersededocs_test.go** (new) — `TestSupersedeDocsMatchShippedContract` with four sub-tests: rendered-example agreement (calling `renderTargetRejection`, never reproducing it), schema-versus-documented-type-and-bounds agreement, two-directional stale-claim sweep plus positive key-support assertion, and the failure-paragraph honesty check
</artifacts_produced>

<output>
Create `.planning/phases/03.1-merge-supersession-supersede-memory-accepts-multiple-targets/03.1-05-SUMMARY.md` when done.
</output>
