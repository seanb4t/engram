---
phase: 03.1
plan: 04
type: execute
wave: 3
depends_on: ["03.1-03"]
files_modified:
  - internal/server/idempotency.go
  - internal/server/idempotency_test.go
  - internal/server/tools.go
  - internal/server/tools_test.go
  - internal/server/schemarequired_test.go
autonomous: true
requirements: [REQ-merge-idempotency]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A merge retried with the same idempotency_key, the same content, and the same target set returns the original survivor's id and short_id instead of creating a second record"
    - "A merge retried with the same key and the same content but a DIFFERENT target set is rejected as an idempotency conflict, never silently applied"
    - "The same target set supplied in a different order, or with a duplicate, replays identically — the fingerprint is order-independent and duplicate-independent"
    - "A retry after an unobserved success returns the original answer rather than the already-superseded rejection, because the replay check runs before the already-superseded stage"
    - "A caller-supplied idempotency_key appears in the advertised supersede_memory JSON schema and is read, not silently discarded"
    - "A merge with no idempotency_key behaves exactly as before: a fresh record id, no replay lookup, no fingerprint stamped"
    - "Two different target sets that differ only at a target-id boundary produce different fingerprints — the encoding is injective over the set, not a naive join"
    - "A retry whose targets no longer exist is rejected as not found rather than replayed; the replay check sits after existence and ownership, which is a defined, tested outcome"
    - "A merge can never self-link: on the unkeyed path the survivor id is freshly minted and unnameable by the caller, and on the keyed path a call listing the deterministic survivor id as its own target is rejected as an idempotency conflict before any write"
  prohibitions:
    - "A keyed retry MUST NOT silently apply a different operation — a fingerprint mismatch is always a rejection, never a success (D-13)"
    - "The idempotency point-ID derivation MUST NOT be changed: altering its hash input un-dedups every previously keyed record on its next replay"
  artifacts:
    - path: "internal/server/idempotency.go"
      provides: "the content-plus-target-set fingerprint for the merge verb"
      contains: "func mergeFingerprint"
    - path: "internal/server/tools.go"
      provides: "the merge replay check, ordered before the already-superseded stage"
      contains: "func (d *deps) checkIdempotentMergeReplay"
    - path: "internal/server/idempotency_test.go"
      provides: "order-independence, distinctness, and boundary-injectivity proofs for the merge fingerprint"
      contains: "func TestMergeFingerprintOrderIndependent"
  key_links:
    - from: "internal/server/tools.go"
      to: "internal/server/idempotency.go"
      via: "the replay check compares the stored fingerprint against the content-plus-target-set hash"
      pattern: "mergeFingerprint[(]a[.]storeArgs, targetIDs[)]"
    - from: "internal/server/tools.go"
      to: "internal/server/idempotency.go"
      via: "the deterministic lookup point id is reused unchanged from the store_memory path"
      pattern: "idempotencyPointID[(]owner, a[.]Scope, a[.]IdempotencyKey[)]"
---

<objective>
Support `idempotency_key` on `supersede_memory`, with the replay fingerprint keyed on content AND
the target set, and the replay check ordered before the already-superseded stage.

Purpose: a multi-target merge is exactly when an ambiguous failure is expensive. The retry that
matters is the one the caller did not see fail — the server succeeded and the response was lost. With
the replay check after the already-superseded stage, every such retry is rejected with a conflict the
caller cannot act on, and the key is never consulted. Ordered correctly, the retry returns the answer
it lost.

Output: `mergeFingerprint`, `checkIdempotentMergeReplay`, the unwinding of Phase 25's deliberate
absence, and three inverted tests that used to pin that absence.
</objective>

<decision_history>
This plan reverses no design principle. `plan T-25-10` deferred `idempotency_key` on this verb **for
scope**, not on principle. Phase 25's review findings WR-03/WR-04 established that the code *claimed*
to exclude the key but did not: the `json:"-"` shadow field removes `idempotency_key` from the
advertised schema only, never from the wire decode, because a field with no JSON name never enters
`encoding/json`'s same-name shadowing contest. The promoted field on the embedded struct stayed the
sole decode target, and it was the defensive clear at the top of the handler — not the shadow — that
actually made the field inert. The Phase 25 fix made the absence honest and tested; it never argued
the feature should not exist. D-12 closes the deferred scope.

Three existing tests exist solely to pin that absence and are inverted here, not deleted:
`TestSupersedeMemorySchemaExcludesIdempotencyKey`, `TestSupersedeArgsDecodePopulatesPromotedIdempotencyKey`,
and `TestSupersedeMemoryIgnoresIdempotencyKey`.
</decision_history>

<planner_decisions>
- **PD-09 — the fingerprint keys on RESOLVED target UUIDs, and the replay check sits after existence
  and ownership.** Resolved ids are the identity of the records, so a target named by short_id in the
  first call and by UUID in the retry is the same operation; this pairs with the dedupe decision
  (PD-01) so `[A, A]` and `[A]` also fingerprint identically. The cost is that resolution must happen
  first, which places the replay check after the not-found stage rather than at the very top of the
  handler. D-14's requirement is satisfied in full — the check precedes the already-superseded stage,
  which is the case D-14 names — and the motivating scenario is unaffected, because supersession never
  deletes a target, so a retry after an unobserved success always finds its targets. The residual
  case, a retry whose targets were deleted in the meantime, rejects as not found; that is a defined,
  tested outcome, not an unknown. Reversibility: reversible — the ordering is one statement's position
  and the fingerprint input is one function's argument.
</planner_decisions>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-merge-supersession-supersede-memory-accepts-multiple-targets/03.1-CONTEXT.md
@.planning/phases/03.1-merge-supersession-supersede-memory-accepts-multiple-targets/03.1-RESEARCH.md
@.planning/phases/03.1-merge-supersession-supersede-memory-accepts-multiple-targets/03.1-PATTERNS.md
@.planning/phases/03.1-merge-supersession-supersede-memory-accepts-multiple-targets/03.1-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: mergeFingerprint — content plus the canonical target set</name>
  <files>internal/server/idempotency.go, internal/server/idempotency_test.go</files>
  <read_first>
    - internal/server/idempotency.go (the whole file: `engramIdempotencyNS`, `idempotencyPointID`, and `contentFingerprint` including its full doc comment about every client-authored field being represented)
    - internal/server/idempotency_test.go (the existing `contentFingerprint` stability, order-independence, and boundary-shift injectivity tests — the exact shapes to mirror)
    - internal/server/tools.go — `storeArgs` field list, to confirm which fields `contentFingerprint` already covers
  </read_first>
  <behavior>
    - Two calls with identical content and the same target ids in different order hash identically
    - Two calls with identical content and target sets differing by one id hash differently
    - Two calls with identical content where one repeats a target and the other does not hash identically
    - Two target sets whose concatenations coincide at a boundary hash differently
    - Content differences still change the hash exactly as contentFingerprint already guarantees
  </behavior>
  <action>
Add `func mergeFingerprint(a storeArgs, targets []string) string` to `internal/server/idempotency.go`,
directly beneath `contentFingerprint`.

Canonicalize the target list first: clone it so the caller's slice is never mutated, sort it, and
compact it to a deduped set. Sorting gives order-independence, which is what makes the same merge
expressed two ways one operation; deduping keeps it consistent with the handler's own dedupe of the
resolved set, so a retry that spells a target twice still replays.

Encode each target with the same per-element length-prefixed discipline `contentFingerprint` already
uses for tags — length, colon, value, colon — before the whole encoded block enters the outer
field-length prefix. A plain join is not injective over a set of ids: an id containing a literal
separator byte could otherwise collapse two distinct sets into the same string. Then reuse
`contentFingerprint`'s exact field list and order, folding the encoded target block in as one more
length-prefixed field, and hash the result the same way. Do not invent a new hash construction and do
not change the hash function.

Copy `contentFingerprint`'s "every client-authored field MUST be represented here" warning into the
new function's doc comment, extended to require that any future merge-specific field be added in the
same pass. State why: a field absent from the hash is silently excluded from the mismatch check, so a
keyed retry that changes only that field hashes identically, is misclassified as a replay, and returns
success while discarding the caller's value.

Add a second doc-comment paragraph stating that `idempotencyPointID` is deliberately NOT extended
with the target set: the point id is the lookup key for `(owner, scope, key)` and its UUIDv5 namespace
is load-bearing and must never change; the target set participates only in the fingerprint comparison,
which is what turns "same key, different operation" into a conflict rather than a silent overwrite.

Add the fingerprint tests to `internal/server/idempotency_test.go`, mirroring the existing
`contentFingerprint` test shapes:
`TestMergeFingerprintOrderIndependent`, `TestMergeFingerprintDedupeIndependent`,
`TestMergeFingerprintDistinctTargetSets`, and `TestMergeFingerprintTargetBoundaryInjectivity` (two
target sets chosen so a naive concatenation would coincide — the same probe the existing tag-boundary
test uses).
  </action>
  <verify>
    <automated>go test ./internal/server/... -run 'TestMergeFingerprint' -v 2>&amp;1 | rg -c '^--- PASS'</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c 'func mergeFingerprint' internal/server/idempotency.go` equals 1.
    - `rg -c 'func idempotencyPointID' internal/server/idempotency.go` equals 1 and `git diff internal/server/idempotency.go` shows no change inside that function's body.
    - `rg -c 'engramIdempotencyNS = uuid.MustParse' internal/server/idempotency.go` equals 1 and the UUID literal is unchanged from `git show HEAD:internal/server/idempotency.go`.
    - `go test ./internal/server/... -run 'TestMergeFingerprintOrderIndependent|TestMergeFingerprintDedupeIndependent|TestMergeFingerprintDistinctTargetSets|TestMergeFingerprintTargetBoundaryInjectivity' -v 2>&1 | rg -c '^--- PASS'` equals 4.
    - The same command reports zero `^--- FAIL`.
    - `task lint` exits 0.
  </acceptance_criteria>
  <done>The merge fingerprint is order-independent, duplicate-independent, injective at target boundaries, and reuses the existing hash construction without touching the point-id derivation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the replay check ahead of the already-superseded stage</name>
  <reversibility rating="costly">`idempotency_key` becomes an advertised field on `supersede_memory`'s published MCP JSON schema; withdrawing it later breaks any agent written against it, and the field is documented in `docs-site` and `CLAUDE.md` (CONTEXT.md D-12).</reversibility>
  <files>internal/server/tools.go</files>
  <read_first>
    - internal/server/tools.go lines 585-625 — `supersedeArgs`, its doc comment describing the shadow field, and the shadow field itself
    - internal/server/tools.go lines 1706-1800 — `deps.supersedeMemory` as plan 03.1-03 left it, including the defensive clear near the top
    - internal/server/tools.go lines 960-1015 (`checkIdempotentReplay` in full, and `storeMemory`'s call site showing the exact statement ordering idiom relative to its own preconditions)
    - internal/server/tools.go lines 1020-1060 (`persistAndEnqueue` — the keyed-path re-Get that resolves a concurrent keyed racer's short_id, and the comment explaining why it is gated on a non-empty fingerprint)
    - internal/server/idempotency.go — `mergeFingerprint` as Task 1 left it
    - internal/server/tools.go — `resolveSupersedeTargets` as plan 03.1-03 left it, to place the replay call between its class C and class D stages
  </read_first>
  <behavior>
    - A first keyed merge stores the survivor at the deterministic point id and stamps the merge fingerprint
    - An identical keyed retry returns the first survivor's id and short_id without a second write
    - A keyed retry with a different target set returns the idempotency-conflict sentinel
    - A keyed retry after an unobserved success returns the original answer rather than the already-superseded rejection
    - An unkeyed merge is unchanged: fresh id, no lookup, no fingerprint
    - An oversized key is rejected with the existing too-long argument error
  </behavior>
  <action>
Unwind the deliberate absence and wire the replay path.

In `supersedeArgs`: delete the depth-zero `IdempotencyKey` field that carried the no-JSON-name tag, so
the promoted field on the embedded struct becomes the sole schema and decode target. Delete the
doc-comment paragraphs describing the shadow and the defensive clear — they document behavior this
plan reverses. Replace them with a short comment recording the new contract: the key is supported, the
replay fingerprint covers content and the resolved target set, and the replay check runs before the
already-superseded stage.

In `deps.supersedeMemory`: delete the statement near the top that zeroes the promoted key on the
embedded args before it is read. That statement is what made the field inert; leaving it would make
every keyed merge silently keyless.

Add
`func (d *deps) checkIdempotentMergeReplay(ctx context.Context, owner string, a supersedeArgs, targets []string) (replay bool, id, shortID, pointID string, err error)`
to `internal/server/tools.go`, beside `checkIdempotentReplay`. Copy that function's control flow
exactly — empty key returns no-replay; an oversized key returns the existing out-of-range too-long
argument error; derive the point id with `idempotencyPointID(owner, a.Scope, a.IdempotencyKey)`
unchanged; `Get` the point; not-found means no replay with the point id carried out for the write;
any other read error propagates; a stored fingerprint equal to `mergeFingerprint(a.storeArgs, targets)`
is a replay returning the stored id and short_id; anything else is the idempotency-conflict sentinel.
Do not reinvent the branch structure — the same-key-different-content detection is the mechanism being
extended, not replaced.

Place the call inside the handler between the staged preflight's existence-and-ownership stage and its
already-superseded stage. Concretely: have `resolveSupersedeTargets` return after class C when a
caller-supplied hook says so, or split it into a class-A-through-C entry point plus a class-D-and-E
entry point and call the replay check between them — choose whichever keeps the function readable and
state which you chose in the summary. Add a comment at the call site naming the ordering requirement
and the failure it prevents: ordered the other way, the already-superseded stage rejects the retry
before the key is ever consulted, and that failure mode is invisible in a happy-path test.

On a replay, return the stored id and short_id immediately — no embed, no mint, no store write.

On a non-replay with a non-empty point id: set the survivor's id to the point id and stamp
`mergeFingerprint(a.storeArgs, targetIDs)` onto its fingerprint field before embedding, exactly as the
store-memory path does. After a successful `Supersede`, mirror the keyed-path re-read the shared write
tail already performs: when the fingerprint is non-empty, re-`Get` the survivor and prefer the
persisted short id, so a concurrent keyed racer that lost the last-write-wins race returns the short id
that was actually persisted rather than the one it discarded. Gate that extra round trip on the keyed
path only, as the existing tail does.
  </action>
  <verify>
    <automated>go test ./internal/server/... -run 'TestSupersedeMemoryIdempotency|TestSupersedeMemoryHonorsIdempotencyKey' -v 2>&amp;1 | rg -c '^--- PASS'</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c 'func \(d \*deps\) checkIdempotentMergeReplay' internal/server/tools.go` equals 1.
    - `rg -c 'mergeFingerprint\(a\.storeArgs, targetIDs\)' internal/server/tools.go` is at least 1.
    - `rg -c 'idempotencyPointID\(owner, a\.Scope, a\.IdempotencyKey\)' internal/server/tools.go` is at least 2 (the store path and the merge path).
    - `rg -v '^\s*//' internal/server/tools.go | rg -c 'a\.storeArgs\.IdempotencyKey = ""'` equals 0 — the clear is gone.
    - `rg -n 'IdempotencyKey' internal/server/tools.go` returns no hit between the `supersedeArgs` type declaration line and its closing brace — confirm by reading the struct body; the depth-zero shadow field is gone and only the embedded struct's promoted field remains.
    - `go test ./internal/server/... -run 'TestSupersedeMemoryIdempotencyReplay|TestSupersedeMemoryIdempotencyDifferentTargetSetConflicts|TestSupersedeMemoryReplayPrecedesAlreadySupersededPreflight' -v 2>&1 | rg -c '^--- PASS'` equals 3.
    - `go test ./internal/server/... -run 'TestStoreMemory|TestScheduleMemory' -v 2>&1 | rg -c '^--- FAIL'` equals 0 — the shared idempotency machinery is unaffected.
    - `task lint` exits 0.
  </acceptance_criteria>
  <done>A keyed merge replays on retry, conflicts on a different target set, and consults the key before the already-superseded stage ever runs.</done>
</task>

<task type="auto">
  <name>Task 3: Invert the three absence-pinning tests and cover the replay contract</name>
  <files>internal/server/tools_test.go, internal/server/schemarequired_test.go</files>
  <read_first>
    - internal/server/tools_test.go lines 3018-3130 (`TestSupersedeMemorySchemaExcludesIdempotencyKey`, `TestSupersedeArgsDecodePopulatesPromotedIdempotencyKey`, `TestSupersedeMemoryIgnoresIdempotencyKey` — all three in full, including their doc comments explaining what each pins)
    - internal/server/schemarequired_test.go lines 365-380 (the comment cross-referencing the schema-exclusion test by name)
    - internal/server/tools_test.go — the harness used by the existing idempotency tests for `store_memory`, for the replay/conflict assertion shape
    - internal/server/tools.go — the handler and `checkIdempotentMergeReplay` as Task 2 left them
  </read_first>
  <action>
Invert the three tests that exist to pin the absence, keeping each one's identity and doc comment
lineage so the history stays legible. All three are identified by exact name and line range in this
task's read_first list; rename each to its new name below and leave no reference to any prior name
anywhere in the package.

The schema test at `internal/server/tools_test.go:3018` becomes
`TestSupersedeMemorySchemaIncludesIdempotencyKey`, with its assertion flipped: the generated
`supersede_memory` schema now advertises the key. Rewrite its doc comment to record that the field's
prior absence was deferred scope from Phase 25, closed by this phase, and that the test now guards the
opposite direction.

The args-decode test at `internal/server/tools_test.go:3051` becomes
`TestSupersedeArgsDecodePopulatesIdempotencyKey`. Keep its decode assertion — the promoted field is
still the sole decode target — and update its doc comment: the wire decode was never the thing that
excluded the key, and now the decoded value is actually read.

The handler test at `internal/server/tools_test.go:3071` becomes
`TestSupersedeMemoryHonorsIdempotencyKey`, inverted: a caller-sent key now produces a keyed write whose
survivor sits at the deterministic point id and carries a non-empty fingerprint, rather than a normal
unkeyed write.

Update the cross-reference comment in `internal/server/schemarequired_test.go` near line 372 to the
renamed schema test, so no stale name survives the rename.

Then add the replay-contract coverage to `internal/server/tools_test.go`:

`TestSupersedeMemoryIdempotencyReplay`: keyed merge over two owned targets; assert the returned id
equals the deterministic point id; call again with the identical args; assert the same id and short id
come back, assert no second survivor exists, and assert the embedder was called exactly once across
both calls.

`TestSupersedeMemoryIdempotencyDifferentTargetSetConflicts`: same key and content, different target
set; assert the idempotency-conflict sentinel and assert no new record was written.

`TestSupersedeMemoryIdempotencyReorderedSetReplays`: same key and content, same targets supplied in
reverse order and with one duplicated; assert a replay, not a conflict.

`TestSupersedeMemoryReplayPrecedesAlreadySupersededPreflight`: the D-14 proof. Perform a keyed merge
so the targets end up carrying a superseded-by link, then repeat the identical call — simulating a
retry after an unobserved success — and assert the original id and short id come back rather than the
already-superseded rejection. Give it a doc comment stating that ordered the other way this test is
the only thing that catches the mistake, because every happy-path test passes either way.

`TestSupersedeMemoryUnkeyedUnchanged`: no key; assert a fresh non-deterministic id, an empty stored
fingerprint, and no lookup round trip.

`TestSupersedeMemoryOversizedKeyRejected`: a key over the existing byte bound; assert the out-of-range
too-long argument error naming the bound.

`TestSupersedeMemoryCannotSelfLink`: the adjacency edge — a merge naming its own survivor. Perform a
keyed merge with key K and scope S; the survivor now sits at the deterministic point id. Call again
with the same key and scope but list that survivor's id as the only target. Assert the
idempotency-conflict sentinel, assert no record's forward link contains its own id, and assert the
survivor's own link is unchanged. Add a doc comment recording why the unkeyed path needs no equivalent
test: the survivor id is a freshly minted UUID the caller cannot know at call time, so the collision is
unreachable there by construction rather than by check.
  </action>
  <verify>
    <automated>go test ./internal/server/... -run 'TestSupersedeMemory|TestSupersedeArgs' -v 2>&amp;1 | rg -c '^--- PASS'</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c 'TestSupersedeMemorySchemaExcludesIdempotencyKey' internal/server/` equals 0 across the whole package, including `schemarequired_test.go`.
    - `rg -c 'TestSupersedeMemoryIgnoresIdempotencyKey' internal/server/` equals 0 across the whole package.
    - `go test ./internal/server/... -run 'TestSupersedeMemorySchemaIncludesIdempotencyKey|TestSupersedeArgsDecodePopulatesIdempotencyKey|TestSupersedeMemoryHonorsIdempotencyKey' -v 2>&1 | rg -c '^--- PASS'` equals 3.
    - `go test ./internal/server/... -run 'TestSupersedeMemoryIdempotencyReplay|TestSupersedeMemoryIdempotencyDifferentTargetSetConflicts|TestSupersedeMemoryIdempotencyReorderedSetReplays|TestSupersedeMemoryReplayPrecedesAlreadySupersededPreflight|TestSupersedeMemoryUnkeyedUnchanged|TestSupersedeMemoryOversizedKeyRejected|TestSupersedeMemoryCannotSelfLink' -v 2>&1 | rg -c '^--- PASS'` equals 7.
    - The same command reports zero `^--- FAIL` and zero `^--- SKIP`.
    - Deliberate regression check, run manually and NOT committed: temporarily move the replay call to after the already-superseded stage; `TestSupersedeMemoryReplayPrecedesAlreadySupersededPreflight` must FAIL while every other test in the file still passes. Revert before committing and record the observed failure line in the summary — that contrast is the evidence the ordering gate is real.
    - `go test ./internal/server/... -count=1` exits 0.
    - `task` (lint + test) exits 0.
  </acceptance_criteria>
  <done>The three absence-pinning tests now pin the presence, and the replay contract — replay, conflict, reorder, ordering, unkeyed, oversized — is covered end to end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → `deps.supersedeMemory` | `idempotency_key` is now attacker-controlled input on this verb for the first time |
| replay lookup → Qdrant point id | The deterministic point id is derived from owner, scope, and key; owner is part of the hash input, not a filter |
| stored fingerprint → replay decision | A fingerprint that under-represents the request turns a different operation into a silent success |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.1-07 | Tampering | key reuse across a different target set | high | mitigate | The fingerprint folds in the canonical resolved target set, so same-key-different-set is a conflict rather than a silent different-operation success. Pinned by `TestSupersedeMemoryIdempotencyDifferentTargetSetConflicts`. |
| T-03.1-14 | Spoofing | cross-owner collision on the deterministic point id | high | mitigate | `idempotencyPointID` is reused unchanged, with owner as a hash input rather than a filter, so cross-owner collision between two authenticated owners is structurally impossible. The single-anonymous-bucket collapse when no issuer is configured is the project's existing documented invariant, not new here. |
| T-03.1-15 | Tampering | a future client-authored field omitted from the fingerprint | medium | mitigate | The "every client-authored field MUST be represented" warning is copied verbatim into the new function's doc comment, extended to merge-specific fields; the boundary-injectivity test makes a naive encoding fail. |
| T-03.1-16 | Denial of Service | unbounded idempotency key | low | mitigate | The existing byte bound and its out-of-range rejection are reused unchanged, covered by `TestSupersedeMemoryOversizedKeyRejected`. |
| T-03.1-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this phase; `03.1-RESEARCH.md` §Package Legitimacy Audit records "Not applicable". |
</threat_model>

<verification>
- `go test ./internal/server/... -count=1` exits 0 with zero `--- FAIL` and zero `--- SKIP`.
- The point-id derivation and its UUIDv5 namespace are byte-identical to `HEAD`.
- The ordering regression check was observed to fail exactly one test and was reverted.
- `task` (lint + test) is green.
</verification>

<success_criteria>
- An identical keyed retry returns the original survivor without a second write.
- A same-key different-target-set retry conflicts.
- A reordered or duplicated target set replays.
- A retry after an unobserved success returns its answer instead of the already-superseded rejection.
- The three Phase 25 absence tests now pin the presence, and no reference to their old names survives.
</success_criteria>

<artifacts_produced>
## Artifacts this plan produces

**internal/server/idempotency.go**
- `func mergeFingerprint(a storeArgs, targets []string) string`

**internal/server/tools.go**
- `func (d *deps) checkIdempotentMergeReplay(ctx context.Context, owner string, a supersedeArgs, targets []string) (replay bool, id, shortID, pointID string, err error)`
- deletion of `supersedeArgs`'s depth-zero `IdempotencyKey` shadow field and its doc-comment paragraphs
- deletion of the handler's defensive clear of the promoted key
- keyed-path survivor id, fingerprint stamp, and post-write short-id re-read

**internal/server/idempotency_test.go**
- `TestMergeFingerprintOrderIndependent`, `TestMergeFingerprintDedupeIndependent`, `TestMergeFingerprintDistinctTargetSets`, `TestMergeFingerprintTargetBoundaryInjectivity`

**internal/server/tools_test.go**
- renamed: `TestSupersedeMemorySchemaIncludesIdempotencyKey`, `TestSupersedeArgsDecodePopulatesIdempotencyKey`, `TestSupersedeMemoryHonorsIdempotencyKey`
- new: `TestSupersedeMemoryIdempotencyReplay`, `TestSupersedeMemoryIdempotencyDifferentTargetSetConflicts`, `TestSupersedeMemoryIdempotencyReorderedSetReplays`, `TestSupersedeMemoryReplayPrecedesAlreadySupersededPreflight`, `TestSupersedeMemoryUnkeyedUnchanged`, `TestSupersedeMemoryOversizedKeyRejected`, `TestSupersedeMemoryCannotSelfLink`

**internal/server/schemarequired_test.go**
- cross-reference comment updated to the renamed schema test
</artifacts_produced>

<output>
Create `.planning/phases/03.1-merge-supersession-supersede-memory-accepts-multiple-targets/03.1-04-SUMMARY.md` when done.
</output>
