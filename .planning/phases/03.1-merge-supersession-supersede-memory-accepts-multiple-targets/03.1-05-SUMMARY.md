---
phase: 03.1-merge-supersession-supersede-memory-accepts-multiple-targets
plan: 05
subsystem: docs
tags: [supersede-memory, mcp, documentation, drift-gate, idempotency, jsonschema]

# Dependency graph
requires:
  - phase: 03.1-00
    provides: "Developer ruling: PD-07 cap DROPPED, PD-06 collapse-ambiguous ADOPTED, PD-01 dedupe-silently ADOPTED, PD-02 allow-heterogeneous ADOPTED"
  - phase: 03.1-02
    provides: "Store.Supersede's terminal-state-only compensation/reconciliation claim"
  - phase: 03.1-03
    provides: "renderTargetRejection, resolveAndAuthorizeSupersedeTargets/validateSupersedeTargetState, supersedeMemoryInputSchema, the fixed four-class rejection order"
  - phase: 03.1-04
    provides: "checkIdempotentMergeReplay, mergeFingerprint, PD-09's replay-after-deletion narrowing"
provides:
  - "docs-site/reference/errors.md: 'Multi-target rejections' section — fixed four-class order, all-offenders-named, verbatim worked examples"
  - "docs-site/reference/tools.md: supersede_memory retyped to an array supersedes argument, no published maximum (PD-07 dropped), duplicate collapse documented, idempotency_key support documented, honest three-sentence failure-behaviour paragraph"
  - "docs-site/reference/memory-record.md: forward-link field retyped to a list, Supersession section generalized to several predecessors"
  - "CLAUDE.md and skill/engram/skills/curating-memory/SKILL.md: Supersession sections updated for the set-valued verb and key support"
  - "internal/server/supersededocs_test.go: TestSupersedeDocsMatchShippedContract — binds every shipped surface above to the production code that implements it"
affects: []

actuals:
  tokens: 7526
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "Documentation-drift gate that calls the production rendering helper (renderTargetRejection) byte-for-byte, never a re-typed format string, so the documented example and the shipped behaviour cannot independently drift"
    - "Two-directional stale-claim sweep: a phrase-absence check paired with a phrase-presence check, so a document that silently drops a claim without replacing it still fails"
    - "Anchored machine-readable line (`**Maximum targets per call:** N`) as the sole prose-side source of truth for a schema-derived bound, read by regex rather than free-text number scanning"

key-files:
  created:
    - internal/server/supersededocs_test.go
  modified:
    - docs-site/src/content/docs/reference/errors.md
    - docs-site/src/content/docs/reference/tools.md
    - docs-site/src/content/docs/reference/memory-record.md
    - CLAUDE.md
    - skill/engram/skills/curating-memory/SKILL.md

key-decisions:
  - "PD-07 (03.1-00-SUMMARY.md) DROPPED branch taken everywhere: no '**Maximum targets per call:**' anchor was written anywhere in tools.md, and the drift test's schema/doc bound sub-test asserts minItems:1 with no maxItems and no anchor — the two anchored-maximum regression checks in the plan's acceptance criteria do not apply and were not run, per the plan's own conditional."
  - "PD-01 dedupe-silently ADOPTED branch taken: tools.md states duplicates and two spellings of the same record 'collapse to one target'; no duplicate-rejection sentence was published."
  - "PD-06 collapse-ambiguous and PD-02 allow-heterogeneous documented as shipped: not-owned/nonexistent/ambiguous-short-id are one rejection everywhere they're described, and tools.md states a merge may span scopes, categories, and visibilities."
  - "The three-sentence failure-behaviour paragraph is required verbatim structure, not paraphrase: usual no-op, then the cleanup-failure cost (orphaned survivor, dangling links, operator log as only remediation), then the terminal-state scope of the clean-state claim. The word 'guarantee' and the overstated forms ('never observable', 'cannot be observed', 'no reader') do not appear anywhere in tools.md."
  - "The drift test's worked examples use two fixed placeholder target strings (a1b2c3d4e5/f6g7h8j9k0 for addressability, a1b2c3d4e5/m1n2p3q4r5 for already-superseded) chosen once and shared verbatim between the published doc and the Go test — renderTargetRejection is called with these same literals in the test, so the published text is provably the function's actual output, not a hand-typed transcription."

patterns-established:
  - "normalizeWS (collapse all whitespace runs before substring comparison) as the standard technique for asserting a fixed string against hand-wrapped markdown prose, so a doc author's line-wrap choice cannot break a drift assertion that has nothing to do with wrapping."

requirements-completed: [REQ-merge-supersession, REQ-merge-atomicity, REQ-merge-idempotency]

coverage:
  - id: D1
    description: "The published error reference's multi-target worked examples (addressability class, already-superseded class) are the server's actual rendered output, asserted against renderTargetRejection directly rather than a re-typed format string"
    requirement: "REQ-merge-supersession"
    verification:
      - kind: unit
        ref: "internal/server/supersededocs_test.go#TestSupersedeDocsMatchShippedContract/RenderedRejectionExamplesAppearVerbatim"
        status: pass
    human_judgment: false
  - id: D2
    description: "The published tool reference's supersedes array type, idempotency_key advertisement, and target-count bound agree with the generated MCP schema (PD-07 dropped the cap: both sides agree by absence)"
    requirement: "REQ-merge-supersession"
    verification:
      - kind: unit
        ref: "internal/server/supersededocs_test.go#TestSupersedeDocsMatchShippedContract/SchemaTypeAndBoundsAgreeWithDocumentedContract"
        status: pass
    human_judgment: false
  - id: D3
    description: "No shipped document (tool reference, CLAUDE.md, curating-memory skill) claims supersede_memory rejects idempotency keys, and each positively states the key is accepted — checked in both directions so a silently dropped claim cannot pass"
    requirement: "REQ-merge-idempotency"
    verification:
      - kind: unit
        ref: "internal/server/supersededocs_test.go#TestSupersedeDocsMatchShippedContract/NoShippedDocClaimsKeyUnsupportedAndEachPositivelyStatesSupport"
        status: pass
    human_judgment: false
  - id: D4
    description: "The published failed-merge failure paragraph names the cleanup-failure cost (orphaned survivor, dangling links, operator log as only remediation) and contains none of the overstated atomicity claims"
    requirement: "REQ-merge-atomicity"
    verification:
      - kind: unit
        ref: "internal/server/supersededocs_test.go#TestSupersedeDocsMatchShippedContract/FailureBehaviorParagraphIsHonestNotOverstated"
        status: pass
    human_judgment: false
  - id: D5
    description: "The complete shipped merge-supersession contract (target-set cap ruling, rejection class order and ambiguous-short-id collapse, duplicate handling, cross-scope merges) matches what the developer ruled in 03.1-00-SUMMARY.md"
    requirement: "REQ-merge-supersession"
    verification: []
    human_judgment: true
    rationale: "This is the checkpoint's own deliverable — a developer comparing the shipped documentation against their own wave-1 ruling. No test can substitute for the developer confirming their own decision was implemented as they intended; the drift test only proves internal self-consistency (docs match code), not that the ruling itself was honored."

duration: ~50min
completed: 2026-08-11
status: complete
---

# Phase 03.1 Plan 05: Documentation and Drift-Gate Summary

**Multi-target rejection envelope documented in the error reference with verbatim server-rendered examples, `supersede_memory`'s `supersedes` argument retyped to an array across three docs-site pages, `idempotency_key` support and the honest three-sentence failure paragraph published, CLAUDE.md and the curating-memory skill brought current, and `TestSupersedeDocsMatchShippedContract` binds all of it to the production code so a future edit fails the build instead of drifting.**

## Performance

- **Duration:** ~50 min (not precisely instrumented at start)
- **Tasks:** 4/4 completed (Task 4, the checkpoint, was verified and approved by the developer)
- **Files modified:** 5 modified, 1 created

## Accomplishments

- **`docs-site/src/content/docs/reference/errors.md`** — new "Multi-target rejections" section placed after the relational example: the fixed four-class order (set shape, addressability-and-access, rule target, already-superseded), the all-offenders-named-in-caller-order rule, the three-way indistinguishability statement (not-owned/nonexistent/ambiguous), and two worked examples copied verbatim from what `renderTargetRejection` actually returns.
- **`docs-site/src/content/docs/reference/tools.md`** — `supersede_memory`'s `supersedes` argument retyped `array of string`; PD-07's dropped-cap branch taken (no maximum published anywhere in the section); PD-01's dedupe-silently branch taken (`collapse to one target`, no duplicate-rejection sentence); a new "Idempotency" paragraph documenting `idempotency_key` acceptance, the content-plus-target-set fingerprint, and PD-09's replay-after-deletion narrowing; the required three-sentence honest failure-behaviour paragraph (usual no-op → cleanup-failure cost → terminal-state scope); Constraints bullets extended to name every offending/rule/already-superseded target in the set.
- **`docs-site/src/content/docs/reference/memory-record.md`** — `supersedes` field row retyped to `string[]`; Supersession section generalized to "one correcting record may replace several predecessors."
- **`CLAUDE.md`** — Supersession paragraph in "Memory contract (stable)" rewritten: `supersedes` as a set, the forward link as a list, the per-target single-live-head rule, the three-way indistinguishability statement, and `idempotency_key` acceptance. The duplicate-RECORDS-merge sentence stays unconditional and disambiguated from PD-01's conditional duplicate-target-IDs handling, per cycle-3 review's disposition.
- **`skill/engram/skills/curating-memory/SKILL.md`** — supersession rules block extended: a new rule stating several targets may be corrected in one call (with the array-literal example), the own-the-target and rule-immutability rules extended to the whole set, `idempotency_key` support with a usage note, and the never-automatic rule strengthened ("no matter how large the target set") rather than softened.
- **`internal/server/supersededocs_test.go`** (new) — `TestSupersedeDocsMatchShippedContract` with four sub-tests, all calling production code or reading the shipped files directly: rendered-example verbatim match against `renderTargetRejection` (never a re-typed format string), schema-vs-documented-type-and-bounds agreement (minItems:1 unconditional, maxItems/anchor absent-by-absent per PD-07), the two-directional stale-claim sweep across the tool reference/CLAUDE.md/skill file, and the failure-paragraph honesty check.
- Four deliberate drift regressions were run manually, observed to FAIL exactly as predicted, and reverted before committing (see "Regression checks" below). The two anchored-maximum regression checks in the plan's acceptance criteria are conditional on a cap being adopted; PD-07 dropped it, so those two do not apply and were not run.

## Task Commits

1. **Task 1: Document the multi-target rejection envelope and the array wire shape** — `7429f90b` (docs)
2. **Task 2: Correct the project contract and the agent-facing skill** — `49cb1dd0` (docs)
3. **Task 3: Bind the docs to the shipped contract with a drift gate** — `7b54cac7` (test)

4. **Task 4: Verify the shipped contract matches what was ruled in plan 00** (checkpoint:human-verify, gate="blocking") — **RESOLVED: approved** by the developer on 2026-08-11. No code change; the checkpoint's deliverable is the verification itself.

### Task 4 checkpoint outcome

The developer replied **`approved`** — the shipped contract matches the wave-1 ruling recorded in
`03.1-00-SUMMARY.md` on all four decisions. Verified before the gate was presented, against this
plan's worktree rather than from the executor's self-report:

| Ruling (`03.1-00-SUMMARY.md`) | Shipped | Check |
|---|---|---|
| PD-07 cap **dropped** | No `**Maximum targets per call:**` anchor anywhere; "There is no maximum target count — the set is unbounded"; schema `minItems: 1`, no `maxItems` | anchor count = 0 |
| PD-06 **collapse-ambiguous** | not-owned / nonexistent / ambiguous-short-id documented as the same rejection in `errors.md`, `tools.md`, `CLAUDE.md` | present |
| PD-01 **dedupe-silently** | "…collapse to one target"; no duplicate-rejection sentence published | count = 1 |
| PD-02 **allow-heterogeneous** | "A merge may span different scopes, categories, and visibilities…" | present |

Failure-semantics prohibition honoured: the no-op is published as the usual outcome and never as a
guarantee, with the cleanup-failure cost in the very next sentence. `rg -in 'guarantee|never
observable|cannot be observed|no reader'` over `tools.md` returns nothing.

Two corrections to the executor's own checkpoint report, recorded so the next reader is not misled:

1. Its "All work is committed" claim was **inaccurate** — seven files were modified-but-uncommitted
   in the worktree (`.claude/settings.json`, `docs-site/package.json`, `ui/tsconfig.json`,
   `internal/webauth/static/_app/version.json`, `cmd/engram/client_list_test.go`,
   `cmd/engram/migrate_test.go`, `internal/server/tools_test.go`). None was plan work — all of it was
   `task fmt` churn — and `worktree.cleanup-wave` correctly refused to merge with `worktree_dirty`
   until it was explicitly discarded. All four commits were within declared scope.
2. Its reported `expected_base` was its own HEAD (`97845914`) rather than the dispatch base. The true
   fork point was confirmed as `8ff09fa7` via `git merge-base`; no real divergence.

**Observed during that discard, and worth knowing:** one piece of the churn was a *corruption* rather
than a formatting change — in `cmd/engram/migrate_test.go` a formatter rewrote the comment
`"--from ''"` (two apostrophes denoting an empty string) into `"--from ”"` (one right double quote).
The main tree carries the correct form. Something in `task fmt` applies smart-quote substitution
inside Go comments; it was discarded here but will recur. Not filed as an issue (developer declined).

## Files Created/Modified

- `docs-site/src/content/docs/reference/errors.md` — "Multi-target rejections" section
- `docs-site/src/content/docs/reference/tools.md` — `supersede_memory` section rewritten (array type, idempotency, failure honesty, extended constraints)
- `docs-site/src/content/docs/reference/memory-record.md` — `supersedes` field row and Supersession section generalized
- `CLAUDE.md` — Supersession paragraph rewritten
- `skill/engram/skills/curating-memory/SKILL.md` — supersession rules block rewritten
- `internal/server/supersededocs_test.go` (new) — `TestSupersedeDocsMatchShippedContract`

## Decisions Made

See `key-decisions` in frontmatter. The two worth calling out beyond that list:

- **The worked-example target strings are shared literals, not independently chosen.** The doc's two examples (`a1b2c3d4e5, f6g7h8j9k0` for addressability; `a1b2c3d4e5, m1n2p3q4r5` for already-superseded) are the exact strings passed to `renderTargetRejection` in the Go test — picking them once and using them in both places is what makes the "verbatim" claim meaningful rather than coincidental.
- **`normalizeWS` (collapse whitespace before substring comparison) is used for every doc-content assertion in the drift test**, not just the ones that risked line-wrap breakage. This was a deliberate choice to keep all doc-reading assertions in the file behaviorally uniform, rather than mixing raw and normalized comparisons file-by-file.

## Deviations from Plan

None — plan executed exactly as written. PD-07's dropped-cap branch and PD-01's dedupe-silently branch were both taken as ruled in `03.1-00-SUMMARY.md`; no new bug, missing-functionality gap, or blocking issue required an unplanned fix (Rules 1-3 not triggered).

## Regression checks (observed red, then reverted — not committed)

- **Addressability worked example changed by one character** (`f6g7h8j9k0` → `f6g7h8j9k1` in `errors.md`): `TestSupersedeDocsMatchShippedContract/RenderedRejectionExamplesAppearVerbatim` failed: `rendered addressability rejection "not found: a1b2c3d4e5, f6g7h8j9k0" not found verbatim in ../../docs-site/src/content/docs/reference/errors.md — update the worked example to match renderTargetRejection's actual output`. Reverted via `git checkout -- docs-site/.../errors.md`; suite green again.
- **Stale phrase reinstated in CLAUDE.md** (prepended `` `idempotency_key` is not supported on this verb. `` before the current sentence): the stale-claim sub-test failed: `project contract (../../CLAUDE.md) still contains the stale claim "is not supported on this verb" that supersede_memory rejects idempotency keys`. Reverted; suite green again.
- **Key-support sentence deleted from CLAUDE.md without reinstating any stale phrase** (removed the whole `idempotency_key is accepted on this verb...` clause): the positive half of the same sub-test failed: `project contract (../../CLAUDE.md) does not positively state that idempotency_key is accepted — want "idempotency_key\` is accepted on this verb"`. Reverted; suite green again — this is the check that proves the sweep is two-directional, per cycle-1 review's LOW finding.
- **Cleanup-failure sentence replaced with an unqualified no-op claim** (tools.md's failure paragraph collapsed to "...so a failed merge is always a no-op."): the failure-honesty sub-test failed on all three required fragments: `is missing the cleanup-failure cost fragment "orphaned new record"`, `"logs the affected ids for the operator"`, `"that log is the only remediation path"`. Reverted; suite green again.
- **Anchored-maximum regression checks — NOT APPLICABLE.** PD-07 dropped the target-set cap, so no `**Maximum targets per call:**` anchor exists in `tools.md` and no `maxItems` exists on the generated schema. The plan's two anchored-maximum regression checks ("change the integer by one" / "delete the anchor entirely") are explicitly conditional on a cap having been adopted and were correctly not run.

## Issues Encountered

None.

## User Setup Required

None — no external service configuration required.

## Verification Evidence (for the checkpoint)

Run once, ahead of the checkpoint, so the developer has concrete values in hand rather than needing to re-derive them:

- `task` (lint + test, full repo, Go + Python) — **green** (all packages pass, `golangci-lint run ./...` reports 0 issues, `rumdl check .` reports 0 issues across 125 files).
- `go test ./internal/server/... -run 'TestSupersedeDocsMatchShippedContract' -v` — all 4 sub-tests **PASS**, 0 FAIL, 0 SKIP.
- `go test ./internal/store/... -run TestSupersedeMultiTOCTOU -v` — **PASS** (both `TestSupersedeMultiTOCTOU` and `TestSupersedeMultiTOCTOUNoDanglingLink`), confirmed to RUN rather than skip.
- `task lint:markdown` was run only as a general repository gate (0 issues, 125 files) — per this plan's explicit prohibition, it is NOT cited as evidence for anything under `docs-site/**`; the drift test and the `rg` content assertions above are the only evidence for those three files.

## Next Phase Readiness

- Tasks 1-3 are complete and committed. Task 4 (the checkpoint) is the only remaining step in this plan — it asks the developer to compare the shipped documentation above against their own ruling in `03.1-00-SUMMARY.md` (cap dropped, collapse-ambiguous, dedupe-silently, allow-heterogeneous) and confirm they agree, plus review the two decisions that were always the planner's (PD-05, PD-10).
- This is the last plan in phase `03.1-merge-supersession-supersede-memory-accepts-multiple-targets`. Once the checkpoint is approved, the phase is complete.

## Self-Check

- `docs-site/src/content/docs/reference/errors.md` — FOUND
- `docs-site/src/content/docs/reference/tools.md` — FOUND
- `docs-site/src/content/docs/reference/memory-record.md` — FOUND
- `CLAUDE.md` — FOUND
- `skill/engram/skills/curating-memory/SKILL.md` — FOUND
- `internal/server/supersededocs_test.go` — FOUND
- Commit `7429f90b` (Task 1) — FOUND in `git log`
- Commit `49cb1dd0` (Task 2) — FOUND in `git log`
- Commit `7b54cac7` (Task 3) — FOUND in `git log`

## Self-Check: PASSED

---
*Phase: 03.1-merge-supersession-supersede-memory-accepts-multiple-targets*
*Completed: 2026-08-11*
