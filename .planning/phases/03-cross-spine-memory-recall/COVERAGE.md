No external API integration: the detector matched the verb "connect" and the noun "mcp" in success criterion 4 ("Available on MCP and Connect at parity via an additive proto field"), but engram is the *provider* of both surfaces, not a consumer of an external one — MCP and ConnectRPC are engram's own served protocols, and this phase adds a field to engram's own `EngramService` schema. The only outbound clients engram holds (Qdrant, the OpenAI-compatible embedder) are pre-existing infrastructure this phase does not touch.
