---
phase: 06-install-documentation
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - .planning/phases/06-install-documentation/06-POST-RELEASE.md
  - .planning/phases/06-install-documentation/06-RELEASE-OBSERVATIONS.md
  - docs-site/src/content/docs/guides/install.md
  - docs-site/src/content/docs/guides/agent-setup.md
  - docs-site/src/content/docs/guides/plugin.md
autonomous: true
requirements:
  - REQ-homebrew-cask-published
  - REQ-docs-install-path
  - REQ-docs-setup-documented
estimate:
  tokens: 16000
  raw_tokens: 16000
  tasks: 3
  confidence: low
artifacts_produced:
  - .planning/phases/06-install-documentation/06-RELEASE-OBSERVATIONS.md
must_haves:
  truths:
    - A tagged release has demonstrably published the cask through the existing pipeline with upload enabled.
    - The documented Homebrew invocation has actual installation observations on macOS and Linux, each on amd64 and arm64, with native or emulated execution identified.
    - Pre-merge guides label final setup as unreleased; a post-release handoff gates any later shipped setup claim.
    - Four v0.15.1 actual installations satisfy distribution observation; qualifying setup release checks remain pending in the post-release handoff.
  artifacts:
    - path: .planning/phases/06-install-documentation/06-RELEASE-OBSERVATIONS.md
      provides: Dated publication, four-platform installation and setup-release evidence with explicit pending rows
    - path: docs-site/src/content/docs/guides/install.md
      provides: Acquisition instructions tied to observed released artifacts
  key_links:
    - from: .planning/phases/06-install-documentation/06-RELEASE-OBSERVATIONS.md
      to: https://github.com/seanb4t/engram/issues/514
      via: Release-run upload evidence and Phase 1 handoff ownership
      pattern: https://github[.]com/seanb4t/engram/issues/514
    - from: .planning/phases/06-install-documentation/06-RELEASE-OBSERVATIONS.md
      to: docs-site/src/content/docs/guides/agent-setup.md
      via: Source contract and release provenance control availability wording
      pattern: docs-site/src/content/docs/guides/agent-setup[.]md
---

<objective>
D-10 shipping amendment supersedes the original pre-release blocking sequence.
The post-release handoff retains all qualifying setup release checks.

Close the actual release/install acceptance of REQ-homebrew-cask-published and reconcile the guides with shipped setup per D-08,D-09. Collect every locally authorized observation before requesting unavailable host access or release action.

Purpose: Publication, installation and available setup are independently evidenced, so Phase 6 cannot seal from local documentation checks alone.
Output: Auditable release/host observations and accurately released availability wording, or a concrete checkpoint with missing evidence.
</objective>

<execution_context>
@/Users/sean/.codex/gsd-core/workflows/execute-plan.md
@/Users/sean/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-install-documentation/06-CONTEXT.md
@.planning/phases/06-install-documentation/06-RESEARCH.md
@.planning/phases/06-install-documentation/06-PATTERNS.md
@.planning/phases/06-install-documentation/06-VALIDATION.md
@.planning/phases/06-install-documentation/06-01-SUMMARY.md
@.planning/phases/01-version-homebrew-distribution/01-VERIFICATION.md
@RELEASING.md
</context>

<tasks>
<task type="tracer">
  <name>Task 1: Collect publication evidence and execute all feasible disposable installs</name>
  <files>.planning/phases/06-install-documentation/06-RELEASE-OBSERVATIONS.md</files>
  <read_first>Read .goreleaser.yaml, .github/workflows/release.yaml, 06-01-SUMMARY.md and the Install guide. Read issue #514 body and all comments together, latest release assets, tap cask and introducing commit, and the matching successful release run/log. Read current official Homebrew documentation through Context7 only if new install-environment syntax is needed; reuse existing research syntax otherwise.</read_first>
  <action>Per D-08,D-09, create a dated evidence artifact with separate publication, setup availability and four-platform installation sections. Use read-only gh issue view 514 --repo seanb4t/engram --comments, gh release view --repo seanb4t/engram --json tagName,publishedAt,url,assets, and tap/release workflow APIs. Identify the real tag, release run URL, upload-guard value and upload outcome, cask version and tap revision, archive/checksum URLs. Inspect the tagged source and installed artifact help to determine whether final setup/client-ID/delegation implementation is included; the existing 0.15.1 publication does not prove setup availability. Record precise evidence and failures, not assumptions from run color or cask existence.

Inventory available environments without changing them: host OS/arch, Homebrew prefix/version and active engram path; Docker availability and supported execution architectures; macOS Intel/Rosetta capability. Use already-authorized isolated/reversible verification autonomously. Establish disposable, non-root Homebrew prefixes or container homes, caches and configuration, ensuring the resolved brew executable, binary target, Caskroom and completion paths all stay inside the disposable environment before installing. Do not repurpose HOME or write to the user's existing prefix/configuration. On macOS try a clean arm64 prefix and a genuine amd64 Homebrew execution under available Rosetta; on Linux use disposable amd64/arm64 Docker environments when usable. Record native versus translated/emulated execution explicitly. If a supported isolated setup cannot be established, record the command/error and missing environment; do not mutate the active install, install privileged host facilities or silently substitute an archive run.

Run the exact documented brew install seanb4t/tap/engram in each viable clean environment. Start with one available target end-to-end (published cask → actual installer → resolved binary/version → generated completions), then expand the identical observation contract to the other three. Capture install exit result, OS/version, execution architecture, emulation mechanism, Homebrew version/prefix, cask/tap revision and release identity; invoke the newly installed binary by its resolved disposable absolute path with version --output json and setup --help when present. Verify binary version equals cask version and record bash/zsh/fish completion file results. Existing completion hooks are observed, not changed. A downloaded archive, simulated architecture branch, source review or standalone version run is not an install observation.

Keep all successful observations even when a release containing setup is unavailable. If a newer qualifying release appears, repeat affected install observations against that cask before final acceptance. Remove only disposable resources created for this task after preserving evidence; run no setup registration, no third-party CI gate, no tag/merge/push/workflow dispatch and no issue comment. For every unavailable row, state the exact evidence missing and the attempted isolation command/result. Complete all feasible checks before Task 2.</action>
  <verify>
    <automated>gh release view --repo seanb4t/engram --json tagName,publishedAt,url,assets &amp;&amp; rumdl check .planning/phases/06-install-documentation/06-RELEASE-OBSERVATIONS.md</automated>
    <human-check>Review each saved install transcript against its environment identity, installed absolute binary, cask version and completion paths. Review release-run upload evidence. This one-time actual installation observation is deliberately not a CI test.</human-check>
  </verify>
  <acceptance_criteria>Publication and setup availability have independent evidence; all four installation rows contain actual results or exact missing-host/failure facts; every feasible isolated install is attempted; no existing binary or agent registration is changed.</acceptance_criteria>
  <done>The evidence artifact is concrete and all locally feasible work is complete; any remaining external prerequisite is precisely identified.</done>
</task>

<task type="auto">
  <name>Task 2: Separate pre-merge acceptance from post-release checks</name>
  <files>.planning/phases/06-install-documentation/06-POST-RELEASE.md, .planning/phases/06-install-documentation/06-RELEASE-OBSERVATIONS.md</files>
  <action>Apply D-10, accepted through the user's shipping request after the sequencing correction was proposed. Preserve the complete Task 1 observations and historical checkpoint. Record the release-dependent work in an explicit post-release handoff linked to existing issue #514: qualifying tag/run, final setup/client-ID/delegation provenance, repeated four actual installs, and only then availability updates. Do not claim those checks passed or manufacture a release. The original pre-PR release checkpoint is superseded because it creates a circular dependency with shipping's verification gate.</action>
  <verify><automated>rumdl check --no-exclude .planning/phases/06-install-documentation/06-POST-RELEASE.md .planning/phases/06-install-documentation/06-RELEASE-OBSERVATIONS.md</automated></verify>
  <done>Pre-merge acceptance and pending post-release evidence have explicit, non-circular ownership.</done>
</task>

<task type="auto">
  <name>Task 3: Verify truthful pre-merge guide availability</name>
  <files>docs-site/src/content/docs/guides/install.md, docs-site/src/content/docs/guides/agent-setup.md, docs-site/src/content/docs/guides/plugin.md</files>
  <action>Retain v0.15.1 archive identities and unreleased setup warnings. Confirm the guides cover final verified source contracts without claiming setup is in the installed release. Reuse the successful focused docs checks unless content changes. Hand off to canonical Phase 6 goal verification. Moving the availability update after release is a recorded sequence correction, not evidence that release work was performed.</action>
  <verify><automated>rumdl check --no-exclude docs-site/src/content/docs/guides/install.md docs-site/src/content/docs/guides/agent-setup.md docs-site/src/content/docs/guides/plugin.md</automated><human-check>Cross-check source behavior, truthful availability and the post-release handoff.</human-check></verify>
  <done>Pre-merge docs and publication/install observations are ready for verification; release-dependent claims remain pending.</done>
</task>
</tasks>

<threat_model>
ASVS level: 1. Blocking threshold: high. No new application/package dependency or schema change.

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Published release/tap → disposable host | Installer hooks execute downloaded release code |
| Disposable host → user environment | Prefix, binary and completion paths must stay isolated |
| Observation → acceptance claim | Publication cannot stand in for an observed install |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-06-05 | Tampering | Install observation | high | mitigate | Task 1 verifies all write paths inside disposable prefixes/containers; never overwrites installed engram or runs setup registration |
| T-06-06 | Spoofing | Artifact identity | high | mitigate | Task 1 ties canonical cask/tap and release/run to checksum assets and installed JSON version; record actual execution architecture |
| T-06-07 | Repudiation | Release/install acceptance | medium | mitigate | Tasks 1–3 retain exact commands/results, failed and pending rows, and qualifying release provenance before removing warnings |
| T-06-08 | Elevation of privilege | Release publication | high | mitigate | Task 2 requests only actual missing prerequisites after local work; no fabricated tag, direct main push, merge or release mutation is authorized here |
</threat_model>

<verification>
Verify the original three roadmap criteria against the complete guides, source contracts,
v0.15.1 publication and four actual install observations. Verify D-10's explicit
post-release handoff. Neither phase verification nor PR creation claims that this
branch's setup implementation has been released.
</verification>

<success_criteria>
The three Phase 6 requirements have pre-merge supporting evidence. Qualifying setup
release validation and subsequent availability updates remain tracked in issue #514
and 06-POST-RELEASE.md. No real agent registration or unauthorized release action occurred.
</success_criteria>

<output>
Create 06-02-SUMMARY.md for this corrected pre-merge scope, documenting the original
checkpoint, its sequencing correction and the pending post-release work.
</output>
