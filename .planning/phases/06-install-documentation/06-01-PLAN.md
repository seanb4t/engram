---
phase: 06-install-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs-site/src/content/docs/guides/install.md
  - docs-site/src/content/docs/guides/agent-setup.md
  - docs-site/src/content/docs/guides/quickstart.md
  - docs-site/src/content/docs/guides/cli.md
  - docs-site/src/content/docs/guides/plugin.md
autonomous: true
requirements:
  - REQ-docs-install-path
  - REQ-docs-setup-documented
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low
artifacts_produced:
  - docs-site/src/content/docs/guides/install.md
  - docs-site/src/content/docs/guides/agent-setup.md
must_haves:
  truths:
    - A new reader can navigate from Quickstart to Homebrew or release archives and then to agent setup, including when a server already exists.
    - Readers see which installed releases support setup and can distinguish unreleased source instructions from available binary behavior.
    - Setup examples cover actual runtimes, all four auth modes, preview/apply, skills, and the unsupported-client manual route without exposing credentials.
    - CLI and plugin readers reach canonical acquisition and setup instructions while retaining their distinct existing capabilities.
  artifacts:
    - path: docs-site/src/content/docs/guides/install.md
      provides: Canonical binary acquisition guide with release availability notice
    - path: docs-site/src/content/docs/guides/agent-setup.md
      provides: Canonical MCP setup guide based on current CLI help and runtime plans
    - path: docs-site/src/content/docs/guides/quickstart.md
      provides: Docker walkthrough and existing-server navigation
  key_links:
    - from: docs-site/src/content/docs/guides/quickstart.md
      to: docs-site/src/content/docs/guides/install.md
      via: Root-relative /guides/install/ link
    - from: docs-site/src/content/docs/guides/install.md
      to: docs-site/src/content/docs/guides/agent-setup.md
      via: Root-relative /guides/agent-setup/ next step
    - from: docs-site/src/content/docs/guides/plugin.md
      to: skill/engram/commands/engram-setup.md
      via: Repository link to generated command reference instead of copied argv
---

<objective>
Deliver complete, buildable onboarding documentation per D-01–D-07 and D-09, with honest availability wording while final setup remains unreleased. Publication/install acceptance belongs to 06-02; local docs checks do not close that evidence gap.

Purpose: Users can find the binary and understand the final setup contract from a single canonical route.
Output: Two new guides and three reconciled entry points.
</objective>

<execution_context>
@/Users/sean/.codex/gsd-core/workflows/execute-plan.md
@/Users/sean/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-install-documentation/06-CONTEXT.md
@.planning/phases/06-install-documentation/06-RESEARCH.md
@.planning/phases/06-install-documentation/06-PATTERNS.md
@.planning/phases/06-install-documentation/06-VALIDATION.md
@.planning/phases/05-slash-command-delegation/05-CONTEXT.md
</context>

<assumption_delta_decision>
Primary noun: canonical onboarding guides. Decision: no-change to runtime identity or behavior; Install and Agent Setup become the primary documentation routes per D-01–D-04, with Docker, standalone plugin and Connect as their existing variants. No model or schema change and no new API integration.
</assumption_delta_decision>

<tasks>
<task type="tracer">
  <name>Task 1: Publish a local rendered acquisition-to-preview reading path</name>
  <files>docs-site/src/content/docs/guides/install.md, docs-site/src/content/docs/guides/agent-setup.md, docs-site/src/content/docs/guides/quickstart.md</files>
  <read_first>Read the existing Quickstart; docs-site/package.json, docs-site/astro.config.mjs and .github/workflows/docs-site.yaml for build/navigation; .goreleaser.yaml for archive names; cmd/engram/setup.go help and skill/engram/commands/engram-setup.md for the source-authored examples. Use 06-PATTERNS.md analogs and 06-RESEARCH.md release evidence.</read_first>
  <action>Create frontmatter-first Install and Agent Setup pages and wire the existing-server branch from Quickstart through them, per D-01–D-03. Keep the Docker provisioning walkthrough, configuration link and first-memory step. Autogenerated Guides navigation needs no config change. Install leads with the exact invocation brew install seanb4t/tap/engram, then the GitHub release archive alternative: map macOS/Linux and amd64/arm64 to darwin/linux archive names, select one real published release, verify its checksums, extract it and put the executable in a user-chosen PATH directory; include engram version --output json and scoped unsigned-macOS quarantine guidance from the release hooks. Never suggest global security-policy changes. Explain server provisioning is separate from obtaining a client binary.

As of the research snapshot, the published binary is 0.15.1 and has no setup command. Refresh release metadata read-only if needed and prominently label setup instructions as unreleased until 06-02 obtains qualifying evidence, per D-09. Do not predict a tag or direct a released-binary user into an unavailable command. Provide an accurate local-source build option for readers intentionally using unreleased code, using a user-chosen temporary output path and a recorded checkout revision; this is an availability route, not a release-install observation.

Agent Setup first explains a full MCP endpoint supplied verbatim through --url or ENGRAM_URL, runtime detection via binaries on PATH, and one source-derived OAuth example: preview, inspect every row, append --apply to the same chosen invocation, then complete runtime OAuth login. Document explicit --runtime selection and that generic is opt-in. Explain preview performs read probes and may contact the endpoint, so it is neither offline-only nor connection proof. These examples are reader instructions; verification only runs help and renders pages, per D-05 and D-08. No real agent registration is a docs test. Keep credentials out of command text, logs and examples.

Use the repository-selected pnpm 11.20.0 and frozen existing lockfile if dependencies require preparation; add no dependency and make no lockfile or source change. Build the docs and inspect the rendered Quickstart → Install → Agent Setup path, proving the Markdown, navigation and generated routes together.</action>
  <verify>
    <automated>rumdl check docs-site/src/content/docs/guides/install.md docs-site/src/content/docs/guides/agent-setup.md docs-site/src/content/docs/guides/quickstart.md &amp;&amp; pnpm --dir docs-site build</automated>
    <human-check>Inspect docs-site/dist/guides/{quickstart,install,agent-setup}/index.html or their local preview routes; follow the new links and confirm existing-server readers bypass Docker steps and released-binary readers see the availability warning before setup instructions.</human-check>
  </verify>
  <acceptance_criteria>Both new routes render with frontmatter titles; Quickstart reaches acquisition and setup; Homebrew precedes archives; all four archive targets and checksum/PATH steps are accurate; current released setup availability is explicit.</acceptance_criteria>
  <done>The complete acquisition-to-OAuth-preview reading path builds and is navigable; setup availability is stated truthfully.</done>
</task>

<task type="auto">
  <name>Task 2: Complete setup coverage and reconcile CLI/plugin entry points</name>
  <files>docs-site/src/content/docs/guides/agent-setup.md, docs-site/src/content/docs/guides/cli.md, docs-site/src/content/docs/guides/plugin.md</files>
  <read_first>Read current CLI and plugin guides, cmd/engram/setup.go, internal/setup/plan.go, internal/setup/generic.go, and the generated setup command. Use 06-RESEARCH.md support/outcome tables and Phase 5 D-17–D-20 as the overriding credential contract.</read_first>
  <action>Complete Agent Setup per D-02,D-05,D-06: include oauth, oauth-client, bearer and none alternatives, runtime/auth matrix (Claude Code and Codex support all four; opencode and generic reject oauth-client), runtime binary detection, explicit target selection, noninteractive/JSON use, registration and skill facets, and visible unsupported results. OAuth-client requires non-secret --client-id and rejects it for other modes; scripted Claude receives MCP_CLIENT_SECRET through its inherited environment without interactive stdin. Native bearer uses a literal ENGRAM_TOKEN reference; --token-file is generic-only and is reported ignored for native paths. Do not read credentials or expand them into argv.

Explain not-present, would-write, already-correct, wrote and failed, and derive exit semantics from current help rather than assuming zero exit proves registration. Generic emits portable config and skill guidance, has no destination or registration writes even with --apply, and remains would-write. Its JSON config is a string; its credential references/provenance require adaptation to the destination client's documented format. Route Cursor and other unsupported clients here without promising native integration. Explain reinstallation converges without duplicate entries but may perform writes again; after interruption/partial failure, inspect each runtime and preview again, with no claimed cross-runtime transaction or concurrent-run guarantee. Preserve per-runtime results and subsequent OAuth login as separate from registration. Describe native skill installation and AGENTS.md fallback according to current skill plans, without suggesting plugin hooks are installed by the binary.

Per D-04,D-07, add canonical links from CLI and plugin, replace the obsolete plugin-only registration assertion and duplicate native argv table with binary delegation versus standalone Claude-only fallback prose, and link the generated command source for exact native commands. Keep standalone plugin installation, hooks, and no-binary fallback intact. Carry the same unreleased-setup notice to any plugin acquisition/delegation claim affected by D-09; do not change slash-command runtime behavior. Preserve the headless Connect guide's --server/ENGRAM_SERVER_URL and credential precedence, explicitly separating them from MCP setup inputs. Do not copy a second argv authority.

Lint edited pages, run current setup help and the existing read-only generated-surface check, build once at the wave boundary, and inspect rendered new routes and incoming links from all three entry pages. Record command results and manual content review in the summary. Full task is needed only if code changes; code is outside this plan.</action>
  <verify>
    <automated>rumdl check docs-site/src/content/docs/guides/agent-setup.md docs-site/src/content/docs/guides/cli.md docs-site/src/content/docs/guides/plugin.md &amp;&amp; go run ./cmd/engram setup --help &amp;&amp; task lint:setup &amp;&amp; pnpm --dir docs-site build</automated>
    <human-check>Inspect the five rendered guide routes and follow all changed internal links and the generated-reference link. Compare command alternatives, support matrix and credential prose to current help and Plans without executing live registration.</human-check>
  </verify>
  <acceptance_criteria>Every supported auth/runtime combination and unsupported row is explained; client-ID/env/token-file contracts match Phase 5; generic, outcomes and repeat-run semantics are accurate; CLI/plugin use canonical links and preserve existing Connect, standalone-plugin and hook behavior.</acceptance_criteria>
  <done>All five guides build and agree with current help/generated contracts; only released availability and installation observations remain for 06-02.</done>
</task>
</tasks>

<threat_model>
ASVS level: 1. Blocking threshold: high. No schema, auth implementation or new dependency change.

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Guide → reader shell | Examples can expose secrets or mutate runtime registrations |
| Release artifact → local executable | Reader obtains an unsigned executable from a named release |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-06-01 | Information disclosure | Agent Setup auth examples | high | mitigate | Task 2 preserves environment references, non-secret client IDs and generic-only token-file semantics; no real credential inspection |
| T-06-02 | Tampering | Reader registration instructions | high | mitigate | Task 1 documents preview/review/explicit apply; verification only invokes help, never registration |
| T-06-03 | Spoofing | Install guide | medium | mitigate | Task 1 uses canonical release/tap URLs, checksum verification and installed version checks; quarantine handling is scoped to the downloaded executable |
| T-06-04 | Repudiation | Availability claims | medium | mitigate | Task 1 carries dated unreleased wording; 06-02 requires release-backed evidence before claiming shipped setup |
</threat_model>

<verification>
Targeted Markdown lint, existing docs build, setup help and task lint:setup pass; rendered routes and links are reviewed. No generated argv duplication, live agent registration, new CI gate or dependency is introduced. These results establish local documentation quality, not release acceptance.
</verification>

<success_criteria>
Two canonical guides and three entry points accurately cover D-01–D-07,D-09; D-08 verification boundaries hold. Docs are ready for release-backed acceptance in 06-02, with unavailable setup explicitly labelled.
</success_criteria>

<output>
Create .planning/phases/06-install-documentation/06-01-SUMMARY.md with checks, rendered-route review, and the exact remaining release/install conditions.
</output>
