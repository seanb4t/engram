---
phase: 01-gate-ci-integrity
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - internal/keylinks/sweep_test.go
  - .planning/phases/01-gate-ci-integrity/01-KEYLINK-REASSESSMENT.md
autonomous: false
requirements:
  - REQ-keylink-past-gates-reassessed

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every v0.13.x Phase 1 and Phase 2 key-link carries exactly one recorded verdict — no link is absent from the table and no link carries two verdicts."
    - "A verdict of `pinned` means the corrected pattern resolves against its `from` file, or against its `to` file by the same fallback the consuming tool uses, at HEAD (D-11)."
    - "A link whose `from` and `to` files could not be read carries its own verdict class, distinct from both `pinned` and `unpinned`."
    - "Every `unpinned` verdict names a reason — symbol renamed, routed through a wrapper, file moved, or symbol never present."
    - "No gate found unpinned is repaired in this phase (D-14); the record is the deliverable."
    - "The one-time sweep and the recurring guard share one matcher — there is no second implementation of pattern resolution (D-12)."
  artifacts:
    - path: "internal/keylinks/sweep_test.go"
      provides: "the one-time v0.13.x Phase 1-2 reassessment, reusing the recurring guard's matcher"
      contains: "func TestReassessV013Phase12"
    - path: ".planning/phases/01-gate-ci-integrity/01-KEYLINK-REASSESSMENT.md"
      provides: "the verdict table: every v0.13.x Phase 1-2 key-link with its verdict and reason"
  key_links:
    - from: "internal/keylinks/sweep_test.go"
      to: "internal/keylinks/keylinks.go"
      via: "the sweep resolves each corrected pattern through the guard's own CheckSatisfiable rather than a second matcher"
      pattern: "CheckSatisfiable[(]"
    - from: ".planning/phases/01-gate-ci-integrity/01-KEYLINK-REASSESSMENT.md"
      to: "internal/keylinks/sweep_test.go"
      via: "the verdict table names the test that produced it so the sweep is reproducible from the record"
      pattern: "sweep_test[.]go"
  prohibitions:
    - "MUST NOT record a verdict of `pinned` for any link whose `from` and `to` files could not be read or did not exist — unreadable is its own recorded verdict, never absorbed into `pinned`."
    - "MUST NOT silently omit a link from the verdict table; a link the sweep could not classify is recorded as unclassified with the reason, never dropped."
---

<objective>
Re-resolve every v0.13.x Phase 1 and Phase 2 key-link against HEAD, one time, and record what each
one actually pins.

Purpose: both of those phases passed verification while their key-link gates were checking nothing.
`REQ-keylink-past-gates-reassessed` says a past "key-links passed" is not evidence — so each of those
links gets re-resolved now and lands as either genuinely pinned or explicitly recorded as unpinned
with a reason. This is a record, not a repair (D-14).
Output: `internal/keylinks/sweep_test.go` and the verdict table at
`.planning/phases/01-gate-ci-integrity/01-KEYLINK-REASSESSMENT.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-gate-ci-integrity/01-CONTEXT.md
@.planning/phases/01-gate-ci-integrity/01-02-SUMMARY.md
@internal/keylinks/keylinks.go
</context>

## Artifacts this phase produces

New symbols and paths created by this plan (none exist at HEAD):

| Kind | Symbol / path |
|------|---------------|
| file | `internal/keylinks/sweep_test.go` |
| file | `.planning/phases/01-gate-ci-integrity/01-KEYLINK-REASSESSMENT.md` |
| test | `TestReassessV013Phase12` |
| test | `TestReassessmentTableIsComplete` |

## Flagged planner assumptions (spec-less probe, unresolved)

This phase has no SPEC.md, so the deterministic edge probe ran against the requirement text. Two rows
came back `unclassified` and are therefore carried forward as explicit assumptions rather than
auto-resolved with a backstop:

| Requirement | Probe row | Assumption the plan makes |
|-------------|-----------|----------------------------|
| `REQ-keylink-pattern-matchable` | unclassified — review manually | The requirement's edge surface is taken to be the pattern-grammar boundary (which constructs are accepted or rejected), which plan 01-01 covers by fixture. If the intended edge surface was something else, it is unaddressed. |
| `REQ-keylink-past-gates-reassessed` | unclassified — review manually | The requirement's edge surface is taken to be verdict completeness and the unreadable-file case, which this plan covers by the completeness assertion below. If the intended edge surface was something else, it is unaddressed. |

Neither was dismissed and neither was silently dropped. Surface both at verification time.

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "resolve one v0.13.x key-link at HEAD and emit its verdict" — then all of them</name>
  <reversibility rating="reversible">A test file and a markdown record; both deletable with no downstream consumer.</reversibility>
  <precondition>Plan 01-02's rewrite has landed — no key-link pattern under `.planning/milestones/v0.13.x-phases/` carries a backslash character, so what the sweep resolves is the corrected pattern rather than the corrupted one.</precondition>
  <read_first>
    - internal/keylinks/keylinks.go — `ParsePlanKeyLinks`, `ValidatePattern`, `CheckSatisfiable`, `ScanPlans`; the sweep composes these, it does not reimplement them
    - internal/keylinks/gate_test.go — the `repoRoot` derivation helper to reuse rather than duplicate
    - .planning/milestones/v0.13.x-phases/01-interface-enforceability/ and .planning/milestones/v0.13.x-phases/02-interface-discoverability/ — the 20 plan files whose 30 key-links are the sweep's input set
    - .planning/phases/01-gate-ci-integrity/01-CONTEXT.md — D-11 (what "genuinely pinned" means and what was explicitly rejected as too strong), D-12 (one-time, distinct from the recurring guard), D-14 (record only, never repair)
  </read_first>
  <files>internal/keylinks/sweep_test.go</files>
  <action>
Create `internal/keylinks/sweep_test.go` with `TestReassessV013Phase12` — a one-time reassessment that
reuses the recurring guard's matcher rather than growing a second one (D-12).

Input set: every key-link in every `-PLAN.md` under
`.planning/milestones/v0.13.x-phases/01-interface-enforceability/` and
`.planning/milestones/v0.13.x-phases/02-interface-discoverability/`. A planning-time count found 30
key-link entries across those two directories, 26 of which carried an escape before plan 01-02's
rewrite. Report the count the sweep actually finds; if it diverges from 30, say so in the SUMMARY
rather than reconciling it silently.

Per link, produce exactly one verdict from this closed set:
- `pinned` — `ValidatePattern` returns a compiled regexp and `CheckSatisfiable` returns nil because
  the pattern matched the `from` file. Record which file matched.
- `pinned-via-target` — matched the `to` file rather than the `from` file, through the same fallback
  the consuming tool applies. This is still pinned by D-11's definition, but recording it separately
  makes visible how many links are pinned only by the fallback leg.
- `unpinned` — compiled and escape-free, but matched neither file. Record the reason by inspection:
  symbol renamed, routed through a wrapper, file moved, or the symbol was never present in the
  `from` file at all. This last case is #479's second finding and is expected to appear.
- `unreadable` — the `from` file does not exist at HEAD and the `to` file does not either. This is its
  own verdict, never folded into `pinned` and never folded into `unpinned`: "we could not look" and
  "we looked and it was not there" are different findings, and collapsing them is how a sweep
  manufactures false confidence.
- `invalid` — `ValidatePattern` still returns an offender. After plan 01-02 this should be empty; if
  it is not, that is a defect in the rewrite and the sweep must surface it rather than skip the link.

Emit the verdicts with `t.Logf` in a deterministic, table-shaped line per link carrying the plan file
and line, the `from` path, the `to` path, the pattern, the verdict, and the reason. Sort by plan file
then line so a re-run diffs cleanly. This test is a reporting test, so its assertions are about
completeness rather than about the verdicts themselves — a link being unpinned is a finding to
record, not a build failure (D-14).

Add `TestReassessmentTableIsComplete` asserting that the number of verdicts equals the number of
key-links parsed from the input set, and that every verdict is a member of the closed set above. A
link that produced no verdict fails this test. This is the assertion that makes "no link is silently
omitted" a mechanical guarantee rather than a promise.

Prove the completeness assertion goes red: temporarily make the sweep skip one link (an early
`continue`) and confirm `TestReassessmentTableIsComplete` fails naming the count mismatch; revert.
Record the red output verbatim in the SUMMARY.

Add the SPDX header to `sweep_test.go` (`task license:add`).
  </action>
  <acceptance_criteria>
    - `go test ./internal/keylinks/... -run 'TestReassessV013Phase12|TestReassessmentTableIsComplete' -v` prints `--- PASS` for both tests; a bare package-level `ok` with no RUN/PASS pair does not satisfy this.
    - The `-v` output contains one verdict line per key-link in the input set, and the SUMMARY records the observed link count alongside the 30 found at planning time.
    - Every emitted verdict is one of `pinned`, `pinned-via-target`, `unpinned`, `unreadable`, `invalid` — no sixth value and no empty value appears in the output.
    - Every `unpinned` line carries a non-empty reason; a line whose reason column is empty fails the completeness test.
    - The `invalid` bucket is empty (plan 01-02 corrected every pattern); if it is not, the SUMMARY names each entry and treats it as a defect in 01-02 rather than an accepted state.
    - The completeness assertion's red direction is observed and its verbatim output recorded in the SUMMARY.
    - `gofmt -l internal/keylinks` prints nothing; `go vet ./internal/keylinks/...` is clean; `task license:check` passes.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/keylinks/... -run 'TestReassess' -v &amp;&amp; gofmt -l internal/keylinks &amp;&amp; go vet ./internal/keylinks/...</automated>
  </verify>
  <done>Every v0.13.x Phase 1-2 key-link has exactly one verdict from a closed set, completeness is enforced by a red-proven test, and unreadable is recorded as its own class rather than absorbed into pinned.</done>
</task>

<task type="auto">
  <name>Task 2: Write the verdict table as a phase-owned artifact</name>
  <read_first>
    - internal/keylinks/sweep_test.go — the verdict output this task transcribes
    - .planning/phases/01-gate-ci-integrity/01-CONTEXT.md — D-13 (verdicts land in this phase's own record, explicitly not `.planning/WINDOWS.md` and explicitly not one GitHub issue per unpinned gate) and D-14 (record only)
    - .planning/phases/01-gate-ci-integrity/01-VALIDATION.md — an example of this phase's own artifact frontmatter shape; the reassessment record follows the same convention of `---` frontmatter on line 1 with no SPDX header
  </read_first>
  <files>.planning/phases/01-gate-ci-integrity/01-KEYLINK-REASSESSMENT.md</files>
  <action>
Transcribe the sweep's output into
`.planning/phases/01-gate-ci-integrity/01-KEYLINK-REASSESSMENT.md` — a table with one row per
v0.13.x Phase 1-2 key-link carrying: plan file and line, `from`, `to`, the corrected pattern, the
verdict, and the reason. Above the table, state the resolution rule in one sentence (D-11: the
corrected pattern resolves against its `from` file, or its `to` file by the tool's own fallback, at
HEAD) and the commit SHA the sweep ran against, so the record is reproducible. Below the table, a
short rollup: counts per verdict class. Name `sweep_test.go` in the record as the test that produced
it and give the exact `go test` invocation to re-run it.

State explicitly in the record that a gate found unpinned is recorded and NOT repaired in this phase
(D-14), and that repinning is deliberately deferred — if the sweep surfaces a gate whose loss
actually matters, that is its own scoped work.

**Deliberate placement note, record it in the document and in the SUMMARY.** D-13 named this phase's
`VERIFICATION.md` as the home for the table. `VERIFICATION.md` is written and re-parsed by the GSD
tooling, and repo rule `8dfdhfs5nn` forbids authoring structure into a tool-owned generated file — a
hand-added section there is exactly the class of edit that has already produced silent
misclassification in this repo's planning artifacts. The table therefore lands in a phase-owned
document that no tool generates, and this phase's `VERIFICATION.md` cites it by path. D-13's
substance is honored (the verdicts live in this phase's own record, not in `WINDOWS.md` and not as a
burst of GitHub issues); only the file changes, and the reason is on the record.

Do NOT add an SPDX header to this file — `.planning/**` requires `---` frontmatter on line 1 and
`.licenserc.yaml` already excludes the tree (rule `2rjnv8sc9a`). Do NOT add any new heading to
`.planning/ROADMAP.md` or `.planning/STATE.md` as part of this task (rule `8dfdhfs5nn`).

Commit with an explicit pathspec (rule `n6m4as49mr`).
  </action>
  <acceptance_criteria>
    - `.planning/phases/01-gate-ci-integrity/01-KEYLINK-REASSESSMENT.md` exists, begins with `---` on line 1, and carries no SPDX header; `task license:check` passes.
    - The table's row count equals the verdict count `TestReassessmentTableIsComplete` asserts, verified by comparing the two numbers in the SUMMARY.
    - Every table row's verdict is a member of the closed set, and every `unpinned` row has a non-empty reason cell.
    - The document names `sweep_test.go`, gives a runnable `go test` invocation, and records the commit SHA the sweep resolved against.
    - The document states that unpinned gates are recorded and not repaired in this phase, and states the D-13 placement rationale.
    - `git diff --stat` for this task shows no change to `.planning/ROADMAP.md` or `.planning/STATE.md`.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/01-gate-ci-integrity/01-KEYLINK-REASSESSMENT.md &amp;&amp; head -1 .planning/phases/01-gate-ci-integrity/01-KEYLINK-REASSESSMENT.md | rg -q '^---$' &amp;&amp; task license:check</automated>
  </verify>
  <done>The verdict table exists as a phase-owned, reproducible record with a stated resolution rule, a commit SHA, per-class counts, and an explicit statement that nothing was repaired.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Decide how the upstream unescaping gap is reported (D-01)</name>
  <decision>How the upstream key-link unescaping gap is reported (D-01)</decision>
  <context>
The root cause lives in the GSD tool, not in this repo: `parseMustHavesBlock` strips a single pair of
leading and trailing quote characters and never backslash-unescapes, so the raw scalar reaches
`new RegExp` intact. D-01 fixes this repo-locally and does NOT patch the tool, but it does require
the gap to be reported — and it states explicitly that whether that report is *filed upstream* or
*tracked in the engram spine* is Sean's call, not the executor's. Memory `cvvrwjbsnz` records a live
precedent where a prior GSD-core bug was, by explicit decision, spine-tracked rather than filed.

Both artifacts for this phase are already written by the time this checkpoint fires; nothing here
blocks the guard. The only open question is where the upstream gap is recorded.
  </context>
  <options>
    <option id="file-upstream">
      <name>File a GitHub issue against the GSD tool's repository</name>
      <pros>The gap reaches the people who can fix it at the source; other consumers of the tool benefit; the fix, if it lands, eventually makes this repo's guard redundant rather than permanent.</pros>
      <cons>Requires deciding what to disclose about this repo's plan contents; creates an external tracking item this project does not control the lifecycle of.</cons>
    </option>
    <option id="spine-track">
      <name>Record it as a durable engram memory in this repo's spine</name>
      <pros>Follows the live precedent in `cvvrwjbsnz`; keeps the record where this project's agents will actually recall it; zero external coordination.</pros>
      <cons>The upstream defect stays unreported, so every other consumer of the tool keeps the same silent no-op; this repo's guard becomes permanent rather than transitional.</cons>
    </option>
    <option id="both">
      <name>File upstream AND record the spine memory</name>
      <pros>Upstream gets the report and this project's agents get the durable recall; the spine record can cite the issue number.</pros>
      <cons>Two places to keep current if the upstream fix lands.</cons>
    </option>
  </options>
  <resume-signal>Select: file-upstream, spine-track, or both</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shipped v0.13.x plan documents → the sweep's verdicts | Historical claims are being re-adjudicated; the verdict becomes the record of what those gates were actually worth. |
| the sweep's output → `01-KEYLINK-REASSESSMENT.md` | A hand transcription step sits between the machine's verdicts and the durable record. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-07 | Repudiation | a link silently omitted from the sweep, leaving a gate un-reassessed while the record reads complete | high | mitigate | `TestReassessmentTableIsComplete` asserts verdict count equals parsed-link count and rejects any verdict outside the closed set; its red direction is proven by a deliberate skip before shipping. |
| T-01-08 | Repudiation | `unreadable` collapsed into `pinned`, manufacturing confidence in a gate nobody could resolve | high | mitigate | `unreadable` is a distinct verdict class in the closed set; the prohibition block forbids the collapse; an acceptance criterion requires every emitted verdict to be a set member. |
| T-01-09 | Tampering | hand transcription drifting from the machine verdicts | medium | mitigate | The record carries the producing test name, a runnable invocation, and the commit SHA, so any reader can re-derive it; an acceptance criterion compares the table's row count against the test's asserted count. |
| T-01-10 | Information Disclosure | filing an upstream issue that quotes this repo's plan contents | low | mitigate | The disposition is a blocking human decision (the D-01 checkpoint), not an autonomous action; disclosure scope is decided by the person, not the executor. |
| T-01-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs zero packages. |
</threat_model>

<verification>
- `go test ./internal/keylinks/... -v` passes with `--- PASS` lines for both sweep tests.
- The verdict table's row count equals the count `TestReassessmentTableIsComplete` asserts.
- `task license:check` passes; `.planning/**` carries no SPDX header (rule `2rjnv8sc9a`).
- `git diff` shows no change to `.planning/ROADMAP.md` or `.planning/STATE.md` (rule `8dfdhfs5nn`).
- `gofmt -l .` prints nothing (memory `4g5gbrmv29` — `task`'s default target does not run it).
</verification>

<success_criteria>
- Every v0.13.x Phase 1-2 key-link is reassessed, with exactly one verdict each, and completeness is
  mechanically enforced.
- `unreadable` never masquerades as `pinned`.
- Nothing found unpinned is repaired here (D-14) — the record is the deliverable.
- The upstream gap's disposition is a recorded human decision, not an executor default.
</success_criteria>

<output>
Create `.planning/phases/01-gate-ci-integrity/01-03-SUMMARY.md` when done. Record the observed link
count, the per-class verdict rollup, the completeness test's verbatim red output, and the checkpoint
decision as selected.
</output>
