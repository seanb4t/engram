---
phase: 01-gate-ci-integrity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/keylinks/keylinks.go
  - internal/keylinks/keylinks_test.go
  - internal/keylinks/testdata/good_key_links.md
  - internal/keylinks/testdata/bad_key_links.md
autonomous: true
requirements:
  - REQ-keylink-pattern-matchable

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "ValidatePattern rejects any pattern whose raw string contains a backslash character, independently of whether that pattern compiles (D-02)."
    - "ValidatePattern rejects a pattern that fails regexp.Compile, and rejects a pattern carrying a named capture group in either the Go or the JavaScript named-group syntax (D-08)."
    - "CheckSatisfiable reports an offender only when the from file exists on disk and neither the from nor the to file content matches the compiled pattern; a from file that does not exist yet is never reported as an offender."
    - "The committed known-good fixture yields zero offenders and the committed known-corrupted fixture yields at least one offender naming file, line, failure shape, and the corrected character-class form — both asserted in the same test run (D-06)."
    - "ScanPlans is deterministic: two runs over the same inputs return byte-identical offender output (D-07)."
    - "The parser reads a pattern key only inside a key_links block of a plan's must_haves frontmatter — a line elsewhere in the document that merely contains the word pattern followed by a colon is never treated as a key-link pattern."
  artifacts:
    - path: "internal/keylinks/keylinks.go"
      provides: "escaping check, RE2-intersect-JavaScript subset check, satisfiability check, deterministic offender formatting, and the shared ScanPlans entry point"
      contains: "func ScanPlans"
    - path: "internal/keylinks/keylinks_test.go"
      provides: "committed-fixture fail-first gate plus determinism proof"
      contains: "func TestFixturePairEscaping"
    - path: "internal/keylinks/testdata/good_key_links.md"
      provides: "known-good fixture — escape-free character-class patterns that resolve"
    - path: "internal/keylinks/testdata/bad_key_links.md"
      provides: "known-corrupted fixture — one escaped pattern, one named-group pattern, one unsatisfiable pattern"
  key_links:
    - from: "internal/keylinks/keylinks_test.go"
      to: "internal/keylinks/keylinks.go"
      via: "the fixture-pair test calls ValidatePattern on every pattern parsed out of both committed fixtures"
      pattern: "ValidatePattern[(]"
    - from: "internal/keylinks/keylinks_test.go"
      to: "internal/keylinks/keylinks.go"
      via: "ScanPlans is the single entry point both the recurring gate tests and the one-time reassessment sweep call"
      pattern: "ScanPlans[(]"
    - from: "internal/keylinks/keylinks_test.go"
      to: "internal/keylinks/testdata/bad_key_links.md"
      via: "the fail-first half of the fixture pair reads the committed known-corrupted fixture from testdata"
      pattern: "bad_key_links[.]md"
  prohibitions:
    - "MUST NOT acquire a per-file exclusion, skip, or allowlist that removes a plan file from the escaping gate's scope — an exclusion list is how a gate quietly stops gating, and it is the exact mechanism that let 39 unmatchable patterns accumulate."
    - "MUST NOT treat successful regexp.Compile as evidence a pattern is well-formed — the escaping shape compiles cleanly under RE2 with the wrong semantics, which is the same conflation that produced the defect being fixed."
---

<objective>
Build `internal/keylinks`: a stdlib-only leaf package that decides whether a plan's key-link
`pattern:` field is a real gate or a silent no-op, plus the committed fixture pair that proves the
decision goes both green and red.

Purpose: every downstream piece of this phase — the repo-wide normalization (plan 01-02) and the
one-time v0.13.x reassessment (plan 01-03) — consumes this package's matcher. Building it first, and
proving it discriminates against a committed known-bad input before anything depends on it, is what
keeps the rest of the phase from inheriting the very failure mode it exists to close.
Output: `internal/keylinks/keylinks.go`, its test, and two committed testdata fixtures.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-gate-ci-integrity/01-CONTEXT.md
@.planning/phases/01-gate-ci-integrity/01-RESEARCH.md
@.planning/phases/01-gate-ci-integrity/01-PATTERNS.md
@internal/surfaces/conformance_test.go
@internal/openaiurl/openaiurl.go
</context>

## Artifacts this phase produces

New symbols and paths created by this plan (none of these exist at HEAD — do not treat them as
drifted references):

| Kind | Symbol / path |
|------|---------------|
| package | `internal/keylinks` |
| file | `internal/keylinks/keylinks.go` |
| file | `internal/keylinks/keylinks_test.go` |
| file | `internal/keylinks/testdata/good_key_links.md` |
| file | `internal/keylinks/testdata/bad_key_links.md` |
| type | `keylinks.KeyLink` (from, to, via, pattern, file, line) |
| type | `keylinks.Offender` (file, line, shape, raw, fix) |
| type | `keylinks.Shape` (escaping, unsupported-syntax, named-group, compile-error, unsatisfiable) |
| type | `keylinks.Mode` (escaping-only, satisfiability) |
| func | `keylinks.ParsePlanKeyLinks(path string) ([]KeyLink, error)` |
| func | `keylinks.ValidatePattern(raw string) (*regexp.Regexp, *Offender)` |
| func | `keylinks.CheckSatisfiable(link KeyLink, re *regexp.Regexp, repoRoot string) *Offender` |
| func | `keylinks.ScanPlans(repoRoot string, roots []string, mode Mode) ([]Offender, error)` |
| func | `keylinks.OffenderLine(o Offender) string` |
| func | `keylinks.SuggestCharClassForm(raw string) string` |
| test | `TestFixturePairEscaping`, `TestFixturePairSubsetAndSatisfiability`, `TestScanPlansDeterministic` |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end "a corrupted pattern in a real plan file goes red" — one path only</name>
  <reversibility rating="reversible">The package is a new leaf with no importers; deleting it restores HEAD exactly.</reversibility>
  <read_first>
    - internal/surfaces/conformance_test.go — the collect-all-violations gate shape (`runGate` returning `[]string`, `violationLine` as one small deterministic formatter, `t.Error` per offender, never `t.Fatal` on first)
    - internal/surfaces/surfaces.go — the error-value convention: `(T, error)` with `fmt.Errorf("surfaces: ...: %w", err)`; package-prefixed messages; no panics in the leaf
    - internal/openaiurl/openaiurl.go — package doc-comment shape for a stdlib-only leaf
    - .planning/milestones/v0.13.x-phases/01-interface-enforceability/01-06-PLAN.md — a real `key_links:` block (lines 33-46) to model both the parser and the fixture content on
    - .planning/phases/01-gate-ci-integrity/01-PATTERNS.md — the analog assignments this package must follow
  </read_first>
  <behavior>
    - `ParsePlanKeyLinks` on a plan file returns one `KeyLink` per `- from:` entry inside the frontmatter `must_haves:` → `key_links:` block, each carrying `From`, `To`, `Via`, `Pattern`, `File`, and the 1-based `Line` of its own `pattern:` key.
    - `ParsePlanKeyLinks` on a document whose body prose contains a line mentioning a pattern key outside any `key_links:` block returns zero links from that line.
    - `ValidatePattern` on an escape-free character-class pattern returns a compiled regexp and a nil offender.
    - `ValidatePattern` on a pattern whose raw string contains a backslash character returns a nil regexp and an offender whose `Shape` is the escaping shape and whose `Fix` is the character-class rewrite.
    - `OffenderLine` renders one offender as a single deterministic line carrying file, 1-based line number, shape, the raw pattern, and the corrected form.
    - Running the gate over `testdata/good_key_links.md` yields zero offenders; running it over `testdata/bad_key_links.md` yields at least one.
  </behavior>
  <files>internal/keylinks/keylinks.go, internal/keylinks/keylinks_test.go, internal/keylinks/testdata/good_key_links.md, internal/keylinks/testdata/bad_key_links.md</files>
  <action>
Create the `internal/keylinks` package as a stdlib-only leaf (imports limited to `fmt`, `os`,
`path/filepath`, `regexp`, `sort`, `strings`; zero repo-internal imports), then wire ONE path
end-to-end: committed fixture file on disk → `ParsePlanKeyLinks` → `ValidatePattern` (escaping shape
only for now) → `OffenderLine` → a Go test that asserts the good fixture is silent and the bad
fixture is loud. Do not add the RE2-intersect-JavaScript subset checks or the satisfiability check
in this task — Task 2 owns those.

Add the Apache-2.0 SPDX header to both new `.go` files (`task license:add` applies it). Do NOT add
an SPDX header to either `testdata/*.md` fixture or to any `.planning/**` file.

Package doc comment: follow `internal/openaiurl`'s shape — name what the package decides, name its
two callers (this phase's recurring gate tests and the one-time v0.13.x reassessment sweep), and
state that it is a stdlib-only leaf so a test anywhere can import it with zero cycle risk. Reference
the defect being worked around: `parseMustHavesBlock` in the GSD tool hands a `pattern:` string to
`new RegExp` without unescaping it, so a pattern written with doubled escape characters reaches the
regex engine meaning "a literal backslash, then any character" — valid regex, silently unmatchable.
Per D-01 this package is a repo-local guard; it does not patch the upstream tool.

Types to declare: `KeyLink{From, To, Via, Pattern, File string; Line int}`; `Shape` as a string-typed
constant set with the members `ShapeEscaping`, `ShapeUnsupportedSyntax`, `ShapeNamedGroup`,
`ShapeCompileError`, `ShapeUnsatisfiable` (declare all five now; Task 2 populates the last four);
`Offender{File string; Line int; Shape Shape; Raw, Fix string}`; and `Mode` with members
`ModeEscapingOnly` and `ModeSatisfiability` (declared now, consumed in Task 3).

`ParsePlanKeyLinks(path string) ([]KeyLink, error)`: a line-based scanner over the file, mirroring
the flat, YAML-ish parsing the consuming tool itself does. It MUST be scoped: only enter link-parsing
state after seeing a `key_links:` key inside the frontmatter `must_haves:` block, and leave that
state at the next key at or above `must_haves:`'s indentation. A `pattern:` key encountered outside
that state is not a key-link and is ignored — this scoping is load-bearing, because this phase's own
plan documents quote pattern keys in prose and acceptance criteria, and a naive whole-file line match
would report them as offenders. Strip a single pair of leading/trailing quote characters from each
scalar value exactly as the consuming tool does, and never backslash-unescape. Record the 1-based
line number of the `pattern:` key itself so an offender points at the exact line to edit. Errors wrap
as `fmt.Errorf("keylinks: parse %s: %w", path, err)`.

`ValidatePattern(raw string) (*regexp.Regexp, *Offender)`: the escaping check runs FIRST and
unconditionally, before any compile attempt — if `raw` contains a backslash character (U+005C),
return a nil regexp and an offender with `Shape: ShapeEscaping` and `Fix: SuggestCharClassForm(raw)`.
Gating this on compile failure is the defect's own logic relocated: the escaping shape compiles
cleanly. Otherwise compile with `regexp.Compile` and return the compiled regexp with a nil offender
(Task 2 adds the remaining rejections after the compile).

`SuggestCharClassForm(raw string) string`: mechanically derive D-02's escape-free form. For each
backslash followed by a regex metacharacter, emit that metacharacter wrapped in a character class —
so an escaped dot becomes the two-character-class form, an escaped open paren and an escaped close
paren likewise. Collapse a doubled backslash to a single one before this rewrite, since the observed
corruption is a doubled escape. When a backslash precedes a double-quote character the correct fix is
not a character class at all but dropping the escape (the quote needs no escaping inside a regex);
handle that case explicitly — `.planning/milestones/v0.13.x-phases/01-interface-enforceability/01-03-PLAN.md`
line 51 carries exactly this shape and would otherwise get a nonsense suggestion. When no mechanical
rewrite is derivable, return an empty string and let `OffenderLine` render the fix column as
`no mechanical rewrite — rewrite by hand`.

`OffenderLine(o Offender) string`: one small deterministic formatter, modeled on
`internal/surfaces/conformance_test.go`'s `violationLine`. Render as
`file=<path>:<line> shape=<shape> pattern=<quoted raw> fix=<quoted fix>` using `%q` for the two
pattern columns so an embedded quote or escape character is unambiguous in the failure output.

Fixtures: write `testdata/good_key_links.md` and `testdata/bad_key_links.md` as minimal but
structurally real plan documents — YAML frontmatter with `must_haves:` → `key_links:` holding
`- from:`/`to:`/`via:`/`pattern:` entries, plus a short body. The good fixture's patterns use only the
character-class form. The bad fixture carries, in this task, one entry whose pattern uses the doubled
escape form. Both fixtures MUST also carry, in their body prose, a line that mentions a pattern key
outside any `key_links:` block — that line is what proves the parser's scoping, and the good
fixture's zero-offender assertion is what makes the proof real. Fixture `from`/`to` values point at
paths under `internal/keylinks/` so Task 2's satisfiability check has real files to resolve against.

Test `TestFixturePairEscaping` in `keylinks_test.go`: a `runFixture(t, name) []string` helper that
parses one fixture, validates every pattern, and returns the formatted offender lines — collecting
all, never returning on the first (D-07). Assert the good fixture returns an empty slice, and assert
the bad fixture returns a non-empty slice whose joined text names the bad fixture's path, the
escaping shape, and the corrected character-class form. Use plain `testing` with
`if got != want { t.Errorf(...) }`; no third-party assertion library (this repo has zero
`stretchr/testify` imports).
  </action>
  <acceptance_criteria>
    - `go test ./internal/keylinks/... -run TestFixturePairEscaping -v` prints a `--- PASS: TestFixturePairEscaping` line. A bare package-level `ok` with no RUN/PASS pair is a false green (STATE.md blocker `bsbsvn4hbc`) and does not satisfy this criterion.
    - Temporarily deleting the offending entry from `testdata/bad_key_links.md` makes `TestFixturePairEscaping` FAIL; restoring it makes it pass again. Both directions observed and recorded in the SUMMARY (memory `v5q7jdbw43` — an assertion is not shippable until run against known-good and known-bad input).
    - `go list -deps ./internal/keylinks | grep -c 'github.com/seanb4t/engram'` returns `1` (the package itself and nothing else from this module) — proving the stdlib-only leaf constraint in D-05.
    - `go vet ./internal/keylinks/...` is clean and `gofmt -l internal/keylinks` prints nothing.
    - `internal/keylinks/keylinks.go` and `internal/keylinks/keylinks_test.go` each begin with the Apache-2.0 SPDX header; `task license:check` passes.
    - `ParsePlanKeyLinks` returns zero links for the body-prose line in each fixture that mentions a pattern key outside a `key_links:` block — asserted by the good fixture's zero-offender result, which cannot hold otherwise.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/keylinks/... -run TestFixturePairEscaping -v &amp;&amp; gofmt -l internal/keylinks &amp;&amp; go vet ./internal/keylinks/...</automated>
  </verify>
  <done>A committed known-corrupted key-link fixture makes `go test ./internal/keylinks/...` fail with a `file:line`-addressed offender naming the corrected form, and the committed known-good fixture passes — both proven in one test run, and the red direction observed by hand.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reject the JavaScript-only regex grammar and the unsatisfiable pattern</name>
  <reversibility rating="costly">D-08's restriction to the RE2-intersect-JavaScript common subset is cheap to loosen later, but any pattern authored against the wider grammar in the meantime would need re-auditing against both engines.</reversibility>
  <read_first>
    - internal/keylinks/keylinks.go — current state after Task 1
    - internal/keylinks/testdata/bad_key_links.md and testdata/good_key_links.md — the fixtures to extend
    - .planning/phases/01-gate-ci-integrity/01-RESEARCH.md — "Architecture Patterns" Pattern 1 and Pattern 2 (which constructs RE2 rejects for free versus which need explicit checks, empirically probed against this repo's toolchain) and "Common Pitfalls" Pitfall 1
    - $HOME/.claude/gsd-core/bin/lib/verify.cjs lines 1085-1120 — READ-ONLY reference (D-01 forbids modifying it): the consuming tool tries the `from` file first and falls back to the `to` file, and a missing source file is reported as a missing-file condition, never as a pattern mismatch. The Go checker must mirror this exactly.
  </read_first>
  <behavior>
    - `ValidatePattern` on a lookahead, negative-lookahead, lookbehind, or backreference pattern returns an offender whose `Shape` is `ShapeCompileError` carrying the compiler's own message — these are rejected by RE2 at compile time and need no bespoke detector.
    - `ValidatePattern` on a pattern using the Go named-group syntax returns an offender whose `Shape` is `ShapeNamedGroup`.
    - `ValidatePattern` on a pattern using the JavaScript named-group syntax returns the same `ShapeNamedGroup` offender — this repo's toolchain compiles both syntaxes, so one `SubexpNames` sweep must catch both.
    - `CheckSatisfiable` returns nil when the compiled pattern matches the `from` file's content.
    - `CheckSatisfiable` returns nil when the `from` file does not match but the `to` file does.
    - `CheckSatisfiable` returns an offender with `Shape: ShapeUnsatisfiable` when the `from` file exists and neither it nor the `to` file matches.
    - `CheckSatisfiable` returns nil when the `from` file does not exist on disk — an unwritten file is a promise, not a broken gate.
  </behavior>
  <files>internal/keylinks/keylinks.go, internal/keylinks/keylinks_test.go, internal/keylinks/testdata/bad_key_links.md, internal/keylinks/testdata/good_key_links.md</files>
  <action>
Extend `ValidatePattern` with the two post-compile rejections D-08 requires, and add
`CheckSatisfiable` for D-03's second silent-no-op shape.

Post-compile rejections, in order after the existing escaping check:
1. `regexp.Compile` error → offender with `Shape: ShapeCompileError` and `Fix` set to the compiler's
   error text. Do NOT hand-write detectors for lookahead, negative lookahead, lookbehind, or
   backreferences: RE2 already refuses all four at parse time with a stable message, and a bespoke
   scanner would duplicate logic the standard library already enumerates.
2. Named capture groups → iterate the compiled regexp's `SubexpNames()` and reject on any non-empty
   entry, with `Shape: ShapeNamedGroup`. One `SubexpNames` sweep catches both the Go and the
   JavaScript named-group syntax without distinguishing which was written; a substring scan for the
   two syntax prefixes would need to special-case both and would false-positive inside a character
   class.

Set `ShapeUnsupportedSyntax` aside as the classification for a compile error whose message names an
unsupported Perl construct, so the offender output distinguishes "you wrote JavaScript-only regex"
from "your regex is malformed" — both are compile failures to RE2 but they need different fixes from
a reader.

`CheckSatisfiable(link KeyLink, re *regexp.Regexp, repoRoot string) *Offender`: mirror the consuming
tool's resolution order exactly. Read `filepath.Join(repoRoot, link.From)`. If that read fails with a
not-exist error, return nil — the `from` file is a promise from a plan that has not executed yet, and
the tool itself treats a missing source as a pending condition rather than a pattern mismatch;
reporting it here would make the recurring gate go red on every plan authored before it runs. If the
content matches, return nil. If it does not, read `filepath.Join(repoRoot, link.To)` and return nil
if that matches. Otherwise return an offender with `Shape: ShapeUnsatisfiable`, `Raw` set to the
pattern, and `Fix` set to a reader-actionable sentence naming both resolved paths — this is #479's
second finding, where a pattern was pinned on a file whose leaf had been routed through a different
function, so the symbol appeared in neither file.

Mirroring the tool's matching in Go is only valid evidence about the tool's JavaScript-side behavior
because D-08 already restricts patterns to constructs that mean the same thing in both engines. State
that dependency in a comment on `CheckSatisfiable` so a future contributor who loosens D-08 sees what
they are breaking.

Extend `testdata/bad_key_links.md` with three more entries: one using the Go named-group syntax, one
using a JavaScript-only construct that RE2 refuses to compile, and one whose pattern is escape-free,
compiles cleanly, and names a symbol that appears in neither its `from` nor its `to` file. Extend
`testdata/good_key_links.md` with one entry whose pattern matches only in the `to` file, proving the
fallback leg, and one whose `from` path deliberately does not exist, proving the not-yet-written
file is silent.

Add `TestFixturePairSubsetAndSatisfiability` as a table-driven `t.Run` subtest set over the fixture
entries, asserting the expected `Shape` per entry and asserting the good fixture still yields zero
offenders under both checks. Keep the collect-all discipline: append offenders, never return on the
first.
  </action>
  <acceptance_criteria>
    - `go test ./internal/keylinks/... -run TestFixturePairSubsetAndSatisfiability -v` prints a `--- PASS` line for the parent test and for each named subtest covering the escaping, named-group, compile-error, and unsatisfiable shapes.
    - The good fixture's entry whose `from` path does not exist produces zero offenders — asserted by name in a subtest, not merely implied by the aggregate count.
    - The good fixture's `to`-file-fallback entry produces zero offenders and the test names it, proving the fallback leg is exercised rather than accidentally short-circuited by the `from` leg.
    - Changing the unsatisfiable bad-fixture entry's pattern to a symbol that does exist in its `from` file makes the test FAIL (the entry is expected to be an offender); reverting restores green. Both directions observed and recorded in the SUMMARY.
    - `go list -deps ./internal/keylinks | grep -c 'github.com/seanb4t/engram'` still returns `1`.
    - `gofmt -l internal/keylinks` prints nothing and `go vet ./internal/keylinks/...` is clean.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/keylinks/... -run 'TestFixturePair' -v &amp;&amp; gofmt -l internal/keylinks &amp;&amp; go vet ./internal/keylinks/...</automated>
  </verify>
  <done>All five offender shapes — escaping, unsupported syntax, named group, compile error, unsatisfiable — are detected and classified, the two silent-pass cases (missing `from` file, `to`-file fallback) are proven silent, and every case is pinned by a committed fixture entry.</done>
</task>

<task type="auto">
  <name>Task 3: ScanPlans — the single entry point both the recurring gate and the one-time sweep call</name>
  <read_first>
    - internal/keylinks/keylinks.go — current state after Task 2
    - internal/keylinks/keylinks_test.go — the fixture helpers to reuse
    - internal/surfaces/conformance_test.go lines 232-259 — `TestSurfaceConformanceDeterministic`: run the check twice and compare with `reflect.DeepEqual`; adapt the structure, not the temp-file mechanics
    - .planning/phases/01-gate-ci-integrity/01-CONTEXT.md — D-04 (the escaping/satisfiability scope asymmetry) and D-12 (the sweep is one-time and distinct from the recurring guard)
  </read_first>
  <files>internal/keylinks/keylinks.go, internal/keylinks/keylinks_test.go</files>
  <action>
Add `ScanPlans(repoRoot string, roots []string, mode Mode) ([]Offender, error)` — the one function
plan 01-02's recurring gate tests and plan 01-03's one-time reassessment sweep both call, so the two
never drift into two matchers that disagree.

Behavior: walk each root under `repoRoot` with `filepath.WalkDir`, selecting files whose base name
ends in `-PLAN.md`. For every selected file, call `ParsePlanKeyLinks`, then for every link call
`ValidatePattern`. Under `ModeEscapingOnly`, stop at validation — collect every offender and move on.
Under `ModeSatisfiability`, additionally call `CheckSatisfiable` for links whose pattern validated
cleanly (a pattern that failed validation has no compiled regexp to resolve, and reporting it twice
would double-count one defect).

`ModeSatisfiability` MUST additionally skip any plan file that has not been executed yet, determined
by the absence of its sibling SUMMARY: for `<dir>/<phase>-<NN>-PLAN.md`, the plan is executed when
`<dir>/<phase>-<NN>-SUMMARY.md` exists. An un-executed plan's key-links describe files it is about to
create; resolving them against HEAD would make the gate report a defect for work that has not
happened. This is the same distinction D-04 draws in time — the escaping property is true or false
forever, the satisfiability property is only meaningful once the code it points at exists.

Determinism (D-07): sort the collected offenders by file path, then line, then shape, before
returning. `filepath.WalkDir` is lexical per directory but the sort makes the contract explicit
rather than incidental.

Errors: a file that cannot be parsed is a hard error wrapped as `keylinks: scan <path>: %w` — never
swallowed into a silent zero-offender result, which would be this phase's own failure mode
reintroduced. A root directory that does not exist is also an error, not an empty result.

Add `TestScanPlansDeterministic`: point `ScanPlans` at `internal/keylinks/testdata` under both modes,
run each twice, and assert `reflect.DeepEqual` between the two runs. Add a subtest asserting that a
nonexistent root returns a non-nil error rather than an empty slice, and a subtest asserting that
under `ModeSatisfiability` a fixture plan with no sibling SUMMARY contributes zero offenders while
the same fixture with a SUMMARY present (written to `t.TempDir()`) contributes its unsatisfiable
entry. That pair is the mechanism proof for the skip rule — without it the rule is a claim, not a
gate.
  </action>
  <acceptance_criteria>
    - `go test ./internal/keylinks/... -run TestScanPlansDeterministic -v` prints `--- PASS` for the parent and for each subtest covering determinism, the nonexistent-root error, and both halves of the SUMMARY-presence pair.
    - `ScanPlans` with a nonexistent root returns a non-nil error; a test asserts the error is non-nil rather than asserting only that the slice is empty.
    - The SUMMARY-presence subtest asserts both directions in one run: absent SUMMARY yields zero offenders, present SUMMARY yields the unsatisfiable offender. A test that only asserts the absent direction does not satisfy this criterion.
    - `go test ./internal/keylinks/... -v` shows PASS for every test in the package and no SKIP lines.
    - `gofmt -l internal/keylinks` prints nothing; `go vet ./internal/keylinks/...` is clean; `task license:check` passes.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/keylinks/... -v &amp;&amp; gofmt -l internal/keylinks &amp;&amp; go vet ./internal/keylinks/...</automated>
  </verify>
  <done>One exported `ScanPlans` serves both the recurring gate and the one-time sweep, with D-04's scope asymmetry and the un-executed-plan skip rule each pinned by a two-direction test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.planning/**` markdown → `internal/keylinks` regex compiler | Repo-authored plan content crosses into a regex engine at test time. Authored by maintainers, not by external input, but it is the only untrusted-shaped data this package handles. |
| `internal/keylinks` → filesystem reads under `repoRoot` | `from:`/`to:` values from plan frontmatter become file paths that get read. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Denial of Service | `ValidatePattern` compiling a `pattern:` field | low | accept | Go's `regexp` is RE2 — linear-time by construction, no catastrophic-backtracking case exists. D-08's restriction to the RE2-intersect-JavaScript subset means the guard never even sees a construct RE2 would have to emulate. Patterns are repo-authored, not attacker-supplied. |
| T-01-02 | Tampering | `from:`/`to:` values used as filesystem paths in `CheckSatisfiable` | low | mitigate | Join every `from`/`to` value onto `repoRoot` with `filepath.Join` (which cleans the path) and read-only. No write, no exec, no path echoed into a shell. Traversal outside the repo is a canon path-traversal concern covered by `/gsd-secure-phase` and not minted as a bespoke prohibition here. |
| T-01-03 | Repudiation | a parse failure swallowed into a zero-offender result | medium | mitigate | `ScanPlans` returns a hard wrapped error on any unparseable plan file; a silent empty result is explicitly forbidden by Task 3's action and asserted by the nonexistent-root subtest. |
| T-01-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs zero packages. `internal/keylinks` is stdlib-only and `go.mod` is untouched — the package-legitimacy gate has no applicable rows (RESEARCH.md "Package Legitimacy Audit: Not applicable"). |
</threat_model>

<verification>
- `go test ./internal/keylinks/... -v` — every test PASSes, no SKIP.
- `go list -deps ./internal/keylinks | grep -c 'github.com/seanb4t/engram'` returns `1` (stdlib-only leaf, D-05).
- `git diff --stat go.mod go.sum` is empty (zero new dependencies).
- `gofmt -l .` prints nothing — note that `task`'s default target does NOT run gofmt (`Taskfile.yaml:115` sits outside it, and CI runs it as a separate step at `ci.yaml:47`), so "`task` passes" is not evidence this holds (memory `4g5gbrmv29`).
- `task license:check` passes with no `.planning/**` or `testdata/*.md` file carrying an SPDX header.
</verification>

<success_criteria>
- All five offender shapes are classified and each is pinned by a committed fixture entry.
- The fixture pair proves both directions: known-good is silent, known-corrupted is loud (D-06).
- `ScanPlans` is the single matcher; plans 01-02 and 01-03 have no reason to write a second one.
- Zero new module dependencies; the package imports nothing from this repo.
</success_criteria>

<output>
Create `.planning/phases/01-gate-ci-integrity/01-01-SUMMARY.md` when done. Record both fail-first
observations (Task 1 and Task 2) verbatim — the red output, not a claim that red was seen.
</output>
