---
phase: 03-migration-foundation-registry-invariants-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/migrate/step.go
  - internal/migrate/registry.go
  - internal/migrate/additive.go
  - internal/store/migratebacklog.go
  - internal/store/migrate.go
  - internal/store/migrate_test.go
  - .planning/phases/03-migration-foundation-registry-invariants-sweep/red-evidence/03-01-red-1-range-only-filter.patch
  - .planning/phases/03-migration-foundation-registry-invariants-sweep/red-evidence/03-01-red-2-declared-drift-written.patch
autonomous: true
requirements:
  - REQ-migration-step-registry
  - REQ-migration-additive-only-gated
  - REQ-migration-step-reversibility
  - REQ-migrate-converges-without-lock

estimate:
  tokens: 96000
  raw_tokens: 96000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A migration step can be constructed only through migrate.NewStep(from, to Version, addsKeys []string, rev Reversibility, apply ApplyFunc) — every field of migrate.Step is unexported, so no struct-literal path exists outside the package and neither addsKeys nor rev can be omitted (D-02)."
    - "Reversibility is satisfiable only by the two unexported types this package constructs, because its single method is unexported and no exported type in the package carries it — sealing closes the embedding hatch (D-03)."
    - "NewStep panics on a nil rev and on a nil apply. Sealing does NOT close this hole: Go assigns nil to any interface-typed parameter regardless of sealing, so without this explicit check `nobody declared reversibility` stays representable and SC3 is false (RESEARCH Pitfall 1)."
    - "Store.Migrate derives its backlog with Should:[Range(schema_version, Lt: target), IsEmpty(schema_version)], so a record whose schema_version key is GENUINELY ABSENT is in the backlog. A bare Range condition evaluates false for an absent key, which would make the sweep converge on pass 1 having migrated nothing."
    - "Store.Migrate re-derives the backlog with a fresh Count and a fresh Scroll on EVERY pass, and passes Offset: nil on every scroll — there is no cursor persisted across passes, so a resume is nothing more than calling Migrate again (D-07)."
    - "The sweep never branches its control flow on a per-point SetPayload error: an error increments a counter, the pass continues, and the next pass's fresh re-derivation decides what is still outstanding (D-09)."
    - "The sweep terminates on a persistently failing backend: if a freshly re-derived backlog is not strictly smaller than the previous pass's, Migrate stops and returns an error wrapping the last write error, rather than looping forever."
    - "The sweep's only Qdrant write verb is SetPayload, issued one point at a time, so it is structurally incapable of removing or overwriting a payload key it did not add."
    - "Before writing anything for a record, the sweep runs migrate.CheckAdditive over the step's actual before/after key sets and refuses the whole call on a violation — the additive-only declaration is enforced at apply time, fail-closed, not only in a test."
    - "Every migrate.Version written into a Qdrant payload map passes through an explicit int(...) conversion — qdrant.NewValueMap PANICS on a named type over int (durable record tdt50852ww, already hit once in Phase 2)."
    - "Store.Migrate short-circuits to a zero-work, no-error return when the resolved target is <= 0: no record can be below v0, so the IsEmpty arm must not be allowed to sweep every legacy record for a no-op stamp."
    - "migrate.CurrentVersion is still 0 and internal/migrate.Registry is still empty at the end of this plan — every proof runs against test-only fixture steps (D-06), never against the production registry."
  artifacts:
    - path: "internal/migrate/step.go"
      provides: "ApplyFunc, the sealed Reversibility interface with its two constructors and introspection helpers, Step and the NewStep constructor"
      contains: "func NewStep(from, to Version, addsKeys []string, rev Reversibility, apply ApplyFunc) Step"
    - path: "internal/migrate/registry.go"
      provides: "The empty production Registry, the Validate ordering/idempotency invariant, and StepsFrom chain selection"
      contains: "func Validate(steps []Step) error"
    - path: "internal/migrate/additive.go"
      provides: "AddedKeys, RemovedKeys and the two-direction CheckAdditive key-set diff"
      contains: "func CheckAdditive(s Step, before, after map[string]any) error"
    - path: "internal/store/migratebacklog.go"
      provides: "backlogFilter's Should:[Range,IsEmpty] OR-shape, versionOf's absent-means-v0 read, and payloadToMap's exhaustive Qdrant Value conversion"
      contains: "func backlogFilter(target migrate.Version) *qdrant.Filter"
    - path: "internal/store/migrate.go"
      provides: "MigrateOptions, MigrateResult and the re-derive-every-pass Store.Migrate sweep"
      contains: "func (s *Store) Migrate(ctx context.Context, opts MigrateOptions) (res MigrateResult, err error)"
    - path: "internal/store/migrate_test.go"
      provides: "The end-to-end tracer, the absent-key backlog-derivation proof, and the fail-closed non-additive refusal proof"
      contains: "TestBacklogFilterMatchesAbsentAndBelowTarget"
  key_links:
    - from: "internal/store/migratebacklog.go"
      to: "internal/store/store.go"
      via: "backlogFilter copies activeWindowConditions' Range+IsEmpty OR pairing, the in-repo precedent for an absent key that must count as a match"
      pattern: 'qdrant[.]NewIsEmpty'
    - from: "internal/store/migrate.go"
      to: "internal/migrate/registry.go"
      via: "the sweep selects each record's step chain from its own stored version up to the target through StepsFrom"
      pattern: 'migrate[.]StepsFrom'
    - from: "internal/store/migrate.go"
      to: "internal/migrate/additive.go"
      via: "the sweep runs the two-direction key-set diff before writing, and refuses the call on a violation"
      pattern: 'migrate[.]CheckAdditive'
    - from: "internal/migrate/step.go"
      to: "internal/migrate/registry.go"
      via: "the unexported marker method that seals Reversibility to this package's two constructors"
      pattern: 'isReversibility'
---

<objective>
Tracer: wire one legacy, genuinely-key-absent record all the way from `migrate.NewStep` through
`migrate.Registry`/`Validate`/`StepsFrom`, through `Store.Migrate`'s re-derive-every-pass sweep and
`backlogFilter`, to a real pinned Qdrant and back — then prove the two lines that decide whether the
whole mechanism does anything at all.

Purpose: this phase's single highest-risk line is the backlog filter. `Range(schema_version, {Lt:
target})` returns **false** for a record with no `schema_version` key; Qdrant does not treat an absent
field as "less than everything". Every pre-migration record is exactly that case, so the naive filter
derives an EMPTY backlog and the sweep converges to zero having migrated nothing — a silent, total
no-op that every green test agrees with. Proving that line first, end-to-end, on this agent's best
early-context tokens is worth more than any amount of layer-by-layer construction.

Output: `internal/migrate` grown from a two-symbol leaf into a step registry with a sealed
reversibility type and a two-direction additive-only diff; `internal/store` gaining a sweep that
re-derives its backlog every pass and never trusts a write call's error signal; and three tests
against real Qdrant, one of which is proven RED against a range-only filter with a committed,
reviewer-reproducible patch.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-migration-foundation-registry-invariants-sweep/03-CONTEXT.md
@.planning/phases/03-migration-foundation-registry-invariants-sweep/03-RESEARCH.md
@.planning/phases/03-migration-foundation-registry-invariants-sweep/03-PATTERNS.md
@.planning/phases/03-migration-foundation-registry-invariants-sweep/03-VALIDATION.md
@CLAUDE.md
</context>

<planner_assumptions>

Flagged explicitly rather than buried in task text. This repo has a recorded case (durable record
`xqqzxxtmry`) where a planner's wrong assumption was implemented faithfully AND covered by a passing
test that asserted the buggy behavior. If any assumption below is wrong, say so in the SUMMARY and
stop — do not implement around it.

**PA-1 (carried forward from RESEARCH A1).** D-03's claim that `Irreversible("")` "panics at package
init, before any test runs" holds only if the call appears in a package-level `var`/`init()`, not
inside a function called later. This phase's production `Registry` is EMPTY, so nothing exercises the
init-time half at all. `Registry`'s doc comment must state that Phase 4 MUST build it as a
package-level `var Registry = []Step{ NewStep(...) }` literal, because that placement is what makes
the init-time guarantee true. This plan proves only the panics-on-bad-input half (plan 03-02), which
is the half this repo already has an idiom for (`TestRemapFromPanicsOnEmptyValue`).

**PA-2 (per-point SetPayload, resolving RESEARCH A3).** The sweep issues **one `SetPayload` per
point**, not one per chunk — mirroring `BackfillShortIDs` (`store.go:2782-2789`), and diverging from
RESEARCH.md's Pattern 4 sketch, which selects a whole chunk of ids. The reason is correctness, not
taste: a step's added values are derived from the record's OWN payload (Phase 4's first customer mints
a per-record `short_id`), so a single multi-ID `SetPayload` would write one record's values onto
every id in the chunk. Consequence to keep consistent everywhere downstream: "the Nth SetPayload" in
plan 03-04's fault injection means the Nth POINT of a pass, and "partial" means
some-points-of-a-pass-landed, not some-ids-within-one-call-landed. This is a strictly stronger
position with respect to qdrant/qdrant#9371, not a weaker one: within-call ambiguity is removed by
construction, and cross-call ambiguity is handled by unconditional per-pass re-derivation.

**PA-3 (termination guard).** D-07's loop-to-zero has no stated termination condition for a
persistently failing backend. This plan adds one, and derives it the same way D-09 requires
everything else to be derived — from a **fresh re-derivation**, never from a write call's error: if
a pass's freshly-counted backlog is not strictly smaller than the previous pass's, `Migrate` returns
an error wrapping the last write error. This guard assumes D-08's stamp-then-sweep ordering (no
writer is producing below-target records faster than the sweep drains them). That assumption is
exactly what plan 03-05 proves; state its dependence in the doc comment.

**PA-4 (`target <= 0` short-circuit).** `backlogFilter`'s `IsEmpty` arm matches every legacy record
unconditionally, so it is only correct for `target >= 1`. At `target == 0` a legacy record is
already at v0 and needs nothing. `Migrate` therefore short-circuits at `target <= 0`. Without this,
`engram migrate` against today's `CurrentVersion = 0` would stamp a semantically-meaningless
`schema_version: 0` across the whole collection.

**PA-5 (`CheckAdditive`'s signature).** `CheckAdditive(s Step, before, after map[string]any) error`
takes both maps rather than applying the step itself, so the sweep — which must compute `after`
anyway in order to write — and the fixture table in plan 03-03 drive exactly the same function. A
signature that re-applied the step internally would mean the sweep validates a *different* execution
than the one it writes.

**PA-6 (error wording).** `internal/migrate` is a stdlib-only leaf with no access to the repo's
`field=<name> hint=<code>` envelope machinery (which lives in `internal/server`). Its errors are
plain `fmt.Errorf` values prefixed `migrate: `, naming the offending index/version/keys. CONTEXT.md
leaves error-envelope wording to discretion; this is the resolution. Phase 4 wraps these into the
operator envelope at the CLI boundary.

</planner_assumptions>

## Artifacts this phase produces

Every symbol below is **created by this phase** and does not exist in the codebase today. Exclude all
of them from drift/source-grounding verification. Plans 03-02 … 03-05 reference this table.

| Artifact | Kind | Plan |
|---|---|---|
| `internal/migrate/step.go` | new file | 03-01 |
| `migrate.ApplyFunc` (`func(map[string]any) (map[string]any, error)`) | new exported type | 03-01 |
| `migrate.Reversibility` (sealed interface, unexported `isReversibility()` marker) | new exported type | 03-01 |
| `reversibleStep`, `irreversibleStep` | new unexported types | 03-01 |
| `migrate.Reversible(inverse ApplyFunc) Reversibility` | new exported func | 03-01 |
| `migrate.Irreversible(reason string) Reversibility` | new exported func | 03-01 |
| `migrate.Inverse(r Reversibility) (ApplyFunc, bool)` | new exported func | 03-01 |
| `migrate.IrreversibleReason(r Reversibility) (string, bool)` | new exported func | 03-01 |
| `migrate.Step` (struct, every field unexported) | new exported type | 03-01 |
| `migrate.NewStep(from, to Version, addsKeys []string, rev Reversibility, apply ApplyFunc) Step` | new exported constructor | 03-01 |
| `Step.From`, `Step.To`, `Step.AddsKeys`, `Step.Reversibility`, `Step.Apply` | new exported methods | 03-01 |
| `internal/migrate/registry.go` | new file | 03-01 |
| `migrate.Registry` (`var Registry = []Step{}`, empty this phase) | new exported var | 03-01 |
| `migrate.Validate(steps []Step) error` | new exported func | 03-01 |
| `migrate.StepsFrom(steps []Step, from, to Version) ([]Step, error)` | new exported func | 03-01 |
| `internal/migrate/additive.go` | new file | 03-01 |
| `migrate.AddedKeys`, `migrate.RemovedKeys`, `migrate.CheckAdditive` | new exported funcs | 03-01 |
| `internal/store/migratebacklog.go` | new file | 03-01 |
| `backlogFilter`, `versionOf`, `payloadToMap` | new unexported funcs | 03-01 |
| `internal/store/migrate.go` | new file | 03-01 |
| `migrateBatch` (default scroll page / chunk size) | new unexported const | 03-01 |
| `store.MigrateOptions`, `store.MigrateResult` | new exported types | 03-01 |
| `(*Store).Migrate` | new exported method | 03-01 |
| `internal/store/migrate_test.go` | new file | 03-01 |
| `TestMigrateTracerLegacyRecordEndToEnd` | new test | 03-01 |
| `TestBacklogFilterMatchesAbsentAndBelowTarget` | new test | 03-01 |
| `TestMigrateRefusesNonAdditiveStep` | new test | 03-01 |
| `seedLegacyRecord`, `markerStep`, `migrateBacklogIDs`, `hasPayloadKey` | new test helpers | 03-01 |
| `internal/migrate/decoder.go`, `migrate.Decoder` | new file / exported interface | 03-02 |
| `internal/migrate/step_test.go`, `registry_test.go`, `decoder_test.go`, `leafpurity_test.go` | new files | 03-02 |
| `TestNewStepPanicsOnNilReversibility`, `TestNewStepPanicsOnNilApplyFunc`, `TestIrreversiblePanicsOnEmptyReason`, `TestReversiblePanicsOnNilInverse`, `TestReversibilityIsSealedToThisPackage`, `TestValidateRejectsOrderingAndIdempotencyViolations`, `TestStepsFromSelectsContiguousChain`, `TestDecoderDoorIsOpenAndUnclaimed`, `TestMigratePackageIsStdlibOnlyLeaf` | new tests | 03-02 |
| `internal/migrate/additive_test.go`, `TestAdditiveOnlyKeySetDiff` | new file / test | 03-03 |
| `internal/store/migrate_faultinject_test.go`, `dialFaultInjectingTestClient`, `setPayloadFaultInjector`, `TestMigratePartialFailureResume` | new file / helpers / test | 03-04 |
| `internal/store/migrate_converge_test.go`, `dialMidSweepTestClient`, `midSweepInterceptor`, `TestMigrateConvergesWithoutLock` | new file / helpers / test | 03-05 |
| `.planning/phases/03-migration-foundation-registry-invariants-sweep/red-evidence/*.patch` | new evidence artifacts | 03-01 … 03-05 |

**Unchanged by this phase, deliberately:** `migrate.Version`, `migrate.CurrentVersion` (stays `0`) and
their doc comments in `internal/migrate/migrate.go`. Append new declarations in new files; do not edit
`migrate.go`.

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end — one absent-key legacy record migrates v0 to v1 through every layer</name>
  <precondition>A Qdrant instance is reachable for `internal/store`'s TestMain: either `docker info` responds or `ENGRAM_QDRANT_TEST_ADDR` is set. The tracer asserts against a real pinned server, not a fake.</precondition>
  <files>internal/migrate/step.go, internal/migrate/registry.go, internal/store/migratebacklog.go, internal/store/migrate.go, internal/store/migrate_test.go</files>
  <read_first>
    - `internal/migrate/migrate.go` (whole file, 46 lines) — `Version` and `CurrentVersion`. The `CurrentVersion` doc comment's three numbered reasons and its closing "never a standalone bump" sentence are LOCKED: preserve them verbatim, change no character of that file, and add nothing to it.
    - `internal/openaiurl/openaiurl.go` lines 1-33 — the stdlib-only leaf-package shape SC1 names: SPDX header, a package doc comment that states WHY it is a leaf and which direction the import runs, and a doc comment on every exported symbol that states the invariant, not just the mechanics.
    - `internal/store/store.go` lines 1001-1020 — `activeWindowConditions`. This is the in-repo precedent for `backlogFilter`: a `Range` condition and a `NewIsEmpty` condition paired inside one `Should` group, because a `Range` alone cannot see an absent key. Read the doc comment too — it already explains the trap for `not_before`/`not_after`.
    - `internal/store/store.go` lines 2727-2797 — `BackfillShortIDs`. Copy its telemetry/span wrapper verbatim in shape (`tracer.Start`, deferred `telemetry.RecordStoreOp`, `span.RecordError`/`SetStatus` on error, `attribute.Int64("engram.result_count", ...)` on success) and its per-point `SetPayload` idiom. Do NOT copy its across-the-whole-run `offset` threading — see the action below.
    - `internal/store/store.go` lines 127-140 — `qdrantPayloadOpBatchSize` and its doc comment on qdrant/qdrant#9371, and lines 2213-2223 — `Supersede`'s doc comment on the same hazard. `Store.Migrate`'s doc comment cites these line ranges rather than re-deriving the citation.
    - `internal/store/store.go` lines 640-650 and 738-746 — `payload()`'s `p[schemaVersionKey] = int(max(...))` write and `fromPayload`'s `migrate.Version(v.GetIntegerValue())` read. The `int(...)` conversion is mandatory at every Qdrant write boundary: `qdrant.NewValueMap` panics on a named type over int.
    - `internal/store/store.go` lines 3066-3090 — `ReindexResult` and `reindexBatch`, plus `ReindexOptions.Batch`'s `// scroll page size (0 -> a sane default)` idiom at `:3019`. `MigrateOptions.Batch` copies that idiom exactly.
    - `internal/store/schemaversion_compat_test.go` lines 490-542 — `injectRawPayload`, `deleteRawPayloadKeys`, `rawPayload`. `deleteRawPayloadKeys` is the ONLY way to build a genuinely key-absent record, since `Store.Upsert`'s `payload()` unconditionally stamps `schema_version`. Reuse all three; do not write new equivalents.
    - `internal/store/store_test.go` lines 82-100 and 300-320 — `newTestStore` and `testStore`, the standard per-test collection setup.
    - `$(go env GOMODCACHE)/github.com/qdrant/go-client@v1.18.3/qdrant/json_with_int.pb.go` lines 186-295 — the `Value` oneof. Verified at plan time: SEVEN variants (`Value_NullValue`, `Value_DoubleValue`, `Value_IntegerValue`, `Value_StringValue`, `Value_BoolValue`, `Value_StructValue`, `Value_ListValue`). If what you read differs, the module pin moved — stop and reconcile before writing `payloadToMap`.
    - `.planning/phases/03-migration-foundation-registry-invariants-sweep/03-RESEARCH.md` Patterns 1, 2 and 4 plus Pitfalls 1, 3, 4 and 5 — the sealed-interface code is concrete and should be followed near-verbatim; the sweep sketch is followed EXCEPT for the per-chunk write, per PA-2.
  </read_first>
  <action>
    Build the thinnest path that touches every layer this phase modifies, wired end to end, with one
    real runnable check at the far end. This is production code written for keeps, not a prototype:
    everything below ships as-is and later tasks expand outward from it.

    **`internal/migrate/step.go`** (new; SPDX header, `package migrate`). Declare, each with a doc
    comment stating the invariant it enforces:
    - `ApplyFunc` — `func(payload map[string]any) (map[string]any, error)`. Document that a step
      receives the record's decoded raw payload and returns the payload it wants stored, and that the
      caller — not the step — decides what is written, from the key-set difference.
    - `Reversibility` — an interface whose only method is the unexported `isReversibility()`. Its doc
      comment names the one real escape hatch this closes (a foreign type satisfying it) and the one
      it does NOT (a literal `nil`, which `NewStep` closes instead), and states that no exported type
      in this package may ever carry the marker method, because embedding an exported carrier
      re-opens the seal.
    - `reversibleStep{inverse ApplyFunc}` and `irreversibleStep{reason string}`, both unexported,
      both with a value-receiver `isReversibility()`.
    - `Reversible(inverse ApplyFunc) Reversibility` — panics when `inverse` is nil.
    - `Irreversible(reason string) Reversibility` — panics when `reason` is empty. Mirror
      `store.RemapFrom`'s panic-message convention exactly: `<pkg>.<Func>: <what> must be non-empty`.
    - `Inverse(r Reversibility) (ApplyFunc, bool)` and `IrreversibleReason(r Reversibility) (string,
      bool)` — the read side Phase 4's revert needs; both return `false` for the other variant and
      for nil. Document that these exist so the sealed types stay unexported.
    - `Step` — a struct whose fields `from`, `to`, `addsKeys`, `rev`, `apply` are ALL unexported.
    - `NewStep(from, to Version, addsKeys []string, rev Reversibility, apply ApplyFunc) Step` — the
      only exported constructor. It panics when `rev` is nil and when `apply` is nil, and it stores a
      defensive copy of `addsKeys` (via `slices.Clone`) so a caller mutating its own slice afterwards
      cannot desynchronize the declaration from what was registered. Its doc comment must state, in
      plain terms, that positional required parameters prevent OMISSION but not an explicit `nil`,
      and that the nil check is therefore what actually makes SC3 true.
    - Accessors `From() Version`, `To() Version`, `AddsKeys() []string` (returns a clone),
      `Reversibility() Reversibility`, `Apply(payload map[string]any) (map[string]any, error)`.

    **`internal/migrate/registry.go`** (new). Declare:
    - `var Registry = []Step{}` — empty this phase, with a doc comment that (a) states the production
      registry ships empty and `CurrentVersion` stays 0 because Phase 4 registers the first step, and
      (b) records PA-1: Phase 4 MUST build this as a package-level `var` literal, because that
      placement is what makes a bad `Irreversible("")` fail at package init before any test runs; a
      registry built inside a function weakens that guarantee to whenever the function runs.
    - `Validate(steps []Step) error` — the single ordering-and-idempotency invariant SC1 names. An
      empty registry is valid. Otherwise three independent rules, each with its own distinct message
      text: (idempotency) no two steps may share a `from` version and no two may share a `to` version;
      (advance) every step must have `to` strictly greater than `from`; (contiguity) from index 1 on,
      `from` must equal the previous step's `to`. Accumulate EVERY violation and return
      `errors.Join(...)` rather than the first one — this is what lets each rule be exercised
      independently by a fixture, and it is what an operator staring at a broken registry actually
      needs. Each message names the index and both versions. Document the choice of
      contiguity-over-topological-sort and why: every other locked decision in this phase assumes a
      linear v0->v1->v2 chain, and `CurrentVersion` is a single scalar, not a graph head.
    - `StepsFrom(steps []Step, from, to Version) ([]Step, error)` — returns the contiguous sub-chain
      starting at `from` and ending at `to`. `from == to` returns an empty slice and no error. A
      missing link returns an error naming the version at which the chain broke. Document that this
      is what lets one sweep pass carry records sitting at different versions.

    **`internal/store/migratebacklog.go`** (new; `package store`). Declare:
    - `backlogFilter(target migrate.Version) *qdrant.Filter` — `Should` holding exactly two
      conditions: `qdrant.NewRange(schemaVersionKey, &qdrant.Range{Lt: qdrant.PtrOf(float64(target))})`
      and `qdrant.NewIsEmpty(schemaVersionKey)`. Its doc comment must state, as the reason the second
      condition exists, that Qdrant evaluates a range condition on an absent key as false rather than
      as "below everything", so the first condition alone cannot see a pre-migration record; and that a
      top-level bare `Should` with no `Must` is a hard OR-restriction, not a ranking hint. Cite
      `activeWindowConditions` (`store.go:1004-1020`) as the same shape already solved in this file for
      `not_before`/`not_after`. Also state that this filter is operator-tier only and is never reachable
      from a recall entry point, so Phase 2's `TestSchemaVersionNeverGatesRecall` gate continues to hold.
    - `versionOf(p map[string]*qdrant.Value) migrate.Version` — reads `schemaVersionKey` exactly as
      `fromPayload` does (`store.go:741-743`); an absent key returns `migrate.Version(0)`, per
      `Version`'s own "the zero value IS v0 IS absent" doc comment.
    - `payloadToMap(p map[string]*qdrant.Value) (map[string]any, error)` — converts a stored payload
      into the `map[string]any` an `ApplyFunc` consumes. Type-switch on the value's oneof kind and
      handle all seven variants BY NAME, recursing for the struct and list variants. An unrecognized
      kind returns an error naming the key and the Go type — never a silent drop, never a zero value.
      Write a comment naming all seven and stating that an unhandled variant is a hole a client
      upgrade must consciously fill.

    **`internal/store/migrate.go`** (new; `package store`). Declare:
    - `const migrateBatch = 256` — the default scroll page size, copying `reindexBatch`'s role.
    - `MigrateOptions{Target migrate.Version; Steps []migrate.Step; Batch uint32}` — with the
      "0 -> a sane default" comment idiom on `Batch` copied from `ReindexOptions.Batch`, `Target` zero
      meaning `migrate.CurrentVersion`, and `Steps` nil meaning `migrate.Registry`. Document that
      `Steps` exists so a test can drive fixture steps against a production-identical code path
      without mutating the production registry (D-06), and that Phase 4's CLI passes neither.
    - `MigrateResult{Migrated, Failed, Passes, Backlog uint64}` — `Migrated` counts points whose write
      returned no error, `Failed` counts points whose write returned one, `Passes` counts outer
      re-derivations, and `Backlog` is the freshly re-derived backlog size at return (0 means
      converged). Document explicitly that `Migrated` and `Failed` are write-signal counters reported
      for telemetry only, and that `Backlog` — a fresh count — is the field that actually describes
      the collection.
    - `(s *Store) Migrate(ctx context.Context, opts MigrateOptions) (res MigrateResult, err error)`.
      Open with `BackfillShortIDs`'s telemetry/span wrapper (span name `store.Migrate`, telemetry op
      `Migrate`, `engram.result_count` set from `res.Migrated`). Then, before any network hop —
      matching `RemapOwner`'s validate-first shape — resolve `target`, `steps` and `batch` from the
      defaults above, return a zero result and no error when `target` is at or below zero (PA-4, with
      the reason in a comment), and return `migrate.Validate(steps)`'s error if the chain is invalid.

      The loop, with a doc comment stating the divergence from `BackfillShortIDs` in so many words —
      this is the first sweep in the file that re-derives across an outer loop, and a future reader
      must not "fix" it back into a single cursor-threaded pass:
      1. Fresh exact `Count` with `backlogFilter(target)`; store it in `res.Backlog`. Zero means
         converged: return with no error.
      2. If this count is not strictly smaller than the previous pass's, stop and return an error that
         names both counts and wraps the last write error (PA-3). State in the comment that this guard
         is what makes the loop terminate against a failing backend, that it is derived from a fresh
         count rather than from any write's error, and that it assumes D-08's stamp-then-sweep ordering.
      3. One `ScrollAndOffset` with the same fresh filter, `Limit` = batch, **`Offset: nil`**, and
         `WithPayload: qdrant.NewWithPayload(true)`. Comment that `Offset` is nil on every pass by
         design (D-07): there is no cursor persisted across passes, which is exactly why a resume is
         nothing more than calling this method again.
      4. For each returned point: read its version with `versionOf`, select its chain with
         `migrate.StepsFrom`, decode with `payloadToMap`, and apply each step in order to a clone.
         After each step, call `migrate.CheckAdditive` with that step's before/after maps and return
         its error immediately, before any write — a declared-vs-actual drift is a defect in the step,
         not a per-record condition to skip past.
      5. Build the write map from `migrate.AddedKeys(originalBefore, finalAfter)`, then set
         `schemaVersionKey` to `int(target)` — the explicit conversion is mandatory. Issue ONE
         `SetPayload` for this point's id (`qdrant.NewPointsSelectorIDs` over a single-element slice),
         `Wait: true`. On error: record it as the last write error, increment `res.Failed`, and
         `continue` to the next point. Comment that this is D-09: the error does not describe what
         landed, so no control-flow branch may be taken on it; the next pass's fresh count decides.
         On success increment `res.Migrated`.

      `SetPayload` is the sweep's only Qdrant write verb, and it is issued one point at a time per
      PA-2. Record that reason in the doc comment together with `CheckAdditive`'s placement, so the
      two halves of "additive only" — structural at the write layer, checked at the apply layer — are
      both visible from the method's own documentation.

    **`internal/store/migrate_test.go`** (new; `package store`). Write the tracer and the helpers the
    later plans reuse. Name helpers exactly as listed, because plans 03-04 and 03-05 depend on them:
    - `markerStep(from, to migrate.Version, key string) migrate.Step` — a test-only conforming step
      built with `migrate.NewStep`, declaring exactly `[]string{key}`, carrying
      `migrate.Reversible(...)` with a real inverse that removes that key, whose `ApplyFunc` sets
      `payload[key]` to a fixed string. This marker key is what makes "was this record processed?"
      directly observable in later plans; without it a zero-step sweep's only effect would be a
      version stamp that cannot distinguish a first pass from a second.
    - `seedLegacyRecord(ctx, t, s, id)` — upserts a normal record through `Store.Upsert`, then calls
      `deleteRawPayloadKeys` to strip `schema_version`, producing the genuinely-absent-key legacy
      shape. Assert inside the helper, via `rawPayload`, that the key really is gone before returning;
      a helper that silently failed to strip it would make every test in this phase vacuous.
    - `migrateBacklogIDs(ctx, t, s, target) []string` — an INDEPENDENT re-derivation: scroll
      `backlogFilter(target)` directly through `s.client` and return the sorted ids. Tests assert
      against this, never against `MigrateResult`'s counters — the same re-derivation discipline the
      production code follows.
    - `hasPayloadKey(ctx, t, s, id, key) bool` — thin wrapper over the existing `rawPayload`.

    `TestMigrateTracerLegacyRecordEndToEnd`: seed ONE legacy record, run
    `Store.Migrate(ctx, MigrateOptions{Target: 1, Steps: []migrate.Step{markerStep(0, 1, "tracer_marker")}, Batch: 8})`,
    and assert: no error; `migrateBacklogIDs(ctx, t, s, 1)` is empty; the record's raw payload now
    carries `tracer_marker` and `schema_version == 1`; and the record's pre-existing `content` key
    still has its original value. That last assertion is the one that proves the sweep merged rather
    than replaced.
  </action>
  <verify>
    <automated>go test -count=1 -v -run 'TestMigrateTracerLegacyRecordEndToEnd$' ./internal/store/ 2>&1 | rg -c -- '--- PASS: TestMigrateTracerLegacyRecordEndToEnd '</automated>
  </verify>
  <acceptance_criteria>
    - The command above prints `1`. A package-level `ok` line is a FALSE GREEN and a failure of this task: `go test -run` matching nothing exits 0 with `[no tests to run]` (durable record `bsbsvn4hbc`).
    - `git diff --exit-code -- internal/migrate/migrate.go` succeeds: `Version`, `CurrentVersion` and the `CurrentVersion` doc comment are byte-identical to their pre-task state.
    - `rg -c 'CurrentVersion Version = 0' internal/migrate/migrate.go` prints `1`.
    - `rg -c 'Registry = \[\]Step\{\}' internal/migrate/registry.go` prints `1` — the production registry ships empty.
    - `go list -deps ./internal/migrate | rg -c '^[^/]+\.[^/]+/'` prints `0`, and `go list -deps ./internal/migrate | wc -l` prints a number greater than `0`. The package has no non-stdlib dependency and the check scanned something.
    - `rg -o 'qdrant\.New(Range|IsEmpty)\(schemaVersionKey' internal/store/migratebacklog.go | sort | uniq -c` reports exactly one `qdrant.NewRange(schemaVersionKey` and one `qdrant.NewIsEmpty(schemaVersionKey`, and `rg -c 'Should:' internal/store/migratebacklog.go` prints at least `1`. A filter built with `Must` as its only top-level clause is a failure of this task regardless of what any test says.
    - `rg -c 'Offset: *nil' internal/store/migrate.go` prints at least `1`, and `rg -c 'offset = next|Offset: *offset' internal/store/migrate.go` prints `0` — no cursor is threaded across passes.
    - `rg -o 'client\.(SetPayload|DeletePayload|OverwritePayload|Upsert|Delete|ClearPayload)\(' internal/store/migrate.go internal/store/migratebacklog.go | rg -o '[A-Za-z]+\(' | sort -u` outputs exactly one line, `SetPayload(`.
    - `rg -c 'int\(target\)' internal/store/migrate.go` prints at least `1` — the `migrate.Version` value crossing the `qdrant.NewValueMap` boundary is explicitly converted.
    - `NewStep` panics on a nil `rev` and on a nil `apply`; `Irreversible` panics on an empty reason; `Reversible` panics on a nil inverse. (Plan 03-02 supplies the tests; this task supplies the checks.)
    - Every field of `migrate.Step` is unexported: `rg -c '^\tFrom |^\tTo |^\tAddsKeys |^\tRev |^\tApply ' internal/migrate/step.go` prints `0`.
    - No exported type in `internal/migrate` carries the marker method: `rg -c 'func \([A-Z]' internal/migrate/step.go` counts only value-receiver accessor methods on `Step`, and `rg -n 'isReversibility\(\) *\{' internal/migrate/step.go` shows exactly two definitions, both on unexported receivers.
    - `payloadToMap` handles all seven `Value` oneof variants by name and returns an error — not a zero value — for an unrecognized kind.
    - `task lint` exits 0, `gofmt -l internal/migrate internal/store` prints nothing, and `task license:check` exits 0 (both new `internal/**` Go files carry the SPDX header; `.planning/**` files must NOT).
  </acceptance_criteria>
  <done>One genuinely-key-absent legacy record travels from a `migrate.NewStep` fixture through `Validate`/`StepsFrom`, through `Store.Migrate`'s re-derived backlog and `backlogFilter`'s OR-shape, to a real pinned Qdrant, and comes back carrying the step's declared key, `schema_version == 1` and its original content — with the backlog independently re-derived to empty.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the backlog filter — absent key, below target, at target — and prove RED against a range-only filter</name>
  <precondition>A Qdrant instance is reachable for `internal/store`'s TestMain: either `docker info` responds or `ENGRAM_QDRANT_TEST_ADDR` is set.</precondition>
  <files>internal/store/migrate_test.go, internal/store/migratebacklog.go, .planning/phases/03-migration-foundation-registry-invariants-sweep/red-evidence/03-01-red-1-range-only-filter.patch</files>
  <read_first>
    - Task 1's `internal/store/migratebacklog.go` and `internal/store/migrate_test.go` as written.
    - `internal/store/schemaversion_compat_test.go` lines 490-542 — `injectRawPayload`, `deleteRawPayloadKeys`, `rawPayload` again; this task uses `injectRawPayload` to place an explicit below-target value and `deleteRawPayloadKeys` for the absent-key case.
    - `.planning/phases/03-migration-foundation-registry-invariants-sweep/03-VALIDATION.md` § "Non-Vacuity Requirements" — the third bullet is binding on this task specifically, and the fourth defines the RED-evidence procedure.
    - `.planning/phases/02-record-schema-versioning-foundation/red-evidence/02-03-red-1-toplevel.patch` — the committed-patch format and granularity this repo established for RED evidence.
  </read_first>
  <action>
    Add `TestBacklogFilterMatchesAbsentAndBelowTarget` to `internal/store/migrate_test.go`, then prove
    it is a live gate rather than a decorative one.

    **The fixture — three records, one collection, target 1.** Build each through a distinct path so
    the three cases are genuinely distinct on disk, not three spellings of one shape:
    - `absent` — seeded with `seedLegacyRecord`, so `schema_version` is truly missing. Assert via
      `rawPayload` inside the test (not only inside the helper) that the key is absent before the
      derivation runs. This record is the whole point of the test: it is the only one that can tell a
      correct filter from a broken one.
    - `below` — a normally-upserted record, then `injectRawPayload` writing an explicit
      `schema_version` of `0`. The key is PRESENT with a below-target value.
    - `current` — a normally-upserted record whose `Memory.SchemaVersion` is `migrate.Version(1)`, so
      `payload()`'s monotonic max stamps `1` through the real production write path. Assert via
      `rawPayload` that the stored value is `1`.

    **Assert as a sorted set, in both directions.** `migrateBacklogIDs(ctx, t, s, 1)` must be exactly
    `{absent, below}` — a set comparison with both difference directions printed on failure. Do not
    assert a length, and do not assert "contains absent": a length check passes for the wrong two
    records and a contains-check passes for a filter that matches everything.

    **Add a non-vacuity assertion.** The derived set must be non-empty AND must exclude `current`.
    Both halves matter: an empty result would satisfy any "does not contain current" phrasing.

    **Add the `target <= 0` sub-case** (PA-4). Assert `migrateBacklogIDs(ctx, t, s, 0)` — the
    derivation any caller would perform at today's `migrate.CurrentVersion` — and separately assert
    that `Store.Migrate(ctx, MigrateOptions{Target: 0})` returns no error, a zero `Migrated`, and
    leaves `absent`'s payload still without a `schema_version` key. A record at v0 needs no work, and
    the sweep must not stamp the whole collection to prove otherwise.

    **Prove RED.** Capture the hunk first, then apply it: temporarily rewrite `backlogFilter` in
    `internal/store/migratebacklog.go` to the naive shape — a single top-level `Must` holding only the
    `Range` condition, with the `IsEmpty` condition removed. Run the test. It MUST fail, and the
    failure MUST name `absent` as missing from the derived set. Record the exact failing output.

    This is the cycle that matters most in the phase: before the fix the derived backlog contains only
    `below`, the sweep converges on pass 1, and nothing that needed migrating was migrated. Say that
    in the SUMMARY entry in those terms.

    **Revert by exact inverse, never by checkout.** Save the injected hunk as
    `.planning/phases/03-migration-foundation-registry-invariants-sweep/red-evidence/03-01-red-1-range-only-filter.patch`,
    revert with `git apply -R` of that same patch, and verify with
    `git diff --exit-code -- internal/store/migratebacklog.go`. Do not run `git checkout --` on a
    tracked file: it discards unrelated uncommitted work, and this repo has a standing rule about
    agents sharing one git index. Re-run the test: it MUST pass. Record the three-command reproduce
    recipe (`git apply` -> `go test` expecting FAIL -> `git apply -R`) in the SUMMARY.
  </action>
  <verify>
    <automated>go test -count=1 -v -run 'TestBacklogFilterMatchesAbsentAndBelowTarget$' ./internal/store/ 2>&1 | rg -c -- '--- PASS: TestBacklogFilterMatchesAbsentAndBelowTarget'</automated>
  </verify>
  <acceptance_criteria>
    - The command above prints a number greater than or equal to `1` (the parent plus any subtests), and the run includes a line matching `--- PASS: TestBacklogFilterMatchesAbsentAndBelowTarget ` exactly once.
    - The fixture builds its absent-key record via `deleteRawPayloadKeys` (through `seedLegacyRecord`), and the test itself asserts the key's absence with `rawPayload` before deriving anything. A fixture whose three records are all produced by `Store.Upsert` is a failure of this task: it cannot distinguish the correct filter from the broken one.
    - The `current` record's `schema_version` of `1` is produced by the real `Store.Upsert`/`payload()` path via `Memory.SchemaVersion`, not by `injectRawPayload`.
    - The derived id set is compared for SET EQUALITY against `{absent, below}`, with both difference directions named in the failure message. No assertion in this test is a length check, a `contains` check, or a `len(x) > 0`.
    - The test asserts the derived set is non-empty AND excludes `current` — both, as separate assertions.
    - `Store.Migrate` at `Target: 0` returns no error, `Migrated == 0`, and leaves the absent-key record without a `schema_version` key.
    - `.planning/.../red-evidence/03-01-red-1-range-only-filter.patch` exists, applies cleanly with `git apply`, and reverses cleanly with `git apply -R`.
    - `03-01-SUMMARY.md` records, for this cycle: the injected shape, the observed failing output naming `absent`, the exact three-command reproduce recipe, and the observed pass after revert. It states in plain terms that under the range-only filter the sweep converges on pass 1 having migrated nothing.
    - `git diff --exit-code -- internal/store/migratebacklog.go` succeeds at task end. `git checkout --` was not run on any tracked file.
    - `.planning/**` files carry NO SPDX header — do not run `task license:add` over the red-evidence directory.
  </acceptance_criteria>
  <done>A record whose `schema_version` key is genuinely absent is proven to be in the derived backlog, a record already at the target is proven to be out of it, the `target <= 0` case is proven to be no work at all, and a range-only filter has been observed turning the test RED with a reviewer-reproducible committed patch.</done>
</task>

<task type="auto">
  <name>Task 3: Enforce additive-only at apply time — the two-direction key-set diff, wired fail-closed into the sweep</name>
  <precondition>A Qdrant instance is reachable for `internal/store`'s TestMain: either `docker info` responds or `ENGRAM_QDRANT_TEST_ADDR` is set.</precondition>
  <files>internal/migrate/additive.go, internal/store/migrate.go, internal/store/migrate_test.go, .planning/phases/03-migration-foundation-registry-invariants-sweep/red-evidence/03-01-red-2-declared-drift-written.patch</files>
  <read_first>
    - Task 1's `internal/migrate/step.go` and `internal/store/migrate.go` as written.
    - `.planning/phases/03-migration-foundation-registry-invariants-sweep/03-CONTEXT.md` D-04 — the two directions and which one is load-bearing. Read the second paragraph carefully: a subset or superset comparison passes a step whose declaration has drifted from its behavior, which is the exact failure the declaration exists to prevent.
    - `.planning/phases/03-migration-foundation-registry-invariants-sweep/03-RESEARCH.md` § "Code Examples > Additive-only key-set diff test" — the `keysOf`/`setDiff`/`setEqual` shape; this task puts that logic in production code so the fixture table in plan 03-03 and the sweep drive the same function (PA-5).
    - `internal/store/store.go` lines 2213-2223 — `Supersede`'s fail-closed posture on an invariant it cannot verify after the fact; the same posture applies here, before the write rather than after.
  </read_first>
  <action>
    **`internal/migrate/additive.go`** (new; SPDX header, `package migrate`). Three exported functions,
    stdlib only (`fmt`, `slices`, `sort` or `maps` as needed — nothing else):
    - `AddedKeys(before, after map[string]any) []string` — keys in `after` absent from `before`,
      returned SORTED so every caller's comparison is order-independent.
    - `RemovedKeys(before, after map[string]any) []string` — the mirror, also sorted.
    - `CheckAdditive(s Step, before, after map[string]any) error` — returns nil only when BOTH hold:
      1. `RemovedKeys(before, after)` is empty. The error names every vanished key.
      2. `AddedKeys(before, after)` is SET-EQUAL to `s.AddsKeys()`. The error names both differences
         separately — keys added but not declared, and keys declared but not added.

      Document, in the function's own doc comment, why direction 2 is set equality and not a subset or
      superset relation: a superset check passes a step that quietly adds an undeclared key, and a
      subset check passes a step that declares keys it never writes. Either way the declaration stops
      describing the behavior, and the declaration is the entire mechanism. Also state that comparison
      is over KEY SETS only — this function makes no claim about values, and a step that overwrites an
      existing key's VALUE in place is not caught here. That limit is real; write it down rather than
      letting a future reader infer a stronger guarantee than the code provides.

    **Wire it into the sweep, fail-closed.** In `Store.Migrate`'s per-point loop, after each step's
    `Apply` and BEFORE any write, call `migrate.CheckAdditive(step, beforeThisStep, afterThisStep)` and
    return its error immediately, wrapped with the point id and the step's version transition. Nothing
    is written for that record and the sweep stops. Comment that this is deliberately not a per-record
    skip: a declared-vs-actual drift is a defect in the STEP, identical for every record it touches, so
    continuing would write an unbounded number of records under a declaration known to be false.

    **`TestMigrateRefusesNonAdditiveStep`** in `internal/store/migrate_test.go`. Two sub-cases, both
    seeded with `seedLegacyRecord` and both run through the real `Store.Migrate`:
    - a fixture step declaring one key whose `ApplyFunc` writes two — `Migrate` returns a non-nil
      error, `res.Migrated` is `0`, and an independent `rawPayload` read shows the record gained
      NEITHER key and still has no `schema_version`. That last assertion is what proves fail-closed:
      the refusal happened before the write, not after it.
    - a fixture step whose `ApplyFunc` deletes a pre-existing payload key — same three assertions.

    Assert on the error's text naming the offending keys, so a future refactor that collapses both
    directions into one generic message is caught.
  </action>
  <verify>
    <automated>go test -count=1 -v -run 'TestMigrateRefusesNonAdditiveStep$' ./internal/store/ 2>&1 | rg -c -- '--- PASS: TestMigrateRefusesNonAdditiveStep/'</automated>
  </verify>
  <acceptance_criteria>
    - The command above prints `2` — both sub-cases (undeclared extra key, removed key) ran and passed.
    - `CheckAdditive`'s added-key comparison is SET EQUALITY. `rg -c 'AddsKeys\(\)' internal/migrate/additive.go` prints at least `1`, and the function reports the two difference directions in separate, separately-worded error text.
    - `AddedKeys` and `RemovedKeys` both return sorted slices, so no caller's verdict depends on map iteration order.
    - `internal/migrate/additive.go` imports only stdlib packages; `go list -deps ./internal/migrate | rg -c '^[^/]+\.[^/]+/'` still prints `0`.
    - In both sub-cases the record is left with no `schema_version` key and none of the step's keys — the refusal is proven to precede the write, not to follow it.
    - `res.Migrated` is `0` in both sub-cases and `Migrate` returns a non-nil error naming the offending key(s).
    - `.planning/.../red-evidence/03-01-red-2-declared-drift-written.patch` exists and records a cycle in which `CheckAdditive`'s call site in `Store.Migrate` is removed (the check bypassed): the test MUST fail with the record having gained the undeclared key. `git apply -R` restores the file and `git diff --exit-code -- internal/store/migrate.go` succeeds.
    - `03-01-SUMMARY.md` records that cycle's injection, observed failing output, reproduce recipe and observed pass after revert.
    - `task lint` exits 0 and `task license:check` exits 0.
  </acceptance_criteria>
  <done>The two-direction key-set diff exists as one production function that both the sweep and plan 03-03's fixture table drive, the sweep refuses to write anything for a step whose behavior has drifted from its declaration, and bypassing the check has been observed letting an undeclared key reach Qdrant.</done>
</task>

</tasks>

<threat_model>
Security enforcement is active (`security_enforcement` absent from `.planning/config.json` = enabled).
ASVS level: L1 (default — `workflow.security_asvs_level` is unset). Blocking threshold: high.
No new external package is introduced this phase, so `03-RESEARCH.md`'s Package Legitimacy Audit is
"not applicable" and no `T-03-SC` supply-chain checkpoint is required.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| step author -> `internal/migrate` | A registered step is trusted, same-binary Go code written by a developer. The boundary is a correctness boundary (a step can silently corrupt every record), not an authentication one — no end user or operator supplies a step at runtime. |
| `Store.Migrate` -> Qdrant | Collection-wide operator-tier writes with no owner/scope predicate, matching the existing `PruneExpired`/`Reindex`/`BackfillShortIDs` tier. Access control to the CLI verb is Phase 4's `registerDestructive` wiring. |
| stored payload -> `ApplyFunc` | `payloadToMap` converts attacker-influenceable stored content (record text, tags, citations) into the `map[string]any` a step consumes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | a step whose behavior removes or renames a payload key | high | mitigate | `migrate.CheckAdditive` runs before any write and `Store.Migrate` returns fail-closed on violation (Task 3); the sweep's only write verb is a per-point `SetPayload`, so removal is structurally unreachable from the sweep itself (Task 1). |
| T-03-02 | Denial of Service | `Store.Migrate` looping forever against a failing backend | medium | mitigate | The non-shrinking-backlog guard (PA-3) terminates the loop and returns the wrapped last write error; the guard is derived from a fresh exact `Count`, not from a write signal (Task 1). |
| T-03-03 | Denial of Service | `payloadToMap` on a deeply nested struct/list payload | low | accept | Payload depth is bounded by what `Store.Upsert` writes through `payload()`, which produces flat scalars and a single-level tag list. No untrusted party can author an arbitrarily nested payload today; revisit if a future phase admits nested payload authoring. |
| T-03-04 | Information Disclosure | `schema_version` reaching a recall or authz filter and shrinking what a user can retrieve | high | mitigate | `backlogFilter` lives in the operator tier and is unreachable from any recall entry point; Phase 2's `TestSchemaVersionNeverGatesRecall` reachability derivation flags it automatically if that ever changes. Task 1 states this in `backlogFilter`'s doc comment. |
| T-03-05 | Elevation of Privilege | a foreign type satisfying `Reversibility` to register an undeclared step | medium | mitigate | The unexported `isReversibility()` marker seals the interface, and no exported type in the package carries it, closing the embedding hatch; `NewStep`'s explicit nil check closes the nil-interface hatch that sealing does not (Task 1, plan 03-02's tests). |
| T-03-06 | Repudiation | a partially-applied sweep leaving no record of what landed | low | accept | `MigrateResult` reports `Migrated`/`Failed`/`Passes` for telemetry and the span carries `engram.result_count`, but the authoritative statement is always a fresh re-derivation (`Backlog`), never the counters. Accepted because per-record audit logging is out of this phase's scope and the collection's own `schema_version` values are self-describing. |
</threat_model>

<verification>
- `go test -count=1 -v ./internal/migrate/ ./internal/store/` is green, with `--- PASS` lines observed
  for all three of this plan's tests by name — never a package-level `ok`.
- `go test -list '.*' ./internal/store/ | rg -c 'TestMigrateTracerLegacyRecordEndToEnd|TestBacklogFilterMatchesAbsentAndBelowTarget|TestMigrateRefusesNonAdditiveStep'` prints `3`, confirming every `-run` in this plan resolves to a real test.
- `go list -deps ./internal/migrate | rg -c '^[^/]+\.[^/]+/'` prints `0` and `go list -deps ./internal/migrate | wc -l` is greater than `0`.
- `git diff --exit-code -- internal/migrate/migrate.go` succeeds.
- Both red-evidence patches apply and reverse cleanly, and `git diff --exit-code` is clean for every
  file they touch.
- `task` (lint + test) is green.
</verification>

<success_criteria>
- `internal/migrate` holds an ordered step registry with a sealed `Reversibility`, a
  positional-required `NewStep`, a single `Validate` invariant and a two-direction additive-only diff,
  with zero non-stdlib dependencies.
- `Store.Migrate` re-derives its backlog every pass with no persisted cursor, writes only through a
  per-point `SetPayload`, never branches control flow on a write error, and terminates on a
  persistently failing backend.
- A record whose `schema_version` key is genuinely absent is provably in the backlog, and a range-only
  filter has been observed turning that proof RED.
- A step whose behavior diverges from its declaration is refused before any write, and bypassing the
  check has been observed letting the undeclared key through.
- `migrate.CurrentVersion` is still `0` and `migrate.Registry` is still empty.
</success_criteria>

<output>
Create `.planning/phases/03-migration-foundation-registry-invariants-sweep/03-01-SUMMARY.md` when done.
Record both prove-RED cycles with their patch paths and three-command reproduce recipes, and record any
deviation from PA-1 … PA-6 explicitly.
</output>
