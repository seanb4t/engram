---
phase: 17-wired-write-handlers-full-crud-schedule
plan: 05
type: tdd
wave: 4
depends_on: ["17-04"]
files_modified:
  - internal/server/connectapi_write_parity_test.go
  - internal/server/connectapi_crossowner_test.go
autonomous: true
requirements: [REQ-connect-write-authz-parity]

must_haves:
  truths:
    - "One shared scenario table drives both the MCP deps.* path and the Connect engramAPI client through the fake store, asserting identical rejection codes per RPC (SC2/D-10)"
    - "The parity table covers: rule un-share attempt (DEC-iedk), stale-summary conflict (DEC-ddiw), cross-owner id (DEC-xa6), tags-only-content-preserved (landmine 2), and Actor equality on both lanes (landmine 3)"
    - "Each by-id write RPC (UpdateMemory/DeleteMemory/SetVisibility) re-wraps store.ErrNotFound with the caller's ORIGINAL input, never the resolved UUID — proven by a cross-owner table test per RPC (SC4/D-11)"
    - "No write RPC carries the no-side-effects idempotency level — the Phase-15 CI gate re-asserts green now that real logic exists (SC5/D-12)"
  artifacts:
    - internal/server/connectapi_write_parity_test.go   # the D-10 shared-table parity test
    - internal/server/connectapi_crossowner_test.go       # per-RPC SC4/D-11 re-wrap tables
  key_links:
    - "This test structurally proves 'identical code path' (both closures reach the same deps.* call) rather than 'coincidentally-matching' — the whole point of SC2"
    - "The cross-owner tables are the browser-network-tab existence-leak defense (DEC-xa6) turned into an automated per-RPC assertion"
  prohibitions:
    - "The parity test MUST NOT assert two independently-implemented rejection paths that merely return the same code — both lanes must reach the same deps.* method (D-10)"
    - "No write RPC may carry idempotency_level = NO_SIDE_EFFECTS (SC5/D-12) — the ban gate stays green"
    - "A by-id write ErrNotFound message MUST NOT contain the resolved UUID for a cross-owner id (SC4/DEC-xa6)"
---

<objective>
Prove the phase's headline guarantee with tests: one shared scenario table driving
both the MCP `deps.*` path and the Connect `engramAPI` client through the hermetic
fake store (Plan 04), asserting identical rejection codes per write RPC — the D-10
structural parity proof. Add a cross-owner re-wrap table per by-id write RPC
(UpdateMemory/DeleteMemory/SetVisibility) proving the `store.ErrNotFound` message
carries the caller's original input and never the resolved UUID (SC4/D-11/DEC-xa6).
Re-assert the Phase-15 idempotency-ban CI gate now that real logic sits behind the
six RPCs (SC5/D-12).

This is the requirement's acceptance gate: REQ-connect-write-authz-parity is
"proven by MCP↔Connect parity tests per RPC." It is also the #322-closing evidence.

Purpose: turn SC2/SC4/SC5 into enforced, regression-proof assertions. Output:
`connectapi_write_parity_test.go` + `connectapi_crossowner_test.go`, plus a green
idempotency-ban gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-wired-write-handlers-full-crud-schedule/17-CONTEXT.md
@.planning/phases/17-wired-write-handlers-full-crud-schedule/17-RESEARCH.md
@.planning/phases/17-wired-write-handlers-full-crud-schedule/17-PATTERNS.md
@.planning/codebase/TESTING.md
</context>

## Artifacts this phase produces (Plan 05)

New symbols this plan introduces (source-grounding pass: exclude these from drift verification):
- `internal/server/connectapi_write_parity_test.go` — the D-10 shared-scenario-table parity test (clone of `TestRerankParityMCPAndConnect`), driving both lanes through the fake store from Plan 04.
- `internal/server/connectapi_crossowner_test.go` — per-RPC cross-owner re-wrap tables (clone of `TestConnectGetMemoryCrossOwnerShortIDDoesNotLeakUUID`) for UpdateMemory/DeleteMemory/SetVisibility.
- A helper mapping a `deps.*` error to a Connect code for the parity comparison (name at executor discretion), if the test needs one.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MCP↔Connect shared-scenario parity table (D-10)</name>
  <files>internal/server/connectapi_write_parity_test.go</files>
  <read_first>
    - internal/server/connectapi_test.go (TestRerankParityMCPAndConnect 356-432 — the exact shape to clone: mcpIDs/connectIDs closures calling d.searchMemory and api.SearchMemories directly, no HTTP)
    - internal/server/fakestore_test.go (the Plan 04 fake — construct deps over it, no live Qdrant)
    - internal/server/identity.go (caller struct; withConnectTokenInfo to inject the Connect-lane TokenInfo; callerFromContext for the MCP closure)
    - internal/server/tools.go (deps.updateMemory/deleteMemory/setVisibility/storeMemory — the caller-threaded methods both closures call)
    - internal/server/connectapi.go (the six write handler methods from Plan 04 — the Connect closure calls these)
    - 17-RESEARCH.md § "The parity-test model to clone (D-10)", § Anti-Patterns ("coincidentally matching" is rejected), and the Code Examples analogous pair
    - 17-PATTERNS.md § "connectapi_write_parity_test.go — clone TestRerankParityMCPAndConnect"
  </read_first>
  <behavior>
    - rule un-share attempt: SetVisibility on a rule-category record rejects identically on both lanes (DEC-iedk) — same Connect code.
    - stale-summary conflict: UpdateMemory changing content with an unaddressed client summary rejects identically on both lanes (DEC-ddiw).
    - cross-owner id: a by-id write against another owner's id returns CodeNotFound identically on both lanes (DEC-xa6).
    - tags-only content preserved: a mask=[tags] UpdateMemory over Connect leaves stored content unchanged and does not re-embed from empty (landmine 2 — Pitfall 1 case).
    - Actor equality: a record authored via MCP and one via Connect for the same identity carry the same non-empty Memory.Actor (landmine 3 — Pitfall 2 case).
  </behavior>
  <action>
    Clone the `TestRerankParityMCPAndConnect` structure: build one deps over the Plan-04 fake store; construct `api := &engramAPI{d: d}`; build an MCP caller context and a Connect TokenInfo context (`withConnectTokenInfo`) for the same identity. For each scenario in a shared table, run the MCP closure (`d.updateMemory(mcpCaller, args)` etc.) and the Connect closure (`api.UpdateMemory(connectCtx, connect.NewRequest(&engramv1.UpdateMemoryRequest{...}))`) and assert the resulting Connect codes are identical — structurally, both closures must reach the same deps.* method (do NOT assert two hand-written rejection paths that merely coincide — RESEARCH Anti-Pattern). Include all five scenarios from the behavior block. For the Actor-equality and content-preserved scenarios, assert on the fake store's stored record (Memory.Actor non-empty and equal; content unchanged after a tags-only update). Write the test first against the (already-wired) Plan-04 handlers; it should pass once correct, but any deviation (e.g. a missing landmine fix) fails a specific scenario row.
  </action>
  <acceptance_criteria>
    - `go test ./internal/server/... -run TestWriteParity -v` passes with a table row per RPC/scenario; each row asserts identical Connect codes across the MCP and Connect closures.
    - The tags-only-content-preserved row fails if updateArgs.Content were still a non-nilable string (landmine 2 regression guard) — assert the stored content equals the original after a mask=[tags] update.
    - The Actor-equality row asserts a non-empty Memory.Actor equal across an MCP-authored and Connect-authored record (landmine 3 regression guard).
    - The cross-owner row asserts CodeNotFound on both lanes.
    - `task lint:go` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/server/... -run TestWriteParity -v && task lint:go</automated>
  </verify>
  <done>One shared table proves per-RPC MCP↔Connect parity through the same deps.* calls, including the landmine-2 and landmine-3 regression rows (SC2/D-10).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Per-RPC cross-owner re-wrap tables (SC4/D-11) + idempotency-ban re-assert (SC5/D-12)</name>
  <files>internal/server/connectapi_crossowner_test.go</files>
  <read_first>
    - internal/server/connectapi_test.go (TestConnectGetMemoryCrossOwnerShortIDDoesNotLeakUUID ~627 — the existing GetMemory analog to clone for the three write RPCs)
    - internal/server/connectapi.go (the UpdateMemory/DeleteMemory/SetVisibility handlers from Plan 04; their CodeNotFound mapping)
    - internal/server/tools.go (deps.updateMemory/deleteMemory/setVisibility — the re-wrap returns fmt.Errorf("%w: %s", store.ErrNotFound, a.ID) with the ORIGINAL input)
    - Taskfile.yaml (the idempotency-ban grep gate ~141-143) and .github/workflows/ci.yaml (~126-127) — the D-12 gate mirrored local↔CI
    - proto/engram/v1/engram.proto (the six write RPCs — confirm none declares the no-side-effects idempotency level)
    - 17-RESEARCH.md § "ErrNotFound Original-Input Re-wrap (D-11 / SC4)" and the Validation Architecture SC4/SC5 rows
    - 17-PATTERNS.md § "Not-found re-wrap with original input (DEC-xa6 / D-11)"
  </read_first>
  <behavior>
    - Owner A holds a record with short_id S and resolved UUID U. Owner B calls UpdateMemory/DeleteMemory/SetVisibility with S (and separately with U-as-supplied): the returned CodeNotFound error message contains the caller's supplied input (S or the value B supplied), never A's resolved UUID U.
    - The idempotency-ban gate finds no write RPC declaring the no-side-effects idempotency level.
  </behavior>
  <action>
    Clone the existing GetMemory cross-owner test for each of the three by-id write RPCs. Seed owner A's record (short_id + UUID) in the fake store; as owner B, call each write RPC with A's short_id, asserting the error is CodeNotFound AND the error message does not contain A's resolved UUID (it contains only B's supplied input). Repeat with B supplying A's UUID directly, asserting the message echoes B's supplied value, not a differently-resolved id. These prove SC4/DEC-xa6 per RPC with no existence leak. Separately, add a lightweight guard for SC5/D-12: rely on the existing idempotency-ban grep gate (Taskfile + ci.yaml) which this plan re-runs, and — if a descriptor-level test already exists from Phase 15 — keep it green; do not weaken or duplicate the gate. Reference the ban by concept in code comments (the acceptance criterion greps the proto, not the test file).
  </action>
  <acceptance_criteria>
    - `go test ./internal/server/... -run TestCrossOwnerRewrap -v` passes with a table per by-id RPC; each asserts CodeNotFound and that the error message excludes the other owner's resolved UUID.
    - The idempotency-ban gate is green: `task lint` exits 0 (the grep gate at Taskfile.yaml + ci.yaml finds no banned idempotency annotation on the six write RPCs) — SC5/D-12.
    - `grep -rn 'idempotency_level' proto/engram/v1/*.proto` shows no write RPC carries the no-side-effects level (the ban holds).
    - `task test` (full suite) and `task lint:go` exit 0.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/server/... -run TestCrossOwnerRewrap -v && task lint</automated>
  </verify>
  <done>Each by-id write RPC has a cross-owner table proving original-input re-wrap with no UUID leak (SC4/D-11); the idempotency-ban gate re-asserts green (SC5/D-12).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Connect client → by-id write RPC error surface | The error message returned to a browser (network-tab visible) must not disclose another owner's resolved UUID |
| proto contract → transport semantics | A write RPC must not advertise itself as side-effect-free (retry/caching hazard) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-02 | Information Disclosure | by-id write RPC ErrNotFound message | high | mitigate | Per-RPC cross-owner table (Task 2) asserts CodeNotFound carries only the caller's supplied input, never the resolved UUID (SC4/D-11/DEC-xa6) |
| T-17-01 | Elevation of Privilege | MCP↔Connect authz parity | high | mitigate | The shared-table parity test (Task 1) proves both lanes reach the same deps.* method and reject identically per RPC (SC2/D-10) — structural, not coincidental |
| T-17-05 | Tampering | write RPC idempotency annotation | high | mitigate | The Phase-15 idempotency-ban gate (Taskfile + ci.yaml) re-asserts green now that real logic exists (SC5/D-12); proto grep confirms no banned annotation |
| T-17-SC | Tampering | dependency supply chain | low | accept | Zero new external packages (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `go test ./internal/server/... -run 'TestWriteParity|TestCrossOwnerRewrap' -v` green.
- `task test` (full suite) + `task lint` (incl. the idempotency-ban gate) + `task lint:go` exit 0.
- `grep -rn 'idempotency_level' proto/engram/v1/*.proto` confirms the ban holds for all six write RPCs.
</verification>

<success_criteria>
- Per-RPC MCP↔Connect parity is proven through the same deps.* code path (SC2/D-10) — REQ-connect-write-authz-parity acceptance gate.
- Each by-id write RPC has a cross-owner re-wrap table with no UUID existence leak (SC4/D-11/DEC-xa6).
- The idempotency-ban gate re-asserts green (SC5/D-12).
- Full suite + all lint gates green — #322 closable.
</success_criteria>

<output>
Create `.planning/phases/17-wired-write-handlers-full-crud-schedule/17-05-SUMMARY.md` when done.
</output>
