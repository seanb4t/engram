---
phase: 17-wired-write-handlers-full-crud-schedule
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/auth/auth.go
  - internal/auth/auth_test.go
  - internal/config/config.go
  - internal/config/config_test.go
  - cmd/engram/serve.go
  - cmd/engram/serve_test.go
  - internal/webauth/oidc.go
autonomous: true
requirements: [REQ-connect-write-authz-parity]

must_haves:
  truths:
    - "ClaimIdentity resolves owner by iterating an ordered claim list; the first non-empty claim value wins (D-04)"
    - "A present-but-unverified email owner-claim REJECTS outright and never falls through to a later claim (D-05)"
    - "A non-email claim value is encoded with a PROVABLY INJECTIVE length-prefixed scheme; an email value stays bare and is guarded against occupying the reserved namespace (D-06 HARDENED per review)"
    - "A single-value ENGRAM_OWNER_CLAIM=email deployment resolves byte-for-byte identically to today (default list [email]); existing email-owned store records need zero migration"
    - "Parsing is separate from defaulting: the registry default supplies `email`; the parser returns an empty list for an explicit/after-trim-empty value; the startup guard rejects the empty list when an auth lane is active"
    - "Malformed claim entries (duplicates, whitespace-only, delimiter-bearing claim names) are validated/rejected, not silently accepted"
  artifacts:
    - internal/auth/auth.go       # ClaimIdentity([]string) + injective namespace encoder + Verifier.ownerClaims
    - internal/config/config.go   # ordered comma-list parser (no defaulting inside the parser)
    - cmd/engram/serve.go         # ownerClaimGuard rejects empty list when auth active + threads the list to both lanes
    - internal/webauth/oidc.go    # cookie-lane ClaimIdentity call site uses the list
  key_links:
    - "ClaimIdentity is the single owner-resolution choke point both auth lanes call — evolving it in place propagates D-04/D-05/D-06 to MCP and cookie lanes with no parallel logic"
    - "The owner string IS the authorization key (compared directly at internal/store/store.go:487 and :509); a non-injective encoding is an AUTHORIZATION COLLISION, so the encoding must be provably injective over (claim,value)"
  prohibitions:
    - "A present-but-unverified email MUST NOT resolve to a later claim in the ordered list (D-05 bypass)"
    - "The non-email owner encoding MUST NOT be the ambiguous `<claim>:<value>` form — `(\"sub\",\"x:y\")` and `(\"sub:x\",\"y\")` collide under it and a collision is an authz collision"
    - "A bare email value that matches the reserved namespace grammar MUST be rejected, not written as-is"
---

<objective>
Evolve the shared `auth.ClaimIdentity` owner resolver from a single claim to an
ordered claim list so authenticated service/machine tokens without an `email`
claim resolve to a stable, namespaced owner instead of failing closed — while
keeping every existing single-value `email` deployment byte-for-byte unchanged.

This is an authorization-key change (owner is the authz key, compared directly in
the store's authz filters). Per the cross-AI review, the original `<claim>:<value>`
D-06 encoding is NOT collision-free and is HARDENED here to a provably injective
length-prefixed scheme, with a guard so a crafted email value cannot occupy the
reserved (non-email) namespace. Three invariants are load-bearing: the
`email_verified` gate (D-05), namespace injectivity/disjointness (D-06 hardened),
and fail-closed empty resolution (D-04).

Purpose: closes the "service token has no email → fails closed" gap and lays the
collision-safe owner-space foundation the six write RPCs (later plans) attribute
records under.
Output: an ordered-list `ClaimIdentity` with an injective encoder, list-valued
config plumbing with parse/default separation, and dedicated D-05/D-06 security
unit tests (including the two adversarial colliding pairs from the review).

Addresses review concerns: D-06 hardening (Codex HIGH), parse/default separation
(Codex MEDIUM), malformed-entry validation (Codex MEDIUM), test/file scope
understated (Codex LOW).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-wired-write-handlers-full-crud-schedule/17-CONTEXT.md
@.planning/phases/17-wired-write-handlers-full-crud-schedule/17-RESEARCH.md
@.planning/phases/17-wired-write-handlers-full-crud-schedule/17-REVIEWS.md
@.planning/phases/17-wired-write-handlers-full-crud-schedule/17-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
</context>

## Artifacts this phase produces (Plan 01)

New/changed symbols this plan introduces (source-grounding pass: exclude these from drift verification):
- `auth.ClaimIdentity(raw map[string]any, ownerClaims []string) (owner, email, username string, err error)` — signature changes from a single `string` to an ordered `[]string`; the owner return is now the INJECTIVE-ENCODED namespaced form for non-email winners.
- A namespace encoder (name at executor discretion, e.g. `namespacedOwner(claim, value string) string`) producing the length-prefixed form `fmt.Sprintf("%d:%s:%d:%s", len(claim), claim, len(value), value)` for non-email claims.
- An email-namespace guard (name at executor discretion) that rejects a bare email value which would occupy the reserved namespace grammar (`^[0-9]+:`).
- `auth.Verifier.ownerClaims []string` — replaces the `ownerClaim string` field.
- `auth.New(ctx, issuer, audience string, ownerClaims []string)` — signature change.
- A config helper (name at executor discretion, e.g. `config.ParseOwnerClaims(string) []string`) that comma-splits + trims `ENGRAM_OWNER_CLAIM` and returns an EMPTY list for an explicit/after-trim-empty value (NO defaulting inside the parser).
- `webauth` authenticator `ownerClaims []string` field + `NewAuthenticator(..., ownerClaims []string)` signature change.
- New auth unit tests: present-but-unverified-email-rejects-never-falls-through (D-05); injectivity across adversarial (claim,value) pairs (D-06); reserved-namespace email guard.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ClaimIdentity ordered-list resolution + email_verified invariant + INJECTIVE namespace encoder</name>
  <files>internal/auth/auth.go, internal/auth/auth_test.go</files>
  <read_first>
    - internal/auth/auth.go (ClaimIdentity 83-97, TokenVerifier 99-146, New 61-73, Verifier struct ~52, identity() ~148)
    - internal/auth/auth_test.go (TestClaimIdentity 128-156, TestTokenVerifierStampsOwnerClaimKey 158-172)
    - internal/store/store.go (owner authz filters at 487 and 509 — the direct owner-string comparison that makes a collision an authz collision; and subject.go Authenticated 43-48 confirming it does NO transformation, just wraps)
    - 17-REVIEWS.md § "17-01" Concerns (the `("sub","x:y")` vs `("sub:x","y")` collision; bare-email-in-reserved-namespace) and Suggestions (length-prefix/base64url; separate parsing from defaulting)
    - 17-RESEARCH.md § "The Caller/Identity Seam" (D-04/D-05/D-06 landing point) and Pitfall 5 (atomic call-site update)
    - 17-PATTERNS.md § "internal/auth/auth.go — ClaimIdentity ordered-list"
  </read_first>
  <behavior>
    - Ordered list [email]: verified email present → owner is the bare email; unchanged from today.
    - Ordered list [email]: email present but email_verified false/absent → error (reject), never "".
    - Ordered list [email, sub]: email absent/empty, sub present → owner is the injective form for ("sub", subvalue) — length-prefixed, NOT "sub:<value>".
    - Ordered list [email, sub]: email present but UNVERIFIED → error (reject); MUST NOT fall through to sub (D-05).
    - Ordered list [sub, email]: sub present → owner is the injective form for ("sub", subvalue); email gate not consulted.
    - INJECTIVITY: encode("sub","x:y") != encode("sub:x","y") — the two review collision pairs produce distinct strings.
    - RESERVED-NAMESPACE GUARD: winning claim is email but its value matches `^[0-9]+:` (could impersonate a namespaced owner) → error (reject), never written bare.
    - Empty/exhausted list (all claims absent) → owner "" with nil error (fail-closed preserved; caller rejects empty downstream).
  </behavior>
  <action>
    Change `ClaimIdentity`'s third parameter from `ownerClaim string` to `ownerClaims []string`. Iterate the list in order. For each claim: if the claim name is "email" AND raw["email"] is a non-empty string, apply the existing email_verified gate — if not a JSON bool true, return the reject error immediately (D-05: never continue to a later claim). If raw[claim] is a non-empty string, that claim WINS. When the winning claim is "email", the owner is the bare value — but FIRST reject it if it matches the reserved-namespace grammar `^[0-9]+:` (a crafted email must not occupy the reserved namespace). When the winning claim is non-email, the owner is the PROVABLY INJECTIVE length-prefixed encoding `fmt.Sprintf("%d:%s:%d:%s", len(claim), claim, len(value), value)` via a small encoder helper — do NOT use the ambiguous `<claim>:<value>` form (the review proves it collides, and a collision is an authz collision because store.go:487/:509 compare owner strings directly). Keep returning `email` and `username` raw (unchanged; identity() still uses them for UserID legibility, D-03). If no claim yields a non-empty value, return owner "" with nil error (fail-closed preserved — the caller treats empty as fatal). Update `Verifier.ownerClaim string` → `ownerClaims []string`; `New(...)` accepts `ownerClaims []string`; `TokenVerifier` calls `ClaimIdentity(raw, v.ownerClaims)`. Update `TestClaimIdentity` and `TestTokenVerifierStampsOwnerClaimKey` to the list signature, and ADD dedicated sub-tests: (a) D-05 unverified-email-never-falls-through (list [email, sub]); (b) D-06 injectivity — assert `ClaimIdentity` under `["sub"]` for value "x:y" and under `["sub:x"]`... use the encoder directly to assert `encode("sub","x:y") != encode("sub:x","y")` plus a decode/round-trip or explicit distinct-string assertion covering the two colliding pairs; (c) the reserved-namespace email guard rejects a `^[0-9]+:` email value; (d) delimiter-containing values (a sub value with embedded colons) still encode injectively. Do NOT touch internal/store/subject.go. Update all in-package call sites in the same commit (Pitfall 5).
  </action>
  <acceptance_criteria>
    - `go test ./internal/auth/... -run TestClaimIdentity -v` passes with the new ordered-list signature.
    - An injectivity sub-test proves the two review collision pairs are distinct: the encoded owner for ("sub","x:y") differs from the encoded owner for ("sub:x","y").
    - A D-05 sub-test proves: `ClaimIdentity(raw{email present, email_verified:false, sub present}, []string{"email","sub"})` returns a non-nil error and owner "" — it does NOT return an encoded sub owner.
    - A guard sub-test proves: an email value matching `^[0-9]+:` (e.g. `"3:sub:1:x"`-shaped) selected via `["email"]` is rejected.
    - `ClaimIdentity(raw{email present+verified}, []string{"email"})` returns the bare email owner (byte-for-byte unchanged from the pre-refactor single-value path — zero store migration).
    - `grep -rn 'ClaimIdentity(' internal/ cmd/` shows every call site passes a `[]string` (no remaining single-string call).
    - `task lint:go` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/auth/... -run 'TestClaimIdentity|TestTokenVerifier' -v && task lint:go</automated>
  </verify>
  <done>ClaimIdentity iterates an ordered claim list with the D-05 email gate and a PROVABLY INJECTIVE D-06 encoder; both lanes inherit the change through the one shared function; the two review collision pairs and the reserved-namespace email guard are covered by green tests.</done>
</task>

<task type="auto">
  <name>Task 2: Comma-list config plumbing with parse/default separation + guard + both call sites</name>
  <files>internal/config/config.go, internal/config/config_test.go, cmd/engram/serve.go, cmd/engram/serve_test.go, internal/webauth/oidc.go</files>
  <read_first>
    - internal/config/config.go (OIDCConfig.OwnerClaim ~122-124 — stays a koanf string)
    - internal/config/config_test.go (~line 136 — the existing owner-claim behavior pinned here; update to the parser + list semantics)
    - internal/config/registry.go (ENGRAM_OWNER_CLAIM registration line 52, Default "email" — the DEFAULTING lives HERE, not in the parser; DO NOT change the key or default)
    - cmd/engram/serve.go (ownerClaimGuard 260-272, auth.New call site 284, webauth.NewAuthenticator call site 152)
    - cmd/engram/serve_test.go (the owner-claim guard/startup tests — update for the list + empty-rejection semantics)
    - internal/webauth/oidc.go (NewAuthenticator + Callback ClaimIdentity call ~line 78; the authenticator's ownerClaim field)
    - 17-REVIEWS.md § "17-01" Suggestions (separate parsing from defaulting: registry default handles unset; parser returns empty for explicit-empty; startup guard rejects empty when auth active)
    - 17-RESEARCH.md § Open Question 3 (reuse singular ENGRAM_OWNER_CLAIM as a comma list)
  </read_first>
  <action>
    Keep `OIDCConfig.OwnerClaim` a plain koanf string and keep the `ENGRAM_OWNER_CLAIM` registry key + `Default: "email"` unchanged — the registry supplies `email` when the var is UNSET (defaulting lives there). Add a parse helper (executor discretion on name/location, e.g. `config.ParseOwnerClaims`) that comma-splits the string, trims whitespace on each element, drops empty elements, DEDUPLICATES while preserving first-seen order, and rejects/normalizes malformed claim names (reject an element that is whitespace-only after trim; reject a claim name containing the reserved delimiters that would break namespace encoding — executor picks a conservative allowlist such as no leading digit-colon and no embedded `:`/`,`). Critically: the parser returns an EMPTY list for an explicit-empty / after-trim-empty input — it does NOT default to `["email"]` (defaulting is the registry's job, so the guard can still observe an operator's explicit `--owner-claim=""`). Document on the helper that `ENGRAM_OWNER_CLAIM` now accepts an ordered comma-separated list (default `email`; e.g. `email,sub` opts into service-token fallback). In `serve.go`, parse `cfg.OIDC.OwnerClaim` once and pass the resulting `[]string` to BOTH `auth.New(...)` and `webauth.NewAuthenticator(...)`. Update `ownerClaimGuard` to take the parsed list: REJECT when the list is empty while an auth lane is active (preserves the existing explicit `--owner-claim=""` rejection at serve.go:250); WARN (not reject) when `email` is absent from the list (its email_verified enforcement then does not apply). Update `webauth.NewAuthenticator`'s signature + authenticator field to `ownerClaims []string` and its `ClaimIdentity(raw, a.ownerClaims)` call. All signature changes land in one commit so the module compiles.
  </action>
  <acceptance_criteria>
    - `go test ./internal/config/... ./internal/webauth/... ./cmd/... -run 'OwnerClaim|Authenticator|Guard' -v` passes; add a table test for the parser: `""` → [] (empty, NOT [email]); `"email"` → [email]; `"email, sub"` → [email, sub]; `",,"` → []; `"email,email"` → [email] (dedup); a whitespace-only or delimiter-bearing malformed element is rejected/dropped per the chosen rule.
    - A serve/guard test proves: an explicitly empty `ENGRAM_OWNER_CLAIM` with an auth lane active is REJECTED at startup (empty parsed list), while an UNSET var resolves via the registry default `[email]` and does NOT warn.
    - `grep -rn 'NewAuthenticator(' cmd/ internal/ && grep -rn 'auth.New(' cmd/` confirm both call sites pass a `[]string` derived from the single ENGRAM_OWNER_CLAIM value.
    - `task lint:go` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/config/... ./internal/webauth/... ./cmd/... && task lint:go</automated>
  </verify>
  <done>ENGRAM_OWNER_CLAIM is parsed once as an ordered comma list with parsing separated from defaulting; malformed entries are validated; the guard rejects an explicit empty list when auth is active; both auth lanes inherit the list; the singular-value default path is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IdP token claims → owner authz key | Untrusted claim values (`email`, `sub`, `client_id`) cross into the resolved owner string that gates every read/write and is compared directly in store authz filters |
| operator config → claim-selection order | `ENGRAM_OWNER_CLAIM` ordering decides which claim (and whether the email gate) governs a request |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-03 | Spoofing/EoP | ClaimIdentity email owner-claim gate | high | mitigate | D-05: a present-but-unverified email rejects outright and never falls through to a later claim; dedicated unit test in Task 1 |
| T-17-04 | Spoofing/EoP (authz-key collision) | ClaimIdentity non-email owner encoding | high | mitigate | D-06 HARDENED: non-email owners use a provably-injective length-prefixed encoding (`%d:%s:%d:%s`) so no two distinct (claim,value) pairs share an owner string; the two review collision pairs `("sub","x:y")`/`("sub:x","y")` are proven distinct by the injectivity unit test — closing the authz-collision surface at store.go:487/:509 |
| T-17-08 | Spoofing/EoP | reserved-namespace email guard | high | mitigate | An email value matching the reserved namespace grammar `^[0-9]+:` is rejected, so a crafted email cannot masquerade as a namespaced service owner; guard unit test in Task 1 |
| T-17-SC | Tampering | dependency supply chain | low | accept | Zero new external packages this plan (RESEARCH Package Legitimacy Audit: N/A) — no npm/pip/cargo installs |
</threat_model>

<verification>
- `go test ./internal/auth/... ./internal/config/... ./internal/webauth/... ./cmd/...` green.
- `task lint:go` exits 0 (wide-signature change must not leave revive/unused findings — Phase-15 executor blind spot).
- D-05, D-06-injectivity, and the reserved-namespace guard each have a dedicated failing-if-broken unit test, including the two adversarial collision pairs from the review.
</verification>

<success_criteria>
- Ordered-list `ClaimIdentity` with the email_verified hard invariant (D-05) and a PROVABLY INJECTIVE non-email owner encoding (D-06 hardened) is live on both auth lanes through the one shared function.
- Single-value `email` deployments are byte-for-byte unchanged (default list `[email]`, zero store migration).
- Parsing is separated from defaulting; an explicit empty list is rejected at startup when auth is active; malformed entries are validated.
- Full suite + `task lint:go` green.
</success_criteria>

<output>
Create `.planning/phases/17-wired-write-handlers-full-crud-schedule/17-01-SUMMARY.md` when done.
</output>
