---
phase: 17-wired-write-handlers-full-crud-schedule
plan: 04
type: execute
wave: 3
depends_on: ["17-03", "17-06"]
files_modified:
  - internal/server/connecterror.go
  - internal/server/connectapi.go
  - internal/server/fakestore_test.go
  - internal/server/connectapi_negative_test.go
  - internal/server/connectcsrf_test.go
autonomous: true
requirements: [REQ-connect-write-authz-parity]

must_haves:
  truths:
    - "A single production connectError(ctx, err) mapper lives in the Connect handler layer: not-found->CodeNotFound, store.ErrInvalidArgument (incl parseWindow past-not_after + validateRuleSummary, wrapped in 17-02)->CodeInvalidArgument, rule-mutation (errRuleImmutable)->CodeFailedPrecondition, stale-summary (errStaleSummary)->CodeFailedPrecondition, ambiguous-short-id (store.ErrAmbiguousShortID, from ResolvePointID on legacy duplicate handles, store.go:1217)->CodeFailedPrecondition (NOT the generic CodeInternal — round-3 MED-5), unexpected->CodeInternal — matching TYPED sentinels, not strings (review HIGH). The mapper takes ctx so the CodeInternal branch logs request-scoped trace/log fields (review LOW). NOTE (round-5 LOW, Codex): there is NO CodeAborted mapping — the current sentinel set is `ErrNotFound`/`ErrInvalidArgument`/`ErrAmbiguousShortID` (store.go:42-56) + `errStaleSummary` (summary.go:14-18), and NO distinct concurrency/edit-conflict sentinel exists today; a CodeAborted arm is added ONLY IF a distinct conflict sentinel is later introduced (out of scope here). This must-have does NOT assert an unconditional CodeAborted mapping."
    - "Each of the six Connect write RPCs is a real thin method on engramAPI: resolve caller -> protoconv -> the SAME deps.* method the MCP tool calls -> protoconv result -> connectError; never store.* directly (SC2)"
    - "The Connect read handlers call the 17-06 typed core deps read methods (caller-threaded) preserving every Connect field (offset, categories, visibility, exact total, cursor) and the Connect k=20 default; SearchDiscoveries applies k=20 in the adapter BEFORE calling deps.searchDiscovery (whose internal MCP default is 8) so the Connect default does not regress 20->8 (review finding 7); ListScopes is the one documented exception (no MCP counterpart, D-07)"
    - "The test fake is a SCRIPTED SPY implementing memStore that records which method was invoked with which subject/args (for 17-05 delegation parity), returns scripted results/sentinels, and is backed by a non-nil embedder so valid StoreMemory/StoreDiscovery/ScheduleMemory reach d.em.Embed (review HIGH)"
    - "TestWriteRPCNegativeMatrix constructs a non-nil (spy-backed) deps and no longer expects CodeUnimplemented for authenticated-valid cells (landmine 1)"
    - "TestConnectCSRFTokenMatrix (connectcsrf_test.go) is fixed in the SAME plan as the handler wiring (round-5 HIGH, Codex+grok — a permanent Phase-16 gate): it constructs `d := &deps{}` (connectcsrf_test.go:225) and its happy-path 'matching cookie and header' cell expects `want: connect.CodeUnimplemented` (:250); once StoreMemory is wired a CSRF-passing request reaches nil `d.em`/`d.st` and panics or returns a non-Unimplemented result, reddening CI in the same commit that wires handlers. The matrix is re-pointed at the spy-backed deps (non-nil store + non-nil embedder, Task 1) and its happy-path cell's `want` flips off CodeUnimplemented to a REAL outcome (a scripted success, or an assertion that the code is NOT in {PermissionDenied, Unimplemented} if only CSRF pass-through is under test) (Task 2). The other CSRF matrices (`TestNoAnonymousWrite` :194, `TestConnectCSRFInterceptor_EmptyOwner` :372) are rejected by the CSRF interceptor BEFORE the handler runs, so they never reach the store and stay valid unchanged"
    - "On the Search read rewire, the handler-local `a.d.em.EmbedQuery` calls (connectapi.go:158 in SearchMemories, :220 in SearchDiscoveries) are DELETED, not merely bypassed: the rewired handlers delegate to `deps.searchMemory`/`deps.searchDiscovery`, which embed the query INTERNALLY, so leaving the handler-local embed would double-embed / leave a dead vector (round-5 MED, grok). After the rewire only `deps.*` embeds on the Search lanes; connectapi.go has zero handler-local query-embed calls"
    - "Connect SearchDiscoveries maps an EMPTY request scope to `CrossSpine: true` (while preserving the caller's `Scope`) so an empty-scope Connect discovery search still spans ALL discovery scopes — today's Connect handler passes empty scope straight to Store.SearchDiscovery (empty = all, store.go:688) but the shared deps.searchDiscovery REJECTS empty scope via effectiveDiscoveryScope unless CrossSpine=true (tools.go:884-892), so the D-07 rewire would regress cross-scope discovery search without this mapping (round-4 HIGH-3); the proto does not require a non-empty scope"
    - "After the GetMemory D-07 rewire, `deps.getMemory` is the SOLE usage-enqueue point: the Connect GetMemory handler's own `a.d.usageQueue.tryEnqueue(pid)` (connectapi.go:211) is REMOVED because deps.getMemory already enqueues on a successful fetch (tools.go:1000-1003) — keeping both would double-increment AccessCount on every Connect get; AccessCount increments EXACTLY ONCE per Connect get (round-4 MED, consensus Codex+grok)"
    - "The Connect read lane does NOT re-introduce a Limit==0 default: after 17-06 removes the shared `Limit==0 -> 20` from the list core, the Connect ListMemories adapter passes `limit` through unchanged so a Connect `limit=0` still means 'all' (store.go:873-874), NOT silently capped to 20 (round-4 finding-7); each transport's created_after/before is parsed to `time.Time` AT THE CONNECT BOUNDARY returning CodeInvalidArgument on a bad value, so a parse error never enters the typed core to be misclassified as CodeInternal (round-4 MED-6)"
  artifacts:
    - internal/server/connecterror.go     # the single production error mapper
    - internal/server/connectapi.go        # six write methods + read handlers rewired to the typed core deps
    - internal/server/fakestore_test.go     # scripted spy memStore + non-nil embedder wiring
    - internal/server/connectapi_negative_test.go  # nil-store fix + real-outcome expectations
  key_links:
    - "connectError is the ONE place business rejections become Connect codes — it must live in production (not be re-implemented as a test-only oracle in 17-05)"
    - "Wiring the six methods flips the negative-matrix authenticated-valid cells off CodeUnimplemented — the nil-store fix MUST land in the same plan or CI redlines mid-phase (landmine 1)"
    - "The spy fake is the 17-05 delegation-parity substitution point; recording method+subject is what proves both lanes hit the SAME deps.* method (not merely the same code)"
  prohibitions:
    - "Connect write handlers MUST NOT call store.* (a.d.st.*) directly except the documented ListScopes exception — only deps.* (SC2/D-07); no ownership comparison in any handler (DEC-cgb)"
    - "The six write handlers MUST NOT re-implement the ErrNotFound original-input re-wrap — deps.* already returns it; the handler only calls connectError (D-11)"
    - "connectError MUST NOT be duplicated as a test-only oracle in 17-05 (that only proves two mappings agree, not that the handler delegated) — 17-05 asserts via the spy"
    - "connectError MUST NOT surface raw store/embed internals for CodeInternal — log the underlying error and return a generic message"
---

<objective>
Wire the six Connect write RPCs and rewire the read handlers onto the shared,
typed `deps.*` core, backed by a scripted-spy fake store and a single production
error mapper. Today `engramAPI` has ZERO methods for the six write RPCs (they fall
through to `UnimplementedEngramServiceHandler`); this plan authors six real thin
adapters. The read handlers move off direct `a.d.st.*` onto the 17-06 typed core
read methods, closing Pitfall 1 for reads AND writes without regressing the Connect
read contract.

Four review fixes are load-bearing (round-1 + round-2):
- **Production connectError mapper (HIGH):** the original `ErrNotFound`+
  `ErrInvalidArgument`-only mapping sent expected business failures (rule
  immutability, stale-summary/concurrency conflicts) to `CodeInternal`. A single
  `connectError(ctx, err)` mapper in the handler layer maps typed sentinels to
  `CodeNotFound` / `CodeInvalidArgument` / `CodeFailedPrecondition`
  / `CodeInternal` (no `CodeAborted` arm — no distinct conflict sentinel exists
  today, round-5 LOW). Round-2: the mapper takes `ctx` so the CodeInternal branch logs
  request-scoped fields (finding LOW); and because 17-02 now wraps `parseWindow`
  (past `not_after`, ordering) and `validateRuleSummary` with `store.ErrInvalidArgument`,
  those semantic failures map to `CodeInvalidArgument` not `CodeInternal` (finding 5).
- **SearchDiscoveries k=20 (finding 7):** the read rewire must pre-apply the Connect
  k=20 default in the SearchDiscoveries adapter before calling `deps.searchDiscovery`
  (whose internal default is the MCP 8) — the same one-path-missed class as the
  round-1 SearchMemories k fix.
- **Scripted-spy fake (HIGH):** the fake is a spy that records method+subject/args
  (for 17-05 delegation parity) rather than reimplementing full store authz (which
  drifts). The real Qdrant isolation suite stays the authz gate. The spy-backed
  deps also needs a non-nil embedder (valid StoreMemory/StoreDiscovery/
  ScheduleMemory reach `d.em.Embed`).
- **Read rewire without regression (HIGH):** Connect read handlers build the 17-06
  typed core superset (all fields + Connect k=20) — no dropped field.

Landmine 1 is pre-empted: `TestWriteRPCNegativeMatrix` builds `d := &deps{}` with a
nil store — safe only while the writes are Unimplemented. The spy fake + the
negative-test fix land in THIS plan alongside the handler bodies.

Addresses review concerns: connectError mapper (HIGH), by-id result mapping usage
(HIGH), read rewire regression (HIGH, via 17-06), spy fake + non-nil embedder (HIGH);
**round-2: connectError(ctx, err) signature (LOW), store.ErrInvalidArgument mapping
covers parseWindow/rule-summary (finding 5), SearchDiscoveries k=20 (finding 7)**.

**Round-3 review (this revision):** connectError ALSO maps `store.ErrAmbiguousShortID`
(returned by ResolvePointID on legacy duplicate short-id handles) to
`CodeFailedPrecondition` rather than the generic `CodeInternal`, with a dedicated
table case (MED-5). The ctx-threaded `slog.ErrorContext` CodeInternal logging (LOW-8)
is already in place from round-2 — no further change.

**Round-4 review (this revision):** the read-lane rewire (Task 3) closes three
newly-surfaced defects, all localized to the rewire adapters. (1) HIGH-3 — the
Connect `SearchDiscoveries` adapter now maps an EMPTY request scope to
`CrossSpine: true` (preserving the caller's `Scope`); today's handler passes empty
scope straight to `Store.SearchDiscovery` (empty = all, store.go:688) but the shared
`deps.searchDiscovery` rejects empty scope unless CrossSpine (tools.go:884-892), so
the rewire would silently break cross-scope discovery search without it. (2) MED —
the Connect `GetMemory` handler REMOVES its own `usageQueue.tryEnqueue` (connectapi.go:211)
after rewiring to `deps.getMemory` (which already enqueues, tools.go:1000-1003), so
AccessCount increments exactly once, not twice. (3) finding-7/MED-6 — the Connect
ListMemories adapter passes `limit=0` through as 'all' (17-06 removes the shared
`limit==0 -> 20` default) and parses `created_after/before` to `time.Time` at the
Connect boundary returning CodeInvalidArgument, so a parse error never enters the
typed core to be misclassified as CodeInternal. No round-1/2/3 fix is regressed.

**Round-5 review (this revision):** three fixes, no design change. (1) HIGH (Codex+grok,
consensus) — the permanent Phase-16 CSRF gate `TestConnectCSRFTokenMatrix` (connectcsrf_test.go)
constructs `d := &deps{}` (:225) and its happy-path cell expects `CodeUnimplemented` (:250);
once StoreMemory is wired, a CSRF-passing request reaches the nil store/embedder and reddens
CI in the SAME commit as the wiring. `connectcsrf_test.go` is now in `files_modified`; Task 1
re-points the matrix at the spy-backed deps and Task 2 flips the happy-path cell off
CodeUnimplemented to a real outcome. (`TestNoAnonymousWrite`/`TestConnectCSRFInterceptor_EmptyOwner`
are rejected by CSRF before the handler and stay valid.) (2) MED (grok) — the Search read
rewire DELETES the handler-local `a.d.em.EmbedQuery` (connectapi.go:158/:220) because
deps.searchMemory/searchDiscovery embed internally; leaving it double-embeds. Task 3 adds the
explicit deletion + a zero-count grep gate. (3) LOW (Codex) — the unconditional `CodeAborted`
must-have is dropped: no distinct concurrency/edit-conflict sentinel exists today (store.go:42-56
+ summary.go:14-18), so connectError has NO CodeAborted arm; one is added only if such a
sentinel is later introduced. No round-1/2/3/4 fix is regressed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-wired-write-handlers-full-crud-schedule/17-CONTEXT.md
@.planning/phases/17-wired-write-handlers-full-crud-schedule/17-RESEARCH.md
@.planning/phases/17-wired-write-handlers-full-crud-schedule/17-REVIEWS.md
@.planning/phases/17-wired-write-handlers-full-crud-schedule/17-PATTERNS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/TESTING.md
</context>

## Artifacts this phase produces (Plan 04)

New/changed symbols this plan introduces (source-grounding pass: exclude these from drift verification):
- `internal/server/connecterror.go` — `connectError(ctx context.Context, err error) error` (name at executor discretion): the single production mapper from typed domain errors to Connect codes; ctx feeds request-scoped fields to the CodeInternal slog line (review LOW).
- Six new methods on `*engramAPI`: `StoreMemory`, `StoreDiscovery`, `UpdateMemory`, `DeleteMemory`, `SetVisibility`, `ScheduleMemory` (signatures matching the generated `engramv1connect` interface).
- Rewired bodies for `ListMemories`, `SearchMemories`, `GetMemory`, `SearchDiscoveries` (now calling the 17-06 typed core deps read methods with the Connect superset; `SearchMemories` and `SearchDiscoveries` each apply their Connect k=20 default in the adapter); `ListScopes` left calling `a.d.st.ListScopes` as a documented exception.
- `internal/server/fakestore_test.go` — a scripted-spy `memStore` implementation (name at executor discretion, e.g. `spyStore`) recording invoked method + subject + args and returning scripted results/sentinels; a non-nil test embedder wired into the spy-backed `deps`.
- Updated `TestWriteRPCNegativeMatrix` (non-nil spy store; authenticated-valid cells expect real outcomes).
- Updated `internal/server/connectcsrf_test.go` `TestConnectCSRFTokenMatrix` (round-5 HIGH, Codex+grok): the matrix's `d := &deps{}` (connectcsrf_test.go:225) is re-pointed at the spy-backed deps (Task 1) and its happy-path cell's expected code flips off `CodeUnimplemented` to a real outcome (Task 2), so the permanent Phase-16 CSRF gate does not redline CI when handlers wire up.

<tasks>

<task type="auto">
  <name>Task 1: Production connectError mapper + scripted-spy fake (non-nil embedder) + nil-store negative-matrix + CSRF-matrix fix (landmine 1)</name>
  <files>internal/server/connecterror.go, internal/server/fakestore_test.go, internal/server/connectapi_negative_test.go, internal/server/connectcsrf_test.go</files>
  <read_first>
    - internal/server/store_iface.go (the memStore interface from 17-02 — the exact surface the spy implements, incl DeleteAll/ListScopes/payload method)
    - internal/server/identity.go (the typed sentinels errRuleImmutable/errStaleSummary + mutationResult from 17-02)
    - internal/store/store.go (ErrNotFound 44, ErrInvalidArgument 51 — the store sentinels the mapper matches; ErrAmbiguousShortID 56, returned by ResolvePointID at :1217 on legacy duplicate short-id handles — the mapper matches it to CodeFailedPrecondition, round-3 MED-5)
    - internal/server/tools.go (the deps.* write methods now returning typed sentinels; d.em.Embed reached by storeMemory 641 / scheduleMemory 681 / storeDiscovery 739 — why the spy-backed deps needs a non-nil embedder)
    - internal/server/tools_test.go (testDeps 192-214 — the deps{} wiring incl the embedder field to mirror; the fake embedder used in tests)
    - internal/server/connectapi_negative_test.go (TestWriteRPCNegativeMatrix, d := &deps{} at line 64; callWrite helper; CSRF cookie/header auto-attach)
    - internal/server/connectcsrf_test.go (TestConnectCSRFTokenMatrix 224-276 — `d := &deps{}` at :225; the happy-path 'matching cookie and header' cell at :247-251 expects `want: connect.CodeUnimplemented`; it drives StoreMemory (`csrfWriteCases(...)[0].call`, :236) over the REAL interceptor chain via `mountConnect` with `csrfStubResolve`+`csrfTestVerify`, so a CSRF-passing request reaches the handler — round-5 HIGH, Codex+grok. NOTE: `TestNoAnonymousWrite` :193-217 and `TestConnectCSRFInterceptor_EmptyOwner` :371+ also use `d := &deps{}` but are rejected by the CSRF interceptor BEFORE the handler, so they never touch the store and DO NOT need re-pointing — only `TestConnectCSRFTokenMatrix`'s happy-path cell reaches the (soon-real) handler)
    - internal/server/connectapi.go (GetMemory 183-213 — how an existing handler maps ErrNotFound/ErrInvalidArgument to codes; the shape connectError generalizes)
    - 17-REVIEWS.md § Consensus "[HIGH] Connect error mapping" + § disposition items 2 and 7 (spy fake, non-nil embedder, keep real Qdrant isolation suite)
    - 17-PATTERNS.md § "internal/server/fakestore_test.go" and § "connectapi_negative_test.go — fix nil-store panic"
  </read_first>
  <action>
    Create `internal/server/connecterror.go` (Apache-2.0 SPDX): a single `connectError(ctx context.Context, err error) error` mapping — nil->nil; `errors.Is(err, store.ErrNotFound)`->CodeNotFound; `errors.Is(err, store.ErrInvalidArgument)`->CodeInvalidArgument (this is what makes 17-02's wrapped parseWindow past-`not_after`/ordering rejections and validateRuleSummary rejections map correctly — finding 5); `errors.Is(err, errRuleImmutable)`->CodeFailedPrecondition; `errors.Is(err, errStaleSummary)`->CodeFailedPrecondition; `errors.Is(err, store.ErrAmbiguousShortID)`->CodeFailedPrecondition (a legacy-data precondition — a short_id matched >1 record via ResolvePointID at store.go:1217; NOT the generic CodeInternal — round-3 MED-5); else->CodeInternal. Take `ctx` as the first param (review LOW) so the CodeInternal branch logs with `slog.ErrorContext(ctx, ...)` (request-scoped trace/log fields) and returns a GENERIC message so raw store/embed internals never reach the client; the already-client-safe business messages (from deps, carrying the caller's original input) pass through for the mapped codes. Then create `fakestore_test.go` (Apache-2.0 SPDX): a scripted-spy type implementing every memStore method (incl DeleteAll/ListScopes/payload), map-backed for the minimal state the scenarios need, that RECORDS the invoked method name + subject + key args (a slice/struct the 17-05 parity test reads) and returns scripted results or scripted sentinels per test setup — do NOT reimplement full store authz (the real Qdrant isolation suite remains the authz gate; note this in a comment). Wire a spy-backed `deps` with a NON-NIL test embedder (reuse the fake embedder from tools_test.go) so valid StoreMemory/StoreDiscovery/ScheduleMemory reach `d.em.Embed` without panicking. Fix `TestWriteRPCNegativeMatrix`: replace `d := &deps{}` with a deps constructed over the spy store + non-nil embedder so the soon-to-be-real handlers do not nil-panic (the authenticated-valid expected codes flip in Task 2 alongside the handler bodies, in the same commit sequence). Round-5 HIGH (Codex+grok): APPLY THE SAME nil-store fix to `TestConnectCSRFTokenMatrix` in `connectcsrf_test.go` — replace its `d := &deps{}` (:225) with the SAME spy-backed deps (non-nil store + non-nil embedder) so the happy-path 'matching cookie and header' cell (which passes CSRF and reaches the soon-real StoreMemory handler) does not nil-panic once handlers are wired. Leave the cell's expected `want` value alone in THIS task (it flips off `CodeUnimplemented` in Task 2, same as the negative matrix). Do NOT change `TestNoAnonymousWrite` or `TestConnectCSRFInterceptor_EmptyOwner` — the CSRF interceptor rejects those before the handler, so their `d := &deps{}` is never dereferenced by a wired handler and they remain valid.
  </action>
  <acceptance_criteria>
    - `internal/server/connecterror.go` exists with the SPDX header and a `connectError(ctx, err)` signature; a table-test asserts each mapping: ErrNotFound->CodeNotFound, ErrInvalidArgument->CodeInvalidArgument, errRuleImmutable->CodeFailedPrecondition, errStaleSummary->CodeFailedPrecondition, store.ErrAmbiguousShortID->CodeFailedPrecondition (round-3 MED-5 — NOT CodeInternal), an arbitrary error->CodeInternal with a generic (non-leaking) message. There is NO CodeAborted arm (round-5 LOW — no distinct conflict sentinel exists today; add one only if such a sentinel is later introduced). The table INCLUDES a wrapped-`store.ErrInvalidArgument` case standing in for a past-`not_after` (finding 5) asserting CodeInvalidArgument, and a wrapped-`store.ErrInvalidArgument` rule-summary case.
    - `fakestore_test.go` exists with a compile assertion `var _ memStore = (*spyStore)(nil)` (or chosen name); a unit test asserts the spy records the method name + subject after a call.
    - The spy-backed deps carries a non-nil embedder; a valid StoreMemory-shaped deps.storeMemory call over the spy does not nil-panic on d.em.Embed.
    - `go test ./internal/server/... -run 'TestWriteRPCNegativeMatrix|TestConnectError|TestSpyStore'` compiles and does not nil-panic.
    - `TestConnectCSRFTokenMatrix` is re-pointed at the spy-backed deps (the `TestConnectCSRFTokenMatrix` body constructs the spy-backed deps helper instead of a bare `&deps{}`; `TestNoAnonymousWrite` and `TestConnectCSRFInterceptor_EmptyOwner` MAY keep `&deps{}` since the interceptor rejects them before the handler). `go test ./internal/server/... -run 'TestConnectCSRFTokenMatrix'` compiles and does not nil-panic (the happy-path cell's `want` still flips in Task 2).
    - `task lint:go` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/server/... -run 'TestWriteRPCNegativeMatrix|TestConnectError|TestSpyStore|TestConnectCSRFTokenMatrix' && task lint:go</automated>
  </verify>
  <done>A single production connectError mapper (typed sentinels, no leak) exists; a scripted-spy memStore records method+subject and is backed by a non-nil embedder; the negative matrix constructs a non-nil store (landmine 1 defused).</done>
</task>

<task type="auto">
  <name>Task 2: Six Connect write handler methods (thin adapters) + connectError + updated negative matrix + CSRF happy-path cell</name>
  <files>internal/server/connectapi.go, internal/server/connectapi_negative_test.go, internal/server/connectcsrf_test.go</files>
  <read_first>
    - internal/server/connectapi.go (GetMemory 183-213 — the thin-adapter template; mountConnect 241; interceptor chain 262-267)
    - internal/server/connecterror.go (the mapper from Task 1)
    - internal/server/identity.go (callerFromConnectContext + mutationResult from 17-02)
    - internal/server/protoconv.go (the 17-03 request mappers + mutationResult->response mappers to call)
    - internal/server/tools.go (deps.storeMemory/scheduleMemory/storeDiscovery/updateMemory/deleteMemory/setVisibility — caller-threaded; updateMemory/setVisibility now return mutationResult; their ErrNotFound re-wrap already returns the caller's original input)
    - internal/server/connectapi_negative_test.go (authenticated-valid cells currently asserting CodeUnimplemented ~line 169)
    - internal/server/connectcsrf_test.go (TestConnectCSRFTokenMatrix — the happy-path 'matching cookie and header' cell at :247-251, `want: connect.CodeUnimplemented` at :250, drives StoreMemory over the spy-backed deps re-pointed in Task 1; this is the cell to flip to a real outcome — round-5 HIGH, Codex+grok)
    - proto/engram/v1/engram.proto (the six write request/response messages; DeleteMemoryResponse empty)
    - 17-REVIEWS.md § disposition item 2 (mapper) + item 3 (by-id results); § "17-04" (thin adapter, no re-gate)
    - 17-PATTERNS.md § "internal/server/connectapi.go — six new write handlers"
  </read_first>
  <action>
    Add six real methods on `*engramAPI` (StoreMemory, StoreDiscovery, UpdateMemory, DeleteMemory, SetVisibility, ScheduleMemory) with the generated Connect signatures. Each body: resolve the caller via `callerFromConnectContext(ctx)` (map its error to CodeUnauthenticated as GetMemory does); convert `req.Msg` to args via the 17-03 protoconv mappers; call the SAME `deps.*` method the MCP tool calls, passing the caller; on error return `connectError(ctx, err)` (the single mapper — DO NOT hand-roll per-handler code mapping, DO NOT re-wrap ErrNotFound: deps already carries the original input — D-11); on success convert the result to the proto response via protoconv — for UpdateMemory/SetVisibility map the returned `mutationResult` (id+short_id); for StoreMemory/ScheduleMemory/StoreDiscovery map the (id, short_id); DeleteMemory returns the empty response. No ownership comparison anywhere (DEC-cgb). Then update `TestWriteRPCNegativeMatrix`'s authenticated-valid cells to expect the real outcomes (success or the scenario's mapped rejection code) instead of CodeUnimplemented, using the non-nil spy store from Task 1. Round-5 HIGH (Codex+grok): ALSO flip `TestConnectCSRFTokenMatrix`'s happy-path 'matching cookie and header' cell (connectcsrf_test.go:247-251) OFF its stale `want: connect.CodeUnimplemented` — now that StoreMemory is a real handler and the matrix runs over the Task-1 spy-backed deps (non-nil store + embedder), a CSRF-passing StoreMemory reaches the real handler and returns a REAL outcome. Set the cell to the scripted-success outcome the spy produces for that StoreMemory input (prefer asserting the concrete success code), OR — if this matrix is only meant to prove CSRF PASS-THROUGH rather than the full store outcome — assert the returned code is NOT in {`connect.CodePermissionDenied`, `connect.CodeUnimplemented`} (CSRF passed AND the request reached a wired handler). Update the cell's comment to match (it no longer 'reaches the still-stub handler'). Leave the three rejection cells (missing header, cookie!=header, different-owner cookie) asserting `CodePermissionDenied` unchanged — CSRF still rejects those before the handler.
  </action>
  <acceptance_criteria>
    - `grep -cE 'func \(a \*engramAPI\) (StoreMemory|StoreDiscovery|UpdateMemory|DeleteMemory|SetVisibility|ScheduleMemory)\(' internal/server/connectapi.go` returns 6.
    - `grep -nE 'a\.d\.st\.' internal/server/connectapi.go | grep -v 'ListScopes'` returns no matches inside the six write handlers (only the documented ListScopes exception references a.d.st) — SC2.
    - Each write handler calls `connectError(ctx,` for its error return (grep confirms no per-handler `connect.NewError(connect.CodeInternal` bespoke fall-through in the six methods).
    - UpdateMemory/SetVisibility responses carry the canonical id + short_id from the mutationResult (a test asserts the response id is the canonical UUID even when the caller supplied a short_id).
    - `grep -n 'CodeUnimplemented' internal/server/connectapi_negative_test.go` no longer marks the six authenticated-valid write cells.
    - `TestConnectCSRFTokenMatrix`'s happy-path 'matching cookie and header' cell no longer asserts `connect.CodeUnimplemented`: it asserts the real spy-backed StoreMemory outcome (a success code) or that the returned code is NOT in {PermissionDenied, Unimplemented}; `go test ./internal/server/... -run 'TestConnectCSRFTokenMatrix'` passes with the wired handler (the permanent Phase-16 CSRF gate is green in the same commit sequence as the wiring — round-5 HIGH). The three rejection cells still assert `CodePermissionDenied`.
    - A behavior test creates a memory over Connect StoreMemory and reads it back (GetMemory/List) asserting the content is present (SC3).
    - `go test ./internal/server/...` passes; `task lint:go` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/server/... && task lint:go</automated>
  </verify>
  <done>The six write RPCs are thin adapters delegating to deps.* via protoconv + the single connectError mapper; by-id responses carry the fetched id+short_id; the negative matrix reflects real mapped outcomes.</done>
</task>

<task type="auto">
  <name>Task 3: Rewire the Connect read handlers onto the 17-06 typed core (no field dropped, k=20 preserved)</name>
  <files>internal/server/connectapi.go</files>
  <read_first>
    - internal/server/connectapi.go (ListMemories 104-151, SearchMemories 153-181, GetMemory 183-213, SearchDiscoveries 215-233 — all calling a.d.st.* today; the SearchDiscoveries k=20 default at 224-228; ListScopes 88-102; the SearchMemories k=20 default at 162; the full ListMemories field set at 124-138 + Total/NextPageToken at 145-150; the handler-local `a.d.em.EmbedQuery(ctx, req.Msg.Query)` calls at :158 (SearchMemories) and :220 (SearchDiscoveries) — these MUST be DELETED on the rewire because deps.searchMemory/searchDiscovery embed the query INTERNALLY, so keeping them double-embeds, round-5 MED grok)
    - internal/server/tools.go (the 17-06 typed core deps read methods: deps.listMemory(coreListRequest)->coreListResult, deps.searchMemory(coreSearchRequest)->[]store.Memory, getMemory, searchDiscovery — caller-threaded; NOTE deps.searchDiscovery retains its internal MCP k=8 default at 904-906, so the Connect adapter MUST pre-set k=20; `effectiveDiscoveryScope` 884-892 REJECTS an empty scope unless `CrossSpine=true` — the Connect adapter MUST map empty scope -> CrossSpine=true, round-4 HIGH-3; deps.getMemory enqueues a usage event on success at tools.go:1000-1003 — the SOLE enqueue point after the rewire)
    - internal/server/connectapi.go (SearchDiscoveries 215-233 — today passes `req.Msg.Scope` (empty = all) straight to `a.d.st.SearchDiscovery` at :228 with k default 20 at :224-227; GetMemory 183-213 — its handler-level `a.d.usageQueue.tryEnqueue(pid)` at :211 that MUST be removed once GetMemory rewires to deps.getMemory, round-4 MED)
    - internal/store/store.go (SearchDiscovery empty-scope = all discovery scopes at store.go:688; List treats `Limit==0` as 'all' at store.go:873-874 — why the Connect lane must leave limit=0 as 'all' after 17-06 removes the shared default, round-4 finding-7)
    - internal/server/identity.go (callerFromConnectContext)
    - internal/server/connectapi_cookie_test.go (TestConnectCookieLaneIsolation 27-90 — must stay green after the rewire)
    - 17-REVIEWS.md § "17-04" HIGH (offset/categories/visibility/total/cursor must survive; k default must stay 20) + § disposition items 5 and 7 (SearchDiscoveries k=20 regression — the same class as the round-1 SearchMemories fix, one path missed)
    - 17-RESEARCH.md § "Connect Write-Handler Wiring (D-07 reads too)" (the ListScopes OQ2 exception)
  </read_first>
  <action>
    Rewire the four read handlers with a deps counterpart onto the 17-06 typed core: ListMemories builds a `coreListRequest` carrying EVERY current field (Scope, Limit, Offset, Categories, Visibility, Tags, CreatedAfter/Before, PageToken as Cursor, and CursorMode = req.CursorMode || PageToken != "") and maps the returned `coreListResult` to the proto response INCLUDING the exact `Total` and `NextPageToken` (no field dropped); SearchMemories applies the Connect k=20 default (k==0->20) BEFORE building the `coreSearchRequest`, then shapes the returned `[]store.Memory` via the existing `shapeProtoMemories`, and DELETES its handler-local `vec, err := a.d.em.EmbedQuery(ctx, req.Msg.Query)` embed (connectapi.go:158) — `deps.searchMemory` embeds the query INTERNALLY (proven by `embed_wiring_test.go`'s `TestSearchMemoryUsesEmbedQuery`), so keeping the handler embed would double-embed / strand a dead vector; after the rewire ONLY `deps.*` embeds on the Search lanes (round-5 MED, grok); GetMemory -> deps.getMemory, and REMOVE the handler-level `a.d.usageQueue.tryEnqueue(pid)` (connectapi.go:211): deps.getMemory already enqueues a usage event on a successful fetch (tools.go:1000-1003), so after the rewire deps.getMemory is the SOLE enqueue point — keeping BOTH would double-increment AccessCount on every Connect get (round-4 MED, consensus Codex+grok). SearchDiscoveries -> deps.searchDiscovery, building `searchDiscoveryArgs` that (a) PRESERVE the caller's `Scope` (`req.Msg.Scope`), (b) set `CrossSpine: req.Msg.Scope == ""` so an empty Connect scope still spans ALL discovery scopes — today's Connect handler passes an empty scope straight to `Store.SearchDiscovery` (empty = all discovery scopes, store.go:688), but the shared `deps.searchDiscovery` REJECTS an empty scope via `effectiveDiscoveryScope` (tools.go:884-892) unless `CrossSpine=true`, so without this mapping the rewire regresses cross-scope discovery search (round-4 HIGH-3; the proto does not require a non-empty scope), and (c) apply the Connect k=20 default (k==0->20) in the adapter BEFORE the call (review finding 7 — deps.searchDiscovery's internal default is the MCP 8, so without this pre-set the Connect discovery-search default silently regresses 20->8; this mirrors the round-1 SearchMemories fix), and DELETES its handler-local `vec, err := a.d.em.EmbedQuery(ctx, req.Msg.Query)` embed (connectapi.go:220) — `deps.searchDiscovery` embeds the query internally, so keeping the handler embed double-embeds (round-5 MED, grok). Each resolves the caller via `callerFromConnectContext(ctx)` and returns errors via `connectError(ctx, err)` (the ErrNotFound re-wrap already lives inside deps.getMemory — do NOT duplicate it). Leave `ListScopes` calling `a.d.st.ListScopes` unchanged with a one-line comment documenting it as the intentional D-07 exception (no MCP-side deps.listScopes counterpart; read-only scope-count listing — research OQ2). The Connect ListMemories adapter passes `req.Msg.Limit` THROUGH unchanged (do NOT re-introduce a `limit==0 -> 20` default: 17-06 removes that from the shared core, and the store treats `Limit==0` as 'all' at store.go:873-874, so a Connect `limit=0` must still mean 'all', NOT be silently capped to 20 — round-4 finding-7). Parse `created_after`/`created_before` to `time.Time` AT THE CONNECT BOUNDARY and return `connect.NewError(connect.CodeInvalidArgument, ...)` on a bad value BEFORE building the coreListRequest (round-4 MED-6: the 17-06 typed core takes `time.Time`, so the transport owns parse errors — a raw parse error must never reach the core, where `connectError` would misclassify it as CodeInternal). Keep the cursor_mode/offset mutual-exclusion guard. TestConnectCookieLaneIsolation must stay green.
  </action>
  <acceptance_criteria>
    - `grep -nE 'a\.d\.st\.' internal/server/connectapi.go` returns only the single ListScopes reference — D-07 closed for reads.
    - ListMemories still returns a non-zero Total and NextPageToken on a paged request, and still honors Offset/Categories/Visibility (a test asserts the Connect list contract is intact — no regression).
    - SearchMemories still defaults k to 20 (a test asserts k==0 yields up to 20 results, matching pre-refactor behavior).
    - SearchDiscoveries still defaults k to 20 after the rewire: a regression test (analogous to the SearchMemories default test) asserts a k==0 Connect SearchDiscoveries request applies k=20 to `deps.searchDiscovery` (NOT the MCP 8) — finding 7.
    - SearchDiscoveries with an EMPTY request scope still spans all discovery scopes after the rewire (round-4 HIGH-3): a regression test asserts a `req.Msg.Scope == ""` Connect SearchDiscoveries maps to `CrossSpine=true` on `deps.searchDiscovery` (does NOT error with "scope is required unless cross_spine is true") and preserves a non-empty caller Scope when supplied; a non-empty scope with CrossSpine=false still filters to that scope.
    - The handler-local query embed is DELETED on BOTH Search lanes after the rewire (round-5 MED, grok): `grep -c 'EmbedQuery' internal/server/connectapi.go` returns 0 — SearchMemories and SearchDiscoveries no longer embed in the handler; `deps.searchMemory`/`deps.searchDiscovery` (tools.go, out of this grep scope) are the sole query-embed points, so no double-embed / dead vector survives. The rerank/discovery parity and search regression tests stay green (a single embed still happens via deps).
    <!-- planner-discipline-allow: EmbedQuery -->
    <!-- The greped literal names the handler-local embed call the action instructs the executor to DELETE from connectapi.go; it legitimately appears in the action/read_first as the concrete removal target, and the ==0 grep proves the deletion. deps.searchMemory/searchDiscovery in tools.go retain the sole query embed (out of grep scope). -->

    - GetMemory increments AccessCount EXACTLY ONCE after the rewire (round-4 MED): `grep -c 'usageQueue.tryEnqueue' internal/server/connectapi.go` returns 0 (the handler-level enqueue is removed — deps.getMemory is the sole enqueue point), and a test asserts a single Connect GetMemory bumps the usage count by exactly 1 (not 2).
    <!-- planner-discipline-allow: usageQueue.tryEnqueue -->
    <!-- The greped literal names the call the action instructs the executor to DELETE from connectapi.go; it legitimately appears in the action/read_first as a concrete removal target, and the ==0 grep proves the deletion. deps.getMemory in tools.go retains the sole enqueue (out of grep scope). -->

    - Connect ListMemories with `limit=0` returns ALL matches, NOT silently capped to 20 (round-4 finding-7): a regression cell over N>20 seeded records asserts a Connect list with limit=0 returns all N (Total reflects the full count; the page is not capped at 20).
    - A malformed `created_after`/`created_before` on a Connect list request returns `CodeInvalidArgument` (parsed at the Connect boundary), NOT `CodeInternal` (round-4 MED-6) — a table cell asserts the code.
    - `go test ./internal/server/... -run 'TestConnectCookieLaneIsolation|TestRerankParityMCPAndConnect|TestConnectGetMemory|TestListMemories|TestSearchMemories|TestSearchDiscover'` passes.
    - The ListScopes exception carries a one-line documenting comment; `go test ./internal/server/...` passes; `task lint:go` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/server/... && task lint:go</automated>
  </verify>
  <done>Every Connect read handler except the documented ListScopes exception calls the typed core deps.* (caller-threaded) with no Connect field dropped and k=20 preserved; read-lane isolation stays green — Pitfall 1 closed for reads and writes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Connect client → engramAPI write handler | Untrusted (CSRF-fronted, Phase-16) write requests cross into handler code that must delegate authz to the store layer |
| engramAPI handler → deps.* → memStore | The handler must not re-gate or bypass; authz lives in the store (DEC-cgb) |
| deps.* error → Connect client (connectError) | Business rejections cross out mapped to precise codes; internal faults must not leak raw text |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-01 | Elevation of Privilege | six Connect write handlers | high | mitigate | Each handler is a thin adapter delegating to the same deps.* method as MCP; no a.d.st.* call and no ownership comparison in any write handler (grep-gated); authz stays in the store (DEC-cgb) |
| T-17-02 | Information Disclosure | by-id write RPCs ErrNotFound path | high | mitigate | Handlers call connectError which maps errors.Is(ErrNotFound)->CodeNotFound without re-wrapping; the caller's original input is already in the message from deps.* (D-11); cross-owner tables proven in 17-05 |
| T-17-12 | Information Disclosure | connectError CodeInternal branch | medium | mitigate | `connectError(ctx, err)` maps all TYPED business rejections (incl 17-02's store.ErrInvalidArgument-wrapped parseWindow/rule-summary — finding 5, and store.ErrAmbiguousShortID -> CodeFailedPrecondition — round-3 MED-5) to precise non-Internal codes, so a caller-caused semantic failure is NOT bucketed into Internal; only genuinely-unexpected errors hit the Internal branch, which logs via `slog.ErrorContext(ctx, ...)` (request-scoped) and returns a generic message so raw store/embed internals never reach the client; a unit test asserts the generic (non-leaking) message |
| T-17-07 | Elevation of Privilege | read-lane store bypass (Pitfall 1) | high | mitigate | Read handlers rewired through the typed core deps.* (D-07) preserving every field; only ListScopes remains a documented read-only exception; TestConnectCookieLaneIsolation stays green. Round-4: the rewire preserves the empty-scope = all-discovery-scopes semantics by mapping Connect empty scope -> `CrossSpine=true` (HIGH-3), so cross-scope discovery reachability is not silently lost; and the Connect `limit=0` = 'all' contract is preserved (finding-7), so the rewire neither over- nor under-exposes records |
| T-17-SC | Tampering | dependency supply chain | low | accept | Zero new external packages (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `go test ./internal/server/...` green including TestWriteRPCNegativeMatrix (non-nil spy store, real mapped outcomes), TestConnectCSRFTokenMatrix (spy-backed deps; happy-path cell off CodeUnimplemented — round-5 HIGH, the permanent Phase-16 gate stays green through the wiring), and TestConnectCookieLaneIsolation.
- `grep -c 'EmbedQuery' internal/server/connectapi.go` returns 0 — both Search handlers' query embed is deleted; deps.* embeds are the sole query-embed point (round-5 MED, grok — no double-embed).
- `grep -nE 'a\.d\.st\.' internal/server/connectapi.go` returns only the documented ListScopes exception.
- The connectError mapper is a single production function (not duplicated as a test oracle), signature `connectError(ctx, err)`; its table-test covers the store.ErrInvalidArgument (parseWindow/rule-summary) → CodeInvalidArgument case (finding 5) and the store.ErrAmbiguousShortID → CodeFailedPrecondition case (round-3 MED-5).
- A SearchDiscoveries k==0 regression test confirms the Connect k=20 default survives the rewire (finding 7).
- A SearchDiscoveries EMPTY-scope regression test confirms an empty Connect scope maps to CrossSpine=true and still spans all discovery scopes after the rewire (round-4 HIGH-3).
- `grep -c 'usageQueue.tryEnqueue' internal/server/connectapi.go` returns 0 — GetMemory's handler-level enqueue is removed; deps.getMemory is the sole enqueue point (AccessCount +1 exactly, round-4 MED).
- A Connect `limit=0` list regression returns all matches (not capped to 20 — round-4 finding-7); a malformed created_after/before returns CodeInvalidArgument at the boundary (round-4 MED-6).
- `task lint:go` exits 0.
</verification>

<success_criteria>
- The six Connect write RPCs are thin adapters on the identical deps.* code path (SC2); a write is reflected in subsequent reads and a rule stays immutable/un-shareable (SC3/DEC-iedk).
- A single production `connectError(ctx, err)` maps business rejections to precise codes (NotFound/InvalidArgument [incl parseWindow past-not_after + rule-summary, finding 5]/FailedPrecondition [incl store.ErrAmbiguousShortID, round-3 MED-5]/Internal; no CodeAborted arm — no distinct conflict sentinel exists today, round-5 LOW) with no internal-text leak; the Internal branch logs with request context.
- All read handlers except ListScopes go through the typed core deps.* (D-07 / Pitfall 1 closed) with no Connect field dropped; BOTH SearchMemories and SearchDiscoveries preserve their Connect k=20 default (finding 7).
- The nil-store negative-matrix landmine is defused; full server suite + `task lint:go` green.
</success_criteria>

<output>
Create `.planning/phases/17-wired-write-handlers-full-crud-schedule/17-04-SUMMARY.md` when done.
</output>
