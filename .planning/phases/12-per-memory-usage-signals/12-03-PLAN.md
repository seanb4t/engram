---
phase: 12-per-memory-usage-signals
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - proto/engram/v1/engram.proto
  - gen/go/engram/v1/engram.pb.go
  - gen/ts/engram/v1/engram_pb.ts
autonomous: true
requirements: [REQ-usage-signals]
user_setup: []

must_haves:
  truths:
    - "proto Memory message gains additive fields access_count=19 (uint64) and last_accessed_at=20 (google.protobuf.Timestamp); buf breaking against main passes (additive is non-breaking)."
    - "task proto:gen regenerates gen/go and gen/ts; the regenerated tree is committed so CI's buf drift-check (git diff --exit-code -- gen/) stays green."
  artifacts:
    - "proto/engram/v1/engram.proto Memory.access_count = 19"
    - "proto/engram/v1/engram.proto Memory.last_accessed_at = 20"
    - "regenerated gen/go/**/engram.pb.go with AccessCount/LastAccessedAt accessors"
    - "regenerated gen/ts/**/engram_pb.ts fields"
  key_links:
    - "engram.proto Memory fields 19/20 -> task proto:gen -> gen/go engramv1.Memory.AccessCount/LastAccessedAt -> memoryToProto population (Plan 06)"
  prohibitions:
    - "Field numbers 19/20 MUST be new/additive — MUST NOT renumber or reuse any existing 1–18 field (breaking change)."
    - "gen/ MUST NOT be hand-edited — it is codegen output; the only mutation path is task proto:gen + commit."
---

<objective>
Isolate the proto + codegen ripple: add the two additive `Memory` wire fields
(`access_count = 19`, `last_accessed_at = 20`), regenerate the committed `gen/`
tree with `task proto:gen`, and commit the diff so CI's separate drift-check step
stays green. This is a BLOCKING, must-not-skip mechanical step sequenced before
Plan 06's `memoryToProto` edit, which references the generated fields.

Purpose: The Go build passes WITHOUT regen until `memoryToProto` references the
new generated field — a false-positive green. Worse, `gen/ts` drift is invisible
to the Go build entirely but fails CI's `buf` job (`git diff --exit-code -- gen/`).
Isolating this churn in its own Wave 1 plan keeps the proto/gen diff reviewable and
prevents a downstream plan from silently shipping a stale `gen/`.
Output: `engram.proto` fields 19/20 + a freshly regenerated, committed `gen/` tree.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-per-memory-usage-signals/12-CONTEXT.md
@.planning/phases/12-per-memory-usage-signals/12-RESEARCH.md
@.planning/phases/12-per-memory-usage-signals/12-PATTERNS.md
@proto/engram/v1/engram.proto
@Taskfile.yaml
</context>

## Artifacts this phase produces (this plan's subset)

- `proto/engram/v1/engram.proto` `Memory.access_count = 19` (uint64)
- `proto/engram/v1/engram.proto` `Memory.last_accessed_at = 20` (`google.protobuf.Timestamp`)
- Regenerated committed `gen/go` + `gen/ts`

<tasks>

<task type="auto">
  <name>Task 1: Add proto fields 19/20 and regenerate gen/ (BLOCKING)</name>
  <files>proto/engram/v1/engram.proto, gen/go/engram/v1/engram.pb.go, gen/ts/engram/v1/engram_pb.ts</files>
  <read_first>
    - proto/engram/v1/engram.proto — the `Memory` message (11–32), fields 1–18, `short_id = 18` as the last field; the existing `import "google/protobuf/timestamp.proto"` (already present) and the `created_at = 14` Timestamp field as the type analog.
    - Taskfile.yaml — the `proto:lint` and `proto:gen` targets (~136–143).
    - 12-RESEARCH.md Pitfall 3 (additive fields pass `buf breaking` but a missed regen fails the separate drift-check) and Open Question 2.
    - 12-PATTERNS.md §"engram.proto — Memory message +2 fields".
  </read_first>
  <action>
    In `proto/engram/v1/engram.proto`, append two additive fields to the `Memory` message after `short_id = 18`: `uint64 access_count = 19;` and `google.protobuf.Timestamp last_accessed_at = 20;`, each with a one-line comment mirroring the doc style of the surrounding fields (access_count = monotonic strong-signal touch count; last_accessed_at = recency for curation). Do not touch fields 1–18. Run `task proto:lint` to confirm the schema is clean, then `task proto:gen` to regenerate both `gen/go` and `gen/ts`. Stage the entire regenerated `gen/` tree (`git add gen/`) so the committed output matches a fresh regen. Confirm `git status --porcelain -- gen/` is empty after staging (no unstaged drift). The proto file already carries the Apache-2.0 SPDX header; do not disturb it. This task performs NO Go handler edits — `memoryToProto` population is Plan 06 (it depends on this generated code existing).
  </action>
  <acceptance_criteria>
    - `Memory` message contains `access_count = 19` (uint64) and `last_accessed_at = 20` (Timestamp); fields 1–18 unchanged.
    - `task proto:lint` passes.
    - After `task proto:gen`, `gen/go` exposes `AccessCount` and `LastAccessedAt` on the generated `Memory` struct, and `gen/ts` carries the two new fields.
    - `git diff --exit-code -- gen/` is clean AFTER the regenerated tree is committed/staged (drift-check parity with CI).
  </acceptance_criteria>
  <verify>
    <automated>task proto:lint && task proto:gen && git diff --exit-code -- gen/</automated>
  </verify>
  <done>Wire schema carries the two additive fields; gen/ regenerated and committed drift-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build/codegen → committed gen/ tree | Codegen output is a build artifact checked into the repo; CI verifies it matches the schema. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-09 | Tampering | stale/hand-edited gen/ tree | medium | mitigate | `gen/` is regenerated by `task proto:gen`, never hand-edited; CI's `git diff --exit-code -- gen/` drift-check blocks a stale commit. |
| T-12-10 | Repudiation | wire-schema change without regen | low | mitigate | The verify step reproduces CI's drift-check locally so a missed regen fails before merge. |
| T-12-SC | Tampering | buf toolchain | low | accept | `go tool buf` is pinned in go.mod's tool block; no new external package. |
</threat_model>

<verification>
- `task proto:lint` green; `buf breaking` (via CI) treats fields 19/20 as non-breaking.
- `task proto:gen` + `git diff --exit-code -- gen/` reproduces CI's drift-check locally, green.
</verification>

<success_criteria>
The wire Memory message additively gains access_count=19 / last_accessed_at=20 and the committed gen/ tree matches a fresh regeneration.
</success_criteria>

<output>
Create `.planning/phases/12-per-memory-usage-signals/12-03-SUMMARY.md` when done.
</output>
