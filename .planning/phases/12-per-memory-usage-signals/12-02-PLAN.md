---
phase: 12-per-memory-usage-signals
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/config/config.go
  - internal/config/registry.go
  - internal/config/validate.go
  - internal/config/validate_test.go
  - internal/telemetry/metrics.go
autonomous: true
requirements: [REQ-usage-signals]
user_setup: []

must_haves:
  truths:
    - "ENGRAM_USAGE_SIGNALS is a registered koanf field (key usage.signals) defaulting to \"true\" (on) — unlike summarize.on_write which defaults off, because usage signals are local, non-egressing curation metadata (D-09)."
    - "Config.Validate() rejects an unparseable ENGRAM_USAGE_SIGNALS with a clear boolean error, unconditionally, mirroring the ENGRAM_SUMMARY_ON_WRITE parseability check."
    - "telemetry.UsageQueueMetrics exposes Enqueued/Dropped/Failed OTLP counters (no retry counter — D-10 mandates no retry)."
  artifacts:
    - "config.UsageConfig struct with Signals string field (koanf:\"signals\")"
    - "Config.Usage field (koanf:\"usage\")"
    - "registry row {Key: \"usage.signals\", Env: \"ENGRAM_USAGE_SIGNALS\", Default: \"true\"}"
    - "validate.go strconv.ParseBool(c.Usage.Signals) check"
    - "telemetry.UsageQueueMetrics + NewUsageQueueMetrics + Enqueued/Dropped/Failed methods"
  key_links:
    - "registry row -> config.UsageConfig.Signals -> parsed at point-of-use in buildUsageQueue (Plan 06), never a native bool at Load()"
    - "UsageQueueMetrics constructed in serve.go (Plan 06) -> passed to usageQueue (Plan 05) -> Dropped() fired on drop-on-full"
  prohibitions:
    - "Config.Signals MUST be kept as a string parsed at point-of-use — never a native bool coerced by the koanf unmarshal (mirrors ui.enabled / summarize.on_write)."
    - "UsageQueueMetrics MUST NOT carry a retry/retried counter (D-10: single-attempt, no retry)."
---

<objective>
Add the D-09 config gate (`ENGRAM_USAGE_SIGNALS`, default on) and the D-10 OTLP
instruments (`UsageQueueMetrics`) that the async get-path incrementer (Plan 05)
and its wiring (Plan 06) consume. Two small, independent, supporting-infra
additions with exact in-repo analogs.

Purpose: Decouple the config surface and telemetry instruments from the async
engine so Wave 1 can build them in parallel with the store foundation, and the
Wave 2 engine + Wave 3 wiring can consume finished contracts.
Output: `config.UsageConfig` + `usage.signals` registry row + validate check +
`telemetry.UsageQueueMetrics`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-per-memory-usage-signals/12-CONTEXT.md
@.planning/phases/12-per-memory-usage-signals/12-RESEARCH.md
@.planning/phases/12-per-memory-usage-signals/12-PATTERNS.md
@internal/config/config.go
@internal/config/registry.go
@internal/config/validate.go
@internal/telemetry/metrics.go
</context>

## Artifacts this phase produces (this plan's subset)

- `config.UsageConfig{ Signals string }` + `Config.Usage` field
- registry row `usage.signals` / `ENGRAM_USAGE_SIGNALS` (default `"true"`)
- `validate.go` parseability check for `ENGRAM_USAGE_SIGNALS`
- `telemetry.UsageQueueMetrics` + `NewUsageQueueMetrics` + `Enqueued`/`Dropped`/`Failed`

<tasks>

<task type="auto">
  <name>Task 1: ENGRAM_USAGE_SIGNALS config field + registry + validate</name>
  <files>internal/config/config.go, internal/config/registry.go, internal/config/validate.go, internal/config/validate_test.go</files>
  <read_first>
    - internal/config/config.go — `Config` struct (22–31, add a `Usage UsageConfig` field), `UIConfig` (119–124) and `SummarizeConfig.OnWrite` (87–90) as struct+comment analogs.
    - internal/config/registry.go — the `summarize.on_write` row (Default "false") and `ui.enabled` row as the exact shape (note this one defaults "true").
    - internal/config/validate.go — the unconditional `strconv.ParseBool(c.Summarize.OnWrite)` check (108–111).
    - internal/config/validate_test.go — the base valid `Config` builder (~18) and the `ENGRAM_SUMMARY_ON_WRITE` non-bool table case (~46) to mirror.
    - 12-PATTERNS.md §"registry.go + validate.go — ENGRAM_USAGE_SIGNALS".
  </read_first>
  <action>
    Add a `UsageConfig` struct to config.go with a single field `Signals string` tagged `koanf:"signals"`, documented as "gates the payload access_count write on get/update (D-09); default \"true\" — local, non-egressing curation metadata, parsed with strconv.ParseBool at point of use in buildUsageQueue. The D-06 OTLP recall-id spans are independent of this flag." Add `Usage UsageConfig` (tag `koanf:"usage"`) to the `Config` struct. Add the registry row `{Key: "usage.signals", Env: "ENGRAM_USAGE_SIGNALS", Default: "true"}` next to the summarize rows. In `validate.go`, add an unconditional parseability check mirroring the OnWrite one: `if _, err := strconv.ParseBool(c.Usage.Signals); err != nil { errs = append(errs, fmt.Errorf("ENGRAM_USAGE_SIGNALS %q: must be a boolean: %w", c.Usage.Signals, err)) }`. Update the base valid `Config` in validate_test.go to set `Usage: UsageConfig{Signals: "true"}` so existing happy-path tests stay green, and add a table case asserting an unparseable value (e.g. `"yes"`) surfaces an `ENGRAM_USAGE_SIGNALS` error, plus a case asserting `"false"` is accepted. Do NOT parse the flag into a bool at Load() — it stays a string (point-of-use parse lives in Plan 06's buildUsageQueue).
  </action>
  <acceptance_criteria>
    - `config.UsageConfig.Signals` and `Config.Usage` exist with the stated koanf tags.
    - `registry.go` contains the `usage.signals` / `ENGRAM_USAGE_SIGNALS` row with Default `"true"`.
    - `Config.Validate()` returns an error mentioning `ENGRAM_USAGE_SIGNALS` for a non-bool value and accepts `"true"`/`"false"`.
    - `go test ./internal/config/... -run 'TestValidate|TestUsage' -count=1` passes.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/config/... -count=1</automated>
  </verify>
  <done>Config gate registered with default-on, parseability-validated, tests green.</done>
</task>

<task type="auto">
  <name>Task 2: telemetry.UsageQueueMetrics OTLP counters</name>
  <files>internal/telemetry/metrics.go</files>
  <read_first>
    - internal/telemetry/metrics.go — `SummaryQueueMetrics` struct + `NewSummaryQueueMetrics` constructor + `Enqueued`/`Dropped`/`Failed`/`Retried` methods (~111–158). Copy the shape MINUS retry.
    - 12-PATTERNS.md §"metrics.go — usage-signal counters".
    - 12-CONTEXT.md D-10 (drop-on-full → OTLP counter, no retry) and Claude's Discretion (instrument names are planner discretion).
  </read_first>
  <action>
    Add a `UsageQueueMetrics` struct holding three `metric.Int64Counter` instruments (`enqueued`, `dropped`, `failed`) and a `NewUsageQueueMetrics(m metric.Meter) *UsageQueueMetrics` constructor that creates counters named `engram.usage_queue.enqueued`, `engram.usage_queue.dropped`, `engram.usage_queue.failed` (mirror the `engram.summary_queue.*` naming). Add nil-safe `Enqueued(ctx)`, `Dropped(ctx)`, `Failed(ctx)` methods that `Add(ctx, 1)`. Do NOT add a `retried` counter or a fill-latency histogram (D-10: single-attempt, no retry). Keep the file's existing Apache-2.0 SPDX header intact.
  </action>
  <acceptance_criteria>
    - `UsageQueueMetrics` with `enqueued`/`dropped`/`failed` counters and a constructor `NewUsageQueueMetrics(metric.Meter)` compiles.
    - Instrument names are `engram.usage_queue.enqueued`/`.dropped`/`.failed`; no `retried`/histogram present.
    - `go build ./internal/telemetry/...` succeeds and `go test ./internal/telemetry/... -count=1` (if tests exist) passes.
  </acceptance_criteria>
  <verify>
    <automated>go build ./internal/telemetry/... && go vet ./internal/telemetry/...</automated>
  </verify>
  <done>UsageQueueMetrics (enqueued/dropped/failed, no retry) available for Plans 05/06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator env → config loader | ENGRAM_USAGE_SIGNALS is operator-set; validated at startup. |
| process → OTLP exporter | Usage-queue counters ride existing OTLP; no PII in counter values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-07 | Denial of Service | misconfigured ENGRAM_USAGE_SIGNALS | low | mitigate | Config.Validate() fails loud+early on an unparseable value (mirrors OnWrite); no silent fallback. |
| T-12-08 | Information Disclosure | usage_queue OTLP counters | low | accept | Counters are scalar increments (enqueued/dropped/failed) carrying no record content or owner; no disclosure surface. |
| T-12-SC | Tampering | package installs | low | accept | No new external packages (Go-only; otel/koanf already vendored). |
</threat_model>

<verification>
- `go test ./internal/config/... -count=1` green (validate happy-path + usage-signals cases).
- `go build ./internal/telemetry/...` green; `UsageQueueMetrics` has no retry counter.
</verification>

<success_criteria>
ENGRAM_USAGE_SIGNALS is registered (default on), parseability-validated, and kept as a point-of-use string; UsageQueueMetrics exposes exactly enqueued/dropped/failed.
</success_criteria>

<output>
Create `.planning/phases/12-per-memory-usage-signals/12-02-SUMMARY.md` when done.
</output>
