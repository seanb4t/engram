---
phase: 06-typed-operator-renderer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/fieldset/fieldset.go
  - internal/fieldset/fieldset_test.go
  - cmd/engram/operator_golden_test.go
  - cmd/engram/operator_output.go
  - cmd/engram/operator_output_test.go
  - cmd/engram/prune.go
  - cmd/engram/prune_test.go
  - cmd/engram/reindex.go
  - cmd/engram/summarize.go
  - cmd/engram/migrate.go
  - cmd/engram/migrate_family.go
  - cmd/engram/spine_review_archive.go
  - cmd/engram/spine_review_consolidate.go
  - cmd/engram/spine_review_purge.go
  - cmd/engram/spine_review_scan.go
  - cmd/engram/spine_review_verify.go
  - cmd/engram/testdata/operator-reports/
  - internal/store/redevidence_harness_test.go
  - .planning/phases/06-typed-operator-renderer/red-evidence/
autonomous: false
requirements: [REQ-operator-renderer-typed]

estimate:
  tokens: 155000
  raw_tokens: 155000
  tasks: 3
  confidence: low
  note: >-
    Raised from 110000 by the first 06-REVIEWS.md cycle (HIGH-1 moved the mechanism into its own
    package behind a validating constructor, MEDIUM-2 added a malformed-template negative suite,
    HIGH-4 added the red-evidence predicate hardening plus its own unit gate), from 130000 by the
    second (cycle-2 HIGH-1's defensive clone plus its mutation suite, cycle-2 MEDIUM-1's nested
    validated-bit assertion, and the labeled-constructor plus operatorReport/operatorRow helpers that
    make 06-09's variant universe production-derived), and from 142000 by the third (cycle-3 HIGH-1's
    per-lane golden resolution in the harness, and cycle-3 HIGH-2's value-kind allowlist plus
    construction-time text materialization with their two new tests). This is well over the
    smart-zone budget and is flagged, not hidden. Task 3 step (a) — the harness hardening — remains the natural extraction
    point if the executor's context runs short: it touches only
    internal/store/redevidence_harness_test.go, shares no file with Tasks 1 or 2, and Task 3 steps
    (b) and (c) are the only work that depends on it.

must_haves:
  truths:
    - "D-08 is decided by the developer at a blocking checkpoint before any report converts: when a JSON field has no text presence in a sentence variant, and when a sentence states a value the JSON has no key for, the plan does not choose silently between narrowing JSON, widening text, or relaxing D-03."
    - "Every one of D-08's three options carries a downstream execution contract, recorded per conversion plan, stating whether that plan's goldens are frozen-unchanged or regenerated-with-a-named-justification. No option is selectable without an executable downstream branch."
    - "renderOperator takes a fieldset.Set and no free-form second document parameter, so a report whose json states more than its sentence is unconstructible rather than merely detectable (D-02)."
    - "fieldset.Set and fieldset.Field have NO exported struct fields and live in their own package, so cmd/engram cannot assemble either from a NON-EMPTY composite literal. fieldset.New is the only path that yields a usable Set, and it runs the coverage validation internally — validation is not a step on the render path that a caller can bypass, it is the construction path."
    - "The zero fieldset.Set is inert, not silently empty: RenderText returns the empty string and MarshalJSON returns fieldset.ErrUnvalidated, and renderOperator returns that error before writing any byte. A Set that did not come from New cannot render. The EMPTY composite literal fieldset.Set{} and var s fieldset.Set are legal outside the package — Go forbids only setting an unexported field — so the zero value is a reachable state that must be rejected, never an unreachable one that can be assumed away."
    - "A validated Set is immutable through every handle its constructor was given, closed by THREE properties that are each necessary: New CLONES the variadic []Field and every caller-owned []Field, []Set, and []string it stores inside a Field value; New REJECTS any field value outside the closed value-kind allowlist, so no pointer, function, map, channel, or non-Set struct can reach a stored field at all; and New MATERIALIZES each field's rendered text exactly once at construction, so RenderText never invokes a caller-supplied render function after New returns. Mutating the caller's slice, the caller's pointee, or a closure's captured variable after New returns cannot change what the Set renders in either lane. Keys() and ListKeys() return fresh slices, never the package's own backing array."
    - "The value-kind allowlist is CLOSED and enforced at construction, not at marshal time: New returns a non-nil error naming the key and the value's %T for anything outside string, bool, the integer widths the fifteen reports use, float32, time.Time, []string, Set, and []Set — including every pointer and every nil val. A builder holding a *float32 dereferences at the BUILDER boundary and passes the value; the pointer is unrepresentable inside a Field rather than merely discouraged."
    - statement: "The immutability claim is stated at exactly its true width, not by partition. Named residuals: time.Time carries an internal *time.Location, unreachable through any API this phase ships and recorded by name rather than folded into 'scalars are value copies'; and a value kind added to the allowlist later must be checked against all three properties — cloned if slice-shaped, rejected if reference-shaped, materialized either way — which the closed allowlist makes a deliberate act rather than an omission."
      verification: backstop
    - "The golden harness resolves a golden path PER LANE from the fixture's own Divergence declaration, so all three D-08 options are executable through it: the converted lane always compares against the canonical <Key>.<Variant>.{txt,json}, while the legacy lane compares against <Key>.<Variant>.legacy.{txt,json} on exactly the lane the recorded option moved. The pre-image bytes therefore survive unchanged under every option, and -update sources each file from exactly one named lane."
    - "New rejects an UNVALIDATED nested value: every Set and every element of every []Set reached by validate must itself report validated==true, or New returns a non-nil error naming the key and the row index. make([]fieldset.Set, n) with partial assignment is legal outside the package, so a zero row is a reachable input — and a zero row would otherwise be dropped silently by RenderText while MarshalJSON returned ErrUnvalidated, manufacturing the exact text/json divergence this phase exists to prevent."
    - "A renderable operator report carries a (command, variant) LABEL minted in production code: fieldset.NewLabeled stamps it, Set.Label() reads it, and renderOperator refuses a validated-but-unlabeled Set before writing any byte. Nested rows are deliberately unlabeled and are therefore unrenderable as top-level reports."
    - "fieldset.New returns a non-nil error naming every offending key when a Set would declare a field no placeholder references (D-03), and when a template references a placeholder naming no declared field."
    - "fieldset.New rejects a malformed template rather than reinterpreting it: an unmatched open or close delimiter of either kind, a nested conditional span, a conditional span carrying zero placeholders, and a conditional span carrying more than one placeholder each yield a non-nil error naming the byte offset."
    - "Set.MarshalJSON emits object keys in declaration order, never map-sorted order, and delegates every scalar's escaping to encoding/json rather than to hand-written escaping."
    - "engram prune-expired and engram prune-expired --apply emit byte-identical text and byte-identical json before and after conversion, pinned by checked-in golden files generated from the pre-image builders."
    - "The coverage gate is demonstrated RED by real checked-in mutation patches, and phase 06's red-evidence directory is listed in redEvidenceDirs, so the evidence is gated rather than merely present."
    - "TestRedEvidencePatchesAreLive accepts a patch as RED only on positive evidence that the NAMED mapped test ran and failed — a `--- FAIL: <target>` line in the -v transcript — and never on exit status alone, so a patch that merely breaks compilation, an import, or package init is reported as a FAILURE of the evidence rather than as proof."
    - "JSON object key order is the declaration order; text placeholder order is independent of it; two fixtures differing only in declaration order produce different golden json bytes."
    - "An empty Set renders as an empty text line and as an empty json object; a field whose value is a zero-length list renders as an empty json array and contributes no text; a single-element list renders as a one-element array; none of these outputs carries the four-character json null literal."
    - "Template parsing scans bytes, and every delimiter it recognises is ASCII and so cannot occur inside a multi-byte UTF-8 sequence; a template and a value carrying non-ASCII runes and a double quote render byte-identically in text and escape identically to encoding/json in json."
    - "Two fields sharing one key inside one Set collide: fieldset.New errors naming the duplicated key. Two placeholders naming one key inside one template also collide and are rejected. Keys never silently merge and never silently separate."
    - statement: "The temporary two-signature period is bounded: renderOperatorLegacy exists only between plan 06-01 and plan 06-09, and 06-09 carries an acceptance criterion asserting zero remaining references to it."
      verification: backstop
    - statement: "The residual is stated, not implied: Go cannot evaluate placeholder/field identity at compile time, so enforcement is CONSTRUCTION-time. What the mechanism guarantees is that no unvalidated Set value can exist and no existing Set can render — which is what D-02's 'unconstructible' means here — and 06-01-SUMMARY.md says so in those words rather than letting SC1's wording imply a compiler check."
      verification: backstop
  artifacts:
    - internal/fieldset/fieldset.go
    - internal/fieldset/fieldset_test.go
    - cmd/engram/operator_golden_test.go
    - cmd/engram/testdata/operator-reports/prune-expired.preview.txt
    - cmd/engram/testdata/operator-reports/prune-expired.preview.json
    - cmd/engram/testdata/operator-reports/prune-expired.applied.txt
    - cmd/engram/testdata/operator-reports/prune-expired.applied.json
    - .planning/phases/06-typed-operator-renderer/red-evidence/06-01-red-1-unreferenced-field.patch
    - .planning/phases/06-typed-operator-renderer/red-evidence/06-01-red-2-golden-widening.patch
  key_links:
    - "prune's two report builders -> fieldset.New (the validating construction path) -> renderOperator(fieldset.Set) -> both lanes -> the checked-in goldens"
    - "fieldset.New's validation and Set.RenderText share ONE parse of the template via the package's single unexported parser, so the checker's notion of a placeholder cannot drift from the renderer's"
    - "unexported struct fields + package boundary -> no NON-EMPTY composite literal of Set or Field anywhere in cmd/engram -> New is the only entry that yields a validated Set. The EMPTY literal stays legal, which is why the zero value must be inert and why nested values must be checked rather than assumed."
    - "New's defensive clone of every caller-owned slice -> a validated Set shares no backing array with its caller -> the validation is a property of the value, not a snapshot of a moment"
    - "operatorReport(key, variant, ...) in production -> fieldset.NewLabeled -> Set.Label() -> renderOperator's label guard -> 06-09's AST-derived (command, variant) universe"
    - "redEvidenceDirs entry for .planning/phases/06-typed-operator-renderer/red-evidence -> TestRedEvidencePatchesAreLive -> the hardened `--- FAIL: <target>` predicate"
  prohibitions:
    - "Do not introduce reflection-based type dispatch inside internal/fieldset (D-01)."
    - "Do not add a templating-engine import anywhere in internal/fieldset or cmd/engram (D-01)."
    - "Do not export any struct field on fieldset.Set or fieldset.Field, and do not add a constructor that skips validation. New, NewLabeled, and their Must wrappers all route through the same validate call."
    - "Do not store a caller-supplied slice — the variadic []Field, a []Set row list, a []string value — without cloning it first. A constructor that retains the caller's backing array validates a snapshot, not a value (record brwjpnx6bz)."
    - "Do not assume a nested Set arrived validated. The empty composite literal is legal outside the package, so validate must ASSERT the nested validated bit rather than infer it from how the value must have been built."
    - "Do not call fieldset.New, fieldset.NewLabeled, fieldset.MustNew, or fieldset.MustNewLabeled from anywhere in cmd/engram except the bodies of operatorReport and operatorRow. Every report variant's identity is a production string literal at an operatorReport call site; that is what makes 06-09's variant universe derivable instead of hand-listed."
    - "Do not let internal/fieldset import internal/store, cmd/engram, or any engram package; it is a leaf that depends on the standard library alone, which is what keeps record content structurally unreachable from it."
    - "Do not build an operator report body from a Go map; map iteration order is randomised and encoding/json sorts map keys, which silently reorders every document."
    - "Do not hand-write json string escaping; call encoding/json per scalar value and hand-write only the structural bytes."
    - "Do not modify or delete TestOperatorOutputParity or operatorParityRows in this plan; D-07 retirement is plan 06-09's job."
    - "Do not change addOperatorOutputFlag or operatorOutputFormat; flag registration and TTY detection are out of scope."
    - "Do not add an external module to go.mod; this phase is stdlib-only."
    - "Do not pass an internal/store result type straight through to a report; every field stays hand-declared so record content stays unreachable by construction."
    - "Do not delete any xxxSummary or xxxDoc function in this plan; they are the pre-image the goldens are generated from and the data TestOperatorOutputParity still reads."
    - "Do not add an SPDX header to any file under .planning/."
---

<objective>
Decide the one invariant question this phase cannot answer from its locked decisions, then build the
field-set mechanism and prove it end-to-end against one real operator command.

Purpose: `renderOperator(cmd, format, text string, doc any)` couples the sentence and the document by
convention only. This plan replaces that with a single ordered field-set declaration and proves the
mechanism by converting `prune-expired` — D-03's worked example — through every layer to real CLI
bytes, gated by checked-in goldens generated from the pre-image.

Output: `internal/fieldset/fieldset.go`, `internal/fieldset/fieldset_test.go`,
`cmd/engram/operator_golden_test.go`, prune's two `fieldset.Set` builders, four golden files, two
red-evidence patches, a hardened red-evidence RED predicate, and a `redEvidenceDirs` entry for phase 06.

Addresses review concerns (`06-REVIEWS.md`). Cycle 1: HIGH-1 (widening was not unconstructible as
specified), HIGH-2 (D-08 had one executable branch), HIGH-4 (the red-evidence harness accepted a build
failure as RED evidence), MEDIUM-2 (the shared parser had no malformed-input coverage). Cycle 2:
HIGH-1 (`New` aliased the caller's slice, so a validated `Set` stayed mutable — empirically reproduced),
MEDIUM-1 (nested unvalidated `Set` was not rejected, and the stated justification for skipping the check
was false), and the production half of HIGH-2 (`operatorReport` puts each variant's identity in
production code so 06-09 can derive the variant universe instead of reconciling hand-written lists).
Cycle 3: HIGH-1 (the D-08 branch tables were unexecutable because the harness compared both lanes to one
golden and sourced `-update` from the legacy lane — the branch now lives in the harness as per-lane
golden resolution keyed on a declared `Divergence`), and HIGH-2 (the immutability guarantee excluded
pointers and closures, which the phase already ships in 06-06 — closed by a rejected-value-kind
allowlist and a render callback spent at construction, each with its own falsifiable test).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-typed-operator-renderer/06-CONTEXT.md
@.planning/phases/06-typed-operator-renderer/06-RESEARCH.md
@.planning/phases/06-typed-operator-renderer/06-PATTERNS.md
@.planning/phases/06-typed-operator-renderer/06-VALIDATION.md
@cmd/engram/operator_output.go
</context>

## Artifacts this phase produces

Everything below is created by phase 06 and does not exist at phase start. A source-grounding pass that
finds these symbols absent from the pre-phase tree is seeing new work, not drift.

**New files**

- `internal/fieldset/fieldset.go`
- `internal/fieldset/fieldset_test.go`
- `cmd/engram/operator_golden_test.go`
- `cmd/engram/testdata/operator-reports/` — one CANONICAL `<key>.<variant>.txt` and one
  `<key>.<variant>.json` per report variant, where `<key>` is the `commandKey` with spaces replaced by
  hyphens; plus, on exactly the lane a D-08 option-a or option-b decision moves for a given variant, one
  PRE-IMAGE golden `<key>.<variant>.legacy.json` or `<key>.<variant>.legacy.txt`. Under option-c no
  `.legacy.*` file exists anywhere, and the harness fails if one does. Every `.legacy.*` file is deleted
  in 06-09 when the legacy lane itself is deleted.
- `.planning/phases/06-typed-operator-renderer/red-evidence/*.patch`

**New types (package `fieldset`, `internal/fieldset`)**

`Field` (all struct fields unexported), `Set` (all struct fields unexported), `segment` (unexported)

**New exported API (package `fieldset`)**

`F(key string, val any) Field`, `(Field) WithPresent(bool) Field`,
`(Field) WithRender(func(any) string) Field`, `(Field) WithSep(string) Field`,
`New(text string, fields ...Field) (Set, error)`, `MustNew(text string, fields ...Field) Set`,
`NewLabeled(label, text string, fields ...Field) (Set, error)`,
`MustNewLabeled(label, text string, fields ...Field) Set`,
`Validated(s Set) error`, `(Set) Label() string`, `(Set) RenderText() string`,
`(Set) MarshalJSON() ([]byte, error)`, `(Set) Keys() []string`, `(Set) ListKeys() []string`,
`ErrUnvalidated`, `ErrUnlabeled`, `ErrNonValueKind`

**New unexported internals (package `fieldset`)**

`parseText(string) ([]segment, error)`, `validate(string, []Field) error`,
`cloneFields([]Field) []Field`, `isValueKind(any) bool`, `materializeText([]Field) ([]Field, error)`

**New types and functions (package `main`, `cmd/engram`)**

`operatorReportFixture`, `renderOperatorLegacy(...)` (temporary; deleted in 06-09),
`registerOperatorReport(...)`, `operatorReportFixtures() []operatorReportFixture`,
`operatorReport(key, variant, text string, fields ...fieldset.Field) fieldset.Set` (the ONLY production
path to a renderable report, and the source of 06-09's derived variant universe),
`operatorRow(text string, fields ...fieldset.Field) fieldset.Set` (nested rows; deliberately unlabeled)

**New report builders, one per sentence variant, each returning `fieldset.Set` (plans 06-01..06-08)**

`prunePreviewReport`, `pruneAppliedReport`, `reindexReport`, `summarizeReport`,
`migrateSetOwnerReport`, `migrateRemapReport`, `migrateSweepReport`, `backfillSweepReport`,
`revertReport`, `archiveReport`, `spineScanReport`, `consolidateReport`, `verifyReport`,
`purgePreviewReport`, `purgeAppliedReport`, `migrateStatusReport`

`backfillSweepReport` (06-04) exists only to carry the `backfill-short-ids` literal identity: it shares
`migrate`'s sweep template constants and field-list helper outright, because one render call site serves
both cobra commands and a single literal key cannot cover two.

**New tests (`internal/fieldset/fieldset_test.go`)**

`TestFieldSetCoverage_CatchesWidening`, `TestFieldSetCoverage_CatchesMissingField`,
`TestFieldSetRejectsDuplicateKey`, `TestFieldSetRejectsRepeatedPlaceholder`,
`TestFieldSetJSONPreservesDeclarationOrder`, `TestFieldSetEmptyShapes`,
`TestFieldSetTextIsByteExactUTF8`, `TestFieldSetConditionalSpanDrops`,
`TestFieldSetRejectsMalformedTemplate`, `TestFieldSetEscapedDelimiters`,
`TestFieldSetValueDelimitersAreNotRescanned`, `TestFieldSetRejectsListWithoutSeparator`,
`TestFieldSetZeroValueIsInert`, `TestFieldSetNewClonesCallerSlices`,
`TestFieldSetAccessorsReturnFreshSlices`, `TestFieldSetRejectsUnvalidatedNestedSet`,
`TestFieldSetLabelRoundTrips`, `TestFieldSetRejectsNonValueKind`,
`TestFieldSetRenderIsSpentAtConstruction`

**New tests (`cmd/engram`)**

`TestOperatorReportGolden`, `TestOperatorReportGoldenSetIsComplete`,
`TestRenderOperatorRejectsUnlabeledSet`,
`TestFieldSetRegistryCoversEveryOperatorCommandAndVariant` (06-09)

**New test (`internal/store`)**

`TestRedEvidenceRedProofRejectsBuildFailure`

**Modified existing symbols**

`renderOperator` (signature becomes
`(cmd *cobra.Command, format outputFormat, fs fieldset.Set) error`), `redEvidenceDirs` (gains a
phase-06 entry), `TestRedEvidencePatchesAreLive` (RED confirmation moves from exit status to a named
`--- FAIL:` assertion via the new `confirmsTargetWentRed` predicate)

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: D-08 — what the widening invariant means where today's json and today's sentence describe different value sets</name>
  <decision>
D-03 (every field must be referenced by a `{key}` placeholder), D-04 (all 15 sentences preserved
byte-identical), and ROADMAP Success Criterion 2 (every operator command's `--output json|text`
behavior unchanged) cannot all hold. At least eight of the fifteen reports already carry json keys
their sentence never states, or state a value their json has no key for. One of the three must give,
and that choice defines what the phase's central invariant actually means.
  </decision>
  <context>
Verified from source this session — evidence, not speculation:

1. **JSON carries what the sentence never states.** `prunePreviewDoc` emits `deleted: 0`, and the
   preview sentence never mentions a deleted count; symmetrically `pruneReportDoc` emits `eligible: 0`
   and the applied sentence never mentions eligibility. The same shape appears in
   `migrateRemapReportDoc` (`would_remap` and `remapped`, one always zero, neither carrying
   `omitempty`), `migrateOutputDoc`, and `revertOutputDoc`.
2. **The worst case is `purgeAppliedSummary`.** `purgeAppliedDoc` calls `purgePreviewDoc` then
   overrides, so the applied document carries `eligible`, `eligible_count`, `category`, `tags`,
   `older_than`, `same_run_limitation`, and `intersection_scoping` — seven keys — while the applied
   sentence states none of them. RESEARCH.md's proposed out (route `category`/`tags`/`older_than`
   through `purgeRerunCommand`'s printed text) is CONFIRMED VALID for the PREVIEW sentence, which does
   carry a `re-run: engram spine-review purge --apply ...` line (cmd/engram/spine_review_purge.go:285),
   and CONFIRMED INVALID for the applied sentence, which has no re-run line at all
   (cmd/engram/spine_review_purge.go:294-309). RESEARCH.md Open Question 2 is therefore half-resolved;
   this checkpoint owns the open half.
3. **The sentence states what the json has no key for.** `spineScanSummary` renders the scan target in
   its header, and `spineScanReportDoc` has no `scope` key at all
   (cmd/engram/spine_review_scan.go:82-98). Under set equality that forces ADDING a json key.

Whichever option is chosen, the golden fixtures Task 2 builds make the full per-report delta explicit
and reviewable before any of it ships.

**Downstream execution contract (added in response to `06-REVIEWS.md` HIGH-2).** The first review cycle
found that only option-c was executable: plans 06-02 through 06-08 each carried an unconditional
"do not regenerate any golden file in the conversion commit", which options a and b necessarily
violate. That is fixed — each of those seven plans now carries a `## D-08 downstream execution
contract` section giving all three options a branch. In summary:

| Option | Text goldens | JSON goldens | Fixture `Divergence` | The conversion commit's acceptance criterion |
|--------|--------------|--------------|----------------------|-----------------------------------------------|
| a | frozen, unchanged | canonical REGENERATED for the affected variants; pre-image preserved as `<K>.<V>.legacy.json` | `divergeJSON` on affected variants, `divergeNone` elsewhere | `git diff` on `*.txt` is empty; every changed `*.json` is listed in the plan SUMMARY with the dropped/added key and its D-08 rationale |
| b | canonical REGENERATED for the affected variants; pre-image preserved as `<K>.<V>.legacy.txt` | frozen, unchanged | `divergeText` on affected variants, `divergeNone` elsewhere | `git diff` on `*.json` is empty; every changed `*.txt` is listed in the plan SUMMARY with the new sentence bytes and its D-08 rationale, and D-04 is amended in `06-CONTEXT.md` in the same commit |
| c | frozen, unchanged | frozen, unchanged | `divergeNone` everywhere; no `.legacy.*` file may exist | `git diff --exit-code cmd/engram/testdata/operator-reports/` is clean, and every exempt field appears in the closed allowlist with its one-line rationale |

Under a and b, a golden regenerated WITHOUT a named per-key justification in the plan SUMMARY is a
defect, not an update — the freeze is replaced by a justification requirement, never by silence.

**Every row of this table is EXECUTABLE through the harness, which is the cycle-3 HIGH-1 correction.**
The first three columns are not prose the conversion plans have to remember: they are Task 2 step (c)'s
per-lane golden resolution, driven by the `Divergence` column, which each fixture declares and
`TestOperatorReportGoldenSetIsComplete` cross-checks against the recorded option. The cycle-2 design
put the branch tables in the conversion plans but left the harness comparing both lanes to one golden
and sourcing `-update` from the legacy lane, so options a and b could not execute at all — the branch
now lives where it runs.
  </context>
  <options>
    <option id="option-a">
      <name>A — the field set is the json shape (keep D-03 and D-04; the json key set follows the sentence)</name>
      <pros>Strongest possible SC1 claim: a json document literally cannot carry a value its sentence does not state, with no exemption of any kind. This is the reading RESEARCH.md's own worked prune conversion already assumes — preview and applied become two distinct FieldSet-returning functions rather than one struct with mode-dependent population. Needs no extra mechanism. Every sentence stays byte-identical.</pros>
      <cons>JSON key sets change for roughly eight reports: keys drop (prune preview's deleted, prune applied's eligible, purge applied's seven, the inapplicable half of each migrate mode-pair) and one key is added (spine-review scan gains scope). That is a change to --output json behavior, which ROADMAP SC2 says must be unchanged. Needs a docs-site and CHANGELOG note, and Phase 7 must be told the shape moved.</cons>
    </option>
    <option id="option-b">
      <name>B — the sentence follows the field set (keep D-03 and the json key set; the text changes)</name>
      <pros>Zero risk to any json consumer; every current key survives untouched. SC1 stays maximally strong with no exemption.</pros>
      <cons>Directly violates D-04, which is locked with an explicit no-named-exception-list clause. Roughly eight operator-facing sentences gain new text — a preview sentence would have to state a deleted count of zero, and the applied purge sentence would have to restate the eligible set and both notices. Every pinned-sentence test in the package would be rewritten, destroying their value as an independent regression gate on this refactor.</cons>
    </option>
    <option id="option-c">
      <name>C — bounded coverage exemption (keep D-04 and the json key set; relax D-03 in one enumerated, gated way)</name>
      <pros>Nothing an operator reads or a script parses changes at all. The exemption is visible rather than implicit: a field may carry an explicit exemption marker plus a mandatory one-line rationale, and `fieldset.New` additionally requires every exempt field to appear in one closed allowlist that a test holds set-equal to a checked-in inventory — so the hatch cannot grow silently and Phase 7's six new record-state fields cannot take it by default.</pros>
      <cons>Reintroduces the escape hatch D-03 rejected outright, on the stated grounds that every future field can take it and thereby reopen the widening hole this phase exists to close. SC1's by-construction claim becomes by-construction-except-for-N-enumerated-fields. Costs one extra mechanism plus its own allowlist gate.</cons>
    </option>
  </options>
  <read_first>
    - .planning/phases/06-typed-operator-renderer/06-CONTEXT.md — D-03, D-04, D-07 verbatim
    - .planning/ROADMAP.md — Phase 6 Success Criteria 1-3
    - .planning/phases/06-typed-operator-renderer/06-RESEARCH.md — Pattern 3 omitempty inventory, Pitfall 1, Pitfall 2, Open Question 1
    - cmd/engram/prune.go lines 109-155 — the two-variant doc and both sentences
    - cmd/engram/spine_review_purge.go lines 157-183, 185-215, 270-309 — purgeRerunCommand, purgeReportDoc, both summaries
    - cmd/engram/spine_review_scan.go lines 78-100, 131-147 — spineScanReportDoc and spineScanSummary
  </read_first>
  <acceptance_criteria>
    - The developer's selection is recorded in `06-01-SUMMARY.md` under a `## D-08` heading, naming the
      chosen option id and the developer's own wording of the rationale.
    - The same decision is appended to `06-CONTEXT.md`'s existing `<decisions>` block as a
      `- **D-08:** ...` bullet under a new `### ` sub-heading, matching the shape of the D-01..D-07
      bullets already there — a value fill into an existing structure, never a new artifact shape.
    - If option-a is chosen, `06-01-SUMMARY.md` additionally records that a docs-site and CHANGELOG
      note for the json key-set delta is required before the phase closes, and names it as a follow-up.
    - If option-c is chosen, `06-01-SUMMARY.md` records the exemption's mechanism name and the fact
      that its allowlist must be set-equality-gated, so Task 2 can implement it.
    - `06-01-SUMMARY.md` names the selected option's row from the downstream execution contract table
      above verbatim, so every conversion plan's executor reads one instruction and not three.
    - HARD STOP: before Task 2 begins, confirm the selected option's branch is present in all seven
      conversion plans by checking that `rg -l 'D-08 downstream execution contract'
      .planning/phases/06-typed-operator-renderer/06-0[2-8]-PLAN.md` lists seven files. If the selected
      option has no matching downstream branch, halt and escalate rather than improvising one — a
      checkpoint whose answer is unexecutable downstream is the exact defect `06-REVIEWS.md` HIGH-2
      raised.
    - If option-a or option-b is chosen, `06-01-SUMMARY.md` additionally records that
      `spine-review purge`'s preview coverage construct changes shape — see `06-07-PLAN.md`'s
      `## D-08 downstream execution contract`, which is the one place where the three options produce
      three genuinely different field-set designs rather than three golden-regeneration policies.
  </acceptance_criteria>
  <resume-signal>Select: option-a, option-b, or option-c, and state the rationale in your own words.</resume-signal>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: End-to-end — one operator report renders both lanes from one field set (prune only)</name>
  <files>internal/fieldset/fieldset.go, internal/fieldset/fieldset_test.go, cmd/engram/operator_golden_test.go, cmd/engram/operator_output.go, cmd/engram/operator_output_test.go, cmd/engram/prune.go, cmd/engram/prune_test.go, cmd/engram/testdata/operator-reports/, cmd/engram/reindex.go, cmd/engram/summarize.go, cmd/engram/migrate.go, cmd/engram/migrate_family.go, cmd/engram/spine_review_archive.go, cmd/engram/spine_review_consolidate.go, cmd/engram/spine_review_purge.go, cmd/engram/spine_review_scan.go, cmd/engram/spine_review_verify.go</files>
  <read_first>
    - cmd/engram/operator_output.go — the whole file; `renderOperator` at line 64 is the pre-image
    - cmd/engram/operator_output_test.go lines 79-119 and 439-473 — `TestRenderOperatorTextAndJSON` and `TestOperatorOutputStream`, the two I/O-contract assertions that must survive character-for-character
    - cmd/engram/prune.go lines 60-155 — both call sites, both sentences, `pruneOutputDoc`, both doc converters
    - cmd/engram/golden_test.go lines 15-30 and around line 272 — `updateGolden`, the package's existing `-update` convention and its read/compare/write shape
    - internal/surfaces/rules.go lines 30-88 and 271-335 — the declared-slice, exported-validator, named-error analog PATTERNS.md selects as primary
    - internal/surfaces/conformance_test.go lines 267-278 — `TestZeroApplicableSurfacesFailsGate`, the red-proof template
    - internal/server/connectapi_parity_test.go lines 344-361 — `assertDecodeBackCoversAllFields`, the dedupe/sort/`slices.Equal`/name-every-mismatch discipline `internal/fieldset`'s validator copies
    - .planning/phases/06-typed-operator-renderer/06-RESEARCH.md — the "Don't Hand-Roll" and "Code Examples" sections
  </read_first>
  <behavior>
Tests written into `internal/fieldset/fieldset_test.go` before the implementation is complete. Each is
a named `Test...` function so `go test -list` resolves it. Every one drives the PUBLIC constructor
`fieldset.New`, never an internal helper, because after this task there is no other way to obtain a
`Set` and the tests must exercise the path production code takes.

- `TestFieldSetCoverage_CatchesWidening`: `New` called with a template referencing one key while two
  fields are supplied yields a non-nil error and no usable `Set`, and the error text names the
  un-referenced key.
- `TestFieldSetCoverage_CatchesMissingField`: the symmetric direction — a template referencing a key no
  supplied field declares yields a non-nil error naming that key.
- `TestFieldSetRejectsDuplicateKey`: two `Field` values sharing one key in one `New` call yield a
  non-nil error naming the duplicated key. They collide; they never merge and never separate.
- `TestFieldSetRejectsRepeatedPlaceholder`: one template referencing the same key twice yields a
  non-nil error naming that key.
- `TestFieldSetJSONPreservesDeclarationOrder`: marshalling a five-field set produces exact bytes in
  declaration order, and the same five fields in a permuted declaration order produce different bytes.
- `TestFieldSetEmptyShapes`: a `New("")` with no fields renders as the empty string in text and as an
  empty json object; a field whose value is a zero-length list marshals as an empty json array; a
  single-element list marshals as a one-element array; and none of these outputs carries the
  four-character json null literal.
- `TestFieldSetTextIsByteExactUTF8`: a template and a value both carrying non-ASCII runes and a double
  quote render byte-identically in text mode, and the json bytes for that value equal what
  `encoding/json` produces for the same string on its own.
- `TestFieldSetConditionalSpanDrops`: a field whose present predicate is false drops its key from the
  json object and drops its whole enclosing template span — placeholder together with the literal text
  inside that span — from the rendered sentence, leaving no stray literal behind.

Malformed-input coverage (addresses `06-REVIEWS.md` MEDIUM-2: the validator and both render lanes share
ONE parser, which prevents drift but cannot prove correct parsing — a misreading makes checker and
renderer agree on the same wrong interpretation, so the parser needs its own negative suite). All of
these are table-driven subtests with a named case per row, so `-v` shows one `--- PASS` line each:

- `TestFieldSetRejectsMalformedTemplate`, one case per malformation, each asserting a non-nil error
  whose text names the byte offset: unmatched open placeholder delimiter; unmatched close placeholder
  delimiter; unmatched open conditional-span delimiter; unmatched close conditional-span delimiter; a
  conditional span opened inside another conditional span (nesting); a placeholder opened inside a
  placeholder; a conditional span containing zero placeholders; a conditional span containing two
  placeholders.
- `TestFieldSetEscapedDelimiters`: the doubled-delimiter escape asserted by this plan's parser contract
  renders as exactly one literal delimiter byte and is NOT treated as a placeholder or span boundary,
  for all four delimiter characters, including a doubled delimiter immediately adjacent to a real
  placeholder on each side.
- `TestFieldSetValueDelimitersAreNotRescanned`: a substituted VALUE whose bytes contain every delimiter
  character, and a value that looks exactly like a well-formed placeholder naming another declared key,
  both appear verbatim in the rendered text — the substituted value is written to the output buffer and
  never re-scanned, so a value can never inject a placeholder.
- `TestFieldSetRejectsListWithoutSeparator`: a field whose value is a `[]Set` and whose separator is
  the empty string yields a non-nil error from `New` naming that key. This is the behavioral gate that
  replaces the `rg -c 'Sep:'` line-counting acceptance criteria in plans 06-05 through 06-08
  (`06-REVIEWS.md` MEDIUM-4).
- `TestFieldSetZeroValueIsInert`: the zero `Set` — obtainable as `var s fieldset.Set`, as the empty
  composite literal `fieldset.Set{}`, and as any element of `make([]fieldset.Set, n)`, all three of
  which are legal from outside the package because Go forbids only SETTING an unexported field —
  returns the empty string from `RenderText`, returns an error satisfying
  `errors.Is(err, fieldset.ErrUnvalidated)` from `MarshalJSON`, and causes `renderOperator` to return
  that error before writing any byte to the command's writer. A `Set` that did not come from `New`
  renders nothing, rather than silently rendering an empty document. All three construction forms are
  asserted, because the empty-literal form is the one the first design wrongly assumed unreachable.

Immutability coverage (addresses `06-REVIEWS.md` cycle-2 HIGH-1, empirically reproduced by the
reviewer: a variadic parameter aliases the caller's backing array, so a constructor that stores it
validates a snapshot rather than a value — record `brwjpnx6bz`). These tests live in an EXTERNAL test
package (`package fieldset_test`) so they exercise exactly the surface `cmd/engram` has:

- `TestFieldSetNewClonesCallerSlices`, one subtest per retained-handle route, each mutating the caller's
  slice AFTER `New` returned and asserting `RenderText()` and `MarshalJSON()` produce byte-identical
  output to a snapshot taken before the mutation: (i) the caller declares `fields := []fieldset.Field{...}`,
  calls `New(text, fields...)`, then assigns a wholly different `fieldset.F(...)` to `fields[0]`;
  (ii) the caller builds `rows := []fieldset.Set{...}`, stores it as a `[]Set`-valued field, then
  replaces `rows[0]` with a different row AND with the zero `fieldset.Set{}`; (iii) the caller builds a
  `[]string` value, stores it, then mutates element zero. Each subtest additionally asserts
  `fieldset.Validated(s)` still returns nil, so the test cannot be satisfied by invalidating the set.
- `TestFieldSetRejectsNonValueKind` (addresses `06-REVIEWS.md` cycle-3 HIGH-2, the pointer half):
  table-driven, one named subtest per rejected kind — `*float32`, `*string`, `*fieldset.Set`,
  `func() string`, `map[string]string`, `chan int`, a non-`Set` struct value, and an untyped `nil` —
  each asserting `New` returns a non-nil error satisfying `errors.Is(err, fieldset.ErrNonValueKind)`
  whose text names the offending key AND the value's `%T`. The `*float32` row is not hypothetical: it is
  the exact shape `06-06`'s consolidate builder was specified to pass. The same table carries one
  ACCEPT row per allowlisted kind (`string`, `bool`, each integer width the reports use, `float32`,
  `time.Time`, `[]string`, `Set`, `[]Set`), so the test cannot be satisfied by a switch that rejects
  everything.
- `TestFieldSetRenderIsSpentAtConstruction` (addresses `06-REVIEWS.md` cycle-3 HIGH-2, the closure
  half): a `WithRender` closure over a captured variable and over a call counter. After `New` returns,
  the counter is exactly 1; the caller then mutates the captured variable and calls `RenderText()`
  twice and `MarshalJSON()` once; all three outputs are byte-identical to a snapshot taken before the
  mutation, `fieldset.Validated(s)` still returns nil, and the counter is STILL exactly 1. The counter
  assertion is the one that goes red for a lazy-render implementation, which is why it is named rather
  than implied — a `RenderText` that re-invokes the closure passes every other test in this plan.
- `TestFieldSetAccessorsReturnFreshSlices`: mutating the slice returned by `Keys()` and by `ListKeys()`
  changes neither a subsequent `Keys()`/`ListKeys()` call nor either render lane.
- `TestFieldSetRejectsUnvalidatedNestedSet` (addresses cycle-2 MEDIUM-1): `New` returns a non-nil error
  naming the key and the row index when a `[]Set`-valued field carries a zero `fieldset.Set{}` row —
  built the way a caller really would, `rows := make([]fieldset.Set, 2)` with only `rows[0]` assigned —
  and likewise when a `Set`-valued field carries the zero value. The asymmetry this closes is the point:
  without the check, `RenderText()` silently drops the row while `MarshalJSON()` returns
  `ErrUnvalidated`, which is a text/json divergence manufactured by the mechanism built to prevent
  divergence.
- `TestFieldSetLabelRoundTrips`: `NewLabeled(label, ...)` yields a `Set` whose `Label()` returns that
  label exactly; `New(...)` yields one whose `Label()` is empty; `NewLabeled` with an empty label
  returns a non-nil error; and a label never appears in either render lane's bytes — it is identity for
  the gate, never content.
  </behavior>
  <action>
Implement in four steps, in this order, honouring the D-08 selection from Task 1.

**(a) `internal/fieldset/fieldset.go`** — SPDX header, `package fieldset`, stdlib imports only
(`encoding/json`, `errors`, `fmt`, `slices`, `strings`, `time`). This is a leaf package: it imports no
engram package, which is what keeps `internal/store` record types structurally unreachable from a
report rather than merely absent by convention.

The package lives OUTSIDE `cmd/engram` deliberately, and that is the correction `06-REVIEWS.md` HIGH-1
demands. The first-cycle design declared `FieldSet{Text string; Fields []Field}` with exported fields
and put `validateFieldSet` off the render path, so `FieldSet{Text: "{count}", Fields: []Field{{Key:
"count"}, {Key: "secret"}}}` was a legal literal that rendered perfectly well — widening was detected
by whichever test happened to build that value, not prevented. Unexported fields alone do not fix this,
because a composite literal inside the same package can still set them; the package boundary is what
makes a NON-EMPTY literal unwritable in `cmd/engram`. Five properties together deliver the guarantee —
properties 4 and 5 are the cycle-2 corrections, and without them the first three are a snapshot rather
than an invariant:

1. `type Field struct { key string; val any; present *bool; render func(any) string; sep string }` and
   `type Set struct { label string; text string; fields []Field; validated bool }` — NO exported struct
   field on either type. `cmd/engram` cannot name a single one, so it cannot write a NON-EMPTY composite
   literal for either type. Be precise about the boundary of that claim: Go forbids SETTING an
   unexported field from another package, and forbids nothing else, so `fieldset.Set{}`,
   `var s fieldset.Set`, and `make([]fieldset.Set, n)` all stay legal in `cmd/engram`. The zero value is
   a REACHABLE state, which is why property 3 makes it inert and property 5 makes it rejected as a
   nested value rather than assumed away.
2. `func New(text string, fields ...Field) (Set, error)` and
   `func NewLabeled(label, text string, fields ...Field) (Set, error)` are the ONLY functions that
   return a `Set` with `validated` true, and `NewLabeled` is `New` plus a non-empty label check —
   ONE validation implementation, two entry points. Each returns the zero `Set` plus a non-nil error on
   any failure. There is no exported setter and no `Set`-returning function that skips validation.
   Validation is therefore not a step on the render path a caller could bypass — it IS the construction
   path. `MustNew` and `MustNewLabeled` wrap them and panic on error, exactly as `regexp.MustCompile`
   does for developer-authored patterns; report builders reach them only through `cmd/engram`'s
   `operatorReport`/`operatorRow` helpers in step (b), and the 06-09 gate plus every golden fixture
   invoke every builder, so a malformed set cannot reach a release without CI panicking first.
   The `label` is the report's `<key>/<variant>` identity, read back by `Label() string`; it never
   reaches either render lane's bytes. Nested row sets are built with the unlabeled `New`/`MustNew`,
   which is what makes a row unrenderable as a top-level report.
3. The zero `Set` is INERT, not silently empty: `RenderText()` returns `""` and `MarshalJSON()` returns
   `ErrUnvalidated`. `renderOperator` checks and returns that error before writing any byte. A value
   that did not come from a constructor cannot produce output.
4. **Both constructors CLONE every caller-owned slice before storing it.** This is `06-REVIEWS.md`
   cycle-2 HIGH-1, which the reviewer reproduced with a compiled program: a variadic parameter aliases
   the caller's backing array, so storing `fields` directly leaves the caller holding a live handle to a
   validated `Set`'s contents — `fields[0] = fieldset.F("secret", "leak")` after a successful `New`
   changes what the `Set` renders while `validated` stays true, and struct copies of that `Set` share
   the same array. `Validated(s)` cannot detect it, because it reads a construction-time bit and
   therefore reports history, not current state. Clone in this order: copy the variadic `[]Field` with
   an unexported `cloneFields` helper FIRST, then walk the copy and replace every `[]Set`, `[]Field`,
   and `[]string` value inside a `Field.val` with its own fresh slice, then validate the CLONE, then
   store the CLONE. Validating before cloning would validate the caller's array and store a different
   one. A nested `Set`'s own `fields` slice needs no second clone — its constructor already cloned it,
   and no caller holds that array — but the `[]Set` row slice the caller passed in does. Document in the
   package doc comment which value kinds are cloned. Do NOT partition the remainder as
   "slice-shaped kinds added later versus scalars that copy already": `06-REVIEWS.md` cycle-3 HIGH-2
   found that partition is not exhaustive — a POINTER and a CLOSURE fall into neither side — which is
   what properties 6 and 7 close. `Keys()` and `ListKeys()` likewise return fresh slices rather than the
   package's own array.
5. **`validate` ASSERTS the nested validated bit rather than inferring it.** This is `06-REVIEWS.md`
   cycle-2 MEDIUM-1. For every `Set` value and every element of every `[]Set` value it recurses into,
   `validate` requires `validated == true` and otherwise returns an error naming the key and, for a row
   list, the row index. Do not reason that a nested value must have come from a constructor: per
   property 1 it need not have, and `make([]fieldset.Set, n)` with partial assignment is the ordinary
   way a caller builds a row list. A zero row has empty text and no fields, so a naive re-run of the
   same set-equality check passes it trivially — and the resulting behaviour is asymmetric in the worst
   direction for this phase, with `RenderText()` dropping the row silently while `MarshalJSON()` returns
   `ErrUnvalidated`.
6. **`New` REJECTS any field value outside a CLOSED value-kind allowlist.** This is `06-REVIEWS.md`
   cycle-3 HIGH-2, first half. `Field.val` is `any`, and the phase already ships a mutable one: 06-06's
   consolidate builder was specified to take `minScore *float32` straight from `parseMinScore`
   (`cmd/engram/spine_review_consolidate.go:58`, threaded live through `:97` to `:129`/`:131`) into a
   field. A pointer is neither a slice `cloneFields` walks nor a scalar that copies, so property 4 does
   not reach it: mutating `*minScore` after a successful `New` changes BOTH lanes — text through the
   render closure, json through `encoding/json` following the pointer — while `validated` stays true,
   and no slice-mutation test can go red for it. Close it by construction. Add
   `isValueKind(v any) bool` running the SAME type switch `MarshalJSON` uses — `string`, `bool`, the
   integer widths the fifteen reports actually use, `float32`, `time.Time`, `[]string`, `Set`, `[]Set`
   — and have `validate` return `ErrNonValueKind` wrapped with the offending key and the value's `%T`
   for ANYTHING else: every pointer, every function, every map, every channel, every non-`Set` struct,
   and a nil `val`. A builder holding a `*float32` DEREFERENCES at the builder boundary and passes the
   value, or drops the field with `WithPresent(p != nil)`; that is a two-line change at one call site
   and it makes the pointer unrepresentable inside a `Field` rather than merely discouraged. This also
   moves the type-switch failure from marshal time to construction time, where every other coverage
   failure already surfaces.
7. **The render callback is SPENT at construction.** This is `06-REVIEWS.md` cycle-3 HIGH-2, second
   half. A `render func(any) string` closing over mutable state changes the text lane after a successful
   `New`, and Go offers no way to inspect a closure's captures, so it cannot be rejected the way a
   pointer can. Precomputing removes the need to: add
   `materializeText(fields []Field) ([]Field, error)`, called by `New` on the CLONE after cloning and
   before storing, which invokes each field's `render` exactly ONCE and stores the resulting string on
   the field. `RenderText()` then substitutes those stored strings and never invokes a caller-supplied
   function again. Materialize bottom-up, so a `Set`- or `[]Set`-valued field's text form is the join of
   its already-materialized rows with its `Sep`; a field with no `render` materializes through the same
   default formatter both lanes already agree on. State the consequence plainly in the doc comment: a
   `Set` is a snapshot of CONSTRUCTION-TIME state by design — that is what D-01's "declaration" means —
   so a render function is required to be a pure formatter of the value it is handed, it runs once
   inside `New`, and a render function with side effects runs at construction and never again.

Properties 4, 6, and 7 together are the whole immutability claim, and each is necessary: 4 closes
retained slice handles, 6 closes pointers and every other reference-shaped value, 7 closes closures.
With all three, a validated `Set` cannot change after `New` returns through ANY handle its caller kept.
State the residuals by NAME rather than by partition, in the package doc comment and in
`06-01-SUMMARY.md`: (i) `time.Time` carries an internal `*time.Location`, so a caller that mutated a
`*Location` in place could move a rendered timestamp — not reachable through any API this phase ships,
recorded by name and not folded into a "scalars copy already" generalization; (ii) a value kind added to
the allowlist LATER must be checked against all three properties before it is added — cloned if
slice-shaped, rejected if reference-shaped, materialized either way — which the allowlist being closed
and enforced makes a deliberate act rather than an omission.

Field construction is `F(key string, val any) Field` plus the chainable `WithPresent(cond bool) Field`
(a non-nil false is the D-06 drop; the nil default means always-present), `WithRender(fn func(any)
string) Field` (overrides TEXT formatting only), and `WithSep(sep string) Field` (the join separator
for a `[]Set`-valued field, REQUIRED non-empty on such fields: per RESEARCH.md assumption A4 there is
no correct default, because `statusSummary` joins inline while the other six list reports join on a
newline). `Keys() []string` and `ListKeys() []string` are read-only introspection for tests — each
returns a FRESH slice, never the package's own backing array, so a caller cannot reach a `Set`'s
contents through a returned handle any more than through the one it passed in. `ListKeys` is what lets
plans 06-05 through 06-08 assert exact set equality over a report's list-bearing fields instead of
grepping for a separator declaration.
`Validated(s Set) error` returns nil when `s` came from a constructor and `ErrUnvalidated` otherwise;
it is how the conversion plans and 06-09's gate assert "this builder produced a validated set" without
being handed a second construction path — it returns an error, never a `Set`. Be honest about what it
is: a read of a construction-time bit, so it reports HISTORY, not current state. That is sufficient
here only BECAUSE properties 4, 6, and 7 TOGETHER make a validated `Set` immutable — clone the slices,
reject the reference-shaped values, spend the render callback. Name all three in its doc comment, and do
not claim immutability from the clone alone: a `Validated` helper over an alias-retaining constructor
reports "was validated once" about a value that has since changed (`06-REVIEWS.md` cycle-2 HIGH-1,
record `brwjpnx6bz`), and a `Validated` helper over a constructor that stores a live pointer or a lazy
closure does exactly the same thing one value kind down (`06-REVIEWS.md` cycle-3 HIGH-2 — the same
record's reasoning, extended). Say so, so the next reader does not mistake it for revalidation and does
not read the claim as wider than the three properties deliver.

Write ONE unexported parser, `parseText(text string) ([]segment, error)`, returning an ordered segment
list of literal, placeholder, and conditional-span segments. Placeholder syntax is `{key}` for an
unconditional reference and a bracketed span carrying its own literal text plus one `{key}` for a
conditional reference, emitted whole or dropped whole. Doubled delimiters escape to a single literal
delimiter. An unmatched or nested delimiter, a span with zero placeholders, and a span with more than
one placeholder are each an error naming the byte offset — never silently literal text. The scanner
walks BYTES; every delimiter it recognises is ASCII and so cannot appear inside a multi-byte UTF-8
sequence, which is what makes byte scanning safe for arbitrary UTF-8 template text. Substituted VALUES
are written straight to the output buffer and are never re-scanned for delimiters.

The unexported `validate(text string, fields []Field) error` that `New` calls uses that same parser and
asserts exact SET EQUALITY between the declared key set and the referenced placeholder key set — never
a count and never a partition identity, because this repo's recorded gotcha `01mdq5qq9j` documents a
partition identity that stayed invariant under the very mutation it appeared to guard. It rejects a
duplicated key, rejects a key referenced twice, rejects any field value outside the closed
`isValueKind` allowlist per property 6, requires a field carrying a non-nil present predicate
to be referenced from a conditional span rather than a bare placeholder, requires a non-empty separator
on a `[]Set`-valued field, and recurses into `Set` and `[]Set` values one level at a time (D-05),
asserting each nested value's own `validated` bit per property 5 above. Do NOT justify skipping that
assertion on the grounds that a nested value can only have come from a constructor: the empty composite
literal makes that false, and `[]fieldset.Set` is the dominant row-list shape in plans 06-05 through
06-08. Every error is built with `fmt.Errorf` naming the offending key, row index, or byte offset —
never a bare boolean — matching `internal/surfaces/rules.go`'s named-error style.

`(s Set) RenderText() string` walks the parsed segments, substituting each field's MATERIALIZED text
from property 7 — it never invokes a caller-supplied `render`, because by the time a `Set` exists that
function has already been called and its result stored. `(s Set) MarshalJSON()` walks the fields in
declaration order, writing the structural bytes by hand and delegating every scalar to `encoding/json`
so escaping is not re-derived. Type dispatch is an explicit Go type switch over `any` covering
`string`, `bool`, the integer widths the fifteen reports actually use, `float32`, `time.Time`,
`[]string`, `Set`, and `[]Set` — the SAME closed set `isValueKind` enforces at construction, declared
once and shared by both, so a kind can never be marshalable without being constructible or the reverse.
No reflection-based dispatch (D-01), no templating-engine import
(D-01), and no Go map as the object body, since map iteration order is randomised and `encoding/json`
sorts map keys, which would silently reorder every document.

Record the residual honestly in `06-01-SUMMARY.md` rather than letting SC1's wording imply more than
the code delivers: Go cannot evaluate placeholder/field identity at compile time, so enforcement is
CONSTRUCTION-time, not compile-time. What is genuinely by-construction is that no unvalidated `Set`
value can exist, no `Set` can be assembled outside `New`, and no `Set` that did not come from `New` can
render — which is the operative content of D-02's "unconstructible". CONTEXT.md's Claude's-Discretion
clause explicitly delegates the surfacing locus ("at compile time, at `init()`, or at first render"),
so this is a discretionary choice within a locked decision, not a departure from one.

If Task 1 selected option-c, additionally implement the exemption exactly as the developer worded it,
with its allowlist held set-equal to a checked-in inventory by its own named test. The exemption marker
must be a chainable `Field` method in the same style — never an exported struct field, which would
reopen the literal hole this design closes.

**(b) `cmd/engram/operator_output.go`** — rename the existing four-parameter renderer to
`renderOperatorLegacy`, body unchanged, its doc comment marking it as the temporary pre-conversion path
plan 06-09 deletes. Add
`renderOperator(cmd *cobra.Command, format outputFormat, fs fieldset.Set) error` with the same two-mode
I/O contract, preceded by TWO guards, both before any byte is written. First: if the set is not
validated — detected by `fieldset.Validated(fs)` — return that error, so an inert zero value can never
produce an empty document that looks like a real one. Second: if the set is validated but its
`Label()` is empty, return `fieldset.ErrUnlabeled` wrapped with the caller's command path, so a nested
ROW set can never be rendered as though it were a top-level report. The second guard is what makes
06-09's derived variant universe airtight: a renderable report must carry a label, a label comes only
from `operatorReport`, and every `operatorReport` call site is a source-level `(key, variant)` literal
pair 06-09 enumerates. Add `TestRenderOperatorRejectsUnlabeledSet` in `operator_output_test.go`
covering both guards — a zero `fieldset.Set{}` and a validated-but-unlabeled `fieldset.MustNew(...)` —
each asserting a non-nil error AND an empty output buffer.

Add the two production helpers to the same file, which are the ONLY places in `cmd/engram` that may
name a `fieldset` constructor:

- `operatorReport(key, variant, text string, fields ...fieldset.Field) fieldset.Set` returns
  `fieldset.MustNewLabeled(key+"/"+variant, text, fields...)`, panicking with a message naming the key
  and the variant. Both identity arguments must be written as string LITERALS at every call site — that
  is a hard requirement 06-09's gate enforces, not a style note, because a computed argument would make
  the variant universe underivable. Its doc comment says so and names `06-09-PLAN.md` Task 1 as the
  enforcing gate. The panic message is also a real improvement over a bare constructor panic, which
  would not say which report failed.
- `operatorRow(text string, fields ...fieldset.Field) fieldset.Set` returns
  `fieldset.MustNew(text, fields...)` for the nested row sets plans 06-05 through 06-08 build. Rows are
  deliberately UNLABELED, which is exactly why the renderer's second guard rejects one at top level.

Then text mode writes `fs.RenderText()` through
`fmt.Fprintln` to `cmd.OutOrStdout()`, and json mode writes exactly one document plus one trailing
newline through `json.NewEncoder(cmd.OutOrStdout()).Encode(fs)`. Leave `addOperatorOutputFlag`, `operatorOutputFormat`,
and `isTTYWriter` alone — D-02 scopes the change to the render entry point. Mechanically update the 19
production call sites in `reindex.go`, `prune.go`, `summarize.go`, `migrate.go`, `migrate_family.go`,
`spine_review_archive.go`, `spine_review_consolidate.go`, `spine_review_purge.go`,
`spine_review_scan.go`, and `spine_review_verify.go` to name `renderOperatorLegacy` — one identifier
per site, no other change in those files. In `operator_output_test.go`, repoint
`TestRenderOperatorTextAndJSON` and `TestOperatorOutputStream` at the NEW `renderOperator`, converting
only their throwaway fixtures to `fieldset.NewLabeled(...)` values — labeled, since the renderer now
rejects an unlabeled set — and leaving every assertion exactly as it stands today. Leave `TestOperatorOutputParity`, `operatorParityRows`, `TestOperatorOutputEmpty`, and
`TestOperatorOutputEncoding` untouched.

**(c) `cmd/engram/operator_golden_test.go`** — the SC2 regression harness. Declare

```
type divergence string            // divergeNone | divergeJSON | divergeText
type operatorReportFixture struct {
    Key, Variant     string
    LegacyText       string
    LegacyDoc        any
    Report           func() fieldset.Set
    Divergence       divergence
    DivergenceReason string
}
```

with `func registerOperatorReport(key, variant, legacyText string, legacyDoc any, report func() fieldset.Set, div divergence, reason string)`
appending to a package-level slice, and `operatorReportFixtures()` returning that slice sorted by Key
then Variant. Registration lives in each report's OWN `_test.go` file through `init()`, so no two
conversion plans ever edit one file. Every fixture input must be deterministic — fixed `time.Time`
values, no wall-clock reads.

`TestOperatorReportGolden` drives BOTH lanes through the real renderers into a `bytes.Buffer` behind a
throwaway `cobra.Command` — `renderOperatorLegacy` for the legacy pair, `renderOperator` for `Report()`
when non-nil — where `<Key>` is the `commandKey` with spaces replaced by hyphens.

**Per-lane golden resolution — the D-08 branch is a property of THIS HARNESS, not only of the
conversion plans' prose.** This is `06-REVIEWS.md` cycle-3 HIGH-1, and it is an execution blocker in the
cycle-2 design: that harness compared both lanes to the same golden and sourced `-update` bytes from the
legacy lane "while `LegacyDoc` is non-nil", but no plan nils `LegacyDoc` before 06-09, so option-a's
mandated json regeneration could neither be produced nor pass. Two golden families now live in
`cmd/engram/testdata/operator-reports/`:

- the CANONICAL pair `<Key>.<Variant>.txt` and `<Key>.<Variant>.json` — what the shipped renderer emits;
- the PRE-IMAGE golden `<Key>.<Variant>.legacy.txt` or `<Key>.<Variant>.legacy.json` — present for ONE
  lane of ONE variant if and only if that fixture declares a divergence on that lane.

Resolution is mechanical, with no per-plan judgement:

| Fixture `Divergence` | Legacy lane text vs | Legacy lane json vs | Converted lane (both) vs |
|---|---|---|---|
| `divergeNone` (always, under D-08 option-c) | `<K>.<V>.txt` | `<K>.<V>.json` | the canonical pair |
| `divergeJSON` (D-08 option-a, affected variants) | `<K>.<V>.txt` | `<K>.<V>.legacy.json` | the canonical pair |
| `divergeText` (D-08 option-b, affected variants) | `<K>.<V>.legacy.txt` | `<K>.<V>.json` | the canonical pair |

The converted lane compares against the canonical pair ALWAYS; only the legacy lane's path moves. Where
a fixture declares `divergeNone` — every fixture under option-c, and every unaffected variant under a
and b — both lanes compare to the same file and a matching pair transitively proves the converted
report is byte-identical to its pre-image on both lanes, exactly as before. Where a fixture declares a
divergence, the pre-image bytes are not discarded: they move into the `.legacy.*` file and stay under
test, so "the pre-image survives the conversion unchanged" remains literally true under all three
options.

Reuse the package's existing `-update` flag (`updateGolden` in `golden_test.go`). Under it each file is
sourced from EXACTLY ONE named lane, so a regeneration is never ambiguous:

- the canonical pair is written from `Report()` when the fixture's `Report` is non-nil, and from the
  legacy lane when it is nil — which is what makes every conversion plan's Task 1 pre-image freeze work,
  since `Report` is nil in that commit;
- a `.legacy.*` file is written from the legacy lane, and only for the lane the fixture declares
  diverged.

The cycle-2 rule "bytes are written from the legacy lanes while `LegacyDoc` is non-nil" is REPLACED by
this one and must not survive anywhere in the file or its comments.

`TestOperatorReportGoldenSetIsComplete` globs the testdata directory, fails loudly when the glob matches
zero files, and asserts FOUR things:

1. exact set equality in BOTH directions between the discovered CANONICAL basenames and the registered
   fixtures, so a dropped registration and an orphan golden each fail their own assertion;
2. exact set equality in BOTH directions between the discovered `.legacy.*` basenames and the
   `(fixture, lane)` pairs whose `Divergence` names that lane — a `.legacy.json` with no declared
   divergence is an orphan and fails; a declared divergence with no `.legacy.json` fails too;
3. `DivergenceReason` is non-empty exactly when `Divergence != divergeNone`, and empty otherwise —
   this is how "a golden regenerated WITHOUT a named justification is a defect, not an update" becomes
   a test rather than a SUMMARY convention;
4. every fixture's `Divergence` is compatible with the recorded D-08 option, read from one package-level
   `const d08Option` the executor sets from Task 1's decision: option-c admits only `divergeNone`;
   option-a admits `divergeNone` and `divergeJSON` and never `divergeText`; option-b mirrors it. A
   fixture diverging on the lane the selected option FREEZES fails by name — which is how "under
   option-a the `*.txt` goldens are frozen" stops being a per-plan instruction anyone can forget.

**(d) `cmd/engram/prune.go` and `cmd/engram/prune_test.go`** — add
`prunePreviewReport(eligible uint64, before time.Time) fieldset.Set` and
`pruneAppliedReport(deleted uint64, before time.Time) fieldset.Set`, one per sentence variant, each
built with `operatorReport` and `fieldset.F` — `operatorReport("prune-expired", "preview", ...)` and
`operatorReport("prune-expired", "applied", ...)`, both identity arguments written as string literals,
never `fieldset.MustNew` directly — and point `prunePreview`'s and `pruneApplyRun`'s render
calls at the new `renderOperator`. These two call sites are the first two elements of the
production-derived `(command, variant)` universe 06-09 enumerates, and the shape every conversion plan
imitates. Reproduce both
sentences byte-identically per D-04: `before` carries a `WithRender` emitting `time.RFC3339`, and
`best_effort` carries a `WithRender` emitting the exact caveat words the sentence already uses when true
and the empty string when false — D-03's worked example, where the field becomes structurally
referenced while the bytes do not move. Apply the D-08 selection to `preview`, `eligible`, and
`deleted`, which are precisely the fields Task 1 decided about. Do NOT delete `prunePreviewSummary`,
`pruneSummary`, `pruneOutputDoc`, `prunePreviewDoc`, or `pruneReportDoc`: they are the pre-image the
goldens are generated from and the data `TestOperatorOutputParity` still reads. In `prune_test.go`, add
an `init()` registering both fixtures with a fixed cutoff of 2031-06-15T12:00:00Z and a count of 31,
matching `operatorParityRows`' existing prune inputs, wired to both the legacy pair and the new
builders. Each registration passes the `Divergence` the D-08 selection produces for that variant —
`divergeNone` under option-c, and under option-a or option-b whichever lane the decision moved for
`preview`'s `deleted` and `applied`'s `eligible` — together with the one-line `DivergenceReason`
criterion 3 above requires. Generate the goldens once with the package `-update` flag, then re-run
without it. Under option-c that is four files; under option-a or option-b it is four canonical files
plus one `.legacy.*` file per diverged lane, and the `.legacy.*` file must be produced by copying the
canonical golden BEFORE regenerating it, never by hand-editing bytes.
  </action>
  <reversibility rating="one-way">D-08 defines what the phase's invariant means and, under option-a, changes the published `--output json` key set for roughly eight operator commands; downstream json consumers and Phase 7 both build on the answer. Task 1's blocking checkpoint is the gate.</reversibility>
  <verify>
    <automated>go test ./internal/fieldset/... ./cmd/engram/... -count=1 -run 'TestFieldSet|TestOperatorReportGolden|TestRenderOperator|TestOperatorOutputStream|TestOperatorOutputParity|TestOperatorOutputEmpty|TestOperatorOutputEncoding|TestPrune' -v 2>&1 | grep -E '^( *)(=== RUN|--- (PASS|FAIL)|FAIL|ok)'</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/fieldset/... -list 'TestFieldSet.*'` lists all nineteen names from the
      `<behavior>` block, and the `-v` run above shows a `=== RUN` and a `--- PASS` line for each; a
      package-level `ok` alone is not acceptance (record `bsbsvn4hbc`: a `-run` matching nothing exits
      0). `TestFieldSetRejectsMalformedTemplate`, `TestFieldSetNewClonesCallerSlices`, and
      `TestFieldSetRejectsNonValueKind` each additionally show one `--- PASS` subtest line per case
      named in the `<behavior>` block.
    - The immutability claim is proven at its full stated width, not only for slices. All three
      properties have a test that goes red for exactly one of them and for nothing else:
      `go test ./internal/fieldset/ -count=1 -run '^(TestFieldSetNewClonesCallerSlices|TestFieldSetRejectsNonValueKind|TestFieldSetRenderIsSpentAtConstruction)$' -v`
      shows `--- PASS` for all three, with a `*float32` REJECT subtest named in the second and the
      render-call-counter assertion in the third. `rg -n 'func isValueKind\(|func materializeText\(' internal/fieldset/fieldset.go`
      returns exactly one match each, and `rg -n 'f\.render\(' internal/fieldset/fieldset.go` shows the
      render function invoked ONLY from inside `materializeText` — never from `RenderText`, which is
      the structural form of the property-7 guarantee.
    - The value-kind allowlist is declared once and shared, not duplicated: the type switch
      `MarshalJSON` dispatches on and the one `isValueKind` enforces are the same closed set, verified
      by reading both — a kind that is marshalable but not constructible, or the reverse, fails this
      criterion regardless of what the tests report.
    - The harness executes the recorded D-08 option rather than assuming one:
      `rg -n 'divergeNone|divergeJSON|divergeText|d08Option' cmd/engram/operator_golden_test.go` shows
      the divergence type, the per-lane golden resolution, and the `d08Option` compatibility assertion;
      `rg -n 'LegacyDoc' cmd/engram/operator_golden_test.go` shows `LegacyDoc` used ONLY to decide
      whether a legacy lane exists and never to decide which lane `-update` writes from — the cycle-2
      rule cycle-3 HIGH-1 found unexecutable. Under option-c,
      `ls cmd/engram/testdata/operator-reports/*.legacy.* 2>/dev/null | wc -l` reports 0 and
      `TestOperatorReportGoldenSetIsComplete` fails if that ever stops being true.
    - The construction guarantee is proven structurally, not asserted:
      `go doc ./internal/fieldset Set` and `go doc ./internal/fieldset Field` each show a struct whose
      every field is unexported, and
      `rg -n 'fieldset\.(Set|Field)\{' cmd/engram/ --glob '!*_test.go'` returns no match — no composite
      literal of either type is written anywhere in the command package's PRODUCTION code. The test
      glob exclusion is load-bearing and deliberate: `TestRenderOperatorRejectsUnlabeledSet` must write
      the empty literal on purpose to prove the renderer rejects it, so an unscoped pattern here would
      be a permanently red gate. Note precisely what this proves and what it does not: a NON-EMPTY
      literal is impossible, while the EMPTY literal this pattern also catches is legal Go and is caught
      here by convention plus the inert zero value and the renderer's guard, not by the compiler.
    - The immutability guarantee is proven by a mutation test, not by reading the constructor:
      `go test ./internal/fieldset/ -count=1 -run '^TestFieldSetNewClonesCallerSlices$' -v` shows a
      `--- PASS` subtest for each of the three retained-handle routes, and
      `rg -n 'func cloneFields\(' internal/fieldset/fieldset.go` returns exactly one match called from
      both constructors. A `New` that stores its variadic parameter directly passes every other test in
      this plan and fails only this one — which is why it is a named acceptance criterion rather than a
      code-review note.
    - `rg -n 'func New\(|func NewLabeled\(|func MustNew\(|func MustNewLabeled\(' internal/fieldset/fieldset.go`
      returns exactly those four, and
      `rg -n ') (Set|\(Set, error\))$|\) Set \{|\) \(Set, error\) \{' internal/fieldset/fieldset.go`
      shows no fifth `Set`-returning constructor. If the executor adds a helper that returns a `Set`,
      it must route through `New` and the SUMMARY must say so.
    - `rg -n 'fieldset\.(New|NewLabeled|MustNew|MustNewLabeled)\(' cmd/engram/ --glob '!*_test.go'`
      returns matches only inside `operator_output.go`, and only within the bodies of `operatorReport`
      and `operatorRow` — the production constructor routing 06-09's derived variant universe depends
      on. `rg -n 'operatorReport\("prune-expired"' cmd/engram/prune.go` returns exactly two matches,
      one per variant, each with both identity arguments written as string literals.
    - `go test ./cmd/engram/ -count=1 -run '^TestRenderOperatorRejectsUnlabeledSet$' -v` shows
      `--- PASS` with one subtest for the unvalidated zero value and one for the validated-but-unlabeled
      row set, each proving the writer received zero bytes.
    - `go list -deps ./internal/fieldset | rg -c 'engram'` reports no match beyond the module path of
      the package itself — the package depends on the standard library alone.
    - `go test ./cmd/engram/... -list 'TestOperatorReportGolden.*'` lists both
      `TestOperatorReportGolden` and `TestOperatorReportGoldenSetIsComplete`, and both show
      `--- PASS`.
    - `TestOperatorOutputParity`, `TestOperatorOutputEmpty`, and `TestOperatorOutputEncoding` still show
      `--- PASS`, and `git diff --stat cmd/engram/operator_output_test.go` shows changes only inside
      `TestRenderOperatorTextAndJSON` and `TestOperatorOutputStream`.
    - The four files
      `cmd/engram/testdata/operator-reports/prune-expired.{preview,applied}.{txt,json}` exist and are
      committed; re-running the suite with the `-update` flag leaves `git diff --exit-code
      cmd/engram/testdata/operator-reports/` clean.
    - The pre-image bytes are on disk and under test under EVERY option. Under option-c (and for any
      variant the selected option left undiverged),
      `cmd/engram/testdata/operator-reports/prune-expired.applied.txt` contains exactly the bytes
      `pruneSummary(31, 2031-06-15T12:00:00Z)` produces plus one trailing newline, and the `.json`
      sibling contains exactly what `json.NewEncoder` produces for `pruneReportDoc(31, ...)`. Where the
      selected option moved a lane, that same assertion holds against the `.legacy.<ext>` file instead,
      and the canonical file carries the post-decision bytes with its `DivergenceReason` recorded — the
      claim is that the pre-image bytes survive somewhere under test, never that the canonical file is
      frozen regardless of the decision.
    - `rg -c '"reflect"' internal/fieldset/fieldset.go` prints nothing and exits 1 (no match).
      `rg -c 'text/template|html/template' internal/fieldset/ cmd/engram/` likewise prints nothing and
      exits 1; that one IS scoped across both trees because both are clean today. The reflect pattern
      stays file-scoped: seven existing `cmd/engram` test files legitimately import that package, so a
      package-wide pattern there would be a permanently red gate.
    - `git diff go.mod go.sum` is empty.
    - `rg -n 'renderOperatorLegacy\(' cmd/engram/ --glob '!*_test.go' | wc -l` reports 17 — the 19
      pre-image call sites minus prune's two, which now call the new `renderOperator`.
    - `task lint` and `task test` are green.
  </acceptance_criteria>
  <done>`engram prune-expired` and `engram prune-expired --apply` render both lanes from one `fieldset.Set` each, byte-identical to their pre-image under the D-08 selection; the only way to obtain a `Set` is a validating constructor; that `Set` cannot be changed afterwards through any handle its caller kept; an unvalidated nested value is rejected at construction; and the coverage check is proven able to reject an un-referenced field.</done>
</task>

<task type="auto">
  <name>Task 3: Harden the red-evidence RED predicate, then prove the coverage gate and the golden gate can go RED and list phase 06 in redEvidenceDirs</name>
  <files>internal/store/redevidence_harness_test.go, .planning/phases/06-typed-operator-renderer/red-evidence/06-01-red-1-unreferenced-field.patch, .planning/phases/06-typed-operator-renderer/red-evidence/06-01-red-2-golden-widening.patch</files>
  <read_first>
    - internal/store/redevidence_harness_test.go lines 18-121 — the harness contract and the `redEvidenceDirs` map, including its doc comment stating the map is declared per phase
    - internal/store/redevidence_harness_test.go lines 195-327 — `TestRedEvidencePatchesAreLive`: the per-directory zero-applicability guard, the both-directions mapping set-equality, and the `go test -run ^Target$ ./...` invocation at line 303 that makes an out-of-package target test reachable from this harness
    - internal/store/redevidence_harness_test.go lines 303-316 specifically — the defect: `runErr == nil` is rejected, then EVERY `*exec.ExitError` is accepted as RED at line 312-316, so a patch that breaks compilation satisfies the harness identically to one that trips an assertion
    - .planning/phases/03-migration-foundation-registry-invariants-sweep/red-evidence/03-03-red-1-superset-not-equality.patch — an existing patch's exact shape
    - internal/fieldset/fieldset.go and internal/fieldset/fieldset_test.go as written by Task 2
  </read_first>
  <action>
**(a) Harden the RED predicate FIRST, before authoring either patch.** This addresses `06-REVIEWS.md`
HIGH-4 and durable record `366pjeht8e`. Today `TestRedEvidencePatchesAreLive` accepts any
`*exec.ExitError` as proof the mapped test went RED. It never asserts the named test ran, so a patch
that breaks compilation, an import, or package initialisation is indistinguishable from one that trips
an assertion. Phase 06 would inherit that weakness the moment it joins `redEvidenceDirs`, and
`06-01-red-2-golden-widening.patch` — the artifact 06-09 retains as its Success Criterion 3 proof —
would rest on it.

Extract the confirmation into a named, directly testable predicate
`confirmsTargetWentRed(target, output string) error` and require POSITIVE evidence:

- the transcript must contain a line whose trimmed form is `--- FAIL: <target>` or begins
  `--- FAIL: <target>/` (a failing subtest of the mapped target counts; a failing sibling test does
  not);
- the transcript must NOT contain `[build failed]`, `[setup failed]`, or `no tests to run` — each is
  its own named error, so the harness log says WHICH way the evidence was invalid rather than only
  that it was;
- a nil `runErr` and a non-`*exec.ExitError` `runErr` stay rejected exactly as they are today; exit
  status becomes a necessary precondition rather than the whole test.

Add `-v` to the `go test` invocation at line 303 so `--- FAIL:` lines are actually emitted, keeping
`-run '^<target>$' -count=1 ./...` otherwise unchanged — the `./...` scope is load-bearing, since a
phase's target test may live in a sibling package, and phase 06's targets live in `internal/fieldset`
and `cmd/engram`.

Add `TestRedEvidenceRedProofRejectsBuildFailure` in the same file: a table-driven test feeding
synthetic transcripts through `confirmsTargetWentRed` — a real `--- FAIL: TestX` transcript (accepted),
a `# pkg\n./x.go:1:1: undefined: y\nFAIL pkg [build failed]` transcript (rejected), a
`ok pkg 0.1s [no tests to run]` transcript (rejected), a transcript failing a DIFFERENT test name
(rejected), and a `--- FAIL: TestX/subcase` transcript (accepted). This is the gate on the gate, and it
is a unit test rather than a patch because a patch proving it would have to break the harness that
verifies patches.

Then re-run the harness across EVERY listed directory — phases 02 and 03 as well as 06 — and enumerate
the complete patch set with its result. Record `fqznw5nc1g` is explicit that sampling is not
verification. If hardening reveals an EXISTING phase-02 or phase-03 patch that only ever broke the
build, do not weaken the predicate to accommodate it: repair that patch so it trips its mapped test's
assertion, or, if repair is out of this plan's reach, leave the harness hardened, record the failing
patch by name in `06-01-SUMMARY.md`, open a GitHub issue for it, and say plainly in the SUMMARY that a
previously-green phase's evidence was found vacuous. A hardened gate that reports an old defect is the
gate working.

**(b)** Author exactly two mutation patches under
`.planning/phases/06-typed-operator-renderer/red-evidence/`, each a real unified diff produced with
`git diff` after making the mutation in the working tree, then reverting.

`06-01-red-1-unreferenced-field.patch` weakens the set-equality assertion inside
`internal/fieldset`'s unexported `validate` into a one-directional containment check — declared keys
must be referenced, with the reverse direction dropped — which is precisely the failure mode
`01mdq5qq9j` records and the direction the retired `TestOperatorOutputParity` never covered. It must
trip an ASSERTION, not the build: after applying it the package still compiles and only
`TestFieldSetCoverage_CatchesMissingField` fails. Its mapped target test is
`TestFieldSetCoverage_CatchesMissingField`.

`06-01-red-2-golden-widening.patch` adds ONE extra declared field plus its placeholder to
`pruneAppliedReport` — one field, one builder, nothing else — widening the report's json document past
its checked-in golden. It must likewise compile: the added field is referenced by a placeholder, so
`fieldset.New` accepts it and the failure lands on the golden comparison rather than on a panic from
`MustNew`. Its mapped target test is `TestOperatorReportGolden`.

Verify each patch's RED-ness under the HARDENED predicate from step (a), not merely under a non-zero
exit code, and record in `06-01-SUMMARY.md` the exact `--- FAIL:` line each one produced.

**(c)** Add one new entry to `redEvidenceDirs` in `internal/store/redevidence_harness_test.go` keyed
`.planning/phases/06-typed-operator-renderer/red-evidence`, mapping each patch basename to its target
test name. This map is DECLARED, not discovered: it fails closed within a listed directory and fails
OPEN across directories, so an unlisted phase 06 would be silently ungated (issue #501, record
`bqpfcnrnjs`). Adding the entry is the whole point of this task, not a side effect of it. Do not
disturb the phase 02 and phase 03 entries.

Enumerate the WHOLE patch set when reporting, never a sample: record `fqznw5nc1g` documents a prior
phase verifying 2 of 9 patches while the corrupt one sat in the unsampled remainder.
  </action>
  <verify>
    <automated>go test ./internal/store/ -count=1 -run '^(TestRedEvidencePatchesAreLive|TestRedEvidenceRedProofRejectsBuildFailure)$' -v 2>&1 | grep -E '^( *)(=== RUN|--- (PASS|FAIL))' </automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/store/ -list '^TestRedEvidenceRedProofRejectsBuildFailure$'` lists that name,
      and the `-v` run shows `--- PASS` for it plus one `--- PASS` subtest per synthetic transcript
      case named in the action — including the two REJECT cases, which are the ones that prove the
      hardened predicate no longer accepts a build failure as RED evidence.
    - `rg -n 'exec.ExitError' internal/store/redevidence_harness_test.go` shows the type used only as a
      necessary precondition, and `rg -n 'confirmsTargetWentRed' internal/store/redevidence_harness_test.go`
      shows it called on the RED path — exit status alone no longer confirms anything.
    - `rg -n '"-v"' internal/store/redevidence_harness_test.go` shows `-v` in the `go test` argument
      list, without which no `--- FAIL:` line is emitted to parse.
    - `go test ./internal/store/ -run '^TestRedEvidencePatchesAreLive$' -v` shows a
      `--- PASS` subtest line for
      `TestRedEvidencePatchesAreLive/.planning/phases/06-typed-operator-renderer/red-evidence` and a
      nested `--- PASS` for each of the two patch basenames — a package-level `ok` alone is not
      acceptance.
    - The same `-v` run shows a `--- PASS` subtest for EVERY directory in `redEvidenceDirs` and for
      every patch basename under each, and `06-01-SUMMARY.md` enumerates that complete set with each
      patch's mapped target and the `--- FAIL:` line it produced. A sampled subset is not acceptable
      (record `fqznw5nc1g`). If any pre-existing patch fails under the hardened predicate, the SUMMARY
      names it and the opened issue.
    - The harness log line `confirmed RED: 06-01-red-1-unreferenced-field.patch applied ->
      TestFieldSetCoverage_CatchesMissingField failed as expected` appears in that `-v` output, and the
      analogous line appears for `06-01-red-2-golden-widening.patch` and `TestOperatorReportGolden`.
    - `rg -n '06-typed-operator-renderer/red-evidence' internal/store/redevidence_harness_test.go`
      returns exactly one match, inside the `redEvidenceDirs` literal.
    - `ls .planning/phases/06-typed-operator-renderer/red-evidence/*.patch | wc -l` reports 2, and both
      basenames appear in that new map entry — the harness's own both-directions mapping check proves
      the sets are equal, so no patch is unmapped and no mapping entry is stale.
    - `git status --porcelain cmd/engram internal/store` is clean after the harness run; the harness
      applies and reverts real patches, so a dirty tree afterwards is a failure.
    - The plan SUMMARY enumerates BOTH patch filenames with their target tests; a sampled subset is not
      acceptable.
  </acceptance_criteria>
  <done>The harness confirms RED only on a named `--- FAIL: <target>` line and never on exit status alone; both phase-06 patches drive their mapped target tests RED under that hardened predicate; and phase 06's directory is a first-class entry in `redEvidenceDirs` rather than an unlisted directory the harness fails open across.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (no new boundary) | This phase adds no network-reachable input, no persisted data, no authentication or authorization path, and no dependency. Report values are already-computed, already-authorized results, and every `FieldSet.Text` template is a developer-authored Go string literal in package `main` — never attacker-controlled input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| 06-01:T-06-PARSE | Information Disclosure | `fieldset.parseText` / `fieldset.validate` / `Set.RenderText` | medium | mitigate | A coverage-checker/renderer parsing divergence would let a json document carry a value never described in its text sentence — the phase's one security-adjacent property (RESEARCH.md Security Domain). Mitigated structurally: ONE parser shared by the validator and both render lanes, the malformed-template negative suite added in Task 2's `<behavior>` block, and `06-01-red-1-unreferenced-field.patch` proving the checker can go RED. Not an ASVS control. |
| 06-01:T-06-BYPASS | Tampering | `fieldset.Set` construction | high | mitigate | If a `Set` could be assembled outside the validating constructor, the whole invariant is advisory — this is `06-REVIEWS.md` cycle-1 HIGH-1. Mitigated by the package boundary plus unexported struct fields (no NON-EMPTY composite literal is writable in `cmd/engram`), constructors that validate internally and share one `validate`, and an inert zero value that `renderOperator` rejects before writing a byte. The empty literal stays legal, so it is covered by 06-01:T-06-NESTED rather than assumed away. |
| 06-01:T-06-ALIAS | Tampering | `fieldset.New` / `fieldset.NewLabeled` parameter retention | high | mitigate | A constructor that stores its variadic `[]Field` validates a snapshot: the caller keeps a live handle to the backing array and can change a validated `Set`'s contents while `validated` stays true, and `Validated` — a construction-time bit — cannot see it (`06-REVIEWS.md` cycle-2 HIGH-1, reproduced by the reviewer; record `brwjpnx6bz`). Mitigated by cloning the variadic slice and every caller-owned `[]Set`/`[]Field`/`[]string` value BEFORE validating, by `Keys()`/`ListKeys()` returning fresh slices, and by `TestFieldSetNewClonesCallerSlices`, which fails for exactly this defect and for nothing else. |
| 06-01:T-06-LIVEVAL | Tampering | `fieldset.Field` value and render callback | high | mitigate | Cloning slices does not reach a POINTER or a CLOSURE, and the phase already shipped one: 06-06's `minScore *float32` with a `Render` closure dereferencing it (`06-REVIEWS.md` cycle-3 HIGH-2; the pointer is live at `cmd/engram/spine_review_consolidate.go:58,97,129,131`). Mutating the pointee after a successful `New` moved BOTH lanes while `validated` stayed true, and `TestFieldSetNewClonesCallerSlices` could not go red for it. Mitigated by property 6 (a CLOSED value-kind allowlist enforced in `validate`, rejecting every pointer, function, map, channel, and non-`Set` struct by key and `%T`) and property 7 (the render callback is invoked once inside `New` and its result stored, so `RenderText` never calls caller code), each with its own named test that fails for exactly that defect. Named residual: `time.Time`'s internal `*time.Location`, unreachable through any API this phase ships. |
| 06-01:T-06-D08UNEXEC | Repudiation | `TestOperatorReportGolden` per-lane golden resolution | medium | mitigate | A decision whose downstream branch cannot run is evidence of a policy nobody executed: cycle 2 added per-option branch tables to the conversion plans while the harness compared both lanes to one golden and sourced `-update` from the legacy lane, so options a and b were unexecutable and contradicted their own plans' "do not regenerate" text (`06-REVIEWS.md` cycle-3 HIGH-1). Mitigated by moving the branch into the harness: per-lane golden resolution keyed on a declared `Divergence`, one named source lane per file under `-update`, a mandatory `DivergenceReason`, and a `d08Option` compatibility assertion that fails a fixture diverging on the lane the recorded option freezes. |
| 06-01:T-06-NESTED | Tampering | `fieldset.validate` recursion | medium | mitigate | An unvalidated nested `Set` — reachable via the legal empty literal or `make([]fieldset.Set, n)` with partial assignment — would be dropped silently by `RenderText` while `MarshalJSON` returned `ErrUnvalidated`, a text/json divergence produced by the anti-divergence mechanism itself (`06-REVIEWS.md` cycle-2 MEDIUM-1). Mitigated by requiring `validated == true` on every nested `Set` and every `[]Set` element, with the error naming the key and row index, and by `TestFieldSetRejectsUnvalidatedNestedSet`. |
| 06-01:T-06-UNLABELED | Spoofing | `renderOperator` label guard | medium | mitigate | A nested row set rendered as a top-level report would be a report shape outside the derived `(command, variant)` universe 06-09 gates. Mitigated by `NewLabeled` minting the identity in production code, by `renderOperator` refusing a validated-but-unlabeled `Set` before writing a byte, and by `TestRenderOperatorRejectsUnlabeledSet`. |
| 06-01:T-06-VACUOUSRED | Repudiation | `TestRedEvidencePatchesAreLive` | high | mitigate | The harness accepted any non-zero exit as RED evidence, so a patch that merely broke the build proved nothing while reading as proof (`06-REVIEWS.md` HIGH-4, record `366pjeht8e`). Mitigated by requiring a named `--- FAIL: <target>` line, rejecting `[build failed]` / `[setup failed]` / `no tests to run` transcripts, and gating the predicate itself with `TestRedEvidenceRedProofRejectsBuildFailure`. |
| 06-01:T-06-CONTENT | Information Disclosure | `fieldset.Set` report builders | low | mitigate | Record content must stay unreachable from an operator report. Preserved by keeping every field hand-declared, by `internal/fieldset` importing no engram package at all, and by never passing an `internal/store` result type through; enforced per report by the goldens, which would show any content leak as changed bytes. |
| 06-01:T-06-NONE | (no applicable STRIDE category) | phase scope as a whole | low | accept | RESEARCH.md's ASVS review found V2/V3/V4/V6 inapplicable and V5 marginal (developer-authored templates, no external input). Recorded as a reasoned no-applicable-threat finding rather than a fabricated register. |

**Package legitimacy:** no package-manager install task exists in this phase. RESEARCH.md's Package
Legitimacy Audit records "Not applicable — this phase adds no external package to `go.mod`", and
`git diff go.mod go.sum` being empty is an acceptance criterion of Task 2.
</threat_model>

<verification>
- `task lint` and `task test` green.
- `go test ./internal/fieldset/... ./cmd/engram/... -count=1` green, with `TestOperatorOutputParity`
  still passing unmodified.
- `go test ./internal/store/ -run '^TestRedEvidencePatchesAreLive$' -v` shows EVERY listed directory's
  subtree passing under the hardened predicate, with both phase-06 patches confirmed RED.
- `git diff --exit-code cmd/engram/testdata/operator-reports/` clean after a `-update` re-run.
</verification>

<success_criteria>
- D-08 is decided by the developer and recorded, not chosen silently by the executor, and the selected
  option has a matching downstream execution branch in all seven conversion plans.
- `renderOperator` accepts a `fieldset.Set` and nothing else; `renderOperatorLegacy` carries the 17
  not-yet-converted call sites.
- A `fieldset.Set` is obtainable only from a validating constructor, no composite literal of `Set` or
  `Field` exists in `cmd/engram`'s production code, the zero value cannot render, and an unvalidated
  nested `Set` is rejected at construction rather than dropped at render.
- A validated `Set` cannot be changed after `New` returns through ANY handle its caller kept: not a
  retained slice (cloned), not a pointer or any other reference-shaped value (rejected by the closed
  value-kind allowlist), and not a closure over mutable state (the render callback is spent at
  construction). Each of the three has a test that goes red for exactly that defect, and the SUMMARY
  states the guarantee at exactly this width with both residuals named.
- The recorded D-08 option is EXECUTABLE end-to-end: the golden harness resolves a path per lane from
  each fixture's declared `Divergence`, `-update` sources every file from one named lane, and a fixture
  diverging on the lane the option freezes fails by name.
- Every production report variant is minted by `operatorReport(key, variant, ...)` with both identity
  arguments as string literals, and `renderOperator` refuses an unlabeled set — the two facts 06-09's
  derived variant universe rests on.
- `prune-expired` renders both lanes from one field set per variant, byte-identical to its pre-image.
- The red-evidence harness confirms RED only on a named `--- FAIL:` line; the coverage gate and the
  golden gate are each proven able to go RED by a checked-in patch under that hardened predicate; and
  phase 06 is a listed entry in `redEvidenceDirs`.
</success_criteria>

<output>
Create `.planning/phases/06-typed-operator-renderer/06-01-SUMMARY.md` when done. It MUST record the
D-08 selection verbatim along with its downstream execution contract row INCLUDING the `Divergence`
value every fixture in the phase will carry under it; state the construction-time
(not compile-time) residual in those words; state what the immutability guarantee covers and what it
does not, naming all THREE properties (clone the slices, reject the reference-shaped values, spend the
render callback) and both named residuals (`time.Time`'s internal `*time.Location`, and that a value
kind added to the allowlist later must be checked against all three or the guarantee silently narrows)
— do NOT restate the residual as "slice-shaped kinds added later", which is the partition
`06-REVIEWS.md` cycle-3 HIGH-2 found non-exhaustive; enumerate the COMPLETE red-evidence patch set across every
listed directory with each patch's target test and the `--- FAIL:` line it produced; and note
that `TestOperatorOutputParity`'s two retirement-relevant properties (its `facts` were hand-listed, and
it was one-directional text-to-json only) are carried forward to 06-09's SUMMARY per D-07.
</output>
