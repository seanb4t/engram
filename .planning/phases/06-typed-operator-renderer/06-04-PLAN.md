---
phase: 06-typed-operator-renderer
plan: 04
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - cmd/engram/migrate_family.go
  - cmd/engram/migrate_family_test.go
  - cmd/engram/backfill_test.go
  - cmd/engram/testdata/operator-reports/
autonomous: true
requirements: [REQ-operator-renderer-typed]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "engram migrate renders both sentence variants (preview, applied) from two fieldset.Set builders, byte-identical to their pre-image on both lanes (D-04)."
    - "engram backfill-short-ids keeps sharing migrate's report builder outright — no backfill-specific fieldset.Set builder is introduced — and its own golden pair proves the shared envelope still renders identically."
    - "engram migrate revert renders all four render sites (preview, applied, bare refusal, refusal carrying already-landed progress) from fieldset.Set builders, byte-identical to their pre-image on both lanes."
    - "migrateOutputDoc's and revertOutputDoc's mode-dependent fields — would_migrate, migrated, failed, passes, backlog, spared, appeared, none of which carries omitempty today — are resolved by the D-08 selection recorded in 06-01-SUMMARY.md, never by a per-mode presence guess."
    - "revertOutputDoc's Refusal field, which carries omitempty and belongs to a wholly different sentence shape rather than to a dropped segment, is modelled as its own sentence variant with its own builder, not as a conditional span inside the preview or applied variant."
    - "Every field of every converted variant is referenced by exactly one placeholder in that variant's template, proven by fieldset.New's validation passing on each builder's output (D-03)."
    - "Goldens for all seven variants are generated from the pre-image builders BEFORE conversion and are unchanged by the conversion commit."
  artifacts:
    - cmd/engram/testdata/operator-reports/migrate.preview.txt
    - cmd/engram/testdata/operator-reports/migrate.applied.txt
    - cmd/engram/testdata/operator-reports/backfill-short-ids.applied.txt
    - cmd/engram/testdata/operator-reports/migrate-revert.preview.txt
    - cmd/engram/testdata/operator-reports/migrate-revert.applied.txt
    - cmd/engram/testdata/operator-reports/migrate-revert.refusal.txt
    - cmd/engram/testdata/operator-reports/migrate-revert.refusal-with-progress.txt
  key_links:
    - "migrateSweepReport / revertReport -> renderOperator(fieldset.Set) -> TestOperatorReportGolden -> the frozen goldens"
    - "backfill-short-ids' delegating run paths -> migrateSweepReport (shared, never duplicated)"
  prohibitions:
    - "Do not add a backfill-specific fieldset.Set builder; backfill-short-ids is a thin delegating alias and must keep sharing migrate's builder."
    - "Do not set a field's Present predicate from which mode is running; default every field to always-present unless today's struct tag already carries omitempty."
    - "Do not model migrate revert's refusal path as a conditional span inside another variant; it is a different sentence shape and gets its own builder."
    - "Do not delete migrateSummary, migrateReportDoc, migrateOutputDoc, revertSummary, revertReportDoc, or revertOutputDoc in this plan."
    - "Do not touch statusSummary, statusReportDoc, or the migrate status render call site; plan 06-08 owns them."
    - "Do not modify or delete TestOperatorOutputParity or operatorParityRows."
    - "Do not edit cmd/engram/operator_golden_test.go."
    - "Do not regenerate any golden file in the conversion commit EXCEPT exactly as the `## D-08 downstream execution contract` section permits for the option 06-01-SUMMARY.md recorded, and never without the named per-key or per-sentence justification that section requires. Under option-c both lanes stay frozen."
    - "Do not change any operator-facing sentence byte (D-04)."
---

<objective>
Convert `migrate`'s sweep reports (shared by `backfill-short-ids`) and `migrate revert`'s four render
sites to `fieldset.Set` builders, gated by goldens generated from their pre-image.

Purpose: this is the largest multi-variant Group A conversion — six of the phase's nineteen render call
sites live in this one file, and `migrate revert`'s refusal path is the tier's only case where a
report's conditionality is a whole different sentence rather than a dropped segment.

Output: `migrateSweepReport`, `revertReport`, seven checked-in golden pairs, and six call sites moved
off `renderOperatorLegacy`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-typed-operator-renderer/06-CONTEXT.md
@.planning/phases/06-typed-operator-renderer/06-PATTERNS.md
@.planning/phases/06-typed-operator-renderer/06-01-SUMMARY.md
@internal/fieldset/fieldset.go
@cmd/engram/prune.go
</context>

## Artifacts this phase produces

See `06-01-PLAN.md` § "Artifacts this phase produces" for the full phase-level list. This plan
contributes: `migrateSweepReport`, `revertReport`, and the seven golden pairs named in `must_haves`.

## D-08 downstream execution contract

Added in response to `06-REVIEWS.md` HIGH-2: the first-cycle plans froze both lanes unconditionally,
which options a and b of the D-08 checkpoint necessarily violate — leaving two of three options with no
executable downstream path. All three now branch. Read the option recorded in `06-01-SUMMARY.md` and
follow exactly one row.

| Selected option | This plan's goldens | This plan's conversion-commit acceptance criterion |
|---|---|---|
| **a** — the field set is the json shape | `*.txt` frozen; `*.json` REGENERATED for the variants whose key set changes | `git diff --exit-code` clean over `cmd/engram/testdata/operator-reports/*.txt`. Every changed `*.json` is listed in this plan's SUMMARY with the dropped or added key and its D-08 rationale; a `*.json` that changed without a SUMMARY line is a defect, not an update. |
| **b** — the sentence follows the field set | `*.json` frozen; `*.txt` REGENERATED for the variants whose sentence gains text | `git diff --exit-code` clean over `cmd/engram/testdata/operator-reports/*.json`. Every changed `*.txt` is listed in the SUMMARY with its old and new bytes; D-04 must already have been amended in `06-CONTEXT.md` by 06-01. |
| **c** — bounded coverage exemption | both lanes frozen | `git diff --exit-code cmd/engram/testdata/operator-reports/` clean, and every field taking the exemption in this plan appears in the closed allowlist with its one-line rationale. |

Task 1's pre-image freeze is unaffected under all three options: the goldens are always generated from
the pre-image first, in a commit carrying no production-code change. Only Task 2's behavior branches.
Under a and b the freeze is replaced by a justification requirement, never by silence.

**The fields this plan routes through the decision:** `migrateOutputDoc`'s and `revertOutputDoc`'s
mode-dependent fields — `would_migrate`, `migrated`, `failed`, `passes`, `backlog`, `spared`,
`appeared` — none of which carries `omitempty` today. This plan owns seven variants, and `revertSummary`
alone has three shapes (refusal, preview, applied), so enumerate the affected `(variant, key)` pairs
explicitly in the SUMMARY under every option: under a, which keys drop from which variant's `.json`;
under b, which sentence bytes each affected `.txt` gained; under c, which fields took the exemption.

<tasks>

<task type="auto">
  <name>Task 1: Freeze migrate, backfill-short-ids, and migrate revert goldens from the pre-image</name>
  <files>cmd/engram/migrate_family_test.go, cmd/engram/backfill_test.go, cmd/engram/testdata/operator-reports/</files>
  <read_first>
    - cmd/engram/operator_golden_test.go — `registerOperatorReport`'s parameter order and `TestOperatorReportGolden`'s comparison contract
    - cmd/engram/prune_test.go — the `init()` registration 06-01 wrote
    - cmd/engram/migrate_family.go lines 80-100 (`migrateOutputDoc`), 160-220 (both sweep render call sites and `migrateSummary`)
    - cmd/engram/migrate_family.go lines 325-345 (`revertOutputDoc`), 380-410 (`revertSummary`'s three shapes), and the four revert render call sites at 455, 517, 539, 548
    - cmd/engram/backfill.go lines 19-58 — the doc comment establishing backfill-short-ids as a thin delegating alias sharing migrate's envelope
    - cmd/engram/operator_output_test.go lines 170-180 and 255-285 — `operatorParityRows`' existing hand-built migrate, backfill, and revert inputs, reused verbatim
  </read_first>
  <action>
Add an `init()` to `cmd/engram/migrate_family_test.go` registering two fixtures under key `migrate`
(variants `preview` and `applied`) and four under key `migrate-revert` (variants `preview`, `applied`,
`refusal`, `refusal-with-progress`). Add an `init()` to `cmd/engram/backfill_test.go` registering one
fixture under key `backfill-short-ids` with variant `applied`, built from the SAME shared migrate
sentence and document pair the backfill run paths already use — never a backfill-specific formatter.
Every registration passes nil for the `Report` argument.

Reuse `operatorParityRows`' existing inputs verbatim where they exist: `store.MigrateResult{Migrated:
23, Failed: 2, Passes: 1, Backlog: 5, Spared: []string{"a"}, Appeared: []string{"b", "c"}}` at
`migrate.CurrentVersion` with 26 scanned for `migrate`, `store.MigrateResult{Migrated: 29, Backlog: 0}`
with 29 scanned for `backfill-short-ids`, and `store.RevertPlan{To: 0, Candidates: 12, Reversible:
true}` for `migrate-revert`'s preview. Construct the two refusal fixtures from the inputs
`TestMigrateFamilyRevertRefusals` and `TestMigrateFamilyRevertApplyRefusalReportsPartialProgress`
already exercise, so both refusal sub-shapes are covered and neither is invented. Every input must be
deterministic.

Generate the fourteen golden files with the package `-update` flag, inspect them by eye against current
CLI output, then commit. This commit contains no production-code change.

Record in the SUMMARY the exact json key set each of the seven variants emits — this is the baseline
the D-08 selection is applied against in Task 2, and RESEARCH.md names a per-mode presence guess here
as the phase's highest-probability silent regression.
  </action>
  <verify>
    <automated>go test ./cmd/engram/... -count=1 -run 'TestOperatorReportGolden' -v 2>&1 | grep -E '^( *)(=== RUN|--- (PASS|FAIL))'</automated>
  </verify>
  <acceptance_criteria>
    - `ls cmd/engram/testdata/operator-reports/ | rg -c '^(migrate|backfill-short-ids|migrate-revert)\.'`
      reports 14, and `ls cmd/engram/testdata/operator-reports/ | rg -c '^migrate-status\.'` reports 0
      — `migrate status` belongs to plan 06-08 and must not appear here.
    - `go test ./cmd/engram/... -run 'TestOperatorReportGolden' -v` shows a `--- PASS` subtest line for
      each of the seven new `<key>/<variant>` fixtures.
    - `TestOperatorReportGoldenSetIsComplete` passes.
    - `git diff --stat cmd/engram/migrate_family.go` is empty for this commit.
    - `cmd/engram/testdata/operator-reports/migrate.preview.json` and `migrate.applied.json` carry the
      identical key set, since none of `would_migrate`, `migrated`, `failed`, `passes`, `backlog`,
      `spared`, or `appeared` carries `omitempty` today — these two files are the literal record of the
      pre-conversion key set.
    - `cmd/engram/testdata/operator-reports/migrate-revert.refusal.json` carries a `refusal` key while
      `migrate-revert.preview.json` does not, since that field alone carries `omitempty`.
  </acceptance_criteria>
  <done>All seven variants are pinned to committed golden bytes produced by the pre-image code, with each variant's json key set recorded explicitly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert migrate's sweep reports and migrate revert to fieldset.Set builders</name>
  <files>cmd/engram/migrate_family.go, cmd/engram/migrate_family_test.go, cmd/engram/backfill_test.go</files>
  <read_first>
    - internal/fieldset/fieldset.go — `F`, `WithPresent`, `New`, `MustNew`, `fieldset.New`'s validation, placeholder syntax
    - cmd/engram/prune.go — `prunePreviewReport` and `pruneAppliedReport`, the shape to imitate
    - .planning/phases/06-typed-operator-renderer/06-01-SUMMARY.md — the D-08 selection
    - .planning/phases/06-typed-operator-renderer/06-RESEARCH.md § "Pitfall 1", § "Anti-Patterns to Avoid", and Assumptions Log row A3
    - cmd/engram/migrate_family.go lines 80-100, 160-220, 325-345, 380-410, and the four revert render call sites at 455, 517, 539, 548
  </read_first>
  <behavior>
    - `fieldset.Validated(migrateSweepReport(...))` returns nil for both variants.
    - `fieldset.Validated(revertReport(...))` returns nil for all four variants.
    - `TestOperatorReportGolden` passes for all seven fixtures with `Report` now non-nil.
    - `TestMigrateFamilyPreviewAndApply`, `TestMigrateFamilyReportFields`,
      `TestMigrateFamilyRevertRefusals`, `TestMigrateFamilyRevertReversible`,
      `TestMigrateFamilyRevertApplyRefusalReportsPartialProgress`, and the four
      `TestBackfill...Envelope`/`Parity`/`Delegates` tests continue to pass without edits to their
      assertions.
  </behavior>
  <action>
Add `migrateSweepReport(res store.MigrateResult, target <version type>, dryRun bool, scanned uint64)
fieldset.Set` to `cmd/engram/migrate_family.go`, returning one of two distinct `fieldset.Set` values, one per
sentence variant. Both `migrate`'s two render call sites and, transitively,
`backfill-short-ids`' delegating run paths use this one builder — do not add a backfill-specific
builder, and do not change `backfill.go`.

Add `revertReport(plan store.RevertPlan, applied bool, res store.RevertResult) fieldset.Set` returning one
of four distinct `fieldset.Set` values. The refusal path is a wholly different sentence shape, not a
segment dropped from the preview or applied sentence, so it gets its own variants — one bare and one
carrying already-landed progress. Point all four revert render call sites at `renderOperator`.

For every field carrying a mode-dependent value, set `Present` from today's struct tag, never from
which mode is running: `migrateOutputDoc`'s `would_migrate`, `migrated`, `failed`, `passes`, `backlog`,
`spared`, and `appeared` carry no `omitempty` today and are emitted in both modes. Apply the D-08
selection from `06-01-SUMMARY.md` to whichever fields have no textual presence in the running variant;
do not re-decide it here. `revertOutputDoc`'s `refusal` is the one genuinely `omitempty` field and
belongs only to the refusal variants.

Reproduce all seven sentences byte-identically (D-04), attaching a `Render` function to any field whose
prose form differs from its natural Go form rather than editing template literals. Leave
`migrateSummary`, `migrateReportDoc`, `migrateOutputDoc`, `revertSummary`, `revertReportDoc`, and
`revertOutputDoc` in place for 06-09, and leave `statusSummary`, `statusReportDoc`, and the
`migrate status` render call site entirely alone. Also declare this plan's variant inventory beside the
builders with one `registerReportVariants(key, variants...)` call per command, naming every variant this
plan converts — `revertSummary`'s refusal, preview and applied shapes included, since `06-REVIEWS.md`
HIGH-3 names them as the multi-shape report the first-cycle gate left structurally ungated. 06-09's
pair-level gate holds these set-equal to the golden fixtures.
Update all three `init()` registrations to pass the
new builders as `Report`, and regenerate nothing.
  </action>
  <verify>
    <automated>go test ./cmd/engram/... -count=1 -run 'TestMigrateFamily|TestBackfill|TestOperatorReportGolden|TestOperatorOutputParity|TestOperatorOutputEmpty' -v 2>&1 | grep -E '^( *)(=== RUN|--- (PASS|FAIL)|FAIL|ok)'</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd/engram/... -list 'TestMigrateFamily.*|TestBackfill.*'` lists at least
      `TestMigrateFamilyPreviewAndApply`, `TestMigrateFamilyApplyIntersection`,
      `TestMigrateFamilyReportFields`, `TestMigrateFamilyRevertRefusals`,
      `TestMigrateFamilyRevertApplySecondPreflightRefusal`, `TestMigrateFamilyRevertReversible`,
      `TestMigrateFamilyRevertToValidation`,
      `TestMigrateFamilyRevertApplyRefusalReportsPartialProgress`,
      `TestBackfillPreviewsByDefaultAndSharesMigrateEnvelope`,
      `TestBackfillApplyPerformsSharedEnvelope`, `TestBackfillApplyPathParityWithMigrateApply`, and
      `TestBackfillPreBackfilledRecordDelegates`, and the `-v` run shows `--- PASS` for each.
    - `git diff --exit-code cmd/engram/testdata/operator-reports/` is clean for this commit.
    - `rg -n 'renderOperatorLegacy\(' cmd/engram/migrate_family.go | wc -l` reports 1 — the sole
      remaining site is `migrate status`, which plan 06-08 owns.
    - `rg -n 'func migrateSweepReport\(|func revertReport\(' cmd/engram/` returns exactly one match
      each, and `rg -n 'fieldset\.' cmd/engram/backfill.go` returns no match.
    - `TestOperatorOutputParity` still shows `--- PASS`, and
      `git diff --stat cmd/engram/operator_output_test.go` is empty for this commit.
    - `task lint` and `task test` are green.
  </acceptance_criteria>
  <done>Six of the phase's nineteen render call sites now take a fieldset.Set, the shared migrate envelope still serves backfill-short-ids from one builder, and the pre-image goldens are unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (no new boundary) | Conversion of already-authorized operator reports. No new input, persisted data, authz path, or dependency. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| 06-04:T-06-PARSE | Information Disclosure | `migrateSweepReport`, `revertReport` | medium | mitigate | A converted report whose json states more than its sentence. Mitigated by `fieldset.New`'s validation plus the frozen goldens; inherits 06-01:T-06-PARSE's red-evidence proof. |
| 06-04:T-06-MODEKEY | Tampering | `migrateOutputDoc`'s and `revertOutputDoc`'s mode-dependent fields | high | mitigate | Silently dropping a json key a consumer relies on across seven variants — the phase's highest-probability regression per RESEARCH.md assumption A3. Mitigated by setting `Present` from today's struct tag rather than the running mode, and by the fourteen frozen golden files carrying the pre-conversion key sets. |
| 06-04:T-06-ALIAS | Spoofing | `backfill-short-ids`' delegating run paths | low | mitigate | A backfill-specific builder would let the alias drift from the command it claims to alias. Mitigated by the prohibition plus the acceptance criterion that `backfill.go` contains no field-set reference. |
| 06-04:T-06-NONE | (no applicable STRIDE category) | plan scope | low | accept | RESEARCH.md's ASVS review found no conventional threat applies. Recorded, not fabricated. |

**Package legitimacy:** no install task in this plan; `go.mod` and `go.sum` are untouched.
</threat_model>

<verification>
- `task lint` and `task test` green.
- `go test ./cmd/engram/... -count=1` green with `TestOperatorOutputParity` unmodified and passing.
- `git diff --exit-code cmd/engram/testdata/operator-reports/` clean after the conversion commit.
- Exactly one `renderOperatorLegacy` call remains in `migrate_family.go`.
</verification>

<success_criteria>
- `migrate`, `backfill-short-ids`, and `migrate revert` render from `fieldset.Set` builders.
- Seven golden pairs, frozen from the pre-image, survive the conversion byte-for-byte.
- The refusal path is its own sentence variant with its own builder, not a conditional span.
</success_criteria>

<output>
Create `.planning/phases/06-typed-operator-renderer/06-04-SUMMARY.md` when done, naming the seven
fixture keys and recording each variant's json key set before and after conversion.

It MUST additionally enumerate this plan's per-command VARIANT inventory by name — 06-09's
pair-level gate reconciles against it, and record `1xe3ze1v9s` says re-derive rather than
transcribe — and, under D-08 option-a or option-b, list every regenerated golden with its
per-key or per-sentence justification. A golden that changed without a line here is a defect.
</output>
