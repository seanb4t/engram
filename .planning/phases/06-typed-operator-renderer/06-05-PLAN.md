---
phase: 06-typed-operator-renderer
plan: 05
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - cmd/engram/spine_review_archive.go
  - cmd/engram/spine_review_archive_test.go
  - cmd/engram/spine_review_scan.go
  - cmd/engram/spine_review_test.go
  - cmd/engram/testdata/operator-reports/
autonomous: true
requirements: [REQ-operator-renderer-typed]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "engram spine-review archive and engram spine-review restore render from one shared FieldSet builder whose per-row lines come from a []FieldSet-valued field with an explicit newline separator (D-05), byte-identical to their pre-image on both lanes."
    - "archiveResultDoc's ID is the tier's one genuine D-06 case: a single Present predicate drops the json key AND the surrounding ' id={id}' template span together, so a row whose token resolved to nothing renders exactly as it does today with no stray literal left behind."
    - "engram spine-review scan renders its header, its three fixed count lines, and its trailing per-(scope,category) breakdown list from one FieldSet, byte-identical to its pre-image on both lanes."
    - "spineScanSummary's rendered scan target, which spineScanReportDoc has no json key for today, is resolved by the D-08 selection recorded in 06-01-SUMMARY.md — this is the phase's one confirmed instance of the sentence stating what the json cannot."
    - "Every field of every converted variant, and of every nested row field set, is referenced by exactly one placeholder in its own template, proven by validateFieldSet recursing through the nested values (D-03 + D-05)."
    - "Goldens for all four variants are generated from the pre-image builders BEFORE conversion and are unchanged by the conversion commit."
  artifacts:
    - cmd/engram/testdata/operator-reports/spine-review-archive.rows.txt
    - cmd/engram/testdata/operator-reports/spine-review-archive.unresolved-id.txt
    - cmd/engram/testdata/operator-reports/spine-review-restore.rows.txt
    - cmd/engram/testdata/operator-reports/spine-review-scan.only.txt
  key_links:
    - "archiveReport's []FieldSet rows -> validateFieldSet's recursion -> per-row coverage one level down (D-05)"
    - "archiveResultDoc.ID's Present predicate -> both the json key and the text span, from one decision (D-06)"
    - "archiveReport / spineScanReport -> renderOperator(FieldSet) -> TestOperatorReportGolden -> the frozen goldens"
  prohibitions:
    - "Do not give a []FieldSet-valued field an implicit newline separator; the separator is an explicit per-field property because migrate status joins inline while every other list report joins on a newline."
    - "Do not implement the conditional id segment as a bare placeholder substituting to the empty string; that leaves the surrounding literal behind, which is the exact regression class D-06 exists to prevent."
    - "Do not pass a store result type through to a report; archiveReportDoc's own doc comment records that the record-content exclusion is enforced by the hand-declared type itself, and the field set must preserve that property."
    - "Do not delete archiveSummary, archiveResultDoc, archiveReportDoc, archiveDoc, spineScanSummary, spineScanReportDoc, spineScanBreakdownDoc, or spineScanDoc in this plan."
    - "Do not modify or delete TestOperatorOutputParity or operatorParityRows."
    - "Do not edit cmd/engram/operator_golden_test.go."
    - "Do not regenerate any golden file in the conversion commit."
    - "Do not change any operator-facing sentence byte (D-04)."
---

<objective>
Convert `spine-review archive`, `spine-review restore`, and `spine-review scan` to `FieldSet` builders,
exercising the D-05 nesting mechanism and the tier's one genuine D-06 conditional-span case.

Purpose: `archiveSummary` is the canonical two-level report — a header plus one line per row, with the
`id=` segment dropping when a token resolved to nothing — and is therefore the proof that the nesting
and conditional-span mechanisms work against real production output. `spine-review scan` adds the
trailing-list shape and the phase's one confirmed sentence-states-what-the-json-lacks case.

Output: `archiveReport`, `spineScanReport`, four checked-in golden pairs, and two call sites moved off
`renderOperatorLegacy`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-typed-operator-renderer/06-CONTEXT.md
@.planning/phases/06-typed-operator-renderer/06-PATTERNS.md
@.planning/phases/06-typed-operator-renderer/06-01-SUMMARY.md
@cmd/engram/fieldset.go
@cmd/engram/prune.go
</context>

## Artifacts this phase produces

See `06-01-PLAN.md` § "Artifacts this phase produces" for the full phase-level list. This plan
contributes: `archiveReport`, `spineScanReport`, and the four golden pairs named in `must_haves`.

<tasks>

<task type="auto">
  <name>Task 1: Freeze archive, restore, and scan goldens from the pre-image, including an unresolved-id row</name>
  <files>cmd/engram/spine_review_archive_test.go, cmd/engram/spine_review_test.go, cmd/engram/testdata/operator-reports/</files>
  <read_first>
    - cmd/engram/operator_golden_test.go — `registerOperatorReport`'s parameter order and `TestOperatorReportGolden`'s comparison contract
    - cmd/engram/prune_test.go — the `init()` registration 06-01 wrote
    - cmd/engram/spine_review_archive.go lines 22-77 — `archiveSummary`'s row loop and its `ID == ""` branch, `archiveResultDoc`, `archiveReportDoc`, `archiveDoc`
    - cmd/engram/spine_review_scan.go lines 78-147 — `spineScanReportDoc`, `spineScanDoc`, `spineScanSummary`
    - cmd/engram/operator_output_test.go lines 193-244 — `operatorParityRows`' existing hand-built scan, archive, and restore inputs, reused verbatim
  </read_first>
  <action>
Locate the test file that currently declares `TestSpineScanSummaryFormat` with
`rg -l 'func TestSpineScanSummaryFormat'` and register scan's fixture there, so registration sits
beside the report's existing tests rather than in a file another plan may touch.

Register four fixtures, each passing nil for the `Report` argument:

- key `spine-review-archive`, variant `rows` — reuse `operatorParityRows`' archive inputs verbatim
  (`id-changed` with the changed outcome, `id-already` with the already outcome).
- key `spine-review-archive`, variant `unresolved-id` — a NEW fixture whose result slice contains one
  row with an empty canonical id and the not-found outcome alongside one fully-resolved row. This
  fixture exists specifically to pin the D-06 drop; `operatorParityRows` has no row with an empty
  canonical id, so this shape is currently ungoldened.
- key `spine-review-restore`, variant `rows` — reuse `operatorParityRows`' restore inputs verbatim.
- key `spine-review-scan`, variant `only` — reuse `operatorParityRows`' `store.SpineScanResult` verbatim
  with the same scan target, and add at least two breakdown entries so the trailing list is non-empty
  and its ordering is pinned. Set `ScannedAt` to a fixed instant; no wall-clock reads.

Generate the eight golden files with the package `-update` flag, inspect them by eye against current
CLI output, then commit. This commit contains no production-code change.

Record in the SUMMARY that `spine-review-scan.only.json` carries no key for the scan target rendered in
`spine-review-scan.only.txt`'s header — the phase's one confirmed instance of the sentence stating a
value the json has no key for, which Task 2 resolves under D-08.
  </action>
  <verify>
    <automated>go test ./cmd/engram/... -count=1 -run 'TestOperatorReportGolden' -v 2>&1 | grep -E '^( *)(=== RUN|--- (PASS|FAIL))'</automated>
  </verify>
  <acceptance_criteria>
    - `ls cmd/engram/testdata/operator-reports/ | rg -c '^(spine-review-archive|spine-review-restore|spine-review-scan)\.'`
      reports 8.
    - `go test ./cmd/engram/... -run 'TestOperatorReportGolden' -v` shows a `--- PASS` subtest line for
      each of the four new `<key>/<variant>` fixtures.
    - `TestOperatorReportGoldenSetIsComplete` passes.
    - `git diff --stat cmd/engram/spine_review_archive.go cmd/engram/spine_review_scan.go` is empty for
      this commit.
    - In `cmd/engram/testdata/operator-reports/spine-review-archive.unresolved-id.txt` exactly one row
      line lacks an `id=` segment and exactly one carries it, and the corresponding `.json` has exactly
      one results element without an `id` key — the pre-image behavior the D-06 conversion must
      reproduce.
    - `cmd/engram/testdata/operator-reports/spine-review-scan.only.json` contains a non-empty
      `by_scope_category` array with at least two elements in the order the fixture declares them.
  </acceptance_criteria>
  <done>All four variants, including a row whose token resolved to nothing, are pinned to committed golden bytes produced by the pre-image code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert archive, restore, and scan to nested FieldSet builders</name>
  <files>cmd/engram/spine_review_archive.go, cmd/engram/spine_review_archive_test.go, cmd/engram/spine_review_scan.go, cmd/engram/spine_review_test.go</files>
  <read_first>
    - cmd/engram/fieldset.go — `Field`, `FieldSet`, `present`, `Sep`, `validateFieldSet`'s recursion, and the conditional-span placeholder syntax
    - cmd/engram/prune.go — `prunePreviewReport` and `pruneAppliedReport`, the flat shape to build on
    - .planning/phases/06-typed-operator-renderer/06-01-SUMMARY.md — the D-08 selection
    - .planning/phases/06-typed-operator-renderer/06-PATTERNS.md § Group B, including the canonical D-06 excerpt and the per-report join ledger
    - cmd/engram/spine_review_archive.go lines 22-77 and cmd/engram/spine_review_scan.go lines 78-147
  </read_first>
  <behavior>
    - `validateFieldSet(archiveReport(...))` returns nil for a result slice containing both a resolved
      row and an unresolved-id row, and its recursion reaches each row's own field set.
    - `validateFieldSet(spineScanReport(...))` returns nil, including its nested breakdown rows.
    - `TestOperatorReportGolden` passes for all four fixtures with `Report` now non-nil, proving both
      lanes byte-match the pre-image goldens — including the unresolved-id row's dropped span.
    - `TestArchiveSummaryFormat`, `TestArchiveReportCorrelatesRequestedToken`,
      `TestSpineReviewArchiveIDDoesNotLeakBetweenRows`, and `TestSpineScanSummaryFormat` continue to
      pass without edits to their assertions.
  </behavior>
  <action>
Add `archiveReport(results []store.ArchiveResult, verb string) FieldSet` to
`cmd/engram/spine_review_archive.go`. The header carries the verb and the row count; the rows are a
single `[]FieldSet`-valued field whose `Sep` is an explicit newline — never an implicit default, since
`migrate status` joins inline and no single default is correct (RESEARCH.md assumption A4). Each row's
own `FieldSet` declares `requested`, `id`, and `outcome`, with `id` carrying
`Present: present(r.ID != "")` and referenced from a conditional span so the json key and the leading
space plus `id=` literal drop together — one decision read by both lanes, which is D-06's whole point.
Do not substitute a bare placeholder to the empty string; that leaves the literal behind. Preserve the
trailing-newline trim the pre-image applies so the rendered sentence stays byte-identical, and keep
every row field hand-declared so record content stays unreachable by construction, exactly as
`archiveReportDoc`'s doc comment already requires. One builder serves both verbs, as today.

Add `spineScanReport(res store.SpineScanResult, scope string) FieldSet` to
`cmd/engram/spine_review_scan.go`: the header, the three fixed count lines, then a `[]FieldSet`-valued
breakdown field with an explicit newline `Sep`, each element declaring `scope`, `category`, and
`count`. The scan target rendered in the header has no json key today; apply the D-08 selection from
`06-01-SUMMARY.md` to it rather than re-deciding, and state in the SUMMARY which way it went.

Point both render call sites at `renderOperator` with the new builders. Leave `archiveSummary`,
`archiveResultDoc`, `archiveReportDoc`, `archiveDoc`, `spineScanSummary`, `spineScanReportDoc`,
`spineScanBreakdownDoc`, and `spineScanDoc` in place for 06-09. Update both `init()` registrations to
pass the new builders as `Report`, and regenerate nothing.
  </action>
  <verify>
    <automated>go test ./cmd/engram/... -count=1 -run 'TestArchive|TestSpineReviewArchive|TestSpineReviewRestore|TestSpineScan|TestSpineReviewScan|TestOperatorReportGolden|TestOperatorOutputParity|TestOperatorOutputEmpty|TestOperatorOutputEncoding' -v 2>&1 | grep -E '^( *)(=== RUN|--- (PASS|FAIL)|FAIL|ok)'</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd/engram/... -list 'TestArchive.*|TestSpineReviewArchive.*|TestSpineReviewRestore.*|TestSpineScan.*|TestSpineReviewScan.*'`
      lists at least `TestArchiveSummaryFormat`, `TestArchiveReportCorrelatesRequestedToken`,
      `TestArchiveDocMarshalsExplicitOutcomes`, `TestArchiveDocEmptyResultsMarshalsEmptyArray`,
      `TestSpineReviewArchiveIDDoesNotLeakBetweenRows`, `TestSpineReviewArchiveRejectsInvalidOutput`,
      `TestSpineReviewRestoreRejectsMissingID`, `TestSpineScanSummaryFormat`,
      `TestSpineScanDocEmptyResultMarshalsEmptyArray`, and `TestSpineReviewScanRejectsInvalidOutput`,
      and the `-v` run shows `--- PASS` for each.
    - `git diff --exit-code cmd/engram/testdata/operator-reports/` is clean for this commit.
    - `rg -n 'renderOperatorLegacy\(' cmd/engram/spine_review_archive.go cmd/engram/spine_review_scan.go`
      returns no match.
    - `rg -n 'func archiveReport\(|func spineScanReport\(' cmd/engram/` returns exactly one match each.
    - Every `Field` whose value is a `[]FieldSet` in these two files sets a non-empty `Sep`; verify with
      `rg -n -A3 'Val: *rows|Val: *breakdown' cmd/engram/spine_review_archive.go cmd/engram/spine_review_scan.go`
      and confirm each hit is followed by a `Sep:` assignment.
    - `TestOperatorOutputParity`, `TestOperatorOutputEmpty`, and `TestOperatorOutputEncoding` still show
      `--- PASS`, and `git diff --stat cmd/engram/operator_output_test.go` is empty for this commit.
    - `task lint` and `task test` are green.
  </acceptance_criteria>
  <done>The canonical two-level report and its conditional row segment render from one nested field set per report, byte-identical to their pre-image, with per-row coverage enforced one level down.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (no new boundary) | Conversion of already-authorized operator reports. No new input, persisted data, authz path, or dependency. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| 06-05:T-06-PARSE | Information Disclosure | `archiveReport`, `spineScanReport` | medium | mitigate | A converted report whose json states more than its sentence, now including nested rows. Mitigated by `validateFieldSet` recursing into nested values plus the frozen goldens; inherits 06-01:T-06-PARSE's red-evidence proof. |
| 06-05:T-06-CONTENT | Information Disclosure | nested row field sets | medium | mitigate | These two reports are the ones whose doc comments state that the record-content exclusion is enforced by the hand-declared type itself. Mitigated by keeping every row field hand-declared and never passing a store result type through, with the goldens showing any leak as changed bytes. |
| 06-05:T-06-SPAN | Tampering | the conditional `id` span | medium | mitigate | A bare placeholder substituting to the empty string would leave a stray literal in the sentence and could desynchronise the two lanes. Mitigated by the span-drop mechanism, the `unresolved-id` golden fixture, and `TestSpineReviewArchiveIDDoesNotLeakBetweenRows`. |
| 06-05:T-06-NONE | (no applicable STRIDE category) | plan scope | low | accept | RESEARCH.md's ASVS review found no conventional threat applies. Recorded, not fabricated. |

**Package legitimacy:** no install task in this plan; `go.mod` and `go.sum` are untouched.
</threat_model>

<verification>
- `task lint` and `task test` green.
- `go test ./cmd/engram/... -count=1` green with `TestOperatorOutputParity` unmodified and passing.
- `git diff --exit-code cmd/engram/testdata/operator-reports/` clean after the conversion commit.
</verification>

<success_criteria>
- Archive, restore, and scan render from nested `FieldSet` builders through the new `renderOperator`.
- The unresolved-id row drops its json key and its text span together, from one predicate.
- Four golden pairs, frozen from the pre-image, survive the conversion byte-for-byte.
</success_criteria>

<output>
Create `.planning/phases/06-typed-operator-renderer/06-05-SUMMARY.md` when done, naming the four
fixture keys, stating how D-08 resolved `spine-review scan`'s rendered scan target, and confirming the
D-06 span-drop reproduced the unresolved-id row byte-for-byte.
</output>
