---
phase: 06-typed-operator-renderer
plan: 03
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - cmd/engram/migrate.go
  - cmd/engram/migrate_test.go
  - cmd/engram/testdata/operator-reports/
autonomous: true
requirements: [REQ-operator-renderer-typed]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "engram migrate-set-owner renders its single sentence variant from a NAMED FieldSet builder, not from a composite literal written at the render call site."
    - "engram migrate-remap-owner renders both sentence variants (dry-run, applied) from two FieldSet builders, byte-identical to their pre-image on both lanes (D-04)."
    - "migrateRemapReportDoc's mode-dependent pair — would_remap and remapped, both always serialized today with neither carrying omitempty — is resolved by the D-08 selection recorded in 06-01-SUMMARY.md, never by a per-mode presence guess (RESEARCH.md Pitfall 1, assumption A3, the phase's highest-probability regression)."
    - "Every field of every converted variant is referenced by exactly one placeholder in that variant's template, proven by validateFieldSet passing on each builder's output (D-03)."
    - "Goldens for all three variants are generated from the pre-image builders BEFORE conversion and are unchanged by the conversion commit."
  artifacts:
    - cmd/engram/testdata/operator-reports/migrate-set-owner.only.txt
    - cmd/engram/testdata/operator-reports/migrate-remap-owner.dry-run.txt
    - cmd/engram/testdata/operator-reports/migrate-remap-owner.applied.txt
  key_links:
    - "migrateSetOwnerReport / migrateRemapReport -> renderOperator(FieldSet) -> TestOperatorReportGolden -> the frozen goldens"
  prohibitions:
    - "Do not construct a FieldSet as a composite literal at a render call site; migrate-set-owner is the one report that does this today (RESEARCH.md Pitfall 5) and carrying the habit forward reintroduces the hazard that a converter-function-name search silently skips it."
    - "Do not set a field's Present predicate from which mode is running; default every field to always-present unless today's struct tag already carries omitempty."
    - "Do not delete migrateSetOwnerSummary, migrateSetOwnerReportDoc, migrateRemapSummary, migrateRemapDoc, or migrateRemapReportDoc in this plan."
    - "Do not modify or delete TestOperatorOutputParity or operatorParityRows."
    - "Do not edit cmd/engram/operator_golden_test.go."
    - "Do not regenerate any golden file in the conversion commit."
    - "Do not change any operator-facing sentence byte (D-04)."
---

<objective>
Convert `migrate-set-owner` and `migrate-remap-owner` to `FieldSet` builders, gated by goldens
generated from their pre-image.

Purpose: `migrate-remap-owner` is the cleanest instance of the phase's highest-probability regression —
a mode-dependent field pair emitted unconditionally today — and `migrate-set-owner` is the one report
in the tier whose document is built inline at the call site rather than by a named converter.

Output: `migrateSetOwnerReport`, `migrateRemapReport`, three checked-in golden pairs, and two call
sites moved off `renderOperatorLegacy`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-typed-operator-renderer/06-CONTEXT.md
@.planning/phases/06-typed-operator-renderer/06-PATTERNS.md
@.planning/phases/06-typed-operator-renderer/06-01-SUMMARY.md
@cmd/engram/fieldset.go
@cmd/engram/prune.go
</context>

## Artifacts this phase produces

See `06-01-PLAN.md` § "Artifacts this phase produces" for the full phase-level list. This plan
contributes: `migrateSetOwnerReport`, `migrateRemapReport`, and the three golden pairs named in
`must_haves`.

<tasks>

<task type="auto">
  <name>Task 1: Freeze migrate-set-owner and migrate-remap-owner goldens from the pre-image</name>
  <files>cmd/engram/migrate_test.go, cmd/engram/testdata/operator-reports/</files>
  <read_first>
    - cmd/engram/operator_golden_test.go — `registerOperatorReport`'s parameter order and `TestOperatorReportGolden`'s comparison contract
    - cmd/engram/prune_test.go — the `init()` registration 06-01 wrote, imitated exactly here
    - cmd/engram/migrate.go lines 55-90 — the `migrate-set-owner` call site, its inline document literal, and `migrateSetOwnerSummary`
    - cmd/engram/migrate.go lines 175-225 — the `migrate-remap-owner` call site, `migrateRemapSummary`, `migrateRemapDoc`, and `migrateRemapReportDoc`'s tags
    - cmd/engram/operator_output_test.go lines 181-192 — `operatorParityRows`' existing hand-built inputs for both commands, reused verbatim
  </read_first>
  <action>
Add an `init()` to `cmd/engram/migrate_test.go` registering one fixture under key `migrate-set-owner`
with variant `only`, and two fixtures under key `migrate-remap-owner` with variants `dry-run` and
`applied`. Each registration passes the legacy sentence and the legacy document built from one shared
input per fixture, and passes nil for the `Report` argument.

Reuse `operatorParityRows`' existing inputs verbatim: owner `bob` with 7 stamped for
`migrate-set-owner`, and 13 records remapped to `alice` for `migrate-remap-owner`, varying only the
`dryRun` boolean across its two variants. For `migrate-set-owner` the legacy document is the composite
literal the call site builds today; pass an equivalent value here rather than inventing a converter.

Generate the six golden files with the package `-update` flag, inspect them by eye against current CLI
output, then commit. This commit contains no production-code change.

While reading, record in the SUMMARY the exact key set `migrateRemapReportDoc` marshals in each mode —
this is the baseline the D-08 selection is applied against in Task 2, and RESEARCH.md flags a
per-mode presence guess here as the single highest-probability silent regression in the phase.
  </action>
  <verify>
    <automated>go test ./cmd/engram/... -count=1 -run 'TestOperatorReportGolden' -v 2>&1 | grep -E '^( *)(=== RUN|--- (PASS|FAIL))'</automated>
  </verify>
  <acceptance_criteria>
    - `ls cmd/engram/testdata/operator-reports/ | rg -c '^(migrate-set-owner|migrate-remap-owner)\.'`
      reports 6.
    - `go test ./cmd/engram/... -run 'TestOperatorReportGolden' -v` shows a `--- PASS` subtest line for
      each of the three new `<key>/<variant>` fixtures.
    - `TestOperatorReportGoldenSetIsComplete` passes.
    - `git diff --stat cmd/engram/migrate.go` is empty for this commit.
    - `cmd/engram/testdata/operator-reports/migrate-remap-owner.dry-run.json` contains both the
      `would_remap` and the `remapped` keys, since neither carries `omitempty` today — this file is the
      literal record of the pre-conversion key set.
  </acceptance_criteria>
  <done>All three variants are pinned to committed golden bytes produced by the pre-image code, with the mode-dependent key set recorded explicitly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert migrate-set-owner and migrate-remap-owner to FieldSet builders</name>
  <files>cmd/engram/migrate.go, cmd/engram/migrate_test.go</files>
  <read_first>
    - cmd/engram/fieldset.go — `Field`, `FieldSet`, `present`, `validateFieldSet`, placeholder syntax
    - cmd/engram/prune.go — `prunePreviewReport` and `pruneAppliedReport`, the shape to imitate
    - .planning/phases/06-typed-operator-renderer/06-01-SUMMARY.md — the D-08 selection
    - .planning/phases/06-typed-operator-renderer/06-RESEARCH.md § "Pitfall 1" and § Assumptions Log row A3
    - cmd/engram/migrate.go lines 55-90 and 175-225
  </read_first>
  <behavior>
    - `validateFieldSet(migrateSetOwnerReport(...))` returns nil.
    - `validateFieldSet(migrateRemapReport(...))` returns nil for both variants.
    - `TestOperatorReportGolden` passes for all three fixtures with `Report` now non-nil.
    - `TestMigrateSetOwnerSummaryUnchanged`, `TestMigrateRemapSummary`, and
      `TestMigrateRemapDryRunJSONDistinguishesPreviewFromApplied` continue to pass without edits to
      their assertions.
  </behavior>
  <action>
Add `migrateSetOwnerReport(owner string, stamped uint64) FieldSet` as a NAMED function in
`cmd/engram/migrate.go`, beside its siblings — not a composite literal at the render call site.
RESEARCH.md Pitfall 5 records that this one report builds its document inline today, which is exactly
why a converter-function-name search misses it; the named builder removes the hazard permanently.

Add `migrateRemapReport(n uint64, to string, dryRun bool) FieldSet` returning one of two distinct
`FieldSet` values, one per sentence variant, mirroring prune's split.

For every field carrying a mode-dependent value, set `Present` from today's struct tag, never from
which mode is running: a field whose tag has no `omitempty` today stays unconditionally present.
`migrateRemapReportDoc`'s `would_remap` and `remapped` are the live case — both are serialized in both
modes today with one always zero. Apply the D-08 selection from `06-01-SUMMARY.md` to whichever of the
two has no textual presence in the running variant; do not re-decide it here and do not reach for a
per-mode presence guess.

Reproduce both sentences byte-identically (D-04), attaching a `Render` function to any field whose
prose form differs from its natural Go form rather than editing template literals. Point both render
call sites at `renderOperator` with the new builders. Leave `migrateSetOwnerSummary`,
`migrateSetOwnerReportDoc`, `migrateRemapSummary`, `migrateRemapDoc`, and `migrateRemapReportDoc` in
place for 06-09. Update both `init()` registrations to pass the new builders as `Report`, and
regenerate nothing.
  </action>
  <verify>
    <automated>go test ./cmd/engram/... -count=1 -run 'TestMigrateSetOwner|TestMigrateRemap|TestRemapOwnerFlagValidation|TestOperatorReportGolden|TestOperatorOutputParity|TestOperatorOutputEmpty' -v 2>&1 | grep -E '^( *)(=== RUN|--- (PASS|FAIL)|FAIL|ok)'</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd/engram/... -list 'TestMigrateSetOwner.*|TestMigrateRemap.*|TestRemapOwner.*'`
      lists `TestMigrateSetOwnerRejectsInvalidOutput`, `TestMigrateSetOwnerSummaryUnchanged`,
      `TestMigrateRemapOwnerRejectsInvalidOutput`, `TestMigrateRemapSummary`,
      `TestMigrateRemapDryRunJSONDistinguishesPreviewFromApplied`, and
      `TestRemapOwnerFlagValidation`, and the `-v` run shows `--- PASS` for each.
    - `git diff --exit-code cmd/engram/testdata/operator-reports/` is clean for this commit.
    - `rg -n 'renderOperatorLegacy\(' cmd/engram/migrate.go` returns no match.
    - `rg -n 'func migrateSetOwnerReport\(|func migrateRemapReport\(' cmd/engram/` returns exactly one
      match each, and `rg -n 'renderOperator\(cmd, format, FieldSet\{' cmd/engram/migrate.go` returns
      no match, proving no field set is built inline at a call site.
    - `TestOperatorOutputParity` still shows `--- PASS`, and
      `git diff --stat cmd/engram/operator_output_test.go` is empty for this commit.
    - `task lint` and `task test` are green.
  </acceptance_criteria>
  <done>Both commands render both lanes from named FieldSet builders, with the pre-image goldens unchanged and the mode-dependent key pair resolved by the recorded D-08 decision rather than a per-mode guess.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (no new boundary) | Conversion of two already-authorized operator reports. No new input, persisted data, authz path, or dependency. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| 06-03:T-06-PARSE | Information Disclosure | `migrateSetOwnerReport`, `migrateRemapReport` | medium | mitigate | A converted report whose json states more than its sentence. Mitigated by `validateFieldSet` plus the frozen goldens; inherits 06-01:T-06-PARSE's red-evidence proof. |
| 06-03:T-06-MODEKEY | Tampering | `migrateRemapReport`'s mode-dependent pair | medium | mitigate | Silently dropping a json key a consumer relies on. Mitigated by setting `Present` from today's struct tag rather than from the running mode, and by the frozen `migrate-remap-owner.*.json` goldens carrying the pre-conversion key set. |
| 06-03:T-06-NONE | (no applicable STRIDE category) | plan scope | low | accept | RESEARCH.md's ASVS review found no conventional threat applies. Recorded, not fabricated. |

**Package legitimacy:** no install task in this plan; `go.mod` and `go.sum` are untouched.
</threat_model>

<verification>
- `task lint` and `task test` green.
- `go test ./cmd/engram/... -count=1` green with `TestOperatorOutputParity` unmodified and passing.
- `git diff --exit-code cmd/engram/testdata/operator-reports/` clean after the conversion commit.
</verification>

<success_criteria>
- Both commands render from named `FieldSet` builders through the new `renderOperator`.
- Three golden pairs, frozen from the pre-image, survive the conversion byte-for-byte.
- The mode-dependent key pair's disposition is traceable to the recorded D-08 decision.
</success_criteria>

<output>
Create `.planning/phases/06-typed-operator-renderer/06-03-SUMMARY.md` when done, naming the three
fixture keys and recording the exact json key set each `migrate-remap-owner` mode emits before and
after conversion.
</output>
