---
phase: 06-typed-operator-renderer
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - cmd/engram/reindex.go
  - cmd/engram/reindex_test.go
  - cmd/engram/summarize.go
  - cmd/engram/summarize_test.go
  - cmd/engram/migrate.go
  - cmd/engram/migrate_test.go
  - cmd/engram/operator_view_flat_test.go
autonomous: true
requirements: [REQ-operator-renderer-typed]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`reindex`, `summarize-missing`, `migrate-set-owner` and `migrate-remap-owner` each render, under `--output text`, one headline line followed by exactly one aligned label line per key their `--output json` document emits."
    - "All four commands' `--output json` documents are unchanged — same keys, same tags, same values (ROADMAP SC2)."
    - "`reindexSummary`, `summarizeSummary`, `migrateSetOwnerSummary` and `migrateRemapSummary` remain pure, single-line, no-I/O headline producers; every sentence variant is preserved (3 for reindex, 2 for summarize, 1 for set-owner, 2 for remap)."
    - "No fact any of the four sentences states is lost: every stated value is present as a doc key or derivable from one, so `06-01-PLAN.md §Conversion Rules` R2 requires no new key in this group."
    - "The doc comments claiming these sentences are byte-stable for anyone parsing them are corrected, because 06-CONTEXT.md D-03 declares the text lane explicitly not a stable interface."
  artifacts:
    - "cmd/engram/operator_view_flat_test.go — `flatViewFixtures`, `TestFlatViewIdentity`"
  key_links:
    - from: "cmd/engram/operator_view_flat_test.go"
      to: "cmd/engram/cmdwalk.go"
      via: "`flatViewFixtures` keys -> `commandKey` values `reindex`, `summarize-missing`, `migrate-set-owner`, `migrate-remap-owner` -> plan 06-07's both-directions enumeration gate against `operatorCommands()`."
    - from: "cmd/engram/operator_view_test.go"
      to: "cmd/engram/operator_view_flat_test.go"
      via: "`assertViewIdentity` (defined in `cmd/engram/operator_view_test.go` by plan 06-01) -> every fixture in this plan."
  prohibitions:
    - statement: "No doc struct in this group gains, loses, or renames a JSON key — this group has no `06-01-PLAN.md §Conversion Rules` R2 gap to close."
      status: enforced
      verification: "`git diff cmd/engram/reindex.go cmd/engram/summarize.go cmd/engram/migrate.go` shows no change inside any `type *Doc struct` block"
    - statement: "This plan does not edit `cmd/engram/operator_output_test.go`; fixtures live in this plan's own file per R3."
      status: enforced
      verification: "`git diff --exit-code cmd/engram/operator_output_test.go` exits 0"
---

<objective>
Convert the four flat single-sentence operator reports to the view mechanism.

Purpose: these four are the mechanical repetitions of the shape plan 06-01 proved on `prune-expired` —
one pure `xxxSummary` producing one line, one hand-declared doc struct, one `renderOperator` call site
per variant. Converting them establishes the per-report cost of the mechanism as near zero, which is
ROADMAP Success Criterion 3's whole point.

Output: four converted reports, their headline-producer doc comments corrected, and this group's view
fixtures under the shared identity gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-typed-operator-renderer/06-CONTEXT.md
@.planning/phases/06-typed-operator-renderer/06-01-PLAN.md
@.planning/phases/06-typed-operator-renderer/06-01-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
</context>

**Conversion recipe:** apply `06-01-PLAN.md §Conversion Rules` (R1 headline trim, R2 gap closure, R3
fixtures, R4 structural pinning only, R5 call sites keep their shape). That section is the single
normative statement of the recipe; it is not restated here.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Convert `reindex` and `summarize-missing`</name>
  <files>cmd/engram/reindex.go, cmd/engram/reindex_test.go, cmd/engram/summarize.go, cmd/engram/summarize_test.go</files>
  <read_first>
    cmd/engram/operator_view.go (as created by plan 06-01 — `renderOperatorView`, `viewFields`, `humanizeKey`; the executor must know what the view already does before deciding what a report still needs),
    .planning/phases/06-typed-operator-renderer/06-01-PLAN.md §Conversion Rules,
    cmd/engram/reindex.go lines 80-146 (`reindexCmd`'s RunE tail, `reindexSummary` and its 3 variants, `reindexOutputDoc`, `reindexReportDoc`),
    cmd/engram/reindex_test.go (`TestReindexSummary` and every other assertion over reindex text output),
    cmd/engram/summarize.go lines 60-110 (`summarizeSummary` and its 2 variants, `summarizeOutputDoc`, `summarizeReportDoc`),
    cmd/engram/summarize_test.go (`TestSummarizeSummary`)
  </read_first>
  <behavior>
    - `reindexSummary` still returns exactly one line for each of its three variants (dry-run, dry-run with `--resume`, cutover).
    - `reindexReportDoc(...)` marshals to the same nine keys in the same order as before this plan.
    - Rendering `reindexReportDoc(...)` through `renderOperatorView` produces exactly nine top-level field lines.
    - The same three properties hold for `summarizeSummary` (two variants) and `summarizeOutputDoc` (five keys).
  </behavior>
  <action>
Apply `06-01-PLAN.md §Conversion Rules` R1 to `reindexSummary` and `summarizeSummary`. Both already
return a single `fmt.Sprintf` line per variant with no trailing newline and no row loops, so R1's trim
is a confirmation here, not an edit to the returned strings: keep all three reindex variants and both
summarize variants exactly as they read today.

Apply R2's gap check explicitly and record the result: walk each sentence variant's stated values
against its doc struct's keys. For `reindexSummary` the stated values are `WouldUpsert`, `Unchanged`,
`Skipped`, `Scanned`, `Upserted`, `target` and `dim`, all present as `would_upsert`, `unchanged`,
`skipped`, `scanned`, `upserted`, `target` and `dim`; the cutover variant's
`ENGRAM_QDRANT_COLLECTION=` value is `target` again and is therefore derivable, not a gap. For
`summarizeSummary` the stated values are `Filled`, `Scanned`, `Skipped` and `Failed`, all present.
Conclusion: no key is added in this task. State that conclusion in the SUMMARY so a later reader can
see the check ran rather than assuming it was skipped.

Correct `reindexSummary`'s doc comment. Its current claim — that the dry-run-without-resume variant
"returns the pre-D-14 format string unchanged, character for character — that is what keeps this
change additive for anyone parsing that line" — is now false as a guarantee: D-03 declares the text
lane explicitly not a stable interface, so nobody may parse that line. Replace that clause with a
statement that the function is a headline producer per D-04 (one line, non-exhaustive prose above a
complete field table), that the field table below it is what carries every value, and that the
sentence's wording may change in any release because `--output json` is the contract. Keep the
"pure (no I/O), unit-testable without a live Qdrant" part of the comment, which is still true and still
load-bearing.

Make the same doc-comment correction on `summarizeSummary` if it carries a comparable byte-stability
claim; if it does not, extend its comment to name it a headline producer per D-04.

Update `cmd/engram/reindex_test.go` and `cmd/engram/summarize_test.go` per R4: keep the existing
`TestReindexSummary` and `TestSummarizeSummary` variant assertions (they pin the headline producers
themselves, which is legitimate — the prohibition is on pinning *rendered report* text), but change
any assertion that ran a command end to end and compared its whole `--output text` stdout to an exact
string into a structural assertion — first line equals the headline the producer returns, and the
rendered top-level field-line count equals the document's key count. Do not create any golden file.

Leave `reindexOutputDoc`, `reindexReportDoc`, `summarizeOutputDoc`, `summarizeReportDoc` and every
`renderOperator` call site untouched (R5).
  </action>
  <verify>
    <automated>go test ./cmd/engram/ -run 'TestReindex|TestSummarize' -v</automated>
  </verify>
  <acceptance_criteria>
    - `git diff cmd/engram/reindex.go` shows no change between `type reindexOutputDoc struct {` and its closing brace
    - `git diff cmd/engram/summarize.go` shows no change between `type summarizeOutputDoc struct {` and its closing brace
    - `rg -c 'character for character' cmd/engram/reindex.go` outputs `0`
    - `rg -o 'renderOperator\(' cmd/engram/reindex.go cmd/engram/summarize.go | wc -l` outputs `2` — one call site each, unchanged
    - `go test ./cmd/engram/ -list 'TestReindex|TestSummarize'` lists at least `TestReindexSummary` and `TestSummarizeSummary`
    - `go test ./cmd/engram/ -run 'TestReindex|TestSummarize' -v` prints `--- PASS` for each and exits 0
    - `task lint` exits 0
  </acceptance_criteria>
  <done>Both reports render as headline-plus-complete-table under `--output text`, their json documents are byte-unchanged, and no doc comment still promises a stable text sentence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert `migrate-set-owner` and `migrate-remap-owner`</name>
  <files>cmd/engram/migrate.go, cmd/engram/migrate_test.go</files>
  <read_first>
    cmd/engram/operator_view.go (as created by plan 06-01),
    .planning/phases/06-typed-operator-renderer/06-01-PLAN.md §Conversion Rules,
    cmd/engram/migrate.go lines 60-82 (`migrateSetOwnerCmd`'s RunE tail with its INLINE `migrateSetOwnerReportDoc{...}` literal at the call site, `migrateSetOwnerSummary`, `migrateSetOwnerReportDoc`),
    cmd/engram/migrate.go lines 180-232 (`migrateRemapSummary` and its 2 variants, `migrateRemapReportDoc`, `migrateRemapDoc`),
    cmd/engram/migrate_test.go lines 125-155 (`TestMigrateSetOwnerSummary`-equivalent and `TestMigrateRemapSummary`)
  </read_first>
  <behavior>
    - `migrateSetOwnerSummary("bob", 7)` still returns exactly one line naming both values.
    - `migrateSetOwnerReportDoc{Owner: "bob", Stamped: 7}` renders as exactly two top-level field lines, labelled `Owner` and `Stamped`.
    - `migrateRemapSummary` still returns one line for each of its preview and applied variants.
    - `migrateRemapDoc(...)` renders as exactly four top-level field lines for both variants.
  </behavior>
  <action>
Apply `06-01-PLAN.md §Conversion Rules` R1 to `migrateSetOwnerSummary` and `migrateRemapSummary`. Both
already return a single line per variant, so R1 is a confirmation, not an edit to the returned strings.

Apply R2's gap check and record the result: `migrateSetOwnerSummary` states `owner` and `stamped`,
both present as `owner` and `stamped`; `migrateRemapSummary` states `n` and `owner`, present as
`would_remap`/`remapped` and `owner`, and its literal `--apply` mention is flag prose, not a value.
Conclusion: no key is added in this task. State that conclusion in the SUMMARY.

Correct `migrateSetOwnerSummary`'s doc comment: replace its claim that the function "returns the
pre-D-13 sentence unchanged, character for character" with the D-04 headline-producer framing and the
D-03 statement that the wording may change in any release. Keep the "pure (no I/O)" clause and keep
the note that `renderOperator` supplies the trailing newline.

`migrate-set-owner` builds its document as an INLINE struct literal at the `renderOperator` call site
(`migrateSetOwnerReportDoc{Owner: migrateOwner, Stamped: n}`) rather than through a named converter
like every sibling. Per R5 this call site keeps its shape and is NOT refactored — under the view
mechanism there is no converter-function registry for it to be missing from, so the historical hazard
that motivated naming it is gone. Add a one-line comment at that call site recording that the inline
literal is deliberate and that its document is enumerated by `flatViewFixtures` in Task 3, so a reader
grepping for converter functions does not conclude the report is unconverted.

Update `cmd/engram/migrate_test.go` per R4 the same way as Task 1: keep the pure summary-variant
assertions, convert any whole-stdout exact-string comparison into a structural assertion. Do not touch
`cmd/engram/migrate_family_test.go` — it belongs to plan 06-04.
  </action>
  <verify>
    <automated>go test ./cmd/engram/ -run 'TestMigrateSetOwner|TestMigrateRemap' -v</automated>
  </verify>
  <acceptance_criteria>
    - `git diff cmd/engram/migrate.go` shows no change between `type migrateSetOwnerReportDoc struct {` and its closing brace, and none between `type migrateRemapReportDoc struct {` and its closing brace
    - `rg -c 'character for character' cmd/engram/migrate.go` outputs `0`
    - `rg -o 'renderOperator\(' cmd/engram/migrate.go | wc -l` outputs `2` — both call sites unchanged
    - `rg -c 'migrateSetOwnerReportDoc\{Owner: migrateOwner, Stamped: n\}' cmd/engram/migrate.go` outputs `1` — the inline literal is preserved, not refactored
    - `go test ./cmd/engram/ -run 'TestMigrateSetOwner|TestMigrateRemap' -v` prints `--- PASS` for each and exits 0
    - `git diff --exit-code cmd/engram/migrate_family_test.go` exits 0
    - `task lint` exits 0
  </acceptance_criteria>
  <done>Both `migrate.go` reports render as headline-plus-complete-table, their json documents are byte-unchanged, and the inline call-site literal is preserved with its rationale recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Put this group's four reports under the shared identity gate</name>
  <files>cmd/engram/operator_view_flat_test.go</files>
  <read_first>
    cmd/engram/operator_view_test.go (plan 06-01's `assertViewIdentity`, `pruneViewFixtures`, `jsonTopLevelKeys`, `orderedKeyDiff`, `countTopLevelFieldLines` — the shape this file must mirror),
    .planning/phases/06-typed-operator-renderer/06-01-PLAN.md §Conversion Rules R3,
    cmd/engram/cmdwalk.go lines 86-116 (`operatorCommands`) and cmd/engram/cmdwalk_test.go lines 141-147 (`commandKeySet`) — the `commandKey` strings these fixture keys must match,
    cmd/engram/operator_output_test.go lines 150-195 (the retiring `operatorParityRows` entries for these four commands — reuse their fixed sample VALUES, and nothing else from them)
  </read_first>
  <action>
Create `cmd/engram/operator_view_flat_test.go` (new file, Apache-2.0 SPDX header) in package `main`.

Declare `func flatViewFixtures() map[string][]any` with exactly four keys, each a `commandKey` string
as `operatorCommands()` produces it: `reindex`, `summarize-missing`, `migrate-set-owner`,
`migrate-remap-owner`. Populate each key's slice with one sample document per sentence VARIANT the
command has, so every variant's document shape is exercised: three for `reindex` (dry-run, dry-run
with resume, cutover), two for `summarize-missing` (dry-run, applied), one for `migrate-set-owner`,
two for `migrate-remap-owner` (preview, applied). Build every sample by calling the report's real
converter — `reindexReportDoc`, `summarizeReportDoc`, `migrateRemapDoc` — or, for `migrate-set-owner`,
the same inline struct literal shape the call site uses. Reuse the fixed input values from the
corresponding `operatorParityRows` entries so the fixtures stay comparable to what the retiring test
exercised; do NOT copy that test's `facts` slices or any part of its structure — the hand-listed facts
are exactly the defect ROADMAP Success Criterion 1 rejects.

Declare `func TestFlatViewIdentity(t *testing.T)` iterating `flatViewFixtures` and calling
`assertViewIdentity(t, name, doc)` for every document, with the command key as the subtest name.

Add a file-level doc comment stating that this file holds ONLY this plan's group fixtures, that plan
06-07 merges every group's fixture function into `operatorViewFixtures` and gates the merged key set
against `operatorCommands()` in both directions, and that adding a command without adding its fixture
here is what that gate exists to catch.
  </action>
  <verify>
    <automated>go test ./cmd/engram/ -run 'TestFlatViewIdentity' -v</automated>
  </verify>
  <acceptance_criteria>
    - `cmd/engram/operator_view_flat_test.go` exists and contains `func flatViewFixtures() map[string][]any` and `func TestFlatViewIdentity(`
    - `rg -o '"(reindex|summarize-missing|migrate-set-owner|migrate-remap-owner)":' cmd/engram/operator_view_flat_test.go | wc -l` outputs `4`
    - `rg -c 'facts' cmd/engram/operator_view_flat_test.go` outputs `0`
    - `go test ./cmd/engram/ -list 'TestFlatViewIdentity'` lists `TestFlatViewIdentity`
    - `go test ./cmd/engram/ -run 'TestFlatViewIdentity' -v` prints one `--- PASS` subtest line per command key and exits 0
    - `task license:check` exits 0
    - `go test ./cmd/engram/...` exits 0 and `task` exits 0
  </acceptance_criteria>
  <done>All four flat reports, across every sentence variant, are asserted to render one text line per JSON key by the same gate that guards `prune-expired`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Qdrant payload -> operator CLI process | `target`, `owner`, and count values in these four reports originate outside the process. |
| operator CLI process -> terminal / pipe / log | Rendered bytes reach a device that interprets control sequences. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Information Disclosure | `reindexOutputDoc`, `summarizeOutputDoc`, `migrateSetOwnerReportDoc`, `migrateRemapReportDoc` | high | mitigate | All four remain hand-declared structs carrying counts, a collection name, and an owner claim value only — no record content, no summary text, and no embedded `store.` result type. This plan changes no field, verified by the two `git diff` criteria in Tasks 1 and 2. |
| T-06-07 | Information Disclosure | `owner` value in `migrate-set-owner` / `migrate-remap-owner` | medium | accept | The owner-claim value (an OIDC `sub` or email) is the operator's own input to the command and is already echoed in today's text sentence and json document. Rendering it in the field table discloses nothing the invocation did not already contain. |
| T-06-03 | Tampering | rendered values in this group | medium | mitigate | Inherited from plan 06-01: `sanitizeViewValue` strips C0 controls and DEL from every rendered value, so a `target` or `owner` value carrying ESC cannot inject a terminal escape. No per-report work needed. |
| T-06-SC | Tampering | package-manager installs | low | accept | No installs — Go source edits and one new test file. |
</threat_model>

## Artifacts this phase produces

Symbols and files created by THIS plan (newly created — exclude from drift verification):

- `cmd/engram/operator_view_flat_test.go` (new file)
- `flatViewFixtures` (test helper func)
- `TestFlatViewIdentity` (test)

<verification>
- `go test ./cmd/engram/...` exits 0.
- `task` (lint + test) exits 0 and `task license:check` exits 0.
- All four commands' json documents are unchanged: `git diff` shows no edit inside any `*Doc struct`
  block in `reindex.go`, `summarize.go`, or `migrate.go`.
- `git diff --exit-code cmd/engram/operator_output_test.go` exits 0 — fixtures stayed in this plan's
  own file per R3, so this plan can run in parallel with 06-04, 06-05 and 06-06.
</verification>

<success_criteria>
- Four operator reports render as headline-plus-complete-table with zero per-report renderer code.
- Their json lanes are byte-unchanged (ROADMAP SC2).
- Their fixtures are under the shared identity gate and ready for 06-07's enumeration merge.
</success_criteria>

<output>
Create `.planning/phases/06-typed-operator-renderer/06-03-SUMMARY.md` when done. Record the R2 gap-check
conclusion for each of the four reports explicitly — "checked, no key added" is the finding, and it
must be visible rather than inferred from silence.
</output>
