---
phase: 06-typed-operator-renderer
plan: 08
type: execute
wave: 3
depends_on: [06-01, 06-04]
files_modified:
  - cmd/engram/migrate_family.go
  - cmd/engram/migrate_family_test.go
  - cmd/engram/testdata/operator-reports/
autonomous: true
requirements: [REQ-operator-renderer-typed]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "engram migrate status renders from a hand-declared fieldset.Set builder rather than by passing store.MigrateStatusResult straight through — the last report in the tier reusing a store-layer type's own json tags loses that shortcut, because renderOperator accepts nothing but a fieldset.Set (D-02)."
    - "The version-bucket list and the future-version list are two separate []fieldset.Set-valued fields, each carrying an INLINE separator rather than a newline — this is the tier's only non-newline join and the strongest reason the separator is an explicit per-field property rather than a default."
    - "The whole future-version clause, prefix text and every element together, is gated by ONE Present predicate keyed on the future list being non-empty, so the json key and the text clause disappear together (D-06)."
    - "Every field of the converted report, and of every nested bucket row, is referenced by exactly one placeholder in its own template, proven by fieldset.New's validation recursing through both lists (D-03 + D-05)."
    - "The converted report never marshals a json null for either list, preserving what TestMigrateFamilyStatusReportDocNeverMarshalsNull pins today."
    - "Every one of the three variants is minted by an operatorReport(\"migrate status\", variant, ...) call whose identity arguments are string LITERALS in the same production statement as the branch producing that shape, and both inline-joined row lists by operatorRow, which leaves rows unlabeled and therefore unrenderable as top-level reports. 06-09 derives the phase's variant universe by parsing those call sites out of production source (06-REVIEWS.md cycle-2 HIGH-2 and MEDIUM-2)."
    - "Goldens for all three variants are generated from the pre-image builders BEFORE conversion, and the pre-image bytes survive the conversion commit unchanged: on any lane the recorded D-08 option left frozen they stay in the canonical golden, and on a lane it moved they stay in that variant's <Key>.<Variant>.legacy.<ext> pre-image golden, which the harness keeps under test on the legacy lane (06-REVIEWS.md cycle-3 HIGH-1)."
  artifacts:
    - cmd/engram/testdata/operator-reports/migrate-status.with-future.txt
    - cmd/engram/testdata/operator-reports/migrate-status.no-future.txt
    - cmd/engram/testdata/operator-reports/migrate-status.empty.txt
  key_links:
    - "migrateStatusReport's two []fieldset.Set fields -> inline separators -> the single-line rendering statusSummary produces today"
    - "the future clause's single Present predicate -> both the json key and the whole text clause (D-06)"
    - "migrateStatusReport -> renderOperator(fieldset.Set) -> TestOperatorReportGolden -> the frozen goldens"
  prohibitions:
    - "Do not pass store.MigrateStatusResult, or any other internal/store type, through to a report; every field is hand-declared from here on, which is what makes record content unreachable by construction."
    - "Do not give either list a newline separator; this report joins inline on one line and a newline default would produce byte-different output."
    - "Do not split the future clause's prefix text from its elements into two independently-gated pieces; one predicate governs the whole clause."
    - "Do not batch this conversion with the flat Group A reports; RESEARCH.md flags exactly that as the warning sign for this report."
    - "Do not delete statusSummary or statusReportDoc in this plan."
    - "Do not modify or delete TestOperatorOutputParity or operatorParityRows."
    - "Do not edit cmd/engram/operator_golden_test.go."
    - "Do not regenerate any golden file in the conversion commit EXCEPT exactly as the `## D-08 downstream execution contract` section permits for the option 06-01-SUMMARY.md recorded, and never without the named per-key or per-sentence justification that section requires. Under option-c both lanes stay frozen."
    - "Do not change any operator-facing sentence byte (D-04) — except on a lane the recorded D-08 option moves, where this plan's `## D-08 downstream execution contract` section names the moved bytes and the plan SUMMARY records them as an operator-visible regression."
---

<objective>
Convert `migrate status` — the tier's structural exception — to a hand-declared `fieldset.Set` builder,
gated by goldens generated from its pre-image.

Purpose: this is the only report that reuses a `store`-layer type's own json tags instead of a
hand-declared CLI struct, the only one that joins its lists inline on a single line rather than one row
per newline, and one of only two with a genuinely conditional clause. It combines all three, so it gets
its own plan and its own wave.

Output: `migrateStatusReport`, three checked-in golden pairs, and the last remaining
`renderOperatorLegacy` call site in `migrate_family.go` moved to `renderOperator`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-typed-operator-renderer/06-CONTEXT.md
@.planning/phases/06-typed-operator-renderer/06-PATTERNS.md
@.planning/phases/06-typed-operator-renderer/06-01-SUMMARY.md
@.planning/phases/06-typed-operator-renderer/06-04-SUMMARY.md
@internal/fieldset/fieldset.go
@cmd/engram/spine_review_archive.go
</context>

## Artifacts this phase produces

See `06-01-PLAN.md` § "Artifacts this phase produces" for the full phase-level list. This plan
contributes: `migrateStatusReport` and the three golden pairs named in `must_haves`.

## D-08 downstream execution contract

Added in response to `06-REVIEWS.md` HIGH-2: the first-cycle plans froze both lanes unconditionally,
which options a and b of the D-08 checkpoint necessarily violate — leaving two of three options with no
executable downstream path. All three now branch. Read the option recorded in `06-01-SUMMARY.md` and
follow exactly one row.

| Selected option | This plan's goldens | This plan's conversion-commit acceptance criterion |
|---|---|---|
| **a** — the field set is the json shape | `*.txt` frozen; `*.json` REGENERATED for the variants whose key set changes | `git diff --exit-code` clean over `cmd/engram/testdata/operator-reports/*.txt`. Every changed `*.json` is listed in this plan's SUMMARY with the dropped or added key and its D-08 rationale; a `*.json` that changed without a SUMMARY line is a defect, not an update. |
| **b** — the sentence follows the field set | `*.json` frozen; `*.txt` REGENERATED for the variants whose sentence gains text | `git diff --exit-code` clean over `cmd/engram/testdata/operator-reports/*.json`. Every changed `*.txt` is listed in the SUMMARY with its old and new bytes; D-04 must already have been amended in `06-CONTEXT.md` by 06-01. |
| **c** — bounded coverage exemption | both lanes frozen | `git diff --exit-code cmd/engram/testdata/operator-reports/` clean, and every field taking the exemption in this plan appears in the closed allowlist with its one-line rationale. |

Task 1's pre-image freeze is unaffected under all three options: the goldens are always generated from
the pre-image first, in a commit carrying no production-code change. Only Task 2's behavior branches.

**Mechanics — the branch executes through the harness, and the harness does the branching**
(`06-REVIEWS.md` cycle-3 HIGH-1 found the cycle-2 branch tables unexecutable, because the harness
compared both lanes to one golden and sourced `-update` from the legacy lane). Do not improvise a
golden-regeneration procedure here. Each fixture this plan registers declares a `Divergence`
(`divergeNone` | `divergeJSON` | `divergeText`) and, when it is not `divergeNone`, a one-line
`DivergenceReason`. `TestOperatorReportGolden` resolves a golden path PER LANE from that declaration:
the converted lane always against the canonical `<Key>.<Variant>.{txt,json}`, the legacy lane against
`<Key>.<Variant>.legacy.{txt,json}` on exactly the lane the recorded option moved.

- **Option-c:** every fixture is `divergeNone`, no `.legacy.*` file may exist, and Task 2 regenerates
  nothing at all.
- **Option-a or option-b:** for each affected variant, in this order — (1) `cp` the canonical golden of
  the MOVING lane to its `.legacy.<ext>` sibling, preserving the pre-image bytes; (2) set that fixture's
  `Divergence` and `DivergenceReason`; (3) run the package `-update` flag, which writes the canonical
  file from `Report()` and the `.legacy.*` file from the legacy lane; (4) re-run without `-update` and
  confirm both lanes pass. Never hand-edit golden bytes.

`TestOperatorReportGoldenSetIsComplete` holds the `.legacy.*` file set equal to the declared divergences
in both directions, requires the reason to be non-empty exactly when a divergence is declared, and fails
any fixture that diverges on the lane the recorded option FREEZES — so "regenerated without a named
justification is a defect" and "under option-a the text goldens are frozen" are assertions rather than
instructions to remember. The pre-image bytes survive the conversion unchanged under all three options;
under a and b they survive in the `.legacy.*` file.
Under a and b the freeze is replaced by a justification requirement, never by silence.

**The fields this plan routes through the decision:** any `migrate status` field with no textual
presence in a given variant. Enumerate them by name in the SUMMARY under every option; `migrate status`
is the tier's structural exception (it passes a store-layer value through after null-normalization and
joins its two lists inline), so do not assume its key set is fully stated just because its sentence is
compact.

<tasks>

<task type="auto">
  <name>Task 1: Freeze migrate status goldens from the pre-image, covering both future-clause branches and an empty spine</name>
  <files>cmd/engram/migrate_family_test.go, cmd/engram/testdata/operator-reports/</files>
  <read_first>
    - cmd/engram/operator_golden_test.go — `registerOperatorReport`'s parameter order and `TestOperatorReportGolden`'s comparison contract
    - cmd/engram/migrate_family_test.go — the `init()` registration 06-04 added for `migrate` and `migrate-revert`, which this task extends in the same file
    - cmd/engram/migrate_family.go lines 270-320 — the `migrate status` render call site, `statusReportDoc`'s passthrough and its doc comment, and `statusSummary`'s two inline loops
    - internal/store — `MigrateStatusResult` and `VersionBucket`, specifically each field's own json tag, since those tags are the current json contract this report inherits
    - cmd/engram/operator_output_test.go lines 265-276 — `operatorParityRows`' existing hand-built status inputs, reused verbatim
  </read_first>
  <action>
Extend the existing `init()` in `cmd/engram/migrate_family_test.go` with three fixtures under key
`migrate-status`, each passing nil for the `Report` argument. Every registration in THIS task also passes
`divergeNone` and an empty `DivergenceReason` — the freeze commit's canonical goldens ARE the pre-image
on both lanes, so nothing has diverged yet, and only Task 2 may change a fixture's `Divergence`, and
only as the `## D-08 downstream execution contract` **Mechanics** paragraph directs.

The fixtures:

- variant `with-future` — reuse `operatorParityRows`' status inputs verbatim (one bucket at version 1
  with 40 records, 3 absent, one future bucket at version 2 with 1 record, future total 1, total 44),
  which exercises the future clause.
- variant `no-future` — the same result with an empty future list and a zero future total, so the
  dropped-clause branch is goldened.
- variant `empty` — a zero-valued result with both lists empty, which is the shape
  `TestMigrateFamilyStatusReportDocNeverMarshalsNull` guards.

Add a second bucket to the `with-future` fixture so the inline join between bucket elements is pinned
rather than degenerate at one element.

Generate the six golden files with the package `-update` flag, inspect them by eye against current CLI
output, then commit. This commit contains no production-code change.

Record in the SUMMARY the complete json key set `statusReportDoc` emits, transcribed from the golden
files rather than from the struct — this report's keys come from `store`-layer tags, so reading them
off the actual output is the only reliable inventory, and it is the contract the hand-declared builder
in Task 2 must reproduce exactly.
  </action>
  <verify>
    <automated>go test ./cmd/engram/... -count=1 -run 'TestOperatorReportGolden|TestMigrateFamilyStatusReportDocNeverMarshalsNull' -v 2>&1 | grep -E '^( *)(=== RUN|--- (PASS|FAIL))'</automated>
  </verify>
  <acceptance_criteria>
    - `ls cmd/engram/testdata/operator-reports/ | rg -c '^migrate-status\.'` reports 6.
    - `go test ./cmd/engram/... -run 'TestOperatorReportGolden' -v` shows a `--- PASS` subtest line for
      each of the three new `<key>/<variant>` fixtures.
    - `TestOperatorReportGoldenSetIsComplete` passes.
    - `git diff --stat cmd/engram/migrate_family.go` is empty for this commit.
    - `cmd/engram/testdata/operator-reports/migrate-status.with-future.txt` is a SINGLE line — the
      buckets and the future clause join inline, not one row per newline — and contains two bucket
      elements.
    - `cmd/engram/testdata/operator-reports/migrate-status.no-future.txt` carries no future clause at
      all, and its `.json` sibling's future-list key holds an empty array rather than being absent, or
      is absent, exactly as the pre-image emits it.
    - `cmd/engram/testdata/operator-reports/migrate-status.empty.json` contains no occurrence of the
      four-character json null literal.
    - The SUMMARY transcribes the full json key set from the golden files.
  </acceptance_criteria>
  <done>All three status variants, including both future-clause branches and an empty spine, are pinned to committed golden bytes produced by the pre-image code, with the inherited json key set recorded from real output.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert migrate status to a hand-declared fieldset.Set builder with inline list joins</name>
  <files>cmd/engram/migrate_family.go, cmd/engram/migrate_family_test.go</files>
  <read_first>
    - internal/fieldset/fieldset.go — `F`, `WithPresent`, `WithSep`, `WithRender`, `New`, `MustNew`, `fieldset.New`'s recursive validation
    - cmd/engram/operator_output.go — `operatorReport(key, variant, text, fields...)` and `operatorRow(text, fields...)` as written in 06-01. These are the ONLY constructor routes a report builder may use: `operatorReport` for a top-level variant, with both identity arguments written as string literals, and `operatorRow` for a nested row. 06-09 parses those literals to derive the phase's variant universe, so a computed argument fails there by `file:line`.
    - cmd/engram/spine_review_archive.go — `archiveReport` as written in 06-05, the nested-rows shape, and the conditional-span form
    - .planning/phases/06-typed-operator-renderer/06-01-SUMMARY.md — the D-08 selection
    - .planning/phases/06-typed-operator-renderer/06-RESEARCH.md § "Pitfall 3" and § "Pattern 2" status row
    - .planning/phases/06-typed-operator-renderer/06-PATTERNS.md § Group C
    - cmd/engram/migrate_family.go lines 270-320
  </read_first>
  <behavior>
    - `fieldset.Validated(migrateStatusReport(...))` returns nil for all three fixture shapes, and its
      recursion reaches every bucket row's own field set in both lists.
    - `TestOperatorReportGolden` passes for all three fixtures with `Report` now non-nil, proving both
      lanes byte-match the pre-image goldens — including the single-line inline joins and the dropped
      future clause.
    - `TestMigrateFamilyStatusReportDocNeverMarshalsNull` continues to pass without edits to its
      assertions.
  </behavior>
  <action>
Add `migrateStatusReport(res store.MigrateStatusResult) fieldset.Set` to `cmd/engram/migrate_family.go`,
declaring every field by hand with the json keys transcribed in Task 1's SUMMARY entry. The
`store`-layer passthrough that `statusReportDoc` relies on today is not reachable from the new
`renderOperator`, so this report joins the other fourteen in declaring its own shape — which is also
what keeps record content unreachable by construction rather than by remembering to omit it.

Declare the version buckets as a `[]fieldset.Set`-valued field whose separator is the INLINE one set via `WithSep`
`statusSummary` uses today, each row declaring its version and its count. Declare the future buckets as
a SECOND `[]fieldset.Set`-valued field with its own inline separator and its own row template, since the
two lists render differently. Gate the entire future clause — its prefix text and every element
together — behind ONE `Present` predicate keyed on the future list being non-empty, referenced from a
conditional span so the json key and the text clause drop together (D-06). Do not split the prefix from
the elements into two independently-gated pieces; that is exactly the two-conditionals shape D-06
removes.

Keep both lists non-nil on the json lane so an empty spine marshals empty arrays rather than a json
null, preserving what `TestMigrateFamilyStatusReportDocNeverMarshalsNull` pins. Reproduce the sentence
byte-identically (D-04) on every lane the recorded D-08 option freezes — it is a single line, so a newline separator anywhere in this report is a
defect. Apply the D-08 selection from `06-01-SUMMARY.md` to any field with no textual presence; do not
re-decide it here. Point the render call site at `renderOperator`. Leave `statusSummary` and
`statusReportDoc` in place for 06-09.

Construct every top-level variant through `operatorReport`, one call site per variant this plan
converts, with the command key written as the string literal `"migrate status"` — the exact
`commandKey` spelling `operatorCommands()` returns — and the variant name as a string literal too, both
in the same statement as the branch producing that shape. Construct both inline-joined row lists
through `operatorRow` instead; rows are deliberately unlabeled, and `renderOperator` refuses an
unlabeled set. Do not name a `fieldset` constructor directly in either case. 06-09's gate parses these
call sites out of production source to derive the phase's variant universe, so a computed identity
argument fails there by `file:line` rather than being skipped. Update the `init()` registrations to pass the new builder as
`Report`, and regenerate goldens ONLY as this plan's `## D-08 downstream execution contract` directs for the option
`06-01-SUMMARY.md` recorded — nothing at all under option-c, and under option-a or option-b exactly the
four-step preserve-then-update procedure that section's **Mechanics** paragraph specifies, for exactly
the affected variants.
  </action>
  <verify>
    <automated>go test ./cmd/engram/... -count=1 -run 'TestMigrateFamily|TestBackfill|TestOperatorReportGolden|TestOperatorOutputParity|TestOperatorOutputEmpty' -v 2>&1 | grep -E '^( *)(=== RUN|--- (PASS|FAIL)|FAIL|ok)'</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd/engram/... -list 'TestMigrateFamily.*'` lists
      `TestMigrateFamilyStatusReportDocNeverMarshalsNull` among the rest, and the `-v` run shows
      `--- PASS` for every listed name.
    - The golden-diff criterion is the one this plan's `## D-08 downstream execution contract` names
      for the recorded option, never an unconditional freeze: under option-c,
      `git diff --exit-code cmd/engram/testdata/operator-reports/` is clean for this commit and
      `ls cmd/engram/testdata/operator-reports/*.legacy.* 2>/dev/null | wc -l` reports 0; under
      option-a it is clean over `*.txt` and every changed `*.json` belongs to a variant this plan
      declared `divergeJSON`, with its justification line in the SUMMARY and a new
      `<Key>.<Variant>.legacy.json` whose bytes equal the pre-conversion canonical golden; under
      option-b the same, mirrored onto the text lane.
    - `rg -n 'renderOperatorLegacy\(' cmd/engram/migrate_family.go` returns no match — this was the
      file's last remaining legacy call site after 06-04.
    - `rg -n 'func migrateStatusReport\(' cmd/engram/` returns exactly one match.
    - `rg -n 'operatorReport\("migrate status"' cmd/engram/migrate_family.go` returns one match per
      variant this plan converts, each with a string-literal variant name matching a registered
      fixture, and
      `rg -n 'fieldset\.(New|MustNew|NewLabeled|MustNewLabeled)\(' cmd/engram/migrate_family.go`
      returns no match — every construction routes through the helpers 06-09's derivation parses.
    - Separator presence and shape are proven BEHAVIORALLY, not by an `rg` window (`06-REVIEWS.md`
      MEDIUM-4: a grep-plus-eyeball is one-directional and silently passes on a case it did not match).
      Two checks, each red-able in both directions:
      (i) `go test ./internal/fieldset/ -count=1 -run '^TestFieldSetRejectsListWithoutSeparator$' -v`
      shows `--- PASS`; and (ii) a new `TestMigrateStatusDeclaresInlineJoinedLists` asserting exact set
      equality with `slices.Equal` over sorted slices between `migrateStatusReport(...).ListKeys()` and
      the two list keys named explicitly, plus an assertion that the rendered text for a two-element
      fixture of each list contains no newline byte — `migrate status` joins INLINE while the other six
      list reports join on a newline (RESEARCH.md assumption A4), and that difference is the thing
      worth gating.
    - `rg -n 'store\.MigrateStatusResult' cmd/engram/migrate_family.go` shows the type only as the
      builder's parameter, never as a value handed to a render call.
    - `TestOperatorOutputParity` and `TestOperatorOutputEmpty` still show `--- PASS`, and
      `git diff --stat cmd/engram/operator_output_test.go` is empty for this commit.
    - `task lint` and `task test` are green.
  </acceptance_criteria>
  <done>The tier's structural exception renders from a hand-declared field set with two inline-joined lists and one predicate governing the whole future clause, byte-identical to its pre-image, and `migrate_family.go` carries no legacy render call.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (no new boundary) | Conversion of an already-authorized operator report. No new input, persisted data, authz path, or dependency. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| 06-08:T-06-PARSE | Information Disclosure | `migrateStatusReport` | medium | mitigate | A converted report whose json states more than its sentence, now across two nested lists and one conditional clause. Mitigated by `fieldset.New`'s validation recursing into both lists plus the frozen goldens; inherits 06-01:T-06-PARSE's red-evidence proof. |
| 06-08:T-06-PASSTHROUGH | Information Disclosure | the retired `store.MigrateStatusResult` passthrough | high | mitigate | This is the one report where a `store`-layer type's fields reach the operator surface directly, so a future field added to that type would surface here automatically. Mitigated permanently by hand-declaring every field in the builder, which is the property every other report's doc comment already claims. |
| 06-08:T-06-JOIN | Tampering | the two inline-joined lists | medium | mitigate | A newline separator anywhere would turn a one-line report into a multi-line one and break D-04. Mitigated by the explicit per-field separator requirement, the single-line golden, and the acceptance criterion that neither separator is a newline. |
| 06-08:T-06-NONE | (no applicable STRIDE category) | plan scope | low | accept | RESEARCH.md's ASVS review found no conventional threat applies. Recorded, not fabricated. |

**Package legitimacy:** no install task in this plan; `go.mod` and `go.sum` are untouched.
</threat_model>

<verification>
- `task lint` and `task test` green.
- `go test ./cmd/engram/... -count=1` green with `TestOperatorOutputParity` unmodified and passing.
- `git diff --exit-code cmd/engram/testdata/operator-reports/` clean after the conversion commit under option-c, and clean over every lane the recorded D-08 option freezes under option-a or option-b —
  with each changed canonical file justified by name in the SUMMARY and its pre-image preserved in
  the matching `.legacy.<ext>` golden.
- All fifteen reports now render through `renderOperator`; the only remaining `renderOperatorLegacy`
  references are its own definition and any test still naming it.
</verification>

<success_criteria>
- `migrate status` renders from a hand-declared `fieldset.Set` builder with inline list joins.
- The future clause drops from both lanes from one predicate.
- Three golden pairs, frozen from the pre-image, survive the conversion byte-for-byte — in the
  canonical golden on every lane the recorded D-08 option left frozen, and in the `.legacy.<ext>`
  pre-image sibling on any lane it moved.
</success_criteria>

<output>
Create `.planning/phases/06-typed-operator-renderer/06-08-SUMMARY.md` when done, transcribing the json
key set the report emitted before conversion and confirming the hand-declared builder reproduces it,
and stating that all fifteen reports have now converted so 06-09's retirement may proceed.

It MUST additionally enumerate this plan's per-command VARIANT inventory by name — 06-09's
pair-level gate reconciles against it, and record `1xe3ze1v9s` says re-derive rather than
transcribe — and, under D-08 option-a or option-b, list every regenerated golden with its
per-key or per-sentence justification. A golden that changed without a line here is a defect.
</output>
