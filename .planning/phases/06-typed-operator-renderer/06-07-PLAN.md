---
phase: 06-typed-operator-renderer
plan: 07
type: execute
wave: 3
depends_on: ["06-02", "06-03", "06-04", "06-05", "06-06"]
files_modified:
  - cmd/engram/operator_output_test.go
autonomous: true
requirements: [REQ-operator-renderer-typed]

estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`TestOperatorOutputParity` and `operatorParityRows` are deleted, along with their now-unreferenced helpers `operatorParityRow`, `jsonScalarValues` and `containsString` — obsolete because there is no longer a text/json divergence for a parity test to detect (06-CONTEXT.md D-09)."
    - "`operatorViewFixtures()` merges every group's fixture function and its key set is gated against `commandKeySet(operatorCommands())` in BOTH directions — a command with no fixture fails, and a fixture naming no command fails."
    - "The gated set is derived from the live cobra tree via `operatorCommands()`, never from a hand-written command list; the `backfill-short-ids` preview variant that a hand-written list once missed is covered by a fixture built from the sweep converter it actually delegates to."
    - "Adding one field to one operator report's doc struct makes it appear in BOTH `--output json` and `--output text` with no second call site edited (ROADMAP SC3), demonstrated live and recorded, not claimed in prose."
    - "No operator report document embeds a `store` package result type; every one is a hand-declared struct whose fields bound what can be disclosed in either lane (threat T-06-01)."
    - "All 15 operator commands are covered: `reindex`, `prune-expired`, `summarize-missing`, `backfill-short-ids`, `migrate`, `migrate status`, `migrate revert`, `migrate-set-owner`, `migrate-remap-owner`, `spine-review scan`, `spine-review verify`, `spine-review consolidate`, `spine-review archive`, `spine-review restore`, `spine-review purge`."
  artifacts:
    - "cmd/engram/operator_output_test.go — `operatorViewFixtures`, `setDiff`, `TestOperatorViewFixturesCoverEveryOperatorCommand`, `TestSetDiffDetectsDivergence`"
  key_links:
    - "`operatorCommands()` (cmd/engram/cmdwalk.go:101) -> `commandKeySet` (cmd/engram/cmdwalk_test.go:141) -> `TestOperatorViewFixturesCoverEveryOperatorCommand`: the both-directions set equality carried forward from the retired parity test, the one property D-09 says was worth keeping."
    - "`pruneViewFixtures` + `flatViewFixtures` + `migrateViewFixtures` + `archivePurgeViewFixtures` + `spineViewFixtures` -> `operatorViewFixtures`: five group functions, one merged map, one gate."
  prohibitions:
    - statement: "The retired parity test is not reimplemented in any form — no test asserts that a hand-listed set of fact strings appears in a document."
      status: enforced
      verification: "`rg -c 'facts' cmd/engram/operator_output_test.go` outputs `0` and `rg -c 'operatorParityRow' cmd/engram/` outputs `0`"
    - statement: "The enumeration gate's expected command set is never hand-listed; it is computed from `operatorCommands()` at test time."
      status: enforced
      verification: "`rg -n 'commandKeySet\\(operatorCommands\\(\\)\\)' cmd/engram/operator_output_test.go` shows the gate deriving its expectation, and no string literal command list appears in the gate body"
    - statement: "This phase does not add itself to `redEvidenceDirs` and does not modify `internal/store/redevidence_harness_test.go` — that harness is deferred per 06-CONTEXT.md `<deferred>` because it currently accepts a build failure as proof of RED."
      status: enforced
      verification: "`git diff --exit-code internal/store/redevidence_harness_test.go` exits 0 and `rg -c 'redEvidenceDirs' cmd/engram/` outputs `0`"
---

<objective>
Retire the superseded parity gate, land the both-directions enumeration gate over the merged fixture
set, and prove Success Criterion 3 by observation rather than assertion.

Purpose: 06-CONTEXT.md D-09 retires `TestOperatorOutputParity` as obsolete — there is no longer a
text/json divergence for it to detect — while carrying forward its one genuinely good property: it
gated its row set against `operatorCommands()` in both directions. This plan closes the phase by
moving that property onto the new fixtures and by demonstrating the ergonomic promise Phase 7 depends
on.

Output: a deleted parity test, a merged fixture map under a bidirectional gate, and a recorded live
demonstration of SC3.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-typed-operator-renderer/06-CONTEXT.md
@.planning/phases/06-typed-operator-renderer/06-01-PLAN.md
@.planning/phases/06-typed-operator-renderer/06-01-SUMMARY.md
@.planning/phases/06-typed-operator-renderer/06-03-SUMMARY.md
@.planning/phases/06-typed-operator-renderer/06-04-SUMMARY.md
@.planning/phases/06-typed-operator-renderer/06-05-SUMMARY.md
@.planning/phases/06-typed-operator-renderer/06-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retire `TestOperatorOutputParity` and its hand-built row table</name>
  <files>cmd/engram/operator_output_test.go</files>
  <read_first>
    cmd/engram/operator_output_test.go lines 117-140 (`operatorParityRow` and `operatorParityRows`' doc comments), lines 138-295 (the 15 hand-built rows), lines 295-330 (`jsonScalarValues`, `containsString`), lines 337-382 (`TestOperatorOutputParity` including its both-directions row-set gate — read this carefully, it is the ONE property being carried forward), lines 383-470 (`TestOperatorOutputEncoding`, `TestOperatorOutputEmpty`, `TestOperatorOutputStream` — all three STAY),
    .planning/phases/06-typed-operator-renderer/06-CONTEXT.md §Fate of the Existing Gate (D-09),
    .planning/phases/06-typed-operator-renderer/06-01-SUMMARY.md (where the two facts about the retired test were recorded for preservation)
  </read_first>
  <action>
Delete from `cmd/engram/operator_output_test.go`: the `operatorParityRow` type, the
`operatorParityRows` function and all 15 of its rows, the `jsonScalarValues` helper, the
`containsString` helper, and `TestOperatorOutputParity` itself. All four helpers are referenced only
by that test — confirm with a repository grep before deleting, and if any other reference exists,
stop and report it rather than deleting the referenced symbol.

Do NOT delete `TestOperatorOutputFormat`, `TestRenderOperatorTextAndJSON`, `TestOperatorOutputEncoding`,
`TestOperatorOutputEmpty`, or `TestOperatorOutputStream`. The last three are behavioral contracts the
new renderer must keep satisfying and the first two pin the flag validator and the renderer's two-mode
shape.

Prune any import of `cmd/engram/operator_output_test.go` that only the deleted code used — expect
`strconv` to become unused — and leave the rest.

Record the retirement rationale in a short comment where the test used to be, or in the file's
existing header commentary: the parity test is obsolete rather than superseded by a stronger
assertion, because under 06-CONTEXT.md D-01 both lanes derive from one serialization and there is no
divergence left to detect. Name the two facts about it that must not be lost (durable record
`b3wd4wwwda`): its `facts` strings were hand-listed — precisely the "test over hand-built rows"
ROADMAP Success Criterion 1 rejects — and it was one-directional, asserting every declared text fact
appeared in the json document but never that the json document failed to widen past the text. Name
`TestOperatorViewFixturesCoverEveryOperatorCommand` (Task 2) as the inheritor of its one good property.
  </action>
  <verify>
    <automated>go test ./cmd/engram/ -run 'TestOperatorOutput|TestRenderOperator' -v</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c 'operatorParityRow' cmd/engram/` outputs `0`
    - `rg -c 'jsonScalarValues|containsString|TestOperatorOutputParity' cmd/engram/` outputs `0`
    - `rg -c 'facts' cmd/engram/operator_output_test.go` outputs `0`
    - `go test ./cmd/engram/ -list 'TestOperatorOutput|TestRenderOperator'` lists `TestOperatorOutputFormat`, `TestRenderOperatorTextAndJSON`, `TestOperatorOutputEncoding`, `TestOperatorOutputEmpty` and `TestOperatorOutputStream`, and does NOT list `TestOperatorOutputParity`
    - `go test ./cmd/engram/ -run 'TestOperatorOutput|TestRenderOperator' -v` prints `--- PASS` for each listed test and exits 0
    - `task lint` exits 0 (no unused import survives)
  </acceptance_criteria>
  <done>The hand-built parity row table is gone, the three preserved contracts still pass, and the reasons the retired test mattered are recorded in source rather than only in git history.</done>
</task>

<task type="auto">
  <name>Task 2: Land the both-directions enumeration gate over the merged fixture set</name>
  <files>cmd/engram/operator_output_test.go</files>
  <read_first>
    cmd/engram/cmdwalk.go lines 63-116 (`operatorCommandExclusions` and `operatorCommands` — the structural predicate the gate derives from),
    cmd/engram/cmdwalk_test.go lines 141-200 (`commandKeySet` and `TestOperatorCommands`, which already asserts the exclusion set is exactly `{"serve","version"}`),
    cmd/engram/operator_view_test.go (`pruneViewFixtures`), cmd/engram/operator_view_flat_test.go (`flatViewFixtures`), cmd/engram/operator_view_migrate_test.go (`migrateViewFixtures`), cmd/engram/operator_view_archive_purge_test.go (`archivePurgeViewFixtures`), cmd/engram/operator_view_scan_test.go (`spineViewFixtures`) — the five group functions being merged and the exact `commandKey` strings each uses,
    cmd/engram/operator_output_test.go as left by Task 1,
    .planning/phases/06-typed-operator-renderer/06-CONTEXT.md §Fate of the Existing Gate (D-09's both-ways requirement) and §Specific Ideas (the `backfill-short-ids` preview variant that a hand-written list missed)
  </read_first>
  <action>
Add `func operatorViewFixtures() map[string][]any` to `cmd/engram/operator_output_test.go`. It
allocates one map and merges the five group functions into it: `pruneViewFixtures`,
`flatViewFixtures`, `migrateViewFixtures`, `archivePurgeViewFixtures`, `spineViewFixtures`. If two
group functions ever supply the same key, fail loudly rather than silently overwriting — append to the
existing slice is wrong here because a duplicate key means two plans both claimed one command, so
detect it and report the colliding key.

Add `func setDiff(want, got map[string]bool) (missing, extra []string)` returning, sorted, the keys
present in `want` but not `got` and the keys present in `got` but not `want`. Keep it pure and
independent of any command or fixture so it can be negative-tested directly.

Add `func TestOperatorViewFixturesCoverEveryOperatorCommand(t *testing.T)`. It computes
`want := commandKeySet(operatorCommands())` from the live cobra tree — never a string literal list —
and `got` from `operatorViewFixtures()`'s keys, then reports every entry of `setDiff`'s `missing` as an
operator command with no view fixture and every entry of its `extra` as a fixture naming a command that
is not in `operatorCommands()`. Assert additionally that every fixture entry's slice is non-empty, so a
key registered with zero documents cannot satisfy the gate vacuously. Document the test as the
inheritor of `TestOperatorOutputParity`'s one good property per D-09, and record that deriving the
expectation from the cobra tree is what caught `backfill-short-ids`'s unregistered preview variant that
a hand-written variant list had missed.

Add `func TestSetDiffDetectsDivergence(t *testing.T)` — the committed non-vacuity proof for the
enumeration gate, since the red-evidence patch harness is deferred and unavailable. Table-driven over
`setDiff` alone: a key in `want` only yields exactly that key in `missing` and an empty `extra`; a key
in `got` only yields the reverse; disjoint sets yield both non-empty; identical sets yield both empty;
empty inputs yield both empty. Add a comment stating that this is what makes the enumeration gate
non-vacuous and that it is deliberately independent of `operatorCommands()` so it cannot be satisfied
by the same data the gate reads.

Add `func TestOperatorViewIdentityAcrossEveryOperatorCommand(t *testing.T)` iterating the merged
`operatorViewFixtures()` and calling `assertViewIdentity` for every document of every command, so the
identity property is asserted once over the complete set rather than only per group. Name each subtest
by its command key.
  </action>
  <verify>
    <automated>go test ./cmd/engram/ -run 'TestOperatorViewFixturesCoverEveryOperatorCommand|TestSetDiffDetectsDivergence|TestOperatorViewIdentityAcrossEveryOperatorCommand' -v</automated>
  </verify>
  <acceptance_criteria>
    - `cmd/engram/operator_output_test.go` contains `func operatorViewFixtures() map[string][]any`, `func setDiff(`, `func TestOperatorViewFixturesCoverEveryOperatorCommand(`, `func TestSetDiffDetectsDivergence(` and `func TestOperatorViewIdentityAcrossEveryOperatorCommand(`
    - `rg -n 'commandKeySet\(operatorCommands\(\)\)' cmd/engram/operator_output_test.go` shows at least one hit
    - `rg -o '(pruneViewFixtures|flatViewFixtures|migrateViewFixtures|archivePurgeViewFixtures|spineViewFixtures)\(\)' cmd/engram/operator_output_test.go | wc -l` outputs `5`
    - `go test ./cmd/engram/ -run 'TestOperatorViewIdentityAcrossEveryOperatorCommand' -v` prints exactly 15 subtest `--- PASS` lines, one per operator command, and exits 0
    - `go test ./cmd/engram/ -run 'TestOperatorViewFixturesCoverEveryOperatorCommand|TestSetDiffDetectsDivergence' -v` prints `--- PASS` for both and exits 0
    - `git diff --exit-code internal/store/redevidence_harness_test.go` exits 0
    - `task lint` exits 0
  </acceptance_criteria>
  <done>Every command `operatorCommands()` returns has at least one view fixture and every fixture names a live operator command, gated in both directions from the cobra tree, with a committed case proving the gate can fail.</done>
</task>

<task type="auto">
  <name>Task 3: Demonstrate Success Criterion 3 live and verify the disclosure invariant across all 15 reports</name>
  <files>cmd/engram/operator_output_test.go</files>
  <read_first>
    cmd/engram/prune.go lines 127-152 (`pruneOutputDoc` — the report the SC3 probe mutates, chosen because it is the tracer's own report and the smallest),
    cmd/engram/spine_review_archive.go lines 58-66 (`archiveReportDoc`'s "so this exclusion is enforced by the type itself" comment — the canonical statement of the disclosure invariant this task verifies),
    .planning/ROADMAP.md §"Phase 6: Typed Operator Renderer" (the three success criteria, verbatim),
    .planning/phases/06-typed-operator-renderer/06-VALIDATION.md §Manual-Only Verifications (the SC3 probe's original framing and the `1xe3ze1v9s` warning that a prose-only non-vacuity claim retains no artifact)
  </read_first>
  <action>
Run the Success Criterion 3 probe live and record its transcript in the plan's execution notes. Add a
throwaway field to `pruneOutputDoc` only — for example `Probe string` tagged `json:"probe"` — set it in
`pruneReportDoc` only, then run `go run ./cmd/engram prune-expired --output json` and
`go run ./cmd/engram prune-expired --output text` against whatever store the local environment offers,
or, if no store is reachable, render `pruneReportDoc(...)` through `json.Encoder` and
`renderOperatorView` directly in a scratch test and capture both outputs. Confirm the new field
appears in BOTH lanes with exactly two edited lines and no second call site touched. Capture both
outputs verbatim. Then revert both edits completely and confirm `git diff --exit-code cmd/engram/prune.go`
exits 0 before committing. Per record `1xe3ze1v9s`, the captured transcript IS the evidence — a prose
claim that the probe was run is not.

Add `func TestOperatorDocsAreHandDeclared(t *testing.T)` verifying threat T-06-01 across the whole
tier at once rather than report by report: for every document in the merged `operatorViewFixtures()`,
reflect over its dynamic type and assert that it is a struct declared in this `main` package — its
`reflect.Type.PkgPath()` must be this package's own path and not any `internal/store` path — and that
it has no anonymous embedded field whose type's `PkgPath()` names `internal/store`. Document the test
as the tier-wide statement of the property `archiveReportDoc`'s comment asserts per report: every
operator document is hand-declared, so record content is unreachable by construction, and under D-01
that bound now covers the text lane as well as the json lane. Note in the comment that a
`store.VersionBucket` used as an ELEMENT type inside `migrateStatusReportDoc` is permitted and why —
it is a two-scalar value type carrying no record content, and it is a field's element type rather than
an embedded struct that would promote unknown fields.

Confirm the phase-level outcome and record it in the SUMMARY: `operatorCommands()` returns 15
commands; `operatorViewFixtures()` covers all 15; `go test ./cmd/engram/...` and `task` are green;
`git diff --exit-code go.mod go.sum` exits 0.
  </action>
  <verify>
    <automated>go test ./cmd/engram/ -run 'TestOperatorDocsAreHandDeclared' -v && go test ./cmd/engram/... && task</automated>
  </verify>
  <acceptance_criteria>
    - The plan's execution notes contain the verbatim `--output json` and `--output text` outputs from the SC3 probe, both showing the throwaway field, together with the `git diff --stat` proving exactly two edited lines
    - `git diff --exit-code cmd/engram/prune.go` exits 0 after the probe is reverted
    - `cmd/engram/operator_output_test.go` contains `func TestOperatorDocsAreHandDeclared(`
    - `go test ./cmd/engram/ -run 'TestOperatorDocsAreHandDeclared' -v` prints one subtest `--- PASS` line per operator command and exits 0
    - `go test ./cmd/engram/...` exits 0
    - `task` exits 0 and `task license:check` exits 0
    - `git diff --exit-code go.mod go.sum` exits 0
  </acceptance_criteria>
  <done>SC3 is demonstrated by a recorded transcript rather than claimed, and the hand-declared-document invariant is asserted once across every operator report instead of per file by comment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `store` package types -> operator serialization | Under D-01 the doc struct is the sole serialization, so an embedded store type would disclose every future field it gains, in both lanes at once. |
| live cobra tree -> gate expectation | A command added later must not be able to escape coverage by simply not being listed anywhere. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Information Disclosure | every operator report document | high | mitigate | `TestOperatorDocsAreHandDeclared` (Task 3) asserts across all 15 reports that each document's type is declared in this package and embeds no `internal/store` type, turning a per-file comment convention into a tier-wide runtime assertion. |
| T-06-12 | Elevation of Privilege | a future operator command added with no view fixture | medium | mitigate | `TestOperatorViewFixturesCoverEveryOperatorCommand` (Task 2) derives its expectation from `operatorCommands()` walking the live cobra tree, so a new command fails the build until it is covered; `TestSetDiffDetectsDivergence` proves that gate can fail. |
| T-06-13 | Repudiation | the SC3 ergonomic claim | medium | mitigate | Task 3 records the probe's verbatim two-lane output rather than asserting the property in prose, per record `1xe3ze1v9s`. |
| T-06-SC | Tampering | package-manager installs | low | accept | No installs — test-file edits only, and `git diff --exit-code go.mod go.sum` gates it. |
</threat_model>

## Artifacts this phase produces

Symbols created by THIS plan (newly created — exclude from drift verification):

- `operatorViewFixtures` (test helper func merging the five group fixture functions)
- `setDiff` (pure test helper func)
- `TestOperatorViewFixturesCoverEveryOperatorCommand`, `TestSetDiffDetectsDivergence`,
  `TestOperatorViewIdentityAcrossEveryOperatorCommand`, `TestOperatorDocsAreHandDeclared` (tests)

Symbols DELETED by this plan: `operatorParityRow`, `operatorParityRows`, `jsonScalarValues`,
`containsString`, `TestOperatorOutputParity`.

<verification>
- `go test ./cmd/engram/...` exits 0 and `task` exits 0.
- `go test ./cmd/engram/ -run 'TestOperatorViewIdentityAcrossEveryOperatorCommand' -v` prints exactly
  15 subtest PASS lines — one per operator command — proving the phase converted the complete set.
- `rg -c 'operatorParityRow' cmd/engram/` outputs `0`.
- `git diff --exit-code internal/store/redevidence_harness_test.go` exits 0 — the deferred harness was
  neither used nor modified.
- `git diff --exit-code go.mod go.sum` exits 0 — zero new dependencies across the whole phase.
</verification>

<success_criteria>
- ROADMAP SC1: text and json both derive from one serialization; identity holds by construction and
  the gate that observes it is provably able to fail.
- ROADMAP SC2: every operator command's `--output json|text` behavior is regression-free, with exactly
  three additive keys across the whole phase (`current_version`, `rerun`, `scope`), each closing a
  documented text/json gap.
- ROADMAP SC3: demonstrated live — one field added to one struct appears in both lanes with no second
  call site edited.
</success_criteria>

<output>
Create `.planning/phases/06-typed-operator-renderer/06-07-SUMMARY.md` when done.

Record: the retirement rationale and the two preserved facts about `TestOperatorOutputParity`; the
final 15-command coverage list; the three additive json keys the phase introduced and which gap each
closed; the SC3 probe transcript; and the fact that the deferred red-evidence harness
(`internal/store/redevidence_harness_test.go:303-316`, durable record `366pjeht8e`) was deliberately
not used and remains open as a follow-up.
</output>
