---
phase: 06-typed-operator-renderer
plan: 07
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - cmd/engram/spine_review_purge.go
  - cmd/engram/spine_review_purge_test.go
  - cmd/engram/testdata/operator-reports/
autonomous: true
requirements: [REQ-operator-renderer-typed]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "engram spine-review purge renders both sentence variants from two FieldSet builders, byte-identical to their pre-image on both lanes (D-04) — the preview's five template segments (header, id list, two fixed notice sentences, re-run command line) and the applied variant's header plus three independent id lists."
    - "The preview variant's category, tags, and older-than fields resolve their D-03 coverage through the re-run command the preview sentence already prints, so the field's placeholder interpolates through that same command-echoing logic rather than through a bare scalar — RESEARCH.md's proposed out for Open Question 2, confirmed valid for the preview sentence only."
    - "The applied variant's seven fields with no textual presence — eligible, eligible count, category, tags, older-than, and the two notice fields — are resolved by the D-08 selection recorded in 06-01-SUMMARY.md. The re-run-command out does NOT apply here: purgeAppliedSummary prints no re-run line, which is the open half of RESEARCH.md Open Question 2."
    - "Each of the applied variant's three id lists is its own []FieldSet-valued field with its own explicit separator and its own row template; they are never merged into one list."
    - "Both published notice sentences reach the json lane with the identical wording a human reading the text lane sees, preserving today's guarantee that a json consumer is not left to infer them from prose."
    - "Goldens for both variants are generated from the pre-image builders BEFORE conversion and are unchanged by the conversion commit."
  artifacts:
    - cmd/engram/testdata/operator-reports/spine-review-purge.preview.txt
    - cmd/engram/testdata/operator-reports/spine-review-purge.applied.txt
  key_links:
    - "purgePreviewReport's category/tags/older-than placeholders -> purgeRerunCommand's output -> the re-run line the preview sentence already prints"
    - "purgeAppliedReport's three id lists -> three separate []FieldSet fields, each with its own Sep and row template"
    - "purgePreviewReport / purgeAppliedReport -> renderOperator(FieldSet) -> TestOperatorReportGolden -> the frozen goldens"
  prohibitions:
    - "Do not resolve the applied variant's unstated fields by claiming the re-run-command out applies; purgeAppliedSummary prints no re-run line, and asserting otherwise would make the coverage claim false."
    - "Do not merge the applied variant's deleted, spared, and appeared lists into one list field; they carry three different row templates."
    - "Do not give any []FieldSet-valued field an implicit separator."
    - "Do not reword, reflow, or re-punctuate either published notice sentence; both are shared constants whose exact bytes appear in text and json today."
    - "Do not print record content on any purge path; only ids reach a field."
    - "Do not delete purgePreviewSummary, purgeAppliedSummary, purgeReportDoc, purgePreviewDoc, purgeAppliedDoc, purgeRerunCommand, purgeScopeText, purgeClassesText, olderThanText, or nonNilStrings in this plan."
    - "Do not modify or delete TestOperatorOutputParity or operatorParityRows."
    - "Do not edit cmd/engram/operator_golden_test.go."
    - "Do not regenerate any golden file in the conversion commit."
    - "Do not change any operator-facing sentence byte (D-04)."
---

<objective>
Convert `spine-review purge`'s preview and applied reports to `FieldSet` builders, gated by goldens
generated from their pre-image.

Purpose: this is the report where the phase's central tension is sharpest. `purgeReportDoc` is the
largest document in the tier at sixteen fields, the preview sentence has five distinct template
segments, and the applied sentence carries seven json fields it never states — the concrete case that
forced the D-08 checkpoint in plan 06-01.

Output: `purgePreviewReport`, `purgeAppliedReport`, two checked-in golden pairs, and two call sites
moved off `renderOperatorLegacy`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-typed-operator-renderer/06-CONTEXT.md
@.planning/phases/06-typed-operator-renderer/06-PATTERNS.md
@.planning/phases/06-typed-operator-renderer/06-01-SUMMARY.md
@cmd/engram/fieldset.go
@cmd/engram/spine_review_archive.go
</context>

## Artifacts this phase produces

See `06-01-PLAN.md` § "Artifacts this phase produces" for the full phase-level list. This plan
contributes: `purgePreviewReport`, `purgeAppliedReport`, and the two golden pairs named in
`must_haves`.

<tasks>

<task type="auto">
  <name>Task 1: Freeze both purge goldens from the pre-image and record the applied variant's unstated-field inventory</name>
  <files>cmd/engram/spine_review_purge_test.go, cmd/engram/testdata/operator-reports/</files>
  <read_first>
    - cmd/engram/operator_golden_test.go — `registerOperatorReport`'s parameter order and `TestOperatorReportGolden`'s comparison contract
    - cmd/engram/prune_test.go — the `init()` registration 06-01 wrote
    - cmd/engram/spine_review_purge.go lines 140-183 — `purgeScopeText` and `purgeRerunCommand`, including exactly which flags the re-run line echoes
    - cmd/engram/spine_review_purge.go lines 185-268 — `purgeReportDoc`'s sixteen fields with their tags, plus `purgePreviewDoc` and `purgeAppliedDoc`
    - cmd/engram/spine_review_purge.go lines 270-309 — both summaries, including the applied variant's three row loops and the absence of any re-run line there
    - cmd/engram/operator_output_test.go lines 245-254 — `operatorParityRows`' existing hand-built purge inputs, reused verbatim
  </read_first>
  <action>
Register two fixtures in `cmd/engram/spine_review_purge_test.go`, each passing nil for the `Report`
argument:

- key `spine-review-purge`, variant `preview` — a `store.PurgeOptions` value that supplies a scope, at
  least one class, a category, at least one tag, and a non-zero older-than duration, so every flag the
  re-run line echoes is exercised, with at least two eligible ids.
- key `spine-review-purge`, variant `applied` — reuse `operatorParityRows`' purge inputs verbatim
  (one deleted, one spared, one appeared id, with the expired class and a scope), extended with a
  category, tags, and an older-than duration so the applied document's unstated fields are non-empty
  and therefore visible in the golden.

Every input must be deterministic. Generate the four golden files with the package `-update` flag,
inspect them by eye against current CLI output, then commit. This commit contains no production-code
change.

Record in the SUMMARY the exact list of json keys present in `spine-review-purge.applied.json` whose
values appear nowhere in `spine-review-purge.applied.txt`. That list is the concrete evidence the D-08
decision was made against; Task 2 applies the decision to exactly these keys and no others.
  </action>
  <verify>
    <automated>go test ./cmd/engram/... -count=1 -run 'TestOperatorReportGolden' -v 2>&1 | grep -E '^( *)(=== RUN|--- (PASS|FAIL))'</automated>
  </verify>
  <acceptance_criteria>
    - `ls cmd/engram/testdata/operator-reports/ | rg -c '^spine-review-purge\.'` reports 4.
    - `go test ./cmd/engram/... -run 'TestOperatorReportGolden' -v` shows a `--- PASS` subtest line for
      both new `<key>/<variant>` fixtures.
    - `TestOperatorReportGoldenSetIsComplete` passes.
    - `git diff --stat cmd/engram/spine_review_purge.go` is empty for this commit.
    - `cmd/engram/testdata/operator-reports/spine-review-purge.preview.txt` contains a re-run line
      whose text carries the category, every tag, and the older-than value the fixture supplied —
      confirming RESEARCH.md's Open Question 2 out for the preview sentence against real bytes.
    - `cmd/engram/testdata/operator-reports/spine-review-purge.applied.txt` contains no re-run line,
      and its `.json` sibling contains the eligible, eligible-count, category, tags, older-than, and
      both notice keys — confirming the open half of Open Question 2 against real bytes.
    - The SUMMARY enumerates that key list explicitly.
  </acceptance_criteria>
  <done>Both purge variants are pinned to committed golden bytes produced by the pre-image code, with the applied variant's unstated-field inventory recorded from real output rather than inferred.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert both purge reports to FieldSet builders under the recorded D-08 decision</name>
  <files>cmd/engram/spine_review_purge.go, cmd/engram/spine_review_purge_test.go</files>
  <read_first>
    - cmd/engram/fieldset.go — `Field`, `FieldSet`, `present`, `Sep`, `Render`, `validateFieldSet`'s recursion
    - cmd/engram/spine_review_archive.go — `archiveReport` as written in 06-05, the nested-rows shape to build on
    - .planning/phases/06-typed-operator-renderer/06-01-SUMMARY.md — the D-08 selection, which governs this report more than any other
    - cmd/engram/testdata/operator-reports/spine-review-purge.applied.json and .txt, frozen by Task 1 — the two files the enumerated unstated-key list was read off, and the list itself as recorded in this plan's in-progress SUMMARY
    - .planning/phases/06-typed-operator-renderer/06-RESEARCH.md § "Pitfall 2" and § "Pattern 2" purge rows
    - cmd/engram/spine_review_purge.go lines 140-309
  </read_first>
  <behavior>
    - `validateFieldSet(purgePreviewReport(...))` returns nil, and its recursion reaches each eligible-id
      row's own field set.
    - `validateFieldSet(purgeAppliedReport(...))` returns nil, and its recursion reaches each row of all
      three id lists.
    - `TestOperatorReportGolden` passes for both fixtures with `Report` now non-nil, proving both lanes
      byte-match the pre-image goldens.
    - `TestPurgeReportDocFieldsNeverNull`, `TestPurgeAppliedSummaryNamesAppearedExplicitly`, and
      `TestSpineReviewPurgeSameRunNoticePublished` continue to pass without edits to their assertions.
  </behavior>
  <action>
Add `purgePreviewReport(eligible []string, opts store.PurgeOptions) FieldSet` and
`purgeAppliedReport(eligible []string, res store.PurgeResult, opts store.PurgeOptions) FieldSet` to
`cmd/engram/spine_review_purge.go`, matching the pre-image converters' parameter lists so the two
render call sites change only in the function they name.

The preview template has five segments in this order: the header line carrying the eligible count, the
classes text, and the scope text; a `[]FieldSet`-valued eligible-id field with an explicit newline
`Sep` and a per-id row template; the two published notice sentences, each as its own field carrying
the shared constant so the json lane keeps the identical wording; and the re-run line. Attach the
category, tags, and older-than fields' placeholders to a `Render` that routes through
`purgeRerunCommand`'s own flag-echoing logic rather than emitting a bare scalar, so those three fields
are structurally referenced by text that already exists — RESEARCH.md's Open Question 2 out, which
Task 1 confirmed valid for this sentence against real bytes.

The applied template is the header line plus three separate `[]FieldSet`-valued fields — deleted,
spared, appeared — each with its own explicit newline `Sep` and its own row template, since the three
row prefixes differ and the appeared rows carry their own trailing clause. Do not merge them.

For the applied variant's fields that appear nowhere in its sentence — the eligible set and its count,
category, tags, older-than, and both notices, exactly as enumerated in Task 1's SUMMARY entry — apply
the D-08 selection from `06-01-SUMMARY.md` verbatim. Do NOT claim the re-run-command out here: the
applied sentence prints no re-run line, so routing those placeholders through `purgeRerunCommand` would
inject text that does not exist today and break D-04.

Set every field's `Present` predicate from today's struct tag rather than from which mode is running.
Reproduce both sentences byte-identically (D-04), including the pre-image's trailing-newline trim, and
keep only ids reaching a field so record content stays unprintable on every purge path. Point both
render call sites at `renderOperator`. Leave every pre-image function and struct named in this plan's
prohibitions in place for 06-09. Update the `init()` registrations to pass the new builders as
`Report`, and regenerate nothing.
  </action>
  <verify>
    <automated>go test ./cmd/engram/... -count=1 -run 'TestPurge|TestSpineReviewPurge|TestParsePurgeClasses|TestRequirePurgeFilterScope|TestOperatorReportGolden|TestOperatorOutputParity|TestOperatorOutputEmpty' -v 2>&1 | grep -E '^( *)(=== RUN|--- (PASS|FAIL)|FAIL|ok)'</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd/engram/... -list 'TestPurge.*|TestSpineReviewPurge.*|TestParsePurgeClasses|TestRequirePurgeFilterScope'`
      lists at least `TestPurgeReportDocFieldsNeverNull`,
      `TestPurgeAppliedSummaryNamesAppearedExplicitly`, `TestSpineReviewPurgeSameRunNoticePublished`,
      `TestSpineReviewPurgeClassTagsDoNotLatchAcrossRows`, `TestSpineReviewPurgeRejectsInvalidOutput`,
      `TestSpineReviewPurgeFilterPathRequiresScope`,
      `TestSpineReviewPurgeClassOnlyDoesNotRequireScope`, `TestParsePurgeClasses`, and
      `TestRequirePurgeFilterScope`, and the `-v` run shows `--- PASS` for each.
    - `git diff --exit-code cmd/engram/testdata/operator-reports/` is clean for this commit.
    - `rg -n 'renderOperatorLegacy\(' cmd/engram/spine_review_purge.go` returns no match.
    - `rg -n 'func purgePreviewReport\(|func purgeAppliedReport\(' cmd/engram/` returns exactly one
      match each.
    - `rg -c 'Sep:' cmd/engram/spine_review_purge.go` reports at least 4 — one for the preview's
      eligible list and one for each of the applied variant's three lists.
    - `rg -n 'purgeRerunCommand' cmd/engram/spine_review_purge.go` shows the call reached from
      `purgePreviewReport` and NOT from `purgeAppliedReport`.
    - `TestOperatorOutputParity` and `TestOperatorOutputEmpty` still show `--- PASS`, and
      `git diff --stat cmd/engram/operator_output_test.go` is empty for this commit.
    - `task lint` and `task test` are green.
  </acceptance_criteria>
  <done>The tier's largest document renders both variants from field sets, with the preview's indirect coverage routed through the re-run line it already prints and the applied variant's unstated fields disposed of exactly as the recorded D-08 decision says.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (no new boundary) | Conversion of already-authorized operator reports. No new input, persisted data, authz path, or dependency. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| 06-07:T-06-PARSE | Information Disclosure | `purgePreviewReport`, `purgeAppliedReport` | high | mitigate | This is the report where a json document most plausibly carries state its sentence never names — seven such fields today on the applied path. Mitigated by `validateFieldSet`'s set equality, by applying the recorded D-08 decision to exactly the enumerated key list rather than to a guess, and by the frozen goldens; inherits 06-01:T-06-PARSE's red-evidence proof. |
| 06-07:T-06-CONTENT | Information Disclosure | purge row field sets | high | mitigate | Purge output must never print record content on any path. Mitigated by declaring only ids on every row field set and by the frozen goldens, which would show any leak as changed bytes. |
| 06-07:T-06-NOTICE | Repudiation | the two published notice fields | medium | mitigate | A json consumer must see the identical notice wording a human sees, so an operator cannot later claim the limitation was never surfaced. Mitigated by carrying each notice as its own field sourced from the shared constant, and by the prohibition against rewording either sentence. |
| 06-07:T-06-JOIN | Tampering | the applied variant's three id lists | medium | mitigate | Merging the lists or defaulting a separator would silently change rendered bytes and could misattribute an id to the wrong outcome. Mitigated by three separate fields with explicit separators and by the `Sep:` count acceptance criterion. |
| 06-07:T-06-NONE | (no applicable STRIDE category) | plan scope | low | accept | RESEARCH.md's ASVS review found no conventional threat applies. Recorded, not fabricated. |

**Package legitimacy:** no install task in this plan; `go.mod` and `go.sum` are untouched.
</threat_model>

<verification>
- `task lint` and `task test` green.
- `go test ./cmd/engram/... -count=1` green with `TestOperatorOutputParity` unmodified and passing.
- `git diff --exit-code cmd/engram/testdata/operator-reports/` clean after the conversion commit.
</verification>

<success_criteria>
- Both purge variants render from `FieldSet` builders through the new `renderOperator`.
- The preview's indirect field coverage is proven against the re-run line's real bytes.
- The applied variant's unstated fields are disposed of by the recorded D-08 decision, applied to an
  enumerated key list.
- Two golden pairs, frozen from the pre-image, survive the conversion byte-for-byte.
</success_criteria>

<output>
Create `.planning/phases/06-typed-operator-renderer/06-07-SUMMARY.md` when done. It MUST enumerate the
applied variant's unstated json keys, state how D-08 disposed of each, and record that the
re-run-command resolution applies to the preview sentence only.
</output>
