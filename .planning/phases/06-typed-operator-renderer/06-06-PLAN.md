---
phase: 06-typed-operator-renderer
plan: 06
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - cmd/engram/spine_review_consolidate.go
  - cmd/engram/spine_review_consolidate_test.go
  - cmd/engram/spine_review_verify.go
  - cmd/engram/spine_review_verify_test.go
  - cmd/engram/testdata/operator-reports/
autonomous: true
requirements: [REQ-operator-renderer-typed]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "engram spine-review consolidate renders its header, its conditional min-score line, and its per-pair rows from one FieldSet, byte-identical to its pre-image on both lanes, in both the filter-supplied and the no-filter case."
    - "consolidateReportDoc's MinScore is a replacement, not a drop: something always renders in text — the score when a filter was supplied and the no-filter prose otherwise — while the json key keeps its omitempty behavior. It is modelled with a Render function, never with a conditional span, and must not be conflated with archiveResultDoc's ID."
    - "engram spine-review verify renders its header plus THREE independent per-tier row lists (moved, broken, unverifiable) from three separate []FieldSet-valued fields, each with its own explicit separator — never one merged list."
    - "Each verify row's field set declares its own record id, short id, ref, and reason, and validateFieldSet's recursion enforces coverage one level down for every tier (D-03 + D-05)."
    - "A verify report with all three tiers empty renders the header alone in text and three empty json arrays, never a json null."
    - "Goldens for all four variants are generated from the pre-image builders BEFORE conversion and are unchanged by the conversion commit."
  artifacts:
    - cmd/engram/testdata/operator-reports/spine-review-consolidate.min-score.txt
    - cmd/engram/testdata/operator-reports/spine-review-consolidate.no-min-score.txt
    - cmd/engram/testdata/operator-reports/spine-review-verify.all-tiers.txt
    - cmd/engram/testdata/operator-reports/spine-review-verify.empty.txt
  key_links:
    - "consolidateReport's min-score Render -> alternate prose in text while the json key keeps omitempty semantics"
    - "verifyReport's three []FieldSet fields -> validateFieldSet's recursion -> per-row coverage for every tier"
    - "consolidateReport / verifyReport -> renderOperator(FieldSet) -> TestOperatorReportGolden -> the frozen goldens"
  prohibitions:
    - "Do not model the min-score line as a conditional span; nothing is dropped from the text on either branch, and treating a replacement as a drop changes the rendered bytes."
    - "Do not merge verify's three per-tier lists into one list field; they carry three different row templates and are three independent fields."
    - "Do not give any []FieldSet-valued field an implicit separator; every one sets an explicit Sep."
    - "Do not let a verify report's excerpt text reach a field; TestVerifyReportNeverIncludesExcerpt pins that exclusion and the field set must preserve it by construction."
    - "Do not delete consolidateSummary, consolidateReportDoc, consolidatePairDoc, consolidateDoc, verifySummary, verifyReportDoc, verifyEntryDoc, or verifyDoc in this plan."
    - "Do not modify or delete TestOperatorOutputParity or operatorParityRows."
    - "Do not edit cmd/engram/operator_golden_test.go."
    - "Do not regenerate any golden file in the conversion commit."
    - "Do not change any operator-facing sentence byte (D-04)."
---

<objective>
Convert `spine-review consolidate` and `spine-review verify` to nested `FieldSet` builders, gated by
goldens generated from their pre-image.

Purpose: these two carry the tier's remaining nesting shapes that archive does not — a conditional line
that REPLACES rather than drops, and three independent per-tier row lists in one report. Both are
places where a single generic list abstraction would silently change bytes.

Output: `consolidateReport`, `verifyReport`, four checked-in golden pairs, and two call sites moved off
`renderOperatorLegacy`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-typed-operator-renderer/06-CONTEXT.md
@.planning/phases/06-typed-operator-renderer/06-PATTERNS.md
@.planning/phases/06-typed-operator-renderer/06-01-SUMMARY.md
@cmd/engram/fieldset.go
@cmd/engram/spine_review_archive.go
</context>

## Artifacts this phase produces

See `06-01-PLAN.md` § "Artifacts this phase produces" for the full phase-level list. This plan
contributes: `consolidateReport`, `verifyReport`, and the four golden pairs named in `must_haves`.

<tasks>

<task type="auto">
  <name>Task 1: Freeze consolidate and verify goldens from the pre-image, covering both min-score branches and an all-empty verify report</name>
  <files>cmd/engram/spine_review_consolidate_test.go, cmd/engram/spine_review_verify_test.go, cmd/engram/testdata/operator-reports/</files>
  <read_first>
    - cmd/engram/operator_golden_test.go — `registerOperatorReport`'s parameter order and `TestOperatorReportGolden`'s comparison contract
    - cmd/engram/prune_test.go — the `init()` registration 06-01 wrote
    - cmd/engram/spine_review_consolidate.go lines 150-210 — `consolidateReportDoc`, `consolidateDoc`, and `consolidateSummary`'s conditional min-score branch and per-pair loop
    - cmd/engram/spine_review_verify.go lines 485-545 — `verifyReportDoc`, `verifyEntryDoc`, `verifyDoc`, and `verifySummary`'s three per-tier loops
    - cmd/engram/operator_output_test.go lines 199-220 — `operatorParityRows`' existing hand-built consolidate and verify inputs, reused verbatim
  </read_first>
  <action>
Register four fixtures, each passing nil for the `Report` argument:

- key `spine-review-consolidate`, variant `min-score` — reuse `operatorParityRows`' consolidate inputs
  verbatim, including the supplied min-score pointer.
- key `spine-review-consolidate`, variant `no-min-score` — the same inputs with a nil min-score
  pointer, so the alternate no-filter prose branch is goldened. `operatorParityRows` covers only the
  supplied branch, so this shape is currently ungoldened even though
  `TestConsolidateSummaryMinScoreRendering` exercises it.
- key `spine-review-verify`, variant `all-tiers` — reuse `operatorParityRows`' verify report verbatim,
  which populates one entry in each of the moved, broken, and unverifiable tiers.
- key `spine-review-verify`, variant `empty` — a report with all three tiers empty and zero counts, so
  the empty-list rendering is pinned on both lanes.

Every input must be deterministic. Generate the eight golden files with the package `-update` flag,
inspect them by eye against current CLI output, then commit. This commit contains no production-code
change.

Record in the SUMMARY that `spine-review-consolidate.no-min-score.json` omits the min-score key while
its `.txt` sibling still renders a min-score line carrying the no-filter prose — the replacement shape
that Task 2 must reproduce with a `Render` function rather than a conditional span.
  </action>
  <verify>
    <automated>go test ./cmd/engram/... -count=1 -run 'TestOperatorReportGolden' -v 2>&1 | grep -E '^( *)(=== RUN|--- (PASS|FAIL))'</automated>
  </verify>
  <acceptance_criteria>
    - `ls cmd/engram/testdata/operator-reports/ | rg -c '^(spine-review-consolidate|spine-review-verify)\.'`
      reports 8.
    - `go test ./cmd/engram/... -run 'TestOperatorReportGolden' -v` shows a `--- PASS` subtest line for
      each of the four new `<key>/<variant>` fixtures.
    - `TestOperatorReportGoldenSetIsComplete` passes.
    - `git diff --stat cmd/engram/spine_review_consolidate.go cmd/engram/spine_review_verify.go` is
      empty for this commit.
    - `cmd/engram/testdata/operator-reports/spine-review-consolidate.no-min-score.txt` contains a
      min-score line and its `.json` sibling contains no min-score key — the pre-image replacement
      shape.
    - `cmd/engram/testdata/operator-reports/spine-review-verify.empty.json` contains three empty
      arrays and no occurrence of the four-character json null literal.
  </acceptance_criteria>
  <done>Both min-score branches and both verify shapes are pinned to committed golden bytes produced by the pre-image code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert consolidate and verify to nested FieldSet builders</name>
  <files>cmd/engram/spine_review_consolidate.go, cmd/engram/spine_review_consolidate_test.go, cmd/engram/spine_review_verify.go, cmd/engram/spine_review_verify_test.go</files>
  <read_first>
    - cmd/engram/fieldset.go — `Field`, `FieldSet`, `present`, `Sep`, `Render`, `validateFieldSet`'s recursion
    - cmd/engram/spine_review_archive.go — `archiveReport` as written in 06-05: the nested-rows shape to build on
    - .planning/phases/06-typed-operator-renderer/06-01-SUMMARY.md — the D-08 selection
    - .planning/phases/06-typed-operator-renderer/06-PATTERNS.md § Group B, including the replacement-not-drop excerpt and the per-report join ledger
    - .planning/phases/06-typed-operator-renderer/06-RESEARCH.md § "Pattern 3" — the full omitempty inventory distinguishing drop from replacement
    - cmd/engram/spine_review_consolidate.go lines 150-210 and cmd/engram/spine_review_verify.go lines 485-545
  </read_first>
  <behavior>
    - `validateFieldSet(consolidateReport(...))` returns nil with a supplied min-score and with a nil
      min-score, and its recursion reaches each pair row's own field set.
    - `validateFieldSet(verifyReport(...))` returns nil for an all-tiers report and for an all-empty
      report, and its recursion reaches each row of each of the three tiers.
    - `TestOperatorReportGolden` passes for all four fixtures with `Report` now non-nil.
    - `TestConsolidateSummaryMinScoreRendering`, `TestConsolidateRowNamesBothScopes`,
      `TestConsolidateNeverLabelsPairAsDuplicateOrCluster`, `TestVerifySummaryFormat`, and
      `TestVerifyReportNeverIncludesExcerpt` continue to pass without edits to their assertions.
  </behavior>
  <action>
Add `consolidateReport(pairs []store.DuplicatePair, scope string, allScopes bool, minScore *float32,
topK, scanned, considered int) FieldSet` to `cmd/engram/spine_review_consolidate.go`, matching the
pre-image function's own parameter list. The min-score field carries a `Render` function returning the
formatted score when the pointer is non-nil and the no-filter prose otherwise, so something always
renders in text; its json presence keeps today's `omitempty` semantics through its `Present` predicate.
This is a replacement, not a drop — do not reach for the conditional span, which would delete the line
entirely and change the bytes. The candidate pairs become a `[]FieldSet`-valued field with an explicit
newline `Sep`, each row declaring score, both ids, both short ids, and both scopes.

Add `verifyReport(report verifyReport) FieldSet` — or a name that does not collide with the existing
`verifyReport` type; if it collides, name the builder `verifyFieldSetReport` and record the deviation
in the SUMMARY — to `cmd/engram/spine_review_verify.go`. Declare THREE separate `[]FieldSet`-valued
fields, one per tier, each with its own explicit newline `Sep` and its own row template, because the
three tiers carry three different row prefixes. Do not merge them into one list. Each row's field set
declares the record id, the short id, the ref, and the reason, and nothing else — excerpt text must
stay unreachable from a field, which `TestVerifyReportNeverIncludesExcerpt` already pins.

Reproduce both sentences byte-identically (D-04), including the pre-image's trailing-newline trim.
Apply the D-08 selection from `06-01-SUMMARY.md` to any field with no textual presence in a given
variant; do not re-decide it here. Point both render call sites at `renderOperator`. Leave
`consolidateSummary`, `consolidateReportDoc`, `consolidatePairDoc`, `consolidateDoc`, `verifySummary`,
`verifyReportDoc`, `verifyEntryDoc`, and `verifyDoc` in place for 06-09. Update both `init()`
registrations to pass the new builders as `Report`, and regenerate nothing.
  </action>
  <verify>
    <automated>go test ./cmd/engram/... -count=1 -run 'TestConsolidate|TestSpineReviewConsolidate|TestVerify|TestSpineReviewVerify|TestOperatorReportGolden|TestOperatorOutputParity|TestOperatorOutputEmpty' -v 2>&1 | grep -E '^( *)(=== RUN|--- (PASS|FAIL)|FAIL|ok)'</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd/engram/... -list 'TestConsolidate.*|TestSpineReviewConsolidate.*|TestVerify.*|TestSpineReviewVerify.*'`
      lists at least `TestConsolidateDocEmptyCandidatesMarshalsEmptyArray`,
      `TestConsolidateRowNamesBothScopes`, `TestConsolidateNeverLabelsPairAsDuplicateOrCluster`,
      `TestConsolidateSummaryMinScoreRendering`, `TestSpineReviewConsolidateRejectsInvalidOutput`,
      `TestVerifyDocEmptyResultMarshalsEmptyArrays`, `TestVerifySummaryFormat`,
      `TestVerifyReportNeverIncludesExcerpt`, and `TestSpineReviewVerifyRejectsInvalidOutput`, and the
      `-v` run shows `--- PASS` for each.
    - `git diff --exit-code cmd/engram/testdata/operator-reports/` is clean for this commit.
    - `rg -n 'renderOperatorLegacy\(' cmd/engram/spine_review_consolidate.go cmd/engram/spine_review_verify.go`
      returns no match.
    - `rg -c 'Sep:' cmd/engram/spine_review_verify.go` reports at least 3 — one per tier — and
      `rg -c 'Sep:' cmd/engram/spine_review_consolidate.go` reports at least 1.
    - `TestOperatorOutputParity` and `TestOperatorOutputEmpty` still show `--- PASS`, and
      `git diff --stat cmd/engram/operator_output_test.go` is empty for this commit.
    - `task lint` and `task test` are green.
  </acceptance_criteria>
  <done>Both reports render from nested field sets with per-list separators declared explicitly, the replacement-shaped min-score line reproduced by a Render function, and the pre-image goldens unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (no new boundary) | Conversion of already-authorized operator reports. No new input, persisted data, authz path, or dependency. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| 06-06:T-06-PARSE | Information Disclosure | `consolidateReport`, `verifyReport` | medium | mitigate | A converted report whose json states more than its sentence, across nested rows in three tiers. Mitigated by `validateFieldSet` recursing into every nested value plus the frozen goldens; inherits 06-01:T-06-PARSE's red-evidence proof. |
| 06-06:T-06-EXCERPT | Information Disclosure | verify row field sets | high | mitigate | A verify row must never carry excerpt text out of a cited file. Mitigated by declaring only record id, short id, ref, and reason on each row, by the existing `TestVerifyReportNeverIncludesExcerpt` gate, and by the frozen goldens, which would show any leak as changed bytes. |
| 06-06:T-06-JOIN | Tampering | the three per-tier list fields | medium | mitigate | Merging the tiers or defaulting a separator would silently change rendered bytes. Mitigated by the explicit per-field separator requirement and by the `Sep:` count acceptance criteria. |
| 06-06:T-06-NONE | (no applicable STRIDE category) | plan scope | low | accept | RESEARCH.md's ASVS review found no conventional threat applies. Recorded, not fabricated. |

**Package legitimacy:** no install task in this plan; `go.mod` and `go.sum` are untouched.
</threat_model>

<verification>
- `task lint` and `task test` green.
- `go test ./cmd/engram/... -count=1` green with `TestOperatorOutputParity` unmodified and passing.
- `git diff --exit-code cmd/engram/testdata/operator-reports/` clean after the conversion commit.
</verification>

<success_criteria>
- Consolidate and verify render from nested `FieldSet` builders through the new `renderOperator`.
- The replacement-shaped min-score line and the three independent tier lists reproduce byte-for-byte.
- Four golden pairs, frozen from the pre-image, survive the conversion.
</success_criteria>

<output>
Create `.planning/phases/06-typed-operator-renderer/06-06-SUMMARY.md` when done, naming the four
fixture keys, recording any builder-name deviation forced by a collision with the existing
`verifyReport` type, and confirming the replacement-versus-drop distinction held on both lanes.
</output>
