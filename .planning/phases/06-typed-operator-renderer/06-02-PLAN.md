---
phase: 06-typed-operator-renderer
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - cmd/engram/reindex.go
  - cmd/engram/reindex_test.go
  - cmd/engram/summarize.go
  - cmd/engram/summarize_test.go
  - cmd/engram/testdata/operator-reports/
autonomous: true
requirements: [REQ-operator-renderer-typed]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "engram reindex renders all three sentence variants (dry-run, dry-run with resume, applied) from three fieldset.Set builders, byte-identical to their pre-image on both lanes (D-04)."
    - "engram summarize-missing renders both sentence variants (dry-run, live) from two fieldset.Set builders, byte-identical to their pre-image on both lanes (D-04)."
    - "Every field of every converted variant is referenced by exactly one placeholder in that variant's template, proven by fieldset.New's validation passing on each builder's output (D-03)."
    - "summarize-missing's dry-run variant, whose Failed count has no textual presence today, is handled by the D-08 selection recorded in 06-01-SUMMARY.md rather than by a fresh judgement call."
    - "Every one of the five variants is minted by an operatorReport(key, variant, ...) call whose identity arguments are string LITERALS in the same production statement as the branch producing that shape. 06-09 derives the phase's variant universe by parsing those call sites out of production source, so no test-side variant declaration exists to drift from them (06-REVIEWS.md cycle-2 HIGH-2 and MEDIUM-2)."
    - "Goldens for all five variants are generated from the pre-image builders BEFORE conversion, and the pre-image bytes survive the conversion commit unchanged: on any lane the recorded D-08 option left frozen they stay in the canonical golden, and on a lane it moved they stay in that variant's <Key>.<Variant>.legacy.<ext> pre-image golden, which the harness keeps under test on the legacy lane (06-REVIEWS.md cycle-3 HIGH-1)."
  artifacts:
    - cmd/engram/testdata/operator-reports/reindex.dry-run.txt
    - cmd/engram/testdata/operator-reports/reindex.dry-run-resume.txt
    - cmd/engram/testdata/operator-reports/reindex.applied.txt
    - cmd/engram/testdata/operator-reports/summarize-missing.dry-run.txt
    - cmd/engram/testdata/operator-reports/summarize-missing.applied.txt
  key_links:
    - "reindexReport / summarizeReport -> renderOperator(fieldset.Set) -> TestOperatorReportGolden -> the frozen goldens"
  prohibitions:
    - "Do not delete reindexSummary, reindexReportDoc, summarizeSummary, or summarizeReportDoc in this plan; TestOperatorOutputParity still reads them and the goldens were generated from them."
    - "Do not modify or delete TestOperatorOutputParity or operatorParityRows."
    - "Do not edit cmd/engram/operator_golden_test.go; fixture registration belongs in each report's own test file so parallel conversion plans never collide."
    - "Do not regenerate any golden file in the conversion commit EXCEPT exactly as the `## D-08 downstream execution contract` section permits for the option 06-01-SUMMARY.md recorded, and never without the named per-key or per-sentence justification that section requires. Under option-c both lanes stay frozen."
    - "Do not change any operator-facing sentence byte (D-04)."
    - "Do not introduce reflection-based dispatch or a templating-engine import."
---

<objective>
Convert the two flat, single-shape Group A operator reports — `reindex` and `summarize-missing` — to
`fieldset.Set` builders, gated by goldens generated from their pre-image.

Purpose: these are the mechanical repetitions of the prune conversion the 06-01 tracer proved. They
carry no nesting and no conditional presence, so they exercise the multi-variant path only.

Output: `reindexReport`, `summarizeReport`, five checked-in golden pairs, and two call sites moved off
`renderOperatorLegacy`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-typed-operator-renderer/06-CONTEXT.md
@.planning/phases/06-typed-operator-renderer/06-PATTERNS.md
@.planning/phases/06-typed-operator-renderer/06-01-SUMMARY.md
@internal/fieldset/fieldset.go
@cmd/engram/prune.go
</context>

## Artifacts this phase produces

See `06-01-PLAN.md` § "Artifacts this phase produces" for the full phase-level list. This plan
contributes: `reindexReport`, `summarizeReport`, and the five golden pairs named in `must_haves`.

## D-08 downstream execution contract

Added in response to `06-REVIEWS.md` HIGH-2: the first-cycle plans froze both lanes unconditionally,
which options a and b of the D-08 checkpoint necessarily violate — leaving two of three options with no
executable downstream path. All three now branch. Read the option recorded in `06-01-SUMMARY.md` and
follow exactly one row.

| Selected option | This plan's goldens | This plan's conversion-commit acceptance criterion |
|---|---|---|
| **a** — the field set is the json shape | `*.txt` frozen; `*.json` REGENERATED for the variants whose key set changes | `git diff --exit-code` clean over `cmd/engram/testdata/operator-reports/*.txt`. Every changed `*.json` is listed in this plan's SUMMARY with the dropped or added key and its D-08 rationale; a `*.json` that changed without a SUMMARY line is a defect, not an update. |
| **b** — the sentence follows the field set | `*.json` frozen; `*.txt` REGENERATED for the variants whose sentence gains text | `git diff --exit-code` clean over `cmd/engram/testdata/operator-reports/*.json`. Every changed `*.txt` is listed in the SUMMARY with its old and new bytes; D-04 must already have been amended in `06-CONTEXT.md` by 06-01. |
| **c** — bounded coverage exemption | both lanes frozen | `git diff --exit-code cmd/engram/testdata/operator-reports/` clean, and every field taking the exemption in this plan appears in the closed allowlist with its one-line rationale. |

Task 1's pre-image freeze is unaffected under all three options: the goldens are always generated from
the pre-image first, in a commit carrying no production-code change. Only Task 2's behavior branches.

**Mechanics — the branch executes through the harness, and the harness does the branching**
(`06-REVIEWS.md` cycle-3 HIGH-1 found the cycle-2 branch tables unexecutable, because the harness
compared both lanes to one golden and sourced `-update` from the legacy lane). Do not improvise a
golden-regeneration procedure here. Each fixture this plan registers declares a `Divergence`
(`divergeNone` | `divergeJSON` | `divergeText`) and, when it is not `divergeNone`, a one-line
`DivergenceReason`. `TestOperatorReportGolden` resolves a golden path PER LANE from that declaration:
the converted lane always against the canonical `<Key>.<Variant>.{txt,json}`, the legacy lane against
`<Key>.<Variant>.legacy.{txt,json}` on exactly the lane the recorded option moved.

- **Option-c:** every fixture is `divergeNone`, no `.legacy.*` file may exist, and Task 2 regenerates
  nothing at all.
- **Option-a or option-b:** for each affected variant, in this order — (1) `cp` the canonical golden of
  the MOVING lane to its `.legacy.<ext>` sibling, preserving the pre-image bytes; (2) set that fixture's
  `Divergence` and `DivergenceReason`; (3) run the package `-update` flag, which writes the canonical
  file from `Report()` and the `.legacy.*` file from the legacy lane; (4) re-run without `-update` and
  confirm both lanes pass. Never hand-edit golden bytes.

`TestOperatorReportGoldenSetIsComplete` holds the `.legacy.*` file set equal to the declared divergences
in both directions, requires the reason to be non-empty exactly when a divergence is declared, and fails
any fixture that diverges on the lane the recorded option FREEZES — so "regenerated without a named
justification is a defect" and "under option-a the text goldens are frozen" are assertions rather than
instructions to remember. The pre-image bytes survive the conversion unchanged under all three options;
under a and b they survive in the `.legacy.*` file.

**The fields this plan routes through the decision:** `summarize-missing`'s dry-run `Failed` count,
which has no textual presence today. Under option-a the `failed` key drops from
`summarize-missing.dry-run.json`; under option-b the dry-run sentence gains a failed-count clause and
`summarize-missing.dry-run.txt` is regenerated; under option-c the field takes the exemption and
neither file moves.

<tasks>

<task type="auto">
  <name>Task 1: Freeze reindex and summarize-missing goldens from the pre-image</name>
  <files>cmd/engram/reindex_test.go, cmd/engram/summarize_test.go, cmd/engram/testdata/operator-reports/</files>
  <read_first>
    - cmd/engram/operator_golden_test.go — `registerOperatorReport`'s parameter order and `TestOperatorReportGolden`'s comparison contract, both authored in 06-01
    - cmd/engram/prune_test.go — the `init()` registration 06-01 wrote, which this task imitates exactly
    - cmd/engram/reindex.go lines 80-130 — the call site, `reindexSummary`'s three branches, and `reindexOutputDoc`
    - cmd/engram/summarize.go lines 60-100 — the call site, `summarizeSummary`'s two branches, and `summarizeOutputDoc`
    - cmd/engram/operator_output_test.go lines 138-200 — `operatorParityRows`' existing hand-built reindex and summarize result values, reused verbatim as fixture inputs
  </read_first>
  <action>
Add an `init()` to `cmd/engram/reindex_test.go` registering three fixtures under key `reindex` with
variants `dry-run`, `dry-run-resume`, and `applied`, and an `init()` to
`cmd/engram/summarize_test.go` registering two fixtures under key `summarize-missing` with variants
`dry-run` and `applied`. Each registration passes the legacy sentence and the legacy doc built from ONE
shared result value per fixture, and passes nil for the `Report` argument — the fieldset.Set builders do
not exist yet.

Every registration in THIS task also passes `divergeNone` and an empty `DivergenceReason`: the
freeze commit's canonical goldens ARE the pre-image on both lanes, so nothing has diverged yet. Only
Task 2 may change a fixture's `Divergence`, and only as the `## D-08 downstream execution contract`
**Mechanics** paragraph directs.

Reuse `operatorParityRows`' existing inputs verbatim so the fixtures are anchored to values the repo
already treats as representative: `store.ReindexResult{Scanned: 57, Upserted: 23, Skipped: 11,
Unchanged: 19}` with target `target-coll` and dim 1536, and `store.SummarizeResult{Scanned: 41,
Filled: 17, Skipped: 20, Failed: 4}`. Vary only the `dryRun` and `resume` booleans across variants so
all three reindex sentences and both summarize sentences are covered. Every input must be
deterministic — no wall-clock reads.

Generate the ten golden files with the package `-update` flag, inspect them by eye against the current
CLI output, then commit them. This commit contains NO production-code change.
  </action>
  <verify>
    <automated>go test ./cmd/engram/... -count=1 -run 'TestOperatorReportGolden' -v 2>&1 | grep -E '^( *)(=== RUN|--- (PASS|FAIL))'</automated>
  </verify>
  <acceptance_criteria>
    - `ls cmd/engram/testdata/operator-reports/ | rg -c '^(reindex|summarize-missing)\.'` reports 10.
    - `go test ./cmd/engram/... -run 'TestOperatorReportGolden' -v` shows a `--- PASS` subtest line for
      each of the five new `<key>/<variant>` fixtures, not only a package-level `ok`.
    - `TestOperatorReportGoldenSetIsComplete` passes, proving the discovered golden files and the
      registered fixtures are set-equal in both directions.
    - `git diff --stat cmd/engram/reindex.go cmd/engram/summarize.go` is empty for this commit.
    - `cmd/engram/testdata/operator-reports/reindex.applied.txt` contains exactly the bytes
      `reindexSummary` produces for the fixture inputs plus one trailing newline.
  </acceptance_criteria>
  <done>All five variants of both reports are pinned to committed golden bytes produced by the pre-image code, so any behavioral drift introduced by Task 2 is a test failure rather than a judgement call.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert reindex and summarize-missing to fieldset.Set builders</name>
  <files>cmd/engram/reindex.go, cmd/engram/reindex_test.go, cmd/engram/summarize.go, cmd/engram/summarize_test.go</files>
  <read_first>
    - internal/fieldset/fieldset.go — `F`, `WithPresent`, `WithRender`, `WithSep`, and the placeholder syntax the package's parser accepts. Note there is no exported struct field on `Set` or `Field`: a report is built by calling a constructor, never by writing a composite literal.
    - cmd/engram/operator_output.go — `operatorReport(key, variant, text, fields...)` as written in 06-01. This is the ONLY constructor route a report builder may use, and both identity arguments must be written as string literals at the call site; 06-09's gate parses those literals to derive the phase's variant universe, so a computed argument fails there by `file:line`.
    - cmd/engram/prune.go — `prunePreviewReport` and `pruneAppliedReport` as written in 06-01: the exact shape to imitate, including the one-builder-per-sentence-variant rule
    - .planning/phases/06-typed-operator-renderer/06-01-SUMMARY.md — the D-08 selection, which governs `summarize-missing`'s dry-run `Failed` field
    - cmd/engram/reindex.go lines 80-130 and cmd/engram/summarize.go lines 60-100
    - .planning/phases/06-typed-operator-renderer/06-PATTERNS.md § Group A
  </read_first>
  <behavior>
    - `fieldset.Validated(reindexReport(...))` returns nil for all three variants.
    - `fieldset.Validated(summarizeReport(...))` returns nil for both variants.
    - `TestOperatorReportGolden` passes for all five fixtures with the `Report` argument now non-nil,
      proving both lanes byte-match the goldens Task 1 froze from the pre-image.
    - `TestReindexSummary`, `TestReindexTextModeUnchanged`, `TestReindexRenderOperatorJSONMode`, and
      `TestSummarizeSummary` continue to pass without edits to their assertions.
  </behavior>
  <action>
Add
`reindexReport(res store.ReindexResult, target string, dim uint64, dryRun, resume bool) fieldset.Set`
to `cmd/engram/reindex.go`, returning one of three distinct `fieldset.Set` values — one per sentence
variant — rather than one field set with mode-dependent population. RESEARCH.md's anti-pattern section
is explicit that one struct serving all variants is what makes D-03 unsatisfiable; mirror
`prunePreviewReport`/`pruneAppliedReport`'s split instead. Add
`summarizeReport(res store.SummarizeResult, dryRun bool) fieldset.Set` on the same rule, with two
variants.

Reproduce each sentence byte-identically (D-04). Where a value's prose form differs from its natural Go
form, attach `WithRender` to that field rather than editing the template's literal text — the same
technique `best_effort` uses in prune. Apply the D-08 selection from `06-01-SUMMARY.md` to
`summarize-missing`'s dry-run variant, whose `Failed` count has no textual presence today, following
this plan's `## D-08 downstream execution contract` row for the selected option; do not re-decide it
here and do not improvise a golden policy.

Construct every variant through `operatorReport`, with the command key and the variant name written as
string LITERALS in the same statement as the branch that produces the shape:
`operatorReport("reindex", "dry-run", ...)`, `operatorReport("reindex", "dry-run-resume", ...)`,
`operatorReport("reindex", "applied", ...)`, `operatorReport("summarize-missing", "dry-run", ...)`, and
`operatorReport("summarize-missing", "applied", ...)`. Do not name a `fieldset` constructor directly.
That is not bookkeeping: 06-09's gate parses these five call sites out of production source to derive
the variant universe, so this is where each variant's existence becomes a fact the gate can see.
`06-REVIEWS.md` cycle-2 MEDIUM-2 retired the previous shape, which put the declaration in a `_test.go`
file where a production-only branch addition would never touch it.

Point `reindex.go`'s single render call and `summarize.go`'s single render call at `renderOperator`
with the new builders. Leave `reindexSummary`, `reindexReportDoc`, `summarizeSummary`, and
`summarizeReportDoc` in place — `TestOperatorOutputParity` still reads them and 06-09 deletes them.

Update both `init()` registrations from Task 1 to pass the new builders as the `Report` argument, along
with each fixture's `Divergence` and, where it is not `divergeNone`, its `DivergenceReason`.

Regenerate goldens ONLY as this plan's `## D-08 downstream execution contract` directs for the option
`06-01-SUMMARY.md` recorded — nothing at all under option-c, and under option-a or option-b exactly the
four-step preserve-then-update procedure that section's **Mechanics** paragraph specifies, for exactly
the affected variants. `06-REVIEWS.md` cycle-3 HIGH-1 flagged the previous unconditional wording here
("do not regenerate any golden in this commit") as contradicting this plan's own option-a row; the
branch is now stated once, in one place, and executed by the harness. What survives untouched under
every option is the PRE-IMAGE — in the canonical golden on a frozen lane, in the `.legacy.<ext>` sibling
on a moved one — and that survival is the SC2 proof.
  </action>
  <verify>
    <automated>go test ./cmd/engram/... -count=1 -run 'TestReindex|TestSummarize|TestOperatorReportGolden|TestOperatorOutputParity|TestOperatorOutputEmpty' -v 2>&1 | grep -E '^( *)(=== RUN|--- (PASS|FAIL)|FAIL|ok)'</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd/engram/... -list 'TestReindex.*|TestSummarize.*'` lists
      `TestReindexSummary`, `TestReindexReportDocCarriesEverySummaryFact`,
      `TestReindexRenderOperatorJSONMode`, `TestReindexTextModeUnchanged`,
      `TestReindexRejectsInvalidOutput`, `TestSummarizeSummary`,
      `TestSummarizeReportDocSeparatesCounts`, and `TestSummarizeMissingRejectsInvalidOutput`, and the
      `-v` run shows `--- PASS` for each.
    - The golden-diff criterion is the one this plan's `## D-08 downstream execution contract` names
      for the selected option — clean over the whole directory under option-c, clean over `*.txt` under
      option-a, clean over `*.json` under option-b — and under a or b every changed file has its
      justification line in the SUMMARY.
    - `rg -n 'renderOperatorLegacy\(' cmd/engram/reindex.go cmd/engram/summarize.go` returns no match.
    - `rg -n 'func reindexReport\(|func summarizeReport\(' cmd/engram/` returns exactly one match each.
    - `rg -n 'operatorReport\("reindex"' cmd/engram/reindex.go` returns exactly three matches and
      `rg -n 'operatorReport\("summarize-missing"' cmd/engram/summarize.go` exactly two, each with a
      string-literal variant name matching a registered fixture, and
      `rg -n 'fieldset\.(New|MustNew|NewLabeled|MustNewLabeled)\(' cmd/engram/reindex.go cmd/engram/summarize.go`
      returns no match — every construction routes through the helper the 06-09 derivation parses.
    - `TestOperatorOutputParity` still shows `--- PASS`, and
      `git diff --stat cmd/engram/operator_output_test.go` is empty for this commit.
    - `task lint` and `task test` are green.
  </acceptance_criteria>
  <done>Both reports render both lanes from one field set per sentence variant, with the pre-image bytes still under test on the legacy lane — unchanged in the canonical golden, or preserved in the `.legacy.<ext>` sibling on whichever lane the recorded D-08 option moved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (no new boundary) | Conversion of two already-authorized, already-computed operator reports. No new input, persisted data, authz path, or dependency. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| 06-02:T-06-PARSE | Information Disclosure | `reindexReport`, `summarizeReport` | medium | mitigate | A converted report whose json states more than its sentence is the phase's one security-adjacent property. Mitigated by `fieldset.New`'s validation (shared parser, set equality) plus the frozen goldens, which turn any lane divergence into a byte diff. Inherits 06-01:T-06-PARSE's red-evidence proof. |
| 06-02:T-06-CONTENT | Information Disclosure | `reindexReport`, `summarizeReport` | low | mitigate | Record content stays unreachable: every field hand-declared, no `internal/store` type passed through; the goldens would show any leak as changed bytes. |
| 06-02:T-06-NONE | (no applicable STRIDE category) | plan scope | low | accept | RESEARCH.md's ASVS review found no conventional threat applies to this internal rendering refactor. Recorded, not fabricated. |

**Package legitimacy:** no install task in this plan; `go.mod` and `go.sum` are untouched.
</threat_model>

<verification>
- `task lint` and `task test` green.
- `go test ./cmd/engram/... -count=1` green with `TestOperatorOutputParity` unmodified and passing.
- `git diff --exit-code cmd/engram/testdata/operator-reports/` clean after the conversion commit.
</verification>

<success_criteria>
- `reindex` and `summarize-missing` render from `fieldset.Set` builders through the new `renderOperator`.
- Five golden pairs, frozen from the pre-image, survive the conversion byte-for-byte — in the
  canonical golden on every lane the recorded D-08 option left frozen, and in the `.legacy.<ext>`
  pre-image sibling on any lane it moved.
- No sentence byte moved; no legacy summary or doc function deleted.
</success_criteria>

<output>
Create `.planning/phases/06-typed-operator-renderer/06-02-SUMMARY.md` when done, naming the five
fixture keys, enumerating this plan's per-command variant inventory by name (06-09's pair gate
reconciles against it), stating how the D-08 selection resolved `summarize-missing`'s dry-run `Failed`
field, and — under option-a or option-b — listing every regenerated golden with its per-key or
per-sentence justification.
</output>
