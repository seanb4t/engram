---
phase: 06-typed-operator-renderer
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - cmd/engram/reindex.go
  - cmd/engram/reindex_test.go
  - cmd/engram/summarize.go
  - cmd/engram/summarize_test.go
  - cmd/engram/testdata/operator-reports/
autonomous: true
requirements: [REQ-operator-renderer-typed]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "engram reindex renders all three sentence variants (dry-run, dry-run with resume, applied) from three FieldSet builders, byte-identical to their pre-image on both lanes (D-04)."
    - "engram summarize-missing renders both sentence variants (dry-run, live) from two FieldSet builders, byte-identical to their pre-image on both lanes (D-04)."
    - "Every field of every converted variant is referenced by exactly one placeholder in that variant's template, proven by validateFieldSet passing on each builder's output (D-03)."
    - "summarize-missing's dry-run variant, whose Failed count has no textual presence today, is handled by the D-08 selection recorded in 06-01-SUMMARY.md rather than by a fresh judgement call."
    - "Goldens for all five variants are generated from the pre-image builders BEFORE conversion and are unchanged by the conversion commit."
  artifacts:
    - cmd/engram/testdata/operator-reports/reindex.dry-run.txt
    - cmd/engram/testdata/operator-reports/reindex.dry-run-resume.txt
    - cmd/engram/testdata/operator-reports/reindex.applied.txt
    - cmd/engram/testdata/operator-reports/summarize-missing.dry-run.txt
    - cmd/engram/testdata/operator-reports/summarize-missing.applied.txt
  key_links:
    - "reindexReport / summarizeReport -> renderOperator(FieldSet) -> TestOperatorReportGolden -> the frozen goldens"
  prohibitions:
    - "Do not delete reindexSummary, reindexReportDoc, summarizeSummary, or summarizeReportDoc in this plan; TestOperatorOutputParity still reads them and the goldens were generated from them."
    - "Do not modify or delete TestOperatorOutputParity or operatorParityRows."
    - "Do not edit cmd/engram/operator_golden_test.go; fixture registration belongs in each report's own test file so parallel conversion plans never collide."
    - "Do not regenerate any golden file in the conversion commit; the goldens are the pre-image and must survive the conversion untouched."
    - "Do not change any operator-facing sentence byte (D-04)."
    - "Do not introduce reflection-based dispatch or a templating-engine import."
---

<objective>
Convert the two flat, single-shape Group A operator reports — `reindex` and `summarize-missing` — to
`FieldSet` builders, gated by goldens generated from their pre-image.

Purpose: these are the mechanical repetitions of the prune conversion the 06-01 tracer proved. They
carry no nesting and no conditional presence, so they exercise the multi-variant path only.

Output: `reindexReport`, `summarizeReport`, five checked-in golden pairs, and two call sites moved off
`renderOperatorLegacy`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-typed-operator-renderer/06-CONTEXT.md
@.planning/phases/06-typed-operator-renderer/06-PATTERNS.md
@.planning/phases/06-typed-operator-renderer/06-01-SUMMARY.md
@cmd/engram/fieldset.go
@cmd/engram/prune.go
</context>

## Artifacts this phase produces

See `06-01-PLAN.md` § "Artifacts this phase produces" for the full phase-level list. This plan
contributes: `reindexReport`, `summarizeReport`, and the five golden pairs named in `must_haves`.

<tasks>

<task type="auto">
  <name>Task 1: Freeze reindex and summarize-missing goldens from the pre-image</name>
  <files>cmd/engram/reindex_test.go, cmd/engram/summarize_test.go, cmd/engram/testdata/operator-reports/</files>
  <read_first>
    - cmd/engram/operator_golden_test.go — `registerOperatorReport`'s parameter order and `TestOperatorReportGolden`'s comparison contract, both authored in 06-01
    - cmd/engram/prune_test.go — the `init()` registration 06-01 wrote, which this task imitates exactly
    - cmd/engram/reindex.go lines 80-130 — the call site, `reindexSummary`'s three branches, and `reindexOutputDoc`
    - cmd/engram/summarize.go lines 60-100 — the call site, `summarizeSummary`'s two branches, and `summarizeOutputDoc`
    - cmd/engram/operator_output_test.go lines 138-200 — `operatorParityRows`' existing hand-built reindex and summarize result values, reused verbatim as fixture inputs
  </read_first>
  <action>
Add an `init()` to `cmd/engram/reindex_test.go` registering three fixtures under key `reindex` with
variants `dry-run`, `dry-run-resume`, and `applied`, and an `init()` to
`cmd/engram/summarize_test.go` registering two fixtures under key `summarize-missing` with variants
`dry-run` and `applied`. Each registration passes the legacy sentence and the legacy doc built from ONE
shared result value per fixture, and passes nil for the `Report` argument — the FieldSet builders do
not exist yet.

Reuse `operatorParityRows`' existing inputs verbatim so the fixtures are anchored to values the repo
already treats as representative: `store.ReindexResult{Scanned: 57, Upserted: 23, Skipped: 11,
Unchanged: 19}` with target `target-coll` and dim 1536, and `store.SummarizeResult{Scanned: 41,
Filled: 17, Skipped: 20, Failed: 4}`. Vary only the `dryRun` and `resume` booleans across variants so
all three reindex sentences and both summarize sentences are covered. Every input must be
deterministic — no wall-clock reads.

Generate the ten golden files with the package `-update` flag, inspect them by eye against the current
CLI output, then commit them. This commit contains NO production-code change.
  </action>
  <verify>
    <automated>go test ./cmd/engram/... -count=1 -run 'TestOperatorReportGolden' -v 2>&1 | grep -E '^( *)(=== RUN|--- (PASS|FAIL))'</automated>
  </verify>
  <acceptance_criteria>
    - `ls cmd/engram/testdata/operator-reports/ | rg -c '^(reindex|summarize-missing)\.'` reports 10.
    - `go test ./cmd/engram/... -run 'TestOperatorReportGolden' -v` shows a `--- PASS` subtest line for
      each of the five new `<key>/<variant>` fixtures, not only a package-level `ok`.
    - `TestOperatorReportGoldenSetIsComplete` passes, proving the discovered golden files and the
      registered fixtures are set-equal in both directions.
    - `git diff --stat cmd/engram/reindex.go cmd/engram/summarize.go` is empty for this commit.
    - `cmd/engram/testdata/operator-reports/reindex.applied.txt` contains exactly the bytes
      `reindexSummary` produces for the fixture inputs plus one trailing newline.
  </acceptance_criteria>
  <done>All five variants of both reports are pinned to committed golden bytes produced by the pre-image code, so any behavioral drift introduced by Task 2 is a test failure rather than a judgement call.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert reindex and summarize-missing to FieldSet builders</name>
  <files>cmd/engram/reindex.go, cmd/engram/reindex_test.go, cmd/engram/summarize.go, cmd/engram/summarize_test.go</files>
  <read_first>
    - cmd/engram/fieldset.go — `Field`, `FieldSet`, `present`, `validateFieldSet`, and the placeholder syntax `parseFieldSetText` accepts
    - cmd/engram/prune.go — `prunePreviewReport` and `pruneAppliedReport` as written in 06-01: the exact shape to imitate, including the one-builder-per-sentence-variant rule
    - .planning/phases/06-typed-operator-renderer/06-01-SUMMARY.md — the D-08 selection, which governs `summarize-missing`'s dry-run `Failed` field
    - cmd/engram/reindex.go lines 80-130 and cmd/engram/summarize.go lines 60-100
    - .planning/phases/06-typed-operator-renderer/06-PATTERNS.md § Group A
  </read_first>
  <behavior>
    - `validateFieldSet(reindexReport(...))` returns nil for all three variants.
    - `validateFieldSet(summarizeReport(...))` returns nil for both variants.
    - `TestOperatorReportGolden` passes for all five fixtures with the `Report` argument now non-nil,
      proving both lanes byte-match the goldens Task 1 froze from the pre-image.
    - `TestReindexSummary`, `TestReindexTextModeUnchanged`, `TestReindexRenderOperatorJSONMode`, and
      `TestSummarizeSummary` continue to pass without edits to their assertions.
  </behavior>
  <action>
Add `reindexReport(res store.ReindexResult, target string, dim uint64, dryRun, resume bool) FieldSet`
to `cmd/engram/reindex.go`, returning one of three distinct `FieldSet` values — one per sentence
variant — rather than one field set with mode-dependent population. RESEARCH.md's anti-pattern section
is explicit that one struct serving all variants is what makes D-03 unsatisfiable; mirror
`prunePreviewReport`/`pruneAppliedReport`'s split instead. Add
`summarizeReport(res store.SummarizeResult, dryRun bool) FieldSet` on the same rule, with two variants.

Reproduce each sentence byte-identically (D-04). Where a value's prose form differs from its natural Go
form, attach a `Render` function to that field rather than editing the template's literal text — the
same technique `best_effort` uses in prune. Apply the D-08 selection from `06-01-SUMMARY.md` to
`summarize-missing`'s dry-run variant, whose `Failed` count has no textual presence today; do not
re-decide it here.

Point `reindex.go`'s single render call and `summarize.go`'s single render call at `renderOperator`
with the new builders. Leave `reindexSummary`, `reindexReportDoc`, `summarizeSummary`, and
`summarizeReportDoc` in place — `TestOperatorOutputParity` still reads them and 06-09 deletes them.

Update both `init()` registrations from Task 1 to pass the new builders as the `Report` argument. Do
not regenerate any golden in this commit: the goldens are the pre-image, and their surviving the
conversion untouched is the whole SC2 proof.
  </action>
  <verify>
    <automated>go test ./cmd/engram/... -count=1 -run 'TestReindex|TestSummarize|TestOperatorReportGolden|TestOperatorOutputParity|TestOperatorOutputEmpty' -v 2>&1 | grep -E '^( *)(=== RUN|--- (PASS|FAIL)|FAIL|ok)'</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd/engram/... -list 'TestReindex.*|TestSummarize.*'` lists
      `TestReindexSummary`, `TestReindexReportDocCarriesEverySummaryFact`,
      `TestReindexRenderOperatorJSONMode`, `TestReindexTextModeUnchanged`,
      `TestReindexRejectsInvalidOutput`, `TestSummarizeSummary`,
      `TestSummarizeReportDocSeparatesCounts`, and `TestSummarizeMissingRejectsInvalidOutput`, and the
      `-v` run shows `--- PASS` for each.
    - `git diff --exit-code cmd/engram/testdata/operator-reports/` is clean for this commit.
    - `rg -n 'renderOperatorLegacy\(' cmd/engram/reindex.go cmd/engram/summarize.go` returns no match.
    - `rg -n 'func reindexReport\(|func summarizeReport\(' cmd/engram/` returns exactly one match each.
    - `TestOperatorOutputParity` still shows `--- PASS`, and
      `git diff --stat cmd/engram/operator_output_test.go` is empty for this commit.
    - `task lint` and `task test` are green.
  </acceptance_criteria>
  <done>Both reports render both lanes from one field set per sentence variant, with the pre-image goldens unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (no new boundary) | Conversion of two already-authorized, already-computed operator reports. No new input, persisted data, authz path, or dependency. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| 06-02:T-06-PARSE | Information Disclosure | `reindexReport`, `summarizeReport` | medium | mitigate | A converted report whose json states more than its sentence is the phase's one security-adjacent property. Mitigated by `validateFieldSet` (shared parser, set equality) plus the frozen goldens, which turn any lane divergence into a byte diff. Inherits 06-01:T-06-PARSE's red-evidence proof. |
| 06-02:T-06-CONTENT | Information Disclosure | `reindexReport`, `summarizeReport` | low | mitigate | Record content stays unreachable: every field hand-declared, no `internal/store` type passed through; the goldens would show any leak as changed bytes. |
| 06-02:T-06-NONE | (no applicable STRIDE category) | plan scope | low | accept | RESEARCH.md's ASVS review found no conventional threat applies to this internal rendering refactor. Recorded, not fabricated. |

**Package legitimacy:** no install task in this plan; `go.mod` and `go.sum` are untouched.
</threat_model>

<verification>
- `task lint` and `task test` green.
- `go test ./cmd/engram/... -count=1` green with `TestOperatorOutputParity` unmodified and passing.
- `git diff --exit-code cmd/engram/testdata/operator-reports/` clean after the conversion commit.
</verification>

<success_criteria>
- `reindex` and `summarize-missing` render from `FieldSet` builders through the new `renderOperator`.
- Five golden pairs, frozen from the pre-image, survive the conversion byte-for-byte.
- No sentence byte moved; no legacy summary or doc function deleted.
</success_criteria>

<output>
Create `.planning/phases/06-typed-operator-renderer/06-02-SUMMARY.md` when done, naming the five
fixture keys and stating how the D-08 selection resolved `summarize-missing`'s dry-run `Failed` field.
</output>
