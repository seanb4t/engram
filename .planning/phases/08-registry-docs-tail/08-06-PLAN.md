---
phase: 08-registry-docs-tail
plan: 06
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - internal/surfaces/rules.go
  - cmd/engram/sweep_scope_test.go
autonomous: true
requirements: [REQ-sweep-scope-rule-registered]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`internal/surfaces/rules.go`'s doc comment for `RuleSweepScopeOrAllScopesRequired` describes only enforcement that actually exists. The retired present-tense error-envelope claim is gone (zero occurrences), and the replacement's two factual assertions are each gated against the code they describe: `internal/surfaces.RuleSweepScopeOrAllScopesRequired` is referenced ZERO times in non-test `internal/server/` (where every `conditionalErrf` call site sources its rule via `surfaces.RuleByID`), and `cmd/engram/sweep_scope.go` raises exactly one `usageErrorf`. The zero clause is proven non-vacuous by a control: the same reference shape for `RuleScopeRequiredUnlessCrossSpine`, a rule that DOES have an envelope-bearing lane, is greater than zero (repairs 08-VERIFICATION.md truth 5, corroborated by 08-REVIEW.md WR-02)."
    - "A durable gate asserts ZERO commands in the live cobra tree expose both `--scope` and `--all-scopes` without being classified as either enforcing or non-enforcing. It walks `walkCommands(rootCmd, commandWalkSkip)` and compares the derived set against the classification sets in BOTH directions via the package's existing `setDiff` — so an unclassified new leaf fails as `extra` and a stale classification naming a command no longer in the tree fails as `missing`. It runs inside `go test ./...`, which is what `task test:go` and CI already execute; no new Taskfile target or CI step is introduced (repairs 08-VERIFICATION.md truth 2 and 08-01's own must_have truth #2 wording, corroborated by 08-REVIEW.md WR-03)."
    - "The classification sets are declared ONCE at package level and every sweep-scope test reads them, so classifying a newly-appeared leaf as enforcing automatically extends the rejection assertion, the present-but-empty assertion, and the Usage assertion to it. Before this plan the enumeration lived in three independent literal lists, so a leaf added to one was not covered by the others — the residual hole a classification-only gate would have left open."
    - "The gate has been OBSERVED failing, not merely observed passing. A deliberately constructed defect — a throwaway cobra command exposing both flags with its own inline hand-rolled guard, registered into the live tree — makes `TestNoHandRolledSweepScopeGuards` fail, and the failing output is recorded verbatim in the SUMMARY. The proof script creates the defect, captures the RED, removes the defect, and re-proves GREEN in one run, so it is repeatable rather than a one-time anecdote."
    - "Every `go test` invocation in this plan's gates asserts the `=== RUN` line for the test it names. `go test -run <NonexistentName>` exits 0 with `[no tests to run]`, so a gate that checks only the exit code passes against a test that was renamed out of existence — the exact vacuity class this phase is closing."
    - "Runtime behavior is unchanged. `internal/surfaces/rules.go` changes only inside a `//` doc comment: `Sentence`, `Fields`, `SurfaceFields`, `Hint`, `TagForm`, and the rule's position in the `rules` slice are byte-identical, so no anchored region body, golden, or generated artifact moves. Proven by running the CI `surfaces` job's own regeneration sequence and requiring `git diff --exit-code` over its exact path set to be clean. `cmd/engram/sweep_scope_test.go` is a test file; no production code path is touched."
    - statement: "The rewritten rule comment reads as a contributor-facing explanation of why `Fields` and `Hint` are declared for a lane that does not exist yet, rather than as an apology or a changelog entry — the next contributor wiring an MCP or Connect lane for a sweep verb should finish it knowing exactly what is and is not live. Verified by reading the whole comment block, not by an assertion."
      verification: backstop
  artifacts:
    - path: "internal/surfaces/rules.go"
      provides: "A registry declaration whose doc comment claims only the enforcement the tree actually contains"
      contains: "RuleSweepScopeOrAllScopesRequired"
    - path: "cmd/engram/sweep_scope_test.go"
      provides: "The single package-level classification of every command exposing the sweep flag pair, plus the tree-walking both-directions gate that makes an unclassified leaf a test failure"
      contains: "TestNoHandRolledSweepScopeGuards"
  key_links:
    - from: "cmd/engram/sweep_scope_test.go"
      to: "internal/surfaces/rules.go"
      via: "every sweep-scope test resolves the constraint through the registry rather than an inlined literal, so a test cannot pass while a leaf disagrees with the declared rule"
      pattern: "surfaces[.]RuleByID[(]surfaces[.]RuleSweepScopeOrAllScopesRequired[)]"
    - from: "cmd/engram/sweep_scope_test.go"
      to: "cmd/engram/cmdwalk.go"
      via: "the durable gate derives its command set from the shared live-tree walker, never from a hand-listed set, so a leaf added anywhere in the tree is in scope by construction"
      pattern: "walkCommands[(]rootCmd, commandWalkSkip[)]"
---

<objective>
Close the two remaining Phase 8 gaps that both sit on `RuleSweepScopeOrAllScopesRequired`: the
registry doc comment asserts an error-envelope attribution that no deployed surface produces
(08-VERIFICATION.md truth 5 / 08-REVIEW.md WR-02), and the phase's own "zero hand-rolled
occurrences" claim is enforced by a one-time manual check rather than anything `task` or CI re-runs
(08-VERIFICATION.md truth 2 / 08-REVIEW.md WR-03).

Purpose: `internal/surfaces/rules.go` is the single declared source of truth every other surface
composes from, so a false present-tense claim there is believed rather than checked. And this repo's
#1 recurring defect across three consecutive phases is the vacuous gate — a green that proves
nothing. 08-01's own must_have truth #2 said the gate "requires zero"; it shipped as a manual `rg`
run recorded in a SUMMARY. This plan makes it a test.

Output: a corrected registry comment whose claims are each gated against the code, and a
tree-walking both-directions classification gate that has been observed RED against a deliberately
constructed defect before being accepted.

**On tracer-first:** this plan has no tracer task and deliberately so. A tracer proves an
architecture end-to-end through the layers a phase touches; this plan adds no layer, no call path,
and no runtime behavior — it corrects a comment and adds a test. The value tracer-first buys
(discovering the design is wrong on the agent's best early-context tokens) is bought here instead by
Task 3, which forces the gate to be seen failing before the plan is allowed to close.

**On the `SurfaceFields` unit-level negative proof (08-REVIEW.md WR-04):** out of scope. It is a
warning in the review and explicitly NOT a failed truth in 08-VERIFICATION.md (truth 3 is VERIFIED).
Do not add it here.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-registry-docs-tail/08-VERIFICATION.md
@.planning/phases/08-registry-docs-tail/08-CONTEXT.md
@.planning/phases/08-registry-docs-tail/08-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make the rule's doc comment claim only the enforcement that exists</name>
  <files>internal/surfaces/rules.go</files>
  <read_first>
    - `internal/surfaces/rules.go` lines 160-205 — the whole `RuleSweepScopeOrAllScopesRequired` doc
      comment block and its `const`. Only the FIRST paragraph's closing clause is wrong; the
      `SurfaceFields` narrowing paragraph, the derived-prose-surface paragraph, and the `TagForm`
      paragraph are all accurate and stay.
    - `internal/surfaces/rules.go` around the `RuleDestructiveRequiresApply` declaration — the
      in-file precedent for a rule that states its own no-envelope case rather than inheriting the
      generic `Fields` prose.
    - `cmd/engram/sweep_scope.go` — the whole file. It is 32 lines and is the rule's sole
      enforcement site: `requireSweepScope` returns `usageErrorf("%s", sweepScopeRule().Sentence)`,
      a bare usage error with no `field=` / `hint=` envelope.
    - `internal/server/conditionalerr.go` — `conditionalErrf(class argClass, rule surfaces.ConditionalRule) error`,
      the single construction site that turns a rule into a `field=`/`hint=` envelope, and the only
      thing that could make `Fields` and `Hint` operative for this rule.
    - `internal/server/tools.go` around lines 604-640 and 1577-1602, and `internal/server/connectapi.go`
      around line 258 — how a lane actually resolves a rule: `rule, _ := surfaces.RuleByID(surfaces.RuleXxx)`
      immediately before `conditionalErrf(class, rule)`. This is why "zero references to this rule's
      const in non-test `internal/server/`" is the correct, non-vacuous way to state that no
      envelope-bearing lane exists for it.
    - `.planning/phases/08-registry-docs-tail/08-VERIFICATION.md` — the `gaps:` entry for truth 5,
      whose `missing[]` item is the acceptance wording.
  </read_first>
  <action>
    Repairs 08-VERIFICATION.md truth 5 (gap 2). The rule itself is correct as registered under D-01
    and D-02; only its self-description is wrong.

    In the first paragraph of the doc comment (`rules.go` lines 164-168, the block ending just before
    the blank comment line that introduces the `SurfaceFields` paragraph), the sentence that begins
    `Fields is the flag pair alone` currently closes with a present-tense claim that the field set
    drives error-envelope attribution. That is inherited boilerplate from the generic `Fields`
    field-doc on the `ConditionalRule` type, restated here as if operationally true for this rule.
    It is not: this rule is CLI-only, `internal/server` never resolves it, and the sole enforcement
    site raises a bare `usageErrorf`.

    Replace that closing claim with a conditional statement of the same facts. The replacement must
    assert, in this order:

    - `Fields` is the flag pair alone, `["scope", "all-scopes"]` (keep this half — it is true).
    - This rule is CLI-only. Its sole enforcement site is `cmd/engram`'s `requireSweepScope`, which
      raises a bare `usageErrorf`, so nothing carries a `field=` / `hint=` envelope for it. Name
      `requireSweepScope` and `usageErrorf` explicitly — the reader needs the file to look at.
    - There is no `conditionalErrf` call site for this rule anywhere in the tree; `internal/server`
      does not reference this rule's const at all.
    - `Fields` WOULD drive `field=scope` attribution, and `Hint`'s `conditional_required` value
      WOULD become live, only if a future MCP or Connect lane raised this rule through
      `internal/server.conditionalErrf`. Both are declared for that future lane, not for a live
      surface.

    Write it in the file's own register: dense, factual, `--`-joined clauses, ASCII only, and
    wrapped to the surrounding comment's column width. Do not add a date, a phase number, an issue
    number beyond the `(issue #480)` already present, or any "was previously"/"used to say" framing.
    The comment states the current contract; it is not a changelog and not an apology.

    Two hard constraints:

    1. **Do not reproduce the retired sentence anywhere in Go source** — not in the new comment, not
       in a `// NOTE:` beside it, not in a test. A gate below asserts zero occurrences of it in this
       file; quoting it to explain its removal would make that gate self-invalidating. The word
       `drive` in the conditional form ("WOULD drive") is fine and expected; the retired present-tense
       form is what must not return.
    2. **Change nothing outside the `//` comment.** `Sentence`, `Fields`, `SurfaceFields`, `Hint`,
       `TagForm`, `declared`, the const's value, and the literal's position at the tail of the `rules`
       slice are all untouched. Declaration order is emission order for `internal/surfacesgen`, and
       `Sentence` is compared by plain byte equality across five surfaces.

    **Does `task surfaces:gen` need to re-run?** Not for correctness — a Go doc comment is not an
    input to any generated artifact. But run it anyway, as proof rather than assumption, and require
    the tree to stay clean. Use `task surfaces:gen`, never a bare `go run ./internal/surfacesgen`:
    the Taskfile target chains `proto:gen` and the golden `-update` step, and CI's `surfaces` job runs
    that same sequence. `internal/surfacesgen`'s `ruleTargets` map and
    `internal/surfaces/normalize_test.go`'s synthetic fixture are both hand-maintained, but neither
    keys on a doc comment, so neither needs an edit here — the clean diff is what proves that.
  </action>
  <verify>
    <automated>test "$(rg -o -F -- 'it drives field=scope attribution' internal/surfaces/rules.go | wc -l | tr -d ' ')" = "0"</automated>
    <automated>set -e; lanes=$(rg -o 'surfaces[.]RuleSweepScopeOrAllScopesRequired' internal/server/ -g '!*_test.go' | wc -l | tr -d ' '); control=$(rg -o 'surfaces[.]RuleScopeRequiredUnlessCrossSpine' internal/server/ -g '!*_test.go' | wc -l | tr -d ' '); printf 'this rule in internal/server: %s (want 0) / control rule: %s (want >0)\n' "$lanes" "$control"; test "$lanes" = "0"; test "$control" -gt 0</automated>
    <automated>test "$(rg -v '^\s*//' cmd/engram/sweep_scope.go | rg -o 'usageErrorf[(]' | wc -l | tr -d ' ')" = "1"</automated>
    <automated>set -e; git diff --stat -- internal/surfaces/rules.go; noncomment=$(git diff -U0 -- internal/surfaces/rules.go | rg -N '^[+-]' | rg -v '^[+][+][+]|^[-][-][-]' | rg -v '^[+-]//' | wc -l | tr -d ' '); printf 'non-comment changed lines: %s (want 0)\n' "$noncomment"; test "$noncomment" = "0"</automated>
    <automated>set -e; task surfaces:gen; git diff --exit-code -- proto/ docs-site/ skill/ gen/ ui/src/lib/gen/ cmd/engram/testdata/</automated>
    <automated>go test ./internal/surfaces/... -count=1</automated>
  </verify>
  <done>
    - The retired present-tense claim has zero occurrences in `internal/surfaces/rules.go`. This gate
      is RED against `HEAD` (count `1`) before the edit — record the observed pre-state in the SUMMARY.
    - The lane gate prints `this rule in internal/server: 0` and a control greater than zero. The
      control clause is what stops this from being a gate that would pass against a typo'd rule name:
      it proves the same reference shape is non-zero for a rule that genuinely has an envelope lane.
    - `cmd/engram/sweep_scope.go` raises exactly one `usageErrorf`, comment lines stripped — the
      "sole enforcement site" half of the new comment's claim.
    - The fourth gate reports zero non-comment changed lines in `rules.go`: every `+`/`-` line in the
      diff is a `//` comment line, so no registry value moved.
    - `task surfaces:gen` leaves CI's exact path set clean under `git diff --exit-code`, proving no
      anchored region, golden, or generated artifact depended on the comment.
    - `go test ./internal/surfaces/...` passes, including `validateRuleSet`'s ASCII, substring-collision,
      and non-empty-field constraints over the unchanged registry.
  </done>
</task>

<task type="auto">
  <name>Task 2: Make the sweep-leaf classification single-source and gate the tree against it in both directions</name>
  <files>cmd/engram/sweep_scope_test.go</files>
  <read_first>
    - `cmd/engram/sweep_scope_test.go` — the whole file (4 tests, ~165 lines). Note that
      `TestSweepLeavesRejectMissingScopeIdentically`, `TestSweepLeavesRejectPresentButEmptyScope`,
      and `TestSweepLeavesUsageStatesRegisteredRule` each carry their OWN independent literal
      enumeration of the same leaves. That triplication is the residual hole this task closes.
    - `cmd/engram/cmdwalk.go` — `walkCommands(from *cobra.Command, skip func(*cobra.Command) bool) []*cobra.Command`
      (depth-first, lexicographic, skips a Hidden/help/completion command AND its whole subtree),
      `commandWalkSkip`, and `commandKey` (returns `"spine-review scan"` / `"summarize-missing"`,
      i.e. the qualified path with the binary name trimmed). Read `operatorCommands()` in the same
      file as the in-repo template for "derive a set from the live tree, never from a literal list".
    - `cmd/engram/operator_output_test.go` around lines 183-202 — `setDiff(want, got map[string]bool) (missing, extra []string)`,
      already in this package and already sorted. Use it; do not write a second one.
    - `cmd/engram/catalog_test.go` around lines 56-64 — `sortedKeys(m map[string]bool) []string`,
      already in this package. Use it for deterministic subtest ordering.
    - `cmd/engram/cmdwalk_test.go` around lines 140-148 — `commandKeySet([]*cobra.Command) map[string]bool`,
      and `TestOperatorCommands` just below it, the both-directions set-equality idiom to mirror.
    - `.planning/phases/08-registry-docs-tail/08-VERIFICATION.md` — the `gaps:` entry for truth 2,
      whose `missing[]` item is the acceptance wording.
  </read_first>
  <action>
    Repairs 08-VERIFICATION.md truth 2 (gap 4) and delivers 08-01's own must_have truth #2 as an
    executable gate rather than a SUMMARY-recorded manual `rg` run. Implements D-01's planner note:
    "the acceptance gate should assert ZERO remaining ... not 'three converted', so that a
    newly-added fourth site fails the gate instead of slipping past a hardcoded number."

    All work is inside `cmd/engram/sweep_scope_test.go`. Four changes:

    **(a) Lift the classification to package level.** Replace the `enforcing` and `nonEnforcing`
    local maps currently declared inside `TestSweepLeavesUsageStatesRegisteredRule` with two
    package-level `map[string]bool` vars, `enforcingSweepLeaves` and `nonEnforcingSweepLeaves`, keyed
    by `commandKey` form. Their membership is unchanged from what the file declares today:
    enforcing is `spine-review scan`, `spine-review verify`, `summarize-missing`; non-enforcing is
    `spine-review consolidate`, `spine-review purge`. Give each a doc comment stating what
    membership MEANS and what a future contributor must do when the gate below reports a new key —
    for the enforcing set, that the leaf routes through `requireSweepScope`; for the non-enforcing
    set, WHY the leaf exposes the flag pair without enforcing (the reasoning is already written in
    `internal/surfaces/rules.go`'s `SurfaceFields` paragraph — reference it, do not restate it).
    Neither name collides with anything in the package today.

    **(b) Derive the live set.** Add an unexported helper, `sweepScopeFlagPairCommands() map[string]bool`,
    that walks `walkCommands(rootCmd, commandWalkSkip)`, keeps every command whose own flag set
    resolves both `Flags().Lookup("scope")` and `Flags().Lookup("all-scopes")` to non-nil, and returns
    their `commandKey`s as a set. Mirror `operatorCommands()`'s structure and its "reads the LIVE
    flag set, so nothing needs maintaining here" framing. Use `Flags().Lookup`, matching the lookup
    form `TestSweepLeavesUsageStatesRegisteredRule` and `operatorCommands` already use in this
    package — do not substitute `LocalFlags()` or `InheritedFlags()`.

    **(c) Add the gate.** Add `TestNoHandRolledSweepScopeGuards`. It must:

    - Fail immediately if `enforcingSweepLeaves` and `nonEnforcingSweepLeaves` share any key — a
      command cannot be both, and an overlap would let the union hide a misclassification.
    - Build `want` as the union of the two sets and `got` as `sweepScopeFlagPairCommands()`.
    - Fail immediately if `got` is empty. An empty walk would satisfy the set comparison vacuously
      if `want` were ever emptied too; this is the same non-vacuity guard `internal/keylinks`'
      `assertScannedSomething` exists for.
    - Call `setDiff(want, got)` and report both directions with distinct, actionable messages.
      `extra` (a live command exposing both flags in neither set) is the regression this gate exists
      for: the message must tell the contributor to classify it — route it through `requireSweepScope`
      and add it to `enforcingSweepLeaves`, or record why it is exempt in `nonEnforcingSweepLeaves`.
      `missing` (a classified key not in the tree) is a stale entry: the message must tell them to
      remove it.
    - Carry a doc comment stating plainly that this is the ZERO assertion — zero unclassified, zero
      stale — and that the classification sets it reads are what extend every other test in this
      file to a newly-appeared leaf.

    **(d) Wire the three existing tests to the shared sets**, so classifying a leaf as enforcing
    automatically extends coverage to it instead of requiring three separate hand-edits:

    - `TestSweepLeavesUsageStatesRegisteredRule`: read the package-level vars instead of its own
      locals. Its logic is otherwise unchanged and its assertions stay exactly as they are.
    - `TestSweepLeavesRejectMissingScopeIdentically`: replace the literal `cases` slice with an
      iteration over `sortedKeys(enforcingSweepLeaves)`, deriving argv as `strings.Fields(key)`
      (`"spine-review scan"` becomes `{"spine-review", "scan"}`; `"summarize-missing"` becomes
      `{"summarize-missing"}`) and using the key as the subtest name. Keep every assertion it makes
      today, including the `err.Error() != rule.Sentence` comparison against the registry-resolved
      Sentence.
    - `TestSweepLeavesRejectPresentButEmptyScope`: the same conversion, with argv derived as
      `strings.Fields(key)` followed by `"--scope"` and `""`.
    - Both converted tests must fail fast if `enforcingSweepLeaves` is empty, so emptying the set
      cannot turn either into a zero-iteration pass.

    `TestRequireSweepScope` is a direct table test of the helper with no command invocation; leave it
    exactly as it is.

    **Do NOT add a committed source-level grep for the removed guard literal.** 08-REVIEW.md WR-03
    suggests pairing the tree gate with one; decline it, and say so in the SUMMARY with this reason:
    a gate in `cmd/engram` that greps `cmd/engram` for a literal must itself contain that literal,
    so the gate matches its own source and is green-by-construction — the precise vacuity shape this
    phase exists to close. The behavioral coverage is strictly stronger and has no such hole: an
    EXISTING leaf regressing to any hand-rolled string fails
    `TestSweepLeavesRejectMissingScopeIdentically` (which compares against the registry-resolved
    Sentence, so it catches every wrong string, not one remembered one), and a NEW leaf appearing
    with any guard at all fails `TestNoHandRolledSweepScopeGuards`.

    Do not add a Taskfile target and do not touch `.github/workflows/`. `task test:go` runs
    `go test ./...` and CI runs the same; a test in `cmd/engram` is already in both. Adding a
    bespoke target would create a second path that can drift from the first.

    Add nothing to `cmd/engram/sweep_scope.go` — this task is test-only. The file already has the
    SPDX header `task license:check` requires; keep it.
  </action>
  <verify>
    <automated>set -e; go test ./cmd/engram -run 'TestNoHandRolledSweepScopeGuards' -count=1 -v > /tmp/08-06-gate.txt 2>&1 || (cat /tmp/08-06-gate.txt; exit 1); test "$(rg -o -F -- '=== RUN   TestNoHandRolledSweepScopeGuards' /tmp/08-06-gate.txt | wc -l | tr -d ' ')" = "1"; test "$(rg -o -F -- '--- PASS: TestNoHandRolledSweepScopeGuards' /tmp/08-06-gate.txt | wc -l | tr -d ' ')" = "1"</automated>
    <automated>set -e; go test ./cmd/engram -run 'TestRequireSweepScope|TestSweepLeavesRejectMissingScopeIdentically|TestSweepLeavesRejectPresentButEmptyScope|TestSweepLeavesUsageStatesRegisteredRule|TestSurfaceConformanceCobraUsage|TestEveryScopeAllScopesPairHasAFlagGroup' -count=1 -v > /tmp/08-06-suite.txt 2>&1 || (cat /tmp/08-06-suite.txt; exit 1); for t in TestRequireSweepScope TestSweepLeavesRejectMissingScopeIdentically TestSweepLeavesRejectPresentButEmptyScope TestSweepLeavesUsageStatesRegisteredRule TestSurfaceConformanceCobraUsage TestEveryScopeAllScopesPairHasAFlagGroup; do test "$(rg -o -F -- "=== RUN   $t" /tmp/08-06-suite.txt | wc -l | tr -d ' ')" = "1" || (echo "NOT RUN: $t"; exit 1); done; test "$(rg -o -F -- '--- FAIL' /tmp/08-06-suite.txt | wc -l | tr -d ' ')" = "0"</automated>
    <automated>set -e; rg -v '^\s*//' cmd/engram/sweep_scope_test.go > /tmp/08-06-code.txt; for leaf in 'spine-review scan' 'spine-review verify' 'spine-review consolidate' 'spine-review purge'; do n=$(rg -o -F -- "$leaf" /tmp/08-06-code.txt | wc -l | tr -d ' '); printf '%s: %s (want 1)\n' "$leaf" "$n"; test "$n" = "1"; done</automated>
    <automated>set -e; test "$(git diff --name-only -- cmd/engram/ | tr -d ' ')" = "cmd/engram/sweep_scope_test.go"</automated>
  </verify>
  <done>
    - `TestNoHandRolledSweepScopeGuards` both RUNS (its `=== RUN` line is present exactly once) and
      PASSES. The `=== RUN` assertion is load-bearing: `go test -run` on a name that does not exist
      exits 0 with `[no tests to run]`, so exit code alone would green-light a renamed-away test.
    - The whole existing sweep suite still runs and reports zero `--- FAIL`, each named test proven
      to have actually run. Runtime behavior at the three leaves is unchanged.
    - Each of the four `spine-review` leaf names appears EXACTLY ONCE in
      `cmd/engram/sweep_scope_test.go` outside `//` comment lines. That is the mechanical proof the
      triplicated enumeration is gone and the classification is single-source: before this task,
      `spine-review scan` and `spine-review verify` each appeared three times in code. The
      comment-line strip is load-bearing and not cosmetic — the two new doc comments are expected to
      discuss these leaves, and an unfiltered count would go red on exactly the prose this task's own
      `<action>` asks for. (`summarize-missing` is deliberately excluded from this clause: it is a
      bare token that also occurs in ordinary sentences.)
    - `cmd/engram/sweep_scope_test.go` is the only file changed under `cmd/engram/`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Prove the gate goes RED against a deliberately constructed defect</name>
  <files>cmd/engram/sweep_scope_test.go</files>
  <read_first>
    - `cmd/engram/client_common.go` around lines 220-255 — `exitUsage = 2` and
      `usageErrorf(format string, a ...any) error`, the signature the scratch command's inline guard
      must match. Use a constant format string; `go test` runs `vet`'s `printf` analyzer by default.
    - The `TestNoHandRolledSweepScopeGuards` you wrote in Task 2, so you can predict the exact
      failure message before running it.
  </read_first>
  <action>
    A gate that has never been observed failing is a claim, not evidence. This task turns the Task 2
    gate into evidence, and it is the reason this plan is three tasks and not two.

    The `<verify>` block below is a single self-contained script that: asserts the scratch file does
    not already exist, writes a throwaway `_test.go` file into `cmd/engram/` whose `init()` registers
    a command exposing both `--scope` and `--all-scopes` with its own inline hand-rolled guard, runs
    `TestNoHandRolledSweepScopeGuards`, requires it to FAIL and to name the throwaway command,
    deletes the scratch file, re-runs, and requires it to PASS. Run it as written.

    Four things to know while running it:

    - The scratch guard deliberately uses a NEUTRAL message, not the literal string 08-01 removed
      from the three sweep leaves. That is the stronger proof: the gate is structural, so it catches
      an unclassified leaf regardless of what its guard says, and running the proof never
      reintroduces the removed literal anywhere under `cmd/engram/`.
    - While the scratch file exists, `TestHelpGolden` and `TestCatalogGolden` will also fail, because
      the live cobra tree really does carry an extra command. That is corroborating evidence the
      scratch is live rather than inert. **Never run `task surfaces:gen`, `task`, `task lint`, or any
      `go test ... -update` while the scratch file exists** — `-update` would bake the throwaway into
      the committed goldens, and `task license:check` would flag the header-less scratch file.
    - The scratch file must not be committed and must not appear in `git status` when this task
      finishes. The script deletes it; the final `<automated>` clause re-asserts its absence
      independently.
    - Capture the RED output verbatim. It goes in the SUMMARY.

    After the script passes, run the full `task` (lint + test) once, with the scratch file gone, and
    confirm it is clean.
  </action>
  <verify>
    <automated>set -e
SCRATCH=cmd/engram/zz_sweepgate_redproof_test.go
test ! -e "$SCRATCH"
cat > "$SCRATCH" <<'GOEOF'
package main

import "github.com/spf13/cobra"

// Throwaway RED-proof fixture for TestNoHandRolledSweepScopeGuards.
// Created and deleted by 08-06-PLAN.md Task 3. Never committed.
func init() {
	var scope string
	var allScopes bool
	c := &cobra.Command{
		Use:   "zz-redproof-sweep",
		Short: "scratch RED-proof command",
		RunE: func(cmd *cobra.Command, args []string) error {
			if scope == "" && !allScopes {
				return usageErrorf("scratch guard: neither scope flag was supplied")
			}
			return nil
		},
	}
	c.Flags().StringVar(&scope, "scope", "", "scope selector")
	c.Flags().BoolVar(&allScopes, "all-scopes", false, "sweep every scope")
	rootCmd.AddCommand(c)
}
GOEOF
set +e
go test ./cmd/engram -run 'TestNoHandRolledSweepScopeGuards' -count=1 -v > /tmp/08-06-red.txt 2>&1
RED_STATUS=$?
set -e
rm -f "$SCRATCH"
echo "--- observed RED output ---"
cat /tmp/08-06-red.txt
echo "--- end RED output ---"
test "$RED_STATUS" -ne 0
test "$(rg -o -F -- '=== RUN   TestNoHandRolledSweepScopeGuards' /tmp/08-06-red.txt | wc -l | tr -d ' ')" = "1"
test "$(rg -o -F -- '--- FAIL: TestNoHandRolledSweepScopeGuards' /tmp/08-06-red.txt | wc -l | tr -d ' ')" = "1"
test "$(rg -o -F -- 'zz-redproof-sweep' /tmp/08-06-red.txt | wc -l | tr -d ' ')" -ge 1
go test ./cmd/engram -run 'TestNoHandRolledSweepScopeGuards' -count=1 -v > /tmp/08-06-green.txt 2>&1
test "$(rg -o -F -- '=== RUN   TestNoHandRolledSweepScopeGuards' /tmp/08-06-green.txt | wc -l | tr -d ' ')" = "1"
test "$(rg -o -F -- '--- PASS: TestNoHandRolledSweepScopeGuards' /tmp/08-06-green.txt | wc -l | tr -d ' ')" = "1"</automated>
    <automated>set -e; test ! -e cmd/engram/zz_sweepgate_redproof_test.go; test "$(git status --porcelain | rg -o -F -- 'zz_sweepgate_redproof_test.go' | wc -l | tr -d ' ')" = "0"</automated>
    <automated>task</automated>
    <automated>go test ./internal/keylinks/... -count=1</automated>
  </verify>
  <done>
    - The RED half fired: `TestNoHandRolledSweepScopeGuards` RAN (its `=== RUN` line present) and
      FAILED, and its failure output named `zz-redproof-sweep`. A non-zero exit alone would not have
      been enough — a compile error also exits non-zero — so the `=== RUN` and `--- FAIL` clauses are
      what distinguish "the gate fired" from "the package did not build".
    - The GREEN half fired after the scratch file was removed: the same test RAN and PASSED.
    - The observed RED output is quoted verbatim in the SUMMARY, including the `extra`-direction
      message the gate produced. If that message did not read as actionable to a contributor seeing
      it cold, say so and improve it in `sweep_scope_test.go` before closing.
    - `cmd/engram/zz_sweepgate_redproof_test.go` does not exist and does not appear in
      `git status --porcelain`.
    - Full `task` (lint + test) is clean.
    - `go test ./internal/keylinks/...` passes, covering this plan's own `key_links` patterns under
      the repo's escaping and satisfiability gates.
  </done>
</task>

</tasks>

<threat_model>
No threat model applies. This plan changes one Go doc comment and one `_test.go` file. It adds no
production code path, no input handling, no dependency, no network or filesystem surface, and no
credential; the only new executable code is a test that reads the in-process cobra tree. Task 1's
fourth gate proves every changed line in `internal/surfaces/rules.go` is a comment line, and Task 2's
fourth gate proves `cmd/engram/sweep_scope_test.go` is the only file changed under `cmd/engram/`.
Manufacturing a STRIDE register for this would be noise, not analysis. No package-manager install
occurs, so no package-legitimacy checkpoint applies.
</threat_model>

<verification>
After all three tasks:

1. `task` (lint + test) is clean.
2. `task surfaces:gen` followed by `git diff --exit-code -- proto/ docs-site/ skill/ gen/ ui/src/lib/gen/ cmd/engram/testdata/`
   is clean, proving the comment edit moved no generated artifact.
3. `git diff --name-only` reports exactly `internal/surfaces/rules.go` and
   `cmd/engram/sweep_scope_test.go`.
4. `go test ./internal/keylinks/... -count=1` passes.
5. Read the rewritten `RuleSweepScopeOrAllScopesRequired` comment block whole and confirm it reads as
   a contributor-facing explanation of a not-yet-wired lane — the backstop truth. Quote it in the
   SUMMARY.
</verification>

<success_criteria>
- The registry comment's retired present-tense envelope claim has zero occurrences, and each claim
  that replaced it is gated against the code, including a non-vacuity control for the zero clause.
- `TestNoHandRolledSweepScopeGuards` exists, runs under `go test ./...`, compares the live tree
  against the classification in both directions, and refuses to pass on an empty walk.
- The classification is declared once and read by every sweep-scope test; each of the four leaf names
  appears exactly once in `cmd/engram/sweep_scope_test.go`.
- The gate has been observed RED against a deliberately constructed defect and GREEN after its
  removal, in one repeatable script, with the RED output recorded.
- No Taskfile target, no CI step, no production code, and no committed literal grep were added.
</success_criteria>

<output>
Create `.planning/phases/08-registry-docs-tail/08-06-SUMMARY.md` when done.

Record in it: the observed RED pre-state of Task 1's retired-claim gate; the rewritten comment block
quoted whole; the verbatim RED output from Task 3 including the gate's own failure message; the
stated reason a committed source-level literal grep was declined; and confirmation that the scratch
file is gone.
</output>
