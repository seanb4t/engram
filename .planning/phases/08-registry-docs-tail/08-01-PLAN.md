---
phase: 08-registry-docs-tail
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/surfaces/rules.go
  - internal/surfacesgen/main.go
  - cmd/engram/sweep_scope.go
  - cmd/engram/sweep_scope_test.go
  - cmd/engram/summarize.go
  - cmd/engram/summarize_test.go
  - cmd/engram/spine_review_scan.go
  - cmd/engram/spine_review_verify.go
  - docs-site/src/content/docs/reference/tools.md
  - docs-site/src/content/docs/guides/cli.md
  - skill/engram/skills/curating-memory/SKILL.md
  - cmd/engram/testdata/help.golden
  - cmd/engram/testdata/catalog.golden
autonomous: true
requirements: [REQ-sweep-scope-rule-registered]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The sweep scope-or-all-scopes constraint is stated exactly once, in `internal/surfaces/rules.go`, and every sweep leaf that enforces it composes that one declared value rather than re-typing it (D-01, REQ-sweep-scope-rule-registered)."
    - "Zero hand-rolled occurrences of the removed guard literal remain anywhere under `cmd/engram/` — the gate counts occurrences and requires zero, never `three converted`, so a fourth sweep leaf that copy-pastes the string fails the gate instead of slipping past a hardcoded number (D-01)."
    - "Runtime behavior is unchanged at all three sweep leaves: the rejection fires on exactly the same inputs it fires on today and still exits `exitUsage`. Registration changes how the constraint is stated and gated, never when it fires."
    - "Adjacency edge: the new rule's `Sentence` is neither a substring of, nor a superstring of, any other registered rule's `Sentence` — including `RulePurgeFilterRequiresScope`'s, whose text also opens on `--scope`/`--all-scopes`. `validateRuleSet` (`internal/surfaces/rules.go:337-345`) treats literal substring containment as a collision and rejects the WHOLE registry on one, so the pair must not merely differ, it must not contain. Proven by `ValidateRules()` passing over the extended registry (edge: adjacency, REQ-sweep-scope-rule-registered)."
    - "Empty edge: a flag that is PRESENT but EMPTY (`--scope \"\"`) is treated identically to an absent `--scope` at all three sweep leaves — each rejects with the registered rule's `Sentence` at `exitUsage`, and all three agree with each other. `--scope` absent, `--scope \"\"`, and neither-flag-supplied are one behavior class, not three (edge: empty, REQ-sweep-scope-rule-registered)."
    - "Ordering edge: the new rule literal is APPENDED to the tail of the `rules` slice, so the declaration order of all eight pre-existing rules — which is `surfacesgen`'s emission order — is unchanged, and every pre-existing generated region is byte-identical after regeneration. Running `task surfaces:gen` a second time produces no further diff (edge: ordering, REQ-sweep-scope-rule-registered)."
    - "The anchored region body is byte-identical to the registered `Sentence` in every anchored file, and the conformance gate goes red if any one of them drifts."
    - "The anchored surface set is DERIVED from `ApplicableSurfaces`, not hand-picked to match issue #480's prediction. Measured against the live tree it is `SurfaceDocsSite` + `SurfaceSkill`; `SurfaceProtoComment` correctly does not resolve, because `all_scopes` appears nowhere in `proto/engram/v1/engram.proto`."
    - "Every generated artifact the change touches is committed in the same change: `task surfaces:gen` leaves the tree clean under `git diff --exit-code` over the same path set CI's `surfaces` job checks."
    - statement: "The prose surrounding each new anchor reads as a genuine sentence in its host document rather than an orphaned statement dropped in to satisfy the gate — verified by reading the rendered page, not by an assertion."
      verification: backstop
  artifacts:
    - path: "internal/surfaces/rules.go"
      provides: "The single declared statement of the sweep scope-or-all-scopes constraint"
      contains: "RuleSweepScopeOrAllScopesRequired"
    - path: "cmd/engram/sweep_scope.go"
      provides: "The one CLI-side composition point converting the declared rule into a usage rejection for every sweep leaf"
      contains: "func requireSweepScope"
    - path: "cmd/engram/sweep_scope_test.go"
      provides: "The three-leaf agreement proof, including the present-but-empty --scope case"
      contains: "requireSweepScope"
    - path: "internal/surfacesgen/main.go"
      provides: "The generator's hand-maintained anchor-target map entry for the new rule"
      contains: "RuleSweepScopeOrAllScopesRequired"
  key_links:
    - from: "cmd/engram/summarize.go"
      to: "cmd/engram/sweep_scope.go"
      via: "the sweep leaf calls the one shared composition helper instead of raising its own usage error"
      pattern: "requireSweepScope[(]"
    - from: "cmd/engram/sweep_scope.go"
      to: "internal/surfaces/rules.go"
      via: "registry lookup of the declared rule, panicking when it is not registered"
      pattern: "surfaces[.]RuleByID[(]surfaces[.]RuleSweepScopeOrAllScopesRequired[)]"
    - from: "internal/surfacesgen/main.go"
      to: "internal/surfaces/rules.go"
      via: "ruleTargets entry keyed by the exported rule const, naming every anchored file"
      pattern: "surfaces[.]RuleSweepScopeOrAllScopesRequired:"
  prohibitions:
    - statement: "The registered sentence MUST NOT be less actionable to an operator than the hand-rolled string it replaces. An operator who has just been rejected must still learn, from the rejection text alone, exactly which two flags satisfy the constraint and that supplying either one is enough. A sentence that is technically correct but names no flag, or names them only obliquely, turns a teaching rejection into a bare refusal."
---

<objective>
Register the sweep `--scope`-or-`--all-scopes` constraint as a declared `surfaces.ConditionalRule`,
anchor its canonical sentence on every surface `ApplicableSurfaces` resolves it to, and convert every
sweep leaf that currently hand-rolls it onto that one declared value (D-01, D-02,
REQ-sweep-scope-rule-registered, issue #480).

Purpose: close the registry gap #480 has tracked since v0.13.x Phase 3. The constraint is enforced
identically at three leaves today by three independently-typed string literals sitting outside the
conformance gate — which is exactly the drift the registry exists to make impossible.

Output: one declared rule, one CLI-side composition point, three anchored prose regions the
generator maintains, and a zero-occurrence gate that fails on a fourth hand-rolled copy.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-registry-docs-tail/08-CONTEXT.md
@.planning/phases/08-registry-docs-tail/08-PATTERNS.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Newly created in THIS plan (they do not exist on `main`; do not verify them as pre-existing drift):

**Go — `internal/surfaces/rules.go`**
- `const RuleSweepScopeOrAllScopesRequired = "sweep-scope-or-all-scopes-required"` (exported ID const)
- the matching `ConditionalRule` struct literal appended to the `rules` slice

**Go — `cmd/engram/sweep_scope.go` (new file)**
- `func requireSweepScope(command, scope string, allScopes bool) error`

**Go — `cmd/engram/sweep_scope_test.go` (new file)**
- `func TestSweepLeavesRejectMissingScopeIdentically(t *testing.T)`
- `func TestSweepLeavesRejectPresentButEmptyScope(t *testing.T)`

**Go — `cmd/engram/summarize_test.go`**
- `func TestSummarizeMissingRequiresScopeOrAllScopes(t *testing.T)`

**Go — `internal/surfacesgen/main.go`**
- a `ruleTargets` map entry keyed `surfaces.RuleSweepScopeOrAllScopesRequired`

**Anchored region IDs (new; three files)**
- `sweep-scope-or-all-scopes-required` in `docs-site/src/content/docs/reference/tools.md`
- `sweep-scope-or-all-scopes-required` in `docs-site/src/content/docs/guides/cli.md`
- `sweep-scope-or-all-scopes-required` in `skill/engram/skills/curating-memory/SKILL.md`

## Plan-time verification already performed (do not redo, but do re-confirm the count)

- The hand-rolled guard literal has **exactly 3 live occurrences under `cmd/engram/`** as of
  planning (`summarize.go:39`, `spine_review_scan.go:44`, `spine_review_verify.go:623`). A fourth
  occurrence exists in `docs/superpowers/plans/2026-06-25-auto-summary-curated-memories.md`, an
  archived planning document that is **out of the gate's scope** — the gate is scoped to
  `cmd/engram/` and must stay scoped there.
- The exposed-field scan `ApplicableSurfaces` performs was reproduced by hand against the live tree.
  `SurfaceDocsSite` resolves (both files carry `scope` and `--all-scopes` substantively).
  `SurfaceSkill` resolves, but ONLY because `--all-scopes` occurs exactly once in
  `skill/engram/skills/curating-memory/SKILL.md`, at line 388, **inside the unrelated
  `purge-filter-requires-scope` anchor's own rendered body**. `skill/engram/skills/discovering/SKILL.md`
  contains no occurrence at all. `SurfaceProtoComment` does NOT resolve — zero occurrences in
  `proto/engram/v1/engram.proto`.
- Consequence: `SurfaceSkill` is applicable but `curating-memory/SKILL.md` does not currently discuss
  `spine-review scan`, `spine-review verify`, or `summarize-missing` **anywhere**. Placing that anchor
  is a genuine writing task, not an annotation of existing prose. `checkProseSurface` requires only
  ONE anchored file per surface, so `discovering/SKILL.md` gets nothing.
- `go test ./internal/keylinks/...` is GREEN as of planning.

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "the sweep constraint is declared once" — registry through generator through one leaf</name>
  <reversibility rating="costly">D-01/D-02: the `Sentence` becomes the published payload of anchored
  regions across docs-site, the skill, and the CLI rejection path. Unwinding means regenerating every
  anchor and restoring the hand-rolled literals at each leaf.</reversibility>
  <precondition>`go tool buf --version` succeeds — `task surfaces:gen` chains `task proto:gen`, which
  invokes buf; without it the generator step cannot complete and the tree cannot be left clean.</precondition>
  <files>internal/surfaces/rules.go, internal/surfacesgen/main.go, cmd/engram/sweep_scope.go, cmd/engram/spine_review_scan.go, docs-site/src/content/docs/reference/tools.md, docs-site/src/content/docs/guides/cli.md, skill/engram/skills/curating-memory/SKILL.md</files>
  <read_first>
    - `internal/surfaces/rules.go` — the `ConditionalRule` field-by-field doc comment (lines 21-94), the `declared` provenance comment (97-108), `RulePurgeFilterRequiresScope`'s doc-comment const (150-162) and its struct literal (257-265), and `validateRuleSet`'s full body (307-347), especially the Sentence-substring loop at 337-345
    - `internal/surfaces/normalize.go` — `NormalizeField` (~52), `ApplicableSurfaces` (~82), `SurfaceApplicabilityFields` (~102)
    - `internal/surfaces/anchor.go` — `WriteRegion` (164-206) and its atomic-rewrite helper (210-231); note it ERRORS when the anchor pair is absent
    - `internal/surfaces/conformance_test.go` — `proseTargets` (18-27), `exposedFileFields` (38-56), `buildProseExposed` (63-77), `assertRuleAppliesSomewhere` (90-97), `checkProseSurface` (113-133)
    - `internal/surfacesgen/main.go` — the `target` struct and the `ruleTargets` map, especially the `RulePurgeFilterRequiresScope` entry and its explanatory comment (118-123)
    - `cmd/engram/spine_review_purge.go:79-91` — `requirePurgeFilterScope`, the proven CLI-side composition template (registry lookup, panic-on-missing, `usageErrorf("%s", rule.Sentence)`)
    - `cmd/engram/spine_review_scan.go:30-50` — the `RunE` guard and the comment that names this exact follow-up
    - `skill/engram/skills/curating-memory/SKILL.md:375-395` — the `spine-review purge` paragraph family and the existing filled anchor at 388
    - `docs-site/src/content/docs/guides/cli.md:155-250` and `:300-315` — the sweep-command subsections and the existing filled anchor
    - `docs-site/src/content/docs/reference/tools.md:506-530` — the `## CLI: summarize-missing` block and its "either flag is required" bold line
    - `Taskfile.yaml:245-263` — what `surfaces:gen` actually chains
  </read_first>
  <action>
Register the rule and prove one full path through the machinery before widening to the other leaves.

**Step 1 — declare the rule.** Add an exported ID const `RuleSweepScopeOrAllScopesRequired` with the
value `sweep-scope-or-all-scopes-required` (the Pascal-to-kebab convention every other rule follows),
carrying a doc comment in the house style: what the rule constrains, which leaves enforce it, why
`Fields` is the flag pair, and why `TagForm` is left empty. **APPEND** the struct literal to the tail
of the `rules` slice — declaration order is `surfacesgen`'s emission order, so inserting mid-slice
churns every downstream generated region for no reason.

Field values, per D-02: `Fields: []string{"scope", "all-scopes"}`; `Hint: "conditional_required"`
(matching `RulePurgeFilterRequiresScope`, the closest precedent); no `SurfaceFields` override — the
two sets are identical for this rule, and `SurfaceFields` exists to disambiguate a field shared across
differently-behaving struct types, not to filter an incidental prose collision; `TagForm` empty, for
the same reason `RuleDestructiveRequiresApply` and `RuleVerifyFailOnValues` leave it empty (no MCP arg
struct carries an `all_scopes` field on any schema, so there is no jsonschema tag to compress into).
Add a comment recording that reason at the empty `TagForm`, matching the sibling literals.

The `Sentence` is **flag-shaped** per D-02, ASCII-only, and must satisfy the substring constraint in
BOTH directions against all eight existing sentences. The recommended text, verified at plan time
against every registered sentence, is:

`a sweep requires an explicit --scope or --all-scopes: name one scope, or opt into every scope`

You may reword within D-02's constraints, but if you do, re-run `go test ./internal/surfaces -run
TestValidateRules -count=1` immediately and before touching any other file. `RulePurgeFilterRequiresScope`'s
sentence also opens on this flag pair, so a careless rephrasing that happens to nest inside it fails
the entire registry, not just the new rule.

**Step 2 — hand-write the anchor pairs.** `WriteRegion` fills regions; it never creates them, and it
returns an error when the pair is absent. Write an EMPTY pair into each of the three target files
before the generator runs:

- `docs-site/src/content/docs/reference/tools.md` — in the `## CLI: summarize-missing` block, replacing
  the standalone bold line that currently states the requirement in hand-written prose (around line
  515). The generated body renders as bare text with no added punctuation, so any trailing period must
  sit OUTSIDE the closing anchor comment, exactly as the existing anchor at line 128 does.
- `docs-site/src/content/docs/guides/cli.md` — in the sweep-command region (the `summarize-missing`
  paragraph and the `spine-review scan` / `spine-review verify` subsections, 155-250). Choose one
  placement that reads naturally for all three leaves rather than repeating the anchor per subsection;
  the rule is one constraint shared by all of them.
- `skill/engram/skills/curating-memory/SKILL.md` — this file does NOT currently mention
  `spine-review scan`, `spine-review verify`, or `summarize-missing` at all, so this anchor needs
  genuinely new prose around it. Extend the paragraph family near line 382 that already covers
  `spine-review purge --apply`'s deletion contract to also name the sibling sweep leaves and their
  shared scope requirement, then anchor inside that new sentence. Do NOT anchor in
  `skill/engram/skills/discovering/SKILL.md` — it discusses no operator sweep command, so an anchor
  there is an orphaned statement, and `checkProseSurface` requires only one anchored file per surface.

**Step 3 — add the generator entry.** Add a `ruleTargets` entry keyed by the exported const (never a
bare ID string — every existing entry keys by the const), listing the three files with `kindMarkdown`.
Carry an explanatory comment in the house style stating that there is no proto anchor because
`all_scopes` is not a proto field on any message, and that the `SurfaceSkill` leg resolves via a raw
substring scan of live file content rather than because the skill file previously discussed sweeps.

**Step 4 — convert ONE leaf, end to end.** Create `cmd/engram/sweep_scope.go` with the Apache-2.0 SPDX
header every Go file in this repo carries, holding one shared helper
`requireSweepScope(command, scope string, allScopes bool) error`. It returns nil when either the scope
is non-empty or all-scopes is set; otherwise it looks the rule up via `surfaces.RuleByID`, panics with
the house-style message naming the calling command and the registry const when the lookup fails, and
returns `usageErrorf("%s", rule.Sentence)`. One shared helper rather than three near-identical
per-file copies: the whole point of the change is that the constraint is stated once.

Rewire `cmd/engram/spine_review_scan.go`'s `RunE` guard to call it. This leaf is converted first
precisely because it is ALREADY guarded by `TestSpineReviewScanRequiresScopeOrAllScopes` — the other
two are handled in Tasks 2 and 3, after the missing coverage is written. Delete the stale comment
above that guard which asserts the registry holds no rule for this constraint; that comment is the
TODO this task closes.

**Do not reproduce the removed literal anywhere under `cmd/engram/`** — not in a code comment, not in
a test fixture, not in a commemorative note about what it replaced. The acceptance gate counts raw
occurrences under that directory and does not filter comments, so a commemorative mention turns a
correct implementation red. If the change needs explaining, name the file and line it came from.

**Do NOT change the three commands' `--scope` / `--all-scopes` cobra `Usage` strings.** Composing the
sentence into Usage is precedented (`spine_review_purge.go` does it) but is not required by D-01 or
D-02, and it widens the golden-drift surface for no acceptance benefit. The existing Usage text
already names each flag's partner, which is what `TestEveryScopeAllScopesPairHasAFlagGroup` asserts.

**Step 5 — generate.** Run `task surfaces:gen`, never the bare `go run ./internal/surfacesgen`. The
task additionally chains `proto:gen` (buf generate plus the vendored TS re-copy) and regenerates the
pinned help and catalog goldens; skipping it is how the CI `surfaces` job fails on a diff that looks
unrelated to the change. Then run it a SECOND time and confirm it produces no further diff — that is
the ordering-stability check.
  </action>
  <verify>
    <automated>go test ./internal/surfaces/... -count=1 && go test ./cmd/engram -run 'TestSpineReviewScanRequiresScopeOrAllScopes|TestEveryScopeAllScopesPairHasAFlagGroup|TestHelpGolden|TestCatalogGolden' -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `rg -o 'RuleSweepScopeOrAllScopesRequired' internal/surfaces/rules.go | wc -l` reports `2` — the const declaration and the one registry literal that references it, no more.
    - `go test ./internal/surfaces -run TestValidateRules -count=1 -v` passes, proving the new `Sentence` collides with no other rule's in either direction. To see this gate RED, temporarily set the new `Sentence` to a phrase nested inside `RulePurgeFilterRequiresScope`'s (for example, its own opening clause verbatim) and confirm `validateRuleSet` reports a Sentence-substring collision; restore before committing and record the observed red in the summary.
    - `go test ./internal/surfaces -run TestSurfaceConformanceProseFiles -count=1 -v` passes. To see it RED, hand-edit one anchored body by a single character and confirm the gate reports the divergence; restore before committing.
    - `rg -o 'engram:rule:start sweep-scope-or-all-scopes-required' docs-site/src/content/docs/reference/tools.md docs-site/src/content/docs/guides/cli.md skill/engram/skills/curating-memory/SKILL.md | wc -l` reports `3`, and `rg -c 'engram:rule:start sweep-scope-or-all-scopes-required' skill/engram/skills/discovering/SKILL.md` reports no match.
    - `rg -o 'surfaces[.]RuleByID[(]surfaces[.]RuleSweepScopeOrAllScopesRequired[)]' cmd/engram/sweep_scope.go | wc -l` reports `1`.
    - `rg -o 'requireSweepScope[(]' cmd/engram/spine_review_scan.go | wc -l` reports `1`, and `rg -o -- '--scope <scope> or --all-scopes is required' cmd/engram/spine_review_scan.go | wc -l` reports `0` — this leaf no longer states the constraint itself. It reports `1` against `main`, so the gate's ability to fail is already established.
    - After `task surfaces:gen`, `git diff --exit-code -- proto/ docs-site/ skill/ gen/ ui/src/lib/gen/ cmd/engram/testdata/` exits 0. Running `task surfaces:gen` a second time also leaves that command exiting 0 — the ordering-stability check.
    - `git diff --stat -- skill/engram/skills/curating-memory/SKILL.md` shows added prose lines beyond the anchor pair itself: the anchor landed inside a sentence, not on its own.
    - `task license:check` passes with the new `cmd/engram/sweep_scope.go` carrying the SPDX header.
  </acceptance_criteria>
  <done>The constraint is declared once, anchored on both applicable surfaces, generated rather than hand-written, and enforced at one leaf through the registry — the whole path from `rules.go` to a live rejection works end to end.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin the two unguarded rejection behaviors before converting them</name>
  <files>cmd/engram/summarize_test.go, cmd/engram/sweep_scope_test.go</files>
  <read_first>
    - `cmd/engram/spine_review_test.go:47-59` — `TestSpineReviewScanRequiresScopeOrAllScopes`, the exact shape to mirror
    - `cmd/engram/spine_review_verify_test.go:523-535` — the sibling with the identical shape
    - `cmd/engram/summarize_test.go` — the whole file; note it has a mutual-exclusivity test but no required-rejection test, and note the `resetClientFlags` / `resetCommandFlagState` pairing its tests use
    - `cmd/engram/summarize.go:33-40`, `cmd/engram/spine_review_scan.go:35-46`, `cmd/engram/spine_review_verify.go:620-625` — the three guards, so the test reaches the guard and not some earlier validation
    - `cmd/engram/sweep_scope.go` — the helper Task 1 created, for the rule lookup the assertions compare against
  </read_first>
  <behavior>
    - `summarize-missing` with neither flag supplied returns a non-nil error whose exit code is `exitUsage`.
    - The same holds for `spine-review scan` and `spine-review verify`, and all three return the SAME message: the registered rule's `Sentence`.
    - `--scope ""` (flag PRESENT, value empty) behaves identically to the flag being absent, at all three leaves: same exit code, same message. It is not a distinct behavior class.
    - The message assertion reads the sentence from `surfaces.RuleByID`, never from a literal copied into the test — a test carrying its own copy of the sentence is a fourth unregistered statement of the constraint.
    - Supplying either flag alone does NOT trip the guard: `--all-scopes` alone, and `--scope "x"` alone, both get past it (they may fail later for unrelated environment reasons, which the test must not conflate with a usage rejection).
  </behavior>
  <action>
Author the coverage the conversion needs BEFORE the conversion, so the two currently-unguarded leaves
are not converted blind. These tests pin behavior that already exists today, so they pass immediately
against `main`'s hand-rolled guards as well as against the converted ones — that is exactly what makes
them a valid characterization pin for a refactor whose stated contract is "runtime behavior unchanged".

Add `TestSummarizeMissingRequiresScopeOrAllScopes` to `cmd/engram/summarize_test.go`, mirroring
`TestSpineReviewScanRequiresScopeOrAllScopes` field for field: reset the client flags and the
command's own flag state, invoke `summarize-missing` with no scope flags, assert a non-nil error, and
assert `exitCodeFromError(err)` is `exitUsage`. Use the same `resetCommandFlagState` pairing the other
tests in that file use — the shared `rootCmd` singleton leaks flag state across tests in a full-package
run, and this is the established mitigation.

Create `cmd/engram/sweep_scope_test.go` with the SPDX header, holding two table-driven tests over all
three sweep leaves:

`TestSweepLeavesRejectMissingScopeIdentically` — for each leaf, invoke it with no scope flags and
assert the exit code is `exitUsage` AND the error message equals the registered rule's `Sentence`,
looked up through `surfaces.RuleByID(surfaces.RuleSweepScopeOrAllScopesRequired)` inside the test.
Comparing against the looked-up value rather than an inlined string is the point: it makes the test
incapable of passing while the leaves disagree with the registry.

`TestSweepLeavesRejectPresentButEmptyScope` — for each leaf, invoke it with `--scope` supplied and its
value empty, and assert the identical outcome. This is the case the three leaves are not currently
tested for at all, and it is the one where they could silently diverge: cobra records a present-empty
flag as `Changed`, so a leaf that switched to a `Changed`-based check instead of an emptiness check
would accept an empty scope and sweep a scope that matches nothing. Assert on the message too, so a
rejection arriving from cobra's mutual-exclusion machinery instead of from the guard cannot pass as
a green.

Include the positive control in the same table: each leaf with `--all-scopes` alone, and each leaf
with a non-empty `--scope`, must NOT produce a usage rejection carrying the rule sentence. Without
that control the tests would still pass if a leaf rejected everything unconditionally.

Do not reproduce the removed guard literal in these test files.
  </action>
  <verify>
    <automated>go test ./cmd/engram -run 'RequiresScopeOrAllScopes|TestSweepLeaves' -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd/engram -run 'RequiresScopeOrAllScopes|TestSweepLeaves' -count=1 -v` shows a RUN/PASS pair for `TestSummarizeMissingRequiresScopeOrAllScopes`, `TestSweepLeavesRejectMissingScopeIdentically`, and `TestSweepLeavesRejectPresentButEmptyScope` — a package-level `ok` alone is not evidence the names matched.
    - `rg -o 'surfaces[.]RuleByID[(]surfaces[.]RuleSweepScopeOrAllScopesRequired[)]' cmd/engram/sweep_scope_test.go | wc -l` reports at least `1` — the expected message is derived from the registry, not inlined.
    - The two new table-driven tests each cover all three leaves: `rg -o 'summarize-missing' cmd/engram/sweep_scope_test.go | wc -l` reports at least `2`, and the same holds for the two `spine-review` leaves.
    - Both new tests go RED when the guard is defeated: temporarily change one leaf's guard to return nil unconditionally and confirm a failure naming that leaf; restore and record the observed red in the summary.
    - `go test ./cmd/engram/... -count=1` passes in full, including every pre-existing test in `summarize_test.go`.
  </acceptance_criteria>
  <done>All three sweep leaves have a test pinning the required-rejection path and the present-but-empty-scope path, asserted against the registry's own sentence rather than a copy of it.</done>
</task>

<task type="auto">
  <name>Task 3: Convert the remaining two leaves and close the gate at zero</name>
  <reversibility rating="costly">D-01: the conversion set is all sweep leaves, including
  `spine-review verify`, which neither the requirement text nor issue #480 names. Reverting means
  restoring three hand-rolled literals and unwinding the anchors regenerated from the rule.</reversibility>
  <files>cmd/engram/summarize.go, cmd/engram/spine_review_verify.go, cmd/engram/testdata/help.golden, cmd/engram/testdata/catalog.golden</files>
  <read_first>
    - `cmd/engram/sweep_scope.go` — the helper Task 1 created and its call convention
    - `cmd/engram/spine_review_scan.go` — the converted leaf, as the shape to replicate
    - `cmd/engram/summarize.go:33-45` — the guard and what runs immediately after it, so the conversion preserves ordering relative to `operatorOutputFormat`
    - `cmd/engram/spine_review_verify.go:618-630` — the guard, and note this file already imports `internal/surfaces` for `RuleVerifyFailOnValues`
    - `cmd/engram/flaggroup_test.go:490-524` — `TestEveryScopeAllScopesPairHasAFlagGroup`, to confirm the untouched Usage strings still satisfy it
  </read_first>
  <action>
Convert `cmd/engram/summarize.go` and `cmd/engram/spine_review_verify.go` onto `requireSweepScope`,
exactly as `spine_review_scan.go` was converted in Task 1. Preserve each guard's position in its
`RunE` body — it must still run before `operatorOutputFormat` and before any store construction, so
a missing scope is a usage rejection rather than a connection failure.

Per D-01 this set is **all** sweep leaves, not the two the requirement text and issue #480 name.
`spine-review verify` postdates the issue and was never added to its list; leaving it hand-rolled
would preserve an identical unregistered statement outside the conformance gate, one site smaller —
which is precisely the drift the change exists to close.

Before finalizing, re-run the occurrence scan yourself rather than trusting the count recorded at
plan time. If it finds a leaf beyond the three named here, convert that one too and note the
divergence in the summary. The gate asserts zero remaining, not three converted, exactly so that a
leaf added between planning and execution fails loudly instead of slipping through.

Do not reproduce the removed literal anywhere under `cmd/engram/`, including in a comment explaining
the conversion — the gate counts raw occurrences and does not filter comments.

Re-run `task surfaces:gen` and confirm the tree stays clean. The Usage strings are unchanged by this
task, so the goldens should not move; if they do, something else changed and it needs explaining
before it is committed.

Then run the full local reproduction of CI's `surfaces` job and the full suite.
  </action>
  <verify>
    <automated>test "$(rg -o -- '--scope <scope> or --all-scopes is required' cmd/engram/ | wc -l | tr -d ' ')" = "0" && go test ./cmd/engram/... ./internal/surfaces/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `rg -o -- '--scope <scope> or --all-scopes is required' cmd/engram/ | wc -l` reports `0`. This gate is RED today by construction — it reports `3` against `main` — so its ability to fail is already established; state the observed before-count in the summary. Note the gate is scoped to `cmd/engram/` and deliberately does not sweep `docs/superpowers/plans/`, which carries an archived, out-of-scope copy.
    - `rg -o 'requireSweepScope[(]' cmd/engram/ | wc -l` reports `4` — one declaration site plus one call per sweep leaf. A higher number means a leaf beyond the three was found; a lower one means a conversion was missed.
    - `go test ./cmd/engram -run 'RequiresScopeOrAllScopes|TestSweepLeaves|TestEveryScopeAllScopesPairHasAFlagGroup' -count=1 -v` shows RUN/PASS for every named test, with no change to `flaggroup_test.go` required.
    - `git diff --exit-code -- cmd/engram/testdata/` exits 0 after `task surfaces:gen` — the Usage strings were left alone, so the goldens did not move.
    - `go run ./internal/surfacesgen && go tool buf generate && git diff --exit-code -- proto/ docs-site/ skill/ gen/ ui/src/lib/gen/ cmd/engram/testdata/` exits 0, reproducing CI's `surfaces` job locally.
    - `task` (lint plus the full test suite) is green, and `go test ./internal/keylinks/... -count=1` is green.
    - `git diff --exit-code go.mod go.sum` exits 0 — no dependency was added.
  </acceptance_criteria>
  <done>Every sweep leaf composes the declared rule; no hand-rolled copy of the constraint survives under `cmd/engram/`; issue #480 is closable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator argv → sweep `RunE` | Operator-supplied `--scope` / `--all-scopes` values cross into a command that can sweep every scope in the collection |
| rule registry → operator terminal | The declared `Sentence` becomes text rendered into an operator's terminal on rejection |
| rule registry → generated prose | The same `Sentence` is written verbatim into three committed documentation files by the generator |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Elevation of Privilege | the three sweep `RunE` guards | medium | mitigate | A refactor of an input-validation rejection can silently widen what it accepts. The narrowing case that matters is `--scope ""`: cobra records a present-empty flag as `Changed`, so a conversion that switched from an emptiness check to a `Changed` check would accept an empty scope and sweep against a scope selector that matches nothing, with no rejection. Task 2's `TestSweepLeavesRejectPresentButEmptyScope` pins the emptiness semantics at all three leaves before either unguarded leaf is converted, and Task 3's positive control proves the guard is not rejecting unconditionally instead. |
| T-08-02 | Denial of Service | `requireSweepScope`'s panic-on-unregistered-rule | low | accept | The panic mirrors the shipped `requirePurgeFilterScope` precedent and is unreachable in a built binary: the rule const and the registry literal are in the same package, `ValidateRules` covers the registry, and `TestValidateRules` runs in CI. A reachable panic here would require deleting the registry entry while keeping the const, which does not compile past the literal's own reference. |
| T-08-03 | Repudiation | anchored regions in `tools.md`, `cli.md`, `SKILL.md` | low | mitigate | Hand-editing inside an anchor would let documentation state a constraint the binary does not enforce, with no signal. `TestSurfaceConformanceProseFiles` compares each region body for exact equality with the registered `Sentence` and CI's `surfaces` job re-runs the generator and diffs the tree, so a hand-edit fails the PR rather than shipping. |
| T-08-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package in any ecosystem. `08-RESEARCH.md` records the Package Legitimacy Audit as not applicable; `git diff --exit-code go.mod go.sum` is expected to exit 0 and is an acceptance criterion on Task 3. |
</threat_model>

<verification>
- `task` (lint plus the full Go and Python suite) is green.
- `go test ./internal/keylinks/... -count=1` is green.
- The zero-occurrence gate reports `0`: `rg -o -- '--scope <scope> or --all-scopes is required' cmd/engram/ | wc -l`. It reported `3` before the change; state both numbers in the summary.
- CI's `surfaces` job reproduces clean locally: `go run ./internal/surfacesgen && go tool buf generate && git diff --exit-code -- proto/ docs-site/ skill/ gen/ ui/src/lib/gen/ cmd/engram/testdata/`.
- `task license:check` passes (the two new Go files carry the SPDX header; no `.planning/**` or `docs-site/**` file gained one).
- `docs-site` builds: `cd docs-site && pnpm build`.
- `git diff --exit-code go.mod go.sum` exits 0.
</verification>

<success_criteria>
- `RuleSweepScopeOrAllScopesRequired` is a declared `surfaces.ConditionalRule` reused by every sweep leaf.
- Its canonical sentence is anchored and conformance-gated on every surface `ApplicableSurfaces` resolves it to, with the derived set recorded in the summary alongside the reason the `SurfaceSkill` leg resolves.
- No hand-rolled copy of the constraint survives under `cmd/engram/`, and the gate that proves it asserts zero rather than a count.
- The rejection fires on exactly the inputs it fired on before, including the present-but-empty `--scope` case, proven at all three leaves.
</success_criteria>

<output>
Create `.planning/phases/08-registry-docs-tail/08-01-SUMMARY.md` when done
</output>
