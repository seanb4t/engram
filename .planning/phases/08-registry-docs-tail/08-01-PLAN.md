---
phase: 08-registry-docs-tail
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/surfaces/rules.go
  - internal/surfacesgen/main.go
  - cmd/engram/sweep_scope.go
  - cmd/engram/sweep_scope_test.go
  - cmd/engram/summarize.go
  - cmd/engram/summarize_test.go
  - cmd/engram/spine_review_scan.go
  - cmd/engram/spine_review_verify.go
  - docs-site/src/content/docs/reference/tools.md
  - cmd/engram/testdata/help.golden
  - cmd/engram/testdata/catalog.golden
autonomous: true
requirements: [REQ-sweep-scope-rule-registered]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The sweep scope-or-all-scopes constraint is stated exactly once, in `internal/surfaces/rules.go`, and every sweep leaf that enforces it composes that one declared value rather than re-typing it — in its runtime rejection AND in its `--all-scopes` cobra `Usage` string (D-01, REQ-sweep-scope-rule-registered)."
    - "Zero hand-rolled occurrences of the removed guard literal remain anywhere under `cmd/engram/` — the gate counts occurrences and requires zero, never `three converted`, so a fourth sweep leaf that copy-pastes the string fails the gate instead of slipping past a hardcoded number (D-01)."
    - "Runtime behavior is unchanged at all three sweep leaves: the rejection fires on exactly the same inputs it fires on today and still exits `exitUsage`. Registration changes how the constraint is stated and gated, never when it fires."
    - "The rule's `SurfaceFields` diverges from `Fields` because the flag pair ALONE cannot distinguish a command that ENFORCES the rule from one that merely EXPOSES both flags. Five commands declare both; two of them (`spine-review consolidate`, `spine-review purge`) do not enforce it, and no field set can select the three that do — `spine-review scan`'s entire flag set is a strict subset of both non-enforcing commands'. `SurfaceFields` therefore resolves the cobra lane to the ONE isolable enforcing leaf, and a dedicated whitelist test pins the other two (D-02, 08-CONTEXT.md Claude's Discretion)."
    - "Adjacency edge: the new rule's `Sentence` is neither a substring of, nor a superstring of, any other registered rule's `Sentence` — including `RulePurgeFilterRequiresScope`'s, whose text also opens on `--scope`/`--all-scopes`. `validateRuleSet` treats literal substring containment as a collision and rejects the WHOLE registry on one, so the pair must not merely differ, it must not contain. Proven by `ValidateRules()` passing over the extended registry (edge: adjacency, REQ-sweep-scope-rule-registered)."
    - "Empty edge: a flag that is PRESENT but EMPTY (`--scope \"\"`) is treated identically to an absent `--scope` at all three sweep leaves — each rejects with the registered rule's `Sentence` at `exitUsage`, and all three agree with each other. `--scope` absent, `--scope \"\"`, and neither-flag-supplied are one behavior class, not three (edge: empty, REQ-sweep-scope-rule-registered)."
    - "Ordering edge: the new rule literal is APPENDED to the tail of the `rules` slice. `Rules()` returns declaration order and both conformance gates iterate it, so appending keeps every pre-existing rule's position — and therefore every gate's failure-report ordering — unchanged. Regeneration is a fixed point: running `task surfaces:gen` a second time adds no further diff (edge: ordering, REQ-sweep-scope-rule-registered)."
    - "The anchored region body is byte-identical to the registered `Sentence` in the anchored file, and the conformance gate goes red if it drifts."
    - "The anchored surface set is DERIVED from `ApplicableSurfaces`, not hand-picked to match issue #480's prediction. Measured against the live tree under this rule's `SurfaceFields`, it is `SurfaceDocsSite` alone: `SurfaceSkill` does NOT resolve (no skill file mentions `--dry-run`, and the earlier apparent resolution came only from `--all-scopes` occurring inside an unrelated rule's rendered anchor body), and `SurfaceProtoComment` does NOT resolve (`all_scopes` appears nowhere in `proto/engram/v1/engram.proto`)."
    - "The pinned `--help` and catalog goldens are regenerated in the same change, because composing the `Sentence` into three `Usage` strings moves them. `task surfaces:gen` — which IS CI's `surfaces` job sequence, `-update` step included — leaves the committed tree clean under `git diff --exit-code` over the same path set that job checks."
    - statement: "The prose surrounding the new anchor reads as a genuine sentence in its host document rather than an orphaned statement dropped in to satisfy the gate — verified by reading the rendered page, not by an assertion."
      verification: backstop
  artifacts:
    - path: "internal/surfaces/rules.go"
      provides: "The single declared statement of the sweep scope-or-all-scopes constraint"
      contains: "RuleSweepScopeOrAllScopesRequired"
    - path: "cmd/engram/sweep_scope.go"
      provides: "The one CLI-side composition point converting the declared rule into a usage rejection for every sweep leaf"
      contains: "func requireSweepScope"
    - path: "cmd/engram/sweep_scope_test.go"
      provides: "The three-leaf agreement proof, the present-but-empty --scope case, and the Usage-composition whitelist the field model cannot express"
      contains: "requireSweepScope"
    - path: "internal/surfacesgen/main.go"
      provides: "The generator's hand-maintained anchor-target map entry for the new rule"
      contains: "RuleSweepScopeOrAllScopesRequired"
  key_links:
    - from: "cmd/engram/summarize.go"
      to: "cmd/engram/sweep_scope.go"
      via: "the sweep leaf calls the one shared composition helper instead of raising its own usage error"
      pattern: "requireSweepScope[(]"
    - from: "cmd/engram/sweep_scope.go"
      to: "internal/surfaces/rules.go"
      via: "registry lookup of the declared rule, panicking when it is not registered"
      pattern: "surfaces[.]RuleByID[(]surfaces[.]RuleSweepScopeOrAllScopesRequired[)]"
    - from: "internal/surfacesgen/main.go"
      to: "internal/surfaces/rules.go"
      via: "ruleTargets entry keyed by the exported rule const, naming the anchored file"
      pattern: "surfaces[.]RuleSweepScopeOrAllScopesRequired:"
  prohibitions:
    - statement: "The registered sentence MUST NOT be less actionable to an operator than the hand-rolled string it replaces. An operator who has just been rejected must still learn, from the rejection text alone, exactly which two flags satisfy the constraint and that supplying either one is enough. A sentence that is technically correct but names no flag, or names them only obliquely, turns a teaching rejection into a bare refusal."
    - statement: "The registered sentence MUST NOT be published onto any surface that does not enforce it. `spine-review consolidate` has no such guard at all, and `spine-review purge` is deliberately EXEMPT on its structural-class path (D-10) — with neither flag a class-only purge applies no scope filter and spans every scope. Stating the constraint in either command's `--help` would tell an operator a bare invocation is refused when it is not; on `purge` that is a deletion-blast-radius misstatement, not a cosmetic one."
---

<!-- planner-discipline-allow: --scope <scope> or --all-scopes is required -->

<objective>
Register the sweep `--scope`-or-`--all-scopes` constraint as a declared `surfaces.ConditionalRule`,
anchor its canonical sentence on the surface `ApplicableSurfaces` resolves it to, and convert every
sweep leaf that currently hand-rolls it onto that one declared value — in the rejection path AND in
the `--all-scopes` help text (D-01, D-02, REQ-sweep-scope-rule-registered, issue #480).

Purpose: close the registry gap #480 has tracked since v0.13.x Phase 3. The constraint is enforced
identically at three leaves today by three independently-typed string literals sitting outside the
conformance gate — which is exactly the drift the registry exists to make impossible.

Output: one declared rule, one CLI-side composition point, one generated prose region, three help
strings composed from the registry, and a zero-occurrence gate that fails on a fourth hand-rolled copy.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-registry-docs-tail/08-CONTEXT.md
@.planning/phases/08-registry-docs-tail/08-PATTERNS.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Newly created in THIS plan (they do not exist on `main`; do not verify them as pre-existing drift):

**Go — `internal/surfaces/rules.go`**
- `const RuleSweepScopeOrAllScopesRequired = "sweep-scope-or-all-scopes-required"` (exported ID const)
- the matching `ConditionalRule` struct literal appended to the `rules` slice

**Go — `cmd/engram/sweep_scope.go` (new file)**
- `func requireSweepScope(scope string, allScopes bool) error` — two parameters, not three. An
  earlier draft prescribed a leading `command` parameter that the prescribed body never used
  (cycle-2 review finding L-2.3). It cannot be put to work either: the rejection message must EQUAL
  the registered `Sentence` byte for byte, which is what makes `TestSweepLeavesRejectMissingScopeIdentically`
  incapable of passing while a leaf disagrees with the registry — so there is nowhere for a
  command-specific string to go. Dropping it removes a misleading API surface on the single
  composition point of the whole change. Task 3's `<behavior>` block already describes the helper
  with two arguments (`("x", false)`, `("", true)`, `("", false)`); that is now the signature too.
- `func sweepScopeRule() surfaces.ConditionalRule` (the single registry-lookup-with-panic site the
  helper and the three `init()` Usage compositions share)

**Go — `cmd/engram/sweep_scope_test.go` (new file)**
- `func TestRequireSweepScope(t *testing.T)` — direct table test of the helper, no command, no store
- `func TestSweepLeavesRejectMissingScopeIdentically(t *testing.T)`
- `func TestSweepLeavesRejectPresentButEmptyScope(t *testing.T)`
- `func TestSweepLeavesUsageStatesRegisteredRule(t *testing.T)`

**Go — `cmd/engram/summarize_test.go`**
- `func TestSummarizeMissingRequiresScopeOrAllScopes(t *testing.T)`

**Go — `internal/surfacesgen/main.go`**
- a `ruleTargets` map entry keyed `surfaces.RuleSweepScopeOrAllScopesRequired`

**Anchored region ID (new; ONE file)**
- `sweep-scope-or-all-scopes-required` in `docs-site/src/content/docs/reference/tools.md`

## Plan-time verification already performed (facts, not guesses)

Every claim below was reproduced against the live tree during replanning. Re-confirm the two marked
**RE-CONFIRM**; take the rest as established.

1. **RE-CONFIRM — the hand-rolled guard literal has exactly 3 live occurrences under `cmd/engram/`**
   (`spine_review_verify.go:623`, `summarize.go:39`, `spine_review_scan.go:44`). A fourth occurrence
   exists in `docs/superpowers/plans/2026-06-25-auto-summary-curated-memories.md`, an archived
   planning document **out of the gate's scope** — the gate is scoped to `cmd/engram/` and stays there.

2. **Exactly five non-hidden commands declare BOTH `--scope` and `--all-scopes`**, with these full
   flag sets (dumped from `cobraExposedFields` against the live tree):

   | Command | own flag set | enforces the rule? |
   |---|---|---|
   | `spine-review scan` | `all-scopes, output, scope, timeout` | **yes** (hand-rolled today) |
   | `spine-review verify` | `all-scopes, fail-on, output, scope, timeout` | **yes** (hand-rolled today) |
   | `summarize-missing` | `all-scopes, dry-run, limit, older-than, output, scope, timeout` | **yes** (hand-rolled today) |
   | `spine-review consolidate` | `all-scopes, min-score, output, scope, timeout` | **no guard at all** |
   | `spine-review purge` | `all-scopes, apply, category, class, older-than, output, scope, tags, timeout` | **no** — exempt on the class path (D-10) |

3. **No field set can select the three enforcing leaves.** Their intersection is
   `{scope, all-scopes, output, timeout}` — which is `spine-review scan`'s *entire* flag set — and
   that set is a strict subset of both `consolidate`'s and `purge`'s. Every subset of the
   intersection therefore also resolves onto both non-enforcing commands. This is a closed proof,
   not a search that gave up.

4. **Publishing onto the two non-enforcers would be false, and on `purge` dangerous.**
   `spine-review consolidate` runs `NearDuplicates` with `Scope:"" AllScopes:false`, which
   `internal/store/spine.go:384-387` defines as a well-defined EMPTY result — no rejection.
   `spine-review purge` applies a scope filter only when `!AllScopes && Scope != ""`
   (`internal/store/spine.go:991`), so a class-only purge naming neither flag spans **every scope**.

5. **`SurfaceFields: {"scope","all-scopes","dry-run"}` resolves cobra to exactly `summarize-missing`.**
   Verified by registering the rule in a scratch copy and running `TestSurfaceConformanceCobraUsage`:
   the sole failure line named `command=summarize-missing`. Without the override the same run named
   all five. Probe reverted; `git status --porcelain` clean.

6. **The derived prose surface set under that `SurfaceFields` is `SurfaceDocsSite` alone.** Verified
   by running `TestSurfaceConformanceProseFiles` with the scratch rule: one violation line,
   `surface=docs_site`. `SurfaceSkill` drops out because neither SKILL.md mentions `dry-run`;
   `SurfaceProtoComment` drops out because `all-scopes` appears nowhere in the proto. Note that the
   pre-narrowing `SurfaceSkill` resolution was an artifact — `--all-scopes` occurs exactly once in
   `curating-memory/SKILL.md`, at line 388, **inside the unrelated `purge-filter-requires-scope`
   anchor's own rendered body** — so narrowing removes a spurious resolution rather than dropping a
   real one.

7. **`ValidateRules()` passes with the recommended `Sentence`** (no substring collision in either
   direction against the eight existing sentences).

8. **Composing the `Sentence` into a `Usage` string MOVES the goldens.** Verified: with the probe's
   Usage edit in place, `go test ./cmd/engram -run 'TestHelpGolden|TestCatalogGolden'` FAILED.
   `task surfaces:gen` regenerates them (`Taskfile.yaml:263` runs the same `-update` invocation CI
   does). Any acceptance criterion asserting the goldens do not move is wrong.

9. **RE-CONFIRM — `go test ./internal/keylinks/...` is GREEN.** It was green at replan time.

10. **`spine-review verify` already has rejection coverage** —
    `cmd/engram/spine_review_verify_test.go:526` `TestSpineReviewVerifyRequiresScopeOrAllScopes`.
    Only `summarize-missing` has none. (An earlier draft of this plan claimed two leaves were
    uncovered; that was wrong.)

## Decision record — why `SurfaceFields` diverges (addresses review finding H-1)

The previous draft of this plan foreclosed `SurfaceFields` and forbade touching the cobra `Usage`
strings. Registering that rule fails `TestSurfaceConformanceCobraUsage` on five commands. The three
alternatives were evaluated against the live tree and the two rejected ones are recorded here so the
next review cycle does not re-litigate them:

| Alternative | Verdict |
|---|---|
| No `SurfaceFields`; compose the `Sentence` into all five matched commands' `Usage` | **REJECTED.** Fact 4: false on `consolidate`, and on `purge` it misstates deletion blast radius. |
| Add the guard to `consolidate` / `purge` so the sentence becomes true | **REJECTED.** Out of phase scope — `08-CONTEXT.md` `<domain>` excludes runtime-behavior change — and impossible for `purge` regardless, whose exemption is deliberate (D-10). |
| Extend `ConditionalRule` with an explicit command allow-list | **REJECTED for this phase.** A registry-model change needs its own justification; filed as a follow-up instead. |
| `SurfaceFields` narrowing + a whitelist test for the leaves it cannot reach | **CHOSEN.** Fact 5/6. |

The residual — `spine-review consolidate` silently reporting zero candidates when neither flag is
supplied — is a real defect this phase does not fix. Task 3 files it.

## Cycle-2 review findings addressed here

| Finding | Where |
|---|---|
| **M-2.3** — Task 1's and Task 2's `<verify>` were already green without their artifacts | Both `<verify>` blocks now carry a red-before-work assertion: Task 1 requires a literal `=== RUN` line for the exact test name, Task 2 requires the anchor pair and the helper call that only it can create. Both measured RED on the current tree. |
| **M-2.4** — the internal-link loop silently skipped underscore/uppercase targets | The class in `<verification>` is widened to `[A-Za-z0-9/#_.-]`. Probed: the narrow form reported nothing for `](/guides/DOES-NOT-EXIST/#schema_version)`; the widened form reports `BROKEN /guides/DOES-NOT-EXIST`, with no new false positives on this page. |
| **L-2.3** — `requireSweepScope`'s `command` parameter was never used | Dropped. The signature is `requireSweepScope(scope string, allScopes bool) error`. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin the one genuinely uncovered rejection before anything is refactored</name>
  <files>cmd/engram/summarize_test.go</files>
  <read_first>
    - `cmd/engram/spine_review_test.go:47-59` — `TestSpineReviewScanRequiresScopeOrAllScopes`, the exact shape to mirror
    - `cmd/engram/spine_review_verify_test.go:523-535` — the sibling with the identical shape; note this leaf is ALREADY covered, so only `summarize-missing` needs a new test
    - `cmd/engram/summarize_test.go` — the whole file; note it has a mutual-exclusivity test but no required-rejection test, and note the `resetClientFlags` / `resetCommandFlagState` pairing its tests use
    - `cmd/engram/summarize.go:33-45` — the guard and what runs immediately after it, so the test reaches the guard rather than some earlier validation or a store dial
  </read_first>
  <behavior>
    - `summarize-missing` invoked with neither scope flag returns a non-nil error.
    - `exitCodeFromError` on that error is `exitUsage`.
    - The rejection arrives from the leaf's own guard, before `operatorOutputFormat` and before any
      store construction — so the test never touches the network.
  </behavior>
  <action>
Author the one piece of coverage the conversion needs and does not have. This task deliberately
references NO symbol that does not already exist on `main`: it must compile and pass against the
unmodified tree, because its entire value is being a characterization pin taken BEFORE the refactor.

Add `TestSummarizeMissingRequiresScopeOrAllScopes` to `cmd/engram/summarize_test.go`, mirroring
`TestSpineReviewScanRequiresScopeOrAllScopes` field for field: reset the client flags and the
command's own flag state, invoke `summarize-missing` with no scope flags, assert a non-nil error, and
assert `exitCodeFromError(err)` is `exitUsage`. Use the same `resetCommandFlagState` pairing the
other tests in that file use — the shared `rootCmd` singleton leaks flag state across tests in a
full-package run, and this is the established mitigation.

Do NOT assert on the message text here. The message is about to become a registry lookup, and an
inlined expected string in this file would become a fourth unregistered statement of the constraint.
Message equality across all three leaves is Task 3's job, asserted against the registry.

Do not reproduce the removed guard literal in this file — the Task 3 gate counts raw occurrences
under `cmd/engram/` and does not filter comments or test fixtures.
  </action>
  <verify>
    <automated>out=$(go test ./cmd/engram -run 'TestSummarizeMissingRequiresScopeOrAllScopes' -count=1 -v 2>&1); rc=$?; printf '%s\n' "$out"; test $rc -eq 0 && printf '%s\n' "$out" | rg -q '^=== RUN   TestSummarizeMissingRequiresScopeOrAllScopes$'</automated>
  </verify>
  <acceptance_criteria>
    - **The `<verify>` command asserts a MATCHED test, not merely a passing package** (cycle-2 review finding M-2.3). `go test -run 'Name'` exits 0 with `testing: warning: no tests to run` when `Name` matches nothing — measured on this tree, the bare command was GREEN before this task existed and would have stayed green if the test were written under a mistyped name. The command above therefore also requires a literal `=== RUN` line for the exact name. Note the separator is `=== RUN` followed by **three** spaces — that is go's format, confirmed by byte-inspecting `go test -v` output on this tree; do not collapse it to one. Confirmed RED today (the name does not exist) and GREEN in the same form against an already-present name.
    - `go test ./cmd/engram -run 'TestSummarizeMissingRequiresScopeOrAllScopes' -count=1 -v` shows a RUN/PASS pair for that exact name. A package-level `ok` alone is not evidence the name matched.
    - The test passes against the UNMODIFIED tree. Confirm by running it before any other file in this plan is touched, and state in the summary that it was green at that point — that is what makes it a characterization pin rather than a test written to the new behavior.
    - It goes RED when the behavior it pins is defeated: temporarily change `summarize.go`'s guard to return nil unconditionally, confirm the failure, restore, and record the observed red in the summary.
    - `rg -o 'surfaces[.]' cmd/engram/summarize_test.go | wc -l` reports `0` — this file references no registry symbol yet.
    - `go test ./cmd/engram/... -count=1` passes in full.
  </acceptance_criteria>
  <done>`summarize-missing` — the only sweep leaf with zero coverage of its neither-flag rejection — has a passing characterization test taken against unmodified `main`.</done>
</task>

<task type="tracer">
  <name>Task 2: End-to-end "the sweep constraint is declared once" — registry, generator, anchor, helper, and one leaf</name>
  <reversibility rating="costly">D-01/D-02: the `Sentence` becomes the published payload of an
  anchored region in docs-site, of three cobra `Usage` strings, and of the CLI rejection path.
  Unwinding means regenerating the anchor, reverting the goldens, and restoring the hand-rolled
  literals at each leaf.</reversibility>
  <precondition>`go tool buf --version` succeeds — `task surfaces:gen` chains `task proto:gen`, which
  invokes buf; without it the generator step cannot complete and the tree cannot be left clean.</precondition>
  <files>internal/surfaces/rules.go, internal/surfacesgen/main.go, cmd/engram/sweep_scope.go, cmd/engram/summarize.go, docs-site/src/content/docs/reference/tools.md, cmd/engram/testdata/help.golden, cmd/engram/testdata/catalog.golden</files>
  <read_first>
    - `internal/surfaces/rules.go:21-94` — the `ConditionalRule` field-by-field doc comment, ESPECIALLY the `SurfaceFields` comment at 48-67 and the `RuleDiscoveryNotSchedulable` precedent it describes
    - `internal/surfaces/rules.go:92-99` — `IsDeclared` and the `declared` provenance marker
    - `internal/surfaces/rules.go:149-162` — `RulePurgeFilterRequiresScope`'s doc-comment const, and its struct literal at `:263-271`
    - `internal/surfaces/rules.go:196-207` — `RuleDiscoveryNotSchedulable`'s literal, the ONE existing `SurfaceFields` override, for the house style of justifying a divergence in-comment
    - `internal/surfaces/rules.go:296-350` — `ValidateRules` / `validateRuleSet` in full, especially the Sentence-substring loop
    - `internal/surfaces/normalize.go:60-107` — `NormalizeField`, `ApplicableSurfaces`, `SurfaceApplicabilityFields`
    - `internal/surfaces/anchor.go` — `WriteRegion` (~162) and `writeFileAtomic` (~216); note `WriteRegion` ERRORS when the anchor pair is absent, it never creates one
    - `internal/surfaces/conformance_test.go:19-27` (`proseTargets`), `:43-59` (`exposedFileFields`), `:71-80` (`buildProseExposed`), `:113-134` (`checkProseSurface`)
    - `cmd/engram/surfaces_test.go:37-95` — `TestSurfaceConformanceCobraUsage`, the gate this rule's `SurfaceFields` exists to keep honest. Read the per-command loop at `:70-90`.
    - `internal/surfacesgen/main.go:100-123` — the `ruleTargets` entries for `RuleVerifyFailOnValues` and `RulePurgeFilterRequiresScope`, and their explanatory comments naming which surfaces do NOT resolve and why
    - `cmd/engram/spine_review_purge.go:81-93` — `requirePurgeFilterScope`, the proven CLI-side composition template (registry lookup, panic-on-missing, `usageErrorf("%s", rule.Sentence)`)
    - `cmd/engram/spine_review_purge.go:426-442` — the `init()` that looks the rule up once and appends `scopeRule.Sentence` to three flag `Usage` strings, with a panic-on-missing comment. This is the exact Usage-composition template.
    - `cmd/engram/summarize.go:33-45` and `:113-125` — the guard, and the `init()` flag registrations to compose into
    - `docs-site/src/content/docs/reference/tools.md:124-130` — the existing `purge-filter-requires-scope` anchor, for the exact on-page anchor formatting (body inline, trailing period OUTSIDE the closing comment)
    - `docs-site/src/content/docs/reference/tools.md:506-535` — the `## CLI: summarize-missing` block, including the standalone bold line at ~515 this task replaces
    - `Taskfile.yaml:245-264` — what `surfaces:gen` chains, including the golden `-update` step
    - `.github/workflows/ci.yaml:291-298` — CI's `surfaces` job, to confirm `task surfaces:gen` reproduces it exactly
  </read_first>
  <action>
Prove one full path through the machinery — registry to generated prose to a live rejection to a
help string — before widening to the other two leaves.

**Step 1 — declare the rule.** Add an exported ID const `RuleSweepScopeOrAllScopesRequired` with the
value `sweep-scope-or-all-scopes-required` (the Pascal-to-kebab convention every other rule follows),
carrying a doc comment in the house style. That comment MUST record, in prose:

- what the rule constrains and which three leaves enforce it;
- why `Fields` is the flag pair (it drives `field=scope` attribution on the error envelope);
- **why `SurfaceFields` diverges** — the five-command enumeration in this plan's fact table, the
  closed proof in fact 3 that no field set selects the three enforcing leaves, and the two concrete
  behaviors in fact 4 that make publishing onto `consolidate`/`purge` false. Name
  `internal/store/spine.go:991` for the purge case; that citation is the load-bearing one.
- that the two leaves `SurfaceFields` cannot reach are pinned by
  `TestSweepLeavesUsageStatesRegisteredRule` in `cmd/engram` instead — the explicit whitelist the
  field-set model cannot express;
- why `TagForm` is left empty.

**APPEND** the struct literal to the tail of the `rules` slice. `Rules()` returns declaration order
and both conformance gates iterate it, so appending leaves every existing rule's position — and
therefore every gate's failure-report ordering — untouched. (Note for the record: each generated
region is keyed by its own rule ID and rewritten independently by `WriteRegion`, so slice position
does NOT churn downstream regions; appending is a convention and an ordering-stability choice, not a
regeneration requirement.)

Field values, per D-02 and this plan's decision record:

- `Fields: []string{"scope", "all-scopes"}`
- `SurfaceFields: []string{"scope", "all-scopes", "dry-run"}`
- `Hint: "conditional_required"` (matching `RulePurgeFilterRequiresScope`, the closest precedent)
- `TagForm` empty, with the sibling-matching comment: no MCP arg struct carries an `all_scopes` field
  on any schema, so there is no jsonschema tag to compress the statement into.

The `Sentence` is **flag-shaped** per D-02, ASCII-only, and must satisfy the substring constraint in
BOTH directions against all eight existing sentences. The recommended text, verified at replan time
against every registered sentence by actually running `ValidateRules`, is:

`a sweep requires an explicit --scope or --all-scopes: name one scope, or opt into every scope`

You may reword within D-02's constraints, but if you do, re-run `go test ./internal/surfaces -run
TestValidateRules -count=1` immediately and before touching any other file.
`RulePurgeFilterRequiresScope`'s sentence also opens on this flag pair, so a careless rephrasing that
happens to nest inside it fails the entire registry, not just the new rule.

**Step 2 — hand-write the anchor pair.** `WriteRegion` fills regions; it never creates them, and it
returns an error when the pair is absent. Write an EMPTY pair into
`docs-site/src/content/docs/reference/tools.md`, inside the `## CLI: summarize-missing` block,
replacing the standalone bold line around 515 that currently states the requirement in hand-written
prose. Match the existing anchor at `:128` exactly: the generated body renders as bare text with no
added punctuation, so any trailing period sits OUTSIDE the closing anchor comment, and the anchor
needs a lead-in clause so it reads as a sentence rather than a dropped-in fragment.

ONE anchored file, not three. Per fact 6 the derived prose surface is `SurfaceDocsSite` alone, and
`checkProseSurface` requires only one anchored file per surface. Do NOT anchor in
`guides/cli.md` — its sweep subsections sit adjacent to `### spine-review consolidate`
(`guides/cli.md:227`), a command this rule deliberately does not reach. Do NOT anchor in either
SKILL.md — neither mentions `dry-run`, so the skill surface does not resolve, and the earlier
apparent resolution was an artifact of another rule's rendered anchor body.

**Step 3 — add the generator entry.** Add a `ruleTargets` entry keyed by the exported const (never a
bare ID string — every existing entry keys by the const), listing the one file with `kindMarkdown`.
Carry an explanatory comment in the house style, matching the `RuleVerifyFailOnValues` entry's shape
at `:104-110`: state that there is no proto anchor because `all_scopes` is not a proto field on any
message, no skill anchor because neither SKILL.md mentions this rule's `SurfaceFields`, and no
`guides/cli.md` anchor because the narrowed applicability set does not reach the commands documented
in the sections adjacent to where it would land.

**Step 4 — convert `summarize-missing`, end to end.** Create `cmd/engram/sweep_scope.go` with the
Apache-2.0 SPDX header every Go file in this repo carries, holding:

- `sweepScopeRule() surfaces.ConditionalRule` — the single registry lookup, panicking with the
  house-style message naming the registry const when the lookup fails, mirroring
  `spine_review_purge.go`'s two panic sites. One lookup site, shared by the helper and by the three
  `init()` Usage compositions, so the composition cannot drift from the rejection.
- `requireSweepScope(scope string, allScopes bool) error` — returns nil when the scope is
  non-empty or all-scopes is set; otherwise returns `usageErrorf("%s", sweepScopeRule().Sentence)`.
  One shared helper rather than three near-identical per-file copies: the whole point of the change
  is that the constraint is stated once. Keep the emptiness check — NOT a cobra `Changed` check; see
  the threat register. **Two parameters.** Do not add a leading `command` parameter: the returned
  message must equal the registered `Sentence` exactly, so a per-command string has nowhere to go,
  and an accepted-but-unused parameter on the change's single composition point advertises a
  capability the helper does not have (cycle-2 review finding L-2.3).

Rewire `cmd/engram/summarize.go`'s `RunE` guard to call it, preserving its position: it must still
run before `operatorOutputFormat` and before `server.StoreAndSummarizerFromEnv()`, so a missing scope
is a usage rejection rather than a connection failure.

**Step 5 — compose the sentence into `summarize-missing`'s help.** In `summarize.go`'s `init()`,
append `"; "+sweepScopeRule().Sentence` to the `--all-scopes` flag's `Usage`, exactly as
`spine_review_purge.go:436-442` composes its own rule's sentence. This is required, not optional:
`SurfaceFields` resolves the cobra lane to this command, and `TestSurfaceConformanceCobraUsage`
demands one of its flag `Usage` strings contain the `Sentence` verbatim. Leave the existing
"mutually exclusive with `--scope`" clause in place — `TestEveryScopeAllScopesPairHasAFlagGroup`
asserts on it.

**Do not reproduce the removed literal anywhere under `cmd/engram/`** — not in a code comment, not in
a test fixture, not in a commemorative note about what it replaced. The Task 3 gate counts raw
occurrences under that directory and does not filter comments, so a commemorative mention turns a
correct implementation red. If the change needs explaining, name the file and line it came from.

**Step 6 — generate.** Run `task surfaces:gen`, never the bare `go run ./internal/surfacesgen`. That
target IS CI's `surfaces` job sequence — generator, `proto:gen` (buf generate plus the vendored TS
re-copy), and the golden `-update` run. **The goldens WILL move**, because both store flag `Usage`
text verbatim; that is expected here, not a signal something else changed. Then run it a SECOND time
and confirm it is a fixed point (see the acceptance criteria for the exact form — do not use
`git diff --exit-code` against `HEAD` for this, which measures uncommitted work rather than
generator stability).
  </action>
  <verify>
    <automated>test "$(rg -o -F 'engram:rule:start sweep-scope-or-all-scopes-required' docs-site/src/content/docs/reference/tools.md | wc -l | tr -d ' ')" = "1" && test "$(rg -o 'requireSweepScope[(]' cmd/engram/summarize.go | wc -l | tr -d ' ')" = "1" && go test ./internal/surfaces/... -count=1 && go test ./cmd/engram -run 'TestSummarizeMissingRequiresScopeOrAllScopes|TestSurfaceConformanceCobraUsage|TestEveryScopeAllScopesPairHasAFlagGroup|TestHelpGolden|TestCatalogGolden' -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - **The `<verify>` command is falsifiable before the work, not after** (cycle-2 review finding M-2.3). Every `go test` name in it — `TestSurfaceConformanceCobraUsage`, `TestEveryScopeAllScopesPairHasAFlagGroup`, `TestHelpGolden`, `TestCatalogGolden`, and the whole `./internal/surfaces/...` package — ALREADY PASSES on `main`, so the test half of this command was vacuously green and would have stayed green if this task did nothing. The two `rg` assertions in front of it are the red-before-work half: they check the two artifacts only this task can create — the anchor pair in `reference/tools.md` and the helper call in `summarize.go`. Both were measured RED on the current tree (exit 1 each) during replanning. Run them first and record both observed exits in the summary.
    - `test "$(rg -o 'RuleSweepScopeOrAllScopesRequired' internal/surfaces/rules.go | wc -l | tr -d ' ')" -ge 3` — the doc comment's own mention, the const declaration, and the registry literal's reference. The four existing precedents report 3, 4, 4 and 5, so a floor is the correct shape; an exact count would forbid the house-style doc comment this task mandates.
    - `go test ./internal/surfaces -run TestValidateRules -count=1 -v` passes, proving the new `Sentence` collides with no other rule's in either direction. To see this gate RED, temporarily set the new `Sentence` to a phrase nested inside `RulePurgeFilterRequiresScope`'s (for example, its own opening clause verbatim) and confirm `validateRuleSet` reports a Sentence-substring collision; restore before committing and record the observed red in the summary.
    - `go test ./internal/surfaces -run TestSurfaceConformanceProseFiles -count=1 -v` passes. To see it RED, hand-edit the anchored body by a single character and confirm the gate reports the divergence; restore before committing.
    - **The applicability set is the derived one, proven by the gate's own output, not by assertion.** `go test ./cmd/engram -run TestSurfaceConformanceCobraUsage -count=1 -v` passes; and with `summarize-missing`'s `--all-scopes` Usage composition temporarily removed, the SAME run fails with exactly ONE `command=` line naming `summarize-missing` — never `consolidate`, `purge`, `scan` or `verify`. Restore, and record both observations in the summary. This is the single most important criterion in the plan: it is what proves the rule does not over-publish.
    - `test "$(rg -o -F 'engram:rule:start sweep-scope-or-all-scopes-required' docs-site/src/content/docs/reference/tools.md | wc -l | tr -d ' ')" = "1"` and the same pattern reports `0` in each of `docs-site/src/content/docs/guides/cli.md`, `skill/engram/skills/curating-memory/SKILL.md`, and `skill/engram/skills/discovering/SKILL.md`.
    - `test "$(rg -o 'surfaces[.]RuleByID[(]surfaces[.]RuleSweepScopeOrAllScopesRequired[)]' cmd/engram/sweep_scope.go | wc -l | tr -d ' ')" = "1"` — one lookup site in the whole package, shared by the rejection and the Usage compositions.
    - `test "$(rg -o 'requireSweepScope[(]' cmd/engram/summarize.go | wc -l | tr -d ' ')" = "1"`, and `rg -o -F -- '--scope <scope> or --all-scopes is required' cmd/engram/summarize.go | wc -l` reports `0`. It reports `1` against `main`, so this gate's ability to fail is already established.
    - **Regeneration is a fixed point.** Run: `task surfaces:gen; A=$(git diff -- proto/ docs-site/ skill/ gen/ ui/src/lib/gen/ cmd/engram/testdata/ | shasum); task surfaces:gen; B=$(git diff -- proto/ docs-site/ skill/ gen/ ui/src/lib/gen/ cmd/engram/testdata/ | shasum); test "$A" = "$B"`. This compares the second run against the post-first-run state, which is the property actually claimed. `git diff --exit-code` against `HEAD` is NOT usable here — the tree legitimately carries this task's own uncommitted generated output, so it would false-RED. The clean-tree form belongs after the commit; see `<verification>`.
    - The goldens DID move, and the move is confined to the `--all-scopes` Usage text: `git diff -U0 -- cmd/engram/testdata/ | rg -c '^[+-][^+-]'` is non-zero, and every changed line mentions `all-scopes` or the new sentence. Read the diff and confirm; a golden change anywhere else means something other than the Usage composition changed and needs explaining before it is committed.
    - `task license:check` passes with the new `cmd/engram/sweep_scope.go` carrying the SPDX header, and `git diff --exit-code go.mod go.sum` exits 0.
  </acceptance_criteria>
  <done>The constraint is declared once, anchored on the one surface that derives, generated rather than hand-written, and enforced AND published at one leaf through the registry — the whole path from `rules.go` to a live rejection and a live `--help` line works end to end.</done>
</task>

<task type="auto">
  <name>Task 3: Convert the remaining two leaves, pin all three against the registry, and close the gate at zero</name>
  <reversibility rating="costly">D-01: the conversion set is all sweep leaves, including
  `spine-review verify`, which neither the requirement text nor issue #480 names. Reverting means
  restoring three hand-rolled literals, three Usage strings, and the regenerated goldens.</reversibility>
  <files>cmd/engram/spine_review_scan.go, cmd/engram/spine_review_verify.go, cmd/engram/sweep_scope_test.go, cmd/engram/testdata/help.golden, cmd/engram/testdata/catalog.golden</files>
  <read_first>
    - `cmd/engram/sweep_scope.go` — the helper and `sweepScopeRule()` Task 2 created, and their call convention
    - `cmd/engram/summarize.go` — the converted leaf and its `init()` Usage composition, as the shape to replicate
    - `cmd/engram/spine_review_scan.go:30-50` — the `RunE` guard and the comment that names this exact follow-up (delete that comment; it is the TODO this task closes), plus `:155-165` for the flag registration
    - `cmd/engram/spine_review_verify.go:615-630` — the guard; note this file already imports `internal/surfaces` for `RuleVerifyFailOnValues` — plus `:660-670` for the flag registration
    - `cmd/engram/spine_review_test.go:47-59` and `cmd/engram/spine_review_verify_test.go:523-535` — the two existing rejection tests that must keep passing on unchanged inputs
    - `cmd/engram/flaggroup_test.go:490-524` — `TestEveryScopeAllScopesPairHasAFlagGroup`, to confirm the amended Usage strings still satisfy it (it looks for the substring `mutually exclusive`, which the compositions preserve)
    - `cmd/engram/catalog.go` — `collectFlags` and `commandKey`, for how the new whitelist test addresses a nested leaf (`spine-review scan`, not `scan`)
  </read_first>
  <behavior>
    - All three sweep leaves, invoked with neither scope flag, return an error whose `exitCodeFromError` is `exitUsage` AND whose message EQUALS the registered rule's `Sentence`, looked up through `surfaces.RuleByID` inside the test rather than inlined.
    - `--scope ""` (flag PRESENT, value empty) is the same behavior class: same exit code, same message, at all three leaves.
    - `requireSweepScope` itself returns nil for `("x", false)` and for `("", true)`, and non-nil for `("", false)` — tested directly, with no command invocation and therefore no store dial.
    - Each of the three leaves' `--all-scopes` flag `Usage` contains the registered `Sentence`, and `spine-review consolidate`'s and `spine-review purge`'s do NOT.
  </behavior>
  <action>
Convert the two remaining leaves, then pin the whole contract against the registry.

**Convert.** Rewire `cmd/engram/spine_review_scan.go` and `cmd/engram/spine_review_verify.go` onto
`requireSweepScope`, exactly as `summarize.go` was converted in Task 2, preserving each guard's
position in its `RunE` body — it must still run before `operatorOutputFormat` and before any store
construction. Then compose `"; "+sweepScopeRule().Sentence` into each one's `--all-scopes` flag
`Usage` in its `init()`, the same way Task 2 did for `summarize-missing`.

Per D-01 this set is **all** sweep leaves, not the two the requirement text and issue #480 name.
`spine-review verify` postdates the issue and was never added to its list; leaving it hand-rolled
would preserve an identical unregistered statement outside the conformance gate, one site smaller —
which is precisely the drift the change exists to close.

Do NOT touch `spine-review consolidate` or `spine-review purge`. Neither enforces this constraint;
see this plan's decision record and the prohibition block for why composing the sentence into either
one's help would be a false statement.

Before finalizing, re-run the occurrence scan yourself rather than trusting the count recorded at
plan time. If it finds a leaf beyond the three named here, convert that one too and note the
divergence in the summary. The gate asserts zero remaining, not three converted, exactly so that a
leaf added between planning and execution fails loudly instead of slipping through.

**Pin.** Create `cmd/engram/sweep_scope_test.go` with the SPDX header, holding four tests:

`TestRequireSweepScope` — a direct table test of the helper, no command invocation: `("", false)`
returns a non-nil error whose message equals the registered `Sentence` and whose
`exitCodeFromError` is `exitUsage`; `("x", false)` and `("", true)` both return nil. Testing the
positive cases here rather than through a command invocation is deliberate: a command-level
positive control falls through to `server.StoreAndSummarizerFromEnv()` / `spineScanStoreFromEnv()`
and may dial a real Qdrant, which would make the suite environment-dependent. Without these
positive cases the negative tests would still pass if the helper rejected everything unconditionally.

`TestSweepLeavesRejectMissingScopeIdentically` — for each of the three leaves, invoke it with no
scope flags and assert the exit code is `exitUsage` AND the error message equals the registered
rule's `Sentence`, looked up through `surfaces.RuleByID(surfaces.RuleSweepScopeOrAllScopesRequired)`
inside the test. Comparing against the looked-up value rather than an inlined string is the point:
it makes the test incapable of passing while the leaves disagree with the registry. Address the two
nested leaves through their real invocation path (`runClient(t, "spine-review", "scan")`), not by a
name string.

`TestSweepLeavesRejectPresentButEmptyScope` — for each leaf, invoke it with `--scope` supplied and
its value empty, and assert the identical outcome. This is the case the leaves are not currently
tested for at all, and the one where they could silently diverge: cobra records a present-empty flag
as `Changed`, so a leaf that switched to a `Changed`-based check instead of an emptiness check would
accept an empty scope and sweep against a selector that matches nothing. Assert on the message too,
so a rejection arriving from cobra's mutual-exclusion machinery instead of from the guard cannot
pass as a green.

`TestSweepLeavesUsageStatesRegisteredRule` — the whitelist the field-set model cannot express, and
the reason this plan's `SurfaceFields` narrowing does not cost coverage. Walk the live command tree,
resolve each of the three leaves by its `commandKey`, and assert its `--all-scopes` flag `Usage`
contains the registered `Sentence`. In the SAME test, assert the inverse for
`spine-review consolidate` and `spine-review purge`: their `--all-scopes` Usage must NOT contain it.
Both halves matter — the positive half replaces what the conformance gate cannot reach, and the
negative half is the executable form of this plan's prohibition, so a future edit that "helpfully"
adds the sentence to a non-enforcing command fails here. Fail with the command key named, so the
report says which leaf drifted.

Do not reproduce the removed guard literal in any of these files.

**Regenerate and reproduce CI.** Run `task surfaces:gen` — the two new Usage compositions move the
goldens again. That single target IS CI's `surfaces` job: generator, `proto:gen`, the vendored TS
re-copy, and the golden `-update` run. Do not hand-assemble a partial substitute for it; an earlier
draft of this plan omitted three of its steps, including the `-update` that is the only thing that
can detect golden drift.

**File the residual.** `spine-review consolidate` exposes both flags, enforces neither, and reports
zero candidates when given neither — an operator reasonably reads that as "no duplicates". This
phase deliberately does not change it. Open a GitHub issue for it (check first with
`gh issue list --search 'consolidate scope' --state open` so a re-run does not duplicate), naming
`cmd/engram/spine_review_consolidate.go`'s `RunE`, `internal/store/spine.go:384-387`, and this
plan's decision record. Record the issue number in the summary. Also note in the summary that #480
is closable by this plan.
  </action>
  <verify>
    <automated>test "$(rg -o -F -- '--scope <scope> or --all-scopes is required' cmd/engram/ | wc -l | tr -d ' ')" = "0" && go test ./cmd/engram/... ./internal/surfaces/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `rg -o -F -- '--scope <scope> or --all-scopes is required' cmd/engram/ | wc -l` reports `0`. This gate is RED today by construction — it reports `3` against `main` — so its ability to fail is already established; state the observed before-count in the summary. The gate is scoped to `cmd/engram/` and deliberately does not sweep `docs/superpowers/plans/`, which carries an archived, out-of-scope copy.
    - `test "$(rg -o 'requireSweepScope[(]' cmd/engram/ | wc -l | tr -d ' ')" -ge 4` — one declaration plus at least one call per sweep leaf. A floor, not an equality: the binding invariant is the zero-occurrence gate above, and an equality here would contradict this task's own instruction to convert a fourth leaf if one is found.
    - `go test ./cmd/engram -run 'TestRequireSweepScope|TestSweepLeaves|RequiresScopeOrAllScopes|TestEveryScopeAllScopesPairHasAFlagGroup' -count=1 -v` shows a RUN/PASS pair for every one of `TestRequireSweepScope`, `TestSweepLeavesRejectMissingScopeIdentically`, `TestSweepLeavesRejectPresentButEmptyScope`, `TestSweepLeavesUsageStatesRegisteredRule`, `TestSummarizeMissingRequiresScopeOrAllScopes`, `TestSpineReviewScanRequiresScopeOrAllScopes` and `TestSpineReviewVerifyRequiresScopeOrAllScopes` — a package-level `ok` alone is not evidence the names matched. No change to `flaggroup_test.go` is required.
    - `test "$(rg -o 'surfaces[.]RuleByID[(]surfaces[.]RuleSweepScopeOrAllScopesRequired[)]' cmd/engram/sweep_scope_test.go | wc -l | tr -d ' ')" -ge 1` — the expected message is derived from the registry, not inlined.
    - Both halves of the whitelist test go RED on their own failure mode, observed and recorded: (a) delete the Usage composition from `spine_review_verify.go` and confirm the failure names `spine-review verify`; (b) append the `Sentence` to `spine_review_consolidate.go`'s `--all-scopes` Usage and confirm the failure names `spine-review consolidate`. Restore both before committing.
    - The rejection tests go RED when the guard is defeated: temporarily change one leaf's guard to return nil unconditionally and confirm a failure naming that leaf; restore and record the observed red in the summary.
    - Regeneration is a fixed point, by the same two-run shasum comparison Task 2 specifies.
    - `go test ./cmd/engram/... ./internal/surfaces/... -count=1` passes in full, and `task` (lint plus the full suite) is green.
    - `go test ./internal/keylinks/... -count=1` is green, and `git diff --exit-code go.mod go.sum` exits 0.
  </acceptance_criteria>
  <done>Every sweep leaf composes the declared rule in both its rejection and its help text; no hand-rolled copy survives under `cmd/engram/`; the two commands that expose the flags without enforcing the rule are provably untouched; issue #480 is closable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator argv → sweep `RunE` | Operator-supplied `--scope` / `--all-scopes` values cross into a command that can sweep every scope in the collection |
| rule registry → operator terminal | The declared `Sentence` becomes text rendered into an operator's terminal on rejection and in `--help` |
| rule registry → generated prose | The same `Sentence` is written verbatim into a committed documentation file by the generator |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Elevation of Privilege | the three sweep `RunE` guards | medium | mitigate | A refactor of an input-validation rejection can silently widen what it accepts. The narrowing case that matters is `--scope ""`: cobra records a present-empty flag as `Changed`, so a conversion that switched from an emptiness check to a `Changed` check would accept an empty scope and sweep against a scope selector that matches nothing, with no rejection. Task 1 pins `summarize-missing`'s rejection against unmodified `main` before anything is refactored; Task 3's `TestSweepLeavesRejectPresentButEmptyScope` pins the emptiness semantics at all three leaves, and `TestRequireSweepScope`'s positive rows prove the guard is not rejecting unconditionally instead. |
| T-08-02 | Spoofing | the `--all-scopes` `Usage` strings on `spine-review consolidate` and `spine-review purge` | high | mitigate | Publishing "a sweep requires an explicit `--scope` or `--all-scopes`" into a command that does not enforce it tells an operator a bare invocation is refused when it is not. On `purge` this is a deletion-blast-radius misstatement: `internal/store/spine.go:991` applies a scope filter only when `!AllScopes && Scope != ""`, so a class-only purge naming neither flag spans EVERY scope — the operator would believe the command had refused. This severity is at the configured blocking threshold, so it is a mitigate, never an accept. Mitigation is the `SurfaceFields` narrowing (which keeps the conformance gate from demanding the text there) plus the NEGATIVE half of `TestSweepLeavesUsageStatesRegisteredRule`, which fails if a future edit adds the sentence to either command. |
| T-08-03 | Denial of Service | `sweepScopeRule()`'s panic-on-unregistered-rule | low | accept | The panic mirrors the shipped `requirePurgeFilterScope` and `spine_review_purge.go` `init()` precedents and is unreachable in a built binary: the rule const and the registry literal are in the same package, `ValidateRules` covers the registry, and `TestValidateRules` runs in CI. A reachable panic here would require deleting the registry entry while keeping the const, which does not compile past the literal's own reference. |
| T-08-04 | Repudiation | the anchored region in `reference/tools.md` | low | mitigate | Hand-editing inside the anchor would let documentation state a constraint the binary does not enforce, with no signal. `TestSurfaceConformanceProseFiles` compares the region body for exact equality with the registered `Sentence`, and CI's `surfaces` job re-runs the generator and diffs the tree, so a hand-edit fails the PR rather than shipping. |
| T-08-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package in any ecosystem and runs no package manager. `08-RESEARCH.md` records the Package Legitimacy Audit as not applicable; `git diff --exit-code go.mod go.sum` is expected to exit 0 and is an acceptance criterion on Tasks 2 and 3. |
</threat_model>

<verification>
Run these AFTER this plan's work is committed — several are clean-tree checks that legitimately
false-RED at a task boundary.

- `task` (lint plus the full Go and Python suite) is green.
- `go test ./internal/keylinks/... -count=1` is green.
- The zero-occurrence gate reports `0`: `rg -o -F -- '--scope <scope> or --all-scopes is required' cmd/engram/ | wc -l`. It reported `3` before the change; state both numbers in the summary.
- CI's `surfaces` job reproduces clean locally on the COMMITTED tree: `task surfaces:gen && git diff --exit-code -- proto/ docs-site/ skill/ gen/ ui/src/lib/gen/ cmd/engram/testdata/`. `task surfaces:gen` is that job's exact sequence — do not substitute a hand-assembled subset of it.
- The applicability set is the derived one and is recorded in the summary as such: `cobra_usage → summarize-missing only`, `docs_site → tools.md`, `skill → does not resolve`, `proto_comment → does not resolve`, each with the observation that produced it.
- `task license:check` passes (the two new Go files carry the SPDX header; no `.planning/**` or `docs-site/**` file gained one).
- `git diff --exit-code go.mod go.sum` exits 0.
- Internal links on the edited page still resolve: `for l in $(rg -o '\]\((/[A-Za-z0-9/#_.-]+)\)' docs-site/src/content/docs/reference/tools.md | rg -o '\(/[A-Za-z0-9/#_.-]+\)' | tr -d '()' | sed 's/#.*//' | sed 's:/$::' | sort -u); do p="docs-site/src/content/docs${l}"; [ -f "$p.md" ] || [ -f "$p.mdx" ] || [ -f "$p/index.md" ] || echo "BROKEN $l"; done` prints nothing. Note this check exists because nothing else validates internal links — see 08-02's and 08-03's notes on the docs lane's gate coverage. The character class is `[A-Za-z0-9/#_.-]`, widened from `[a-z0-9/#-]` per cycle-2 review finding M-2.4: the narrow class omitted `_`, so a link was skipped BEFORE the `sed 's/#.*//'` fragment strip ever ran, and this page's own `](/reference/tools/#supersede_memory)` was silently not being checked. Do not narrow it back.
</verification>

<success_criteria>
- `RuleSweepScopeOrAllScopesRequired` is a declared `surfaces.ConditionalRule` reused by every sweep leaf, in the rejection path and in the `--all-scopes` help text.
- Its canonical sentence is anchored and conformance-gated on the surface `ApplicableSurfaces` resolves it to, with the derived set recorded in the summary alongside the observation that produced it.
- The rule provably does NOT resolve onto `spine-review consolidate` or `spine-review purge`, and a test fails if a future edit publishes it there.
- No hand-rolled copy of the constraint survives under `cmd/engram/`, and the gate that proves it asserts zero rather than a count.
- The rejection fires on exactly the inputs it fired on before, including the present-but-empty `--scope` case, proven at all three leaves.
</success_criteria>

<output>
Create `.planning/phases/08-registry-docs-tail/08-01-SUMMARY.md` when done
</output>
