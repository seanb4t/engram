---
phase: 08-registry-docs-tail
plan: 04
type: execute
wave: 2
depends_on: ["08-03"]
files_modified:
  - CLAUDE.md
autonomous: true
requirements: [REQ-claude-md-migrations-convention]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "CLAUDE.md's `Not used here` bullet no longer excludes database migrations outright. It states what this milestone actually ships — schema-version-driven payload migrations through a registered step chain — and names the deliberate boundary: the owner-remap, summary-fill, and embedder-reindex sweeps are NOT part of that mechanism (D-05.1, SC3, REQ-claude-md-migrations-convention)."
    - "CLAUDE.md's `cmd/engram/` layout row names every command the live cobra tree exposes, derived from the generated self-describe catalog rather than from a hand-kept list — so the routing doc cannot silently fall behind the binary again (D-05.2)."
    - "CLAUDE.md's Memory contract section names `schema_version` and `archived_at`, which appear nowhere in the file today, and states the record-state vocabulary — `archived`, `superseded`, `expired`, `scheduled` — that Phases 5 through 7 shipped (D-05.3)."
    - "Every addition states the CURRENT contract. CLAUDE.md is normative routing, not a changelog: no addition narrates what changed, in which release, or which phase shipped it (D-05 constraint)."
    - "The file keeps its existing table-and-bullet idiom and density. This is an edit to a routing doc, not an expansion of it — the Layout table stays a table, the Conventions list stays a bullet list, and the Memory contract stays prose paragraphs."
    - "CLAUDE.md gains no SPDX header. `.licenserc.yaml` owns scope and `task license:check` is the arbiter; the file is unchanged in that respect."
    - statement: "An agent reading only CLAUDE.md forms an accurate picture of which operator command to reach for, without needing to open `cmd/engram/` to discover a command the routing doc omitted — verified by reading the revised file against the catalog, not by an assertion."
      verification: backstop
  artifacts:
    - path: "CLAUDE.md"
      provides: "The project's normative routing doc, brought into agreement with the shipped command surface and record-state vocabulary"
      contains: "schema_version"
  key_links:
    - from: "CLAUDE.md"
      to: "docs-site/src/content/docs/guides/migrate.md"
      via: "the migration convention routing the reader to the operator procedure, mirroring how the reindex entry already routes"
      pattern: "guides[/]migrate"
  prohibitions:
    - statement: "The revised migrations line MUST NOT overclaim scope. Stating or implying that the owner-remap, summary-fill, or embedder-reindex sweeps are part of the schema-version mechanism would tell an agent that the migration status report accounts for work it deliberately does not track, and would send it to the wrong command for a recovery it needs to perform. The boundary is as load-bearing as the claim."
    - statement: "CLAUDE.md MUST NOT become a changelog. It is the instruction set every agent working in this repo reads first; narrating history there displaces routing signal with archive, and an agent that has to date-reason about which sentence is current is reading an unreliable doc."
---

<objective>
Bring CLAUDE.md into agreement with what this milestone shipped: the migrations convention that
currently contradicts the code, the command inventory that omits four families, and the Memory
contract section that never mentions the schema-version stamp or the archived state (D-05,
REQ-claude-md-migrations-convention).

Purpose: CLAUDE.md is the normative routing doc every agent reads before touching this repo. A line
asserting the project does not use database migrations, in a repo that just shipped a migration
registry, a sweep, a status histogram, and a revert, is not a stale detail — it actively routes
agents away from the mechanism.

Output: a routing doc that describes the current surface, at its existing density.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-registry-docs-tail/08-CONTEXT.md
@CLAUDE.md
</context>

## Artifacts this phase produces

This plan creates no new file and no new code symbol. It edits three regions of one existing file:
the `cmd/engram/` row of the Layout table, the final bullet of the Conventions list, and the Memory
contract section.

## Note on editing your own instruction file

CLAUDE.md is both an instruction you are following and the artifact you are editing. Read it as
subject here. Its own rules still bind you while you work — Conventional Commits, branch-and-PR,
`task` before claiming done — and nothing in this plan changes any of them.

<tasks>

<task type="auto">
  <name>Task 1: The migrations convention and the command inventory</name>
  <reversibility rating="reversible">Documentation only; a single-file revert restores it (D-05).</reversibility>
  <files>CLAUDE.md</files>
  <read_first>
    - `CLAUDE.md` — the whole file, as subject: the Layout table (12-20), the Conventions bullet list (22-70), and the Memory contract section that follows
    - `cmd/engram/testdata/catalog.golden` — the generated self-describe catalog; its top-level `"name"` entries at six-space indentation are the authoritative command inventory
    - `cmd/engram/migrate_family.go:248-286` and `:605-618` — the three migrate verbs and their one-line summaries
    - `cmd/engram/client_migration_status.go:18-30` — the Connect-lane status sibling
    - `.planning/REQUIREMENTS.md` — the Migration Mechanism section and the Out of Scope table, particularly the row explaining why the owner-remap, summary-fill, and embedder-reindex sweeps are deliberately excluded from the registry
    - `internal/migrate/migrate.go` and `internal/migrate/step.go` — what the mechanism actually is: an ordered registry of additive-only steps, each declaring reversibility
    - `docs-site/src/content/docs/guides/migrate.md` — the guide `08-03` wrote, which this file should route to exactly as the existing row routes to the reindex guide
  </read_first>
  <action>
Two edits, both in the top third of the file.

**The Conventions bullet.** The final bullet of the Conventions list currently lists three things the
project does not use, the first of which is now false. Revise it so it states what this milestone
ships and where its boundary lies. It must convey four things, in the file's own compressed voice:

1. This project DOES have payload migrations, and they are schema-version-driven: an ordered registry
   of additive-only steps in `internal/migrate`, each of which must declare its reversibility, swept
   by `engram migrate`.
2. Nothing runs automatically — never on startup, and the mutating verbs preview by default.
3. The boundary, stated positively rather than as a list of exclusions where possible: the registry
   covers version-driven payload evolution only. The three other sweep commands key off an IdP claim
   change, ongoing async summary fill, and embedder config identity respectively, none of which is
   version-driven, so none is in the registry and none appears in the status histogram. Naming them
   is fine and useful; implying they are part of the mechanism is the failure this bullet exists to
   prevent.
4. A pointer to the operator guide, in the same shape the `cmd/engram/` row already uses for the
   reindex guide.

The other two items the bullet excludes are still accurate and should survive in whatever form the
revision takes. If the result is too long for one bullet, split it — a migrations entry alongside a
shorter not-used-here entry is fine, and matches how the surrounding bullets are sized.

**The `cmd/engram/` Layout row.** It names three core verbs plus a parenthesized operator-command
list that predates four families. Bring it current against the generated catalog. Missing today:
the whole `migrate` family (`migrate`, `migrate status`, `migrate revert`), `migration-status`, and
the whole `spine-review` family (`scan`, `verify`, `consolidate`, `purge`, `archive`, `restore`).
`get` is also absent from the row as a command, though the string appears elsewhere in the file.

Do not expand the row into a table of its own or into a per-command description list — it is one
cell in a Layout table and its job is routing, not reference. Group the two families by their parent
verb so the row stays readable at its current length, the way the existing text groups by
responsibility. Where a command has an operator guide, point at it in the same shape the row already
uses for reindex.

Derive the inventory from `cmd/engram/testdata/catalog.golden`, not from memory or from the previous
row. That file is regenerated from the live cobra tree, so it cannot disagree with the binary.

State the current contract. Do not write which release or phase added anything, and do not mark
anything as new.
  </action>
  <verify>
    <automated>missing=""; while IFS= read -r n; do rg -qF -- "$n" CLAUDE.md || missing="$missing|$n"; done < <(rg -o '^      "name": "[a-z][a-z -]*"' cmd/engram/testdata/catalog.golden | rg -o '"[a-z][a-z -]*"$' | tr -d '"' | sort -u); test -z "$missing"</automated>
  </verify>
  <acceptance_criteria>
    - The derived command-coverage gate passes: every top-level command name in `cmd/engram/testdata/catalog.golden` appears somewhere in CLAUDE.md. This gate is RED against `main` with exactly ten misses — `migrate revert`, `migrate status`, `migration-status`, `spine-review`, and its five leaves; record the before-list in the summary so the fix is traceable to the gate rather than to a hand-kept list.
    - The Layout row itself names them, not merely some other part of the file. Extract the row and check each name against it: `row=$(rg -N '^\|.*cmd/engram/' CLAUDE.md); for n in 'migrate status' 'migrate revert' 'migration-status' 'spine-review' 'get'; do printf '%s' "$row" | rg -qF -- "$n" || echo "row missing: $n"; done` prints nothing.
    - `rg -o 'database migrations, viper, cocogitto' CLAUDE.md | wc -l` reports `0`. It reports `1` against `main`.
    - `rg -o 'guides[/]migrate' CLAUDE.md | wc -l` reports at least `1`.
    - The revised convention names the boundary: `rg -c 'histogram' CLAUDE.md` or an equivalent phrase confirms the bullet says the excluded sweeps are absent from the status report, not merely absent from the registry. Quote the sentence in the summary.
    - No date, release number, phase number, or "new in" phrasing was added: `git diff -- CLAUDE.md | rg -o '^\+.*(new in|as of|shipped in|this release)' | wc -l` reports `0`.
    - `git diff --stat -- CLAUDE.md` shows the change confined to the Layout table and the Conventions list; the Memory contract section is untouched by this task (Task 2 owns it).
    - `task lint` is green, including `rumdl` over CLAUDE.md, and `task license:check` passes with no header added.
  </acceptance_criteria>
  <done>An agent reading CLAUDE.md's routing surface finds every shipped command and an accurate statement of what the migration mechanism covers and what it deliberately does not.</done>
</task>

<task type="auto">
  <name>Task 2: The record-state vocabulary in the Memory contract section</name>
  <reversibility rating="reversible">Documentation only.</reversibility>
  <files>CLAUDE.md</files>
  <read_first>
    - `CLAUDE.md` — the Memory contract section in full, especially the opening record-shape paragraph that enumerates a record's fields, the supersession paragraph, and the scheduled-tools paragraph, for where each addition belongs and at what density
    - `internal/store/store.go` — the `SchemaVersion` field, `schemaVersionKey`, and the stamp site, for what is actually server-set
    - `internal/store/store.go` — `activeWindowConditions` and the four recall-gate sites pairing `IsEmpty("superseded_by")` with `IsEmpty("archived_at")`
    - `cmd/engram/memory_state.go:1-70` — `memoryStateWords`, the canonical order and the precedence rule
    - `docs-site/src/content/docs/reference/memory-record.md` — the field contract, so CLAUDE.md compresses rather than duplicates it
    - `cmd/engram/spine_review_archive.go` or the archive/restore verbs in the spine-review family — for the vocabulary the section must name
  </read_first>
  <action>
Neither `schema_version` nor `archived_at` appears anywhere in CLAUDE.md today. The section already
documents supersession and the scheduling window in real depth, so the gap is a genuine omission
rather than stale text.

Add, at the section's existing density:

**The schema-version stamp.** Fold `schema_version` into the record-shape enumeration in the opening
paragraph, alongside the other server-set keys it already names. State that it is server-set on every
write, that absence reads as version 0, and that it never gates recall. One clause each; the field
contract lives on the reference page and the procedure on the operator guide.

**The archived state.** Add `archived_at` and the `spine-review archive` / `restore` pair. The
section already explains supersession's soft-hide-but-still-fetchable-by-id contract in a way that
`archived_at` shares exactly, so state the shared property once and note that archiving is an
orthogonal key: archiving never writes an expiry and never writes a supersession link, and each state
clears independently of the others.

**The state vocabulary.** State the four derived words — `archived`, `superseded`, `expired`,
`scheduled` — their canonical order by descending finality, and that `expired` and `scheduled` are
mutually exclusive with `expired` winning. This is the vocabulary every surface renders, so an agent
that knows it can read any of them. Do not restate the window boundary rule here — that belongs on
the reference page, and a second statement of it is a second thing to keep correct.

Keep the paragraph idiom. Do not introduce a new table or a bulleted list into a section built from
prose paragraphs, and do not let the additions push it past the density of what is already there. If
something has to give, compress the additions, not the existing content.

State the current contract. No release narration, no phase numbers, no "new" markers.
  </action>
  <verify>
    <automated>rg -q 'schema_version' CLAUDE.md && rg -q 'archived_at' CLAUDE.md && rg -q 'spine-review archive' CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -o 'schema_version' CLAUDE.md | wc -l` reports at least `1`, where `main` reports `0`.
    - `rg -o 'archived_at' CLAUDE.md | wc -l` reports at least `1`, where `main` reports `0`.
    - All four state words appear: `for w in archived superseded expired scheduled; do rg -qF -- "$w" CLAUDE.md || echo "missing: $w"; done` prints nothing, and the summary quotes the sentence that carries the canonical order so a reviewer can check it against `cmd/engram/memory_state.go` without re-deriving.
    - The additions landed in the Memory contract section, not elsewhere: `git diff --stat -- CLAUDE.md` for this task shows changes only below the `## Memory contract` heading.
    - No new table or list was introduced into the section: `git diff -- CLAUDE.md | rg -o '^\+[|-] ' | wc -l` reports `0` for this task's diff.
    - No date, release number, phase number, or "new in" phrasing was added: `git diff -- CLAUDE.md | rg -o '^\+.*(new in|as of|shipped in|this release)' | wc -l` reports `0`.
    - `task lint` is green and `task license:check` passes with no header added.
  </acceptance_criteria>
  <done>CLAUDE.md's Memory contract names the schema-version stamp, the archived state, and the four-word state vocabulary an agent will meet on every surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLAUDE.md → every agent acting in this repo | This file is loaded ahead of the task and treated as normative; a wrong statement here propagates into agent behavior with no further review step |
| shipped command surface → documented inventory | The Layout row asserts what commands exist, and nothing recompiles it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-11 | Spoofing | the revised migrations convention | medium | mitigate | A convention line that folds the owner-remap, summary-fill, or embedder-reindex sweeps into the schema-version mechanism would send an agent to `engram migrate` for a recovery that command cannot perform, and would make it read the status histogram as accounting for work it does not track. Both failures are silent. Task 1 requires the boundary stated explicitly in the bullet, with the summary quoting the sentence, and the prohibition block carries the same constraint into the goal-backward pass. |
| T-08-12 | Repudiation | the `cmd/engram/` inventory row | medium | mitigate | A hand-kept inventory is what produced the current ten-command gap, and re-writing it by hand would only reset the clock. The acceptance gate derives the expected set from `cmd/engram/testdata/catalog.golden`, which is regenerated from the live cobra tree, so the row is checked against the binary rather than against the planner's recollection — and the same gate catches the next omission. |
| T-08-13 | Information Disclosure | the Memory contract additions | low | accept | Everything added is already public in `docs-site` and in the tool schemas; CLAUDE.md compresses it for routing. No credential, endpoint, or internal-only detail is introduced. |
| T-08-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package and touches no dependency manifest. `08-RESEARCH.md` records the Package Legitimacy Audit as not applicable; `git diff --exit-code go.mod go.sum` is expected to exit 0. |
</threat_model>

<verification>
- The derived command-coverage gate passes: every top-level command name in `cmd/engram/testdata/catalog.golden` appears in CLAUDE.md. It fails against `main` with ten misses.
- `rg -o 'database migrations, viper, cocogitto' CLAUDE.md | wc -l` reports `0` (it reports `1` against `main`).
- `rg -o 'schema_version' CLAUDE.md | wc -l` and `rg -o 'archived_at' CLAUDE.md | wc -l` each report at least `1` (both report `0` against `main`).
- `task lint` is green, including `rumdl` over CLAUDE.md.
- `task license:check` passes with no SPDX header added.
- `git diff --exit-code go.mod go.sum` exits 0.
- Manual check, per `08-VALIDATION.md`: read the revised Layout row and Conventions bullet against the live command tree and confirm the migration boundary matches the Out of Scope table in `.planning/REQUIREMENTS.md`.
</verification>

<success_criteria>
- The migrations convention describes the schema-version mechanism this milestone ships and names its deliberate boundary.
- The `cmd/engram/` inventory agrees with the generated catalog, and the gate that proves it derives the expected set from that catalog rather than from a list.
- The Memory contract section names `schema_version`, `archived_at`, and the four-word state vocabulary.
- Nothing added reads as a changelog entry.
</success_criteria>

<output>
Create `.planning/phases/08-registry-docs-tail/08-04-SUMMARY.md` when done
</output>
