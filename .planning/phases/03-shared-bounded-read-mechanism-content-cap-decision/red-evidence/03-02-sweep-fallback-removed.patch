diff --git a/internal/store/spine.go b/internal/store/spine.go
index 2799112d..649ed094 100644
--- a/internal/store/spine.go
+++ b/internal/store/spine.go
@@ -80,10 +80,6 @@ func (s *Store) scrollAllPoints(ctx context.Context, filter *qdrant.Filter, view
 			WithPayload:    view.selector,
 		})
 		if err != nil {
-			if view.budgeted() && limit > 1 && errors.Is(err, ErrResponseTooLarge) {
-				fallbackLeft = int(limit)
-				continue
-			}
 			return err
 		}
 		for _, p := range pts {
