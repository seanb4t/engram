diff --git a/internal/server/tools.go b/internal/server/tools.go
index c9a6e195..0c2a8277 100644
--- a/internal/server/tools.go
+++ b/internal/server/tools.go
@@ -1890,7 +1890,7 @@ func (d *deps) updateMemory(ctx context.Context, c caller, a updateArgs) (mutati
 	// runs AFTER FetchForUpdate's owner gate above, so a non-owner learns
 	// nothing beyond the existing uniform not-found (T-03-03-03).
 	caps := d.writeCaps.resolved()
-	if contentChanged {
+	if a.Content != nil {
 		if err := checkContentBytes(*a.Content, caps.contentBytes); err != nil {
 			return mutationResult{}, err
 		}
