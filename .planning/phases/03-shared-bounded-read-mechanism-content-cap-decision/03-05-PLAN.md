---
phase: 03-shared-bounded-read-mechanism-content-cap-decision
plan: 05
type: execute
wave: 3
depends_on:
  - 03-03
files_modified:
  - internal/e2e/contentcap_cli_test.go
  - internal/e2e/cli_exitcode_test.go
  - docs-site/src/content/docs/guides/configure.md
  - docs-site/src/content/docs/reference/errors.md
  - docs-site/src/content/docs/reference/tools.md
  - docs-site/src/content/docs/guides/upgrade.md
  - .planning/PROJECT.md
  - CLAUDE.md
  - skill/engram/skills/curating-memory/SKILL.md
  - internal/skills/data/curating-memory/SKILL.md
autonomous: true
requirements:
  - REQ-content-cap-decided

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The real built `engram` binary's `store` verb, pointed at a real `engram serve` (headless Connect, static-token auth), exits `2` with `field=content hint=too_long` on stderr for a 65537-byte `--content`, exits `2` with `field=tags hint=too_many` for 129 `--tags`, exits `2` with `field=tags hint=too_long` for one 129-byte tag, and exits `0` for a 65536-byte `--content` — the CLI lane of D-01/D-10 proven end to end, with the CLI adding no client-side cap of its own (it cannot know the server's configured value)"
    - "`guides/configure.md` documents `ENGRAM_MEMORY_MAX_CONTENT_BYTES` (65536), `ENGRAM_MEMORY_MAX_TAGS` (128) and `ENGRAM_MEMORY_MAX_TAG_BYTES` (128) beside `ENGRAM_MEMORY_MAX_SUMMARY_BYTES`, states that `0` is rejected at startup for these three (unlike the summary bound), names the rejection hints, and states that existing over-cap records stay readable and trimmable (D-01, D-09, D-10)"
    - "`reference/errors.md` carries worked `field=content hint=too_long` and `field=tags hint=too_many` examples, still lists exactly the eleven hint codes (no new code), and the registered patch `02-03-errors-doc-drops-too-large.patch` still passes `git apply --check`"
    - "`reference/tools.md` states the content and tags bounds on the `store_memory`, `schedule_memory` and `update_memory` field rows"
    - "`guides/upgrade.md`'s `## Unreleased` section gains entry `### 15.` telling a caller that a memory write which used to succeed (content over 64 KiB, more than 128 tags, a tag over 128 bytes — including an idempotent retry of such a write) is now rejected, which hints it carries, and that stored records are not rewritten"
    - "`.planning/PROJECT.md` Key Decisions records decision A — memory `content` capped (plus the D-10 tags caps), always enforced, existing records untouched — with its rationale and outcome (REQ-content-cap-decided)"
    - "`CLAUDE.md`'s memory-contract paragraph and the `curating-memory` skill (canonical and vendored copies identical) state the content and tags bounds beside the summary bound"
    - "The docs site builds (`pnpm --dir docs-site build` exits 0) and every docs gate that reads these pages stays green"
  artifacts:
    - path: "internal/e2e/contentcap_cli_test.go"
      provides: "TestCLIStoreRejectsOversizedContentAndTags and the runCLIEnv helper"
      contains: "TestCLIStoreRejectsOversizedContentAndTags"
    - path: "docs-site/src/content/docs/guides/configure.md"
      provides: "the three ENGRAM_MEMORY_MAX_* rows"
      contains: "ENGRAM_MEMORY_MAX_CONTENT_BYTES"
    - path: ".planning/PROJECT.md"
      provides: "decision A in Key Decisions"
      contains: "ENGRAM_MEMORY_MAX_CONTENT_BYTES"
  key_links:
    - from: "internal/e2e/contentcap_cli_test.go"
      to: "cmd/engram/client_store.go"
      via: "the real binary's store verb reaches the server-side cap through Connect"
      pattern: "field=content hint=too_long"
    - from: "docs-site/src/content/docs/guides/configure.md"
      to: "internal/config/registry.go"
      via: "the documented variables and defaults are the registry's"
      pattern: "ENGRAM_MEMORY_MAX_TAG_BYTES"
    - from: ".planning/PROJECT.md"
      to: ".planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/03-CONTEXT.md"
      via: "the Key Decisions row records decision A (REQ-content-cap-decided)"
      pattern: "ENGRAM_MEMORY_MAX_CONTENT_BYTES"
---

<objective>
Prove decision A on the one lane not yet exercised end to end — the `engram` CLI — and publish it:
the operator guide, the error reference, the tool reference, the upgrade guide, the PROJECT.md Key
Decisions record REQ-content-cap-decided requires, and the two agent-facing surfaces that already
document the summary bound (CLAUDE.md's memory contract and the `curating-memory` skill).

Decisions implemented: D-01 ("every memory write path … on MCP, Connect, and the `engram` CLI";
"Record the decision in PROJECT.md Key Decisions … and document the variable (docs-site
configure/errors pages)"), D-09 (the documented divergence from the summary bound's `0 disables`),
D-10 ("Document both variables beside the content cap"). Framing (D-00): the CLI is a Connect client
and gets the cap from the server — no client-side duplicate that could disagree with the server's
configured value; documentation follows the existing hand-maintained configure table (there is no
registry→docs generator or drift gate for `configure.md` in this repo, verified at planning time)
and the upgrade guide's numbered `## Unreleased` entries.

Purpose: REQ-content-cap-decided (CLI proof, decision record, documentation).

Output: one e2e test, four docs-site pages, the PROJECT.md row, CLAUDE.md and the skill.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/PROJECT.md
@.planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/03-CONTEXT.md
@.planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/03-01-SUMMARY.md
@.planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/03-03-SUMMARY.md
@CLAUDE.md

<interfaces>
internal/e2e (package `e2e`): `TestMain` builds the binary into `engramBin` and delegates Qdrant to `storetest.Run`; `startServer(t, extraEnv map[string]string) *serverProc` launches `engram serve` with a stub embedder and a per-port test collection and surfaces the server log on failure; `(*serverProc).baseURL()` returns `http://<addr>`; `childEnv(vars map[string]string) []string` builds a hermetic env (PATH, HOME + vars); `runCLI(t, args ...string) (stdout, stderr string, exitCode int)` in `cli_exitcode_test.go` runs `engramBin` with `childEnv(nil)`. No e2e test drives a client verb against a real server yet. Registered patch `01-05-bare-qdrant-newclient-in-test.patch` targets `internal/e2e/spine_review_test.go` — do not edit that file.

Headless Connect (docs-site `guides/configure.md` lines 164-183): `ENGRAM_CONNECT_HEADLESS=true` mounts Connect with the UI off and refuses to start without an auth lane; `ENGRAM_SERVICE_AUTH_STATIC_TOKENS` takes comma-separated `owner=token` pairs. The CLI reads the bearer from `ENGRAM_TOKEN` (`cmd/engram/client_common.go` `resolveToken`) and the server from `--server`.

`engram store` (`cmd/engram/client_store.go`): flags `--content`, `--scope`, `--source`, `--category`, `--tags` (comma-separated `StringSliceVar`); pre-checks only presence; sends `StoreMemoryRequest`; text output `stored: <short_id> (<id>)`. A Connect `CodeOutOfRange` maps to exit `2` (`exitCodeForConnectErr`, `exitUsage`). The server requires `source` and `category`.

Docs: `guides/configure.md` `## Memory` table (line ~40-50) has one row, `ENGRAM_MEMORY_MAX_SUMMARY_BYTES`, then a paragraph and a `Source:` line. `reference/errors.md`: `## The envelope grammar` shows a single-field `field=summary hint=too_long` example (lines 23-27); `## The eleven hint codes` table (line 96+) — the `too_large` row at ~line 113 is the target of registered patch `02-03-errors-doc-drops-too-large.patch` (context lines 110-117); `TestErrorsDocHintCodesMatchArgErrorConstants` (internal/server/hintcodedocs_test.go) binds that table to `argerror.go`. `reference/tools.md` rows: `store_memory` content line 78 / tags line 82, `schedule_memory` 106 / 110, `update_memory` 361 / 363 (the `summary` rows at 87/115/364 show the "Max `ENGRAM_…` bytes (default …)" wording to mirror). `guides/upgrade.md` `## Unreleased` ends with `### 14. New exit code 10 …` followed by `---`. Doc gates reading these pages: `TestErrorsDocHintCodesMatchArgErrorConstants`, `TestSupersedeDocsMatchShippedContract` (internal/server), `TestSurfaceConformanceProseFiles` (internal/surfaces), `TestUpgradeGuideNamesEveryChangedCommand` (cmd/engram).

`.planning/PROJECT.md` `## Key Decisions` table `| Decision | Rationale | Outcome |` — the last two rows are this milestone's Phase 1/2 decisions (style to mirror). `CLAUDE.md` lines 103-106: "…with a memory `summary` bounded at `ENGRAM_MEMORY_MAX_SUMMARY_BYTES` (default 512 bytes)." `skill/engram/skills/curating-memory/SKILL.md` lines 489-492: the summary-bound paragraph; `task skills:vendor` copies `skill/engram/skills/` into `internal/skills/data/`; `TestSkillsEmbedMatchesVendored` (internal/skills/drift_test.go) fails if they differ.
</interfaces>
</context>

## Artifacts this phase produces (plan 03-05's share)

Nothing below exists on HEAD today.

| Kind | Artifact | Created by |
|------|----------|------------|
| Test file | `internal/e2e/contentcap_cli_test.go`: `TestCLIStoreRejectsOversizedContentAndTags`, helper `runCLIEnv(t, env, args...)` | Task 1 |
| Docs rows | `configure.md` rows for `ENGRAM_MEMORY_MAX_CONTENT_BYTES`, `ENGRAM_MEMORY_MAX_TAGS`, `ENGRAM_MEMORY_MAX_TAG_BYTES` | Task 2 |
| Docs examples | `errors.md` worked `field=content hint=too_long` and `field=tags hint=too_many` examples | Task 2 |
| Upgrade entry | `upgrade.md` `### 15.` under `## Unreleased` | Task 2 |
| Decision record | `.planning/PROJECT.md` Key Decisions row for decision A | Task 3 |
| Agent-facing | `CLAUDE.md` memory contract sentence; `curating-memory` SKILL.md paragraph (canonical + vendored) | Task 3 |

## Executor notes (every task)

- NEVER run any git stash subcommand — to inspect a pristine file use `git show HEAD:<path>` into the scratchpad instead.
- Commit with an explicit pathspec (rule `n6m4as49mr`), Conventional Commits, ending the message with the session's attribution trailer lines. Commit signing is off: commit normally and never add signing configuration.
- `.planning/**` files (PROJECT.md) and `docs-site/**` carry NO SPDX header; `CLAUDE.md` and SKILL.md keep whatever header state they already have (the skill's first line must stay `---`).
- Docs builds need `pnpm --dir docs-site install --frozen-lockfile` first (existing lockfile; no new packages).
- After editing `errors.md`, run `git apply --check .planning/phases/02-error-classification-resourceexhausted-mapping/red-evidence/02-03-errors-doc-drops-too-large.patch`; if it no longer applies, regenerate exactly that patch in the SAME commit (reintroduce the defect, `git diff` into the patch, prove `TestErrorsDocHintCodesMatchArgErrorConstants` RED, revert) and record it.
- Edit only the canonical skill, then run `task skills:vendor`; never hand-edit `internal/skills/data/`.

<tasks>

<task type="tracer">
  <name>Task 1: End to end: the real engram binary's store verb is rejected by a real server for over-cap content and tags</name>
  <files>internal/e2e/contentcap_cli_test.go, internal/e2e/cli_exitcode_test.go</files>
  <precondition>Docker is reachable (`docker info` exits 0) or ENGRAM_QDRANT_TEST_ADDR names a live Qdrant gRPC endpoint, and `go build ./cmd/engram` succeeds: the e2e TestMain builds the binary and boots the server against real Qdrant.</precondition>
  <read_first>
    - `internal/e2e/harness_test.go` (whole file: `TestMain`, `childEnv`, `startServer`, `serverProc`) and `internal/e2e/cli_exitcode_test.go` lines 1-80 (`runCLI`, `TestCLIExitCodes`).
    - `cmd/engram/client_store.go` (flags, pre-checks, output) and `cmd/engram/client_common.go` `resolveToken`, `exitCodeForConnectErr`.
    - `docs-site/src/content/docs/guides/configure.md` lines 140-190 (service principals, headless Connect).
    - `internal/server/contentcap_test.go` (plan 03-01's lane tests — this task proves the same contract through the real binary).
  </read_first>
  <action>
Implements the CLI lane of D-01 and D-10 as an end-to-end proof; no production code changes.

(1) `internal/e2e/cli_exitcode_test.go`: add `func runCLIEnv(t *testing.T, env map[string]string, args ...string) (stdout, stderr string, exitCode int)` holding `runCLI`'s current body with `cmd.Env = childEnv(env)`, and reduce `runCLI` to `return runCLIEnv(t, nil, args...)`. No other change to the file.

(2) New `internal/e2e/contentcap_cli_test.go` (package `e2e`, SPDX header, file doc comment: the CLI is a Connect client with no cap of its own — it cannot know the server's configured value — so this proves the server-side cap reaches the operator through the real binary's exit status and stderr; D-01, D-10) with `TestCLIStoreRejectsOversizedContentAndTags`:
- `srv := startServer(t, map[string]string{"ENGRAM_CONNECT_HEADLESS": "true", "ENGRAM_SERVICE_AUTH_STATIC_TOKENS": "e2e-cap=tok-e2e-cap"})`; CLI env `map[string]string{"ENGRAM_TOKEN": "tok-e2e-cap"}`; common args `store --server <srv.baseURL()> --scope repo:e2e-cap --source user-said --category decision`.
- Subtests, in this order: `at-cap` (`--content` of exactly 65536 `x` bytes → exit `0`, stdout starts `stored: `) — the positive control proving auth and mounting work, so a later rejection cannot be a setup failure; `content-over-cap` (65537 bytes → exit `2`, stderr contains `field=content hint=too_long`); `too-many-tags` (`--tags` of 129 comma-joined `t000`..`t128` → exit `2`, stderr contains `field=tags hint=too_many`); `tag-too-long` (`--tags` of one 129-byte tag → exit `2`, stderr contains `field=tags hint=too_long`).
- On any unexpected exit code, `t.Fatalf` with the exit code, stdout and stderr.

Detection power (the cap already exists, so RED is shown by mutation, not by ordering): temporarily delete the `checkContentBytes(a.Content, caps.contentBytes)` call in `validateStoreArgs`, re-run the test and observe `content-over-cap` FAIL (exit `0`), then restore the line — record the `--- FAIL:` line in the SUMMARY and confirm `git diff --exit-code -- internal/server/tools.go` exits 0 before committing.

Commit as `test(e2e): prove the memory caps through the real engram store verb (D-01, D-10)` with pathspec `internal/e2e/contentcap_cli_test.go internal/e2e/cli_exitcode_test.go`.
  </action>
  <verify>
    <automated>out=$(ENGRAM_REQUIRE_QDRANT=1 go test ./internal/e2e/ -run '^(TestCLIStoreRejectsOversizedContentAndTags|TestCLIExitCodes)$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^\s*--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e '    --- PASS: TestCLIStoreRejectsOversizedContentAndTags/(at-cap|content-over-cap|too-many-tags|tag-too-long)' | wc -l)" -eq 4 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e '--- SKIP: ' | wc -l)" -eq 0</automated>
    <fails_when>non-zero exit, fewer than four passing subtests, any `--- SKIP:` line (Qdrant or the server was not reached), or a `TestCLIExitCodes` failure (the runCLI refactor broke it)</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -o 'return runCLIEnv[(]t, nil, args[.][.][.][)]' internal/e2e/cli_exitcode_test.go | wc -l` prints `1`.
    - No CLI code or patched e2e file changed: with `C` set to the commit SHA of this task (record it in the SUMMARY), `d=$(git diff --stat "$C^..$C" -- internal/e2e/spine_review_test.go cmd/engram) || exit 1; test -z "$d"` exits 0.
    - The SUMMARY quotes the detection-power run's `--- FAIL: TestCLIStoreRejectsOversizedContentAndTags/content-over-cap` line and states `internal/server/tools.go` was restored.
    - `go vet ./internal/e2e/` exits 0; `task license:check` exits 0.
  </acceptance_criteria>
  <done>The real binary against a real server accepts an at-cap write and rejects each over-cap shape with exit 2 and the named envelope on stderr; committed with an explicit pathspec.</done>
</task>

<task type="auto">
  <name>Task 2: Document the three caps, the rejection shapes, the tool bounds and the upgrade impact on docs-site</name>
  <files>docs-site/src/content/docs/guides/configure.md, docs-site/src/content/docs/reference/errors.md, docs-site/src/content/docs/reference/tools.md, docs-site/src/content/docs/guides/upgrade.md</files>
  <read_first>
    - `docs-site/src/content/docs/guides/configure.md` lines 36-52 (`## Memory`).
    - `docs-site/src/content/docs/reference/errors.md` lines 1-48 and 96-125, and the patch `.planning/phases/02-error-classification-resourceexhausted-mapping/red-evidence/02-03-errors-doc-drops-too-large.patch`.
    - `docs-site/src/content/docs/reference/tools.md` lines 71-120 and 349-370.
    - `docs-site/src/content/docs/guides/upgrade.md` lines 1-40 and 365-392.
    - `internal/server/tools.go` (plans 03-01/03-03: `checkContentBytes`, `checkTags` detail wording) and `03-CONTEXT.md` D-01, D-09, D-10.
  </read_first>
  <action>
Implements D-01's and D-10's documentation instruction and D-09's documented divergence.

(1) `guides/configure.md` `## Memory` table: add three rows after the summary row — `ENGRAM_MEMORY_MAX_CONTENT_BYTES` | `65536` | max byte length of a memory `content` on `store_memory`/`schedule_memory`/`supersede_memory`/`update_memory` (when the content changes), on MCP, Connect and `engram store`; over-cap writes are rejected (`field=content hint=too_long`), never truncated; `ENGRAM_MEMORY_MAX_TAGS` | `128` | max number of `tags` on those paths (`field=tags hint=too_many`); `ENGRAM_MEMORY_MAX_TAG_BYTES` | `128` | max byte length of one tag (`field=tags hint=too_long`). After the table add a short paragraph: unlike `ENGRAM_MEMORY_MAX_SUMMARY_BYTES`, these three are always enforced — `0` or a negative value fails startup — because the server sizes its bounded reads from them; existing records larger than a cap are not rewritten, stay readable (`get_memory`), and their owner can trim them with `update_memory`; `store_discovery` and `store_rule` keep their own bounds. Extend the `Source:` line to name `memoryWriteCapsFromConfig`, `checkContentBytes` and `checkTags`.

(2) `reference/errors.md` `## The envelope grammar`: after the single-field summary example add a sentence and two fenced examples — `field=content hint=too_long: content too large: 70000 bytes (max 65536)` and `field=tags hint=too_many: too many tags: 129 (max 128)` — noting the detail carries sizes, never the submitted value, and that no new hint code was added. In the hint table change only the `too_many` row's example to "(e.g. `citations`, `tags`)"; do not touch the `too_large` row or anything within three lines of it. Then run the executor-notes `git apply --check`.

(3) `reference/tools.md`: extend the `content` rows of `store_memory` and `schedule_memory` and the `content` row of `update_memory` with "Max `ENGRAM_MEMORY_MAX_CONTENT_BYTES` bytes (default 65536; see [Configuration](/guides/configure/))" (for `update_memory`: "enforced when the content changes"), and the `tags` rows of the same three tools with "At most `ENGRAM_MEMORY_MAX_TAGS` tags (default 128) of at most `ENGRAM_MEMORY_MAX_TAG_BYTES` bytes each (default 128)" (for `update_memory`: "enforced when the tag set changes").

(4) `guides/upgrade.md`: under `## Unreleased`, after entry 14 and before its closing `---`, add `### 15. Memory content and tags are now capped: an oversized write is rejected` — before: a memory write with any content size or tag count was stored; now `store_memory`/`schedule_memory`/`supersede_memory`, `update_memory` when it changes content or tags, Connect `StoreMemory`/`ScheduleMemory`/`UpdateMemory`, and `engram store` (exit `2`) reject content over 64 KiB, more than 128 tags or a tag over 128 bytes with the existing hints (link to the errors reference and the configure guide); an idempotent retry (`idempotency_key`) of such a write is rejected the same way; stored records are never rewritten. A `**Who should act:**` paragraph in the house style: an agent or script storing large documents or tag-heavy records — shorten, split, or raise the cap via the three variables (never to `0`).

Verify locally with the docs build and the doc gates below, then commit as `docs: document the memory content and tags caps (D-01, D-09, D-10)` with pathspec `docs-site/src/content/docs/guides/configure.md docs-site/src/content/docs/reference/errors.md docs-site/src/content/docs/reference/tools.md docs-site/src/content/docs/guides/upgrade.md` (plus a regenerated patch file only if the check forced one).
  </action>
  <verify>
    <automated>pnpm --dir docs-site install --frozen-lockfile >/dev/null 2>&amp;1 &amp;&amp; out=$(pnpm --dir docs-site build 2>&amp;1); rc=$?; printf '%s\n' "$out" | tail -5; test "$rc" -eq 0</automated>
    <fails_when>non-zero exit from the install or the build (a broken link, frontmatter or MDX error in an edited page)</fails_when>
    <automated>out=$(go test ./internal/server/ ./internal/surfaces/ ./cmd/engram/ -run '^(TestErrorsDocHintCodesMatchArgErrorConstants|TestSupersedeDocsMatchShippedContract|TestSurfaceConformanceProseFiles|TestUpgradeGuideNamesEveryChangedCommand)$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e '^--- PASS: ' | wc -l)" -eq 4</automated>
    <fails_when>non-zero exit or fewer than four top-level `--- PASS:` lines (a docs gate reading these pages regressed)</fails_when>
    <automated>git apply --check .planning/phases/02-error-classification-resourceexhausted-mapping/red-evidence/02-03-errors-doc-drops-too-large.patch</automated>
    <fails_when>non-zero exit (the errors.md edit broke a registered red-evidence patch and it was not regenerated)</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -o '^[|] `ENGRAM_MEMORY_MAX_(CONTENT_BYTES|TAGS|TAG_BYTES)` [|] `(65536|128)` [|]' docs-site/src/content/docs/guides/configure.md | wc -l` prints `3`.
    - `rg -o 'field=content hint=too_long: content too large' docs-site/src/content/docs/reference/errors.md | wc -l` prints `1`; `rg -o 'field=tags hint=too_many' docs-site/src/content/docs/reference/errors.md | wc -l` prints at least `1`.
    - `rg -o 'ENGRAM_MEMORY_MAX_CONTENT_BYTES' docs-site/src/content/docs/reference/tools.md | wc -l` prints `3`; `rg -o 'ENGRAM_MEMORY_MAX_TAGS' docs-site/src/content/docs/reference/tools.md | wc -l` prints `3`.
    - `rg -n '^### 15[.] ' docs-site/src/content/docs/guides/upgrade.md | wc -l` prints `1`, and it sits between `### 14.` and `## v0.7.10`.
  </acceptance_criteria>
  <done>The three caps, their always-enforced divergence, the rejection shapes, the per-tool bounds and the upgrade impact are documented, the site builds, every docs gate is green and the registered errors.md patch still applies; committed with an explicit pathspec.</done>
</task>

<task type="auto">
  <name>Task 3: Record decision A in PROJECT.md and update the agent-facing contract (CLAUDE.md, curating-memory skill)</name>
  <files>.planning/PROJECT.md, CLAUDE.md, skill/engram/skills/curating-memory/SKILL.md, internal/skills/data/curating-memory/SKILL.md</files>
  <read_first>
    - `.planning/PROJECT.md` `## Key Decisions` (the last four rows — this milestone's Phase 1/2 style).
    - `CLAUDE.md` lines 95-110 (the memory-contract paragraph).
    - `skill/engram/skills/curating-memory/SKILL.md` lines 470-495 and `Taskfile.yaml` `skills:vendor`.
    - `03-CONTEXT.md` D-01, D-09, D-10 and `03-02-SUMMARY.md` (the recorded residual: fields with no write cap are budgeted by allowance).
  </read_first>
  <action>
Implements D-01's "Record the decision in PROJECT.md Key Decisions (REQ-locked)" and keeps the agent-facing contract correct-by-reading.

(1) `.planning/PROJECT.md` `## Key Decisions`: append ONE row (a value in the existing three-column table — no new heading or structure) after the last Phase 2 row. Decision: memory `content` gets an always-enforced write cap, `ENGRAM_MEMORY_MAX_CONTENT_BYTES` (default 65536), plus the tags caps `ENGRAM_MEMORY_MAX_TAGS` (128) and `ENGRAM_MEMORY_MAX_TAG_BYTES` (128); `0` is rejected (2026-09-18.01 Phase 3, decision A: D-01, D-09, D-10). Rationale: byte-budget paging needs a provable per-record ceiling and the per-RPC count is derived from these caps, so a disabled cap would silently remove the bound; citations (50 × 16 KiB) already dominated once content was capped and tags were the other unbounded field; rejection reuses `too_long`/`too_many` so no new wire vocabulary; existing over-cap records are never rewritten and stay readable and trimmable. Outcome: `✓ Good — enforced on every memory write path (MCP, Connect incl. UpdateMemory's field-mask lane, engram store); residual: payload fields with no write cap are budgeted by documented allowances, covered by the batch-of-1 fallback and the named error`.

(2) `CLAUDE.md`: extend the memory-contract sentence ending "…with a memory `summary` bounded at `ENGRAM_MEMORY_MAX_SUMMARY_BYTES` (default 512 bytes)." so it also names `content` bounded at `ENGRAM_MEMORY_MAX_CONTENT_BYTES` (default 65536 bytes) and `tags` at `ENGRAM_MEMORY_MAX_TAGS` entries of `ENGRAM_MEMORY_MAX_TAG_BYTES` bytes each (default 128 / 128), always enforced. One sentence; no other CLAUDE.md change.

(3) `skill/engram/skills/curating-memory/SKILL.md`: after the summary-bound paragraph add a short paragraph in the same voice: `content` is bounded too — 64 KiB by default (`ENGRAM_MEMORY_MAX_CONTENT_BYTES`) — and `tags` to 128 entries of 128 bytes each (`ENGRAM_MEMORY_MAX_TAGS` / `ENGRAM_MEMORY_MAX_TAG_BYTES`); an oversized write is rejected with `field=content hint=too_long` or `field=tags hint=too_many|too_long`, so store a durable fact, not a document, and split or trim instead of retrying. Then run `task skills:vendor` to refresh `internal/skills/data/`.

Commit as `docs: record decision A and the memory caps in the project and agent contract (D-01, D-10)` with pathspec `.planning/PROJECT.md CLAUDE.md skill/engram/skills/curating-memory/SKILL.md internal/skills/data/curating-memory/SKILL.md`.
  </action>
  <verify>
    <automated>out=$(go test ./internal/skills/ -run '^TestSkillsEmbedMatchesVendored$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; printf '%s\n' "$out" | rg -q -e '^--- PASS: TestSkillsEmbedMatchesVendored'</automated>
    <fails_when>non-zero exit or no `--- PASS: TestSkillsEmbedMatchesVendored` line (the vendored skill drifted from the canonical one)</fails_when>
    <automated>task lint:markdown</automated>
    <fails_when>non-zero exit (rumdl rejects the edited CLAUDE.md or SKILL.md)</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -o 'ENGRAM_MEMORY_MAX_CONTENT_BYTES' .planning/PROJECT.md | wc -l` prints at least `1`, and no heading was added or removed: with `C` set to the commit SHA of this task (record it in the SUMMARY), `d=$(git diff "$C^..$C" -- .planning/PROJECT.md) || exit 1; printf '%s\n' "$d" | rg -o '^[+-]#' | wc -l` prints `0`.
    - `rg -o 'ENGRAM_MEMORY_MAX_CONTENT_BYTES' CLAUDE.md | wc -l` prints `1`.
    - `diff -q skill/engram/skills/curating-memory/SKILL.md internal/skills/data/curating-memory/SKILL.md` exits 0 and `rg -o 'ENGRAM_MEMORY_MAX_CONTENT_BYTES' skill/engram/skills/curating-memory/SKILL.md | wc -l` prints `1`.
    - `head -1 skill/engram/skills/curating-memory/SKILL.md` is `---`; `task license:check` exits 0.
  </acceptance_criteria>
  <done>Decision A is recorded in PROJECT.md Key Decisions, and CLAUDE.md and the shipped skill state the content and tags bounds beside the summary bound; committed with an explicit pathspec.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI operator → Connect `StoreMemory` | The CLI forwards untrusted content and tags; the server enforces the caps. |
| Documentation → operators and agents | Wrong docs lead operators to disable a cap or agents to retry an unchanged oversized write. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-05-01 | Elevation of Privilege (bypass) | A client lane reaching the store without the cap | medium | mitigate | `TestCLIStoreRejectsOversizedContentAndTags` drives the real binary against a real server; the CLI holds no second copy of the cap to drift. |
| T-03-05-02 | Spoofing | The e2e static token | low | accept | Test-only token on a loopback, per-test server with a per-port collection; never a shipped credential. |
| T-03-05-03 | Tampering | Operator misconfiguring a cap from docs | low | mitigate | configure.md states `0` is rejected and why; startup validation (plan 03-01) is the backstop. |
| T-03-05-SC | Tampering | npm/pip/cargo installs | low | accept | `pnpm install --frozen-lockfile` installs only the existing lockfile; no package is added. |
</threat_model>

<verification>
- `ENGRAM_REQUIRE_QDRANT=1 go test ./internal/e2e/ ./internal/server/ ./internal/surfaces/ ./internal/skills/ ./cmd/engram/ -count=1` exits 0.
- `pnpm --dir docs-site install --frozen-lockfile && pnpm --dir docs-site build` exits 0.
- `task lint` exits 0; `task license:check` exits 0.
- `go test ./internal/keylinks/ -run TestNoEscapedPatternsRepoWide -count=1` exits 0; after the SUMMARY is written, `go test ./internal/keylinks/ -count=1` exits 0.
</verification>

<success_criteria>
- ROADMAP Phase 3 success criterion 3: the content-cap decision is recorded in PROJECT.md Key Decisions, and the registry-declared `ENGRAM_MEMORY_MAX_CONTENT_BYTES` with its documented default rejects an oversized write on MCP, Connect and the CLI with a named hint while existing oversized records stay readable.
</success_criteria>

<output>
Create `.planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/03-05-SUMMARY.md` when done.
</output>
