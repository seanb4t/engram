diff --git a/cmd/engram/client_common.go b/cmd/engram/client_common.go
index 901a6ea0..4561275c 100644
--- a/cmd/engram/client_common.go
+++ b/cmd/engram/client_common.go
@@ -342,10 +342,6 @@ func renderCoverageFooter(w io.Writer, crossSpine bool, searchedScopes []string,
 	if !crossSpine {
 		return nil
 	}
-	if scopesUnknown {
-		_, err := fmt.Fprintf(w, "scopes_unknown: true\n")
-		return err
-	}
 	if scopesTruncated {
 		_, err := fmt.Fprintf(w, "searched_scopes: %d  scopes_truncated: true\n", len(searchedScopes))
 		return err
