diff --git a/internal/server/tools.go b/internal/server/tools.go
index 6255d2f6..6dff40a7 100644
--- a/internal/server/tools.go
+++ b/internal/server/tools.go
@@ -2726,6 +2726,9 @@ func registerTools(s *mcp.Server, d *deps) error {
 				return nil, nil, err
 			}
 			cov := d.searchedScopes(ctx, c, a.CrossSpine)
+			if cov.Unknown {
+				return nil, nil, fmt.Errorf("coverage query failed")
+			}
 			// MCP-specific recall shaping lives here, not in the shared core
 			// (D-07): the core returns raw []store.Memory.
 			mems := shapeRecall(res.Memories, a.Full, d.summaryMaxChars)
