---
phase: 02-decision-interface-jev-backend
plan: 05
type: execute
wave: 5
depends_on:
  - 02-03
  - 02-04
files_modified:
  - internal/server/decider.go
  - internal/server/decider_test.go
  - internal/server/tools.go
  - internal/server/tools_test.go
autonomous: true
coupling_justified: ["02-07: 02-05's wiring tests drive jev.Decide black-box; 02-07's retry and classification keep their assertions (a timeout is never retried and a 200 decodes unchanged), so either merge order is correct"]
requirements:
  - DEC-01
  - DEC-03

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "With `ENGRAM_DECISIONS_PROVIDER` unset, `deciderFromConfig` returns a nil `decide.Decider` interface value with no error. It constructs no client, and an httptest server named as `ENGRAM_DECISIONS_BASE_URL` receives zero requests, even with every other decisions field set (DEC-01, spec-less edge E01)"
    - "`buildDepsFromEnv` assigns `deps.decider` from `deciderFromConfig`: nil when the provider is unset, non-nil when it is `jev`. Constructing an enabled decider at startup makes zero outbound calls (no probe or health request). An unknown provider fails startup through `Config.Validate` before any store dial (DEC-01)"
    - "`deciderFromConfig` passes every config-driven option to `jev.New`: timeout, max timeout, drain bytes, drain timeout and concurrency. A configured `ENGRAM_DECISIONS_TIMEOUT` of `150ms` bounds a call to a stalled endpoint, and a configured `ENGRAM_DECISIONS_CONCURRENCY` of `2` caps `DecideMany`'s peak in-flight requests at exactly 2 (D-02, D-10, spec-less edge E07)"
    - "The option resolvers mirror the summarize helpers. An unparseable or out-of-range value logs a warning and falls back to its default (timeout `10s`, max timeout `10m`, drain bytes `262144`, drain timeout `2s`, concurrency `4`). A drain value of 0 is honored. A non-positive max timeout or concurrency is never passed on"
    - "The registry's default `ENGRAM_DECISIONS_MODEL` equals `jev.DefaultModel`, asserted by a test so the two literals cannot drift"
    - "Enabling decisions logs one Info line, `typed decisions enabled`, with `provider`, `model`, `endpoint_host` (the base URL's host only: never userinfo, path or query) and `api_key_source` (`ENGRAM_DECISIONS_API_KEY`, `ENGRAM_OPENAI_API_KEY` or `none`). The API key value never appears (D-03 key fallback made visible)"
  artifacts:
    - path: "internal/server/decider.go"
      provides: "deciderFromConfig with full options, decisions* resolvers, logDeciderEnabled"
      contains: "func logDeciderEnabled(cfg *config.Config)"
    - path: "internal/server/tools.go"
      provides: "deps.decider field and its gated construction in buildDepsFromEnv"
      contains: "decider decide.Decider"
    - path: "internal/server/decider_test.go"
      provides: "TestDeciderFromConfigProviderUnset, TestDeciderFromConfigUnknownProvider, TestDeciderFromConfigAppliesOptions, TestDeciderResolverDefaults, TestDecisionsModelDefaultMatchesJev, TestDeciderEnabledLogLine"
      contains: "func TestDeciderFromConfigProviderUnset("
  key_links:
    - from: "internal/server/tools.go"
      to: "internal/server/decider.go"
      via: "startup builds the decider through the single gated seam (D-01)"
      pattern: "deciderFromConfig[(]cfg[)]"
    - from: "internal/server/decider.go"
      to: "internal/decide/jev/jev.go"
      via: "ENGRAM_DECISIONS_CONCURRENCY reaches the DecideMany pool (D-10)"
      pattern: "jev[.]WithConcurrency[(]decisionsConcurrency[(]cfg[)][)]"
  prohibitions:
    - statement: "MUST NOT construct a decision client or send any request to a decisions endpoint while ENGRAM_DECISIONS_PROVIDER is unset: with the feature off, no record content, state or credential may leave the deployment on this lane"
      category: privacy
      status: resolved
      verification: test
      reason: "TestDeciderFromConfigProviderUnset points ENGRAM_DECISIONS_BASE_URL at a counting server and asserts zero requests plus a nil Decider. No wired check descriptor by design (spec-less fallback, flagged-unverified)"
---

<objective>
Wire the decider into engram's startup exactly the way the summarizer is wired, and prove the off-by-default guarantee.

- **DEC-01 / D-01:** `deps.decider` is nil unless an operator sets the provider. Unset constructs nothing and sends nothing. An unknown value fails startup.
- **D-02 / D-10:** every `ENGRAM_DECISIONS_*` knob reaches `jev.New`, including concurrency, which bounds `DecideMany`.
- **D-03:** the key-only fallback stays at the wiring seam. The startup log names which key source is in use, never the key.
- **RESEARCH A4, resolved here:** the decider goes into the `deps` struct now (an unused field this phase), not a standalone constructor. The "construct nothing when off" path then runs through the same startup builder every existing integration test exercises. It also gives Phase 4's `search_memory` reranker its seam. Phase 3's operator command reaches the same `deciderFromConfig`.

No caller of `Decide` is added (Phases 3 and 4 own the callers).

Purpose: make "turn it on with ENGRAM_ config" mean something observable (a constructed client plus a startup log line) while keeping "off" provably inert.
Output: the finished `internal/server/decider.go`, the `deps.decider` field, and the DEC-01 tests.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-decision-interface-jev-backend/02-CONTEXT.md
@.planning/phases/02-decision-interface-jev-backend/02-03-SUMMARY.md
@.planning/phases/02-decision-interface-jev-backend/02-04-SUMMARY.md
@internal/server/decider.go
</context>

<interfaces>
Existing code:

- `internal/server/tools.go`: `type deps struct` at line 36, with fields `st`, `em`, `now`, `summaryMaxChars`, `maxSummaryBytes`, `summaryQueue`, `usageQueue`, `embedderIdentity` and `writeCaps`, each with a dense doc comment. `func buildDepsFromEnv(sqm *telemetry.SummaryQueueMetrics, uqm *telemetry.UsageQueueMetrics) (*deps, error)` is at line 301. It calls `loadAndValidate()`, `ensureStoreFromConfig`, `embedderFromConfig` and `config.EmbedderIdentity`, then returns a `&deps{...}` literal. `slog.Info("async-on-write summaries enabled", ...)` at line 373 is the enablement-log precedent. `summaryTimeout`, `summaryDrainBytes`, `summaryDrainTimeout` and `summaryMaxTimeout` (lines 460-600) are the resolver precedent (`config.ParseNonNegativeIntCap`, `slog.Warn` on an unparseable non-empty value, a hardcoded default).
- `internal/server/tools_test.go`: `TestBuildDepsFromEnvLoadsConfigOnce` (line 5167) is Qdrant-gated through `storetest.Addr()` and `storetest.SkipOrFailNoQdrant(t)`, sets `ENGRAM_QDRANT_*` and `ENGRAM_SUMMARY_*` with `t.Setenv`, and wraps `configLoad`.
- `internal/server/decider.go` (plan 02-03): `func deciderFromConfig(cfg *config.Config) (decide.Decider, error)`. `""` returns nil, nil. `jev` returns `jev.New(base, cmp.Or(key, openaiKey), model, jev.WithHTTPTransport(otelhttp...))`. Anything else returns an error naming `ENGRAM_DECISIONS_PROVIDER`.
- `internal/decide/jev` options (plan 02-03): `WithHTTPTransport`, `WithTimeout`, `WithMaxTimeout`, `WithDrainBytes`, `WithDrainTimeout`, `WithConcurrency`, and `const DefaultModel`.
- `internal/decide` (plan 02-04): `Decider` now also has `DecideMany(ctx, []Request) []Result`, and `jev.Client.Decide` runs `req.Validate()` first, so every test request must be a valid question (non-empty instructions and criteria). That is why this plan depends on 02-04.
</interfaces>

## Artifacts this phase produces

| Artifact | Kind | Plan |
|----------|------|------|
| `02-SDK-EVALUATION.md` and `COVERAGE.md` | phase docs | 02-01, 02-02 |
| `internal/config`: `DecisionsConfig`, registry rows, gated `Validate` | config | 02-03 |
| `internal/decide` and `internal/decide/jev` | contract, backend | 02-03, 02-04, 02-07, 02-08 |
| `internal/server/decider.go`: full-option `deciderFromConfig`, `decisions*` resolvers, `logDeciderEnabled`, and `deps.decider` | wiring | 02-03, **02-05** |
| DEC-01 proofs: `TestDeciderFromConfigProviderUnset`, `TestBuildDepsFromEnvRejectsUnknownProvider`, gated `TestBuildDepsFromEnv*` decider assertions | tests | **02-05** |
| Helm, chart:validate, docs, `CLAUDE.md` row | chart, docs | 02-06 |
| `task eval:decisions` and `TestJevLive` | verification | 02-08 |

## Spec-less fallback ledger (this plan's share)

- **E01** (DEC-01, unclassified): **explicit** (construction half). Provider unset gives a nil Decider and zero requests to a counting server. An unknown provider fails startup.
- **E07** (DEC-03, concurrency): **explicit** (config half). `ENGRAM_DECISIONS_CONCURRENCY=2` caps the peak in flight at 2 through the real wiring.
- **Prohibition P1** (privacy, no egress when off): authored above, descriptor-less.

## Executor notes (every task)

- NEVER run any `git stash` subcommand. To inspect a pristine file, use `git show HEAD:<path>` into the scratchpad.
- Commit with an explicit pathspec, using Conventional Commits, and end the message with the session's attribution trailer lines. If the 1Password SSH signer is locked or errors, you MAY retry that ONE commit with `git -c commit.gpgsign=false commit …` (never change persistent git config) and must record it in the SUMMARY.
- `tools.go` is about 3000 lines. Read only the regions named in `read_first`, and use `rg -n` to locate anything else.
- Tests that change the default slog logger or the global TracerProvider must restore them in `t.Cleanup` and must not call `t.Parallel()`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Every ENGRAM_DECISIONS_* knob reaches the jev client; off constructs nothing; enablement is logged without the key (D-01, D-02, D-03, D-10)</name>
  <files>internal/server/decider.go, internal/server/decider_test.go</files>
  <read_first>
    - `internal/server/decider.go` and `internal/server/decider_test.go` (from plan 02-03)
    - `internal/server/tools.go` lines 540-600 (the `summaryDrainBytes`, `summaryDrainTimeout` and `summaryMaxTimeout` resolver shape) and lines 360-375 (the enablement log)
    - `internal/decide/jev/jev.go` (the option set and `DefaultModel`)
    - `02-CONTEXT.md` D-01, D-02, D-03, D-10
  </read_first>
  <behavior>
    - `TestDeciderFromConfigProviderUnset`: a counting httptest server. `cfg.Decisions` sets `BaseURL` to `srv.URL`, `APIKey` to `k`, `Timeout` to `x` and `Concurrency` to `0`, with `Provider` empty. Three `deciderFromConfig` calls each return a nil interface value (`d == nil`) and a nil error. The server count stays 0.
    - `TestDeciderFromConfigUnknownProvider`: provider `bogus` returns nil and an error containing `ENGRAM_DECISIONS_PROVIDER`.
    - `TestDeciderFromConfigAppliesOptions`, two subtests:
      - `timeout`: `Timeout` is `150ms` and the server sleeps 2 s. `Decide` returns an error in under 1.5 s.
      - `concurrency`: `Concurrency` is `2` and the server tracks its peak in-flight count while sleeping 100 ms. `DecideMany` over 6 valid noul requests gives a peak of exactly 2.
    - `TestDeciderResolverDefaults` (table over the five resolvers): empty or unparseable input gives the default. Drain bytes and drain timeout of `0` pass through as 0. A max timeout of `0` or `-1s` gives `10m`. A concurrency of `0` or `-1` gives 4. A valid value passes through.
    - `TestDecisionsModelDefaultMatchesJev`: with `t.Setenv("ENGRAM_DECISIONS_MODEL", "")`, `config.Load(nil)` gives `cfg.Decisions.Model == jev.DefaultModel`.
    - `TestDeciderEnabledLogLine`: install a JSON slog handler over a buffer (restore the default in `t.Cleanup`). The base is `https://user:s3cret-userinfo@gateway.example/openrouter?x=1`, and the key cases are: own key `own-key-VALUE`; empty own key with OpenAI key `openai-key-VALUE`; both empty. For each case the buffer holds one `typed decisions enabled` record with `endpoint_host` equal to `gateway.example` and the expected `api_key_source`, and does not contain `s3cret-userinfo`, `own-key-VALUE`, `openai-key-VALUE`, `/openrouter` or `x=1`.
    - RED first, recorded.
  </behavior>
  <action>
Implements D-01, D-02, D-03 (the fallback made visible, never the value) and D-10 (the concurrency wiring).

(1) `internal/server/decider.go`:
- Add five resolvers mirroring the `summary*` helpers exactly (`slog.Warn` naming the var and the value, then return the default):
  - `decisionsTimeout(cfg) time.Duration`: default `10*time.Second`. It accepts values of 0 or more, and `jev.New` resolves 0 to the ceiling.
  - `decisionsMaxTimeout(cfg) time.Duration`: default `10*time.Minute`. A non-positive value gives the default.
  - `decisionsDrainBytes(cfg) int64`: default `262144`, parsed with `config.ParseNonNegativeIntCap`. 0 is honored.
  - `decisionsDrainTimeout(cfg) time.Duration`: default `2*time.Second`. 0 is honored.
  - `decisionsConcurrency(cfg) int`: default `4`, parsed with `config.ParsePositiveIntCap`.
- Change the `jev` branch of `deciderFromConfig` to pass `jev.WithHTTPTransport(otelhttp.NewTransport(http.DefaultTransport))`, `jev.WithTimeout(decisionsTimeout(cfg))`, `jev.WithMaxTimeout(decisionsMaxTimeout(cfg))`, `jev.WithDrainBytes(decisionsDrainBytes(cfg))`, `jev.WithDrainTimeout(decisionsDrainTimeout(cfg))` and `jev.WithConcurrency(decisionsConcurrency(cfg))`.
- Keep the `""` branch returning `nil, nil` before any other work. Keep it the FIRST case.
- Add `func logDeciderEnabled(cfg *config.Config)`. Parse the base URL with `url.Parse` and log only `u.Host`. Compute `api_key_source` from which of `cfg.Decisions.APIKey` and `cfg.OpenAI.APIKey` is non-empty, in that order, else `none`. Call `slog.Info("typed decisions enabled", "provider", ..., "model", ..., "endpoint_host", ..., "api_key_source", ...)`. The doc comment says the key value and any URL userinfo, path or query are never logged (T-02-08).

(2) `internal/server/decider_test.go`: add the tests in `<behavior>`, keeping `TestDeciderTracerEndToEnd` intact. For the concurrency subtest, build the valid noul requests with `decide.Noul(...)`.

Order: tests (RED), then implementation (GREEN). Commit as `feat(server): thread ENGRAM_DECISIONS_* options into the jev decider and log enablement (DEC-01, D-02, D-10)` with pathspec `internal/server/decider.go internal/server/decider_test.go`.
  </action>
  <verify>
    <automated>out=$(go test ./internal/server/ -run '^(TestDeciderFromConfig|TestDeciderResolverDefaults|TestDecisionsModelDefaultMatchesJev|TestDeciderEnabledLogLine|TestDeciderTracerEndToEnd)' -count=1 -race -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e '^--- PASS: Test(DeciderFromConfigProviderUnset|DeciderFromConfigUnknownProvider|DeciderFromConfigAppliesOptions|DeciderResolverDefaults|DecisionsModelDefaultMatchesJev|DeciderEnabledLogLine|DeciderTracerEndToEnd)$' | wc -l)" -eq 7</automated>
    <fails_when>non-zero exit, or not exactly seven named top-level PASS lines</fails_when>
  </verify>
  <acceptance_criteria>
    - `deciderFromConfig`'s first case is the empty provider: `awk '/^func deciderFromConfig/,/^}/' internal/server/decider.go | rg -n 'case ' | head -1` shows `case "":`.
    - All six jev options are passed: `awk '/^func deciderFromConfig/,/^}/' internal/server/decider.go | rg -o 'jev[.]With[A-Za-z]+' | sort -u | wc -l` prints `6`.
    - `logDeciderEnabled`'s slog call carries exactly four attribute keys: `awk '/^func logDeciderEnabled/,/^}/' internal/server/decider.go | rg -o '"(provider|model|endpoint_host|api_key_source)"' | sort -u | wc -l` prints `4`. `TestDeciderEnabledLogLine` is the behavioral proof that no key value, userinfo, path or query reaches the output.
  </acceptance_criteria>
  <done>Every decisions knob reaches the jev client with summarize-style fallbacks. The concurrency and timeout settings are proven to take effect. Off constructs nothing and sends nothing. The enablement log names the key source and host only. RED then GREEN recorded, and the change is committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: deps.decider built at startup through the one gated seam, with DEC-01's startup proofs</name>
  <files>internal/server/tools.go, internal/server/tools_test.go</files>
  <read_first>
    - `internal/server/tools.go` lines 25-110 (imports and the `deps` struct) and 296-330 (`buildDepsFromEnv`)
    - `internal/server/tools_test.go` lines 5140-5225 (`TestStoreAndEmbedderFromEnvNoEnsureValidatesConfig`, `TestBuildDepsFromEnvLoadsConfigOnce`)
    - `internal/server/decider.go` (post-Task-1)
  </read_first>
  <behavior>
    - `TestBuildDepsFromEnvRejectsUnknownProvider` (hermetic, no Qdrant): `t.Setenv("ENGRAM_DECISIONS_PROVIDER", "bogus")`. `buildDepsFromEnv(nil, nil)` returns an error containing `ENGRAM_DECISIONS_PROVIDER`, before any store dial.
    - `TestBuildDepsFromEnvLoadsConfigOnce` (existing, Qdrant-gated): add `t.Setenv("ENGRAM_DECISIONS_PROVIDER", "")`, and assert `d.decider == nil` alongside the existing assertions.
    - `TestBuildDepsFromEnvConstructsDecider` (new, Qdrant-gated with the same skip idiom): a counting httptest server. `t.Setenv` sets the provider to `jev`, `ENGRAM_DECISIONS_BASE_URL` to `srv.URL + "/api"`, `ENGRAM_DECISIONS_API_KEY` to `k`, plus the Qdrant and summary isolation vars the sibling test sets. `d.decider` is non-nil, and the counting server saw 0 requests (no startup probe).
    - RED first, recorded.
  </behavior>
  <action>
Implements D-01 at the startup seam (RESEARCH A4 resolved: wire it into `deps` now).

(1) `internal/server/tools.go`:
- Import `github.com/seanb4t/engram/internal/decide`.
- Add a field `decider decide.Decider` to `deps`, after `writeCaps`. Its doc comment, in the file's density, says:
  - it is nil unless `ENGRAM_DECISIONS_PROVIDER` is set (D-01), decided once in `buildDepsFromEnv` through `deciderFromConfig`;
  - this phase adds no caller;
  - Phase 4's `search_memory` reranker reads it from here;
  - per the `decide.Decider` contract, a decision failure never fails the handler that asked, so callers treat any error as "no decision".
- In `buildDepsFromEnv`, after the embedder identity is computed, add `dec, err := deciderFromConfig(cfg)` with the standard `if err != nil { return nil, err }`. Then `if dec != nil { logDeciderEnabled(cfg) }`. Add `decider: dec,` to the returned literal.
- Change nothing else in `buildDepsFromEnv`.

(2) `internal/server/tools_test.go`: the three changes in `<behavior>`. Reuse the existing collection-name helper (`testCollection`) for the new gated test.

Order: tests (RED), then implementation (GREEN). Commit as `feat(server): build deps.decider at startup only when a provider is set (DEC-01, D-01)` with pathspec `internal/server/tools.go internal/server/tools_test.go`.
  </action>
  <verify>
    <automated>out=$(go test ./internal/server/ -run '^TestBuildDepsFromEnvRejectsUnknownProvider$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; printf '%s\n' "$out" | rg -q -e '^--- PASS: TestBuildDepsFromEnvRejectsUnknownProvider'</automated>
    <fails_when>non-zero exit or no `--- PASS: TestBuildDepsFromEnvRejectsUnknownProvider` line</fails_when>
    <automated>out=$(go test ./internal/server/ -run '^TestBuildDepsFromEnv(LoadsConfigOnce|ConstructsDecider)$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e '^--- (PASS|SKIP): TestBuildDepsFromEnv(LoadsConfigOnce|ConstructsDecider)$' | wc -l)" -eq 2</automated>
    <fails_when>non-zero exit, or either Qdrant-gated test is missing from the output (it must PASS, or SKIP only when no Qdrant is available; CI's fail-closed run turns that SKIP into a failure)</fails_when>
    <automated>go test ./internal/server/ -count=1</automated>
    <fails_when>non-zero exit (an existing server test regressed)</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -o 'decider:\s+dec,' internal/server/tools.go | wc -l` prints `1`, and `rg -o 'deciderFromConfig[(]cfg[)]' internal/server/tools.go | wc -l` prints `1`.
    - No caller of `Decide` or `DecideMany` was added in this phase: `rg -o '[.]decider[.]Decide' internal/server cmd | wc -l` prints `0`.
    - `TestBuildDepsFromEnvLoadsConfigOnce` asserts `d.decider == nil`: `rg -o 'd[.]decider != nil' internal/server/tools_test.go | wc -l` prints at least `1`.
  </acceptance_criteria>
  <done>Startup builds `deps.decider` only when a provider is set, rejects an unknown provider before dialing the store, and makes no outbound call when constructing an enabled decider. The existing single-load test asserts nil when the feature is off. RED then GREEN recorded, and the change is committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator env → startup | `ENGRAM_DECISIONS_*` resolved once in `buildDepsFromEnv` |
| engram → Decisions endpoint | only reachable when the provider is set |
| engram logs → log pipeline | the enablement line exported through the slog/OTLP bridge |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-13 | Information disclosure | third-party egress while the feature is off | high | mitigate | `deciderFromConfig`'s first case returns nil for an empty provider. `TestDeciderFromConfigProviderUnset` asserts zero requests to a configured base (prohibition P1) |
| T-02-05 | Information disclosure | enablement log | high | mitigate | `logDeciderEnabled` logs the host only (no userinfo, path or query) and the key's source name, never its value. `TestDeciderEnabledLogLine` greps the captured output for the secret sentinels |
| T-02-08 | Information disclosure | key fallback sends `ENGRAM_OPENAI_API_KEY` to the decisions host | medium | mitigate | `api_key_source=ENGRAM_OPENAI_API_KEY` makes the D-03 fallback visible at startup. Plan 02-06 documents the cross-gateway implication |
| T-02-10 | Denial of service / cost | concurrency setting | medium | mitigate | `decisionsConcurrency` never passes a non-positive value, Validate rejects one, and the peak-in-flight test proves the cap |
</threat_model>

<verification>
- The seven named `internal/server` decider tests pass with `-race`. The gated startup tests PASS (CI) or SKIP only without Qdrant. `go test ./internal/server/ -count=1` is green.
- `golangci-lint run ./internal/server/...` is clean.
</verification>

<success_criteria>
- ROADMAP Phase 2 success criterion 1 holds: with the provider unset, engram constructs no decider and makes no decision call, and setting `ENGRAM_DECISIONS_PROVIDER=jev` (plus a base URL) turns it on at startup.
- Success criterion 3's config half holds: the Jev backend takes its own base URL, key (falling back to the shared key), model and timeout.
</success_criteria>

<output>
Create `.planning/phases/02-decision-interface-jev-backend/02-05-SUMMARY.md` when done. Record whether the Qdrant-gated tests ran or skipped locally.
</output>
