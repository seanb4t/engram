---
phase: 02-decision-interface-jev-backend
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - internal/config/config.go
  - internal/decide/decide.go
  - internal/decide/jev/jev.go
  - internal/server/decider.go
  - internal/server/decider_test.go
  - internal/config/registry.go
  - internal/config/validate.go
  - internal/config/decisions_config_test.go
  - go.mod
  - go.sum
autonomous: true
requirements:
  - DEC-01
  - DEC-02
  - DEC-03
  - DEC-06

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Tracer: a `config.Config` with `Decisions.Provider` `jev` and `Decisions.BaseURL` `<srv>/api`, passed through `deciderFromConfig`, yields a `decide.Decider` whose `Decide` sends one POST to exactly `/api/alpha/decisions` and returns a typed noul answer, the dated model snapshot and the usage (DEC-02, DEC-03)"
    - "The API key resolves at the wiring seam via `cmp.Or(cfg.Decisions.APIKey, cfg.OpenAI.APIKey)`. An empty `Decisions.APIKey` sends `Bearer <OpenAI key>`, and a set one wins (D-03, the ChatAPIKey precedent)"
    - "Every `Decide` call emits one OTLP span named `decide` carrying `engram.decide.provider`, `engram.decide.model`, `engram.decide.questions` and `engram.decide.status`. On success it also carries `engram.decide.model_snapshot`, `engram.decide.input_tokens`, `engram.decide.output_tokens` and `engram.decide.cost_usd`, with the cost as a float64 attribute within 1e-12 of the decoded `usage.cost`. Latency is the span duration. A debug-level slog line is written for every call (DEC-06, D-13)"
    - "The nine `ENGRAM_DECISIONS_*` vars are registered exactly once each, with no Legacy and no Flag. Defaults: provider empty, model `typesafe/jev-1.13`, timeout `10s`, max_timeout `10m`, drain_bytes `262144`, drain_timeout `2s`, concurrency `4`. Each round-trips from the environment through `config.Load(nil)` (D-01, D-02)"
    - "`Config.Validate` checks the provider enum unconditionally (empty or `jev`). Every other decisions field is checked only when the provider is `jev`, so a config without the provider validates exactly as before, whatever the other `ENGRAM_DECISIONS_*` vars hold (D-01, spec-less edge E01)"
    - "With the provider `jev` and `ENGRAM_DECISIONS_BASE_URL` empty, `Validate` fails with an error naming `ENGRAM_DECISIONS_BASE_URL` that says it does not fall back to `ENGRAM_OPENAI_BASE_URL`, even when that var holds a valid URL. Errors follow `Validate`'s plain `ENGRAM_VAR %q: reason` style, joined with `errors.Join` (D-03 as corrected, spec-less edge E05)"
    - "Branch fidelity: `internal/decide/jev` is built on the branch `02-SDK-EVALUATION.md` records in its `resolution:` line. `reject-hand-write` leaves engram's `go.mod` without `github.com/OpenRouterTeam/go-sdk`. `adopt-and-wrap` adds it at exactly the recipe's version (D-06)"
  artifacts:
    - path: "internal/decide/decide.go"
      provides: "Decider interface (Decide), Request, State, Question, QuestionNoul, Noul, Answer, Response, Usage, caller-contract doc"
      contains: "type Decider interface"
    - path: "internal/decide/jev/jev.go"
      provides: "Jev backend: DefaultModel, Client, Option set, New, Decide, decide span"
      contains: "func New(baseURL, apiKey, model string, opts ...Option) *Client"
    - path: "internal/server/decider.go"
      provides: "deciderFromConfig gate: provider empty constructs nothing, jev constructs the client, unknown errors"
      contains: "func deciderFromConfig(cfg *config.Config) (decide.Decider, error)"
    - path: "internal/config/config.go"
      provides: "DecisionsConfig and Config.Decisions"
      contains: "type DecisionsConfig struct"
    - path: "internal/config/decisions_config_test.go"
      provides: "TestDecisionsRegistryEntries, TestDecisionsValidate"
      contains: "func TestDecisionsValidate("
  key_links:
    - from: "internal/server/decider.go"
      to: "internal/decide/jev/jev.go"
      via: "the wiring seam constructs the jev backend only for provider jev (D-01)"
      pattern: "jev[.]New[(]"
    - from: "internal/server/decider.go"
      to: "internal/config/config.go"
      via: "key-only fallback resolved at the wiring seam (D-03)"
      pattern: "cmp[.]Or[(]cfg[.]Decisions[.]APIKey, cfg[.]OpenAI[.]APIKey[)]"
    - from: "internal/decide/jev/jev.go"
      to: "internal/decide/decide.go"
      via: "the backend satisfies the provider-neutral interface (DEC-02)"
      pattern: "decide[.]Decider"
    - from: "internal/decide/jev/jev.go"
      to: "internal/decide/jev/jev.go"
      via: "the endpoint is the base URL plus the fixed Decisions suffix (D-03)"
      pattern: "/alpha/decisions"
    - from: "internal/config/validate.go"
      to: "internal/config/registry.go"
      via: "the no-fallback base URL rule names the registered var (D-03)"
      pattern: "ENGRAM_DECISIONS_BASE_URL is empty"
---

<objective>
Prove the typed-decision architecture end to end on one thin path, then make that path reachable from real operator config.

- **Tracer (Task 1):** config struct, then the wiring seam, then the `decide.Decider` contract, then the Jev backend, then an httptest `/alpha/decisions` endpoint, then back to a typed noul answer with usage and a `decide` span. Only one question type (noul) and one success path are covered. The failure classification, choice and score, validation and DecideMany are expansion plans (02-04, 02-07, 02-08). They extend this slice without changing its architecture.
- **Enablement (Task 2):** register the nine `ENGRAM_DECISIONS_*` vars and add a gated `Validate` block, so the slice turns on from the environment and stays byte-identical when off.

Decisions implemented: D-01 (provider enum, off by default), D-02 (capability-named prefix, knobs mirroring embed/summarize), D-03 (key-only fallback at the wiring seam, no base-URL fallback, `Validate`'s real error style), D-06 (build the branch 02-02 resolved), D-08 (package layout), D-10 (`Decide(ctx, Request{State, Questions map[name]Question})`), D-13 (the `decide` span and slog line).

The tracer follows the adopt-or-reject evaluation because D-07 requires the evaluation record to precede any client code.

Purpose: catch an architectural dead end (wiring, contract, transport, telemetry) after one commit, not after six plans.
Output: `internal/decide`, `internal/decide/jev`, `internal/server/decider.go`, `DecisionsConfig`, the registry rows, the gated validation, and tests.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-decision-interface-jev-backend/02-CONTEXT.md
@.planning/phases/02-decision-interface-jev-backend/02-RESEARCH.md
@.planning/phases/02-decision-interface-jev-backend/02-PATTERNS.md
@.planning/phases/02-decision-interface-jev-backend/02-SDK-EVALUATION.md
@internal/embed/embed.go
@internal/httpdrain/httpdrain.go
</context>

<interfaces>
Existing code (verified at planning time):

- `internal/config/config.go`: the `type Config struct` fields run `Server, Qdrant, Embed, Memory, Summarize, OpenAI, OIDC, ServiceAuth, UI, Connect, Log, Usage, Client, Setup`, each with a `koanf:"..."` tag. Every field is a `string`, validated by consumers ("keep as strings"). `type OpenAIConfig` has `BaseURL`, `APIKey`, `EmbeddingsURL`, `ChatBaseURL` and `ChatAPIKey`.
- `internal/config/registry.go`: `type field struct{ Key, Env, Legacy, Flag, Default string }` and `var registry = []field{...}`. The `embed.*`/`summarize.*` drain/timeout rows (lines 40-93) are the precedent for comment density and for no Legacy and no Flag. An explicitly empty env var keeps the registry default (`TestLoadEmptyEnvPreservesDefault`).
- `internal/config/validate.go`: `func (c *Config) Validate() error` appends to `errs []error` and returns `fmt.Errorf("invalid configuration: %w", errors.Join(errs...))`. Helpers: `ParseNonNegativeIntCap(string) (int, error)` (zero valid) and `ParsePositiveIntCap(string) (int, error)`. The URL-shape switch idiom is at lines 138-149 (empty, `url.Parse` error, scheme not http/https, empty host). The `Summarize.Model` gate is at lines 238-281.
- `internal/config/providerbounds_test.go`: the table-driven registry-shape plus `Load(nil)` round-trip plus `Validate` pattern to mirror. Its fixtures `validConfig()` and `summarizeEnabled()` build full valid configs.
- `internal/server/tools.go`: `summarizerFromConfig` (line 652) is the `cmp.Or` key-fallback precedent. Provider clients get `otelhttp.NewTransport(http.DefaultTransport)` via `WithHTTPTransport`. The `deps` struct is at line 36 (this plan does not touch it; plan 02-05 does).
- `internal/embed/embed.go`: the whole New+Option contract to mirror. Drain defaults are set in the struct literal before the options loop, so an explicit 0 is honored. `maxTimeout` falls back after the loop, and a non-positive request timeout resolves to it. The span pattern is at lines 349-363, `maxErrorBodyBytes = 4096` at line 38, and `httpdrain.Drain(resp.Body, c.drainBytes, c.drainTimeout)` sits on both response paths.
- `internal/embed/embed_test.go` lines 392-427: the `tracetest.NewSpanRecorder` pattern (install a TracerProvider, restore it in `t.Cleanup`, read `sr.Ended()`).

Spike-verified wire shapes (`.claude/skills/spike-findings-engram/references/decision-transport.md`):

- Request: `{"model":"typesafe/jev-1.13","state":{...},"questions":{"<name>":{"type":"noul","instructions":"...","criteria":{"true":"...","false":"..."}}}}`
- Response: `{"model":"typesafe/jev-1.13-20260917","answers":{"<name>":{"type":"noul","noul":0.92}},"usage":{"input_tokens":497,"output_tokens":67,"cost":0.000020874},"id":"gen-dec-...","provider":"TypeSafe"}`
</interfaces>

## Artifacts this phase produces

| Artifact | Kind | Plan |
|----------|------|------|
| `02-SDK-EVALUATION.md` and `COVERAGE.md` | phase docs | 02-01, 02-02 |
| `sdk-eval/` nested Go module | evaluation harness | 02-02 |
| `internal/config`: `DecisionsConfig`, nine `ENGRAM_DECISIONS_*` registry rows, gated `Validate` block, `TestDecisionsRegistryEntries`, `TestDecisionsValidate` | config | **02-03** |
| `internal/decide`: `Decider`, `Request`, `State`, `Question`, `QuestionNoul`, `Noul`, `Answer`, `Response`, `Usage` | contract package | **02-03** (then 02-04) |
| `internal/decide/jev`: `DefaultModel`, `Client`, options, `New`, `Decide`, `decide` span | backend | **02-03** (then 02-04, 02-07, 02-08) |
| `internal/server/decider.go` (`deciderFromConfig`) and `TestDeciderTracerEndToEnd` | wiring | **02-03** (then 02-05) |
| Helm values, chart:validate, configure/deploy docs, `CLAUDE.md` row | chart, docs | 02-06 |
| `task eval:decisions` and `TestJevLive` | verification | 02-08 |

## Spec-less fallback ledger (this plan's share)

- **E01** (DEC-01, unclassified): resolved **explicit**. `TestDecisionsValidate`'s inert row: a config without the provider validates even with every other decisions field malformed. Plans 02-05 and 02-06 cover the construction and Helm halves.
- **E05** (DEC-03, empty): resolved **explicit** (partial). An empty base URL with provider `jev` is a named `Validate` error with no fallback. Plans 02-04 and 02-07 cover the empty questions, empty batch and empty key halves.
- **E11** (DEC-06, precision): resolved **explicit** (partial). `cost_usd` is a float64 span attribute within 1e-12 of the decoded cost, never formatted or rounded. Plan 02-07 extends this to the failure paths.

## Executor notes (every task)

- NEVER run any `git stash` subcommand. To inspect a pristine file, use `git show HEAD:<path>` into the scratchpad.
- Commit with an explicit pathspec, using Conventional Commits, and end the message with the session's attribution trailer lines. If the 1Password SSH signer is locked or errors, you MAY retry that ONE commit with `git -c commit.gpgsign=false commit …` (never change persistent git config) and must record it in the SUMMARY.
- Every new Go file carries the two-line SPDX header (`// SPDX-License-Identifier: Apache-2.0`, `// Copyright 2026 Sean Brandt`). `task license:check` must stay green.
- Read `02-SDK-EVALUATION.md`'s `resolution:` line FIRST. Build only that branch. The exported surface in this plan is identical on both branches. Only `jev.go`'s internals and `go.mod`/`go.sum` differ.
- Test probabilities by range only (for example `0.87 <= p && p <= 0.97` for a 0.92 fixture), never by exact equality.
- Never write a real gateway hostname in code, tests or comments. Use `gateway.example`.

<tasks>

<task type="tracer">
  <name>Task 1: End to end — a configured jev decider answers one Noul question at {base}/alpha/decisions, with a decide span</name>
  <precondition>`.planning/phases/02-decision-interface-jev-backend/02-SDK-EVALUATION.md` has a line matching `^resolution: (reject-hand-write|adopt-and-wrap)$` (plan 02-02 Task 3)</precondition>
  <reversibility rating="costly">D-06: on the adopt-and-wrap branch this adds a direct module dependency that releases several times a day; on reject-hand-write, engram owns the wire structs for an alpha API. Either sits behind decide.Decider, so a swap touches internal/decide/jev only.</reversibility>
  <files>internal/config/config.go, internal/decide/decide.go, internal/decide/jev/jev.go, internal/server/decider.go, internal/server/decider_test.go</files>
  <read_first>
    - `.planning/phases/02-decision-interface-jev-backend/02-SDK-EVALUATION.md`: the `resolution:` line and the single recipe section (`## Hand-write recipe` or `## Wrap recipe`)
    - `internal/embed/embed.go` (whole file: the New+Option shape, bounded response handling, span)
    - `internal/httpdrain/httpdrain.go`
    - `internal/server/tools.go` lines 600-672 (`embedderFromConfig`, `summarizerFromConfig`)
    - `internal/config/config.go` lines 1-40 and 150-215 (`Config`, `SummarizeConfig` doc style, `OpenAIConfig`)
    - `internal/embed/embed_test.go` lines 392-427 (span assertion pattern)
    - `02-CONTEXT.md` D-01, D-03, D-06, D-08, D-10, D-13. `02-PATTERNS.md` sections on `jev.go`, `decide.go` and `tools.go`
  </read_first>
  <action>
Implements D-01 (nothing constructed when off), D-03 (key-only fallback), D-06 (the resolved branch), D-08 (packages), D-10 (the Decide shape) and D-13 (the span and slog line) on one path.

(1) `internal/config/config.go`: add `Decisions DecisionsConfig` with tag `koanf:"decisions"` to `Config`, after `OpenAI`. Add `type DecisionsConfig struct` with nine string fields and koanf tags: `Provider` (`provider`), `BaseURL` (`base_url`), `APIKey` (`api_key`), `Model` (`model`), `Timeout` (`timeout`), `MaxTimeout` (`max_timeout`), `DrainBytes` (`drain_bytes`), `DrainTimeout` (`drain_timeout`), `Concurrency` (`concurrency`). The doc comment must say:
- Presence enables the feature: an empty Provider constructs no client and makes no call (D-01, like `SummarizeConfig.Model`).
- Only APIKey falls back, to `ENGRAM_OPENAI_API_KEY`, resolved at the wiring seam. BaseURL never falls back (D-03).
- Values stay strings and are validated by `Config.Validate` only when the provider is set.

(2) New `internal/decide/decide.go` (SPDX). Package doc: a provider-neutral, advisory-only typed-decision contract in System One vocabulary (shared state plus named Choice/Score/Noul questions in, typed probabilities out). This package does no I/O. Backends live in `internal/decide/<name>` (jev now, an emulator later, DEC-F2). Declare:
- `type Decider interface { Decide(ctx context.Context, req Request) (Response, error) }`. Its doc comment carries two sentences verbatim: `A decision failure never fails the surrounding read or sweep: callers treat an error as "no decision" and continue.` and `Decisions are advisory: callers surface answers and never act on them automatically.`
- `type State map[string]any`
- `type QuestionType string` with `const QuestionNoul QuestionType = "noul"`
- `type Question struct` with `Type QuestionType`, `Instructions string`, `WhenTrue string` and `WhenFalse string` (the noul criteria descriptions). Plan 02-04 adds the choice and score fields.
- `func Noul(instructions, whenTrue, whenFalse string) Question`
- `type Request struct { State State; Questions map[string]Question }`
- `type Answer struct` with `Type QuestionType` and `Probability float64` (noul: P(true), carried verbatim from the provider, never rounded or renormalized).
- `type Usage struct` with `InputTokens int64`, `OutputTokens int64` and `CostUSD *float64` (nil when the provider omits cost).
- `type Response struct` with `Answers map[string]Answer`, `Model string` (the dated snapshot the provider reports), `ID string`, `Provider string` and `Usage *Usage` (nil when the provider omits usage).

(3) New `internal/decide/jev/jev.go` (SPDX). Package doc: the Jev backend over OpenRouter's Decisions API at `{base}/alpha/decisions`. It works against OpenRouter directly (`https://openrouter.ai/api`) and through a LiteLLM pass-through (`https://<gateway>/openrouter`), and it never uses the chat base URL (D-03). Declare:
- `const DefaultModel = "typesafe/jev-1.13"` (pinned; the floating alias moves thresholds and is never used, RESEARCH Pitfall 3). An unexported `const decisionsPath = "/alpha/decisions"`.
- `var tracer = otel.Tracer("github.com/seanb4t/engram/internal/decide/jev")`
- Defaults: `defaultTimeout = 10 * time.Second` (the spike's worst observed call was 3.5 s at concurrency 4; 10 s leaves room for D-11's single retry inside one budget), `defaultMaxTimeout = 10 * time.Minute`, `defaultDrainBytes = 256 << 10`, `defaultDrainTimeout = 2 * time.Second`, `defaultConcurrency = 4` (D-10), and `maxErrorBodyBytes = 4096` with the "copied verbatim from `internal/embed` and `internal/summarize`" comment.
- `type Client struct` (unexported fields: baseURL, apiKey, model, endpoint, http, timeout, maxTimeout, drainBytes, drainTimeout, concurrency) and `type Option func(*Client)`.
- `WithHTTPTransport`, `WithTimeout`, `WithMaxTimeout`, `WithDrainBytes`, `WithDrainTimeout` and `WithConcurrency`, with `internal/embed`'s exact semantics. Drain defaults go in the struct literal, so 0 is honored. `maxTimeout` and `concurrency` fall back after the loop when non-positive. A non-positive timeout resolves to `maxTimeout`, never unbounded.
- `func New(baseURL, apiKey, model string, opts ...Option) *Client`. An empty model becomes `DefaultModel`. `endpoint = strings.TrimRight(baseURL, "/") + decisionsPath`. `http.Client.Timeout` is set to the resolved timeout as a backstop.
- `var _ decide.Decider = (*Client)(nil)`
- `func (c *Client) Decide(ctx context.Context, req decide.Request) (decide.Response, error)`, which:
  1. starts span `decide` with `engram.decide.provider` = `jev`, `engram.decide.model` = c.model and `engram.decide.questions` = `len(req.Questions)` (int);
  2. derives one `context.WithTimeout(ctx, c.timeout)` budget for the whole call;
  3. sends the request;
  4. on 200, decodes the noul answers plus model, id, provider and usage into `decide.Response`, then calls `httpdrain.Drain`;
  5. on a non-200, reads at most `maxErrorBodyBytes`, calls `httpdrain.Drain`, and returns `fmt.Errorf("decisions: status %d: %s", status, trimmed body)`. Plan 02-07 replaces this with named classification;
  6. on success, sets `engram.decide.model_snapshot`, `engram.decide.input_tokens` and `engram.decide.output_tokens` (int64), `engram.decide.cost_usd` (`attribute.Float64`, only when CostUSD is non-nil) and `engram.decide.status` = `ok`;
  7. on error, sets `engram.decide.status` = `error` and calls `RecordError` and `SetStatus(codes.Error, ...)`;
  8. writes exactly one `slog.DebugContext(ctx, "decide", ...)` line with the keys `provider`, `model`, `model_snapshot`, `questions`, `input_tokens`, `output_tokens`, `cost_usd`, `status` and `duration_ms`. The line never includes state, instructions, criteria or the API key.
  - A non-noul answer type in the response returns `fmt.Errorf("decisions: unsupported answer type %q for question %q", ...)` (plan 02-08 maps choice and score).
  - The Authorization header is `Bearer <apiKey>` and is omitted entirely when the key is empty.
  - **Branch `reject-hand-write`:** use `net/http` with `encoding/json` and unexported wire structs (`wireRequest`, `wireQuestion` with criteria as `map[string]string` for noul, `wireResponse`, `wireAnswer`, `wireUsage`). POST to `c.endpoint` with `Content-Type: application/json`. Decode the success body through `io.LimitReader(resp.Body, 1<<20)`. Plan 02-07 adds the too-large classification and the option.
  - **Branch `adopt-and-wrap`:** run the exact `go get github.com/OpenRouterTeam/go-sdk@<version>` the `## Wrap recipe` names, then `go mod tidy`. Build the SDK client from the recipe's option calls, passing the `*http.Client` via `openrouter.WithClient` and turning off SDK retries as the recipe records. Apply the recipe's endpoint mapping, so `c.endpoint` is still the URL that reaches the wire. Map `decide.Request` to `components.DecisionsRequest` and the response back to `decide.Response` for noul only. Do not touch `go.mod`/`go.sum` on the other branch.

(4) New `internal/server/decider.go` (SPDX). `func deciderFromConfig(cfg *config.Config) (decide.Decider, error)`:
- Provider `""` returns `nil, nil`. It constructs nothing and touches no network (D-01).
- Provider `"jev"` returns `jev.New(cfg.Decisions.BaseURL, cmp.Or(cfg.Decisions.APIKey, cfg.OpenAI.APIKey), cfg.Decisions.Model, jev.WithHTTPTransport(otelhttp.NewTransport(http.DefaultTransport))), nil`. Plan 02-05 adds the config-driven timeout, drain and concurrency options.
- Any other value returns `nil` with `fmt.Errorf("ENGRAM_DECISIONS_PROVIDER %q: unknown provider (want \"\" or \"jev\")", p)`.
- The doc comment cites D-01 and D-03 and notes that the base URL does not fall back.

(5) New `internal/server/decider_test.go` (SPDX), `func TestDeciderTracerEndToEnd(t *testing.T)`. An httptest server records the method, path, `Authorization` and decoded JSON body, and replies with a const `tracerNoulResponse`: spike 001's `happy` probe body with the `relation` choice answer removed, keeping only `same_subject` noul 0.92, usage 497/67/0.000020874, model `typesafe/jev-1.13-20260917`, the id and provider `TypeSafe`. Install a `tracetest.NewSpanRecorder` TracerProvider and restore it in `t.Cleanup`. Build `cfg := &config.Config{}` with `Decisions.Provider` `jev`, `Decisions.BaseURL` `srv.URL+"/api"`, an empty `Decisions.APIKey`, and `OpenAI.APIKey` `fallback-key`. Call `deciderFromConfig` and `Decide` with state `{"record_a": "synthetic text A", "record_b": "synthetic text B"}` and the question `same_subject` built by `decide.Noul(...)`. Assert:
- the method is POST and the path is exactly `/api/alpha/decisions`;
- `Authorization` is `Bearer fallback-key`;
- the body has `model` `typesafe/jev-1.13`, `questions.same_subject.type` `noul`, and criteria with non-empty `true` and `false`;
- `Answers["same_subject"].Probability` is in [0.87, 0.97], `Model` is the snapshot, `Usage.InputTokens` is 497 and `*Usage.CostUSD` is within 1e-12 of 0.000020874;
- among `sr.Ended()` (otelhttp adds its own client span, so do not index `[0]`) there is exactly one span named `decide` carrying the eight D-13 attributes with the values above (`status` = `ok`).

Add the subtest `own key wins`: a non-empty `Decisions.APIKey` of `own-key` sends `Bearer own-key`.

Commit as `feat(decide): trace one typed decision end to end through the jev backend (DEC-02, DEC-03, DEC-06)`. Use the pathspec of this task's files, plus `go.mod go.sum` on the adopt branch only.
  </action>
  <verify>
    <automated>out=$(go test ./internal/server/ -run '^TestDeciderTracerEndToEnd$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^\s*--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e '--- PASS: TestDeciderTracerEndToEnd' | wc -l)" -ge 2</automated>
    <fails_when>non-zero exit, or fewer than two `--- PASS: TestDeciderTracerEndToEnd` lines (the parent plus the `own key wins` subtest; a filter matching nothing exits 0 with no PASS lines)</fails_when>
    <automated>go build ./... &amp;&amp; go vet ./internal/decide/... ./internal/server/ ./internal/config/ &amp;&amp; golangci-lint run ./internal/decide/... ./internal/server/... ./internal/config/...</automated>
    <fails_when>non-zero exit (a compile error, vet finding or lint finding)</fails_when>
    <automated>f=.planning/phases/02-decision-interface-jev-backend/02-SDK-EVALUATION.md; n=$(rg -o -F 'github.com/OpenRouterTeam/go-sdk' go.mod | wc -l); if rg -q '^resolution: reject-hand-write$' "$f"; then test "$n" -eq 0; else test "$n" -eq 1; fi</automated>
    <fails_when>non-zero exit (the module dependency does not match the recorded D-06 resolution)</fails_when>
  </verify>
  <acceptance_criteria>
    - The interface carries the caller contract: `rg -o -F 'A decision failure never fails the surrounding read or sweep' internal/decide/decide.go | wc -l` prints `1`, and `rg -o -F 'Decisions are advisory' internal/decide/decide.go | wc -l` prints `1`.
    - `internal/decide` performs no network I/O: `rg -o '"net/http"' internal/decide/decide.go | wc -l` prints `0`.
    - The pinned default: `rg -o -F 'const DefaultModel = "typesafe/jev-1.13"' internal/decide/jev/jev.go | wc -l` prints `1`, and no non-comment line names the floating alias: `rg -v '^\s*//' internal/decide/jev/jev.go | rg -o -F 'jev-latest' | wc -l` prints `0`.
    - The slog line carries no content keys: `rg -o 'slog[.]DebugContext[(]ctx, "decide"' internal/decide/jev/jev.go | wc -l` prints `1`.
    - `git ls-files internal/decide` lists `internal/decide/decide.go` and `internal/decide/jev/jev.go` only.
  </acceptance_criteria>
  <done>One noul decision flows from a `config.Config` through `deciderFromConfig`, `decide.Decider` and the jev backend to `/api/alpha/decisions` and back as a typed answer with usage and a fully attributed `decide` span. It is built on the recorded D-06 branch, lint-clean and committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Operators enable decisions through ENGRAM_DECISIONS_* — nine registry rows and a provider-gated Validate block (D-01, D-02, D-03)</name>
  <reversibility rating="costly">D-01: ENGRAM_DECISIONS_PROVIDER and its eight siblings become a registered, documented operator surface; renaming or retyping one later is an upgrade-guide change for every deployment that set it.</reversibility>
  <files>internal/config/registry.go, internal/config/validate.go, internal/config/decisions_config_test.go</files>
  <read_first>
    - `internal/config/registry.go` (whole file)
    - `internal/config/validate.go` (whole file)
    - `internal/config/providerbounds_test.go` (whole file: the table shape to mirror, and the `validConfig()` fixture)
    - `internal/config/config_test.go` lines 13-55 (`TestLoadDefaults`, `TestLoadEmptyEnvPreservesDefault`)
    - `02-CONTEXT.md` D-01, D-02, D-03. `02-RESEARCH.md` "Pattern 2" (the Validate-style correction) and "Pitfall 4" (no response-bytes registry row)
  </read_first>
  <behavior>
    - `TestDecisionsRegistryEntries` drives one table of nine `{key, env, default, get}` rows. Subtests: `registry entry` (exactly one row per key, empty Legacy, empty Flag, the expected Default), `from env` (`t.Setenv` then `Load(nil)` reads it back), and `unset` (with the var explicitly set to empty, `Load(nil)` reads the registered default).
    - `TestDecisionsValidate` mutates `validConfig()`. Expected results:
      - Provider empty, with every other decisions field malformed (base `ftp://`, timeout `x`, max_timeout `0`, drain_bytes `-1`, concurrency `0`): nil (the inert-when-off row, E01).
      - Provider `bogus`: an error naming `ENGRAM_DECISIONS_PROVIDER`.
      - Provider `jev` with an empty base: an error containing `ENGRAM_DECISIONS_BASE_URL is empty` and `does not fall back to ENGRAM_OPENAI_BASE_URL`, even though `validConfig()` sets a valid `OpenAI.BaseURL` (E05).
      - Provider `jev` with base `ftp://x`: a scheme error. With base `https://`: a missing-host error. With base `https://openrouter.ai/api`: nil.
      - With provider `jev` and a valid base: timeout `0` passes and `-1s` errors. max_timeout `0` and `-1s` error. drain_bytes `0` passes and `-1` errors. drain_timeout `0` passes and `nope` errors. concurrency `4` passes, while `0`, `-2` and `abc` error. An explicitly empty model errors naming `ENGRAM_DECISIONS_MODEL`. An empty API key passes (keys are never validated, precedent `ENGRAM_OPENAI_CHAT_API_KEY`).
    - RED first: the tests compile against the empty registry rows and a Validate with no decisions block, and fail. Record that in the SUMMARY.
  </behavior>
  <action>
Implements D-01, D-02 and D-03. The error style follows `Validate`'s real plain `ENGRAM_VAR %q: reason` form, per the RESEARCH correction recorded in CONTEXT D-03. It does not use the MCP/Connect argument-rejection envelope.

(1) `internal/config/registry.go`: after the `openai.*` rows, add nine rows with no Legacy and no Flag. Precede them with a comment block in the file's density: brand-new keys, provider-tuning values never typed at a prompt, presence of the provider enables the feature (D-01), only the API key falls back (resolved at the wiring seam, D-03), and the success-path response bound is an internal constant, not a registry row (RESEARCH Pitfall 4). The rows:
- `decisions.provider` / `ENGRAM_DECISIONS_PROVIDER` (no Default)
- `decisions.base_url` / `ENGRAM_DECISIONS_BASE_URL`
- `decisions.api_key` / `ENGRAM_DECISIONS_API_KEY`
- `decisions.model` / `ENGRAM_DECISIONS_MODEL`, Default `typesafe/jev-1.13`
- `decisions.timeout` / `ENGRAM_DECISIONS_TIMEOUT`, Default `10s`
- `decisions.max_timeout` / `ENGRAM_DECISIONS_MAX_TIMEOUT`, Default `10m`
- `decisions.drain_bytes` / `ENGRAM_DECISIONS_DRAIN_BYTES`, Default `262144`
- `decisions.drain_timeout` / `ENGRAM_DECISIONS_DRAIN_TIMEOUT`, Default `2s`
- `decisions.concurrency` / `ENGRAM_DECISIONS_CONCURRENCY`, Default `4`

(2) `internal/config/validate.go`: after the `Summarize.Model`-gated block, add:
- An unconditional provider enum check: a value other than `""` or `jev` gives `fmt.Errorf("ENGRAM_DECISIONS_PROVIDER %q: must be empty (off) or \"jev\"", ...)`.
- `if c.Decisions.Provider == "jev" { ... }` containing:
  - The base URL switch: empty gives `errors.New("ENGRAM_DECISIONS_BASE_URL is empty: required when ENGRAM_DECISIONS_PROVIDER=jev (does not fall back to ENGRAM_OPENAI_BASE_URL)")`. Then the url.Parse error, the scheme-not-http/https and the missing-host cases in the existing wording.
  - An empty model gives an error naming `ENGRAM_DECISIONS_MODEL`.
  - The timeout is a Go duration of at least 0.
  - max_timeout is a positive duration.
  - drain_bytes goes through `ParseNonNegativeIntCap`.
  - drain_timeout is a duration of at least 0.
  - concurrency goes through `ParsePositiveIntCap`.
- A comment above the block: gated like summarize so a deployment that never sets the provider validates byte-identically to before (D-01). The API key is deliberately not validated (it has no verifiable shape, and empty is meaningful: inherit).

Do not add `ENGRAM_DECISIONS_*` to any existing `Config{}` test literal: every decisions check is gated, so a zero-valued `Decisions` is valid.

(3) New `internal/config/decisions_config_test.go` (SPDX): the two tests per `<behavior>`, named with the `TestDecisions` prefix so `go test ./internal/config/ -run TestDecisions` selects exactly them.

Order: tests (RED), then rows and the Validate block (GREEN). Commit as `feat(config): register ENGRAM_DECISIONS_* settings, off by default (DEC-01, D-01..D-03)` with pathspec `internal/config/registry.go internal/config/validate.go internal/config/decisions_config_test.go`.
  </action>
  <verify>
    <automated>out=$(go test ./internal/config/ -run '^TestDecisions' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e '^--- PASS: TestDecisions(RegistryEntries|Validate)' | wc -l)" -eq 2</automated>
    <fails_when>non-zero exit, or not exactly two top-level `--- PASS: TestDecisions…` lines</fails_when>
    <automated>go test ./internal/config/ ./internal/server/ -count=1</automated>
    <fails_when>non-zero exit (an existing config or server test broke, for example a hand-built `Config{}` literal now failing validation)</fails_when>
  </verify>
  <acceptance_criteria>
    - `test "$(rg -o 'Env: "ENGRAM_DECISIONS_[A-Z_]+"' internal/config/registry.go | sort -u | wc -l)" -eq 9`
    - No decisions row carries a Flag or Legacy: `rg 'ENGRAM_DECISIONS_' internal/config/registry.go | rg -o 'Flag:|Legacy:' | wc -l` prints `0`.
    - No response-bytes knob was registered (RESEARCH Pitfall 4): `rg -o -i 'decisions[.]max_response' internal/config | wc -l` prints `0`.
    - Validate does not use the MCP/Connect hint grammar: `rg -o 'hint=' internal/config/validate.go | wc -l` prints `0`.
    - Every decisions check except the enum sits inside the gate: `awk '/c[.]Decisions[.]Provider == "jev"/,/^\t}$/' internal/config/validate.go | rg -o 'ENGRAM_DECISIONS_(BASE_URL|MODEL|TIMEOUT|MAX_TIMEOUT|DRAIN_BYTES|DRAIN_TIMEOUT|CONCURRENCY)' | sort -u | wc -l` prints `7`.
  </acceptance_criteria>
  <done>All nine vars are registered with the stated defaults and load from the environment. Validate enforces the provider enum always and every other decisions rule only when the provider is `jev`, including the no-fallback base URL error. RED then GREEN recorded, and the change is committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engram → Decisions endpoint (OpenRouter or LiteLLM gateway) | outbound HTTPS carrying the API key and caller-supplied state |
| Decisions endpoint → engram | untrusted JSON response and error bodies |
| operator env → config | `ENGRAM_DECISIONS_*` values read at startup |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Information disclosure | API key on the wire and in logs | high | mitigate | The key is set only as the `Authorization` header and never logged. The slog line's key set is fixed and excludes the key (Task 1 acceptance). Plan 02-07's telemetry test asserts it is absent from spans, logs and errors |
| T-02-06 | Spoofing / Tampering | Base URL resolution | high | mitigate | No base-URL fallback (D-03). `Validate` requires an http(s) URL with a host when the provider is `jev`. The endpoint is a fixed suffix on the operator's base, never the chat or embeddings URL |
| T-02-07 | Denial of service | Response read in the tracer path | medium | mitigate | One `context.WithTimeout` budget per call. A non-positive timeout resolves to the 10m ceiling, never unbounded. The success decode is capped with `io.LimitReader(…, 1<<20)` on the hand-write branch. `httpdrain.Drain` runs on every response path. Plan 02-07 adds too-large classification |
| T-02-08 | Information disclosure | Key fallback sends `ENGRAM_OPENAI_API_KEY` to the decisions host | medium | mitigate | A locked user decision (D-03). Plan 02-05 logs which key source is in use (never the key) and plan 02-06 documents the cross-gateway implication, mirroring the chat-lane key paragraph |
| T-02-SC | Tampering | SDK dependency (adopt-and-wrap branch only) | high | mitigate | Added only at the exact version the `blocking-human` checkpoints approved, with `go.sum` hashes. Task 1's third verify asserts that `go.mod` matches the recorded resolution |
</threat_model>

<verification>
- `go test ./internal/server/ -run '^TestDeciderTracerEndToEnd$' -count=1 -v` and `go test ./internal/config/ -run '^TestDecisions' -count=1 -v` pass with their PASS lines present.
- `go build ./...` and `golangci-lint run` on the touched packages are clean. `task license:check` is green.
- `go.mod` matches the D-06 resolution.
</verification>

<success_criteria>
- ROADMAP Phase 2 success criterion 2 is proven for the noul path: a caller sends questions against shared state through one interface and gets typed probabilities back.
- Success criterion 3 is proven for the OpenRouter-shaped base: the Jev backend reaches `{base}/alpha/decisions` with its own base URL, key (with fallback) and model.
- Success criterion 6 holds on the success path: every call emits a `decide` span with latency (duration), question count, input tokens and cost.
- Success criterion 1's config half holds: the provider is off by default, and `ENGRAM_` config turns it on.
</success_criteria>

<output>
Create `.planning/phases/02-decision-interface-jev-backend/02-03-SUMMARY.md` when done. Record which D-06 branch was built and the RED observations.
</output>
