---
phase: 02-decision-interface-jev-backend
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - .planning/phases/02-decision-interface-jev-backend/sdk-eval/go.mod
  - .planning/phases/02-decision-interface-jev-backend/sdk-eval/go.sum
  - .planning/phases/02-decision-interface-jev-backend/sdk-eval/eval_test.go
  - .planning/phases/02-decision-interface-jev-backend/sdk-eval/fixtures_test.go
  - .planning/phases/02-decision-interface-jev-backend/02-SDK-EVALUATION.md
autonomous: false
requirements:
  - DEC-05

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "When legitimacy was approved, a nested Go module under `.planning/phases/02-decision-interface-jev-backend/sdk-eval/` pins the SDK to exactly the `sdk_version` recorded by plan 02-01. It never joins engram's module graph: engram's `go.mod`/`go.sum` are unchanged and `go test ./...` from the repo root never compiles it"
    - "`TestSDKEvaluation` replays the spike's captured OpenRouter (numeric `code`) and LiteLLM (string `code`) error bodies and a success body through `Alpha.Decisions.Create` against an httptest server. It logs exactly one `EVAL-E0n PASS|FAIL <observation>` line for each of E01 to E08, and it fails only on a harness fault, never because the SDK fell short (DEC-05, D-06)"
    - "E04 proves (or disproves) that a caller-supplied `*http.Client` carries every SDK request. E06 proves (or disproves) that an error returned from that client's RoundTripper, or from a response body read, survives the SDK's wrapping and stays `errors.Is`-matchable. These are the two hooks otelhttp, a body-bounding RoundTripper and status classification depend on"
    - "E09 (`TestSDKConcurrency`) drives one SDK client from 4 goroutines under `-race` (DEC-05 concurrency edge). Its exit status is recorded as PASS or FAIL rather than failing the plan"
    - "The doc's `## Verdict` applies the fixed decision table (R1 to R4) to the recorded evidence and writes `verdict: REJECT-HAND-WRITE`, `verdict: ADOPT-AND-WRAP-CANDIDATE` or `verdict: ADOPT-NATIVE`, naming the rule that fired"
    - "A `blocking-human` decision checkpoint resolves D-06, and `resolution: reject-hand-write` or `resolution: adopt-and-wrap` is recorded with the matching recipe section before any file under `internal/decide/` exists (D-06, D-07)"
    - "The doc carries a ready-to-store durable engram decision record (scope, category `decision`, tags, one-line summary, content) for the orchestrator or user to store, because executors are not assumed to hold engram MCP access (D-07)"
  artifacts:
    - path: ".planning/phases/02-decision-interface-jev-backend/sdk-eval/eval_test.go"
      provides: "TestSDKEvaluation (E01-E08) and TestSDKConcurrency (E09) against an httptest Decisions endpoint"
      contains: "func TestSDKEvaluation(t *testing.T)"
    - path: ".planning/phases/02-decision-interface-jev-backend/sdk-eval/fixtures_test.go"
      provides: "Verbatim spike success and error bodies plus labelled synthetic status fixtures"
      contains: "fixtureLiteLLM401"
    - path: ".planning/phases/02-decision-interface-jev-backend/02-SDK-EVALUATION.md"
      provides: "Behavioral results, verdict, D-06 resolution, recipe, durable record text"
      contains: "## Durable decision record"
  key_links:
    - from: ".planning/phases/02-decision-interface-jev-backend/sdk-eval/eval_test.go"
      to: ".planning/phases/02-decision-interface-jev-backend/sdk-eval/fixtures_test.go"
      via: "the LiteLLM string-code dialect is replayed through the SDK (RESEARCH Open Question 1)"
      pattern: "fixtureLiteLLM401"
    - from: ".planning/phases/02-decision-interface-jev-backend/sdk-eval/eval_test.go"
      to: ".planning/phases/02-decision-interface-jev-backend/02-SDK-EVALUATION.md"
      via: "each observation line is copied into the Behavioral evaluation table"
      pattern: "EVAL-E0"
  prohibitions:
    - statement: "MUST NOT commit verbatim spine content: fixtures hold only provider response and error bodies plus synthetic request states, never the spike's request states built from real memory records"
      category: privacy
      status: resolved
      verification: test
      reason: "Task 1 acceptance greps fixtures_test.go for request-state keys. No wired check descriptor by design (spec-less fallback, flagged-unverified)"
---

<objective>
Finish the DEC-05 evaluation with behavioral evidence, apply D-06's rule to it, and have a human resolve the adopt-or-reject fork before any client code exists.

- **D-05 (bar b), behavioral half:** does the SDK decode the typed answers, including a score answer?
- **DEC-03 / DEC-04 / DEC-06 fit (D-06):** does it reach `{base}/alpha/decisions` for both bases? Are both error dialects classifiable? Can a caller-supplied `*http.Client` bound it and trace it? Can its retries be tamed to D-11? Does a context deadline surface as a deadline error?
- **D-06:** a D-05 failure routes to reject and hand-write. A D-05 pass that cannot meet DEC-04/DEC-06 unaided stops at a decision checkpoint. The checkpoint is `blocking-human` in every case, because both outcomes are costly (a new module dependency versus a hand-maintained client for an alpha API).
- **D-07:** the resolution and the durable decision record text land in the phase doc, committed before plan 02-03 writes any client code.

Purpose: plans 02-03 to 02-08 build whichever branch this plan records. Their exported surface is identical either way. Only the jev internals differ.
Output: the `sdk-eval/` harness, a completed `02-SDK-EVALUATION.md` with `verdict:` and `resolution:` lines, a recipe, and the durable record text.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-decision-interface-jev-backend/02-CONTEXT.md
@.planning/phases/02-decision-interface-jev-backend/02-RESEARCH.md
@.planning/phases/02-decision-interface-jev-backend/02-SDK-EVALUATION.md
@.claude/skills/spike-findings-engram/references/decision-transport.md
@.claude/skills/spike-findings-engram/sources/002-jev-gateway-passthrough/README.md
</context>

<interfaces>
Fixture sources (verbatim bodies, extract with `jq`; never retype them):

- `.claude/skills/spike-findings-engram/sources/001-jev-openrouter-transport/results.json`, top-level keys `events` and `latency_ms`. Each event carries `probe`, `status`, `body`. Extract with `jq -r '[.events[] | select(.probe=="<probe>")][0].body' <file>` for the probes `happy` (200: a choice answer `relation` and a noul `same_subject`), `batch50` (200: 50 noul answers), `card256` (400: more than 255 choices), `badkey` (401), `badtype` (400: zod `invalid_union`), `oversize` (400: `max_tokens_exceeded` embedded in `error.message`) and `chatpath` (400).
- `.claude/skills/spike-findings-engram/sources/002-jev-gateway-passthrough/README.md`, "Update — 2026-09-22" section: the LiteLLM 401 body `{"error":{"message":"Authentication Error, No api key passed in.","type":"auth_error","param":"None","code":"401"}}`, where `code` is a string.
- Response bodies carry only answers, usage, the model snapshot, a generation id and the provider. The spike's REQUEST states were built from real spine records and must never be copied. Build every request state here from synthetic text.

SDK entry points (from plan 02-01's inventory; re-confirm against `02-SDK-EVALUATION.md` for the recorded version): `openrouter.New(opts...)`, `openrouter.WithClient(HTTPClient)`, `openrouter.WithSecurity(string)`, `openrouter.WithRetryConfig(retry.Config)`, `(*Decisions).Create(ctx, components.DecisionsRequest, ...operations.Option)` reached as `sdk.Alpha.Decisions.Create`, `operations.WithServerURL(string)`, `operations.WithRetries(retry.Config)`, `components.CreateQuestionsNoul/Choice/Score`, `components.CreateStateMapOfAny`, and the `sdkerrors` types, including `*sdkerrors.APIError` with a `StatusCode` field.
</interfaces>

## Artifacts this phase produces

| Artifact | Kind | Plan |
|----------|------|------|
| `02-SDK-EVALUATION.md`: candidate facts, surface inventory, footprint, legitimacy, behavioral results E01 to E10, verdict, resolution, wrap or hand-write recipe, durable record text | phase doc | 02-01 (created), **02-02** (completed) |
| `COVERAGE.md`: Decisions API coverage matrix | phase doc | planning, 02-01 (reconciled) |
| `sdk-eval/` nested Go module (`eval_test.go`, `fixtures_test.go`, `go.mod`, `go.sum`) | evaluation harness | **02-02** |
| `internal/config`: `DecisionsConfig`, nine `ENGRAM_DECISIONS_*` registry rows, gated `Validate` block | config | 02-03 |
| `internal/decide`: contract types, constructors, `Validate`/`Add`, named errors, `Error`, `Status`, `DecideMany` | contract package | 02-03, 02-04 |
| `internal/decide/jev`: `Client`, `New`, `DefaultModel`, options, `Decide`, `DecideMany`, classifier, wire codec | backend | 02-03, 02-04, 02-07, 02-08 |
| `internal/server/decider.go` and `deps.decider` | wiring | 02-03, 02-05 |
| Helm `memory.decisions`, `engram.containerEnv` rows, `chart:validate` assertions | chart | 02-06 |
| `guides/configure.md`, `guides/deploy.md`, `CLAUDE.md` row, `TestDecisionsVarsDocumented` | docs | 02-06 |
| `task eval:decisions` and `TestJevLive` | verification | 02-08 |
| Durable engram decision record text for DEC-05 (stored by the orchestrator or user) | record text | **02-02** |

## Spec-less fallback ledger (this plan's share)

- **E09** (DEC-05, concurrency): **explicit**. E09 drives one SDK client from 4 goroutines under `-race`, and its outcome is recorded as PASS or FAIL in the evaluation table.
- **Prohibition** (privacy, no verbatim spine content in fixtures): authored above, descriptor-less.

## Executor notes (every task)

- NEVER run any `git stash` subcommand. To inspect a pristine file, use `git show HEAD:<path>` into the scratchpad.
- Commit with an explicit pathspec, using Conventional Commits, and end the message with the session's attribution trailer lines. If the 1Password SSH signer is locked or errors, you MAY retry that ONE commit with `git -c commit.gpgsign=false commit …` (never change persistent git config) and must record it in the SUMMARY.
- The harness lives under `.planning/`, so it needs no SPDX header, and the Go tool's `./...` from the repo root ignores it (dot-prefixed directory). Run it only with `go -C <dir>`.
- Tests assert probabilities only by range or tolerance, never by exact equality: identical Jev requests vary by ±0.03 (spike 001).
- Never write the operator's real gateway hostname into committed files. Use `gateway.example` in synthetic data.

<tasks>

<task type="auto">
  <name>Task 1: Replay both error dialects and a success body through the SDK, record E01–E10, and apply the D-06 decision table</name>
  <precondition>`02-SDK-EVALUATION.md` carries `legitimacy: approved` or `legitimacy: rejected` (recorded by plan 02-01 Task 3)</precondition>
  <files>.planning/phases/02-decision-interface-jev-backend/sdk-eval/go.mod, .planning/phases/02-decision-interface-jev-backend/sdk-eval/go.sum, .planning/phases/02-decision-interface-jev-backend/sdk-eval/eval_test.go, .planning/phases/02-decision-interface-jev-backend/sdk-eval/fixtures_test.go, .planning/phases/02-decision-interface-jev-backend/02-SDK-EVALUATION.md</files>
  <read_first>
    - `.planning/phases/02-decision-interface-jev-backend/02-SDK-EVALUATION.md` (whole file: `sdk_version`, surface inventory, operation mechanics)
    - `.planning/phases/02-decision-interface-jev-backend/02-CONTEXT.md` D-03, D-05, D-06, D-11, D-12, D-13
    - `.planning/phases/02-decision-interface-jev-backend/02-RESEARCH.md` "Common Pitfalls" 1 and 2, "Open Questions" 1, "Alternatives Considered"
    - `.claude/skills/spike-findings-engram/references/decision-transport.md`
    - `internal/embed/embed_test.go` lines 392-427 (the httptest pattern to mirror)
    - The module source files named in the doc's `### Operation mechanics`, under the path `go mod download -json github.com/OpenRouterTeam/go-sdk@<sdk_version>` reports as `Dir`
  </read_first>
  <action>
Implements D-05 (bar b, behavioral half) and the D-06 evidence. Does not touch engram's module.

**If the doc reads `legitimacy: rejected`:** create no harness files. Under `## Behavioral evaluation (plan 02-02)`, write `Not run: the candidate was rejected at the legitimacy gate (plan 02-01).` Leave `verdict: REJECT-HAND-WRITE` in place, write the `## Hand-write recipe` described in (5), and skip to (6). Then commit only the doc.

**Otherwise:**

(1) `sdk-eval/go.mod`: `module engram.invalid/sdkeval` with `go 1.26.7`. Then run `go -C .planning/phases/02-decision-interface-jev-backend/sdk-eval get github.com/OpenRouterTeam/go-sdk@<sdk_version>` (exactly the recorded version) and `go -C <same dir> mod tidy`. Import nothing else outside the standard library.

(2) `sdk-eval/fixtures_test.go`: `package sdkeval`. Add Go raw-string constants, each with a one-line provenance comment:
- Verbatim, extracted with the `jq` form in `<interfaces>`: `fixtureHappy`, `fixtureBatch50`, `fixtureOpenRouter400Choices` (card256), `fixtureOpenRouter401` (badkey), `fixtureOpenRouter400BadType` (badtype), `fixtureOpenRouter400MaxTokens` (oversize) and `fixtureChatPath400` (chatpath).
- Verbatim from the spike 002 README: `fixtureLiteLLM401`.
- Synthetic, each commented `synthetic: shape from spike 001/002 docs, not captured live`:
  - `fixtureScore`: a 200 with one score answer `{"type":"score","score":1,"confidence":0.8,"probabilities":{"0":0.1,"1":0.8,"2":0.1},"legend":{"0":"can wait","1":"this week","2":"blocking"}}` plus usage, model and id.
  - `fixtureLiteLLM403`: string `code` `"403"`, `type` `auth_error`.
  - Numeric-code `{"error":{"message":"...","code":N}}` bodies for 402, 404, 429, 500, 502, 503, 524 and 529.
  - `fixtureHTML502`: a non-JSON `<html>` body.

(3) `sdk-eval/eval_test.go`: `package sdkeval`. Write `func TestSDKEvaluation(t *testing.T)` with one subtest per check. Each subtest logs exactly one line through `t.Logf("EVAL-E0%d %s %s", n, "PASS"|"FAIL", observation)`, and calls `t.Fatal` only on a harness fault (server setup, a fixture that is not JSON where JSON is expected). Build every request state from synthetic text.
- **E01 path fidelity (DEC-03).** Use two httptest bases: B1 = `srv.URL+"/api"` (the OpenRouter shape) and B2 = `srv.URL+"/openrouter"` (the LiteLLM pass-through shape). For each, try only documented options: per-call `operations.WithServerURL(base)`, and `operations.WithServerURL(base with one trailing "/api" removed)`. Record the path the server observed for every attempt. PASS only if one documented option set yields `/api/alpha/decisions` for B1 AND `/openrouter/alpha/decisions` for B2.
- **E02 request fidelity.** Send a request holding one noul, one choice and one score question. Decode the captured body as JSON. PASS if the top-level keys are exactly `model`, `state` and `questions`, noul criteria are an object with `true`/`false`, choice criteria are an object, score criteria are an array, and instructions are strings. Record any extra key the SDK emits by default.
- **E03 typed decode (D-05 bar b).** Decode `fixtureHappy`, `fixtureBatch50` and `fixtureScore`. PASS only if every answer value, the usage (`input_tokens`, `output_tokens`, `cost`), the model snapshot, the id and the provider are reachable through typed fields, with no value reachable only through a `map[string]any` member, a `[]any` member or `UnknownRaw`. Name each field that fails.
- **E04 caller-supplied client.** Use `openrouter.WithClient(&http.Client{Transport: countingRT})`. PASS if `countingRT` saw exactly as many requests as `Create` was called.
- **E05 error dialects (DEC-04, D-12).** For each error fixture, serve it with its status and a JSON Content-Type (`text/html` for `fixtureHTML502`), with SDK retries disabled if E07 finds how. Record for each: `%T` of the error, whether `errors.As` yields `*sdkerrors.APIError` and its `StatusCode`, whether a per-status typed error is returned, and whether the raw body is retrievable. PASS only if EVERY fixture, including `fixtureLiteLLM401` and `fixtureLiteLLM403`, yields an error from which the HTTP status is derivable without engram intercepting the response.
- **E06 errors.Is propagation (DEC-04).** Three probes:
  - (a) A RoundTripper that returns `(nil, errSentinel)`. Is `errors.Is(err, errSentinel)` true?
  - (b) A 200 whose body `Read` returns `errSentinel` after 64 bytes. Is `errors.Is(err, errSentinel)` true?
  - (c) A 200 with a 5 MiB body. How many bytes did the SDK read, and did it read the whole body?
  PASS if (a) and (b) hold. Record (c) either way.
- **E07 retries (D-11).** A server that always returns 503, under a 3 s context deadline. Record the request count with no retry config, with `openrouter.WithRetryConfig(retry.Config{Strategy: "none"})`, and with any per-call option that could express exactly one retry. PASS if retries can be turned off entirely, so engram can own D-11's single retry.
- **E08 deadline.** A server that sleeps 2 s, called with a 200 ms context deadline. PASS if `errors.Is(err, context.DeadlineExceeded)`.

Also write `func TestSDKConcurrency(t *testing.T)` (**E09**): one SDK client, 4 goroutines × 25 calls against `fixtureHappy`, logging `EVAL-E09 PASS` when all 100 succeed.

(4) Run `go -C <dir> test -run '^TestSDKEvaluation$' -count=1 -race -v ./...` and `go -C <dir> test -run '^TestSDKConcurrency$' -count=1 -race -v ./...`. For E09, record PASS on exit 0 and FAIL with the race summary on a non-zero exit. **E10 footprint:** run `go -C <dir> list -m all` and `go list -m all` from the repo root, and record the set difference. Under `## Behavioral evaluation (plan 02-02)`, write a table with the columns `check | question | observed | PASS/FAIL`. Paste each observation line verbatim.

(5) Write `## Verdict (plan 02-02)`. Apply this table in order and name the first rule that fires:
- **R1:** D-05(a) FAIL (from plan 02-01) gives `verdict: REJECT-HAND-WRITE`.
- **R2:** E03 FAIL gives `verdict: REJECT-HAND-WRITE`. This is D-05 bar (b), which is scored on the answer/usage side only. Request-side unions are recorded, not scored, because engram sends only text.
- **R3:** any of E01, E04, E05, E06 (a or b), E07 or E08 FAIL, or E06(c) shows an unbounded read, gives `verdict: ADOPT-AND-WRAP-CANDIDATE`.
- **R4:** otherwise, `verdict: ADOPT-NATIVE`.

Replace the `verdict:` line accordingly. Then write the recipe section for the branch the verdict points at, and for `ADOPT-AND-WRAP-CANDIDATE` write BOTH:
- `## Wrap recipe`: the exact option calls, for the recorded version, that reach `{base}/alpha/decisions` for both base shapes (E01; if no documented option does, state the URL-rewriting RoundTripper that would be required). How retries are turned off (E07). Where status classification must happen (at the RoundTripper, before the SDK decodes, if E05 failed). How the response byte bound is enforced (a body-wrapping RoundTripper, valid only if E06(b) passed). How the timeout budget applies (E08).
- `## Hand-write recipe`: `net/http` plus `encoding/json` wire structs following `internal/embed`/`internal/summarize` (the New+Option shape, `httpdrain.Drain` on every response path, `otelhttp` via `WithHTTPTransport`, an endpoint of `strings.TrimRight(base, "/") + "/alpha/decisions"`, and classification by HTTP status).

(6) Write `## Durable decision record` as a fenced block holding the fields `scope` (the repo spine scope this repo's curating-memory skill uses), `category: decision`, `tags` (`dec-05`, `openrouter-go-sdk`, `decisions-api`, `jev`), a one-line `summary`, and a `content` paragraph. The paragraph gives the evaluated version, the date, the verdict and its rule, the decisive evidence lines, and the path of this doc. Leave the final outcome sentence as `Outcome: pending the D-06 checkpoint.` Task 3 replaces it.

Commit as `docs(02): evaluate OpenRouter Go SDK behavior against DEC-03/04/06 (DEC-05)` with pathspec `.planning/phases/02-decision-interface-jev-backend/sdk-eval .planning/phases/02-decision-interface-jev-backend/02-SDK-EVALUATION.md`.
  </action>
  <verify>
    <automated>f=.planning/phases/02-decision-interface-jev-backend/02-SDK-EVALUATION.md; d=.planning/phases/02-decision-interface-jev-backend/sdk-eval; if rg -q '^legitimacy: rejected$' "$f"; then rg -q 'Not run: the candidate was rejected' "$f" &amp;&amp; rg -q '^verdict: REJECT-HAND-WRITE$' "$f"; else out=$(go -C "$d" test -run '^TestSDKEvaluation$' -count=1 -race -v ./... 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e 'EVAL-E0|^--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o 'EVAL-E0[1-8] (PASS|FAIL)' | cut -d' ' -f1 | sort -u | wc -l)" -eq 8; fi</automated>
    <fails_when>non-zero exit, or fewer than 8 distinct `EVAL-E01`..`EVAL-E08` PASS/FAIL lines (a subtest skipped or never logged its observation)</fails_when>
    <automated>f=.planning/phases/02-decision-interface-jev-backend/02-SDK-EVALUATION.md; rg -q '^verdict: (REJECT-HAND-WRITE|ADOPT-AND-WRAP-CANDIDATE|ADOPT-NATIVE)$' "$f" &amp;&amp; rg -q '^## Durable decision record' "$f" &amp;&amp; { rg -q '^## Hand-write recipe' "$f" || rg -q '^## Wrap recipe' "$f"; }</automated>
    <fails_when>non-zero exit (no resolved verdict line, no durable record section, or no recipe section)</fails_when>
    <automated>st=$(git status --porcelain -- go.mod go.sum) || exit 1; test -z "$st" &amp;&amp; test -z "$(git ls-files internal/decide)" &amp;&amp; test -z "$(gofmt -l .planning/phases/02-decision-interface-jev-backend/sdk-eval 2>&amp;1)"</automated>
    <fails_when>non-zero exit (engram's go.mod/go.sum were touched, client code exists before the decision, or the harness is not gofmt-clean)</fails_when>
  </verify>
  <acceptance_criteria>
    - No request-state text from the spike was copied: `rg -o -F -e 'record_a' -e 'record_b' .planning/phases/02-decision-interface-jev-backend/sdk-eval/fixtures_test.go | wc -l` prints `0` (the fixtures file holds response and error bodies only).
    - When the harness ran, the pinned version equals the recorded one: `rg -o 'github.com/OpenRouterTeam/go-sdk v[0-9.]+' .planning/phases/02-decision-interface-jev-backend/sdk-eval/go.mod` prints the doc's `sdk_version` value.
    - The Behavioral evaluation table has a row for each of E01 to E10: `rg -o '^[|] E(0[1-9]|10) ' .planning/phases/02-decision-interface-jev-backend/02-SDK-EVALUATION.md | wc -l` prints `10` (skip this criterion only on the legitimacy-rejected path).
    - `## Verdict (plan 02-02)` names the rule (R1, R2, R3 or R4) that produced the verdict.
  </acceptance_criteria>
  <done>The harness ran against the recorded SDK version (or was skipped on a legitimacy rejection). Every check's observation is in the doc. The verdict is computed by the fixed rule table, and the recipe and durable record text are written. engram's module and `internal/decide/` are untouched. The change is committed.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Resolve D-06 — adopt the OpenRouter Go SDK behind engram-owned wrapping, or reject it and hand-write the Jev client</name>
  <decision>Which client implementation `internal/decide/jev` uses. This is the D-06 fork. Plans 02-03 to 02-08 build the chosen branch. Their exported surface is identical either way, and only the jev internals differ.</decision>
  <context>
    Show the user the `verdict:` line and its rule, the `## Behavioral evaluation (plan 02-02)` table, `### Escape hatches`, `## Dependency footprint`, and whichever recipe sections exist.

    D-06 as locked. If the candidate fails D-05 (verdict `REJECT-HAND-WRITE` via R1 or R2), D-06 already dictates reject-and-hand-write, and this checkpoint only confirms that or records a deliberate override of D-06. If the verdict is `ADOPT-AND-WRAP-CANDIDATE` (R3), this is the live adopt-and-wrap versus reject choice D-06 reserved for the user. If the verdict is `ADOPT-NATIVE` (R4), adopting still records a deliberate exception to prior milestones' zero-new-dependency posture.

    Reversibility: `costly` either way (D-06). Adopting adds a direct module dependency that releases several times a day, plus its transitive modules. Rejecting commits engram to hand-maintaining wire structs for an alpha API. Both sit behind the stable `decide.Decider` interface, so a later swap touches `internal/decide/jev` only.

    Planning-time static read of v0.8.19, to be confirmed by the recorded evidence: the SDK joins `/api/alpha/decisions` onto its server URL, so a LiteLLM base of `https://<gateway>/openrouter` would need a URL-rewriting RoundTripper. The score answer's `Legend` union has `map[string]any` and `[]any` members. Bodies are read with an unbounded `io.ReadAll`. The default retry backs off for up to an hour on 5xx.
  </context>
  <options>
    <option id="reject-hand-write">
      <name>Reject the SDK and hand-write the Jev client on net/http (the embed/summarize pattern)</name>
      <pros>Zero new module dependencies. Bounded reads, `httpdrain`, status classification and both base-URL shapes follow the pattern two provider clients already use. Both error dialects are classified from the HTTP status with no decode dependency.</pros>
      <cons>engram hand-maintains the request/answer wire structs for an alpha API and must track its changes itself.</cons>
    </option>
    <option id="adopt-and-wrap">
      <name>Adopt the SDK at the recorded version, behind the wrap recipe in the evaluation doc</name>
      <pros>Generated request/answer types follow the API the same day it changes.</pros>
      <cons>A new direct dependency plus its transitive modules. engram still owns the URL rewriting, transport-level classification, response byte bound and retry override the recipe lists. If R1 or R2 fired, this overrides D-06 as written.</cons>
    </option>
  </options>
  <resume-signal>Reply `reject-hand-write` or `adopt-and-wrap` (plus a reason if it overrides the recorded verdict)</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the D-06 resolution and finalize the durable decision record text</name>
  <files>.planning/phases/02-decision-interface-jev-backend/02-SDK-EVALUATION.md</files>
  <read_first>
    - `.planning/phases/02-decision-interface-jev-backend/02-SDK-EVALUATION.md` (post-Task-1)
    - The user's Task 2 reply
  </read_first>
  <action>
Implements D-06's resolution and D-07's record.

(1) Replace `resolution: PENDING` with `resolution: reject-hand-write` or `resolution: adopt-and-wrap`, matching the user's reply exactly. Under `## Verdict (plan 02-02)`, add a `### Resolution` subsection with today's date (`date +%Y-%m-%d`), the option chosen, and whether it overrides the recorded verdict, with the user's reason when it does.

(2) Make the recipe for the chosen branch authoritative. Keep only the recipe section matching the resolution (`## Hand-write recipe` for reject, `## Wrap recipe` for adopt), and delete the other one if present. For `adopt-and-wrap`, the recipe must also name the exact `go get github.com/OpenRouterTeam/go-sdk@<sdk_version>` pin that plan 02-03 runs.

(3) In `## Durable decision record`, replace `Outcome: pending the D-06 checkpoint.` with the resolution and its one-line rationale. Add the sentence `Store with store_memory (or supersede_memory if an earlier DEC-05 record exists); an executor without engram MCP access leaves this to the orchestrator.`

Commit as `docs(02): record DEC-05 SDK decision (<resolution>)` with pathspec `.planning/phases/02-decision-interface-jev-backend/02-SDK-EVALUATION.md`.
  </action>
  <verify>
    <automated>f=.planning/phases/02-decision-interface-jev-backend/02-SDK-EVALUATION.md; rg -q '^resolution: (reject-hand-write|adopt-and-wrap)$' "$f" &amp;&amp; { { rg -q '^resolution: reject-hand-write$' "$f" &amp;&amp; rg -q '^## Hand-write recipe' "$f" &amp;&amp; ! rg -q '^## Wrap recipe' "$f"; } || { rg -q '^resolution: adopt-and-wrap$' "$f" &amp;&amp; rg -q '^## Wrap recipe' "$f" &amp;&amp; ! rg -q '^## Hand-write recipe' "$f"; }; } &amp;&amp; ! rg -q 'Outcome: pending the D-06 checkpoint' "$f"</automated>
    <fails_when>non-zero exit (no resolution, a recipe that does not match the resolution, both recipes left in place, or the durable record still pending)</fails_when>
  </verify>
  <acceptance_criteria>
    - Exactly one `resolution:` line: `rg -o '^resolution: ' .planning/phases/02-decision-interface-jev-backend/02-SDK-EVALUATION.md | wc -l` prints `1`.
    - `test -z "$(git ls-files internal/decide)"` still holds when this task commits (D-07: the decision precedes any client code).
  </acceptance_criteria>
  <done>The resolution, the single authoritative recipe and the finished durable record text are committed. Plan 02-03 can now read which branch to build.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Go module proxy → nested harness module | third-party SDK code compiled and run on the developer machine, after legitimacy approval |
| Spike artifacts → committed fixtures | captured provider bodies copied into a public repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-SC | Tampering | SDK and transitive modules in `sdk-eval/` | high | mitigate | Runs only after plan 02-01's `blocking-human` legitimacy approval (precondition). Pinned to the exact recorded version with `go.sum` hashes. Isolated in a nested module that engram's build never imports. Any adoption passes a second `blocking-human` checkpoint (Task 2) |
| T-02-03 | Information disclosure | `fixtures_test.go` | medium | mitigate | Only response and error bodies are copied (they carry no record content). Request states are synthetic. Task 1 acceptance greps for the spike's request-state keys. Synthetic data uses `gateway.example`, never the operator's hostname |
| T-02-04 | Repudiation (decision without evidence) | `02-SDK-EVALUATION.md` verdict | medium | mitigate | The verdict comes from a fixed, ordered rule table over recorded observations. The resolution records any override of D-06 with the user's reason |
</threat_model>

<verification>
- The `TestSDKEvaluation` run logs 8 distinct E01–E08 observations (on the approved path), and E09/E10 are recorded.
- `02-SDK-EVALUATION.md` carries resolved `verdict:` and `resolution:` lines, one matching recipe, and the durable record text.
- engram's `go.mod`/`go.sum` are unchanged, and `git ls-files internal/decide` is empty.
</verification>

<success_criteria>
- ROADMAP Phase 2 success criterion 5 holds: the OpenRouter Go SDK evaluation and its adopt/reject decision are recorded before any client lands (DEC-05, D-07).
- The D-06 fork is resolved by a human on recorded evidence, and the chosen branch's recipe is written down for plans 02-03 to 02-08.
</success_criteria>

<output>
Create `.planning/phases/02-decision-interface-jev-backend/02-02-SUMMARY.md` when done. Include the verdict, the rule that fired, the resolution, and the durable record text, so the orchestrator can store it.
</output>
