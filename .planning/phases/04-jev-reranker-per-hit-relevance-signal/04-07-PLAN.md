---
phase: 04-jev-reranker-per-hit-relevance-signal
plan: 07
type: execute
wave: 4
depends_on:
  - 04-02
  - 04-04
  - 04-05
  - 04-06
files_modified:
  - .planning/phases/04-jev-reranker-per-hit-relevance-signal/04-EVAL-JEV.log
  - .planning/phases/04-jev-reranker-per-hit-relevance-signal/04-EVAL-JEV-LONG-TIMEOUT.log
  - .planning/phases/04-jev-reranker-per-hit-relevance-signal/04-EVAL-JEV.md
  - CLAUDE.md
autonomous: true
requirements:
  - RANK-03
  - RANK-04
  - RANK-05

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Tracer: `task eval:retrieval` runs against live Qdrant (testcontainers), the production embedder and the live Decisions provider with `ENGRAM_DECISIONS_PROVIDER=jev`, exits 0, and its output is recorded verbatim in `04-EVAL-JEV.log` (D-02)"
    - "The recorded run shows `JEV-EVAL | enabled=true`, a `jev` row with numbers (never `Jev: disabled`) and `opt-in` in its D-05 cell, both D-10 gate PASS lines, a `D-05 decision:` line whose winner is not `jev`, a `JEV-EVAL | fallbacks=N/M` line, and one `jev relevance=` line per no-answer query"
    - "When the default-timeout run reports any fallback, one additional run with `ENGRAM_SEARCH_RERANK_TIMEOUT=10s` is recorded verbatim in `04-EVAL-JEV-LONG-TIMEOUT.log` so Jev's ranking quality is measured unconfounded by the 2s production budget; both runs are reported, neither is discarded"
    - "`04-EVAL-JEV.md` records provenance (UTC date, git SHA, endpoint host only, configured decisions model, rerank timeout per run, embed model, the exact command), the verbatim variant table, D-05 and D-10 lines, the JEV-EVAL lines, every no-answer Jev relevance line, and a short reading comparing Jev with lexical and vector-only on paraphrase recall@8, MRR and #261 ranks"
    - "Per D-02 the numbers are recorded, never gated or tuned: no constant, question wording, corpus entry or timeout default is changed after seeing them, and the default ranker stays lexical whatever Jev scores; the results file states that ROADMAP success criterion 1's 'shipped only once the eval numbers justify it' is superseded by locked decision D-02"
    - "Neither log nor the results file contains an API key, bearer token, URL userinfo, or any spine record content (the eval corpus is the committed synthetic set)"
    - "CLAUDE.md's Layout table names `internal/relevance/`, and its Memory contract says that with `ENGRAM_SEARCH_RANKER=jev` search_memory and search_discovery are reordered by the provider's per-hit probability and carry `relevance`, and that a decision failure never fails the search"
    - "The phase gate is green on the final tree: `task` (lint plus the full test suite), `task license:check`, `task proto:lint`, `buf breaking` against main, `task surfaces:gen` and `task ui:build` as no-ops, the key-links gates, a fail-closed Qdrant run of the touched packages, and a clean `git status`"
  artifacts:
    - path: ".planning/phases/04-jev-reranker-per-hit-relevance-signal/04-EVAL-JEV.md"
      provides: "the D-02 live Jev measurement with provenance"
      contains: "JEV-EVAL | enabled=true"
    - path: ".planning/phases/04-jev-reranker-per-hit-relevance-signal/04-EVAL-JEV.log"
      provides: "verbatim live eval output"
      contains: "D-10 gate PASS"
  key_links:
    - from: ".planning/phases/04-jev-reranker-per-hit-relevance-signal/04-EVAL-JEV.md"
      to: "Taskfile.yaml"
      via: "the recorded run came from the registered eval target"
      pattern: "task eval:retrieval"
    - from: "CLAUDE.md"
      to: "internal/relevance/relevance.go"
      via: "the layout row names the new package"
      pattern: "internal/relevance/"
  prohibitions:
    - statement: "MUST NOT tune the relevance question wording, the budget constants, the timeout default, the corpus or the D-05 rule after seeing live Jev numbers, and MUST NOT change the default ranker because of them"
      category: integrity-of-evidence
      status: resolved
      verification: judgment
      reason: "Task 1 records the runs verbatim; the files holding those inputs are outside this plan's files_modified. No wired check descriptor by design (spec-less fallback, flagged-unverified)"
---

<objective>
Record the live Jev numbers the CONTEXT asks for (D-02) and close the phase.

- **Tracer (Task 1):** live `task eval:retrieval` with the Jev slot enabled, recorded verbatim with provenance; a second, longer-timeout run only if the production-budget run fell back.
- **Task 2:** CLAUDE.md rows and the full phase gate on the final tree.

Decisions implemented: D-02 (numbers recorded next to lexical/vector-only, no ship bar), and the phase's specifics (no-answer relevance values recorded).
Output: `04-EVAL-JEV.log`, optionally `04-EVAL-JEV-LONG-TIMEOUT.log`, `04-EVAL-JEV.md`, CLAUDE.md edits, a green gate.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-jev-reranker-per-hit-relevance-signal/04-CONTEXT.md
@.planning/phases/04-jev-reranker-per-hit-relevance-signal/04-VALIDATION.md
@.planning/phases/04-jev-reranker-per-hit-relevance-signal/04-06-SUMMARY.md
@.planning/phases/03-curation-verdicts/03-EVAL-RESULTS.md
@CLAUDE.md
</context>

<interfaces>
- `task eval:retrieval` runs `ENGRAM_RETRIEVAL_EVAL=1 go test ./internal/retrievaleval/ -run TestRetrievalEval -v` (Taskfile.yaml line ~81). It needs Docker (Qdrant testcontainer), the embedder env Phase 1 used for `01-EVAL-SHIPPED.log`, and, for the Jev slot, `ENGRAM_DECISIONS_PROVIDER=jev`, `ENGRAM_DECISIONS_BASE_URL` (for example `https://openrouter.ai/api`) and `ENGRAM_DECISIONS_API_KEY` or the inherited `ENGRAM_OPENAI_API_KEY`.
- From plan 04-06: log lines `JEV-EVAL | enabled=<bool> model=<m> endpoint_host=<h> rerank_timeout=<d>`, `JEV-EVAL | fallbacks=<N>/<M>`, `no-answer <case>/<query>: jev relevance=[...]` or `relevance=none`; the variant table starts `| variant |`; `D-05 decision: winner=...`; `D-10 gate PASS: ...`; `shipped (SearchReranked) matches: ...`.
- Precedents: `.planning/phases/01-eval-foundation-lexical-reranker-fix/01-EVAL-SHIPPED.log` (verbatim run log) and `.planning/phases/03-curation-verdicts/03-EVAL-RESULTS.md` (provenance + verbatim lines + reading).
- CLAUDE.md `## Layout` table (the `internal/decide/` and `internal/verdict/` rows) and `## Memory contract (stable)` (the `score` sentence, line ~104).
</interfaces>

## Artifacts this phase produces (plan 04-07's share)

| Kind | Artifact | Task |
|------|----------|------|
| Evidence | `04-EVAL-JEV.log` (and `04-EVAL-JEV-LONG-TIMEOUT.log` only when the first run fell back) | 1 |
| Doc | `04-EVAL-JEV.md` — provenance, verbatim lines, reading | 1 |
| Doc | CLAUDE.md `internal/relevance/` row and Memory contract sentence | 2 |

## Flagged assumptions (spec-less probe)

- The orchestrator's environment has Docker, the embedder and Decisions credentials (planning notes). If any is missing the task halts at its precondition rather than recording a partial run.

## Executor notes (every task)

- Execution is sequential on the main tree. NEVER run any git stash subcommand. Commit each task with an explicit pathspec, Conventional Commits, and end the message with the session's attribution trailer lines.
- `.planning/**` files never get an SPDX header.
- Never print or commit a credential value; export them in the shell, never inline them in a recorded command line.

<tasks>

<task type="tracer">
  <name>Task 1: Live retrieval eval with the Jev slot enabled, recorded verbatim with provenance</name>
  <precondition>`docker info` succeeds; the embedder variables used for Phase 1's live run are exported; `ENGRAM_DECISIONS_BASE_URL` and a decisions key (`ENGRAM_DECISIONS_API_KEY` or `ENGRAM_OPENAI_API_KEY`) are exported. Halt and report which is missing otherwise.</precondition>
  <files>.planning/phases/04-jev-reranker-per-hit-relevance-signal/04-EVAL-JEV.log, .planning/phases/04-jev-reranker-per-hit-relevance-signal/04-EVAL-JEV-LONG-TIMEOUT.log, .planning/phases/04-jev-reranker-per-hit-relevance-signal/04-EVAL-JEV.md</files>
  <read_first>
    - `.planning/phases/03-curation-verdicts/03-EVAL-RESULTS.md` (results-file shape)
    - `.planning/phases/01-eval-foundation-lexical-reranker-fix/01-EVAL-SHIPPED.log` lines 1-80 (log shape, no-answer lines)
    - `internal/retrievaleval/retrieval_eval_test.go` (post-04-06: the JEV-EVAL lines)
  </read_first>
  <action>
Implements D-02. Run `ENGRAM_DECISIONS_PROVIDER=jev task eval:retrieval` once with the default rerank timeout, capturing combined stdout and stderr verbatim into `04-EVAL-JEV.log` (strip nothing except ANSI colour codes, if any). The run must exit 0; a non-zero exit (a D-10 gate or harness failure) is recorded unaltered and reported as a blocker in the SUMMARY — never re-run until it passes, never edit code to make it pass. If its `JEV-EVAL | fallbacks=` line shows N greater than 0, run once more with `ENGRAM_SEARCH_RERANK_TIMEOUT=10s` into `04-EVAL-JEV-LONG-TIMEOUT.log`; otherwise do not create that file.

Write `04-EVAL-JEV.md` in `03-EVAL-RESULTS.md`'s shape: `## Provenance` (UTC date from `date -u`, `git rev-parse HEAD`, endpoint host and configured model from the JEV-EVAL line, rerank timeout per run, embed model from the environment, the exact command without secrets); `## Variant table (verbatim)` per run in a fenced block with the file:line prefix stripped, followed by the D-05 decision, both D-10 lines, the shipped-matches line and both JEV-EVAL lines; `## No-answer relevance (verbatim)` with every `jev relevance=` line; `## Reading`: Jev's paraphrase recall@8 and MRR and #261 ranks beside lexical's and vector-only's, whether the no-answer values sit near the spike's 0.02, the fallback count and what it means for the 2s default, and two fixed statements — the Jev ranker ships as an opt-in regardless of these numbers and lexical stays the default (D-02), and ROADMAP Phase 4 success criterion 1's "shipped only once the eval numbers justify it" is superseded by locked decision D-02 (the roadmap text itself is reconciled by the orchestrator through the roadmap edit verb, not in this file).

Commit as `docs(04): record the live Jev retrieval eval (D-02)` with pathspec `.planning/phases/04-jev-reranker-per-hit-relevance-signal/04-EVAL-JEV.log .planning/phases/04-jev-reranker-per-hit-relevance-signal/04-EVAL-JEV.md` plus the long-timeout log when it exists.
  </action>
  <verify>
    <automated>d=.planning/phases/04-jev-reranker-per-hit-relevance-signal; rg -q -F 'JEV-EVAL | enabled=true' $d/04-EVAL-JEV.log &amp;&amp; test "$(rg -o -F 'D-10 gate PASS' $d/04-EVAL-JEV.log | wc -l)" -eq 2 &amp;&amp; rg -q -e 'JEV-EVAL [|] fallbacks=[0-9]+/[0-9]+' $d/04-EVAL-JEV.log &amp;&amp; test "$(rg -o -F 'jev relevance=' $d/04-EVAL-JEV.log | wc -l)" -ge 1 &amp;&amp; ! rg -q -e 'D-05 decision: winner=jev' $d/04-EVAL-JEV.log &amp;&amp; ! rg -q -F 'Jev: disabled' $d/04-EVAL-JEV.log &amp;&amp; rg -q -e '^ok[[:space:]]+github.com/seanb4t/engram/internal/retrievaleval' $d/04-EVAL-JEV.log</automated>
    <fails_when>the Jev slot was not enabled, fewer than two D-10 PASS lines, the fallback or no-answer relevance lines missing, jev selected as the D-05 winner, a disabled jev row, or no package `ok` line (the run failed or was cut short)</fails_when>
    <automated>d=.planning/phases/04-jev-reranker-per-hit-relevance-signal; test "$(rg -o -i -e 'sk-or-v1-[A-Za-z0-9]+|authorization: *bearer|api_key=[^ ]+|https?://[^/ ]*:[^/ ]*@' $d/04-EVAL-JEV.log $d/04-EVAL-JEV.md | wc -l)" -eq 0 &amp;&amp; rg -q -F 'D-02' $d/04-EVAL-JEV.md &amp;&amp; rg -q -F 'task eval:retrieval' $d/04-EVAL-JEV.md</automated>
    <fails_when>a credential-shaped string in either artifact, or the results file missing the D-02 statement or the command</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -o -e '^## (Provenance|Variant table [(]verbatim[)]|No-answer relevance [(]verbatim[)]|Reading)$' .planning/phases/04-jev-reranker-per-hit-relevance-signal/04-EVAL-JEV.md | wc -l` prints `4`.
    - `git status --porcelain -- internal/ cmd/ proto/ gen/` prints nothing (no code changed by this task).
  </acceptance_criteria>
  <done>The live Jev numbers, their provenance, fallbacks and no-answer relevance values are recorded verbatim; nothing was tuned; committed.</done>
</task>

<task type="auto">
  <name>Task 2: CLAUDE.md rows and the full phase gate</name>
  <files>CLAUDE.md</files>
  <read_first>
    - `CLAUDE.md` lines 1-40 (Layout) and 95-125 (Memory contract search paragraph)
  </read_first>
  <action>
Adds the durable project context for the new package and contract. In CLAUDE.md's `## Layout` table add, after the `internal/verdict/` row: `internal/relevance/` — the search-path relevance question set shared by the server's rank hook and the retrieval eval (one noul question per candidate, budgeted `verdict.State` per candidate, response mapping), built over `internal/decide`; the store composes it through `store.RankHook`. In `## Memory contract (stable)`, directly after the sentence about the always-on `score`, add one or two sentences: with `ENGRAM_SEARCH_RANKER=jev` (opt-in; requires `ENGRAM_DECISIONS_PROVIDER`), `search_memory` and `search_discovery` results are reordered by the decision provider's per-hit probability that the record answers the query and carry `relevance` (0 to 1, omitted when reranking is off or fell back); a decision error or timeout never fails the search. Keep every other line intact.

Then run the phase gate on the final tree and fix only what this phase introduced (record any pre-existing failure separately, never fix it here): `task`; `task license:check`; `task proto:lint`; `go tool buf breaking --against '.git#branch=main'`; `task surfaces:gen` then `git diff --exit-code -- proto/ docs-site/ skill/ gen/ ui/src/lib/gen/ cmd/engram/testdata/`; `task ui:build` then `git diff --exit-code -- internal/webauth/static`; `go test ./internal/keylinks/ -count=1`; `ENGRAM_REQUIRE_QDRANT=1 go test ./internal/relevance/ ./internal/store/ ./internal/server/ ./internal/retrievaleval/ ./cmd/engram/ -count=1`; `git status --porcelain` empty after the commit.

Commit as `docs: record internal/relevance and the opt-in relevance signal in CLAUDE.md` with pathspec `CLAUDE.md`.
  </action>
  <verify>
    <automated>task &amp;&amp; task license:check &amp;&amp; task proto:lint &amp;&amp; go tool buf breaking --against '.git#branch=main' &amp;&amp; go test ./internal/keylinks/ -count=1</automated>
    <fails_when>non-zero exit from any gate command</fails_when>
    <automated>ENGRAM_REQUIRE_QDRANT=1 go test ./internal/relevance/ ./internal/store/ ./internal/server/ ./internal/retrievaleval/ ./cmd/engram/ -count=1</automated>
    <fails_when>non-zero exit (including a Qdrant-backed test that would otherwise have skipped)</fails_when>
    <automated>d1=$(git diff HEAD -- proto/ docs-site/ skill/ gen/ ui/src/lib/gen/ cmd/engram/testdata/ internal/webauth/static) &amp;&amp; task surfaces:gen &amp;&amp; task ui:build &amp;&amp; d2=$(git diff HEAD -- proto/ docs-site/ skill/ gen/ ui/src/lib/gen/ cmd/engram/testdata/ internal/webauth/static) &amp;&amp; test "$d1" = "$d2" &amp;&amp; test -z "$(git status --porcelain --untracked-files=all -- internal/webauth/static)"</automated>
    <fails_when>regenerating the anchored surfaces or rebuilding the vendored console changed any tracked file (a stale generated artifact), or either generator exited non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -o -e '^[|] .internal/relevance/. [|]' CLAUDE.md | wc -l` prints `1` (the Layout row).
    - `rg -o -F 'ENGRAM_SEARCH_RANKER=jev' CLAUDE.md | wc -l` prints at least `1`.
    - After the commit, `test -z "$(git status --porcelain)"` exits 0 (nothing left uncommitted).
  </acceptance_criteria>
  <done>CLAUDE.md describes the new package and the opt-in relevance contract; every phase gate is green on a clean tree; committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell → committed evidence | credentials live in the environment during the live run; the log is committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-14 | Information disclosure | `04-EVAL-JEV.log` / `.md` | high | mitigate | credentials exported, never inlined; the harness logs host and model only; Task 1's second verify scans both files for key, bearer, api_key and URL-userinfo shapes before commit |
| T-04-11 | Repudiation / integrity of evidence | recorded numbers | medium | mitigate | verbatim capture, one run per configuration, a non-zero exit recorded as a blocker, and no inputs in files_modified (prohibition above) |
</threat_model>

<verification>
- The live evidence files exist with every required line; the full phase gate is green on a clean tree.
</verification>

<success_criteria>
- D-02: live Jev recall@k/MRR, #261 rank and no-answer relevance values are recorded next to lexical and vector-only.
- RANK-03/04/05 are shipped and gated green end to end.
</success_criteria>

<output>
Create `.planning/phases/04-jev-reranker-per-hit-relevance-signal/04-07-SUMMARY.md` when done. Record the run count (one or two), the fallback counts, whether the long-timeout log exists, the ROADMAP SC1 reconciliation request, and the Helm follow-up from plan 04-03.
</output>
