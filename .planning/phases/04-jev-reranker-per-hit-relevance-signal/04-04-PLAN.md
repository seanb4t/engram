---
phase: 04-jev-reranker-per-hit-relevance-signal
plan: 04
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - internal/server/summary.go
  - internal/server/summary_test.go
  - internal/server/connectapi_test.go
  - cmd/engram/client_common.go
  - cmd/engram/client_search_test.go
  - docs-site/src/content/docs/guides/cli.md
autonomous: true
requirements:
  - RANK-04
  - RANK-03

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "MCP `search_memory` compact results (`full=false`, the default) carry `relevance` on each hit exactly when that hit's store.Memory carries it, with the value unchanged; the key is absent otherwise; a genuine 0 still serializes as a present relevance key with value 0, never omitted (RANK-04, D-05)"
    - "MCP `search_memory` full results (`full=true`, store.Memory verbatim) carry the same `relevance` values; `list_memory` results never carry `relevance`"
    - "`engram search` text output adds a `RELEVANCE` column between SCORE and SUMMARY only when at least one returned memory carries relevance, printing each value to four decimals; with no relevance anywhere (the lexical default) the header and every row are byte-identical to today; `engram list` output never changes"
    - "`engram search --output json` carries `relevance` on each memory exactly when the server set it, with the value unchanged, and omits the key otherwise"
    - "For the same request with a rank hook set, MCP `search_memory` and Connect `SearchMemories` return the same ids in the same order and the same relevance per id, both scope-confined and with `cross_spine`; with a failing hook both return the identical lexical order and no relevance; the hook never surfaces another owner's private record on either lane (RANK-03, RANK-04, D-07)"
    - "No response-level relevance summary exists: the MCP result object's keys and the Connect SearchMemoriesResponse fields are unchanged with the hook set — only per-hit values are added (D-06)"
    - "FLAGGED ASSUMPTION (CLI column formatting is Claude's discretion): the column header is RELEVANCE, four decimals like SCORE, and a memory without relevance inside a relevance-bearing response prints `-` (unreachable under plan 04-01's all-or-nothing rule, kept defensive)"
  artifacts:
    - path: "internal/server/summary.go"
      provides: "recallView.Relevance and its toRecallView population"
      contains: 'json:"relevance,omitempty"'
    - path: "cmd/engram/client_common.go"
      provides: "data-derived RELEVANCE column in renderMemoryTable"
      contains: "RELEVANCE"
    - path: "internal/server/connectapi_test.go"
      provides: "jev-hook parity subtests in TestRerankParityMCPAndConnect"
      contains: "rankHook"
  key_links:
    - from: "internal/server/summary.go"
      to: "internal/store/store.go"
      via: "the compact allow-list copies the transient relevance pointer"
      pattern: "Relevance:[ ]+m[.]Relevance"
    - from: "cmd/engram/client_common.go"
      to: "gen/go/engram/v1/engram.pb.go"
      via: "the CLI reads the additive proto field's presence, not its zero value"
      pattern: "[.]Relevance != nil"
  prohibitions:
    - statement: "MUST NOT act on relevance beyond reordering and reporting it — no hit is filtered, dropped or truncated by a relevance threshold, and no response-level 'nothing relevant' flag is added; the caller decides from the per-hit values"
      category: safety
      status: resolved
      verification: test
      reason: "Parity subtests assert the returned id set equals the no-hook id set (same membership, k-truncation only) and the response keys/fields are unchanged; D-06 locks per-hit only. No wired check descriptor by design (spec-less fallback, flagged-unverified)"
---

<objective>
Carry the per-hit relevance signal onto the two remaining surfaces and prove the lanes agree.

- **Task 1 (TDD):** MCP compact `recallView` gains `relevance` (full view already carries it through store.Memory).
- **Task 2 (TDD):** `engram search` shows a data-derived RELEVANCE column and its JSON lane carries the proto field; the CLI guide documents both.
- **Task 3:** `TestRerankParityMCPAndConnect` gains jev-hook subtests — order, relevance, fallback, cross_spine and no cross-owner leak.

Decisions implemented: D-05 (MCP JSON field and CLI column/field; Connect landed in plan 04-01), D-06 (per-hit only, no response-level flag), D-07 (both SearchReranked lanes, with and without cross_spine).
Output: two shaper changes, CLI docs, tests.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-jev-reranker-per-hit-relevance-signal/04-CONTEXT.md
@.planning/phases/04-jev-reranker-per-hit-relevance-signal/04-01-SUMMARY.md
@internal/server/summary.go
@cmd/engram/client_common.go
</context>

<interfaces>
- `internal/server/summary.go`: `recallView` (hand-written allow-list; its doc comment says a new field must be added to the struct AND populated in `toRecallView`), `toRecallView(m, maxChars)`, `shapeRecall(ms, full, maxChars)` (full returns store.Memory verbatim). `internal/server/summary_test.go`: `TestShapeRecallFullVsSummary`, `TestToRecallViewSurfacesUsageSignals` (JSON-marshal-and-inspect precedent).
- From plan 04-01: `store.Memory.Relevance *float64 json:"relevance,omitempty"`; `deps.rankHook store.RankHook` threaded into `searchMemory`; Connect `memoryToProto` sets `Relevance` via `proto.Float64`; proto `optional double relevance = 31;` gives the generated `engramv1.Memory` a `Relevance *float64` field and `GetRelevance() float64`.
- `cmd/engram/client_common.go`: `renderMemoryTable(w, mems, withScore bool)` — search passes true (`client_search.go:79`), list passes false (`client_list.go:80`); `renderJSON` uses protojson with `EmitDefaultValues` (an unset proto3 optional field is still omitted). `cmd/engram/client_search_test.go`: `stubEngramService{searchFn}`, `startStubServer`, `runClient`, `resetClientFlags`, `resetCommandFlagState`, `lineContaining`, `TestClientSearchTextOutputStateColumn` (header-fields precedent).
- `internal/server/connectapi_test.go`: `TestRerankParityMCPAndConnect` (line ~367) — `testDeps(t)`, seeded records owned by actor-A, `mcpIDs`/`connectIDs` helpers, `authedContext`, `withConnectTokenInfo`; cross-spine precedent in `internal/server/connectapi_crossspine_test.go`.
- `docs-site/src/content/docs/guides/cli.md`: `## Output contract` (line ~92).
</interfaces>

## Artifacts this phase produces (plan 04-04's share)

| Kind | Artifact | Task |
|------|----------|------|
| Server | `recallView.Relevance`, `toRecallView` population | 1 |
| CLI | `renderMemoryTable` RELEVANCE column (data-derived); JSON lane via the proto field | 2 |
| Docs | cli.md Output contract paragraph on `relevance` | 2 |
| Tests | `TestToRecallViewCarriesRelevance`, `TestShapeRecallRelevanceFullAndCompact`, `TestClientSearchTextOutputRelevanceColumn`, `TestClientSearchJSONCarriesRelevance`, `TestClientListNeverShowsRelevance`, jev-hook subtests of `TestRerankParityMCPAndConnect` | 1-3 |

## Flagged assumptions (spec-less probe)

- RANK-04 was `unclassified` by the probe; the presence/absence/zero rules above are the planner's resolution (see plan 04-01's flagged-assumption note).

## Executor notes (every task)

- Execution is sequential on the main tree. NEVER run any git stash subcommand. Commit each task with an explicit pathspec, Conventional Commits, and end the message with the session's attribution trailer lines.
- `cmd/engram` client files must not import `internal/server` (TestClientFilesImportBoundary).
- Scripted rank hooks in tests are plain functions assigned to `d.rankHook` — no network.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MCP compact recall carries per-hit relevance</name>
  <files>internal/server/summary.go, internal/server/summary_test.go</files>
  <read_first>
    - `internal/server/summary.go` lines 38-110 (recallView, shapeRecall, toRecallView)
    - `internal/server/summary_test.go` lines 30-110 (full-vs-summary and usage-signal precedents)
  </read_first>
  <behavior>
    - `TestToRecallViewCarriesRelevance`: a memory with Relevance 0.97 marshals with `"relevance":0.97`; with Relevance pointing at 0 it marshals `"relevance":0`; with nil Relevance the key is absent.
    - `TestShapeRecallRelevanceFullAndCompact`: `shapeRecall` with full false and full true both yield JSON whose per-hit `relevance` equals the input value, and neither yields the key for a nil input.
  </behavior>
  <action>
Implements D-05's MCP compact field. Write the two tests first (they fail: the compact view has no such key). Add `Relevance *float64` tagged `json:"relevance,omitempty"` to `recallView` directly after `Score`, with a doc comment: the Jev reranker's per-hit probability that the record answers the query; present only when the reranker ran and succeeded for this search; a pointer so a genuine 0 still serializes; like AccessCount, it must be populated in toRecallView as well. Populate `Relevance: m.Relevance` in `toRecallView`.

Commit as `feat(server): surface per-hit relevance on compact search_memory results (D-05)` with pathspec `internal/server/summary.go internal/server/summary_test.go`.
  </action>
  <verify>
    <automated>out=$(go test ./internal/server/ -run '^(TestToRecallViewCarriesRelevance|TestShapeRecallRelevanceFullAndCompact|TestShapeRecallFullVsSummary|TestToRecallViewSurfacesUsageSignals)$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e '^--- PASS: (TestToRecallViewCarriesRelevance|TestShapeRecallRelevanceFullAndCompact|TestShapeRecallFullVsSummary|TestToRecallViewSurfacesUsageSignals) ' | wc -l)" -eq 4</automated>
    <fails_when>non-zero exit, or fewer than four PASS lines</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -o -F 'json:"relevance,omitempty"' internal/server/summary.go | wc -l` prints `1`.
    - `golangci-lint run ./internal/server/...` reports no findings.
  </acceptance_criteria>
  <done>Compact and full MCP search results carry relevance exactly when the store set it; RED then GREEN; committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: engram search shows a RELEVANCE column and JSON field; list unchanged; CLI guide</name>
  <files>cmd/engram/client_common.go, cmd/engram/client_search_test.go, docs-site/src/content/docs/guides/cli.md</files>
  <read_first>
    - `cmd/engram/client_common.go` lines 485-560 (renderJSON, renderMemoryTable, truncateSummary)
    - `cmd/engram/client_search_test.go` lines 20-70 and 315-365 (JSON and STATE-column tests)
    - `docs-site/src/content/docs/guides/cli.md` lines 90-130 (Output contract)
  </read_first>
  <behavior>
    - `TestClientSearchTextOutputRelevanceColumn`: a stub response with two memories carrying relevance 0.97 and 0.02 prints header fields exactly `SHORT_ID SCOPE CATEGORY STATE SCORE RELEVANCE SUMMARY`, and the rows show `0.9700` and `0.0200` in that column; a stub response with no relevance prints header fields exactly `SHORT_ID SCOPE CATEGORY STATE SCORE SUMMARY` (unchanged).
    - `TestClientSearchJSONCarriesRelevance`: `--output json` over a response where one memory has relevance 0.5 and one has none decodes to a `relevance` of 0.5 on the first and no `relevance` key on the second.
    - `TestClientListNeverShowsRelevance`: `engram list` text output over memories that carry relevance keeps its header without SCORE or RELEVANCE.
  </behavior>
  <action>
Implements D-05's CLI half. Write the three tests first (the column test fails). In `renderMemoryTable`, derive `withRelevance` as `withScore` AND any memory has a non-nil `Relevance` field (presence, never `GetRelevance() != 0`); when true, the header gains `RELEVANCE` between SCORE and SUMMARY and each row prints the value with `%.4f`, or `-` for a memory without it. Keep every other header and row format byte-identical, and keep the function signature (the list call site passes false and is untouched). Update the function's doc comment to say the column is data-derived. The JSON lane needs no code: protojson already emits the optional field only when set — the test proves it. In `cli.md`'s `## Output contract`, add a paragraph: when the server runs the Jev reranker (`ENGRAM_SEARCH_RANKER=jev`), `engram search` text output gains a RELEVANCE column and JSON output a per-memory `relevance` (the provider's probability, 0 to 1, that the record answers the query; values all near zero mean nothing returned answers it); without it, output is unchanged byte for byte; link the configure guide's Search reranking section.

Commit as `feat(cli): show per-hit relevance in engram search output (D-05)` with pathspec `cmd/engram/client_common.go cmd/engram/client_search_test.go docs-site/src/content/docs/guides/cli.md`.
  </action>
  <verify>
    <automated>out=$(go test ./cmd/engram/ -run '^(TestClientSearchTextOutputRelevanceColumn|TestClientSearchJSONCarriesRelevance|TestClientListNeverShowsRelevance|TestClientSearchTextOutputStateColumn|TestClientSearchEndToEndJSON)$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e '^--- PASS: (TestClientSearchTextOutputRelevanceColumn|TestClientSearchJSONCarriesRelevance|TestClientListNeverShowsRelevance|TestClientSearchTextOutputStateColumn|TestClientSearchEndToEndJSON) ' | wc -l)" -eq 5</automated>
    <fails_when>non-zero exit, or fewer than five PASS lines (the unchanged STATE-column and JSON tests included)</fails_when>
    <automated>go test ./cmd/engram/ -run 'TestClient|TestHelpGolden|TestCatalogGolden' -count=1</automated>
    <fails_when>non-zero exit (another client test or a pinned golden regressed)</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -o -e '[.]Relevance != nil' cmd/engram/client_common.go | wc -l` prints at least `1`.
    - `task lint:markdown` exits 0 and `golangci-lint run ./cmd/engram/...` reports no findings.
  </acceptance_criteria>
  <done>The CLI shows relevance only when the server produced it, JSON carries it verbatim, list is untouched, and the guide documents it; committed.</done>
</task>

<task type="auto">
  <name>Task 3: MCP and Connect agree under the jev hook — order, relevance, fallback, cross_spine, no cross-owner leak, no response-level flag</name>
  <files>internal/server/connectapi_test.go</files>
  <read_first>
    - `internal/server/connectapi_test.go` lines 360-560 (TestRerankParityMCPAndConnect, its helpers and existing subtests)
    - `internal/server/connectapi_crossspine_test.go` (cross-spine request/response precedent)
  </read_first>
  <action>
Implements D-07's lane parity and D-06's no-flag rule. Extend `TestRerankParityMCPAndConnect` (never replace existing subtests) with a helper that sets `d.rankHook` to a scripted `store.RankHook` for the duration of a subtest and restores nil. The scripted hook records the ids it was handed and returns deterministic probabilities keyed by record (for example recOldPython 0.9, recCI 0.6, the rest 0.1). Add subtests:
- `jev hook: MCP and Connect agree on order and relevance`: both lanes return identical ids in identical order and, per id, MCP's `store.Memory.Relevance` equals Connect's `GetRelevance()` with both non-nil; the returned id set equals the no-hook id set for the same k (reordering only — nothing filtered).
- `jev hook error: both fall back to the identical lexical order`: a hook returning an error gives both lanes the exact no-hook order and no relevance on any hit.
- `jev hook with cross_spine`: with `CrossSpine` on both lanes (Connect `CrossSpine: true`, MCP core request `CrossSpine: true`) both lanes agree on order and relevance.
- `jev hook never surfaces another owner's record`: seed one private record owned by actor-B in the same scope, have the hook return 1.0 for its id as well; neither lane returns it and the hook was never handed it.
- `jev hook adds no response-level flag`: the Connect response's populated fields equal the no-hook response's populated field set (compare `ProtoReflect().Range` field names), so only per-memory values change.
Keep the existing subtests' fixtures and assertions untouched.

Commit as `test(server): prove MCP/Connect rerank parity under the jev hook (D-06, D-07)` with pathspec `internal/server/connectapi_test.go`.
  </action>
  <verify>
    <automated>out=$(go test ./internal/server/ -run '^TestRerankParityMCPAndConnect$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; printf '%s\n' "$out" | rg -q -e '^--- PASS: TestRerankParityMCPAndConnect ' &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e '^ +--- PASS: TestRerankParityMCPAndConnect/jev_hook' | wc -l)" -eq 5</automated>
    <fails_when>non-zero exit, the parent test not reporting PASS (including a Qdrant SKIP), or fewer than five passing jev-hook subtests</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -o -e 't[.]Run[(]"jev hook' internal/server/connectapi_test.go | wc -l` prints `5`.
    - `golangci-lint run ./internal/server/...` reports no findings.
  </acceptance_criteria>
  <done>Both SearchReranked lanes agree on order and relevance under the hook, fall back identically, span cross_spine, never leak another owner's record, and add no response-level flag; committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → MCP / Connect / CLI callers | relevance values are rendered to every caller of the search lanes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-02 | Information disclosure | search lanes under the hook | high | mitigate | parity subtest `jev hook never surfaces another owner's record` on both lanes; plan 04-01's store-level test covers the hook's input |
| T-04-09 | Tampering | relevance used to hide results | medium | mitigate | no threshold, filter or flag — parity subtests assert the no-hook membership and unchanged response fields (D-06) |
| T-04-10 | Tampering | a shared record's content engineered to raise its own relevance for other callers | low | accept | reordering only affects records the caller can already read; lexical and vector ranking have the same keyword-stuffing exposure; no action is taken on a relevance value |
</threat_model>

<verification>
- `go test ./internal/server/ ./cmd/engram/ -count=1` exits 0.
- `task lint:markdown` is clean for cli.md.
</verification>

<success_criteria>
- RANK-04: with the reranker enabled, MCP (compact and full), Connect and the CLI all carry the per-hit relevance probability; with it off nothing changes.
- D-07: the parity holds scope-confined and with cross_spine.
</success_criteria>

<output>
Create `.planning/phases/04-jev-reranker-per-hit-relevance-signal/04-04-SUMMARY.md` when done. Record the RED evidence for Tasks 1 and 2.
</output>
