---
phase: 04-jev-reranker-per-hit-relevance-signal
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - internal/decide/jev/jev.go
  - internal/decide/jev/jev_test.go
  - internal/config/config.go
  - internal/config/registry.go
  - internal/config/validate.go
  - internal/config/search_config_test.go
  - internal/config/search_docs_test.go
  - docs-site/src/content/docs/guides/configure.md
  - internal/server/decider.go
  - internal/server/decider_test.go
  - internal/server/tools.go
  - internal/server/tools_test.go
autonomous: true
requirements:
  - RANK-03

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`ENGRAM_SEARCH_RANKER` is a registered key (`search.ranker`, default `lexical`, no Legacy, no Flag) accepting `lexical` or `jev` (empty also validates, meaning lexical); any other value fails `Config.Validate` unconditionally, naming the variable (D-01)"
    - "`ENGRAM_SEARCH_RANKER=jev` with `ENGRAM_DECISIONS_PROVIDER` empty fails `Config.Validate` with an error naming both variables, and `buildDepsFromEnv` returns it before any store dial (D-01)"
    - "`ENGRAM_SEARCH_RERANK_TIMEOUT` is a registered key (`search.rerank_timeout`, default `2s`, no Legacy, no Flag); when the ranker is `jev` it must parse as a strictly positive Go duration, else `Config.Validate` fails naming the variable (a zero would resolve to the 10m decisions ceiling on the synchronous search path) (D-09)"
    - "`jev.WithNoRetry()` makes `Client.Decide` perform exactly one HTTP attempt on a retryable failure (503, 429, a dropped connection); a client without it still retries exactly once (D-09; D-11 unchanged for sweeps)"
    - "The search-path decider is a SECOND `jev.Client` built from the shared `ENGRAM_DECISIONS_*` base URL, key (with the ENGRAM_OPENAI_API_KEY fallback), model and drain bounds, plus `WithNoRetry()` and `WithTimeout(ENGRAM_SEARCH_RERANK_TIMEOUT)`; the consolidate decider (`deciderFromConfig`) is unchanged and still retries once (D-09)"
    - "`buildDepsFromEnv` sets `deps.rankHook` to `relevance.Hook(searchDecider, relevance.DefaultBudget())` only when `ENGRAM_SEARCH_RANKER=jev`; with the ranker `lexical` (default) `deps.rankHook` is nil even when `ENGRAM_DECISIONS_PROVIDER=jev` is set for consolidate, and a search then makes zero Decisions requests and attaches no relevance (default byte-identical)"
    - "With the ranker `jev`, a provider answering 503 receives exactly one request per search, and a provider that stalls past the rerank timeout (150ms in test) makes the search return well inside one second; in both cases the search succeeds in lexical order with no relevance (RANK-03, D-09)"
    - "Enabling the ranker logs one Info line `search reranking enabled` naming ranker, model, endpoint_host (host only), rerank_timeout and api_key_source — never the key value, userinfo, path or query; nothing is logged and no request is made at startup when the ranker is lexical"
    - "`server.SearchRankHookFromEnv()` loads config once and returns the same search-path hook whenever `ENGRAM_DECISIONS_PROVIDER` is set (regardless of the ranker, for the retrieval eval, D-02), a nil hook when it is not, plus a `SearchRerankInfo{Enabled, Model, EndpointHost, Timeout}` for provenance"
    - "The configure guide has a `## Search reranking (Jev)` section documenting both variables in its table, the opt-in, the per-search disclosure (query plus up to 100 candidates' summary-and-content head, 600 characters each, sent on every search while enabled), the timeout, no retry, and the lexical fallback; a registry-driven docs gate fails when any `ENGRAM_SEARCH_*` row lacks a table row"
  artifacts:
    - path: "internal/decide/jev/jev.go"
      provides: "WithNoRetry option gating the single retry"
      contains: "func WithNoRetry() Option"
    - path: "internal/config/registry.go"
      provides: "search.ranker and search.rerank_timeout rows"
      contains: "ENGRAM_SEARCH_RERANK_TIMEOUT"
    - path: "internal/server/decider.go"
      provides: "searchRerankTimeout, searchDeciderFromConfig, searchRankHook, SearchRankHookFromEnv, SearchRerankInfo, logSearchRankerEnabled"
      contains: "func SearchRankHookFromEnv() (store.RankHook, SearchRerankInfo, error)"
    - path: "internal/config/search_docs_test.go"
      provides: "registry-driven docs gate for ENGRAM_SEARCH_*"
      contains: "## Search reranking (Jev)"
  key_links:
    - from: "internal/server/decider.go"
      to: "internal/decide/jev/jev.go"
      via: "the search-path client disables the retry and carries its own timeout"
      pattern: "jev[.]WithNoRetry[(][)]"
    - from: "internal/server/decider.go"
      to: "internal/relevance/relevance.go"
      via: "the server builds the store hook from the shared relevance question set"
      pattern: "relevance[.]Hook[(]"
    - from: "internal/server/tools.go"
      to: "internal/server/decider.go"
      via: "buildDepsFromEnv resolves the hook from the one loaded config"
      pattern: "searchRankHook[(]cfg[)]"
    - from: "internal/config/search_docs_test.go"
      to: "docs-site/src/content/docs/guides/configure.md"
      via: "every ENGRAM_SEARCH_* registry row must have a table row in the section"
      pattern: "Search reranking [(]Jev[)]"
  prohibitions:
    - statement: "MUST NOT send any memory content to the decisions provider on the search path, or change search order or response shape, unless the operator explicitly sets ENGRAM_SEARCH_RANKER=jev — ENGRAM_DECISIONS_PROVIDER alone (enabled for consolidate) never turns reranking on"
      category: privacy
      status: resolved
      verification: test
      reason: "TestBuildDepsFromEnvRankerDefaultIsInert (provider set, ranker unset: nil hook, zero Decisions requests during a search, no relevance). No wired check descriptor by design (spec-less fallback, flagged-unverified)"
---

<objective>
Turn the tracer's injected hook into operator configuration with a search-path client that never retries.

- **Task 1 (TDD):** `jev.WithNoRetry()` — one attempt on a retryable failure.
- **Task 2 (TDD):** `ENGRAM_SEARCH_RANKER` / `ENGRAM_SEARCH_RERANK_TIMEOUT` registry rows, `Config.Search`, validation (enum unconditional; jev requires a provider; positive timeout when jev), the configure guide section with its disclosure, and a registry-driven docs gate.
- **Task 3 (TDD):** a dedicated no-retry search decider, `searchRankHook(cfg)` wired into `buildDepsFromEnv`, the enablement log, and `SearchRankHookFromEnv` for the retrieval eval; proofs that the default stays inert and that a failing or stalled provider never fails a search.

Decisions implemented: D-01 (ranker enum, costly registered key, provider required), D-09 (dedicated timeout, no retry on the search path, sweeps keep theirs), D-02's harness hook (the eval reaches the same hook when a provider is configured).
Output: the jev option, two registry rows and their validation, server wiring, docs and tests.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-jev-reranker-per-hit-relevance-signal/04-CONTEXT.md
@.planning/phases/04-jev-reranker-per-hit-relevance-signal/04-PATTERNS.md
@.planning/phases/04-jev-reranker-per-hit-relevance-signal/04-01-SUMMARY.md
@internal/server/decider.go
@internal/config/registry.go
</context>

<interfaces>
- `internal/decide/jev/jev.go`: `Client` fields incl. `timeout`, `retryDelay func() time.Duration`; `Option func(*Client)`; `WithTimeout`, `WithMaxTimeout`, `WithDrainBytes`, `WithDrainTimeout`, `WithConcurrency`, `WithHTTPTransport`; `Decide` runs `c.attempt` then `if err != nil && isRetryable(err) {...}` (the single retry, lines ~299-316). `internal/decide/jev/jev_test.go`: `noRetryDelay(c)`, `oneNoulRequest()`, `TestJevRetryAndBounds` (503/429/hijack subtests), fixtures `fixtureOpenRouter503`, `fixtureNoulOK`.
- `internal/config/registry.go`: the `decisions.*` block (lines ~99-130) is the row-shape and comment precedent. `internal/config/config.go`: `Config` struct (line ~23) and `DecisionsConfig` (line ~230). `internal/config/validate.go`: unconditional provider-enum check then `if c.Decisions.Provider == "jev" {...}` gated block (lines ~284-330). `internal/config/decisions_config_test.go` and `decisions_docs_test.go` are the test precedents (registry entry / from env / empty-means-default; heading-to-next-heading section cut with a positive-control count).
- `internal/server/decider.go`: `deciderFromConfig(cfg)`, `decisionsTimeout`, `decisionsMaxTimeout`, `decisionsDrainBytes`, `decisionsDrainTimeout`, `logDeciderEnabled(cfg)` (url.Parse host-only, api_key_source), `DeciderFromEnv()` (single config load precedent), `loadAndValidate()`.
- `internal/server/tools.go`: `buildDepsFromEnv` (line ~314) — `dec, err := deciderFromConfig(cfg)`, `if dec != nil { logDeciderEnabled(cfg) }`, `&deps{... decider: dec}`; `deps.rankHook store.RankHook` (added in plan 04-01). `internal/server/tools_test.go`: `TestBuildDepsFromEnvLoadsConfigOnce`, `TestBuildDepsFromEnvConstructsDecider` (httptest counter, `storetest.Addr()`, `testCollection`, t.Setenv isolation of ambient `ENGRAM_DECISIONS_PROVIDER`), `testDeps(t)`.
- From plan 04-01: `relevance.Hook(dec decide.Decider, b Budget) store.RankHook`, `relevance.DefaultBudget()`, `store.RankHook`.
- `docs-site/src/content/docs/guides/configure.md`: `## Typed decisions (Jev)` (line ~152) is the section and disclosure precedent; the next heading is `## OIDC / Auth`.
</interfaces>

## Artifacts this phase produces (plan 04-03's share)

| Kind | Artifact | Task |
|------|----------|------|
| Client | `jev.WithNoRetry()`, `Client.noRetry` | 1 |
| Config | `SearchConfig{Ranker, RerankTimeout}`, `Config.Search`, registry rows `search.ranker` / `search.rerank_timeout`, validation block | 2 |
| Docs | configure.md `## Search reranking (Jev)` (table rows, disclosure, failure behavior); one cross-reference sentence in `## Typed decisions (Jev)` | 2 |
| Server | `searchRerankTimeout`, `searchDeciderFromConfig`, `searchRankHook`, `SearchRerankInfo`, `SearchRankHookFromEnv`, `logSearchRankerEnabled`, `buildDepsFromEnv` wiring | 3 |
| Tests | `TestJevNoRetryOption`, `TestSearchRegistryEntries`, `TestSearchConfigValidate`, `TestSearchVarsDocumented`, `TestSearchRankHookFromConfig`, `TestSearchRerankTimeoutResolver`, `TestSearchRankerEnabledLogLine`, `TestDeciderFromConfigStillRetries`, `TestBuildDepsFromEnvRankerDefaultIsInert`, `TestBuildDepsFromEnvRankerJev`, `TestBuildDepsFromEnvRejectsJevRankerWithoutProvider`, `TestSearchRerankNoRetryAndTimeout`, `TestSearchRankHookFromEnv` | 1-3 |

## Flagged assumptions (spec-less probe)

- The rerank timeout validation runs only when the ranker is `jev` (like every gated `decisions.*` field); the resolver still falls back to 2s with a Warn for the eval path, where the ranker may be lexical.
- Helm values for both keys (D-10) are plan 04-08's; this plan does not edit `charts/` or `Taskfile.yaml`.

## Executor notes (every task)

- Execution is sequential on the main tree. NEVER run any git stash subcommand. Commit each task with an explicit pathspec, Conventional Commits, and end the message with the session's attribution trailer lines.
- New Go files carry the Apache-2.0 SPDX header; docs-site files never do.
- Every test that loads config through the environment isolates ambient `ENGRAM_SEARCH_RANKER`, `ENGRAM_DECISIONS_PROVIDER` and `ENGRAM_SUMMARY_*` with `t.Setenv`, as `TestBuildDepsFromEnvLoadsConfigOnce` does.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: jev.WithNoRetry — exactly one attempt on a retryable failure</name>
  <files>internal/decide/jev/jev.go, internal/decide/jev/jev_test.go</files>
  <read_first>
    - `internal/decide/jev/jev.go` lines 80-175 (Client, Option family) and 215-320 (Decide, retry branch)
    - `internal/decide/jev/jev_test.go` lines 25-140 (noRetryDelay, TestJevRetryAndBounds subtests incl. the hijacked-connection case)
  </read_first>
  <behavior>
    - `TestJevNoRetryOption`: subtests `503_once`, `429_once`, `dropped_connection_once` — a client built with `WithNoRetry()` (and `noRetryDelay`) against a handler that fails the first request and would succeed on the second makes exactly ONE request and returns the classified error (ErrDecisionUnavailable, ErrDecisionRateLimited, the transport class respectively); subtest `default_client_still_retries` — the same 503-then-200 handler without the option makes exactly two requests and succeeds.
  </behavior>
  <action>
Implements D-09's client half. Write `TestJevNoRetryOption` in `jev_test.go` first (it fails to compile until the option exists — record that as RED). Add a `noRetry bool` field to `Client` and `func WithNoRetry() Option` with a doc comment: disables D-11's single retry so a caller on a latency-bound synchronous path (search reranking, D-09) gets exactly one attempt inside its timeout; sweeps keep the retry by not passing it. Gate the existing retry branch in `Decide` on `!c.noRetry` in the same condition that checks `isRetryable(err)` — no second retry path, no change to what `isRetryable` classifies.

Commit as `feat(jev): add WithNoRetry for latency-bound callers (D-09)` with pathspec `internal/decide/jev/jev.go internal/decide/jev/jev_test.go`.
  </action>
  <verify>
    <automated>out=$(go test ./internal/decide/jev/ -run '^(TestJevNoRetryOption|TestJevRetryAndBounds)$' -count=1 -race -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e '^--- PASS: (TestJevNoRetryOption|TestJevRetryAndBounds) ' | wc -l)" -eq 2</automated>
    <fails_when>non-zero exit, a race report, or either test not reporting PASS</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -o -e 'func WithNoRetry[(][)] Option' internal/decide/jev/jev.go | wc -l` prints `1`.
    - `rg -o -e 'isRetryable[(]err[)]' internal/decide/jev/jev.go | wc -l` prints `1` (still exactly one retry gate).
    - `golangci-lint run ./internal/decide/...` reports no findings.
  </acceptance_criteria>
  <done>A no-retry jev client makes one attempt on every retryable failure; the default client's single retry is unchanged; committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ENGRAM_SEARCH_RANKER and ENGRAM_SEARCH_RERANK_TIMEOUT — registry, config, validation, configure guide, docs gate</name>
  <reversibility rating="costly">Two new registered operator config keys (D-01 rates the ranker key costly): renaming or removing either later needs a Legacy alias and an upgrade note.</reversibility>
  <files>internal/config/config.go, internal/config/registry.go, internal/config/validate.go, internal/config/search_config_test.go, internal/config/search_docs_test.go, docs-site/src/content/docs/guides/configure.md</files>
  <read_first>
    - `internal/config/registry.go` lines 95-135 (decisions block and its comment)
    - `internal/config/config.go` lines 20-45 (Config) and 220-250 (DecisionsConfig)
    - `internal/config/validate.go` lines 280-335 (decisions provider enum and gated block)
    - `internal/config/decisions_config_test.go` (whole file) and `internal/config/decisions_docs_test.go` (whole file)
    - `docs-site/src/content/docs/guides/configure.md` lines 150-220 (Typed decisions section, disclosure, table)
  </read_first>
  <behavior>
    - `TestSearchRegistryEntries` (mirrors TestDecisionsRegistryEntries): exactly one registry row each for `search.ranker` (Env `ENGRAM_SEARCH_RANKER`, Default `lexical`) and `search.rerank_timeout` (Env `ENGRAM_SEARCH_RERANK_TIMEOUT`, Default `2s`), empty Legacy and Flag; both reach `cfg.Search` from the environment through `Load(nil)`; both read back as their default when set to empty.
    - `TestSearchConfigValidate` (table on a Config otherwise valid): ranker `""`, `lexical`, and `jev` with provider `jev` plus a base URL all validate; ranker `JEV`, `vector` and `jev ` each fail naming `ENGRAM_SEARCH_RANKER` even with the provider empty; ranker `jev` with provider empty fails naming both `ENGRAM_SEARCH_RANKER` and `ENGRAM_DECISIONS_PROVIDER`; with ranker `jev`, rerank timeout `0`, `-1s` and `soon` each fail naming `ENGRAM_SEARCH_RERANK_TIMEOUT`, and `150ms` validates; with ranker `lexical`, timeout `soon` validates (gated).
    - `TestSearchVarsDocumented`: the registry-derived `ENGRAM_SEARCH_*` set has exactly 2 names (positive control), each has a `| \`<ENV>\` |` table row inside the `## Search reranking (Jev)` section of configure.md, and the section contains the disclosure anchor `What leaves your deployment` and the phrase `falls back`.
  </behavior>
  <action>
Implements D-01 and D-09's configuration half.

(1) `internal/config/config.go`: new `SearchConfig struct { Ranker string \`koanf:"ranker"\`; RerankTimeout string \`koanf:"rerank_timeout"\` }` with a doc comment (search-path reranking; values kept as strings and validated by Validate, per this package's convention), and `Search SearchConfig \`koanf:"search"\`` in `Config` after `Decisions`.

(2) `internal/config/registry.go`: after the `decisions.*` rows add `{Key: "search.ranker", Env: "ENGRAM_SEARCH_RANKER", Default: "lexical"}` and `{Key: "search.rerank_timeout", Env: "ENGRAM_SEARCH_RERANK_TIMEOUT", Default: "2s"}`, preceded by a comment in the decisions block's style: brand-new keys, no Legacy, no Flag (deployment-topology values); `jev` requires ENGRAM_DECISIONS_PROVIDER and reuses its base URL, key and model; the timeout is dedicated to the synchronous search path with no retry (D-09), while sweeps keep ENGRAM_DECISIONS_TIMEOUT and their single retry.

(3) `internal/config/validate.go`, directly after the decisions block: an unconditional enum check (empty, `lexical` or `jev`; else an error naming ENGRAM_SEARCH_RANKER and the allowed values); then, when the ranker is `jev`: an error naming both variables if `c.Decisions.Provider` is empty, and a duration check on `c.Search.RerankTimeout` rejecting a parse error or any value at or below zero with an error naming ENGRAM_SEARCH_RERANK_TIMEOUT (comment: zero would resolve to the decisions max-timeout ceiling, unacceptable on the synchronous search path).

(4) `docs-site/src/content/docs/guides/configure.md`: a new `## Search reranking (Jev)` section immediately after the Typed decisions section's `Source:` line and before `## OIDC / Auth`, with: an opt-in paragraph (off by default; `ENGRAM_SEARCH_RANKER=jev` reorders `search_memory` and `search_discovery` results by the provider's probability that each record answers the query, and adds a per-hit `relevance` value; requires `ENGRAM_DECISIONS_PROVIDER` and reuses its base URL, key and model; lexical stays the default); a bold `What leaves your deployment.` paragraph (on EVERY search while enabled: the query and, for up to 100 candidate records the caller can already read, each record's summary followed by the head of its content, 600 characters each, shrunk further if needed to fit the provider's context; same provider and data policy as the Typed decisions section, linked to it); a bold `Failure behavior.` paragraph (one request per search bounded by `ENGRAM_SEARCH_RERANK_TIMEOUT`, no retry; on any failure the search still succeeds and falls back to the default lexical order with no `relevance` values; startup logs `search reranking enabled`); and a table with rows for both variables in the existing four-column format. Add one sentence to the Typed decisions intro saying the provider is also asked by search reranking when `ENGRAM_SEARCH_RANKER=jev`, linking to the new section. Keep every existing anchor and table row of the Typed decisions section intact.

(5) Tests: new `internal/config/search_config_test.go` (SPDX) with `TestSearchRegistryEntries` and `TestSearchConfigValidate`; new `internal/config/search_docs_test.go` (SPDX) with `TestSearchVarsDocumented`, reusing the decisions docs gate's section-cut approach with its own heading constant.

Commit as `feat(config): register ENGRAM_SEARCH_RANKER and ENGRAM_SEARCH_RERANK_TIMEOUT (D-01, D-09)` with pathspec `internal/config/config.go internal/config/registry.go internal/config/validate.go internal/config/search_config_test.go internal/config/search_docs_test.go docs-site/src/content/docs/guides/configure.md`.
  </action>
  <verify>
    <automated>out=$(go test ./internal/config/ -run '^(TestSearchRegistryEntries|TestSearchConfigValidate|TestSearchVarsDocumented|TestDecisionsVarsDocumented|TestDecisionsRegistryEntries)$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e '^--- PASS: (TestSearchRegistryEntries|TestSearchConfigValidate|TestSearchVarsDocumented|TestDecisionsVarsDocumented|TestDecisionsRegistryEntries) ' | wc -l)" -eq 5</automated>
    <fails_when>non-zero exit, or fewer than five PASS lines (a new gate missing, or the Typed decisions docs gate broken by the edit)</fails_when>
    <automated>go test ./internal/config/ -count=1</automated>
    <fails_when>non-zero exit (an existing config test broken by the new struct or validation)</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -o -e 'Key: "search[.](ranker|rerank_timeout)"' internal/config/registry.go | wc -l` prints `2`.
    - `rg -o -F '## Search reranking (Jev)' docs-site/src/content/docs/guides/configure.md | wc -l` prints `1`.
    - `task lint:markdown` exits 0 and `golangci-lint run ./internal/config/...` reports no findings; `task license:check` exits 0.
  </acceptance_criteria>
  <done>Both keys are registered, loaded, validated per D-01/D-09 and documented with the per-search disclosure behind a registry-driven docs gate; committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Dedicated no-retry search decider wired into deps — inert by default, one attempt and a bounded wait when enabled</name>
  <files>internal/server/decider.go, internal/server/decider_test.go, internal/server/tools.go, internal/server/tools_test.go</files>
  <read_first>
    - `internal/server/decider.go` (whole file)
    - `internal/server/tools.go` lines 20-100 (deps) and 300-350 (buildDepsFromEnv)
    - `internal/server/tools_test.go` lines 5160-5280 (LoadsConfigOnce, RejectsUnknownProvider, ConstructsDecider)
    - `internal/server/decider_test.go` lines 460-540 (TestDeciderEnabledLogLine) and 700-760 (TestDeciderFromEnv)
    - `internal/server/rerank_jev_test.go` (plan 04-01's tracer: seeding, httptest Decisions handler, Connect call)
  </read_first>
  <behavior>
    - `TestSearchRerankTimeoutResolver`: `2s` default on empty; `150ms` honored; `0`, `-1s` and `soon` fall back to 2s (the latter three with a Warn naming the variable).
    - `TestSearchRankHookFromConfig`: provider `jev` with ranker `lexical` gives a nil hook; ranker `jev` gives a non-nil hook; provider empty gives a nil hook and no error.
    - `TestSearchRankerEnabledLogLine`: mirrors TestDeciderEnabledLogLine over the same three key-source cases — one Info record `search reranking enabled` with `ranker`, `model`, `endpoint_host` (host only), `rerank_timeout`, `api_key_source`; the key values, userinfo, path and query never appear.
    - `TestDeciderFromConfigStillRetries`: the consolidate decider from `deciderFromConfig` against a 503-then-200 handler makes two requests and succeeds.
    - `TestBuildDepsFromEnvRankerDefaultIsInert` (Qdrant): provider `jev` with a counting httptest base URL and the ranker unset: `d.rankHook` is nil, `d.decider` is non-nil, no enablement log line for the ranker, and a `d.searchMemory` call over seeded records leaves the counter at 0 and returns no relevance.
    - `TestBuildDepsFromEnvRankerJev` (Qdrant): ranker and provider `jev`: `d.rankHook` non-nil, zero requests during startup.
    - `TestBuildDepsFromEnvRejectsJevRankerWithoutProvider` (hermetic): ranker `jev`, provider empty: error naming ENGRAM_SEARCH_RANKER, no store dialed.
    - `TestSearchRerankNoRetryAndTimeout` (Qdrant via testDeps with `d.rankHook` from `searchRankHook(cfg)`): subtest `503_single_attempt` — exactly one Decisions request, the Connect search succeeds in the same order as with a nil hook, no relevance; subtest `stall_bounded_by_timeout` — rerank timeout 150ms against a handler that sleeps 2s: the search returns in under 1s, same order as nil hook, no relevance.
    - `TestSearchRankHookFromEnv`: provider unset gives a nil hook and `Enabled` false; provider set (ranker lexical) gives a non-nil hook, `Enabled` true, `EndpointHost` host-only and `Timeout` 2s; one config load per call.
  </behavior>
  <action>
Implements D-09's wiring and D-01's runtime gate. In `internal/server/decider.go`:
- `searchRerankTimeout(cfg) time.Duration`: parse `cfg.Search.RerankTimeout`; on a parse error or a value at or below zero, Warn naming ENGRAM_SEARCH_RERANK_TIMEOUT when it was non-empty and return 2s.
- `searchDeciderFromConfig(cfg) (decide.Decider, error)`: nil, nil for an empty provider; for `jev`, `jev.New` with the same base URL, `cmp.Or` key fallback, model, OTel transport, `decisionsMaxTimeout`, drain bytes and drain timeout as `deciderFromConfig`, plus `jev.WithTimeout(searchRerankTimeout(cfg))` and `jev.WithNoRetry()`; unknown providers error exactly as `deciderFromConfig` does. Doc comment: a second, dedicated client so the search path never shares or doubles the sweep client's retry (D-09).
- `searchRankHook(cfg) (store.RankHook, error)`: nil, nil unless `cfg.Search.Ranker == "jev"`; otherwise `relevance.Hook(dec, relevance.DefaultBudget())` over `searchDeciderFromConfig(cfg)`.
- `type SearchRerankInfo struct { Enabled bool; Model, EndpointHost string; Timeout time.Duration }` and `func SearchRankHookFromEnv() (store.RankHook, SearchRerankInfo, error)`: one `loadAndValidate()`; when the provider is set, build the hook from `searchDeciderFromConfig` regardless of the ranker (the retrieval eval measures the Jev row whenever a provider is configured, D-02) and fill the info (host via url.Parse, never userinfo); else a nil hook and zero info.
- `logSearchRankerEnabled(cfg)`: one Info line `search reranking enabled` with `ranker`, `model`, `endpoint_host`, `rerank_timeout` and `api_key_source` computed exactly like `logDeciderEnabled`, never the key value. Doc comment names T-04-01: the startup log is the operator-visible disclosure that every search now egresses candidate text.
In `internal/server/tools.go` `buildDepsFromEnv`: after the decider, `hook, err := searchRankHook(cfg)`; return the error; when hook is non-nil call `logSearchRankerEnabled(cfg)`; set `rankHook: hook` in the deps literal. Update the `rankHook` field's doc comment to name `searchRankHook` as its only production source.
Tests per `<behavior>` in `decider_test.go` and `tools_test.go`; for the Qdrant search subtests reuse the tracer's seeding and handler approach (seed with the store directly, call `api.SearchMemories` through `&engramAPI{d: d}`). Extend `TestBuildDepsFromEnvLoadsConfigOnce` with `t.Setenv("ENGRAM_SEARCH_RANKER", "")` and a `d.rankHook == nil` assertion.

Commit as `feat(server): build a dedicated no-retry search rank hook from ENGRAM_SEARCH_RANKER (D-01, D-09)` with pathspec `internal/server/decider.go internal/server/decider_test.go internal/server/tools.go internal/server/tools_test.go`.
  </action>
  <verify>
    <automated>out=$(go test ./internal/server/ -run '^(TestSearchRerankTimeoutResolver|TestSearchRankHookFromConfig|TestSearchRankerEnabledLogLine|TestDeciderFromConfigStillRetries|TestBuildDepsFromEnvRankerDefaultIsInert|TestBuildDepsFromEnvRankerJev|TestBuildDepsFromEnvRejectsJevRankerWithoutProvider|TestSearchRerankNoRetryAndTimeout|TestSearchRankHookFromEnv|TestBuildDepsFromEnvLoadsConfigOnce|TestBuildDepsFromEnvConstructsDecider)$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e '^--- PASS: (TestSearchRerankTimeoutResolver|TestSearchRankHookFromConfig|TestSearchRankerEnabledLogLine|TestDeciderFromConfigStillRetries|TestBuildDepsFromEnvRankerDefaultIsInert|TestBuildDepsFromEnvRankerJev|TestBuildDepsFromEnvRejectsJevRankerWithoutProvider|TestSearchRerankNoRetryAndTimeout|TestSearchRankHookFromEnv|TestBuildDepsFromEnvLoadsConfigOnce|TestBuildDepsFromEnvConstructsDecider) ' | wc -l)" -eq 11</automated>
    <fails_when>non-zero exit, or fewer than eleven top-level PASS lines (a Qdrant-backed test skipped counts as missing)</fails_when>
    <automated>go test ./internal/server/ -run 'Decider|BuildDeps|SearchRerank' -count=1</automated>
    <fails_when>non-zero exit (an existing decider or deps test regressed)</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -o -e 'jev[.]WithNoRetry[(][)]' internal/server/decider.go | wc -l` prints `1` (only the search-path client disables the retry).
    - `rg -o -e 'searchRankHook[(]cfg[)]' internal/server/tools.go | wc -l` prints `1`.
    - `golangci-lint run ./internal/server/...` reports no findings.
  </acceptance_criteria>
  <done>The ranker key alone turns reranking on through a dedicated one-attempt, timeout-bounded client; the default is proven inert even with a provider configured; the eval can obtain the same hook; committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator config → server | the ranker enum and timeout decide whether search egresses content |
| engram → decisions provider | per-search egress of query plus candidate heads when the ranker is jev |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Information disclosure | `searchRankHook` / `buildDepsFromEnv` | high | mitigate | egress only when `ENGRAM_SEARCH_RANKER=jev` (never implied by the provider alone — TestBuildDepsFromEnvRankerDefaultIsInert); startup Info line `search reranking enabled` (host only, key source only); configure.md `What leaves your deployment` paragraph behind TestSearchVarsDocumented |
| T-04-04 | Denial of service | synchronous search latency | high | mitigate | dedicated `ENGRAM_SEARCH_RERANK_TIMEOUT` (default 2s, strictly positive when enabled), `WithNoRetry()` so a failing provider costs one attempt (TestSearchRerankNoRetryAndTimeout), lexical fallback |
| T-04-06 | Denial of service | shared retrying client | medium | mitigate | a second client instance; the consolidate client keeps its retry and the search client never inherits it (TestDeciderFromConfigStillRetries, TestJevNoRetryOption) |
| T-04-07 | Information disclosure | enablement log | low | mitigate | logs host and key source only (TestSearchRankerEnabledLogLine sentinel checks for key value, userinfo, path, query) |
| T-04-08 | Tampering | misconfiguration | low | mitigate | unconditional ranker enum validation; jev without a provider and a non-positive timeout fail startup (TestSearchConfigValidate) |
</threat_model>

<verification>
- `go test ./internal/decide/jev/ ./internal/config/ ./internal/server/ -count=1` exits 0.
- `task lint` is clean for the touched packages and docs.
</verification>

<success_criteria>
- D-01: `ENGRAM_SEARCH_RANKER` = `lexical` (default) | `jev`; `jev` without a provider fails clearly.
- D-09: the search path uses a dedicated timeout and never retries; sweeps keep their timeout and single retry.
- RANK-03's fallback holds for a failing and a stalled provider through the real configured wiring.
</success_criteria>

<output>
Create `.planning/phases/04-jev-reranker-per-hit-relevance-signal/04-03-SUMMARY.md` when done. Record the RED evidence for each task.
</output>
