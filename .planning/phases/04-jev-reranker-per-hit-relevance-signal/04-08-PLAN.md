---
phase: 04-jev-reranker-per-hit-relevance-signal
plan: 08
type: execute
wave: 3
depends_on:
  - 04-03
files_modified:
  - charts/engram/values.yaml
  - charts/engram/templates/_helpers.tpl
  - Taskfile.yaml
  - docs-site/src/content/docs/guides/deploy.md
autonomous: true
requirements:
  - RANK-03

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The chart has a `memory.search` block with `ranker` (default `lexical`) and `rerankTimeout` (default empty, meaning the binary default of 2s); the default render contains no `ENGRAM_SEARCH_` variable and is byte-identical to the merge-base chart's default render (D-10)"
    - "Every `ENGRAM_SEARCH_*` row in `engram.containerEnv` sits under one gate that is true only for a non-empty, non-`lexical` `memory.search.ranker`: `ranker=lexical` or an empty ranker with `rerankTimeout` set renders no search variable (D-10)"
    - "`memory.search.ranker=jev` renders `ENGRAM_SEARCH_RANKER` with value `jev` even when `memory.decisions.provider` is empty, so a ranker enabled without a provider reaches the server's `Config.Validate` and fails startup naming both variables (D-01) instead of rendering nothing; `ENGRAM_SEARCH_RERANK_TIMEOUT` renders only when `rerankTimeout` is set (D-10, D-09)"
    - "`task chart:validate` asserts the gate in both directions (default, lexical-with-timeout, empty-with-timeout, jev-without-provider, jev-with-provider-and-timeout) and passes with a re-pinned `engram.containerEnv` checksum whose re-pin log names the search rows and D-10; the new assertions were observed failing before the template existed (D-10)"
    - "`guides/deploy.md`'s Helm key-values table lists `memory.search.ranker` and `memory.search.rerankTimeout` with their `ENGRAM_SEARCH_*` variables, and a paragraph states the gate, the provider requirement, the lexical fallback, and links to the configure guide's `Search reranking (Jev)` section for what every search sends (D-10)"
  artifacts:
    - path: "charts/engram/values.yaml"
      provides: "memory.search values: ranker (default lexical) and rerankTimeout"
      contains: "rerankTimeout"
    - path: "charts/engram/templates/_helpers.tpl"
      provides: "ranker-gated ENGRAM_SEARCH_RANKER and ENGRAM_SEARCH_RERANK_TIMEOUT rows in engram.containerEnv"
      contains: "ENGRAM_SEARCH_RERANK_TIMEOUT"
    - path: "Taskfile.yaml"
      provides: "chart:validate search assertions and the re-pinned containerEnv checksum"
      contains: "ENGRAM_SEARCH_RANKER"
    - path: "docs-site/src/content/docs/guides/deploy.md"
      provides: "memory.search Helm key-value rows and the gate paragraph"
      contains: "memory.search.rerankTimeout"
  key_links:
    - from: "charts/engram/templates/_helpers.tpl"
      to: "charts/engram/values.yaml"
      via: "every search row is gated on memory.search.ranker being set and not lexical (D-10)"
      pattern: "memory[.]search[.]ranker"
    - from: "Taskfile.yaml"
      to: "charts/engram/templates/_helpers.tpl"
      via: "chart:validate renders the gate in both directions and pins the containerEnv checksum"
      pattern: "ENGRAM_SEARCH_RERANK_TIMEOUT"
    - from: "docs-site/src/content/docs/guides/deploy.md"
      to: "docs-site/src/content/docs/guides/configure.md"
      via: "the Helm rows point at the configure section that holds the per-search disclosure"
      pattern: "/guides/configure/#search-reranking-jev"
  prohibitions:
    - statement: "MUST NOT render any ENGRAM_SEARCH_* variable, or otherwise turn on per-search egress, on a chart install that leaves memory.search.ranker at its default"
      category: privacy
      status: resolved
      verification: test
      reason: "chart:validate assertions (a) and (b) plus Task 1's merge-base default-render comparison. No wired check descriptor by design (spec-less fallback, flagged-unverified)"
---

<objective>
Expose the search ranker on the Helm chart per D-10, off by default, with the default render byte-identical.

- **Task 1:** `memory.search.ranker` (default `lexical`) and `memory.search.rerankTimeout` values, a ranker-gated block in `engram.containerEnv` rendering `ENGRAM_SEARCH_RANKER` / `ENGRAM_SEARCH_RERANK_TIMEOUT`, `chart:validate` both-directions assertions (RED first), and the re-pinned checksum.
- **Task 2:** the deploy guide's Helm key-values rows and gate paragraph, linking to the configure guide's disclosure.

Decisions implemented: D-10 (Helm values gated like `memory.decisions`, default render byte-identical, `chart:validate` checksum and deploy docs updated); keeps D-01's startup rejection of `jev` without a provider reachable from the chart, and exposes D-09's dedicated timeout.
Output: chart values, template rows, Taskfile assertions and checksum, deploy guide rows.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-jev-reranker-per-hit-relevance-signal/04-CONTEXT.md
@.planning/phases/04-jev-reranker-per-hit-relevance-signal/04-03-SUMMARY.md
</context>

<interfaces>
Verified at planning time (line numbers are pre-phase and approximate):

- `charts/engram/values.yaml`: the `memory.decisions` block (lines ~129-146: a leading comment, then `provider`, `baseURL`, `apiKeySecret{name,key}`, `model`, `timeout`, `concurrency`, each with an inline `# ENGRAM_…` comment) is followed by `qdrant:` (line ~147). No `memory.search` key exists yet.
- `charts/engram/templates/_helpers.tpl`: `{{- define "engram.containerEnv" -}}` at line 1. The decisions block is lines ~58-82: a leading template comment, `{{- if .Values.memory.decisions.provider }}`, flow-style rows such as `- { name: ENGRAM_DECISIONS_PROVIDER, value: "{{ .Values.memory.decisions.provider }}" }`, `with`-guarded optional rows, and its closing `{{- end }}`. The next line is the `ENGRAM_MCP_PATH` comment. The define closes with `{{- end -}}` (line ~206).
- `Taskfile.yaml` `chart:validate`: the decisions both-directions block is lines ~242-298 (comment, then `helm template … | grep -q` assertions, each failure `echo "chart:validate: …"` then `exit 1`); `helm lint charts/engram` at line ~299; the drift-pin comment (a running re-pin log whose last sentence covers the decisions rows) lines ~300-320; `EXPECTED_CHECKSUM="c9eea846…"` at line ~321, computed by `awk '/define "engram.containerEnv"/{f=1} f{print} f && /^\{\{- end -\}\}$/{exit}' charts/engram/templates/_helpers.tpl | shasum -a 256`; the task ends with `echo "chart:validate: OK"`.
- `docs-site/src/content/docs/guides/deploy.md` `### Key values` (line ~20): table `| Helm value | ENGRAM_* variable set | Description |`, the five `memory.decisions.*` rows at lines ~38-42, then the `ENGRAM_QDRANT_ADDR` sentence, the `ENGRAM_OPENAI_API_KEY` Secret sentence, the `ENGRAM_DECISIONS_API_KEY` paragraph (lines ~48-51), and `For the full environment variable reference, see [Configure](/guides/configure/).`
- Anchor convention: docs link configure sections as `/guides/configure/#typed-decisions-jev`, so plan 04-03's `## Search reranking (Jev)` is `/guides/configure/#search-reranking-jev`.
- Helm 4: `--set key=` sets an empty string (verified: `--set memory.summarize.timeout=` drops `ENGRAM_SUMMARY_TIMEOUT`).
- Verified at planning time: the default render at HEAD is byte-identical to the default render of the chart at `git merge-base HEAD main`.
</interfaces>

## Artifacts this phase produces (plan 04-08's share)

| Kind | Artifact | Task |
|------|----------|------|
| Chart | `memory.search.ranker` / `memory.search.rerankTimeout` values; ranker-gated `ENGRAM_SEARCH_RANKER` / `ENGRAM_SEARCH_RERANK_TIMEOUT` rows in `engram.containerEnv` | 1 |
| Validation | `chart:validate` search assertions (a)-(d), re-pinned `EXPECTED_CHECKSUM` and re-pin log sentence | 1 |
| Docs | `guides/deploy.md` Key values rows for both values and the gate paragraph linking to `#search-reranking-jev` | 2 |

## Flagged assumptions (spec-less probe)

- The search gate is independent of `memory.decisions.provider` (not nested under it): `ranker=jev` without a provider must reach the server, whose `Config.Validate` refuses to start naming both variables (D-01), rather than render nothing and leave the operator's opt-in silently inert. No render-time `fail` is added — the `memory.decisions` block D-10 names as the model has none, and the rule keeps its single home in `Config.Validate`.
- Any non-empty, non-`lexical` ranker renders (not only `jev`), so a typo such as `Jev` fails startup through D-01's unconditional enum check instead of silently rendering nothing.
- `rerankTimeout` defaults to `""` (binary default 2s), mirroring `memory.decisions.timeout`.
- The configure guide is not edited here; plan 04-03 owns its `## Search reranking (Jev)` section and the deploy guide links to it.

## Executor notes (every task)

- Execution is sequential on the main tree. NEVER run any git stash subcommand. Write any pre-change render to the session scratchpad. Commit each task with an explicit pathspec, Conventional Commits, and end the message with the session's attribution trailer lines.
- `docs-site/**` takes no SPDX header. No Go file is touched.
- `task lint` covers yamlfmt and rumdl and must stay clean for `values.yaml`, `Taskfile.yaml` and the markdown.
- New `chart:validate` assertions use the neighbouring blocks' `grep -q` style for consistency within the task.

<tasks>

<task type="auto">
  <name>Task 1: Helm memory.search values, ranker-gated env rows, chart:validate both directions and re-pinned checksum (D-10)</name>
  <files>charts/engram/values.yaml, charts/engram/templates/_helpers.tpl, Taskfile.yaml</files>
  <read_first>
    - `charts/engram/values.yaml` lines 125-150 (the `memory.decisions` block and its comment)
    - `charts/engram/templates/_helpers.tpl` lines 55-90 (the decisions block and what follows it)
    - `Taskfile.yaml` lines 240-325 (the decisions assertions, `helm lint`, the drift-pin comment and checksum)
    - `.planning/phases/02-decision-interface-jev-backend/02-06-PLAN.md` Task 1 (the precedent this task mirrors)
  </read_first>
  <action>
Implements D-10 (Helm values for the search ranker, gated like `memory.decisions`, default render byte-identical, `chart:validate` checksum updated).

(0) Before editing, run `helm template charts/engram` into `<scratchpad>/render-before.yaml`.

(1) RED first — `Taskfile.yaml` `chart:validate`: directly after the last decisions assertion and before `helm lint charts/engram`, add a block whose leading comment matches its neighbours: search reranking (Jev, D-10); every search row sits under the `memory.search.ranker` gate (set and not `lexical`), asserted in both directions for the same reason as the decisions block; the ranker is deliberately independent of `memory.decisions.provider`, and (c) pins that, so `jev` without a provider reaches the server's D-01 validation instead of rendering nothing. Assertions, each failing with an `echo "chart:validate: …"` reason and `exit 1`:
- (a) the default render must NOT contain `ENGRAM_SEARCH_`;
- (b) `--set memory.search.ranker=lexical --set memory.search.rerankTimeout=5s` must NOT contain `ENGRAM_SEARCH_`, and neither may `--set memory.search.ranker= --set memory.search.rerankTimeout=5s` (the timeout sits under the ranker gate; an empty ranker means the binary default);
- (c) `--set memory.search.ranker=jev` alone (provider unset) must contain `name: ENGRAM_SEARCH_RANKER, value: "jev"` and must NOT contain `ENGRAM_SEARCH_RERANK_TIMEOUT`;
- (d) `--set memory.search.ranker=jev --set memory.search.rerankTimeout=1500ms --set memory.decisions.provider=jev --set memory.decisions.baseURL=https://openrouter.example/api` must contain `name: ENGRAM_SEARCH_RANKER, value: "jev"`, `name: ENGRAM_SEARCH_RERANK_TIMEOUT, value: "1500ms"` and `name: ENGRAM_DECISIONS_PROVIDER`.
Run `task chart:validate` now: it must exit non-zero with the (c) reason (the template does not exist yet). Record that line in the SUMMARY as the RED evidence.

(2) `charts/engram/values.yaml`: after the `memory.decisions` block and before `qdrant:`, add `memory.search`. Precede it with a comment block: search reranking (Jev); the default `lexical` renders no search variable, so search order and response shape stay as shipped (D-10); `jev` reorders `search_memory` / `search_discovery` results by the decision provider's per-hit relevance probability and adds a per-hit `relevance`; it requires `memory.decisions.provider` (the server refuses to start without it); while enabled, EVERY search sends the query plus up to 100 candidate records' summary and content head to that provider, so read the configure guide's "Search reranking (Jev)" section first. Keys, each with an inline `# ENGRAM_…` comment: `ranker: lexical` (`ENGRAM_SEARCH_RANKER`; `lexical` default or `jev`) and `rerankTimeout: ""` (`ENGRAM_SEARCH_RERANK_TIMEOUT`; empty means the binary default of 2s; one attempt, no retry; on timeout or error the search falls back to lexical order). Run `yamlfmt` on the file.

(3) `charts/engram/templates/_helpers.tpl`: inside `engram.containerEnv`, directly after the decisions block's closing `{{- end }}` and before the `ENGRAM_MCP_PATH` comment, add one block with a leading template comment (an unset or `lexical` ranker omits every search variable, so the default render is byte-identical, D-10; deliberately not nested under `memory.decisions.provider`, so `jev` without a provider fails loudly at server startup, D-01). The block opens with `{{- if and .Values.memory.search.ranker (ne .Values.memory.search.ranker "lexical") }}`, emits `- { name: ENGRAM_SEARCH_RANKER, value: "{{ .Values.memory.search.ranker }}" }`, then a `{{- with .Values.memory.search.rerankTimeout }}` row `- { name: ENGRAM_SEARCH_RERANK_TIMEOUT, value: "{{ . }}" }` closed by `{{- end }}`, and closes with `{{- end }}`. Touch no other row.

(4) Recompute the checksum with the task's own `awk | shasum` pipeline, replace `EXPECTED_CHECKSUM`, and append one sentence to the drift-pin re-pin log comment: re-pinned on today's date (`date +%Y-%m-%d`) for the ranker-gated `ENGRAM_SEARCH_RANKER` / `ENGRAM_SEARCH_RERANK_TIMEOUT` rows (D-10); the default render omits both and the rest of the block is byte-identical. Run `task chart:validate` — it must now print `chart:validate: OK`.

(5) Run `helm template charts/engram` into `<scratchpad>/render-after.yaml` and `diff` it against `render-before.yaml`; it must print nothing. Record that and the new checksum in the SUMMARY.

Commit as `feat(chart): add memory.search ranker values, lexical by default (D-10)` with pathspec `charts/engram/values.yaml charts/engram/templates/_helpers.tpl Taskfile.yaml`.
  </action>
  <verify>
    <automated>out=$(task chart:validate 2>&amp;1); rc=$?; printf '%s\n' "$out" | tail -5; test "$rc" -eq 0 &amp;&amp; printf '%s\n' "$out" | rg -q 'chart:validate: OK'</automated>
    <fails_when>non-zero exit, or no `chart:validate: OK` line (a search assertion, an existing assertion, or the checksum pin failed)</fails_when>
    <automated>b=$(mktemp -d) &amp;&amp; base=$(git merge-base HEAD main) &amp;&amp; git archive -o "$b/base.tar" "$base" charts/engram &amp;&amp; tar -x -C "$b" -f "$b/base.tar" &amp;&amp; helm template "$b/charts/engram" > "$b/base.yaml" &amp;&amp; helm template charts/engram > "$b/head.yaml" &amp;&amp; helm template charts/engram --set memory.search.ranker=jev > "$b/on.yaml" &amp;&amp; diff "$b/base.yaml" "$b/head.yaml" &amp;&amp; ! diff -q "$b/head.yaml" "$b/on.yaml" > /dev/null</automated>
    <fails_when>the default render differs from the merge-base chart's default render (D-10's byte-identical default broken), or setting the ranker to jev changes nothing (the comparison is blind)</fails_when>
    <automated>r=$(helm template charts/engram --set memory.search.ranker=jev --set memory.search.rerankTimeout=1500ms) || exit 1; test "$(printf '%s\n' "$r" | rg -o -e 'name: ENGRAM_SEARCH_(RANKER, value: "jev"|RERANK_TIMEOUT, value: "1500ms")' | sort -u | wc -l)" -eq 2</automated>
    <fails_when>the enabled render lacks either search variable or carries the wrong value</fails_when>
    <automated>yamlfmt -lint charts/engram/values.yaml Taskfile.yaml</automated>
    <fails_when>non-zero exit (a yamlfmt formatting finding)</fails_when>
  </verify>
  <acceptance_criteria>
    - The template names exactly the two search variables: `rg -o -e 'ENGRAM_SEARCH_[A-Z_]+' charts/engram/templates/_helpers.tpl | sort -u | wc -l` prints `2`.
    - Both values are read by the template: `rg -o -e 'memory[.]search[.](ranker|rerankTimeout)' charts/engram/templates/_helpers.tpl | sort -u | wc -l` prints `2`.
    - The ranker does not depend on the provider gate: `helm template charts/engram --set memory.search.ranker=jev | rg -o -F 'name: ENGRAM_SEARCH_RANKER' | wc -l` prints `1` while `helm template charts/engram --set memory.search.ranker=jev | rg -o 'ENGRAM_DECISIONS_' | wc -l` prints `0`.
    - The SUMMARY quotes the RED `chart:validate` failure from step (1), the empty before/after diff from step (5), and the new checksum.
  </acceptance_criteria>
  <done>The chart exposes the search ranker, lexical by default, with both variables under one ranker gate independent of the provider; `chart:validate` asserts the gate in both directions (observed RED first) and the re-pinned checksum; the default render is byte-identical; committed.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy guide lists the memory.search Helm values and links to the per-search disclosure (D-10)</name>
  <files>docs-site/src/content/docs/guides/deploy.md</files>
  <read_first>
    - `docs-site/src/content/docs/guides/deploy.md` lines 18-55 (`### Key values` table and the paragraphs after it)
    - `docs-site/src/content/docs/guides/configure.md` the `## Search reranking (Jev)` section (plan 04-03: opt-in, `What leaves your deployment.`, `Failure behavior.`)
  </read_first>
  <action>
Implements D-10's deploy-docs half. Before editing, run the first `<verify>` row check and note that it prints `0` (the gate can go red).

In `### Key values`, directly after the `memory.decisions.concurrency` row, add two rows in the table's three-column format:
- `memory.search.ranker` → `ENGRAM_SEARCH_RANKER`: search ranker, `lexical` (default; renders no variable) or `jev` (opt-in reranking of `search_memory` and `search_discovery` by the decision provider, adding a per-hit `relevance`; requires `memory.decisions.provider`).
- `memory.search.rerankTimeout` → `ENGRAM_SEARCH_RERANK_TIMEOUT`: per-search rerank call timeout, one attempt and no retry (empty → binary default `2s`).

After the `ENGRAM_DECISIONS_API_KEY` paragraph and before the `For the full environment variable reference` line, add one paragraph: both `memory.search.*` variables render only when `memory.search.ranker` is set to a value other than `lexical`, so a default install is unchanged; `jev` needs `memory.decisions.provider` with its base URL and key, and without it the server refuses to start and names both variables; while `jev` is on, every search sends the query and candidate record text to the decisions provider, and a failed or slow call falls back to lexical order without failing the search; read [Search reranking (Jev)](/guides/configure/#search-reranking-jev) before enabling it. Wrap prose like the neighbouring paragraphs.

Run `rumdl check docs-site/src/content/docs/guides/deploy.md` and fix any finding.

Commit as `docs(deploy): list the memory.search Helm values (D-10)` with pathspec `docs-site/src/content/docs/guides/deploy.md`.
  </action>
  <verify>
    <automated>f=docs-site/src/content/docs/guides/deploy.md; test "$(rg -o -e '^[|] .memory[.]search[.]ranker. [|] .ENGRAM_SEARCH_RANKER. [|]|^[|] .memory[.]search[.]rerankTimeout. [|] .ENGRAM_SEARCH_RERANK_TIMEOUT. [|]' $f | sort -u | wc -l)" -eq 2 &amp;&amp; test "$(rg -o -F '/guides/configure/#search-reranking-jev' $f | wc -l)" -ge 1</automated>
    <fails_when>either Helm row is missing or pairs the wrong variable, or the paragraph does not link the configure section</fails_when>
    <automated>test "$(rg -o -e '^## Search reranking [(]Jev[)]$' docs-site/src/content/docs/guides/configure.md | wc -l)" -eq 1</automated>
    <fails_when>the link target heading from plan 04-03 is absent or duplicated (the anchor would dangle)</fails_when>
    <automated>rumdl check docs-site/src/content/docs/guides/deploy.md</automated>
    <fails_when>non-zero exit (a markdown lint finding)</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -o -e 'memory[.]search[.][a-zA-Z]+' docs-site/src/content/docs/guides/deploy.md | sort -u | wc -l` prints `2`.
    - The existing decisions rows and paragraph are intact: `rg -o -e 'memory[.]decisions[.][a-zA-Z]+' docs-site/src/content/docs/guides/deploy.md | sort -u | wc -l` prints at least `6`.
    - The SUMMARY records the pre-edit `0` from the row check.
  </acceptance_criteria>
  <done>The deploy guide lists both Helm values with their variables, states the gate, the provider requirement and the fallback, and links to the configure section's disclosure; lint clean; committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Helm values → pod env | operator values decide whether every search egresses candidate text |
| docs → operator decision | what an operator believes enabling the ranker does |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-15 | Information disclosure | default chart render / `engram.containerEnv` | high | mitigate | every search row sits under the ranker gate with default `lexical`; `chart:validate` (a)/(b) and Task 1's merge-base default-render comparison prove a default install renders no search variable (D-10) |
| T-04-16 | Tampering | ranker enabled without a provider | low | mitigate | the ranker row is not nested under the provider gate, so `jev` without a provider reaches `Config.Validate` and fails startup naming both variables (D-01); `chart:validate` (c) pins that it renders |
| T-04-17 | Information disclosure | operator enables per-search egress from Helm without reading the disclosure | medium | mitigate | the `values.yaml` comment states that every search sends the query and candidate text; the deploy guide paragraph links to the configure section's `What leaves your deployment` paragraph (docs-gated by `TestSearchVarsDocumented`, plan 04-03) |
</threat_model>

<verification>
- `task chart:validate` prints `chart:validate: OK`; the default render equals the merge-base chart's default render and differs once the ranker is `jev`.
- `yamlfmt -lint charts/engram/values.yaml Taskfile.yaml` and `rumdl check docs-site/src/content/docs/guides/deploy.md` are clean.
</verification>

<success_criteria>
- D-10: `memory.search.ranker` (default `lexical`) and `memory.search.rerankTimeout` render as `ENGRAM_SEARCH_RANKER` / `ENGRAM_SEARCH_RERANK_TIMEOUT` under a gate like `memory.decisions`; the default render is byte-identical; the `chart:validate` checksum and the deploy docs are updated.
- An operator on the chart can enable the Jev ranker (RANK-03 opt-in) without editing templates.
</success_criteria>

<output>
Create `.planning/phases/04-jev-reranker-per-hit-relevance-signal/04-08-SUMMARY.md` when done. Record the RED `chart:validate` line, the empty before/after render diff, the new checksum, and the deploy-guide row check's pre-edit `0`.
</output>
