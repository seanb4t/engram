---
phase: 01-version-homebrew-distribution
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - cmd/engram/buildversion.go
  - cmd/engram/buildversion_test.go
  - cmd/engram/version.go
  - cmd/engram/serve.go
  - release-please-config.json
autonomous: true
requirements:
  - REQ-version-json

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A binary built by `go build` inside the git working tree reports a version of the form `X.Y.Z-dev.0+g<hash>`, with `.dirty` appended when the tree is modified"
    - "`versionFromModuleVersion` maps a bare release-tag module version `vX.Y.Z` to `X.Y.Z`, and rejects Go pseudo-versions, `(devel)`, unprefixed input, and the empty string — this is the parser half of the `go install …/cmd/engram@vX.Y.Z` path and is all this phase can establish. The end-to-end `go install` observation is NOT verifiable in-phase: every tag that exists today predates `cmd/engram/buildversion.go`, so no `go install` of a real tag can reach the new resolver. That observation is carried by 01-03 Task 4's post-merge checklist item C."
    - "A GoReleaser-built release binary reports exactly the ldflags-injected version, unchanged by this plan"
    - "The reported dev version is a SemVer prerelease of `nextPatch(lastRelease)`, and `nextPatch(lastRelease)` is greater than `lastRelease` — i.e. it orders strictly above the last release and strictly below the patch-bumped lower bound. It is NOT a claim about release-please's eventual next version number, which under `bump-minor-pre-major: true` is usually a minor bump."
    - "The `lastRelease` constant in the Go tree equals the version recorded in `.release-please-manifest.json`"
    - "No version string this binary reports carries a leading `v`"
    - "When no VCS information and no usable module version are available, the reported version falls back to the existing `dev` sentinel rather than to a malformed string"
    - "`engram serve` reports the same resolved version on all three of its version surfaces — OpenTelemetry `service.version` (`serve.go:83`), the MCP handshake's `Implementation.Version` (`serve.go:231`), and the startup log line (`serve.go:296`) — and that string equals what `engram version` prints, on a local build as well as a release build. No single `engram serve` process reports two different version strings."
    - "`cmd/engram/root.go` and `cmd/engram/catalog.go` are NOT rewired: `engram --version` and the bare-invocation catalog keep reporting the raw `version` package var, because D-02 pins cobra's built-in version template and the `help.golden` line that mirrors it. This is a recorded, bounded divergence, not an oversight."
  artifacts:
    - path: "cmd/engram/buildversion.go"
      provides: "lastRelease const (release-please managed), nextPatch, deriveDevVersion, versionFromModuleVersion, resolvedVersion"
      contains: "x-release-please-version"
    - path: "cmd/engram/buildversion_test.go"
      provides: "table tests for the three pure functions plus the lastRelease/manifest drift gate"
      min_lines: 80
    - path: "release-please-config.json"
      provides: "fourth extra-files entry registering the Go const with the generic updater"
      contains: "cmd/engram/buildversion.go"
  key_links:
    - from: "release-please-config.json"
      to: "cmd/engram/buildversion.go"
      via: "generic extra-files updater rewrites the annotated const on every release"
      pattern: "cmd/engram/buildversion[.]go"
    - from: "cmd/engram/version.go"
      to: "cmd/engram/buildversion.go"
      via: "the version subcommand renders whatever resolvedVersion returns, in both lanes"
      pattern: "resolvedVersion"
    - from: "cmd/engram/serve.go"
      to: "cmd/engram/buildversion.go"
      via: "all three of serve.go's version surfaces call resolvedVersion() — telemetry.ConfigFromEnv's serviceVersion argument (:83), mcp.Implementation.Version (:231), and the startup slog line (:296) — so one engram serve process never reports two different version strings"
      pattern: "resolvedVersion"
  prohibitions:
    - statement: "MUST NOT silently drop, rename, or improvise a meaning for the `.N` component of the locked dev-version format — any interpretation must be the recorded decision below, stated in the source, never an implementer's guess left undocumented"
      category: values
      requirement_id: REQ-version-json
---

<objective>
Replace the bare `dev` sentinel with a real, SemVer-ordered version for every build that GoReleaser
did not stamp: a `go build` inside the working tree, and `go install …/cmd/engram@vX.Y.Z`.

Implements D-04 (derive from `runtime/debug.ReadBuildInfo()`, no Taskfile/Makefile/CI change),
D-05 (the patch-bump base is a release-please-managed Go const, registered as a fourth `extra-files`
entry with the `generic` updater), D-07 (both lanes render the same resolved value, so the invariant
plan 01-01 pinned keeps holding), and D-08 (no `v` prefix on any reported version).

**Recorded decision — the `.N` in `0.14.1-dev.N+g<hash>` is the fixed literal `0`.** RESEARCH.md's
Open Question 1 established that `debug.BuildInfo.Settings` has no commit-distance field at all
(only `vcs`, `vcs.revision`, `vcs.time`, `vcs.modified`), so a real "commits since last tag" count
would need either a `git rev-list --count` shell-out at build time via ldflags — reopening exactly
the no-Taskfile-change premise D-04 exists to preserve — or `git` present at runtime on the end
user's machine. D-04's only ordering requirement is `lastRelease < dev-string < nextRelease`, and a
fixed `0` satisfies it completely: `0.14.0 < 0.14.1-dev.0+g800a98f1 < 0.15.0` under SemVer
precedence, because build metadata after `+` is ignored for ordering and `-dev.0` is a prerelease of
`0.14.1`. Per-build uniqueness is already carried by `vcs.revision` and the `.dirty` marker, which
are the parts that actually identify a build. This decision is stated in `buildversion.go`'s own
source comment so a later reader finds the reasoning rather than an unexplained zero.

**Do not "fix" the patch arithmetic.** With `bump-minor-pre-major: true`, the real next release after
`0.14.0` is usually `0.15.0`, not `0.14.1`. The patch bump is a correctly-ordering lower bound, never
a prediction of the next version number. Everything this plan's ordering test proves is scoped to
that lower bound: the derived string is a prerelease of `nextPatch(lastRelease)`, which is itself
greater than `lastRelease`. Nothing here proves an ordering against release-please's eventual next
version, and no truth, comment, or test name may claim otherwise.

**Recorded decision — every `engram serve` version surface is wired, and the seams are in
`cmd/engram/serve.go`, not `internal/telemetry/config.go`.**

Cycle-2 review (M-B) corrected this plan's own inventory: an earlier draft claimed `main.version` had
"exactly three consumers". It has **five**, verified by reading the tree
(`rg -n 'version' cmd/engram/serve.go` returns `:83`, `:231`, `:296`):

| # | Consumer | Site | Disposition |
|---|---|---|---|
| 1 | `version` subcommand output (and the cask's install-time gate that reads it) | `cmd/engram/version.go:16`, as 01-01 left it | **Rewired** to `resolvedVersion()` (Task 2). On a release build the value is byte-identical: `resolvedVersion` returns the ldflags-stamped `version` at step 1, before it reads any build info. |
| 2 | OpenTelemetry `service.version` | `telemetry.ConfigFromEnv("engram", version)` at `cmd/engram/serve.go:83` | **Rewired** (Task 2). `ConfigFromEnv` (`internal/telemetry/config.go:34`) merely *accepts* the string and stores it at `:41` — a parameter sink, never a reader of `main.version`. The change is the **argument at the call site**. |
| 3 | MCP `Implementation.Version` reported to every connected MCP client | `mcp.NewServer(&mcp.Implementation{Name: "engram", Version: version}, nil)` at `cmd/engram/serve.go:231` | **Rewired** (Task 2). Previously unnamed and undecided. |
| 4 | The startup log line operators read first | `slog.Info("engram listening", "version", version, …)` at `cmd/engram/serve.go:296` | **Rewired** (Task 2). Previously unnamed and undecided. |
| 5 | `rootCmd.Version` — cobra's built-in `-v, --version` and `buildCatalog`'s `Version` field | `cmd/engram/root.go:28` (struct literal) and `:71` (`rootCmd.Version = version` in `init`), consumed at `cmd/engram/catalog.go:87` | **Deliberately left on the raw package var.** D-02 forecloses it — see Task 2's recorded consequence. |

Consumers 3 and 4 are wired rather than accepted because they live in **the same file this plan already
edits** and describe **the same running process** as consumer 2. Leaving them would mean one
`engram serve` reporting three different version strings — `service.version` resolved, the MCP
handshake `dev`, the startup log `dev` — which is a worse outcome than either wiring all three or
wiring none, and is not a divergence anyone reading a trace could reconcile. The `catalog.go`
accept-and-record treatment is reserved for consumer 5, where a locked decision (D-02) genuinely
blocks the change; nothing blocks 3 and 4.

Output: a `cmd/engram/buildversion.go` unit with three pure, directly-testable functions plus a thin
`debug.ReadBuildInfo()` wrapper; a test file covering all of them and pinning the const against the
release-please manifest; the fourth `extra-files` entry that keeps the const current; and the two
call-site rewires (`version.go`'s `RunE`, `serve.go`'s telemetry argument).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-version-homebrew-distribution/01-CONTEXT.md
@.planning/phases/01-version-homebrew-distribution/01-RESEARCH.md
@.planning/phases/01-version-homebrew-distribution/01-PATTERNS.md
@.planning/phases/01-version-homebrew-distribution/01-01-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Symbols, files, flags, and config blocks **created** by Phase 01. Nothing in this list exists on
`main` today; drift verification must treat every entry as new, not as a reference to existing code.

| Kind | Artifact | Created by |
|------|----------|------------|
| CLI flag | `--output` on the `version` subcommand (string, default `text`) | 01-01 |
| Go type | `versionDoc` struct in `cmd/engram/version.go` (single field, json tag `version`) | 01-01 |
| Go func | `runVersion` — the `RunE` body replacing today's `Run` closure | 01-01 |
| Go file | `cmd/engram/version_test.go` | 01-01 |
| Test func | `TestVersionJSONLane`, `TestVersionTextLane`, `TestVersionExplicitTextLane`, `TestVersionTextEqualsJSON`, `TestVersionOutputFlagDefault` | 01-01 |
| Test row | `exitCodeBaseline` row `version/output-bogus` (`introduced: true`, `after: exitUsage`) | 01-01 |
| YAML block | `homebrew_casks:` top-level key in `.goreleaser.yaml` | 01-01 |
| YAML key | `homebrew_casks[0].hooks.post.install` (Ruby postflight) | 01-01 |
| YAML key | `homebrew_casks[0].hooks.post.uninstall` (Ruby uninstall postflight) | 01-01 |
| Go file | `cmd/engram/buildversion.go` | 01-02 |
| Go const | `lastRelease` (carrying the `x-release-please-version` annotation) | 01-02 |
| Go func | `nextPatch`, `deriveDevVersion`, `versionFromModuleVersion`, `resolvedVersion` | 01-02 |
| Go var | `moduleTagPattern` (the compiled `^v<major>.<minor>.<patch>$` matcher) and `patchCorePattern` (the anchored bare-SemVer-core matcher `nextPatch` validates against) | 01-02 |
| Go file | `cmd/engram/buildversion_test.go` | 01-02 |
| Test func | `TestNextPatch`, `TestDeriveDevVersion`, `TestVersionFromModuleVersion`, `TestLastReleaseMatchesManifest` | 01-02 |
| JSON entry | 4th `extra-files` entry in `release-please-config.json` (`"type": "generic"`) | 01-02 |
| YAML key | `homebrew_casks[0].skip_upload` (templated) | 01-03 |
| Workflow step | `Resolve Homebrew upload guard` step in `.github/workflows/release.yaml` | 01-03 |
| Workflow env | `SKIP_HOMEBREW_UPLOAD` (exported via `$GITHUB_ENV`) | 01-03 |
| Workflow input | `repositories: engram,homebrew-tap` on the existing App-token mint step | 01-03 |
| Workflow job | `verify-tap-credential` (`workflow_dispatch`-only) | 01-03 |
| Tap file | `Casks/engram.rb` in `seanb4t/homebrew-tap` — **not produced by Phase 1.** GoReleaser writes it on a tagged release; observing that write is Phase 6's `REQ-homebrew-cask-published` (moved there by developer decision on 2026-08-23, commit `b87071f6`) | Phase 6 |
| Tracking issue | GitHub issue "Phase 01 post-merge: verify tap credential (blocks release-please merge)" | 01-03 |

## Review dispositions (cycle 1 — codex `gpt-5.6-sol` + orchestrator source-grounding pass)

Every cycle-1 finding assigned to this plan, and where it now lives.

| Finding | Disposition | Where it landed |
|---|---|---|
| **HIGH-2** — plan claimed an OpenTelemetry outcome it did not deliver; the real consumer is `cmd/engram/serve.go:83`, and the criteria guarded `internal/telemetry/config.go`, a parameter sink that could never have been touched | **Incorporated (deliver, not narrow)** | `<objective>` now enumerates the three real consumers of `main.version` and names the call site; Task 2 `<action>` changes the `serve.go:83` argument to `resolvedVersion()`; `<read_first>` points at `serve.go:75-95` and states `internal/telemetry/config.go` is **not** the seam; a new positive-and-negative telemetry-wiring gate replaces the old criteria; `cmd/engram/serve.go` added to `files_modified` and to `key_links`; new `must_haves.truths` entry; T-01-07 updated to note the exposure is now genuinely reachable |
| **M-1** — `strconv.Atoi` does not enforce the stated "plain non-negative integer" grammar (`+14`, `-0`, leading zeros) | **Incorporated** | Task 1 `<action>`: anchored `patchCorePattern` (`^(0\|[1-9][0-9]*)\.…$`) as the gate, `ParseUint` after it, leading zeros **rejected not normalized** (policy stated); eight new not-ok rows in `<behavior>`; a criterion asserting `strconv.Atoi` is absent from non-comment lines |
| **L-6** — `patch+1` increments an `int` with no overflow consideration | **Incorporated** | Task 1 `<action>`: `strconv.ParseUint(c, 10, 32)` bounds every component, making the limit explicit and tested (`1.2.4294967296` → not-ok) |
| **M-8** — the ordering assertion is described as `lastRelease < dev < nextRelease` but only establishes a prerelease of the patch-bumped lower bound | **Incorporated** | Task 1 `<behavior>` names the narrower invariant and explicitly forbids the broader wording in the test name or comment; the corresponding `must_haves.truths` entry was rewritten; the `<objective>` states the scope limit |
| **L-3** — `buildCatalog` reads `root.Version`, leaving disagreeing version surfaces | **Incorporated as an accepted consequence** | Task 2 `<action>` "Recorded consequence" paragraph explains why D-02 forecloses rewiring it, a criterion asserts `catalog.go`/`root.go` are untouched, and the SUMMARY must record the divergence; `<read_first>` adds `catalog.go:84-90` |
| **L-5** — acceptance snippets used raw `grep` | **Incorporated** | Both tasks' `<acceptance_criteria>` and `<verify>` converted to `rg`, with the tooling note |
| **Premise correction** — CONTEXT.md D-05's `"(devel)"` claim is empirically false; a local `go build` yields a pseudo-version | **Already corrected; re-verified** | Task 1 `<action>` already instructs a source comment recording the correction, and `<behavior>`'s `TestVersionFromModuleVersion` rejects the pseudo-version shape. `(devel)` survives only as one more *rejected input row* — no text in this plan asserts it is what `Main.Version` returns |

## Review dispositions (cycle 2 — codex `gpt-5.6-sol`, 2026-08-23T16:18 + independent orchestrator re-verification)

Cycle 2 confirmed cycle 1's HIGH fixes landed. It also confirmed the structural corruption this file
briefly carried (a truncated 146-line prefix followed by the complete document) was repaired in
`1923b3a8`; this revision was made by editing in place, and the file still contains exactly one
frontmatter block and one `phase:`/`plan:`/`wave:` key.

| Finding | Disposition | Where it landed |
|---|---|---|
| **User decision** — `REQ-homebrew-cask-published` moved to Phase 6 | **No frontmatter change needed here** | This plan's `requirements:` was already exactly `REQ-version-json` and never claimed the moved ID. The shared "Artifacts this phase produces" `Casks/engram.rb` row is re-attributed to Phase 6 for consistency across all three plans. |
| **M-B (NEW)** — `<objective>` claimed `main.version` has "exactly three consumers"; there are **five**, and two of them (`serve.go:231` MCP `Implementation.Version`, `serve.go:296` startup log) were unnamed and undecided *in a file this plan already edits*, so one `engram serve` would report three different version strings | **Incorporated — decided by wiring, not by accepting** | `<objective>`'s inventory is replaced with a five-row table verified against the tree (`rg -n 'version' cmd/engram/serve.go` → `:83`, `:231`, `:296`), each row carrying an explicit disposition. Task 2 `<action>` now changes all three `serve.go` sites to `resolvedVersion()` and states why 3 and 4 are wired rather than accept-and-recorded (nothing blocks them; only consumer 5 is blocked, by D-02). `<read_first>` points at all three line ranges and instructs re-deriving the criteria if that `rg` returns anything else. `<acceptance_criteria>` gains three positive + three negative + one total gate; `must_haves.truths`, `key_links`, `<verify>`, `<verification>`, `<success_criteria>`, `<done>`, the L-3 recorded-consequence paragraph, and T-01-07 all updated to the wider surface. **Proven both directions:** against the current unwired `serve.go` all three positives = 0 and all three negatives = 1 (RED); against a rewired copy all three positives = 1, negatives = 0, total = 3 (GREEN); appending a `//` comment naming the old form left the negative at 0 (comment-proof). |
| **M-D (NEW)** — the truth "`go install …@vX.Y.Z` reports `X.Y.Z`" has no verification and cannot have one in-phase: every existing tag predates `buildversion.go`, and `TestVersionFromModuleVersion` proves only parsing | **Incorporated (narrowed, with the E2E claim rehomed — not dropped)** | The `must_haves.truths` entry is rewritten to the parser-level claim and states outright why the end-to-end run is unreachable in-phase. `<success_criteria>` narrowed the same way. `<verification>` gains an explicit "not verifiable in-phase, by design" row forbidding a verifier from failing it as missing. The end-to-end observation is rehomed to 01-03 Task 4's post-merge checklist item C, so it is carried by a tracked issue rather than silently deleted. |
| **L-B (NEW)** — criteria asserting `rg -c … is 0` are unsatisfiable: `rg -c` prints nothing and exits 1 on zero matches, contradicting these tasks' own `rg -o … \| wc -l` convention | **Incorporated** | Every zero-assertion in both tasks converted to `test "$(rg -o … \| wc -l \| tr -d ' ')" = 0`; the two `-list \| rg -c '^Test'` count checks converted to the same form; a standing note added to both `<acceptance_criteria>` preambles explaining the rule. Verified: `rg -c` on a zero-match file printed an empty string and exited 1, so `= 0` could never hold; the counting form returned `0`/exit 0 on absence and a nonzero count/exit 1 on presence. |
| **L-C (carried from cycle 1, previously unresolved)** — two places called `always-update: true` "package-level"; it is at the **root** of `release-please-config.json:3` | **Incorporated (both mentions)** | Task 1 `<read_first>` and Task 2 `<action>` both corrected. Verified by reading the file: `:3` `"always-update": true` sits at the top level beside `$schema` and `packages`; the only package-level setting nearby is `"bump-minor-pre-major": true` at `:7` inside `packages["."]`. Task 2 additionally warns against adding a second copy under `packages["."]`. |
| **M-A / M-C / M-E / L-A / L-D** | **Not this plan** | M-A, L-A, and L-D land in 01-01; M-C and M-E land in 01-03. |

## Review dispositions (cycle 3 — codex `gpt-5.6-sol` 2026-08-23T17:14 + orchestrator verification pass)

Cycle 3 returned **0 HIGH** and confirmed all five cycle-1 and all nine cycle-2 findings fully
resolved. One MEDIUM lands on this plan. The file was re-checked and remains **exactly one document**:
one `---` at line 1, one `phase:`/`plan:`/`wave:` each, no truncated table row — the `1923b3a8` repair
still holds and this revision was made by editing in place.

| Finding | Disposition | Where it landed |
|---|---|---|
| **M-F (NEW, MEDIUM)** — the `resolvedVersion` count gates introduced by the cycle-2 fix are not comment-guarded, while the same task's `<action>` **mandates** writing a comment naming that identifier. Reviewer fixtures: three sites rewired → `3`, exit 0; the same plus the mandated comment → `4`, **exit 1**; the `catalog.go` zero-gate with its mandated `// Deliberately NOT resolvedVersion() — D-02` comment → `1`, **exit 1**. A **false-RED** on correct content. | **Incorporated — comment-stripped, not "keep it out of comments"** | An instruction to keep the literal out of comments was rejected outright: it contradicts the `<action>`'s own mandate. Every affected count is now `rg -o '^[^/]*' <file> \| rg -o -F '<literal>'`, chosen over the reviewer's suggested `^[^/]*<literal>` anchor because the anchored form counts **lines** (two calls on one line would read as one and slip past a `= 1` gate) while the pipeline counts **occurrences** and is equally comment-proof. Applied at Task 1's `ParseUint`, `lastRelease = "[0-9]`, `lastRelease = "v`, and `debug.ReadBuildInfo` criteria; at Task 2's `<verify><automated>`, its `version.go` count, its three `serve.go` positives, its total, and its `catalog.go`/`root.go` zero-gate; and at the `<verification>` section's `serve.go` total. A standing note in each `<acceptance_criteria>` preamble states the idiom and both ways to get it wrong. |
| **M-G / L-E** | **Not this plan** | Both land in 01-03. |
| **INFO — 01-01:390 uses `rg -c` against its own convention** | **Not this plan, and explicitly not actionable** | 01-01 is untouched this cycle. The reviewer recorded it as cosmetic: the criterion is a `-ge`-style positive, so `rg -c`'s zero-match semantics never bite it. |

### The `-F`-plus-anchor trap, and why it is not in this revision

Naively prefixing `^[^/]*` to the existing `rg -o -F` gates would have made `-F` treat the anchor as a
literal: every pattern would become the string `^[^/]*resolvedVersion()`, matching nothing in any file,
forever — a permanent-fail gate, and the sixth instance of exactly the defect family this pass exists to
close. Two mutually exclusive escapes exist, and **both** were tested before choosing: drop `-F` and
escape the metacharacters (`resolvedVersion\(\)`), or keep `-F` and strip comments in a separate stage.
The second was chosen everywhere the count must stay occurrence-accurate. Both idioms, and the
line-vs-occurrence consequence, are written into the `<acceptance_criteria>` preambles so the next editor
does not re-derive them.

### Full acceptance-criteria sweep (cycle 3) — every criterion in this plan, against the four questions

The four questions: **(1)** does the literal also appear in the task's own mandated prose or comment
(self-trip)? **(2)** does the guard exclude a character the target legitimately contains (the M-A trap)?
**(3)** is this a fixed-string search wearing a regex anchor, or vice versa? **(4)** can it run
standalone?

| Criterion | Q1 self-trip | Q2 M-A trap | Q3 `-F`/anchor | Q4 standalone | Outcome |
|---|---|---|---|---|---|
| T1 `go test -list … \| rg -o '^Test'` = 3 | no — counts `go test` output, which has no comments | n/a | regex, no `-F` | yes | **unchanged** |
| T1 `--- PASS` per-function, exact dev-version strings, pseudo-version rejection, grammar rows | prose/test-table assertions, no pattern | n/a | n/a | yes (via the test binary) | **unchanged** |
| T1 `strconv\.Atoi` = 0 | yes — `<action>` mandates a comment explaining the rejection | `/` never precedes it in Go code here | correct: regex, `-F` dropped, `.` escaped | yes | **unchanged** (already guarded in cycle 1) |
| T1 `ParseUint` ≥ 1 | yes — the same mandated comment names it | no | was `-F`, no anchor | yes | **FIXED** — comment-stripped pipeline |
| T1 `lastRelease = "[0-9]` = 1 | yes — `<action>` mandates a doc comment above the const | no | regex, no `-F` | yes | **FIXED** — `^[^/]*` anchor added |
| T1 `lastRelease = "[0-9].*x-release-please-version` = 1 | n/a | **yes — the match must cross the `//` into the annotation** | regex, no `-F` | yes | **deliberately unchanged**, and marked never-to-guard |
| T1 `lastRelease = "v` = 0 | plausible — a comment could quote the rejected spelling | no | regex, no `-F` | yes | **FIXED** — `^[^/]*` anchor added |
| T1 `debug.ReadBuildInfo` = 1 | yes — `<action>` mandates a numbered resolution-order comment | no | was `-F`, no anchor | yes | **FIXED** — comment-stripped pipeline |
| T1 `git diff --name-only`, `go vet`, `gofmt -l`, `task license:check` | n/a | n/a | n/a | yes | **unchanged** |
| T1 `<verify>` `rg -q -F '--- PASS: …'` over a log | no — log output, no comments | n/a | `-F`, no anchor — correct pairing | yes | **unchanged** |
| T2 `go test -list … \| rg -o '^Test'` = 1 | no | n/a | regex, no `-F` | yes | **unchanged** |
| T2 three `jq -e` checks over `release-please-config.json` | JSON has no comments | n/a | n/a | yes | **unchanged** |
| T2 `resolvedVersion` in `version.go` = 1 | yes | no | was `-F`, no anchor | yes | **FIXED** — pipeline (occurrence-accurate, so two-on-one-line still fails) |
| T2 three `serve.go` positives, each = 1 | yes — `<action>` mandates the `:83` comment | no `/` on any of `:83`/`:231`/`:296` | was `-F`, no anchor | yes | **FIXED** — comment-stripped pipeline |
| T2 three `serve.go` negatives, each = 0 | yes | no | correct: regex, `-F` dropped, metacharacters escaped | yes | **unchanged** (proven in cycle 2) |
| T2 `serve.go` total = 3 | yes | no | was `-F`, no anchor | yes | **FIXED** — comment-stripped pipeline |
| T2 `catalog.go`/`root.go` = 0 | yes — the L-3 disposition mandates the comment | no | was `-F`, no anchor | yes | **FIXED** — comment-stripped pipeline |
| T2 `git diff --name-only` (telemetry sink, `help.golden`), `go test ./internal/telemetry`, `task` | n/a | n/a | n/a | yes | **unchanged** |
| `<verification>` `serve.go` total = 3 | yes | no | was `-F`, no anchor | yes | **FIXED** — comment-stripped pipeline |

No criterion in this plan depends on a variable assigned in another bullet (Q4 is clean throughout);
the only shell state any criterion needs is stated in its own line.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The pure version-derivation unit — const, three functions, and their tests</name>
  <reversibility rating="costly">Undoing D-04 touches the version-derivation path, the release-please `extra-files` entry from D-05, and every test pinning the dev-build format; the string is also already flowing into OpenTelemetry as `service.version`. Recorded, not gated — 01-CONTEXT.md records the developer choosing this option.</reversibility>
  <files>cmd/engram/buildversion.go, cmd/engram/buildversion_test.go</files>
  <read_first>
    - `cmd/engram/root.go` lines 16-19 — the `version` package var, its ldflags contract, and the `dev` default this plan is displacing.
    - `cmd/engram/client_common.go` lines 190-213 — `outputFormatFromConfig`, the repo's established "take the environment fact as a parameter so a table test can force both branches" seam. `deriveDevVersion` must mirror this shape.
    - `.release-please-manifest.json` — currently `{".": "0.14.0"}`; this is the value the const must match.
    - `release-please-config.json` — the three existing `extra-files` entries, plus **`always-update: true` at the file's ROOT (`:3`, a sibling of `$schema` and `packages`, not a package key)** and `bump-minor-pre-major: true` at the package level (`:7`, inside `packages["."]`). The two live at different levels; do not restate them as one.
    - `.planning/phases/01-version-homebrew-distribution/01-RESEARCH.md` — the "Verified finding" callout above Standard Stack (what `ReadBuildInfo` actually returns on this toolchain), the "Dev-build version derivation" code skeleton, and Open Question 1.
    - `cmd/engram/exitcode_baseline_test.go` lines 1-10 — the SPDX header shape every new Go file in this repo carries.
  </read_first>
  <behavior>
    Write `cmd/engram/buildversion_test.go` BEFORE the implementation and observe it fail. Three table
    tests over pure functions — no test may call `debug.ReadBuildInfo()`, because a `go test` binary
    carries its own embedded build info rather than an injectable fixture.

    `TestNextPatch` — `nextPatch` over: `0.14.0` yields `0.14.1` and ok; `1.2.9` yields `1.2.10` and ok;
    `0.14.0-rc.1` yields not-ok (a prerelease base is not a released version); `v0.14.0` yields not-ok
    (D-08: the const is bare SemVer, a `v` prefix is a defect not an input to tolerate); `0.14` yields
    not-ok; the empty string yields not-ok.

    Plus the grammar cases that `strconv.Atoi` alone would have let through, which is why the
    implementation below uses an anchored pattern instead: `+0.14.0`, `0.+14.0`, and `0.14.+0` all yield
    not-ok (a leading `+` is an `Atoi`-legal sign, not a SemVer numeric identifier); `-0.14.0` and
    `0.14.-0` yield not-ok; `01.14.0`, `0.014.0`, and `0.14.00` yield not-ok — leading zeros are
    **rejected**, not normalized, because SemVer forbids them in numeric identifiers and because
    `Atoi`-then-reformat would silently rewrite the release-please-managed const's own spelling.
    `1.2.4294967296` yields not-ok — a component wider than the bounded parse below.

    `TestDeriveDevVersion` — `deriveDevVersion(lastRelease, revision, dirty)` over:
    - `("0.14.0", "800a98f1a2b3c4d5e6f7", false)` yields exactly `0.14.1-dev.0+g800a98f1` — the hash is
      truncated to its first 8 characters.
    - the same inputs with `dirty=true` yields exactly `0.14.1-dev.0+g800a98f1.dirty`.
    - a revision shorter than 8 characters is used whole, not padded and not panicking.
    - `("0.14.0", "", false)` yields the empty string — no VCS revision means no derivable dev version.
    - `("garbage", "800a98f1a2b3c4d5e6f7", false)` yields the empty string — an unparseable base is not
      papered over with a partial string.
    - An ordering assertion in the same test: `golang.org/x/mod/semver`-style precedence is not
      available without a new dependency, so assert the ordering facts structurally instead — the
      produced string starts with the `nextPatch` result, and the character immediately after it is `-`
      (making it a prerelease of that patch, hence ordered below it). Name the invariant in the test's
      comment **exactly as narrow as it is**: *the derived string is a prerelease of the patch-bumped
      lower bound `nextPatch(lastRelease)`, which `TestNextPatch` establishes is greater than
      `lastRelease`.* Do **not** write, in the comment or the test name, that this establishes
      `lastRelease < dev-string < nextRelease` — it does not. `nextRelease` is whatever release-please
      computes from Conventional Commits and is usually a minor bump under `bump-minor-pre-major: true`;
      this test never observes it and must not imply that it does.

    `TestVersionFromModuleVersion` — `versionFromModuleVersion` over: `v0.14.0` yields `0.14.0` and ok
    (the `go install pkg@version` path D-04 names as a live bug today); `(devel)` yields not-ok;
    `v0.14.1-0.20260823183658-cc16ea664fb6` yields not-ok (a Go pseudo-version is not a release tag);
    `v0.14.1-0.20260823183658-cc16ea664fb6+dirty` yields not-ok; the empty string yields not-ok;
    `0.14.0` without the `v` yields not-ok (module versions are always `v`-prefixed).
  </behavior>
  <action>
Create `cmd/engram/buildversion.go` with the Apache-2.0 SPDX header and `package main`.

Declare the release-please-managed base: `const lastRelease = "0.14.0" // x-release-please-version`.
The trailing annotation comment is what release-please's `generic` updater matches on — it is
load-bearing, not decoration, and must stay on the same line as the value. Bare SemVer, no `v` prefix
(D-08). Document in a comment directly above it that this const is rewritten automatically on every
release by the `extra-files` entry Task 2 adds, and that it is the patch-bump BASE, not a prediction
of the next version — with `bump-minor-pre-major: true` the next release after `0.14.0` is usually
`0.15.0`, so the derived `0.14.1-dev.0` is a correctly-ordering lower bound and nothing more.

Implement `nextPatch(v string) (string, bool)` against a package-level `patchCorePattern` compiled from
`^(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)$`, using `FindStringSubmatch` to capture the three
components. **Do not gate on `strconv.Atoi` alone.** `Atoi` accepts a leading sign, so `+14` and `-0`
parse cleanly, and reformatting the parsed int silently normalizes `014` to `14` — none of which is the
"plain non-negative integer" grammar this function claims. The anchored alternation above is what
actually enforces it: `^`/`$` reject `0.14.0-rc.1`, `v0.14.0`, and `0.14`; the `(0|[1-9][0-9]*)` branch
rejects signs and leading zeros in one construct.

Then parse each captured component with `strconv.ParseUint(c, 10, 32)` and return `"", false` if any
parse fails. The 32-bit width is the overflow bound: it caps a component at 4294967295 (a version
component far beyond anything release-please could produce), so `patch+1` cannot wrap, and it makes
the bound an explicit, tested rejection rather than an unstated assumption. Format the result with the
`%d` of the three `uint64` values, patch incremented. Return `"", false` on any rejection — never a
partially-formed string.

Implement `deriveDevVersion(lastRelease, revision string, dirty bool) string`: call `nextPatch`; if it
fails, return the empty string. If `revision` is empty, return the empty string. Truncate `revision`
to its first 8 characters when it is longer. Compose `<nextPatch>-dev.0+g<short>`, and append
`.dirty` when `dirty` is true. Take `revision` and `dirty` as parameters rather than reading them
inside — this is the same seam `outputFormatFromConfig` uses for `isTTY`, and it is what lets the
table test force every branch without a real build-info fixture.

The literal `0` in `-dev.0` is the recorded decision from this plan's objective — write a comment at
that exact line explaining that `debug.BuildInfo.Settings` carries no commit-distance field, that a
real count would require a build-time `git rev-list --count` via ldflags (reopening the
no-Taskfile-change premise) or `git` on the end user's machine, and that a fixed `0` fully satisfies
D-04's only ordering requirement. Uniqueness comes from the revision and the dirty marker, which is
where it belongs.

Implement `versionFromModuleVersion(mainVersion string) (string, bool)`: match against a package-level
`moduleTagPattern` compiled from `^v[0-9]+\.[0-9]+\.[0-9]+$` and, on a match, return the value with
its leading `v` stripped (D-08) and true; otherwise `"", false`. The strict anchor is what
distinguishes a real release tag from Go's own pseudo-version shape
(`v0.14.1-0.<timestamp>-<12charhash>`, optionally `+dirty`), which RESEARCH.md confirmed empirically is
what a local `go build` actually produces — not the `(devel)` sentinel 01-CONTEXT.md D-05 assumed.
Note that correction in a comment; it does not change the locked decision, because the pseudo-version
carries a timestamp and a full-length hash rather than the git-describe-style short hash D-04 specifies.

Implement `resolvedVersion() string` as the thin wrapper — the only function in the file that touches
`runtime/debug` — resolving in this order:
1. If the `version` package var from `root.go` is anything other than `dev`, return it unchanged. This
   is the GoReleaser-stamped release path and the only path the cask's install gate ever exercises; it
   must stay byte-identical to today's behavior.
2. Call `debug.ReadBuildInfo()`; if it reports not-ok, return `version` (the `dev` sentinel).
3. Pass `bi.Main.Version` to `versionFromModuleVersion`; on ok, return that. This closes D-04's named
   bug: `go install github.com/seanb4t/engram/cmd/engram@v0.14.0` applies no ldflags and reports `dev`
   today, while the module version is correct — and that path embeds no VCS settings at all, so it must
   be checked before the VCS branch.
4. Walk `bi.Settings` once, capturing the value of the `vcs.revision` key and whether the `vcs.modified`
   key equals the string `true`. Read these from the settings slice; do not reimplement VCS detection.
5. Pass those to `deriveDevVersion` with `lastRelease`; return the result when it is non-empty.
6. Otherwise return `version` — the unchanged `dev` sentinel, which is the correct answer when the
   binary genuinely carries no identifying information (for example a `-buildvcs=false` build).

Do not add a Go module dependency: `runtime/debug`, `regexp`, `strconv`, and `strings` are all stdlib,
and this phase's whole point is that it needs no new dependency and no build-system change.
  </action>
  <verify>
    <automated>go test ./cmd/engram -run 'TestNextPatch|TestDeriveDevVersion|TestVersionFromModuleVersion' -count=1 -v 2>&1 | tee /tmp/engram-01-02-t1.log; rg -q -F -e '--- PASS: TestDeriveDevVersion' /tmp/engram-01-02-t1.log &amp;&amp; rg -q -F -e '--- PASS: TestVersionFromModuleVersion' /tmp/engram-01-02-t1.log &amp;&amp; rg -q -F -e '--- PASS: TestNextPatch' /tmp/engram-01-02-t1.log</automated>
  </verify>
  <acceptance_criteria>
    Shell snippets use `rg`, not `grep` — this repo's and this environment's search tool is ripgrep,
    and an active `rg-guard` `PreToolUse` hook rejects the `grep` habits these gates would otherwise be
    written with. Occurrence counts use `rg -o … | wc -l` rather than `rg -c`, which counts lines.

    **Zero-assertions are written as counts, never as `rg -c … is 0` (L-B).** `rg -c` prints *nothing*
    and exits 1 when there are no matches — it never emits the string `0` — so a criterion reading
    "`rg -c …` is 0" can never be satisfied and is vacuous. Every negative below is therefore
    `test "$(rg -o … | wc -l | tr -d ' ')" = 0`, which returns `0` and exits 0 on absence and returns a
    nonzero count on presence. Both directions were confirmed during planning.

    **Every count over a `.go` file is comment-stripped first (M-F, cycle 3).** The source-level gates
    below are written
    `test "$(rg -o '^[^/]*' <file> | rg -o -F '<literal>' | wc -l | tr -d ' ')" <op> <n>`. The first
    stage truncates each line at its first `/`, so a `//` comment — including a trailing one — can never
    contribute a match; the second stage still counts **occurrences**, not lines, so two calls on one
    line count as two and a "one call site" gate stays honest.

    Two ways to get this wrong, both of which produce a gate that can never pass:
    - Do **not** write `rg -o -F '^[^/]*<literal>'`. `-F` makes the anchor a *literal*, so the pattern
      becomes the string `^[^/]*<literal>`, which matches nothing in any file, forever.
    - Do **not** collapse it to `rg -o '^[^/]*<literal>'` unless line-counting is what you want. That
      form can match at most once per line, so two occurrences on one line count as one.

    Proven this cycle in both directions against fixtures: on a `buildversion.go` whose comments name
    `strconv.ParseUint` and `debug.ReadBuildInfo()` — comments this task's own `<action>` invites — the
    raw counts were **2** each, so the `-ge 1`/`= 1` gates were passing or failing for the wrong reason,
    while the stripped counts were **1** each. On a defect fixture using `strconv.Atoi` with a
    `v`-prefixed const, every gate below went RED as designed (`Atoi` 1, `ParseUint` 0,
    `debug.ReadBuildInfo` 0, `lastRelease = "v` 1).

    **One gate deliberately carries no comment guard and must keep none:** the
    `x-release-please-version` annotation check reads *across* the `//` into the trailing annotation by
    design. Guarding it would make it permanently unsatisfiable — the M-A trap, a guard excluding a
    character that legitimately appears in the target. It is called out inline below.

    - `test "$(go test ./cmd/engram -list 'TestNextPatch|TestDeriveDevVersion|TestVersionFromModuleVersion' | rg -o '^Test' | wc -l | tr -d ' ')" = 3` exits 0 — the `-run` filter above is not silently matching nothing. No comment guard: this counts lines of `go test -list` output, which carries no comments.
    - The `-v` run prints a `--- PASS` line for each of the three test functions individually.
    - `deriveDevVersion("0.14.0", "800a98f1a2b3c4d5e6f7", false)` returns exactly `0.14.1-dev.0+g800a98f1`, asserted as a byte-equal string in the test, not a prefix or regex match.
    - `deriveDevVersion("0.14.0", "800a98f1a2b3c4d5e6f7", true)` returns exactly `0.14.1-dev.0+g800a98f1.dirty`.
    - `versionFromModuleVersion("v0.14.1-0.20260823183658-cc16ea664fb6")` returns not-ok — a pseudo-version is rejected.
    - **Grammar gate (M-1/L-6, runnable through the test table).** `nextPatch` returns not-ok for every one of `+0.14.0`, `-0.14.0`, `0.+14.0`, `0.14.-0`, `01.14.0`, `0.014.0`, `0.14.00`, and `1.2.4294967296`, each an explicit row in `TestNextPatch`. Every one of those inputs passes `strconv.Atoi`-based validation, so this row set is exactly what proves the anchored pattern is in place rather than the weaker parse.
    - **Rejected-mechanism gate (runnable, comment-proof).** `test "$(rg -o '^[^/]*strconv\.Atoi' cmd/engram/buildversion.go | wc -l | tr -d ' ')" = 0` exits 0 — the rejected mechanism is absent from the code, not merely supplemented. The `^[^/]*` prefix scopes the check to non-comment lines: a `//` comment explaining *why* `Atoi` was rejected is expected and must not turn this red. Counting form, not `rg -c … is 0` (L-B).
    - `test "$(rg -o '^[^/]*' cmd/engram/buildversion.go | rg -o -F 'ParseUint' | wc -l | tr -d ' ')" -ge 1` exits 0, and the call carries the bit size `32`, bounding the increment. Comment-stripped (M-F): the raw form returned **2** on the fixture whose comment explains *why* `ParseUint` replaced `Atoi`, which means it could also be satisfied by a file that only *mentions* `ParseUint` in prose.
    - `cmd/engram/buildversion.go` carries `x-release-please-version` on the same line as the `lastRelease` value: `test "$(rg -o '^[^/]*lastRelease = "[0-9]' cmd/engram/buildversion.go | wc -l | tr -d ' ')" = 1` and `test "$(rg -o 'lastRelease = "[0-9].*x-release-please-version' cmd/engram/buildversion.go | wc -l | tr -d ' ')" = 1` both exit 0 — proving they are the same line, not merely both present.

      **The second pattern intentionally carries NO comment guard, and must never be given the comment-stripping one (M-F sweep).** The annotation it looks for lives *inside* the trailing `// x-release-please-version` comment — that is the whole point of the gate, which exists to prove the value and the annotation share a line. Run the comment-stripping pipeline over it and it returns **0 on a perfectly correct file**, permanently: verified on the fixture, `rg -o '^[^/]*' … | rg -o 'lastRelease = "[0-9].*x-release-please-version'` = **0** against raw = **1**. That is the M-A trap — a guard excluding a character the target legitimately contains. (A bare `^[^/]*` *prefix* on this same pattern happens to survive, returning **1**, because the pattern's own `.*` crosses the `//` on its own; it is still omitted, because it buys nothing here and invites the pipeline form to be substituted for it later.) The first pattern *is* guarded, because a doc comment above the const naming `lastRelease = "0.…` pushes its raw count to **2** and turns a correct file RED — verified: raw **2**, guarded **1** on that fixture, and **0** on a defect fixture with no annotated const.
    - `cmd/engram/buildversion.go` carries no `v` prefix in the const value: `test "$(rg -o '^[^/]*lastRelease = "v' cmd/engram/buildversion.go | wc -l | tr -d ' ')" = 0` exits 0. Guarded so a comment quoting the rejected `v`-prefixed spelling cannot turn a correct file RED; verified **0** on the good fixture and **1** on the `v`-prefixed defect fixture.
    - `test "$(rg -o '^[^/]*' cmd/engram/buildversion.go | rg -o -F 'debug.ReadBuildInfo' | wc -l | tr -d ' ')" = 1` exits 0 — exactly one call site, inside the wrapper, so every other function stays directly unit-testable. Comment-stripped (M-F): `<action>` mandates a numbered resolution-order comment that names `debug.ReadBuildInfo()`, and the raw form counted **2** on that fixture — an exact-count gate going RED on correct content. Stripped: **1** on the good fixture, **0** on the defect fixture that never calls it.
    - `git diff --name-only` for this task lists only `cmd/engram/buildversion.go` and `cmd/engram/buildversion_test.go`.
    - `go vet ./cmd/engram/...` exits 0, `gofmt -l cmd/engram` prints nothing, and `task license:check` exits 0.
  </acceptance_criteria>
  <done>
    `cmd/engram/buildversion.go` exists with the annotated const and four functions; all three pure
    functions pass their table tests including the exact-string dev-version cases and the
    pseudo-version rejection. Committed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire the resolver into the version command and `serve`'s telemetry, and register the const with release-please</name>
  <files>cmd/engram/version.go, cmd/engram/serve.go, cmd/engram/buildversion_test.go, release-please-config.json</files>
  <read_first>
    - `cmd/engram/version.go` — as plan 01-01 left it; find the single place the version value is read before rendering.
    - `cmd/engram/version_test.go` — plan 01-01's tests; confirm none of them hard-codes a version VALUE, so swapping the source of that value keeps them green. If any does, that assertion is the defect — fix the test to assert structure and cross-lane equality, not the literal.
    - `cmd/engram/buildversion.go` — `resolvedVersion` and `lastRelease` from Task 1.
    - `release-please-config.json` — the three existing `extra-files` entries whose shape the fourth must match.
    - `.release-please-manifest.json` — the `{".": "…"}` shape the drift test parses.
    - `cmd/engram/serve.go` — **all three** `version` reads, which `rg -n 'version' cmd/engram/serve.go` returns as `:83`, `:231`, `:296`. Read each with its surroundings: `:75-95` (`telemetry.ConfigFromEnv("engram", version)` — confirm the argument is positional and that `telemetry.Setup` immediately consumes the returned config), `:225-235` (`mcp.NewServer(&mcp.Implementation{Name: "engram", Version: version}, nil)` — the MCP handshake version), and `:290-300` (`slog.Info("engram listening", "version", version, …)` — the startup log). All three are this task's seams. Run that `rg` yourself before editing: if it returns anything other than those three lines, the file has moved and the criteria below must be re-derived rather than forced.
    - `internal/telemetry/config.go` lines 30-48 — `ConfigFromEnv`'s signature at `:34` and where it stores `serviceVersion` at `:41`. Read to confirm the opposite of what an earlier draft of this plan assumed: this function only *accepts* the string, never reads `main.version`, so it is **not** the seam and must not be edited.
    - `cmd/engram/catalog.go` lines 84-90 — `buildCatalog` reading `root.Version`. Read to understand the surface this task deliberately leaves alone, and why.
  </read_first>
  <action>
Change `cmd/engram/version.go`'s `RunE` to obtain its value from `resolvedVersion()` instead of reading
the `version` package var directly. This is a one-expression change and it must be the ONLY place the
value is obtained, so both the json lane and the text lane render the same resolved string and D-07's
text-equals-json invariant keeps holding by construction across the two separate render paths.

**Wire all three `serve` version surfaces — this is the change, not a note (M-B).** `cmd/engram/serve.go`
reads the raw `version` package var at three places, and all three describe the same running process.
Change each argument to `resolvedVersion()`, leaving every other token on the line untouched:

1. `:83` — `telemetry.ConfigFromEnv("engram", version)` becomes
   `telemetry.ConfigFromEnv("engram", resolvedVersion())`. This call site is the only path by which
   `main.version` becomes the OpenTelemetry `service.version` resource attribute.
2. `:231` — `mcp.NewServer(&mcp.Implementation{Name: "engram", Version: version}, nil)` becomes
   `… Version: resolvedVersion()}, nil)`. This is the version every connected MCP client sees in the
   handshake; leaving it would report `dev` to agents while the trace reported the resolved string.
3. `:296` — `slog.Info("engram listening", "version", version, "addr", …)` becomes
   `slog.Info("engram listening", "version", resolvedVersion(), "addr", …)`. This is the first line an
   operator reads when diagnosing which build is running, and it is the surface most likely to be
   quoted in a bug report.

Cycle-2 review found this plan's earlier inventory naming only `:83` and claiming `main.version` had
"exactly three consumers" when it has five. Wiring one of the three `serve` surfaces and silently
leaving two would make a single `engram serve` process report three different version strings, which is
strictly worse than either extreme. Add one short comment at `:83` naming `resolvedVersion` as the
single source for every version surface this file emits; the other two sites need no comment of their
own beyond that. On a release build all three values are byte-identical to today, because
`resolvedVersion` returns the ldflags-stamped `version` unchanged at step 1 before it touches build
info.

Do **not** edit `internal/telemetry/config.go`. `ConfigFromEnv` takes `serviceVersion` as a parameter
and stores it (`:41`); it never reads `main.version`, so there is nothing there to fix and its
existing tests (`internal/telemetry/config_test.go`) pass literal versions and stay untouched.

There is no behavioral test for these three lines, and that is deliberate rather than an omission:
exercising them would require booting the server plus an OTLP collector and an MCP client to observe
three strings, which is disproportionate for three argument swaps in a file with no branching involved.
The gate is the source-level criteria below — three positive and three negative, each proven to pass on
the rewired form and fail on the current one — plus Task 1's unit tests on `resolvedVersion`'s own
behavior, which is where the logic that could be wrong actually lives. This is the repo's stated
layer-appropriate posture: application logic is test-driven, wiring is verified by reading.

Leave `rootCmd.Version` and cobra's built-in `-v, --version` reading the raw `version` package var
(D-02) — changing the built-in template would break the pinned `help.golden` line 29 that D-02
explicitly protects.

**Recorded consequence — the self-describe catalog keeps reporting the raw value.**
`buildCatalog` sets its `Version` field from `root.Version` (`cmd/engram/catalog.go:87`), and
`root.Version` is assigned the raw `version` package var at `cmd/engram/root.go:28`. Because D-02
forbids touching `rootCmd.Version`, that surface cannot be brought onto `resolvedVersion()` without
also changing `-v, --version` and the pinned golden. So on a local build the surfaces split by design:
`engram version` and all three `engram serve` surfaces (`service.version`, the MCP handshake
`Implementation.Version`, the startup log) report the resolved string, while `engram --version` and the
bare-invocation catalog JSON report `dev`. This is accepted, not overlooked, and it is the *only*
remaining divergence — cycle-2 review (M-B) closed the two `serve.go` surfaces that were previously
undecided, so the split now falls exactly on the D-02 boundary and nowhere else. Record it in
`01-02-SUMMARY.md` so a later reader finds a decision rather than a bug, and do not "fix" it inside this
phase — doing so reverses D-02.

Add a fourth entry to `release-please-config.json`'s `extra-files` array, matching the shape of the
three that are already there: `"type": "generic"` and `"path": "cmd/engram/buildversion.go"`. The
`generic` updater is used rather than `yaml` or `json` because the target is a Go source line, matched
by the `x-release-please-version` annotation Task 1 placed on it. No `jsonpath` key — that field
belongs to the structured updaters only. `always-update: true` is already set at the **root** of
`release-please-config.json` (`:3`, alongside `$schema` and `packages`) — it is a top-level setting,
not a package key, and the only package-level setting nearby is `bump-minor-pre-major: true` at `:7`.
Either way it needs no change; do not add a second copy inside `packages["."]`.

Add `TestLastReleaseMatchesManifest` to `cmd/engram/buildversion_test.go`: read
`../../.release-please-manifest.json` relative to the package directory (Go tests run with the package
as the working directory), decode it into a `map[string]string`, and assert the value at key `.` is
byte-equal to `lastRelease`. Let a missing or unparseable file fail the test outright — do not wrap the
read in a fallback that substitutes a default, because a defaulted value on both sides would make the
comparison pass while measuring nothing. This is the drift gate for D-05's whole mechanism: if the
`extra-files` entry is ever dropped, renamed, or silently stops matching the annotation, the const goes
stale and every dev build reports a version derived from the wrong base. This test turns that into a
red build on the very next release rather than a quietly wrong version string. It verifies our own two
files against each other — it asserts nothing about release-please's behavior.
  </action>
  <verify>
    <automated>go test ./cmd/engram ./internal/telemetry -run 'TestLastReleaseMatchesManifest|TestVersionJSONLane|TestVersionTextLane|TestVersionExplicitTextLane|TestVersionTextEqualsJSON|TestVersionOutputFlagDefault|TestConfigFromEnv' -count=1 -v 2>&1 | tee /tmp/engram-01-02-t2.log; rg -q -F -e '--- PASS: TestLastReleaseMatchesManifest' /tmp/engram-01-02-t2.log &amp;&amp; rg -q -F -e '--- PASS: TestVersionTextEqualsJSON' /tmp/engram-01-02-t2.log &amp;&amp; test "$(rg -o '^[^/]*' cmd/engram/serve.go | rg -o -F 'resolvedVersion()' | wc -l | tr -d ' ')" = 3 &amp;&amp; test "$(rg -o '^[^/]*telemetry\.ConfigFromEnv\("engram", version\)' cmd/engram/serve.go | wc -l | tr -d ' ')" = 0 &amp;&amp; test "$(rg -o '^[^/]*Version: version\}' cmd/engram/serve.go | wc -l | tr -d ' ')" = 0 &amp;&amp; test "$(rg -o '^[^/]*"version", version,' cmd/engram/serve.go | wc -l | tr -d ' ')" = 0 &amp;&amp; jq -e '.packages["."]["extra-files"] | length == 4' release-please-config.json &amp;&amp; jq -e '.packages["."]["extra-files"] | map(select(.type=="generic" and .path=="cmd/engram/buildversion.go")) | length == 1' release-please-config.json</automated>
  </verify>
  <acceptance_criteria>
    Same tooling note as Task 1: `rg`, not `grep`; occurrence counts via `rg -o … | wc -l`; and every
    zero-assertion is written `test "$(rg -o … | wc -l | tr -d ' ')" = 0`, never `rg -c … is 0`, which
    is unsatisfiable because `rg -c` prints nothing and exits 1 on zero matches (L-B).

    **Comment-stripping applies here too, and this task is where it matters most (M-F, cycle 3).** This
    task's own `<action>` *mandates* writing a comment at `serve.go:83` naming `resolvedVersion`, and
    the L-3 recorded consequence invites a `// Deliberately NOT resolvedVersion() — D-02` comment at
    `root.go:28`. Every count below is therefore comment-stripped with the Task 1 idiom
    (`rg -o '^[^/]*' <file> | rg -o -F '<literal>'`) — occurrence-counting, comment-proof, and with
    `-F` kept on the literal stage so no metacharacter escaping is needed. The two "don't do this"
    warnings in Task 1's preamble (`-F` plus a regex anchor; anchored patterns counting lines rather
    than occurrences) apply verbatim.

    The three **negatives** below are the exception in form only: they are already regex
    (`rg -o '^[^/]*telemetry\.ConfigFromEnv\("engram", version\)'`) with `-F` correctly dropped and the
    metacharacters escaped, so they are left exactly as cycle 2 proved them.

    - `test "$(go test ./cmd/engram -list 'TestLastReleaseMatchesManifest' | rg -o '^Test' | wc -l | tr -d ' ')" = 1` exits 0 — the test exists and the `-run` filter is not matching zero tests. No comment guard: `go test -list` output carries no comments.
    - The `-v` run prints `--- PASS: TestLastReleaseMatchesManifest` and `--- PASS: TestVersionTextEqualsJSON`. The second is the load-bearing one: 01-01's text-equals-json invariant still holds after the value's source changed.
    - `jq '.packages["."]["extra-files"] | length' release-please-config.json` outputs `4`.
    - `jq -r '.packages["."]["extra-files"][3] | "\(.type) \(.path)"' release-please-config.json` outputs `generic cmd/engram/buildversion.go`.
    - `jq -e '.packages["."]["extra-files"][3] | has("jsonpath") | not' release-please-config.json` exits 0 — the generic updater carries no `jsonpath`.
    - `test "$(rg -o '^[^/]*' cmd/engram/version.go | rg -o -F 'resolvedVersion' | wc -l | tr -d ' ')" = 1` exits 0 — exactly one call site, so both render lanes read the same value. Comment-stripped **and** occurrence-counting: the pipeline form was chosen over a bare `rg -o '^[^/]*resolvedVersion'` precisely because the anchored form matches at most once per line, so a defect putting two calls on one line would have read as `1` and passed. Verified on fixtures: one call plus a comment naming `resolvedVersion()` → **1**; two calls on one line plus the same comment → **2** (RED, as intended); no call → **0** (RED).
    - **`serve` version-surface gate (HIGH-2 + M-B, runnable, comment-proof, proven both directions).**
      Three positive counts, each exactly 1:
      `test "$(rg -o '^[^/]*' cmd/engram/serve.go | rg -o -F 'telemetry.ConfigFromEnv("engram", resolvedVersion())' | wc -l | tr -d ' ')" = 1`;
      `test "$(rg -o '^[^/]*' cmd/engram/serve.go | rg -o -F 'Version: resolvedVersion()' | wc -l | tr -d ' ')" = 1`;
      `test "$(rg -o '^[^/]*' cmd/engram/serve.go | rg -o -F '"version", resolvedVersion()' | wc -l | tr -d ' ')" = 1`.
      Three matching negatives, each exactly 0:
      `test "$(rg -o '^[^/]*telemetry\.ConfigFromEnv\("engram", version\)' cmd/engram/serve.go | wc -l | tr -d ' ')" = 0`;
      `test "$(rg -o '^[^/]*Version: version\}' cmd/engram/serve.go | wc -l | tr -d ' ')" = 0`;
      `test "$(rg -o '^[^/]*"version", version,' cmd/engram/serve.go | wc -l | tr -d ' ')" = 0`.
      And one total: `test "$(rg -o '^[^/]*' cmd/engram/serve.go | rg -o -F 'resolvedVersion()' | wc -l | tr -d ' ')" = 3`
      — three surfaces wired, no fourth invented.

      **Both directions were proven during planning, not asserted.** Against the *current* (unwired)
      `serve.go`, all three positives returned `0` and all three negatives returned `1` — every gate
      correctly RED. Against a rewired copy, all three positives returned `1`, all three negatives
      returned `0`, and the total returned `3` — every gate correctly GREEN. A `//` comment naming the
      old form (`// was: telemetry.ConfigFromEnv("engram", version) before 01-02`) was then appended and
      the negative still returned `0`, confirming the `^[^/]*` guard excludes comments without excluding
      the construct. The paired positive/negative shape is what makes a partial edit — one surface wired,
      two left — impossible to pass.

      **Cycle 3 (M-F) re-proved the positives and the total, which were NOT comment-proof.** The earlier
      form counted with a bare `rg -o -F`, and this task's `<action>` mandates a comment at `:83` naming
      `resolvedVersion`. Against a rewired fixture carrying that mandated comment written the idiomatic
      Go way — with parentheses, and quoting the `:83` call in full, which is exactly what "naming
      `resolvedVersion` as the single source" invites — the raw counts were: total **5** against a `= 3`
      gate, and the `telemetry.ConfigFromEnv("engram", resolvedVersion())` positive **2** against a `= 1`
      gate. Both RED on correct content. Comment-stripped, that same fixture returned total **3** and all
      three positives **1**.

      Still RED on the real defects, verified on the same idiom: the unwired file returns total **0** and
      all three positives **0**; a partially-wired fixture (only `:83` rewired, `:231` and `:296` left)
      returns total **1**, `ConfigFromEnv` **1**, and the other two positives **0** — a partial edit
      cannot pass. `serve.go`'s three target lines carry no `/` before the literal (verified against the
      live file at `:83`, `:231`, `:296`), so the first stage's truncation cannot hide a real match.
    - `git diff --name-only` does **not** list `internal/telemetry/config.go` — the parameter sink is untouched; only the callers changed. `go test ./internal/telemetry -count=1` exits 0, unchanged.
    - **Catalog consequence recorded (L-3, comment-stripped in cycle 3).** `test "$(rg -o '^[^/]*' cmd/engram/catalog.go | rg -o -F 'resolvedVersion' | wc -l | tr -d ' ')" = 0` and `test "$(rg -o '^[^/]*' cmd/engram/root.go | rg -o -F 'resolvedVersion' | wc -l | tr -d ' ')" = 0` both exit 0 — the D-02-protected surfaces are deliberately not rewired. The guard is load-bearing here (M-F): the L-3 disposition makes a `// Deliberately NOT resolvedVersion() — D-02` comment at `root.go:28` the *expected* thing for an executor to write, and the raw form returned **1** on exactly that fixture — RED on correct content. Stripped: **0** on the commented-correct fixture, **1** (RED) on a fixture that actually rewires `rootCmd.Version` to `resolvedVersion()`. `git diff --name-only` lists neither file, and `01-02-SUMMARY.md` records the divergence (`engram --version` and the bare-invocation catalog report the raw var; `engram version` and all three `engram serve` surfaces report the resolved string) as an accepted consequence rather than an open defect.
    - `git diff cmd/engram/testdata/help.golden` is empty for this task — cobra's built-in `-v, --version` line is unchanged (D-02).
    - `task` (lint + test) exits 0.
  </acceptance_criteria>
  <done>
    `engram version` in both lanes renders `resolvedVersion()`'s output, `engram serve` passes the same
    resolved string to all three of its version surfaces (OpenTelemetry `service.version`, the MCP
    handshake `Implementation.Version`, and the startup log line), a local `go build` reports a real
    `X.Y.Z-dev.0+g<hash>` string instead of `dev`, `release-please-config.json` carries the fourth
    `extra-files` entry, a committed test fails if the const and the manifest ever disagree, and
    `01-02-SUMMARY.md` records the D-02-protected catalog/`--version` surfaces as a knowingly accepted
    divergence. Committed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build environment → reported version string | VCS state and module metadata embedded at build time become a string the binary reports to operators, to the cask gate, and to OpenTelemetry as `service.version`. |
| release-please automation → Go source | An automated updater rewrites a constant in the compiled source tree on every release. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05 | Tampering | `release-please-config.json` generic updater rewriting `lastRelease` in `cmd/engram/buildversion.go` | medium | mitigate | `TestLastReleaseMatchesManifest` compares the const against `.release-please-manifest.json` on every test run, so a dropped, renamed, or silently non-matching `extra-files` entry becomes a red build instead of a quietly stale version base. The test performs no fallback defaulting on either side. |
| T-01-06 | Spoofing | the derived dev-build version string | low | accept | The dev string is self-reported build metadata for an unreleased local build. It is never an authorization input, never consulted by the cask install gate (which only ever runs an ldflags-stamped release build, resolved at step 1 of `resolvedVersion` before any build-info is read), and cannot be reached on a release path. Accepted: no trust boundary is crossed by a wrong local version string. |
| T-01-07 | Information Disclosure | the derived version string (VCS revision + `.dirty` marker) now reaching three `serve` surfaces: OpenTelemetry `service.version`, the MCP handshake `Implementation.Version`, and the startup log line | low | accept | Now genuinely reachable, and on a wider surface than the previous revision of this row stated: Task 2 passes `resolvedVersion()` at `cmd/engram/serve.go:83`, `:231`, and `:296` (M-B), so a locally built `engram serve` emits the derived string rather than `dev` on all three. The revision is a commit hash of a **public** repository and `.dirty` reveals only that a working tree was modified — neither is a secret. The OTLP attribute never leaves the operator's own collector (engram is self-hosted, no phone-home); the startup log is local; the MCP handshake value reaches only clients already authenticated to this server. On a release build all three are byte-identical to today's ldflags value. Accepted with that rationale; no redaction control added. |
| T-01-SC | Tampering | npm/pip/cargo installs | low | accept | Not applicable — this plan adds no package-manager install task and no Go module dependency. `runtime/debug`, `regexp`, `strconv`, and `strings` are stdlib. RESEARCH.md's Package Legitimacy Audit records zero new external packages, so no `[ASSUMED]`/`[SUS]`/`[SLOP]` verdict exists to gate. |
</threat_model>

<verification>
- `task` (lint + test) exits 0.
- `go test ./cmd/engram -run 'TestNextPatch|TestDeriveDevVersion|TestVersionFromModuleVersion|TestLastReleaseMatchesManifest|TestVersion' -count=1 -v` shows a `--- PASS` line for every named test.
- `go build -o /tmp/engram-devcheck ./cmd/engram; /tmp/engram-devcheck version` prints a string matching `^[0-9]+\.[0-9]+\.[0-9]+-dev\.0\+g[0-9a-f]{1,8}(\.dirty)?$` — the live end-to-end proof that the derivation actually fires on a real `go build` inside this tree.
- `/tmp/engram-devcheck version --output json | jq -r .version` prints the identical string.
- `/tmp/engram-devcheck version --output text` prints the identical string too — the explicit text lane, pinned by 01-01's `TestVersionExplicitTextLane`, still agrees after the value's source changed.
- `git diff --name-only` touches only the five files in `files_modified`.
- `go test ./internal/telemetry -count=1` exits 0 and `internal/telemetry/config.go` is unmodified — the telemetry change is at the caller, not in the sink.
- `test "$(rg -o '^[^/]*' cmd/engram/serve.go | rg -o -F 'resolvedVersion()' | wc -l | tr -d ' ')" = 3` exits 0 — every `serve` version surface is wired, none invented. Comment-stripped (M-F): Task 2's `<action>` mandates a comment naming `resolvedVersion` in this same file, and the raw form counted **5** on a fixture carrying it.
- **Not verifiable in-phase, by design (M-D):** the end-to-end `go install github.com/seanb4t/engram/cmd/engram@vX.Y.Z` observation. Every tag that exists today predates `cmd/engram/buildversion.go`, so no `go install` of a real tag can reach the new resolver — the claim cannot be tested until a tag containing this file exists. `TestVersionFromModuleVersion` proves the parser half and nothing more. The end-to-end check is carried by 01-03 Task 4's post-merge checklist item C and must not be failed here as missing.
</verification>

<success_criteria>
- A `go build` of `./cmd/engram` inside the git tree reports `X.Y.Z-dev.0+g<hash>`, with `.dirty` when the tree is modified, instead of `dev`.
- `versionFromModuleVersion` maps `vX.Y.Z` to `X.Y.Z` (no `v` prefix) and rejects pseudo-versions, `(devel)`, unprefixed input, and the empty string — the parser half of the `go install …@vX.Y.Z` path. The end-to-end `go install` run is explicitly **out of scope for this phase** (M-D): every existing tag predates `buildversion.go`, so it cannot be exercised until a tag containing this file exists. It is carried by 01-03 Task 4's post-merge checklist item C.
- A GoReleaser release build's reported version is byte-identical to today's ldflags-injected value.
- Both `version` lanes render the same resolved string; 01-01's text-equals-json test is still green.
- `cmd/engram/serve.go` passes `resolvedVersion()` at all three of its version surfaces (`:83` OpenTelemetry `service.version`, `:231` MCP `Implementation.Version`, `:296` startup log), so no single `engram serve` process reports two different version strings, and none of them reports `dev` on a local build.
- `release-please-config.json` has four `extra-files` entries, the fourth being `generic` on `cmd/engram/buildversion.go`, and a committed test fails if the const drifts from the manifest.
- The `engram --version` / bare-catalog surfaces still report the raw package var, recorded in the SUMMARY as an accepted D-02 consequence rather than left unexplained.
</success_criteria>

<output>
Create `.planning/phases/01-version-homebrew-distribution/01-02-SUMMARY.md` when done
</output>
