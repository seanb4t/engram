---
phase: 01-version-homebrew-distribution
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - .github/workflows/release.yaml
  - .github/workflows/verify-tap-credential.yaml
  - .goreleaser.yaml
autonomous: false
requirements:
  - REQ-homebrew-cask-published
  - REQ-cask-credential-verified
  - REQ-cask-reship-recovery

user_setup:
  - service: github-app
    why: "The release-please GitHub App must be able to write Casks/engram.rb to seanb4t/homebrew-tap (D-12). The App's installation repository list is GitHub UI state outside this repo and has no workflow-reachable API equivalent."
    dashboard_config:
      - task: "Add seanb4t/homebrew-tap to the release-please App's selected repositories, keeping seanb4t/engram selected. Repository selection is the human action; Contents: Read and write is declared by the App and accepted at installation, so read and record it rather than expecting a per-repository toggle. The credential probe is the source of truth on whether the token can actually push."
        location: "GitHub -> your account Settings -> Applications -> Installed GitHub Apps -> (the release-please App) -> Configure -> Repository access"

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "SKIP_HOMEBREW_UPLOAD is exported as `false` exactly when the tag being shipped equals the newest `v*` tag under `git tag -l 'v*' --sort=-v:refname`, and as `true` for every tag older than that — the boundary case where the shipped tag IS the newest uploads, and the case one tag older does not"
    - "A `workflow_dispatch` re-ship of a non-newest tag leaves `Casks/engram.rb` in the tap unwritten, so a backfill cannot regress the tap to an older version"
    - "The release workflow **requests** a token limited to `engram` and `homebrew-tap`: `repositories:` is a closed allowlist in the workflow YAML, and that configuration — not any runtime probe — is what bounds the scope. The probe confirms positive access to the tap and observes nothing about the absence of access to any third repository."
    - "Release-please's writes to `engram` stay inside the requested scope because `engram` is named in that same allowlist. This is established by configuration review, and observed for real on the next release that runs through this workflow; the credential probe never exercises release-please and proves nothing about it."
    - "A read-only, `workflow_dispatch`-triggered check reports whether the App token can push to `seanb4t/homebrew-tap` and fails the run loudly when it cannot"
    - "The credential check performs no write to the tap: the tap's HEAD commit SHA captured before the dispatch is byte-identical to the SHA after it"
    - "The App token reaches every consumer through an environment variable, never as a command-line argument"
    - "Running the credential check requires no release, no tag input, and no artifact re-ship — but it does require the workflow file to exist on the default branch, so its first dispatch is a post-merge observation, recorded as such rather than claimed in-branch"
  artifacts:
    - path: ".github/workflows/release.yaml"
      provides: "newest-tag guard step exporting SKIP_HOMEBREW_UPLOAD before the GoReleaser step, and an explicitly scoped App-token mint"
      contains: "SKIP_HOMEBREW_UPLOAD"
    - path: ".github/workflows/verify-tap-credential.yaml"
      provides: "workflow_dispatch-only read-only push-permission probe against seanb4t/homebrew-tap"
      contains: "permissions.push"
    - path: ".goreleaser.yaml"
      provides: "templated skip_upload on the homebrew_casks entry, driven by the workflow's computation"
      contains: "skip_upload"
  key_links:
    - from: ".github/workflows/release.yaml"
      to: ".goreleaser.yaml"
      via: "the guard step writes SKIP_HOMEBREW_UPLOAD into $GITHUB_ENV, which the cask's skip_upload template reads as .Env"
      pattern: "SKIP_HOMEBREW_UPLOAD"
    - from: ".github/workflows/verify-tap-credential.yaml"
      to: "seanb4t/homebrew-tap"
      via: "gh api repos/seanb4t/homebrew-tap --jq .permissions.push, read-only"
      pattern: "repos/seanb4t/homebrew-tap"
---

<objective>
Make the publishing pipeline correct by construction: prove the cross-repo credential before any
release depends on it, and make a `workflow_dispatch` backfill structurally unable to regress the tap.

Implements D-12 (the existing release-please GitHub App is the tap-writing identity — no new secret,
no expiry to rotate), D-13 (a read-only `workflow_dispatch` credential probe asserting
`.permissions.push` is true, run once before the first cask release and re-runnable after any
re-scope), and D-14 (a per-cask `skip_upload` driven by a newest-tag comparison computed before the
GoReleaser step — there is no `--skip=homebrew` CLI value; the unified `--skip` flag accepts only
`before`, `validate`, and `publish`).

Under D-15, ROADMAP success criteria 3 and 5 are satisfied **by construction**, not by rehearsal.
No staged `brew install` failure and no backfill rehearsal will be performed: criterion 3 rests on
D-09's reviewed post-install hook plus D-11's ownership boundary, and criterion 5 on this plan's
`skip_upload` guard plus its credential probe. A verifier must not fail those two criteria on their
literal rehearsal wording.

**Recorded decision — the credential probe lives in its own workflow file, not in `release.yaml`.**
01-PATTERNS.md placed it alongside the release job, but `release.yaml`'s `workflow_dispatch` trigger
declares `tag` as `required: true`, and its `target` resolver step treats any supplied tag as a
re-ship instruction. Dispatching that workflow to run a read-only probe would therefore also re-ship
release artifacts and — with the newest tag supplied — rewrite the tap's cask. A probe that cannot be
run without performing a release is not a probe. `.github/workflows/verify-tap-credential.yaml`
carries `on: workflow_dispatch` with no inputs, so it is dispatchable at any time with no side
effects. This satisfies D-13 exactly as written (a `workflow_dispatch`-only, read-only job); only the
file placement differs from the pattern map, which is advisory rather than a locked decision.

**Recorded decision — the App-token mint names BOTH repositories.** `actions/create-github-app-token`'s
`repositories:` input is a closed allowlist, not an addition to the default: unset means "the current
repository only", and setting it replaces that default outright. Scoping it to `homebrew-tap` alone
would break the release-please step running earlier in the *same job*, which writes release PRs, tags,
and Releases to `engram` — a different and worse failure than the one this phase prevents. The input
is therefore `repositories: engram,homebrew-tap` in both workflows, and the probe uses the identical
value so it verifies the exact scope the release job will hold rather than an approximation of it.

Purpose: the v0.11.0 release failed at the Go proxy/sumdb wait and shipped zero artifacts, which is
why the `workflow_dispatch` re-ship path exists. That same path already dragged `docker pull …:latest`
backwards once. The cask is version-pinned in a way `latest` is not, so the identical regression here
would leave every `brew install` serving an older build until someone noticed.

**Recorded sequencing correction — the probe is authored here and first dispatched after merge.**
Cycle-1 review (HIGH-3) found the earlier version of this plan instructing a blocking
`gh workflow run verify-tap-credential.yaml --ref <feature-branch>` before merge. GitHub exposes
`workflow_dispatch` only for workflow definitions present on the **default** branch; a feature `--ref`
selects which ref's copy of an *already registered* workflow runs, it does not register a new one. The
gate was therefore unsatisfiable and the plan could not complete. That was a defect in **our**
sequencing, not a claim about GitHub, and it is repaired by splitting Task 3 into the part that is
executable now (the App installation grant, plus capturing the tap's baseline HEAD SHA) and Task 4,
which files the tracked post-merge checklist for the dispatch itself. D-13 is unaffected: the probe is
still a read-only, `workflow_dispatch`-only job run **once before any real release depends on it** —
merging this branch is not a release.

Output: a guard step and an explicitly scoped token mint in `release.yaml`, a templated `skip_upload`
on the cask, a standalone read-only credential-probe workflow, one blocking human step to grant the
App access to the tap and capture the tap's baseline commit, and a tracked post-merge checklist that
carries the two observations this phase cannot honestly make from a feature branch.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-version-homebrew-distribution/01-CONTEXT.md
@.planning/phases/01-version-homebrew-distribution/01-RESEARCH.md
@.planning/phases/01-version-homebrew-distribution/01-PATTERNS.md
@.planning/phases/01-version-homebrew-distribution/01-01-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Symbols, files, flags, and config blocks **created** by Phase 01. Nothing in this list exists on
`main` today; drift verification must treat every entry as new, not as a reference to existing code.

| Kind | Artifact | Created by |
|------|----------|------------|
| CLI flag | `--output` on the `version` subcommand (string, default `text`) | 01-01 |
| Go type | `versionDoc` struct in `cmd/engram/version.go` (single field, json tag `version`) | 01-01 |
| Go func | `runVersion` — the `RunE` body replacing today's `Run` closure | 01-01 |
| Go file | `cmd/engram/version_test.go` | 01-01 |
| Test func | `TestVersionJSONLane`, `TestVersionTextLane`, `TestVersionExplicitTextLane`, `TestVersionTextEqualsJSON`, `TestVersionOutputFlagDefault` | 01-01 |
| Test row | `exitCodeBaseline` row `version/output-bogus` (`introduced: true`, `after: exitUsage`) | 01-01 |
| YAML block | `homebrew_casks:` top-level key in `.goreleaser.yaml` | 01-01 |
| YAML key | `homebrew_casks[0].hooks.post.install` (Ruby postflight) | 01-01 |
| YAML key | `homebrew_casks[0].hooks.post.uninstall` (Ruby uninstall postflight) | 01-01 |
| Go file | `cmd/engram/buildversion.go` | 01-02 |
| Go const | `lastRelease` (carrying the `x-release-please-version` annotation) | 01-02 |
| Go func | `nextPatch`, `deriveDevVersion`, `versionFromModuleVersion`, `resolvedVersion` | 01-02 |
| Go var | `moduleTagPattern` (the compiled `^v<major>.<minor>.<patch>$` matcher) and `patchCorePattern` (the anchored bare-SemVer-core matcher `nextPatch` validates against) | 01-02 |
| Go file | `cmd/engram/buildversion_test.go` | 01-02 |
| Test func | `TestNextPatch`, `TestDeriveDevVersion`, `TestVersionFromModuleVersion`, `TestLastReleaseMatchesManifest` | 01-02 |
| JSON entry | 4th `extra-files` entry in `release-please-config.json` (`"type": "generic"`) | 01-02 |
| YAML key | `homebrew_casks[0].skip_upload` (templated) | 01-03 |
| Workflow step | `Resolve Homebrew upload guard` step in `.github/workflows/release.yaml` | 01-03 |
| Workflow env | `SKIP_HOMEBREW_UPLOAD` (exported via `$GITHUB_ENV`) | 01-03 |
| Workflow input | `repositories: engram,homebrew-tap` on the existing App-token mint step | 01-03 |
| Workflow file | `.github/workflows/verify-tap-credential.yaml` | 01-03 |
| Workflow job | `verify-tap-credential` (`workflow_dispatch`-only, read-only) | 01-03 |
| Tap file | `Casks/engram.rb` in `seanb4t/homebrew-tap` (written by GoReleaser at release time) | 01-03 |
| Tracking issue | GitHub issue "Phase 01 post-merge: verify tap credential and observe first cask publication" | 01-03 |

## Flagged Assumptions (edge-probe, unresolved rows)

Two of the phase's six probe rows belong to this plan. The `boundary` row on REQ-cask-reship-recovery
was resolvable and is authored as a plain-string truth in `must_haves.truths` above (the first entry).
The two below stay unresolved and are surfaced here rather than dropped.

| Requirement | Probe category | Assumption recorded |
|---|---|---|
| REQ-cask-credential-verified | unclassified | The probe could not classify this requirement. Assumed correct: the requirement has one binary outcome — the token can push to the tap or it cannot — with no threshold, ordering, or precision surface. The single genuine edge is the "App installed but without Contents: write" case, which the probe's `.permissions.push` assertion resolves to false and fails on. No further edge is authored. |
| REQ-cask-reship-recovery | precision | **Very likely a classification miss, but deliberately not dismissed.** The probe asked where precision loss, overflow, or rounding could occur. This requirement's only computation is a string equality between two git tag names, decided by `git tag -l 'v*' --sort=-v:refname`; there is no arithmetic, no float, no truncation, and no tie-breaking rule of ours to specify. The nearest real concern is that `--sort=-v:refname` is git's own version-sort ordering rather than ours — which under D-11 is third-party behavior we do not gate. Recorded as an assumption so the reasoning is on the record rather than silently absent. |

## Review dispositions (cycle 1 — codex `gpt-5.6-sol` + orchestrator source-grounding pass)

Every cycle-1 finding assigned to this plan, and where it now lives.

| Finding | Disposition | Where it landed |
|---|---|---|
| **HIGH-3** — the blocking checkpoint dispatched a workflow absent from the default branch, so the plan could never complete | **Incorporated (resequenced)** | `<objective>` "Recorded sequencing correction"; Task 3 rewritten to the part executable pre-merge (App grant + baseline SHA capture) and renamed accordingly; the dispatch moved into new Task 4's tracked post-merge checklist; Task 2's criteria now state plainly that they do not claim the probe has run |
| **HIGH-4** — REQ-homebrew-cask-published claimed by three plans, fulfilled by none | **Incorporated (claim narrowed explicitly + observation owned)** | New "Requirement scope" table below states, per requirement, what closes in-branch and what is observed post-merge; new Task 4 files a GitHub issue carrying the first-release publication observation with runnable steps; `<verification>` marks both State B items as not-in-phase and forbids failing them as missing; `<success_criteria>` reworded to what this phase actually delivers |
| **M-3** — "grants access to exactly two repositories … and to no others" overstates a one-repo probe | **Incorporated** | `must_haves.truths` reworded to "the release workflow **requests** a token limited to…", naming the YAML allowlist as the mechanism and the probe as positive confirmation only; `<success_criteria>` follows suit |
| **M-4** — the release-please truth is proven by nothing in the plan | **Incorporated** | `must_haves.truths` reframed as configuration review plus next-real-release observation, and states outright that the probe never exercises release-please |
| **M-5** — instructions conflated installation repository *selection* with App *permissions* | **Incorporated** | Task 3 `<action>` "Precision on what the human actually controls" and `<instructions>` steps 3-4 separate the selection (yours) from the permission (App-declared, read and record); `user_setup.dashboard_config` reworded the same way; the probe is named the source of truth |
| **M-6** — `task release:check` claimed to prove the guarded `skip_upload` template renders | **Incorporated** | Task 1 `<acceptance_criteria>`: the claim is replaced by a precise statement of what `goreleaser check` validates (schema, not templates), with the render observation moved to the manual `release:snapshot` row already in 01-VALIDATION.md |
| **L-1** — the "tap unchanged" check compared commit messages with no baseline | **Incorporated** | Task 3 captures the tap's HEAD SHA as a dated baseline in the SUMMARY; Task 4 step A5 compares that exact SHA and says why messages are unusable; T-01-11's mitigation records it; a `must_haves.truths` entry states the SHA form |
| **L-5** — acceptance snippets used raw `grep` | **Incorporated** | All tasks' `<acceptance_criteria>` and `<verify>` converted to `rg`, with a tooling note covering explicit-file/hidden behavior and `rg -o … \| wc -l` occurrence counting |

## Requirement scope — what this phase closes, and what it explicitly does not

Cycle-1 review (HIGH-4) found `REQ-homebrew-cask-published` claimed by three plans and fulfilled by
none: the probe proves the *ability* to write the tap, and the artifact table defers `Casks/engram.rb`
to "release time", so the phase could finish fully green with no installable cask. That is resolved
here by splitting the requirement into two named states and being explicit about which one this phase
reaches. **Nothing below is a reduction of what was decided — it is a correction of what was claimed.**

| Requirement | State A — closed by this phase, in-branch | State B — observed after merge |
|---|---|---|
| REQ-version-json | Fully closed. Go behavior, test-driven (01-01, 01-02). | — |
| REQ-cask-install-gate | Fully closed. The hook and its ordering gate land in 01-01; D-11 forbids gating Homebrew's own execution of it. | — |
| REQ-cask-reship-recovery | Fully closed by construction (D-14/D-15): the guard, both branches of the boundary, and the templated `skip_upload` are all in-tree and gated. | — |
| **REQ-cask-credential-verified** | Configuration closed: `repositories: engram,homebrew-tap` on both mints, the read-only probe workflow, and the App installation grant (Task 3). | **The probe's first dispatch.** GitHub only exposes `workflow_dispatch` for workflows present on the default branch, so a brand-new workflow file cannot be dispatched from a feature branch. The dispatch happens on `main` after merge and **before any release depends on it**, which is precisely what REQ-cask-credential-verified asks for. Tracked by Task 4. |
| **REQ-homebrew-cask-published** | Configuration closed: the `homebrew_casks:` entry (01-01), the tap-writing credential scope, and the upload guard. A cask *would* be published by the next tagged release. | **The publication itself.** Publishing requires a tag, which requires merging a release-please PR — an event this phase cannot perform from a feature branch and must not fake. Task 4 records the observation checklist: the release run wrote `Casks/engram.rb`, at which version, and at which tap commit. |

**Consequences a verifier must honor.** State B items are **not** in-phase acceptance criteria and
must not be failed as missing at phase verification; they are also **not** silently dropped — Task 4
creates a durable, dated tracking record for each, following this repo's own stated practice of filing
GitHub issues for remaining work (`CLAUDE.md`, Session Completion). `01-03-SUMMARY.md` must list both
State B items under open items with the issue number. This is the honest reading of D-11 as well: the
tagged release is our own publishing outcome and is worth *observing*, but it is not something to
rehearse or red-gate from a feature branch.

<tasks>

<task type="auto">
  <name>Task 1: Re-ship guard — compute the newest tag before GoReleaser and template it into skip_upload</name>
  <files>.github/workflows/release.yaml, .goreleaser.yaml</files>
  <read_first>
    - `.github/workflows/release.yaml` — the whole file. Specifically lines 44-75 (`target` resolver: the one place "are we shipping, and at which tag" is decided), lines 78-82 (checkout at the tag with `fetch-depth: 0`, which is what makes local tags visible), lines 131-137 (the `goreleaser-action` step the new step must precede), and lines 144-163 ("Reconcile :latest after a re-ship", whose newest-tag computation is the logic being lifted).
    - `.goreleaser.yaml` — the `homebrew_casks:` entry plan 01-01 added; this task adds one field to it.
    - `.planning/phases/01-version-homebrew-distribution/01-RESEARCH.md` Pitfall 6 and the D-14 entry — why the computation cannot live inside GoReleaser and must be an env var set beforehand.
  </read_first>
  <action>
Add one step to the `release` job in `.github/workflows/release.yaml` and one field to the cask entry
in `.goreleaser.yaml`. They are a matched pair and must land in the same commit — the template reads
an env var only this step sets.

**Workflow step.** Insert a step named `Resolve Homebrew upload guard` immediately before the
`goreleaser/goreleaser-action` step, gated on the same `if: ${{ steps.target.outputs.ship == 'true' }}`
condition every shipping step already carries. It must sit after the `actions/checkout` step, because
`git tag -l` needs the tags that `fetch-depth: 0` fetched. Pass the shipped tag in through an `env:`
block as `SHIPPED_TAG: ${{ steps.target.outputs.tag }}`, following the file's established
`env:`-block-then-`run:`-script shape rather than interpolating the expression into the shell body.

The script runs under `set -euo pipefail` and reuses the exact newest-tag computation already present
in the "Reconcile :latest after a re-ship" step: `git tag -l 'v*' --sort=-v:refname | head -1`. One
computation, one place — do not write a second, parallel implementation inside the GoReleaser template.
Echo both values so a run's log records the comparison. If no `v*` tag is found, emit a
`::error::`-annotated message and exit 1 rather than guessing a default; an empty result here means the
checkout is wrong, and silently choosing either branch would be worse than failing. Otherwise write
`SKIP_HOMEBREW_UPLOAD=false` to `$GITHUB_ENV` when the newest tag equals `SHIPPED_TAG`, and
`SKIP_HOMEBREW_UPLOAD=true` when it does not.

Note the boundary explicitly in a comment: equal to the newest tag means upload, anything older means
skip. The push path always ships the newest tag and so always uploads; only a `workflow_dispatch`
backfill of an older tag lands on the skip branch. Run the step unconditionally on both entry paths
rather than gating it on `github.event_name == 'workflow_dispatch'` the way the `:latest` reconciliation
does — the env var must always be defined by the time GoReleaser runs, and a push of the newest tag
correctly evaluates to `false` on its own.

**Cask field.** Add `skip_upload` to the single `homebrew_casks:` entry in `.goreleaser.yaml`, as a
quoted Go template using GoReleaser's documented optional-env idiom: an `if index .Env "SKIP_HOMEBREW_UPLOAD"`
guard that emits the variable's value when it is present and the literal `false` otherwise. A bare
`{{ .Env.SKIP_HOMEBREW_UPLOAD }}` would hard-fail template evaluation whenever the variable is unset,
which would break `task release:snapshot` for anyone running it locally. The guarded form keeps local
snapshot renders working while CI always supplies the real value.

Do not use `skip_upload: auto`. `auto` skips uploads for tags carrying a prerelease indicator, which is
a different rule from the newest-tag rule D-14 specifies, and layering it on top would make the
effective condition harder to reason about than either rule alone. Document that choice in a comment
above the field, along with the reason the value is computed in the workflow rather than in the
template: GoReleaser's template functions cannot run `git tag -l`.

Add a comment on the field naming the regression class it prevents — this repo already lost `latest`
to a v0.11.0 backfill after v0.11.1 had shipped, and a version-pinned cask would fail the same way with
no floating tag to notice it by.
  </action>
  <verify>
    <automated>task lint:actions &amp;&amp; task lint:yaml &amp;&amp; task release:check &amp;&amp; test "$(rg -o -F 'SKIP_HOMEBREW_UPLOAD' .github/workflows/release.yaml | wc -l | tr -d ' ')" -ge 3 &amp;&amp; test "$(rg -o -F 'SKIP_HOMEBREW_UPLOAD' .goreleaser.yaml | wc -l | tr -d ' ')" -ge 1</automated>
  </verify>
  <acceptance_criteria>
    Shell snippets use `rg`, not `grep` — this repo's and this environment's search tool is ripgrep,
    and an active `rg-guard` `PreToolUse` hook rejects the `grep` habits these gates would otherwise be
    written with. `rg` searches an explicitly named file regardless of hidden/ignore status, so the
    dot-prefixed paths below need no `--hidden`. Occurrence counts use `rg -o … | wc -l`, not `rg -c`,
    which counts matching lines.

    - `.github/workflows/release.yaml` contains a step named `Resolve Homebrew upload guard`.
    - Ordering gate (runnable): `G=$(rg -n -F 'Resolve Homebrew upload guard' .github/workflows/release.yaml | head -1 | cut -d: -f1); R=$(rg -n -F 'goreleaser/goreleaser-action' .github/workflows/release.yaml | head -1 | cut -d: -f1); C=$(rg -n -F 'actions/checkout' .github/workflows/release.yaml | head -1 | cut -d: -f1); test -n "$G" && test -n "$R" && test -n "$C" && test "$C" -lt "$G" && test "$G" -lt "$R"` exits 0. Every line number must resolve; an empty value fails rather than defaulting.
    - `rg -o -F "git tag -l 'v*' --sort=-v:refname" .github/workflows/release.yaml | wc -l` is exactly 2 — the new guard step and the pre-existing `:latest` reconciliation, and no third copy.
    - `.github/workflows/release.yaml` contains both `SKIP_HOMEBREW_UPLOAD=false` and `SKIP_HOMEBREW_UPLOAD=true`, each written to `$GITHUB_ENV`, so both branches of the boundary are actually implemented.
    - `.goreleaser.yaml` contains a `skip_upload:` key whose value contains `index .Env` — the guarded optional-env form, not a bare `.Env` dereference.
    - `.goreleaser.yaml` does not contain `skip_upload: auto`.
    - `task lint:actions` (actionlint) exits 0 and `task lint:yaml` exits 0.
    - `task release:check` exits 0 with `SKIP_HOMEBREW_UPLOAD` unset in the environment. **What this proves, precisely (M-6):** `task release:check` is `goreleaser check` (`Taskfile.yaml:221`), which validates the config's **schema** — it does not evaluate Go templates, so a green run here is not evidence that the `index .Env` guard renders without the variable. The guarded form is chosen on GoReleaser's documented optional-env idiom, and the render is observed manually via `task release:snapshot` with `SKIP_HOMEBREW_UPLOAD` unset (01-VALIDATION.md, Manual-Only). Per D-11, no golden/snapshot assertion is made over the rendered cask file — the observation is that the snapshot completes, not what it contains.
  </acceptance_criteria>
  <done>
    `release.yaml` computes the newest-vs-shipped comparison before GoReleaser runs and exports
    `SKIP_HOMEBREW_UPLOAD`; the cask's `skip_upload` reads it through a guarded template that still
    renders when the variable is absent. Committed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Cross-repo credential — explicit token scope plus a standalone read-only probe</name>
  <reversibility rating="costly">D-12 makes the release-please App the tap-writing identity, and the App's installation scope is manual GitHub UI configuration outside this repo. Changing credential strategy later is a human step in a web UI, not a code change. Recorded, not gated — 01-CONTEXT.md records the developer choosing this option over a fine-grained PAT and over a second purpose-built App.</reversibility>
  <files>.github/workflows/release.yaml, .github/workflows/verify-tap-credential.yaml</files>
  <read_first>
    - `.github/workflows/release.yaml` lines 17-23 (the permissions comment explaining which identity does what), lines 32-37 (the `actions/create-github-app-token` mint pinned at `bcd2ba49218906704ab6c1aa796996da409d3eb1`), line 41 (release-please consuming the token via a `with: token:` input), and line 137 (GoReleaser consuming it via `env: GITHUB_TOKEN:`).
    - `.github/workflows/release.yaml` lines 96-100 — the `env:`-block-then-`run:`-shell shape for credential-consuming steps; the new probe must mirror it.
    - `.planning/phases/01-version-homebrew-distribution/01-RESEARCH.md` Pitfall 4, Pitfall 5, the "GitHub Actions: workflow_dispatch-only credential-verify job" code example, and Assumptions Log rows A2 and A3.
  </read_first>
  <action>
Two changes, both about making the credential's scope explicit and checkable.

**Widen the existing mint's scope, explicitly.** Add a `repositories: engram,homebrew-tap` input to the
`actions/create-github-app-token` step already in the `release` job. Keep the action pinned at its
current commit SHA — RESEARCH confirmed that pin already IS the latest tag, so no bump is needed and
none should be made here. Both repositories must be named. The input is a closed allowlist: leaving it
unset means "the current repository only", and supplying it replaces that default rather than adding
to it, so a list containing only the tap would strip `engram` from the token's scope and break the
release-please step that runs earlier in the same job. Add a comment saying exactly that, so a later
reader tempted to "tighten" the list to just the tap sees why it cannot be.

Do not use the `owner:` input as an alternative — it scopes the token to every repository the App is
installed on under the owner, which is strictly broader than the two this phase needs.

**Add `.github/workflows/verify-tap-credential.yaml`.** A new workflow named `verify tap credential`,
triggered by `on: workflow_dispatch` with no inputs, so it is dispatchable at any time without
supplying a tag and without any release side effect. Top-level `permissions: contents: read` — the
default `GITHUB_TOKEN` is not what this workflow tests and needs nothing.

One job, `verify-tap-credential`, on `ubuntu-latest`, with two steps and no checkout (nothing in this
workflow reads the repository):

1. The same `actions/create-github-app-token` step at the same pinned SHA, with the same
   `app-id: ${{ secrets.RELEASE_APP }}`, `private-key: ${{ secrets.RELEASE_APP_PRIVATE_KEY }}`, and the
   identical `repositories: engram,homebrew-tap` input. Using the identical input is deliberate: the
   probe then verifies the exact scope the release job will hold, rather than an approximation that
   could pass while the real one fails.
2. A step named `Assert push access to seanb4t/homebrew-tap` that consumes the minted token through an
   `env:` block as `GH_TOKEN` — never as a command-line argument, matching how release.yaml already
   passes this token at lines 41 and 137. Under `set -euo pipefail`, capture
   `gh api repos/seanb4t/homebrew-tap --jq '.permissions.push'`, and when the captured value is
   anything other than the string `true`, emit a `::error::`-annotated message naming the observed
   value and exit 1. On success, print a line confirming the token can push. Let the `gh api` call's
   own failure propagate — do not add a fallback that substitutes a default value, because a defaulted
   result would make the comparison pass while measuring nothing (and a 404 here is itself the answer:
   an App with no access to the tap cannot see it).

This probe is read-only by construction. It never writes to the tap, so it leaves no commit to clean
up and has no cleanup path that could itself fail — which is why it is preferred over a real write
probe. It also verifies our own configuration rather than GitHub's documented behavior, keeping it on
the correct side of D-11's ownership boundary.

`gh` is preinstalled on GitHub-hosted runners; add no setup step for it.
  </action>
  <verify>
    <automated>task lint:actions &amp;&amp; task lint:yaml &amp;&amp; test "$(rg -o -F 'repositories: engram,homebrew-tap' .github/workflows/release.yaml | wc -l | tr -d ' ')" = 1 &amp;&amp; test "$(rg -o -F 'repositories: engram,homebrew-tap' .github/workflows/verify-tap-credential.yaml | wc -l | tr -d ' ')" = 1</automated>
  </verify>
  <acceptance_criteria>
    Same tooling note as Task 1: `rg`, not `grep`; occurrence counts via `rg -o … | wc -l`.

    - `rg -o -F 'repositories: engram,homebrew-tap' .github/workflows/release.yaml | wc -l` is exactly 1.
    - `.github/workflows/release.yaml` contains no `owner:` input on the `create-github-app-token` step, and the action's pinned SHA `bcd2ba49218906704ab6c1aa796996da409d3eb1` is unchanged: `rg -o -F 'bcd2ba49218906704ab6c1aa796996da409d3eb1' .github/workflows/release.yaml | wc -l` is 1.
    - `.github/workflows/verify-tap-credential.yaml` exists, contains `workflow_dispatch:`, and carries no other trigger: `rg -c -F 'workflow_dispatch' .github/workflows/verify-tap-credential.yaml` is 1, `rg -c -F 'pull_request' .github/workflows/verify-tap-credential.yaml` is 0, and `rg -c '^\s+push:' .github/workflows/verify-tap-credential.yaml` is 0.
    - `.github/workflows/verify-tap-credential.yaml` contains `inputs:` zero times — the workflow is dispatchable with no arguments: `rg -c -F 'inputs:' .github/workflows/verify-tap-credential.yaml` is 0.
    - `.github/workflows/verify-tap-credential.yaml` contains `GH_TOKEN: ${{ steps.app-token.outputs.token }}` inside an `env:` block, and the token value does not appear on any `run:` command line: `rg -c -F -- '--token' .github/workflows/verify-tap-credential.yaml` is 0.
    - `.github/workflows/verify-tap-credential.yaml` contains `repos/seanb4t/homebrew-tap` and `.permissions.push`, and contains none of `gh api --method PUT`, `git push`, or `git commit` — the probe is read-only.
    - `.github/workflows/verify-tap-credential.yaml` contains `contents: read` and does not contain `contents: write`.
    - `task lint:actions` exits 0 (actionlint parses the new workflow) and `task lint:yaml` exits 0.

    What these criteria do **not** claim: that the probe has run. It cannot run from this branch —
    `workflow_dispatch` is only exposed for workflows present on the default branch, so the first
    dispatch is Task 4's post-merge observation. See "Requirement scope" above.
  </acceptance_criteria>
  <done>
    The release job's App token is scoped to exactly `engram` and `homebrew-tap`, and a standalone,
    input-free, read-only `workflow_dispatch` workflow that will assert the token can push to the tap
    exists in the tree and parses under actionlint. Committed.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Grant the release-please App access to seanb4t/homebrew-tap, and capture the tap's pre-dispatch baseline</name>
  <read_first>
    - `.github/workflows/verify-tap-credential.yaml` — the workflow Task 2 added; confirm it is on the branch being worked and that its job name matches what will later be reported. It is **not** dispatchable yet; that is Task 4's business.
    - `.planning/phases/01-version-homebrew-distribution/01-RESEARCH.md` Pitfall 4 and Open Question 2 — the App may or may not already be installed on the tap (`Casks/codegraph.rb` was published there by some credential, but which one was not determined), so this is empirically answered by the probe rather than assumed either way.
    - The "Requirement scope" table above — this task closes the *grant*; the probe run that confirms it is a post-merge observation for a reason stated there.
  </read_first>
  <action>
Add `seanb4t/homebrew-tap` to the release-please GitHub App's installation, and capture the tap's
current HEAD commit SHA as the baseline the post-merge probe will be compared against.

Everything automatable is already committed: the probe workflow exists, both token mints name both
repositories, and the upload guard is in place. A GitHub App's installation repository list is
account-level UI state with no workflow-reachable API equivalent, so this grant is the human step —
and unlike the probe dispatch, it is executable right now, from this branch, before merge.

**Why the probe dispatch is not part of this task (HIGH-3).** The previous version of this plan
instructed `gh workflow run verify-tap-credential.yaml --ref <this-branch>` here, as a blocking gate.
GitHub only exposes `workflow_dispatch` for workflow definitions present on the **default** branch;
selecting a feature `--ref` runs that ref's copy of an already-registered workflow, it does not
register a brand-new one. So the gate as written could never be satisfied and the plan could never
complete. The sequencing was ours to fix, and it is fixed by moving the dispatch to Task 4's
post-merge checklist — not by asserting anything about GitHub's behavior.

**Precision on what the human actually controls (M-5).** Repository *selection* is the human action:
an installation's selected-repositories list is edited in the UI. Repository *permissions* (Contents:
Read and write among them) are declared by the App and accepted at installation; they are generally
not an independently adjustable per-repository toggle on that screen. Do not promise a control that
may not exist. Read the permissions as displayed, record what you see, and let the probe be the source
of truth on whether the token can actually push.
  </action>
  <instructions>
    Already automated and committed by Tasks 1-2:
    - `.github/workflows/verify-tap-credential.yaml`, a read-only `workflow_dispatch` probe with no inputs.
    - `repositories: engram,homebrew-tap` on both App-token mints.
    - The `skip_upload` guard that keeps a backfill from regressing the tap.

    The two things requiring you, both doable now on this branch:

    1. Open GitHub -> your account Settings -> Applications -> Installed GitHub Apps.
    2. Open the release-please App (the one behind `secrets.RELEASE_APP`) and click Configure.
    3. Under **Repository access**, add `seanb4t/homebrew-tap` to the selected repositories, keeping
       `seanb4t/engram` selected. Save. This selection is the action that is yours to take.
    4. On the same screen, **read** the Repository permissions section and note what Contents says.
       You are recording it, not setting it — the App declares its permission set and the installation
       accepted it. If Contents is not "Read and write", the App itself needs its permission set
       updated (and the installation re-accepted), which is a different and larger change; stop and
       say so rather than improvising a workaround.
    5. Capture the tap's baseline HEAD so Task 4's no-write check has something exact to compare
       against (a commit *message* is not unique and is not a baseline):
       `gh api repos/seanb4t/homebrew-tap --jq '.default_branch'` then
       `gh api repos/seanb4t/homebrew-tap/commits/<default_branch> --jq '.sha'`
       Record that SHA verbatim in `01-03-SUMMARY.md`, with the date you captured it.

    Do not work around a permissions problem by widening the token with the `owner:` input or by
    switching to a personal access token; both were considered and rejected in 01-CONTEXT.md (D-12).
  </instructions>
  <verification>
    The App's Configure screen lists both `seanb4t/engram` and `seanb4t/homebrew-tap` under Repository
    access, and `01-03-SUMMARY.md` records (a) that list as observed, (b) the Contents permission as
    displayed, and (c) the tap's baseline HEAD SHA with its capture date.
  </verification>
  <acceptance_criteria>
    - `gh api repos/seanb4t/homebrew-tap/commits/$(gh api repos/seanb4t/homebrew-tap --jq '.default_branch') --jq '.sha'` returns a 40-character SHA, and that exact string appears in `01-03-SUMMARY.md`. This is the baseline half of the no-write check; the comparison half is Task 4's.
    - `01-03-SUMMARY.md` names both repositories as observed in the installation's Repository access list, and records the Contents permission value as displayed.
    - `01-03-SUMMARY.md` records this task as the *grant*, and explicitly notes that the credential is not yet confirmed — the confirming observation is the Task 4 issue's probe dispatch.
    - No `git push` to, and no commit in, `seanb4t/homebrew-tap` resulted from this task: the SHA captured above is the same one Task 4 will compare against.
  </acceptance_criteria>
  <resume-signal>Reply "granted" once both repositories appear under the App's Repository access and you have the baseline SHA, or paste what the Configure screen shows if it does not match.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: File the post-merge verification issue that carries REQ-cask-credential-verified and REQ-homebrew-cask-published to closure</name>
  <files>.planning/phases/01-version-homebrew-distribution/01-03-SUMMARY.md (open-items section; the issue itself lives in GitHub, not in the tree — no source file is modified by this task)</files>
  <read_first>
    - The "Requirement scope" table above — the two State B items this issue tracks, and why each is post-merge rather than in-branch.
    - `CLAUDE.md` "Session Completion" — this repo's stated practice: file GitHub issues for remaining work, never markdown TODO lists.
    - `CLAUDE.md` "Issue Tracking" — GitHub Issues is the tracker (`gh issue create`); `.planning/BACKLOG.md` indexes issues for milestone promotion.
    - `.github/workflows/release.yaml` lines 44-75 — the `target` resolver, so the issue body can name accurately what triggers a publishing run (a merged release-please PR, or a dispatch with a tag).
  </read_first>
  <action>
Create one GitHub issue that carries this phase's two post-merge observations to closure, so neither is
left as an unowned assumption. This is the mechanism the "Requirement scope" section commits to, and it
is executable now: it writes no code and depends on nothing being merged.

Use `gh issue create` with title `Phase 01 post-merge: verify tap credential and observe first cask
publication`, labelled `from-beads`-free (this is new work, not a migration) — apply whatever label
this repo uses for release/distribution work, or none if no fitting label exists; do not invent one.

The body must contain both checklists verbatim enough to be executed without re-reading this plan:

**A. REQ-cask-credential-verified — dispatch the probe from `main` (do this first).**
1. Confirm the workflow is registered on the default branch:
   `gh api repos/seanb4t/engram/contents/.github/workflows/verify-tap-credential.yaml?ref=main --jq '.name'`
   returns the filename. Until this succeeds, `workflow_dispatch` will not expose the workflow — that
   is the whole reason this step is post-merge.
2. `gh workflow run verify-tap-credential.yaml --ref main`
3. `gh run watch $(gh run list --workflow=verify-tap-credential.yaml --limit 1 --json databaseId --jq '.[0].databaseId')`
4. Expected: conclusion `success`, and the run log contains the line confirming the token can push to
   `seanb4t/homebrew-tap`. A failure reporting `permissions.push` as anything other than `true` means
   the Task 3 grant did not take effect — recheck the installation's repository list before
   re-dispatching, and do not widen the token with `owner:` or swap in a PAT (D-12).
5. **No-write check, with the real baseline (L-1).** Re-read the tap's HEAD SHA
   (`gh api repos/seanb4t/homebrew-tap/commits/<default_branch> --jq '.sha'`) and assert it is
   byte-identical to the SHA recorded in `01-03-SUMMARY.md` by Task 3. Compare SHAs, never commit
   messages — messages are not unique and a repeated message would read as "unchanged" while the tap
   had in fact moved.
6. Record the run URL, its conclusion, the observed `.permissions.push` value, and the SHA comparison
   in the issue, with the date.

**B. REQ-homebrew-cask-published — observe the first publishing release.**
This step happens on whichever release runs next through `release.yaml` after merge (normally: a
release-please PR merges, the tag is cut, and the shipping steps run). Observe, do not force: do not
hand-cut a tag to make this happen sooner.
1. From the release run, confirm the GoReleaser step reported the Homebrew cask upload rather than
   skipping it, and that `SKIP_HOMEBREW_UPLOAD` was `false` for that run (it is echoed by the
   `Resolve Homebrew upload guard` step Task 1 added).
2. `gh api repos/seanb4t/homebrew-tap/contents/Casks/engram.rb --jq '.sha'` returns a blob SHA — the
   cask now exists in the tap.
3. Record the tap commit SHA that introduced it, the cask's declared version, and the release tag it
   came from, in the issue.
4. Only after both A and B are recorded may `REQ-homebrew-cask-published` be marked complete in
   `.planning/REQUIREMENTS.md`. Do not mark it complete at phase verification.

Do not include the tap credential, the App private key, or any token value in the issue body.

Finally, add the created issue's number to `01-03-SUMMARY.md`'s open items, so the SUMMARY names an
owner and a tracking location for both State B rows rather than leaving them implicit.
  </action>
  <verify>
    <automated>gh issue list --limit 20 --json number,title --jq '[.[] | select(.title | test("Phase 01 post-merge"))] | length' | rg -q '^[1-9]'</automated>
  </verify>
  <acceptance_criteria>
    - `gh issue list --limit 20 --json number,title --jq '.[] | select(.title | test("Phase 01 post-merge")) | .number'` returns at least one issue number.
    - That issue's body contains both `verify-tap-credential.yaml` and `Casks/engram.rb`, so both State B items are actually carried: `gh issue view <n> --json body --jq '.body' | rg -c -F 'verify-tap-credential.yaml'` is at least 1 and `gh issue view <n> --json body --jq '.body' | rg -c -F 'Casks/engram.rb'` is at least 1.
    - That issue's body carries the SHA-based no-write check, not a message-based one: `gh issue view <n> --json body --jq '.body' | rg -c -F "--jq '.sha'"` is at least 2 (the baseline re-read in step A5 and the cask blob in step B2).
    - `01-03-SUMMARY.md` cites the issue number under open items, alongside the baseline SHA Task 3 recorded.
    - No token, private key, or credential value appears in the issue body.
  </acceptance_criteria>
  <done>
    A GitHub issue exists carrying both post-merge observations — the credential probe dispatched from
    `main`, and the first tagged release's cask publication — each with runnable steps and an explicit
    recording requirement, and `01-03-SUMMARY.md` names it under open items.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI job -> `seanb4t/homebrew-tap` | A GitHub App installation token crosses a repository boundary to commit `Casks/engram.rb` into a repository that is not the one running the workflow. |
| `workflow_dispatch` input -> published artifacts | A human-supplied tag decides which commit's artifacts are published and, without a guard, which version the tap advertises. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-08 | Elevation of Privilege | `actions/create-github-app-token` scope in both workflows | high | mitigate | `repositories: engram,homebrew-tap` is a closed allowlist naming exactly the two repositories the phase needs; the `owner:` input, which would grant every repository the App is installed on, is explicitly not used. The App's own installation list is the outer bound regardless. |
| T-01-09 | Tampering | `Casks/engram.rb` regressed by a `workflow_dispatch` backfill of an older tag | high | mitigate | `skip_upload` is driven by a newest-tag comparison computed in the workflow before the GoReleaser step, so a backfill never writes the cask at all. The guard fails the run rather than defaulting when no `v*` tag resolves. |
| T-01-10 | Information Disclosure | the App token in the release job and the probe job | medium | mitigate | Consumed only through `with: token:` and `env:` blocks (`GITHUB_TOKEN`, `GH_TOKEN`), never as a command-line argument where the process table or a shell history would capture it — matching the existing pattern at release.yaml lines 41 and 137. The probe's assertion prints the observed permission value, never the token. |
| T-01-11 | Tampering | the credential probe itself | medium | mitigate | The probe is read-only by construction (`gh api repos/... --jq .permissions.push`), so it cannot corrupt the tap even if misconfigured, and it has no cleanup path that could fail and leave debris. A real write probe was considered and rejected for exactly that reason. Verified against a **captured baseline**: Task 3 records the tap's HEAD SHA before any dispatch and the Task 4 checklist compares the exact SHA afterward — not the latest commit *message*, which is not unique and would read as "unchanged" even if the tap had moved. |
| T-01-12 | Repudiation | authorship of the tap commit | low | accept | The tap write is performed by the same App identity that already cuts this repository's tags and GitHub Releases, so the actor is attributable through the App's own audit trail. Accepted: no additional signing or audit control is added, and the tap is a personal repository with a single maintainer. |
| T-01-13 | Denial of Service | the newest-tag guard failing a legitimate release | low | accept | The guard exits 1 only when no `v*` tag resolves at all, which means the checkout is wrong. Failing loudly there is the intended behavior; the alternative (defaulting to upload) would publish an unverified cask. Accepted with that rationale. |
| T-01-SC | Tampering | npm/pip/cargo installs | low | accept | Not applicable — this plan adds no package-manager install task. The only action used is `actions/create-github-app-token`, already pinned by commit SHA in this repository at `bcd2ba49218906704ab6c1aa796996da409d3eb1`, and `gh` is preinstalled on the runner. RESEARCH.md's Package Legitimacy Audit records zero new external packages, so no `[ASSUMED]`/`[SUS]`/`[SLOP]` verdict exists to gate. |
</threat_model>

<verification>
- `task lint:actions` and `task lint:yaml` exit 0.
- `task release:check` exits 0 with `SKIP_HOMEBREW_UPLOAD` unset. This is `goreleaser check` — schema validation, not template evaluation (see Task 1's criterion).
- `task` (lint + test) exits 0.
- `git diff --name-only` touches only the three files in `files_modified`.
- The App's installation lists both `seanb4t/engram` and `seanb4t/homebrew-tap`, and the tap's baseline HEAD SHA is recorded in the SUMMARY (Task 3).
- A GitHub issue titled "Phase 01 post-merge: …" exists and is cited in the SUMMARY's open items (Task 4).
- Manual, not a gate (01-VALIDATION.md, Manual-Only): `task release:snapshot` with `SKIP_HOMEBREW_UPLOAD` unset completes — the only place the cask's Go templates are actually evaluated. Per D-11 no assertion is made over the rendered file's contents.
- **Not verified in-phase, by design (see "Requirement scope"):** the `verify-tap-credential` run and the first cask publication. `workflow_dispatch` requires the workflow on the default branch, and publication requires a tag. Both are carried by the Task 4 issue and must not be failed here as missing.
- Per D-15, no staged `brew install` failure and no backfill rehearsal is performed; ROADMAP criteria 3 and 5 are satisfied by construction and must not be failed on their literal rehearsal wording.
</verification>

<success_criteria>
- The release job's App token requests exactly `engram` and `homebrew-tap`, and `engram` remains in the allowlist so release-please's own writes stay in scope.
- A read-only, input-free `workflow_dispatch` workflow that will prove the token can push to `seanb4t/homebrew-tap` exists in the tree and parses under actionlint; its first dispatch is tracked as a post-merge observation.
- `SKIP_HOMEBREW_UPLOAD` is computed before GoReleaser runs and is `false` only when the shipped tag is the newest `v*` tag.
- The cask's `skip_upload` reads that variable through a guarded template that still renders when it is unset.
- The release-please App is installed on `seanb4t/homebrew-tap`, and the tap's pre-dispatch HEAD SHA is recorded so the probe's no-write claim can be checked against a real baseline.
- A tracking issue carries both post-merge observations, and `01-03-SUMMARY.md` cites it under open items.
</success_criteria>

<output>
Create `.planning/phases/01-version-homebrew-distribution/01-03-SUMMARY.md` when done
</output>
