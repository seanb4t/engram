---
phase: 01-version-homebrew-distribution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/engram/version.go
  - cmd/engram/version_test.go
  - cmd/engram/exitcode_baseline_test.go
  - cmd/engram/testdata/help.golden
  - cmd/engram/testdata/catalog.golden
  - .goreleaser.yaml
autonomous: true
requirements:
  - REQ-version-json
  - REQ-homebrew-cask-published
  - REQ-cask-install-gate

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`engram version --output json` writes exactly one JSON document to stdout whose only key is `version`"
    - "`engram version` with no flags writes the bare version string plus a trailing newline and nothing else, on a non-TTY stdout as well as a TTY"
    - "The text lane's output equals the JSON lane's `version` field for the same invocation"
    - "`engram version --output bogus` exits 2 through the existing usage-error taxonomy"
    - "The cask's post-install hook strips `com.apple.quarantine` before it invokes the installed binary for the first time"
    - "The cask's post-install hook raises when the installed binary's reported version differs from the version the cask declares"
    - "Uninstalling the cask removes every completion file the post-install hook wrote"
  artifacts:
    - path: "cmd/engram/version.go"
      provides: "version subcommand with --output json|text, versionDoc, and its own text/json render pair"
      contains: "versionDoc"
    - path: "cmd/engram/version_test.go"
      provides: "behavioral gates for the json lane, the bare-scalar text lane, and the text==json invariant"
      min_lines: 60
    - path: ".goreleaser.yaml"
      provides: "homebrew_casks: block with the quarantine-strip-then-version-gate post-install hook"
      contains: "homebrew_casks:"
  key_links:
    - from: ".goreleaser.yaml"
      to: "cmd/engram/version.go"
      via: "the cask post-install hook shells the installed binary with the version subcommand in json mode and compares the parsed version against the cask's declared version"
      pattern: "--output.*json"
    - from: "cmd/engram/version.go"
      to: "internal/config/client_validate.go"
      via: "the --output value is validated by the single shared output-format validator"
      pattern: "ValidateOutputFormat"
  prohibitions:
    - statement: "MUST NOT let the install-time correctness gate degrade into a warning — no path in the cask hook may rescue, swallow, or downgrade a failed version comparison, and the gate must never be delegated to a Homebrew helper that rescues execution failures to a warning"
      category: safety
      requirement_id: REQ-cask-install-gate
    - statement: "MUST NOT add any test, CI job, or acceptance gate that asserts behavior owned by Homebrew, Apple Gatekeeper, or GoReleaser — verification stops at the boundary of code and config this repository owns"
      category: values
      requirement_id: REQ-cask-install-gate
---

<objective>
Deliver the phase's one end-to-end contract: `engram version --output json` emits a machine-readable
payload, and the Homebrew cask's post-install hook is the consumer that asserts it. Both ends land
together so the contract is proven at the point it is created rather than assumed across a phase
boundary.

Implements D-01 (unconditional text default, no TTY detection), D-02 (cobra's built-in `-v/--version`
untouched), D-03 (`--output bogus` routes through the existing validator to exit 2), D-06 (the JSON
payload carries `version` and nothing else), D-07 (`version` writes its own minimal render pair),
D-08 (no `v` prefix on the version string), D-09 (quarantine strip first, then the version assertion),
D-10 (bash/zsh/fish completions and their removal on uninstall), and D-11 (verification is Go-only —
nothing here gates Homebrew's, Apple's, or GoReleaser's own behavior).

Purpose: `version.go:16` prints a bare string today, so a cask has nothing machine-readable to assert
against. Without this the install gate cannot exist, and Homebrew's `generate_completions_from_executable`
cannot substitute for it because it rescues a broken binary's failure to a warning.

Output: a `version` subcommand with a stable json lane, a behavioral test suite pinning the
text==json invariant, regenerated `--help`/catalog goldens, and a `homebrew_casks:` block in
`.goreleaser.yaml` whose post-install hook consumes exactly that json lane.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-version-homebrew-distribution/01-CONTEXT.md
@.planning/phases/01-version-homebrew-distribution/01-RESEARCH.md
@.planning/phases/01-version-homebrew-distribution/01-PATTERNS.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Symbols, files, flags, and config blocks **created** by Phase 01. Nothing in this list exists on
`main` today; drift verification must treat every entry as new, not as a reference to existing code.

| Kind | Artifact | Created by |
|------|----------|------------|
| CLI flag | `--output` on the `version` subcommand (string, default `text`) | 01-01 |
| Go type | `versionDoc` struct in `cmd/engram/version.go` (single field, json tag `version`) | 01-01 |
| Go func | `runVersion` — the `RunE` body replacing today's `Run` closure | 01-01 |
| Go file | `cmd/engram/version_test.go` | 01-01 |
| Test func | `TestVersionJSONLane`, `TestVersionTextLane`, `TestVersionTextEqualsJSON`, `TestVersionOutputFlagDefault` | 01-01 |
| Test row | `exitCodeBaseline` row `version/output-bogus` (`introduced: true`, `after: exitUsage`) | 01-01 |
| YAML block | `homebrew_casks:` top-level key in `.goreleaser.yaml` | 01-01 |
| YAML key | `homebrew_casks[0].hooks.post.install` (Ruby postflight) | 01-01 |
| YAML key | `homebrew_casks[0].hooks.post.uninstall` (Ruby uninstall postflight) | 01-01 |
| Go file | `cmd/engram/buildversion.go` | 01-02 |
| Go const | `lastRelease` (carrying the `x-release-please-version` annotation) | 01-02 |
| Go func | `nextPatch`, `deriveDevVersion`, `versionFromModuleVersion`, `resolvedVersion` | 01-02 |
| Go file | `cmd/engram/buildversion_test.go` | 01-02 |
| Test func | `TestNextPatch`, `TestDeriveDevVersion`, `TestVersionFromModuleVersion`, `TestLastReleaseMatchesManifest` | 01-02 |
| JSON entry | 4th `extra-files` entry in `release-please-config.json` (`"type": "generic"`) | 01-02 |
| YAML key | `homebrew_casks[0].skip_upload` (templated) | 01-03 |
| Workflow step | `Resolve Homebrew upload guard` step in `.github/workflows/release.yaml` | 01-03 |
| Workflow env | `SKIP_HOMEBREW_UPLOAD` (exported via `$GITHUB_ENV`) | 01-03 |
| Workflow input | `repositories: engram,homebrew-tap` on the existing App-token mint step | 01-03 |
| Workflow job | `verify-tap-credential` (`workflow_dispatch`-only) | 01-03 |
| Tap file | `Casks/engram.rb` in `seanb4t/homebrew-tap` (written by GoReleaser at release time) | 01-03 |

## Flagged Assumptions (edge-probe, unresolved rows)

The deterministic edge probe returned six rows across the phase's five requirement IDs, all
`unresolved`. Three belong to this plan. Per protocol an `unclassified` row is never auto-resolved
with a backstop marker — it is surfaced here as an explicit assumption.

| Requirement | Probe category | Assumption recorded |
|---|---|---|
| REQ-version-json | unclassified | The probe could not classify this requirement into a boundary/precision/state category. Assumed correct: the requirement's whole edge surface is the `--output` value domain, which is closed by the existing three-value validator (`json`/`text`/empty) and pinned by the `version/output-bogus` baseline row. No numeric, temporal, or ordering edge exists. |
| REQ-homebrew-cask-published | unclassified | Unclassified. Assumed correct: the requirement's edges are platform-matrix edges (darwin/linux × amd64/arm64) that GoReleaser's existing `builds:` matrix already produces and that D-11 forbids gating. No in-repo edge remains for this plan to author. |
| REQ-cask-install-gate | unclassified | Unclassified. Assumed correct: the gate's only edge is string equality between the binary's reported version and the cask's declared version, which D-08 (no `v` prefix on either side) makes total — there is no normalization step that could partially succeed. |

<!-- planner-discipline-allow: renderOperator -->
<!-- planner-discipline-allow: config.FlagDefault -->

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end version-json contract — the binary emits it, the cask asserts it</name>
  <reversibility rating="one-way">D-01's unconditional text default is a published output contract: flipping to TTY detection later silently changes output for every piped caller and every `$(engram version)` in a script, including the cask's own postflight. No `checkpoint:decision` is inserted — 01-CONTEXT.md records the developer choosing this exact option, so the door is already walked through and re-asking would be gating a decision already made.</reversibility>
  <files>cmd/engram/version.go, cmd/engram/version_test.go, cmd/engram/exitcode_baseline_test.go, cmd/engram/testdata/help.golden, cmd/engram/testdata/catalog.golden, .goreleaser.yaml</files>
  <read_first>
    - `cmd/engram/version.go` — the 16-line file being replaced; note the `Run` (not `RunE`) closure printing the bare `version` var.
    - `cmd/engram/root.go` lines 16-19, 28, 71-72 — the `version` package var and its ldflags contract, cobra's built-in `Version` field (D-02: untouched), and `rootCmd.AddCommand(serveCmd, versionCmd)`.
    - `cmd/engram/operator_output.go` — `addOperatorOutputFlag`, `operatorOutputFormat`, and `renderOperator`. Read for the validator-call shape to copy and the render helper to deliberately NOT call.
    - `cmd/engram/client_common.go` lines 42-55, 190-213, 215-253 — `addClientFlags`, `outputFormatFromConfig`, the exit-code constants, and `usageErrorf`.
    - `internal/config/client_validate.go` around line 58 — `ValidateOutputFormat`, the single shared validator.
    - `cmd/engram/exitcode_baseline_test.go` lines 1-60 and 165-210 — the `exitCodeBaselineCase` struct, the `introduced` field's doc comment, and an existing `introduced: true` row to copy.
    - `cmd/engram/clienttest_test.go` around line 240 — the `runClient` harness (`rootCmd.SetOut`/`SetArgs`/`Execute`) and `resetEveryCommandFlagState`.
    - `cmd/engram/testdata/catalog.golden` around line 1132 — the existing `version` entry with `"flags": []`.
    - `cmd/engram/testdata/help.golden` around lines 455-469 — the existing `## engram version` block.
    - `.goreleaser.yaml` — the whole file; note `archives.name_template` is `{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}` with no `v` prefix (D-08's in-repo precedent), and `builds.env: CGO_ENABLED=0` (why the binary ships unsigned).
    - `.planning/phases/01-version-homebrew-distribution/01-RESEARCH.md` Pattern 2, Pattern 3, Pitfall 1, Pitfall 2, Pitfall 7 — the composed cask skeleton and the two path/OS traps.
  </read_first>
  <behavior>
    Write these tests in `cmd/engram/version_test.go` BEFORE the implementation, and observe them fail:

    - `TestVersionJSONLane`: running `version --output json` through `runClient` produces stdout that
      `json.Unmarshal`s into a `map[string]any` with exactly one key, `version`, whose value is a
      non-empty string. Asserting the key SET (length 1) is the D-06 gate — a second field added later
      turns this red.
    - `TestVersionTextLane`: running `version` with no flags through `runClient` produces stdout that
      equals the reported version string plus exactly one `\n` — no headline, no blank line, no padded
      label/value row. This is the behavioral proof that the shared operator render path is not in use
      (D-07): that path always emits a headline first, so a bare-scalar assertion cannot pass through it.
    - `TestVersionTextEqualsJSON`: the string `TestVersionTextLane` observed, trimmed of its trailing
      newline, is byte-equal to the `version` field `TestVersionJSONLane` decoded. This is D-07's
      required pin — the two lanes are produced by separate code paths and must not drift.
    - `TestVersionOutputFlagDefault`: `versionCmd.Flags().Lookup("output").DefValue` is exactly `text`.
      Combined with `TestVersionTextLane` (whose stdout is a `bytes.Buffer`, i.e. always non-TTY) this
      is the D-01 gate: a default that auto-detected from stdout would make the text-lane test emit
      JSON, so TTY detection cannot be reintroduced without turning both tests red.
    - None of these tests may hard-code a version VALUE. Assert structure and cross-lane equality only —
      plan 01-02 changes what the version string contains, and a value-pinning test would break it.

    Add one row to `exitCodeBaseline` in `cmd/engram/exitcode_baseline_test.go`:
    `name: "version/output-bogus"`, `args: []string{"version", "--output", "bogus"}`, `after: exitUsage`,
    `introduced: true`. `introduced` rows assert only `after` and are exempt from the
    distinct/identical claim checks — this capability does not exist at the baseline commit.
  </behavior>
  <action>
Rewrite `cmd/engram/version.go` and add a `homebrew_casks:` block to `.goreleaser.yaml` so the
binary's json lane and the cask's install gate are the same contract, created together.

**Go side — `cmd/engram/version.go`:**

Keep the Apache-2.0 SPDX header and `package main`. Declare a `versionDoc` struct with exactly one
exported field, `Version string`, tagged `json:"version"` — D-06 pins the payload to this single
field, because D-04's version string already folds the commit and dirty state in as SemVer build
metadata and a separate `commit` field would be a second spelling of the same fact.

Convert `versionCmd` from `Run` to `RunE` so an error can carry an exit code. In an `init()` in this
file, register the flag as `versionCmd.Flags().String("output", "text", …)` — the default is the
string literal `text`, hardcoded. Do **not** route it through `config.FlagDefault("output")` the way
`addClientFlags` and `addOperatorOutputFlag` do: that helper resolves to the empty string (the
`client.output` registry row carries no `Default`), which means "auto-detect from stdout", and
adopting it would silently make `engram version | cat` emit JSON. Write the flag's usage sentence to
state the divergence out loud, in the repo's correct-by-reading style: json or text, defaulting to
text always, and unlike every other `--output` flag in this binary this one never auto-detects from
stdout.

The `RunE` body reads the flag, passes the value to `config.ValidateOutputFormat`, and on error
returns `usageErrorf("%w", err)` — the identical validator-then-`usageErrorf` shape
`operatorOutputFormat` uses, which lands on `exitUsage` (2) through the existing taxonomy (D-03).
Do **not** call `outputFormatFromConfig` afterwards; that is where the TTY detection lives.

Render with this command's own minimal pair (D-07), not the operator tier's shared `renderOperator`:
when the validated value is `json`, construct `versionDoc{Version: v}` and write it with
`json.NewEncoder(cmd.OutOrStdout()).Encode(...)`; otherwise write `v` with `fmt.Fprintln(cmd.OutOrStdout(), v)`.
Use `cmd.OutOrStdout()` in both lanes, never the process `os.Stdout` — the test harness swaps in a
buffer. `renderOperator` is deliberately unused here because it derives text FROM the json document
via `renderOperatorView`, which always emits a headline, a blank line, then padded label/value rows
and therefore cannot produce a bare scalar.

For `v`, read the existing `version` package var from `root.go` directly in this plan. Plan 01-02
replaces that read with a build-info-aware resolver; nothing here should anticipate it.

Leave cobra's built-in `-v, --version` flag and `rootCmd.Version` completely untouched (D-02). Only
the subcommand gains `--output`. Add a file-level comment naming this as the local tier's own,
deliberately fourth `--output` registration site — a bounded divergence from the client and operator
tiers, documented so a later reader does not "fix" it toward one of them.

Regenerate both goldens with `task surfaces:gen`. Review the diff: it must touch only the `version`
command's section of `cmd/engram/testdata/catalog.golden` (its `"flags"` array gains an `output`
entry with `"default": "text"`) and only the `Flags:` block of the `## engram version` section of
`cmd/engram/testdata/help.golden`. `internal/surfaces/toolclass.go` already classifies `version`
(`ReadOnly: true, Destructive: false, Idempotent: true, OpenWorld: false`) — do not add a
classification row, and do not hand-edit either golden.

**Cask side — `.goreleaser.yaml`:**

Add a new top-level `homebrew_casks:` key (GoReleaser's current cask publisher — `brews:` is
deprecated and must not be introduced). One entry, `name: engram`, with `repository.owner: seanb4t`,
`repository.name: homebrew-tap`, `directory: Casks`, `homepage: https://github.com/seanb4t/engram`,
and `description` matching the repo's own one-liner: self-hosted, correctable, OAuth-secured memory
MCP server for coding agents. Do not set `repository.token` — the GoReleaser step in `release.yaml`
already passes the App token as `GITHUB_TOKEN`. Do not set `skip_upload` in this plan; plan 01-03
owns that field.

Write `hooks.post.install` as a Ruby block doing exactly two things in this order (completions are
Task 2's):

1. Guarded by `if OS.mac?`, call `system_command "/usr/bin/xattr", args: ["-dr", "com.apple.quarantine", "#{staged_path}/engram"]`.
   This is the literal first statement in the hook. engram ships unsigned (`CGO_ENABLED=0`, no
   GoReleaser Pro and no Apple Developer Program membership), so a hook that runs the binary before
   stripping quarantine gets SIGKILLed by Gatekeeper instead of failing cleanly with a diagnosable
   message. The `OS.mac?` guard is required, not optional: `xattr` does not exist on Linux and an
   unguarded call breaks `brew install` for every Linux user, who has no Gatekeeper quarantine to
   strip in the first place. Use `#{staged_path}/engram` here — the extracted archive contents — not
   the `HOMEBREW_PREFIX` symlink.
2. Assign `binary = "#{HOMEBREW_PREFIX}/bin/engram"`, then `result = system_command binary, args: ["version", "--output", "json"]`.
   This path is correct for this step specifically because the hook runs after Homebrew's
   `install_artifacts` has already created the symlink. Parse with `JSON.parse(result.stdout)["version"].to_s`,
   compare against `version.to_s` (the cask's own declared version), and `raise` with a message naming
   both values when they differ. No `.sub(/\Av/, "")` normalization on either side: D-08 pins engram's
   version string and archive names to bare SemVer with no `v` prefix, so both sides are already in the
   same shape — the `codegraph.rb` precedent in the same tap needs that strip only because ITS archives
   are `v`-prefixed.

`system_command` defaults to `must_succeed: true`, so a non-zero exit from the binary raises on its
own; the explicit `raise` covers the case where the binary exits zero but reports the wrong version.
Do not use `generate_completions_from_executable` as the gate under any circumstance — Homebrew's
`write_completion` wraps execution in a rescue that downgrades a failure to a warning, so a broken
binary would install green.

Per D-11, add no CI job and no golden/snapshot assertion over the rendered cask file. `goreleaser check`
validating our own config is the gate; Homebrew's and Apple's behavior is documented, never gated.
  </action>
  <verify>
    <automated>go test ./cmd/engram -run 'TestVersionJSONLane|TestVersionTextLane|TestVersionTextEqualsJSON|TestVersionOutputFlagDefault|TestExitCodeBaseline|TestHelpGolden|TestCatalogGolden' -count=1 -v 2>&1 | tee /tmp/engram-01-01.log | grep -Eq -- '--- PASS: TestVersionTextEqualsJSON' &amp;&amp; grep -Eq -- '--- PASS: TestExitCodeBaseline/version/output-bogus' /tmp/engram-01-01.log &amp;&amp; task release:check</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd/engram -list 'TestVersion.*' | grep -c '^TestVersion'` is at least 4 — proves the four named tests actually exist and that the `-run` filter above is not silently matching nothing.
    - `go test ./cmd/engram -run 'TestVersionTextEqualsJSON' -count=1 -v` prints a `--- PASS: TestVersionTextEqualsJSON` line (a package-level `ok` alone is not acceptance; a `-run` pattern matching zero tests also exits 0).
    - `go test ./cmd/engram -run 'TestExitCodeBaseline/version/output-bogus' -count=1 -v` prints a `--- PASS` line for that subtest.
    - `cmd/engram/version.go` contains `Flags().String("output", "text"` — the default is the hardcoded literal, verifiable at source level.
    - `versionCmd.Flags().Lookup("output").DefValue` is `text`, asserted by `TestVersionOutputFlagDefault`.
    - `git diff --stat cmd/engram/testdata/` shows changes to exactly `catalog.golden` and `help.golden`; `git diff cmd/engram/testdata/catalog.golden` shows added lines only inside the `version` command object, and the added flag entry carries `"default": "text"`.
    - `git diff cmd/engram/testdata/help.golden` shows no change to the `-v, --version` line (D-02).
    - `.goreleaser.yaml` contains a top-level `homebrew_casks:` key and does NOT contain a `brews:` key.
    - Ordering gate (the D-09 invariant, runnable): `Q=$(grep -n 'com.apple.quarantine' .goreleaser.yaml | head -1 | cut -d: -f1); V=$(grep -n '"--output", "json"' .goreleaser.yaml | head -1 | cut -d: -f1); test -n "$Q" && test -n "$V" && test "$Q" -lt "$V"` exits 0. Both line numbers must resolve; an empty result fails rather than defaulting.
    - `.goreleaser.yaml` contains `if OS.mac?` on a line whose number is less than `$Q` above, or on the same statement group — the quarantine strip is OS-guarded (Pitfall 7).
    - `.goreleaser.yaml` contains `staged_path` in the xattr call and `HOMEBREW_PREFIX}/bin/engram` in the version-check call — the two paths are not interchanged (Pitfall 2).
    - `.goreleaser.yaml` does not contain `generate_completions_from_executable`.
    - `task release:check` exits 0.
    - `task lint:yaml` exits 0 and `task license:check` exits 0 (the new `cmd/engram/version_test.go` carries the Apache-2.0 SPDX header; `.planning/**` files never do).
  </acceptance_criteria>
  <done>
    `engram version --output json` emits `{"version":"…"}`, `engram version` emits the bare string, the
    two are proven equal by a test, `--output bogus` exits 2, both goldens are regenerated from the live
    cobra tree, and `.goreleaser.yaml` carries a cask whose post-install hook strips quarantine and then
    asserts that exact json contract. Committed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Cask completions for bash, zsh, and fish — written by the gate, removed on uninstall</name>
  <files>.goreleaser.yaml</files>
  <read_first>
    - `.goreleaser.yaml` — the `homebrew_casks:` block Task 1 just added; this task extends the same `hooks.post.install` block and adds `hooks.post.uninstall`.
    - `cmd/engram/testdata/help.golden` around line 11 — confirms `completion` is a live subcommand (cobra's default, not disabled), so `engram completion bash|zsh|fish` is a real invocation.
    - `.planning/phases/01-version-homebrew-distribution/01-CONTEXT.md` D-09 and D-10 — the three-step hook ordering and the exact three completion paths.
    - `.planning/phases/01-version-homebrew-distribution/01-RESEARCH.md` Pattern 3's "Note on `completions:` vs `generate_completions_from_executable`" and Assumptions Log row A4 — why the hand-written hook route is chosen over the declarative `completions:` field.
  </read_first>
  <action>
Extend the `hooks.post.install` Ruby block in `.goreleaser.yaml` with its third and final step, and
add a sibling `hooks.post.uninstall` block.

Implement completions inside the install hook, after the version assertion, not via GoReleaser's
declarative `completions:` field. The declarative field expects completion FILES generated at build
time, which would require a new `before:` hook and a new build-time step; D-09 instead specifies
writing them from the installed binary via `system_command`, whose default `must_succeed: true` makes
a failure raise. This is the second time the hook exercises the binary, which is the point: a binary
that cannot produce completions is a binary that fails the install rather than one that installs with
a warning.

Iterate over the three shell/destination pairs, using `#{HOMEBREW_PREFIX}`-rooted absolute paths:

- `bash` → `#{HOMEBREW_PREFIX}/etc/bash_completion.d/engram`
- `zsh` → `#{HOMEBREW_PREFIX}/share/zsh/site-functions/_engram`
- `fish` → `#{HOMEBREW_PREFIX}/share/fish/vendor_completions.d/engram.fish`

For each pair: call `system_command binary, args: ["completion", <shell>]` reusing the same `binary`
local the version-assertion step already assigned, `FileUtils.mkdir_p` the destination's directory,
and `File.write` the captured `.stdout` to the destination. PowerShell is excluded deliberately — it
is not reachable through Homebrew on macOS or Linux.

Add `hooks.post.uninstall` as the `uninstall_postflight` analogue, removing exactly those three paths
with `FileUtils.rm_f`. This block is required, not optional: Homebrew tracks the files IT installs, and
has no record of files a hook wrote, so without it an uninstall leaves all three behind. Remove only
those three paths — do not remove their parent directories, which are shared with other formulae and
casks.

Do not port `Casks/codegraph.rb`'s per-path mtime-and-size freshness baseline. That mechanism exists to
guard generated files against staleness; here it would only be guarding files this same hook just wrote,
in the same invocation, which adds a moving part with nothing to catch.
  </action>
  <verify>
    <automated>task release:check &amp;&amp; task lint:yaml &amp;&amp; grep -c 'engram.fish' .goreleaser.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `.goreleaser.yaml` contains all three destination paths verbatim: `etc/bash_completion.d/engram`, `share/zsh/site-functions/_engram`, `share/fish/vendor_completions.d/engram.fish`.
    - Each of those three paths appears exactly twice in `.goreleaser.yaml` — once in the install hook and once in the uninstall hook. Check with `for p in 'etc/bash_completion.d/engram' 'share/zsh/site-functions/_engram' 'share/fish/vendor_completions.d/engram.fish'; do test "$(grep -o -F "$p" .goreleaser.yaml | wc -l | tr -d ' ')" = 2 || exit 1; done` exiting 0.
    - `.goreleaser.yaml` contains a `uninstall:` key nested under `hooks.post` (the uninstall postflight exists).
    - `.goreleaser.yaml` contains `FileUtils.rm_f` and does not contain `rm_rf`.
    - The install hook's completion writes appear at a line number greater than the version-assertion line: `V=$(grep -n '"--output", "json"' .goreleaser.yaml | head -1 | cut -d: -f1); C=$(grep -n 'bash_completion.d/engram' .goreleaser.yaml | head -1 | cut -d: -f1); test -n "$V" && test -n "$C" && test "$V" -lt "$C"` exits 0.
    - `task release:check` exits 0 and `task lint:yaml` exits 0.
    - `task` (lint + test) exits 0 for the plan as a whole.
  </acceptance_criteria>
  <done>
    The cask's post-install hook performs D-09's three steps in order — quarantine strip, version
    assertion, completions — and `hooks.post.uninstall` removes exactly the three files the install hook
    wrote. Committed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| release artifact → user machine | An unsigned, Gatekeeper-quarantined binary crosses onto an end user's machine during `brew install`; the cask's post-install hook is the only correctness control on this crossing. |
| argv → `version` command | An untrusted `--output` value reaches the command's rendering choice. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | `.goreleaser.yaml` `hooks.post.install` version gate | high | mitigate | The hook runs `engram version --output json` against `#{HOMEBREW_PREFIX}/bin/engram` and `raise`s unless the parsed `version` equals the cask's declared `version`; `system_command`'s `must_succeed: true` default makes a non-zero exit raise on its own. The quarantine strip is the hook's literal first statement so the compare actually runs instead of the process being SIGKILLed before it. The gate is never delegated to `generate_completions_from_executable`. |
| T-01-02 | Spoofing | released archive provenance | medium | accept | Cryptographic integrity is the existing `checksum:` block plus the `sha256` stanza GoReleaser auto-populates in the cask; the version gate is a correctness check, not an integrity check. Signing/notarizing is scoped out at milestone level (REQUIREMENTS.md Out of Scope — it costs a GoReleaser Pro licence plus Apple Developer Program membership, not a code change). Accepted: this phase introduces no new provenance weakness, and the tap is third-party, so Homebrew's official-cask Gatekeeper policy does not apply. |
| T-01-03 | Tampering | `version --output` argv value (ASVS V5) | low | mitigate | Routed through the single shared `config.ValidateOutputFormat` — no new validation logic, no second parse path — and rejected via `usageErrorf` onto `exitUsage` (2). |
| T-01-04 | Denial of Service | post-install hook invoking the binary twice | low | accept | Both invocations are local, bounded, and run under Homebrew's own installer; a hang is a failed install the user sees immediately, not a service outage. No timeout control added. |
| T-01-SC | Tampering | npm/pip/cargo installs | low | accept | Not applicable — this phase adds no package-manager install task. RESEARCH.md's Package Legitimacy Audit records zero new external packages: the json lane uses only `encoding/json`, and GoReleaser is a build-time tool that never enters `go.sum`. No `[ASSUMED]`, `[SUS]`, or `[SLOP]` verdicts exist to gate. |
</threat_model>

<verification>
- `task` (lint + test) exits 0.
- `task release:check` exits 0.
- `go test ./cmd/engram -run 'TestVersion' -count=1 -v` shows a `--- PASS` line for each of the four named tests.
- `git diff` touches only the six files in `files_modified`.
- Manual (per 01-VALIDATION.md's Manual-Only table, not a gate): `task release:snapshot` renders `dist/` and the cask can be read there. Not automated — D-11 forbids gating Homebrew's or GoReleaser's own behavior.
</verification>

<success_criteria>
- `engram version --output json` prints exactly one JSON document whose only key is `version`.
- `engram version` prints the bare version string plus one newline, on a non-TTY stdout as well as a TTY.
- Text and json lanes are proven byte-equal on the version value by a committed test.
- `engram version --output bogus` exits 2.
- `.goreleaser.yaml` carries a `homebrew_casks:` entry whose post-install hook strips quarantine (macOS-guarded) before invoking the binary, raises on a version mismatch, and writes bash/zsh/fish completions; `hooks.post.uninstall` removes exactly those three files.
</success_criteria>

<output>
Create `.planning/phases/01-version-homebrew-distribution/01-01-SUMMARY.md` when done
</output>
