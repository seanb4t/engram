---
phase: 09-report-pending-in-migrate-status
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - cmd/engram/migrate_docs_test.go
  - docs-site/src/content/docs/guides/migrate.md
autonomous: true
requirements:
  - REQ-docs-record-state
  - REQ-migrate-status-histogram

estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "guides/migrate.md no longer claims that the CLI derives a pending-equivalent number from absent and buckets, and no longer claims the field is confined to the Connect lane — both claims are false once plan 09-01 lands, and the second was never true."
    - "The pending row survives the correction: the fix is a rewrite that states the arithmetic, never a deletion of the row and never a softening that admits the gap."
    - "EDGE/boundary: the rewritten pending row states the threshold exactly — absent plus every bucket STRICTLY below current_version, with buckets at current_version and every future bucket excluded — and it names both current_version and future by their table-row names."
    - "The rewritten row names all three reporting surfaces (engram migrate status in text and json, engram migration-status, and the Connect MigrateStatusResponse field 7) and names the one server-side definition all three read."
    - "The docs gate asserts ZERO remaining occurrences of each anchor, never a count of conversions, and it ships with a positive control that proves the gate discriminates a clean fixture from an injected violation."
    - "The protojson uint64-as-string paragraph immediately below the pending row is preserved unchanged — it describes an encoding difference, not a field-presence difference, and stays true after the correction."
    - statement: "EDGE/precision: pending is exact uint64 arithmetic with no rounding or tie-breaking — Total is a fresh exact count and never the derived sum, so the only representational difference across lanes is encoding (the CLI json lane emits a JSON number, the Connect lane emits a protojson string), which the preserved paragraph below the row already documents."
      verification: backstop
  prohibitions:
    - "The W3 correction must never be achieved by deleting or weakening the pending row — documenting the gap instead of closing it is the failure mode this phase exists to end."
  artifacts:
    - path: "cmd/engram/migrate_docs_test.go"
      provides: "the zero-occurrence docs gate, its shared violation helper, and the table-driven positive control"
      contains: "migrateGuidePendingRowViolations"
      min_lines: 60
    - path: "docs-site/src/content/docs/guides/migrate.md"
      provides: "the corrected pending row of the shared engram migrate status / engram migration-status field table"
      contains: "MigrateStatusResult.Pending()"
  key_links:
    - from: "cmd/engram/migrate_docs_test.go"
      to: "docs-site/src/content/docs/guides/migrate.md"
      via: "the gate reads the guide by a package-relative path and asserts on its content"
      pattern: "docs-site/src/content/docs/guides/migrate.md"
---

<objective>
Correct `docs-site/src/content/docs/guides/migrate.md`'s `pending` row (audit item **W3**) and put a
durable, self-tested gate behind the correction.

Purpose: the row makes two claims. "Connect lane only" becomes wrong the moment plan 09-01 lands;
"the CLI's own text and json output derive the equivalent number from `absent` and `buckets`
directly" was **never** true — `statusSummary` did none of that arithmetic and the operator did it by
hand. That second half is W3 standing alone. This plan replaces the row with an accurate one and
makes the false claims unable to return.

Output: a rewritten table row, plus `cmd/engram/migrate_docs_test.go` — the repo's second
docs-content gate, modelled on `cmd/engram/docsync_test.go`.

**Why wave 2.** The gate must not run before plan 09-01's code change lands, or it would pass against
prose that is momentarily true and code that is not.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/09-report-pending-in-migrate-status/09-CONTEXT.md
@.planning/phases/09-report-pending-in-migrate-status/09-RESEARCH.md
@.planning/phases/09-report-pending-in-migrate-status/09-PATTERNS.md
@.planning/phases/09-report-pending-in-migrate-status/09-VALIDATION.md
@.planning/phases/09-report-pending-in-migrate-status/09-01-SUMMARY.md

@cmd/engram/docsync_test.go
</context>

## Artifacts this phase produces

Every symbol this phase creates, across both plans. Nothing else is added.

| Artifact | Kind | File | Plan |
|---|---|---|---|
| `migrateStatusReportDoc.Pending` | Go struct field, `uint64` | `cmd/engram/migrate_family.go` | 09-01 |
| `pending` | JSON key (published in `--output json`) | `cmd/engram/migrate_family.go` | 09-01 |
| `TestMigrateFamilyStatusReportDocPendingNeverRederived` | Go test func | `cmd/engram/migrate_family_test.go` | 09-01 |
| `TestMigrateFamilyStatusSummaryPendingClause` | Go test func | `cmd/engram/migrate_family_test.go` | 09-01 |
| `cmd/engram/migrate_docs_test.go` | new file | — | 09-02 |
| `migrateGuideRelPath` | Go const | `cmd/engram/migrate_docs_test.go` | 09-02 |
| `migrateGuideStaleClaimAnchors` | Go var, `[]string` | `cmd/engram/migrate_docs_test.go` | 09-02 |
| `migrateGuidePendingRowViolations` | Go func | `cmd/engram/migrate_docs_test.go` | 09-02 |
| `TestMigrateGuidePendingRowIsAccurate` | Go test func | `cmd/engram/migrate_docs_test.go` | 09-02 |
| `TestMigrateGuidePendingRowGateFiresOnInjectedViolation` | Go test func (positive control) | `cmd/engram/migrate_docs_test.go` | 09-02 |

The multi-source coverage audit, capability-detector outcomes, and the spec-less probe accounting
table for the whole phase live in `09-01-PLAN.md` and are not restated here. This plan owns probe
edges 6 (boundary) and 7 (precision) and the third kept prohibition.

## Gate anchors (baseline measured 2026-08-22)

Both anchor strings occur **exactly once** across `docs-site/`, both on
`docs-site/src/content/docs/guides/migrate.md:279`. Neither is repeated in `upgrade.md` or under
`reference/`. The first anchor is chosen because it carries no verb and therefore cannot inflect —
the milestone audit already recorded a near-miss where searching for the singular verb form returned
nothing and read as "W3 fixed" while the sentence (plural subject, uninflected verb) was intact.

<!-- planner-discipline-allow: the equivalent number from -->
<!-- planner-discipline-allow: Connect lane only -->

| Anchor | Occurrences in `docs-site/` today | Why this anchor |
|---|---|---|
| `the equivalent number from` | 1 | noun phrase, no verb, cannot inflect |
| `Connect lane only` | 1 | fixed phrase, the second false claim |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: author the docs gate and observe it RED against the live guide</name>
  <precondition>Both anchor strings are still present exactly once in `docs-site/src/content/docs/guides/migrate.md`. Assert before starting: `A=$(rg -o -F 'the equivalent number from' docs-site/ | wc -l | tr -d ' '); B=$(rg -o -F 'Connect lane only' docs-site/ | wc -l | tr -d ' '); echo "a=$A b=$B"; test "$A" -eq 1 && test "$B" -eq 1`. If either is already 0, the RED observation this task depends on is impossible — halt and report rather than proceeding to a gate that has only ever been seen green.</precondition>
  <files>cmd/engram/migrate_docs_test.go</files>
  <read_first>
    - `cmd/engram/docsync_test.go` (whole file, 100 lines) — the repo's ONLY existing docs-content-assertion Go test. Copy three things verbatim in shape: the SPDX header (lines 1-2), the package-relative path constant idiom and its comment explaining why two levels up reaches the repo root (lines 13-18), and the `os.ReadFile` plus `os.IsNotExist` skip plus `t.Fatalf`-on-other-error plus fail-on-empty pattern (lines 68-79). Do NOT rename or rescope anything in this file — the new gate lands in a sibling file because `docsync_test.go` is scoped to the upgrade guide by its constant name and test name.
    - `docs-site/src/content/docs/guides/migrate.md:270-285` — the shared `engram migrate status` / `engram migration-status` field table, the current `pending` row at line 279, and the protojson paragraph at 281-284 that must survive untouched.
    - `.planning/phases/09-report-pending-in-migrate-status/09-CONTEXT.md` §`Acceptance gate shape` (D-07) — why the gate asserts zero occurrences on an inflection-free anchor and why it must ship with a positive control.
    - `.planning/2026-08-12.01-MILESTONE-AUDIT.md:125-134` — the two recorded near-miss false negatives, including the inflection miss on this exact sentence and the piped-`tail` exit-status trap.
    - `.planning/phases/09-report-pending-in-migrate-status/09-01-SUMMARY.md` — confirm plan 09-01 landed and that `pending` is now reported by the CLI, which is what makes the rewritten prose true.
  </read_first>
  <action>
Create `cmd/engram/migrate_docs_test.go` with the Apache-2.0 SPDX header (this is an in-scope Go file
and `task license:check` enforces it) in `package main`.

Declare `migrateGuideRelPath` as the package-relative path to the migrate guide, mirroring
`upgradeGuideRelPath`'s two-levels-up idiom and carrying the same explanatory comment.

Declare `migrateGuideStaleClaimAnchors` as a `[]string` holding the two anchor substrings from the
Gate anchors table above, each with a comment naming which false claim it pins and, for the first,
why an anchor containing a verb was rejected.

Declare `migrateGuidePendingRowViolations(doc string) []error` — one shared assertion function that
both tests call, so the positive control exercises the same code path as the real-file gate rather
than a parallel reimplementation. It returns one error per violation, checking five legs:

1. For each anchor in `migrateGuideStaleClaimAnchors`, a violation if `strings.Count(doc, anchor)` is
   anything other than zero. Gate on ZERO remaining occurrences, never on a count of conversions — a
   count gate silently passes when a new occurrence lands after planning, whereas a zero gate fails,
   which is correct.
2. A violation if no `pending` table row is present. Match it with a compiled regexp for a line
   beginning with a table cell whose content is the backticked word `pending`, and capture the whole
   line for legs 3 and 4. This closes the hole where deleting the row would satisfy leg 1.
3. A violation if the captured row does not contain `current_version`.
4. A violation if the captured row does not contain `future`.
5. A violation if the document no longer contains both `protojson` and `uint64`, which is how the
   encoding paragraph below the row is held in place.

Each error message must name the offending leg, the anchor or token involved, and
`migrateGuideRelPath`, so a failure reads as an instruction rather than a puzzle.

Add `TestMigrateGuidePendingRowIsAccurate`: read `migrateGuideRelPath`; on `os.IsNotExist` call
`t.Skipf` explaining that a trimmed checkout skips rather than passing silently; on any other read
error call `t.Fatalf`; call `t.Fatalf` if the file content is empty; then call
`migrateGuidePendingRowViolations` and `t.Error` each returned error.

Add `TestMigrateGuidePendingRowGateFiresOnInjectedViolation` — the D-07 positive control, table
driven over in-memory fixture strings so no file is ever mutated. Build one `cleanFixture` string
containing a well-formed `pending` row that names `current_version` and `future`, followed by a
sentence containing `protojson` and `uint64`; it must not contain either anchor. Cases and their
expectations:

| case | fixture | expect violation |
|---|---|---|
| `clean` | `cleanFixture` | no |
| `stale_derivation_claim` | `cleanFixture` with the first anchor appended | yes |
| `stale_connect_lane_claim` | `cleanFixture` with the second anchor appended | yes |
| `row_deleted` | empty string | yes |
| `row_omits_boundary` | `cleanFixture` with `current_version` removed from the row | yes |
| `row_omits_future_exclusion` | `cleanFixture` with `future` removed from the row | yes |
| `encoding_note_removed` | `cleanFixture` with the protojson sentence removed | yes |

Each case asserts `len(migrateGuidePendingRowViolations(fixture)) > 0` equals the expected boolean.
The `clean` case is as load-bearing as the violation cases: a gate that fires on everything
discriminates nothing.

Do NOT edit `docs-site/src/content/docs/guides/migrate.md` in this task. Run the gate against the
live, unmodified guide and OBSERVE `TestMigrateGuidePendingRowIsAccurate` fail on both anchor legs
while `TestMigrateGuidePendingRowGateFiresOnInjectedViolation` passes every case. Commit the red gate
on its own as `test(09-02): add failing docs gate for the stale pending-row claims`. This deliberate
red-then-green commit split closes the granularity half of the open broken windows recorded in
`STATE.md` (WINDOWS.md ids 1 and 2) rather than repeating it.
  </action>
  <verify>
    <automated>go test ./cmd/engram/... -run 'TestMigrateGuidePendingRow' -v -count=1 > /tmp/gsd-09-02-t1.log 2>&1; status=$?; FAILS=$(rg -o -- '--- FAIL: TestMigrateGuidePendingRowIsAccurate' /tmp/gsd-09-02-t1.log | wc -l | tr -d ' '); CTRL=$(rg -o -- '--- PASS: TestMigrateGuidePendingRowGateFiresOnInjectedViolation' /tmp/gsd-09-02-t1.log | wc -l | tr -d ' '); echo "exit=$status realfile_fails=$FAILS control_passes=$CTRL"; test "$status" -ne 0 && test "$FAILS" -ge 1 && test "$CTRL" -ge 8</automated>
  </verify>
  <acceptance_criteria>
    - Inverted gate: the `<verify>` command above exits 0 *because* `go test` exited NON-zero. `status` must not be 0 — a green run at this point means the gate is vacuous or the anchors were already gone, and the task must halt.
    - `realfile_fails` is at least 1: `TestMigrateGuidePendingRowIsAccurate` genuinely failed against the live guide, on both anchor legs. Record the two failure lines verbatim in the SUMMARY.
    - `control_passes` is at least 8: the positive control's parent plus all seven table cases passed, so the gate machinery is sound while the real-file assertion is red. Counted from the redirected file — `=== RUN` lines are stripped from rendered agent stdout and Go prefixes every subtest line with the parent's full name, so this count is a lower bound assertion rather than an equality.
    - Source assertion: `rg -c 'SPDX-License-Identifier: Apache-2.0' cmd/engram/migrate_docs_test.go` returns 1, and `task license:check` exits 0.
    - Source assertion: `migrateGuidePendingRowViolations` has exactly two callers, one per test — confirm with `rg -c 'migrateGuidePendingRowViolations' cmd/engram/migrate_docs_test.go` returning 3 (the declaration plus two call sites). A parallel reimplementation inside the control would make the control prove nothing about the real gate.
    - Source assertion: `rg -c -F 'the equivalent number from' docs-site/src/content/docs/guides/migrate.md` still returns 1 at the end of this task — the doc is deliberately untouched here.
    - `git diff --name-only` for this task's commit lists exactly `cmd/engram/migrate_docs_test.go`.
    - Never shape any check in this task as `go test ... | tail -N; echo $?`; that reports `tail`'s exit status, not the test's, and passes unconditionally.
  </acceptance_criteria>
  <done>
    `cmd/engram/migrate_docs_test.go` exists with the shared five-leg violation helper, the real-file
    gate, and the seven-case positive control; the real-file gate has been observed RED against the
    live guide with its two failure lines recorded, the control passed every case, and the red gate is
    committed on its own.
  </done>
</task>

<task type="auto">
  <name>Task 2: rewrite the `pending` row so the gate goes green</name>
  <files>docs-site/src/content/docs/guides/migrate.md</files>
  <read_first>
    - `docs-site/src/content/docs/guides/migrate.md:270-285` again, with the failure output from Task 1 in hand — the row to replace is the one the gate named.
    - `.planning/phases/09-report-pending-in-migrate-status/09-CONTEXT.md` §`Documentation correction` (D-04) — the three things the replacement must state, and the standing rule that the row belongs in the shared two-verb table (which is now accurate for the first time, since the heading always covered both verbs while the row claimed only one had the field).
    - `internal/store/migrate_status.go:64-84` — restate the arithmetic from the authority, not from memory: `absent` plus every bucket strictly below `current_version`, with `future` deliberately excluded because `pending` answers whether running `engram migrate` would do work.
    - `cmd/engram/client_migration_status.go:41-59` — why `engram migration-status` already reports `pending` in both its lanes, so the row can name it as a third surface truthfully.
    - `internal/server/connectapi.go:212` — `Pending: status.Pending()` on the Connect response, the field-7 surface.
  </read_first>
  <action>
Replace the single `pending` row of the shared field table. Prose is yours per D-04's discretion,
subject to five requirements the gate enforces: the row must remain a row of that table; it must
state the arithmetic including the *strictly below* boundary; it must name `current_version`; it must
name the `future` exclusion; and the document must retain its `protojson` / `uint64` encoding
sentence.

Two further requirements the gate cannot check but D-04 locks: the row must name all three reporting
surfaces (`engram migrate status` in both `text` and `json`, `engram migration-status`, and the
Connect `MigrateStatusResponse` field 7), and it must name `MigrateStatusResult.Pending()` as the one
server-side definition all three read.

A row satisfying all of the above, offered as a starting point rather than a mandate:

`absent` plus every bucket **strictly below** `current_version` — buckets sitting *at*
`current_version` and every `future` bucket are excluded, because `pending` answers "would running
`engram migrate` do work?". Reported by `engram migrate status` (both `text` and `json`), by
`engram migration-status`, and by the Connect `MigrateStatusResponse` (field 7); all three read the
same server-side `MigrateStatusResult.Pending()`, never a re-derivation.

Do not touch any other row of the table, do not touch the paragraph immediately below the row, and do
not re-litigate the rest of `guides/migrate.md` — the audit verified the remainder accurate. Do not
add a cross-link to `reference/memory-record.md`: that was left to discretion and is declined here,
because it adds no accuracy to the corrected claim and widens a deliberately narrow debt-closure
diff. Record that decision in the SUMMARY.

Commit as `docs(09-02): correct the pending row of the migrate status field table`.
  </action>
  <verify>
    <automated>go test ./cmd/engram/... -run 'TestMigrateGuidePendingRow' -v -count=1 > /tmp/gsd-09-02-t2.log 2>&1; status=$?; PASSES=$(rg -o -- '--- PASS' /tmp/gsd-09-02-t2.log | wc -l | tr -d ' '); FAILS=$(rg -o -- '--- FAIL' /tmp/gsd-09-02-t2.log | wc -l | tr -d ' '); A=$(rg -o -F 'the equivalent number from' docs-site/ | wc -l | tr -d ' '); B=$(rg -o -F 'Connect lane only' docs-site/ | wc -l | tr -d ' '); echo "exit=$status passes=$PASSES fails=$FAILS anchor_a=$A anchor_b=$B"; test "$status" -eq 0 && test "$FAILS" -eq 0 && test "$PASSES" -ge 10 && test "$A" -eq 0 && test "$B" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - Zero-occurrence gate: `anchor_a` and `anchor_b` are both 0 across the whole of `docs-site/`. Gated on zero remaining occurrences, not on "one row converted" — a count gate would silently pass if a new occurrence landed elsewhere in the tree.
    - Scope check: the two anchors remain findable in `cmd/engram/migrate_docs_test.go` (that is the gate's own fixture data), so the sweep is deliberately scoped to `docs-site/` and never run repo-wide.
    - Test command: the `<verify>` command exits 0 with zero `--- FAIL` lines and at least 10 `--- PASS` lines (both test parents plus the control's seven cases), counted from the redirected file.
    - Source assertion: `rg -c -F 'MigrateStatusResult.Pending()' docs-site/src/content/docs/guides/migrate.md` returns at least 1.
    - Source assertion: `rg -n 'protojson' docs-site/src/content/docs/guides/migrate.md` still returns the encoding paragraph, and `git diff docs-site/src/content/docs/guides/migrate.md` shows exactly one changed line.
    - CLI/behavior assertion: the row's claims are now true of the shipped binary — `pending` is present in `engram migrate status --output json`, in its `text` field table, in `engram migration-status`, and on the Connect response, all from the one store method. Plan 09-01's tests are the evidence; re-run them here with `go test ./cmd/engram/... -run 'TestMigrateFamilyStatusReportDocPendingNeverRederived|TestMigrateFamilyStatusSummaryPendingClause' -count=1` and confirm exit 0.
    - Constructed-defect re-check, recorded in the SUMMARY: re-inject either anchor into a scratch copy of the guide (`cp` to `/tmp`, append the anchor, point a throwaway run of `migrateGuidePendingRowViolations` at it via the control's own table) and confirm the gate still fires. The permanent positive control already encodes this; the SUMMARY records that it was observed, not assumed.
  </acceptance_criteria>
  <done>
    The `pending` row states the arithmetic, the `future` exclusion, all three reporting surfaces and
    the single definition; both stale anchors are at zero occurrences across `docs-site/`; the docs
    gate and both of plan 09-01's tests are green; and `git diff` on the guide shows exactly one
    changed line.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → `docs-site` build | `guides/migrate.md` is hand-authored markdown outside any generated or anchored region; no user input crosses this boundary and no build-time codegen consumes the row. |
| published documentation → operator | The row tells an operator what a reported number means; a wrong row is an integrity defect in operator decision support, which is exactly what W3 is. |
| test process → repo filesystem | The new gate reads one file by a package-relative path and never writes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-04 | Information Disclosure | `guides/migrate.md` `pending` row | low | accept | The corrected row publishes only the arithmetic of an already-published field (`MigrateStatusResponse` field 7, already documented in this same table). No new field, no new value, no new surface is disclosed. Accepted. |
| T-09-05 | Tampering | `migrateGuidePendingRowViolations` (the gate itself) | medium | mitigate | A gate written to close a vacuous-gate finding is the highest-risk gate in the phase. Mitigated three ways: the anchor is inflection-free so it cannot silently miss the sentence; the gate asserts zero occurrences rather than a conversion count; and the seven-case positive control — including a `clean` case — proves the gate discriminates, with Task 1 additionally observing it RED against the real defect before the fix lands. |
| T-09-06 | Repudiation | operator acting on a wrong `pending` reading | low | mitigate | Closed by the correction itself: the row now names the exclusion an operator is most likely to get wrong when checking the arithmetic by hand against the `buckets` and `future` rows directly above it. |
| T-09-SC | Tampering | npm/pip/cargo installs | low | accept | Zero package-manager installs in this phase — no `go.mod`, `go.sum`, `package.json` or lockfile change. `09-RESEARCH.md` §Package Legitimacy Audit records "not applicable". Nothing to audit, so no legitimacy checkpoint is earned. |

ASVS level 1, `security_block_on: high`. No threat in this register is rated high or critical, so
nothing here blocks the phase.
</threat_model>

<verification>
- `go test ./cmd/engram/... -count=1 > /tmp/gsd-09-02-pkg.log 2>&1; status=$?; FAILS=$(rg -o -- '^FAIL' /tmp/gsd-09-02-pkg.log | wc -l | tr -d ' '); echo "exit=$status fails=$FAILS"; test "$status" -eq 0 && test "$FAILS" -eq 0`
- `task test:go > /tmp/gsd-09-02-full.log 2>&1; status=$?; tail -40 /tmp/gsd-09-02-full.log; echo "exit=$status"; test "$status" -eq 0`
- `task lint > /tmp/gsd-09-02-lint.log 2>&1; status=$?; echo "exit=$status"; test "$status" -eq 0` — includes `lint:markdown` (rumdl) over the edited guide.
- `task license:check > /tmp/gsd-09-02-lic.log 2>&1; status=$?; echo "exit=$status"; test "$status" -eq 0` — the new Go test file must carry the SPDX header; `docs-site/**` is excluded by `.licenserc.yaml` and must NOT receive one.
- `task fmt:check > /tmp/gsd-09-02-fmt.log 2>&1; status=$?; echo "exit=$status"` — informational. Pre-existing dprint drift is out of scope for this phase; confirm only that neither file this plan touches is newly drifted.
- `git diff --name-only` across both tasks lists exactly `cmd/engram/migrate_docs_test.go` and `docs-site/src/content/docs/guides/migrate.md`. No `ui/` and no `charts/` file is touched, so `task ui:build` and `task chart:validate` are not required.
</verification>

<success_criteria>
- `the equivalent number from` and `Connect lane only` each occur zero times across `docs-site/`.
- The `pending` row still exists, states the strictly-below boundary, names `current_version` and the
  `future` exclusion, names all three reporting surfaces, and names `MigrateStatusResult.Pending()`.
- The protojson `uint64`-as-string paragraph below the row is byte-identical to its pre-phase state.
- `cmd/engram/migrate_docs_test.go` carries the SPDX header, was observed RED against the live guide
  before the fix, and is green after it.
- The positive control passes all seven cases, including the `clean` case that proves the gate does
  not simply fire on everything.
- `task test:go`, `task lint` and `task license:check` are green.
</success_criteria>

<output>
Create `.planning/phases/09-report-pending-in-migrate-status/09-02-SUMMARY.md` when done. It must
record: the two verbatim failure lines observed in Task 1's RED run, the final
`exit=/passes=/fails=/anchor_a=/anchor_b=` line from Task 2's verify command, the exact final wording
of the rewritten row, and the recorded decision not to cross-link `reference/memory-record.md`.
</output>
