---
phase: 09-report-pending-in-migrate-status
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/engram/migrate_family.go
  - cmd/engram/migrate_family_test.go
autonomous: true
requirements:
  - REQ-migrate-status-histogram
  - REQ-docs-record-state

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "engram migrate status reports pending in both lanes: the --output json document carries a pending key and the --output text field table carries a Pending row, sourced from store.MigrateStatusResult.Pending()."
    - "pending has exactly one arithmetic definition repo-wide (store.MigrateStatusResult.Pending()); cmd/engram adds a fourth CALL SITE and never a second loop, accumulator, or inlined copy."
    - "EDGE/adjacency: a bucket whose version is exactly migrate.CurrentVersion contributes nothing to the reported pending — only absent plus buckets STRICTLY below current_version do, and every future bucket is excluded."
    - 'EDGE/empty: a zero-valued store.MigrateStatusResult still marshals a "pending":0 key — never null, never an omitted key.'
    - "EDGE/ordering: pending is the LAST key of the marshalled report document and therefore the last row of the text field table; the six pre-existing keys keep their pre-existing positions and tags."
    - "EDGE/idempotency: statusReportDoc and statusSummary are pure — calling either twice on the same store.MigrateStatusResult returns identical output and leaves the input value unmutated (no in-place sort, dedupe, or slice rewrite of the caller's Buckets/Future)."
    - "The migrate status text headline emits a pending clause UNCONDITIONALLY (present at zero pending as well as non-zero), positioned after the per-bucket enumeration and before the future clause."
    - "The report struct stays hand-declared: no store type is embedded, so record content remains unreachable from this report by construction."
    - statement: "EDGE/concurrency: this phase adds no new concurrency surface — pending is derived in-process by a pure function from an already-fetched MigrateStatusResult, so the non-atomic three-RPC reconciliation plus single retry that Store.MigrateStatus already documents remains the only concurrency contract for migrate status."
      verification: backstop
  prohibitions:
    - "The migrate status report must never carry memory-record content — no record ids, owners, summaries, tags, or content — only counts and version numbers."
    - "pending must never be re-derived inside cmd/engram, not even as a verbatim copy of the store method's loop."
  artifacts:
    - path: "cmd/engram/migrate_family.go"
      provides: "migrateStatusReportDoc.Pending (json tag pending, appended last) plus the unconditional pending clause in statusSummary"
      contains: "res.Pending()"
    - path: "cmd/engram/migrate_family_test.go"
      provides: "the discriminating-fixture gate, the summary-clause gate, and the extended key-order want slice"
      contains: "TestMigrateFamilyStatusReportDocPendingNeverRederived"
  key_links:
    - from: "cmd/engram/migrate_family.go"
      to: "internal/store/migrate_status.go"
      via: "statusReportDoc and statusSummary each read the single arithmetic definition"
      pattern: "res.Pending()"
    - from: "cmd/engram/migrate_family.go"
      to: "cmd/engram/operator_view.go"
      via: "the json-tagged Pending field is rendered into the text field table by viewFields walking document order"
      pattern: 'json:"pending"'
---

<objective>
Land `pending` end-to-end in `engram migrate status` — the offline operator verb that milestone
`2026-08-12.01` left as the one surface not reporting the value it declared canonical (audit item
**W2**).

Purpose: close W2 by adding a *consumer* of `store.MigrateStatusResult.Pending()`, never a fifth
definition of its arithmetic. The method's own doc comment records that it exists to collapse the
half-applied N-site-invariant defect class this repo repeatedly hits; this plan adds the fourth call
site.

Output: one `uint64` struct field with a `pending` json tag, one converter line, one unconditional
headline clause, and the behavioural gates that prove the value came from the store method rather
than from a copied loop.

**This is debt closure, not new scope.** `REQ-migrate-status-histogram` and `REQ-docs-record-state`
are both already Complete in `REQUIREMENTS.md`. Nothing here re-implements or re-verifies the
original requirements — the plan carries both IDs because W2/W3 are one defect against them.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/09-report-pending-in-migrate-status/09-CONTEXT.md
@.planning/phases/09-report-pending-in-migrate-status/09-RESEARCH.md
@.planning/phases/09-report-pending-in-migrate-status/09-PATTERNS.md
@.planning/phases/09-report-pending-in-migrate-status/09-VALIDATION.md

@cmd/engram/migrate_family.go
@internal/store/migrate_status.go
</context>

## Artifacts this phase produces

Every symbol this phase creates, across both plans. Nothing else is added.

| Artifact | Kind | File | Plan |
|---|---|---|---|
| `migrateStatusReportDoc.Pending` | Go struct field, `uint64` | `cmd/engram/migrate_family.go` | 09-01 |
| `pending` | JSON key (published in `--output json`) | `cmd/engram/migrate_family.go` | 09-01 |
| `TestMigrateFamilyStatusReportDocPendingNeverRederived` | Go test func | `cmd/engram/migrate_family_test.go` | 09-01 |
| `TestMigrateFamilyStatusSummaryPendingClause` | Go test func | `cmd/engram/migrate_family_test.go` | 09-01 |
| `cmd/engram/migrate_docs_test.go` | new file | — | 09-02 |
| `migrateGuideRelPath` | Go const | `cmd/engram/migrate_docs_test.go` | 09-02 |
| `migrateGuideStaleClaimAnchors` | Go var, `[]string` | `cmd/engram/migrate_docs_test.go` | 09-02 |
| `migrateGuidePendingRowViolations` | Go func | `cmd/engram/migrate_docs_test.go` | 09-02 |
| `TestMigrateGuidePendingRowIsAccurate` | Go test func | `cmd/engram/migrate_docs_test.go` | 09-02 |
| `TestMigrateGuidePendingRowGateFiresOnInjectedViolation` | Go test func (positive control) | `cmd/engram/migrate_docs_test.go` | 09-02 |

No new file is created by plan 09-01. No proto change, no RPC change, no MCP tool-schema change, no
new flag, no new command, no package install.

## Multi-source coverage audit (covers plans 09-01 and 09-02)

**GOAL** — `.planning/ROADMAP.md` §`### Phase 9`

| Item | Status | Where |
|---|---|---|
| G1 add `pending` to the CLI report struct | COVERED | 09-01 T1 |
| G2 add `pending` to the text summary | COVERED | 09-01 T2 |
| G3 via the single existing `Pending()` definition, never a re-derivation | COVERED | 09-01 T1 |
| G4 correct the doc sentence to match | COVERED | 09-02 T2 |

**REQ** — `.planning/REQUIREMENTS.md`

| Item | Status | Where |
|---|---|---|
| REQ-migrate-status-histogram (already Complete — debt closure only) | COVERED | 09-01, 09-02 |
| REQ-docs-record-state (already Complete — debt closure only) | COVERED | 09-02, 09-01 |

**RESEARCH** — `09-RESEARCH.md`

| Item | Status | Where |
|---|---|---|
| R1 append-scalar / append-never-interleave field order | COVERED | 09-01 T1 |
| R2 `cur := int(migrate.CurrentVersion)` fixture convention | COVERED | 09-01 T1 |
| R3 discriminating fixture arithmetic (97 vs 137 vs 142 vs 102) | COVERED | 09-01 T1 |
| R4 inflection-free doc-gate anchor | COVERED | 09-02 T1 |
| R5 Open Question 1 — where the doc gate lives | COVERED | 09-02 T1 (new sibling file; `docsync_test.go` is upgrade-guide-scoped) |
| R6 Open Question 2 — final test names | COVERED | this plan set (names fixed above) |
| R7 file-redirect exit-status hazard | COVERED | every `<verify>` in both plans |
| R8 arithmetic stays in `internal/store` (responsibility map) | COVERED | 09-01 T1 |
| R9 `TestOperatorDocsAreHandDeclared` unaffected by a scalar field | COVERED | 09-01 T1 verify |
| R10 Package Legitimacy Audit — not applicable, zero installs | COVERED | `<threat_model>` T-09-SC, both plans |

**CONTEXT** — `09-CONTEXT.md` locked decisions

| Item | Status | Where |
|---|---|---|
| D-01 `Pending uint64` appended LAST | COVERED | 09-01 T1 |
| D-02 value is `res.Pending()`, one call, never re-derived | COVERED | 09-01 T1 |
| D-03 unconditional pending clause between buckets and future | COVERED | 09-01 T2 |
| D-04 doc row rewritten to name all three surfaces | COVERED | 09-02 T2 |
| D-05 behavioural discriminating fixture, never a `Pending()` grep | COVERED | 09-01 T1 |
| D-06 extend `TestMigrateFamilyStatusReportDocKeyOrder`'s `want` | COVERED | 09-01 T1 |
| D-07 zero-occurrence doc gate, inflection-free anchor, positive control | COVERED | 09-02 T1 |
| D-08 re-check the `operatorViewFixtures()` migrate-status entries | COVERED | 09-01 T1 |
| Discretion: clause wording | COVERED | 09-01 T2 (one constraint: the clause names `pending`) |
| Discretion: doc row prose | COVERED | 09-02 T2 |
| Discretion: plan decomposition | COVERED | 2 plans, 2 waves — doc gate never runs before the code lands |
| Discretion: link the row to `reference/memory-record.md` | COVERED | 09-02 T2 — decided NOT to add; keeps the debt-closure diff minimal and adds no accuracy |

**No item is MISSING.** Deferred Ideas (W1, W4, `schema_version` proto typing, dprint drift, the
pending-todo file move) are excluded by CONTEXT.md and are not gaps.

## Capability detector outcomes (recorded, not skipped)

| Detector | Result | Evidence |
|---|---|---|
| API Coverage Decision Checkpoint | **NOT-APPLICABLE** | `api-coverage.cjs --json` over the phase scope returned `{"detected":false,"signals":[]}`. This phase integrates no external API/SDK/service — it adds one `uint64` field to a CLI report struct and rewrites one documentation table row. No `COVERAGE.md` is produced. |
| Assumption-Delta Architecture Checkpoint | **NOT-APPLICABLE** | `assumption-delta scan 9 --json` returned `{"detected":false,"signals":[]}`. No singular→plural, required→optional, or derived→chosen transition: `pending` gains a fourth *consumer* of an existing single definition; the identity model is untouched. |
| Schema Push Detection Gate | **NOT-APPLICABLE** | No ORM schema files exist in this repo (Go + Qdrant; no `prisma/`, `drizzle/`, `supabase/`, `src/collections/`, `src/entities/`). No `[BLOCKING]` push task is injected. |

## Spec-less probe accounting (no silent drops)

The phase has no SPEC.md, so the deterministic edge probe ran with `EDGE_ABSENT`/`PROHIB_ABSENT` set.
7 applicable edges, all `unresolved` on input. Resolution across the plan set:

| # | Requirement | Category | Resolution | Authored as | Plan |
|---|---|---|---|---|---|
| 1 | REQ-migrate-status-histogram | adjacency | resolved (explicit) | `must_haves.truths` plain string (EDGE/adjacency) | 09-01 |
| 2 | REQ-migrate-status-histogram | empty | resolved (explicit) | `must_haves.truths` plain string (EDGE/empty) | 09-01 |
| 3 | REQ-migrate-status-histogram | ordering | resolved (explicit) | `must_haves.truths` plain string (EDGE/ordering) | 09-01 |
| 4 | REQ-migrate-status-histogram | idempotency | resolved (explicit) | `must_haves.truths` plain string (EDGE/idempotency) | 09-01 |
| 5 | REQ-migrate-status-histogram | concurrency | resolved (backstop) | `must_haves.truths` structured `{statement, verification: backstop}` | 09-01 |
| 6 | REQ-docs-record-state | boundary | resolved (explicit) | `must_haves.truths` plain string (EDGE/boundary) | 09-02 |
| 7 | REQ-docs-record-state | precision | resolved (backstop) | `must_haves.truths` structured `{statement, verification: backstop}` | 09-02 |

**Equality check:** 7 applicable == 7 authored into `must_haves` + 0 surfaced as flagged assumptions.
No edge was dropped.

**Prohibition recall (§B, in-prompt).** Stage 1 over-produced ~9 candidates across the two
requirements; Stage 2 dropped the routine-engineering ones (DRY-for-its-own-sake, "must not reorder
existing keys" — already the ordering edge, "must not re-litigate audited-accurate doc content").
Three survived and are authored **descriptor-less** (two here, one in 09-02). Canon-referral
breadcrumb: *generic OWASP excessive-data-exposure is canon — covered by `/gsd-secure-phase` plus the
existing tier-wide `TestOperatorDocsAreHandDeclared` gate; not minted here.* The kept
information-disclosure prohibition is the bespoke, repo-specific form (memory-record content
specifically), which no generic tool would catch.

<tasks>

<task type="tracer">
  <name>Task 1: `pending` lands end-to-end — store method through the converter into both render lanes</name>
  <reversibility rating="costly">`pending` becomes a published key in an operator verb's `--output json` contract (D-01); removing or renaming it later breaks any script that adopted it.</reversibility>
  <files>cmd/engram/migrate_family.go, cmd/engram/migrate_family_test.go</files>
  <read_first>
    - `cmd/engram/migrate_family.go:291-337` — `migrateStatusReportDoc` and `statusReportDoc`. Read the struct's doc comment BEFORE editing: it states the field-order invariant D-01 preserves ("its first five fields reproduce `store.MigrateStatusResult`'s own keys, tags, and order exactly") and why this struct is hand-declared rather than embedding the store type. `statusReportDoc`'s nil-to-empty-slice normalization must survive the edit unchanged.
    - `internal/store/migrate_status.go:44-84` — `MigrateStatusResult` and `Pending()`. Read `Pending()`'s doc comment IN FULL: it states the arithmetic (`Absent` plus every bucket strictly below `migrate.CurrentVersion`), states that `Future` is deliberately EXCLUDED and why, and names the half-applied N-site-invariant defect class it exists to close. Also read `MigrateStatusResult`'s comment for why `Total` is a fresh exact count and never the derived sum.
    - `internal/migrate/migrate.go:54` — `CurrentVersion Version = 1`. This is why the fixture must be built `cur`-relative and why a bucket at `cur - 1` (version 0) is the "below current" leg.
    - `internal/store/migrate_status_test.go:21-53` — `TestMigrateStatusResultPending` and its zero-value sibling: the `cur := int(migrate.CurrentVersion)` fixture convention the new test must follow, and the comment explaining why a literal fixture silently tests the wrong partition once `CurrentVersion` advances.
    - `cmd/engram/migrate_family_test.go:400-438` — `TestMigrateFamilyStatusReportDocNeverMarshalsNull` (must keep passing, do not modify) and `TestMigrateFamilyStatusReportDocKeyOrder` (whose `want` slice D-06 extends).
    - `cmd/engram/operator_view_test.go:35-91` — `jsonTopLevelKeys` and `orderedKeyDiff`. `orderedKeyDiff` errors on length mismatch AND on every positional difference, which is why appending one string to `want` is a sufficient gate and why it must NOT be relaxed to a subset check.
    - `cmd/engram/operator_view.go:20-80` — `viewField` and `viewFields`. `viewFields` marshals the doc once and walks the resulting object in document order, so struct order is render order and one json-tagged field covers both lanes.
    - `cmd/engram/operator_view_migrate_test.go:57-66` — the `"migrate status"` view fixtures (D-08). Confirm they build through `statusReportDoc` and therefore pick up the new field automatically.
    - `cmd/engram/operator_output_test.go:326-360` and `:495-525` — `TestOperatorDocsAreHandDeclared` (a `uint64` scalar does not trip it; embedding the store type would) and `TestOperatorOutputEmpty`'s inline comment explaining why `"migrate status"` is deliberately absent from that map.
  </read_first>
  <action>
Append a single field `Pending uint64` with json tag `pending` to `migrateStatusReportDoc`, placed
after `CurrentVersion` and after no other field (D-01). Do not reorder, rename, or retag any
existing field. Do not embed `store.MigrateStatusResult`. Extend the struct's doc comment with one
sentence recording that `Pending` is appended last for the same append-never-interleave reason
`CurrentVersion` was, and that its value is read from the store method rather than computed here.

In `statusReportDoc`, populate the new field from `res.Pending()` — one call, no local accumulator,
no loop over `res.Buckets`, no reference to `res.FutureTotal`, no comparison against
`migrate.CurrentVersion` (D-02). Leave the `buckets` and `future` nil-to-empty-slice normalization
exactly as it stands.

Append `pending` as the seventh and final element of `TestMigrateFamilyStatusReportDocKeyOrder`'s
`want` slice (D-06). Do not relax that test to a subset check and do not touch `orderedKeyDiff`.

Add one new test function named `TestMigrateFamilyStatusReportDocPendingNeverRederived` to
`cmd/engram/migrate_family_test.go`, placed immediately after
`TestMigrateFamilyStatusReportDocKeyOrder`, with five subtests. Build the shared fixture with
`cur := int(migrate.CurrentVersion)` and never with literal version numbers: `Absent` 88, `Buckets`
holding `{Version: cur - 1, Count: 9}` then `{Version: cur, Count: 40}`, `Future` holding
`{Version: cur + 1, Count: 5}`, `FutureTotal` 5, `Total` 142.

- `equals_store_pending` — assert `statusReportDoc(res).Pending` equals `res.Pending()` and that the
  value is 97.
- `fixture_discriminates_every_naive_rederivation` — compute the three named naive formulas from the
  same fixture (absent plus every bucket regardless of version, which is 137 and is also what a
  non-strict boundary comparison yields; that value plus `FutureTotal`, which is 142; `Total` minus
  the at-current bucket's count, which is 102) and assert each differs from `res.Pending()` and from
  the other two. This subtest is what keeps the fixture discriminating if someone later simplifies
  its counts — it fails loudly instead of silently degrading into a fixture a copied loop could pass.
- `zero_value_marshals_pending_zero` — marshal `statusReportDoc(store.MigrateStatusResult{})` and
  assert the output contains a pending key whose value is zero, proving the key is emitted rather
  than omitted for the empty input.
- `converter_is_pure` — call `statusReportDoc` twice on the fixture, assert the two results are
  `reflect.DeepEqual`, and assert the input `store.MigrateStatusResult` still deep-equals a
  separately constructed identical value, so the converter cannot sort, dedupe, or otherwise mutate
  the caller's slices.
- `renders_last_in_both_lanes` — this is the end-to-end leg. Take `viewFields` over the doc and
  assert the final element's `Key` is `pending` and its `Label` is `Pending`; render the doc through
  `renderOperatorView` into a buffer and assert the rendered text carries the `Pending` label and the
  value 97; encode the doc with a `json.Encoder` into a second buffer and assert the json lane
  carries a pending key with value 97. One fixture, both lanes, the value taken from the store
  method — that is the whole slice proven in one assertion set.

Give the test a doc comment naming D-02/D-05 and stating why the fixture discriminates.

Do not add a `"migrate status"` entry to `TestOperatorOutputEmpty` (D-08). Do not modify
`TestMigrateFamilyStatusReportDocNeverMarshalsNull`. Re-check the `operatorViewFixtures()`
`"migrate status"` entries before editing rather than after: they build through `statusReportDoc` so
they inherit the new field, but confirm by search that no test in `cmd/engram` compares rendered
migrate-status output against a hand-written expected block or a fixed field count; if one exists,
update it in this task rather than leaving it red.

One-time constructed-defect observation, recorded in the SUMMARY: after the tests are green,
temporarily replace the `res.Pending()` call in `statusReportDoc` with the naive
absent-plus-every-bucket sum, run the task's verify command, and record the observed failing
assertion line verbatim; then `git checkout -- cmd/engram/migrate_family.go` is NOT sufficient (it
would also revert the field) — revert only that one line by hand and confirm with `git diff` that the
`res.Pending()` call is back before committing. This is the evidence that the new gate is wired to
production code, which the pairwise-distinctness subtest alone does not prove.
  </action>
  <verify>
    <automated>LOG=/tmp/gsd-09-01-t1.log; go test ./cmd/engram/... -run 'TestMigrateFamilyStatusReportDocPendingNeverRederived|TestMigrateFamilyStatusReportDocKeyOrder|TestMigrateFamilyStatusReportDocNeverMarshalsNull|TestOperatorViewIdentityAcrossEveryOperatorCommand|TestOperatorDocsAreHandDeclared|TestMigrateViewIdentity|TestOperatorOutputEmpty' -v -count=1 > "$LOG" 2>&1; status=$?; NEW=$(rg -o -- '--- PASS: TestMigrateFamilyStatusReportDocPendingNeverRederived' "$LOG" | wc -l | tr -d ' '); FAILS=$(rg -o -- '--- FAIL' "$LOG" | wc -l | tr -d ' '); PRE=0; for t in TestMigrateFamilyStatusReportDocKeyOrder TestMigrateFamilyStatusReportDocNeverMarshalsNull TestOperatorViewIdentityAcrossEveryOperatorCommand TestOperatorDocsAreHandDeclared TestMigrateViewIdentity TestOperatorOutputEmpty; do rg -q -- "--- PASS: $t" "$LOG" && PRE=$((PRE+1)) || true; done; echo "exit=$status new=$NEW pre=$PRE fails=$FAILS"; test "$status" -eq 0 && test "$FAILS" -eq 0 && test "$NEW" -ge 6 && test "$PRE" -eq 6</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `rg -c -F 'Pending uint64' cmd/engram/migrate_family.go` returns exactly one line, and that line is the last field line of `migrateStatusReportDoc` — the line immediately following it is the struct's closing brace.
    - Source assertion: `rg -v '^\s*//' cmd/engram/migrate_family.go | rg -c -F 'res.Pending()'` returns 1 after this task. Derivation: exactly one call site (`statusReportDoc`), none elsewhere, = 1; Task 2's clause is what takes it to 2. Comment lines are stripped FIRST and deliberately — this task's action extends the struct doc comment with a sentence about reading the store method, and an unfiltered `rg -c` would count that prose and make a correct implementation read as 2.
    - Source assertion (no re-derivation): `sed -n '/^func statusReportDoc/,/^}/p' cmd/engram/migrate_family.go | rg -v '^\s*//' > /tmp/gsd-09-01-conv.txt; rg -c -e 'for ' -e '\+=' /tmp/gsd-09-01-conv.txt || echo 0` returns 0. Derivation: the correct converter is a straight-line struct literal — 0 loop lines and 0 accumulator lines. `rg -c` exits 1 and prints nothing when there is no match, hence the `|| echo 0`. Separately confirm the only `migrate.CurrentVersion` reference inside the extracted body is the pre-existing `CurrentVersion: int(migrate.CurrentVersion)` line.
    - Behavior assertion: `statusReportDoc(res).Pending == res.Pending() == 97` for the fixture above, while the three named naive formulas yield 137, 142 and 102 — four pairwise-distinct values. Derivation from the fixture (`Absent` 88; buckets `{cur-1: 9}`, `{cur: 40}`; future `{cur+1: 5}`; `FutureTotal` 5; `Total` 142): `Pending()` = 88 + 9 = 97; absent-plus-every-bucket = 88 + 9 + 40 = 137; that plus `FutureTotal` = 137 + 5 = 142; `Total` minus the at-current bucket = 142 - 40 = 102. The fixture is internally consistent because 88 + 9 + 40 + 5 = 142 = `Total`.
    - Behavior assertion: `viewFields(statusReportDoc(res))`'s last element has `Key` `pending` and `Label` `Pending`; the json lane emits a pending key with value 97; the text lane renders a `Pending` row carrying 97.
    - Behavior assertion: a zero-valued `store.MigrateStatusResult` marshals a pending key with value 0 (present, not omitted, not null).
    - Test command: the `<verify>` command above exits 0, reports `fails=0`, `new` at least 6, and `pre` exactly 6. Both thresholds are derived from the test structure this task specifies, not measured historically. `new >= 6` = 1 parent PASS line + 5 subtest PASS lines (`equals_store_pending`, `fixture_discriminates_every_naive_rederivation`, `zero_value_marshals_pending_zero`, `converter_is_pure`, `renders_last_in_both_lanes`) = 6. `pre == 6` counts DISTINCT pre-existing test FUNCTIONS from the `-run` set that emitted a PASS line; six are named there, so all six must have run and passed.
    - Why there is deliberately no total-PASS-line floor: an absolute floor across this `-run` set drifts with unrelated work landing on `cmd/engram` (`TestOperatorViewIdentityAcrossEveryOperatorCommand` alone carries many subtests), so it would false-RED a correct implementation the moment someone else adds a table case. `pre == 6` proves the same non-vacuity property — every named test actually ran — while counting functions, which this plan controls, rather than a subtest census it does not.
    - Non-vacuity: `new` and `pre` are both read from the redirected file, never from rendered stdout. A bare `go test -run <name>` exits 0 with `[no tests to run]`, so a green console alone is not evidence.
    - Recorded evidence: the SUMMARY quotes the failing assertion line observed when `res.Pending()` was temporarily swapped for the naive sum, and confirms `git diff` showed the call restored before commit.
    - `TestMigrateFamilyStatusReportDocNeverMarshalsNull` and `TestOperatorOutputEmpty` are byte-identical to their pre-task state (`git diff --stat` shows changes only in the two files this task owns).
  </acceptance_criteria>
  <done>
    `migrateStatusReportDoc` carries `Pending uint64` as its final field with json tag `pending`;
    `statusReportDoc` populates it from a single `res.Pending()` call; the key-order gate's `want`
    ends with `pending`; the new discriminating test passes all five subtests; and the verify command
    exits 0 with a confirmed non-vacuous run.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: unconditional pending clause in the `migrate status` headline</name>
  <files>cmd/engram/migrate_family.go, cmd/engram/migrate_family_test.go</files>
  <read_first>
    - `cmd/engram/migrate_family.go:339-358` — `statusSummary` and its doc comment. Read the doc comment first: it records D-04's standing rule that this sentence is *additive prose* over the field table, that `--output json` (not this sentence) is the contract, and that the per-future-version enumeration was deliberately moved out of this sentence into the field table. The new clause must not enumerate.
    - `cmd/engram/migrate_family.go:288` — the single call site, `renderOperator(cmd, format, statusSummary(res), statusReportDoc(res))`. Both of this plan's edits converge here.
    - `internal/store/migrate_status.go:64-84` — `Pending()` again, for the same reason as Task 1: the clause reads the method, never a second loop.
    - `.planning/phases/09-report-pending-in-migrate-status/09-CONTEXT.md` §`Text headline` (D-03) — why the clause is unconditional while the future clause stays conditional, and why the order is total, absent, buckets, pending, future.
  </read_first>
  <behavior>
    - For the discriminating fixture (`Pending()` is 97), the headline contains `97`, positioned after
      the last per-bucket count (`40`) and before the future clause's count (`5`).
    - For a zero-valued `store.MigrateStatusResult` the headline still carries a pending clause — the
      clause is emitted when `Pending()` is 0, so "no pending clause" can never mean "this binary
      predates the fix".
    - The clause names the field: its text contains the lowercase token `pending`. That is the ONLY
      wording constraint; punctuation, phrasing and word order stay discretionary per D-04.
    - The three ordering tokens (`40`, `97`, `5`) each occur exactly once in the rendered headline, so
      the positional assertion cannot silently mis-index. Derivation against the current sentence
      (`migrate status: 142 record(s) total, 88 absent (never migrated), 9 at v0, 40 at v1` + the new
      pending clause + `; 5 record(s) at a version newer than this binary's current target (v1)` at
      `cur` = 1): the only digit runs present are 142, 88, 9, 0, 40, 1, 97, 5, 1 — so `40`, `97` and
      `5` each appear exactly once and neither is a substring of another token.
    - That uniqueness depends on `migrate.CurrentVersion`, because the fixture renders `v{cur-1}`,
      `v{cur}` and `v{cur+1}`. It holds for `cur` = 1 (today) and breaks first at `cur` = 4, where
      `v5` would collide with the future-count token. `ordering_tokens_are_unambiguous` is what makes
      that a loud failure rather than a silent mis-index, so its failure message must name
      `migrate.CurrentVersion` as the likely cause and instruct the maintainer to re-pick the fixture
      counts — never to weaken the exactly-once assertion or drop the positional subtest.
  </behavior>
  <action>
Add one `fmt.Fprintf` call to `statusSummary`'s existing `strings.Builder`, positioned after the
per-bucket `for` loop and before the `if len(res.Future) > 0` block (D-03). Emit it
**unconditionally** — no length check, no zero check. Read the value from `res.Pending()`; do not
re-derive it and do not reuse a local from the bucket loop.

Wording is yours within two constraints: the clause must contain the lowercase token `pending`, and
it must introduce no decimal digits other than the pending count itself (so the fixture's ordering
tokens stay unambiguous). Do not enumerate per-version detail — that belongs in the field table.
Extend `statusSummary`'s doc comment with one sentence recording that the pending clause is
unconditional by design while the future clause remains conditional, and why.

Add one new test function named `TestMigrateFamilyStatusSummaryPendingClause` to
`cmd/engram/migrate_family_test.go`, immediately after the test Task 1 added, with three subtests.
Reuse the same `cur`-relative fixture shape as Task 1 (`Absent` 88; buckets at `cur - 1` count 9 and
`cur` count 40; future at `cur + 1` count 5; `FutureTotal` 5; `Total` 142; `Pending()` 97).

- `ordering_tokens_are_unambiguous` — assert `strings.Count` of each of `40`, `97` and `5` in the
  rendered headline is exactly 1. Run this subtest FIRST in the file so that a wording change which
  makes a token ambiguous fails here with a clear message rather than corrupting the positional
  assertion below.
- `states_pending_value_between_buckets_and_future` — assert `strings.Index` of `40` is less than
  `strings.Index` of `97`, which is less than `strings.Index` of `5`, and that all three indices are
  non-negative. This encodes D-03's ordering without pinning any wording.
- `emitted_unconditionally_at_zero` — call `statusSummary(store.MigrateStatusResult{})` and assert
  the lower-cased result contains `pending`. This is the unconditionality gate: a clause emitted only
  when the count is non-zero fails here.

Give the test a doc comment naming D-03 and D-04, stating explicitly that it asserts on values and
positions rather than on the sentence's wording, because the sentence carries no stability contract.
  </action>
  <verify>
    <automated>LOG=/tmp/gsd-09-01-t2.log; go test ./cmd/engram/... -run 'TestMigrateFamilyStatusSummaryPendingClause|TestMigrateFamilyStatusReportDocPendingNeverRederived|TestMigrateFamilyStatusReportDocKeyOrder' -v -count=1 > "$LOG" 2>&1; status=$?; NEW=$(rg -o -- '--- PASS: TestMigrateFamilyStatusSummaryPendingClause' "$LOG" | wc -l | tr -d ' '); PRIOR=$(rg -o -- '--- PASS: TestMigrateFamilyStatusReportDocPendingNeverRederived' "$LOG" | wc -l | tr -d ' '); ORDER=$(rg -o -- '--- PASS: TestMigrateFamilyStatusReportDocKeyOrder' "$LOG" | wc -l | tr -d ' '); FAILS=$(rg -o -- '--- FAIL' "$LOG" | wc -l | tr -d ' '); echo "exit=$status new=$NEW prior=$PRIOR order=$ORDER fails=$FAILS"; test "$status" -eq 0 && test "$FAILS" -eq 0 && test "$NEW" -ge 4 && test "$PRIOR" -ge 6 && test "$ORDER" -ge 1</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `rg -v '^\s*//' cmd/engram/migrate_family.go | rg -c -F 'res.Pending()'` returns 2 after this task. Derivation: 1 call site in `statusReportDoc` (Task 1) + 1 in `statusSummary` (this task) = 2. Comments are stripped first for the same reason as Task 1 — both tasks extend a doc comment that names the store method, and an unfiltered count would read a correct implementation as 3 or 4.
    - Source assertion (position): extract the function with `sed -n '/^func statusSummary/,/^}/p' cmd/engram/migrate_family.go | rg -v '^\s*//' > /tmp/gsd-09-01-sum.txt`, then confirm the line number of the `res.Pending()` call is greater than the line number of the bucket `for` line and less than the line number of the `if len(res.Future)` line, using `rg -n -- 'for _, bucket|res.Pending\(\)|if len\(res.Future\)' /tmp/gsd-09-01-sum.txt`. Derivation: that command must print exactly 3 numbered lines, in the order bucket-loop, pending, future-guard.
    - Source assertion (unconditional): the `res.Pending()` `Fprintf` in `statusSummary` is not inside any `if` block — the extracted function shows it at the same indentation depth as the opening `fmt.Fprintf`.
    - CLI/behavior assertion: `statusSummary` over the fixture renders `40` before `97` before `5`, each exactly once (uniqueness derived in `<behavior>` above from the fixture's full digit inventory at `cur` = 1); `statusSummary(store.MigrateStatusResult{})` contains the lowercase token `pending`.
    - Test command: the `<verify>` command above exits 0 and reports `fails=0`, `new` at least 4, `prior` at least 6, `order` at least 1 — every count read from the redirected file, never from rendered stdout, and each derived from the test structure rather than from a historical measurement. `new >= 4` = 1 parent + 3 subtests (`ordering_tokens_are_unambiguous`, `states_pending_value_between_buckets_and_future`, `emitted_unconditionally_at_zero`). `prior >= 6` = Task 1's test, 1 parent + 5 subtests. `order >= 1` = `TestMigrateFamilyStatusReportDocKeyOrder`, one function with no subtests. The three tests are counted SEPARATELY rather than summed, so a shortfall in one can never be masked by a surplus in another.
    - Constructed-defect check, recorded in the SUMMARY: temporarily wrap the new clause in `if res.Pending() > 0 {` and confirm `emitted_unconditionally_at_zero` goes RED; then remove the wrapper and confirm green again. A gate observed only passing is not evidence.
    - No test anywhere in `cmd/engram` asserts `statusSummary`'s exact sentence text — confirm with `rg -n 'migrate status: ' cmd/engram/*_test.go` returning no full-sentence literal.
  </acceptance_criteria>
  <done>
    `statusSummary` emits a pending clause unconditionally between the bucket enumeration and the
    future clause, reading `res.Pending()`; `TestMigrateFamilyStatusSummaryPendingClause` passes all
    three subtests; the unconditionality gate has been observed RED against a constructed
    zero-suppressing defect and green after its removal.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell → local CLI process | An operator runs `engram migrate status` with their own configured credentials. No untrusted network input crosses into this report; the verb is offline and operator-tier. |
| Qdrant collection payload → operator report doc | Stored record payloads are aggregated server-side by `Store.MigrateStatus`; only counts and version numbers cross into the rendered report. |
| report doc → operator terminal | Rendered through the shared `renderOperator` / `viewFields` path into a possibly-TTY writer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Information Disclosure | `migrateStatusReportDoc` (`cmd/engram/migrate_family.go`) | medium | mitigate | Add a `uint64` scalar only; never embed `store.MigrateStatusResult`. The tier-wide `TestOperatorDocsAreHandDeclared` (`cmd/engram/operator_output_test.go:326-360`) keeps record content unreachable by construction — a scalar field does not trip it, embedding would. That test is in Task 1's `<verify>` set, so the mitigation is proven green in this plan rather than assumed. |
| T-09-02 | Tampering | rendered text field table (`viewFields` / `sanitizeViewValue`) | low | accept | `pending` is a `uint64`, never a free-form string; `sanitizeViewValue` only runs on JSON string values (`cmd/engram/operator_view.go:206-222`), so no control-character or terminal-escape injection path is added. Accepted: no new mitigation needed. |
| T-09-03 | Denial of Service | `statusReportDoc` / `statusSummary` | low | accept | Both are pure in-process functions over an already-fetched result; `Pending()` is a single pass over an already-materialised bucket slice. No new I/O, no new allocation loop, no unbounded input. Accepted. |
| T-09-SC | Tampering | npm/pip/cargo installs | low | accept | Zero package-manager installs in this phase — no `go.mod`, `go.sum`, `package.json` or lockfile change. `09-RESEARCH.md` §Package Legitimacy Audit records "not applicable". Nothing to audit, so no legitimacy checkpoint is earned. |

ASVS level 1, `security_block_on: high`. No threat in this register is rated high or critical, so
nothing here blocks the phase.
</threat_model>

<verification>
- `go test ./cmd/engram/... -count=1 > /tmp/gsd-09-01-pkg.log 2>&1; status=$?; FAILS=$(rg -o -- '^FAIL' /tmp/gsd-09-01-pkg.log | wc -l | tr -d ' '); echo "exit=$status fails=$FAILS"; test "$status" -eq 0 && test "$FAILS" -eq 0`
- `task test:go > /tmp/gsd-09-01-full.log 2>&1; status=$?; tail -40 /tmp/gsd-09-01-full.log; echo "exit=$status"; test "$status" -eq 0` — full Go suite. Never shape this as `task test:go | tail; echo $?`; that reports `tail`'s status and passes unconditionally.
- `task lint > /tmp/gsd-09-01-lint.log 2>&1; status=$?; echo "exit=$status"; test "$status" -eq 0`
- `task license:check > /tmp/gsd-09-01-lic.log 2>&1; status=$?; echo "exit=$status"; test "$status" -eq 0` — both files this plan touches already carry the SPDX header; this confirms neither lost it.
- `git diff --name-only` lists only `cmd/engram/migrate_family.go` and `cmd/engram/migrate_family_test.go`. No `ui/` and no `charts/` file is touched, so `task ui:build` and `task chart:validate` are not required for this plan.
</verification>

<success_criteria>
- `migrateStatusReportDoc` has exactly seven fields, with `Pending uint64` (`json:"pending"`) last.
- `pending` appears in `--output json` and as a `Pending` row last in the `--output text` field table.
- `res.Pending()` is called exactly twice in `cmd/engram/migrate_family.go` (counted over non-comment
  lines: one in `statusReportDoc`, one in `statusSummary`) and the pending arithmetic appears nowhere
  else in `cmd/engram`.
- The `migrate status` headline carries a pending clause for every input, including a zero-valued
  result, positioned between the bucket enumeration and the future clause.
- Both new tests pass, both were observed RED against a constructed defect, and both runs were
  confirmed non-vacuous by counting `--- PASS` lines in a redirected file.
- `task test:go`, `task lint` and `task license:check` are green.
</success_criteria>

<output>
Create `.planning/phases/09-report-pending-in-migrate-status/09-01-SUMMARY.md` when done. It must
record: the verbatim failing assertion observed for each of the two constructed-defect checks, the
final echo line from each task's verify command (`exit=/new=/pre=/fails=` for Task 1,
`exit=/new=/prior=/order=/fails=` for Task 2), and the exact wording chosen for the headline clause
(discretionary per D-04, so the SUMMARY is where it is on record).
</output>
