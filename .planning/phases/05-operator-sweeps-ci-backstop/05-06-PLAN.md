---
phase: 05-operator-sweeps-ci-backstop
plan: 06
type: execute
wave: 6
depends_on:
  - 05-01
  - 05-02
  - 05-03
  - 05-04
  - 05-05
files_modified:
  - internal/store/redevidence_harness_test.go
  - .planning/REQUIREMENTS.md
  - .planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/03-INVENTORY.md
  - .planning/phases/05-operator-sweeps-ci-backstop/red-evidence/05-05-backstop-appended-after-caller-options.patch
  - .planning/phases/05-operator-sweeps-ci-backstop/red-evidence/05-01-scanspine-view-unbudgeted.patch
  - .planning/phases/05-operator-sweeps-ci-backstop/red-evidence/05-01-citations-view-unbudgeted.patch
  - .planning/phases/05-operator-sweeps-ci-backstop/red-evidence/05-01-purge-view-unbudgeted.patch
  - .planning/phases/05-operator-sweeps-ci-backstop/red-evidence/05-02-revert-preview-view-unbudgeted.patch
  - .planning/phases/05-operator-sweeps-ci-backstop/red-evidence/05-03-migrate-sweep-view-unbudgeted.patch
  - .planning/phases/05-operator-sweeps-ci-backstop/red-evidence/05-03-migrate-pass-sentinel-escapes-as-failure.patch
  - .planning/phases/05-operator-sweeps-ci-backstop/red-evidence/05-03-revert-pass-sentinel-escapes-as-failure.patch
  - .planning/phases/05-operator-sweeps-ci-backstop/red-evidence/05-04-summarize-limit-sentinel-escapes.patch
  - .planning/phases/05-operator-sweeps-ci-backstop/red-evidence/05-04-reindex-walks-store-collection-not-source.patch
  - .planning/phases/05-operator-sweeps-ci-backstop/red-evidence/05-04-reindex-final-partial-page-dropped.patch
autonomous: true
requirements:
  - REQ-bounded-read-mechanism
  - REQ-sweeps-bounded
  - REQ-recv-limit-backstop
  - REQ-ci-store-green

estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`redEvidenceDirs` gains exactly one new entry, keyed by this phase's red-evidence directory, mapping eleven patches to their target tests; the Phase 1, Phase 2, Phase 3 and Phase 4 entries are byte-unchanged"
    - "Each patch is ONE behavioral mutation that still compiles and makes its named target test itself report a failure — never a build break — hand-verified by apply, run, revert before it is registered"
    - "The registered directions cover every guarantee this phase adds: each migrated sweep's budgeted view (four spine sweeps, the revert preflight, the migrate sweep, the reindex walk), each sweep's own early-stop sentinel being distinguishable from a real failure (migrate, revert apply, summarize), the reindex walk reading its effective source rather than the store's collection, its final partial page being flushed, and — the phase's highest-value direction — the receive-limit backstop appended ahead of caller options rather than after"
    - "`TestRedEvidencePatchesAreLive` passes with fifty-three confirmed RED lines (Phase 1's four, Phase 2's eight, Phase 3's thirteen, Phase 4's seventeen, this phase's eleven) and leaves every mutated file clean"
    - "The inventory's four closing checks are re-run and their real output recorded, including every difference between the derivation's live output and the recorded table — a difference is reconciled in writing, never rounded off"
    - "`Store.reindexTargetContents` is moved to Exempt with a written justification, because it routes through no shared primitive: its batch fetch is bounded by the size of the page its caller accumulates"
    - "GitHub #497 is closed with a comment recording the D-07 reasoning — that it is a CI stability question rather than an overflow one, that #498 fixed the root cause and has held for more than sixty runs, and that this phase's fixtures are bounded by the same budgets as Phases 1, 3 and 4's — with no CI workflow change and no new test policing fixture sizes"
    - "This phase's four requirement checkboxes are ticked and their traceability rows read complete; NO other phase's checkbox, verification document or context document is touched"
    - "`task` exits 0 for the whole repository and `go test ./internal/keylinks/ -count=1` exits 0 with every executed plan's key links satisfiable"
    - "No red-evidence patch carries a licence header, and no planning artifact gains a version-bearing heading"
    - statement: "The `internal/store` CI job stays green with this milestone's oversized fixtures in place, with no connection-refused or unavailable recurrence"
      verification: backstop
  artifacts:
    - path: "internal/store/redevidence_harness_test.go"
      provides: "the redEvidenceDirs entry for this phase's eleven patches"
      contains: "05-operator-sweeps-ci-backstop/red-evidence"
    - path: ".planning/phases/05-operator-sweeps-ci-backstop/red-evidence/05-05-backstop-appended-after-caller-options.patch"
      provides: "RED evidence that the receive-limit backstop is appended before caller options, so no test client inherits it"
      contains: "qdrantDialOptions"
    - path: ".planning/phases/05-operator-sweeps-ci-backstop/red-evidence/05-04-reindex-walks-store-collection-not-source.patch"
      provides: "RED evidence that the reindex walk reads the caller's effective source collection"
      contains: "scrollAllPoints"
    - path: ".planning/REQUIREMENTS.md"
      provides: "this phase's four requirements marked complete in both the checklist and the traceability table"
      contains: "REQ-recv-limit-backstop"
  key_links:
    - from: "internal/store/redevidence_harness_test.go"
      to: ".planning/phases/05-operator-sweeps-ci-backstop/red-evidence/05-05-backstop-appended-after-caller-options.patch"
      via: "the harness applies the phase's highest-value patch and requires its target test to fail"
      pattern: "05-05-backstop-appended-after-caller-options[.]patch"
    - from: "internal/store/redevidence_harness_test.go"
      to: ".planning/phases/05-operator-sweeps-ci-backstop/red-evidence/05-04-reindex-walks-store-collection-not-source.patch"
      via: "the wrong-collection direction, the one failure mode that would otherwise report a confident wrong success"
      pattern: "TestReindexSourceOverride"
  prohibitions:
    - "MUST NOT register a patch that has not been hand-proven to make its own target test fail — a patch that breaks the build, or whose target passes with it applied, is evidence of nothing and makes the gate a false comfort"
    - "MUST NOT edit another phase's verification document, context document or requirement checkbox to make this phase's gate green; the one sanctioned cross-phase edit is the call-site inventory, whose own closing-check section instructs this phase to re-run and reconcile it"
    - "MUST NOT close the CI issue without recording the reasoning that justifies closing it, and MUST NOT substitute a workflow change, a post-merge observation gate or a fixture-size test for that reasoning (D-07 is an explicit user decision)"
    - "MUST NOT raise a timeout in the task runner or in CI to make the red-evidence gate pass"
---

<objective>
Make every guarantee this phase added a live, harness-checked RED proof, reconcile the call-site
inventory against what the tree actually does, close the CI issue on the evidence that already
exists, and close the phase on a green full gate.

This is the phase's LAST plan. It registers the phase's entry in the red-evidence map after every
other plan has shipped, exactly as plans 03-06 and 04-08 did for the previous two phases, and marks
this phase's four requirements complete.

Decisions covered: D-01 and D-04 get patches reverting each migrated sweep's budgeted view; D-01's
sentinel mechanism gets three patches proving an early stop is distinguishable from a failure;
D-05 and D-06 get the append-order patch, the phase's highest-value direction; D-07 is satisfied by
recording the reasoning and closing the issue, with no new test and no CI change.

Purpose: all four of this phase's requirements proven RED-able for the rest of the open milestone.

Output: eleven patch files, one harness entry, one reconciled inventory, one closed issue, and four
ticked requirement boxes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/03-INVENTORY.md
@.planning/phases/04-list-listscheduled-search-bounded-reads/04-08-SUMMARY.md
@.planning/phases/05-operator-sweeps-ci-backstop/05-01-SUMMARY.md
@.planning/phases/05-operator-sweeps-ci-backstop/05-02-SUMMARY.md
@.planning/phases/05-operator-sweeps-ci-backstop/05-03-SUMMARY.md
@.planning/phases/05-operator-sweeps-ci-backstop/05-04-SUMMARY.md
@.planning/phases/05-operator-sweeps-ci-backstop/05-05-SUMMARY.md
@CLAUDE.md

<interfaces>
`internal/store/redevidence_harness_test.go`: `var redEvidenceDirs = map[string]map[string]string{…}`
— one top-level entry per phase directory (repo-root-relative), each mapping
`"<patch file>": "<TargetTest>"` with a trailing `// reverts: …` comment. There are four entries
today, holding forty-two patches between them.
`TestRedEvidencePatchesAreLive` (skipped under `-short`) globs `<dir>/*.patch`, requires set equality
with the mapping, and per patch: refuses to run if a touched file is already dirty, `git apply --check`,
`git apply`, `go test -run '^Target$' -count=1 ./...` (which must exit NON-zero), `git apply -R`, then
asserts the touched files are clean. It logs
`confirmed RED: <patch> applied -> <Target> failed as expected`. It must never run concurrently with
itself (its doc comment records a 2026-08-16 incident).

Per-patch authoring procedure (Phase 1/2/3/4 precedent): make the ONE mutation on a clean tree, write
`git diff -- <file> > <dir>/<name>.patch`, then prove it —
`git apply --check <patch> && git apply <patch> && go test -run '^Target$' -count=1 ./<pkg>/ -v`
shows the target's OWN failure line (a compile error is NOT acceptable) — then `git apply -R <patch>`
and `git status --porcelain -- <file>` prints nothing. Each patch has exactly one `diff --git` header
and no licence header. Never use a git stash; to inspect a pristine file use `git show HEAD:<path>`
into the scratchpad.

`.planning/REQUIREMENTS.md`: the `### Test Harness` and `### Bounded Qdrant Reads` checklists, and the
`## Traceability` table whose rows read `| Requirement | Phase | Status |`. At the start of this plan
thirteen requirement boxes are ticked and seven are not; four of those seven belong to this phase.

`.planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/03-INVENTORY.md`: the
twenty-seven-row call-site table and the `## Phase 5 closing check` section holding checks (a) through
(d) with their exact commands. Check (a)'s derivation is an `awk` pass over non-test `internal/store`
files matching a direct `s.client.{Scroll,ScrollAndOffset,Query,QueryBatch,Get,Count}(` call or an
`s.scrollAllPoints(` call, reduced to `Type.Method` names.

GitHub: `gh issue view 497` and `gh issue view 498` for the CI-stability history;
`.github/workflows/ci.yaml` lines 25-60 for the shared `services:` Qdrant and its explicit health
check. Neither the workflow nor any test in it is modified by this plan.
</interfaces>

<inventory_drift_already_verified>
The orchestrator's planner ran check (a) against the tree at planning time. Do not assume the
inventory is currently in sync — it is not, and the drift predates this phase:

- The derivation prints **26** names; the table has **27** rows.
- `Store.ListScheduled` and `Store.listByCursor` are in the TABLE but not in the derivation. Phase 4
  moved both onto the ordered-page primitive, so neither emits directly any more. They still satisfy
  check (c) — they route through `Store.scrollOrderedPage` — but they are invisible to the
  derivation as written.
- `Store.fetchPayloadBatch` is in the DERIVATION but not in the table. It is Phase 4's two-phase
  search payload fetch (`internal/store/searchfetch.go`), a `Scroll` over an id-set filter.
- Extending the derivation to also match `s.scrollOrderedPage(` does NOT fully close the gap: it
  re-adds `Store.listByCursor` and pulls in `Store.collectOrderedPages` and `Store.walkOffsetPrefix`,
  while `Store.ListScheduled` composes `collectOrderedPages` one level further down and stays
  invisible. A one-line command cannot express transitive reachability, and this plan does not ask
  it to.

Task 3 reconciles all of this in writing. It does NOT get to round any of it off, and it does not
get to declare the check passed by editing the command until the output matches.
</inventory_drift_already_verified>

<runtime_contingency>
Pre-authorized; applies only if it fires.

The harness's nested `go test -run '^Target$' -count=1 ./...` recompiles and starts every package per
patch. Plan 03-06 measured the whole `internal/store` package at 111 seconds with twenty-five patches
registered and its narrowing contingency did not fire; plan 04-08 reached forty-two. This phase
reaches fifty-three, and — unlike earlier phases — most of this phase's own targets seed real
oversized Qdrant fixtures, so the per-patch cost is materially higher than any previous phase's.

If, and only if, the run genuinely exceeds its timeout inside `TestRedEvidencePatchesAreLive`: add a
helper that walks the module (skipping the version-control, dependency, docs, console and planning
directories) for the `_test.go` file declaring the target function, return that package path, pass it
instead of the whole-module pattern, fail the subtest when zero or more than one package matches, and
update the harness doc comment's nested-run paragraph. Record before and after durations in the
SUMMARY if it fires. NEVER raise a timeout in the task runner or in CI to make the gate pass.
</runtime_contingency>
</context>

## Artifacts this phase produces (plan 05-06's share)

The full phase ledger is in `05-01-PLAN.md`. This plan owns:

| Kind | Artifact | Created by |
|------|----------|------------|
| Directory | `.planning/phases/05-operator-sweeps-ci-backstop/red-evidence/` | Task 1 |
| Patch | `05-05-backstop-appended-after-caller-options.patch` | Task 1 |
| Patches | the remaining ten, listed in Task 2 | Task 2 |
| Harness entry | this phase's `redEvidenceDirs` entry | Tasks 1-2 |
| Reconciliation | the inventory's Phase 5 closing-check results and its reclassified rows | Task 3 |
| Issue | GitHub #497 closed with the recorded reasoning | Task 3 |
| Checkboxes | this phase's four requirement boxes and their traceability rows | Task 3 |
| Conditional | the harness's package-narrowing helper, only if the runtime contingency fires | Task 2 |

## Executor notes (every task)

- NEVER run any `git stash` subcommand. To inspect a pristine file use `git show HEAD:<path>` into
  the scratchpad instead.
- Commit with an explicit pathspec (rule `n6m4as49mr`), Conventional Commits, and the session's
  attribution trailer.
- Every patch is authored and proven on a CLEAN tree, one at a time; never run two harness or patch
  cycles concurrently.
- Patches under `.planning/**` carry NO licence header, and no planning artifact gains a
  version-bearing heading.
- Real-Qdrant targets run with `ENGRAM_REQUIRE_QDRANT=1`; a skipped target is not a RED.
- If `task` shows a failure this plan did not cause, STOP and report it rather than editing unrelated
  code — and never edit another phase's verification or context document to make it green.
- After any `roadmap update-plan-progress` or `state.advance-plan`, audit
  `git diff -U0 -- .planning/ROADMAP.md` and revert any hunk touching a milestone other than
  `2026-09-18.01`.

<tasks>

<task type="tracer">
  <name>Task 1: One RED proof end to end — the receive-limit backstop's append order, registered and confirmed by the harness</name>
  <files>internal/store/redevidence_harness_test.go, .planning/phases/05-operator-sweeps-ci-backstop/red-evidence/05-05-backstop-appended-after-caller-options.patch</files>
  <precondition>Plans 05-01 through 05-05 are executed (their SUMMARY files exist), the working tree is clean (`git status --porcelain` prints nothing), and Docker is reachable (`docker info` exits 0) or `ENGRAM_QDRANT_TEST_ADDR` names a live Qdrant gRPC endpoint.</precondition>
  <read_first>
    - `internal/store/redevidence_harness_test.go` in full — the map, the harness, its tree-safety and empty-map guards, and its concurrency warning.
    - `.planning/phases/04-list-listscheduled-search-bounded-reads/04-08-SUMMARY.md` and one of that phase's patches — the shape precedent.
    - `internal/store/store.go` after plan 05-05 — `qdrantDialOptions`' body and the comment recording the append order.
    - `internal/store/qdrantbackstop_test.go` — both tests, so the mutation is aimed at an assertion that exists.
    - `.planning/phases/05-operator-sweeps-ci-backstop/05-05-SUMMARY.md` — the shipped identifier names, before writing any mutation.
  </read_first>
  <action>
Create `.planning/phases/05-operator-sweeps-ci-backstop/red-evidence/` and author
`05-05-backstop-appended-after-caller-options.patch` by the per-patch procedure: in
`internal/store/store.go`, move the dial-option append that carries the default call options so that
it runs AFTER the caller's own options instead of before them. The function still compiles, the
option is still applied, and the only thing that changes is which option wins.

Target `TestQdrantRecvLimitBackstopPrecedesCallerOptions`, whose last-element identity assertion then
fails. Prove it RED by hand and record the observed failure line.

This is the phase's highest-value direction, and the reason it goes first: with the order reversed,
every oversized regression in this milestone silently runs at the raised ceiling and none of them
turns red. The gate's whole job here is to make that mutation impossible to land unnoticed.

Register it: add ONE new top-level entry to `redEvidenceDirs`, keyed by this phase's red-evidence
directory path, mapping the patch name to its target test with a trailing `// reverts:` comment
naming what the mutation undoes and its decision ids. Leave the Phase 1, 2, 3 and 4 entries and
everything else untouched.

Run the harness once, alone (verify). Commit as
`test(store): register phase 5 red-evidence for the receive-limit backstop order` with pathspec
`internal/store/redevidence_harness_test.go .planning/phases/05-operator-sweeps-ci-backstop/red-evidence`.
  </action>
  <verify>
    <automated>out=$(ENGRAM_REQUIRE_QDRANT=1 go test ./internal/store/ -run '^TestRedEvidencePatchesAreLive$' -count=1 -v -timeout 120m 2&gt;&amp;1); rc=$?; printf '%s\n' "$out" | rg -e 'confirmed RED|^\s*--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e 'confirmed RED: ' | wc -l)" -eq 43 &amp;&amp; { dirty=$(git status --porcelain -- internal/store/store.go) || exit 1; test -z "$dirty"; }</automated>
    <fails_when>non-zero exit, a `--- FAIL:` line, a `confirmed RED:` count other than 43 (the earlier phases' forty-two plus this one), or `internal/store/store.go` left dirty after the run</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -o -e '^diff --[g]it a/' .planning/phases/05-operator-sweeps-ci-backstop/red-evidence/05-05-backstop-appended-after-caller-options.patch | wc -l` prints `1`, and its only `diff --git` line names `internal/store/store.go`.
    - `rg -n -F '".planning/phases/05-operator-sweeps-ci-backstop/red-evidence"' internal/store/redevidence_harness_test.go` prints exactly one line.
    - With `C` the commit SHA of this task (record it in the SUMMARY): `d=$(git diff "$C^..$C" -- internal/store/redevidence_harness_test.go) || exit 1; printf '%s\n' "$d" | rg -c -e '^-[^-]'` prints `0` — the task only adds lines.
    - `rg -l 'SPDX-License-Identifier' .planning/phases/05-operator-sweeps-ci-backstop/red-evidence/` prints nothing.
    - The SUMMARY records the hand-verified failure line for `TestQdrantRecvLimitBackstopPrecedesCallerOptions`.
  </acceptance_criteria>
  <done>The phase's highest-value RED direction is a registered, harness-confirmed patch; the harness passes with forty-three confirmed REDs and leaves the tree clean; committed with an explicit pathspec.</done>
</task>

<task type="auto">
  <name>Task 2: The remaining ten RED directions registered and the harness green at fifty-three</name>
  <files>internal/store/redevidence_harness_test.go, .planning/phases/05-operator-sweeps-ci-backstop/red-evidence/05-01-scanspine-view-unbudgeted.patch, .planning/phases/05-operator-sweeps-ci-backstop/red-evidence/05-01-citations-view-unbudgeted.patch, .planning/phases/05-operator-sweeps-ci-backstop/red-evidence/05-01-purge-view-unbudgeted.patch, .planning/phases/05-operator-sweeps-ci-backstop/red-evidence/05-02-revert-preview-view-unbudgeted.patch, .planning/phases/05-operator-sweeps-ci-backstop/red-evidence/05-03-migrate-sweep-view-unbudgeted.patch, .planning/phases/05-operator-sweeps-ci-backstop/red-evidence/05-03-migrate-pass-sentinel-escapes-as-failure.patch, .planning/phases/05-operator-sweeps-ci-backstop/red-evidence/05-03-revert-pass-sentinel-escapes-as-failure.patch, .planning/phases/05-operator-sweeps-ci-backstop/red-evidence/05-04-summarize-limit-sentinel-escapes.patch, .planning/phases/05-operator-sweeps-ci-backstop/red-evidence/05-04-reindex-walks-store-collection-not-source.patch, .planning/phases/05-operator-sweeps-ci-backstop/red-evidence/05-04-reindex-final-partial-page-dropped.patch</files>
  <precondition>Task 1 is committed, the working tree is clean, and Docker is reachable (`docker info` exits 0) or `ENGRAM_QDRANT_TEST_ADDR` names a live Qdrant gRPC endpoint.</precondition>
  <read_first>
    - `internal/store/redevidence_harness_test.go` after Task 1.
    - Each mutation site in the shipped source: `internal/store/spine.go`, `internal/store/revert.go`, `internal/store/migrate.go`, `internal/store/summarize.go`, `internal/store/store.go`.
    - Each target test file, so every mutation is aimed at an assertion that exists: `internal/store/spinesweeps_oversized_test.go`, `revertpreview_oversized_test.go`, `migratesweep_oversized_test.go`, `summarizesweep_oversized_test.go`, `reindexsweep_oversized_test.go`, and `reindex_test.go` for the source-override target.
    - Every target name re-resolved with `go test -list` before it is relied on: a `-run` pattern matching nothing exits 0 with `no tests to run` and reports a permanent false green (durable record `bsbsvn4hbc`).
    - This plan's `<runtime_contingency>` block.
  </read_first>
  <action>
Author ten patches in the same directory by the per-patch procedure, each the smallest mutation that
makes its target test itself fail while the tree still compiles. Note that with the count-only view
constructor deleted in plan 05-02, a "make this view unbudgeted" mutation is written as a `readView`
composite literal with a zero `maxRecordBytes`, which still compiles.

1. `05-01-scanspine-view-unbudgeted.patch` → `TestScanSpineBoundedOverGRPCLimit` (D-01, D-04):
   give the spine scan a zero-ceiling full-payload view, so its per-RPC record count returns to the
   spine batch size.
2. `05-01-citations-view-unbudgeted.patch` → `TestEnumerateCitationsBoundedOverGRPCLimit` (D-04):
   the same for the citation enumeration.
3. `05-01-purge-view-unbudgeted.patch` → `TestPreviewPurgeBoundedOverGRPCLimit` (D-04): the same for
   the purge preflight.
4. `05-02-revert-preview-view-unbudgeted.patch` → `TestRevertPreviewBoundedOverGRPCLimit` (D-01,
   D-04): the same for the revert preflight.
5. `05-03-migrate-sweep-view-unbudgeted.patch` → `TestMigrateBoundedOverGRPCLimit` (D-01): the same
   for the migrate sweep-mode pass.
6. `05-03-migrate-pass-sentinel-escapes-as-failure.patch` → `TestMigrateBoundedOverGRPCLimit` (D-01):
   delete the call site's unwrap of the migrate pass sentinel, so a completed batch is returned to
   the caller as a sweep failure. The sentinel stays declared and returned, so the tree compiles.
7. `05-03-revert-pass-sentinel-escapes-as-failure.patch` → `TestRevertApplyBoundedOverGRPCLimit`
   (D-01): the same for the revert apply pass.
8. `05-04-summarize-limit-sentinel-escapes.patch` → `TestSummarizeMissingBoundedOverGRPCLimit`
   (D-01): the same for the summarize sweep's caller-limit sentinel, which its own limit sub-case
   then observes as an error rather than a completion.
9. `05-04-reindex-walks-store-collection-not-source.patch` → `TestReindexSourceOverride` (D-01, and
   the wrong-collection threat): make the reindex walk pass the store's configured collection instead
   of the effective source, so a source override silently scans nothing.
10. `05-04-reindex-final-partial-page-dropped.patch` → `TestReindexBoundedOverGRPCLimit` (D-01):
    remove the flush of the accumulator's final partial page, so the last records of a scope are
    silently skipped.

Register all ten in the phase entry Task 1 created, each with a trailing `// reverts:` comment naming
what the mutation undoes and its decision ids. Change nothing else in the harness.

If any named direction turns out not to make its target fail — for example because the target's
fixture never reaches the mutated branch — STOP and report which one and why, rather than dropping it
silently or retargeting it to a test that fails for an unrelated reason. A direction that cannot be
proven is a finding, not a rounding error.

Run the harness once, alone, then the full gate (verify). Apply the pre-authorized runtime
contingency only if the run genuinely times out inside the harness.

Commit as `test(store): register phase 5's remaining red-evidence` with pathspec
`internal/store/redevidence_harness_test.go .planning/phases/05-operator-sweeps-ci-backstop/red-evidence`.
  </action>
  <verify>
    <automated>out=$(ENGRAM_REQUIRE_QDRANT=1 go test ./internal/store/ -run '^TestRedEvidencePatchesAreLive$' -count=1 -v -timeout 180m 2&gt;&amp;1); rc=$?; printf '%s\n' "$out" | rg -e 'confirmed RED|^\s*--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e 'confirmed RED: ' | wc -l)" -eq 53 &amp;&amp; { dirty=$(git status --porcelain -- internal/store) || exit 1; test -z "$dirty"; }</automated>
    <fails_when>non-zero exit, a `--- FAIL:` line, a `confirmed RED:` count other than 53, a subtest reporting that a target test PASSED with its patch applied, or any mutated source file left dirty after the run</fails_when>
    <automated>ls .planning/phases/05-operator-sweeps-ci-backstop/red-evidence/*.patch | wc -l | rg -x '11'</automated>
    <fails_when>non-zero exit — the directory holds a number of patches other than eleven, so a named direction was dropped or an extra one added without a mapping</fails_when>
    <automated>for p in .planning/phases/05-operator-sweeps-ci-backstop/red-evidence/*.patch; do n=$(rg -o -e '^diff --[g]it a/' "$p" | wc -l); test "$n" -eq 1 || { echo "MULTI-FILE PATCH: $p ($n)"; exit 1; }; done; echo "all single-file"</automated>
    <fails_when>non-zero exit, or a `MULTI-FILE PATCH:` line — a patch touches more than one file and is therefore not one behavioural mutation</fails_when>
  </verify>
  <acceptance_criteria>
    - `ls .planning/phases/05-operator-sweeps-ci-backstop/red-evidence/*.patch | wc -l` prints `11`.
    - `rg -l 'SPDX-License-Identifier' .planning/phases/05-operator-sweeps-ci-backstop/red-evidence/` prints nothing; `task license:check` exits 0.
    - The phase entry maps exactly the eleven patch names, and the earlier phases' entries are unchanged: with `C` the Task 1 commit SHA recorded in the SUMMARY, `d=$(git diff "$C^" -- internal/store/redevidence_harness_test.go) || exit 1; printf '%s\n' "$d" | rg -o -e '^-\s+"0[1234]-' | wc -l` prints `0`.
    - Every target name in the phase entry appears in `go test -list '.*' ./internal/store/... ./internal/server/...` output; record the resolution in the SUMMARY.
    - The SUMMARY carries a table of all eleven patches with each target's hand-verified failure line, and states whether the runtime contingency fired (with before and after durations if it did).
    - `git status --porcelain` after the final commit shows no modified tracked file.
  </acceptance_criteria>
  <done>Eleven hand-verified patches are registered, the harness confirms fifty-three REDs with a clean tree, and every guarantee this phase added has a live failing direction.</done>
</task>

<task type="auto">
  <name>Task 3 (LAST task of the phase): The inventory reconciled, the CI issue closed on its existing evidence, and the four requirements marked complete</name>
  <files>.planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/03-INVENTORY.md, .planning/REQUIREMENTS.md</files>
  <precondition>Task 2 is committed, the whole `internal/store` suite passes including the red-evidence harness, the working tree is clean, and `gh auth status` exits 0 (the CI issue cannot be closed otherwise — halt and report rather than skipping the close).</precondition>
  <read_first>
    - `.planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/03-INVENTORY.md` in full — the twenty-seven-row table, the derivation command, and all four closing checks with their exact wording.
    - This plan's `<inventory_drift_already_verified>` block.
    - `internal/store/searchfetch.go` — `fetchPayloadBatch`, so its new inventory row describes what actually ships rather than a guess, together with the Phase 4 SUMMARYs that built it.
    - `internal/store/store.go` around `reindexTargetContents` after plan 05-04 — what now bounds its batch.
    - `.planning/REQUIREMENTS.md` in full — both checklists, the traceability table and its Status vocabulary, and the coverage counts at the end.
    - `gh issue view 497` and `gh issue view 498` — the CI-stability history the closing comment cites.
    - `.planning/phases/05-operator-sweeps-ci-backstop/05-CONTEXT.md`, decision D-07, for the exact reasoning to record.
  </read_first>
  <action>
Three pieces of closing work, in this order.

**First, reconcile the inventory.** Run all four closing checks verbatim from the inventory's own
`## Phase 5 closing check` section and capture their real output.

Check (a) will not match the table as recorded, and the differences are already known — read this
plan's `<inventory_drift_already_verified>` block before running it. Reconcile the table to what the
tree actually does, in writing:
- Add a row for the two-phase search's id-set payload fetch, assigned to Phase 4, with a justification
  describing what bounds it as shipped.
- Annotate the two Phase 4 rows the derivation no longer sees, recording that each now composes the
  ordered-page primitive rather than emitting directly, and that the derivation command — a
  single-level textual match — cannot express that composition.
- Move `Store.reindexTargetContents` to Exempt with a written justification: it routes through no
  shared primitive, and its batch fetch is bounded by the size of the page its caller accumulates
  before flushing.
- Restate in writing that the near-duplicate `QueryBatch` stays Exempt because it requests no payload
  at all, which decision D-04 asked this phase to confirm rather than inherit.
If the honest reconciliation requires changing the derivation command itself, that is allowed — but
record the change and its reasoning beside the command, and never edit the command merely until its
output matches the table.

Append a `## Phase 5 closing-check results` section to the inventory recording each check's command,
its captured output, and the verdict. Do not add any heading carrying a version marker. Change
nothing else in that file and nothing at all elsewhere under the Phase 3 directory.

**Second, close the CI issue.** Close GitHub #497 with a comment recording D-07's reasoning: it is a
CI stability question — Qdrant refusing connections when four testcontainers contended for a
two-vCPU runner — rather than an overflow question; #498 fixed the root cause by moving the job onto
one shared service container with an explicit health check, and has held for more than sixty runs;
and this phase's fixtures are bounded by the same budgets as Phases 1, 3 and 4's, which self-assert
that they exceed the named limit. Cite this phase's green full gate. Do NOT change
`.github/workflows/ci.yaml`, do NOT add a post-merge observation step, and do NOT add a test policing
fixture sizes — all three are explicitly excluded by D-07. Record the comment text in the SUMMARY.

**Third, mark the requirements complete.** Query the requirement readiness helper before marking
anything, since a requirement shared with another phase must not be ticked here. Then tick exactly
this phase's four requirements — the shared bounded-read mechanism, the bounded sweeps, the
receive-limit backstop and the CI job's greenness — in the checklists, and change those same four
rows' Status cell in the traceability table from pending to the completed value the table already
uses. Leave every other box and row exactly as it is: the Phase 6 and Phase 7 requirements stay
unticked, and no other phase's verification document, context document or checkbox is touched
anywhere in this task. Update the file's trailing last-updated line the way the previous phase's
close did. Add no heading and no licence header.

Re-run the full gate as the final act of the phase. If anything is red that this plan did not cause,
STOP and report it rather than editing unrelated code.

Commit the inventory reconciliation as `docs(05): reconcile the bounded-read call-site inventory` and
the requirement ticks as `docs(05): mark the phase's four requirements complete`, each with an
explicit pathspec.
  </action>
  <verify>
    <automated>rg -c -e '^- \[x\] \*\*REQ-(bounded-read-mechanism|sweeps-bounded|recv-limit-backstop|ci-store-green)\*\*' .planning/REQUIREMENTS.md</automated>
    <fails_when>the count printed is anything other than `4`, or `rg` exits non-zero because no line matched</fails_when>
    <automated>test "$(rg -o -e '^- \[x\] \*\*REQ-' .planning/REQUIREMENTS.md | wc -l)" -eq 17 &amp;&amp; test "$(rg -o -e '^- \[ \] \*\*REQ-' .planning/REQUIREMENTS.md | wc -l)" -eq 3</automated>
    <fails_when>non-zero exit — the ticked total is not the thirteen already complete plus this phase's four, or the remaining unticked total is not the three that belong to Phases 6 and 7, meaning another phase's box moved</fails_when>
    <automated>test "$(rg -c -e 'Phase 5 \| Complete' .planning/REQUIREMENTS.md)" -eq 4 &amp;&amp; test "$(rg -o -e 'Phase 5 \| Pending' .planning/REQUIREMENTS.md | wc -l)" -eq 0</automated>
    <fails_when>non-zero exit — a traceability row for this phase still reads pending, or fewer than four read complete</fails_when>
    <automated>gh issue view 497 --json state,closedAt --jq '.state' | rg -x 'CLOSED'</automated>
    <fails_when>non-zero exit, or any state other than `CLOSED` — the issue D-07 requires closing is still open</fails_when>
    <automated>git diff --exit-code HEAD -- .github/workflows/ci.yaml</automated>
    <fails_when>non-zero exit — the CI workflow was modified, which D-07 explicitly excludes</fails_when>
    <automated>test "$(rg -o -e '✅|📋|🚧' .planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/03-INVENTORY.md | wc -l)" -eq 0</automated>
    <fails_when>non-zero exit — a milestone-marker or version-bearing heading was introduced into a parsed planning artifact</fails_when>
    <automated>task</automated>
    <fails_when>non-zero exit from `task` (lint or test), including any `--- FAIL:` or `FAIL` package line</fails_when>
    <automated>go test ./internal/keylinks/ -count=1 -v 2&gt;&amp;1 | rg -e '^--- |^ok|^FAIL'; go test ./internal/keylinks/ -count=1</automated>
    <fails_when>non-zero exit from the second command, or a `--- FAIL:` line naming the repo-wide escaping gate or the active-milestone satisfiability gate — a key-links pattern in one of this phase's plans is escaped or no longer matches the shipped code</fails_when>
  </verify>
  <acceptance_criteria>
    - The SUMMARY quotes all four closing checks' commands and their captured output verbatim, and states the verdict for each.
    - The SUMMARY names every difference between check (a)'s live output and the recorded table, and states how each was reconciled; none is described as insignificant without a stated reason.
    - `rg -c -e 'Store[.]reindexTargetContents' .planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/03-INVENTORY.md` prints at least `1`, and that row's Assigned cell reads `Exempt`.
    - `rg -c -e 'Store[.]fetchPayloadBatch' .planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/03-INVENTORY.md` prints at least `1`.
    - `rg -c -e '## Phase 5 closing-check results' .planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/03-INVENTORY.md` prints `1`.
    - `git diff --exit-code HEAD -- .planning/phases/01-test-harness-fixture-helper .planning/phases/02-error-classification-resourceexhausted-mapping .planning/phases/04-list-listscheduled-search-bounded-reads` exits 0 — no other phase's recorded state moved.
    - With `R` the SHA of this task's inventory-reconciliation commit (record it in the SUMMARY): `d=$(git diff --name-only "$R^..$R" -- .planning/phases/03-shared-bounded-read-mechanism-content-cap-decision) || exit 1; printf '%s\n' "$d"` lists only `03-INVENTORY.md`.
    - `rg -c -e 'SPDX-License-Identifier' .planning/REQUIREMENTS.md .planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/03-INVENTORY.md` prints `0` for both files.
    - The SUMMARY records the #497 closing comment text and confirms no workflow file and no fixture-size test was added.
    - `git status --porcelain` after the final commit shows no modified tracked file.
  </acceptance_criteria>
  <done>The call-site inventory describes what the tree actually does, with every difference reconciled in writing; the CI issue is closed on the evidence that already existed, with no workflow change and no new test; the phase's four requirements are ticked in both the checklist and the traceability table with no other phase's state disturbed; the full quality gate and the key-links gates are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness → working tree | The harness applies real patches to real source files and must always restore them. |
| this phase's plans → other phases' recorded state | A cross-phase edit made to turn a gate green would erase another phase's evidence. |
| recorded inventory → live source | A table that no longer describes the tree is a gate that reports clean while seeing nothing. |
| this repository → a public issue tracker | Closing an issue is an external, visible action; closing it without the reasoning leaves no trace of why. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-06-01 | Tampering | a patch left applied after a failed run | medium | mitigate | The harness reverts via deferred and cleanup paths and asserts a clean porcelain status; both harness verifies additionally assert the mutated source trees are clean. |
| T-05-06-02 | Repudiation | a stale patch that no longer proves its gate RED | high | mitigate | Each patch is hand-verified against its target's own failure line before registration, the harness re-proves all fifty-three on every full run, and the verify rejects a run in which a target passed with its patch applied. |
| T-05-06-03 | Repudiation | a target name that resolves to no test, reporting a permanent false green | high | mitigate | Every target is re-resolved with `go test -list` before it is relied on, an acceptance criterion requires that resolution to be recorded, and the harness's own set-equality check rejects an unmapped patch. |
| T-05-06-04 | Repudiation | an inventory reconciled by editing the check until it passes | high | mitigate | The verified drift is stated in the plan before the check is run, so a silent round-off is not available; the action forbids editing the command merely to make the output match; acceptance criteria require every difference to be named and its reconciliation stated. |
| T-05-06-05 | Repudiation | a requirement ticked without shipped evidence | high | mitigate | Task 3 reads every plan SUMMARY and queries the readiness helper before ticking, the totals are asserted by count so no extra box can move, and the whole gate must be green first. |
| T-05-06-06 | Tampering | another phase's recorded state edited to make this gate green | high | mitigate | An acceptance criterion diffs the Phase 1, 2 and 4 directories and requires them unchanged, and restricts the Phase 3 diff to the single sanctioned file; the executor notes forbid the edit outright. |
| T-05-06-07 | Repudiation | a public issue closed with no recorded reason | medium | mitigate | The close is required to carry the reasoning as a comment, the comment text is recorded in the SUMMARY, and a verify asserts the workflow file is untouched so the close cannot be confused with a fix. |
| T-05-06-08 | Denial of Service | the harness outgrowing its timeout at fifty-three patches over real fixtures | medium | mitigate | The pre-authorized per-package narrowing applies if it fires, with before and after durations recorded; a prohibition forbids raising a timeout in the task runner or CI to hide it. |
| T-05-06-SC | Tampering | npm/pip/cargo installs | low | accept | Not applicable: no installs, no manifest change. |
</threat_model>

<verification>
- `ENGRAM_REQUIRE_QDRANT=1 go test ./internal/store/ -run '^TestRedEvidencePatchesAreLive$' -count=1 -v -timeout 180m` shows 53 `confirmed RED:` lines and exits 0.
- All four inventory closing checks are run verbatim and their output is recorded; every difference from the recorded table is reconciled in writing.
- `gh issue view 497 --json state --jq '.state'` prints `CLOSED`, and `git diff --exit-code HEAD -- .github/workflows/ci.yaml` exits 0.
- `task` exits 0; `task license:check` exits 0; `go test ./internal/keylinks/ -count=1` exits 0; `git diff --exit-code HEAD -- go.mod go.sum` exits 0.
- `git status --porcelain` prints nothing after the final commit.
</verification>

<success_criteria>
- Every guarantee this phase added — each migrated sweep's budgeted view, each sweep's own distinguishable early-stop sentinel, the reindex walk's effective source and its final page flush, and the backstop's append order — has a registered, live RED proof.
- The call-site inventory describes the tree as it actually is, with every drift reconciled in writing rather than rounded off.
- The CI issue is closed on the evidence that already existed, with no workflow change, no observation gate and no fixture-size test.
- The phase's four requirements are marked complete on the evidence that actually shipped, and the phase closes on a green `task` with no other phase's recorded state disturbed.
</success_criteria>

<output>
Create `.planning/phases/05-operator-sweeps-ci-backstop/05-06-SUMMARY.md` when done.
</output>
