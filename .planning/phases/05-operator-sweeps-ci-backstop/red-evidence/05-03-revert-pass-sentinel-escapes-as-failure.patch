diff --git a/internal/store/revert.go b/internal/store/revert.go
index e969bbda..b88ca88a 100644
--- a/internal/store/revert.go
+++ b/internal/store/revert.go
@@ -575,7 +575,7 @@ func (s *Store) revertWithSteps(ctx context.Context, to migrate.Version, steps [
 			}
 			return nil
 		})
-		if scanErr != nil && !errors.Is(scanErr, errRevertPassBatchComplete) {
+		if scanErr != nil {
 			err = scanErr
 			return res, err
 		}
