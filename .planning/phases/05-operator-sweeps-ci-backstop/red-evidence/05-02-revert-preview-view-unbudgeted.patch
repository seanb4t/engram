diff --git a/internal/store/revert.go b/internal/store/revert.go
index e969bbda..b1b4a151 100644
--- a/internal/store/revert.go
+++ b/internal/store/revert.go
@@ -287,7 +287,7 @@ func (s *Store) previewRevertWithSteps(ctx context.Context, to migrate.Version,
 	unsupportedCounts := map[migrate.Version]uint64{}
 	var observedChains [][]migrate.Step
 
-	err := s.scrollAllPoints(ctx, s.collection, aboveTargetFilter(to), schemaVersionOnlyView(), func(p *qdrant.RetrievedPoint) error {
+	err := s.scrollAllPoints(ctx, s.collection, aboveTargetFilter(to), readView{selector: qdrant.NewWithPayload(true), maxRecordBytes: 0}, func(p *qdrant.RetrievedPoint) error {
 		plan.Candidates++
 		v := versionOf(p.Payload)
 
