---
phase: 19-console-write-ux
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - ui/src/lib/interceptors/csrf.ts
  - ui/src/lib/interceptors/csrf.test.ts
  - ui/src/lib/interceptors/retryOnce.ts
  - ui/src/lib/interceptors/retryOnce.test.ts
  - ui/src/lib/client.ts
  - ui/src/lib/client.test.ts
autonomous: true
requirements: [REQ-console-write-ux]
must_haves:
  truths:
    - "Every write request carries X-CSRF-Token equal to the current engram_csrf cookie value (SC2 double-submit)"
    - "A write that fails once with Unauthenticated or PermissionDenied gets a SINGLE opportunistic auth-race retry (a session-cookie freshness race — never a re-seal or rotation), transparently (SC3/D-08, amended to backend reality per Codex round-2 HIGH)"
    - "The second identical failure surfaces to the caller unchanged (terminal → drives D-09 in the form layer)"
    - "The read client (engram) is unchanged; only the new engramWrite client carries the interceptors"
  artifacts:
    - "ui/src/lib/interceptors/csrf.ts (attachCsrf Interceptor)"
    - "ui/src/lib/interceptors/retryOnce.ts (retryOnce Interceptor)"
    - "ui/src/lib/client.ts exporting engramWrite alongside engram"
  key_links:
    - "Interceptor array order [retryOnce, attachCsrf] — retryOnce OUTER so a retry re-enters attachCsrf and re-reads the cookie (Pitfall 3)"
    - "engram_csrf cookie (non-HttpOnly, minted/refreshed server-side) → document.cookie → X-CSRF-Token header"
    - "ConnectError.code narrowing (Unauthenticated|PermissionDenied) mirrors client.ts mapAuthError idiom"
---

<objective>
Build the write-lane Connect transport as two composed interceptors and expose a dedicated `engramWrite` client:

- `attachCsrf` reads the JS-readable `engram_csrf` cookie and sets the `X-CSRF-Token` header on every outgoing write request (SC2, D-08 discretion — one choke point, not per-call).
- `retryOnce` catches a `ConnectError` whose code is `Unauthenticated` or `PermissionDenied`, retries `next(req)` exactly once, and rethrows the second failure unchanged (SC3, D-08).

**Retry mechanism — accurate framing (Codex round-1+round-2 HIGH, verified against `connectreseal.go:39`, `resolver.go:49`, `csrf.go:38`, `reseal.go:41`):** the retry is a **single opportunistic auth-race retry**, NOT a "retry through re-seal" and NOT a "rotation recovery." A *failed* request cannot itself trigger a server re-seal — `newConnectResealInterceptor` explicitly skips re-sealing when the handler returns an error or a nil response (`connectreseal.go:39-41`: `if err != nil || resp == nil ...`) — and there is NO "needs rotation" error state: an expired session is rejected pre-handler (`resolver.go:49` `session expired`). The retry therefore repairs only a **session-cookie freshness race** — the browser's `engram_session` cookie state changed between the two attempts (e.g. a concurrent successful read re-sealed the session cookie), so a second attempt built on the fresher cookie succeeds. The "CSRF-freshness" half is **largely theoretical**: the `engram_csrf` token is bound to `Owner` ONLY (`csrf.go:38` HMAC(k_csrf, Owner)) and is intentionally **stable across re-seals** — a re-seal refreshes its Max-Age, not its value (`reseal.go:41` "same HMAC(k_csrf, Owner) value"). A truly hard-expired session deterministically fails the retry too → terminal, drives D-09. Name the retry tests **"auth-race retry,"** never "rotation recovery." Do NOT describe this as re-seal-on-retry or session rotation anywhere in code comments, tests, or the SUMMARY.

Purpose: centralize CSRF attachment and the retry-once semantics in transport, so no mutation wrapper reimplements them and the retry stays invisible to the operator.
Output: two interceptors + a write-only `engramWrite` client, each unit-tested against `createRouterTransport`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-console-write-ux/19-CONTEXT.md
@.planning/phases/19-console-write-ux/19-RESEARCH.md
@.planning/phases/19-console-write-ux/19-PATTERNS.md
@ui/src/lib/client.ts
@internal/webauth/csrf.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: attachCsrf interceptor (reads engram_csrf, sets X-CSRF-Token)</name>
  <files>ui/src/lib/interceptors/csrf.ts, ui/src/lib/interceptors/csrf.test.ts</files>
  <read_first>
    - ui/src/lib/client.ts (import idiom `import { ... type Interceptor } from '@connectrpc/connect'`; same-origin transport construction)
    - internal/webauth/csrf.go (wire-contract constants: `CSRFCookieName = "engram_csrf"`, `CSRFHeaderName = "X-CSRF-Token"` — the exact names to echo)
    - .planning/phases/19-console-write-ux/19-RESEARCH.md (Pattern 2 `attachCsrf`; "Reading the CSRF cookie value"; "Testing an interceptor with createRouterTransport")
    - ui/src/lib/client.test.ts (existing vitest node-test conventions: describe/it/expect, ConnectError/Code)
  </read_first>
  <behavior>
    - When document.cookie contains `engram_csrf=<value>`, the outgoing request header `X-CSRF-Token` equals `<value>`
    - When no engram_csrf cookie is present, no X-CSRF-Token header is set (server will reject — that is the expected/correct outcome)
    - The cookie is re-read per request (not cached across requests), so a refreshed cookie value is picked up on the next call
  </behavior>
  <action>
    Create `ui/src/lib/interceptors/csrf.ts` exporting `attachCsrf: Interceptor` (type from `@connectrpc/connect`). It parses `document.cookie` for the `engram_csrf=` entry and, if present, sets header `X-CSRF-Token` to that value before delegating to `next(req)`. Header name and cookie name MUST match the server constants in `internal/webauth/csrf.go` (echo, never mint). Add a source comment noting the token is echoed per-request and never cached, and that the server (`connectcsrf.go`) is the authoritative verifier (client-side attachment is not a security boundary).
    Create `ui/src/lib/interceptors/csrf.test.ts` (node project) using `createRouterTransport` + `createClient(EngramService, ...)` with the interceptor installed and a handler that asserts `ctx.requestHeader.get('X-CSRF-Token')`. Cover: cookie present → header matches; cookie absent → header unset. Stub `document.cookie` for the node environment.
  </action>
  <acceptance_criteria>
    - `csrf.ts` exports `attachCsrf` typed `Interceptor`; sets `X-CSRF-Token` only when `engram_csrf` cookie present
    - Cookie/header names are `engram_csrf` / `X-CSRF-Token` verbatim (match `internal/webauth/csrf.go`)
    - `csrf.test.ts` asserts both present and absent cases via a router-transport handler reading the request header
    - `cd ui && pnpm test:node -- src/lib/interceptors/csrf.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/engram/ui && pnpm test:node -- src/lib/interceptors/csrf.test.ts</automated>
  </verify>
  <done>Writes carry the CSRF token from the cookie automatically; the read client is untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: retryOnce interceptor (single silent retry on auth-class failure)</name>
  <files>ui/src/lib/interceptors/retryOnce.ts, ui/src/lib/interceptors/retryOnce.test.ts</files>
  <read_first>
    - ui/src/lib/client.ts (`mapAuthError` — the ConnectError/Code narrowing idiom to mirror)
    - .planning/phases/19-console-write-ux/19-RESEARCH.md (Pattern 2 `retryOnce`; Pitfall 1 — no server "needs rotation" signal, both codes are the retry set; Pitfall 3 — array order)
    - .planning/phases/19-console-write-ux/19-CONTEXT.md (D-08 silent retry, D-09 terminal → re-auth)
    - internal/server/connectreseal.go (line 39-41 — reseal SKIPS errored/nil responses; a failed request cannot re-seal, so the retry repairs a cookie/CSRF-freshness race only, never "re-seals on retry")
    - internal/webauth/reseal.go (reseal is best-effort post-SUCCESS on the NEXT successful request, refreshing cookies a later attempt re-reads)
    - .planning/phases/19-console-write-ux/19-REVIEWS.md (Codex HIGH on Plan 19-02: "retry through re-seal" does not exist server-side — recast as opportunistic race retry)
  </read_first>
  <behavior>
    - A handler that fails once with Code.Unauthenticated then succeeds → interceptor retries once, mutation resolves successfully; handler invoked exactly twice
    - A handler that fails once with Code.PermissionDenied then succeeds → same (both codes are retry-worthy)
    - A handler that fails twice with the same code → the second ConnectError propagates unchanged (exactly two invocations, no third)
    - A handler that fails with a non-auth code (e.g. Code.InvalidArgument) → propagates immediately, no retry (single invocation)
  </behavior>
  <action>
    Create `ui/src/lib/interceptors/retryOnce.ts` exporting `retryOnce: Interceptor`. It `await`s `next(req)`; on throw, narrows via `ConnectError.from(err)`; if `err.code` is in the retry set `{ Code.Unauthenticated, Code.PermissionDenied }` it calls `next(req)` exactly once more and returns/propagates that result; otherwise it rethrows immediately. No exponential backoff, no configurable attempt count, no generic retry library — exactly one retry (per D-08). Do NOT add a procedure allowlist inside the interceptor; scope is enforced by only routing write RPCs through `engramWrite` (RESEARCH Open Question 1 recommendation). Add a source comment explaining: (a) the two-code retry set is a client-side interpretation (no dedicated server rotation signal — there is no "needs rotation" error state); (b) this is a **single opportunistic auth-race retry** — a failed request does NOT trigger a server re-seal (`connectreseal.go:39` skips errored responses), so the retry only helps a **session-cookie freshness race** where the `engram_session` cookie state already changed between attempts; the `engram_csrf` token is Owner-bound and stable across re-seals (`csrf.go:38`, `reseal.go:41`), so the CSRF-freshness scenario is largely theoretical; a hard-expired session fails the retry too and is terminal; (c) idempotency of the retry is safe because Unauthenticated/PermissionDenied are rejected before the handler mutates state (CSRF verify + session Resolve run ahead of the write handler). Do NOT write "re-seal on retry," "rotation," or imply the retry causes a session rotation.
    Create `ui/src/lib/interceptors/retryOnce.test.ts` (node) using `createRouterTransport` with a call-counting handler. Name the describe/it blocks **"auth-race retry"** (NOT "rotation recovery"). Cover all four behavior cases above; assert exact invocation counts and final resolution/propagation.
  </action>
  <acceptance_criteria>
    - `retryOnce.ts` exports `retryOnce` typed `Interceptor`; retries once on Unauthenticated OR PermissionDenied; rethrows on second failure; no retry for other codes
    - Retry set contains exactly `Code.Unauthenticated` and `Code.PermissionDenied`
    - `retryOnce.test.ts` asserts exactly-twice on success-on-retry, exactly-twice on hard-fail, exactly-once on non-auth code; the tests are named "auth-race retry," not "rotation recovery"
    - `cd ui && pnpm test:node -- src/lib/interceptors/retryOnce.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/engram/ui && pnpm test:node -- src/lib/interceptors/retryOnce.test.ts</automated>
  </verify>
  <done>Auth-race-recoverable write failures (a session-cookie freshness race) succeed on one silent retry; truly-expired sessions surface a single terminal error for the form layer to handle.</done>
</task>

<task type="auto">
  <name>Task 3: engramWrite client on the two-interceptor transport</name>
  <files>ui/src/lib/client.ts, ui/src/lib/client.test.ts</files>
  <read_first>
    - ui/src/lib/client.ts (existing `engram` client + `mapAuthError`; keep both intact)
    - ui/src/lib/interceptors/csrf.ts, ui/src/lib/interceptors/retryOnce.ts (the interceptors to compose — created in Tasks 1/2)
    - .planning/phases/19-console-write-ux/19-RESEARCH.md (Pattern 2 — `interceptors: [retryOnce, attachCsrf]` order; Pitfall 3)
    - ui/src/lib/client.test.ts (existing test file to extend)
  </read_first>
  <action>
    Extend `ui/src/lib/client.ts`: add a `writeTransport = createConnectTransport({ baseUrl: '/', interceptors: [retryOnce, attachCsrf] })` and `export const engramWrite = createClient(EngramService, writeTransport)`. Array order MUST be `[retryOnce, attachCsrf]` (retryOnce outer) so a retry re-enters attachCsrf and re-reads the cookie (Pitfall 3). Leave the existing `engram` read client and `mapAuthError` untouched — reads keep the plain transport. Add a source comment stating the order rationale.
    Extend `ui/src/lib/client.test.ts` with an assertion that `engramWrite` is exported and is a distinct client from `engram` (both defined, not the same reference), plus a source/structural assertion (or a router-transport smoke) that the write transport composes both interceptors in the [retryOnce, attachCsrf] order. Keep the existing `mapAuthError` tests unchanged.
    Add ONE COMPOSED-interceptor test (Codex round-4 LOW) that proves retryOnce (outer) re-enters attachCsrf on the retry and re-reads the REFRESHED cookie — the two interceptors' unit tests alone never exercise the cookie-change-between-attempts path. Build a `createRouterTransport` handler with `interceptors: [retryOnce, attachCsrf]` and a call counter: on the FIRST invocation, MUTATE `document.cookie` (set `engram_csrf` to a new value) and then throw `Code.PermissionDenied`; on the SECOND invocation, capture the request's `X-CSRF-Token` header. Assert the handler was invoked EXACTLY TWICE and the second invocation's `X-CSRF-Token` equals the NEW cookie value (not the stale first value) — demonstrating the retry rebuilt the header from the refreshed cookie. Name this test in the "auth-race retry" family, never "rotation recovery."
  </action>
  <acceptance_criteria>
    - `client.ts` exports both `engram` (unchanged) and `engramWrite`
    - Write transport interceptor array is literally `[retryOnce, attachCsrf]`
    - `client.test.ts` asserts `engramWrite` exists, differs from `engram`, and the interceptor order is retryOnce-outer
    - A COMPOSED-interceptor test (Codex round-4 LOW): first call changes `document.cookie` then fails `PermissionDenied`; the retry's request carries the REFRESHED `X-CSRF-Token`; handler invoked exactly twice
    - `cd ui && pnpm test:node -- src/lib/client.test.ts` exits 0; `cd ui && pnpm check` clean
  </acceptance_criteria>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/engram/ui && pnpm test:node -- src/lib/client.test.ts</automated>
  </verify>
  <done>Mutation code can import `engramWrite` for all six write RPCs with CSRF + retry-once handled at the transport layer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser JS → same-origin Connect API | Untrusted client attaches X-CSRF-Token; server (connectcsrf.go) is the authoritative verifier |
| engram_csrf cookie (non-HttpOnly) → JS | Cookie is deliberately JS-readable so the SPA can echo it; this is the double-submit design |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-11 | Tampering | retryOnce double-submitting a non-idempotent create (StoreMemory/StoreDiscovery) | high | mitigate | Retry only on Unauthenticated/PermissionDenied — both are rejected before the write handler mutates state (session Resolve at `connectauth.go` + CSRF verify at `connectcsrf.go` precede `next()`; business errors never map to these two codes per `connecterror.go`), so a retry cannot double-create; retry limited to exactly one attempt (Task 2 tests assert exactly-twice). **Codex+grok confirmed this mitigation HOLDS against live source.** Recommended future hardening (out of this UI plan's file scope — do NOT add a Go file here): a server-contract test proving both retry codes stay pre-handler for all six write procedures — file as a follow-up so a future handler returning either code post-mutation cannot silently make create-retries unsafe |
| T-19-12 | Information Disclosure | engram_csrf cookie readable by page JS (XSS could exfiltrate it) | medium | accept | Non-HttpOnly is required by the double-submit design (server mints/refreshes it); token is per-request and short-lived; XSS-hardening is out of this phase's scope (server-owned). Never cache the token beyond a single request |
| T-19-13 | Spoofing | Missing/forged CSRF header on a write | low | mitigate | attachCsrf echoes the server-minted cookie value; server verifies the double-submit and binds token to Subject.Owner — client cannot forge a valid pair |
</threat_model>

## Artifacts this phase produces

New symbols introduced by this plan:
- `ui/src/lib/interceptors/csrf.ts`: exported `attachCsrf` (`Interceptor`)
- `ui/src/lib/interceptors/retryOnce.ts`: exported `retryOnce` (`Interceptor`)
- `ui/src/lib/client.ts`: new export `engramWrite`; new internal `writeTransport`
- Test files: `csrf.test.ts`, `retryOnce.test.ts`, `client.test.ts` (extended)

<verification>
- `cd ui && pnpm test:node -- src/lib/interceptors src/lib/client.test.ts` green
- `cd ui && pnpm check` clean
- `rg "interceptors: \[retryOnce, attachCsrf\]" ui/src/lib/client.ts` matches (order gate)
</verification>

<success_criteria>
SC2 (CSRF header on every write) and the SC3 transport half (a SINGLE opportunistic auth-race retry for a session-cookie freshness race — never a re-seal/rotation — with a terminal second failure) are implemented and unit-tested (tests named "auth-race retry"); `engramWrite` is ready for the mutation layer. Read path unchanged.

**SC3 mapping (round-3, Codex+grok convergent MEDIUM; RECONCILED round-4):** SC3's original "retried once through a re-seal" wording is REINTERPRETED as this single opportunistic auth-race retry. The original reseal-on-retry mechanism is EXPLICITLY MARKED INFEASIBLE under the shipped backend — a failed request cannot itself trigger a server re-seal (`connectreseal.go:39` skips errored/nil responses) and an expired session is rejected pre-handler with no "needs rotation" state (`resolver.go:49`). The verifier MUST accept SC3 as satisfied by the auth-race retry + terminal→D-09 flow, NOT by a reseal; the durable D-09 resume-envelope path (Plans 05/06) is the real salvage for a hard-expired session. **Upstream docs are now reconciled (Codex round-4 LOW):** `ROADMAP.md` (Phase 19 SC3), `REQUIREMENTS.md` (REQ-console-write-ux), and locked `D-08`/`D-09` in `19-CONTEXT.md` now all say "opportunistic auth-race retry" — they are CONSISTENT with this plan's framing; the earlier "stale / user-owned / not-yet-reconciled" caveat no longer applies and has been removed so it can't mislead the executor.
</success_criteria>

<review_incorporation>
## Cross-AI Review — findings addressed in this plan

- **Codex HIGH (a failed request cannot trigger re-sealing / "retry through re-seal" is not real):** objective + retryOnce action recast as an opportunistic cookie/CSRF-race retry, grounded in `connectreseal.go:39` (reseal skips errored responses). Code comment forbidden from implying reseal-on-retry. RESOLVED (recast).
- **Codex MEDIUM (no ordinary "needs rotation" failure state):** documented — the retry repairs a race, not deterministic rotation recovery; a hard-expired session fails the retry and is terminal → D-09. RESOLVED (documented).
- **Codex LOW (safety claim is an undocumented coupling):** T-19-11 now names the `connectauth.go`/`connectcsrf.go`/`connecterror.go` pre-handler rejection and files the server-contract test as a recommended follow-up (kept out of this UI plan's file scope). RESOLVED (documented + follow-up).
- **Round-2 Codex HIGH (SC3/D-08 retry is a rename, not a fix — amend wording to backend reality):** the objective, retryOnce action/comment, test naming, must_haves, and success_criteria now promise a SINGLE OPPORTUNISTIC AUTH-RACE retry (a session-cookie freshness race), never "retry through re-seal" or "rotation" — grounded in `connectreseal.go:39` (reseal skips errored responses) and `resolver.go:49` (expired session rejected pre-handler, no "needs rotation" state). The retry tests are named "auth-race retry." The CSRF-freshness half is documented as largely theoretical because the token is Owner-bound and stable across re-seals (`csrf.go:38`, `reseal.go:41`). This is a plan-wording/semantics amendment, not a new mechanism; locked D-08's intent (one silent retry, invisible to the operator) is preserved — only its inaccurate "re-seal on retry" framing is corrected. RESOLVED (recast).
- **Round-3 CONVERGENT MEDIUM (Codex+grok — authoritative SC3/D-08 text was stale) → RECONCILED round-4:** the plan's auth-race framing was VERIFIED HOLDING in source by all reviewers across rounds 3 and 4. As of the round-4 reconciled commit, `ROADMAP.md` (Phase 19 SC3), `REQUIREMENTS.md` (REQ-console-write-ux), and locked `D-08`/`D-09` (`19-CONTEXT.md`) now all say "opportunistic auth-race retry" — they are CONSISTENT with this plan. The original reseal promise remains EXPLICITLY MARKED INFEASIBLE (grounded in `connectreseal.go:39` skipping errored responses and `resolver.go:49` rejecting expired sessions pre-handler — no "needs rotation" state), and the reinterpretation ("single opportunistic auth-race retry") is documented in the SC3-mapping note under `<success_criteria>`. The earlier "upstream docs are user-owned / stale / not-yet-reconciled" caveat has been REMOVED from this plan (Codex round-4 LOW) because it is no longer true and would mislead the executor. RESOLVED (docs reconciled; plan note corrected).
- **Round-4 Codex LOW (missing composed-interceptor test):** Task 3 now adds a composed `[retryOnce, attachCsrf]` test where the first invocation changes `document.cookie` then fails `PermissionDenied` and the retry's request carries the REFRESHED `X-CSRF-Token` (exactly two invocations) — proving retryOnce re-enters attachCsrf and re-reads the cookie, the seam the per-interceptor unit tests never exercised. RESOLVED.
- **Round-3/4 confirmed HOLDING (all reviewers, not churned):** the auth-race retry semantics, the `[retryOnce, attachCsrf]` interceptor order, and the double-create pre-handler mitigation were re-verified against live source this round and are unchanged.
</review_incorporation>

<output>
Create `.planning/phases/19-console-write-ux/19-02-SUMMARY.md` when done. Record the retry-code set decision (Pitfall 1 interpretation), the interceptor order rationale, and the accurate opportunistic-race framing (NOT reseal-on-retry).
</output>
