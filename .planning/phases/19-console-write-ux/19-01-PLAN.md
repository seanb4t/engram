---
phase: 19-console-write-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Taskfile.yaml
  - .github/workflows/ci.yaml
  - ui/src/lib/gen/engram_pb.ts
  - ui/src/app.css
  - ui/src/app.css.test.ts
autonomous: true
requirements: [REQ-console-write-ux]
must_haves:
  truths:
    - "ui/src/lib/gen/engram_pb.ts exposes the 6 write RPCs and the Citation/Visibility types so write feature code compiles"
    - "Re-vendoring the console gen client is a reproducible `task` target, not a hand copy (D-08 reproducibility)"
    - "CI fails if ui/src/lib/gen/ drifts from canonical gen/ts/engram/v1/"
    - "--destructive design tokens resolve to a visible color so D-06 delete and D-02 destructive row actions render correctly"
  artifacts:
    - "Taskfile.yaml re-vendor step copying gen/ts/engram/v1/engram_pb.ts into ui/src/lib/gen/engram_pb.ts"
    - ".github/workflows/ci.yaml drift guard for ui/src/lib/gen/"
    - "ui/src/lib/gen/engram_pb.ts (re-vendored, StoreMemorySchema..ScheduleMemoryRequestSchema, CitationSchema, Visibility enum present)"
    - "ui/src/app.css --destructive / --destructive-foreground / --color-destructive / --color-destructive-foreground tokens"
  key_links:
    - "gen/ts/engram/v1/engram_pb.ts (source of truth) → ui/src/lib/gen/engram_pb.ts (vendored copy consumed by client.ts)"
    - "--cat-gotcha per-mode value → --destructive alias → shadcn destructive classes already referenced by vendored primitives"
---

<objective>
Close the two hard prerequisites that block every write feature in Phase 19:

1. Re-vendor the stale console Connect gen client (`ui/src/lib/gen/engram_pb.ts`) so it exposes the 6 write RPCs (StoreMemory, StoreDiscovery, UpdateMemory, DeleteMemory, SetVisibility, ScheduleMemory) plus the `Citation` message and `Visibility` enum — and make that a reproducible `task` target with a CI drift guard (not a one-off hand copy). This is the Wave-1 blocker: the write transport (Plan 02) and mutations (Plan 04) cannot compile without it.
2. Define the missing `--destructive` design tokens in `ui/src/app.css` so the vendored shadcn destructive classes (already referenced by `dropdown-menu`, `badge`, `input`, etc.) resolve to a visible color — a prerequisite for the D-06 delete confirm dialog and D-02 destructive row actions (Plan 03).

Purpose: unblock all downstream write UX and eliminate the vendored-gen-drift class of bug (relates to Phase 21 "Renovate vendored-SPA drift").
Output: reproducible re-vendor task + CI guard, re-vendored gen client, `--destructive` tokens with a source-asserting test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-console-write-ux/19-CONTEXT.md
@.planning/phases/19-console-write-ux/19-RESEARCH.md
@.planning/phases/19-console-write-ux/19-PATTERNS.md
@.planning/phases/19-console-write-ux/19-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add reproducible gen-client re-vendor task + CI drift guard</name>
  <files>Taskfile.yaml, .github/workflows/ci.yaml</files>
  <read_first>
    - Taskfile.yaml (the `proto:gen` block ~line 145 and the `ui:build` block ~line 21 — the `rm -rf` + `cp -R` whole-file copy idiom to mirror)
    - .github/workflows/ci.yaml (the `buf` job's "generated-code drift" step ~line 127 that runs `go tool buf generate` then `git diff --exit-code -- gen/`; and the `ui-drift` job ~line 138)
    - .planning/phases/19-console-write-ux/19-PATTERNS.md (section "Taskfile.yaml (extend) — re-vendor gen client task")
    - .planning/phases/19-console-write-ux/19-RESEARCH.md (Summary item 1; Pitfall 4; Anti-Patterns "Hand-copying gen/ts...")
  </read_first>
  <action>
    Extend `proto:gen` so regenerating stubs also re-vendors the TS client into the console. Add the copy as an explicit step of `proto:gen` (or a `proto:gen-ui` task invoked by `proto:gen`) using the repo's whole-file copy idiom: remove the stale `ui/src/lib/gen/engram_pb.ts` and copy `gen/ts/engram/v1/engram_pb.ts` over it (mirror `ui:build`'s `rm`/`cp` pattern, not a hand-patch). Keep `desc` accurate ("Regenerate connect stubs (Go + TS) and re-vendor the console gen client").
    In `.github/workflows/ci.yaml`, extend the existing `buf` job's generated-code drift step so after `go tool buf generate` it re-runs the vendor copy and asserts `git diff --exit-code -- ui/src/lib/gen/` with a clear "run 'task proto:gen'" error message, mirroring the existing `gen/` drift assertion. Do NOT add a new npm/pnpm install to the `buf` job — the copy is a plain filesystem `cp` of an already-generated file. This decision (CI drift guard for the vendored console gen client) is intentional and in-scope per Phase 19 emphasis 1; record it in the SUMMARY.
  </action>
  <acceptance_criteria>
    - `task proto:gen` (or its invoked sub-step) copies `gen/ts/engram/v1/engram_pb.ts` to `ui/src/lib/gen/engram_pb.ts` via `rm`+`cp`, no hand-editing
    - `.github/workflows/ci.yaml` `buf` job asserts `git diff --exit-code -- ui/src/lib/gen/` after regeneration with a remediation message naming `task proto:gen`
    - No fenced-heredoc file authoring; Taskfile/CI edits only
  </acceptance_criteria>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/engram && rg -q "ui/src/lib/gen" Taskfile.yaml && rg -q "ui/src/lib/gen" .github/workflows/ci.yaml</automated>
  </verify>
  <done>Running the re-vendor task reproducibly overwrites the console gen client from the canonical source, and CI reddens on drift.</done>
</task>

<task type="auto">
  <name>Task 2: Re-vendor ui/src/lib/gen/engram_pb.ts (run task, commit generated file)</name>
  <files>ui/src/lib/gen/engram_pb.ts</files>
  <read_first>
    - ui/src/lib/gen/engram_pb.ts (current stale copy — 5 read RPCs only; confirm write RPCs absent before overwriting)
    - gen/ts/engram/v1/engram_pb.ts (canonical source — StoreMemoryRequestSchema, StoreDiscoveryRequestSchema, UpdateMemoryRequestSchema, DeleteMemoryRequestSchema, SetVisibilityRequestSchema, ScheduleMemoryRequestSchema, CitationSchema, `Visibility` enum, EngramService write methods)
    - .planning/phases/19-console-write-ux/19-RESEARCH.md (Pitfall 4 — whole-file copy, never hand-patch)
  </read_first>
  <action>
    Run the re-vendor task added in Task 1 so `ui/src/lib/gen/engram_pb.ts` becomes a byte-for-byte copy of `gen/ts/engram/v1/engram_pb.ts`. Commit the regenerated file. Do NOT hand-edit the generated output. This must add (not just the RPC methods) the `Citation`/`CitationSchema`, `Visibility` enum, and all six write Request/Response schemas the write forms and visibility toggle depend on. Apache-2.0 SPDX header handling: generated files follow whatever `gen/ts/` emits — do not inject a header the generator does not produce (match the canonical file exactly).
  </action>
  <acceptance_criteria>
    - `ui/src/lib/gen/engram_pb.ts` exports StoreMemorySchema, StoreDiscoveryRequestSchema, UpdateMemoryRequestSchema, DeleteMemoryRequestSchema, SetVisibilityRequestSchema, ScheduleMemoryRequestSchema, CitationSchema, and the `Visibility` enum
    - `diff gen/ts/engram/v1/engram_pb.ts ui/src/lib/gen/engram_pb.ts` reports no differences
    - `cd ui && pnpm check` compiles clean (no missing-export TS errors)
  </acceptance_criteria>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/engram && diff gen/ts/engram/v1/engram_pb.ts ui/src/lib/gen/engram_pb.ts && rg -q "StoreMemoryRequestSchema|SetVisibilityRequestSchema|CitationSchema" ui/src/lib/gen/engram_pb.ts && rg -q "export enum Visibility" ui/src/lib/gen/engram_pb.ts</automated>
  </verify>
  <done>The console gen client exposes the full write surface; downstream write code will compile against it.</done>
</task>

<task type="auto">
  <name>Task 3: Add --destructive design tokens + assert them in app.css.test.ts</name>
  <files>ui/src/app.css, ui/src/app.css.test.ts</files>
  <read_first>
    - ui/src/app.css (the `:root` block line 5-18, `.dark` block line 19-31, `@theme inline` block line 33-47 — the existing `--cat-gotcha` alias-into-`@theme` triple)
    - ui/src/app.css.test.ts (existing token-presence test that reads app.css off disk and asserts per-block via regex; extend its assertions)
    - .planning/phases/19-console-write-ux/19-UI-SPEC.md (Color section — the `--destructive` gap and required alias to `--cat-gotcha`)
    - .planning/phases/19-console-write-ux/19-PATTERNS.md (section "ui/src/app.css (extend) — --destructive token")
  </read_first>
  <action>
    In `ui/src/app.css`, alias the existing error-semantic color to a destructive token following the exact `--cat-gotcha` pattern. Add to `:root` and `.dark`: `--destructive: var(--cat-gotcha);` and `--destructive-foreground: #ffffff;` (the per-mode `--cat-gotcha` already differs light/dark, so a single alias covers both mode surfaces). Add to the `@theme inline` block: `--color-destructive: var(--destructive);` and `--color-destructive-foreground: var(--destructive-foreground);` so Tailwind's `bg-destructive`/`text-destructive`/`ring-destructive` utilities (already referenced by vendored shadcn primitives) resolve. Do not introduce a new hue — reuse `--cat-gotcha` so the whole app keeps one error color (D-06/D-07 render in the established amber/error tone).
    Extend `ui/src/app.css.test.ts` with a test asserting `--destructive:` appears in the `:root` and `.dark` bodies and `--color-destructive:` appears in the `@theme` body, reusing the existing `body(re)`/`root`/`dark`/`theme` extraction helpers.
  </action>
  <acceptance_criteria>
    - `:root` and `.dark` bodies each contain `--destructive:` and `--destructive-foreground:`
    - `@theme inline` body contains `--color-destructive:` and `--color-destructive-foreground:`
    - `ui/src/app.css.test.ts` has a passing assertion for the destructive token in all three blocks
    - `cd ui && pnpm test:node -- src/app.css.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/engram/ui && pnpm test:node -- src/app.css.test.ts</automated>
  </verify>
  <done>Destructive-styled controls (delete dialog button, destructive dropdown-menu item) render in the app's error color instead of transparent/inherited.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canonical gen/ts → vendored ui/gen | A stale/hand-edited vendored copy can silently desync the console client from the wire contract (drift class of bug) |
| build tooling → committed artifact | Generated code is committed and CI-checked; a bypass would ship an untested client |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-01 | Tampering | ui/src/lib/gen/engram_pb.ts (hand-edited/stale vendored client) | medium | mitigate | Whole-file copy via reproducible `task` target + CI `git diff --exit-code -- ui/src/lib/gen/` drift guard; never hand-patch (Task 1/2) |
| T-19-02 | Tampering | app.css destructive token missing → destructive controls render invisibly, hiding a destructive action's affordance | low | mitigate | Define `--destructive` aliased to the app-wide error color; source-assert its presence (Task 3) |
| T-19-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages installed this phase (RESEARCH "Package Legitimacy Audit — Not applicable"); no [ASSUMED]/[SUS]/[SLOP] packages introduced |
</threat_model>

## Artifacts this phase produces

New symbols/artifacts introduced by this plan (source-grounding exclusion list for downstream review):
- Task target: `proto:gen` extended (or new `proto:gen-ui`) re-vendor step in `Taskfile.yaml`
- CI step: `ui/src/lib/gen/` drift assertion in `.github/workflows/ci.yaml` `buf` job
- Re-vendored exports in `ui/src/lib/gen/engram_pb.ts`: `StoreMemoryRequestSchema`, `StoreMemoryResponseSchema`, `StoreDiscoveryRequestSchema`, `StoreDiscoveryResponseSchema`, `UpdateMemoryRequestSchema`, `UpdateMemoryResponseSchema`, `DeleteMemoryRequestSchema`, `DeleteMemoryResponseSchema`, `SetVisibilityRequestSchema`, `SetVisibilityResponseSchema`, `ScheduleMemoryRequestSchema`, `ScheduleMemoryResponseSchema`, `CitationSchema`, `Visibility` enum, and the write methods on `EngramService`
- CSS tokens: `--destructive`, `--destructive-foreground`, `--color-destructive`, `--color-destructive-foreground` in `ui/src/app.css`

<verification>
- `cd ui && pnpm check` compiles against the re-vendored client
- `cd ui && pnpm test:node -- src/app.css.test.ts` green
- `diff gen/ts/engram/v1/engram_pb.ts ui/src/lib/gen/engram_pb.ts` empty
- `rg "ui/src/lib/gen" .github/workflows/ci.yaml` matches the new drift guard
</verification>

<success_criteria>
The console gen client exposes all 6 write RPCs + Citation/Visibility, re-vendoring is a reproducible drift-guarded task, and `--destructive` tokens resolve to the app's error color. Downstream plans (02, 03, 04) can now compile and render write UX.
</success_criteria>

<output>
Create `.planning/phases/19-console-write-ux/19-01-SUMMARY.md` when done. Record the re-vendor task shape, the CI drift-guard decision, and confirm write-RPC exports are present.
</output>
