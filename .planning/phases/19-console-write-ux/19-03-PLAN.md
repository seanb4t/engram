---
phase: 19-console-write-ux
plan: 03
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - ui/src/lib/components/DeleteConfirmDialog.svelte
  - ui/src/lib/components/DeleteConfirmDialog.browser.test.ts
  - ui/src/lib/components/ShareWarningInline.svelte
  - ui/src/lib/components/ShareWarningInline.browser.test.ts
  - ui/src/lib/components/MemoryRow.svelte
  - ui/src/lib/components/MemoryList.svelte
  - ui/src/lib/components/MemoryRow.browser.test.ts
  - ui/src/lib/components/MemoryDetail.svelte
  - ui/src/lib/components/MemoryDetail.browser.test.ts
autonomous: true
requirements: [REQ-console-write-ux]
must_haves:
  truths:
    - "Delete is guarded by a confirm dialog with the exact UI-SPEC copy before any delete fires (D-06)"
    - "private→shared shows an inline warning (accurate copy: sharing exposes the record to every authenticated caller and prior disclosure cannot be retracted) the operator must acknowledge before the write fires (D-07)"
    - "MemoryRow's root is NOT a <button>; the dropdown-menu trigger is a sibling of the selection control, not nested inside a button (Codex+grok HIGH: no button-in-button)"
    - "Each MemoryRow exposes edit/delete/share via a hover-revealed dropdown-menu; delete uses the `destructive` Button/DropdownMenu variant added in Plan 01 (D-02)"
    - "MemoryDetail exposes edit/delete/share action buttons next to the existing copy button (D-02)"
    - "The Share affordance is visibility-aware: `onshare` receives the full `Memory`, and the Share item is hidden/omitted in BOTH MemoryRow and MemoryDetail when the record is already shared (`memory.visibility === 'shared'`, normalizing `''` as private) — no dead re-share (Codex+grok round-2 MEDIUM). Making an already-shared record private again is NOT offered (locked D-07: private→shared is one-way)"
    - "Write affordances are mechanically suppressed when `memory.category === 'rule'` in BOTH MemoryRow and MemoryDetail (D-05 scope fence, enforced not assumed)"
  artifacts:
    - "ui/src/lib/components/DeleteConfirmDialog.svelte (reusable, kind-parameterized, onconfirm callback)"
    - "ui/src/lib/components/ShareWarningInline.svelte (inline banner, onconfirm/oncancel)"
    - "MemoryRow.svelte restructured non-button shell + sibling dropdown-menu + MemoryList.svelte callback passthrough"
    - "MemoryDetail.svelte action buttons with rule guard"
  key_links:
    - "--destructive token + `destructive` Button variant (Plan 01) → destructive DropdownMenu.Item + Dialog delete button + ShareWarningInline tone"
    - "onedit(id)/ondelete(id)/onshare(memory) callback props threaded route → MemoryList → MemoryRow (mirrors existing onselect passthrough); onshare carries the full Memory so the host knows visibility; suppressed when category==='rule'; Share item hidden when visibility==='shared'"
---

<objective>
Build the presentational write-action affordances (no mutation wiring yet — that lands in Plan 06):

- `DeleteConfirmDialog.svelte` (D-06): a reusable shadcn `Dialog` confirm, parameterized by kind (memory|discovery), using the exact UI-SPEC copy, with a destructive confirm button. Emits `onconfirm`.
- `ShareWarningInline.svelte` (D-07): an inline, destructive-toned banner stating the irreversibility of private→shared ("readable by every authenticated caller ... can't be narrowed back to private once shared"), with `Share anyway` / `Cancel`. Emits `onconfirm`/`oncancel`.
- `MemoryRow.svelte` + `MemoryList.svelte` (D-02): a hover-revealed `dropdown-menu` (kebab, `icon-sm` ghost) exposing Edit/Delete/Share; delete uses the `variant="destructive"` item. New `onedit(id)`/`ondelete(id)`/`onshare(memory)` callback props thread through `MemoryList` exactly like the existing `onselect`. `onshare` is visibility-aware — it passes the full `Memory` (the row already owns it) so downstream knows the current visibility, and the Share item is suppressed when `memory.visibility === 'shared'` (normalizing `''` as private) so an already-shared record shows no dead re-share.
- `MemoryDetail.svelte` (D-02): Edit/Delete/Share buttons (`Button variant="outline" size="sm"`) in the header bar next to the existing `copy` button.

These components are pure UI: they take callbacks and render; they do not import the write client or mutations. This lets them build in parallel with the transport (Plan 02) and be wired by the route integration (Plan 06). Rules get no affordance (D-05).

Purpose: deliver the destructive/sharing/edit surfaces with correct copy, tone, and density before wiring behavior.
Output: two new reusable components + row/list/detail extensions, each with browser tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-console-write-ux/19-CONTEXT.md
@.planning/phases/19-console-write-ux/19-UI-SPEC.md
@.planning/phases/19-console-write-ux/19-PATTERNS.md
@ui/src/lib/components/MemoryRow.svelte
@ui/src/lib/components/MemoryDetail.svelte
@ui/src/lib/components/MemoryList.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: DeleteConfirmDialog + ShareWarningInline (reusable destructive components)</name>
  <files>ui/src/lib/components/DeleteConfirmDialog.svelte, ui/src/lib/components/DeleteConfirmDialog.browser.test.ts, ui/src/lib/components/ShareWarningInline.svelte, ui/src/lib/components/ShareWarningInline.browser.test.ts</files>
  <read_first>
    - ui/src/lib/components/ui/dialog/index.ts (Dialog.Root/Header/Title/Description/Footer composition; open/onOpenChange)
    - ui/src/lib/components/MemoryDetail.svelte (Button + toast idiom lines 19-29/46; `text-cat-gotcha` inline-alert shape line 38)
    - .planning/phases/19-console-write-ux/19-UI-SPEC.md (Copywriting Contract rows: "Destructive confirmation — delete memory/discovery", "Sharing warning (D-07)"; Component Specs "Delete confirm dialog", spacing `p-4` body, `icon-sm`/`sm` sizing)
    - ui/src/lib/components/MemoryRow.browser.test.ts (browser-test conventions: vitest-browser-svelte render, getByText/getByRole, vi.fn callbacks)
    - .planning/phases/19-console-write-ux/19-PATTERNS.md (DeleteConfirmDialog + ShareWarningInline sections)
  </read_first>
  <action>
    Create `DeleteConfirmDialog.svelte`: a shadcn `Dialog` with `$bindable` `open`, a `kind: 'memory' | 'discovery'` prop, and an `onconfirm` callback prop. Render `Dialog.Title`/`Dialog.Description`/`Dialog.Footer` with the exact UI-SPEC copy per kind (memory: title `Delete this memory?`, body `this can't be undone. the record and its content are removed permanently.`; discovery: title `Delete this discovery?`, body `this can't be undone. the map/fact and its citations are removed permanently.`). Footer: `Cancel` (outline, closes) + `Delete` (`<Button variant="destructive">` — the real variant added in Plan 01 Task 3, consuming the `--destructive` token). Clicking `Delete` calls `onconfirm()` then closes; the component does not perform the delete itself.
    Create `ShareWarningInline.svelte`: an inline (non-modal) banner in the `text-cat-gotcha`/destructive tone (reuse `MemoryDetail`'s single-line inline-alert class shape). Addresses review concern (Codex+grok MEDIUM: the "can't be narrowed back" copy is technically false — a record CAN be unshared; only prior disclosure is irreversible). Use the accurate copy: `sharing makes this readable by every authenticated caller. you can stop sharing later, but you can't retract what's already been read.` Buttons: `Share anyway` (`<Button variant="destructive">` outline-styled, or the destructive variant) → `onconfirm()`; `Cancel` (outline) → `oncancel()`. `role="alert"`.
    Browser tests: assert exact copy renders (the accurate share sentence above), confirm button invokes `onconfirm`, cancel invokes `oncancel`/closes, and the delete confirm renders the kind-appropriate discovery vs memory copy.
  </action>
  <acceptance_criteria>
    - DeleteConfirmDialog renders the exact per-kind title/body strings and a `Button variant="destructive"` `Delete` button; `onconfirm` fires on Delete, not on Cancel
    - ShareWarningInline renders the accurate disclosure sentence (`...you can stop sharing later, but you can't retract what's already been read.`) and `Share anyway`/`Cancel`; `onconfirm`/`oncancel` fire respectively; `role="alert"` present
    - Copy is lowercase/no-exclamation per the console voice (titles keep their sentence-case `?` form as specified in UI-SPEC)
    - `cd ui && pnpm test:browser -- src/lib/components/DeleteConfirmDialog.browser.test.ts src/lib/components/ShareWarningInline.browser.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/engram/ui && pnpm test:browser -- src/lib/components/DeleteConfirmDialog.browser.test.ts src/lib/components/ShareWarningInline.browser.test.ts</automated>
  </verify>
  <done>Delete requires an explicit confirm; sharing requires an acknowledged irreversibility warning — both presentational, ready to wire.</done>
</task>

<task type="auto">
  <name>Task 2: MemoryRow hover dropdown-menu (edit/delete/share) + MemoryList passthrough</name>
  <files>ui/src/lib/components/MemoryRow.svelte, ui/src/lib/components/MemoryList.svelte, ui/src/lib/components/MemoryRow.browser.test.ts</files>
  <read_first>
    - ui/src/lib/components/MemoryRow.svelte (full file — CRITICAL: the root element is `<button type="button" onclick={() => onselect(memory.id)}>` at line 19; a dropdown-menu trigger button CANNOT be nested inside it)
    - ui/src/lib/components/MemoryList.svelte (full file — how `onselect`/`showScope` thread to MemoryRow line 21; mirror for new callbacks)
    - ui/src/lib/components/ui/dropdown-menu/index.ts (DropdownMenu.Root/Trigger/Content/Item; destructive item variant)
    - .planning/phases/19-console-write-ux/19-UI-SPEC.md (Component Specs "Inline row/detail actions (D-02)": kebab `icon-sm` ghost trigger, hover-revealed; Copywriting rows Edit/Delete/Share; Spacing note on `icon-sm` 28px)
    - .planning/phases/19-console-write-ux/19-PATTERNS.md (MemoryRow extend section)
    - .planning/phases/19-console-write-ux/19-REVIEWS.md (Codex+grok HIGH/MEDIUM on Plan 19-03: MemoryRow is itself a `<button>` — nested dropdown trigger is invalid interactive DOM; rule fence must be mechanically enforced)
  </read_first>
  <action>
    Addresses review concern (Codex+grok HIGH: `MemoryRow` root is a `<button>`; nesting a dropdown-menu trigger inside is invalid nested-interactive DOM with fragile `stopPropagation`).
    Restructure the `MemoryRow` shell so it is NOT a `<button>` and the dropdown trigger is a SIBLING of the selection control (not nested). Concrete target: change the root from `<button>` to a non-interactive container — a `<div class="relative ...">` — and make the selection region its own inner control (either an inner `<button type="button" class="w-full text-left ...">` wrapping the summary/meta and calling `onselect(memory.id)`, OR the div carrying `role="option"`/`tabindex`/`onclick`+`onkeydown` for selection). Place the kebab `DropdownMenu.Trigger` (an `icon-sm` ghost button) as a SIBLING inside the container's flex row, absolutely-positioned/hover-revealed, so no button is ever nested inside another button. Preserve the existing category color-bar, summary, meta, tags, `bg-accent` selected state, and hover styling.
    Add optional callback props `onedit?: (id: string) => void`, `ondelete?: (id: string) => void`, and — visibility-aware (Codex+grok round-2 MEDIUM) — `onshare?: (memory: Memory) => void` (passes the full record, NOT just the id, so the host can read the current visibility; the row already owns the `Memory`). Menu items: `Edit` (pencil, calls `onedit(memory.id)`), `Delete` (trash, destructive `DropdownMenu.Item`, calls `ondelete(memory.id)`), and `Share` (share icon, calls `onshare(memory)`). The trigger/menu must `stopPropagation`/`preventDefault` so opening the menu or clicking an item does not also fire the row's `onselect`. Only render the kebab when the callbacks are provided (keeps read-only routes unaffected).
    Share visibility gate (Codex+grok round-2 MEDIUM): render the `Share` item ONLY when the record is currently private — suppress it when `memory.visibility === 'shared'` (normalize a stored `''` as private). An already-shared record shows no Share item (the host must never receive a re-share for a shared record). Do NOT add a "Make private"/unshare item — that is an explicit non-goal under locked D-07 (private→shared is one-way).
    Mechanical D-05 rule fence (Codex+grok MEDIUM): suppress the entire kebab menu when `memory.category === 'rule'` — even if callbacks are passed — so a rule row never shows edit/delete/share. Do not rely on the parent omitting callbacks.
    Extend `MemoryList.svelte`: add matching optional `onedit`/`ondelete`/`onshare` props and thread them to each `<MemoryRow>` exactly as `onselect`/`showScope` are threaded today.
    Extend `MemoryRow.browser.test.ts`: assert (a) the row root is not a `<button>` and selection still fires `onselect`; (b) the kebab trigger appears, opening it exposes Edit/Delete/Share, `onedit`/`ondelete` fire with `memory.id` and `onshare` fires with the full `memory` object, and clicking a menu item does not also fire `onselect`; (c) NO kebab renders when callbacks are omitted; (d) NO kebab renders when `memory.category === 'rule'` even with callbacks supplied; (e) the `Share` item is ABSENT when `memory.visibility === 'shared'` (and present when private, including a stored `''`).
  </action>
  <acceptance_criteria>
    - MemoryRow's root element is NOT a `<button>`; the selection control and the kebab dropdown trigger are siblings (no button nested inside a button)
    - MemoryRow renders a hover-revealed `icon-sm` ghost kebab dropdown only when write callbacks are passed AND `memory.category !== 'rule'`; `onedit`/`ondelete` invoke with `memory.id`, `onshare` invokes with the full `memory`
    - The `Share` item is suppressed when `memory.visibility === 'shared'` (normalizing `''` as private); no "Make private"/unshare item exists (locked D-07 one-way)
    - Delete item uses the destructive variant; opening/clicking menu items does not bubble to `onselect`; selecting the row still fires `onselect`
    - MemoryList forwards `onedit`/`ondelete`/`onshare` to MemoryRow like `onselect`
    - Browser test asserts the rule-category suppression case, the already-shared Share-suppression case, and the not-a-button root
    - `cd ui && pnpm test:browser -- src/lib/components/MemoryRow.browser.test.ts` exits 0 (existing cases still pass)
  </acceptance_criteria>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/engram/ui && pnpm test:browser -- src/lib/components/MemoryRow.browser.test.ts</automated>
  </verify>
  <done>Every list row offers edit/delete/share without disturbing selection; read-only routes that omit the callbacks are unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: MemoryDetail action buttons (edit/delete/share)</name>
  <files>ui/src/lib/components/MemoryDetail.svelte, ui/src/lib/components/MemoryDetail.browser.test.ts</files>
  <read_first>
    - ui/src/lib/components/MemoryDetail.svelte (full file — header bar lines 42-47, the existing `copy` Button `variant="outline" size="sm"` to match)
    - ui/src/lib/components/MemoryDetail.browser.test.ts (existing browser-test conventions)
    - .planning/phases/19-console-write-ux/19-UI-SPEC.md (Component Specs "Inline row/detail actions (D-02)" — explicit action buttons in MemoryDetail header, same outline/sm styling)
    - .planning/phases/19-console-write-ux/19-PATTERNS.md (MemoryDetail extend section)
  </read_first>
  <action>
    Extend `MemoryDetail.svelte`: add optional `onedit?: (id) => void`/`ondelete?: (id) => void`/`onshare?: (memory: Memory) => void` callback props (`onedit`/`ondelete` invoked with `memory.id`; `onshare` visibility-aware, invoked with the full `memory` — Codex+grok round-2 MEDIUM). In the header bar next to the existing `copy` button, render `Edit`, `Share`, and `Delete` buttons as `Button variant="outline" size="sm"` (match the copy button exactly; Delete carries `variant="destructive"` — the real Plan 01 variant — but stays `size="sm"`). Render these only when the callbacks are provided AND `memory.category !== 'rule'` — enforce the rule guard mechanically here, do not assume the parent omits callbacks (Codex+grok MEDIUM). Suppress the `Share` button when `memory.visibility === 'shared'` (normalizing `''` as private) so an already-shared record shows visibility as state, not a dead re-share; do NOT add a "Make private"/unshare button (locked D-07 — private→shared is one-way). Do not render write actions for a read-only render or for rule records (D-05). Keep the existing loading/notFound/error branches and copy behavior untouched.
    Overflow note (Codex LOW): the detail pane is a fixed 360px (`MemoryDetail.svelte:32`); four header buttons (copy + edit + share + delete) may crowd it — use `size="sm"` icon-or-compact buttons and, if they don't fit, collapse edit/share/delete into a single `icon-sm` kebab `DropdownMenu` mirroring the row (keep copy standalone). Either layout is acceptable as long as all actions remain reachable and the guard holds.
    Extend `MemoryDetail.browser.test.ts`: assert Edit/Delete/Share render when callbacks are supplied (Edit/Delete fire with the memory id, Share fires with the full memory); assert they are absent when callbacks are omitted; assert they are absent when `memory.category === 'rule'` even with callbacks supplied; assert the Share button is absent when `memory.visibility === 'shared'` (present when private/`''`).
  </action>
  <acceptance_criteria>
    - MemoryDetail header shows Edit/Share/Delete only when write callbacks are passed AND `memory.category !== 'rule'`; Edit/Delete fire with `memory.id`, Share fires with the full `memory`; the Share button is suppressed when `memory.visibility === 'shared'`; Delete uses `variant="destructive"`
    - Actions fit the 360px pane (compact `size="sm"` or collapsed into an `icon-sm` kebab); copy button behavior unchanged
    - Existing copy/loading/error behavior unchanged
    - `cd ui && pnpm test:browser -- src/lib/components/MemoryDetail.browser.test.ts` exits 0 (includes the rule-suppression case)
  </acceptance_criteria>
  <verify>
    <automated>cd /Volumes/Code/github.com/seanb4t/engram/ui && pnpm test:browser -- src/lib/components/MemoryDetail.browser.test.ts</automated>
  </verify>
  <done>The detail panel offers the same edit/delete/share actions as the row, consistent with the existing copy-button styling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator intent → destructive action | Delete and private→shared are irreversible/high-consequence; the UI must require explicit acknowledgement |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-31 | Tampering | Accidental private→shared (one-way disclosure) | medium | mitigate | ShareWarningInline requires explicit `Share anyway` acknowledgement stating the disclosure is un-retractable before any write fires (D-07); accurate copy (unshare is possible; prior reads are not); no auto-share affordance |
| T-19-32 | Tampering | Accidental delete | medium | mitigate | DeleteConfirmDialog pre-confirm gate (D-06); no optimistic-undo path that could commit without confirm |
| T-19-33 | Elevation of Privilege | Rule write surface appearing in the console | low | mitigate | Mechanical `memory.category === 'rule'` suppression in BOTH MemoryRow and MemoryDetail (not merely "parent omits callbacks"); browser tests assert no affordance renders for rule records (D-05) |
| T-19-34 | Repudiation | Invalid nested-interactive DOM (button-in-button) breaking a11y tree / event dispatch | low | mitigate | MemoryRow root restructured to a non-button container with the kebab trigger as a sibling; browser test asserts the root is not a `<button>` (Codex+grok HIGH) |
| T-19-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages this phase (all shadcn primitives pre-vendored) |
</threat_model>

## Artifacts this phase produces

New symbols introduced by this plan:
- `ui/src/lib/components/DeleteConfirmDialog.svelte` (props: `open` bindable, `kind`, `onconfirm`)
- `ui/src/lib/components/ShareWarningInline.svelte` (props: `onconfirm`, `oncancel`)
- New optional callback props on `MemoryRow.svelte`, `MemoryList.svelte`, `MemoryDetail.svelte`: `onedit(id)`, `ondelete(id)`, `onshare(memory)` (share callback is visibility-aware — carries the full `Memory`); Share item/button suppressed when `visibility === 'shared'`
- Browser test files for the two new components; extended tests for row/detail

<verification>
- `cd ui && pnpm test:browser -- src/lib/components/DeleteConfirmDialog.browser.test.ts src/lib/components/ShareWarningInline.browser.test.ts src/lib/components/MemoryRow.browser.test.ts src/lib/components/MemoryDetail.browser.test.ts` green
- `cd ui && pnpm check` clean
- Destructive controls visibly styled via the real `destructive` Button variant (Plan 01)
- Browser tests assert: MemoryRow root not a `<button>`; rule-category suppression in row and detail
</verification>

<success_criteria>
D-02 row/detail actions, D-06 delete confirm, and D-07 share warning (accurate copy) exist as presentational components with correct copy and tone; MemoryRow is restructured to valid interactive DOM; D-05 honored mechanically (rule affordances suppressed by `category === 'rule'` guard). Ready for route wiring in Plan 06.
</success_criteria>

<review_incorporation>
## Cross-AI Review — findings addressed in this plan

- **Codex+grok HIGH (MemoryRow is itself a `<button>`):** Task 2 restructures the row root to a non-button container with the kebab trigger as a sibling; browser test asserts the root is not a button. RESOLVED.
- **Codex+grok HIGH (no `destructive` Button variant):** DeleteConfirmDialog + row/detail delete now use `<Button variant="destructive">`, the real variant added in Plan 01 Task 3. RESOLVED (variant lands in 19-01, consumed here).
- **Codex+grok MEDIUM (share-warning copy technically false):** ShareWarningInline copy corrected to be accurate (unshare is possible; prior disclosure is not retractable); UI-SPEC copy row aligned. RESOLVED.
- **Codex+grok MEDIUM (rule fence not mechanically specified):** Tasks 2 & 3 add an explicit `memory.category === 'rule'` suppression guard in BOTH MemoryRow and MemoryDetail with browser tests. RESOLVED.
- **Codex LOW (four header buttons overflow 360px pane):** Task 3 mandates compact `size="sm"` or a collapsed `icon-sm` kebab in the detail header. RESOLVED.
- **Round-2 Codex+grok MEDIUM (share callback is ID-only, can't know/enforce visibility):** `onshare` is now visibility-aware — it carries the full `Memory` (the row/detail already own it), and the Share item/button is suppressed IN MemoryRow AND MemoryDetail when `visibility === 'shared'` (normalizing `''` as private), rather than deferring the no-op to the host. RESOLVED (kept the visibility-aware half).
- **Round-2 Codex+grok MEDIUM — REJECTED under locked D-07 (bidirectional "Make private"/unshare):** the reviewers also want a "Make private"/unshare path (esp. for shared discoveries), noting the server technically supports `SetVisibility(private)` at `store.go:1571`. This is DECLINED: locked **D-07** makes private→shared explicitly ONE-WAY (the UI cannot narrow a record back to hidden; the share warning states disclosure can't be undone). Adding a bidirectional make-private affordance would violate a locked decision, so this plan does NOT render one. The tension is surfaced here so the user can revisit D-07 if desired; until then the one-way framing stands. Already-shared records surface visibility as state (no dead "Share").
- **Confirmed-SOUND (round-1, verified LANDED by all three round-2 reviewers — not churned):** the non-button MemoryRow shell, the mechanical rule fence in row+detail, the real `destructive` variant consumption, and the corrected unshare-vs-disclosure share copy are correct and kept.
</review_incorporation>

<output>
Create `.planning/phases/19-console-write-ux/19-03-SUMMARY.md` when done. Note the restructured (non-button) row shell, the mechanical rule guard, and the callback-prop passthrough shape used for row/list/detail so Plan 06 wires them consistently.
</output>
