---
phase: 04-migration-cli-first-customer
plan: 04
type: execute
wave: 4
depends_on:
  - 04-migration-cli-first-customer-03
files_modified:
  - cmd/engram/backfill.go
  - internal/store/store.go
  - internal/store/store_test.go
  - internal/store/migrate.go
  - internal/store/migratebacklog.go
  - internal/store/schemaversion_recallgate_test.go
  - internal/store/schemaversion_stamp_gate_test.go
  - cmd/engram/backfill_test.go
  - cmd/engram/operator_output_test.go
  - cmd/engram/destructive_test.go
  - cmd/engram/testdata/help.golden
  - cmd/engram/testdata/catalog.golden
  - internal/surfaces/toolclass.go
  - docs-site/src/content/docs/guides/upgrade.md
  - docs-site/src/content/docs/guides/cli.md
autonomous: true
requirements:
  - REQ-backfill-shortids-first-step
  - REQ-migrate-never-automatic
estimate:
  tokens: 64000
  raw_tokens: 32000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "The standalone `backfill-short-ids` command becomes a THIN delegating alias: its `--dry-run` flag and var are REMOVED outright (D-09), its bespoke RunE is dropped, and it routes through registerDestructive with preview/apply closures that delegate to `Store.Migrate` with defaults (Target=CurrentVersion, Steps=Registry) — Identical UX to the migrate family (D-10/D-11)."
    - "The alias emits the SHARED migrate envelope (migrateReportDoc: would_migrate/migrated/failed/passes/backlog/spared/appeared + explicit dry_run), NOT the legacy backfillOutputDoc (D-11)."
    - "The alias's APPLY path has FULL parity with `engram migrate --apply`, not merely its preview path (REVIEWS.md cycle-3 #7). Both closures call 04-03's shared `migrateSweepPreviewRun` / `migrateSweepApplyRun` functions verbatim — the alias does NOT call `Store.Migrate` with `DryRun:false` directly, which would skip the DryRun→manifest-limited-apply intersection the canonical command performs and make the 'thin identical alias' claim false for the write path. One sweep implementation exists in the binary; the alias is a flag-registration shell over it."
    - "The alias's `--timeout` value is actually READ: its closures pass `backfillTimeout` into 04-03's `migrateWithTimeout(ctx, d)` helper, which takes the duration as a parameter and treats 0 as 'disable' — exactly the semantics `backfill-short-ids` already ships (cmd/engram/backfill.go:39,80) and is already grouped under in `timeoutGroups`'s `zero-disables` entry. A preserved-but-unread flag is a dead flag the D-12 gate would then assert the existence of (REVIEWS.md N3)."
    - "`backfillShortIDsCmd.Deprecated` is non-empty and points at `migrate` (D-12; exact precedent migrateSetOwnerCmd.Deprecated at migrate.go:261) — soft deprecation, never hard removal."
    - "The alias KEEPS its `--timeout` flag with a 5-minute default (REVIEWS.md H8 — the timeout is NOT removed, unlike the prior plan claimed). The alias's preview and apply closures use the shared `migrateWithTimeout` helper (from 04-03) to enforce a finite deadline on every operator RPC path, exactly like the migrate family. The `--timeout` flag is documented in the upgrade guide as preserved, not removed."
    - "`Store.BackfillShortIDs` and `missingShortIDFilter` are DELETED as dead code once backfill.go no longer calls them; `Store.MintShortID` STAYS (the write path and the sweep's mint closure use it) (D-10)."
    - "`guides/upgrade.md` (the real path docs-site/src/content/docs/guides/upgrade.md) gains an entry documenting BOTH the `--dry-run` REMOVAL and the preview-by-default change for `backfill-short-ids` (the two behavioral breaks: bare invocation now PREVIEWS, `--apply` executes — previously it applied by default). The `--timeout` change is NOT documented as removed because it is PRESERVED (H8)."
    - "A BIDIRECTIONAL doc↔code gate (D-12) asserts BOTH: (i) the upgrade.md entry names the --dry-run removal / preview-by-default change for backfill-short-ids (exact strings); and (ii) the cobra command has no --dry-run flag and carries a non-empty Deprecated message pointing at migrate. The gate is proven RED by reverting either side, and never uses a len > 0 / presence proxy (memory x6v6qxqd6f)."
    - statement: "M10 — 'a record previously backfilled by the OLD standalone Store.BackfillShortIDs (short_id present, schema_version absent) converges through the alias' — is discharged BY COMPOSITION of two proofs that already exist, and the CLI-lane real-Qdrant test is FORMALLY DROPPED with the rationale written into Task 1 step 8 (REVIEWS.md C6-H7). Half one: 04-01 Task 3's `TestMigrateExistingShortIDPreserves` proves the behaviour against real pinned Qdrant at the store layer, where the fixture can actually be constructed — `payload()` unconditionally stamps `schema_version` (internal/store/store.go:646), so the state requires `Upsert` + raw `DeletePayload` and a raw client. Half two: this plan's apply-parity test proves the alias's recorded `Migrate` call sequence EQUALS `engram migrate --apply`'s. `cmd/engram` has zero container harness anywhere, and neither `internal/e2e/*` nor any new harness file is in this plan's `files_modified` — deliberately."
      verification: backstop
    - "Because the alias now delegates to the same Store.Migrate the phase forbids on startup, no migration runs automatically anywhere; the only automatic surface remains the non-blocking startup warning (REQ-migrate-never-automatic)."
    - "The three sibling conformance gates that still derive from `destructiveCommandNames()` are MIGRATED to `mutatingCommandNames()` in this plan (REVIEWS.md N1): `TestDestructiveCommandsRouteThroughGate` (destructive_test.go:130-152), `TestDestructiveCommandsExactFlagSet` (:248-270, BOTH directions), and `TestApplyFlagUsageComposesRuleSentence` (:200-220). None of them FAILS today — which is worse than failing: the safety net silently shrinks exactly as the gate widens, so a hand-rolled RunE or a stray bypass flag on `migrate`/`migrate revert`/`backfill-short-ids` would pass green. This plan is the correct wave for the switch because it is the wave in which the last mutating command gains `--apply`."
    - "`destructiveFlagCases` becomes `mutatingFlagCases` and gains rows for `migrateCmd` and `backfillShortIDsCmd` (the `migrateRevertCmd` row landed in 04-03), so the 'no escape hatch exists' flag-set EQUALITY covers every mutating command. The equality is never loosened; a new row is the sanctioned edit."
    - statement: "04-03's temporary `pendingApplyConversion` exclusion is DELETED **in Task 1, not Task 2** — in the SAME task that gives `backfill-short-ids` its `--apply` flag (INV-1; found by cycle 6's forward-reference sweep). The exclusion exists solely to hold `backfill-short-ids` out of the mutating set for the one wave in which it lacks `--apply`; the instant Task 1 adds the flag, `TestDestructiveCommandsRequireApply`'s SECOND direction (destructive_test.go:101-104, 'carries --apply but is not classified …') fires, because the live `--apply` carriers become six while `mutatingCommandNames()` still subtracts one. Task 1's own `<verify>` runs `go test ./cmd/engram/ -count=1` UNFILTERED, so deferring the deletion to Task 2 would leave Task 1 uncommittable. `TestMutatingCommandNamesMembership`'s pin moves from five names to six in the same edit. Task 2 keeps the three sibling-gate WIDENINGS, which are independent of the flag."
    - statement: "The help/catalog goldens are regenerated in **Task 1**, not Task 2 (INV-1; same sweep). Task 1 is what changes `backfill-short-ids`' flag set — it loses `--dry-run` and gains `--apply` — and `TestHelpGolden`/`TestCatalogGolden` walk the LIVE tree (golden_test.go:290, :306), so they go stale the moment Task 1 lands. `TestCatalogOutputFlagMatchesOperatorCommandsUnionClientVerbs` (cmdwalk_test.go:194-228) additionally decodes the committed `catalog.golden` directly. Task 1's unfiltered `go test ./cmd/engram/ -count=1` therefore cannot pass without the regeneration. Task 2 edits only `_test.go` files and moves no golden, so it re-runs `task surfaces:gen` as a VERIFY-ONLY confirmation that the tree is clean."
  prohibitions:
    - "backfill-short-ids MUST NOT be hard-removed — soft deprecation via the Deprecated field, matching the migrate-set-owner precedent."
    - "The legacy backfill envelope MUST NOT be preserved; the behavior is already breaking (D-09), so consumer compat is marginal and one migrate report shape is kept."
    - "Store.BackfillShortIDs / missingShortIDFilter MUST NOT be deleted before all callers are re-pointed to Store.Migrate; a grep must return zero live callers first."
    - "The --dry-run flag MUST NOT be deprecated (still present) — it is REMOVED outright, exactly as migrate-remap-owner removed its own (REMOVED, not deprecated)."
    - "The --timeout flag MUST NOT be removed — it is PRESERVED with the same 5-minute default (REVIEWS.md H8). The prior plan's proposal to remove it is REJECTED per H8: the operator tier's signal-only cancellation (registerDestructive) does not provide a deadline, and every operator RPC path must have a finite timeout."
    - "The D-12 bidirectional gate MUST assert doc AND code sides with exact strings; a len > 0 / presence proxy is forbidden (memory x6v6qxqd6f). The gate MUST assert the --dry-run removal and preview-by-default, but MUST NOT assert --timeout removal (because --timeout is preserved — H8)."
    - "The alias's apply closure MUST NOT call `st.Migrate(ctx, store.MigrateOptions{DryRun: false})` directly (REVIEWS.md cycle-3 #7) — that bypasses the DryRun→manifest intersection `engram migrate --apply` performs. It calls 04-03's `migrateSweepApplyRun`."
    - "The `--dry-run` flag and its `backfillDryRun` var are REMOVED from `cmd/engram/backfill.go`. **The gate is STRING-LITERAL-shaped, not bare-name (INV-2):** `! rg -n '\"dry-run\"' cmd/engram/backfill.go` — a cobra flag name is only ever a double-quoted string, so the flag cannot survive, while a comment recording the removal in prose is legal and no longer forbidden. The paired comment-text prohibition the prior revision carried is DELETED along with the bare-name gate it existed to prop up. The Go half of the property is `backfillShortIDsCmd.Flags().Lookup(\"dry-run\") == nil`, asserted in Task 3's D-12 gate; the var's removal is enforced by the compiler."
    - "The recall-gate allowlist row for the DELETED `Store.BackfillShortIDs` MUST be removed from internal/store/schemaversion_recallgate_test.go in the same change — `TestRecallEmissionSetIsCompleteAndClassified` is a set equality, so a row naming a function that no longer exists fails it just as loudly as a missing row."
    - "The `partialWriteClassification` row for the DELETED `Store.BackfillShortIDs` MUST be removed from internal/store/schemaversion_stamp_gate_test.go:370 in the same change (REVIEWS.md C4-H4). This is a SECOND, INDEPENDENT set-equality table from the recall gate above — that one covers READS (`Count`/`Scroll`/`ScrollAndOffset`), this one covers WRITES (`SetPayload`/`DeletePayload`/`OverwritePayload`) — and both name the same deleted method. Handling one and not the other leaves the suite RED."
    - statement: "The live-caller gate authorizing the deletion of `Store.BackfillShortIDs`/`missingShortIDFilter` MUST NOT exclude `_test.go` files (REVIEWS.md C4-H3) AND MUST NOT be a bare-name grep (INV-2). A `--glob '!**/*_test.go'` exclusion hides exactly the two files the deletion breaks — `internal/store/store_test.go` (seven call sites) and `cmd/engram/operator_output_test.go` — so it is vacuous; an unfiltered NAME grep matches two surviving doc comments in files this plan barely touches (`internal/store/migrate.go:60`, `internal/store/migratebacklog.go:42`), so it can never go green on a compliant implementation. The gate is CALL/DECLARATION-shaped: `! rg -n 'func \\(s \\*Store\\) BackfillShortIDs|func missingShortIDFilter|\\.BackfillShortIDs\\(|missingShortIDFilter\\(' internal/ cmd/`. Task 1 step 6 carries the discovery-vs-acceptance split and step 6d the independent prose repair."
      verification: backstop
    - "Deleting a test is a COVERAGE DECISION, not cleanup. Every behaviour `TestBackfillShortIDs`, `TestBackfillShortIDsHonorsCancel`, and `TestBackfillShortIDsResumesAfterMidRunFailure` asserted must be named against its replacement coverage on the `Store.Migrate` path before the test is removed, and any behaviour with no replacement gets a new test rather than a silent drop."
    - "`docs-site/src/content/docs/guides/upgrade.md`'s v0.8.4 section (:436-437) MUST be reconciled with the flag removal (REVIEWS.md C4-L3). It currently instructs `engram backfill-short-ids --dry-run` (which exits 2 after this plan) and bare `engram backfill-short-ids` to apply (which previews after this plan). The D-12 gate pins only the NEW `## Unreleased` entry, so without this the file self-contradicts across two sections while every gate stays green — a documentation surface that is wrong AND passing is worse than one that is merely wrong."
    - statement: "**EVERY negative gate in this plan matches the PROPERTY, not a name, and no comment can fail one (INV-2). The comment-stripping idiom `rg -v '^\\s*//' FILE | rg PAT` is used NOWHERE** — it strips only FULL-LINE comments, so a trailing comment on a code line survives it, and trailing comments are exactly what this plan's action text asks the executor to write. The permitted shapes in force, each named at its own site: CALL/DECLARATION (`func \\(s \\*Store\\) X`, `\\.X\\(`), ROW (`enclosingFunc: \"X\"`), STRING-LITERAL (`\"dry-run\"`), IMPORT (`\"os/signal\"`), and a section-scoped Go assertion for the `upgrade.md` removal claim. **Where no permitted shape expresses a property, the gate is DELETED and the property is asserted in Go instead** — that is why `store.MigrateOptions` in `backfill.go` has a POSITIVE adapter-count pin plus a call-sequence-equality test rather than a negative grep. Task 1's `<verify>` comment records each gate's shape beside the gate itself, which is the only place the mapping stays true."
      verification: backstop
    - "`rg -n \"timeout.*remov\" docs-site/.../guides/upgrade.md` MUST NOT be used as a whole-file gate (REVIEWS.md C5-L1) — it SELF-INVALIDATES against this plan's own permitted wording. Task 3 step 1 explicitly allows the `## Unreleased` entry to mention `--timeout` provided it says the flag is preserved, and a single natural sentence — \"`--timeout` is preserved; `--dry-run` is removed\" — trips the regex. Replace it with the same section-scoped Go assertion its C4-L3 sibling uses (reuse `extractUnreleasedSection`, cmd/engram/docsync_test.go:34-48): assert that no line in the guide's `## Unreleased` section claims `--timeout` was REMOVED, matching `--timeout[^\\n]{0,40}\\b(removed|no longer accepted|dropped)\\b` and failing with the offending line. Prove it goes RED once by temporarily writing such a claim, then revert."
    - "New Go files: this plan creates NONE, so no SPDX header is added — but `task license:check` RUNS in ALL THREE tasks' `<automated>` blocks (C6-L3), because the other plans create files in `.licenserc.yaml`-scoped paths, `license:check` is a SEPARATE CI job that `task` does not run (`.github/workflows/ci.yaml:180`), and this is the phase's last wave. A backstop that is asserted but never executed is not a backstop. `docs-site/**` stays excluded: never add a header to `upgrade.md` or `cli.md`."
---

<objective>
Fold `backfill-short-ids` into the migration mechanism as the thin delegating alias (its apply-by-default and `--dry-run` reconciled with `registerDestructive`'s preview-by-default; its `--timeout` PRESERVED per H8), delete the now-dead `Store.BackfillShortIDs`/`missingShortIDFilter`, and reconcile the behavior break (the `--dry-run` removal only) in `guides/upgrade.md` behind the D-12 bidirectional doc↔code gate. Add an integration test proving a pre-backfilled record (existing short_id, no schema_version) converges through the alias delegation, asserting the H1 carve-out makes deletion of the old backfill method safe (REVIEWS.md M10).

Key change from prior plan (H8): the `--timeout` flag is PRESERVED, not removed. The upgrade guide documents only the `--dry-run` removal and preview-by-default change. The prior plan's `--timeout` removal (M11 in the old plan) is REJECTED per REVIEWS.md H8: registerDestructive's signal-only cancellation does not provide a deadline, and removing the deadline from the backfill alias would create an unbounded Qdrant operation. The alias instead PASSES `backfillTimeout` into the shared `migrateWithTimeout(ctx, d)` helper from 04-03.

Three structural commitments this plan makes:
- **The alias's APPLY path goes through the same sweep as `engram migrate --apply`.** Both closures call 04-03's shared `migrateSweepPreviewRun`/`migrateSweepApplyRun`; `backfill.go` constructs no `store.MigrateOptions` of its own, so "thin identical alias" is structural rather than asserted, and a test compares the two recorded `Migrate` call sequences for equality.
- **The preserved `--timeout` is genuinely read** — its value travels into the shared run funcs as an argument (they own the deadline install), and three deadline cases (1s / default / 0-disables) are asserted behaviourally on the context the fake store receives.
- **The sibling conformance gates widen with the mechanism.** Task 2 moves `TestDestructiveCommandsRouteThroughGate`, `TestDestructiveCommandsExactFlagSet` (both directions), and `TestApplyFlagUsageComposesRuleSentence` to `mutatingCommandNames()`, adds the missing flag-case rows, and requires a RED-first observation of both widened gates. This is the correct wave because it is the wave in which the last mutating command gains `--apply`. (`TestDestructiveCommandsRequireApply` already switched in 04-03 Task 2, with `migrate`'s `--apply`.)

Purpose: This closes the "first customer" story — the standalone command becomes a delegation to the very mechanism it seeded (`Store.Migrate` + the v0→v1 step), one report shape for one mechanism (D-11), and the operator-facing upgrade guide names the breaking changes so a scripted `backfill-short-ids` call does not silently flip to preview.

Output: a delegating `backfill-short-ids` alias routed through registerDestructive (with preserved `--timeout` — H8), dead store code removed, an upgrade-guide entry documenting `--dry-run` removal and preview-by-default (NOT `--timeout` removal), the D-12 gate test, and an integration test for the pre-backfilled record state (M10).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/04-migration-cli-first-customer/04-CONTEXT.md
@.planning/phases/04-migration-cli-first-customer/04-RESEARCH.md
@.planning/phases/04-migration-cli-first-customer/04-PATTERNS.md
@.planning/phases/04-migration-cli-first-customer/04-REVIEWS.md

@cmd/engram/backfill.go (current shape — note the `--timeout` flag at backfill.go:37-40, 80; this is PRESERVED, not removed)
@cmd/engram/migrate.go (migrateSetOwnerCmd.Deprecated precedent)
@cmd/engram/migrate_family.go (PRODUCED BY 04-03: the shared `migrateSweepPreviewRun`/`migrateSweepApplyRun` run funcs this alias delegates to, `migrateWithTimeout(ctx, d)` which TAKES a duration, `migrateReportDoc`, and the `migrateFamilyStore`/`migrateFamilyStoreFromEnv` seam)
@cmd/engram/destructive_test.go (PRODUCED BY 04-03: `mutatingCommandNames()` and the temporary `pendingApplyConversion` this plan deletes; plus the three sibling gates this plan widens)
@internal/store/store.go (BackfillShortIDs :2741-2797, missingShortIDFilter :2729-2731, MintShortID :2668)
@internal/store/migrate.go (Store.Migrate with Manifest support from 04-01)
@docs-site/src/content/docs/guides/upgrade.md (the entry conventions; NEVER hard-code the shorthand "guides/upgrade.md")
</context>

<tasks>

<task type="auto">
  <name>Task 1: backfill-short-ids -> thin delegating alias through registerDestructive (with preserved --timeout — H8); delete dead store code; DELETE pendingApplyConversion and regenerate the goldens in this same task (INV-1)</name>
  <files>
    cmd/engram/backfill.go
    internal/store/store.go
    internal/store/migrate.go
    internal/store/migratebacklog.go
    internal/store/store_test.go
    internal/store/schemaversion_recallgate_test.go
    internal/store/schemaversion_stamp_gate_test.go
    cmd/engram/backfill_test.go
    cmd/engram/operator_output_test.go
    cmd/engram/destructive_test.go
    cmd/engram/testdata/help.golden
    cmd/engram/testdata/catalog.golden
    internal/surfaces/toolclass.go
  </files>
  <read_first>
    cmd/engram/backfill.go:19-82 (current shape — RunE owns apply-by-default + --dry-run + backfillTimeout + calls st.BackfillShortIDs; the `--timeout` flag and var at :20,39-42,80 with its shipped "0 disables" semantics — PRESERVED, not removed)
    cmd/engram/migrate_family.go (PRODUCED BY 04-03: `migrateSweepPreviewRun(ctx, cmd, outputFlag, timeout)`, `migrateSweepApplyRun(ctx, cmd, outputFlag, timeout)`, `migrateWithTimeout(ctx, d)` — the helper TAKES a duration, `migrateReportDoc` + its summary func, `migrateFamilyStoreFromEnv`. The alias calls the two shared run funcs; it does NOT re-derive the sweep)
    cmd/engram/migrate.go:261 (migrateSetOwnerCmd.Deprecated — the soft-deprecation precedent)
    cmd/engram/spine_review_purge.go (registerDestructive closure contract)
    internal/store/store.go (BackfillShortIDs :2741-2797 + missingShortIDFilter :2729-2731 to delete; MintShortID :2668 to keep)
    internal/store/store_test.go:5726-5900 (the SEVEN `st.BackfillShortIDs` call sites at :5762,:5777,:5802,:5823,:5861,:5871,:5893 across TestBackfillShortIDs/TestBackfillShortIDsHonorsCancel/TestBackfillShortIDsResumesAfterMidRunFailure, plus the absent-owner-key invariant its doc comment at :5726 names — REVIEWS.md C4-H3; read this BEFORE deleting the method, it is what the prior gate's `--glob '!**/*_test.go'` hid)
    cmd/engram/operator_output_test.go:169-174,374-395 (the `operatorParityRows()` backfill row at :171-172 and the `TestOperatorOutputEmpty` doc-map entry at :379 — both compile against `backfillSummary`/`backfillReportDoc`; the parity row is bidirectionally gated at :316-326 so it must be MIGRATED to the shared migrate formatter, not deleted — REVIEWS.md C4-H3)
    internal/store/schemaversion_recallgate_test.go:516-524 (the slice's restated entry-count doc phrase at :516, and the `Store.BackfillShortIDs` operatorMigrationEmitters row at :520-524 that must be DELETED with the method; :666 TestRecallEmissionSetIsCompleteAndClassified is a set equality in both directions — the READ boundary)
    internal/store/schemaversion_stamp_gate_test.go:364-375 (the `partialWriteClassification` table, whose `Store.BackfillShortIDs` row at :370 also names the deleted method; scan at :700, bidirectional equality at :714-736 — the WRITE boundary, a SECOND and INDEPENDENT table from the recall gate. REVIEWS.md C4-H4: the prior revision handled only the recall gate)
    .planning/phases/04-migration-cli-first-customer/04-01-PLAN.md ("Conformance Registry Impact ledger" — the phase-wide canonical table; this plan owns rows A5, A6, A11, A13, A14, A15, B2, B6, D4)
    .planning/phases/04-migration-cli-first-customer/04-CONTEXT.md (D-09 remove --dry-run, D-10 delegate + delete, D-11 shared envelope)
    .planning/phases/04-migration-cli-first-customer/04-REVIEWS.md (H8 — preserve --timeout, do NOT remove it; M10 — delegation inherits H1, add pre-backfilled convergence test; cycle-3 #7 — the alias APPLY path must go through the same preview→manifest intersection; N3 — the preserved flag must actually feed the helper)
  </read_first>
  <action>
In `cmd/engram/backfill.go`:
1. REMOVE (not deprecate) the `--dry-run` flag registration and its `backfillDryRun` var (D-09). REMOVE the bespoke custom RunE — its hand-rolled signal handling is replaced by registerDestructive's RunE.
2. PRESERVE the `--timeout` flag and its `backfillTimeout` var (REVIEWS.md H8 — the timeout is NOT removed), INCLUDING its shipped default (`5 * time.Minute`) and its shipped usage string ending "(0 disables); also cancellable via Ctrl-C".
   **`backfillTimeout` is PASSED AS AN ARGUMENT, not used at a call site in this file (REVIEWS.md C6-H4).** Step 3 makes both closures one-line adapters over `migrateSweepPreviewRun`/`migrateSweepApplyRun`, and per 04-03 those two functions install the deadline THEMSELVES (`ctx, cancel := migrateWithTimeout(ctx, timeout); defer cancel()`). So **`backfill.go` never calls `migrateWithTimeout` and must not** — doing so would install a second, nested, redundant deadline purely to satisfy a grep. The flag is genuinely read because its value travels into the shared helper as the fourth argument, and the proof is BEHAVIOURAL: step 7's three-case deadline test observes the deadline on the context the fake store receives. Delete the hand-rolled `signal.NotifyContext` + inline `context.WithTimeout` block at :36-43 — `registerDestructive` installs the signal handling and the shared run funcs install the deadline. `backfill-short-ids` keeps its existing `zero-disables` `timeoutGroups` membership unchanged.
3. Rebuild the command as a thin delegate: keep `backfillShortIDsCmd` (`Use: "backfill-short-ids"`) routed through `registerDestructive` (AddCommand BEFORE, per the contract) with two closures that are ONE-LINE ADAPTERS over 04-03's shared sweep run funcs — **this is the cycle-3 #7 fix**:
   - preview closure -> `return migrateSweepPreviewRun(ctx, cmd, backfillOutput, backfillTimeout)`.
   - apply closure -> `return migrateSweepApplyRun(ctx, cmd, backfillOutput, backfillTimeout)`.
   `migrateSweepApplyRun` is the function that performs `Migrate(DryRun:true)` → capture manifest → `Migrate(Manifest:...)`, so the alias's `--apply` gets the SAME preview→fresh-re-derivation intersection, the same Spared/Appeared reporting, and the same envelope as `engram migrate --apply`. **Do NOT write a `st.Migrate(ctx, store.MigrateOptions{DryRun: false})` call in backfill.go** — a direct non-manifest apply is precisely the divergence cycle-3 flagged, and it would silently give the alias different semantics from the command it aliases.
   Reuse the shared `migrateReportDoc`/summary funcs via those run funcs — do NOT define a second backfill-specific report doc (D-11). Drop the legacy `backfillOutputDoc`/`backfillReportDoc`/`backfillSummary` (dead once the alias shares the migrate envelope).
   No comment in `backfill.go` may contain the literal `dry-run` (this task's verify gate negative-greps that file for it).
4. Soft-deprecate with `backfillShortIDsCmd.Deprecated = "use: engram migrate"` (D-12; exact precedent migrateSetOwnerCmd.Deprecated) — never remove the command.
5. Keep the existing `backfill-short-ids` toolclass row in `internal/surfaces/toolclass.go` (already Destructive:false, named exactly this path) unchanged — the alias keeps routing through registerDestructive once the gate is generalized (done in 04-03); do NOT reclassify it.

In `internal/store/store.go`:
6. DELETE `Store.BackfillShortIDs` (:2741-2797) and `missingShortIDFilter` (:2729-2731) as dead code (D-10). KEEP `Store.MintShortID` (:2668) — the write path AND the sweep's mint closure both call it.

   **The caller sweep MUST NOT exclude test files (C4-H3) AND MUST NOT be a bare-name grep (C5-H1).** Both halves matter, and the prior two revisions got one each.

   **Discovery step (run FIRST, before deleting anything):** the UNFILTERED, name-shaped grep
   ```
   rg -n "BackfillShortIDs|missingShortIDFilter" internal/ cmd/
   ```
   is the right tool to BUILD the work list — it finds everything, including the two doc comments. Its 27-odd hits are exactly steps 6b and 6c below, plus `cmd/engram/backfill.go` (step 3), the two classification tables (steps 6a, 6a2), the declarations themselves, and the two cross-reference comments (step 6d).

   **Acceptance gate (run LAST, and it is a DIFFERENT pattern):**
   ```
   ! rg -n 'func \(s \*Store\) BackfillShortIDs|func missingShortIDFilter|\.BackfillShortIDs\(|missingShortIDFilter\(' internal/ cmd/
   ```
   **Why the acceptance gate is call-shaped rather than the discovery grep (INV-2).** The unfiltered NAME grep also matches two explanatory doc comments in files this plan barely touches — `internal/store/migrate.go:60` and `internal/store/migratebacklog.go:42` — so as a GATE it could never go green on a compliant implementation: it would fail unconditionally, the inverse of the vacuous-gate defect and just as bad. The call-shaped pattern asserts the real property ("no declaration and no call site survives") and leaves cross-referencing prose alone. Verified against the shipped tree: `migrate.go:60` writes `BackfillShortIDs store.go:2741` (no paren) and `migratebacklog.go:42` writes `missingShortIDFilter (store.go:2726-2731)` (SPACE before the paren), so neither matches; every real site does.

**6d. Repair the two cross-reference doc comments — for PROSE ACCURACY, not to satisfy the gate.** Both cite line numbers of functions this task deletes, so after step 6 they point at nothing:
   - `internal/store/migrate.go:60` — inside `Store.Migrate`'s "why it re-derives instead of threading a cursor" comment, which contrasts it with the three sweeps that DO page a cursor: `(Reindex store.go:3133, BackfillShortIDs store.go:2741, RemapOwner store.go:2950)`. Drop the `BackfillShortIDs store.go:2741` clause; `Reindex` and `RemapOwner` still make the contrast, so the paragraph's argument survives intact. Do NOT rewrite the paragraph.
   - `internal/store/migratebacklog.go:42` — inside `backlogFilter`'s `IsEmpty`-arm caveat, citing the two sibling filters whose doc comments carry the same three-valued-`NewIsEmpty` caveat: `ownerlessFilter (store.go:2631-2636) and missingShortIDFilter (store.go:2726-2731)`. Drop the `missingShortIDFilter` clause and leave `ownerlessFilter` as the surviving sibling.
   **These edits are deliberately NOT what makes the gate green** — the call-shaped gate above passes with or without them, which is the point: a gate must not depend on an edit in a file it does not own (that coupling is what made the prior revision's gate unsatisfiable). They are here because a comment citing a deleted symbol at a stale line number is a small, real decay, and this is the only wave that can see it. Both files are in `files_modified` for this reason alone.

**6a. In `internal/store/schemaversion_recallgate_test.go` (the READ boundary — ledger row B6):** DELETE the `operatorMigrationEmitters` row whose `enclosingFunc` is `"Store.BackfillShortIDs"` (:520-524). `TestRecallEmissionSetIsCompleteAndClassified` asserts the three-way partition is set-equal to the DERIVED emission set (`:666-745`), so a row naming a function that no longer exists surfaces as "extra (observed but not expected)". That slice's doc comment restates an entry count at `:516` — replace the count phrase with a non-counting sentence rather than decrementing it, for the same reason 04-02 does so in the stamp gate: a restated count is a second, unenforced assertion that goes stale silently. No grep gates this; it is an action obligation recorded in the SUMMARY.

**6a2. In `internal/store/schemaversion_stamp_gate_test.go` (the WRITE boundary — a SECOND, INDEPENDENT set-equality table; REVIEWS.md C4-H4, ledger row B2):** DELETE the `partialWriteClassification` row whose `enclosingFunc` is `"Store.BackfillShortIDs"` (`:370`). `TestPartialWritePathsAreClassifiedNonStamping` (`:698`) derives every `SetPayload`/`DeletePayload`/`OverwritePayload` site by AST-scanning the whole `internal/store` package directory (`:700`) and asserts set equality in BOTH directions (`:714-736`); a row naming a deleted method fails with "stale classification entry" exactly as loudly as a missing row fails with "has no classification entry". **This table is not the recall gate.** The two are adjacent, similarly shaped, and name the same method — handling one and skipping the other is the specific miss cycle 4 recorded. 04-02 will already have replaced this slice's restated doc-comment entry count with a non-counting sentence; if it has not, do so here.

**6b. In `internal/store/store_test.go` (SEVEN live call sites the prior gate hid — REVIEWS.md C4-H3):** `st.BackfillShortIDs` is called at `:5762`, `:5777`, `:5802`, `:5823`, `:5861`, `:5871`, `:5893`, across three test functions — `TestBackfillShortIDs` (:5741), `TestBackfillShortIDsHonorsCancel` (:5810), and `TestBackfillShortIDsResumesAfterMidRunFailure` (:5837). Deleting the method without touching these is a compile failure in `internal/store`, which halts wave 4.

   **Migrate the COVERAGE before deleting the tests.** For each of the three, name its replacement explicitly in the SUMMARY; do not drop a behaviour silently:
   - `TestBackfillShortIDs` — dry-run-then-apply, mint-once, second-run-is-a-no-op, plus the absent-owner-key invariant its doc comment at `:5726` names. Replacement: 04-01's `TestMigrateV0ToV1MintEndToEnd` (bare legacy record mints + stamps + second run reports `Backlog 0`) and `TestMigrateExistingShortIDPreserves` (an existing `short_id` is never re-minted). **Carry the absent-owner-key invariant across explicitly** — assert in the migrate-path test that a record with no `owner` key still has none after the sweep's payload-only `SetPayload`; if 04-01's tests do not already assert it, add that assertion HERE rather than losing it.
   - `TestBackfillShortIDsHonorsCancel` — context cancellation propagates. Replacement: the alias's own timeout-wiring test (step 7 below, the `--timeout 1s` case) plus `Store.Migrate`'s existing context handling. If no test asserts that a cancelled context aborts the sweep, add one to `internal/store/migrate_test.go`-style coverage in this task rather than dropping it.
   - `TestBackfillShortIDsResumesAfterMidRunFailure` — mid-run failure then resume. Replacement: the shipped `TestMigratePartialFailureResume` (internal/store/migrate_faultinject_test.go:310) already covers exactly this on the sweep path, for the same store, with a stronger fault injector.
   Then delete the three functions and any helper **they alone** use.
   **The same naming obligation extends to the two `cmd/engram` tests this wave invalidates (REVIEWS.md C6-L9), which step 7 rewrites:** `TestBackfillCmdHasDryRunAndTimeoutFlags` (`cmd/engram/backfill_test.go:8-13`) asserts the `--dry-run` flag EXISTS, which step 1 removes; and `TestBackfillSummaryUnchanged` (`:33-39`) pins the deleted `backfillSummary` output literals. Name each one's replacement in the SUMMARY exactly as for the three `store_test.go` deletions — the flag assertion is replaced by Task 3's D-12 gate (`Lookup("dry-run") == nil` AND `Lookup("timeout") != nil`), and the formatter pin is replaced by the `operatorParityRows` `backfill-short-ids` row migrated to `migrateSummary`/`migrateReportDoc` in step 6c. Deleting a test is a coverage decision wherever it happens, not only in `internal/store`. **Do NOT delete `newTestStore` (internal/store/store_test.go:82)** — it is the package-wide collection-prefix seam every other test in `internal/store` builds through, and `TestEveryStoreConstructionRoutesThroughSeam` (collectionprefix_conformance_test.go:300-330) exists specifically to catch a test that bypasses it (ledger row B8). Before deleting any helper, grep for other callers. Re-run `go test ./internal/store/ -count=1` unfiltered.

**6c. In `cmd/engram/operator_output_test.go` (references the formatters step 3 drops — REVIEWS.md C4-H3, ledger rows A5/A6):** step 3 deletes `backfillSummary`, `backfillOutputDoc`, and `backfillReportDoc` from `backfill.go`. Two sites in this file compile against them:
   - `operatorParityRows()`'s `backfill-short-ids` row at `:169-174` (`text: backfillSummary(29, false)` at `:171`, `doc: backfillReportDoc(29, false)` at `:172`). **Migrate the row to the shared migrate formatter**, not delete it: `TestOperatorOutputParity` gates the row set bidirectionally against `operatorCommands()` (`:316-326`), and `backfill-short-ids` is still a live operator command, so removing the row fails with "operatorParityRows() is missing a row for operator command". Rebuild it from ONE `store.MigrateResult` fixture driven through `migrateSummary`/`migrateReportDoc` — the same shape 04-03 used for its three new rows — with `facts` naming the numbers the migrate sentence states.
   - `TestOperatorOutputEmpty`'s hardcoded doc map at `:379` (`"backfill-short-ids": backfillReportDoc(0, false)`). This map is NOT set-gated, so it only breaks at COMPILE time — which is why no set-equality failure message points at it. Replace the entry with `migrateReportDoc` over a zero-valued `store.MigrateResult`, and confirm the "must not contain \"null\" anywhere" assertion still holds: `MigrateResult.Spared`/`.Appeared` are `[]string` (04-01), and a nil slice marshals to `null`, so the migrate report doc must render them as empty arrays (`[]`) or omit them — fix the report doc's construction, never the assertion. **This step REPLACES the `backfill-short-ids` entry; it does NOT add a `migrate status` entry** — `statusReportDoc`'s null-safety is gated by 04-03 Task 2's own direct assertion, not by this map (C6-L4).
   - **Two more sites the discovery grep in step 6 does NOT find (REVIEWS.md C6-L10).** `cmd/engram/backfill_test.go:35` and `:38` carry `t.Errorf("backfillSummary(3, true) = %q, want %q", …)` — FORMAT STRINGS, not call sites, so `rg -n "BackfillShortIDs|missingShortIDFilter"` misses them entirely (different name family) while the step-6c acceptance gate `backfill(Summary|ReportDoc|OutputDoc)\(` DOES match them. They are inside `TestBackfillSummaryUnchanged`, which step 7 rewrites — so the finished tree is correct — but the executor's worklist must name them, or the gate fires on a file the worklist said was done.

In `cmd/engram/backfill_test.go`:
7. Rewrite existing backfill tests to the alias contract:
   - Bare `backfill-short-ids` previews (DryRun, no writes, shared migrate envelope with `dry_run:true` and `would_migrate` populated) — the behavior REVERSAL from apply-by-default.
   - `backfill-short-ids --apply` executes with the shared envelope (`dry_run:false`).
   - **Apply-path parity (cycle-3 #7), the load-bearing new assertion:** with a fake store injected through `migrateFamilyStoreFromEnv`, assert `--apply` calls `Migrate` exactly TWICE — first with `DryRun:true` and a nil `Manifest`, then with a non-nil `Manifest` equal to the first call's returned `PreviewManifest` and `DryRun:false`. Assert the SAME two-call sequence for `engram migrate --apply` in the same test table and assert the two recorded sequences are equal. **Write the cheapest-passing-violator first:** an alias that calls `Migrate` once with `{DryRun:false}` records a one-call sequence and goes RED here; a "the alias produced some output" assertion would not.
   - The command carries NO `--dry-run` flag (`cmd.Flags().Lookup("dry-run") == nil`) BUT DOES carry `--timeout` (`cmd.Flags().Lookup("timeout") != nil` — H8).
   - Timeout wiring (N3): `backfill-short-ids --timeout 1s` yields a deadline ≤ ~2s away on the context the fake store receives; no `--timeout` yields ~5m; `--timeout 0` yields `ctx.Deadline()` `ok == false`. A hard-coded helper or an unread flag goes RED on the first and third cases.
   - `backfillShortIDsCmd.Deprecated` is non-empty and contains "migrate".
**8. M10 is DISCHARGED BY COMPOSITION, and the CLI-lane real-Qdrant test is FORMALLY DROPPED — read the rationale before looking for a harness (REVIEWS.md C6-H7).**

   The obligation is: *a record in the state the old `Store.BackfillShortIDs` produced — `short_id` present, `schema_version` absent — converges through the alias without error and without re-minting.* The prior revision discharged it with "against real pinned Qdrant … invoke `backfill-short-ids --apply`" from `cmd/engram/backfill_test.go`. **That test cannot be written where it was put.** `cmd/engram` has ZERO container harness — `rg -l 'testcontainers|StartQdrant|dockertest'` over the module returns only `internal/store/{store_test.go,migrate_converge_test.go,migrate_faultinject_test.go,schemaversion_recallgate_test.go}`, `internal/server/tools_test.go`, `internal/e2e/harness_test.go` and `internal/retrievaleval/retrieval_eval_test.go`, and nothing under `cmd/`. Every shipped `cmd/engram` Qdrant test exercises the UNREACHABLE path through the in-process `runClient` harness. Worse, the fixture itself needs a raw client: `payload()` unconditionally stamps `schema_version` (`internal/store/store.go:646`), so "short_id present, schema_version absent" can only be built by `Upsert` + raw `DeletePayload` — which is precisely why `internal/store` ships `seedLegacyRecord`. Building an unplanned container harness inside a task that already carries the alias rebuild, three registry-row deletions, a golden regeneration, the `pendingApplyConversion` deletion and a doc repair is not a trade worth making, and relocating it to `internal/e2e` widens this plan's blast radius to a package it otherwise never touches.

   **The two halves that compose to the same proof, both already owned and both against real pinned Qdrant:**
   - **The store-level carve-out.** 04-01 Task 3 step 10's `TestMigrateExistingShortIDPreserves` seeds exactly this state and drives it through `Store.Migrate` with default options, asserting `Backlog == 0`, `short_id` UNCHANGED, `schema_version == 1`, and no other key added or removed. That is the entire behavioural claim, at the layer where it can actually be constructed.
   - **The delegation equality.** Step 7's apply-parity test asserts that the alias's recorded `Migrate` call sequence and `engram migrate --apply`'s are EQUAL — two calls, `DryRun:true` then a non-nil `Manifest` — with the alias's `--apply` reaching `migrateSweepApplyRun` and nothing else. Since `backfill.go` constructs no options of its own (step 3), there is no third behaviour for a CLI-lane test to discover: the alias either invokes the same sweep with the same options or it fails that equality.

   Composed, they say: *the sweep handles the state (proved against real Qdrant), and the alias invokes exactly that sweep (proved by call-sequence equality).* A CLI-lane real-Qdrant run would re-exercise the first half through the second and add no discriminating power. **Record this downgrade explicitly in the SUMMARY**, naming both halves, so it reads as a decision rather than an omission.

   **What this task DOES add in `cmd/engram/backfill_test.go`:** `TestBackfillPreBackfilledRecordDelegates` — a fake-store test asserting that a bare and an `--apply` invocation of the alias reach `migrateSweepPreviewRun`/`migrateSweepApplyRun` with `backfillOutput` and `backfillTimeout`, and that no `store.MigrateOptions` value originates in `backfill.go`. No container, no `internal/e2e` file, and no addition to `files_modified`.

**9. DELETE `pendingApplyConversion` and update the membership pin to SIX — HERE, in this task, not Task 2 (INV-1; found by cycle 6's forward-reference sweep).** In `cmd/engram/destructive_test.go`:
   - Delete `pendingApplyConversion` and the subtraction of it inside `mutatingCommandNames()`, plus its doc comment (which references a now-completed conversion). **KEEP `applyRoutedAdditions`** — it is permanent, and `backfill-short-ids` remains one of its two named members.
   - Update `TestMutatingCommandNamesMembership`'s pinned set from 04-03's five wave-3 names to the SIX wave-4 names: `{migrate, migrate revert, migrate-remap-owner, prune-expired, spine-review purge, backfill-short-ids}`. 04-03 declared this pin with a doc comment naming this plan as its updater.
   **Why this cannot wait for Task 2.** Step 3 above gives `backfill-short-ids` an `--apply` flag. The instant it does, the live `--apply` carriers become SIX while `mutatingCommandNames()` still subtracts one — and `TestDestructiveCommandsRequireApply`'s SECOND direction (destructive_test.go:101-104) fires on `backfill-short-ids`: "carries --apply but is not classified …". This task's own `<verify>` runs `go test ./cmd/engram/ -count=1` UNFILTERED, so leaving the deletion to Task 2 makes THIS task uncommittable. The exclusion's entire justification was the one-wave window in which the alias lacked `--apply`; step 3 closes that window, so the deletion belongs in the same commit. Task 2 keeps the three sibling-gate WIDENINGS, which do not depend on the flag.

**10. Regenerate the help/catalog goldens — HERE, in this task (INV-1; same sweep).** Run `task surfaces:gen` and commit what it produces. Step 1 removed `--dry-run` and step 3 added `--apply`, so `backfill-short-ids`' flag set changed; `TestHelpGolden`/`TestCatalogGolden` walk the LIVE tree (golden_test.go:290, :306) and `TestCatalogOutputFlagMatchesOperatorCommandsUnionClientVerbs` (cmdwalk_test.go:194-228) decodes the committed `catalog.golden` directly, so all three are RED until the regeneration lands. Do NOT hand-edit either golden — `task surfaces:gen` is deliberately excluded from `task default`/`test`/`lint` precisely so this stays an explicit, reviewable step (Taskfile.yaml:257-264). `git diff` on the goldens must show `backfill-short-ids` losing `--dry-run` and gaining `--apply`, and nothing else. **This is the phase's THIRD and final regeneration** (04-03 T1 for the rule sentence, 04-03 T3 for the three new commands, this one for the alias) — one per wave that changes the tree. **After it, run `go test ./internal/server/ -run 'TestCuratingSpineVerbTableMatchesCuratingMemory|TestSupersedeDocsMatchShippedContract' -count=1`** (ledger rows D5/D6): `surfaces:gen` rewrites the anchored region of `curating-memory/SKILL.md`, which two `internal/server` gates read back for entirely unrelated tables.
  </action>
  <acceptance_criteria>
    - **`go test ./cmd/engram/ -count=1` and `go test ./internal/store/ -count=1` both pass, unfiltered.** The unfiltered runs are the acceptance gate for steps 6b/6c: a compile break in `store_test.go` or `operator_output_test.go` surfaces as a package-level build failure that no `-run` selector can reach.
    - **C4-L4 closed:** the named-subset runs are TWO separate invocations, never one combined command. `-run` is a package-wide flag, so a second occurrence supersedes the first and one of the two packages silently runs nothing:
      `go test ./cmd/engram/ -run 'TestBackfill|TestMigrate' -count=1 -v` **and, separately,**
      `go test ./internal/store/ -run 'TestRecallEmissionSetIsCompleteAndClassified|TestPartialWritePathsAreClassifiedNonStamping' -count=1 -v`
      (alias preview/apply + shared envelope + no --dry-run + preserved-and-read --timeout + Deprecated + pre-backfilled record convergence — M10; plus both classification gates).
    - **cycle-3 #7 AND C6-H4 closed together, by a POSITIVE structural pin that matches the thin-adapter design instead of contradicting it:** `test "$(rg -o 'migrateSweep(Preview|Apply)Run\(ctx, cmd, backfillOutput, backfillTimeout\)' cmd/engram/backfill.go | wc -l)" = 2` — the two one-line adapters, with `backfillTimeout` visibly passed as the fourth ARGUMENT. **The prior revision's `rg -n "migrateWithTimeout\(ctx, backfillTimeout\)" cmd/engram/backfill.go` is DELETED**: per 04-03 the shared run funcs install the deadline themselves, so satisfying that grep would mean either a nested redundant timeout or dead comment-only text, and it was mutually exclusive with step 3's own instruction. The apply-parity test asserts the alias's and `engram migrate`'s recorded `Migrate` call sequences are EQUAL (two calls: DryRun-then-Manifest), which is what actually proves `backfill.go` constructs no options of its own.
    - **N3 closed BEHAVIOURALLY:** the three-case deadline test passes against the context the fake store receives (`--timeout 1s` → ≤2s; no flag → ~5m; `--timeout 0` → `ok == false`). A hard-coded helper or an unread flag goes RED on the first and third cases. There is no grep for `migrateWithTimeout` in this file, because this file correctly does not call it.
    - `! rg -n '"dry-run"' cmd/engram/backfill.go` holds — STRING-LITERAL-shaped (a cobra flag name is only ever a quoted string), so the flag and its registration are gone while a comment recording the removal is legal (INV-2). `backfillDryRun`'s removal is enforced by the compiler.
    - `rg -n "timeout" cmd/engram/backfill.go` returns the `--timeout` flag registration AND the `backfillTimeout` var (PRESERVED — H8).
    - `! rg -n '"os/signal"' cmd/engram/backfill.go` holds — IMPORT-shaped (INV-2): the hand-rolled signal handling is gone, so the import is gone, and Go's unused-import error makes the two inseparable.
    - **C4-H3 + C5-H1 closed — the gate excludes neither the evidence of its own violation NOR the prose that documents the codebase:** `! rg -n 'func \(s \*Store\) BackfillShortIDs|func missingShortIDFilter|\.BackfillShortIDs\(|missingShortIDFilter\(' internal/ cmd/` holds, with **no `--glob '!**/*_test.go'` exclusion**. It is only satisfiable once `internal/store/store_test.go`'s seven call sites and `cmd/engram/operator_output_test.go`'s three references are migrated (C4-H3's point), and it is satisfiable AT ALL because it is call-shaped rather than name-shaped (C5-H1's point — the prior unfiltered name grep matched two surviving doc comments in files no plan owned and could never go green). `rg -n "func \(s \*Store\) MintShortID" internal/store/store.go` still finds the kept minter.
    - **The two cross-reference doc comments are repaired for accuracy, independently of the gate (step 6d):** `rg -o 'BackfillShortIDs store.go:2741' internal/store/migrate.go | wc -l` returns 0 and `rg -o 'missingShortIDFilter \(store.go' internal/store/migratebacklog.go | wc -l` returns 0, while both surrounding paragraphs still make their original arguments via `Reindex`/`RemapOwner` and `ownerlessFilter` respectively. **Confirm the independence explicitly:** the call-shaped gate above passes BEFORE these two edits as well as after — a gate that only goes green once a file it does not own is edited is the coupling that made the prior revision's version unsatisfiable.
    - **`pendingApplyConversion` is deleted in THIS task (step 9, INV-1):** `! rg -n '^\s*var pendingApplyConversion' cmd/engram/destructive_test.go` holds (DECLARATION-shaped per INV-2 — a comment noting the deletion cannot begin a line with `var`), `go build ./cmd/engram/...` succeeds (a surviving reference to a deleted identifier is a compile error), `rg -n "applyRoutedAdditions" cmd/engram/destructive_test.go` still finds the permanent named set, and `go test ./cmd/engram/ -run 'TestMutatingCommandNamesMembership|TestDestructiveCommandsRequireApply' -count=1 -v` passes with the pin at SIX names. Deferring this to Task 2 leaves this task's own unfiltered run RED on `TestDestructiveCommandsRequireApply`'s second direction.
    - **The goldens are regenerated in THIS task (step 10, INV-1):** `go test ./cmd/engram/ -run 'TestHelpGolden|TestCatalogGolden|TestCatalogOutputFlagMatchesOperatorCommandsUnionClientVerbs' -count=1` passes, `git diff cmd/engram/testdata/` shows `backfill-short-ids` losing `--dry-run` and gaining `--apply` and nothing else, and neither golden was hand-edited. `go test ./internal/server/ -run 'TestCuratingSpineVerbTableMatchesCuratingMemory|TestSupersedeDocsMatchShippedContract' -count=1` passes (ledger rows D5/D6 — `surfaces:gen` touched `curating-memory/SKILL.md`).
    - **C4-H4 closed — BOTH classification tables:** `! rg -n 'enclosingFunc: "Store.BackfillShortIDs"' internal/store/schemaversion_recallgate_test.go internal/store/schemaversion_stamp_gate_test.go` holds (ROW-shaped per INV-2 — the property is "no ROW names the deleted method", so a replacement doc sentence explaining which row was removed stays legal), and `go test ./internal/store/ -run 'TestRecallEmissionSetIsCompleteAndClassified|TestPartialWritePathsAreClassifiedNonStamping' -count=1` passes with no "stale classification entry" on either.
    - `! rg -n 'func backfillSummary|func backfillReportDoc|func backfillOutputDoc|backfill(Summary|ReportDoc|OutputDoc)\(' cmd/ internal/` holds — the three deleted formatters have no surviving DECLARATION or CALL SITE, including in `operator_output_test.go`. CALL-SHAPED per INV-2: step 6c instructs the executor to MIGRATE the parity row FROM `backfillSummary`, and a comment or SUMMARY-facing note recording that migration must not fail the gate.
    - `TestOperatorOutputParity` still carries a `backfill-short-ids` row (MIGRATED to `migrateSummary`/`migrateReportDoc`, not deleted — the row set is bidirectionally gated against `operatorCommands()` and `backfill-short-ids` is still a live operator command), and `TestOperatorOutputEmpty` passes with the migrate doc, emitting no literal `null`.
    - Coverage migration is recorded, not implied: the SUMMARY names, for each of the three deleted `store_test.go` functions, the test that now covers its behaviour, and states explicitly whether the absent-owner-key invariant and the context-cancellation assertion were found already covered or added here.
    - `go build ./cmd/engram/... ./internal/store/...` succeeds (no dangling caller of the deleted symbols).
    - `backfillShortIDsCmd.Deprecated` is non-empty and names "migrate".
    - **M10 is discharged by composition and the downgrade is RECORDED, not implied (C6-H7):** the SUMMARY names both halves — 04-01 Task 3's `TestMigrateExistingShortIDPreserves` (real pinned Qdrant, store layer) and this task's alias call-sequence equality — and states why a CLI-lane real-Qdrant run adds no discriminating power. No container harness is built in `cmd/engram`, and `internal/e2e/*` is not in `files_modified`.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd/engram/ -count=1 &amp;&amp; go test ./internal/store/ -count=1 &amp;&amp; go test ./internal/server/ -run 'TestCuratingSpineVerbTableMatchesCuratingMemory|TestSupersedeDocsMatchShippedContract' -count=1 &amp;&amp; go build ./cmd/engram/... ./internal/store/... &amp;&amp; { ! rg -n 'func \(s \*Store\) BackfillShortIDs|func missingShortIDFilter|\.BackfillShortIDs\(|missingShortIDFilter\(' internal/ cmd/; } &amp;&amp; { ! rg -n 'func backfillSummary|func backfillReportDoc|func backfillOutputDoc|backfill(Summary|ReportDoc|OutputDoc)\(' internal/ cmd/; } &amp;&amp; { ! rg -n 'enclosingFunc: "Store.BackfillShortIDs"' internal/store/schemaversion_recallgate_test.go internal/store/schemaversion_stamp_gate_test.go; } &amp;&amp; test "$(rg -o 'migrateSweep(Preview|Apply)Run\(ctx, cmd, backfillOutput, backfillTimeout\)' cmd/engram/backfill.go | wc -l | tr -d ' ')" = 2 &amp;&amp; rg -n "timeout" cmd/engram/backfill.go &amp;&amp; { ! rg -n '"dry-run"' cmd/engram/backfill.go; } &amp;&amp; { ! rg -n '"os/signal"' cmd/engram/backfill.go; } &amp;&amp; { ! rg -n '^\s*var pendingApplyConversion' cmd/engram/destructive_test.go; } &amp;&amp; task license:check</automated>
    <!-- INV-2 sweep, every gate matching the PROPERTY rather than a name, with NO comment-stripping
         pipeline anywhere:
         * BackfillShortIDs/missingShortIDFilter and the three backfill formatters -> CALL/DECLARATION
           -shaped, so the two surviving cross-reference doc comments (internal/store/migrate.go:60,
           migratebacklog.go:42) cannot fail them. The formatter pattern DOES match the two t.Errorf
           format strings at cmd/engram/backfill_test.go:35,38 — real hits, named in step 6c's
           worklist, removed by step 7's rewrite.
         * The two classification tables -> ROW-SHAPED (`enclosingFunc: "…"`), so a replacement doc
           sentence naming the removed row stays legal.
         * `"dry-run"` -> STRING-LITERAL-shaped (a cobra flag name is always quoted).
         * `"os/signal"` -> IMPORT-shaped; Go's unused-import error ties it to the deleted block.
         * `^\s*var pendingApplyConversion` -> DECLARATION-shaped; `go build` catches any survivor.
         * store.MigrateOptions has NO negative gate: no permitted shape expresses it without being
           failed by the prohibition comment, so it is replaced by the POSITIVE adapter-count pin
           above plus step 7's call-sequence-equality assertion. That pin is also C6-H4's fix — it
           agrees with the thin-adapter design instead of contradicting it. -->

  </verify>
  <done>backfill-short-ids is a flag-registration shell over 04-03's shared migrateSweepPreviewRun/migrateSweepApplyRun, so its APPLY path performs the identical DryRun→manifest-limited-apply intersection as `engram migrate --apply` (cycle-3 #7 closed) and shares the migrate envelope; it lost its --dry-run but PRESERVES a --timeout whose value actually feeds migrateWithTimeout with 0-disables intact (H8/N3); it is soft-deprecated toward migrate; the dead BackfillShortIDs/missingShortIDFilter and their recall-gate row are gone with MintShortID intact; and the M10 integration test proves pre-backfilled records converge safely.</done>
</task>

<task type="auto">
  <name>Task 2: Widen the three sibling conformance gates to the mutating set and delete 04-03's temporary pendingApplyConversion (REVIEWS.md N1)</name>
  <files>
    cmd/engram/destructive_test.go
  </files>
  <precondition>Task 1 has landed: `backfill-short-ids` carries `--apply` and routes through `registerDestructive`; `pendingApplyConversion` is deleted; `TestMutatingCommandNamesMembership` pins SIX names; and `cmd/engram/testdata/{help,catalog}.golden` are regenerated. Assert with `go test ./cmd/engram/ -count=1` (unfiltered, green) before starting — every widening below assumes the last mutating command already gained its flag, and step 6's RED-first experiments are only valid from a GREEN baseline.</precondition>
  <read_first>
    cmd/engram/destructive_test.go:23-36 (destructiveCommandNames — `op.Class.Destructive`), :130-152 (TestDestructiveCommandsRouteThroughGate — proves RunE IS registerDestructive's closure via runtime.FuncForPC substring match; the //go:noinline pragma is what keeps that durable), :200-220 (TestApplyFlagUsageComposesRuleSentence), :231-238 (destructiveFlagCases — keyed by the Go identifier, never a string literal), :240-270 (TestDestructiveCommandsExactFlagSet — flag-set EQUALITY plus BOTH coverage directions)
    cmd/engram/destructive_test.go (as 04-03 and this plan's Task 1 left it: **`mutatingCommandNames()` = `destructiveCommandNames() ∪ applyRoutedAdditions`** — a NAMED union following the `operatorCommandExclusions` precedent — with `pendingApplyConversion` and its subtraction already DELETED by Task 1 step 9, and `TestMutatingCommandNamesMembership` already pinned to the SIX wave-4 names. **This is NOT `!op.Class.ReadOnly && op.CLICommand != ""` (REVIEWS.md C4-H1, restated because the prior revision's `read_first` described the rejected predicate here while its own action precondition below described the correct one — C5-M5).** That predicate selects ELEVEN commands against the live `surfaces.Operations()` table, seven of which carry no `--apply`. Note the SEPARATE and legitimate `!ReadOnly`: `registerDestructive`'s ADMISSION predicate in `destructive.go` genuinely is `!class.ReadOnly` — that one is correct and must stay. The two answer different questions and collapsing them is the original defect.)
    cmd/engram/migrate_family.go (migrateCmd / migrateRevertCmd flag sets, from 04-03)
    cmd/engram/backfill.go (the alias's final flag set, from Task 1 of this plan)
    Taskfile.yaml:257-264 (`task surfaces:gen` — the only sanctioned golden regeneration path)
    .planning/phases/04-migration-cli-first-customer/04-REVIEWS.md (N1 — the three sibling gates that silently stop covering the new mutating commands; the safety net shrinks as the gate widens)
  </read_first>
  <action>
This task exists because widening `registerDestructive`'s gate without widening the gates that police it leaves a hole that PASSES GREEN. `TestDestructiveCommandsRouteThroughGate` and `TestDestructiveCommandsExactFlagSet` are what make the `--apply` mechanism unbypassable; while they iterate `destructiveCommandNames()` only, a hand-rolled RunE or a stray bypass flag on `migrate`, `migrate revert`, or `backfill-short-ids` is invisible to both. This is the wave to fix it: `backfill-short-ids` gained `--apply` in Task 1, so every mutating command now satisfies the widened gates.

**Precondition, assert before starting (REVIEWS.md C4-H1).** `mutatingCommandNames()` must be `destructiveCommandNames() ∪ applyRoutedAdditions` — a NAMED additive-routed union, **not** the rejected `!op.Class.ReadOnly && op.CLICommand != ""` predicate — with `pendingApplyConversion` already gone (Task 1 step 9). Confirm by running `go test ./cmd/engram/ -run TestMutatingCommandNamesMembership -count=1 -v` and checking it passes against the **SIX-name wave-4 pin** (`rg -n "applyRoutedAdditions" cmd/engram/destructive_test.go` should also find the named set). Use the membership test, not a grep for the rejected predicate — a negative grep would also match the legitimate `firstAdditiveRoutedTopLevelCommandName` helper, `registerDestructive`'s CORRECT admission predicate, and any doc comment explaining the rejection (INV-2). **If that pin is absent or the `!ReadOnly` predicate is what shipped, STOP** — every gate this task widens is already RED on seven unrelated commands (`store`, `reindex`, `summarize-missing`, `serve`, `migrate-set-owner`, `spine-review archive`, `spine-review restore`), and step 6's RED-first experiments would prove nothing, because a gate that is already failing cannot demonstrate that YOUR deliberate violation is what broke it. That is the C4-H1 cascade; fix the derivation in 04-03's file first.

In `cmd/engram/destructive_test.go`:
1. **`pendingApplyConversion`'s deletion and the membership pin's move to six are DONE — they landed in Task 1 step 9, not here (INV-1).** The exclusion exists solely to cover the one-wave window in which `backfill-short-ids` lacks `--apply`, and Task 1 step 3 closes that window; deferring the deletion to this task would leave Task 1's own unfiltered `go test ./cmd/engram/ -count=1` RED on `TestDestructiveCommandsRequireApply`'s second direction ("carries --apply but is not classified …"). Assert the outcome as this task's precondition rather than performing it: `! rg -n '^\s*var pendingApplyConversion' cmd/engram/destructive_test.go` holds (DECLARATION-shaped per INV-2) and `go build ./cmd/engram/...` succeeds, and `applyRoutedAdditions` — permanent — still names both `migrate` and `backfill-short-ids`.
2. Switch `TestDestructiveCommandsRouteThroughGate` (:131) from `destructiveCommandNames()` to `mutatingCommandNames()`. Update its doc comment to say the structural gate now covers every MUTATING command, and update the "%q not found in the live tree" / "want a closure installed by registerDestructive" messages from "destructive command" to "mutating command". `migrate`, `migrate revert`, and `backfill-short-ids` all route through `registerDestructive` after Task 1, so this passes — and it now goes RED if any of them is later given a hand-rolled RunE.
3. Switch `TestApplyFlagUsageComposesRuleSentence` (:205) from `destructiveCommandNames()` to `mutatingCommandNames()`. The `--apply` Usage of every mutating command is composed from `surfaces.RuleDestructiveRequiresApply.Sentence` (which 04-03 changed to "a mutating operator command …"), so this widening is what makes the sentence assertion cover the commands the sentence now describes.
4. RENAME `destructiveFlagCases` to `mutatingFlagCases` and switch BOTH coverage directions of `TestDestructiveCommandsExactFlagSet` (:260-269) from `destructiveCommandNames()` to `mutatingCommandNames()`. The reverse direction's message becomes "…names %q which is not currently classified mutating". Keep the table keyed by the Go identifier (`*cobra.Command` var), never by a name string — that is what stops it from being read as a second membership list. Keep the equality; do not loosen it.
5. ADD the missing rows so both directions are satisfied (the `migrateRevertCmd` row already landed in **04-03 Task 3 step 1b**, alongside the identifier it references):
   - `{migrateCmd, []string{"apply", "output", "timeout"}}`
   - `{backfillShortIDsCmd, []string{"apply", "output", "timeout"}}`
   Derive each `want` from the command's ACTUAL registered flags by reading the source, not by guessing; if the equality fails, the correct fix is to correct the row or the command, never to relax `reflect.DeepEqual`.
6. Do NOT add a row for `migrate status` — it is `ReadOnly:true`, so it is not in `mutatingCommandNames()` and has no `--apply`. A row for it would fail the reverse direction, which is the gate working correctly.
7. **The golden regeneration is DONE — it landed in Task 1 step 10, not here (INV-1).** Task 1 is what changed `backfill-short-ids`' flag set (`--dry-run` out, `--apply` in), and `TestHelpGolden`/`TestCatalogGolden` walk the LIVE tree, so Task 1's unfiltered run could not have passed without it. This task edits only `_test.go` files and moves no golden. Run `task surfaces:gen` once as a VERIFY-ONLY confirmation and assert `git status --porcelain -- cmd/engram/testdata/` reports NOTHING — a non-empty diff here means either Task 1 skipped its regeneration or this task changed the live tree in a way it was not supposed to. Never hand-edit a golden.
  </action>
  <acceptance_criteria>
    - `go test ./cmd/engram/ -run 'TestDestructive|TestApplyFlagUsageComposesRuleSentence|TestHelpGolden|TestCatalogGolden' -count=1 -v` passes.
    - `! rg -n '^\s*var pendingApplyConversion' cmd/engram/destructive_test.go` holds — 04-03's temporary exclusion is gone (deleted in **Task 1 step 9**, asserted here as a standing property; DECLARATION-shaped per INV-2, so a note recording the deletion is legal). `rg -n "applyRoutedAdditions" cmd/engram/destructive_test.go` still finds the permanent named set (C4-H1's derivation survives the exclusion's deletion).
    - `TestMutatingCommandNamesMembership` pins SIX names and passes; `TestApplyRoutedAdditionsArePinned` still passes (both `applyRoutedAdditions` entries resolve to live `ReadOnly:false, Destructive:false` table rows).
    - `git status --porcelain -- cmd/engram/testdata/` reports NOTHING after this task's verify-only `task surfaces:gen` — Task 1 step 10 already regenerated them, and this task changes no live-tree surface.
    - `rg -o "destructiveCommandNames\(\)" cmd/engram/destructive_test.go | wc -l` returns the count of remaining intentional uses only — specifically, `firstDestructiveTopLevelCommandName` may still select a Destructive:true command for `TestDestructiveGatePreventsMutation`, but NONE of `TestDestructiveCommandsRequireApply`, `TestDestructiveCommandsRouteThroughGate`, `TestApplyFlagUsageComposesRuleSentence`, or `TestDestructiveCommandsExactFlagSet` may still call it. Verify by reading each of the four, not by the count alone.
    - `rg -n "mutatingFlagCases" cmd/engram/destructive_test.go` finds the renamed table with rows for `migrateCmd`, `migrateRevertCmd`, and `backfillShortIDsCmd` in addition to the three pre-existing rows.
    - **The widened gates are proven non-vacuous, RED-first (memory x6v6qxqd6f):** the executor temporarily (a) assigns `backfillShortIDsCmd.RunE` by hand instead of via `registerDestructive` and observes `TestDestructiveCommandsRouteThroughGate` FAIL, and (b) adds a throwaway `--force` bool flag to `migrateCmd` and observes `TestDestructiveCommandsExactFlagSet` FAIL. Both experiments are reverted; the observed failure output is recorded in the plan SUMMARY. Before this task neither experiment failed — that is the coverage hole N1 names.
      **Each experiment is only valid from a GREEN baseline.** Run the three widened gates unmodified first and confirm they pass; only then inject the violation, and confirm the failure names the command you broke (`backfill-short-ids` in (a), `migrate` in (b)) rather than an unrelated set. A failure listing seven other commands means the C4-H1 derivation regression is back and the experiment proved nothing — revert and fix the derivation, do not record the failure as evidence.
    - The goldens' `backfill-short-ids` flag-set change was committed in **Task 1 step 10**; this task's `task surfaces:gen` produces no further diff.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd/engram/ -count=1 &amp;&amp; go test ./cmd/engram/ -run 'TestDestructive|TestApplyFlagUsageComposesRuleSentence|TestMutatingCommandNamesMembership|TestApplyRoutedAdditionsArePinned|TestHelpGolden|TestCatalogGolden' -count=1 -v &amp;&amp; go build ./cmd/engram/... &amp;&amp; { ! rg -n '^\s*var pendingApplyConversion' cmd/engram/destructive_test.go; } &amp;&amp; rg -n "mutatingFlagCases" cmd/engram/destructive_test.go &amp;&amp; test -z "$(git status --porcelain -- cmd/engram/testdata/)" &amp;&amp; task license:check</automated>
  </verify>
  <done>The routing gate, the flag-set-equality gate, and the rule-sentence gate all iterate the MUTATING command set, so the safety net widened with the mechanism instead of shrinking behind it (N1 closed); the flag-case table covers migrate, migrate revert, and backfill-short-ids; 04-03's temporary pendingApplyConversion is deleted; and both widened gates were observed RED against a deliberately violating implementation before being accepted.</done>
</task>

<task type="auto">
  <name>Task 3: guides/upgrade.md entry documenting the --dry-run removal + preview-by-default change (NOT --timeout — H8) + D-12 bidirectional doc↔code gate</name>
  <files>
    docs-site/src/content/docs/guides/upgrade.md
    docs-site/src/content/docs/guides/cli.md
    cmd/engram/backfill_test.go
  </files>
  <read_first>
    docs-site/src/content/docs/guides/upgrade.md (existing entry conventions — never hard-code the shorthand path)
    cmd/engram/backfill.go (the Deprecated message + removed --dry-run + PRESERVED --timeout the gate asserts)
    .planning/phases/04-migration-cli-first-customer/04-CONTEXT.md (D-12 exact strings, prove-RED, never a len > 0 proxy; memory x6v6qxqd6f)
    .planning/phases/04-migration-cli-first-customer/04-REVIEWS.md (H8 — --timeout is PRESERVED, so the upgrade guide does NOT document its removal — only --dry-run removal and preview-by-default; C6-M11 — §5's "four of the six" group enumeration is already false against the shipped ten-member group and must be amended, not left alone; C6-M5 — guides/cli.md:143-150's `backfill-short-ids --dry-run` paragraph becomes false in this wave)
    docs-site/src/content/docs/guides/upgrade.md:135-150 (§5's heading at :135 and its stale group enumeration at :142-144 — the sentence step 1c amends)
    docs-site/src/content/docs/guides/cli.md:143-150 (the two-idiom-split paragraph step 1b rewrites; note the anchored region at :135 is tool-owned and must not be hand-edited)
    cmd/engram/operator_output_test.go:445-465 (`timeoutGroups` — the LIVE group membership §5's prose is supposed to describe; the `zero-disables` entry spans :451-460 and lists TEN commands at :454-457 before this phase adds three)
  </read_first>
  <action>
In `docs-site/src/content/docs/guides/upgrade.md`:
1. Add a marked entry under the upgrade-guide for this change (match existing date/version-section convention). The entry MUST name, in plain text:
   - The `--dry-run` REMOVAL and preview-by-default change for `backfill-short-ids` (bare invocation now PREVIEWS, `--apply` executes — previously it applied by default).
   - A pointer to `engram migrate` (e.g. "use `engram migrate` for the schema-version migration; scripts that ran bare `backfill-short-ids` to apply must now pass `--apply'").
   - The entry MUST NOT claim the `--timeout` is removed — it is PRESERVED with its default and its "0 disables" semantics intact (H8/N3). If the entry mentions `--timeout` at all, it must state that it is preserved and unchanged.
   Use exact strings the D-12 gate will grep.

**1c. AMEND §5's `--timeout` group ENUMERATION — it is already stale today, and this phase makes it worse (ledger row D10; REVIEWS.md C6-M11).** `docs-site/src/content/docs/guides/upgrade.md:142-144` reads:

   > **This is the opposite convention from four of the six operator commands' own `--timeout`** (`reindex`, `prune-expired`, `summarize-missing`, `backfill-short-ids`, where `0` still disables the deadline, unchanged).

   Against the shipped tree the `zero-disables` group has TEN members — those four plus all six `spine-review` leaves, which were added in an earlier milestone (`cmd/engram/operator_output_test.go:454-457`). So "four of the six" is false BEFORE this phase touches anything, and this phase adds `migrate`, `migrate status` and `migrate revert` to the same group. Nothing reads §5 back, which is why it drifted.

   Replace the parenthetical enumeration with a claim that cannot go stale on the next addition: state that the operator tier's own `--timeout` treats `0` as "disable the deadline" on every command EXCEPT `migrate-remap-owner`/`migrate-set-owner`, which reject it (see §6), and point at `guides/cli.md`'s three-group table for the live membership rather than re-listing it here. **Do NOT re-enumerate the group** — a second hand-maintained list is what produced this finding. Keep §6 as written; it is about a different change and is still accurate. Make this edit in the same `## Unreleased`-adjacent pass as step 1, and note in the SUMMARY that the sentence was stale on arrival.

**1b. Repair the SECOND stale `guides/cli.md` paragraph — the one that only becomes false in THIS wave (ledger row D9, second half; REVIEWS.md C6-M5).** `docs-site/src/content/docs/guides/cli.md:143-150` reads:

   > Every other mutating operator command — `reindex`, `summarize-missing`, and `backfill-short-ids` — is classified **non-destructive** and keeps its pre-existing opt-in **preview** idiom, `--dry-run`.

   After Task 1 that is flatly false for `backfill-short-ids`: it has no `--dry-run`, previews by default, and mutates only under `--apply` — which is the very break this plan exists to document. `task surfaces:gen` rewrites only the anchored `destructive-requires-apply` span at `:135` and `TestSurfaceConformanceProseFiles` checks anchored sentences, so nothing catches this. Rewrite the paragraph: `reindex` and `summarize-missing` keep the `--dry-run` preview idiom; `backfill-short-ids` has moved to the `--apply` idiom and is now an alias for `engram migrate`; and the two-idiom split's boundary sentence must be re-derived accordingly rather than left asserting a boundary that no longer sits where it says. 04-03 Task 1 step 8c owns the paragraph ABOVE the anchor (the destructive-command list); this step owns the one below. Add a pointer to the `## Unreleased` upgrade entry. `docs-site/src/content/docs/guides/cli.md` is in this plan's `files_modified` for this step.

**1a. Reconcile the v0.8.4 section, which currently instructs the removed flag (REVIEWS.md C4-L3; ledger row D4).**
   `docs-site/src/content/docs/guides/upgrade.md:436-437` (under `## v0.8.4 — Memories now carry a short_id handle`, subsection "Backfilling existing records") ships this block:
   ```
   engram backfill-short-ids --dry-run          # run this first: preview without writing
   engram backfill-short-ids                    # apply to all memories
   engram backfill-short-ids --timeout 5m       # custom wall-clock limit
   ```
   After this plan, line 1 exits 2 (unknown flag) and line 2 PREVIEWS instead of applying — the exact inversion of what the comment claims. Line 3 stays correct (`--timeout` is preserved — H8). The D-12 gate pins only the NEW `## Unreleased` entry, so this section would self-contradict while every gate stayed green.
   Fix it in place, keeping the historical section's voice: the preview line becomes the BARE invocation (`engram backfill-short-ids` — which now previews) and the apply line becomes `engram backfill-short-ids --apply`, with their trailing `#` comments updated to match. Add a one-line pointer to the `## Unreleased` entry and to `engram migrate`. Do NOT rewrite the v0.8.4 narrative or restate the whole break here — a superseded-by pointer plus corrected commands is the minimum that makes the page self-consistent. The corrected block must contain NO occurrence of the literal `--dry-run`, which is exactly what the section-scoped gate below asserts.
   **Gate it with a SECTION-SCOPED assertion, not a whole-file negative grep.** A whole-file `! rg -- '--dry-run' upgrade.md` would DIRECTLY CONTRADICT the D-12 gate below, which requires the `## Unreleased` entry to name the `--dry-run` removal — the two could never both pass. Two gates that cannot both be green is the same defect class as a test asserting mutually exclusive postconditions; do not ship it.
   Instead, add a THIRD assertion to `TestUpgradeGuideReconcilesBackfill` (step 2), written in Go where it can section-scope:
   - Split the guide at its `## Unreleased` heading using the same logic `extractUnreleasedSection` already implements (`cmd/engram/docsync_test.go:34-48`: find the heading, take up to the next `## `). Reuse that shipped helper rather than writing a second splitter — it is in the same package.
   - Assert the literal `--dry-run` appears ONLY inside the `## Unreleased` section, i.e. the guide's remainder (everything before and after that section) contains zero occurrences. That is the real property — "no historical section instructs a flag the binary no longer accepts" — and it stays green while the Unreleased entry documents the removal.
   - Fail with a message naming the offending line, so the next person to reintroduce an instruction in an old section sees which one.
   Prove it goes RED once: temporarily restore `engram backfill-short-ids --dry-run` in the v0.8.4 block, observe the failure, revert.
   Note for the executor: `TestUpgradeGuideNamesEveryChangedCommand` (`docsync_test.go:72-100`) scans ONLY `## Unreleased`, so edits to the v0.8.4 section are invisible to it — which is precisely why this third assertion is needed and why it must be scoped to the complement of that section rather than to the whole file.

In `cmd/engram/backfill_test.go`:
2. Add the D-12 bidirectional gate test. It MUST assert BOTH directions with EXACT strings (never a `len > 0`/presence proxy):
   - (i) read the file at `docs-site/src/content/docs/guides/upgrade.md` (the real path; resolve relative to the repo root, not a hard-coded CWD) and assert it contains a string naming the `--dry-run` removal for `backfill-short-ids` AND a string naming preview-by-default / the `--apply` requirement. The gate MUST NOT assert `--timeout` removal (H8 — timeout is preserved).
   - (ii) assert on the cobra command, in the same test, that `backfillShortIDsCmd.Flags().Lookup("dry-run") == nil` AND `backfillShortIDsCmd.Flags().Lookup("timeout") != nil` (preserved — H8) AND `backfillShortIDsCmd.Deprecated` is non-empty AND is set to exactly `"use: engram migrate"` (or a substring contract — pick one exact contract and use it on BOTH sides).
   Prove-RED discipline: the test must fail if EITHER side is changed (doc entry removed OR command re-gains --dry-run / loses --timeout / loses Deprecated). Do not gate on `len(Deprecated) > 0` alone. Arrange the test so its two assertions are separable so an executor can demonstrate a revert of either side failing RED.

**2a. The `--timeout`-not-removed check becomes a FOURTH, SECTION-SCOPED assertion — the whole-file grep self-invalidates (REVIEWS.md C5-L1).**
   A whole-file `! rg -n "timeout.*remov" docs-site/…/guides/upgrade.md` is FORBIDDEN here: step 1 explicitly allows the `## Unreleased` entry to mention `--timeout` provided it says the flag is PRESERVED, and one natural sentence — "`--timeout` is preserved; `--dry-run` is removed" — puts `timeout`, then `remov`, on the same line, failing a compliant document. Same defect class as the whole-file `--dry-run` grep this plan rejects one step earlier.
   Replace it with a Go assertion in `TestUpgradeGuideReconcilesBackfill`, scoped to the `## Unreleased` section via the shipped `extractUnreleasedSection` (`cmd/engram/docsync_test.go:34-48`) that assertion 1a already reuses:
   - Assert no line in `## Unreleased` matches `--timeout[^.;]*\b(removed|no longer accepted|dropped)\b` — i.e. no CLAIM, within a single sentence CLAUSE, that the flag was removed. **The clause boundary is what makes this correct, and a character-count window is what made the prior form wrong (REVIEWS.md C6-H1).** This plan's own permitted wording — "`--timeout` is preserved; `--dry-run` is removed" — puts a `;` between the two, so `[^.;]*` cannot span it and a compliant document passes, at any sentence length. A `{0,40}` window matched that exact sentence (the gap is 31 characters) and failed a compliant document; a `{0,30}` window merely moves the cliff, since it fails the moment someone writes a slightly longer clause. Match on structure, never on distance.
   - Fail with a message naming the offending line, so the next person to write such a claim sees which one.
   - Prove it goes RED once: temporarily write "the `--timeout` flag is removed" into the entry, observe the failure, revert.
   **Confirm the three doc-side assertions are simultaneously satisfiable before committing** — D-12(i) REQUIRES `--dry-run` inside `## Unreleased`; assertion 1a FORBIDS `--dry-run` outside it; this one FORBIDS a `--timeout`-removal claim inside it. Read all three side by side. Two gates that cannot both be green is the same defect class as a test asserting mutually exclusive postconditions, and this plan has now produced that shape twice.
  </action>
  <acceptance_criteria>
    - `go test ./cmd/engram/ -run TestUpgradeGuideReconcilesBackfill -count=1 -v` passes with the entry present.
    - Reverting EITHER side is provably RED: with the upgrade.md entry removed, the test fails; with the command re-gaining --dry-run (or losing --timeout, or losing Deprecated), the test fails. Executor runs each RED case once and records the failure (memory x6v6qxqd6f discipline).
    - The upgrade.md entry mentions `--dry-run` removal and preview-by-default.
    - The upgrade.md entry does NOT claim `--timeout` removal (H8 — timeout is preserved). If it mentions `--timeout`, it states it is preserved.
    - **C5-L1 closed, and its own reinstatement closed with it (C6-H1):** the `--timeout`-not-removed check is `TestUpgradeGuideReconcilesBackfill`'s FOURTH assertion — section-scoped to `## Unreleased` via `extractUnreleasedSection` and matching a REMOVAL CLAIM within a single sentence clause (`--timeout[^.;]*\b(removed|no longer accepted|dropped)\b`), not a co-occurrence of two words and not a character-count window. The whole-file `! rg -n "timeout.*remov"` stays REMOVED from the `<automated>` block. **Verify the permitted sentence passes before committing:** run the assertion against a document containing "`--timeout` is preserved; `--dry-run` is removed" and confirm GREEN — that exact sentence failed the `{0,40}` form the previous revision shipped. Then prove it goes RED once by temporarily writing "the `--timeout` flag is removed", and revert.
    - **All three doc-side assertions are simultaneously satisfiable, and the executor confirmed it by reading them side by side:** D-12(i) requires `--dry-run` INSIDE `## Unreleased`; assertion 1a forbids it OUTSIDE; assertion 2a forbids a `--timeout`-removal claim INSIDE. No pair contradicts.
    - **C4-L3 closed:** `TestUpgradeGuideReconcilesBackfill`'s third assertion passes — the literal `--dry-run` appears ONLY inside the `## Unreleased` section (where it documents the removal), and nowhere in the guide's remainder. The v0.8.4 section's `engram backfill-short-ids --dry-run` instruction (:436) is gone, its bare-invocation line now reads `--apply`, and a pointer to the `## Unreleased` entry / `engram migrate` is present. The `--timeout 5m` example line is UNCHANGED (the flag is preserved — H8). The assertion is proven RED by temporarily restoring the old line, then reverted.
    - **The section-scoped assertion and the D-12 doc-side assertion are simultaneously satisfiable.** Confirm by reading both: D-12(i) requires `--dry-run` INSIDE `## Unreleased`; the C4-L3 assertion forbids it OUTSIDE. A whole-file negative grep for `--dry-run` would contradict D-12 and must not be used.
    - The test file appears in the .planning/ license-allowlist-agnostic path (no SPDX header added to upgrade.md — it is not Go).
    - **Ledger row D10 closed (C6-M11):** `guides/upgrade.md` §5 no longer claims the convention differs from "four of the six" operator commands, and no longer hand-enumerates the zero-disables group at all — it states the rule (`0` disables everywhere in the operator tier except `migrate-remap-owner`/`migrate-set-owner`) and points at `guides/cli.md`'s three-group table for live membership. `! rg -n 'four of the six' docs-site/src/content/docs/guides/upgrade.md` holds. The SUMMARY records that the sentence was already stale on arrival.
    - **Ledger row D9 second half closed (C6-M5):** `guides/cli.md:143-150` no longer says `backfill-short-ids` "keeps its pre-existing opt-in **preview** idiom, `--dry-run`". `! rg -n 'backfill-short-ids' docs-site/src/content/docs/guides/cli.md` would be wrong (the command is legitimately named elsewhere on that page), so the gate is the narrower `! rg -n 'summarize-missing., and .backfill-short-ids. — is classified' docs-site/src/content/docs/guides/cli.md`; the substantive check is that the executor read the rewritten paragraph against the shipped flag set and recorded it in the SUMMARY.
    - `task lint` is green over the edited markdown (rumdl), **and `task license:check` is green** — this is the phase's last wave and the earlier plans create new Go files in `.licenserc.yaml`-scoped paths, while `license:check` is a separate CI job `task` does not run (C6-L3). No SPDX header is added to any `docs-site/**` file.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd/engram/ -count=1 &amp;&amp; go test ./cmd/engram/ -run 'TestUpgradeGuideReconcilesBackfill|TestUpgradeGuideNamesEveryChangedCommand' -count=1 -v &amp;&amp; rg -n -- 'engram backfill-short-ids --apply' docs-site/src/content/docs/guides/upgrade.md &amp;&amp; task lint &amp;&amp; task license:check</automated>
    <!-- INV-2: the prior `! rg -n "timeout.*remov" …` whole-file gate is REMOVED — this plan's own
         step 1 permits "`--timeout` is preserved; `--dry-run` is removed" in the entry, which trips
         it on a compliant document (C5-L1). The property now lives in
         TestUpgradeGuideReconcilesBackfill's fourth assertion, section-scoped to `## Unreleased`
         and matching a removal CLAIM rather than a word co-occurrence — the same shape as its
         C4-L3 sibling one assertion earlier. The `rg -n -- 'preview' … | head -10` line is also
         dropped: `head` masks the exit status and the pattern asserts nothing (INV-3). -->

  </verify>
  <done>A bidirectional gate proves the upgrade-guide entry names the --dry-run removal and preview-by-default change (NOT --timeout removal, which is preserved per H8), AND the cobra command has no --dry-run, has a --timeout flag, and carries a non-empty Deprecated pointing at migrate — doc and code cannot drift apart.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI -> store (backfill alias) | The inherited `backfill-short-ids` command path now delegates to Store.Migrate; scripts calling it change behavior (preview vs apply). The pre-backfilled-record convergence test (M10) proves the alias safely handles the state its predecessor created. |
| docs <-> code | upgrade.md and the cobra command must not silently diverge on the breaking changes (--dry-run removal only, NOT --timeout). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-08 | Tampering | `backfill-short-ids` alias | high | mitigate | Routes through the generalized registerDestructive only (preview DryRun / --apply writes); no hand-rolled RunE or own --apply; delegates to the SINGLE shared sweep implementation so the alias's write path cannot diverge from the canonical command's preview→manifest intersection (cycle-3 #7); shares the migrate envelope, one report shape for one mechanism (D-11). |
| T-04-08d | Elevation of Privilege | conformance-gate coverage shrink | high | mitigate | Widening `registerDestructive` from `Destructive` to `!ReadOnly` without widening `TestDestructiveCommandsRouteThroughGate` / `TestDestructiveCommandsExactFlagSet` would leave the new mutating commands outside the unbypassable-routing and no-escape-hatch-flag gates — a hole that passes GREEN. Task 2 moves both gates (and the rule-sentence gate) to `mutatingCommandNames()`, adds flag-case rows for all three new mutating commands, and requires both widened gates be observed RED against a deliberate violator before acceptance (N1). |
| T-04-08b | Tampering | Existing-short_id record handling | high | mitigate | 04-01's CheckAdditive pre-existing-key carve-out ensures a record backfilled by the old code converges through the new alias (M10 integration test proves this with `--apply` against real Qdrant). |
| T-04-08c | Tampering | Finite deadline preserved | high | mitigate | The alias preserves the `--timeout` flag and uses the shared `migrateWithTimeout` helper (04-03) to enforce a 5-minute default deadline (H8). registerDestructive provides signal cancellation; this provides the deadline that signal cancellation alone does not. The behavior that the old command had is preserved. |
| T-04-09 | Tampering | dead code deletion `Store.BackfillShortIDs` | low | mitigate | Grep-before-delete: all callers re-pointed to Store.Migrate first (only caller was backfill.go's RunE, refactored in Task 1); MintShortID, which the sweep mint closure needs, is kept. M10 test proves the replacement handles the production state the deleted code was the only path for. |
| T-04-10 | Tampering | upgrade-guide reconciliation | low | mitigate | D-12 bidirectional test asserts exact strings on BOTH doc and command sides for the --dry-run removal, proven RED by reverting either side (no presence proxy). The gate does NOT assert --timeout removal (which does not happen — H8). |

</threat_model>

<verification>

After plan execution (wave 4):

- **`go test ./... -count=1` green.** Run the whole module unfiltered — this wave deletes an exported store method and three test functions, so a `-run`-filtered run cannot see a compile break in a package it filtered out.
- The named-subset form, as TWO invocations (never one — `-run` is package-wide and a second occurrence supersedes the first, which is REVIEWS.md C4-L4):
  `go test ./cmd/engram/ -run 'TestBackfill|TestMigrate|TestUpgradeGuideReconcilesBackfill|TestOperatorOutput|TestCatalogBlastRadiusMatchesToolClasses|TestDestructive|TestMutatingCommandNamesMembership|TestApplyFlagUsageComposesRuleSentence|TestTimeoutGroupMatrix|TestHelpGolden|TestCatalogGolden' -count=1 -v`
  and
  `go test ./internal/store/ ./internal/migrate/ -run 'TestRecallEmissionSetIsCompleteAndClassified|TestPartialWritePathsAreClassifiedNonStamping|TestMigrate' -count=1 -v`
- `task` green (lint + full suite), incl. the docs-site markdown lint (rumdl).
- Real-binary smoke: `go run ./cmd/engram backfill-short-ids` against a test Qdrant previews (dry_run:true, no writes); `--apply` executes; `--help` shows a Deprecated pointer to migrate and a PRESERVED --timeout flag (H8).
- Final cross-plan symbol resolution sweep, run once at the end of the phase: `go build ./... && go vet ./...` clean, confirming every symbol any plan referenced (`Store.PreviewRevert`, `store.RevertPlan`, `migrateSweepPreviewRun`, `migrateSweepApplyRun`, `migrateWithTimeout`, `migrateReportDoc`, `mutatingCommandNames`) resolves to a real definition produced by the plan that claimed it.
- REVIEWS.md H8 (timeout preserved AND read), M10, M11 (--dry-run removal documented, NOT --timeout removal) closed, plus cycle-3 #6/N1 (sibling conformance gates widened), #7 (alias apply parity), and N3 (the alias's timeout actually feeds the helper).
- Cycle-4 closed here: **C4-H3** (the live-caller gate no longer excludes `_test.go`, and both hidden call-site clusters — `internal/store/store_test.go` and `cmd/engram/operator_output_test.go` — are owned and in `files_modified`), **C4-H4 second half** (the `partialWriteClassification` stale row), **C4-L3** (upgrade.md's v0.8.4 section), **C4-L4** (the two-`-run` command split).
- **Final phase-wide ledger audit, run once here as the last wave — EVERY search shape in the ledger's "How it was built" table (`04-01-PLAN.md`), not a subset.** Re-run each against the finished tree and confirm every hit resolves to a ledger row OR to a named dismissal in the ledger's "Shapes and triggers this ledger does NOT cover" section. Note in particular that **S9** (call sites reaching a changed constant through a SENTINEL or default argument, and prose restating its value without naming it) is the shape a name-keyed search is structurally blind to — it is what row E1 and two of E4's sites required. A hit with neither a row nor a dismissal is a ledger gap to REPORT, not to fix locally. Record the audit's outcome per search in the SUMMARY, including how many hits each produced, so the next phase can diff against it.
- **Boundary RE-AUDIT, not re-affirmation.** For each row of the ledger's non-coverage table, re-ask the question that produced it — *is this exclusion load-bearing for what this phase actually did?* — rather than re-reading its stated reason. Two exclusions failed that test this cycle and became rows (`internal/keylinks`'s live-milestone gate → F1; the two ungated `docs-site` prose sites the phase itself edits → D9/D10). Record in the SUMMARY, per exclusion, whether the phase touched anything inside it. An exclusion that was never re-tested is indistinguishable from one that is wrong.

</verification>

<success_criteria>

- `backfill-short-ids` is a thin delegating alias through registerDestructive sharing the migrate envelope AND the single shared sweep implementation, so its `--apply` performs the identical preview→manifest intersection as `engram migrate --apply` (REQ-backfill-shortids-first-step CLI half; cycle-3 #7).
- The routing, flag-set-equality, and rule-sentence conformance gates cover every MUTATING command, so the safety net widened alongside the mechanism instead of quietly shrinking (cycle-3 #6/N1); both widened gates were observed RED against a deliberate violator.
- A pre-backfilled record converges through the alias without error or re-minting, proving the H1 carve-out is correct and safe (M10).
- The apply-by-default -> preview-by-default break is documented in guides/upgrade.md and pinned by a bidirectional test (D-12). The `--timeout` is PRESERVED (H8) — NOT removed, NOT documented as removed.
- No migration runs automatically anywhere; the only automatic surface is the non-blocking startup warning (REQ-migrate-never-automatic, co-covered with 04-02).
- Dead `Store.BackfillShortIDs`/`missingShortIDFilter` deleted; `Store.MintShortID` kept.

</success_criteria>

## Artifacts this phase produces

- `cmd/engram/backfill.go`: `backfillShortIDsCmd` rebuilt as a registerDestructive alias whose two closures are one-line adapters over 04-03's `migrateSweepPreviewRun`/`migrateSweepApplyRun`; `backfillDryRun`, `backfillSummary`, `backfillOutputDoc`, `backfillReportDoc`, and the hand-rolled `signal.NotifyContext`/`context.WithTimeout` block all removed; `backfillTimeout` var and `--timeout` flag PRESERVED and PASSED to `migrateWithTimeout(ctx, backfillTimeout)` (H8/N3); `backfillShortIDsCmd.Deprecated = "use: engram migrate"` added.
- `internal/store/store.go`: deleted symbols `Store.BackfillShortIDs`, `missingShortIDFilter`; kept `Store.MintShortID`.
- `internal/store/store_test.go` (**new to `files_modified` this cycle — C4-H3**): deleted `TestBackfillShortIDs`, `TestBackfillShortIDsHonorsCancel`, `TestBackfillShortIDsResumesAfterMidRunFailure` (seven `st.BackfillShortIDs` call sites, :5762–:5893), each only after its behaviour was named against replacement coverage on the `Store.Migrate` path; the absent-owner-key invariant and the context-cancellation assertion are carried across or re-added, never dropped.
- `cmd/engram/operator_output_test.go` (**new to `files_modified` this cycle — C4-H3**): the `operatorParityRows()` `backfill-short-ids` row MIGRATED from `backfillSummary`/`backfillReportDoc` to `migrateSummary`/`migrateReportDoc` (the row set is bidirectionally gated against `operatorCommands()`, so it must move, not vanish — ledger row A6); the `TestOperatorOutputEmpty` doc-map entry at :379 likewise (ledger row A5).
- `internal/store/schemaversion_recallgate_test.go`: deleted the `Store.BackfillShortIDs` `operatorMigrationEmitters` row, and replaced the slice doc's restated entry count (`:516`) with a non-counting sentence (ledger row B6).
- `internal/store/schemaversion_stamp_gate_test.go` (**new to `files_modified` this cycle — C4-H4**): deleted the `Store.BackfillShortIDs` `partialWriteClassification` row at :370 — a SECOND, INDEPENDENT set-equality table from the recall gate, covering WRITES rather than READS (ledger row B2).
- `cmd/engram/destructive_test.go`: **in Task 1** — DELETED `pendingApplyConversion` (04-03's temporary exclusion, owned here, in the same task that gives the alias `--apply`) and updated `TestMutatingCommandNamesMembership`'s pin from five names to six; KEPT `applyRoutedAdditions` (permanent). **In Task 2** — `destructiveFlagCases` RENAMED to `mutatingFlagCases` with new rows for `migrateCmd` and `backfillShortIDsCmd`; `TestDestructiveCommandsRouteThroughGate`, `TestDestructiveCommandsExactFlagSet` (both directions), and `TestApplyFlagUsageComposesRuleSentence` switched from `destructiveCommandNames()` to `mutatingCommandNames()` (N1).
- `internal/store/migrate.go`, `internal/store/migratebacklog.go`: the two cross-reference doc comments that cite `BackfillShortIDs store.go:2741` and `missingShortIDFilter (store.go:2726-2731)` are trimmed of the deleted symbols, for prose accuracy. **The call-shaped deletion gate passes with or without these edits** — that independence is deliberate: a gate must not depend on an edit in a file it does not own.
- `cmd/engram/testdata/help.golden`, `cmd/engram/testdata/catalog.golden`: regenerated via `task surfaces:gen` **in Task 1** for the alias's changed flag set — the THIRD and final regeneration of the phase (04-03 Task 1 for the rule sentence, 04-03 Task 3 for the three new commands, this one for the alias). One per wave that changes the tree; Task 2 re-runs it verify-only and asserts an empty diff.
- `docs-site/src/content/docs/guides/upgrade.md`: new backfill-short-ids `--dry-run`-removal / preview-by-default entry under `## Unreleased` (does NOT document --timeout removal per H8, and does NOT amend the published three-way `--timeout` split, which stays accurate); **plus the v0.8.4 section's stale `--dry-run` instruction at :436-437 corrected with a pointer to the new entry (C4-L3, ledger row D4)**, pinned by a SECTION-SCOPED third assertion in `TestUpgradeGuideReconcilesBackfill` (the literal `--dry-run` may appear only inside `## Unreleased`) rather than by a whole-file negative grep, which would contradict D-12's own doc-side requirement.

## Registry rows this plan owns

Canonical table: **Conformance Registry Impact ledger** in `04-01-PLAN.md`.
Owned here (all wave 4): **A5** `TestOperatorOutputEmpty` doc map (T1), **A6** `operatorParityRows`'
backfill row (T1), **A11** help/catalog goldens — third and final regeneration, **in T1** rather than
T2 (INV-1: T1 is what changes the flag set), **A13** `TestDestructiveCommandsRouteThroughGate` (T2),
**A14** `mutatingFlagCases` (`migrateCmd` + `backfillShortIDsCmd` rows, T2), **A15**
`TestApplyFlagUsageComposesRuleSentence` (T2), **B2** `partialWriteClassification` stale row (T1),
**B6** `operatorMigrationEmitters` stale row (T1), **D4** upgrade.md's v0.8.4 section (T3).
Constraints observed here: **A18** (if any `exitCodeBaseline` row gains `changes:true`, its command
name must appear in `## Unreleased`), **A19** (no new baseline rows, so `wantRows = 37` stands),
**A24** (no new exit-code CONSTANT anywhere in the phase), **D5/D6** (T1 step 10's `surfaces:gen`
rewrites `curating-memory/SKILL.md`'s anchored region; the verb-selection table and the supersede
worked examples two `internal/server` gates read back must survive it).
**Re-audit owner:** this plan runs the phase's final ledger audit across **all eight** search shapes
— see the Verification section.
- New tests: `TestUpgradeGuideReconcilesBackfill` (D-12 bidirectional gate asserting --dry-run removal, NOT --timeout removal) + rewritten backfill alias tests (asserting `--timeout` is PRESERVED, that its value is READ across three deadline cases, and that the alias's and `engram migrate`'s `--apply` `Migrate` call sequences are EQUAL — cycle-3 #7) + `TestBackfillPreBackfilledRecordConverges` (M10 — existing-short_id convergence).

Symbols this plan CONSUMES from sibling plans (must resolve before wave 4 can compile):

| Symbol | Produced by |
|---|---|
| `migrateSweepPreviewRun`, `migrateSweepApplyRun`, `migrateWithTimeout(ctx, d)`, `migrateReportDoc`, `migrateFamilyStoreFromEnv` | 04-03 |
| `mutatingCommandNames()`, `pendingApplyConversion` (consumed only to DELETE it) | 04-03 |
| `store.MigrateOptions.{DryRun,Manifest}`, `store.MigrateResult.{PreviewManifest,Spared,Appeared}` | 04-01 (reached indirectly, through 04-03's shared run funcs) |

This plan produces no symbol that a later Phase-4 plan consumes — it is the last wave.

<output>
Create `.planning/phases/04-migration-cli-first-customer/04-04-SUMMARY.md` when done
</output>