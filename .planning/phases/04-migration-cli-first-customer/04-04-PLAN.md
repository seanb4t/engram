---
phase: 04-migration-cli-first-customer
plan: 04
type: execute
wave: 4
depends_on:
  - 04-migration-cli-first-customer-03
files_modified:
  - cmd/engram/backfill.go
  - internal/store/store.go
  - cmd/engram/backfill_test.go
  - internal/surfaces/toolclass.go
  - docs-site/src/content/docs/guides/upgrade.md
autonomous: true
requirements:
  - REQ-backfill-shortids-first-step
  - REQ-migrate-never-automatic
estimate:
  tokens: 46000
  raw_tokens: 23000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "The standalone `backfill-short-ids` command becomes a THIN delegating alias: its `--dry-run` flag and var are REMOVED outright (D-09), its bespoke RunE is dropped, and it routes through registerDestructive with preview/apply closures that delegate to `Store.Migrate` with defaults (Target=CurrentVersion, Steps=Registry) — Identical UX to the migrate family (D-10/D-11)."
    - "The alias emits the SHARED migrate envelope (migrateReportDoc: migrated/failed/passes/backlog + explicit dry_run), NOT the legacy backfillOutputDoc (D-11)."
    - "`backfillShortIDsCmd.Deprecated` is non-empty and points at `migrate` (D-12; exact precedent migrateSetOwnerCmd.Deprecated at migrate.go:261) — soft deprecation, never hard removal."
    - "`Store.BackfillShortIDs` and `missingShortIDFilter` are DELETED as dead code once backfill.go no longer calls them; `Store.MintShortID` STAYS (the write path and the sweep's mint closure use it) (D-10)."
    - "`guides/upgrade.md` (the real path docs-site/src/content/docs/guides/upgrade.md) gains an entry documenting the --dry-run REMOVAL and the preview-by-default change for `backfill-short-ids`."
    - "A BIDIRECTIONAL doc↔code gate (D-12) asserts BOTH: (i) the upgrade.md entry names the --dry-run removal / preview-by-default change for backfill-short-ids (exact strings); and (ii) the cobra command has no --dry-run flag and carries a non-empty Deprecated message pointing at migrate. The gate is proven RED by reverting either side, and never uses a len > 0 / presence proxy (memory x6v6qxqd6f)."
    - "Because the alias now delegates to the same Store.Migrate the phase forbids on startup, no migration runs automatically anywhere; the only automatic surface remains the non-blocking startup warning (REQ-migrate-never-automatic)."
  prohibitions:
    - "backfill-short-ids MUST NOT be hard-removed — soft deprecation via the Deprecated field, matching the migrate-set-owner precedent."
    - "The legacy backfill envelope MUST NOT be preserved; the behavior is already breaking (D-09), so consumer compat is marginal and one migrate report shape is kept."
    - "Store.BackfillShortIDs / missingShortIDFilter MUST NOT be deleted before all callers are re-pointed to Store.Migrate; a grep must return zero live callers first."
    - "The --dry-run flag MUST NOT be deprecated (still present) — it is REMOVED outright, exactly as migrate-remap-owner removed its own (REMOVED, not deprecated)."
---

<objective>
Fold `backfill-short-ids` into the migration mechanism as the thin delegating alias (its apply-by-default and `--dry-run` reconciled with `registerDestructive`'s preview-by-default), delete the now-dead `Store.BackfillShortIDs`/`missingShortIDFilter`, and reconcile the behavior break in `guides/upgrade.md` behind the D-12 bidirectional doc↔code gate.

Purpose: This closes the "first customer" story — the standalone command becomes a delegation to the very mechanism it seeded (`Store.Migrate` + the v0→v1 step), one report shape for one mechanism (D-11), and the operator-facing upgrade guide names the breaking change so a scripted `backfill-short-ids` call does not silently flip to preview.

Output: a delegating `backfill-short-ids` alias routed through registerDestructive, dead store code removed, an upgrade-guide entry, and the D-12 gate test that keeps doc and code from drifting apart.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/04-migration-cli-first-customer/04-CONTEXT.md
@.planning/phases/04-migration-cli-first-customer/04-RESEARCH.md
@.planning/phases/04-migration-cli-first-customer/04-PATTERNS.md

@cmd/engram/backfill.go
@cmd/engram/migrate.go (migrateSetOwnerCmd.Deprecated precedent)
@cmd/engram/migrate_family.go (the shared migrateReportDoc + preview/apply closures to delegate into)
@internal/store/store.go (BackfillShortIDs :2741-2797, missingShortIDFilter :2729-2731, MintShortID :2668)
@docs-site/src/content/docs/guides/upgrade.md (the entry conventions; NEVER hard-code the shorthand "guides/upgrade.md")
</context>

<tasks>

<task type="auto">
  <name>Task 1: backfill-short-ids → thin delegating alias through registerDestructive; delete dead store code</name>
  <files>
    cmd/engram/backfill.go
    internal/store/store.go
    cmd/engram/backfill_test.go
    internal/surfaces/toolclass.go
  </files>
  <read_first>
    cmd/engram/backfill.go (current shape :19-82 — RunE owns apply-by-default + --dry-run + backfillTimeout + calls st.BackfillShortIDs)
    cmd/engram/migrate_family.go (the shared migrateReportDoc + migratePreview/migrateApplyRun closures to mirror/forward into)
    cmd/engram/migrate.go (migrateSetOwnerCmd.Deprecated :261 — the soft-deprecation precedent)
    cmd/engram/spine_review_purge.go (registerDestructive closure contract)
    internal/store/store.go (BackfillShortIDs :2741-2797 + missingShortIDFilter :2729-2731 to delete; MintShortID :2668 to keep)
    .planning/phases/04-migration-cli-first-customer/04-CONTEXT.md (D-09 remove --dry-run, D-10 delegate + delete, D-11 shared envelope)
  </read_first>
  <action>
In `cmd/engram/backfill.go`:
1. REMOVE (not deprecate) the `--dry-run` flag registration and its `backfillDryRun` var (D-09). REMOVE the bespoke custom RunE and its hand-rolled signal/timeout handling — the destructive tier's `registerDestructive` owns ctx/signal now; the per-command `backfillTimeout` var and `--timeout` flag are REMOVED too (preview/apply closures govern duration the same way the `migrate` family does — keep parity with `migrate`, which carries no bespoke timeout).
2. Rebuild the command as a thin delegate: keep `backfillShortIDsCmd` (`Use: "backfill-short-ids"`) routed through `registerDestructive` (AddCommand BEFORE, per the contract) with two closures that delegate to the SAME `Store.Migrate` defaults the `migrate` family uses:
   - `backfillPreview`: `operatorOutputFormat` → `server.StoreFromEnv()` → `st.Migrate(ctx, store.MigrateOptions{DryRun: true})` → `renderOperator(cmd, format, migrateSummary, migrateReportDoc)`.
   - `backfillApplyRun`: identical but `DryRun: false`.
   Reuse the shared `migrateReportDoc`/summary funcs from `migrate_family.go` — do NOT define a second backfill-specific report doc (D-11). Drop the legacy `backfillOutputDoc`/`backfillReportDoc`/`backfillSummary` (they are dead once the alias shares the migrate envelope).
3. Soft-deprecate with `backfillShortIDsCmd.Deprecated = "use: engram migrate"` (D-12; exact precedent migrateSetOwnerCmd.Deprecated) — never remove the command.
4. Keep the existing `backfill-short-ids` toolclass row in `internal/surfaces/toolclass.go` (already Destructive:false, named exactly this path) unchanged — the alias keeps routing through registerDestructive once the gate is generalized (done in 04-03); do NOT reclassify it.

In `internal/store/store.go`:
5. DELETE `Store.BackfillShortIDs` (:2741-2797) and `missingShortIDFilter` (:2729-2731) as dead code (D-10). KEEP `Store.MintShortID` (:2668) — the write path AND the sweep's mint closure both call it. Before deleting, grep for live callers: `rg -n "BackfillShortIDs|missingShortIDFilter" --glob '!**/*_test.go'` must return only the (now-refactored) definitions or nothing.

In `cmd/engram/backfill_test.go`:
6. Rewrite existing backfill tests to the alias contract: bare `backfill-short-ids` previews (DryRun, no writes, shared migrate envelope with dry_run:true) — the behavior REVERSAL from apply-by-default; `backfill-short-ids --apply` executes (shared envelope dry_run:false); the command carries NO `--dry-run` flag (assert via `cmd.Flags().Lookup("dry-run") == nil`); `backfillShortIDsCmd.Deprecated` is non-empty and contains "migrate".
  </action>
  <acceptance_criteria>
    - `go test ./cmd/engram/ -run 'TestBackfill|TestMigrate' -count=1 -v` passes (alias preview/apply + shared envelope + no --dry-run + Deprecated).
    - `rg -n "dry-run|backfillDryRun" cmd/engram/backfill.go` returns NOTHING (flag and var REMOVED).
    - `rg -n "backfillPlainBackfill|BackfillShortIDs|missingShortIDFilter" --glob '!**/*_test.go' internal/store/store.go` returns nothing; `rg -n "func \(s \*Store\) MintShortID" internal/store/store.go` still finds it.
    - `go build ./cmd/engram/... ./internal/store/...` succeeds (no dangling caller of the deleted symbols).
    - `backfillShortIDsCmd.Deprecated` is non-empty and names "migrate".
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd/engram/ -run 'TestBackfill|TestMigrate' -count=1 -v && go build ./cmd/engram/... ./internal/store/... && ! rg -n "BackfillShortIDs|missingShortIDFilter" --glob '!**/*_test.go' internal/ cmd/</automated>
  </verify>
  <done>backfill-short-ids delegates to Store.Migrate through registerDestructive, shares the migrate envelope, lost its --dry-run and --timeout, is soft-deprecated toward migrate, and the dead BackfillShortIDs/missingShortIDFilter are gone with MintShortID intact.</done>
</task>

<task type="auto">
  <name>Task 2: guides/upgrade.md entry + D-12 bidirectional doc↔code gate</name>
  <files>
    docs-site/src/content/docs/guides/upgrade.md
    cmd/engram/backfill_test.go
  </files>
  <read_first>
    docs-site/src/content/docs/guides/upgrade.md (existing entry conventions — never hard-code the shorthand path)
    cmd/engram/backfill.go (the Deprecated message + removed --dry-run the gate asserts)
    .planning/phases/04-migration-cli-first-customer/04-CONTEXT.md (D-12 exact strings, prove-RED, never a len > 0 proxy; memory x6v6qxqd6f)
  </read_first>
  <action>
In `docs-site/src/content/docs/guides/upgrade.md`:
1. Add a marked entry under the upgrade-guide for this change (match existing date/version-section convention; the milestone does not renumber). The entry MUST name, in plain text, the `--dry-run` REMOVAL and the preview-by-default change for `backfill-short-ids`, and point the operator to `engram migrate` (e.g. "backfill-short-ids no longer takes --dry-run: bare invocation now PREVIEWS, --apply executes — use `engram migrate` for the schema-version migration; scripts that ran bare `backfill-short-ids` to apply must now pass `--apply`"). Use exact strings the D-12 gate will grep.

In `cmd/engram/backfill_test.go`:
2. Add the D-12 bidirectional gate test. It MUST assert BOTH directions with EXACT strings (never a `len > 0`/presence proxy):
   - (i) read the file at `docs-site/src/content/docs/guides/upgrade.md` (the real path; resolve relative to the repo root, not a hard-coded CWD) and assert it contains BOTH a string naming the `--dry-run` removal for `backfill-short-ids` AND a string naming preview-by-default / the `--apply` requirement.
   - (ii) assert on the cobra command, in the same test, that `backfillShortIDsCmd.Flags().Lookup("dry-run") == nil` AND `backfillShortIDsCmd.Deprecated` is non-empty AND is set to exactly `"use: engram migrate"` (or a substring contract — pick one exact contract and use it on BOTH sides).
   Prove-RED discipline: the test must fail if EITHER side is changed (doc entry removed OR command re-gains --dry-run / loses Deprecated). Do not gate on `len(Deprecated) > 0` alone.
   Arrange the test so its two assertions are separable so an executor can demonstrate a revert of either side failing RED.
  </action>
  <acceptance_criteria>
    - `go test ./cmd/engram/ -run TestUpgradeGuideReconcilesBackfill -count=1 -v` passes with the entry present.
    - Reverting EITHER side is provably RED: with the upgrade.md entry removed, the test fails; with the command re-gaining --dry-run (or losing Deprecated), the test fails. Executor runs each RED case once and records the failure (memory x6v6qxqd6f discipline).
    - The test file appears in the .planning/ license-allowlist-agnostic path (no SPDX header added to upgrade.md — it is not Go).
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd/engram/ -run TestUpgradeGuideReconcilesBackfill -count=1 -v && rg -n -- '--dry-run|preview' docs-site/src/content/docs/guides/upgrade.md | head -5</automated>
  </verify>
  <done>A bidirectional gate proves the upgrade-guide entry names the --dry-run removal / preview-by-default change AND the cobra command has no --dry-run with a non-empty Deprecated pointing at migrate — doc and code cannot drift apart.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI → store (backfill alias) | The inherited `backfill-short-ids` command path now bequeaths everything to Store.Migrate; scripts calling it change behavior (preview vs apply). |
| docs ↔ code | upgrade.md and the cobra command must not silently diverge on the breaking change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-08 | Tampering | `backfill-short-ids` alias | high | mitigate | Routes through the generalized registerDestructive only (preview DryRun / --apply writes); no hand-rolled RunE or own --apply; shares the migrate envelope, one report shape for one mechanism (D-11). |
| T-04-09 | Tampering | dead code deletion `Store.BackfillShortIDs` | low | mitigate | Grep-before-delete: all callers re-pointed to Store.Migrate first (only caller was backfill.go's RunE, refactored in Task 1); MintShortID, which the sweep mint closure needs, is kept. |
| T-04-10 | Tampering | upgrade-guide reconciliation | low | mitigate | D-12 bidirectional test asserts exact strings on BOTH doc and command sides, proven RED by reverting either side (no presence proxy). |

</threat_model>

<verification>

After plan execution (wave 4):

- `go test ./cmd/engram/ -run 'TestBackfill|TestMigrate|TestUpgradeGuideReconcilesBackfill|TestCatalogBlastRadiusMatchesToolClasses|TestDestructive' ./internal/store/ ./internal/migrate/ -count=1` green.
- `task` green (lint + full suite), incl. the docs-site markdown lint (rumdl).
- Real-binary smoke: `go run ./cmd/engram backfill-short-ids` against a test Qdrant previews (dry_run:true, no writes); `--apply` executes; `--help` shows a Deprecated pointer to migrate.

</verification>

<success_criteria>

- `backfill-short-ids` is a thin delegating alias through registerDestructive sharing the migrate envelope (REQ-backfill-shortids-first-step CLI half).
- The apply-by-default → preview-by-default break is documented in guides/upgrade.md and pinned by a bidirectional test.
- No migration runs automatically anywhere; the only automatic surface is the non-blocking startup warning (REQ-migrate-never-automatic, co-covered with 04-02).

</success_criteria>

## Artifacts this phase produces

- `cmd/engram/backfill.go`: `backfillShortIDsCmd` rebuilt as a registerDestructive alias; `backfillDryRun`, `backfillTimeout`, `backfillSummary`, `backfillOutputDoc`, `backfillReportDoc` all removed; `backfillShortIDsCmd.Deprecated = "use: engram migrate"` added.
- `internal/store/store.go`: deleted symbols `Store.BackfillShortIDs`, `missingShortIDFilter`; kept `Store.MintShortID`.
- `docs-site/src/content/docs/guides/upgrade.md`: new backfill-short-ids `--dry-run`-removal / preview-by-default entry.
- New test: `TestUpgradeGuideReconcilesBackfill` (D-12 bidirectional gate) + rewritten backfill alias tests.

<output>
Create `.planning/phases/04-migration-cli-first-customer/04-04-SUMMARY.md` when done
</output>