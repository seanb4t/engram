---
phase: 04-migration-cli-first-customer
plan: 02
type: execute
wave: 2
depends_on:
  - 04-migration-cli-first-customer-01
files_modified:
  - internal/store/migrate_status.go
  - internal/store/revert.go
  - internal/store/migrate_status_test.go
  - internal/store/revert_test.go
  - internal/server/tools.go
  - internal/server/tools_test.go
autonomous: true
requirements:
  - REQ-migrate-status-histogram
  - REQ-migrate-revert
  - REQ-migrate-never-automatic
estimate:
  tokens: 56000
  raw_tokens: 28000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "Store.MigrateStatus computes a version-distribution histogram server-side via a qdrant Facet over present schema_version values PLUS one exact Count with an IsEmpty(schema_version) filter for the legacy/absent bucket — never a single scalar, and never a CLI-side collection scroll (D-08, memory 4syx1ggfxk)."
    - "The histogram sums to the total collection count: facet present-buckets + absent Count == a whole-collection exact Count, so a mixed-version collection mid-rollout is correctly represented, not misreported (REQ-migrate-status-histogram)."
    - "The histogram handles future-version records (Version > CurrentVersion): buckets above CurrentVersion are reported as a distinct count with a version annotation so an operator sees 'newer than server' records separately from 'pending, behind CurrentVersion' records (REVIEWS.md M4)."
    - "Store.Revert evaluates irreversibility as a WHOLE-RANGE preflight BEFORE any write: if any step in the requested range is irreversible, the entire revert is refused with zero records touched (D-13)."
    - "The revert refusal message names every irreversible step (From/To), each step's declared IrreversibleReason, and snapshot recovery as the path back, in the repo's field=<name> hint=<code> envelope (D-14)."
    - "Store.Revert selects its reverse chain PER RECORD based on the record's actual current version (versionOf(p.Payload)), applying inverses only for versions that record actually reached — never a fixed global chain for the whole collection (REVIEWS.md H5 / D-15)."
    - "Revert's inverse write contract is specified: keys removed by the inverse become DeletePayload; keys newly added or value-changed become SetPayload; schema_version is SetPayload-stamped to the target version the record actually reaches (REVIEWS.md M3). A step's inverse may add or change keys outside the forward AddsKeys set (the anti-additive direction); CheckAdditive is additive-only and does NOT run on the revert path."
    - "Store.Revert is tested with a REVERSIBLE fixture step injected via an unexported revertWithSteps(ctx, to, steps) helper used by both the production Revert (which defaults steps=Registry) and the test that proves reverse-walk convergence (REVIEWS.md H4)."
    - "Because the only registered step (v0→v1) is Irreversible, any revert --to 0 currently refuses the whole operation, naming that step and snapshot recovery — a legitimate, testable pre-v1 state (D-03)."
    - "Server startup never calls Store.Migrate; at most it emits a non-blocking slog.Warn when a pending-migration check (MigrateStatus) reports records behind CurrentVersion — wired alongside warnOwnerlessRecords and never gating startup (REQ-migrate-never-automatic)."
    - statement: "The startup warning predicate is `pending = Absent + sum(bucket.Count where bucket.Version < CurrentVersion)` — NOT `sum(buckets)+Absent > 0` which is the total collection count and would spuriously warn for every non-empty all-current collection (REVIEWS.md H3 correction). A non-empty bucket at Version > CurrentVersion logs a SEPARATE compatibility warning (never a migration-required warning)."
      verification: backstop
  prohibitions:
    - "Store.MigrateStatus MUST NOT be computed by scrolling the whole collection to the CLI — the absent-key semantics and O(collection) transfer stay out of the store."
    - "Store.Revert MUST NOT be added as a direction parameter on Store.Migrate and MUST NOT disable or exempt CheckAdditive quietly — one method carries one contract."
    - "The sweep path MUST NOT be the only way a migration progresses: Store.Revert reverse-inversion is a dedicated method, never folded into Store.Migrate."
    - "Revert's reverse chain MUST NOT be a fixed global 'all records to target' — each record gets its own reverse chain based on its stored version at the point of the current scroll (REVIEWS.md H5)."
    - "The startup warning MUST NOT gate startup or call Store.Migrate — at most a slog.Warn, wired best-effort non-blocking following warnOwnerlessRecords (REQ-migrate-never-automatic)."
---

<objective>
Add the two remaining store-side migration mechanism methods — `Store.MigrateStatus` (version-distribution histogram, D-08, with future-version handling per REVIEWS.md M4) and `Store.Revert` (reverse-inverse walk with whole-range irreversible preflight, D-13/D-14/D-15, with per-record chain selection per H5 and fixture injection per H4) — and the server-side pending-migration startup warning that correctly computes only records BELOW CurrentVersion (REVIEWS.md H3 correction) and caps the "never automatic" stance (REQ-migrate-never-automatic) at a non-blocking message.

Purpose: These are the store/backend tiers the `migrate status` and `migrate revert` CLI subcommands attach to. `MigrateStatus` makes a mixed-version collection legible (never a scalar); `Revert` gives declared inverses a home without polluting `Store.Migrate` with a direction; the startup warning is the ONLY automatic surface the phase permits. All three incorporate the correctness corrections from REVIEWS.md findings H3, H4, H5.

Output: `Store.MigrateStatus` (with future-version handling), `Store.Revert` (with `revertWithSteps` helper for test injection, per-record chain selection, specified inverse write contract), and a `warnPendingMigrations`-shaped startup warning wired into `buildDepsFromEnv`, each with Qdrant-backed integration tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/04-migration-cli-first-customer/04-CONTEXT.md
@.planning/phases/04-migration-cli-first-customer/04-RESEARCH.md
@.planning/phases/04-migration-cli-first-customer/04-PATTERNS.md
@.planning/phases/04-migration-cli-first-customer/04-REVIEWS.md

@internal/store/migrate.go
@internal/store/store.go (schemaVersionKey at :370; Store.MintShortID)
@internal/migrate/step.go (sealed Reversibility accessors Inverse / IrreversibleReason / Step.Reversibility)
@internal/server/tools.go (warnOwnerlessRecords :459-482 + buildDepsFromEnv wire :209)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Store.MigrateStatus version-distribution histogram + future-version handling</name>
  <files>
    internal/store/migrate_status.go
    internal/store/migrate_status_test.go
  </files>
  <read_first>
    internal/store/migrate.go (schemaVersionKey usage, MigrateResult truth-only doc, telemetry span wrapper idiom)
    internal/migrate/migrate.go (CurrentVersion constant — what the histogram compares against for "behind"/"future")
    .planning/phases/04-migration-cli-first-customer/04-RESEARCH.md (Code Example for MigrateStatus: qdrant.PointsClient.Facet + FacetCounts{Key,Exact} + exact Count PointsFilter NewIsEmpty; memory 4syx1ggfxk gotcha — facets cannot bucket absent keys)
    .planning/phases/04-migration-cli-first-customer/04-CONTEXT.md (D-08 — method name and facet-vs-exact-Count choice are discretion)
    .planning/phases/04-migration-cli-first-customer/04-REVIEWS.md (H3 — warning predicate is NOT total count; M4 — handle future-version buckets separately)
  </read_first>
  <action>
In a NEW file `internal/store/migrate_status.go`:

1. Define the histogram type. Suggested shape (executor discretion per D-08):
   ```go
   type VersionBucket struct {
       Version int    `json:"version"`
       Count   uint64 `json:"count"`
   }
   type MigrateStatusResult struct {
       Buckets       []VersionBucket `json:"buckets"`
       Absent        uint64          `json:"absent"`
       FutureVersion uint64          `json:"future_version"` // counts records at Version > CurrentVersion (REVIEWS.md M4)
       Total         uint64          `json:"total"`
   }
   ```
   Present bucket key = `schemaVersionKey` (`schema_version`, store.go:370). `FutureVersion` is the count of records at a version STRICTLY GREATER than `migrate.CurrentVersion` — distinct from "pending/behind" buckets (REVIEWS.md M4).
2. `func (s *Store) MigrateStatus(ctx context.Context) (MigrateStatusResult, error)`:
   - Wrap with `tracer.Start(ctx, "store.MigrateStatus")`, a `defer span.End()`, and the `telemetry.RecordStoreOp(ctx, "MigrateStatus", start, err)` + error→span-codes pattern shown by the other store methods.
   - Request present buckets with one `s.client.Facet(ctx, &qdrant.FacetCounts{CollectionName: s.collection, Key: schemaVersionKey, Exact: qdrant.PtrOf(true)})`; read each `FacetHit.Value`'s IntegerValue as the version int and `Count` as the bucket count. Sort buckets by version ascending for a stable/deterministic report (edge: equal-valued facets are merged by Qdrant; output order is NOT specified by the backend, so sort on this side).
   - Request the absent/legacy bucket with one exact `s.client.Count(ctx, &qdrant.CountPoints{CollectionName: s.collection, Filter: &qdrant.Filter{Must: []*qdrant.Condition{qdrant.NewIsEmpty(schemaVersionKey)}}, Exact: qdrant.PtrOf(true)})`.
   - After retrieving buckets, separate them: for each bucket, if `Version > int(migrate.CurrentVersion)`, add its count to `FutureVersion` and REMOVE it from the bucket list (these are records from a NEWER server that this binary cannot handle — REPORTED separately, not as "pending migrations"). Keep buckets where `0 <= Version <= CurrentVersion` in the bucket list.
   - `Total` may be a whole-collection exact Count, or derived as `sum(present buckets) + absent`; if derived, do so with a comment noting the invariant is test-asserted against `sum(buckets) + Absent + FutureVersion`. Keep it server-side; never accept a CLI-supplied count.
3. Edge behavior: an empty collection returns zero buckets, Absent 0, FutureVersion 0, Total 0. An all-migrated (all-v1) collection returns a bucket v1:N and Absent 0 — the histogram still represents the (single-version) distribution, not a scalar. A collection with records at version 2+ (future-version) reports them as `FutureVersion`, not as pending.

In `internal/store/migrate_status_test.go`:
4. Against real pinned Qdrant, seed a mixed-version collection (records with schema_version absent, some with 0, some stamped 1, some stamped 2 as future-version) and assert: `MigrateStatus` reports the v0 and v1 buckets plus the Absent bucket, `sum(buckets)+Absent+FutureVersion == Total == a whole-collection exact Count`, and empty + all-migrated cases produce the documented shapes (all-v1 → Absent 0, empty → all-zero). The future-version record must appear in FutureVersion, never in a bucket labeled as pending.
  </action>
  <acceptance_criteria>
    - `go test ./internal/store/ -run TestMigrateStatus -count=1 -v` passes against real pinned Qdrant.
    - `MigrateStatus` returns a distribution (buckets + absent + future_version), never a scalar; `sum(buckets) + Absent + FutureVersion == Total` holds in the mixed-version fixture (REVIEWS.md M4).
    - The bucket list is sorted by version ascending (deterministic output).
    - A future-version record (e.g. schema_version=2) in a CurrentVersion=1 collection reports in FutureVersion, never in Buckets.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/store/ -run TestMigrateStatus -count=1 -v</automated>
  </verify>
  <done>Store.MigrateStatus serves a server-side, facet+absent-count version histogram that correctly represents a mixed-version collection, handles future-version records, and sums to the total count.</done>
</task>

<task type="auto">
  <name>Task 2: Store.Revert with whole-range preflight, per-record chain, reversible test injection, and specified inverse write contract</name>
  <files>
    internal/store/revert.go
    internal/store/revert_test.go
  </files>
  <read_first>
    internal/store/migrate.go (the Store.Migrate re-derive-per-pass loop: fresh exact Count → fresh Scroll with nil Offset each pass, resume = call again; per-point SetPayload-only write; int(target) cast)
    internal/migrate/step.go (sealed Reversibility accessors: `Inverse(r) (ApplyFunc, bool)`, `IrreversibleReason(r) (string, bool)`, `Step.From()/To()/Reversibility()`)
    internal/migrate/registry.go (StepsFrom helper for chain selection; used in REVERSE to build the per-record reverse chain (REVIEWS.md H5))
    .planning/phases/04-migration-cli-first-customer/04-CONTEXT.md (D-13 whole-range preflight before any write; D-14 refusal envelope field=name hint=code; D-15 dedicated method, no direction param; D-16 preview/apply parity later)
    .planning/phases/04-migration-cli-first-customer/04-REVIEWS.md (H4 — fixture injection via `revertWithSteps` not representable through public API alone; H5 — per-record reverse chain; M3 — inverse write contract)
    docs-site/src/content/docs/reference/errors.md (the field=<name> hint=<code> envelope grammar at line 14)
  </read_first>
  <action>
In a NEW file `internal/store/revert.go`:

1. Define internal helpers:
   - `revertOptions` struct: `To migrate.Version`, `Steps []migrate.Step`, `Batch uint32`, `DryRun bool`. Production default: Steps=Registry, To as passed, Batch=migrateBatch, DryRun=false.
   - `revertStepsFrom(steps []migrate.Step, from, to migrate.Version) ([]migrate.Step, error)` — selects the reverse chain from `from` down to `to`: walk StepsFrom forwards, then reverse the result order (inverses run in REVERSE order — D-15). When from <= to, return empty (nothing to revert). When `from` cannot be reached (chain break), error.
   - `reversePreflight(steps []migrate.Step, to migrate.Version) error` — walks every step whose `To() > to`, calls `migrate.IrreversibleReason(step.Reversibility())`; if any returns a non-empty reason, format the full refusal error: `field=steps hint=irreversible: revert cannot reach v%d: step (From=%d To=%d) is irreversible: %s; recovery is a collection snapshot`. Name EVERY irreversible step, its From/To, its reason, and snapshot in the `field=<name> hint=<code>` envelope (D-14). Returns nil when all steps are reversible. Used by both preview and apply to avoid duplication (REVIEWS.md M8 resolution — one shared preflight helper, not duplicated across CLI and store).

2. `func (s *Store) Revert(ctx context.Context, to migrate.Version) (RevertResult, error)` — production convenience. Delegates to `s.revertWithSteps(ctx, to, migrate.Registry)` internally:
   ```go
   func (s *Store) Revert(ctx context.Context, to migrate.Version) (RevertResult, error) {
       return s.revertWithSteps(ctx, to, migrate.Registry)
   }
   ```
   The unexported `revertWithSteps` is the shared test fixture path (REVIEWS.md H4): tests inject a reversible fixture step via `Steps` while the production path uses `Registry`.

3. `func (s *Store) revertWithSteps(ctx context.Context, to migrate.Version, steps []migrate.Step) (RevertResult, error)`:
   - Result type: `type RevertResult struct { Reverted uint64; Failed uint64; Passes uint64; Backlog uint64 }`, with a doc comment stating Reverted/Failed are telemetry and Backlog (a fresh exact Count after the walk) is truth, mirroring `MigrateResult`'s doc.
   - Wrap with `tracer.Start(ctx, "store.Revert")` + defer span.End + `telemetry.RecordStoreOp(ctx, "Revert", start, err)` + error→span on err.
   - Validate steps, then run `reversePreflight(steps, to)`. If preflight fails, return the refusal error — zero records touched (D-13).
   - REVERSE WALK: same re-derive-per-pass loop shape as `Store.Migrate` — per pass a fresh exact Count of records above `to`, then a fresh Scroll (Offset nil). For each record at `fromV`, compute the PER-RECORD reverse chain via `revertStepsFrom(steps, fromV, to)` (REVIEWS.md H5 — each record gets a chain based on ITS stored version, not a fixed global chain). Apply each selected step's `Inverse()` in reverse order via per-point DeletePayload (for keys the inverse REMOVES — present in before but absent after inverse) and SetPayload (for keys the inverse ADDS or CHANGES — present in after but absent before inverse, plus schema_version). Use the `int(target)` cast at every Qdrant value boundary (tdt50852ww), and set `schemaVersionKey` back to the target version on the write.
   - **Inverse write contract (REVIEWS.md M3)**:
     - For each inverse `ApplyFunc` result against the current payload:
       - `RemovedKeys(beforeInverse, afterInverse)` → `DeletePayload` — keys the inverse eliminates.
       - `AddedKeys(beforeInverse, afterInverse)` → `SetPayload` — keys the inverse adds or value-changes.
       - Keys whose VALUE changed (same key, different value in before/after) are NOT detected by this key-set diff; each inverse's declared purpose determines which keys it touches. The only structural guarantee is that the inverse's actual delta — what it added and removed — becomes the write payload. ANY key the inverse adds or removes, even outside the forward `AddsKeys` set, is legal (the anti-additive direction — D-15 explicitly permits this since CheckAdditive is additive-only and never runs on the revert path).
       - `schema_version` is ALWAYS SetPayload-stamped to the target version the record will reach after this inverse pass.
     - No collection lock; resume = call `Revert` again.
   - Line-by-line: do NOT add a direction parameter to `Store.Migrate`; do NOT exempt or bypass `CheckAdditive` — `Revert` is a separate method and the anti-additive direction is why it is separate (D-15).

4. Edge behavior: if no registered step exists to walk (current state: the only step is irreversible), `Revert(ctx, 0)` returns the correct preflight refusal, not a generic "no chain" error.

In `internal/store/revert_test.go`:
5. Preflight refusal (irreversible range): against real pinned Qdrant, seed a collection with records at v1 (minted by 04-01). Assert `s.Revert(ctx, 0)` returns an error containing every `field=steps hint=irreversible` marker, the step's From=0 To=1, `irreversible`, `snapshot`, and that a payload probe confirms ZERO records were touched (no schema_version back-dated, no key removed).
6. Reversible range convergence via fixture injection (REVIEWS.md H4): define a fixture step with a reversible inverse and a test-step-only version (e.g., v1→v2 with a Reversible inverse v2→v1). Seed records at v2; call `s.revertWithSteps(ctx, 1, fixtureSteps)` (NOT the production `Revert` — uses the unexported injection helper). Assert `Backlog == 0`, the collection reached v1, and the inverse's `SetPayload`/`DeletePayload` delta was applied per the inverse write contract (REVIEWS.md M3).
7. Per-record chain selection (REVIEWS.md H5): seed records at BOTH v1 and v2 in one collection. Revert to v0 with a reversible chain (v1→v2 reversible, v0→v1 reversible). Assert all records converge at v0, and the per-record chain selection correctly picks the shorter route for v1 records (only the v0→v1 inverse) vs the longer route for v2 records (both inverses). Confirm Backlog == 0.
  </action>
  <acceptance_criteria>
    - `go test ./internal/store/ -run TestMigrateRevert -count=1 -v` passes against real pinned Qdrant.
    - The preflight refusal names every irreversible step (From/To), its reason, and `snapshot`, and returns BEFORE any write (payload probe shows zero records touched).
    - `Store.Revert` signature is `Revert(ctx, to)` with no direction on `Store.Migrate`; grep confirms no `Revert`-by-direction parameter on Migrate.
    - The fixture-injection test uses `revertWithSteps` and converges a reversible range (H4 closed).
    - The per-record chain-selection test proves records at different starting versions each get the correct sub-chain (H5 closed).
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/store/ -run TestMigrateRevert -count=1 -v</automated>
  </verify>
  <done>Store.Revert refuses an irreversible range before any write (naming each step, reason, and snapshot path), reverse-walks declared inverses for a reversible range to convergence with fixture injection (H4), selects the reverse chain per-record (H5), and applies inverses with a specified DeletePayload/SetPayload write contract (M3).</done>
</task>

<task type="auto">
  <name>Task 3: Non-blocking pending-migration startup warning with corrected predicate</name>
  <files>
    internal/server/tools.go
    internal/server/tools_test.go
  </files>
  <read_first>
    internal/server/tools.go (warnOwnerlessRecords :459-482 — the best-effort, never-gating startup warning shape; buildDepsFromEnv wire at :209; slog import path)
    internal/store/migrate_status.go (the Store.MigrateStatus the warning consumes — now with FutureVersion)
    .planning/phases/04-migration-cli-first-customer/04-CONTEXT.md (REQ-migrate-never-automatic; exact wording and placement is discretion)
    .planning/phases/04-migration-cli-first-customer/04-REVIEWS.md (H3 — CORRECTED predicate: pending = Absent + sum(bucket.Count where bucket.Version < CurrentVersion), NOT total count)
  </read_first>
  <action>
In `internal/server/tools.go`:
1. Add `warnPendingMigrations(st *store.Store)` (name is discretion, mirror `warnOwnerlessRecords`) that:
   - Uses a bounded context (`context.WithTimeout(context.Background(), 10*time.Second)` — copy the 10s idiom from warnOwnerlessRecords).
   - Calls `st.MigrateStatus(ctx)`; on error, `slog.Warn("could not check for pending schema migrations", "err", err)` and return WITHOUT blocking or failing startup (best-effort, never-gating).
   - Computes pending = `st.Absent + sum(bucket.Count for bucket.Version < migrate.CurrentVersion)` (REVIEWS.md H3 — CORRECTED: NOT `sum(buckets)+Absent` which is the total collection count and would spuriously warn for every all-current collection). When pending > 0, `slog.Warn("pending schema migrations exist; run: engram migrate", "pending", pending, "current_version", migrate.CurrentVersion)`.
   - When `st.FutureVersion > 0` (REVIEWS.md M4), logs a SEPARATE compatibility warning: `slog.Warn("records at a future schema version exist — this binary may be too old for some records; downgrade may be incomplete", "future_version_count", st.FutureVersion, "server_version", migrate.CurrentVersion)`.
   - MUST NOT invoke `Store.Migrate` under any circumstance — the ONLY automatic surface this phase allows is the warning (REQ-migrate-never-automatic).
2. Wire it alongside `warnOwnerlessRecords(st)` in `buildDepsFromEnv` (tools.go:209) so it runs once at startup, exactly where the existing warning hangs.

In `internal/server/tools_test.go`:
3. Add a Qdrant-backed server integration test (REVIEWS.md M5 — use a real `*store.Store` behind testcontainers, not a fake seam; the test may also be written as a store-level test that asserts the warning function emits the expected slog lines): assert that a pending-pending collection (records behind CurrentVersion) yields the `pending schema migrations` warning; an already-current collection (all records at CurrentVersion) yields NO warning (proving H3's corrected predicate did not regress into total-count behavior); a future-version collection yields only the compatibility warning, never the pending-migrations warning.
   - For testing: the warning function can be tested by capturing slog output (slog.SetDefault with a test handler + recording handler assertion), or by a behavioral test on the warning function's call tree. Prefer the real-store approach over a fake.

   Additionally: a grep assertion confirms the startup path never calls `Store.Migrate`:
   `rg -n "Store.Migrate" internal/server/tools.go` must return nothing.
  </action>
  <acceptance_criteria>
    - `go test ./internal/server/ -run 'TestStartupWarnPendingMigrations|TestWarnPendingMigrations' -count=1 -v` passes.
    - `rg -n "warnPendingMigrations" internal/server/tools.go` shows the func and its call inside `buildDepsFromEnv`; `rg -n "Store.Migrate" internal/server/tools.go` returns nothing (no mutating call in the startup path).
    - A collection all at CurrentVersion produces no pending-migration warning (H3 closed — the predicate is `pending = Absent + sum(behind buckets)`, not total count).
    - A collection with FutureVersion > 0 produces a compatibility warning without producing a pending-migration warning.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/server/ -run 'TestStartupWarnPendingMigrations|TestWarnPendingMigrations' -count=1 -v && { ! rg -n "Store.Migrate" internal/server/tools.go; }</automated>
  </verify>
  <done>Server startup emits a correctly-predicated non-blocking pending-migration warning (via MigrateStatus — only records BELOW CurrentVersion) without ever invoking a migration, and a separate compatibility warning for future-version records, wiring REQ-migrate-never-automatic (REVIEWS.md H3, M4 closed).</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI → store (revert) | Revert mutates payload keys in the anti-additive direction (inverse-removed keys), the highest-risk write surface in the phase. |
| server bootstrap → store (status) | Startup reads migration state and logs; it must never progress a migration. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-02 | Tampering | `Store.Revert` whole-range preflight | critical | mitigate | Whole-range irreversible preflight (D-13) refuses the entire op before any write; refusal names every irreversible step + snapshot path (D-14). Because the only registered step is irreversible, a partial back-version is unrepresentable in this phase. |
| T-04-03 | Tampering | `Store.Revert` write shape | high | mitigate | Dedicated method (D-15), never a direction on `Store.Migrate`; per-record reverse chain (H5); per-point DeletePayload/SetPayload with the int(target) version stamp and specified inverse write contract (M3); re-derive-per-pass means resume is just calling Revert again. |
| T-04-04 | Tampering | `internal/server/tools.go` startup warning | medium | mitigate | The warning consumes read-only MigrateStatus and never calls Store.Migrate; a startup path that invokes a migration is a build/test defect (asserted green). Corrected predicate (H3) ensures only records BELOW CurrentVersion trigger the warning. |
| T-04-04b | Information Disclosure | `warnPendingMigrations` future-version warning | low | accept | A separate non-actionable compatibility warning for future-version records is diagnostic-only (M4); an operator reading it learns only what version the server binary is. |
| T-04-04c | Tampering | `revertWithSteps` unexported injection helper | medium | mitigate | Unexported method, test-only usage of fixture steps; production `Revert(ctx, to)` always uses `migrate.Registry` as steps, never accepts caller-supplied steps in its public signature (H4). |

</threat_model>

<verification>

After plan execution (wave 2):

- `go test ./internal/store/ -run 'TestMigrateStatus|TestMigrateRevert' ./internal/server/ -count=1` green.
- `go vet ./internal/store/ ./internal/server/` clean.
- `task` green (lint + full test).
- REVIEWS.md H3, H4, H5, M3, M4, M5 closed.

</verification>

<success_criteria>

- A mixed-version collection renders a version histogram that sums to the total (REQ-migrate-status-histogram store half); future-version records are separately reported.
- An irreversible revert range is refused whole with zero records touched; a reversible fixture range reverse-walks to convergence via `revertWithSteps` injection (REQ-migrate-revert store half).
- The reverse chain is selected per-record based on the record's stored version, not a fixed global chain (H5).
- The inverse write contract is specified and tested: DeletePayload for removed keys, SetPayload for added/changed keys, schema_version stamped to the target version (M3).
- Startup warns correctly: only records BELOW CurrentVersion trigger the pending-migration warning (H3), future-version records trigger a separate compatibility warning (M4), and no migration is ever invoked (REQ-migrate-never-automatic).

</success_criteria>

## Artifacts this phase produces

New store methods + types:

- `internal/store/migrate_status.go`: `type VersionBucket struct { Version int; Count uint64 }`, `type MigrateStatusResult struct { Buckets []VersionBucket; Absent uint64; FutureVersion uint64; Total uint64 }`, `func (s *Store) MigrateStatus(ctx context.Context) (MigrateStatusResult, error)`.
- `internal/store/revert.go`: `type RevertResult struct { ... }`, `func (s *Store) Revert(ctx context.Context, to migrate.Version) (RevertResult, error)`, `func (s *Store) revertWithSteps(ctx context.Context, to migrate.Version, steps []migrate.Step) (RevertResult, error)` (unexported), `revertOptions` struct, preflight helper `reversePreflight(steps []migrate.Step, to migrate.Version) error`, `revertStepsFrom(steps []migrate.Step, from, to migrate.Version) ([]migrate.Step, error)`.
- `internal/server/tools.go`: `func warnPendingMigrations(st *store.Store)` with corrected H3 predicate + future-version warning, wired inside `buildDepsFromEnv`.

New tests: `TestMigrateStatus` (mixed/empty/all-migrated/future-version), `TestMigrateRevert` (preflight refusal + reversible fixture injection via `revertWithSteps` + per-record chain selection), `TestStartupWarnPendingMigrations`/`TestWarnPendingMigrations` (corrected predicate + future-version + no Store.Migrate).

<output>
Create `.planning/phases/04-migration-cli-first-customer/04-02-SUMMARY.md` when done
</output>