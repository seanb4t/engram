---
phase: 04-migration-cli-first-customer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/migrate/step.go
  - internal/migrate/registry.go
  - internal/migrate/migrate.go
  - internal/migrate/additive.go
  - internal/migrate/v1_step.go
  - internal/migrate/migrate_test.go
  - internal/migrate/additive_test.go
  - internal/store/migrate.go
  - internal/store/migrate_test.go
autonomous: true
requirements:
  - REQ-backfill-shortids-first-step
  - REQ-migrate-command
  - REQ-migrate-preview-apply-parity
estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "A BARE legacy record (no short_id, no schema_version key) driven through Store.Migrate against real pinned Qdrant with the registered v0→v1 step mints a 10-char Crockford short_id (via Store.MintShortID), stamps schema_version=V1, and a second Migrate run reports Backlog 0 (no-op) — the re-derivation identity that REQ-migrate-preview-apply-parity is built on."
    - "A record that ALREADY HAS a short_id but NO schema_version — the exact storage state produced by the old standalone Store.BackfillShortIDs — passes the v0→v1 step: CheckAdditive accepts the declared `short_id` as already-present (carve-out, does NOT fire 'declared key never added'), short_id is left untouched (never re-minted), and schema_version=V1 is stamped. This is the mixed-state compatibility fix for REVIEWS.md H1 and the precondition for 04-04's safe deletion of Store.BackfillShortIDs."
    - "A registered step that is minter-aware declares a single apply path (constructor-enforced): NewMintingStep panics on nil rev OR nil applyMinter, exactly like NewStep — there is no representable nil-apply state and no step can carry both apply and applyMinter."
    - "migrate.CurrentVersion rises 0→1 in the SAME change that registers the v0→v1 step (never a standalone bump); TestCurrentVersionValue updates from 0 to 1 and passes."
    - "Store.Migrate preview (DryRun:true) performs one exact Count + a FULL-BACKLOG page-through scroll (next_page_offset pagination, never a single batch), computes the per-record chain, write-map, CheckAdditive and mints (into the seen set), counts would-migrate, writes NOTHING, and returns the previewed point-identity manifest plus a point count / payload probe that confirms zero SetPayload."
    - "Store.Migrate APPLY with a non-nil Manifest uses a SINGLE-PASS full-backlog scroll (not the re-derivation loop): for each point, if in both manifest AND fresh-re-derivation → migrate (Migrated); if in manifest but no longer below-target → Spared, skip; if NOT in manifest but still below-target → Appeared, skip, never migrate. Backlog after apply = the true fresh Count (which includes Appeared records intentionally not migrated). The PA-3 guard never fires because the manifest path is a single pass — there is no second pass to compare against."
    - "On a stable collection (no new records arriving between preview and apply), Store.Migrate preview's projected would-migrate set and the applied Migrated set coincide (the store-layer parity REQ-migrate-preview-apply-parity is proven by identity-set, not count-equality). Backlog after apply on such a stable collection IS 0."
    - statement: "No migration ever runs automatically on server startup during this phase; the only automatic surface is a non-blocking warning, and a startup path that calls Store.Migrate is a defect."
      verification: backstop
  prohibitions:
    - "A provider-endpoint minter MUST NOT be captured into the step value: the minter is always a parameter (ApplyMinterFunc), never a stored store, so the step stays a stdlib-only package-literal value."
    - "Store.Migrate MUST NOT gain a direction parameter for revert; reverting is a dedicated Store.Revert, never a mode on Migrate."
    - "The sweep MUST NOT skip the two-clone-per-step discipline, the int(target) cast, or the per-point SetPayload-only write shape when a step is minter-aware."
    - "DryRun MUST NOT silently loop forever: because nothing is written, the PA-3 non-shrinking-backlog guard would treat DryRun as replenishment and error; DryRun performs a single full-backlog page-through (one Count + paginated Scroll) and returns."
    - "CheckAdditive MUST NOT be weakened globally from set-equality to subset — the task-limited carve-out accepts a declared key ALREADY PRESENT in the record's original payload, and keeps every other failure mode (undeclared keys added, removed keys, declared-but-never-added-AND-never-present) identical to Phase 3 (REVIEWS.md H1)."
    - "The v0→v1 step MUST NOT re-mint short_id for a record that already carries one — (D-03) minted short_ids may already be cited by get_memory/supersede_memory, so an existing short_id is preserved verbatim, never regenerated."
    - "The manifest-limited apply MUST NOT claim Backlog == 0 when Appeared records exist — Backlog is the TRUE fresh Count after apply, which intentionally includes Appeared records not processed by this manifest (REVIEWS.md H7)."
    - "The PA-3 guard MUST NOT fire on Appeared records in manifest-limited mode — the single-pass manifest path does NOT re-derive, so PA-3 has no second pass to compare against (H7 resolution)."
---

<objective>
Register the registry's first real customer — the v0→v1 `backfill-short-ids` step — and give the `Store.Migrate` sweep everything it needs to run it: a minter-injected apply path (D-01/D-02/D-04), an idiomatic non-writing full-backlog preview (so the CLI's preview honors "previews only, no writes" per REQ-migrate-command SC1), the manifest-bridged preview/apply intersection that REQ-migrate-preview-apply-parity (SC3) actually requires, and a CheckAdditive carve-out so the step genuinely handles the pre-existing-short_id storage state produced by the old standalone backfill (REVIEWS.md H1).

This plan also resolves H7 (the PA-3 guard conflict with manifest-limited Appeared records) and M13 (the additive_test.go empty-registry assertion that breaks when the v0→v1 step is registered).

Purpose: This is the tracer slice of the phase — the thinnest end-to-end path through every mechanism the phase adds: a new minter-aware step type, its registration + the `CurrentVersion` 0→1 bump, the sweep branch that drives it, the pre-existing-short_id carve-out that makes the step correct on production data, and a preview that both does not write and projects the WHOLE backlog. It proves the additive-only + reversibility invariants still hold once a real step (and a real mint side-effect) are in the chain, against real pinned Qdrant. The manifest-limited apply uses a single-pass approach (not the re-derivation loop) so the PA-3 guard never fires on Appeared records.

Output: `ApplyMinterFunc`, `NewMintingStep`, a minter-aware `Step` apply path, the registered v0→v1 step in `migrate.Registry` with `CurrentVersion` = 1, the CheckAdditive pre-existing-key carve-out, `MigrateOptions.{DryRun,Manifest}`, manifest-consuming apply in `Store.Migrate` (single-pass, PA-3-safe), and the end-to-end tests proving the single-path happy run, the existing-short_id mixed-state run, full-backlog DryRun-no-writes, manifest intersection parity with Appeared/Spared reporting, and second-run convergence.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/04-migration-cli-first-customer/04-CONTEXT.md
@.planning/phases/04-migration-cli-first-customer/04-RESEARCH.md
@.planning/phases/04-migration-cli-first-customer/04-PATTERNS.md
@.planning/phases/04-migration-cli-first-customer/04-REVIEWS.md

@internal/migrate/step.go
@internal/migrate/registry.go
@internal/migrate/additive.go
@internal/migrate/migrate.go
@internal/store/migrate.go
@internal/store/store.go
</context>

<tasks>

<task type="tracer">
  <name>Task 1: v0→v1 minter step — NewMintingStep, minter-aware Step, CheckAdditive carve-out, register + bump, update additive_test.go Registry assertion (M13)</name>
  <files>
    internal/migrate/step.go
    internal/migrate/registry.go
    internal/migrate/migrate.go
    internal/migrate/additive.go
    internal/migrate/v1_step.go
    internal/migrate/migrate_test.go
    internal/migrate/additive_test.go
  </files>
  <read_first>
    internal/migrate/step.go (NewStep nil-panic discipline, sealed Reversibility, Step struct, Step.Apply, Step.ApplyMinter accessor)
    internal/migrate/registry.go (package-scope `var Registry` literal + the `// PHASE4:` marker asserted by TestRegistryIsAPackageLevelVarWithPhase4Marker)
    internal/migrate/migrate.go (CurrentVersion doc — a standalone bump is forbidden)
    internal/migrate/additive.go (CheckAdditive's set-equality invariant + the "declared key never added" failure)
    internal/migrate/additive_test.go:40-42 (the len(Registry)==0 assertion — M13: this MUST be updated when Registry gains the v0→v1 step)
    .planning/phases/04-migration-cli-first-customer/04-CONTEXT.md (D-01, D-02, D-03, D-04)
    .planning/phases/04-migration-cli-first-customer/04-RESEARCH.md (Code Examples: NewMintingStep + ApplyMinterFunc; Pitfall 3: CurrentVersion pin)
    .planning/phases/04-migration-cli-first-customer/04-REVIEWS.md (H1 — the CheckAdditive conflict with existing-short_id records; M13 — additive_test.go Registry assertion)
  </read_first>
  <action>
In `internal/migrate/step.go`:

1. Add the exported func type `type ApplyMinterFunc = func(m map[string]any, mint func() (string, error)) (map[string]any, error)` exactly as D-02 shapes it — the minter is a parameter, never a captured store, so the step stays a pure stdlib package-literal value.
2. Add a field `applyMinter ApplyMinterFunc` to the `Step` struct; keep the existing `apply ApplyFunc` field. A `Step` carries exactly one apply path, constructor-enforced.
3. Add the exported constructor `func NewMintingStep(from, to Version, addsKeys []string, rev Reversibility, applyMinter ApplyMinterFunc) Step` mirroring `NewStep`'s nil-check discipline verbatim: `panic("migrate.NewMintingStep: rev must be non-nil")` when `rev == nil`, `panic("migrate.NewMintingStep: applyMinter must be non-nil")` when `applyMinter == nil`; `addsKeys: slices.Clone(addsKeys)`; set the `applyMinter` field. Do NOT add any builder modifier or sentinel apply (rejected design).
4. Add the accessor `func (s Step) ApplyMinter() (ApplyMinterFunc, bool)` returning `(s.applyMinter, s.applyMinter != nil)` — the only way a caller (`Store.Migrate`) detects a minter-aware step; no type assertion, no exported struct-literal path.
5. Add the apply-call helper `func (s Step) ApplyMinterMint(payload map[string]any, mint func() (string, error)) (map[string]any, error)` that calls `s.applyMinter(payload, mint)` — mirroring `Step.Apply`'s shape so the sweep can choose one apply path uniformly. Only for minter-aware steps; reaching it on a non-minter step is a programming error (guard at the sweep).

In `internal/migrate/additive.go` (REVIEWS.md H1 fix — task-limited carve-out, NOT a global subset weakening):
6. Modify `CheckAdditive(s, before, after)` so its "declared key never added" branch skips any declared key that is ALREADY PRESENT in `before` (the record's ORIGINAL payload). Concretely: when building the `missing` slice, for each declared key not in the added set, only count it as missing if it is also absent from `before`. Update the function's doc comment to state this precisely: a declared key already carried by the record is not a declaration failure — the step's job is to ensure the key exists and it already does; a step that declares a key it neither adds nor finds pre-existing is still caught. Keep the undeclared-keys-added and removed-keys branches byte-for-byte identical to Phase 3. This carve-out is REQUIRED for the minting step to pass on records the old standalone backfill already processed (`short_id` present, `schema_version` absent).

In a new file `internal/migrate/v1_step.go` (any filename, stdlib-only):
7. Implement the v0→v1 apply func that fills `short_id`: `func v1FillShortID(payload map[string]any, mint func() (string, error)) (map[string]any, error)` — if `payload["short_id"]` is already present AND non-empty, return the payload unchanged WITHOUT calling `mint()` (an existing short_id is preserved verbatim — never re-minted, per D-03/REVIEWS.md H1; the CheckAdditive carve-out makes this legal because short_id is pre-existing); else `maps.Clone(payload)`, set `payload["short_id"] = sid` via `mint()`, return it. If `mint()` fails, wrap and return the error. Declared `AddsKeys` is `["short_id"]`.

In `internal/migrate/registry.go`:
8. Register the v0→v1 step in the package-scope literal, keeping the `var Registry = []Step{ ... }` declaration shape and the `// PHASE4:` marker intact (both are load-bearing / asserted). Add `NewMintingStep(0, 1, []string{"short_id"}, Irreversible("snapshot recovery: minted short_ids may already be cited by get_memory/supersede_memory (D-03)"), v1FillShortID)`. The step MUST be declared `Irreversible` with that non-empty reason (D-03); do not pass a `Reversible`.

In `internal/migrate/migrate.go`:
9. Raise `const CurrentVersion Version = 0` to `1` in this same change and update the doc comment's claim that "no phase has yet registered a step". Keep the additive doc (schema-version payload semantics) accurate.

In `internal/migrate/additive_test.go`:
10. UPDATE the `len(Registry) != 0` assertion at line 40-42 (REVIEWS.md M13): this assertion asserts that the Registry is empty, which is true in Phase 3 but false once the v0→v1 step is registered. Replace the empty-registry assertion with an assertion that the Registry now contains the v0→v1 step AND that the test table still runs against test-only fixture steps (never production Registry). The assertion should be: `if len(Registry) < 1 { t.Fatalf("Registry has %d step(s), want >=1: Phase 4 registered the v0→v1 step — this test's table runs against test-only fixtures, not Registry, so it is independent of Registry content") }` AND `if Registry[0].From() != 0 || Registry[0].To() != 1 { t.Fatalf("first registered step must be v0→v1, got From=%d To=%d", ...) }`. Keep the test fixture table unchanged (it still iterates test-only fixture steps, never Registry).

In `internal/migrate/migrate_test.go`:
11. Update `TestCurrentVersionValue` to expect `Version(1)` (it currently pins `0`); update the failure message string. Add assertions that `NewMintingStep` panics on nil `rev` and on nil `applyMinter`; that `Step.ApplyMinter()` returns `(nil, false)` on a plain `NewStep` step and `(non-nil, true)` on a `NewMintingStep` step; and that `v1FillShortID` is idempotent when `short_id` is already present (and does NOT call mint — use a recording stub mint that panics if invoked) and mints-and-adds when absent (using a stub mint func returning a fixed sid).
12. Add a CheckAdditive carve-out test (REVIEWS.md H1/M2): a `NewMintingStep(0,1,["short_id"],...)` step driven against `before = {short_id: "abc123"}` (no schema_version) must return nil from CheckAdditive (no "declared key never added"), while the SAME step against `before = {}` must STILL fail if the apply returns a map with no short_id added (proving the carve-out did not weaken the genuine declared-but-never-added detection). Also assert the undeclared-added-key and removed-key branches are unchanged (a step adding an undeclared key, or removing a key, still fails).
  </action>
  <acceptance_criteria>
    - `go test ./internal/migrate/ -run 'TestCurrentVersionValue|TestNewMintingStep|TestV1FillShortID|TestCheckAdditivePreExistingKey|TestRegistryIsAPackageLevelVarWithPhase4Marker'` passes.
    - `go build ./internal/migrate/...` succeeds with `migrate.CurrentVersion == Version(1)`.
    - `rg -c "func NewMintingStep|type ApplyMinterFunc|func \(s Step\) ApplyMinter|func \(s Step\) ApplyMinterMint"` over `internal/migrate/step.go` yields four matching definitions.
    - `rg -n "NewMintingStep\(0, 1" internal/migrate/registry.go` shows the registered v0→v1 step with an `Irreversible(...)` reason, and the `// PHASE4:` marker line is still present in the file.
    - The CheckAdditive carve-out test proves both directions of H1: existing-short_id+no-schema passes (nil error), missing-short_id+no-add still fails; the undeclared/removed branches still fail.
    - `go test ./internal/migrate/ -run TestRegistryIsAPackageLevelVarWithPhase4Marker` still passes (proves Registry stayed a package-scope literal with the marker).
    - M13 closed: `go test ./internal/migrate/ -run TestAdditiveOnlyKeySetDiff` passes — the additive test fixture table still runs, no longer asserting empty Registry.
    - All prior Phase 3 `go test ./internal/migrate/ -run TestCheckAdditive` cases still pass — the carve-out did not weaken the other failure modes.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/migrate/ -run 'TestCurrentVersionValue|TestNewMintingStep|TestV1FillShortID|TestCheckAdditive|TestRegistryIsAPackageLevelVarWithPhase4Marker|TestAdditiveOnlyKeySetDiff' ./internal/migrate/ && go build ./internal/migrate/...</automated>
  </verify>
  <done>The v0→v1 minter-aware step exists, is registered in the package-scope Registry literal with an Irreversible reason, CurrentVersion is 1 with the pin updated, the minter-step surface (ApplyMinterFunc/NewMintingStep/Step.ApplyMinter) is unit-tested, CheckAdditive accepts a pre-existing declared key without weakening the other failure modes (REVIEWS.md H1 closed), and additive_test.go's Registry assertion is updated for the now-non-empty Registry (M13 closed).</done>
</task>

<task type="auto">
  <name>Task 2: Store.Migrate minter branch + full-backlog preview + manifest-bridged apply (single-pass, PA-3-safe) — end-to-end v0→v1 (H7/H2)</name>
  <files>
    internal/store/migrate.go
    internal/store/migrate_test.go
  </files>
  <read_first>
    internal/store/migrate.go (the whole shipped sweep — re-derivation loop, two-clone PA-5a discipline, int(target) cast, per-point SetPayload-only write, PA-3 termination guard at :167-178, MigrateResult doc: Migrated/Failed are TELEMETRY, Backlog is truth)
    internal/store/store.go:2661-2724 (Store.MintShortID signature `(ctx, seen map[string]struct{}) (string, error)`), :370 (schemaVersionKey), :2729-2797 (missingShortIDFilter + BackfillShortIDs — the state PHASE 4 must coexist with until 04-04)
    internal/store/reindex.go store.go:3010-3020 + summarize.go:33/169 (the established DryRun idiom this plan reuses)
    internal/store/migratebacklog.go (backlogFilter, PA-4 short-circuit, versionOf)
    .planning/phases/04-migration-cli-first-customer/04-CONTEXT.md (D-01, D-04 — lazy seen-map mint closure semantics and placement is discretion; D-06 fixture-DrivenSteps untouched)
    .planning/phases/04-migration-cli-first-customer/04-REVIEWS.md (H2 — DryRun must cover the full backlog, not one batch; H6 — manifest ∩ fresh re-derivation, not count-equality; H7 — PA-3 guard conflict with Appeared records resolved via single-pass manifest path)
  </read_first>
  <action>
In `internal/store/migrate.go`:

1. Add `DryRun bool` to the `MigrateOptions` struct, with a doc comment: true = preview — compute the full projection without writing, following the `ReindexOptions.DryRun` / `SummarizeOptions.DryRun` idiom. This is NOT the minter option D-02 rejected (that was injecting the minter via options); DryRun is the standard operator-tier preview knob. Executors MUST NOT repurpose it for step injection.
2. Add `Manifest map[string]migrate.Version` to `MigrateOptions`, with a doc comment: non-nil = apply-mode manifest from a prior PreviewMigrate; the sweep's eligibility filter intersects this manifest with the fresh re-derivation (SC3 / REVIEWS.md H6). When non-nil, the sweep PROCESSES ONLY points whose id is in the manifest — a point that was NOT previewed can never be migrated by apply. A point that WAS previewed but is no longer below-target in the fresh re-derivation is reported as Spared, never migrated. When non-nil, the manifest path uses a SINGLE PASS (full-backlog scroll, no re-derivation loop) to avoid the PA-3 guard firing on intentionally-skipped Appeared records (REVIEWS.md H7).
3. In `Store.Migrate`'s per-point chain loop (the `for _, step := range chain` block): before the `step.Apply` call, check `if applyMinter, ok := step.ApplyMinter(); ok { ... step.ApplyMinterMint(clone, mint) ... } else { ... step.Apply(clone) ... }`. Build `mint` and `seen` LAZILY on the first minter-aware step in a single Migrate call: `var seen map[string]struct{}` then `mint := func() (string, error) { return s.MintShortID(ctx, seen) }` (D-04; `seen` lazily initialized to `make(map[string]struct{})` on first use). Keep the two-independent-clones-per-step (before & after) and the CheckAdditive call identical to the non-minter path — a minter step's before/after map still feeds `migrate.CheckAdditive`. Because a record may already carry `short_id` (Task 1 carve-out), the mint closure must be invoked ONLY for records whose payload lacks short_id; `v1FillShortID` already guards this.
4. Add the identity-set manifest handling at the RE-DERIVATION ELIGIBILITY boundary (not the per-point chain loop — the eligibility decision is at the POINT level, before the chain runs):
   - When `opts.Manifest != nil`, a point is processed ONLY if its id is in the manifest. Points NOT in the manifest are skipped: if they are below-target in the fresh re-derivation, they count toward `res.Appeared`; if they are already at-target, they are skipped without counting.
   - Points IN the manifest but no longer below-target in the fresh re-derivation → counted into `res.Spared` and skipped.
   - Points IN the manifest AND still below-target → chain runs and the record is migrated (counted into `res.Migrated`).
5. Implement the MANIFEST-LIMITED APPLY path as a SINGLE PASS (REVIEWS.md H7 resolution — the PA-3 guard must not fire on Appeared records):
   - When `opts.Manifest != nil`, do NOT enter the re-derivation loop (`for {}`). Instead:
     a) Perform one exact Count (backlogFilter) to capture the pre-migration backlog for telemetry.
     b) Page through the ENTIRE backlog (like DryRun — full-backlog Scroll via next_page_offset until exhausted). For each point, apply the manifest intersection logic from step 4 above. Migrate eligible points via SetPayload.
     c) After the full scroll, perform ONE MORE exact Count to get the true post-apply backlog.
     d) Set `res.Backlog = postApplyCount` — this is the TRUE count, which includes Appeared records that were intentionally not processed. Do NOT subtract Appeared from Backlog.
     e) Return — there is no second pass, so PA-3 never fires. The single-pass approach is explicitly chosen because manifest-limited apply is a deliberately bounded operation (process exactly the previewed set), not a convergence-driven sweep.
   - Add a doc comment to the manifest branch explaining why it diverges from the re-derivation loop (H7 resolution).
6. Implement the full-backlog preview branch (REVIEWS.md H2): when `opts.DryRun` is true (and `opts.Manifest` is nil — DryRun does not combine with manual manifest):
   - Perform one exact Count.
   - Page through the ENTIRE backlog (like the manifest path), computing would-migrate projections without calling SetPayload.
   - Each point's previewed eligibility is projected: chain built, CheckAdditive checked, mint added to seen set.
   - Build the preview manifest: `previewManifest[id] = fromVersion` for every would-migrate point.
   - After the scroll, Count again (re-derive — same Count the re-derivation loop would use as the next pass's backlog).
   - Set `res.Backlog` to this fresh count.
   - Return the preview manifest from `Store.Migrate` by adding a `PreviewManifest map[string]migrate.Version` field to `MigrateResult`. This is what the CLI/apply closure captures for the manifest-bridged intersection.
   - Do NOT loop again — PA-3 would fire because nothing was written. This is the single-pass full-backlog projection.
7. Expand `MigrateResult`:
   - Add `PreviewManifest map[string]migrate.Version` (set by DryRun, consumed by Manifest-limited apply).
   - Add `Spared uint64` (records in manifest but fresh re-derivation shows at-target).
   - Add `Appeared uint64` (records NOT in manifest but fresh re-derivation shows below-target — visible only in manifest-limited mode).
   - Update the doc comment: `Migrated`/`Failed` are telemetry, `Backlog` is truth, `Spared`/`Appeared` are manifest-parity telemetry. In DryRun mode, `PreviewManifest` is the preview identity set.
8. Telemetry/span already wraps `Store.Migrate` (tracer.Start + RecordStoreOp); leave it, just extend the span attributes to include `Spared`/`Appeared` counts when populated.

In `internal/store/migrate_test.go` (may add one or more new test functions; reuse existing fixture-driver idioms):
9. Bare-record end-to-end: seed a legacy record (payload with no `short_id` key and no `schema_version` key) against real pinned Qdrant; run `s.Migrate(ctx, MigrateOptions{})` (defaults → target CurrentVersion=1, steps Registry). Assert `res.Backlog == 0`, `res.Migrated >= 1`, the stored record now carries a 10-char Crockford `short_id` and `schema_version==1`, and added-keys concern holds (only `short_id` + `schema_version` were added, no other key removed). Run `s.Migrate` again and assert `Backlog == 0` and no records re-processed.
10. Mixed-state end-to-end (REVIEWS.md H1/M2 — the critical production state): seed a record WITH `short_id` (say the literal `"abc123"` a prior `BackfillShortIDs` run wrote) and WITHOUT `schema_version`; run `s.Migrate(ctx, MigrateOptions{})`; assert `res.Backlog == 0`, the record's `short_id` is UNCHANGED (`"abc123"`, never re-minted), `schema_version == 1`, and no other key was added/removed. This is the test that makes 04-04's deletion of `Store.BackfillShortIDs` safe.
11. Full-backlog DryRun-no-write (REVIEWS.md H2): seed a mixed pre-v1 collection of size > `migrateBatch` (e.g. 300 records) and run `s.Migrate(ctx, MigrateOptions{DryRun: true, Batch: 64})`; assert `res.Migrated == 0` (DryRun does not write, so Migrated counts zero — it was a preview projection with previewManifest), a fresh read/probe shows NO point gained a `short_id` or `schema_version` (no SetPayload landed), and the returned `PreviewManifest` has exactly the total would-migrate count. Then run `DryRun:false` and assert `Backlog == 0` and `Migrated` equals the manifest count (actually equals the preview wast project — the "would-migrate" from the manifest).
12. Manifest intersection parity test (REVIEWS.md H6 / SC3): first run DryRun=true to capture the preview manifest. Then simulated-drift: insert a NEW below-target record AFTER the manifest was captured (so it is not in the manifest, i.e. "appeared"), stamp one manifest-record to current (so it is "spared"), then run apply with `Manifest: previewManifest`. Assert: the appeared record is reported `Appeared` and NOT migrated (no schema_version change); the spared record is reported `Spared` and NOT migrated; only the manifest∩fresh-intersection records are migrated; `res.Backlog` is the true fresh Count (which includes the Appeared record).
13. Manifest-limited apply Backlog correctness (REVIEWS.md H7 — the core fix): Run the manifest-limited apply from test 12 again and assert `res.Backlog > 0` and that `res.Backlog == res.Appeared` (the only remaining below-target records are the Appeared ones). Then run without Manifest (full sweep) and assert `Backlog == 0` — proving the Appeared records can be cleaned up by a subsequent full sweep. This test proves the PA-3 guard is never reached (single-pass) and that Backlog truthfully includes Appeared records.
  </action>
  <acceptance_criteria>
    - `go test ./internal/store/ -run 'TestMigrateV0ToV1MintEndToEnd|TestMigrateExistingShortIDPreserves|TestMigrateDryRunWritesNothing|TestMigrateFullBacklogProjection|TestMigrateManifestIntersection|TestMigrateManifestBacklogAppeared|TestMigrateConvergesWithoutLock|TestMigratePartialFailureResume' -count=1 -v` passes against real pinned Qdrant.
    - A grep of `internal/store/migrate.go` shows `ApplyMinter` branching, DryRun/manifest branches, `MigrateOptions.{DryRun,Manifest}`, `MigrateResult.{PreviewManifest,Spared,Appeared}`.
    - The manifest-limited apply path is a single pass (no re-derivation `for {}` loop), with a comment citing H7 resolution.
    - `res.Backlog` after a manifest-limited apply with Appeared records is the true fresh Count (>0), never 0 (H7 fixed — Backlog truthfully includes Appeared).
    - Pre-existing store sweep tests still pass: `go test ./internal/store/ -run 'TestMigrateConvergesWithoutLock|TestMigratePartialFailureResume'` — the two-clone / int(target) / per-point SetPayload discipline is unbroken.
    - PA-3 guard is never reached in manifest mode (single pass — no second pass to compare).
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/store/ -run 'TestMigrateV0ToV1MintEndToEnd|TestMigrateExistingShortIDPreserves|TestMigrateDryRunWritesNothing|TestMigrateFullBacklogProjection|TestMigrateManifestIntersection|TestMigrateManifestBacklogAppeared|TestMigrateConvergesWithoutLock|TestMigratePartialFailureResume' -count=1 -v</automated>
  </verify>
  <done>The minter-aware branch, the full-backlog DryRun preview, and the manifest-bridged single-pass apply all work inside Store.Migrate: a bare legacy record migrates to v1 end-to-end (minted short_id, stamped schema_version, second run no-op), a pre-existing-short_id no-schema record is preserved verbatim and stamped, preview projects the whole backlog without writing, apply migrates only the manifest∩fresh-re-derivation intersection with Spared/Appeared reporting (REQ-migrate-preview-apply-parity / SC3), and Backlog truthfully includes Appeared records without PA-3 firing (H7 resolved via single-pass manifest path).</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI → store | The migration sweep mutates the collection via per-point SetPayload; the minter side-effect mints short_ids that agents may later cite. The preview/apply manifest is the interface between a dry-run and a live migration. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | `Store.Migrate` minter branch | high | mitigate | The only write shape stays per-point SetPayload of added keys + the int(target) version stamp; the minter-aware path runs inside the SAME two-clone + CheckAdditive discipline, so a mint-side effect can add keys but can never remove/overwrite one (D-03/PA-2). |
| T-04-01b | Tampering | `Store.Migrate` manifest/apply intersection | high | mitigate | Apply migrates only the manifest ∩ fresh re-derivation (SC3/H6); a point absent from the preview manifest is NEVER eligible for apply (Appeared); the single-pass manifest path avoids PA-3 firing on Appeared records (H7). |
| T-04-02 | Tampering | `NewMintingStep` construction + CheckAdditive carve-out | medium | mitigate | Constructor-enforced single apply path + nil panics mirror NewStep (D-02); the CheckAdditive pre-existing-key carve-out is task-limited and still catches undeclared keys, removed keys, and declared-never-added-and-never-present keys (REVIEWS.md H1). |
| T-04-03 | Tampering | `Store.Migrate` DryRun full-backlog preview | medium | mitigate | DryRun never reaches `s.client.SetPayload` (single full-backlog page-through, returns before write); REQ-migrate-command SC1's "previews only, no writes" is honored by construction, backstopped by a DryRun-writes-nothing test; existing short_ids are never re-minted (D-03). |

</threat_model>

<verification>

After plan execution (wave 1):

- `go test ./internal/migrate/... ./internal/store/ -run 'Migrate|CurrentVersion|V1Fill|NewMintingStep|CheckAdditive|Manifest|ExistingShortID|FullBacklog|Appeared' -count=1` green.
- `task` green (lint + full test).
- The full store sweep suite (`-run 'TestMigrate'`) still green, proving the minter branch, DryRun, and manifest handling did not regress Phase 3's partial-failure-resume / converges-without-lock behavior.
- REVIEWS.md H1, H2, H6 (store half), H7, M1, M2, M13 closed.

</verification>

<success_criteria>

- A bare legacy record migrates to v1 end-to-end through the registered v0→v1 step against real Qdrant.
- A pre-existing-short_id no-schema record is preserved verbatim and stamped (H1 closed — this is the state the old standalone backfill produces).
- `Store.Migrate` supports a non-writing full-backlog DryRun preview via the established operator-tier idiom (H2 closed).
- Preview/apply parity is proven by manifest ∩ fresh re-derivation identity-set (SC3 / H6 closed).
- Manifest-limited apply uses a single-pass full-backlog scroll (no re-derivation loop), so PA-3 never fires on Appeared records (H7 closed — Backlog truthfully includes Appeared records).
- `migrate.CurrentVersion` is 1, registered in the same change as the step; the pin test is updated.
- The minter-aware step preserves additive-only + reversibility invariants.
- additive_test.go's Registry assertion updated for the now-non-empty Registry (M13 closed).

</success_criteria>

## Artifacts this phase produces

Internal (new/modified symbols):

- `internal/migrate`: `type ApplyMinterFunc`; `func NewMintingStep(...) Step`; method `func (s Step) ApplyMinter() (ApplyMinterFunc, bool)`; method `func (s Step) ApplyMinterMint(...)`; `Step` field `applyMinter ApplyMinterFunc`; **modified** `CheckAdditive` pre-existing-key carve-out; the registered v0→v1 step in `registry.go`; new file `v1_step.go` with `func v1FillShortID(...)`; `CurrentVersion` constant raised 0→1.
- `internal/store`: `MigrateOptions.{DryRun bool, Manifest map[string]migrate.Version}`; `MigrateResult.{PreviewManifest, Spared, Appeared}`; minter-aware branch; full-backlog single-pass DryRun preview; single-pass manifest-limited apply path (no re-derivation loop, PA-3-safe — H7).

New tests: `TestCurrentVersionValue` (updated 0→1), `TestNewMintingStep`, `TestV1FillShortID`, `TestCheckAdditivePreExistingKey`, `TestAdditiveOnlyKeySetDiff` (updated Registry assertion — M13), `TestMigrateV0ToV1MintEndToEnd`, `TestMigrateExistingShortIDPreserves` (H1), `TestMigrateDryRunWritesNothing`, `TestMigrateFullBacklogProjection` (H2), `TestMigrateManifestIntersection` (H6/SC3), `TestMigrateManifestBacklogAppeared` (H7).

<output>
Create `.planning/phases/04-migration-cli-first-customer/04-01-SUMMARY.md` when done
</output>