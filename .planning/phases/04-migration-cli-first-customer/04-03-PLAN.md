---
phase: 04-migration-cli-first-customer
plan: 03
type: execute
wave: 3
depends_on:
  - 04-migration-cli-first-customer-01
  - 04-migration-cli-first-customer-02
files_modified:
  - cmd/engram/destructive.go
  - cmd/engram/destructive_test.go
  - cmd/engram/migrate_family.go
  - cmd/engram/migrate_family_test.go
  - internal/surfaces/toolclass.go
  - cmd/engram/catalog_test.go
autonomous: true
requirements:
  - REQ-migrate-command
  - REQ-migrate-status-histogram
  - REQ-migrate-preview-apply-parity
  - REQ-migrate-revert
estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "registerDestructive's gate is generalized from `Destructive` to `!ReadOnly`, so an additive-but-mutating command (migrate, Destructive:false) and a destructive one (migrate revert, Destructive:true) share the ONE preview/apply routing mechanism; the panic message/doc are updated accordingly (RESEARCH High-risk finding; D-16). The function name stays `registerDestructive` — the terminology debt of 'destructive' for 'any mutating operation' is accepted and documented (REVIEWS.md M9)."
    - "A bare `engram migrate` invocation previews only (DryRun via PreviewMigrate, no writes); `--apply` is the only write choke point, via registerDestructive (REQ-migrate-command SC1)."
    - "`engram migrate --output json|text` renders through addOperatorOutputFlag/renderOperator() one document, mirroring the rest of the operator tier (migrateRemapReportDoc/purgeReportDoc explicit-field precedent)."
    - "`engram migrate status` renders the server-side version-distribution histogram (Store.MigrateStatus), not a scalar, and is a ReadOnly:true command that does NOT route through registerDestructive (D-07/D-08)."
    - "`engram migrate --apply` acts only on the intersection of the previewed set and a fresh re-derivation (REQ-migrate-preview-apply-parity): preview derives a manifest of eligible point IDs via Store's PreviewMigrate/ DryRun; apply reads the preview manifest and passes it into a fresh Migrate call as `MigrateOptions.Manifest`, so only the intersection is migrated. This is the spine-review purge intersection pattern (REVIEWS.md H6/SC3) — NOT count-comparison equality."
    - "`engram migrate revert --to <v>` previews by default (surfacing the reverse plan via the shared store-level `reversePreflight` helper, NEVER duplicating preflight logic in the CLI — REVIEWS.md M8) and `--apply` executes only a fully-reversible range; an irreversible range refuses the entire operation naming every step From/To, each declared reason, and snapshot recovery in the field=<name> hint=<code> envelope (D-13/D-14/D-16)."
    - "`--to <v>` is validated via usageErrorf (a non-integer, negative, or out-of-chain version is a usage error), never a bare unregistered panic."
    - "CLI tests use a narrow local store interface (migrateFamilyStore) with an injectable constructor (migrateFamilyStoreFromEnv), mirroring the spineConsolidateStoreFromEnv pattern — enabling fake-store substitution in CLI tests without touching production routing (REVIEWS.md M7)."
    - statement: "Toolclass rows `migrate` (ReadOnly:false, Destructive:false), `migrate status` (ReadOnly:true, Destructive:false), `migrate revert` (ReadOnly:false, Destructive:true) exist and the catalog↔toolclass both-directions gate stays green."
      verification: backstop
  prohibitions:
    - "No new injectable classification seam — registerDestructive's classification stays DERIVED from internal/surfaces/toolclass.go (the rejected-alternative block, destructive.go) and no command MAY add its own --apply/--output flag outside operator_output.go/registerDestructive."
    - "A `migrate`/`backfill` command MUST NOT be routed in a way that lets preview fall through to apply, or compose the two branches (registerDestructive calls exactly one)."
    - "A ReadOnly command (migrate status) MUST NOT be routed through registerDestructive, and a mutating one (migrate / migrate revert) MUST NOT bypass it."
    - "`migrate revert` MUST NOT partially revert or auto-unwind on a failed --apply (the sweep is resumable; the recovery path is a snapshot, per the milestone Out-of-Scope)."
    - "CLI preview/apply parity MUST NOT be proven by count-equality alone — it requires manifest identity set (the preview manifest ∩ fresh re-derivation), per REVIEWS.md H6/SC3."
    - "Revert preflight logic MUST NOT be duplicated between CLI and store — one shared helper, `reversePreflight`, lives at the store layer (REVIEWS.md M8)."
    - "Nested subcommand cobra `Use` strings MUST be leaf-only: `Use: \"status\"` not `Use: \"migrate status\"` — the qualified path comes from parentage (REVIEWS.md M6, precedent spine_review_purge.go:390)."
---

<objective>
Build the `engram migrate` command family — `migrate` (forward sweep preview/apply with manifest-based intersection), `migrate status` (read-only histogram), `migrate revert --to <v>` (reverse inverses with shared store-level preflight) — as subcommands routed through the standard destructive-tier `registerDestructive`, generalize that mechanism's gate so the additive `migrate` sweep can use it (the phase's single required non-consumption code change), add the three toolclass blast-radius rows, and prove preview/apply parity at the CLI boundary via manifest identity set (REVIEWS.md H6/SC3).

The plan incorporates the review corrections: leaf-only cobra Use strings (M6), local store interface seam for test injection (M7), shared preflight helper with no CLI duplication (M8), and the accepted registerDestructive terminology debt (M9).

Purpose: This is the operator-facing surface for all six Phase-4 requirements except the backfill alias (04-04) and the never-automatic startup cap (04-02). It relies on the store methods from 04-01 (Store.Migrate + DryRun + manifest + minter) and 04-02 (Store.MigrateStatus, Store.Revert with reversePreflight).

Output: a new `cmd/engram/migrate_family.go`, the generalized `registerDestructive` gate with M9 documentation, three toolclass rows, the shared migrate report envelope, a local store interface seam (M7), and the CLI + catalog conformance tests with manifest-based parity (H6).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/04-migration-cli-first-customer/04-CONTEXT.md
@.planning/phases/04-migration-cli-first-customer/04-RESEARCH.md
@.planning/phases/04-migration-cli-first-customer/04-PATTERNS.md
@.planning/phases/04-migration-cli-first-customer/04-REVIEWS.md

@cmd/engram/destructive.go
@cmd/engram/spine_review_purge.go (the preview/apply closure + nested-subcommand precedent SC3 names; manifest bridge at 339-377)
@cmd/engram/migrate.go (migrateRemapReportDoc envelope + Deprecated precedent)
@cmd/engram/operator_output.go (addOperatorOutputFlag/renderOperator)
@internal/surfaces/toolclass.go (backfill-short-ids Destructive:false row; migrate-remap-owner Destructive:true row)
@cmd/engram/catalog_test.go (TestCatalogBlastRadiusMatchesToolClasses both-directions gate :429-469)
@internal/store/migrate.go (MigrateOptions.Manifest + MigrateResult.Spared/Appeared from 04-01)
@internal/store/revert.go (reversePreflight helper from 04-02 — shared preflight, never duplicated in CLI)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generalize registerDestructive gate to admit mutating non-destructive commands + accept M9 terminology debt</name>
  <files>
    cmd/engram/destructive.go
    cmd/engram/destructive_test.go
  </files>
  <read_first>
    cmd/engram/destructive.go (destructiveByClassification :37-48 returns class.Destructive; registerDestructive panic :116-122; the rejected injectable-seam block :89-102 — a hard prohibition)
    internal/surfaces/toolclass.go Class semantics (:16-24: "ReadOnly is true when the operation never modifies environment; Destructive is true only if the operation may remove or overwrite")
    cmd/engram/destructive_test.go (TestDestructiveGatePreventsMutation — must stay green)
    .planning/phases/04-migration-cli-first-customer/04-RESEARCH.md (Pattern 1 + Pitfall 1 — gate must change to `!class.ReadOnly`)
    .planning/phases/04-migration-cli-first-customer/04-REVIEWS.md (M9 — registerDestructive name becomes misleading; accept terminology debt)
  </read_first>
  <action>
In `cmd/engram/destructive.go`:
1. Change the predicate returned by `destructiveByClassification` from `return class.Destructive` to `return !class.ReadOnly`. Do NOT add an injectable classification seam (the rejected-alternative block is a hard prohibition). The panic-on-no-row behavior, `commandKey` lookup, and the `ClassForCommand` call stay identical.
2. Update the comment/doc on `destructiveByClassification` and the `registerDestructive` panic message (currently "command %q is not classified destructive ... only a command whose blast-radius row has Destructive: true may route through this gate") to state the mechanism now serves ANY mutating operator command (ReadOnly:false) — additive mutations (migrate / backfill) included — not only destructive ones. Keep the key claim that routing a ReadOnly:true command is a programming error (that is now the enforcement), and that a command with no classification row still panics.
3. Accept the `registerDestructive` terminology debt (REVIEWS.md M9): Add a doc comment on the `destructiveByClassification` function or the `registerDestructive` function explaining that the name predates the gate's generalization and is preserved for backward compatibility with caller sites — "destructive" now means "any mutating operation" (ReadOnly:false), not exclusively key-removing operations. This is an accepted name debt, not scheduled for rename in this phase.
4. Do NOT touch `addApplyFlag`, `applyRequested`, or the RunE dispatch — they are already classification-agnostic.

In `cmd/engram/destructive_test.go`:
5. Extend the destructive-tier tests so `TestDestructiveGatePreventsMutation` (and any companion) still holds, AND add/confirm the generalized admission: a command classified `ReadOnly:false, Destructive:false` (additive) is admitted by `registerDestructive` and routes preview→preview / --apply→apply; a `ReadOnly:true` command is refused (panics). Mirror how the existing test registers a synthetic command so no production-row churn is required; use t.Cleanup to restore any mutated test state.
  </action>
  <acceptance_criteria>
    - `go test ./cmd/engram/ -run TestDestructive -count=1 -v` passes (including TestDestructiveGatePreventsMutation).
    - `rg -n "return !class.ReadOnly" cmd/engram/destructive.go` finds the new predicate.
    - `rg -n "injectable|seam" cmd/engram/destructive.go` returns the rejected-alternative doc unchanged (no seam added).
    - A comment near the new predicate or on destructiveByClassification acknowledges the terminology debt (M9).
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd/engram/ -run TestDestructive -count=1 -v</automated>
  </verify>
  <done>registerDestructive admits any ReadOnly:false mutating operator command (additive or destructive) while still refusing ReadOnly commands and unclassified commands, with tests proving both admission and refusal, and the name debt is documented.</done>
</task>

<task type="auto">
  <name>Task 2: engram migrate (sweep preview/apply with manifest bridge) + engram migrate status + shared envelope + toolclass rows + store interface seam (M7)</name>
  <files>
    cmd/engram/migrate_family.go
    internal/surfaces/toolclass.go
    cmd/engram/catalog_test.go
    cmd/engram/migrate_family_test.go
  </files>
  <read_first>
    cmd/engram/spine_review_purge.go (the registerDestructive closure contract :314-378 — leaf never assigns RunE, supplies preview+apply closures, addOperatorOutputFlag, AddCommand BEFORE registerDestructive; options-from-flags :120-130; manifest bridge at :339-377 — this is the H6/SC3 pattern the migration manifest mirrors)
    cmd/engram/migrate.go (migrateRemapReportDoc explicit-bool envelope :216-221; Deprecated :261)
    cmd/engram/operator_output.go (addOperatorOutputFlag/renderOperator single-document)
    internal/surfaces/toolclass.go (backfill-short-ids row :194-200 Destructive:false Precedent; migrate-remap-owner Destructive:true)
    internal/store/migrate.go (MigrateOptions + MigrateResult doc: Migrated/Failed telemetry, Backlog truth, Manifest in opts, Spared/Appeared in result)
    cmd/engram/catalog_test.go (both-directions gate :429-469)
    .planning/phases/04-migration-cli-first-customer/04-CONTEXT.md (D-05 subcommand split, D-06 new-file-per-family, D-07 rows, D-08 status, D-11 shared envelope)
    .planning/phases/04-migration-cli-first-customer/04-REVIEWS.md (H6 — manifest-based parity not count-comparison; M6 — leaf-only Use strings; M7 — store interface seam for test injection)
  </read_first>
  <action>
In a NEW file `cmd/engram/migrate_family.go` (any filename per D-06 — separate from the transient owner-migration commands in `cmd/engram/migrate.go`):

0. Define a narrow local store interface (REVIEWS.md M7 — mirror spineConsolidateStoreFromEnv pattern):
   ```go
   type migrateFamilyStore interface {
       Migrate(ctx context.Context, opts store.MigrateOptions) (store.MigrateResult, error)
       MigrateStatus(ctx context.Context) (store.MigrateStatusResult, error)
       Revert(ctx context.Context, to migrate.Version) (store.RevertResult, error)
   }
   var migrateFamilyStoreFromEnv = func() (migrateFamilyStore, error) {
       return server.StoreFromEnv()
   }
   ```
   This injectable constructor enables CLI tests to substitute a fake store without touching production routing. The `MUST` rule: production initialization uses the var verbatim (assigned in an `init()` or at the var's initializer); tests swap via `t.Cleanup(migrateFamilyStoreFromEnv = saved)`.

1. Define the shared migrate report envelope (D-11), the explicit-bool separate-field shape mirroring `migrateRemapReportDoc`/`purgeReportDoc`. Map 1:1 from `store.MigrateResult`, adding the explicit dry-run flag and the migrating target/backlog. Suggested doc struct: `type migrateReportDoc struct { Target int, Migrated uint64, Failed uint64, Passes uint64, Backlog uint64, Spared uint64, Appeared uint64, DryRun bool }` with json tags mirroring the purge/remap precedent. Ferrer never prose-inferred.
2. Build the `migrate` parent command (`migrateCmd`): `Use: "migrate"` (leaf-only — M6: the qualified path comes from parentage), `Short` naming the sweep; flag set = only `--output` (addOperatorOutputFlag) plus nothing custom — no bespoke flags. Route it through `registerDestructive` (after `AddCommand`, per the contract) with two closures:
   - `migratePreview`: `operatorOutputFormat` → `migrateFamilyStoreFromEnv()` → `st.Migrate(ctx, store.MigrateOptions{DryRun: true})` → receives the DryRun result (including preview manifest built in 04-01) → `renderOperator(cmd, format, migrateSummary, migrateReportDoc)`.
   - `migrateApplyRun`: identical but `DryRun: false` AND passes the preview manifest (stored in a package-level var captured from the preview run, exactly as `spine_review_purge.go:339-377` stores the manifest: a bare package-level var `migrateLastPreviewManifest` that the preview closure sets and the apply closure reads). The apply closure calls `st.Migrate(ctx, store.MigrateOptions{Manifest: migrateLastPreviewManifest})` — this re-derives eligibility fresh and migrates only the manifest ∩ fresh-re-derivation intersection (REVIEWS.md H6/SC3). After apply, clear the manifest var. The apply renders `Spared` and `Appeared` counts from the result alongside `Migrated`.
   Both closures derive options ONLY from the defaults (Target=CurrentVersion, Steps=Registry); Phase 4 passes neither field and lets both defaults apply (per MigrateOptions doc) — do NOT add target/steps flags in this phase.
3. Build the `migrate status` command (`migrateStatusCmd`): `Use: "status"` (leaf-only — M6), `Short` naming the histogram; it is a READ-ONLY command (D-07) so it has an ordinary `RunE` (NOT routed through registerDestructive): `operatorOutputFormat` → `migrateFamilyStoreFromEnv()` → `st.MigrateStatus(ctx)` → `renderOperator(cmd, format, statusSummary, statusReportDoc)` where the report carries the buckets + absent + future_version + total (a distribution, never a scalar). `status` adds NO --apply; its flag set is only `--output`.
4. Register both under a parent (either `migrateCmd` containing `status`, or a shared parent command with `AddCommand`); match the `spine-review` nested-subcommand convention. Ensure each space-joined path (`migrate`, `migrate status`) has disjoint flags.

In `internal/surfaces/toolclass.go`:
5. Add rows, per D-07, one per space-joined path:
   - `migrate` = `{ReadOnly:false, Destructive:false, ...}` (additive-only ENFORCED by CheckAdditive; the same reasoning as the existing `backfill-short-ids` row).
   - `migrate status` = `{ReadOnly:true, Destructive:false, ...}`.
   Keep the existing `backfill-short-ids` row (D-07/RESEARCH Open Q3 — keep it so the deprecation-catalog surface stays both-directions green).

In `cmd/engram/catalog_test.go`:
6. Update `TestCatalogBlastRadiusMatchesToolClasses` (both-directions gate) to expect the two new rows AND the two new commands. Must land in the SAME change so the gate never observes a row without its command (or vice-versa). The `migrate` row routes through registerDestructive (which needs its row present at init), so add the row and the command together.

In `cmd/engram/migrate_family_test.go`:
7. CLI tests (cobra, inject a fake store via the `migrateFamilyStoreFromEnv` seam — M7): bare `migrate` renders a preview document and performs NO writes (fake store's Migrate gets DryRun:true); `migrate --apply` renders applied with Migrated/Spared/Appeared counts and the fake store's Migrate gets DryRun:false with the manifest from the preview closure; `migrate status` renders a distribution; both render json|text via renderOperator.
8. Preview/apply parity at the CLI boundary (REVIEWS.md H6/SC3): with a fake store whose PreviewMigrate returns a deterministic set, assert `--apply` acts only on the intersection of the previewed set and the fresh re-derivation. Test includes: a record that was previewed but is no longer eligible (Spared), a record that became eligible after preview (Appeared — NOT migrated), and a record that was previewed and is still eligible (Migrated). This proves identity-set intersection, NOT count-comparison.
  </action>
  <acceptance_criteria>
    - `go test ./cmd/engram/ -run 'TestMigrate|TestCatalogBlastRadiusMatchesToolClasses' -count=1 -v` passes.
    - `rg -n '"migrate"|"migrate status"' internal/surfaces/toolclass.go` finds the two rows, and `rg -n 'CLICommand: "migrate"|CLICommand: "migrate status"'` confirms the space-joined keys.
    - `rg -n "Use: .status.|Use: .migrate." cmd/engram/migrate_family.go` shows leaf-only Use strings (M6): `"migrate"` for the parent, `"status"` for the child — NOT `"migrate status"`.
    - `rg -n "migrateFamilyStoreFromEnv" cmd/engram/migrate_family.go` shows the injectable constructor (M7).
    - The preview/apply parity test proves identity-set intersection via Spared/Appeared counts, not count-equality (H6/SC3).
    - A grep of `cmd/engram/migrate_family.go` shows `registerDestructive(migrateCmd, ...)` and a plain `RunE` for `migrateStatusCmd` (read-only, not registerDestructive).
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd/engram/ -run 'TestMigrate|TestCatalogBlastRadiusMatchesToolClasses' -count=1 -v</automated>
  </verify>
  <done>engram migrate previews (no writes) and --apply executes the sweep with manifest∩re-derivation intersection (SC3/H6); engram migrate status renders the histogram read-only; both emit the shared envelope; the destruct-tier gate + catalog gate are green; store interface seam (M7) and leaf-only Use strings (M6) in place.</done>
</task>

<task type="auto">
  <name>Task 3: engram migrate revert --to <v> with shared reversePreflight (M8) + toolclass row</name>
  <files>
    cmd/engram/migrate_family.go
    internal/surfaces/toolclass.go
    cmd/engram/migrate_family_test.go
    cmd/engram/catalog_test.go
  </files>
  <read_first>
    cmd/engram/spine_review_purge.go (registerDestructive closure contract; usageErrorf validation :65)
    cmd/engram/operror.go (usageErrorf → exit 2)
    internal/store/revert.go (the Store.Revert the CLI attaches to; the `reversePreflight` helper from 04-02 — this is the SHARED helper the CLI uses WITHOUT duplication — M8)
    internal/surfaces/toolclass.go (migrate-remap-owner Destructive:true row precedent)
    cmd/engram/catalog_test.go (the both-directions gate must learn the migrate revert row + command here, same as Task 2 did for migrate/migrate status)
    .planning/phases/04-migration-cli-first-customer/04-CONTEXT.md (D-13 whole-range preflight, D-14 refusal envelope, D-16 revert preview/apply parity via registerDestructive)
    .planning/phases/04-migration-cli-first-customer/04-REVIEWS.md (M6 — leaf-only Use strings for revert; M8 — shared reversePreflight helper, preflight NOT duplicated in CLI)
  </read_first>
  <action>
In `cmd/engram/migrate_family.go`:
1. Add the `migrate revert` command (`migrateRevertCmd`): `Use: "revert --to <v>"` (leaf-only — M6; the qualified path `migrate revert` comes from parentage), `Short` naming the reverse version; flags = `--output` + `--to` (an int). Validate `--to` via `usageErrorf` (non-integer, negative, or inconsistent-with-chain → exit 2 usage error, D-16/precedent spine_review_purge.go:65); a bare `revert` without `--to` is a usage error.
2. Route it through `registerDestructive` (after AddCommand) with:
   - `revertPreview`: `operatorOutputFormat` → `migrateFamilyStoreFromEnv()` → call the store's shared `reversePreflight` helper (04-02 Task 2) on the registry to test if the range is revertable. The CLI does NOT duplicate preflight logic — it calls the same helper `Store.Revert` uses internally (M8). When the range is irreversible, render the refusal error (the `field=steps hint=irreversible` envelope naming every step + reason + snapshot — D-14). When the range is fully reversible, preview estimates what would be reverted (using `Store.MigrateStatus` to count records in the affected versions) and renders a reverse-plan summary. Preview is read-only — no inverses are applied.
   - `revertApplyRun`: call the same shared `reversePreflight` helper again (it is idempotent and fast). Only when fully reversible, call `st.Revert(ctx, to)` and render the result. An irreversible range at --apply time renders the same refusal error with zero records touched.
   So both preview and apply gate on the SAME shared preflight helper (M8) — the CLI never lets --apply execute an irreversible range (REQ-migrate-revert preview/apply parity).
3. Add the `migrate revert` toolclass row `{ReadOnly:false, Destructive:true, ...}` (D-07; Destructive — inverses may remove keys, the anti-additive direction).

In `cmd/engram/catalog_test.go`:
3a. Extend `TestCatalogBlastRadiusMatchesToolClasses` (both-directions gate) to also expect the `migrate revert` row AND the `migrate revert` command, mirroring exactly what Task 2 did for `migrate`/`migrate status`. Must land in the SAME change as the row + command so the gate never observes a row without its command (or vice-versa).

In `cmd/engram/migrate_family_test.go`:
4. CLI tests for revert (using the `migrateFamilyStoreFromEnv` fake seam — M7): an irreversible range (the only registered v0→v1 step makes `--to 0` one) yields the envelope NAMING the step (From/To), its declared reason, and `snapshot`, and proves zero records touched (fake store asserts `Revert` was never called). A reversible fixture range previews the reverse plan and `--apply` executes it, reporting the converged result. `--to` invalid values return a usage error (exit 2). `revert` without `--to` returns a usage error. Assert that the `reversePreflight` call originates from the store helper, NOT from duplicated CLI logic — use an interface that exposes the preflight or a tracking fake.
  </action>
  <acceptance_criteria>
    - `go test ./cmd/engram/ -run 'TestMigrateRevert|TestMigrate|TestCatalogBlastRadiusMatchesToolClasses' -count=1 -v` passes.
    - `rg -n 'CLICommand: "migrate revert"' internal/surfaces/toolclass.go` finds the row; its `Class.Destructive` is `true`.
    - `rg -n "Use: .revert" cmd/engram/migrate_family.go` shows leaf-only Use string: `Use: "revert --to <v>"` (M6).
    - `rg -n "usageErrorf" cmd/engram/migrate_family.go` shows `--to` validation.
    - The revert preflight in the CLI is documented as calling the STORE-LEVEL shared helper `reversePreflight`, not implementing its own preflight (M8 closed).
    - The catalog both-directions gate stays green with all three rows + all three commands.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd/engram/ -run 'TestMigrateRevert|TestMigrate|TestCatalogBlastRadiusMatchesToolClasses' -count=1 -v</automated>
  </verify>
  <done>engram migrate revert previews the reverse plan by default, refuses any irreversible range before a write (naming every step + reason + snapshot), applies only a fully-reversible range via registerDestructive, uses the shared store-level reversePreflight helper (no CLI duplication — M8), and uses leaf-only Use strings (M6).</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI → store (migrate/revert) | The CLI is the write choke point; unsafe/partial revert and preview-writes are the threats. The manifest bridge carries preview identity into apply. |
| classification table → routing | toolclass rows decide which command may mutate; a wrong row or a bypassed gate is a reclassification bypass. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-05 | Tampering | `registerDestructive` gate | high | mitigate | Predicate becomes `!class.ReadOnly`; classification stays DERIVED from toolclass.go (no injectable seam — rejected-alternative block enforced); ReadOnly commands are refused; TestDestructiveGatePreventsMutation stays green. Name debt documented (M9). |
| T-04-06 | Tampering | `migrate` preview writes | high | mitigate | Preview closure passes `MigrateOptions{DryRun: true}` (full-backlog page-through, never SetPayload); only the `--apply` closure uses DryRun:false with the preview manifest; registerDestructive calls exactly one branch (preview→preview, apply→apply), never composed. |
| T-04-06b | Tampering | `migrate` manifest bridge | high | mitigate | Apply passes `MigrateOptions.Manifest` captured from the preview closure — a bare package-level var exactly mirroring spine_review_purge.go's pattern; the manifest is the ONLY vector linking preview identity to apply eligibility. The apply closure clears the manifest after use. |
| T-04-07 | Tampering | `migrate revert` partial/unwanted revert | critical | mitigate | Same shared `reversePreflight` helper runs in BOTH preview and apply before any write; an irreversible range refuses the entire op (field=steps hint=irreversible, naming each step + reason + snapshot); preflight logic is never duplicated in the CLI (M8). |
| T-04-07b | Tampering | CLI store interface seam | low | mitigate | The `migrateFamilyStoreFromEnv` injectable constructor is test-only (M7); production initialization is at var level, no runtime override path exists outside tests. |

</threat_model>

<verification>

After plan execution (wave 3):

- `go test ./cmd/engram/ -run 'TestMigrate|TestDestructive|TestCatalogBlastRadiusMatchesToolClasses' -count=1` green.
- `go vet ./cmd/engram/` clean; `task` green.
- Real-binary smoke check: `go run ./cmd/engram migrate` against a test Qdrant renders a preview document with no writes; `go run ./cmd/engram migrate revert --to 0` renders the irreversible refusal naming the v0→v1 step + snapshot.
- REVIEWS.md H6 (CLI half), M6, M7, M8, M9 closed.

</verification>

<success_criteria>

- `engram migrate` previews only, `--apply` is the write choke point using manifest ∩ fresh re-derivation (REQ-migrate-command, REQ-migrate-preview-apply-parity SC3, H6).
- `engram migrate status` renders a server-side distribution with future-version handling (REQ-migrate-status-histogram).
- `engram migrate revert --to` previews, refuses irreversible ranges whole, applies only reversible ones, using the shared store-level preflight helper (REQ-migrate-revert, M8).
- Leaf-only cobra `Use` strings (M6), injectable store interface seam (M7), and documented name debt (M9) in place.

</success_criteria>

## Artifacts this phase produces

- `cmd/engram/destructive.go`: `destructiveByClassification` now returns `!class.ReadOnly`; updated panic message + doc; M9 debt comment.
- `cmd/engram/migrate_family.go` (new): `migrateFamilyStore` interface + `migrateFamilyStoreFromEnv` injectable var (M7); `migrateCmd`, `migrateStatusCmd`, `migrateRevertCmd` with leaf-only Use strings (M6); `migratePreview`/`migrateApplyRun` closures (manifest bridge); `migrateStatus` run; `revertPreview`/`revertApplyRun` closures (shared reversePreflight); package-level `migrateLastPreviewManifest` var; shared `migrateReportDoc` + `statusReportDoc` + summary funcs.
- `internal/surfaces/toolclass.go`: three rows added — `migrate` (R:false D:false), `migrate status` (R:true D:false), `migrate revert` (R:false D:true).
- Tests: extended `TestDestructive*`; new migrate-family CLI tests with identity-set parity (H6); updated `TestCatalogBlastRadiusMatchesToolClasses`.

<output>
Create `.planning/phases/04-migration-cli-first-customer/04-03-SUMMARY.md` when done
</output>