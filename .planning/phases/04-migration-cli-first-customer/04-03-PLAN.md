---
phase: 04-migration-cli-first-customer
plan: 03
type: execute
wave: 3
depends_on:
  - 04-migration-cli-first-customer-01
  - 04-migration-cli-first-customer-02
files_modified:
  - cmd/engram/destructive.go
  - cmd/engram/destructive_test.go
  - cmd/engram/migrate_family.go
  - cmd/engram/migrate_family_test.go
  - internal/surfaces/toolclass.go
  - cmd/engram/catalog_test.go
autonomous: true
requirements:
  - REQ-migrate-command
  - REQ-migrate-status-histogram
  - REQ-migrate-preview-apply-parity
  - REQ-migrate-revert
estimate:
  tokens: 56000
  raw_tokens: 28000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "registerDestructive's gate is generalized from `Destructive` to `!ReadOnly`, so an additive-but-mutating command (migrate, Destructive:false) and a destructive one (migrate revert, Destructive:true) share the ONE preview/apply routing mechanism; the panic message/doc are updated accordingly (RESEARCH High-risk finding; D-16)."
    - "A bare `engram migrate` invocation previews only (DryRun, no writes); `--apply` is the only write choke point, via registerDestructive (REQ-migrate-command SC1)."
    - "`engram migrate --output json|text` renders through addOperatorOutputFlag/renderOperator() one document, mirroring the rest of the operator tier (migrateRemapReportDoc/purgeReportDoc explicit-field precedent)."
    - "`engram migrate status` renders the server-side version-distribution histogram (Store.MigrateStatus), not a scalar, and is a ReadOnly:true command that does NOT route through registerDestructive (D-07/D-08)."
    - "On a stable collection, `migrate` with --apply migrates exactly the set a preview projected (both consume the same MigrateOptions defaults / Store.Migrate; DryRun projection == apply Migrated, apply ends at Backlog 0) — preview/apply parity proven by test (REQ-migrate-preview-apply-parity, spine-review purge intersection pattern)."
    - "`engram migrate revert --to <v>` previews by default (surfacing the reverse plan + the whole-range irreversible preflight result) and `--apply` executes only a fully-reversible range; an irreversible range refuses the entire operation naming every step From/To, each declared reason, and snapshot recovery in the field=<name> hint=<code> envelope (D-13/D-14/D-16)."
    - "`--to <v>` is validated via usageErrorf (a non-integer, negative, or out-of-chain version is a usage error), never a bare unregistered panic."
    - statement: "Toolclass rows `migrate` (ReadOnly:false, Destructive:false), `migrate status` (ReadOnly:true, Destructive:false), `migrate revert` (ReadOnly:false, Destructive:true) exist and the catalog↔toolclass both-directions gate stays green."
      verification: backstop
  prohibitions:
    - "No new injectable classification seam — registerDestructive's classification stays DERIVED from internal/surfaces/toolclass.go (the rejected-alternative block, destructive.go) and no command MAY add its own --apply/--output flag outside operator_output.go/registerDestructive."
    - "A `migrate`/`backfill` command MUST NOT be routed in a way that lets preview fall through to apply, or compose the two branches (registerDestructive calls exactly one)."
    - "A ReadOnly command (migrate status) MUST NOT be routed through registerDestructive, and a mutating one (migrate / migrate revert) MUST NOT bypass it."
    - "`migrate revert` MUST NOT partially revert or auto-unwind on a failed --apply (the sweep is resumable; the recovery path is a snapshot, per the milestone Out-of-Scope)."
---

<objective>
Build the `engram migrate` command family — `migrate` (forward sweep preview/apply), `migrate status` (read-only histogram), `migrate revert --to <v>` (reverse inverses) — as subcommands routed through the standard destructive-tier `registerDestructive`, generalize that mechanism's gate so the additive `migrate` sweep can use it (the phase's single required non-consumption code change), add the three toolclass blast-radius rows, and prove preview/apply parity at the CLI boundary.

Purpose: This is the operator-facing surface for all six Phase-4 requirements except the backfill alias (04-04) and the never-automatic startup cap (04-02). It relies on the store methods from 04-01 (Store.Migrate + DryRun + minter) and 04-02 (Store.MigrateStatus, Store.Revert).

Output: a new `cmd/engram/migrate_family.go`, the generalized `registerDestructive` gate, three toolclass rows, the shared migrate report envelope, and the CLI + catalog conformance tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/04-migration-cli-first-customer/04-CONTEXT.md
@.planning/phases/04-migration-cli-first-customer/04-RESEARCH.md
@.planning/phases/04-migration-cli-first-customer/04-PATTERNS.md

@cmd/engram/destructive.go
@cmd/engram/spine_review_purge.go (the preview/apply closure + nested-subcommand precedent SC3 names)
@cmd/engram/migrate.go (migrateRemapReportDoc envelope + Deprecated precedent)
@cmd/engram/operator_output.go (addOperatorOutputFlag/renderOperator)
@internal/surfaces/toolclass.go (backfill-short-ids Destructive:false row; migrate-remap-owner Destructive:true row)
@cmd/engram/catalog_test.go (TestCatalogBlastRadiusMatchesToolClasses both-directions gate :429-469)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generalize registerDestructive gate to admit mutating non-destructive commands</name>
  <files>
    cmd/engram/destructive.go
    cmd/engram/destructive_test.go
  </files>
  <read_first>
    cmd/engram/destructive.go (destructiveByClassification :37-48 returns class.Destructive; registerDestructive panic :116-122; the rejected injectable-seam block :89-102 — a hard prohibition)
    internal/surfaces/toolclass.go Class semantics (:16-24: "ReadOnly is true when the operation never modifies environment; Destructive is true only if the operation may remove or overwrite")
    cmd/engram/destructive_test.go (TestDestructiveGatePreventsMutation — must stay green)
    .planning/phases/04-migration-cli-first-customer/04-RESEARCH.md (Pattern 1 + Pitfall 1 — gate must change to `!class.ReadOnly`)
  </read_first>
  <action>
In `cmd/engram/destructive.go`:
1. Change the predicate returned by `destructiveByClassification` from `return class.Destructive` to `return !class.ReadOnly`. Do NOT add an injectable classification seam (the rejected-alternative block is a hard prohibition). The panic-on-no-row behavior, `commandKey` lookup, and the `ClassForCommand` call stay identical.
2. Update the comment/doc on `destructiveByClassification` and the `registerDestructive` panic message (currently "command %q is not classified destructive ... only a command whose blast-radius row has Destructive: true may route through this gate") to state the mechanism now serves ANY mutating operator command (ReadOnly:false) — additive mutations (migrate / backfill) included — not only destructive ones. Keep the key claim that routing a ReadOnly:true command is a programming error (that is now the enforcement), and that a command with no classification row still panics.
3. Do NOT touch `addApplyFlag`, `applyRequested`, or the RunE dispatch — they are already classification-agnostic.

In `cmd/engram/destructive_test.go`:
4. Extend the destructive-tier tests so `TestDestructiveGatePreventsMutation` (and any companion) still holds, AND add/confirm the generalized admission: a command classified `ReadOnly:false, Destructive:false` (additive) is admitted by `registerDestructive` and routes preview→preview / --apply→apply; a `ReadOnly:true` command is refused (panics). Mirror how the existing test registers a synthetic command so no production-row churn is required; use t.Cleanup to restore any mutated test state.
  </action>
  <acceptance_criteria>
    - `go test ./cmd/engram/ -run TestDestructive -count=1 -v` passes (including TestDestructiveGatePreventsMutation).
    - `rg -n "return !class.ReadOnly" cmd/engram/destructive.go` finds the new predicate.
    - `rg -n "injectable|seam" cmd/engram/destructive.go` returns the rejected-alternative doc unchanged (no seam added).
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd/engram/ -run TestDestructive -count=1 -v</automated>
  </verify>
  <done>registerDestructive admits any ReadOnly:false mutating operator command (additive or destructive) while still refusing ReadOnly commands and unclassified commands, with tests proving both admission and refusal.</done>
</task>

<task type="auto">
  <name>Task 2: engram migrate (sweep preview/apply) + engram migrate status (histogram) + shared envelope + toolclass rows</name>
  <files>
    cmd/engram/migrate_family.go
    internal/surfaces/toolclass.go
    cmd/engram/catalog_test.go
    cmd/engram/migrate_family_test.go
  </files>
  <read_first>
    cmd/engram/spine_review_purge.go (the registerDestructive closure contract :314-378 — leaf never assigns RunE, supplies preview+apply closures, addOperatorOutputFlag, AddCommand BEFORE registerDestructive; options-from-flags :120-130; pure *Summary discipline :276-309)
    cmd/engram/migrate.go (migrateRemapReportDoc explicit-bool envelope :216-221; Deprecated :261)
    cmd/engram/operator_output.go (addOperatorOutputFlag/renderOperator single-document)
    internal/surfaces/toolclass.go (backfill-short-ids row :194-200 Destructive:false Precedent; migrate-remap-owner Destructive:true)
    internal/store/migrate.go (MigrateOptions + MigrateResult doc: Migrated/Failed telemetry, Backlog truth)
    cmd/engram/catalog_test.go (both-directions gate :429-469)
    .planning/phases/04-migration-cli-first-customer/04-CONTEXT.md (D-05 subcommand split, D-06 new-file-per-family, D-07 rows, D-08 status, D-11 shared envelope)
  </read_first>
  <action>
In a NEW file `cmd/engram/migrate_family.go` (any filename the executor prefers, but a NEW file per D-06 — separate from the transient owner-migration commands in `cmd/engram/migrate.go`):
1. Define the shared migrate report envelope (D-11), the explicit-bool separate-field shape mirroring `migrateRemapReportDoc`/`purgeReportDoc`. Map 1:1 from `store.MigrateResult`, adding the explicit dry-run flag and the migrating target/backlog. Suggested doc struct: `type migrateReportDoc struct { Target int, Migrated uint64, Failed uint64, Passes uint64, Backlog uint64, DryRun bool }` with json tags mirroring the purge/remap precedent. Ferrer never prose-inferred.
2. Build the `migrate` parent command (`migrateCmd`): `Use: "migrate"`, `Short` naming the sweep; flag set = only `--output` (addOperatorOutputFlag) plus nothing custom — no bespoke flags. Route it through `registerDestructive` (after `AddCommand`, per the contract) with two closures:
   - `migratePreview`: `operatorOutputFormat` → `server.StoreFromEnv()` → `st.Migrate(ctx, store.MigrateOptions{DryRun: true})` → `renderOperator(cmd, format, migrateSummary, migrateReportDoc)`.
   - `migrateApplyRun`: identical but `DryRun: false` (the write choke point).
   Both closures derive options ONLY from the defaults (Target=CurrentVersion, Steps=Registry); Phase 4 passes neither field and lets both defaults apply (per MigrateOptions doc) — do NOT add target/steps flags in this phase.
3. Build the `migrate status` command (`migrateStatusCmd`): `Use: "migrate status"`, `Short` naming the histogram; it is a READ-ONLY command (D-07) so it has an ordinary `RunE` (NOT routed through registerDestructive): `operatorOutputFormat` → `server.StoreFromEnv()` → `st.MigrateStatus(ctx)` → `renderOperator(cmd, format, statusSummary, statusReportDoc)` where the report carries the buckets + absent + total (a distribution, never a scalar). `status` adds NO --apply; its flag set is only `--output`.
4. Register both under a parent (either `migrateCmd` containing `status`, or a shared parent command with `AddCommand`); match the `spine-review` nested-subcommand convention. Ensure each space-joined path (`migrate`, `migrate status`) has disjoint flags.

In `internal/surfaces/toolclass.go`:
5. Add rows, per D-07, one per space-joined path:
   - `migrate` = `{ReadOnly:false, Destructive:false, ...}` (additive-only ENFORCED by CheckAdditive; the same reasoning as the existing `backfill-short-ids` row).
   - `migrate status` = `{ReadOnly:true, Destructive:false, ...}`.
   Keep the existing `backfill-short-ids` row (D-07/RESEARCH Open Q3 — keep it so the deprecation-catalog surface stays both-directions green).

In `cmd/engram/catalog_test.go`:
6. Update `TestCatalogBlastRadiusMatchesToolClasses` (both-directions gate) to expect the two new rows AND the two new commands. Must land in the SAME change so the gate never observes a row without its command (or vice-versa). The `migrate` row routes through registerDestructive (which needs its row present at init), so add the row and the command together.

In `cmd/engram/migrate_family_test.go`:
7. CLI tests (cobra, may stub `server.StoreFromEnv` per the destruct/gate-test idiom; use no-testcontainer where possible by injecting a fake store via the closures' seam): bare `migrate` renders a preview document and performs NO writes (a fake store's Migrate gets DryRun:true); `migrate --apply` renders applied and the fake store's Migrate gets DryRun:false; `migrate status` renders a distribution; both render json|text via renderOperator.
8. Preview/apply parity at the CLI boundary: with a fake store whose Migrate returns a deterministic `MigrateResult`, assert preview reports the projected Migrated/Backlog and apply reports the same migrated set with Backlog 0 — the `--apply` acts only on the intersection of the previewed set and the fresh re-derivation (REQ-migrate-preview-apply-parity).
  </action>
  <acceptance_criteria>
    - `go test ./cmd/engram/ -run 'TestMigrate|TestCatalogBlastRadiusMatchesToolClasses' -count=1 -v` passes.
    - `rg -n '"migrate"|"migrate status"' internal/surfaces/toolclass.go` finds the two rows, and `rg -n 'CLICommand: "migrate"|CLICommand: "migrate status"'` confirms the space-joined keys.
    - A grep of `cmd/engram/migrate_family.go` shows `registerDestructive(migrateCmd, ...)` and a plain `RunE` for `migrateStatusCmd` (read-only, not registerDestructive).
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd/engram/ -run 'TestMigrate|TestCatalogBlastRadiusMatchesToolClasses' -count=1 -v</automated>
  </verify>
  <done>engram migrate previews (no writes) and --apply executes the sweep; engram migrate status renders the histogram read-only; both emit the shared envelope; the destruct-tier gate + catalog gate are green.</done>
</task>

<task type="auto">
  <name>Task 3: engram migrate revert --to <v> with whole-range irreversible preflight + toolclass row</name>
  <files>
    cmd/engram/migrate_family.go
    internal/surfaces/toolclass.go
    cmd/engram/migrate_family_test.go
    cmd/engram/catalog_test.go
  </files>
  <read_first>
    cmd/engram/spine_review_purge.go (registerDestructive closure contract; usageErrorf validation :65)
    cmd/engram/operror.go (usageErrorf → exit 2)
    internal/store/revert.go (the Store.Revert the CLI attaches to; its field=steps hint=irreversible refusal + RevertResult)
    internal/surfaces/toolclass.go (migrate-remap-owner Destructive:true row precedent)
    cmd/engram/catalog_test.go (the both-directions gate must learn the migrate revert row + command here, same as Task 2 did for migrate/migrate status)
    .planning/phases/04-migration-cli-first-customer/04-CONTEXT.md (D-13 whole-range preflight, D-14 refusal envelope, D-16 revert preview/apply parity via registerDestructive)
  </read_first>
  <action>
In `cmd/engram/migrate_family.go`:
1. Add the `migrate revert` command (`migrateRevertCmd`): `Use: "migrate revert --to <v>"`, `Short` naming the reverse version; flags = `--output` + `--to` (an int). Validate `--to` via `usageErrorf` (non-integer, negative, or inconsistent-with-chain → exit 2 usage error, D-16/precedent spine_review_purge.go:65); a bare `revert` without `--to` is a usage error.
2. Route it through `registerDestructive` (after AddCommand) with:
   - `revertPreview`: `operatorOutputFormat` → `server.StoreFromEnv()` → call, BEFORE any write, a whole-range irreversible preflight that walks the reverse range and refuses if any step is irreversible; on refusal return the `field=steps hint=irreversible` error naming every step (From/To) + each declared `IrreversibleReason` + "recovery is a collection snapshot" (D-14). When the range is fully reversible, preview surfaces the reverse plan (steps to invert in order, records at each affected version, the preflight result) via a read-only path — compute it WITHOUT executing inverses, then `renderOperator`.
   - `revertApplyRun`: re-run the SAME whole-range preflight (a refusal here renders zero records touched), and only when fully reversible call `st.Revert(ctx, to)` and render the result.
   So both preview and apply gate on the preflight — the CLI never lets `--apply` execute an irreversible range (REQ-migrate-revert preview/apply parity: preview surfaces the plan, --apply executes the same fully-reversible set).
3. Add the `migrate revert` toolclass row `{ReadOnly:false, Destructive:true, ...}` (D-07; Destructive — inverses may remove keys, the anti-additive direction).

In `cmd/engram/catalog_test.go`:
3a. Extend `TestCatalogBlastRadiusMatchesToolClasses` (both-directions gate) to also expect the `migrate revert` row AND the `migrate revert` command, mirroring exactly what Task 2 did for `migrate`/`migrate status`. Must land in the SAME change as the row + command so the gate never observes a row without its command (or vice-versa).

In `cmd/engram/migrate_family_test.go`:
4. CLI tests for revert: an irreversible range (the only registered v0→v1 step makes `--to 0` one) yields the envelope NAMING the step (From/To), its declared reason, and `snapshot`, and proves zero records touched (fake store asserts never called with an execute). A reversible fixture range previews the reverse plan and `--apply` executes it, reporting the converged result. `--to` invalid values return a usage error (exit 2). `revert` without `--to` returns a usage error.
  </action>
  <acceptance_criteria>
    - `go test ./cmd/engram/ -run 'TestMigrateRevert|TestMigrate|TestCatalogBlastRadiusMatchesToolClasses' -count=1 -v` passes.
    - `rg -n 'CLICommand: "migrate revert"' internal/surfaces/toolclass.go` finds the row; its `Class.Destructive` is `true`.
    - `rg -n "usageErrorf" cmd/engram/migrate_family.go` shows `--to` validation; `rg -n "registerDestructive(migrateRevertCmd" cmd/engram/migrate_family.go` shows the revert routed through the gate.
    - The catalog both-directions gate stays green with all three rows + all three commands.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd/engram/ -run 'TestMigrateRevert|TestMigrate|TestCatalogBlastRadiusMatchesToolClasses' -count=1 -v</automated>
  </verify>
  <done>engram migrate revert previews the reverse plan by default, refuses any irreversible range before a write (naming every step + reason + snapshot), and applies only a fully-reversible range via registerDestructive.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI → store (migrate/revert) | The CLI is the write choke point; unsafe/partial revert and preview-writes are the threats. |
| classification table → routing | toolclass rows decide which command may mutate; a wrong row or a bypassed gate is a reclassification bypass. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-05 | Tampering | `registerDestructive` gate | high | mitigate | Predicate becomes `!class.ReadOnly`; classification stays DERIVED from toolclass.go (no injectable seam — rejected-alternative block enforced); ReadOnly commands are refused; TestDestructiveGatePreventsMutation stays green. |
| T-04-06 | Tampering | `migrate` preview writes | high | mitigate | Preview closure passes `MigrateOptions{DryRun: true}` (never SetPayload); only the `--apply` closure uses DryRun:false; registerDestructive calls exactly one branch (preview→preview, apply→apply), never composed. |
| T-04-07 | Tampering | `migrate revert` partial/unwanted revert | critical | mitigate | Whole-range irreversible preflight runs in BOTH preview and apply before any write; an irreversible range refuses the entire op (field=steps hint=irreversible, naming each step + reason + snapshot); inverse-removed-key writes only in Store.Revert, never Store.Migrate. |

</threat_model>

<verification>

After plan execution (wave 3):

- `go test ./cmd/engram/ -run 'TestMigrate|TestDestructive|TestCatalogBlastRadiusMatchesToolClasses' -count=1` green.
- `go vet ./cmd/engram/` clean; `task` green.
- Real-binary smoke check: `go run ./cmd/engram migrate` against a test Qdrant renders a preview document with no writes; `go run ./cmd/engram migrate revert --to 0` renders the irreversible refusal naming the v0→v1 step + snapshot.

</verification>

<success_criteria>

- `engram migrate` previews only, `--apply` is the write choke point, `--output json|text` matches the tier (REQ-migrate-command).
- `engram migrate status` renders a server-side distribution (REQ-migrate-status-histogram).
- preview/apply parity proven at the CLI (REQ-migrate-preview-apply-parity).
- `engram migrate revert --to` previews, refuses irreversible ranges whole, applies only reversible ones (REQ-migrate-revert).

</success_criteria>

## Artifacts this phase produces

- `cmd/engram/destructive.go`: `destructiveByClassification` now returns `!class.ReadOnly`; updated panic message + doc.
- `cmd/engram/migrate_family.go` (new): `migrateCmd`, `migrateStatusCmd`, `migrateRevertCmd`; `migratePreview`/`migrateApplyRun` closures; `migrateStatus` run; `revertPreview`/`revertApplyRun` closures; shared `migrateReportDoc` + `statusReportDoc` + summary funcs.
- `internal/surfaces/toolclass.go`: three rows added — `migrate` (R:false D:false), `migrate status` (R:true D:false), `migrate revert` (R:false D:true).
- Tests: extended `TestDestructive*`; new migrate-family CLI tests; updated `TestCatalogBlastRadiusMatchesToolClasses`.

<output>
Create `.planning/phases/04-migration-cli-first-customer/04-03-SUMMARY.md` when done
</output>