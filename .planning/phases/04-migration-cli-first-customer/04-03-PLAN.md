---
phase: 04-migration-cli-first-customer
plan: 03
type: execute
wave: 3
depends_on:
  - 04-migration-cli-first-customer-01
  - 04-migration-cli-first-customer-02
files_modified:
  - cmd/engram/destructive.go
  - cmd/engram/destructive_test.go
  - cmd/engram/migrate_family.go
  - cmd/engram/migrate_family_test.go
  - cmd/engram/operator_output_test.go
  - cmd/engram/cmdwalk_test.go
  - cmd/engram/operror_test.go
  - internal/surfaces/toolclass.go
  - cmd/engram/catalog_test.go
  - internal/surfaces/rules.go
  - internal/surfaces/rules_test.go
  - docs-site/src/content/docs/guides/cli.md
  - skill/engram/skills/curating-memory/SKILL.md
  - cmd/engram/testdata/help.golden
  - cmd/engram/testdata/catalog.golden
autonomous: true
requirements:
  - REQ-migrate-command
  - REQ-migrate-status-histogram
  - REQ-migrate-preview-apply-parity
  - REQ-migrate-revert
estimate:
  tokens: 86000
  raw_tokens: 43000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - statement: "**TWO DIFFERENT `!ReadOnly` PREDICATES EXIST IN THIS PLAN AND MUST NEVER BE CONFLATED. Collapsing them is the original C4-H1 defect.**\n  (a) **The ADMISSION predicate — legitimately `!ReadOnly`.** `registerDestructive`'s gate generalizes from `class.Destructive` to `!class.ReadOnly`, so an additive-but-mutating command (`migrate`, `Destructive:false`) and a destructive one (`migrate revert`, `Destructive:true`) share the ONE preview/apply routing mechanism. This answers 'may this command route through the gate?' and `!ReadOnly` is the RIGHT answer. It lives in `cmd/engram/destructive.go`'s `destructiveByClassification`, Task 1 step 1, and nowhere else.\n  (b) **The `--apply`-REQUIRED set — NOT `!ReadOnly`.** `mutatingCommandNames()` = `destructiveCommandNames() ∪ applyRoutedAdditions − pendingApplyConversion`, a NAMED union living in `cmd/engram/destructive_test.go`. This answers 'which commands MUST carry `--apply`?', and `!ReadOnly` is the WRONG answer: executed against the live `surfaces.Operations()` table it selects ELEVEN commands, seven of which carry no `--apply` and are not routed through `registerDestructive`.\n  **The disambiguation rule, so no reader has to check a line number:** an occurrence of `!ReadOnly` in `destructive.go` is (a) and is correct; an occurrence anywhere near `mutatingCommandNames`, `applyRoutedAdditions`, or a `--apply` want-set is (b) and is a defect. Nothing in this plan describes (b) as deriving from `!ReadOnly`. The function name stays `registerDestructive`; the terminology debt of 'destructive' for 'any mutating operation' is accepted and documented (REVIEWS.md M9)."
      verification: backstop
    - "A bare `engram migrate` invocation previews only (DryRun via Store.Migrate with DryRun:true, no writes); `--apply` is the only write choke point, via registerDestructive (REQ-migrate-command SC1)."
    - "`engram migrate --apply` re-previewes inside the apply closure (the purge pattern — REVIEWS.md H5): the apply closure calls `Store.Migrate(DryRun:true)` FIRST to get a fresh preview manifest, then calls `Store.Migrate(Manifest: freshManifest)` with it — within the SAME invocation. No package-level manifest var bridges separate invocations. This mirrors `spine_review_purge.go:339-377` exactly."
    - "`engram migrate --output json|text` renders through addOperatorOutputFlag/renderOperator() one document, mirroring the rest of the operator tier (migrateRemapReportDoc/purgeReportDoc explicit-field precedent)."
    - statement: "`engram migrate status` renders the server-side version-distribution histogram (Store.MigrateStatus), not a scalar, and is a ReadOnly:true command that does NOT route through registerDestructive (D-07/D-08). **It nevertheless carries `--timeout` AND `signal.NotifyContext`, like every other Qdrant-dialing operator command including the read-only ones (REVIEWS.md C5-M6):** `spine-review scan` (spine_review_scan.go:55-59) and `spine-review verify` (spine_review_verify.go:639-643) are both read-only and install both, and every member of all three published `timeoutGroups` carries `--timeout` (operator_output_test.go:445-465). Without them a hung Qdrant makes `engram migrate status` block with no deadline and no graceful Ctrl-C. So: `migrateStatusTimeout` (5m default, 0 disables), the `signal.NotifyContext` + `migrateWithTimeout` wiring, a `zero-disables` `timeoutGroups` row plus its `timeoutGroupCaseArgs` case, and the `guides/cli.md` table entry — all in Task 2. `migrate status` still adds NO `--apply` and is still not routed through `registerDestructive`; its flag set is exactly `{output, timeout}`."
      verification: backstop
    - "The `--timeout` flags are actually READ, not merely registered (REVIEWS.md N3): the shared helper takes the duration as a PARAMETER — `migrateWithTimeout(ctx context.Context, d time.Duration) (context.Context, context.CancelFunc)` — and each leaf passes its own flag-backed var. Zero DISABLES the deadline, matching `spinePurgeWithTimeout`/`pruneWithTimeout` (cmd/engram/spine_review_purge.go:107-113, prune.go:44-52) and putting `migrate`/`migrate revert` in the SAME published `zero-disables` group `backfill-short-ids` already belongs to — not the `reject-zero-operator` group `migrate-remap-owner`/`migrate-set-owner` use. A `--timeout 1s` invocation therefore observably shortens the deadline, and a `--timeout 0` invocation observably removes it."
    - "The new flag-backed vars are `migrateSweepTimeout`, `migrateStatusTimeout` and `migrateRevertTimeout` — one per leaf. The identifier `migrateTimeout` is ALREADY TAKEN by `migrate-set-owner` (cmd/engram/migrate.go:22, registered at :239) and MUST NOT be reused — a second DurationVar into the same package-level var is a compile collision at best and shared cross-command flag state at worst. Per-leaf vars follow the shipped `spineArchiveTimeout`/`spineRestoreTimeout` precedent (spine_review_archive.go:236,243)."
    - statement: "Every `--timeout`-bearing command is assigned to one of the three published groups in `cmd/engram/operator_output_test.go`'s `timeoutGroups` (`:445-465`), because `TestTimeoutGroupMatrix` asserts SET EQUALITY between that union and the live tree (`:625-648`): `migrate`, `migrate status` and `migrate revert` ALL join `zero-disables`, and `docs-site/src/content/docs/guides/cli.md`'s published three-group table (header `:375`, the `zero-disables` row `:378`; ledger row D7) is updated in the same change. **The set equality runs in BOTH directions, so a `timeoutGroups` entry naming a command that is not yet in the live tree fails just as loudly as a live command with no entry (INV-1).** Each entry therefore lands in the SAME TASK as its command: `migrate` + `migrate status` in Task 2, `migrate revert` in Task 3."
      verification: backstop
    - "`engram migrate --apply` acts only on the intersection of the previewed set and a fresh re-derivation (REQ-migrate-preview-apply-parity): the apply closure runs preview internally (re-derives eligibility fresh), receives the manifest, then passes it into `Store.Migrate(Manifest: freshManifest)`. Only the manifest∩fresh-intersection is migrated. This is the spine-review purge intersection pattern (REVIEWS.md H6/SC3) — NOT a package-var bridge (H5)."
    - "`engram migrate revert --to <v>` previews by default (surfacing the reverse plan via the exported Store-level `PreviewRevert(ctx, to) (store.RevertPlan, error)` method) and `--apply` executes only a fully-reversible range; an irreversible or unsupported-version range refuses the entire operation naming every step From/To, each declared reason, every unreachable stored version, and snapshot recovery in the field=<name> hint=<code> envelope (D-13/D-14/D-16). Both preview and apply closures call the SAME exported PreviewRevert method (M8 closed — no unexported store helper called from CLI). **`store.RevertPlan` and `Store.PreviewRevert` are PRODUCED BY 04-02** (internal/store/revert.go, listed in 04-02's files_modified and its 'Artifacts this phase produces' ledger); this plan only CONSUMES them, and this dependency is why 04-03 depends_on 04-02 (cycle-3 HIGH #1 — the prior revision referenced these symbols with no producing plan)."
    - "`PreviewRevert` returns an unreversible range as a VERDICT — `(plan, nil)` with `plan.Reversible == false` — not as a Go error (04-02's contract), so the CLI renders the refusal through the normal report path and reserves `classifyOperatorErr` for genuine backend failures. The apply closure re-checks `plan.Reversible` and returns the refusal WITHOUT calling `Store.Revert`."
    - statement: "**A REFUSED `migrate revert --apply` EXITS NON-ZERO — `exitUsage` (2) — while a bare (preview) invocation of the same unreversible range EXITS 0.** A preview that correctly reports 'this range cannot be reverted' SUCCEEDED at its job; an `--apply` that was asked to mutate and refused did NOT do what it was told, and automation checking `$?` must see that. The shipped mechanism makes the naive form exit 0 end to end: `renderOperator` returns the writer's error (nil on a successful render, cmd/engram/operator_output.go:64-75) and `registerDestructive`'s `RunE` returns the apply closure's error verbatim (destructive.go:124-131). `exitUsage` is the taxonomically correct code — `client_common.go:222` defines it as 'usage or validation error', and an unrevertible `--to` target is a validation refusal already rendered in the `field=<name> hint=<code>` grammar; `exitFindings` (7) is disqualified by its own doc, which scopes it to a command that succeeded (`client_common.go:227-235`). No NEW exit-code constant is declared, so ledger rows A19/A24 stay inert. The shape is render-then-return, with an exact shipped precedent at `spine_review_verify.go:659-662`. Task 3 step 2a carries the mechanics; both refusal classes are exit-code-pinned by CLI test."
      verification: backstop
    - "`--to <v>` is validated via usageErrorf (a non-integer, negative, or out-of-chain version is a usage error), never a bare unregistered panic."
    - "CLI tests use a narrow local store interface (migrateFamilyStore) that includes `PreviewRevert(ctx, to) (store.RevertPlan, error)` (M8 — the exported preflight accessor), with an injectable constructor (migrateFamilyStoreFromEnv), mirroring the spineConsolidateStoreFromEnv pattern — enabling fake-store substitution in CLI tests without touching production routing (REVIEWS.md M7)."
    - statement: "`mutatingCommandNames()` is `destructiveCommandNames() ∪ applyRoutedAdditions`, where `applyRoutedAdditions` is a small NAMED set with per-entry justification and its own pinning test — NOT the predicate `!op.Class.ReadOnly && op.CLICommand != \"\"` (REVIEWS.md C4-H1). The `!ReadOnly` predicate is not merely incomplete, it is WRONG: executed against the live `surfaces.Operations()` table it selects ELEVEN commands, of which only three are `Destructive` and only five will ever carry `--apply` at wave 3. The `--apply`-routed tier is the set of `registerDestructive` callers, and that tier is not expressible from the table because the table has no tier column. The package already carries the right precedent for exactly this situation: `operatorCommandExclusions` (cmd/engram/cmdwalk.go:63-97) — a small named set, each entry carrying the reason a structural filter cannot express it, pinned by `TestOperatorCommands` so neither a silent addition nor a stale entry survives."
      verification: backstop
    - statement: "The derivation's RESULT is stated as an enumerated set at every task boundary, not left as a predicate to be discovered at execution time. **End of Task 2** — `migrate revert` does not exist yet, so `mutatingCommandNames()` returns exactly FOUR names (`migrate`, `migrate-remap-owner`, `prune-expired`, `spine-review purge`) and the live `--apply` carriers are exactly those same four (`registerDestructive`'s callers: prune.go:159, spine_review_purge.go:425, migrate.go:257, plus this task's `migrate`). Both directions of `TestDestructiveCommandsRequireApply` hold, which is why the switch to `mutatingCommandNames()` belongs in Task 2 rather than Task 3. **End of Task 3 (end of wave 3)** — exactly FIVE, adding `migrate revert`, matching five live carriers; `TestMutatingCommandNamesMembership` pins that five here. **End of wave 4**, after 04-04 converts the alias and deletes `pendingApplyConversion` — exactly SIX, adding `backfill-short-ids`, matching six live carriers. No command is left over on either side at any of the three boundaries."
      verification: backstop
    - "The seven commands the rejected `!ReadOnly` predicate wrongly swept in — `store`, `reindex`, `summarize-missing`, `serve`, `migrate-set-owner`, `spine-review archive`, `spine-review restore` — carry no `--apply` flag, are not routed through `registerDestructive`, and are NOT in the mutating set under the named-additions derivation. They need no exclusion entry, because the derivation never proposes them in the first place; `pendingApplyConversion` shrinks to its one true purpose (holding `backfill-short-ids` out for exactly one wave) instead of being the only thing standing between the gate and seven false positives."
    - "`applyRoutedAdditions` carries a PINNING TEST mirroring `TestOperatorCommands`' exclusion-set pinning (cmdwalk_test.go:168-182): every name in it must resolve to a live `surfaces.ClassForCommand` row, must be `ReadOnly:false` (an additive-but-mutating command), and must be `Destructive:false` (a `Destructive:true` entry would be redundant with `destructiveCommandNames()` and is therefore stale by construction). A future reclassification of `migrate` or `backfill-short-ids` to `Destructive:true` fails that pin loudly instead of leaving a silently duplicated name."
    - "TestDestructiveCommandsRequireApply derives the `--apply`-required set from `mutatingCommandNames()` (the D-16 generalization, REVIEWS.md M12) rather than `destructiveCommandNames()`. Because `backfill-short-ids` is a named additive-routed member that does not gain `--apply` until 04-04, this plan carries a NAMED, TEMPORARY exclusion `pendingApplyConversion` that 04-04 deletes — the wave-3 tree must stay green, and an unexplained-but-passing gate is worse than a documented one-wave exception."
    - "`surfaces.RuleDestructiveRequiresApply`'s Sentence is UPDATED — unconditionally, not 'if it would misdescribe' (REVIEWS.md N5) — from 'a **destructive** operator command previews by default and mutates only when apply is set' to 'a **mutating** operator command previews by default and mutates only when apply is set'. `addApplyFlag` composes EVERY `--apply` Usage string from that Sentence (destructive.go:57-63), so once additive `migrate` carries `--apply` the old wording is factually wrong on a live help surface. The rule's ID stays `RuleDestructiveRequiresApply` (renaming it would ripple through the anchor markers and every rule-ID consumer for no behavioral gain)."
    - statement: "Changing the rule Sentence propagates through the SANCTIONED regeneration path, never by hand-editing generated regions: `internal/surfaces/rules.go` is the source, `internal/surfaces/rules_test.go:56`'s pinned `wantSentence` const is hand-updated to match, and `task surfaces:gen` regenerates the anchored `<!-- engram:rule:start destructive-requires-apply -->` regions in `docs-site/src/content/docs/guides/cli.md:135` and `skill/engram/skills/curating-memory/SKILL.md:379` plus the `cmd/engram/testdata/{help,catalog}.golden` fixtures (Taskfile.yaml:244-264)."
      verification: backstop
    - statement: "Toolclass rows `migrate` (ReadOnly:false, Destructive:false), `migrate status` (ReadOnly:true, Destructive:false), `migrate revert` (ReadOnly:false, Destructive:true) exist and the catalog↔toolclass both-directions gate stays green."
      verification: backstop
  prohibitions:
    - "No new injectable classification seam — registerDestructive's classification stays DERIVED from internal/surfaces/toolclass.go (the rejected-alternative block, destructive.go) and no command MAY add its own --apply/--output flag outside operator_output.go/registerDestructive."
    - "A `migrate`/`backfill` command MUST NOT be routed in a way that lets preview fall through to apply, or compose the two branches (registerDestructive calls exactly one)."
    - "A ReadOnly command (migrate status) MUST NOT be routed through registerDestructive, and a mutating one (migrate / migrate revert) MUST NOT bypass it."
    - "`migrate revert` MUST NOT partially revert or auto-unwind on a failed --apply (the sweep is resumable; the recovery path is a snapshot, per the milestone Out-of-Scope)."
    - "CLI preview/apply parity MUST NOT be proven by count-equality alone — it requires manifest identity set (the preview manifest ∩ fresh re-derivation), per REVIEWS.md H6/SC3."
    - "Revert preflight logic MUST NOT be duplicated between CLI and store — one shared exported helper `PreviewRevert` lives on the Store and is accessible through the migrateFamilyStore interface (REVIEWS.md M8)."
    - "Nested subcommand cobra `Use` strings MUST be leaf-only: `Use: \"status\"` not `Use: \"migrate status\"` — the qualified path comes from parentage (REVIEWS.md M6, precedent spine_review_purge.go:390)."
    - "The manifest bridge MUST NOT be implemented as a package-level var populated by one closure and consumed by another (REVIEWS.md H5). The apply closure must re-preview inside itself, mirroring spine_review_purge.go:339-377."
    - "`migrate` and `migrate revert` and `backfill-short-ids` MUST carry a finite default timeout — registerDestructive provides only signal cancellation (H8). Each leaf registers its own `--timeout` var defaulting to 5 minutes and PASSES that var to the shared helper."
    - "`migrateWithTimeout` MUST NOT hard-code a duration and MUST NOT ignore its leaf's flag (REVIEWS.md N3). Its signature takes a `time.Duration`; a registered-but-unread `--timeout` is a dead flag that the D-12/conformance gates then assert the existence of."
    - "The new timeout vars MUST NOT be named `migrateTimeout` — that identifier is already bound to `migrate-set-owner` at cmd/engram/migrate.go:22."
    - "A new `--timeout`-bearing command MUST NOT be added without a `timeoutGroups` assignment in cmd/engram/operator_output_test.go — `TestTimeoutGroupMatrix`'s set equality fails loudly, and silencing it by loosening the equality is forbidden."
    - "`mutatingCommandNames()` MUST NOT be defined as `!op.Class.ReadOnly && op.CLICommand != \"\"` (REVIEWS.md C4-H1). That predicate yields eleven commands against the live table and makes `TestDestructiveCommandsRequireApply` fail on seven of them at wave 3, and `TestDestructiveCommandsRouteThroughGate` / `TestDestructiveCommandsExactFlagSet` / `TestApplyFlagUsageComposesRuleSentence` fail on the same seven at wave 4 — which would also VOID 04-04's RED-first non-vacuity experiments, since a gate that is already red proves nothing when you break it further."
    - "The fix for an over-broad derivation MUST NOT be a growing exclusion list. `pendingApplyConversion` holds exactly ONE name for exactly ONE wave and has a named deletion owner; a second entry added to make a wrong predicate pass is the smell that the predicate, not the list, is the defect."
    - "A candidate derivation MUST NOT be committed until its result has been ENUMERATED against the live `surfaces.Operations()` table and written into this plan as an explicit list of command names. A predicate whose selected set you cannot state is not ready to be a gate."
    - "TestDestructiveCommandsRequireApply MUST derive the `--apply` set from `mutatingCommandNames()` (the D-16 generalization), not from `Destructive:true` alone (REVIEWS.md M12)."
    - "The `RuleDestructiveRequiresApply` Sentence update MUST NOT be left conditional (REVIEWS.md N5) and its downstream anchored regions and goldens MUST NOT be hand-edited — regenerate with `task surfaces:gen`. **Comment-text discipline for its gate (INV-2):** the acceptance criterion negative-counts the OLD sentence across `rules.go` and `rules_test.go`, so no comment in either file may quote `a destructive operator command previews by default` verbatim — describe the change in prose ('the sentence previously said destructive; it now says mutating')."
    - statement: "**EVERY TASK IN THIS PLAN ENDS WITH `go test ./cmd/engram/ -count=1` GREEN, UNFILTERED (INV-1), AND NO TASK GATES ON AN ARTIFACT A LATER TASK PRODUCES.** Those are one rule, not two: the unfiltered run is what makes the second half checkable. `operatorCommands()` admits a command the moment it exists — `registerDestructive` sets a non-nil `RunE` (`destructive.go:124`), the new commands register no `server` flag, and none is in `operatorCommandExclusions` (`cmdwalk.go:101-116`) — so **every registry keyed on that derivation changes in the SAME TASK as the command.** The objective's sequencing table is the authoritative per-task assignment; do not re-derive it here. Task 1 DECLARES `applyRoutedAdditions`, `pendingApplyConversion` and `mutatingCommandNames()` without gating on them — declaring data is not gating on it, an unused package-level `var`/`func` is legal Go (only LOCAL variables error), and `task lint`'s `unused` check runs at wave boundaries where all three are consumed."
      verification: backstop
    - "A `-run` selector MUST NOT name a test that does not exist (INV-3) — `go test -run` silently matches nothing and reports PASS. Every name in this plan's selectors resolves to a shipped `func Test…` or to a test the SAME task creates; where a task's real gate is package-wide, the selector is accompanied by an unfiltered `go test ./cmd/engram/ -count=1`."
    - "New Go files created by this plan (`cmd/engram/migrate_family.go`, `cmd/engram/migrate_family_test.go`) MUST carry the Apache-2.0 SPDX header (REVIEWS.md C5-L7): `cmd/**` is in `.licenserc.yaml`'s `paths`, and `license:check` is a SEPARATE CI job (`.github/workflows/ci.yaml:180`) that `task` (lint + test) does NOT run. `docs-site/**` and `skill/**/SKILL.md` are EXCLUDED — never add a header to `guides/cli.md` or `curating-memory/SKILL.md`."
    - "A refused `migrate revert --apply` MUST NOT exit 0 (REVIEWS.md C5-H4). Rendering the refusal and returning `nil` is exit 0 through `renderOperator` → `registerDestructive`'s `RunE`, and automation reads that as a completed migration. Both refusal classes return a `usageErrorf`-classified error carrying `exitUsage` (2), pinned by a CLI test per class."
    - "The refusal envelope MUST NOT be re-formatted in `cmd/engram` (REVIEWS.md C5-M4). 04-02 exports `store.RevertRefusalError(plan) error`; this plan CALLS it for both the preview rendering and the apply return, and declares no `field=steps`/`field=record_version` format string of its own."
---

<objective>
Build the `engram migrate` command family — `migrate` (forward sweep preview/apply with in-apply-closure re-preview per the purge pattern — H5), `migrate status` (read-only histogram), `migrate revert --to <v>` (reverse inverses with shared exported `PreviewRevert` accessor — M8) — as subcommands routed through the standard destructive-tier `registerDestructive`, generalize that mechanism's gate so the additive `migrate` sweep can use it (the phase's single required non-consumption code change), add the three toolclass blast-radius rows, prove preview/apply parity at the CLI boundary via identity set (H6/SC3), add a 5-minute default timeout to every migrate-family command including the read-only `migrate status` (H8 + C5-M6), re-derive `TestDestructiveCommandsRequireApply`'s want-set from the NAMED union `mutatingCommandNames()` (M12 as corrected by C4-H1 — **not** from `!ReadOnly`), and add PreviewRevert to the store interface (M8).

**Read this before anything else: the two `!ReadOnly` predicates.** This plan changes ONE predicate to `!ReadOnly` and REJECTS a different one that looks identical:
- **ADMISSION — `registerDestructive`'s gate in `cmd/engram/destructive.go` becomes `!class.ReadOnly`.** "May this command route through the preview/apply gate?" Correct, and it is the phase's single required non-consumption code change (Task 1 step 1).
- **`--apply` REQUIREMENT — `mutatingCommandNames()` in `cmd/engram/destructive_test.go` is a NAMED UNION, never `!ReadOnly`.** "Which commands MUST carry `--apply`?" The `!ReadOnly` predicate selects ELEVEN commands against the live `surfaces.Operations()` table; seven of them (`store`, `reindex`, `summarize-missing`, `serve`, `migrate-set-owner`, `spine-review archive`, `spine-review restore`) have no `--apply` and no `registerDestructive` routing (C4-H1), so four conformance gates would go RED across waves 3–4 and 04-04's RED-first non-vacuity experiments would be VOID.

Where you see `!ReadOnly` below, the file it lives in tells you which question it answers.

**Task sequencing, stated once because it is the plan's load-bearing shape (INV-1).** `operatorCommands()` admits a command the instant it exists, so every registry keyed on that derivation must change in the same task as the command — not a task later, or that task's own unfiltered `go test ./cmd/engram/ -count=1` is RED:

| Task | Commands landed | Registries updated in the same task | Regeneration |
|---|---|---|---|
| 1 | none | — (declares `applyRoutedAdditions` / `pendingApplyConversion` / `mutatingCommandNames()` without gating on them); rule Sentence + its pin | `task surfaces:gen` for the Sentence |
| 2 | `migrate`, `migrate status` | A1 `wantOperatorCommandKeys`, A2 `operatorParityRows`, A3 `operatorInvalidOutputArgs`, A4 `timeoutGroups`, A7 catalog-golden `--output` equality, A8 toolclass↔catalog, A22 `operatorCommandFiles`; `TestApplyRoutedAdditionsArePinned`; the `TestDestructiveCommandsRequireApply` switch | `task surfaces:gen` for the two commands |
| 3 | `migrate revert` | the same rows again for `migrate revert`, plus A14 `destructiveFlagCases`; `TestMutatingCommandNamesMembership` pinning five | `task surfaces:gen` for the third command |

One regeneration per task that changes the live tree. 04-04 Task 1 runs the fourth, for the alias.

Purpose: This is the operator-facing surface for all six Phase-4 requirements except the backfill alias (04-04) and the never-automatic startup cap (04-02). It relies on the store methods from 04-01 (Store.Migrate + DryRun + manifest) and 04-02 (Store.MigrateStatus, Store.Revert, the exported PreviewRevert, and the exported RevertRefusalError).

Output: a new `cmd/engram/migrate_family.go`, the generalized `registerDestructive` **admission** gate with M9 documentation, three toolclass rows, the shared migrate report envelope, a local store interface seam (M7) that includes `PreviewRevert` (M8), a shared `migrateWithTimeout` helper wired into all three commands including the read-only `migrate status`, the CLI + catalog conformance tests with manifest-based parity (H6), a non-zero exit code for both revert-refusal classes, and `TestDestructiveCommandsRequireApply` re-derived from the NAMED union `mutatingCommandNames() = destructiveCommandNames() ∪ applyRoutedAdditions − pendingApplyConversion`.

**Consumed from 04-02; declared nowhere here.** `store.RevertPlan`, `Store.PreviewRevert`, `store.RevertRefusalError` (`internal/store/revert.go`, in 04-02's `files_modified` and artifact ledger). If any is absent at execution time, STOP — do not declare it here.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/04-migration-cli-first-customer/04-CONTEXT.md
@.planning/phases/04-migration-cli-first-customer/04-RESEARCH.md
@.planning/phases/04-migration-cli-first-customer/04-PATTERNS.md
@.planning/phases/04-migration-cli-first-customer/04-REVIEWS.md

@cmd/engram/destructive.go (registerDestructive gate at :116-122, RunE dispatch at :124-131)
@cmd/engram/spine_review_purge.go (the preview/apply closure + manifest bridge at :339-377 — the PATTERN TO FOLLOW: PreviewPurge is called INSIDE spinePurgeApplyRun, not from a separate invocation — THIS IS THE H5 PRECEDENT)
@cmd/engram/migrate.go (migrateRemapReportDoc envelope + Deprecated precedent)
@cmd/engram/operator_output.go (addOperatorOutputFlag/renderOperator)
@internal/surfaces/toolclass.go (backfill-short-ids Destructive:false row; migrate-remap-owner Destructive:true row)
@cmd/engram/catalog_test.go (TestCatalogBlastRadiusMatchesToolClasses both-directions gate :429-469)
@internal/store/migrate.go (MigrateOptions.Manifest + MigrateResult.PreviewManifest/Spared/Appeared — produced by 04-01)
@internal/store/migrate_status.go (MigrateStatusResult with per-version Future buckets — produced by 04-02)
@internal/store/revert.go (`type RevertPlan` + the exported `func (s *Store) PreviewRevert(ctx, to) (RevertPlan, error)` — PRODUCED BY 04-02; this plan consumes and never declares them)
@cmd/engram/migrate.go (the already-bound `migrateTimeout` identifier — do not reuse)
@cmd/engram/operator_output_test.go (timeoutGroups + TestTimeoutGroupMatrix's set equality)
@internal/migrate/step.go (IrreversibleReason for refusal rendering)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generalize registerDestructive's ADMISSION predicate to !ReadOnly + accept M9 terminology debt + DECLARE the named `mutatingCommandNames()` union (its gates land with the artifacts they measure, in Tasks 2 and 3 — INV-1) + PIN the rule-sentence update (N5) + repair the unanchored cli.md destructive-command prose (D9)</name>
  <files>
    cmd/engram/destructive.go
    cmd/engram/destructive_test.go
    internal/surfaces/rules.go
    internal/surfaces/rules_test.go
    docs-site/src/content/docs/guides/cli.md
    skill/engram/skills/curating-memory/SKILL.md
    cmd/engram/testdata/help.golden
    cmd/engram/testdata/catalog.golden
  </files>
  <read_first>
    cmd/engram/destructive.go:37-48 (destructiveByClassification returns class.Destructive), :57-63 (addApplyFlag composes the --apply Usage from surfaces.RuleDestructiveRequiresApply's Sentence — this is WHY the sentence must change), :110-122 (registerDestructive panic), the rejected injectable-seam block (a hard prohibition)
    internal/surfaces/toolclass.go Class semantics (:16-24: "ReadOnly is true when the operation never modifies environment; Destructive is true only if the operation may remove or overwrite")
    cmd/engram/destructive_test.go:23-36 (destructiveCommandNames — the `op.Class.Destructive` derivation every gate in the file shares), :88-106 (TestDestructiveCommandsRequireApply, BOTH directions), :130-152 (TestDestructiveCommandsRouteThroughGate), :200-220 (TestApplyFlagUsageComposesRuleSentence), :231-238 (destructiveFlagCases table), :248-270 (TestDestructiveCommandsExactFlagSet, BOTH directions)
    internal/surfaces/rules.go:230-240 (RuleDestructiveRequiresApply's Sentence — the literal string to change)
    internal/surfaces/rules_test.go:56 (the pinned `wantSentence` const that must be updated in the same change)
    Taskfile.yaml:244-264 (`task surfaces:gen` — the ONLY sanctioned way to propagate a Sentence change into the anchored cli.md/SKILL.md regions and the help/catalog goldens; note the goldens are deliberately never regenerated by `task test`)
    .planning/phases/04-migration-cli-first-customer/04-RESEARCH.md (Pattern 1 + Pitfall 1 — gate must change to `!class.ReadOnly`)
    .planning/phases/04-migration-cli-first-customer/04-REVIEWS.md (M9 — accept the registerDestructive terminology debt; M12 — `TestDestructiveCommandsRequireApply` must derive its want-set from the NAMED union `mutatingCommandNames()`, **never from the `!ReadOnly` predicate**, which is `registerDestructive`'s separate ADMISSION question; N1 — the three sibling gates; N5 — the rule-sentence update must be pinned, not conditional)
  </read_first>
  <action>
In `cmd/engram/destructive.go`:
1. Change the predicate returned by `destructiveByClassification` from `return class.Destructive` to `return !class.ReadOnly`. Do NOT add an injectable classification seam (the rejected-alternative block is a hard prohibition). The panic-on-no-row behavior, `commandKey` lookup, and the `ClassForCommand` call stay identical.
2. Update the comment/doc on `destructiveByClassification` and the `registerDestructive` panic message (currently "command %q is not classified destructive ... only a command whose blast-radius row has Destructive: true may route through this gate") to state the mechanism now serves ANY mutating operator command (ReadOnly:false) — additive mutations (migrate / backfill) included — not only destructive ones. Keep the key claim that routing a ReadOnly:true command is a programming error (that is now the enforcement), and that a command with no classification row still panics.
3. Accept the `registerDestructive` terminology debt (REVIEWS.md M9): Add a doc comment on the `destructiveByClassification` function or the `registerDestructive` function explaining that the name predates the gate's generalization and is preserved for backward compatibility with caller sites — "destructive" now means "any mutating operation" (ReadOnly:false), not exclusively key-removing operations. This is an accepted name debt, not scheduled for rename in this phase.
4. Do NOT touch `addApplyFlag`, `applyRequested`, or the RunE dispatch — they are already classification-agnostic.

In `cmd/engram/destructive_test.go`:

**5. Add `mutatingCommandNames()` as `destructiveCommandNames() ∪ applyRoutedAdditions`, NOT as a `!ReadOnly` predicate (REVIEWS.md C4-H1).**

   **Read this before writing the code — the rejected derivation and why.** A prior revision defined this helper as `!op.Class.ReadOnly && op.CLICommand != ""`. Executed against the live `surfaces.Operations()` table that predicate selects ELEVEN commands:
   `backfill-short-ids`, `migrate-remap-owner`, `migrate-set-owner`, `prune-expired`, `reindex`, `serve`, `spine-review archive`, `spine-review purge`, `spine-review restore`, `store`, `summarize-missing`.
   Of those, only THREE are `Destructive:true` (`migrate-remap-owner`, `prune-expired`, `spine-review purge`), and `--apply` exists on exactly the commands routed through `registerDestructive` — whose only callers are `prune.go:159`, `spine_review_purge.go:425`, `migrate.go:257`. So SEVEN commands (`store`, `reindex`, `summarize-missing`, `serve`, `migrate-set-owner`, `spine-review archive`, `spine-review restore`) would be demanded to carry `--apply` and have none. The `--apply`-routed tier is a ROUTING fact, and the blast-radius table has no routing column; `!ReadOnly` is a different question ("does this command write?") that happens to have a larger answer.

   Write it as an explicit union over a NAMED set, following the `operatorCommandExclusions` precedent (`cmd/engram/cmdwalk.go:63-97` — a small named set, per-entry justification, pinned by `TestOperatorCommands` at cmdwalk_test.go:168-182):

   - `var applyRoutedAdditions = map[string]bool{"migrate": true, "backfill-short-ids": true}` with a doc comment stating, per entry, the reason a table-derived predicate cannot express it: these two commands are ADDITIVE (`Destructive:false` — `CheckAdditive` enforces that the sweep only ever adds keys) yet they route through `registerDestructive` because previewing before mutating is the right operator contract for any write, destructive or not (D-16). `migrate revert` is deliberately NOT here: its toolclass row is `Destructive:true` (Task 3), so `destructiveCommandNames()` already returns it, and listing it again would be a stale duplicate.
   - `func mutatingCommandNames() map[string]bool` = start from `destructiveCommandNames()`'s result, add every key of `applyRoutedAdditions`, then subtract `pendingApplyConversion` (step 5a). Mirror `destructiveCommandNames()`'s doc-comment discipline: the destructive half stays DERIVED from `surfaces.Operations()` and is never hand-typed; only the small routed-additions half is named, and it is named because it is not derivable.
   - `destructiveCommandNames()` STAYS — `mutatingCommandNames()` is built on it, and `firstDestructiveTopLevelCommandName` still needs it. The three sibling gates that still call it directly are migrated in 04-04 (see this plan's Verification section).

**5b. The pins and the `RequireApply` switch are DEFERRED — read this, do not write them here (INV-1).**

   Each measures something that does not exist at the end of Task 1, and Task 1's own `<verify>` compiles and runs the whole `cmd/engram` package:

   | Gate | What it needs | Lands in |
   |---|---|---|
   | `TestApplyRoutedAdditionsArePinned` | `surfaces.ClassForCommand("migrate")` to resolve | **Task 2** (with `migrate`'s toolclass row) |
   | `TestDestructiveCommandsRequireApply` switched to `mutatingCommandNames()` | `migrateCmd` to exist AND carry `--apply` | **Task 2** (with `migrateCmd`) |
   | `TestMutatingCommandNamesMembership` | `"migrate revert"` in `destructiveCommandNames()`, i.e. its `Destructive:true` toolclass row | **Task 3** (with `migrate revert`) |

   This task DECLARES the data; the later tasks GATE on it. Declaring is not gating: an unused package-level `var`/`func` is legal Go — only local variables error — and `task lint`'s `unused` check runs at wave boundaries, by which point all three are consumed. **Do not "helpfully" add the pins here because the helper is right in front of you.**

   For reference, the enumerated results those pins will assert: **end of Task 2 → exactly four** — `migrate`, `migrate-remap-owner`, `prune-expired`, `spine-review purge`, matching four live `--apply` carriers (`registerDestructive`'s callers: prune.go:159, spine_review_purge.go:425, migrate.go:257, plus Task 2's `migrate`); **end of Task 3 → exactly five**, adding `migrate revert`; **end of wave 4 → exactly six**, adding `backfill-short-ids` once 04-04 Task 1 gives it `--apply` and deletes `pendingApplyConversion`.

5a. Add the named, temporary exclusion `pendingApplyConversion` next to `mutatingCommandNames()`:
   - `var pendingApplyConversion = map[string]bool{"backfill-short-ids": true}` with a doc comment stating exactly: this command is a named `applyRoutedAdditions` member and therefore already in the mutating set, but its conversion to `registerDestructive` (and therefore its `--apply` flag) lands in plan 04-04, one wave later; this entry keeps the wave-3 tree green and **plan 04-04 Task 1 DELETES both this var and every reference to it**, in the same task that gives `backfill-short-ids` its `--apply` flag. `mutatingCommandNames()` subtracts this map last, after the union.
   - **Why this exists rather than switching everything at once:** `backfill-short-ids` is the last routed-additions member to gain `--apply`, and 04-04 owns `cmd/engram/backfill.go`. Switching the derivation in wave 3 without the exclusion makes `TestDestructiveCommandsRequireApply` RED by design-order, not by defect — a plan whose own wave cannot be committed green is not executable. A documented one-wave exception with a deletion owner is the honest form.
   - **It holds exactly one name, for exactly one wave.** If you find yourself adding a second entry to make a gate pass, the derivation is wrong and the exclusion list is the symptom — that is precisely how the `!ReadOnly` defect stayed invisible for three review cycles.
6. **DO NOT switch `TestDestructiveCommandsRequireApply` in this task — it moves to Task 2 (INV-1).** The switch replaces `want := destructiveCommandNames()` at destructive_test.go:89 with `want := mutatingCommandNames()`, which puts `migrate` in the want-set — but `migrateCmd` and its `--apply` flag are declared in **Task 2**, so at the end of THIS task the gate fires `mutating command "migrate" has no --apply flag`. Leave the test deriving from `destructiveCommandNames()` here; it stays green because nothing this task adds is `Destructive:true`.
7. EXTEND `TestDestructiveGatePreventsMutation` to also prove an additive (`Destructive:false`) mutating command is ADMITTED by the generalized gate. Derive the name rather than hardcoding it: add a `firstAdditiveRoutedTopLevelCommandName(t)` helper alongside `firstDestructiveTopLevelCommandName` (:45-54) that returns the lexicographically-first space-free member of `applyRoutedAdditions` whose `surfaces.ClassForCommand` row reports `Destructive == false` — i.e. it derives from the SAME named set the gate derives from, so the helper and the gate cannot drift apart. Run the same throwaway-command assertions (bare → preview only, `--apply=false` → preview only, `--apply` → apply once) so the additive path is proven behaviourally, not by flag presence.

In `internal/surfaces/rules.go`:
8. UPDATE `RuleDestructiveRequiresApply`'s `Sentence` (:232) — **unconditionally, this is not a judgement call (REVIEWS.md N5)** — from `"a destructive operator command previews by default and mutates only when apply is set"` to `"a mutating operator command previews by default and mutates only when apply is set"`. `addApplyFlag` (destructive.go:57-63) composes EVERY `--apply` Usage string from this Sentence, so the moment additive `migrate` carries `--apply`, the old wording is a factually wrong statement on a live `--help` surface. Keep the rule's `ID`, `Fields`, `Hint`, `TagForm`, and `declared` untouched — only the Sentence changes.
8a. UPDATE the pinned `wantSentence` const in `internal/surfaces/rules_test.go:56` to the same new string, by hand, in the SAME change. That const is a deliberate second declaration of the sentence and is the gate that catches a drive-by reword; it is not generated.
8b. Run `task surfaces:gen` and commit what it produces. It regenerates the anchored `<!-- engram:rule:start destructive-requires-apply -->` regions in `docs-site/src/content/docs/guides/cli.md:135` and `skill/engram/skills/curating-memory/SKILL.md:379` from the registry, then regenerates `cmd/engram/testdata/help.golden` (3 `--apply` usage lines) and `cmd/engram/testdata/catalog.golden` (3 `"usage"` values). **Do NOT hand-edit any of those four files** — the anchored regions are tool-owned, and `task surfaces:gen` is deliberately excluded from `task default`/`test`/`lint` precisely so this stays an explicit, reviewable step (Taskfile.yaml:257-264).
   **This is the FIRST of the wave's regenerations (C4-M2).** It captures the tree as it stands right now, with no migrate-family command registered yet. Tasks 2 and 3 each add commands, and `TestHelpGolden`/`TestCatalogGolden` walk the LIVE tree — so **each of those tasks runs `task surfaces:gen` again as its own final step**, because each ends with an unfiltered `go test ./cmd/engram/ -count=1` that the stale goldens would fail. Do not treat this step as sufficient, and do not move it: the Sentence change must propagate into `rules.go`'s consumers before Task 2's commands compose their `--apply` Usage strings from it.

**8c. Hand-edit the UNANCHORED destructive-command prose that `surfaces:gen` does NOT rewrite (ledger row D9 — REVIEWS.md C6-M5).** `task surfaces:gen` rewrites only the span between the `<!-- engram:rule:start destructive-requires-apply -->` markers (`docs-site/src/content/docs/guides/cli.md:135`), and `TestSurfaceConformanceProseFiles` checks anchored sentences, not neighbouring prose. The paragraph immediately below the anchor (`:136-140`) reads:

   > This applies to every command the [blast-radius](#blast-radius) table below classifies `destructive`: today, `prune-expired`, `migrate-remap-owner`, and `spine-review purge`.

   Both halves are wrong after this wave: the anchored sentence one line above now says **mutating**, not `destructive`, and the enumerated list omits `migrate revert` (a fourth destructive command, Task 3) and `migrate` (mutating, previews by default, classified non-destructive). Rewrite those two sentences to scope to the MUTATING set and to name the commands as of the end of this wave, and say explicitly that a mutating command may be `Destructive:false` — that is the D-16 generalization this phase exists to ship, and this is the operator-facing page that explains it. **Do NOT touch the anchored region itself** (`surfaces:gen` owns it), and do not renumber or restructure the section. Nothing tests this prose, which is precisely why it has an owner rather than a gate. 04-04 Task 3 owns the SECOND stale paragraph in the same section — the one claiming `backfill-short-ids` keeps a `--dry-run` preview idiom (`:143-150`) — because that only becomes false in wave 4.

9. **The `destructiveFlagCases` row for `migrateRevertCmd` lands in Task 3 (C4-M3 / INV-1).** It is keyed on the `migrateRevertCmd` Go identifier, which Task 3 declares; adding it here would fail this task's own package compile.
  </action>
  <acceptance_criteria>
    - **`go test ./cmd/engram/ -count=1` passes UNFILTERED at the end of this task**, and `go test ./internal/surfaces/ -count=1` passes. The unfiltered run is the INV-1 acceptance gate and is what proves this task is committable on its own.
    - `rg -n "return !class.ReadOnly" cmd/engram/destructive.go` finds the new ADMISSION predicate (this is the `!ReadOnly` that is CORRECT — see the objective's two-predicate note).
    - `rg -n "injectable|seam" cmd/engram/destructive.go` returns the rejected-alternative doc unchanged (no seam added).
    - A comment near the new predicate or on destructiveByClassification acknowledges the terminology debt (M9).
    - `rg -n "mutatingCommandNames" cmd/engram/destructive_test.go` finds the new helper and `rg -n "applyRoutedAdditions" cmd/engram/destructive_test.go` finds the named additive-routed set with per-entry justification.
    - **C4-H1's DERIVATION is declared here; its PINS are asserted in Tasks 2 and 3 (INV-1).** This task's criterion is only that the three declarations exist and are shaped correctly: `mutatingCommandNames()` is built ON `destructiveCommandNames()` rather than replacing it, `applyRoutedAdditions` holds exactly `{migrate, backfill-short-ids}` with per-entry justification, and `pendingApplyConversion` holds exactly `{backfill-short-ids}`.
      **A negative grep for the rejected predicate is deliberately NOT used, in any task (INV-2).** `! rg '!op\.Class\.ReadOnly'` would also match the legitimate `firstAdditiveRoutedTopLevelCommandName` helper, the CORRECT admission predicate in `destructive.go`, and any doc comment explaining the rejection. The membership pins are strictly stronger anyway: the rejected predicate yields ten names after the one-name exclusion, so exact set equality goes RED immediately and names every offender.
    - `TestDestructiveCommandsRequireApply` still derives from `destructiveCommandNames()` at the end of THIS task and is green; its switch to `mutatingCommandNames()` (M12) is Task 2, alongside `migrateCmd` and its `--apply`, which is what makes the switched want-set satisfiable.
    - TestDestructiveGatePreventsMutation has a second sub-test proving additive (Destructive:false) commands are admitted, with the name DERIVED from `surfaces.Operations()` rather than hardcoded.
    - N5 closed, with both halves STRING-LITERAL-shaped (INV-2 — the sentence is only ever a Go string literal, so a comment describing the change in prose cannot trip either): `rg -o '"a mutating operator command previews by default[^"]*"' internal/surfaces/rules.go internal/surfaces/rules_test.go | wc -l` returns 2 (the registry Sentence and its pinned `wantSentence`), and `rg -o '"a destructive operator command previews by default[^"]*"' internal/surfaces/rules.go internal/surfaces/rules_test.go | wc -l` returns 0. (Count with `-o | wc -l`, never `rg -c`, which counts LINES.)
    - After `task surfaces:gen`, `git status --porcelain` shows the four regenerated files changed and `git diff` shows the new sentence in the anchored cli.md/SKILL.md regions and in all six golden occurrences; no hand-edit is present in any of them.
    - `pendingApplyConversion` exists, holds EXACTLY ONE name (`backfill-short-ids`), has a doc comment naming plan 04-04 Task 1 as its deletion owner, and the `cmd/engram` suite is green with it in place.
    - `destructiveFlagCases` is NOT touched by this task — its `migrateRevertCmd` row lands in Task 3 alongside the identifier it references (C4-M3 closed).
    - **Ledger row D9 (first half) closed:** `docs-site/src/content/docs/guides/cli.md`'s destructive-command paragraph below the anchored region no longer says "every command the table below classifies `destructive`" and no longer enumerates a three-command list that omits the migrate family. `rg -n 'today, `prune-expired`, `migrate-remap-owner`, and `spine-review purge`' docs-site/src/content/docs/guides/cli.md` returns nothing. The anchored region itself is untouched by hand.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd/engram/ -count=1 &amp;&amp; go test ./internal/surfaces/ -count=1 &amp;&amp; go test ./cmd/engram/ -run 'TestDestructive|TestApplyFlagUsageComposesRuleSentence|TestHelpGolden|TestCatalogGolden|TestSurfaceConformanceCobraUsage' -count=1 -v &amp;&amp; rg -n "applyRoutedAdditions" cmd/engram/destructive_test.go &amp;&amp; rg -n "pendingApplyConversion" cmd/engram/destructive_test.go &amp;&amp; rg -n "return !class.ReadOnly" cmd/engram/destructive.go &amp;&amp; test "$(rg -o '"a mutating operator command previews by default[^"]*"' internal/surfaces/rules.go internal/surfaces/rules_test.go | wc -l | tr -d ' ')" = 2 &amp;&amp; test "$(rg -o '"a destructive operator command previews by default[^"]*"' internal/surfaces/rules.go internal/surfaces/rules_test.go | wc -l | tr -d ' ')" = 0</automated>
    <!-- INV-1: `TestMutatingCommandNamesMembership` and `TestApplyRoutedAdditionsArePinned` are
         DELIBERATELY ABSENT from this selector — they are created in Tasks 3 and 2 respectively,
         alongside the artifacts they measure, and naming a not-yet-existing test in `-run` silently
         selects nothing and reports PASS (INV-3). The leading unfiltered
         `go test ./cmd/engram/ -count=1` is what proves this task is committable on its own.
         INV-2: both sentence counts are STRING-LITERAL-shaped (`"…"`-anchored), so a comment
         recording the wording change cannot fail either one. -->

  </verify>
  <done>`registerDestructive`'s ADMISSION predicate is `!class.ReadOnly`, so it admits any mutating operator command (additive or destructive) while still refusing ReadOnly and unclassified commands, with tests proving both admission and refusal; the name debt is documented (M9); the NAMED union `mutatingCommandNames() = destructiveCommandNames() ∪ applyRoutedAdditions − pendingApplyConversion` is DECLARED (its exact-set pins and the `TestDestructiveCommandsRequireApply` switch land in Tasks 2 and 3 with the artifacts they measure — INV-1); the rule Sentence now reads "a mutating operator command …" across the registry, its pin, both anchored doc regions, and both goldens via `task surfaces:gen` (N5 closed); and `guides/cli.md`'s unanchored destructive-command paragraph, which `surfaces:gen` does not own, is rewritten to the mutating scope (D9). `go test ./cmd/engram/ -count=1` is green UNFILTERED, so this task is committable on its own.</done>
</task>

<task type="auto">
  <name>Task 2: engram migrate + engram migrate status — commands, toolclass rows, shared envelope, timeout, store seam (M7/M8), AND every registry keyed on `operatorCommands()` for those two commands, in this same task (INV-1)</name>
  <files>
    cmd/engram/migrate_family.go
    cmd/engram/migrate_family_test.go
    cmd/engram/operator_output_test.go
    cmd/engram/cmdwalk_test.go
    cmd/engram/operror_test.go
    cmd/engram/destructive_test.go
    cmd/engram/catalog_test.go
    internal/surfaces/toolclass.go
    docs-site/src/content/docs/guides/cli.md
    cmd/engram/testdata/help.golden
    cmd/engram/testdata/catalog.golden
  </files>
  <read_first>
    cmd/engram/spine_review_purge.go (the registerDestructive closure contract :314-378 — H5 PRECEDENT: spinePurgeApplyRun at :339-377 calls PreviewPurge INSIDE the apply closure, captures the manifest, then calls ApplyPurge with it — this is exactly what migrate's apply closure MUST do. NOT a package-level var.)
    cmd/engram/migrate.go:20-24,42-60,234-245 (migrateRemapReportDoc explicit-bool envelope; Deprecated :261; AND the ALREADY-BOUND `migrateTimeout` package var — the identifier this plan MUST NOT reuse)
    cmd/engram/operator_output.go (addOperatorOutputFlag/renderOperator single-document)
    cmd/engram/spine_review_purge.go:95-113 (the flag var block + spinePurgeWithTimeout — the "0 disables" helper shape migrateWithTimeout mirrors, PARAMETERIZED by duration because three leaves share it)
    cmd/engram/prune.go:44-52 (pruneWithTimeout — the second instance of the same zero-disables idiom)
    cmd/engram/operator_output_test.go:434-470 (the three published timeoutGroups + their zeroRejected semantics), :470-500 (timeoutGroupCaseArgs), :585-648 (TestTimeoutGroupMatrix — the SET-EQUALITY assertion a new --timeout-bearing command must satisfy)
    internal/surfaces/toolclass.go (backfill-short-ids row :194-200 Destructive:false Precedent; migrate-remap-owner Destructive:true)
    internal/store/migrate.go (MigrateOptions + MigrateResult — PRODUCED BY 04-01: MigrateOptions.{DryRun,Manifest}, MigrateResult.{PreviewManifest,Spared,Appeared}; there is deliberately NO WouldMigrate field, so the CLI derives the projection count as len(PreviewManifest) — N4)
    internal/store/revert.go (PRODUCED BY 04-02, see its "Artifacts this phase produces" ledger: `type RevertPlan`, `type IrreversibleStepRef`, `type UnsupportedVersionRef`, `func (s *Store) PreviewRevert(ctx, to) (RevertPlan, error)`, `func (s *Store) Revert(ctx, to) (RevertResult, error)`. This plan CONSUMES them and creates none of them — cycle-3 HIGH #1)
    internal/store/migrate_status.go (PRODUCED BY 04-02: `MigrateStatusResult{Buckets, Absent, Future []VersionBucket, FutureTotal, Total}` — the status report renders Future per version, M4)
    cmd/engram/catalog_test.go (both-directions gate :429-469)
    .planning/phases/04-migration-cli-first-customer/04-CONTEXT.md (D-05 subcommand split, D-06 new-file-per-family, D-07 rows, D-08 status, D-11 shared envelope)
    .planning/phases/04-migration-cli-first-customer/04-REVIEWS.md (H5 — in-apply-closure re-preview, NOT package var; H6 — manifest-based parity; H8 — preserve a finite timeout; M6 — leaf-only Use strings; M7 — store interface seam; M8 — PreviewRevert on store interface)
  </read_first>
  <action>
In a NEW file `cmd/engram/migrate_family.go` (any filename per D-06 — separate from the transient owner-migration commands in `cmd/engram/migrate.go`):

0. Define a narrow local store interface (REVIEWS.md M7 — mirror spineConsolidateStoreFromEnv pattern), including the exported PreviewRevert (M8):
   ```go
   type migrateFamilyStore interface {
       Migrate(ctx context.Context, opts store.MigrateOptions) (store.MigrateResult, error)
       MigrateStatus(ctx context.Context) (store.MigrateStatusResult, error)
       Revert(ctx context.Context, to migrate.Version) (store.RevertResult, error)
       PreviewRevert(ctx context.Context, to migrate.Version) (store.RevertPlan, error)  // REVIEWS.md M8 — exported preflight accessor
   }
   var migrateFamilyStoreFromEnv = func() (migrateFamilyStore, error) {
       return server.StoreFromEnv()
   }
   ```
   `store.RevertPlan` and `Store.PreviewRevert` are **created by plan 04-02** in `internal/store/revert.go` — see 04-02's "Artifacts this phase produces" ledger for the exact declared shape (`To int`, `Candidates uint64`, `Reversible bool`, `Irreversible []store.IrreversibleStepRef`, `Unsupported []store.UnsupportedVersionRef`). This plan neither declares nor modifies them, and `internal/store/revert.go` is deliberately NOT in this plan's `files_modified`. If, at execution time, `rg -n "func \(s \*Store\) PreviewRevert" internal/store/revert.go` finds nothing, STOP — 04-02 did not complete and wave 3 cannot compile (this is the exact cycle-3 HIGH #1 failure mode; do not paper over it by declaring the symbol here).
   This injectable constructor enables CLI tests to substitute a fake store without touching production routing. The `MUST` rule: production initialization uses the var verbatim (assigned in an `init()` or at the var's initializer); tests swap via `t.Cleanup(migrateFamilyStoreFromEnv = saved)`.

0b. Define the shared timeout helper — **it TAKES the duration; it does not hard-code one** (REVIEWS.md H8 + N3):
   ```go
   // migrateWithTimeout mirrors spinePurgeWithTimeout (spine_review_purge.go:107)
   // and pruneWithTimeout (prune.go:44): 0 disables the deadline. It takes d as a
   // parameter because three leaves share it — migrate, migrate revert, and the
   // backfill-short-ids alias (04-04) — each passing its own flag-backed var.
   func migrateWithTimeout(ctx context.Context, d time.Duration) (context.Context, context.CancelFunc) {
       if d > 0 {
           return context.WithTimeout(ctx, d)
       }
       return ctx, func() {}
   }
   ```
   - **Zero-semantics decision, stated explicitly (N3):** 0 DISABLES. This puts `migrate`/`migrate revert` in the published `zero-disables` group alongside `backfill-short-ids` (which already has exactly these semantics — backfill.go:39,80), NOT in the `reject-zero-operator` group that `migrate-remap-owner`/`migrate-set-owner` occupy. Rationale to record in the doc comment: the alias and the canonical command must not disagree about `--timeout 0`, and `backfill-short-ids`'s zero-disables behavior is already published in `guides/upgrade.md` §5 and `guides/cli.md`'s three-group table; flipping it would be a second undocumented break in a plan whose whole point is documenting one break.
   - Register the flag vars per leaf, **NOT** as a single shared `migrateTimeout` — that identifier is already bound to `migrate-set-owner` (cmd/engram/migrate.go:22, registered :239):
     - `migrateSweepTimeout time.Duration` for `migrate`
     - `migrateStatusTimeout time.Duration` for `migrate status` (REVIEWS.md C5-M6 — see step 3)
     - `migrateRevertTimeout time.Duration` for `migrate revert` (registered in Task 3)
     All three default to `5 * time.Minute` with the shipped usage string `"max wall-clock (0 disables); also cancellable via Ctrl-C"`, following `spineArchiveTimeout`/`spineRestoreTimeout`'s per-leaf precedent (spine_review_archive.go:236,243).
   - Every closure calls `ctx, cancel := migrateWithTimeout(ctx, <thisLeaf'sVar>); defer cancel()` BEFORE the first store call.

0c. Assign THIS TASK's two new `--timeout`-bearing commands to a published group in `cmd/engram/operator_output_test.go` — **mandatory, not optional**: `TestTimeoutGroupMatrix` (:592-648) asserts SET EQUALITY between `timeoutGroups`' union and every `--timeout`-bearing command in the live tree, so a new one with no assignment fails with "carries --timeout but is not assigned to any of the three published groups".
   - Add **`"migrate": true` and `"migrate status": true`** to the `zero-disables` group's `commands` map (:445-453), plus their matching `timeoutGroupCaseArgs` cases (each reaching its `--timeout` check with every other required flag satisfied and an unreachable Qdrant).
   - **Do NOT add `"migrate revert"` here (INV-1 / REVIEWS.md C5-H5's class).** The set equality runs in BOTH directions, so a group entry naming a command that is not yet in the live tree fails exactly as loudly as a live command with no entry — and `migrateRevertCmd` is declared in **Task 3**. This task's own `<verify>` runs `TestTimeoutGroupMatrix`, so adding it here makes this task unable to pass in isolation. Its entry and case land in Task 3 step 1a, alongside the command. (The prior revision added both here AND again in Task 3 step 1a — duplicated, and RED at Task 2 for the direction nobody was watching.)
   - Update the published three-group table in `docs-site/src/content/docs/guides/cli.md` — header at `:375`, the `zero-disables` row to edit at `:378` (ledger row D7; `TestTimeoutGroupMatrix`'s doc comment cites this table as its source of truth but never reads it back, so nothing will catch a stale one) — to list `migrate` and `migrate status` now, and `migrate revert` in Task 3. Do NOT loosen the set equality.

1. Define the shared migrate report envelope (D-11), the explicit-bool separate-field shape mirroring `migrateRemapReportDoc`/`purgeReportDoc`. Map from `store.MigrateResult`. Suggested doc struct: `type migrateReportDoc struct { Target int, WouldMigrate uint64, Migrated uint64, Failed uint64, Passes uint64, Backlog uint64, Spared uint64, Appeared uint64, DryRun bool }` with json tags mirroring the purge/remap precedent.
   **`Spared` and `Appeared` are `uint64` COUNTS in the CLI doc, derived at this mapping site as `uint64(len(res.Spared))` / `uint64(len(res.Appeared))`** — 04-01 declares them as `[]string` identity sets on `store.MigrateResult` (conforming to the shipped `PurgeResult` shape), and the operator-tier report renders the counts. **If you also surface the id lists in the json document, they MUST marshal as `[]`, never `null`:** `TestOperatorOutputEmpty` (`cmd/engram/operator_output_test.go:374-395`) drives every operator json-doc builder with a zero-valued result and asserts the marshalled bytes "must not contain \"null\" anywhere", and a nil `[]string` marshals to `null`. Initialize any slice field to an empty non-nil slice at construction. The simplest compliant choice — counts only, no id lists in the doc — is also the one that matches `pruneReportDoc`/`migrateRemapDoc`'s scalar shape; take it unless a requirement needs the ids. **`WouldMigrate` is populated from `len(res.PreviewManifest)` at the CLI boundary (REVIEWS.md N4)** — `store.MigrateResult` deliberately carries no such field (04-01 prohibition), so the projection count is derived here, once, and the doc comment says so. In an applied (non-DryRun) render `WouldMigrate` is the manifest count the apply acted from; in a preview render it is the projection and `Migrated` is 0. Never prose-inferred.

1a. Define the two SHARED sweep run funcs — these are what makes 04-04's alias genuinely thin (REVIEWS.md cycle-3 #7). Both take the leaf's own timeout so a caller cannot forget one:
   ```go
   func migrateSweepPreviewRun(ctx context.Context, cmd *cobra.Command, outputFlag string, timeout time.Duration) error
   func migrateSweepApplyRun(ctx context.Context, cmd *cobra.Command, outputFlag string, timeout time.Duration) error
   ```
   `migrateCmd`'s closures and (in 04-04) `backfillShortIDsCmd`'s closures BOTH call these same two functions. There is exactly one preview implementation and exactly one apply implementation for the v0→v1 sweep in the binary; "thin identical alias" is then a structural fact rather than a claim.

2. Build the `migrate` parent command (`migrateCmd`): `Use: "migrate"` (leaf-only — M6: the qualified path comes from parentage), `Short` naming the sweep. Register `--timeout` into `migrateSweepTimeout` (default 5m, "0 disables" — see step 0b; do NOT reuse the taken `migrateTimeout` identifier). Route it through `registerDestructive` (after `AddCommand`, per the contract) with two closures that are one-line adapters over the shared run funcs:
   - preview closure → `migrateSweepPreviewRun(ctx, cmd, migrateOutputFlag, migrateSweepTimeout)`.
   - apply closure → `migrateSweepApplyRun(ctx, cmd, migrateOutputFlag, migrateSweepTimeout)`.
   `migrateSweepPreviewRun` body: `operatorOutputFormat` → `migrateFamilyStoreFromEnv()` → `ctx, cancel := migrateWithTimeout(ctx, timeout); defer cancel()` → `st.Migrate(ctx, store.MigrateOptions{DryRun: true})` → `renderOperator(cmd, format, migrateSummary, migrateReportDoc)` with `WouldMigrate = len(res.PreviewManifest)` and `DryRun: true`.
   `migrateSweepApplyRun` body — H5 KEY CHANGE, the apply re-previews INSIDE itself, never from a package-level var:
     a) `operatorOutputFormat`, dial store, `ctx, cancel := migrateWithTimeout(ctx, timeout); defer cancel()`.
     b) `previewResult, err := st.Migrate(ctx, store.MigrateOptions{DryRun: true})` — a FRESH manifest, the same in-closure re-derivation `spinePurgeApplyRun` performs at spine_review_purge.go:365.
     c) `previewManifest := previewResult.PreviewManifest`.
     d) `res, err := st.Migrate(ctx, store.MigrateOptions{Manifest: previewManifest})` — migrates only the manifest ∩ fresh-re-derivation intersection (H6/SC3).
     e) Render with `WouldMigrate = len(previewManifest)`, `Migrated`/`Spared`/`Appeared` from `res`, `DryRun: false`.
     f) Single CLI invocation; the manifest never crosses a process boundary and no package-level var exists (H5 — mirrors purge precisely).
   Both paths derive options ONLY from the defaults (Target=CurrentVersion, Steps=Registry); Phase 4 passes neither field — do NOT add target/steps flags in this phase.
3. Build the `migrate status` command (`migrateStatusCmd`): `Use: "status"` (leaf-only — M6), `Short` naming the histogram; it is a READ-ONLY command (D-07) so it has an ordinary `RunE` (NOT routed through registerDestructive). **Body order matters** (step 3b(c) depends on it): `operatorOutputFormat` FIRST → `migrateFamilyStoreFromEnv()` → **`ctx, stop := signal.NotifyContext(cmd.Context(), os.Interrupt, syscall.SIGTERM); defer stop()`** → **`ctx, cancel := migrateWithTimeout(ctx, migrateStatusTimeout); defer cancel()`** → `st.MigrateStatus(ctx)` → `renderOperator(cmd, format, statusSummary, statusReportDoc)`.

   **`migrate status` CARRIES `--timeout` and signal handling even though it is read-only (REVIEWS.md C5-M6).** Register `--timeout` into `migrateStatusTimeout` (5m default, `"max wall-clock (0 disables); also cancellable via Ctrl-C"`), so its flag set is exactly `{output, timeout}`. "It keeps the command out of `TestTimeoutGroupMatrix`'s set" is NOT a reason to omit the flag — that gate exists to force a group assignment for a `--timeout`-bearing command, not to discourage the flag. Read-only is not a reason either: `spine-review scan` (spine_review_scan.go:55-59) and `spine-review verify` (spine_review_verify.go:639-643) are BOTH read-only and BOTH install `signal.NotifyContext` plus a `--timeout`-bounded context, and every member of all three published `timeoutGroups` carries `--timeout` (operator_output_test.go:445-465). Without them, a hung Qdrant makes `engram migrate status` block forever with no deadline and no graceful Ctrl-C — in a phase whose own H8/N3 findings are "every RPC path carries a finite deadline". Its `zero-disables` `timeoutGroups` row, `timeoutGroupCaseArgs` case, and `guides/cli.md` table entry all land in step 0c of this task.

   The report carries `buckets` + `absent` + **`future` as a per-version bucket LIST plus `future_total`** (04-02's `MigrateStatusResult.Future []VersionBucket` / `.FutureTotal` — M4) + `total`: a distribution, never a scalar, on both the pending and the future side. The text summary names the distinct future versions when any exist.

   **`statusReportDoc`'s `buckets` and `future` MUST be initialised to EMPTY NON-NIL slices at construction (REVIEWS.md C5-L8).** Both are `[]store.VersionBucket`; a zero-valued `MigrateStatusResult` leaves them nil, and a nil slice marshals to JSON `null`. `TestOperatorOutputEmpty` (cmd/engram/operator_output_test.go:374-395) drives every operator json-doc builder in a HAND-MAINTAINED map with a zero-valued result and asserts the marshalled bytes "must not contain \"null\" anywhere". **That map does NOT gain a `statusReportDoc` entry in this phase** — 04-04 Task 1 step 6c REPLACES the existing `backfill-short-ids` entry with `migrateReportDoc`; it adds no `migrate status` entry (C6-L4). So `TestOperatorOutputEmpty` is not a gate on this doc, and the ONLY gate is the direct unit assertion mandated below: **`func TestMigrateFamilyStatusReportDocNeverMarshalsNull(t *testing.T)` — that exact name (INV-3)** — which marshals `statusReportDoc` over a zero-valued `store.MigrateStatusResult` and asserts the bytes contain neither `"buckets":null` nor `"future":null`. Do it at construction (`if len(res.Buckets) == 0 { doc.Buckets = []store.VersionBucket{} }`, or an unconditional `make(..., 0, len(...))` copy), never by relaxing the assertion.
4. Register both under a parent (either `migrateCmd` containing `status`, or a shared parent command with `AddCommand`); match the `spine-review` nested-subcommand convention. Ensure each space-joined path (`migrate`, `migrate status`) has disjoint flags.

In `internal/surfaces/toolclass.go`:
5. Add rows, per D-07, one per space-joined path:
   - `migrate` = `{ReadOnly:false, Destructive:false, ...}` (additive-only ENFORCED by CheckAdditive; the same reasoning as the existing `backfill-short-ids` row).
   - `migrate status` = `{ReadOnly:true, Destructive:false, ...}`.
   Keep the existing `backfill-short-ids` row (D-07/RESEARCH Open Q3 — keep it so the deprecation-catalog surface stays both-directions green).

In `cmd/engram/catalog_test.go`:
6. Update `TestCatalogBlastRadiusMatchesToolClasses` (both-directions gate) to expect the two new rows AND the two new commands. Must land in the SAME change so the gate never observes a row without its command (or vice-versa). The `migrate` row routes through registerDestructive (which needs its row present at init), so add the row and the command together.

**6a. COMMAND-KEYED REGISTRY SWEEP for the two commands THIS task lands (ledger rows A1, A2, A3, A7, A22 — REVIEWS.md C4-H2, C5-H3).**

   `migrate` and `migrate status` satisfy `operatorCommands()`'s predicate the moment they exist: `migrate` gets a non-nil `RunE` from `registerDestructive` (`destructive.go:124`), `migrate status` declares its own, neither registers the client tier's `server` flag, and neither is in `operatorCommandExclusions` (`cmd/engram/cmdwalk.go:101-116`). **Every registry keyed on that derivation therefore changes NOW, in this task, not in Task 3** — this task's own `<verify>` runs `go test ./cmd/engram/ -count=1` unfiltered, and the four gates below are each bidirectional.

   Run the unfiltered package test and work the failures. Expect exactly these four, each firing twice (once per command):

   a) **`wantOperatorCommandKeys` + `TestOperatorCommands`** (`cmd/engram/cmdwalk_test.go:117-130`, bidirectional at `:155-165`). Add `"migrate": true` and `"migrate status": true`, and extend the doc comment (which currently enumerates the plan-03 spine-review leaves) to name them. Do NOT touch `operatorCommandExclusions` — the same test pins it to exactly `{"serve","version"}`, and neither new command belongs there.

   b) **`operatorParityRows()` + `TestOperatorOutputParity`** (`operator_output_test.go:137`, bidirectional against `commandKeySet(operatorCommands())` at `:316-326`). Add one row per command, built the way every existing row is — **ONE hand-built result value driven through BOTH the pure text formatter and the pure json-doc builder**, never two different values:
      - `migrate` → `migrateSummary(res)` / `migrateReportDoc(res)` over one `store.MigrateResult` fixture; `facts` naming the numbers the sentence states (would-migrate, migrated, backlog).
      - `migrate status` → `statusSummary(st)` / `statusReportDoc(st)` over one `store.MigrateStatusResult` fixture that includes at least one `Future` bucket, so the parity claim covers the M4 per-version rendering.
      Every `fact` must appear literally in the text sentence AND as a json scalar — the test asserts both directions.

   c) **`operatorInvalidOutputArgs()`** (`operator_output_test.go:532-563`), whose `default` branch is `t.Fatalf("operatorInvalidOutputArgs: no row defined for command %q")` at `:560`, reached from `TestEveryOperatorCommandRejectsInvalidOutput` (`:570-583`) which iterates EVERY `operatorCommands()` member. Add `["migrate"]` and `["migrate","status"]`, returning the minimal argv that reaches each command's own `operatorOutputFormat` call WITHOUT dialing a store. The driver appends `--output yaml` and asserts `exitUsage`, so each command's `operatorOutputFormat` call must happen BEFORE `migrateFamilyStoreFromEnv()`. If a closure dials first, the subtest fails with `exitUnavailable` and **the FIX IS THE COMMAND, not the test**.

   d) **`TestCatalogOutputFlagMatchesOperatorCommandsUnionClientVerbs`** (`cmd/engram/cmdwalk_test.go:194-228`) — decodes the committed `testdata/catalog.golden` and asserts SET EQUALITY between the golden's `--output`-bearing command set and `commandKeySet(operatorCommands()) ∪ {search, list, store}`. Both consequences are mandatory: **both new commands MUST register `--output`** (they do, per steps 2 and 3), and **the golden MUST be regenerated in this task** (step 6c below).

   e) **`operatorCommandFiles` + `TestNoBareOperatorErrorReturns`** (`cmd/engram/operror_test.go:179-186`, gate `:203-241`) — **ledger row A22.** Add `"migrate_family.go"` to the literal list. **This one does NOT appear in the failure output of the unfiltered run, and that is exactly why it matters:** the list is a hand-maintained literal, deliberately not a directory walk (its own doc comment says so), so an unlisted file does not make the gate RED — it makes the gate **silently stop covering** this file's operator commands and their `usageErrorf` validation. Note the list ALREADY contains `migrate.go`, the EXISTING owner-migration file, so the name similarity makes nothing self-correcting.
      **Also replace the list's doc-comment COUNT phrase (`operror_test.go:174`) with a non-counting sentence (C6-L2)** — it names a number of files that this edit invalidates, no test reads it, and incrementing a restated count just reschedules the staleness.
      **Prove it non-vacuously before moving on, because no failure will tell you:** after adding the entry, temporarily insert a bare `return fmt.Errorf("throwaway")` into `migrate_family.go`, run `go test ./cmd/engram/ -run TestNoBareOperatorErrorReturns -count=1 -v`, confirm it FAILS naming `migrate_family.go`, then revert. Record the observed failure text in the SUMMARY. Before the list entry, that experiment PASSES — which is the evidence the row exists for.

**6b. The two pins that become satisfiable in this task (INV-1).** In `cmd/engram/destructive_test.go`:
   - **`TestApplyRoutedAdditionsArePinned`** — for every name in `applyRoutedAdditions`, assert (i) `surfaces.ClassForCommand(name)` resolves (a stale entry naming a removed command fails), (ii) `!Class.ReadOnly`, and (iii) `!Class.Destructive` (a `Destructive:true` entry would be redundant with `destructiveCommandNames()` and therefore stale by construction — this catches a future reclassification instead of silently double-counting). The direct analogue of `TestOperatorCommands`' exclusion-set pinning (`cmdwalk_test.go:168-182`). **Step 5's `migrate` toolclass row is what makes (i) resolve**, which is why this pin lives here and not in Task 1.
   - **The `TestDestructiveCommandsRequireApply` switch (M12)** — replace `want := destructiveCommandNames()` at `destructive_test.go:89` with `want := mutatingCommandNames()`. Keep the two-direction structure identical; update the error strings from "destructive command" to "mutating command" and the doc comment to name the D-16 generalization **and the named-union derivation — NOT `!ReadOnly`**. **Both directions hold at the end of THIS task:** `mutatingCommandNames()` returns exactly `{migrate, migrate-remap-owner, prune-expired, spine-review purge}` (`migrate revert` has no toolclass row yet; `backfill-short-ids` is subtracted by `pendingApplyConversion`), and the live `--apply` carriers are exactly those same four. Task 3 adds the fifth on both sides in one edit.
   - **Do NOT add `TestMutatingCommandNamesMembership` here** — its pin must name `migrate revert`, whose toolclass row lands in Task 3. Adding it here pins four and forces a needless second edit; Task 3 declares it once, at five.

**6c. SECOND `task surfaces:gen` — the goldens must be regenerated in the task that added the commands (ledger rows A7, A11).** Task 1's run captured the tree BEFORE any migrate-family command existed. `TestHelpGolden`/`TestCatalogGolden` walk the LIVE tree (`golden_test.go:290`, `:306`), and step 6a(d) decodes the committed `catalog.golden` directly, so all three are RED at the end of this task without a regeneration — and this task's unfiltered run would fail. Run `task surfaces:gen` as the FINAL step and commit what it produces.
   - Do NOT hand-edit `cmd/engram/testdata/help.golden` or `catalog.golden`; `surfaces:gen` is deliberately excluded from `task default`/`test`/`lint` precisely so this stays an explicit, reviewable step (Taskfile.yaml:257-264).
   - `git diff` on the goldens must show TWO new command sections in `help.golden` and two new entries in `catalog.golden`, each carrying `output` in its flag list.
   - **After it, run `go test ./internal/server/ -run 'TestCuratingSpineVerbTableMatchesCuratingMemory|TestSupersedeDocsMatchShippedContract' -count=1`** (ledger rows D5/D6): `surfaces:gen` rewrites `curating-memory/SKILL.md`'s anchored region, which two `internal/server` gates read back for unrelated tables.

In `cmd/engram/migrate_family_test.go` — **every test this task creates carries the `TestMigrateFamily…` prefix (INV-3, REVIEWS.md C6-M10). A `-run 'TestMigrate…'` selector in `cmd/engram` also matches five shipped, unrelated owner-migration tests (`cmd/engram/migrate_test.go:71,86,104,133,145`), so it reports green off those five whether or not this task's tests exist. The prefix is what makes the selector able to go RED for the reason it was written:**
7. `TestMigrateFamilyPreviewAndApply` — CLI tests (cobra, inject a fake store via the `migrateFamilyStoreFromEnv` seam — M7): bare `migrate` renders a preview document and performs NO writes (fake store's Migrate gets DryRun:true); `migrate --apply` renders applied with Migrated/Spared/Appeared counts — AND proves the fake store's Migrate was called TWICE (once for DryRun, once for Manifest), which is the in-apply-closure re-preview (H5). `migrate status` renders a distribution; both render json|text via renderOperator.
8. `TestMigrateFamilyApplyIntersection` — preview/apply parity at the CLI boundary (REVIEWS.md H6/SC3): with a fake store whose DryRun returns a deterministic PreviewManifest, assert `--apply` acts only on the intersection of the previewed set and the fresh re-derivation. Test includes: a record that was previewed but is no longer eligible (Spared), a record that became eligible after preview (Appeared — NOT migrated), and a record that was previewed and is still eligible (Migrated). This proves identity-set intersection, NOT count-comparison.
9. `TestMigrateFamilyTimeoutWiring` — the flag must be READ, not merely registered (H8 + N3). Use a fake store that captures `ctx.Deadline()` on the context passed to `Migrate`, and assert all three cases behaviourally:
   - `migrate --timeout 1s`: a deadline exists AND is at most ~1s away (assert `time.Until(deadline) <= 2*time.Second`). **This is the assertion a hard-coded 5-minute helper cannot pass** — write the cheapest-passing-violator first: a `migrateWithTimeout(ctx)` that ignores the flag yields ~5m here and goes RED.
   - `migrate` with no `--timeout`: a deadline exists and is roughly 5 minutes away (assert `time.Until(deadline) > 4*time.Minute`), proving the default is applied rather than no deadline at all.
   - `migrate --timeout 0`: `ctx.Deadline()` reports `ok == false` — zero DISABLES, matching the published `zero-disables` group this command was assigned to in step 0c.
   Add the same three cases for `migrate status` here (its own `--timeout` — C5-M6) and for `migrate revert` in Task 3.
10. `TestMigrateFamilyReportFields` — the projection-field pin (REVIEWS.md N4): with a fake store whose DryRun returns a 7-entry `PreviewManifest`, assert the rendered JSON's `would_migrate` is 7 and `migrated` is 0 on a bare `migrate`, and that on `migrate --apply` `would_migrate` equals the manifest length the apply acted from. This pins which field carries the projection so no consumer has to guess.
  </action>
  <acceptance_criteria>
    - **`go test ./cmd/engram/ -count=1` passes UNFILTERED at the end of this task, and `go test ./internal/surfaces/ -count=1` passes (INV-1).** The unfiltered run is what makes step 6a checkable: three of its four gates are bidirectional set equalities reached by no `-run` selector this plan names, and step 6c's golden regeneration is required for it to be green at all.
    - `go test ./cmd/engram/ -run 'TestMigrateFamily|TestCatalogBlastRadiusMatchesToolClasses|TestOperatorCommands|TestOperatorOutputParity|TestEveryOperatorCommandRejectsInvalidOutput|TestCatalogOutputFlagMatchesOperatorCommandsUnionClientVerbs|TestTimeoutGroupMatrix|TestApplyRoutedAdditionsArePinned|TestDestructiveCommandsRequireApply|TestHelpGolden|TestCatalogGolden' -count=1 -v` passes. **The selector is `TestMigrateFamily`, not `TestMigrate` (C6-M10):** `TestMigrate` also matches five shipped, unrelated owner-migration tests, so it reports green whether or not this task's tests exist.
    - `rg -n '"migrate"|"migrate status"' internal/surfaces/toolclass.go` finds the two rows, and `rg -n 'CLICommand: "migrate"|CLICommand: "migrate status"'` confirms the space-joined keys.
    - `rg -n "Use: .status.|Use: .migrate." cmd/engram/migrate_family.go` shows leaf-only Use strings (M6): `"migrate"` for the parent, `"status"` for the child — NOT `"migrate status"`.
    - `rg -n "migrateFamilyStoreFromEnv" cmd/engram/migrate_family.go` shows the injectable constructor (M7).
    - `rg -n "PreviewRevert" cmd/engram/migrate_family.go` shows the method in the migrateFamilyStore interface (M8).
    - The preview/apply parity test proves identity-set intersection via Spared/Appeared counts (H6/SC3), AND proves the apply closure re-previewed internally (H5 — fake store's Migrate was called twice in the --apply path: once with DryRun, once with Manifest).
    - **N3 closed:** `rg -n "func migrateWithTimeout" cmd/engram/migrate_family.go` shows a signature taking a `time.Duration` parameter, and `rg -n "migrateWithTimeout\(ctx\)" cmd/engram/` returns nothing (no zero-arg call survives). The three-case deadline test passes, including `--timeout 1s` producing a ≤2s deadline — RED against any hard-coded duration.
    - **The `migrateTimeout` gate is CALL-SHAPED (INV-2):** `! rg -n '&migrateTimeout' cmd/engram/migrate_family.go` — the address-of form is the only way to register a flag into, or otherwise share, that package var, and a comment saying "not `migrateTimeout` — already bound to migrate-set-owner" cannot contain it. A bare-name `! rg -n "migrateTimeout"` would be failed by that very comment, which step 0b instructs the executor to write. Redeclaring the identifier is caught by the compiler regardless (same package), so `go build ./cmd/engram/...` is the second half of this property. `migrateTimeout` stays bound to `migrate-set-owner` (cmd/engram/migrate.go:22).
    - `go test ./cmd/engram/ -run TestTimeoutGroupMatrix -count=1 -v` passes with **`migrate` and `migrate status`** in the `zero-disables` group and the equality NOT loosened; `guides/cli.md`'s three-group table names both. **`migrate revert` is deliberately NOT in the group at the end of this task** — its command does not exist yet, and the equality's reverse direction would fail (INV-1); it joins in Task 3 step 1a.
    - **C5-M6 closed:** `rg -n "signal.NotifyContext" cmd/engram/migrate_family.go` finds the wiring in `migrateStatusCmd`'s `RunE`, `rg -n "migrateWithTimeout\(ctx, migrateStatusTimeout\)" cmd/engram/migrate_family.go` finds the deadline, and `migrateStatusCmd.Flags().Lookup("timeout") != nil` is asserted in a CLI test. The three-case deadline test (1s → ≤2s; default → ~5m; 0 → no deadline) runs for `migrate status` as well as `migrate`.
    - **C5-L8 closed, by the named direct assertion rather than by `TestOperatorOutputEmpty` (C6-L4):** `go test ./cmd/engram/ -run TestMigrateFamilyStatusReportDocNeverMarshalsNull -count=1` passes — it marshals `statusReportDoc` over a ZERO-valued `store.MigrateStatusResult` and confirms the bytes contain neither `"buckets":null` nor `"future":null`. `TestOperatorOutputEmpty`'s hand-maintained map gains no `migrate status` entry in this phase, so it is NOT a gate on this doc. **Write the cheapest-passing-violator first:** a doc built by plain struct-literal assignment from a zero result emits both nulls and goes RED here.
    - **N4 closed:** `rg -n "WouldMigrate" cmd/engram/migrate_family.go` shows the field derived from `len(...PreviewManifest)` at exactly the mapping site, and the report-field test pins `would_migrate == 7` / `migrated == 0` for a 7-entry preview.
    - `migrate status`'s report renders `future` as a per-version list plus `future_total` (M4 consumed end to end), never a single scalar.
    - The shared run funcs exist and are the single implementation: `rg -o "func migrateSweepPreviewRun|func migrateSweepApplyRun" cmd/engram/migrate_family.go | wc -l` returns 2, and `rg -l "store.MigrateOptions" cmd/engram/ --glob '!**/*_test.go'` names ONLY `cmd/engram/migrate_family.go` — no other non-test command file constructs sweep options, so no caller can re-implement the sweep with different semantics. (This is the file-set property; a per-occurrence count would be brittle against option-literal formatting.)
    - A grep of `cmd/engram/migrate_family.go` shows `registerDestructive(migrateCmd, ...)` and a plain `RunE` for `migrateStatusCmd` (read-only, not registerDestructive).
    - **C4-H2 closed for this task's two commands (ledger rows A1, A2, A3, A7):** `TestOperatorCommands` (`wantOperatorCommandKeys` names `migrate` and `migrate status`), `TestOperatorOutputParity` (two new `operatorParityRows` rows, each one result value through both formatters), `TestEveryOperatorCommandRejectsInvalidOutput` (two new `operatorInvalidOutputArgs` cases), and `TestCatalogOutputFlagMatchesOperatorCommandsUnionClientVerbs` (both commands carry `--output` and appear in the regenerated golden) are each green.
    - **C5-H3 closed (ledger row A22):** `rg -o '"migrate_family.go"' cmd/engram/operror_test.go | wc -l` returns 1; `operatorCommandFiles`' doc comment no longer restates a file count (C6-L2); and the executor recorded the RED-first observation — a temporary bare `return fmt.Errorf(...)` in `migrate_family.go` made `TestNoBareOperatorErrorReturns` FAIL naming that file, then was reverted. **Before the list entry, that experiment PASSES**, which is the evidence the gate had silently stopped covering the new commands rather than failing loudly.
    - **The two pins that became satisfiable here pass (INV-1):** `TestApplyRoutedAdditionsArePinned` (every `applyRoutedAdditions` entry resolves to a live `ReadOnly:false, Destructive:false` row) and `TestDestructiveCommandsRequireApply` switched to `mutatingCommandNames()` — green in BOTH directions against exactly four names and four live `--apply` carriers. `TestMutatingCommandNamesMembership` is deliberately NOT declared here; it lands in Task 3 at five.
    - **The goldens were regenerated in THIS task (step 6c):** `git diff cmd/engram/testdata/` shows two new command sections in `help.golden` and two new `--output`-bearing entries in `catalog.golden`, neither hand-edited, and `go test ./internal/server/ -run 'TestCuratingSpineVerbTableMatchesCuratingMemory|TestSupersedeDocsMatchShippedContract' -count=1` passes.
    - The apply closure re-previews inside itself rather than reading a package-level var (H5). **Gate, DECLARATION-shaped (INV-2):** `! rg -n '^var .*PreviewManifest' cmd/engram/migrate_family.go` — a package-level manifest var by any name is caught, and a comment explaining why none exists cannot trip it (a comment line begins `//`, never `var`). The real proof is behavioural: `TestMigrateFamilyPreviewAndApply` asserts the fake store's `Migrate` was called TWICE inside a single `--apply` invocation, first with `DryRun:true` then with a non-nil `Manifest`.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd/engram/ -count=1 &amp;&amp; go test ./internal/surfaces/ -count=1 &amp;&amp; go test ./internal/server/ -run 'TestCuratingSpineVerbTableMatchesCuratingMemory|TestSupersedeDocsMatchShippedContract' -count=1 &amp;&amp; go test ./cmd/engram/ -run 'TestMigrateFamily|TestCatalogBlastRadiusMatchesToolClasses|TestOperatorCommands|TestOperatorOutputParity|TestEveryOperatorCommandRejectsInvalidOutput|TestCatalogOutputFlagMatchesOperatorCommandsUnionClientVerbs|TestTimeoutGroupMatrix|TestApplyRoutedAdditionsArePinned|TestDestructiveCommandsRequireApply|TestNoBareOperatorErrorReturns|TestHelpGolden|TestCatalogGolden' -count=1 -v &amp;&amp; go build ./cmd/engram/... &amp;&amp; rg -n "PreviewRevert" cmd/engram/migrate_family.go &amp;&amp; rg -n "migrateWithTimeout\(ctx, migrateStatusTimeout\)" cmd/engram/migrate_family.go &amp;&amp; rg -n "func migrateWithTimeout\(ctx context.Context, d time.Duration\)" cmd/engram/migrate_family.go &amp;&amp; test "$(rg -o '"migrate_family.go"' cmd/engram/operror_test.go | wc -l | tr -d ' ')" = 1 &amp;&amp; { ! rg -n '&amp;migrateTimeout' cmd/engram/migrate_family.go; } &amp;&amp; { ! rg -n '^var .*PreviewManifest' cmd/engram/migrate_family.go; } &amp;&amp; task license:check</automated>
    <!-- INV-2: both negative gates match the PROPERTY, not a name. `&migrateTimeout` is the
         address-of form a flag registration or a shared binding requires, and `^var .*PreviewManifest`
         is a package-level declaration — a comment line begins `//` after its indent, so neither can
         be tripped by the rationale comments steps 0b and 1a instruct the executor to write. The
         zero-arg `migrateWithTimeout(ctx)` form needs no negative gate at all: the positive signature
         pin plus `go build` makes it a compile error.
         INV-1: `TestMutatingCommandNamesMembership` is deliberately absent — it is declared in Task 3
         at five names. `TestTimeoutGroupMatrix` passes here only because `"migrate revert"` was NOT
         added to `timeoutGroups` in this task.
         INV-3: the selector is `TestMigrateFamily`, not `TestMigrate` — the latter also matches five
         shipped owner-migration tests and cannot go RED for the reason it was written. -->

  </verify>
  <done>engram migrate previews (no writes) and --apply executes the sweep with in-apply-closure re-preview (H5) and manifest∩re-derivation intersection (H6/SC3); engram migrate status renders the histogram read-only; both emit the shared envelope, carry a finite timeout, and are registered in EVERY `operatorCommands()`-keyed registry in this same task — `wantOperatorCommandKeys`, `operatorParityRows`, `operatorInvalidOutputArgs`, `timeoutGroups`, `operatorCommandFiles`, the toolclass table, and the regenerated goldens; `TestApplyRoutedAdditionsArePinned` and the `TestDestructiveCommandsRequireApply` switch land here and pass in both directions at four names; the store interface seam (M7) with PreviewRevert (M8) and leaf-only Use strings (M6) are in place. `go test ./cmd/engram/ -count=1` is green UNFILTERED, so this task is committable on its own.</done>
</task>

<task type="auto">
  <name>Task 3: engram migrate revert --to &lt;v&gt; — command, toolclass row, exported PreviewRevert (M8), shared timeout, AND every registry keyed on `operatorCommands()` for THIS command, in this same task (INV-1)</name>
  <files>
    cmd/engram/migrate_family.go
    cmd/engram/migrate_family_test.go
    cmd/engram/operator_output_test.go
    cmd/engram/cmdwalk_test.go
    cmd/engram/destructive_test.go
    cmd/engram/catalog_test.go
    internal/surfaces/toolclass.go
    docs-site/src/content/docs/guides/cli.md
    cmd/engram/testdata/help.golden
    cmd/engram/testdata/catalog.golden
  </files>
  <read_first>
    cmd/engram/spine_review_purge.go (registerDestructive closure contract; usageErrorf validation :65)
    cmd/engram/client_common.go:220-235 (`usageErrorf` is declared at :251; `exitUsage = 2` is "usage or validation error" at :222, and `exitFindings`'s own doc at :227-235 scopes it to "the command itself succeeded". NOTE: `operror.go` holds `classifyOperatorErr`/`classifyOperatorErrConstruction`, NOT `usageErrorf`)
    internal/store/revert.go (PRODUCED BY 04-02 — `Store.PreviewRevert(ctx, to) (store.RevertPlan, error)` is the exported preflight accessor the CLI calls, never an unexported reversePreflight. Its contract: an unreversible range comes back as `(plan, nil)` with `plan.Reversible == false`, `plan.Irreversible` naming EVERY offending step, and `plan.Unsupported` naming EVERY unreachable stored version — a VERDICT, not a Go error)
    cmd/engram/migrate.go:20-24,239 (the ALREADY-BOUND `migrateTimeout` identifier — revert registers `migrateRevertTimeout` instead)
    cmd/engram/operator_output_test.go:434-470,585-648 (timeoutGroups + the set-equality matrix `migrate revert` must be assigned into)
    internal/surfaces/toolclass.go (migrate-remap-owner Destructive:true row precedent)
    cmd/engram/catalog_test.go (the both-directions gate must learn the migrate revert row + command here, same as Task 2)
    .planning/phases/04-migration-cli-first-customer/04-CONTEXT.md (D-13 whole-range preflight, D-14 refusal envelope, D-16 revert preview/apply parity via registerDestructive)
    .planning/phases/04-migration-cli-first-customer/04-REVIEWS.md (M6 — leaf-only Use strings for revert; M8 — shared exported PreviewRevert preflight accessor, NOT unexported reversePreflight from internal/store; H8 — timeout on revert too)
  </read_first>
  <action>
In `cmd/engram/migrate_family.go`:
1. Add the `migrate revert` command (`migrateRevertCmd`): `Use: "revert --to <v>"` (leaf-only — M6; the qualified path `migrate revert` comes from parentage), `Short` naming the reverse version; flags = `--output` + `--to` (an int) + `--timeout`. Register `--timeout` into a NEW var `migrateRevertTimeout` (default `5 * time.Minute`, usage `"max wall-clock (0 disables); also cancellable via Ctrl-C"`) — **NOT** `migrateTimeout`, which is already bound to `migrate-set-owner` at cmd/engram/migrate.go:22. Validate `--to` via `usageErrorf` (non-integer, negative, or inconsistent-with-chain → exit 2 usage error, D-16/precedent spine_review_purge.go:65); a bare `revert` without `--to` is a usage error.
1a. Add `"migrate revert": true` to the `zero-disables` group in `cmd/engram/operator_output_test.go`'s `timeoutGroups` (`:445-465`) and a matching `timeoutGroupCaseArgs` case supplying `--to 0` plus an unreachable Qdrant. **This entry belongs HERE, in the same task as `migrateRevertCmd`'s declaration (INV-1):** `TestTimeoutGroupMatrix`'s equality runs in BOTH directions, so an entry naming a command not yet in the live tree fails on the reverse direction, and Task 2's own unfiltered run would catch it. Also extend the `guides/cli.md` three-group table's `zero-disables` row (`:378`) with `migrate revert` (ledger row D7). (Ledger row A4.)

1b. In `cmd/engram/destructive_test.go`, add the `destructiveFlagCases` row for `migrateRevertCmd`: `{migrateRevertCmd, []string{"apply", "output", "timeout", "to"}}`. It belongs in THIS task because it is keyed on the `migrateRevertCmd` Go identifier declared in step 1 above — in Task 1 it would be an undefined identifier and Task 1's own `<verify>` compiles the package. `migrate revert` is classified `Destructive:true`, so it enters `destructiveCommandNames()` (and therefore `mutatingCommandNames()`) the moment its toolclass row lands in step 3, and `TestDestructiveCommandsExactFlagSet`'s "no row in destructiveFlagCases" branch (destructive_test.go:260-264) fails without it. Do NOT add rows for `migrate` or `backfill-short-ids` here — they are `Destructive:false`, so at wave 3 the table's REVERSE assertion (:265-269, "names %q which is not currently classified destructive") would fail; those rows land in 04-04 Task 2 together with the table's rename to `mutatingFlagCases` and its migration to the mutating derivation (REVIEWS.md N1). Derive the `want` slice by READING `migrateRevertCmd`'s actual registered flags from step 1, not by guessing. (Ledger row A14.)
2. Route it through `registerDestructive` (after AddCommand) with:
   - `revertPreview`: `operatorOutputFormat` → `migrateFamilyStoreFromEnv()` → `ctx, cancel := migrateWithTimeout(ctx, migrateRevertTimeout); defer cancel()` → `plan, err := st.PreviewRevert(ctx, to)`. A non-nil `err` is a genuine backend failure → `classifyOperatorErr`. Otherwise branch on the VERDICT: when `!plan.Reversible`, render the refusal — **built by calling 04-02's EXPORTED `store.RevertRefusalError(plan)` and rendering its `Error()` text, never by re-formatting the clauses here (REVIEWS.md C5-M4)** — which names the `field=steps hint=irreversible` clause for every entry in `plan.Irreversible` (From/To/reason) AND the `field=record_version hint=unsupported` clause for every entry in `plan.Unsupported` (version + count), both naming `snapshot` recovery (D-14). When `plan.Reversible`, render the reverse-plan summary including `plan.Candidates` as the count that would be reverted. Preview is read-only — no inverses are applied and `Revert` is never called. **`revertPreview` RETURNS `nil` on a refusal: a preview that correctly reports "this range cannot be reverted" SUCCEEDED, and exit 0 is right.**
   - `revertApplyRun`: call the SAME exported `st.PreviewRevert(ctx, to)` again inside this closure (the purge-precedent in-closure re-derivation — H5). **Only when `plan.Reversible` is true**, call `st.Revert(ctx, to)` and render the result. When `!plan.Reversible`, do NOT call `st.Revert` at all — zero records touched, and the fake-store test asserts `Revert` was never invoked.

**2a. A REFUSED `--apply` MUST EXIT NON-ZERO (REVIEWS.md C5-H4).**
   "Return the identical refusal rendering" is exit 0 end to end, and that is the trap: `renderOperator` returns the writer's error, which is `nil` on a successful render (cmd/engram/operator_output.go:64-75), and `registerDestructive`'s `RunE` is `if applyRequested(*target) { return apply(ctx, cmd) }` — it returns the closure's error verbatim (destructive.go:124-131). So `engram migrate revert --to 0 --apply` against the only registered step (which is `Irreversible` by design, D-03) would print a refusal and **exit 0**, and a script checking `$?` would record a completed migration. That contradicts this plan's own must_have ("refuses the entire operation") and REQ-migrate-revert's "refuses" criterion.

   The shape, and its shipped precedent — **render, THEN return a classified error**:
   ```
   refusal := store.RevertRefusalError(plan)          // 04-02's exported constructor (C5-M4)
   if rerr := renderOperator(cmd, format, revertSummary(plan), revertReportDoc(plan)); rerr != nil {
       return rerr
   }
   return usageErrorf("%s", refusal)                   // exitUsage (2)
   ```
   `spine_review_verify.go:659-662` does exactly this: `renderOperator(...)` and then `return verifyFailOnErr(report, spineVerifyFailOn)`, a `*cliError` carrying a non-zero code. `rootCmd` sets `SilenceUsage: true` / `SilenceErrors: true` (root.go:29-30) and `Execute` prints one clean `Error:` line to stderr, so the operator gets the full refusal document on stdout, the message on stderr, and exit 2 — the same triple `spine-review verify --fail-on` already produces.

   **Why `exitUsage` (2), argued against the shipped taxonomy rather than picked.** `client_common.go:222` defines exit 2 as "usage or validation error", and a range the registry cannot reverse IS a validation failure of the operator's `--to <v>` argument — which the refusal already says in the `field=<name> hint=<code>` grammar, naming `field=steps` or `field=record_version`. The same command's `--to` parsing already returns `usageErrorf` (step 1), so a malformed range and an unreversible range agree on the code an operator sees. The alternatives were considered and rejected on their own definitions: `exitFindings` (7) is scoped by its own doc to "a report command's own explicit opt-in flag … the command itself succeeded" (`client_common.go:227-235`) and this command did not succeed; `exitUnavailable` (5) means the backend could not service the request, and here the backend was never asked; a bespoke "refused" constant would add a row to `wantExitCodes` (catalog_test.go:231) and force an entry in `nonConnectProducedCodes` (`:347`) — ledger row A24 — for no operator-visible gain.

   **No `exitCodeBaseline` row is added, and `wantRows = 37` STANDS (ledger row A19).** That table's rows are `runClient` argv against client verbs with before/after exit codes; `migrate revert` is an operator command and does not fit its driver, and `TestExitCodeBaselineRowCount` is a COUNT pin plus name-uniqueness (`exitcode_baseline_test.go:440-446`), not a set-equality gate — so adding a row would require bumping the pin for a claim a dedicated CLI test states better. Pin the code in `migrate_family_test.go` instead (step 4 below), once per refusal class.

   Both preview and apply gate on the SAME exported `PreviewRevert` method (M8) — the CLI never lets `--apply` execute an irreversible or unsupported-version range (REQ-migrate-revert preview/apply parity). Note that `Store.Revert` runs the same whole-range preflight internally (04-02) and returns the SAME `RevertRefusalError`, so the CLI gate is defense in depth, not the only gate — and because both sides call one constructor, the two refusal strings cannot drift.
3. Add the `migrate revert` toolclass row `{ReadOnly:false, Destructive:true, ...}` (D-07; Destructive — inverses may remove keys, the anti-additive direction).

In `cmd/engram/catalog_test.go`:
3a. Extend `TestCatalogBlastRadiusMatchesToolClasses` (both-directions gate) to also expect the `migrate revert` row AND the `migrate revert` command, mirroring exactly what Task 2 did for `migrate`/`migrate status`. Must land in the SAME change as the row + command so the gate never observes a row without its command (or vice-versa). (Ledger row A8.)

**3b. COMMAND-KEYED REGISTRY SWEEP for `migrate revert` — the same four registries Task 2 handled for its two commands (ledger rows A1, A2, A3, A7).**

   `migrate revert` satisfies `operatorCommands()`'s predicate the moment it exists: `registerDestructive` gives it a non-nil `RunE` (`destructive.go:124`), it registers no `server` flag, and it is not in `operatorCommandExclusions` (`cmd/engram/cmdwalk.go:101-116`). Run `go test ./cmd/engram/ -count=1` (the WHOLE package, no `-run` filter) and work the failures:

   a) **`wantOperatorCommandKeys` + `TestOperatorCommands`** (`cmd/engram/cmdwalk_test.go:117-130`, bidirectional at `:155-165`). Add `"migrate revert": true` and extend the doc comment Task 2 already updated. Do NOT touch `operatorCommandExclusions` — the same test pins it to exactly `{"serve","version"}`.

   b) **`operatorParityRows()` + `TestOperatorOutputParity`** (`operator_output_test.go:137`, bidirectional against `commandKeySet(operatorCommands())` at `:316-326`). Add the `migrate revert` row: `revertSummary(...)` / `revertReportDoc(...)` over ONE hand-built fixture driven through both, `facts` naming `plan.Candidates` and the target version. Every `fact` must appear literally in the text sentence AND as a json scalar.

   c) **`operatorInvalidOutputArgs()`** (`operator_output_test.go:532-563`, fatal `default` at `:560`, driver `:570-583`). Add `["migrate","revert","--to","0"]` — the minimal argv reaching `operatorOutputFormat` WITHOUT dialing a store. The driver appends `--output yaml` and asserts `exitUsage`, so the format check must precede `migrateFamilyStoreFromEnv()`; if the closure dials first the subtest fails with `exitUnavailable` and **the FIX IS THE COMMAND, not the test**.

   d) **`TestCatalogOutputFlagMatchesOperatorCommandsUnionClientVerbs`** (`cmd/engram/cmdwalk_test.go:194-228`) — `migrate revert` MUST register `--output` (step 1) and MUST appear in the regenerated `catalog.golden` (step 6).

   `operatorCommandFiles` (row A22) needs NO edit here — Task 2 added `"migrate_family.go"` when it created the file, and this task adds no new file.

**3c. THE MEMBERSHIP PIN, declared here because this is the task that makes it reach five (INV-1).** Do it AFTER step 3, so the `migrate revert` toolclass row is live.
   - **`TestMutatingCommandNamesMembership`** — assert `mutatingCommandNames()` equals EXACTLY `{"migrate", "migrate revert", "migrate-remap-owner", "prune-expired", "spine-review purge"}` — five names — with a doc comment stating that 04-04 Task 1 deletes `pendingApplyConversion` and updates this pin to six by adding `backfill-short-ids`. A hand-known membership pin next to a derived helper is the shape `wantOperatorCommandKeys` already uses (`cmdwalk_test.go:117-130`), and it makes a future silent widening or narrowing visible in one line of diff. Declaring it here rather than in Task 2 avoids pinning four and then immediately re-pinning five.
   - `TestApplyRoutedAdditionsArePinned` and the `TestDestructiveCommandsRequireApply` switch already landed in **Task 2**, with `migrate`'s toolclass row and `--apply`. This task only makes their sets grow by one on both sides; both stay green with no edit.
   - **Verify against the live table before trusting the enumerated list.** If the pin fails, the enumerated set is what needs correcting — read the actual failure output, do NOT relax the pin. A failure naming seven UNRELATED commands (`store`, `reindex`, `summarize-missing`, `serve`, `migrate-set-owner`, `spine-review archive`, `spine-review restore`) means the rejected `!ReadOnly` predicate shipped after all; fix the derivation in Task 1's file rather than the pin.

In `cmd/engram/migrate_family_test.go` — **every test this task creates carries the `TestMigrateFamily…` prefix (INV-3, C6-M10):**
4. `TestMigrateFamilyRevertRefusals` and `TestMigrateFamilyRevertReversible` — CLI tests for revert (using the `migrateFamilyStoreFromEnv` fake seam — M7):
   - Irreversible range: fake `PreviewRevert` returns `(store.RevertPlan{Reversible:false, Irreversible:[{0,1,"snapshot recovery: ..."}]}, nil)`. Assert the rendered refusal names `field=steps`, `hint=irreversible`, `From=0`, `To=1`, the reason substring, and `snapshot`; and assert **on the fake store that `Revert` was called ZERO times**, on BOTH the bare invocation and the `--apply` invocation. A count of PreviewRevert calls is not the evidence — the never-called `Revert` is.
     **EXIT-CODE PINNING, both directions (REVIEWS.md C5-H4):** assert the BARE invocation returns a `nil` error (exit 0 — the preview succeeded at reporting the refusal) and the `--apply` invocation returns an error whose `ExitCode()` is `exitUsage` (2). Extract it the way `Execute` does — `var ec interface{ ExitCode() int }; errors.As(err, &ec)` — and assert `ec.ExitCode() == exitUsage`, never merely `err != nil`. **Write the cheapest-passing-violator first:** the prior revision's specified behaviour ("return the identical refusal rendering") returns `nil` from the apply closure and passes an `err != nil`-free assertion while exiting 0 — this assertion is the only thing that goes RED against it.
   - Unsupported-version range: fake `PreviewRevert` returns `Unsupported:[{Version:42, Count:3}]`. Assert the refusal names `field=record_version`, `hint=unsupported`, `42`, and `3`; that `Revert` was again never called; and **the SAME exit-code pair** (bare → nil/0, `--apply` → `exitUsage`). This proves the CLI surfaces BOTH refusal classes 04-02 can produce AND fails non-zero on both, not just the irreversible one.
   - **Refusal-text identity (REVIEWS.md C5-M4):** assert the CLI's rendered refusal contains the EXACT `Error()` string of `store.RevertRefusalError(plan)` built from the same plan value in the test — not a re-typed transcription. That is what proves the CLI consumes the exported constructor rather than re-formatting the clauses, and it goes RED against any hand-built format string that drifts by a word.
   - Reversible range: fake returns `Reversible:true, Candidates:5`. Bare invocation renders the reverse-plan summary naming 5 and calls `Revert` zero times; `--apply` calls `PreviewRevert` then `Revert` exactly once each, in that order, and renders the converged result.
   - `--to` with a non-integer or negative value returns a usage error (exit 2); `revert` with no `--to` returns a usage error.
   - The preflight call goes through the EXPORTED `PreviewRevert` method on `migrateFamilyStore` (M8), never an unexported internal/store helper.
5. `TestMigrateFamilyRevertTimeoutWiring` — the same three behavioural cases Task 2 step 9 specifies (H8 + N3), against a fake store that captures `ctx.Deadline()`: `--timeout 1s` → deadline ≤ ~2s away (RED against a hard-coded helper); no `--timeout` → deadline ~5m away; `--timeout 0` → `ctx.Deadline()` reports `ok == false` (zero disables, matching the assigned `zero-disables` group).

**6. `task surfaces:gen` — the goldens must be regenerated in the task that added the command (ledger rows A7, A11).**
   `TestHelpGolden` and `TestCatalogGolden` walk the LIVE tree (`golden_test.go:290`, `:306`), and step 3b(d) reads the committed `catalog.golden` directly, so both are RED at the end of this task without a regeneration — and this task's unfiltered run would fail. Run `task surfaces:gen` as the FINAL step and commit what it produces.
   - Do NOT hand-edit `cmd/engram/testdata/help.golden` or `catalog.golden`; `task surfaces:gen` is deliberately excluded from `task default`/`test`/`lint` precisely so this stays an explicit, reviewable step (Taskfile.yaml:257-264).
   - `git diff` on the goldens must show ONE new command section in `help.golden` and one new entry in `catalog.golden`, carrying `output` in its flag list — the second half of step 3b(d)'s equality.
   - **After it, run `go test ./internal/server/ -run 'TestCuratingSpineVerbTableMatchesCuratingMemory|TestSupersedeDocsMatchShippedContract' -count=1`** (ledger rows D5/D6). 04-04 Task 1 runs the phase's last regeneration for the alias's changed flag set — one per task that changes the tree.
  </action>
  <acceptance_criteria>
    - **`go test ./cmd/engram/ -count=1` passes with NO `-run` filter**, and `go test ./internal/surfaces/ -count=1` passes. This is the acceptance gate for step 3b: a `-run` selector is exactly what let four command-keyed registries stay invisible for three review cycles, because each is reached only by a test whose name matches none of this plan's selectors.
    - **C4-H2 closed for `migrate revert` (ledger rows A1, A2, A3, A7):** `TestOperatorCommands`, `TestOperatorOutputParity`, `TestEveryOperatorCommandRejectsInvalidOutput` and `TestCatalogOutputFlagMatchesOperatorCommandsUnionClientVerbs` are each green with the new key/row/case/golden entry. Combined with Task 2's, all three migrate-family leaves are now registered in every one of them.
    - `rg -o '"migrate revert"' cmd/engram/cmdwalk_test.go | wc -l` returns at least 1.
    - **C4-M2 closed:** `task surfaces:gen` was run as the FINAL step of this task; `git diff cmd/engram/testdata/` shows one new command section in `help.golden` and one new `--output`-bearing entry in `catalog.golden`; no golden was hand-edited; and `go test ./internal/server/ -run 'TestCuratingSpineVerbTableMatchesCuratingMemory|TestSupersedeDocsMatchShippedContract' -count=1` passes afterwards (ledger rows D5/D6).
    - **C4-M3 closed:** the `destructiveFlagCases` row for `migrateRevertCmd` is in THIS task (step 1b), not Task 1, and `rg -n "migrateRevertCmd" cmd/engram/destructive_test.go` resolves to the identifier this task declares.
    - **INV-1 closed as a CLASS:** every gate lives in the task that produces the artifact it measures. `go test ./cmd/engram/ -run 'TestMutatingCommandNamesMembership|TestApplyRoutedAdditionsArePinned' -count=1 -v` passes, pinning `mutatingCommandNames()` to exactly the five names live at the end of this task. **Tasks 1 and 2 each pass `go test ./cmd/engram/ -count=1` unfiltered on their own, and each of their `<verify>` blocks CONTAINS that unfiltered run** — that is the acceptance evidence for the class fix, not this task's green.
    - **C5-H4 closed:** `engram migrate revert --to <v> --apply` against an unreversible plan exits `exitUsage` (2) for BOTH refusal classes, asserted via `errors.As` on an `interface{ ExitCode() int }`, while the BARE invocation of the same range exits 0. `rg -n "usageErrorf" cmd/engram/migrate_family.go` finds the apply-path refusal return alongside the `--to` validation. No `exitCodeBaseline` row was added and `TestExitCodeBaselineRowCount`'s `wantRows = 37` is unchanged (ledger rows A19/A24) — `go test ./cmd/engram/ -run TestExitCodeBaseline -count=1` passes untouched.
    - **C5-M4 closed, by a GO ASSERTION rather than a grep (INV-2).** `rg -n "RevertRefusalError" cmd/engram/migrate_family.go` finds the calls in BOTH the preview rendering and the apply return (positive). The negative half — "the CLI declares no `field=`/`hint=` format string of its own" — has **no grep form that a rationale comment naming the hint codes would not fail**, so it is asserted in Go instead: the refusal-text identity assertion in step 4 requires the CLI's rendered refusal to CONTAIN the exact `Error()` string of `store.RevertRefusalError(plan)` built from the same plan value in the test. A hand-built format string that drifts by a word goes RED; one that does not drift is, by construction, indistinguishable from calling the constructor.
    - **C5-H3 closed (ledger row A22):** `rg -o '"migrate_family.go"' cmd/engram/operror_test.go | wc -l` returns 1, and the executor recorded the RED-first observation — a temporary bare `return fmt.Errorf(...)` in `migrate_family.go` made `TestNoBareOperatorErrorReturns` FAIL naming that file, then was reverted. **Before the list entry, that experiment PASSES** — which is the evidence that the gate had silently stopped covering the phase's three new operator commands rather than failing loudly.
    - **C5-L7 closed:** `task license:check` is green — `cmd/engram/migrate_family.go` and `cmd/engram/migrate_family_test.go` carry the two-line SPDX header; `guides/cli.md` and `curating-memory/SKILL.md` do NOT (both are excluded in `.licenserc.yaml`).
    - `go test ./cmd/engram/ -run 'TestMigrateFamily|TestCatalogBlastRadiusMatchesToolClasses' -count=1 -v` passes. **`TestMigrateFamily`, not `TestMigrate` (C6-M10)** — the latter also matches five shipped, unrelated owner-migration tests (`cmd/engram/migrate_test.go:71,86,104,133,145`) and reports green off those five regardless of this task's work.
    - `rg -n 'CLICommand: "migrate revert"' internal/surfaces/toolclass.go` finds the row; its `Class.Destructive` is `true`.
    - `rg -n "Use: .revert" cmd/engram/migrate_family.go` shows leaf-only Use string: `Use: "revert --to <v>"` (M6).
    - `rg -n "usageErrorf" cmd/engram/migrate_family.go` shows `--to` validation.
    - `rg -o "st\.PreviewRevert\(" cmd/engram/migrate_family.go | wc -l` returns 2 — one call in the preview closure, one in the apply closure (M8 closed; not an unexported reversePreflight, and not a single shared call hoisted out of the gate).
    - Both refusal classes are surfaced and tested: an `Irreversible` plan and an `Unsupported` plan each render their own `field=`/`hint=` envelope, and in BOTH cases the fake store records `Revert` called zero times.
    - The catalog both-directions gate stays green with all three rows + all three commands; `destructiveFlagCases` carries the `migrateRevertCmd` row added in **step 1b of this task** and `TestDestructiveCommandsExactFlagSet` is green.
    - `rg -n "migrateWithTimeout\(ctx, migrateRevertTimeout\)" cmd/engram/migrate_family.go` finds the wiring in both revert closures; `! rg -n '&migrateTimeout' cmd/engram/migrate_family.go` holds (call-shaped per INV-2 — see Task 2).
    - `go test ./cmd/engram/ -run TestTimeoutGroupMatrix -count=1` passes with `migrate revert` assigned to `zero-disables`.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd/engram/ -count=1 &amp;&amp; go test ./internal/surfaces/ -count=1 &amp;&amp; go test ./internal/server/ -run 'TestCuratingSpineVerbTableMatchesCuratingMemory|TestSupersedeDocsMatchShippedContract' -count=1 &amp;&amp; go test ./cmd/engram/ -run 'TestMigrateFamily|TestMutatingCommandNamesMembership|TestApplyRoutedAdditionsArePinned|TestNoBareOperatorErrorReturns|TestExitCodeBaseline|TestTimeoutGroupMatrix|TestDestructiveCommandsExactFlagSet|TestHelpGolden|TestCatalogGolden' -count=1 -v &amp;&amp; test "$(rg -o 'st\.PreviewRevert\(' cmd/engram/migrate_family.go | wc -l | tr -d ' ')" = 2 &amp;&amp; rg -n "RevertRefusalError" cmd/engram/migrate_family.go &amp;&amp; { ! rg -n '&amp;migrateTimeout' cmd/engram/migrate_family.go; } &amp;&amp; task license:check</automated>
    <!-- INV-2: the only negative gate here is CALL-SHAPED (`&migrateTimeout`, the address-of form a
         flag registration requires), which the rationale comment step 1 mandates cannot contain.
         There is deliberately NO grep for `hint=irreversible`/`hint=unsupported`: every form of it
         is failed by the plan's own mandated rationale comment, so the property is asserted in Go
         instead — step 4's refusal-text identity assertion binds the CLI's output to
         `store.RevertRefusalError`'s actual `Error()`.
         A `git diff --stat -- cmd/engram/testdata/` check is deliberately absent: it exits 0 on an
         empty diff, so it passes when the goldens were never regenerated. The golden property is
         enforced by `TestHelpGolden`/`TestCatalogGolden` inside the unfiltered run.
         INV-3: the selector is `TestMigrateFamily`, never `TestMigrate`. -->

  </verify>
  <done>engram migrate revert previews the reverse plan by default (via exported PreviewRevert — M8), refuses any irreversible range before a write (naming every step + reason + snapshot), applies only a fully-reversible range via registerDestructive, uses the shared migrateWithTimeout helper for a finite deadline (H8), and uses leaf-only Use strings (M6); all four `operatorCommands()`-keyed registries carry the three new commands (C4-H2), the `migrateRevertCmd` flag-case row lands with its identifier (C4-M3), and the goldens were regenerated after the last command landed (C4-M2).</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI → store (migrate/revert) | The CLI is the write choke point; unsafe/partial revert and preview-writes are the threats. The manifest bridge carries preview identity into apply — re-derived inside the apply closure (H5), never bridged across separate invocations. |
| classification table → routing | toolclass rows decide which command may mutate; a wrong row or a bypassed gate is a reclassification bypass. **Two predicates, never conflated (C5-M5):** `registerDestructive`'s ADMISSION predicate becomes `!class.ReadOnly` (correct — "may this command route through the gate?"); the `--apply`-REQUIRED set is the NAMED union `mutatingCommandNames() = destructiveCommandNames() ∪ applyRoutedAdditions − pendingApplyConversion` (M12 as corrected by C4-H1), **not** `!ReadOnly`, which selects eleven commands of which seven carry no `--apply`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-05 | Tampering | `registerDestructive` gate | high | mitigate | Predicate becomes `!class.ReadOnly`; classification stays DERIVED from toolclass.go (no injectable seam — rejected-alternative block enforced); ReadOnly commands are refused; TestDestructiveGatePreventsMutation stays green. Name debt documented (M9). |
| T-04-06 | Tampering | `migrate` preview writes | high | mitigate | Preview closure passes `MigrateOptions{DryRun: true}` (full-backlog page-through, never SetPayload); only the `--apply` closure uses Manifest with re-preview (H5 — DryRun then Manifest within the same invocation); registerDestructive calls exactly one branch. |
| T-04-06b | Tampering | `migrate` manifest bridge | high | mitigate | Apply closure re-previewes inside itself (the purge pattern — spine_review_purge.go:339-377; REVIEWS.md H5). No package-level var bridges separate invocations. The manifest is re-derived fresh within the apply invocation and consumed immediately. |
| T-04-07 | Tampering | `migrate revert` partial/unwanted revert | critical | mitigate | Same exported `PreviewRevert` helper (M8) runs in BOTH preview and apply before any write; an irreversible range refuses the entire op (field=steps hint=irreversible, naming each step + reason + snapshot); preflight logic is never duplicated in the CLI, and the refusal string comes from 04-02's single exported `store.RevertRefusalError` constructor (C5-M4). |
| T-04-07c | Repudiation | refused `--apply` reported as success | high | mitigate | **A refused `migrate revert --apply` returns a `usageErrorf`-classified error carrying `exitUsage` (2) AFTER rendering, on the shipped `spine_review_verify.go:659-662` render-then-return precedent (C5-H4).** Without it, `renderOperator` returning nil and `registerDestructive`'s `RunE` forwarding it verbatim (destructive.go:124-131) make a REFUSED MUTATION exit 0 — automation checking `$?` records a migration that never happened, and the operator-tier's exit-code contract silently stops distinguishing "did it" from "refused to". The bare preview of the same range still exits 0, because reporting a correct refusal IS success. Both refusal classes are exit-code-pinned by CLI test. |
| T-04-07b | Tampering | CLI store interface seam | low | mitigate | The `migrateFamilyStoreFromEnv` injectable constructor is test-only (M7); production initialization is at var level, no runtime override path exists outside tests. |
| T-04-08 | Denial of Service | migrate family unbounded ops | high | mitigate | A shared `migrateWithTimeout(ctx, d)` helper installs a 5-minute DEFAULT deadline on every migrate-family store call, taking the leaf's own flag-backed duration so `--timeout` is genuinely read (H8/N3). registerDestructive provides signal cancellation; this provides the deadline it does not. `--timeout 0` disables, consistent with the published zero-disables group the commands are assigned to; an operator wanting no bound must say so explicitly. |
| T-04-08b | Spoofing | stale `--apply` rule sentence on live help surfaces | low | mitigate | The `--apply` Usage string is composed from the registry Sentence (destructive.go:57-63), so leaving it as "a destructive operator command …" would tell an operator that an additive `migrate --apply` is destructive. The Sentence is updated unconditionally and re-anchored into cli.md, SKILL.md, and both goldens via `task surfaces:gen` (N5). |

</threat_model>

<verification>

After plan execution (wave 3):

- **`go test ./cmd/engram/ -count=1` green with NO `-run` filter**, and `go test ./internal/surfaces/ -count=1` green. The unfiltered run is mandatory: every registry in Conformance Registry Impact ledger section A is reached only by a test whose name matches none of this plan's `-run` selectors, which is precisely how four of them survived three review cycles.
- `go test ./cmd/engram/ -run 'TestMigrate|TestDestructive|TestOperatorCommands|TestOperatorOutputParity|TestEveryOperatorCommandRejectsInvalidOutput|TestCatalogOutputFlagMatchesOperatorCommandsUnionClientVerbs|TestCatalogBlastRadiusMatchesToolClasses|TestTimeoutGroupMatrix|TestApplyFlagUsageComposesRuleSentence|TestMutatingCommandNamesMembership|TestApplyRoutedAdditionsArePinned|TestHelpGolden|TestCatalogGolden' -count=1 -v` green (the named subset, for a readable failure report — never as a substitute for the unfiltered run above).
- `go vet ./cmd/engram/` clean; `task` green (which now includes rumdl over the edited docs-site markdown).
- `! rg -n '^var .*PreviewManifest' cmd/engram/migrate_family.go` holds (declaration-shaped — no package-level manifest var, H5); the behavioural proof is `TestMigrateFamilyPreviewAndApply`'s two-call assertion.
- `rg -n "PreviewRevert" cmd/engram/migrate_family.go` confirmed (M8 — exported preflight accessor, produced by 04-02).
- `rg -n "mutatingCommandNames|applyRoutedAdditions" cmd/engram/destructive_test.go` confirmed (M12 satisfied via the NAMED additive-routed union, not the rejected `!ReadOnly` predicate — C4-H1). The proof is `TestMutatingCommandNamesMembership`'s exact set equality, not a grep.
- `TestMutatingCommandNamesMembership` pins the end-of-wave result to exactly `{migrate, migrate revert, migrate-remap-owner, prune-expired, spine-review purge}`, and `TestDestructiveCommandsRequireApply` — switched in Task 2 — passes in both directions at four names at the end of T2 and five at the end of T3.
- All four `operatorCommands()`-keyed registries carry all three new commands (C4-H2) — `cmdwalk_test.go`'s `wantOperatorCommandKeys` and its catalog-golden `--output` equality, plus `operator_output_test.go`'s `operatorParityRows` and `operatorInvalidOutputArgs` — with each command's entries added in the SAME task as the command (INV-1).
- `! rg -n '&migrateTimeout' cmd/engram/migrate_family.go` holds (call-shaped); `rg -n "func migrateWithTimeout\(ctx context.Context, d time.Duration\)" cmd/engram/migrate_family.go` confirmed (N3).
- `task surfaces:gen` ran once per task that changed the tree (T1 for the Sentence, T2 for `migrate`/`migrate status`, T3 for `migrate revert`), each regeneration committed with the change that required it (N5); no anchored region was hand-edited. `guides/cli.md`'s UNANCHORED destructive-command paragraph was hand-repaired in T1 step 8c (ledger row D9), which `surfaces:gen` does not own.
- Real-binary smoke check: `go run ./cmd/engram migrate` against a test Qdrant renders a preview document with no writes; `go run ./cmd/engram migrate revert --to 0` renders the irreversible refusal naming the v0→v1 step + snapshot; `go run ./cmd/engram migrate --help` shows the new "a mutating operator command …" `--apply` sentence.
- REVIEWS.md H5, H6 (CLI half), H8, M6, M7, M8, M9, M12 closed, plus cycle-3 N3 (timeout wiring), N4 (projection field), N5 (rule sentence pinned), and the consuming half of cycle-3 HIGH #1.
- Deferred by design to 04-04, with a named owner (REVIEWS.md N1): migrating `TestDestructiveCommandsRouteThroughGate`, `TestDestructiveCommandsExactFlagSet`, and `TestApplyFlagUsageComposesRuleSentence` to `mutatingCommandNames()` (04-04 Task 2), and deleting `pendingApplyConversion` (04-04 **Task 1**, in the same task that gives the alias `--apply`). Rationale: `backfill-short-ids` is (ReadOnly:false, Destructive:false) and does not gain `--apply` until 04-04, so switching those derivations in wave 3 would make wave 3 uncommittably RED by ordering rather than by defect. `TestDestructiveCommandsRequireApply` is NOT in that deferral — it switches in this plan's Task 2, because `migrate` gains `--apply` there.

</verification>

<success_criteria>

- `engram migrate` previews only, `--apply` is the write choke point using in-apply-closure re-preview (H5) + manifest ∩ fresh re-derivation (REQ-migrate-command, REQ-migrate-preview-apply-parity SC3, H6).
- `engram migrate status` renders a server-side distribution with future-version handling (REQ-migrate-status-histogram).
- `engram migrate revert --to` previews via exported PreviewRevert (M8), refuses irreversible ranges whole, applies only reversible ones (REQ-migrate-revert).
- Every migrate-family mutating command has a finite 5-minute DEFAULT deadline that its own `--timeout` flag actually changes, with 0 disabling — shared `migrateWithTimeout(ctx, d)` helper, per-leaf vars, and a `timeoutGroups` assignment (H8/N3).
- One sweep implementation (`migrateSweepPreviewRun`/`migrateSweepApplyRun`) that 04-04's alias reuses verbatim, so alias parity is structural (cycle-3 #7).
- Leaf-only cobra `Use` strings (M6), injectable store interface seam consuming 04-02's exported `PreviewRevert` (M7/M8), and documented name debt (M9) in place.
- `TestDestructiveCommandsRequireApply` derives its want-set from the NAMED union `mutatingCommandNames()` (M12 as corrected by C4-H1 — **not** `!ReadOnly`, which is `registerDestructive`'s separate and legitimate ADMISSION predicate), admitting additive migrate commands in the `--apply` set, with the one-wave `pendingApplyConversion` exclusion 04-04 removes.
- A refused `migrate revert --apply` exits `exitUsage` (2) for both refusal classes while its bare preview exits 0 (C5-H4), and the refusal string has exactly one constructor — 04-02's exported `store.RevertRefusalError` (C5-M4).
- `migrate status` carries `--timeout` + `signal.NotifyContext` like every other Qdrant-dialing operator command including the read-only ones (C5-M6), and its report doc's `buckets`/`future` slices marshal as `[]`, never `null` (C5-L8).
- `cmd/engram/migrate_family.go` is registered in `operatorCommandFiles`, so `TestNoBareOperatorErrorReturns` actually covers the phase's three new operator commands (C5-H3, ledger row A22) — proven by a RED-first experiment, because this gate fails silently rather than loudly.
- **Every task's `<verify>` CONTAINS `go test ./cmd/engram/ -count=1` and passes it unfiltered (INV-1)** — that, not a claim in this section, is what makes "no task forward-references a later task's artifact" checkable.
- The canonical `--apply` rule sentence reads "a mutating operator command …" across the registry, its pin, both anchored doc regions, and both goldens (N5).

</success_criteria>

## Artifacts this phase produces

- `cmd/engram/destructive.go`: `destructiveByClassification` now returns `!class.ReadOnly`; updated panic message + doc; M9 debt comment.
- `cmd/engram/destructive_test.go`: new `applyRoutedAdditions` NAMED set (`{migrate, backfill-short-ids}`, per-entry justification); new `mutatingCommandNames()` = `destructiveCommandNames() ∪ applyRoutedAdditions − pendingApplyConversion` (**not** the rejected `!ReadOnly` predicate — C4-H1); new `pendingApplyConversion` map (temporary, holds exactly one name, DELETED by 04-04 Task 2); new `TestApplyRoutedAdditionsArePinned` and `TestMutatingCommandNamesMembership` (the enumerated five-name wave-3 pin, updated to six by 04-04 Task 2 step 1a); new `firstAdditiveRoutedTopLevelCommandName(t)` helper (derives from `applyRoutedAdditions`, so helper and gate cannot drift); updated `TestDestructiveCommandsRequireApply` using the mutating set (M12); extended `TestDestructiveGatePreventsMutation` for additive commands; new `destructiveFlagCases` row for `migrateRevertCmd` (added in **Task 3**, not Task 1 — C4-M3).
- `cmd/engram/cmdwalk_test.go`: `wantOperatorCommandKeys` gains `migrate` + `migrate status` (T2) and `migrate revert` (T3); its doc comment extended to name the Phase-4 leaves. `operatorCommandExclusions` is untouched (still exactly `{serve, version}`). `TestCatalogOutputFlagMatchesOperatorCommandsUnionClientVerbs` passes against the regenerated `catalog.golden`.
- `cmd/engram/operror_test.go` (T2): `operatorCommandFiles` gains `"migrate_family.go"` (ledger row A22), and the list's doc-comment file COUNT is replaced with a non-counting sentence (C6-L2).
- `internal/surfaces/rules.go`: `RuleDestructiveRequiresApply.Sentence` changed to `"a mutating operator command previews by default and mutates only when apply is set"` (N5, unconditional). ID/Fields/Hint unchanged.
- `internal/surfaces/rules_test.go`: pinned `wantSentence` const updated to match (hand-edited, not generated).
- Regenerated by `task surfaces:gen` (never hand-edited): anchored `destructive-requires-apply` regions in `docs-site/src/content/docs/guides/cli.md` and `skill/engram/skills/curating-memory/SKILL.md`; `cmd/engram/testdata/help.golden`; `cmd/engram/testdata/catalog.golden`.
- `docs-site/src/content/docs/guides/cli.md`: additionally, the published three-group `--timeout` table's `zero-disables` row (`:378`) gains `migrate` and `migrate status` (T2) and `migrate revert` (T3), and the UNANCHORED destructive-command paragraph below the anchored region (`:136-140`) is rewritten to the mutating scope and the current command set (T1 step 8c, ledger row D9). All three edits are by hand — none is an anchored region, so `surfaces:gen` will not do them.
- `cmd/engram/migrate_family.go` (new file):
  - `type migrateFamilyStore` interface (`Migrate`, `MigrateStatus`, `Revert`, `PreviewRevert`) + `var migrateFamilyStoreFromEnv` injectable constructor (M7).
  - `func migrateWithTimeout(ctx context.Context, d time.Duration) (context.Context, context.CancelFunc)` — **takes the duration; 0 disables** (H8/N3).
  - `var migrateSweepTimeout time.Duration`, `var migrateStatusTimeout time.Duration`, `var migrateRevertTimeout time.Duration`, plus the per-leaf `--output` vars — note `migrateTimeout` is NOT reused (it is bound to `migrate-set-owner` at cmd/engram/migrate.go:22).
  - `func migrateSweepPreviewRun(ctx, cmd, outputFlag, timeout) error` and `func migrateSweepApplyRun(ctx, cmd, outputFlag, timeout) error` — **the single sweep implementation, also called by 04-04's backfill alias** (cycle-3 #7).
  - `migrateCmd`, `migrateStatusCmd`, `migrateRevertCmd` with leaf-only `Use` strings (M6); `revertPreview`/`revertApplyRun` closures calling the exported `PreviewRevert` (M8) and branching on `plan.Reversible`.
  - `type migrateReportDoc` (with `WouldMigrate` derived from `len(PreviewManifest)`, and `Spared`/`Appeared` derived as `uint64(len(...))` — N4/C4-L6), `type statusReportDoc` (with per-version `future` + `future_total` — M4), `type revertReportDoc`; and their text formatters, named on the shipped `pruneSummary` (prune.go:122) pattern: **`func migrateSummary(...) string`, `func statusSummary(...) string`, `func revertSummary(...) string`**. These three names are load-bearing across plans: Tasks 2 and 3 build `operatorParityRows()` entries from them, and **04-04 Task 1 step 6c re-points the surviving `backfill-short-ids` parity row and the `TestOperatorOutputEmpty` doc map at `migrateSummary`/`migrateReportDoc`** when it deletes `backfillSummary`/`backfillReportDoc`. Each `*Summary` func and its matching `*ReportDoc` builder must be PURE (result value in, string / doc out, no store, no `*cobra.Command`) — `operatorParityRows` drives both from one hand-built fixture with no I/O.
- `cmd/engram/operator_output_test.go`: `migrate`, `migrate status` (T2) and `migrate revert` (T3) added to the `zero-disables` `timeoutGroups` entry + matching `timeoutGroupCaseArgs` cases (A4); three new `operatorParityRows()` rows, each one result value through both the text and json formatters (A2); three new `operatorInvalidOutputArgs()` cases `["migrate"]`, `["migrate","status"]`, `["migrate","revert","--to","0"]` (A3).
- `internal/surfaces/toolclass.go`: three rows added — `migrate` (R:false D:false), `migrate status` (R:true D:false), `migrate revert` (R:false D:true). All three carry `MCPTool: ""` (CLI-only), so `TestToolAnnotationsBothDirections` is unaffected (ledger row A21).
- `cmd/engram/testdata/{help,catalog}.golden`: regenerated via `task surfaces:gen` at the END of EACH task that changed the tree — T1 (Sentence), T2 (`migrate`, `migrate status`), T3 (`migrate revert`) — because each task's own unfiltered `go test ./cmd/engram/ -count=1` walks the live tree and would otherwise be RED (C4-M2 / ledger rows A7, A11).

## Registry rows this plan owns

Canonical table: **Conformance Registry Impact ledger** in `04-01-PLAN.md`.
Owned here (all wave 3). **Every command-keyed row is split across T2 and T3 so each entry lands in
the same task as its command (INV-1)** — `migrate` + `migrate status` in T2, `migrate revert` in T3:
**A1** `wantOperatorCommandKeys`, **A2** `operatorParityRows`, **A3** `operatorInvalidOutputArgs`,
**A4** `timeoutGroups`/`timeoutGroupCaseArgs`, **A7**
`TestCatalogOutputFlagMatchesOperatorCommandsUnionClientVerbs`, **A8**
`TestCatalogBlastRadiusMatchesToolClasses`, **A11** help/catalog goldens (one regeneration per task
that changes the tree: T1, T2, T3).
Task-specific: **A12** `TestDestructiveCommandsRequireApply` (derivation declared in T1, SWITCH in
**T2** with `migrateCmd`'s `--apply`), **A14** `destructiveFlagCases` (`migrateRevertCmd` row, T3),
**A22** `operatorCommandFiles` (**T2**, with the file it names), **A20** `ValidateOperations`
duplicate-CLICommand, **D1/D2/D3** rule-sentence pin + anchored regions + prose conformance (T1),
**D7** `guides/cli.md`'s three-group `--timeout` table (T2 step 0c + T3 step 1a), **D9** (first half)
`guides/cli.md`'s unanchored destructive-command paragraph (T1 step 8c).
Self-maintaining, verify-only: **A9**, **A10**.
Constraints observed here: **A16** (no hand-written `--apply` Usage restating the rule sentence),
**A17** (no flag Usage claiming exclusivity without a declared cobra flag group), **A19** (no new
`exitCodeBaseline` rows — the C5-H4 refusal reuses the shipped `exitUsage`, so `wantRows = 37`
stands), **A21** (`MCPTool` stays `""` on all three new toolclass rows), **A23** (none of the three
new commands is added to `operatorCommandExclusions`), **A24** (no new exit-code CONSTANT is
declared, so `wantExitCodes`/`nonConnectProducedCodes` are inert), **B7** (`migrate_family.go` must
not name the type `qdrant.Client`), **D5/D6** (`task surfaces:gen` rewrites only the anchored
`destructive-requires-apply` region of `curating-memory/SKILL.md`; the verb-selection table and the
supersede worked examples that two `internal/server` gates read back must stay untouched — run
`go test ./internal/server/ -run 'TestCuratingSpineVerbTableMatchesCuratingMemory|TestSupersedeDocsMatchShippedContract' -count=1` after EACH regeneration).
- Tests: extended `TestDestructive*` (M12 gate update); new migrate-family CLI tests with in-apply-closure re-preview (H5), identity-set parity (H6), three-case timeout wiring (H8/N3), `would_migrate` field pinning (N4), both revert refusal classes, and exported `PreviewRevert` (M8); updated `TestCatalogBlastRadiusMatchesToolClasses`.

Symbols this plan CONSUMES from sibling plans (must resolve before wave 3 can compile):

| Symbol | Produced by |
|---|---|
| `store.MigrateOptions.DryRun` / `.Manifest`, `store.MigrateResult.PreviewManifest` / `.Spared` / `.Appeared` | 04-01 |
| `store.MigrateStatusResult` (incl. `Future []VersionBucket`, `FutureTotal`), `Store.MigrateStatus` | 04-02 |
| `store.RevertPlan`, `store.IrreversibleStepRef`, `store.UnsupportedVersionRef`, `Store.PreviewRevert`, `store.RevertResult`, `Store.Revert` | 04-02 |
| **`store.RevertRefusalError(plan) error`** — the EXPORTED single constructor of the D-14 envelope; consumed by BOTH `revertPreview`'s rendering and `revertApplyRun`'s non-zero return (C5-M4 / C5-H4). If `rg -n "func RevertRefusalError" internal/store/revert.go` finds nothing at execution time, STOP — 04-02 did not complete; do NOT re-format the clauses here. | 04-02 |

Symbols this plan produces that a LATER plan consumes:

| Symbol | Consumed by |
|---|---|
| `migrateWithTimeout(ctx, d)` | 04-04 Task 1 (alias closures pass `backfillTimeout`) |
| `migrateSweepPreviewRun` / `migrateSweepApplyRun` | 04-04 Task 1 (the alias delegates to these, which is what makes preview/apply parity identical — cycle-3 #7) |
| `migrateReportDoc` + its summary func | 04-04 Task 1 (shared envelope, D-11) |
| `migrateFamilyStoreFromEnv` | 04-04 Task 1 (alias dials through the same seam) |
| `mutatingCommandNames()` + `pendingApplyConversion` | 04-04 Task 2 (widens the sibling gates; DELETES `pendingApplyConversion`) |

<output>
Create `.planning/phases/04-migration-cli-first-customer/04-03-SUMMARY.md` when done
</output>