---
phase: 14-embedder-model-options-eval
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs-site/src/content/docs/guides/embedding-models.md
  - docs-site/src/content/docs/guides/embedding-instructions.md
  - charts/engram/values.yaml
autonomous: true
requirements:
  - REQ-embed-model-docs
must_haves:
  truths:
    - "guides/embedding-models.md renders an at-a-glance comparison table (model | base URL | dim | query instruction | reindex note) PLUS per-provider copy-paste env blocks for OpenRouter, Gemini, OpenAI, and local TEI/Ollama/vLLM — the local blocks are CONCRETE and complete (each pairs base URL + exact server-side model id + dim + query instruction), not left 'operator-chosen' (D-11, D-12; review B7). All API-key assignments are shell-safe quoted placeholders (`export ENGRAM_OPENAI_API_KEY='replace-with-your-key'`), never unquoted angle-bracket tokens (review B6)."
    - "Every recipe calls out that a model or dimension change requires `engram reindex`: Markdown recipes LINK `/guides/reindex/`; Helm comment blocks REFERENCE the reindex guide route (comments cannot hyperlink) (D-14; review B11)."
    - "The Gemini recipe uses the instruction-prefix env vars (ENGRAM_EMBED_QUERY_INSTRUCTION / ENGRAM_EMBED_DOCUMENT_INSTRUCTION), NOT the task_type *_PARAMS mechanism, and ships gemini-embedding-2 at native full 3072 dims (D-01, D-02, research supersession)."
    - "charts/engram/values.yaml keeps its neutral uncommented default (ollama/bge-m3) and adds commented-out OpenRouter/Gemini/OpenAI recipe blocks with dim + instruction + reindex note (D-13)."
  artifacts:
    - docs-site/src/content/docs/guides/embedding-models.md
    - docs-site/src/content/docs/guides/embedding-instructions.md
    - charts/engram/values.yaml
  key_links:
    - "embedding-instructions.md ↔ embedding-models.md cross-links (clean split: which model + how to connect vs how to tune instructions — D-11)."
    - "Every recipe cross-links guides/reindex (the Phase-13 embedder-config-identity reindex boundary — D-14)."
    - "The stale 'Google Gemini / Vertex' task_type row in embedding-instructions.md is corrected to the instruction-prefix mechanism for the OpenAI-compat path (research supersession, Pitfall 12)."
---

<objective>
Document every supported embedding model as copy-paste operator recipes and make
the reindex boundary unmissable — REQ-embed-model-docs (#337).

Create a new `guides/embedding-models.md` (D-11) with a comparison table (D-12)
and per-provider env blocks for OpenRouter, Gemini, OpenAI, and local
TEI/Ollama/vLLM, using the exact base URLs, model ids, dims, and query
instructions from RESEARCH § Provider Recipe Matrix. Add commented recipe blocks
to `charts/engram/values.yaml` (D-13). Every recipe calls out `engram reindex`
on model/dim change, cross-linking guides/reindex (D-14).

**Framing (load-bearing):** recipes are EQUAL operator choices — there is no
blessed/standardized production model or provider. Present the concrete configs
(OpenRouter-hosted qwen3, the Gemini config) as the eval/CI reference only, never
a mandated recommendation.

Purpose: An operator can point engram at any supported provider from a
copy-paste block and knows exactly when a reindex is required.
Output: One new docs page, a corrected cross-linked existing page, and
self-documenting Helm recipe comments.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-embedder-model-options-eval/14-CONTEXT.md
@.planning/phases/14-embedder-model-options-eval/14-RESEARCH.md
@docs-site/src/content/docs/guides/embedding-instructions.md
@docs-site/src/content/docs/guides/reindex.md
@charts/engram/values.yaml

SPDX scope note (verified against .licenserc.yaml): `docs-site/**` is EXCLUDED
from the license header check (Astro `.md` opens with YAML frontmatter on line
1). The new `embedding-models.md` MUST open with the `--- title: ... --- `
frontmatter like the sibling guides — do NOT add an SPDX comment. `charts/**`
is likewise outside the SPDX paths. `task` (lint incl. rumdl markdown lint) must
be clean at commit.

Model-id caveat: RESEARCH flags a doc-lag between Google's native guide
(`gemini-embedding-2`, Stable) and its OpenAI-compat sample
(`gemini-embedding-2-preview`). Write the recipe with `gemini-embedding-2`; plan
14-03's human-verify checkpoint confirms the exact compat-endpoint id via a live
curl and corrects this file if it differs.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create guides/embedding-models.md (comparison table + per-provider env blocks + eval-run recipe)</name>
  <files>docs-site/src/content/docs/guides/embedding-models.md</files>
  <read_first>
    - docs-site/src/content/docs/guides/embedding-instructions.md (Starlight frontmatter style, table style, the /guides/reindex/ cross-link form, the Qwen3 / bge / E5 / cloud rows)
    - docs-site/src/content/docs/guides/reindex.md (the exact route to cross-link: /guides/reindex/)
    - .planning/phases/14-embedder-model-options-eval/14-RESEARCH.md § Standard Stack / Provider Recipe Matrix (verified base URLs, model ids, native dims, query instructions) and § Architecture Patterns Pattern 1/Pattern 2 (the Gemini + qwen3@4096 env blocks)
  </read_first>
  <action>
    Create the page with Starlight frontmatter (`title: Embedding model recipes`
    and a one-line `description`) — no SPDX header (docs-site is license-exempt).
    Open with the operator-choice framing: recipes are equal choices; the
    concrete configs are the eval/CI reference, not a mandated default. Add an
    at-a-glance comparison table with columns: Provider | Model id | Base URL |
    Native dim | Query-side mechanism | Reindex on change. Populate rows from
    RESEARCH § Provider Recipe Matrix — present every row as an equal operator
    choice, not a recommended default; qwen3@4096 is one option among several AND
    doubles as the #261 eval reference (D-10). OpenRouter `qwen/qwen3-embedding-8b` @
    `https://openrouter.ai/api/v1` dim 4096; Gemini `gemini-embedding-2` @
    `https://generativelanguage.googleapis.com/v1beta/openai` dim 3072; OpenAI
    `text-embedding-3-large` @ `https://api.openai.com/v1` dim 3072 (`-small`
    1536); local providers as CONCRETE complete rows (review B7 — not
    "operator-chosen"): TEI model `BAAI/bge-m3` @ `http://<host>:8080/v1` dim 1024
    empty query instruction; Ollama model `bge-m3` @ `http://<host>:11434/v1` dim
    1024 empty query instruction; vLLM model `BAAI/bge-m3` @
    `http://<host>:8000/v1` dim 1024 empty query instruction (each names the exact
    server-side model id that runtime expects, mirroring the shipped
    `ollama/bge-m3`@1024 default). Then per-provider copy-paste env blocks. Every
    API-key line MUST be shell-safe: `export
    ENGRAM_OPENAI_API_KEY='replace-with-your-key'` (quoted single-quoted
    placeholder). Do NOT use an unquoted angle-bracket placeholder after the `=`:
    POSIX shells parse unquoted angle brackets as redirection tokens, so an
    unquoted placeholder is a syntax error in a block labelled "copy-paste"
    (review B6). For local providers whose gateway tolerates no key, set it to an
    empty quoted value (`export ENGRAM_OPENAI_API_KEY=''`) and say so. Each block
    also sets ENGRAM_EMBED_MODEL, ENGRAM_EMBED_DIM, ENGRAM_OPENAI_BASE_URL, and:
    for Gemini set ENGRAM_EMBED_QUERY_INSTRUCTION='task: search result | query:
    {query}' and ENGRAM_EMBED_DOCUMENT_INSTRUCTION='title: none | text:
    {document}' (instruction-prefix — the mechanism gemini-embedding-2 requires,
    D-01/D-02/D-03); for OpenRouter qwen3 set ENGRAM_EMBED_QUERY_INSTRUCTION='Given
    a web search query, retrieve relevant passages that answer the query'
    (documents raw); for OpenAI leave ENGRAM_EMBED_QUERY_INSTRUCTION empty
    (symmetric); for TEI/Ollama/vLLM leave ENGRAM_EMBED_QUERY_INSTRUCTION empty
    (bge-m3 is symmetric). After each block, one sentence: changing the model or
    dim requires `engram reindex` — link `/guides/reindex/` (D-14). Add a "Running
    the retrieval eval (optional)" section documenting the exact env + the
    `task eval:retrieval` command for BOTH the Gemini differ run and the qwen3@4096
    recall run (D-06/D-08 — from RESEARCH Pattern 1/Pattern 2, including
    ENGRAM_RETRIEVAL_EVAL=1). `task eval:retrieval` now runs the differ assertion
    because plan 14-01 named it `TestRetrievalEval_AsymmetryDiffer`, which the
    task target's `-run TestRetrievalEval` regex substring-matches (review finding
    A). For a Gemini-only differ run that skips the gh261 recall case, document the
    targeted form `ENGRAM_RETRIEVAL_EVAL=1 go test ./internal/retrievaleval/ -run
    TestRetrievalEval_AsymmetryDiffer -v`, and note it still needs Docker (or
    `ENGRAM_QDRANT_TEST_ADDR`) because the package TestMain starts Qdrant on the
    eval flag (review B9). Cross-link `/guides/embedding-instructions/` for
    instruction-tuning mechanics (D-11). Keep API keys as
    `replace-with-your-key`-style quoted placeholders throughout (never a real
    key — T-14-01).
  </action>
  <verify>
    <automated>F=docs-site/src/content/docs/guides/embedding-models.md; test -f "$F" && grep -q 'generativelanguage.googleapis.com/v1beta/openai' "$F" && grep -q 'openrouter.ai/api/v1' "$F" && grep -q '/guides/reindex/' "$F" && grep -q 'ENGRAM_EMBED_QUERY_INSTRUCTION' "$F" && grep -q 'replace-with-your-key' "$F" && grep -q '11434' "$F" && grep -q 'bge-m3' "$F" && grep -q 'TestRetrievalEval_AsymmetryDiffer' "$F" && ! grep -q 'API_KEY=<' "$F"</automated>
  </verify>
  <acceptance_criteria>
    - `docs-site/src/content/docs/guides/embedding-models.md` exists, opens with Starlight `--- title/description ---` frontmatter, and carries NO SPDX header.
    - It contains a comparison table with a row for each of OpenRouter, Gemini, OpenAI, and local (TEI/Ollama/vLLM) pairing base URL + model + dim + query mechanism + reindex note.
    - The local (TEI/Ollama/vLLM) recipes are CONCRETE and complete: each names an exact server-side model id (`BAAI/bge-m3` for TEI/vLLM, `bge-m3` for Ollama), dim `1024`, the concrete base URL, and an empty query instruction — none is left "operator-chosen" (review B7).
    - The Gemini env block sets `ENGRAM_EMBED_QUERY_INSTRUCTION` and `ENGRAM_EMBED_DOCUMENT_INSTRUCTION` (instruction-prefix) with model id `gemini-embedding-2` and dim `3072`.
    - The qwen3 env block sets model `qwen/qwen3-embedding-8b`, dim `4096`, base URL `https://openrouter.ai/api/v1`, and a query instruction.
    - Every recipe cross-links `/guides/reindex/`; a "Running the retrieval eval" section documents the `task eval:retrieval` command with `ENGRAM_RETRIEVAL_EVAL=1` AND the targeted `-run TestRetrievalEval_AsymmetryDiffer` differ command (review finding A).
    - Every API-key line is a shell-safe quoted placeholder `export ENGRAM_OPENAI_API_KEY='replace-with-your-key'`; no unquoted angle-bracket placeholder (`API_KEY=<…>`) appears (review B6).
    - Lint gate is SCOPED (review B2): `docs-site/**` is rumdl-EXEMPT (`.rumdl.toml` exclude), so the new page is validated by the content greps above (and the Astro build is a manual doc-correctness step per VALIDATION.md); the whole-tree `task lint` is not a gate here because it fails on pre-existing `.planning/` markdown noise.
  </acceptance_criteria>
  <done>The recipes page renders with the full provider matrix, instruction-prefix Gemini recipe, eval-run commands, placeholder-only secrets, and reindex cross-links.</done>
</task>

<task type="auto">
  <name>Task 2: Add commented recipe blocks to charts/engram/values.yaml</name>
  <files>charts/engram/values.yaml</files>
  <read_first>
    - charts/engram/values.yaml (the memory.embed.* block: model/dim/queryInstruction/queryParams/documentParams/documentInstruction at lines 21-38, and memory.openai.* baseURL/apiKeySecret at lines 39-43)
    - .planning/phases/14-embedder-model-options-eval/14-RESEARCH.md § Provider Recipe Matrix (values to embed as comments)
  </read_first>
  <action>
    Keep the neutral uncommented default exactly as-is (model `ollama/bge-m3`,
    dim 1024 — D-13; the default choice is deliberately neutral/symmetric).
    Directly under the existing `memory.embed` comment block, add commented-out
    recipe blocks (each a `# ...` comment group, not active YAML keys) for
    OpenRouter qwen3-embedding-8b @4096, Gemini gemini-embedding-2 @3072, and
    OpenAI text-embedding-3-large @3072. Each commented block lists the
    model/dim/queryInstruction (and, for Gemini, documentInstruction using the
    instruction-prefix values from RESEARCH — NOT queryParams/task_type) plus the
    matching memory.openai.baseURL, and ends with a one-line note: changing model
    or dim requires `engram reindex` — REFERENCE the reindex guide route
    `guides/reindex` (a YAML comment cannot hyperlink, so it names the route
    rather than "linking" it — review B11). Reference guides/embedding-models for
    the full recipes. Do NOT inline any API key — point to the existing
    `memory.openai.apiKeySecret` secretKeyRef indirection; if a block shows a key
    env at all, it uses the same shell-safe quoted placeholder convention as the
    docs (`'replace-with-your-key'`), never an unquoted angle-bracket token
    (T-14-01; review B6). Keep yamlfmt-clean formatting.
  </action>
  <verify>
    <automated>grep -q 'gemini-embedding-2' charts/engram/values.yaml && grep -q 'qwen/qwen3-embedding-8b' charts/engram/values.yaml && grep -q 'reindex' charts/engram/values.yaml && grep -q 'ollama/bge-m3' charts/engram/values.yaml && helm lint charts/engram</automated>
  </verify>
  <acceptance_criteria>
    - `charts/engram/values.yaml` keeps the uncommented default `model: "ollama/bge-m3"` / `dim: 1024`.
    - Commented recipe blocks for OpenRouter (`qwen/qwen3-embedding-8b`), Gemini (`gemini-embedding-2`), and OpenAI (`text-embedding-3-large`) are present, each with dim + instruction + a reindex note that REFERENCES the `guides/reindex` route (comments reference the route; they do not hyperlink — review B11).
    - The Gemini commented block uses `queryInstruction`/`documentInstruction` (instruction-prefix), not `queryParams`/`documentParams`.
    - No API key is inlined — blocks reference `memory.openai.apiKeySecret`. `helm lint charts/engram` and `task lint:yaml` (yamlfmt) exit 0 (scoped gate — full `task lint` blocks on pre-existing `.planning/` rumdl noise; review B2).
  </acceptance_criteria>
  <done>values.yaml stays self-documenting: neutral default retained plus commented OpenRouter/Gemini/OpenAI recipes with reindex notes and secretKeyRef indirection.</done>
</task>

<task type="auto">
  <name>Task 3: Cross-link the two guides and correct the stale Gemini task_type row</name>
  <files>docs-site/src/content/docs/guides/embedding-instructions.md</files>
  <read_first>
    - docs-site/src/content/docs/guides/embedding-instructions.md (the intro paragraph, the "Cloud models — asymmetry is an API parameter" section, and its `| Google Gemini / Vertex | {"task_type":"RETRIEVAL_QUERY"} / {"task_type":"RETRIEVAL_DOCUMENT"} |` row)
    - .planning/phases/14-embedder-model-options-eval/14-RESEARCH.md § Summary + § Common Pitfalls Pitfall 1 (gemini-embedding-2 does NOT honor task_type via the OpenAI-compat endpoint; use the instruction-prefix mechanism)
    - docs-site/src/content/docs/guides/embedding-models.md (the new page to cross-link)
  </read_first>
  <action>
    Add a cross-link near the top of embedding-instructions.md pointing to
    `/guides/embedding-models/` for "which model + how to connect" (the clean
    D-11 split: this page stays about instruction/param tuning mechanics).
    Correct the "Cloud models" section's `Google Gemini / Vertex` row: the
    OpenAI-compat `/v1/embeddings` endpoint engram calls does NOT honor
    `task_type` for `gemini-embedding-2` (research supersession, Pitfall 12).
    Update that row (or add a note beside it) to direct Gemini users to the
    instruction-prefix mechanism (ENGRAM_EMBED_QUERY_INSTRUCTION /
    ENGRAM_EMBED_DOCUMENT_INSTRUCTION) and link the new embedding-models page for
    the exact Gemini recipe. Leave the Cohere / Voyage / Jina rows unchanged
    (their task_type/input_type/task params remain correct). Do not alter the
    file's frontmatter shape; no SPDX (docs-site is license-exempt).
  </action>
  <verify>
    <automated>grep -q '/guides/embedding-models/' docs-site/src/content/docs/guides/embedding-instructions.md && grep -q 'embedding-instructions' docs-site/src/content/docs/guides/embedding-models.md</automated>
  </verify>
  <acceptance_criteria>
    - `embedding-instructions.md` cross-links `/guides/embedding-models/`, and `embedding-models.md` cross-links back to `/guides/embedding-instructions/` (bidirectional D-11 split).
    - The Gemini guidance in embedding-instructions.md no longer presents `task_type` params as the OpenAI-compat path for `gemini-embedding-2`; it directs to the instruction-prefix mechanism.
    - The Cohere / Voyage / Jina param rows are unchanged. `embedding-instructions.md` is under `docs-site/**` (rumdl-EXEMPT), so this edit is validated by the cross-link greps above, not the whole-tree `task lint` (which blocks on pre-existing `.planning/` noise — review B2).
  </acceptance_criteria>
  <done>The two guides cross-link cleanly and the stale Gemini task_type guidance is corrected to the instruction-prefix mechanism.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs / Helm values → operators & public repo | Recipe examples are committed to a public repo and rendered on a public docs site; an inlined secret would be disclosed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01 | Information Disclosure | embedding-models.md env blocks + values.yaml recipe comments | medium | mitigate | All API keys are shell-safe quoted placeholders (`export ENGRAM_OPENAI_API_KEY='replace-with-your-key'`) in docs — never a real key and never an unquoted angle-bracket token (review B6); Helm comments reference the existing `memory.openai.apiKeySecret` `secretKeyRef` indirection — never an inline secret (mirrors the existing values.yaml pattern). |
| T-14-SC | Tampering | npm/pip/cargo installs | low | accept | N/A — docs/config only; no package installs (RESEARCH § Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `docs-site/src/content/docs/guides/embedding-models.md` exists with the full provider matrix (concrete local TEI/Ollama/vLLM rows — review B7), instruction-prefix Gemini recipe, shell-safe quoted key placeholders (review B6), and eval-run commands (including the `-run TestRetrievalEval_AsymmetryDiffer` differ command — review finding A).
- Bidirectional cross-links between embedding-models.md and embedding-instructions.md resolve; every recipe cross-links `/guides/reindex/` (Helm comments reference the route — review B11).
- `charts/engram/values.yaml` keeps the neutral default and adds commented OpenRouter/Gemini/OpenAI recipes with reindex-route references.
- No inline API keys anywhere. Scoped lint (review B2): `task lint:yaml` + `helm lint charts/engram` + `task license:check` exit 0; `docs-site/**` is rumdl-exempt so the docs pages are validated by content greps, not the whole-tree `task lint`.
</verification>

<success_criteria>
REQ-embed-model-docs is satisfied: a new docs-site recipes page + commented Helm
recipes document OpenRouter/Gemini/OpenAI/local models, each pairing base URL +
model + dim + query instruction, with every model/dim change called out as
requiring `engram reindex` (cross-linking guides/reindex). Gemini uses the
instruction-prefix mechanism, not task_type params. Success criterion #3.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (this plan)

NEW files/sections (excluded from source-grounding drift verification):
- `docs-site/src/content/docs/guides/embedding-models.md` — NEW recipes page (comparison table + per-provider env blocks + eval-run section).
- Commented recipe blocks in `charts/engram/values.yaml` (OpenRouter / Gemini / OpenAI).
- Cross-links + corrected Gemini row in `docs-site/src/content/docs/guides/embedding-instructions.md`.

No new Go code, no new packages, no SPDX headers (docs-site and charts are
license-exempt per .licenserc.yaml).
</artifacts_produced>

<review_notes>
## Review notes (Phase-14 cross-AI reviews — Codex + Antigravity)

- **B6 (MEDIUM, Codex — `<your-key>` not shell-safe):** ADDRESSED — every copy-paste key line uses `export ENGRAM_OPENAI_API_KEY='replace-with-your-key'`; the verify negative-greps that no unquoted `API_KEY=<…>` placeholder remains. Helm guidance follows the same convention.
- **B7 (MEDIUM, Codex — local recipes too abstract):** ADDRESSED — TEI/Ollama/vLLM are now concrete complete rows (exact server-side model id `BAAI/bge-m3` / `bge-m3`, dim 1024, concrete base URL, empty query instruction), each pairing base URL + model + dim + query instruction as REQ-embed-model-docs requires.
- **Finding A (HIGH — documented eval command must run the differ):** ADDRESSED — the "Running the retrieval eval" section documents `task eval:retrieval` (which now runs the renamed `TestRetrievalEval_AsymmetryDiffer` via `-run TestRetrievalEval`) AND the targeted `-run TestRetrievalEval_AsymmetryDiffer` form for a Gemini-only differ run. No stale non-working command is published.
- **B11 (LOW, Codex — Helm cross-link wording):** ADDRESSED — Helm comment recipes "reference the reindex guide route"; Markdown recipes "link" `/guides/reindex/`. Wording split in the must_haves + acceptance criteria.
- **B2 (HIGH, Antigravity — full-`task` blocks on `.planning/` rumdl noise):** ADDRESSED by SCOPING the gate: `docs-site/**` is rumdl-exempt (`.rumdl.toml` exclude) so the new/edited docs pages are validated by content greps + the manual Astro build; the Helm/YAML work is gated by `task lint:yaml` + `helm lint`. The whole-tree `task lint` is not a gate. Rationale (shared with 14-01/14-03): the `.planning/` rumdl exclusion is REQ-lint-planning-exclude, scoped to **Phase 21** — not pulled forward into Phase 14.
- **B1/Helm-lint-of-comments (LOW, Codex — helm lint cannot validate comments):** ACKNOWLEDGED — commented recipes are inherently unverifiable by `helm lint`; the plan mitigates with per-string greps (model/dim/base URL/reindex route) rather than claiming `helm lint` validates the comment content.
</review_notes>

<output>
Create `.planning/phases/14-embedder-model-options-eval/14-02-SUMMARY.md` when done
</output>