---
phase: 14-embedder-model-options-eval
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/retrievaleval/fixtures.go
  - internal/retrievaleval/retrieval_eval_test.go
autonomous: true
requirements:
  - REQ-embed-gemini-direct
must_haves:
  truths:
    - "A permanent, skip-gated differ-case (`TestRetrievalEval_AsymmetryDiffer` — named so `task eval:retrieval`'s `-run TestRetrievalEval` regex substring-reaches it) embeds one synthetic string via both EmbedQuery and Embed through the prod-parity embedder, asserts BOTH vectors are non-empty AND `len == the configured dim` (the value StoreAndEmbedderFromEnvNoEnsure returns, not discarded), then asserts they differ — all failures via `t.Fatal`. The Pitfall-12 correctness gate (D-04): it PASSES with the correct instruction-prefix recipe and FAILS if an operator wired the no-op task_type *_PARAMS mechanism (same raw input, ignored param, identical vectors) OR if a side returns an empty/wrong-sized vector."
    - "Under a symmetric config (all of ENGRAM_EMBED_QUERY_INSTRUCTION / ENGRAM_EMBED_DOCUMENT_INSTRUCTION / ENGRAM_EMBED_QUERY_PARAMS / ENGRAM_EMBED_DOCUMENT_PARAMS empty), the differ-case t.Skip()s its inequality assertion — query==document is valid there — so the eval suite stays green for symmetric operators (OpenAI text-embedding-3-small, bare bge-m3) (review B3)."
    - "The differ-case adds zero cost to the default `task` gate: it skips like gh261Case when ENGRAM_RETRIEVAL_EVAL is unset (D-05)."
  artifacts:
    - internal/retrievaleval/fixtures.go
    - internal/retrievaleval/retrieval_eval_test.go
  key_links:
    - "`task eval:retrieval` reaches the differ-case: naming it `TestRetrievalEval_AsymmetryDiffer` makes Go's `-run TestRetrievalEval` substring-match it — the documented eval command genuinely runs the differ assertion, with NO Taskfile change (review finding A, rename approach)."
    - "The differ-case runs through server.StoreAndEmbedderFromEnvNoEnsure -> em.EmbedQuery/em.Embed (the same prod-parity path gh261Case uses), never a bespoke embed shortcut (D-03)."
    - "Gemini asymmetry rides ENGRAM_EMBED_QUERY_INSTRUCTION/ENGRAM_EMBED_DOCUMENT_INSTRUCTION (instruction-prefix), never ENGRAM_EMBED_QUERY_PARAMS/ENGRAM_EMBED_DOCUMENT_PARAMS/task_type (research supersession, Pitfall 12)."
---

<objective>
Build the permanent, skip-gated retrieval-eval differ-case that proves engram's
asymmetric query/document embedding actually takes effect — the Pitfall-12
correctness gate for REQ-embed-gemini-direct (#331).

Per the research supersession, `gemini-embedding-2` does NOT honor a `task_type`
JSON param; its asymmetry is a text prefix on the input string, which maps to
engram's existing `ENGRAM_EMBED_QUERY_INSTRUCTION`/`ENGRAM_EMBED_DOCUMENT_INSTRUCTION`
mechanism (D-03 — no new wiring). The differ-case embeds one identical string
query-side and document-side through the production embedder and asserts the two
vectors differ (D-04). It PASSES under the correct instruction-prefix recipe and
FAILS if an operator misconfigures the no-op `*_PARAMS`/`task_type` mechanism —
exactly the silent recall regression Pitfall 12 warns about, made catchable.

Purpose: A re-runnable regression guard (D-05) that turns a silent recall
degradation into a hard test failure.
Output: A new skip-gated differ probe + assertion in `internal/retrievaleval/`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-embedder-model-options-eval/14-CONTEXT.md
@.planning/phases/14-embedder-model-options-eval/14-RESEARCH.md
@internal/retrievaleval/fixtures.go
@internal/retrievaleval/retrieval_eval_test.go
@internal/embed/embed.go

Both target `.go` files already carry the Apache-2.0 SPDX header (they are
modified, not created) — no `task license:add` needed. `task` (lint + test) must
be clean at commit. The differ-case is exercised against a live Gemini gateway
in plan 14-03 (manual eval run); this plan only lands the code and proves the
default gate stays a zero-cost skip.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the synthetic differ probe to fixtures.go</name>
  <files>internal/retrievaleval/fixtures.go</files>
  <read_first>
    - internal/retrievaleval/fixtures.go (existing retrievalCase/seedRecord shape, gh261Distractors synthetic-content convention, recallAtK)
    - internal/retrievaleval/retrieval_eval_test.go (how gh261Case is driven; em.EmbedQuery/em.Embed usage; the ENGRAM_RETRIEVAL_EVAL skip pattern)
    - .planning/phases/14-embedder-model-options-eval/14-RESEARCH.md § Code Examples (Option B minimal 2-record/1-string probe — the recommended shape)
  </read_first>
  <action>
    Add a package-level `const differProbe` holding a single synthetic
    public-tooling string (mirror the gh261Distractors "no secrets, synthetic
    OSS-tooling text" convention — e.g. a sentence about running `task lint`
    before commits). Add a doc comment explaining this string is embedded twice
    (query-side via EmbedQuery, document-side via Embed) and the two vectors MUST
    differ once the instruction-prefix asymmetry is configured. Do NOT add a
    testcontainer/Qdrant seedRecord for it — the differ probe never touches
    Qdrant; it only compares two embedding vectors. Keep it in fixtures.go
    alongside gh261Case so it reads as a permanent sibling fixture per D-05.
    Reference in the comment that the correct mechanism is the
    ENGRAM_EMBED_QUERY_INSTRUCTION / ENGRAM_EMBED_DOCUMENT_INSTRUCTION prefix
    path, not the ENGRAM_EMBED_QUERY_PARAMS / ENGRAM_EMBED_DOCUMENT_PARAMS path
    (D-03, research supersession).
  </action>
  <verify>
    <automated>go build ./internal/retrievaleval/ && go vet ./internal/retrievaleval/</automated>
  </verify>
  <acceptance_criteria>
    - `internal/retrievaleval/fixtures.go` declares a `differProbe` string constant with synthetic, secret-free content.
    - `go build ./internal/retrievaleval/` and `go vet ./internal/retrievaleval/` exit 0.
    - The existing Apache-2.0 SPDX header on `fixtures.go` is preserved (`task license:check` stays green).
  </acceptance_criteria>
  <done>fixtures.go carries the synthetic differ probe constant + documenting comment; the package still builds and vets clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add the skip-gated differ assertion (TestRetrievalEval_AsymmetryDiffer) + prove zero-cost default-gate skip</name>
  <files>internal/retrievaleval/retrieval_eval_test.go</files>
  <read_first>
    - internal/retrievaleval/retrieval_eval_test.go (TestRetrievalEval's ENGRAM_RETRIEVAL_EVAL=1 skip guard; the `_, dim, em, _, err := server.StoreAndEmbedderFromEnvNoEnsure()` call — `dim` is the uint64 configured dimension; em.EmbedQuery/em.Embed call sites and error handling; TestMain starting Qdrant on ENGRAM_RETRIEVAL_EVAL=1)
    - internal/retrievaleval/fixtures.go (the differProbe constant from Task 1)
  </read_first>
  <action>
    Add a new top-level test named EXACTLY `TestRetrievalEval_AsymmetryDiffer(t
    *testing.T)`. The `TestRetrievalEval` prefix is load-bearing: Go's `-run`
    treats its argument as an unanchored regex, so `task eval:retrieval`'s
    existing `-run TestRetrievalEval` substring-matches this name and runs the
    differ assertion via the documented command — no Taskfile change needed
    (addresses review finding A; the rename is the smaller diff than broadening
    the Taskfile target).

    As its FIRST statement, mirror TestRetrievalEval's guard: skip unless
    `os.Getenv("ENGRAM_RETRIEVAL_EVAL") == "1"` (defense-in-depth; TestMain
    already short-circuits before Docker).

    Then add a SYMMETRIC-CONFIG guard (addresses review B3): if ALL of
    `ENGRAM_EMBED_QUERY_INSTRUCTION`, `ENGRAM_EMBED_DOCUMENT_INSTRUCTION`,
    `ENGRAM_EMBED_QUERY_PARAMS`, and `ENGRAM_EMBED_DOCUMENT_PARAMS` are empty,
    `t.Skip(...)` with a message noting a symmetric config produces query==document
    legitimately (OpenAI text-embedding-3-small, bare bge-m3), so the inequality
    assertion does not apply. This keeps the suite green for symmetric operators.

    The differ test itself does NOT touch Qdrant, so it does NOT need
    `testQdrantAddr`. Build the prod-parity embedder from
    `server.StoreAndEmbedderFromEnvNoEnsure()` — KEEP the returned `dim` (the
    configured dimension), keep `em`, discard the store; `t.Fatalf` on the
    builder error. Call `em.EmbedQuery(ctx, differProbe)` and `em.Embed(ctx,
    differProbe)`; `t.Fatalf` on either embed error.

    Before comparing, ASSERT the dimension contract (addresses review B4):
    `t.Fatalf` unless BOTH vectors are non-empty AND
    `len(queryVec) == int(dim) && len(documentVec) == int(dim)` — this rejects the
    case where the embed client returns an empty or wrong-sized vector (embed.go
    only checks a `data` entry exists) and a naive inequality would still pass.
    Then assert the two `[]float32` vectors are NOT equal (element-wise compare or
    `reflect.DeepEqual` — add the `reflect` import if used); on equality use
    `t.Fatal`/`t.Fatalf` (NOT `t.Errorf`, so the correctness gate stops
    immediately and cannot be obscured by later output — review B4) with a message
    naming the Pitfall-12 failure: the query and document vectors are identical,
    so the asymmetric instruction-prefix had no effect (the operator likely wired
    the no-op *_PARAMS/task_type mechanism instead of the QUERY/DOCUMENT_INSTRUCTION
    prefix). On success, `t.Logf` a stable PASS line — e.g. `asymmetry differ
    PASS: query vector != document vector (dim=%d) — instruction-prefix took
    effect` — so a live run in plan 14-03 has a greppable PASS token for the
    fail-closed evidence gate. Keep the test content synthetic (differProbe) so no
    private content leaves for the third-party embedding API.

    Note (accepted, not fixed — review B9): running this live via
    `ENGRAM_RETRIEVAL_EVAL=1` still triggers the package `TestMain` to start a
    Qdrant testcontainer even though the differ test never uses Qdrant, so a live
    differ run requires Docker (or `ENGRAM_QDRANT_TEST_ADDR`). We accept and
    document this package-level prerequisite rather than refactoring the
    testcontainer lifecycle (TestMain cannot cheaply know which `-run` selection
    is active). Plan 14-03 lists Docker as a prerequisite.
  </action>
  <verify>
    <automated>go test ./internal/retrievaleval/ -run TestRetrievalEval_AsymmetryDiffer -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `retrieval_eval_test.go` defines `TestRetrievalEval_AsymmetryDiffer` (exact name, `TestRetrievalEval` prefix) gated on `ENGRAM_RETRIEVAL_EVAL=1` as its first statement.
    - It carries a symmetric-config guard that `t.Skip`s when all four of `ENGRAM_EMBED_QUERY_INSTRUCTION` / `ENGRAM_EMBED_DOCUMENT_INSTRUCTION` / `ENGRAM_EMBED_QUERY_PARAMS` / `ENGRAM_EMBED_DOCUMENT_PARAMS` are empty (review B3).
    - It keeps `dim` from `StoreAndEmbedderFromEnvNoEnsure` and `t.Fatal`s unless both vectors are non-empty and `len == int(dim)` before the inequality; the inequality failure uses `t.Fatal`, not `t.Errorf` (review B4).
    - With `ENGRAM_RETRIEVAL_EVAL` unset, `go test ./internal/retrievaleval/ -run TestRetrievalEval_AsymmetryDiffer -count=1` exits 0 by skipping (zero embedder/Docker cost — D-05).
    - On success the test logs a stable `asymmetry differ PASS` line (greppable evidence token for 14-03).
    - `task lint:go` (golangci-lint) and `go test ./...` are green; `task license:check` is green (both files retain their SPDX headers). Full `task` is NOT the gate here — see the review note on the `.planning/` rumdl blocker (review B2).
  </acceptance_criteria>
  <done>The differ assertion (`TestRetrievalEval_AsymmetryDiffer`) exists, is reachable via `task eval:retrieval`, skip-gated to zero default-gate cost, guarded for symmetric configs, asserts the configured dimension before inequality via `t.Fatal`, runs through the prod embedder path, and `task lint:go` + `go test ./...` are clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval fixture → third-party embedding API | The differ probe string is sent over the network to a third-party embedder (Gemini/OpenRouter/OpenAI) during a live eval run. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01 | Information Disclosure | `differProbe` content in fixtures.go | low | mitigate | Probe content is synthetic public-tooling text (mirrors the gh261Distractors "no secrets" convention); no private/customer content is embedded, so nothing sensitive leaves for the provider. |
| T-14-SC | Tampering | npm/pip/cargo installs | low | accept | N/A this phase — no package installs (RESEARCH § Package Legitimacy Audit: N/A; no go.mod/package.json changes). Nothing to gate. |
</threat_model>

<verification>
- `go build ./internal/retrievaleval/` and `go vet ./internal/retrievaleval/` exit 0.
- `go test ./internal/retrievaleval/ -run TestRetrievalEval_AsymmetryDiffer -count=1` exits 0 (skips when the eval gate is unset).
- `task lint:go` (golangci-lint) and `go test ./...` are green; `task license:check` green (both files retain their SPDX headers). Full `task` is intentionally NOT the gate — it runs `rumdl check .` over the whole tree and fails on pre-existing `.planning/` markdown noise (review B2; see review notes).
</verification>

<success_criteria>
The Pitfall-12 differ-case exists as a permanent, skip-gated fixture+assertion,
runs through the production embedder path, adds zero cost to the default gate,
and is ready to be exercised against a live Gemini gateway in plan 14-03. Covers
the code portion of REQ-embed-gemini-direct (success criterion #1's gate).
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (this plan)

NEW symbols (excluded from source-grounding drift verification):
- `differProbe` — string constant in `internal/retrievaleval/fixtures.go`.
- `TestRetrievalEval_AsymmetryDiffer` — top-level test func in `internal/retrievaleval/retrieval_eval_test.go` (named with the `TestRetrievalEval` prefix so `task eval:retrieval` reaches it).

No new files, no new packages, no new Go abstraction (no new `embed.Option`, no
native `genai` client) — honors D-03 / RESEARCH § Don't Hand-Roll. No
`Taskfile.yaml` change (the rename approach reaches the test without one).
</artifacts_produced>

<review_notes>
## Review notes (Phase-14 cross-AI reviews — Codex + Antigravity)

- **Finding A (HIGH, both — differ test unreachable via `task eval:retrieval`):** ADDRESSED by RENAMING the test `TestEmbedAsymmetryDiffer` → `TestRetrievalEval_AsymmetryDiffer` (Task 2). Go's `-run TestRetrievalEval` regex substring-matches it, so the documented command runs the differ — chosen over broadening the Taskfile target because it is the smaller diff and needs no `Taskfile.yaml` change (so `Taskfile.yaml` is NOT in `files_modified`).
- **B3 (HIGH, Antigravity — symmetric-config failure):** ADDRESSED — Task 2 adds a symmetric-config `t.Skip` guard (all four instruction/param env vars empty ⇒ skip the inequality assertion).
- **B4 (MEDIUM, Codex — wrong-sized/empty vectors pass):** ADDRESSED — Task 2 retains `dim` from `StoreAndEmbedderFromEnvNoEnsure`, asserts non-empty + `len == dim` before inequality, and uses `t.Fatal` (not `t.Errorf`).
- **B5 support (Codex — evidence must be fail-closed):** Task 2 emits a stable `asymmetry differ PASS` log line on success so plan 14-03's evidence gate has a positive token to require.
- **B9 (MEDIUM, both — Docker required for the differ-only probe):** ACCEPTED + documented (not refactored). The differ test never uses Qdrant, but the package `TestMain` starts a Qdrant testcontainer whenever `ENGRAM_RETRIEVAL_EVAL=1`; refactoring the lifecycle to inspect the active `-run` selection is fragile, so we document the Docker prerequisite (also stated in 14-03) instead.
- **B2 (HIGH, Antigravity — full-`task` blocks on pre-existing `.planning/` rumdl noise):** ADDRESSED by SCOPING this plan's gate to `task lint:go` + `go test ./...` + `task license:check` (all Go work), not the whole-tree `task`/`task lint:markdown`. Rationale: the `.planning/` rumdl exclusion is REQ-lint-planning-exclude, explicitly scoped to **Phase 21** (REQUIREMENTS.md) — pulling `.rumdl.toml` edits into Phase 14 would cross a phase boundary and do another phase's work. Scoping keeps the gate achievable and within Phase-14 scope.
</review_notes>

<output>
Create `.planning/phases/14-embedder-model-options-eval/14-01-SUMMARY.md` when done
</output>
