---
phase: 14-embedder-model-options-eval
plan: 03
type: execute
wave: 2
depends_on:
  - 14-01
  - 14-02
files_modified:
  - .planning/phases/14-embedder-model-options-eval/14-EVAL-EVIDENCE.md
  - docs-site/src/content/docs/guides/embedding-models.md
  - charts/engram/values.yaml
autonomous: false
requirements:
  - REQ-embed-gemini-direct
  - REQ-embed-prod-parity-eval
user_setup:
  - service: gemini
    why: "Live differ-case eval run against gemini-embedding-2 (Pitfall-12 gate) + model-id confirmation curl"
    env_vars:
      - name: ENGRAM_OPENAI_API_KEY
        source: "Google AI Studio -> Get API key (used with the Gemini OpenAI-compat base URL)"
  - service: openrouter
    why: "Live qwen3-embedding-8b@4096 recall@8 re-point run (#261 parity, D-08)"
    env_vars:
      - name: ENGRAM_OPENAI_API_KEY
        source: "OpenRouter -> Keys (used with https://openrouter.ai/api/v1)"
must_haves:
    - "A live run with the Gemini recipe env produces query vector ≠ document vector at the configured 3072 dim (`TestRetrievalEval_AsymmetryDiffer` PASS via the documented `task eval:retrieval` target, or its targeted `-run TestRetrievalEval_AsymmetryDiffer` form) — the Pitfall-12 correctness gate observed live and recorded in committed evidence (success criterion #1, D-07). Since 14-01 named the test with the `TestRetrievalEval` prefix, the documented command genuinely runs it (review finding A)."
    - "A `task eval:retrieval` run with the qwen3-embedding-8b@4096 recipe env surfaces Record T within default k=8 for BOTH #261 queries (two `hard rank bar: PASS` lines) and reports exactly `recall@8=1.00` — recorded in committed evidence, closing #261/#334 (success criterion #2, D-09)."
    - "The committed evidence gate is FAIL-CLOSED (review B5): it requires the differ PASS token, two `hard rank bar: PASS` lines, exact `recall@8=1.00`, and a recorded zero exit status for each command — and rejects any failing-rank line, non-1.00 recall, or go-test failure summary. A failed run cannot satisfy it."
    - "The exact Gemini compat model-id string is confirmed by one live curl against gemini-embedding-2 (and the -preview fallback), the returned vector length is confirmed to be 3072, and both are reconciled into the docs recipe, Helm comments, and the differ-run env (Pitfall 2, D-01). A non-3072 length or a non-200 model-id blocks Task 2 (review B5 fail-closed checkpoint)."
  artifacts:
    - .planning/phases/14-embedder-model-options-eval/14-EVAL-EVIDENCE.md
  key_links:
    - "The confirmed model-id from the checkpoint is the single source of truth propagated to embedding-models.md, values.yaml, and the differ-run env."
    - "The evidence artifact records only PASS lines + numeric recall@8/MRR + the confirmed model-id — never an API key or private content (info-disclosure mitigation, T-14-01)."
    - "Both runs go through `task eval:retrieval` (= ENGRAM_RETRIEVAL_EVAL=1 go test ./internal/retrievaleval/), the same skip-gated harness gh261Case and the plan-14-01 differ-case live in — no new eval framework (D-06)."
---

<objective>
Close success criteria #1 and #2 with auditable evidence: run the retrieval eval
live against the shipped Gemini and qwen3@4096 recipes, confirm the exact Gemini
compat model-id, and commit the run output as a durable artifact
(REQ-embed-gemini-direct #331 + REQ-embed-prod-parity-eval #334, closes #261).

Execution is local/manual (D-06) — this environment holds no Gemini/OpenRouter
keys, so a single blocking human-verify checkpoint runs the model-id curl and
both evals with the operator's keys and reports the output; the plan then records
it into `14-EVAL-EVIDENCE.md` (D-07) and reconciles any model-id correction back
into the plan-14-02 recipes.

Purpose: Turn "the recipes work" from an assumption into a committed, re-runnable
proof — the differ-assertion PASS (instruction-prefix took effect) and the
gh261 recall@8 PASS on the real prod-parity config.
Output: A committed eval-evidence artifact + any model-id reconciliation.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-embedder-model-options-eval/14-CONTEXT.md
@.planning/phases/14-embedder-model-options-eval/14-RESEARCH.md
@.planning/phases/14-embedder-model-options-eval/14-01-SUMMARY.md
@.planning/phases/14-embedder-model-options-eval/14-02-SUMMARY.md
@internal/retrievaleval/retrieval_eval_test.go
@Taskfile.yaml

Depends on 14-01 (the TestRetrievalEval_AsymmetryDiffer differ-case must exist to run) and
14-02 (the docs/Helm recipe supplies the exact run env, and is the reconciliation
target if the model-id differs). The `14-EVAL-EVIDENCE.md` artifact lives under
`.planning/` (outside SPDX/license scope — no header). Runs need a live gateway +
Docker or `ENGRAM_QDRANT_TEST_ADDR`; `commit_docs: true` version-controls this
phase dir.
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Confirm the Gemini model-id and run both evals live</name>
  <what-built>
    Plan 14-01 shipped the skip-gated `TestRetrievalEval_AsymmetryDiffer` differ-case;
    plan 14-02 shipped the Gemini (`gemini-embedding-2` @3072, instruction-prefix)
    and qwen3-embedding-8b@4096 recipes with the exact `task eval:retrieval` env.
    Everything is ready to exercise against a live gateway — this environment has
    no API keys, so a human runs it (D-06, local/manual).
  </what-built>
  <action>
    Pause for the operator to (1) confirm the exact Gemini OpenAI-compat model-id
    via a live curl, (2) run the Gemini differ eval, and (3) run the
    qwen3-embedding-8b@4096 recall eval, then report the confirmed id and all
    three outputs (keys redacted) as the resume signal. No autonomous work — this
    task exists solely to gate on the human-run evidence per D-06.
  </action>
  <how-to-verify>
    Prerequisite: Docker running (or `ENGRAM_QDRANT_TEST_ADDR` pointing at a
    reachable Qdrant). Docker is required even for the Gemini-only differ probe:
    the package `TestMain` starts a Qdrant testcontainer whenever
    `ENGRAM_RETRIEVAL_EVAL=1`, before it knows the `-run` selection (accepted, not
    refactored — review B9).

    First `export ENGRAM_OPENAI_API_KEY='<your real key>'` in the shell so the
    curl and the go test share it; NEVER paste the real key into the evidence.

    1. CONFIRM THE GEMINI MODEL-ID (Pitfall 2). With your Gemini key exported, run
       each copy-pasteable curl and note which returns HTTP 200 with an embedding
       (review B12):

         curl -sS -o /dev/null -w '%{http_code}\n' -X POST \
           https://generativelanguage.googleapis.com/v1beta/openai/embeddings \
           -H "Authorization: Bearer $ENGRAM_OPENAI_API_KEY" \
           -H "Content-Type: application/json" \
           -d '{"model":"gemini-embedding-2","input":"test"}'

         curl -sS -o /dev/null -w '%{http_code}\n' -X POST \
           https://generativelanguage.googleapis.com/v1beta/openai/embeddings \
           -H "Authorization: Bearer $ENGRAM_OPENAI_API_KEY" \
           -H "Content-Type: application/json" \
           -d '{"model":"gemini-embedding-2-preview","input":"test"}'

       To read the returned vector length for the id that returned 200, re-run
       without `-o /dev/null -w` and pipe through
       `jq '.data[0].embedding | length'`. FAIL-CLOSED: proceed only if a
       candidate id returns HTTP 200 AND its embedding length is exactly 3072. If
       neither returns 200, or the length is not 3072, STOP and report the codes +
       length — this blocks Task 2 (review B5).

    2. GEMINI DIFFER RUN (success criterion #1). Export the Gemini recipe env
       from guides/embedding-models.md using the CONFIRMED model-id:
       ENGRAM_EMBED_MODEL=<confirmed-id>, ENGRAM_EMBED_DIM=3072,
       ENGRAM_EMBED_QUERY_INSTRUCTION='task: search result | query: {query}',
       ENGRAM_EMBED_DOCUMENT_INSTRUCTION='title: none | text: {document}',
       ENGRAM_OPENAI_BASE_URL=https://generativelanguage.googleapis.com/v1beta/openai,
       ENGRAM_OPENAI_API_KEY already exported, ENGRAM_RETRIEVAL_EVAL=1. Run the
       targeted differ command (isolates the differ from the gh261 recall case):
         ENGRAM_RETRIEVAL_EVAL=1 go test ./internal/retrievaleval/ -run TestRetrievalEval_AsymmetryDiffer -v; echo "exit=$?"
       Report the `asymmetry differ PASS` line, the `--- PASS: TestRetrievalEval_AsymmetryDiffer` line, and the `exit=` value.
       (PASS = query vector ≠ document vector at 3072 dim = the instruction-prefix
       asymmetry took effect.) The documented `task eval:retrieval` target also
       reaches this test (its `-run TestRetrievalEval` regex substring-matches the
       renamed test — review finding A); the targeted form above just avoids also
       running the gh261 recall case under the Gemini env.

    3. QWEN3@4096 RECALL RUN (success criterion #2, #261 parity). Export the
       OpenRouter recipe env: ENGRAM_EMBED_MODEL=qwen/qwen3-embedding-8b,
       ENGRAM_EMBED_DIM=4096, ENGRAM_EMBED_QUERY_INSTRUCTION='Given a web search
       query, retrieve relevant passages that answer the query',
       ENGRAM_OPENAI_BASE_URL=https://openrouter.ai/api/v1,
       ENGRAM_OPENAI_API_KEY set to your OpenRouter key, ENGRAM_RETRIEVAL_EVAL=1. Run:
         task eval:retrieval; echo "exit=$?"
       Report the `gh261-sticky-neighbor-crowding` output: BOTH per-query
       `hard rank bar: PASS` lines (rank/k), the `recall@8=1.00 MRR=..` line, and
       the `exit=` value. FAIL-CLOSED: both queries must show `hard rank bar: PASS`
       and the aggregate must be exactly `recall@8=1.00`.

    Paste the confirmed model-id + its 3072 vector length + all run outputs +
    each `exit=` value (redact keys) as the resume signal so the evidence artifact
    can be filled from the structured template (keys redacted — review B8).
  </how-to-verify>
  <resume-signal>Paste: the confirmed Gemini model-id + its embedding length (must be 3072); the differ `asymmetry differ PASS`/`--- PASS` lines + exit status; and the qwen3 two `hard rank bar: PASS` lines + `recall@8=1.00` + exit status (keys redacted). Or describe any non-200 / non-3072 / FAIL so the checkpoint stays blocked.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write the eval-evidence artifact and reconcile the model-id</name>
  <files>.planning/phases/14-embedder-model-options-eval/14-EVAL-EVIDENCE.md, docs-site/src/content/docs/guides/embedding-models.md, charts/engram/values.yaml</files>
  <read_first>
    - The Task 1 resume-signal output (confirmed model-id + the three run outputs)
    - docs-site/src/content/docs/guides/embedding-models.md (Gemini recipe row/block — reconciliation target if the confirmed id differs from gemini-embedding-2)
    - charts/engram/values.yaml (Gemini commented block — same reconciliation target)
  </read_first>
  <action>
    Create `.planning/phases/14-embedder-model-options-eval/14-EVAL-EVIDENCE.md`
    (no SPDX — .planning is license-exempt). Do NOT paste raw terminal output:
    redaction takes precedence over completeness (review B8). Instead fill a
    NARROW STRUCTURED TEMPLATE with exactly these fields, per run:
      - Run date.
      - Provider / model: the confirmed Gemini compat model-id + native dim 3072;
        and qwen3-embedding-8b @4096 for the recall run.
      - Observed vector dimensions (3072 for the Gemini differ run).
      - Sanitized command: the exact command with the API key replaced by the
        literal placeholder `$ENGRAM_OPENAI_API_KEY` (never the real key, never an
        `Authorization: Bearer` value).
      - Recorded exit status as `exit status: 0` for EACH command (from the
        `echo "exit=$?"` the operator reported).
      - The exact success lines only: for the differ run, the `asymmetry differ
        PASS` line and the `--- PASS: TestRetrievalEval_AsymmetryDiffer` line
        (success criterion #1); for the recall run, BOTH `hard rank bar: PASS`
        lines and the `recall@8=1.00 MRR=..` line (success criterion #2, D-09 —
        MRR recorded as an informational baseline).
    Transcribe ONLY those specific success + metric lines; do NOT transcribe any
    failing-rank line, a zero-or-other-than-1.00 recall figure, or a go-test
    failure summary (if the operator reported any of those, the checkpoint was not
    satisfied — do not write the evidence file; surface the failure instead).

    Issue handoff (review B10): state that these runs satisfy the #261 / #334 /
    #331 acceptance gates, and that the committing PR MUST use closing keywords
    (`Closes #261`, `Closes #334`, `Closes #331`) so GitHub closes them on merge —
    the evidence file records the proof; the PR performs the closure.

    If the confirmed model-id differs from `gemini-embedding-2`, update the Gemini
    recipe id in embedding-models.md and values.yaml to the confirmed string
    (single source of truth); if it matched, note "confirmed unchanged" and leave
    those files untouched.
  </action>
  <verify>
    <automated>E=.planning/phases/14-embedder-model-options-eval/14-EVAL-EVIDENCE.md; test -f "$E" && grep -q 'recall@8=1.00' "$E" && [ "$(grep -c 'hard rank bar: PASS' "$E")" -ge 2 ] && grep -Eq 'asymmetry differ PASS|PASS: TestRetrievalEval_AsymmetryDiffer' "$E" && [ "$(grep -c 'exit status: 0' "$E")" -ge 2 ] && grep -q '261' "$E" && ! grep -Eq 'recall@8=0|rank bar FAILED|--- FAIL|differ FAIL' "$E"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/14-embedder-model-options-eval/14-EVAL-EVIDENCE.md` exists and records (structured template — review B8): the confirmed Gemini model-id + observed 3072 dim; the sanitized Gemini differ command + `asymmetry differ PASS`/`--- PASS: TestRetrievalEval_AsymmetryDiffer` line; the sanitized qwen3@4096 command + BOTH `hard rank bar: PASS` lines + `recall@8=1.00`/`MRR`; and an `exit status: 0` for each command.
    - FAIL-CLOSED (review B5): the verify requires exact `recall@8=1.00`, ≥2 `hard rank bar: PASS` lines, the differ PASS token, ≥2 `exit status: 0`, and the absence of any failing-rank / non-1.00-recall / go-test-failure token. A failed or partial run cannot satisfy it.
    - The artifact contains no API keys or private content (sanitized commands use the `$ENGRAM_OPENAI_API_KEY` placeholder) and states the #261/#334/#331 issue-closure handoff (PR closing keywords — review B10).
    - If the confirmed model-id ≠ `gemini-embedding-2`, `embedding-models.md` and `values.yaml` are updated to the confirmed string; otherwise they are left unchanged (noted in the artifact).
    - Scoped lint (review B2): `rumdl check .planning/phases/14-embedder-model-options-eval/14-EVAL-EVIDENCE.md` (the new file only) is clean, and `task license:check` exits 0. The whole-tree `task lint` is not a gate — it fails on pre-existing `.planning/` noise (REQ-lint-planning-exclude is a Phase-21 requirement).
  </acceptance_criteria>
  <done>The committed evidence artifact captures both eval PASSes + the confirmed model-id, keys redacted, and the recipes agree on the model-id.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator keys / eval output → committed repo artifact | Eval runs use real Gemini/OpenRouter keys; their output is transcribed into a committed, version-controlled evidence file. |
| eval fixture → third-party embedding API | The differ probe + gh261 fixture content is sent to third-party embedders during the run. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01 | Information Disclosure | 14-EVAL-EVIDENCE.md contents | medium | mitigate | Redaction takes precedence over completeness (review B8): the artifact is a narrow structured template — sanitized commands (key → `$ENGRAM_OPENAI_API_KEY` placeholder), the (public) model-id + dim, exit status, and only the specific success/metric lines. No raw terminal paste, no `Authorization: Bearer` value, no env dump. Fixture content (differProbe, gh261) is synthetic public-tooling text, so no secrets leave for the provider. |
| T-14-SC | Tampering | npm/pip/cargo installs | low | accept | N/A — eval run + docs artifact only; no package installs (RESEARCH § Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- The human-verify checkpoint reports fail-closed (review B5): the confirmed Gemini compat model-id + its 3072 embedding length, the `TestRetrievalEval_AsymmetryDiffer` PASS line + exit status, and the `gh261-sticky-neighbor-crowding` two `hard rank bar: PASS` lines + exactly `recall@8=1.00` + exit status.
- `14-EVAL-EVIDENCE.md` records all of the above in the structured redacted template and states the #261/#334/#331 closure handoff; the fail-closed verify grep rejects any failing/partial run.
- Recipe files (embedding-models.md, values.yaml) agree with the confirmed model-id.
- Scoped lint (review B2): `rumdl check` of the new evidence file is clean and `task license:check` exits 0 (whole-tree `task lint` is not a gate — pre-existing `.planning/` noise; REQ-lint-planning-exclude is Phase 21).
</verification>

<success_criteria>
Success criteria #1 (Gemini differ-assertion PASS via a live `task eval:retrieval`
run) and #2 (gh261 recall@8 PASS on qwen3-embedding-8b@4096, closing #261/#334)
are closed by a committed, auditable evidence artifact — REQ-embed-gemini-direct
and REQ-embed-prod-parity-eval satisfied.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (this plan)

NEW file (excluded from source-grounding drift verification):
- `.planning/phases/14-embedder-model-options-eval/14-EVAL-EVIDENCE.md` — committed eval-run evidence (differ PASS + recall@8/MRR + confirmed model-id).

Possible reconciliation edits (only if the live curl confirms a different id):
- Gemini model-id in `docs-site/src/content/docs/guides/embedding-models.md` and `charts/engram/values.yaml`.

No new Go code, no new packages — the eval reuses the existing skip-gated
`task eval:retrieval` harness (D-06 / RESEARCH § Don't Hand-Roll).
</artifacts_produced>

<review_notes>
## Review notes (Phase-14 cross-AI reviews — Codex + Antigravity)

- **Finding A (HIGH — differ reachable via documented command):** ADDRESSED — the differ command uses the renamed `TestRetrievalEval_AsymmetryDiffer`; `task eval:retrieval` reaches it, and the checkpoint documents both the targeted `-run TestRetrievalEval_AsymmetryDiffer` form and the task target.
- **B5 (HIGH, Codex — evidence gate accepts failure text):** ADDRESSED — the Task 2 verify is fail-closed: it requires exact `recall@8=1.00`, ≥2 `hard rank bar: PASS` lines, the differ PASS token, ≥2 recorded `exit status: 0`, and negative-greps any failing-rank / non-1.00-recall / go-test-failure token. The checkpoint also fails closed on model-id (must be HTTP 200) and dimension (must be 3072).
- **B8 (MEDIUM, Codex — "verbatim" vs "redact" conflict):** ADDRESSED — redaction wins; Task 2 fills a narrow structured template (date, provider/model, vector dims, sanitized command with `$ENGRAM_OPENAI_API_KEY` placeholder, exit status, exact PASS + metric lines). No raw terminal paste.
- **B5/dimension (MEDIUM, Codex — dimension observational not gating):** ADDRESSED — the checkpoint blocks Task 2 unless the confirmed model-id returns a length-3072 embedding, and 14-01's test now asserts `len == dim` so the differ PASS itself is dimension-gated.
- **B12 (suggestion, Antigravity — copy-pasteable curls):** ADDRESSED — the checkpoint now includes exact `curl` commands (with `Authorization: Bearer $ENGRAM_OPENAI_API_KEY`) for both `gemini-embedding-2` and `-preview`.
- **B9 (MEDIUM, both — Docker required for differ-only probe):** ACCEPTED + documented — the prerequisite section explains the package `TestMain` starts Qdrant on `ENGRAM_RETRIEVAL_EVAL=1`; not refactored (see 14-01 review note).
- **B10 (LOW, Codex — "closes #261/#334" performs no closure):** DEFERRED to ship time WITH a handoff — the evidence records the proof, and Task 2 instructs that the committing PR use closing keywords (`Closes #261`, `Closes #334`, `Closes #331`) so GitHub closes them on merge. Rationale: issue closure legitimately happens at PR-merge, not during plan execution; a handoff note is the appropriate mechanism.
- **B2 (HIGH, Antigravity — `.planning/` rumdl noise):** ADDRESSED by SCOPING the gate to `rumdl check` of the new evidence file only + `task license:check`; not the whole-tree `task lint`. (REQ-lint-planning-exclude is Phase 21 — not pulled forward.)
</review_notes>

<output>
Create `.planning/phases/14-embedder-model-options-eval/14-03-SUMMARY.md` when done
</output>