---
phase: 04-drift-detection-read-only
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04-drift-detection-read-only/04-OBSERVATIONS.md
autonomous: false
requirements:
  - REQ-drift-redaction
  - REQ-drift-observed-registration

estimate:
  tokens: 25000
  raw_tokens: 25000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`.planning/phases/04-drift-detection-read-only/04-OBSERVATIONS.md` exists, is dated, names the observed `claude --version` and `codex --version` outputs, and carries the VERBATIM combined stdout+stderr of `claude mcp get probe-literal-04` and of `codex mcp get probe-literal-04 --json` for a throwaway entry whose custom header value is a LITERAL (not a `${VAR}`/`{env:VAR}` reference), plus each command's observed exit code (D-05, D-07, D-08)"
    - "The record states the exact commands run, in order, including the removal of the throwaway entry and the removal of the isolated `CODEX_HOME`; the throwaway name is `probe-literal-04`, never `engram`, so the maintainer's real registration is never read, written, or removed (04-CONTEXT.md §Specific Ideas)"
    - "Every literal header value in the record is the dummy `sk-DO-NOT-COMMIT-literal-test-abc123` — a real credential never appears; the record itself says so in a one-line provenance note"
    - "The record's `## What this pins` section lists, for Claude Code: the complete label set observed (name line, `Scope:`, `Status:`, `Type:`, `URL:`, `Headers:`, any trailing hint line), the exact `Status:` text produced by the failed dial to `127.0.0.1:1`, and whether the literal value appears in the `Headers:` block verbatim; for Codex: which of `http_headers` / `env_http_headers` carried the hand-edited literal and the JSON shape it took (map / list / other), and whether any key not present in the 03-RESEARCH.md 0.153.4 shape appeared — the facts plan 04-05's scanner fixtures cite by section"
    - "Every scanner fixture built for the literal-echo case (plan 04-05) cites this file in a header comment (D-08); this plan performs the observation ONLY — no Go code changes, no automated test invokes a real `claude`/`codex`/`opencode` binary (rule m45p2b4bp7)"
  artifacts:
    - path: ".planning/phases/04-drift-detection-read-only/04-OBSERVATIONS.md"
      provides: "dated D-05 observation record: commands, versions, verbatim captures, exit codes, 'What this pins' facts"
      contains: "probe-literal-04"
  key_links:
    - from: ".planning/phases/04-drift-detection-read-only/04-OBSERVATIONS.md"
      to: "internal/setup/claudecode_test.go"
      via: "plan 04-05's Claude Code literal-echo fixture cites this record by file name in a header comment (D-08)"
      pattern: 'claude mcp get probe-literal-04'
    - from: ".planning/phases/04-drift-detection-read-only/04-OBSERVATIONS.md"
      to: "internal/setup/codex_test.go"
      via: "plan 04-05's Codex literal-header fixture cites this record by file name in a header comment (D-08)"
      pattern: 'codex mcp get probe-literal-04 --json'
  prohibitions:
    - statement: "MUST NOT record, paste, or commit a real credential: the only literal header value that may appear in 04-OBSERVATIONS.md is the dummy `sk-DO-NOT-COMMIT-literal-test-abc123`; if the maintainer's capture contains any other secret-looking value the executor stops and asks before writing the file"
      category: safety
      status: resolved
      verification: test
      reason: "Task 2's automated verify greps the file for the dummy marker and for the absence of any `sk-` token other than the dummy; the record's provenance line states the value is a dummy — no wired check descriptor by design (spec-less fallback, flagged-unverified)"
    - statement: "MUST NOT touch the maintainer's real `engram` registration on any runtime or the real `~/.codex/config.toml`: the throwaway entry is named `probe-literal-04`, Codex runs under an isolated `CODEX_HOME`, and the protocol removes what it registered"
      category: safety
      status: resolved
      verification: judgment
      reason: "The protocol names the throwaway entry and the isolated CODEX_HOME verbatim; the human confirms in the resume signal that removal ran — not machine-checkable from this repo (rule m45p2b4bp7 forbids a test that invokes the real CLI), so it disposes flagged-unverified"
    - statement: "MUST NOT invoke a real `claude`, `codex`, or `opencode` binary from any automated task or test in this plan — the observation is a human-run protocol, and Task 2 only writes the record from what the human pasted"
      category: safety
      status: resolved
      verification: test
      reason: "Task 2's verify is pure `rg`/`test` over the record; no Go file changes in this plan (`git diff --stat` touches only the record) — no wired check descriptor by design (spec-less fallback, flagged-unverified)"
---

<objective>
Close the ROADMAP's blocking prerequisite for trusting the redaction path: observe, once, on the
maintainer's own machine, what `claude mcp get` and `codex mcp get --json` print for a registration
whose custom header value is a LITERAL rather than a bare `${VAR}`/`{env:VAR}` reference — the one
echo shape no prior research session captured verbatim (STACK.md redacted it before it left the
session; 04-RESEARCH.md Pitfall 2). Record it, dated and versioned, as `04-OBSERVATIONS.md` (D-05,
D-07, D-08) so plan 04-05's scanner fixtures are built from observed reality rather than an
assumed shape.

Purpose: REQ-drift-redaction must be proven "against a fixture whose probe output carries a
literal value"; a fixture invented from the bare-reference shape could silently miss the exact
leak this requirement closes. D-06 makes this a SOFT gate — only the per-runtime scanners and
their fixtures (plan 04-05, wave 3) wait on it; every shape-independent piece (plans 04-01, 04-03,
04-04) proceeds in parallel.

This checkpoint is emitted deliberately despite the project's end-of-phase human-verify default:
the user's locked decision D-05 requires a human-run observation BEFORE the literal-echo fixtures
are written, and the executor cannot run the protocol itself (rule m45p2b4bp7: verification never
invokes a real third-party CLI or touches the operator's `$HOME`). It carries
`gate="blocking-human"` so no auto-mode ever approves it unattended.

Output: `.planning/phases/04-drift-detection-read-only/04-OBSERVATIONS.md`. No Go changes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-drift-detection-read-only/04-CONTEXT.md
@.planning/phases/04-drift-detection-read-only/04-RESEARCH.md
@CLAUDE.md
</context>

## Artifacts this phase produces (plan 04-02's share)

| Kind | Artifact | Created by |
|------|----------|------------|
| Planning record | `.planning/phases/04-drift-detection-read-only/04-OBSERVATIONS.md` — dated; `## Environment` (versions, date, OS); `## Protocol` (exact commands); `## Claude Code` (verbatim capture + exit code); `## Codex` (verbatim capture + exit code + the hand-edited TOML fragment); `## What this pins` (facts for 04-05) | 04-02 Task 2 |

## Flagged Assumptions (edge probe, spec-less fallback)

No edge-probe row is assigned to this plan (plans 04-01, 04-04, 04-05 carry all 13). Recorded here
for the verifier: 04-RESEARCH.md assumptions A1 (the literal echo resembles the bare-reference
shape) and A3 (Codex's `http_headers`/`env_http_headers` are maps) are exactly what this
observation answers — they stay ASSUMED until the record lands and plan 04-05 reconciles the
scanners against it.

## Prohibition probe (spec-less fallback, in-prompt recall)

Stage 1 raw candidates: must not commit a real credential; must not touch the real `engram`
entry; must not touch the real `~/.codex/config.toml`; must not run a real CLI from a test; must
not leave the throwaway entry registered; must not let the dummy value look like a real key; must
not restate the already-known bare-reference finding instead of the new literal finding (Pitfall
2's warning sign); must not skip the exit-code capture. Stage 2 keeps three (authored above,
descriptor-less): the **no-real-credential** safety prohibition, the **never-touch-the-real-
registration** safety prohibition (judgment-tier: only the human can confirm removal ran), and
the **no-real-CLI-from-automation** safety prohibition. "Must not leave the entry registered" folds
into the second; the rest are protocol hygiene the checkpoint text itself enforces. Canon
breadcrumb: secrets-in-VCS is canon (covered by the repo's shipped-bundle privacy guard and
`/gsd-secure-phase`); the bespoke rule minted here is the dummy-marker convention.

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: OBSERVE — what `claude mcp get` and `codex mcp get --json` print for a throwaway entry carrying a LITERAL custom-header value (D-05 protocol, run once by the maintainer)</name>
  <what-built>
Nothing to verify from code yet — this checkpoint IS the observation the phase's redaction fixtures
must be built from. Plans 04-01/04-03/04-04 are building the shape-independent plumbing in parallel
(D-06); plan 04-05's Claude Code scanner and both runtimes' literal-echo fixtures wait on what you
record here (D-08). The executor cannot run these commands for you: rule m45p2b4bp7 forbids any
automated step from invoking a real `claude`/`codex` binary or touching your `$HOME`.

Both throwaway names below are deliberately `probe-literal-04`, never `engram`, so your real
registration is never read, written, or removed. The URL points at `127.0.0.1:1`, an address that
never answers, so Claude Code's live dial (`claude mcp get` dials a user-scope entry — 03-RESEARCH.md
Pitfall 3) is harmless; the failed-dial `Status:` text it produces is itself one of the facts to
capture (plan 04-05 must treat it as chrome, D-11 / 04-RESEARCH.md Pitfall 4). The literal value is
a DUMMY — keep it exactly as written so the committed record can never be mistaken for a secret.
  </what-built>
  <how-to-verify>
Run the two blocks below on your own machine, in order, and paste the results as described in the
resume signal. Capture stdout AND stderr together, VERBATIM (no trimming, no reformatting), and the
exit code of each read verb.

**Claude Code** (the `claude` CLI on PATH; record `claude --version` first):

```sh
claude --version
claude mcp add --transport http probe-literal-04 http://127.0.0.1:1/mcp \
  --scope user --header 'x-litellm-api-key: sk-DO-NOT-COMMIT-literal-test-abc123'
claude mcp get probe-literal-04 > /tmp/04-observe-claude.txt 2>&1; echo "exit=$?"
cat /tmp/04-observe-claude.txt
claude mcp remove probe-literal-04 --scope user
claude mcp get probe-literal-04; echo "exit-after-remove=$?"
```

Also run ONE bare-reference control so the record carries both shapes side by side from the same
CLI version (03-RESEARCH.md captured only the `Headers:` block of this shape, never the full
framing):

```sh
claude mcp add --transport http probe-literal-04 http://127.0.0.1:1/mcp \
  --scope user --header 'x-litellm-api-key: ${LITELLM_KEY}'
claude mcp get probe-literal-04 > /tmp/04-observe-claude-ref.txt 2>&1; echo "exit=$?"
cat /tmp/04-observe-claude-ref.txt
claude mcp remove probe-literal-04 --scope user
```

**Codex** (`codex mcp add` has no header flag at all — D-09/D-10 of Phase 2 — so a literal header
can only reach a registration by hand-editing the config file, exactly the shape a hand-configured
gateway registration takes; use an ISOLATED `CODEX_HOME` so your real Codex config is never
touched; record `codex --version` first):

```sh
codex --version
export CODEX_HOME=/tmp/engram-04-observe
mkdir -p "$CODEX_HOME"
codex mcp add probe-literal-04 --url http://127.0.0.1:1/mcp --bearer-token-env-var DUMMY_04
codex mcp get probe-literal-04 --json; echo "exit=$?"     # control: the bearer-only shape
cat "$CODEX_HOME/config.toml"                              # find the probe-literal-04 table
```

Now hand-edit `$CODEX_HOME/config.toml`: under the `probe-literal-04` server's table (whichever
table codex wrote — record its exact heading), add a literal (non-reference) header, for example:

```toml
http_headers = { "x-litellm-api-key" = "sk-DO-NOT-COMMIT-literal-test-abc123" }
```

If codex rejects that key on read, try the same header under `env_http_headers` (which maps a
header name to an environment VARIABLE name — record what you tried and what happened). Then:

```sh
codex mcp get probe-literal-04 --json > /tmp/04-observe-codex.json 2>&1; echo "exit=$?"
cat /tmp/04-observe-codex.json
rm -rf "$CODEX_HOME"; unset CODEX_HOME
```

Checklist to confirm before resuming:
1. Both read verbs' outputs are captured VERBATIM with their exit codes (the failed-dial exit code for
   Claude Code is a fact plan 04-05 needs: does `mcp get` exit nonzero when the dial fails?).
2. The literal dummy value's position in each output is visible (inside `Headers:` for Claude Code;
   under which JSON key for Codex).
3. The throwaway entry is removed from Claude Code (the post-remove `mcp get` reports not found) and
   the isolated `CODEX_HOME` is deleted.
4. No real credential appears anywhere in what you paste — only `sk-DO-NOT-COMMIT-literal-test-abc123`.
  </how-to-verify>
  <resume-signal>Paste, in your reply: (1) `claude --version` and `codex --version` output; (2) the verbatim content of `/tmp/04-observe-claude.txt` with its exit code; (3) the verbatim content of `/tmp/04-observe-claude-ref.txt` with its exit code; (4) the exact `config.toml` table heading codex wrote and the line(s) you added; (5) the verbatim content of `/tmp/04-observe-codex.json` (and the bearer-only control) with exit codes; (6) the word "removed" confirming both throwaway entries are gone. Or type "skipped" with a reason — in that case plan 04-05 cannot honestly build the literal-echo fixtures and must be deferred, not faked.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Record the observation as a dated `04-OBSERVATIONS.md` — commands, versions, verbatim captures, exit codes, and the facts plan 04-05 cites</name>
  <files>.planning/phases/04-drift-detection-read-only/04-OBSERVATIONS.md</files>
  <read_first>
    - The resume-signal payload from Task 1 (the maintainer's pasted captures) — the ONLY source of the verbatim sections; never reconstruct or "clean up" a capture.
    - `.planning/phases/04-drift-detection-read-only/04-CONTEXT.md` D-05, D-07, D-08 (what the record must carry) and §"Specific Ideas".
    - `.planning/phases/04-drift-detection-read-only/04-RESEARCH.md` §"Code Examples" — the verbatim codex 0.153.4 shape (compare the new capture's key set against it) and §"D-05 Manual Observation Protocol".
    - `.planning/milestones/2026-08-23.01-phases/03-runtime-registration/03-RESEARCH.md` lines 400-470 — the citation style (command, version, verbatim block, one-line finding) to follow.
    - `CLAUDE.md` §Conventions — NO SPDX header on any `.planning/**` file (a header above frontmatter breaks GSD's parsers).
  </read_first>
  <action>
Write `.planning/phases/04-drift-detection-read-only/04-OBSERVATIONS.md` (plain markdown, no
frontmatter, no SPDX header) with these sections in this order: a title `# Phase 4 — D-05 literal
header echo observation`; a provenance block: `**Observed:** <YYYY-MM-DD>` (today's date via
`date +%Y-%m-%d`), `**Observed by:** the maintainer, by hand (D-05); no automated test invokes these
CLIs (rule m45p2b4bp7)`, `**Dummy literal:** sk-DO-NOT-COMMIT-literal-test-abc123 — never a real
credential`; `## Environment` (the pasted `claude --version` / `codex --version` lines and the OS);
`## Protocol` (the exact commands run, verbatim from Task 1, in order, including the removals);
`## Claude Code — literal value` (a fenced block with the verbatim `/tmp/04-observe-claude.txt`
content and a line `exit code: N`); `## Claude Code — bare reference control` (same for the
`${LITELLM_KEY}` capture); `## Codex — bearer-only control` (the first `--json` capture, exit code);
`## Codex — literal header (hand-edited)` (the exact `config.toml` table heading and the added
line(s), then the fenced verbatim `--json` capture and exit code; if codex rejected the key,
record the rejection text verbatim and what was tried next); `## What this pins` — a bulleted fact
list plan 04-05 cites: for Claude Code, the complete ordered label set observed (every line
classified as name line / `Scope:` / `Status:` / `Type:` / `URL:` / `Headers:` / header line /
trailing hint / blank — one bullet per distinct line kind, quoting the label text only), the exact
`Status:` line(s) the failed dial produced, whether the literal value appears verbatim inside the
`Headers:` block in `Name: value` form, the exit code on a failed dial, and the not-found output +
exit code after removal; for Codex, which key carried the literal (`http_headers` /
`env_http_headers` / other), its JSON shape (object-of-strings / array / other — quote the
minimal fragment), whether `bearer_token_env_var` still rendered `DUMMY_04` beside it, and every
key present that is absent from the 03-RESEARCH.md 0.153.4 shape (or `none`). Do NOT paraphrase a
capture into the fact list — quote it.

If the resume signal said "skipped": write the file with the provenance block, the protocol, and
a `## Not observed` section stating the reason and that plan 04-05's literal-echo fixtures are
BLOCKED (never fabricated), then stop and report `## PLANNING INCONCLUSIVE`-style blockage to the
orchestrator in the SUMMARY.

Commit as `docs(04): record literal header echo observation for drift fixtures (D-05, D-08)` with
the explicit pathspec `.planning/phases/04-drift-detection-read-only/04-OBSERVATIONS.md`.
  </action>
  <verify>
    <automated>f=.planning/phases/04-drift-detection-read-only/04-OBSERVATIONS.md; test -s "$f" &amp;&amp; head -1 "$f" | rg -q -e '^# ' &amp;&amp; rg -q -F -e 'sk-DO-NOT-COMMIT-literal-test-abc123' "$f" &amp;&amp; test "$(rg -o -e 'sk-[A-Za-z0-9_-]+' "$f" | rg -v -F -e 'sk-DO-NOT-COMMIT-literal-test-abc123' | wc -l | tr -d ' ')" -eq 0 &amp;&amp; rg -q -F -e 'claude mcp get probe-literal-04' "$f" &amp;&amp; rg -q -F -e 'codex mcp get probe-literal-04 --json' "$f" &amp;&amp; rg -q -e '^\*\*Observed:\*\* 20[0-9]{2}-[0-9]{2}-[0-9]{2}' "$f" &amp;&amp; rg -q -e '^## What this pins' "$f" &amp;&amp; test "$(rg -c -e '^exit code: [0-9]+' "$f")" -ge 3 &amp;&amp; c=$(git log -1 --format=%H -- "$f") &amp;&amp; test -n "$c" &amp;&amp; names=$(git show --format= --name-only "$c") &amp;&amp; test "$(printf '%s\n' "$names" | rg -c -e '.')" -eq 1</automated>
    <fails_when>non-zero exit from any link: the file is missing or empty, its first line is not a `# ` title, the dummy marker is absent, any `sk-` token other than the dummy is present (a real-looking key reached the record), either read-verb command line is absent, the dated `**Observed:**` line is absent, the `## What this pins` heading is absent, fewer than three `exit code: N` lines exist, the record has no commit yet, or the commit that last touched the record lists more than one file (a `.go` or other file rode along)</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -c -e '^## ' .planning/phases/04-drift-detection-read-only/04-OBSERVATIONS.md` prints at least `7` (Environment, Protocol, two Claude Code sections, two Codex sections, What this pins).
    - `rg -n -F 'probe-literal-04' .planning/phases/04-drift-detection-read-only/04-OBSERVATIONS.md` prints at least six lines; `rg -n -e 'mcp (get|add|remove) engram\b' .planning/phases/04-drift-detection-read-only/04-OBSERVATIONS.md` prints nothing (the real registration was never named).
    - `rg -n -F 'Headers:' .planning/phases/04-drift-detection-read-only/04-OBSERVATIONS.md` prints at least one line inside a fenced capture (Claude Code's block) and `rg -n -e 'http_headers|env_http_headers' .planning/phases/04-drift-detection-read-only/04-OBSERVATIONS.md` prints at least one line (Codex's).
    - `head -1 .planning/phases/04-drift-detection-read-only/04-OBSERVATIONS.md` does not begin with `// SPDX` or `---`.
    - `git log -1 --format=%s` is `docs(04): record literal header echo observation for drift fixtures (D-05, D-08)` and `git show --stat HEAD --format=` lists exactly one file.
  </acceptance_criteria>
  <done>`04-OBSERVATIONS.md` is committed, dated, versioned, carries both runtimes' verbatim literal-echo captures with exit codes and the control shapes, names only the dummy literal, and its `## What this pins` section states every fact plan 04-05's scanners and fixtures cite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer's shell → pasted resume signal | Real CLI output from the maintainer's machine crosses into the conversation and then into a committed planning file. |
| conversation → `.planning/**` (git) | Whatever is written here is committed to the repository history permanently. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-08 | Information Disclosure | a real credential pasted from a real registration into `04-OBSERVATIONS.md` and committed | high | mitigate | The protocol registers a THROWAWAY entry with a fixed dummy value (`sk-DO-NOT-COMMIT-literal-test-abc123`) and never names `engram`; Task 2's verify rejects any other `sk-`-prefixed token; the executor stops and asks if the paste contains any other secret-looking value. |
| T-04-09 | Tampering | the protocol overwriting or removing the maintainer's real `engram` registration or real `~/.codex/config.toml` | high | mitigate | Throwaway name `probe-literal-04` on both runtimes; Codex runs under an isolated `CODEX_HOME=/tmp/engram-04-observe` that is deleted afterwards; the checklist requires the post-remove not-found confirmation. |
| T-04-10 | Information Disclosure | the dial to the throwaway URL during `claude mcp get` | low | accept | The URL is `127.0.0.1:1` — a local address that never answers; nothing leaves the machine (T-03-10's accepted dial, further scoped to loopback). |
| T-04-11 | Repudiation | a fixture later claiming an observed shape the record does not support | medium | mitigate | D-08: every 04-05 fixture cites this file by name; the `## What this pins` bullets quote the capture rather than paraphrase it, so the verifier can check fixture-vs-record by text. |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | Not applicable — no package of any kind is installed; the plan changes one markdown file. |
</threat_model>

<verification>
- `04-OBSERVATIONS.md` exists, is dated, names both CLI versions, carries three or more `exit code:` lines, both read-verb command lines, the dummy marker and no other `sk-` token, and a `## What this pins` section — Task 2's automated verify.
- `git diff --stat FIRST^..LAST` for this plan touches only the record.
- Not gated here and never automated (rule m45p2b4bp7): that the throwaway entries were removed — the human's "removed" confirmation in the resume signal is the record, mirrored into the SUMMARY.
- Scope note for the verifier: this plan carries REQ-drift-redaction and REQ-drift-observed-registration as the requirements it INFORMS (the literal-echo shape both depend on); their code-level satisfaction lives in plans 04-01, 04-04, and 04-05.
</verification>

<success_criteria>
- The literal-value echo shape of both `claude mcp get` and `codex mcp get --json` is recorded verbatim, dated, and versioned, from a throwaway entry, with the dummy literal only.
- Plan 04-05 can build every literal-echo fixture by quoting this record and citing it in a header comment (D-08).
</success_criteria>

<output>
Create `.planning/phases/04-drift-detection-read-only/04-02-SUMMARY.md` when done
</output>
