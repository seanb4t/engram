---
phase: 05-apply-time-preserve-gate-documentation
plan: 04
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
  - 05-03
files_modified:
  - .planning/phases/05-apply-time-preserve-gate-documentation/05-POST-RELEASE.md
autonomous: true
requirements:
  - REQ-docs-setup-v2

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`.planning/phases/05-apply-time-preserve-gate-documentation/05-POST-RELEASE.md` exists with EXACTLY the three frontmatter keys the `06-POST-RELEASE.md` precedent carries — `phase`, `status`, `tracker` — with `status: pending` and `tracker` an `https://github.com/seanb4t/engram/issues/<n>` URL, and the precedent's four headings (`# Post-release handoff`, `## Trigger and ownership`, `## Handoff checklist (...)`, `## Existing evidence`) — values filled, no invented key or heading (D-06, planning-artifacts rule 8dfdhfs5nn)"
    - "The handoff checklist names every qualifying-release check D-06 requires as a concrete, falsifiable step: a real `brew install seanb4t/tap/engram` of the next release in disposable environments; man pages present in the cask (`man -w engram-setup` resolving under the prefix's `share/man/man1`); `engram setup` observed on a real machine with plugin-first delivery, a `--header` registration, a `preserved` row, and the apply gate (`--apply` on a preserved registration leaving `claude mcp get engram` byte-identical before and after, the plugin facet still delivered, a converged re-run reading `already-correct`, and the OAuth re-login note on a would-write row in both lanes); the three guides' `Unreleased as of v0.16.1` notices replaced with the observed version and the two notice gate legs updated deliberately; `05-RELEASE-<ver>.md` recorded in the `06-RELEASE-0.16.0.md` shape; `post_release_status` flipped to `complete` (value only) and `REQ-docs-setup-v2` checked off ONLY then"
    - "REQ-docs-setup-v2 stays `[ ]` in `.planning/REQUIREMENTS.md` at the end of this phase, and the phase's verification record carries `post_release_status: pending` (plus `post_release_tracker` = the issue URL) — an interrupted or never-completed closeout leaves the open handoff VISIBLE to the milestone audit, never silently complete (edge probe `concurrency`/interruption for REQ-docs-setup-v2, resolved explicit; D-06)"
    - "The tracking issue exists on GitHub, titled for the Phase 5 post-release observation, and its body links the handoff file and lists the same checks; no other external change is published by this plan"
    - "The whole phase gate is green at the last commit: `task` (lint + test), `task license:check`, `git diff --exit-code -- go.mod go.sum`, `go test ./internal/keylinks/ -count=1`, `go test ./internal/setup/ -count=1 -shuffle=on`, `go run ./internal/surfacesgen --check-setup`, `pnpm --dir docs-site install --frozen-lockfile && pnpm --dir docs-site build`, and `git status --porcelain` empty over `skill/`, `internal/setupgen/`, `cmd/engram/testdata/catalog.golden`, `release-please-config.json`; zero occurrences of a replace-registration flag under `cmd/engram` and `internal/setup` (D-05)"
  artifacts:
    - path: ".planning/phases/05-apply-time-preserve-gate-documentation/05-POST-RELEASE.md"
      provides: "the D-06/D-10 human handoff: trigger, ownership, qualifying-release checklist, existing evidence; frontmatter phase/status/tracker"
      contains: "status: pending"
  key_links:
    - from: ".planning/phases/05-apply-time-preserve-gate-documentation/05-POST-RELEASE.md"
      to: ".planning/milestones/2026-08-23.01-phases/06-install-documentation/06-POST-RELEASE.md"
      via: "same frontmatter keys and headings as the precedent, values filled for Phase 5"
      pattern: 'tracker: https://github[.]com/seanb4t/engram/issues/[0-9]+'
    - from: ".planning/phases/05-apply-time-preserve-gate-documentation/05-POST-RELEASE.md"
      to: ".planning/REQUIREMENTS.md"
      via: "the checklist is what closes REQ-docs-setup-v2 — never code alone"
      pattern: 'REQ-docs-setup-v2'
  prohibitions:
    - statement: "MUST NOT check off REQ-docs-setup-v2 in REQUIREMENTS.md, set `post_release_status: complete`, or record a `05-RELEASE-<ver>.md` from code alone: those three moves happen only after a human observes the qualifying release per the checklist (D-06, the 2026-08-23.01 D-10 precedent)"
      category: values
      status: resolved
      verification: test
      reason: "Task 2's gate asserts the REQUIREMENTS.md line still reads `- [ ] **REQ-docs-setup-v2**` and that no `05-RELEASE-*.md` exists in the phase directory; the verifier reads this plan's verification note — no wired check descriptor by design (spec-less fallback, flagged-unverified)"
    - statement: "MUST NOT invent a frontmatter key, heading, or section shape in 05-POST-RELEASE.md beyond the 06-POST-RELEASE.md precedent, and MUST NOT write 05-VERIFICATION.md, ROADMAP.md, STATE.md, or REQUIREMENTS.md from a task (the verifier and the orchestrator own those; the ROADMAP progress row is filled by hand — gotcha yzmfesbsg0)"
      category: values
      status: resolved
      verification: test
      reason: "Task 1's acceptance criteria pin the exact key set and heading set; the plan's files_modified names only the handoff file — no wired check descriptor by design (spec-less fallback, flagged-unverified)"
    - statement: "MUST NOT run a real `claude`/`codex`/`opencode`/`brew` command or register against a real server as part of this plan: the live observation is the human's post-release step, never an executor's (rule m45p2b4bp7)"
      category: safety
      status: resolved
      verification: test
      reason: "the only external command Task 1 runs is `gh issue create`; Task 2 runs the repo's own gates — no wired check descriptor by design (spec-less fallback, flagged-unverified)"
---

<objective>
Close the phase the way D-06 prescribes: author `05-POST-RELEASE.md` in the exact shape of the
`2026-08-23.01` Phase 6 `06-POST-RELEASE.md` precedent (same frontmatter keys, same headings,
values filled for Phase 5) listing the qualifying-release checks — `brew install` of the next
release, man pages present in the cask, `engram setup` plugin-first + `--header` + `preserved` +
the apply gate observed on a real machine, guide notices replaced — open the GitHub tracking
issue the `tracker:` key names, and run the whole phase gate at the final commit. Phase 5's
verification then PASSES with `post_release_status: pending`, and `REQ-docs-setup-v2` stays
unchecked until a human records `05-RELEASE-<ver>.md`.

Purpose: REQ-docs-setup-v2 is the one requirement in this phase that code cannot close (its own
text says "with a post-release live observation recorded before the requirement is checked
off"). The handoff file is the artifact that keeps the open requirement visible to the milestone
audit instead of silently passing.

Note for the plan reviewer: Task 1 publishes ONE external change — a GitHub issue in
`seanb4t/engram` — because the precedent's `tracker:` key requires an issue URL and the repo's
own CLAUDE.md directs follow-ups into GitHub Issues. Nothing else leaves the repository.

Output: `05-POST-RELEASE.md`, the tracking issue, and a green phase gate.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-apply-time-preserve-gate-documentation/05-CONTEXT.md
@.planning/phases/05-apply-time-preserve-gate-documentation/05-RESEARCH.md
@.planning/phases/05-apply-time-preserve-gate-documentation/05-01-SUMMARY.md
@.planning/phases/05-apply-time-preserve-gate-documentation/05-02-SUMMARY.md
@.planning/phases/05-apply-time-preserve-gate-documentation/05-03-SUMMARY.md
@.planning/milestones/2026-08-23.01-phases/06-install-documentation/06-POST-RELEASE.md
@.planning/milestones/2026-08-23.01-phases/06-install-documentation/06-RELEASE-0.16.0.md
@.planning/milestones/2026-08-23.01-phases/06-install-documentation/06-VERIFICATION.md
@.planning/phases/04-drift-detection-read-only/04-OBSERVATIONS.md
</context>

## Artifacts this phase produces (plan 05-04's share)

| Kind | Artifact | Created by |
|------|----------|------------|
| Planning artifact | `.planning/phases/05-apply-time-preserve-gate-documentation/05-POST-RELEASE.md` (frontmatter `phase`/`status: pending`/`tracker`; four precedent headings) | 05-04 Task 1 |
| GitHub issue | tracking issue for the Phase 5 post-release observation (URL recorded in `tracker:`) | 05-04 Task 1 |
| Gate run | phase gate green at the final commit (recorded in the SUMMARY, no file) | 05-04 Task 2 |

## Flagged Assumptions (edge probe, spec-less fallback)

This plan authors **1** of the phase's 6 probe rows (05-01: 2 authored + 2 flagged; 05-02: 1):

| Requirement | Probe category | Disposition |
|---|---|---|
| REQ-docs-setup-v2 | `concurrency` (interruption) | **Authored (resolved, explicit)** — third truth: an interrupted closeout leaves the requirement `[ ]` and `post_release_status: pending`, visible to the milestone audit; the handoff is a serialized human step, so nothing runs in parallel with it. |

Additional flagged assumption (not a probe row): the `oauth-client` read-back shape on Claude
Code remains unobserved (04-CONTEXT.md D-07); D-03's shape rule treats it as `AuthNone` when it
carries no `Authorization` header. The checklist invites an opportunistic capture; the phase does
not depend on it.

## Prohibition probe (spec-less fallback, in-prompt recall)

Stage 1 raw candidates: must not check off the requirement from code; must not write
`post_release_status: complete`; must not invent frontmatter; must not edit ROADMAP/STATE/
REQUIREMENTS/VERIFICATION from a task; must not run a real CLI; must not mint a release tag;
must not close the issue; must not publish anything but the issue. Stage 2 keeps three
(authored above, descriptor-less): **never-close-from-code** (D-06), **no-invented-shape /
no-tool-owned-file-edits** (rule 8dfdhfs5nn, gotcha yzmfesbsg0), and **no-real-CLI** (m45p2b4bp7).
"Never mint a tag" and "never close the issue" are stated in the handoff's ownership section,
inherited verbatim from the precedent.

<!-- planner-discipline-allow: --replace-registration -->

<tasks>

<task type="auto">
  <name>Task 1: Author `05-POST-RELEASE.md` in the 06-POST-RELEASE.md shape (values only) and open the tracking issue its `tracker:` key names</name>
  <precondition>`gh auth status` exits 0 for github.com as an account with write access to seanb4t/engram (the tracker URL must be a real issue); if it does not, halt and report — do not write a placeholder URL.</precondition>
  <files>.planning/phases/05-apply-time-preserve-gate-documentation/05-POST-RELEASE.md</files>
  <read_first>
    - `.planning/milestones/2026-08-23.01-phases/06-install-documentation/06-POST-RELEASE.md` whole file — the frontmatter (`phase`, `status`, `tracker`), the four headings, the numbered-checklist style (concrete, falsifiable steps; "Release-please owns SemVer tags. Do not mint a tag"; "Do not infer issue closure"), and the `## Existing evidence` shape. The trailing `## Current disposition — <date>` section was added AFTER the observation — do not author one now.
    - `.planning/milestones/2026-08-23.01-phases/06-install-documentation/06-RELEASE-0.16.0.md` lines 1-60 (the observation record's frontmatter `phase`/`status: verified`/`observed`/`release`/`tracker` and its evidence-table style — the checklist points the future human at this shape for `05-RELEASE-<ver>.md`).
    - `.planning/milestones/2026-08-23.01-phases/06-install-documentation/06-VERIFICATION.md` lines 1-12 (`post_release_status` / `post_release_tracker` keys the verifier fills — the checklist tells the human which VALUE to flip, never to restructure).
    - `05-CONTEXT.md` D-06 (the exact check list: `brew install` of the next release; `engram setup` plugin-first + `--header` + `preserved` + the apply gate observed on a real machine; man pages present in the cask), §Deferred Ideas (the opportunistic `oauth-client` capture), `04-CONTEXT.md` D-07.
    - `05-RESEARCH.md` §"Man page naming" (`engram-setup.1`, the `engram{,-*}.1` cask glob), §"Validation Architecture" final row (the Manual-Only post-release observation).
    - The three plans' SUMMARYs (`05-01`, `05-02`, `05-03`) — the tests and docs gates that constitute the pre-merge evidence to list under `## Existing evidence`, with exact test names as shipped.
    - `.planning/REQUIREMENTS.md` line 61 (`REQ-docs-setup-v2` text — quote its "post-release live observation recorded before the requirement is checked off" clause in the ownership section).
    - `CLAUDE.md` §Session Completion ("File follow-ups — open GitHub issues") and §Conventions (no SPDX header on `.planning/**`; explicit pathspec on every commit).
  </read_first>
  <action>
    1. Open the tracking issue FIRST so its URL can be written into the frontmatter. If the
       orchestrator's dispatch prompt supplies an EXISTING tracking-issue URL (`TRACKER_URL`),
       use it verbatim and SKIP issue creation entirely — never mint a second issue. Otherwise
       (no URL supplied), write the body to the scratchpad, then run `gh issue create --repo seanb4t/engram --title "Phase 5
       post-release observation: apply-time preserve gate, plugin-first, --header, man pages
       (D-06 handoff)" --body-file <scratch>` and capture the printed URL. The body: one
       paragraph naming the handoff file path and D-06; the numbered checklist (same steps as the
       file, abbreviated); a closing line that the issue is closed only after
       `05-RELEASE-<ver>.md` is recorded. No labels (none are required; do not guess names). Do
       not assign anyone. End the body with the session's AI-authorship attribution line (the
       same line the session prescribes for PR descriptions).
    2. Write `.planning/phases/05-apply-time-preserve-gate-documentation/05-POST-RELEASE.md`
       with frontmatter EXACTLY: `phase: 05-apply-time-preserve-gate-documentation`, `status:
       pending`, `tracker: <the issue URL>` — three keys, nothing else, no SPDX header. Body
       headings EXACTLY the precedent's four:
       - `# Post-release handoff` — an opening paragraph: the handoff is OPEN; Phase 5's code and
         docs merged with pre-merge evidence only; `REQ-docs-setup-v2` stays unchecked and
         `05-VERIFICATION.md` carries `post_release_status: pending` until the checks below are
         observed on the qualifying release; the milestone audit sees this as an open handoff.
       - `## Trigger and ownership` — after the normal reviewed merge and a release-please release
         that contains Phase 1-5 (man pages, `--header`, plugin-first, drift detection, the apply
         gate), resume with the actual tag and workflow run. Release-please owns SemVer tags; do
         not mint a tag to satisfy this handoff. The observation is a human step on a real
         machine — never a test (rule m45p2b4bp7); use a throwaway MCP entry / disposable
         environment and never a production credential.
       - `## Handoff checklist (qualifying-release observations to collect)` — numbered,
         concrete, falsifiable:
         1. Refresh release metadata: tag, release merge commit, publishing run with
            `SKIP_HOMEBREW_UPLOAD=false`, cask revision and the four archive checksums matching
            the release assets.
         2. Repeat an actual `brew install seanb4t/tap/engram` in disposable macOS and Linux
            environments for amd64 and arm64; preserve OS/arch, native vs translated execution,
            Homebrew version/prefix, binary identity, installed JSON version, and completion
            results (the 06 shape) — PLUS man pages: `man -w engram` and `man -w engram-setup`
            resolve under `<prefix>/share/man/man1`, `engram-setup.1` renders, and `brew
            uninstall` removes `engram{,-*}.1`.
         3. `engram setup` on a real machine with the installed release: (a) plugin-first —
            `--apply` on a Claude Code / Codex with a working plugin CLI reports the plugin facet
            and `claude plugin list` / `codex plugin list` show `engram@engram`; (b) `--header
            x-gateway-api-key=GATEWAY_KEY` registers and the runtime's read verb echoes the bare
            reference (compare with `04-OBSERVATIONS.md`); (c) `preserved` — a throwaway entry
            hand-edited to carry an extra header reads `preserved` in preview naming the header
            and the manual step; (d) the apply gate — `--apply` on that preserved entry leaves
            `claude mcp get engram` (and codex's `mcp get --json`) byte-identical before and after,
            exits 0, and still delivers the plugin facet; (e) a converged install re-run reads
            `already-correct` with no re-login prompt in Claude Code; (f) an OAuth-shaped
            would-write row shows the re-login note in preview and apply `notes`; (g)
            opportunistic — capture the `oauth-client` read-back shape (04-CONTEXT.md D-07).
            Record verbatim, redacted captures.
         4. After those facts pass, replace the `Unreleased as of v0.16.1` asides in
            `install.md`, `agent-setup.md`, `plugin.md` with the observed release, DELIBERATELY
            update the two notice gate legs (`install_docs_test.go` / `plugin_docs_test.go` leg 4)
            to the availability wording, rebuild/lint the site, and record
            `05-RELEASE-<ver>.md` in the `06-RELEASE-0.16.0.md` shape (`phase`, `status:
            verified`, `observed`, `release`, `tracker`; evidence tables).
         5. Flip `post_release_status` to `complete` in `05-VERIFICATION.md` (value only), check
            off `REQ-docs-setup-v2` in `REQUIREMENTS.md`, reconcile the tracking issue. Do not
            infer issue closure or unrelated checklist completion from these observations.
       - `## Existing evidence` — pre-merge only: the apply-gate tests (name them as shipped:
         `TestApplyPreservedIssuesZeroWrites`, `TestApplyPreservedNeverRunsClaudeCodeRemove`,
         `TestApplyAlreadyCorrectIssuesZeroWrites`, `TestApplyWroteRegisteredIsRedacted`,
         `TestOAuthReLoginConsequence`, `TestSetupApplyPreservedRuntimeSkipsRegistrationWrite`,
         `TestSetupHelpStatesApplyGate`), the three docs gates, `04-OBSERVATIONS.md`'s read-verb
         shapes (codex-cli/claude versions as recorded), and the explicit statement that the
         `oauth-client` shape and the man-page cask hook are unobserved on a released build.
    3. Commit with an explicit pathspec: `docs(05): record the post-release handoff for the docs
       requirement` — body names the issue URL and D-06.
  </action>
  <verify>
    <automated>f=.planning/phases/05-apply-time-preserve-gate-documentation/05-POST-RELEASE.md; test -s "$f" && head -1 "$f" | rg -q -e '^---$' && test "$(sed -n '2,/^---$/p' "$f" | rg -v -e '^---$' | rg -o -e '^[a-z_]+:' | wc -l | tr -d ' ')" -eq 3 && rg -q -e '^phase: 05-apply-time-preserve-gate-documentation$' "$f" && rg -q -e '^status: pending$' "$f" && url=$(rg -o -e '^tracker: https://github[.]com/seanb4t/engram/issues/[0-9]+$' "$f" | sed 's/^tracker: //') && test -n "$url" && gh issue view "$url" --json state,title --jq '.state + " " + .title' | rg -q -e '^OPEN Phase 5 post-release observation' && rg -q -e '^# Post-release handoff$' "$f" && rg -q -e '^## Trigger and ownership$' "$f" && rg -q -e '^## Handoff checklist' "$f" && rg -q -e '^## Existing evidence$' "$f" && test "$(rg -o -e '^## ' "$f" | wc -l | tr -d ' ')" -eq 3 && rg -q -F -e 'man -w engram-setup' "$f" && rg -q -F -e 'brew install seanb4t/tap/engram' "$f" && rg -q -F -e 'preserved' "$f" && rg -q -F -e 'REQ-docs-setup-v2' "$f" && rg -q -F -e 'post_release_status' "$f" && ! rg -q -F -e 'SPDX-License-Identifier' "$f" && c=$(git log -1 --format=%H -- "$f") && test -n "$c" && names=$(git show --format= --name-only "$c") && test "$(printf '%s\n' "$names" | rg -o -e '.+' | wc -l | tr -d ' ')" -eq 1</automated>
    <fails_when>non-zero exit at any link: a missing or empty file, a frontmatter key count other than 3, `status` not `pending`, the tracker not an issue URL that `gh issue view` reports as OPEN with the expected title prefix, any of the four headings absent or extra `## ` headings present (count must be exactly 3), a required checklist token absent, an SPDX header present, or the commit touching more than this one file</fails_when>
  </verify>
  <acceptance_criteria>
    - Frontmatter keys are exactly `phase`, `status`, `tracker` in that order (`sed -n '2,4p' "$f"` prints those three lines; line 5 is `---`).
    - `rg -o -e '^[0-9]+[.] ' "$f" | wc -l | tr -d ' '` prints `5` (five numbered checklist steps).
    - The checklist names each D-06 check: `rg -q -F 'plugin-first' "$f"`, `rg -q -F -e '--header' "$f"`, `rg -q -F 'apply gate' "$f"` (or `--apply`), `rg -q -F 'already-correct' "$f"`, `rg -q -F 'log in' "$f"`, `rg -q -F 'oauth-client' "$f"`, `rg -q -F '05-RELEASE-' "$f"`, `rg -q -F 'Unreleased as of v0.16.1' "$f"` all exit 0.
    - Ownership sentences present: `rg -q -F 'Do not mint a tag' "$f"` and `rg -q -F 'Do not infer issue closure' "$f"` exit 0.
    - `.planning/REQUIREMENTS.md` still contains the line beginning `- [ ] **REQ-docs-setup-v2**` (`rg -q -F -e '- [ ] **REQ-docs-setup-v2**' .planning/REQUIREMENTS.md` exits 0) and `ls .planning/phases/05-apply-time-preserve-gate-documentation/05-RELEASE-*.md 2>/dev/null` prints nothing.
    - The issue body links the handoff file path and the issue is not assigned to anyone (`gh issue view "$url" --json assignees --jq '.assignees | length'` prints `0`).
  </acceptance_criteria>
  <done>The handoff file exists in the precedent's exact shape with `status: pending` and a live tracking issue; the requirement remains open and visibly so.</done>
</task>

<task type="auto">
  <name>Task 2: Phase gate at the final commit — full `task`, license, key-links, shuffle, surfaces drift, docs build, generated-path cleanliness, no replace-registration flag, and the docs requirement still open</name>
  <files>none modified — verification-only; if `task fmt` or the license fixer rewrites a file touched by this phase, fold it into a `chore(05)` commit with an explicit pathspec and record it in the SUMMARY</files>
  <read_first>
    - `Taskfile.yaml` — `default` (lint + test), `lint` (golangci-lint, yamlfmt, actionlint, rumdl), `license:check`, `surfaces:gen` (NOT run here — only its `--check-setup` counterpart).
    - `.planning/STATE.md` §Blockers/Concerns — the key-links gate, the `-count=1`-only rule for `cmd/engram` tests, the red-evidence harness (`TestRedEvidencePatchesAreLive` in `internal/store` keeps `task` red until the orchestrator registers this phase's patches — if `task` fails ONLY on that test, record it as the expected orchestrator step, not a phase defect, and still report the rest of the gate).
    - `internal/store/redevidence_harness_test.go` lines 79-110 (what the orchestrator will register; never edit this file from a task).
    - The four plans' `<verification>` blocks (the per-plan gates this task re-runs together).
  </read_first>
  <action>
    Run the phase gate from the repository root in this order and record every exit code in the
    SUMMARY: `task`; `task license:check`; `git diff --exit-code -- go.mod go.sum docs-site/package.json docs-site/pnpm-lock.yaml`;
    `go test ./internal/keylinks/ -count=1`; `go test ./internal/setup/ -count=1 -shuffle=on`;
    `go test ./cmd/engram/ -count=1`; `go run ./internal/surfacesgen --check-setup`;
    `pnpm --dir docs-site install --frozen-lockfile && pnpm --dir docs-site build`; `git status --porcelain -- skill/ internal/setupgen/ cmd/engram/testdata/catalog.golden release-please-config.json` (must print nothing);
    the D-05 no-flag grep over `cmd/engram` and `internal/setup` (zero occurrences of a
    `replace-registration` flag); and the D-06 open-requirement check on `REQUIREMENTS.md`. If
    `task` is red solely because `internal/store`'s red-evidence liveness test has no Phase 5
    entry yet, say so explicitly in the SUMMARY (the orchestrator registers the patches suggested
    in the four plans' `<verification>` blocks after this plan) and confirm every OTHER package
    is green via `go test $(go list ./... | rg -v -F internal/store) -count=1`. Do not modify any
    file to make a gate pass except a formatter/license rewrite as noted in `<files>`.
  </action>
  <verify>
    <automated>task && task license:check && git diff --exit-code -- go.mod go.sum docs-site/package.json docs-site/pnpm-lock.yaml && go test ./internal/keylinks/ -count=1 && go test ./internal/setup/ -count=1 -shuffle=on && go test ./cmd/engram/ -count=1 && go run ./internal/surfacesgen --check-setup && pnpm --dir docs-site install --frozen-lockfile && pnpm --dir docs-site build >/dev/null && test -z "$(git status --porcelain -- skill/ internal/setupgen/ cmd/engram/testdata/catalog.golden release-please-config.json)" && test "$(rg -o -F -e '--replace-registration' cmd/engram internal/setup | wc -l | tr -d ' ')" -eq 0 && rg -q -F -e '- [ ] **REQ-docs-setup-v2**' .planning/REQUIREMENTS.md && test -s .planning/phases/05-apply-time-preserve-gate-documentation/05-POST-RELEASE.md</automated>
    <fails_when>non-zero exit from any command (a `task` failure prints the failing lint or `--- FAIL` test; `surfacesgen --check-setup` prints a drift diff; the Astro build prints an error), `git status --porcelain` printing any generated path, a nonzero count for the replace-registration flag, the REQ-docs-setup-v2 line no longer unchecked, or the handoff file missing</fails_when>
  </verify>
  <acceptance_criteria>
    - Every command in `<automated>` exits 0 at the phase's final commit, OR `task` fails only on `internal/store`'s `TestRedEvidencePatchesAreLive` with every other package green (`go test $(go list ./... | rg -v -F internal/store) -count=1` exits 0) and the SUMMARY names that as the pending orchestrator step.
    - `git status --porcelain` is empty except for files the orchestrator owns (`.planning/state.json`, phase SUMMARY files being written).
    - The SUMMARY's verification section states verbatim that Phase 5's verification is expected to PASS with `post_release_status: pending` and `post_release_tracker: <issue URL>` in `05-VERIFICATION.md`, and that `REQ-docs-setup-v2` stays `[ ]` until `05-RELEASE-<ver>.md` is recorded.
    - No task in this plan ran `roadmap update-plan-progress` or any `roadmap` write verb, and no task edited `redevidence_harness_test.go`, `05-VERIFICATION.md`, `ROADMAP.md`, `STATE.md`, or `REQUIREMENTS.md` (`git log --format=%H -- .planning/ROADMAP.md .planning/STATE.md .planning/REQUIREMENTS.md internal/store/redevidence_harness_test.go` shows no commit authored by this plan).
  </acceptance_criteria>
  <done>The phase gate is green (or red only on the orchestrator-owned red-evidence registration step, recorded as such), the generated surfaces are clean, no consent flag exists, and the docs requirement is visibly still open with its handoff in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning artifact -> milestone audit / verifier | The handoff's frontmatter is parsed by tool-owned readers; an invented key or heading silently changes what the audit sees. |
| executor -> GitHub | One issue is created; nothing is closed, assigned, labeled by guess, or tagged. |
| phase gate -> `main` | The final commit must pass every repo gate the milestone's Phase 1 added (key-links, red-evidence liveness, surfaces drift). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-15 | Repudiation | REQ-docs-setup-v2 checked off, or `post_release_status` set to `complete`, from code alone — the audit then believes the docs were observed on a release | high | mitigate | No task touches REQUIREMENTS.md or the verification record; Task 2's gate asserts the unchecked line and the absence of any `05-RELEASE-*.md`; the SUMMARY carries the explicit `pending` statement for the verifier. |
| T-05-16 | Tampering | an invented frontmatter key/heading in `05-POST-RELEASE.md` (or a stub milestone heading anywhere in ROADMAP.md) truncating what tool-owned parsers see (rule 8dfdhfs5nn) | medium | mitigate | Task 1's verify counts exactly 3 frontmatter keys and exactly 3 `## ` headings and pins the precedent's names; ROADMAP/STATE are never written by a task. |
| T-05-17 | Elevation of Privilege | the executor publishing more than the sanctioned issue (closing issues, tagging a release, assigning people) | medium | mitigate | Task 1 creates one issue with no labels/assignees; the handoff states "Do not mint a tag"; acceptance asserts zero assignees and OPEN state. |
| T-05-05 | Elevation of Privilege | a replace-registration flag slipping in anywhere in the phase | medium | mitigate | Task 2's phase-wide zero-occurrence grep over `cmd/engram` and `internal/setup`. |
| T-05-18 | Denial of Service (CI) | a gate silently green because it examined nothing (a `-run` filter matching no tests, a `porcelain` check over a mistyped path) | low | mitigate | Task 2 runs whole-package `-count=1` tests and the repo's own `task`; every `rg` gate is a positive-presence check or a counted `-o | wc -l`, never a bare `-c` over a commented file. |
| T-05-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages anywhere in the phase; the gate diffs `go.mod`/`go.sum` and the docs-site lockfile. |
</threat_model>

<verification>
- Task 1's `<automated>` chain and Task 2's phase gate both exit 0 (Task 2 modulo the orchestrator-owned red-evidence registration, if `task` is red only there).
- Red-evidence handoff for the orchestrator: this plan suggests no patch of its own (it ships no production code). The phase's suggested patch set is the union of plans 05-01 (five), 05-02 (three), and 05-03 (two) — register them in `internal/store/redevidence_harness_test.go`'s `redEvidenceDirs` under `.planning/phases/05-apply-time-preserve-gate-documentation/red-evidence/` after this plan, before `/gsd-verify-work`.
- Verification-record note for `/gsd-verify-work` (D-06, stated here so the verifier and the audit read the same sentence): **Phase 5's verification PASSES with `post_release_status: pending` and `post_release_tracker: <the issue URL from 05-POST-RELEASE.md>` in `05-VERIFICATION.md`. `REQ-docs-setup-v2` remains `[ ]` in REQUIREMENTS.md until a human records `05-RELEASE-<ver>.md` after the qualifying release; the milestone audit must treat the open handoff as expected, not as a gap.** REQ-apply-preserve-gate and REQ-apply-rewrite-consequence are code-verified by plans 05-01/05-02 and may be checked off.
- ROADMAP progress row: the orchestrator fills it by hand (gotcha yzmfesbsg0, eight occurrences); no task in this phase runs a `roadmap` write verb.
</verification>

<success_criteria>
- SC4's closeout half: `05-POST-RELEASE.md` exists in the precedent's shape with `status: pending` and a live tracker; `REQ-docs-setup-v2` is visibly open with every qualifying-release check enumerated.
- The whole phase gate is green at the final commit; generated surfaces clean; no consent flag; the red-evidence patch set is handed to the orchestrator.
</success_criteria>

<output>
Create `.planning/phases/05-apply-time-preserve-gate-documentation/05-04-SUMMARY.md` when done
</output>
