---
phase: 05
slug: slash-command-delegation
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-09-12
---

# Phase 5 — Validation Strategy

## Test Infrastructure

| Property | Value |
|----------|-------|
| Framework | Go testing with injected environments and temporary files |
| Config file | Taskfile.yaml |
| Quick run command | `go test ./internal/setup/... ./internal/setupgen/... ./cmd/engram/... -count=1` (setupgen added during execution) |
| Full suite command | `task` |
| Estimated runtime | 60 seconds focused; full suite measured during execution |

## Sampling Rate

- After each task: run the relevant affected package tests.
- After every plan wave and before acceptance: run `task`.
- Resolve targeted test names using `go test -list` and require verbose RUN/PASS evidence.
- Max feedback latency target: 60 seconds for focused tests.

## Per-Task Verification Map

The planner fills task IDs and final test names; the executor reconciles them against shipped tests.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| To assign | To assign | To assign | REQ-engram-setup-delegates | Client-ID validation | OAuth-client uses caller input; missing input fails before execution | unit | `go test ./internal/setup/... ./cmd/engram/... -count=1` | Existing packages; new tests required | pending |
| To assign | To assign | To assign | REQ-engram-setup-prose-fallback | Credential exposure | Generated auth rows preserve four choices without real secrets | unit | `go test ./internal/setupgen/... -count=1` | New package required | pending |
| To assign | To assign | To assign | REQ-delegation-equivalence-derived | Stale generation | Mutating an argv token changes output; malformed anchors fail without writes | unit/integration | `go test ./internal/setupgen/... ./internal/surfaces/... -count=1` | New generator tests required | pending |
| To assign | To assign | To assign | REQ-delegation-equivalence-derived | Mutating lint | Read-only drift check rejects corrupt fixture and accepts regenerated bytes | integration | Final focused task assigned by planner; `task` includes check | New check required | pending |

## Wave 0 Requirements

Existing Go test infrastructure is sufficient. First implementation tasks create generator tests
and add client-ID input/validation tests. All environment and process interactions use injected
fakes or scratch paths; no test runs real runtime registration or writes a real user home.

## Manual-Only Verifications

The slash command's branching and preview/confirmation instructions receive source review.
Automated checks prove engram's generated bytes, CLI validation, and fake execution behavior;
they do not claim to prove a third-party agent follows prose. Any actual live observation is
recorded separately with its scope and evidence.

## Validation Sign-Off

Pending implementation: reconcile task mapping, test references, RUN/PASS evidence, sampling
continuity, and full quality gate before setting validated or nyquist_compliant.
