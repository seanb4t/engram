---
phase: 05-slash-command-delegation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/engram/setup.go
  - cmd/engram/setup_test.go
  - cmd/engram/destructive_test.go
  - cmd/engram/testdata/help.golden
  - cmd/engram/testdata/catalog.golden
  - internal/setup/runtime.go
  - internal/setup/claudecode.go
  - internal/setup/claudecode_test.go
  - internal/setup/codex.go
  - internal/setup/codex_test.go
  - internal/setup/plan_test.go
autonomous: true
requirements: [REQ-engram-setup-delegates]
estimate:
  tokens: 27000
  raw_tokens: 27000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - OAuth-client setup accepts a caller-supplied client ID through --client-id and carries that exact value into Claude Code and Codex registration argv.
    - Missing or whitespace-only OAuth-client IDs and a client-ID flag supplied for another auth mode fail with exitUsage before any runtime probe, registration, or skills write.
    - Valid OAuth-client input preserves unsupported runtime/auth rows and existing preview/apply outcome classification.
    - Help and catalog advertise the same client-ID option accepted by the command.
  artifacts:
    - path: cmd/engram/setup.go
      provides: Flag binding, setupResolve validation, and help for client ID
    - path: internal/setup/runtime.go
      provides: Options.ClientID non-secret input
    - path: internal/setup/claudecode.go
      provides: Claude Code registration argv consuming Options.ClientID
    - path: internal/setup/codex.go
      provides: Codex registration argv consuming Options.ClientID
  key_links:
    - from: cmd/engram/setup.go
      to: internal/setup/runtime.go
      via: setupResolve returns Options.ClientID
      pattern: 'ClientID:'
    - from: internal/setup/claudecode.go
      to: internal/setup/runtime.go
      via: Plan consumes caller input
      pattern: 'opts.ClientID'
    - from: internal/setup/codex.go
      to: internal/setup/runtime.go
      via: Plan consumes caller input
      pattern: 'opts.ClientID'
---

<objective>
Make pre-registered OAuth usable through the existing CLI entry point, per D-17, so the following delegation plan can offer all four auth choices without an exception. Preserve D-18's environment-based credential contract and D-19's distinction between registration argv and execution orchestration.

Purpose: Remove the missing input that previously made oauth-client delegation nonfunctional.
Output: Tested client-ID value path, clear validation, and generated CLI help/catalog updates.
</objective>

<execution_context>
@/Users/sean/.codex/gsd-core/workflows/execute-plan.md
@/Users/sean/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-slash-command-delegation/05-CONTEXT.md
@.planning/phases/05-slash-command-delegation/05-PATTERNS.md
@.planning/phases/05-slash-command-delegation/05-RESEARCH.md
</context>

<assumption_delta_decision>
Primary noun: setup.Options is the registration input for every entry point. Decision: no-change. The new ClientID field fills an existing oauth-client variant rather than introducing a competing auth model. Client IDs are non-secret opaque values; no new environment alias is required. D-17 explicitly authorizes this parameter. D-18 keeps secrets in runtime-owned environment prerequisites; this phase adds neither secret flags nor subprocess stdin.
</assumption_delta_decision>

## Artifacts this phase produces

- `setup.Options.ClientID string` in `internal/setup/runtime.go`.
- `--client-id` and its flag-only `setupClientID` binding in `cmd/engram/setup.go`; validation in existing `setupResolve`.
- Existing `ClaudeCode.Plan` and `Codex.Plan` consume ClientID; no new runtime type or auth enum.
- Tests `TestSetupClientID`, `TestClaudeCodeClientID`, and `TestCodexClientID`; existing matrix, help, catalog, and destructive-flag conformance fixtures updated.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Carry a real client ID from the CLI to Claude registration</name>
  <files>cmd/engram/setup.go, cmd/engram/setup_test.go, internal/setup/runtime.go, internal/setup/claudecode.go, cmd/engram/destructive_test.go</files>
  <read_first>cmd/engram/setup.go; cmd/engram/setup_test.go; cmd/engram/destructive_test.go; internal/setup/runtime.go; internal/setup/claudecode.go; internal/setup/environment.go; cmd/engram/clienttest_test.go; .planning/phases/05-slash-command-delegation/05-CONTEXT.md</read_first>
  <behavior>
    - With fake setup and skills environments, oauth-client plus --client-id test-client reaches Claude's add action as one exact argv value in preview and apply.
    - A missing, empty, or whitespace-only ID for oauth-client returns exitUsage before any Run call or skills filesystem write.
    - Supplying --client-id for oauth, bearer, or none returns exitUsage, including an explicitly empty flag; default oauth counts as oauth.
    - IDs with spaces or shell metacharacters remain one opaque argv token and display through existing Action.Command quoting.
  </behavior>
  <action>Add Options.ClientID and a flag-only string binding named setupClientID with empty default and Cobra flag --client-id, per D-17. In setupResolve, after normalizing auth and before building/probing any plan, require strings.TrimSpace(setupClientID) to be nonempty for oauth-client; validate emptiness without rewriting a nonempty caller value. For every other auth mode reject cmd.Flags().Changed("client-id") with usageErrorf. Return the original client ID in Options and replace ClaudeCode.Plan's authored client-ID value with opts.ClientID. Keep secret handling and osRun unchanged per D-18. Extend the destructive-command flag inventory with client-id and retain every pre-existing setup flag. Implement TestSetupClientID through runClient and withFakeSetupEnv, counting all Run and skills mutations; any apply uses fakes exclusively. Add the ID to the existing unsupported-auth CLI fixture so it still reaches the unsupported row rather than failing the new input guard. Reset the new binding via the established command-reset seam between cases.</action>
  <verify><automated>go test ./cmd/engram -run '^TestSetupClientID$' -count=1 -v</automated></verify>
  <acceptance_criteria>The verbose test emits RUN/PASS for preview, fake apply, missing/empty/whitespace IDs, irrelevant flag, and metacharacter value cases. Invalid input yields ExitCode()==exitUsage and zero runtime/skills effects. The captured Claude add argv contains exactly the supplied ID after --client-id.</acceptance_criteria>
  <done>The CLI-to-Claude argv path works end-to-end with caller input and fails early on invalid input; the tracer is committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand client-ID consumption to Codex and reconcile the runtime matrix</name>
  <files>internal/setup/codex.go, internal/setup/codex_test.go, internal/setup/claudecode_test.go, internal/setup/plan_test.go, cmd/engram/setup_test.go</files>
  <read_first>internal/setup/codex.go; internal/setup/codex_test.go; internal/setup/claudecode_test.go; internal/setup/plan_test.go; cmd/engram/setup_test.go; internal/setup/claudecode.go; internal/setup/runtime.go; internal/setup/opencode.go; internal/setup/generic.go</read_first>
  <behavior>
    - Codex oauth-client renders the exact provided ClientID after --oauth-client-id.
    - Claude and Codex tests compare complete authored argv, retaining all unrelated tokens and action tolerance.
    - The all-runtime auth matrix supplies ClientID for oauth-client and retains opencode/generic unsupported results.
    - CLI fake apply and preview carry caller IDs to both supported native runtimes; default selection still includes detected runtimes without adding generic.
  </behavior>
  <action>Per D-17, replace Codex.Plan's client-ID value with opts.ClientID and update every existing oauth-client fixture in these files to supply a synthetic non-secret value. Add TestClaudeCodeClientID and TestCodexClientID using synthetic Environment, asserting full argv and unchanged ancillary actions. Extend TestSetupClientID with Codex and mixed present-runtime cases using the existing environment seams. Keep unsupported combinations explicit; do not add oauth-client support to opencode or generic. Preserve all four choices, remove/add behavior, probes, Outcome vocabulary, and Classify. No validation against a real provider is needed: the contract is an opaque caller value reaching engram-owned argv.</action>
  <verify><automated>go test ./internal/setup ./cmd/engram -run 'Test(ClaudeCodeClientID|CodexClientID|SetupClientID|ClaudeCodePlan|Plan.*OAuthClient|SetupUnsupportedAuthMode)' -count=1 -v</automated></verify>
  <acceptance_criteria>Both supported runtime add forms carry caller input, the full runtime package tests remain green, and existing unsupported-runtime CLI tests still produce failed report rows with their prior classification.</acceptance_criteria>
  <done>All runtime-auth fixtures reflect the new input, while supported and unsupported runtime capabilities retain their authored boundaries.</done>
</task>

<task type="auto">
  <name>Task 3: Publish the input contract through CLI help and catalog</name>
  <files>cmd/engram/setup.go, cmd/engram/setup_test.go, cmd/engram/testdata/help.golden, cmd/engram/testdata/catalog.golden</files>
  <read_first>cmd/engram/setup.go; cmd/engram/setup_test.go; cmd/engram/testdata/help.golden; cmd/engram/testdata/catalog.golden; cmd/engram/golden_test.go; Taskfile.yaml; .planning/phases/05-slash-command-delegation/05-CONTEXT.md</read_first>
  <action>Per D-17 and D-18, state in setup's long help and --client-id flag help that oauth-client requires the non-secret client ID and other modes reject the flag. Add a worked oauth-client preview example with a synthetic client ID. State that Claude's scripted client-secret prerequisite is MCP_CLIENT_SECRET in its inherited environment; the engram executor has no interactive stdin. Preserve the bearer ENGRAM_TOKEN explanation and token-file's generic-only scope. Extend the existing help tests with these accepted constraints. Regenerate help/catalog using task surfaces:gen, the sole production path per D-12 and D-20; inspect generated diffs and include only the two affected goldens. Do not hand-edit goldens or change Phase 6 installation docs.</action>
  <verify><automated>go test ./cmd/engram -run 'Test(SetupHelp|HelpGolden|CatalogGolden)' -count=1 -v</automated></verify>
  <acceptance_criteria>Live Cobra help and catalog expose client-id with its mode restriction; regenerated goldens pass without -update; the diff adds no secret values or unrelated surface changes.</acceptance_criteria>
  <done>The CLI option is discoverable and the repository's generated help/catalog are current.</done>
</task>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| CLI input to runtime argv | Caller supplies an opaque ID; validation precedes external effects. |
| Test harness to local configuration | Fake setup/skills environments contain every simulated apply. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-01 | Tampering | setupResolve / runtime Plan | high | mitigate | Validate required and irrelevant IDs before effects; preserve exact argv tokens and existing quoting; exercise metacharacter cases. |
| T-05-02 | Information disclosure | setup help / subprocess boundary | high | mitigate | Client ID alone is an input; preserve environment credential references and nil stdin; use synthetic test values. |
| T-05-03 | Tampering | command apply tests | high | mitigate | Use withFakeSetupEnv including the skills seam; count writes and process calls on rejection. |

ASVS Level 1; high-severity findings block. No package install or schema change is planned.
</threat_model>

<verification>
Run go test ./internal/setup ./cmd/engram -count=1 and task after the wave. Resolve each targeted test using go test -list and preserve verbose RUN/PASS evidence. Real OAuth completion and runtime compatibility are not inferred from fake execution.
</verification>

<success_criteria>D-17's missing-input defect is closed for both existing supported runtimes; D-18's credential boundary and previous outcomes remain intact. Help and catalog match the actual CLI.</success_criteria>

<output>Create .planning/phases/05-slash-command-delegation/05-01-SUMMARY.md with validation evidence and actual file changes.</output>
