---
phase: 02-record-schema-versioning-foundation
plan: 04
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - internal/store/schemaversion_compat_test.go
autonomous: true
requirements:
  - REQ-schema-version-forward-compatible

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A record whose raw Qdrant payload carries schema_version at migrate.CurrentVersion + 1 PLUS a payload key this binary has never heard of decodes without error, is returned by every recall path, is returned by Store.Get, reads back at CurrentVersion + 1, and is still at CurrentVersion + 1 after a subsequent Store.Update."
    - "The unknown payload key is neither rejected nor allowed to corrupt the decode: fromPayload's per-field guarded reads ignore it, and every known field on the same record decodes correctly alongside it."
    - "The mirror older-than-binary case is covered by the key-absent legacy record: it decodes to migrate.Version(0), is returned by every recall path and by Store.Get, and a subsequent Store.Update neither hides nor rejects it."
    - "The compatibility table's executed row count is asserted equal to a count derived from migrate.CurrentVersion — two rows while CurrentVersion is 0, three once it is greater — so the explicit older-than row activates automatically when a later phase raises the constant, instead of silently staying skipped."
    - "No code path rejects, hides, or downgrades a record for its version: the divergence from internal/webauth's sessionPayloadVersion reject-on-mismatch behaviour is asserted by a test, not assumed from the absence of a reject branch."
  artifacts:
    - path: "internal/store/schemaversion_compat_test.go"
      provides: "Raw-payload-injection proof of forward and backward version compatibility against real Qdrant"
      contains: "TestSchemaVersionForwardBackwardCompat"
  key_links:
    - from: "internal/store/schemaversion_compat_test.go"
      to: "internal/migrate/migrate.go"
      via: "every injected and expected version is derived from migrate.CurrentVersion, never a literal"
      pattern: 'migrate\.CurrentVersion'
    - from: "internal/store/schemaversion_compat_test.go"
      to: "internal/store/store.go"
      via: "raw client SetPayload injection bypassing payload(), then read back through fromPayload via Store.Get and the recall paths"
      pattern: 'SetPayload'
---

<objective>
Prove ROADMAP success criterion 5 — a binary reads a record whose `schema_version` is NEWER than its
own constant without rejecting, hiding, or downgrading it, tested in both the older-than and
newer-than direction.

Purpose: this is what makes rolling the binary back across a schema change safe. The proof must be
raw payload injection against real Qdrant, not a test-only override of the version constant: an
override exercises the stamping plumbing, whereas rollback safety actually depends on the
*decode-an-unfamiliar-payload* behaviour, which is the thing that breaks in production. The
divergence from `internal/webauth`'s `sessionPayloadVersion`, which hard-rejects on version
mismatch, is deliberate and must be asserted rather than inferred from the absence of a branch.

Output: a real-Qdrant compatibility test whose row set is derived from `migrate.CurrentVersion` so it
grows a third row automatically when a later phase raises the constant.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-record-schema-versioning-foundation/02-CONTEXT.md
@.planning/phases/02-record-schema-versioning-foundation/02-RESEARCH.md
@.planning/phases/02-record-schema-versioning-foundation/02-PATTERNS.md
@.planning/phases/02-record-schema-versioning-foundation/02-01-SUMMARY.md
@CLAUDE.md
</context>

## The older-than direction at `CurrentVersion = 0`

Plan 02-01 resolves `migrate.CurrentVersion = 0`. `CurrentVersion - 1` is therefore not a
representable record state, and inventing a negative version to satisfy the letter of D-08 would be
a fabricated test subject.

The genuine older-than-binary state at `CurrentVersion = 0` is **the key-absent legacy record** — a
record written before the field existed, which is exactly the population criterion 2 is about and
exactly what a rolled-back binary encounters. That is the mirror case, and it is real.

**Do not paper over this with a silent `t.Skip`.** A row that quietly disables itself is the vacuous
shape this project has already shipped once. Instead the row set is *derived* from the constant:

- `CurrentVersion == 0` → two rows: `absent` (older-than) and `newer` (`CurrentVersion + 1`).
- `CurrentVersion > 0` → three rows: `absent`, `older-explicit` (`CurrentVersion - 1`), and `newer`.

The test asserts its executed row count equals the derived expected count. When Phase 3 or 4 raises
the constant, the third row activates on its own and the count assertion is what forces it to
actually run.

## Flagged assumption (unresolved probe item — surfaced, not dropped)

The deterministic edge probe classified one item against `REQ-schema-version-forward-compatible` as
**`unclassified`**, and the fallback protocol forbids auto-resolving an unclassified row with a
backstop. It is therefore carried here as an explicit flagged assumption for a human, not silently
dropped:

> **ASSUMPTION (unresolved, needs review):** the probe could not classify which edge category applies
> to "a binary reading a record whose `schema_version` is newer than its own constant." This plan
> assumes the operative edges are *decode tolerance* (an unrecognized payload key is ignored rather
> than fataled) and *monotonic non-downgrade on rewrite*, and tests exactly those two. If the
> intended edge was something else — for example the behaviour when a record's version is newer by
> more than one step, or when two unknown keys collide on decode — this plan does not cover it and a
> follow-up is needed.
>
> Reviewer action: confirm the two assumed edges are the intended ones, or name the missing edge.

<tasks>

<task type="auto">
  <name>Task 1: Newer-than-binary — inject a future version plus an unknown key and prove nothing rejects, hides, or downgrades it</name>
  <precondition>A Qdrant instance is reachable for `internal/store`'s TestMain: either the Docker daemon responds to `docker info` or `ENGRAM_QDRANT_TEST_ADDR` is set. The injection writes raw payload through the Qdrant client, so a live endpoint is required.</precondition>
  <files>internal/store/schemaversion_compat_test.go</files>
  <read_first>
    - `internal/store/store.go` lines 617-730 — `fromPayload`'s per-field `if v, ok := p["key"]; ok` decode, including the new `schema_version` read: this is the tolerance the test is actually exercising, and it already exists for every sibling field.
    - `internal/store/store.go` lines 367-420 — `setPayloadKeys` / `deletePayloadKeys`, for the raw `SetPayload` request shape to mirror in the injection helper.
    - `internal/store/store_test.go` lines 4820-4880 — `TestSupersedesFromPayloadTolerantDecode`, the closest partial precedent: reuse its raw `map[string]*qdrant.Value` construction style as a starting shape, not as a test to extend (it covers tolerant decode only, never the recall-plus-get chain this task needs).
    - `internal/store/store_test.go` lines 50-200 — `testCollection`, `newTestStore`, `dialTestClient`, `TestMain`.
    - `internal/store/store.go` lines 1001-1520 — the signatures of `Search`, `SearchReranked`, `SearchDiscovery`, `List`, `ListScheduled` and `Store.Get`, so each is invoked correctly.
    - `internal/webauth/resolver.go` around line 59 and `internal/webauth/reseal.go` around line 63 — `sessionPayloadVersion`'s hard reject on version mismatch: the precedent this field deliberately diverges from, so the assertion comment can name it precisely.
    - `internal/migrate/migrate.go` — `CurrentVersion`, from which every injected and expected value is derived.
  </read_first>
  <action>
    Create `internal/store/schemaversion_compat_test.go` (SPDX header, `package store`) holding
    `TestSchemaVersionForwardBackwardCompat` against real Qdrant, routed through
    `testCollection("schemaversion_compat")` and `newTestStore` — never a raw `New`, never an
    unprefixed collection name.

    **Injection helper.** A small test-local helper that writes a record's payload directly through
    the Qdrant client's `SetPayload` (or a raw `Upsert` with a hand-built payload map), deliberately
    bypassing `payload()`. It takes the point id and a `map[string]*qdrant.Value` so a caller can
    write any key, including keys the binary has no field for. This is what makes the test evidence
    about decoding an unfamiliar payload rather than about the stamping plumbing — a test-only
    override of the version constant was considered and rejected as primary proof for exactly that
    reason.

    **The `newer` row.** Seed a normal record through `Store.Upsert` so it has a valid vector and all
    the fields the recall paths need, then raw-inject onto that same point: `schema_version` set to
    `migrate.CurrentVersion + 1`, plus at least one payload key this binary has never heard of (an
    invented key name with a plausible future-version value, and a second one of a different value
    type so the decode is exercised against more than one shape). Then assert, in order:

    1. **Decodes without error.** `Store.Get` by id returns the record with no error and no panic,
       and its `SchemaVersion` is `migrate.CurrentVersion + 1` — not downgraded to the binary's own
       constant.
    2. **Known fields survive.** The record's `Content`, `Scope`, `Category` and `Owner` decode
       correctly alongside the unknown keys — the unknown key is ignored, not allowed to corrupt the
       neighbours.
    3. **Fully recallable.** The record is returned by `Search`, by `SearchReranked` and by `List`
       for its owner — assert by id membership in the result set, not by count. Include
       `ListScheduled` and `SearchDiscovery` coverage by seeding the row's record so those paths
       legitimately apply (a windowed record for `ListScheduled`, a discovery-category record for
       `SearchDiscovery`), or, where a path legitimately does not apply to this record shape, record
       that in an explicit per-path justification field rather than omitting the path silently.
    4. **Not hidden.** The record appears in recall results without any opt-in flag — it is not
       soft-hidden the way a superseded or archived record is.
    5. **Not downgraded by a later write.** Call `Store.Update` on it through the normal path, then
       re-read: `SchemaVersion` is STILL `migrate.CurrentVersion + 1`. This is D-05's monotonic rule
       observed on the exact path a rolled-back binary would take.
    6. **Never rejected.** No call in the chain returns an error mentioning a version mismatch.
       Comment this assertion with the named divergence: `internal/webauth`'s `sessionPayloadVersion`
       hard-rejects on mismatch, and `REQ-schema-version-wire-visible` diverges from that
       deliberately; this assertion is what makes the divergence a tested property rather than an
       observation about missing code.

    Every injected and expected version value is written in terms of `migrate.CurrentVersion`. No
    bare integer literal appears as a version anywhere in the file.
  </action>
  <verify>
    <automated>go test -v -run 'TestSchemaVersionForwardBackwardCompat$/newer' ./internal/store/... 2>&1 | rg -q -- '--- PASS: TestSchemaVersionForwardBackwardCompat/newer'</automated>
  </verify>
  <acceptance_criteria>
    - The command above matches; a bare `ok ... [no tests to run]` is a FALSE GREEN and a failure.
    - The injection helper writes payload through the raw Qdrant client, and the test file contains no call to `payload(` for constructing the injected record's payload.
    - The injected payload carries `schema_version` at `migrate.CurrentVersion + 1` AND at least two payload keys with no corresponding `store.Memory` field, of two different value types.
    - The `newer` row asserts all six properties in order: decodes without error at `CurrentVersion + 1`; known fields intact; returned by the recall paths (membership by id, not by count); not hidden; still at `CurrentVersion + 1` after `Store.Update`; no version-mismatch error anywhere in the chain.
    - Any recall path not exercised for this row carries an explicit written justification in the test; no path is silently omitted.
    - No bare integer literal is used as a version value anywhere in the file — every one is derived from `migrate.CurrentVersion`.
    - The collection name comes from `testCollection(...)` and the store from `newTestStore(...)`.
    - On failure, sort failures by timestamp and read the EARLIEST: a leading `connection refused` is the known testcontainer cascade (#497), not an assertion failure.
  </acceptance_criteria>
  <done>A record stamped one version ahead of this binary, carrying keys this binary does not know, is read, recalled, fetched by id and rewritten — and comes back still one version ahead, with no rejection anywhere.</done>
</task>

<task type="auto">
  <name>Task 2: Older-than-binary — the key-absent legacy record, with a row count derived from the constant</name>
  <precondition>A Qdrant instance is reachable for `internal/store`'s TestMain: either the Docker daemon responds to `docker info` or `ENGRAM_QDRANT_TEST_ADDR` is set.</precondition>
  <files>internal/store/schemaversion_compat_test.go</files>
  <read_first>
    - `internal/store/schemaversion_compat_test.go` — the injection helper and the `newer` row written in Task 1.
    - `internal/store/store.go` lines 617-730 — `fromPayload`, to confirm the absent-key path assigns nothing and leaves the Go zero value.
    - `internal/migrate/migrate.go` — `CurrentVersion` and the recorded rationale for its value.
    - `.planning/phases/02-record-schema-versioning-foundation/02-01-PLAN.md` § "Decisions resolved by this plan" — why the constant is 0 and what raising it later means for this table.
  </read_first>
  <action>
    Complete the compatibility table with the older-than direction and the derived row-count
    assertion.

    **The `absent` row.** Seed a record through `Store.Upsert`, then raw-delete the `schema_version`
    key from its payload (via the client's `DeletePayload`, or seed the point with a hand-built
    payload map that never carried the key) so the stored point genuinely has no `schema_version`
    key — the state of every record written before this milestone. Assert the same six properties as
    the `newer` row, with the expectations for this state: `Store.Get` returns it with
    `SchemaVersion == migrate.Version(0)` and no panic; known fields intact; returned by the recall
    paths by id membership; not hidden; and after a `Store.Update` the record is still returned by
    recall and its version is `migrate.CurrentVersion` (the stamp the update legitimately applies) —
    never an error and never a disappearance. Comment that this row IS the older-than-binary case
    while the constant is 0, and that criterion 2's "no backfill required" is what it proves.

    **The `older-explicit` row.** Author it as a real row whose applicability is derived, not as a
    commented-out placeholder: it injects `schema_version` at `migrate.CurrentVersion - 1` and
    asserts the same six properties, with the post-`Update` expectation being `migrate.CurrentVersion`
    (the monotonic maximum raises an older record to current). Gate its execution on
    `migrate.CurrentVersion > 0` — but gate it through the **derived expected row count**, not through
    a bare `t.Skip`: compute the expected row count as two when the constant is zero and three
    otherwise, run the applicable rows, and assert the executed row count equals that expected count.

    Add a standalone assertion that the derivation itself is honest: when `migrate.CurrentVersion` is
    zero, assert explicitly that the `absent` row is recorded as *covering* the older-than direction
    (a named boolean or a table field, asserted true), so the coverage claim is a checked value
    rather than a comment. When the constant is raised, that same assertion must fail unless
    `older-explicit` ran — which is the mechanism that makes the third row activate on its own.

    Finish the test by logging the executed row names and the expected count, so a reader of CI
    output can see which rows actually ran.
  </action>
  <verify>
    <automated>go test -v -run 'TestSchemaVersionForwardBackwardCompat$' ./internal/store/... 2>&1 | rg -c -- '--- PASS: TestSchemaVersionForwardBackwardCompat/'</automated>
  </verify>
  <acceptance_criteria>
    - The command above prints a number equal to the derived expected row count (2 while `migrate.CurrentVersion` is 0) — and never 0, which would mean the `-run` pattern matched nothing.
    - The `absent` row's point genuinely has no `schema_version` payload key, verified by a raw point read asserting key absence, not by a decoded field equalling zero.
    - The `absent` row asserts `Store.Get` returns `migrate.Version(0)` with no panic, recall membership by id, and post-`Update` recall membership plus version `migrate.CurrentVersion`.
    - The `older-explicit` row exists in the source as a real, complete row — not commented out, not a TODO — with its expectations written in terms of `migrate.CurrentVersion`.
    - The expected row count is computed from `migrate.CurrentVersion` (2 when 0, 3 when greater) and the test asserts the executed row count equals it. There is no bare `t.Skip` standing in for the gating.
    - When `migrate.CurrentVersion` is 0 the test asserts, as a checked value, that the `absent` row is recorded as covering the older-than direction.
    - The test logs the executed row names and the expected count.
    - `task` (lint + test) is green and `gofmt -l internal/store` prints nothing.
  </acceptance_criteria>
  <done>Both compatibility directions are covered — a future-stamped record and a key-absent legacy record — with a row set that grows itself when a later phase raises the version constant rather than silently under-testing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stored Qdrant payload → `fromPayload` decode | Payload written by a *different* binary version crosses here. Keys and values this binary has no field for arrive on this boundary. |
| Older binary ↔ newer record | The rollback path: a binary at a lower `CurrentVersion` reads and rewrites a record stamped higher. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-13 | Tampering | `Store.Update` downgrading a newer record's stamp | high | mitigate | The `newer` row raw-injects `CurrentVersion + 1`, runs `Store.Update` through the normal path, and asserts the version is unchanged — D-05's monotonic maximum observed on the exact rollback path. |
| T-02-14 | Denial of Service | a version-mismatch rejection or soft-hide making a record unreachable | high | mitigate | Every row asserts recall membership by id and `Store.Get` success, plus an explicit assertion that no call returns a version-mismatch error — the tested divergence from `sessionPayloadVersion`'s hard reject. |
| T-02-15 | Tampering | an unknown payload key corrupting the decode of neighbouring fields | medium | mitigate | The `newer` row injects two unknown keys of different value types and asserts `Content`, `Scope`, `Category` and `Owner` all decode correctly on the same record. |
| T-02-16 | Repudiation | a compatibility row silently skipping itself and reporting green | high | mitigate | Row applicability is derived from `migrate.CurrentVersion` and the executed row count is asserted equal to the derived expectation; no `t.Skip` gates a row, and the older-than coverage claim is a checked value rather than a comment. |
| T-02-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task in this phase; RESEARCH.md's Package Legitimacy Audit records N/A. |
</threat_model>

<verification>
- `go test -v ./internal/store/... -run 'TestSchemaVersionForwardBackwardCompat$'` shows one explicit `--- PASS:` line per executed row, and the row count matches the derived expectation.
- The injected records are removed or confined to this test's own prefixed collection; no shared collection is polluted.
- `task` (lint + test) is green.
</verification>

<success_criteria>
- Criterion 5 is proven in both directions against real Qdrant by raw payload injection, not by overriding the version constant.
- A newer-than-binary record with unknown payload keys reads, recalls, fetches by id, survives an update undowngraded, and is never rejected.
- The older-than direction is covered honestly at `CurrentVersion = 0` by the key-absent legacy record, with the explicit `older-explicit` row present and self-activating once the constant is raised.
- The unresolved probe item is surfaced as a flagged assumption for review, not dropped.
</success_criteria>

<output>
Create `.planning/phases/02-record-schema-versioning-foundation/02-04-SUMMARY.md` when done, carrying the flagged assumption forward for reviewer action.
</output>
