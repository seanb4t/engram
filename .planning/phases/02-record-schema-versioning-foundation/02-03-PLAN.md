---
phase: 02-record-schema-versioning-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - internal/store/schemaversion_recallgate_test.go
  - internal/store/store.go
autonomous: true
requirements:
  - REQ-schema-version-never-gates-recall

estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "schema_version is absent from the field-key set of the *qdrant.Filter actually transmitted to Qdrant by Search, SearchReranked, SearchDiscovery, List and ListScheduled, captured through a gRPC unary interceptor rather than reconstructed by the test."
    - "The walker matches condition field keys by exact string equality, so a neighbouring key that merely contains or is contained by schema_version is not counted as a hit."
    - "A schema_version condition nested one level inside a NewFilterAsCondition OR-group IS counted — the recursion into Condition.GetFilter() is proven by a positive control, because categoryMatchCondition produces exactly that shape and a non-recursive walker would pass vacuously."
    - "Every recall builder invoked with each representative Subject produces a filter whose walked key set is non-empty, and the number of filters actually captured equals the enumerated expected count — a filter that collapsed to zero conditions cannot make the gate pass vacuously."
    - "The gate's verdict is order-independent: collected condition keys and the derived builder enumeration are both compared as sorted sets, so reordering the f.Must appends or reordering the enumeration cannot change the result."
    - "The enumerated recall-builder set is asserted set-equal to a set derived by scanning internal/store non-test source for &qdrant.Filter{ constructions, partitioned into recall-walked and operator-excluded with a justification on every operator entry, so a new filter-building function must be explicitly classified before the suite goes green."
    - "RED was proven against real source in both shapes: a top-level schema_version condition injected into a real recall builder turns the gate RED, and a schema_version condition injected NESTED inside a NewFilterAsCondition group also turns it RED; each reverts clean. A new unclassified &qdrant.Filter{ construction turns the enumeration assertion RED and reverts clean. All three cycles are recorded in the plan SUMMARY."
    - "The Cedar-derived authz conditions are covered by the same pass: because ownerOrSharedCondition and ownerOnlyCondition compile into the same transmitted filter, walking the captured filter covers both recall and authz with no second mechanism."
  prohibitions:
    - statement: "MUST NOT make any record less recallable than it is today. Introducing schema_version must never reduce what a user can retrieve from their own store, and a pre-migration record silently vanishing from recall is data loss from the user's point of view even though the bytes remain on disk."
      requirement_id: REQ-schema-version-never-gates-recall
      category: safety
  artifacts:
    - path: "internal/store/schemaversion_recallgate_test.go"
      provides: "Recursive *qdrant.Filter key walker, capturing gRPC client, derived builder enumeration, and the criterion-4 negative gate"
      contains: "TestSchemaVersionNeverGatesRecall"
  key_links:
    - from: "internal/store/schemaversion_recallgate_test.go"
      to: "internal/store/store.go"
      via: "captures the *qdrant.Filter transmitted by each recall entry point through a grpc.WithUnaryInterceptor dial option"
      pattern: 'WithUnaryInterceptor'
    - from: "internal/store/schemaversion_recallgate_test.go"
      to: "internal/store/store.go"
      via: "AST scan of non-test source for &qdrant.Filter{ constructions, asserted set-equal to the classified enumeration"
      pattern: 'qdrant\.Filter\{'
---

<objective>
Prove ROADMAP success criterion 4 — a negative "recall gate blast radius" test showing
`schema_version` never appears in any Qdrant recall or authz filter condition built by `Search`,
`SearchReranked`, `SearchDiscovery`, `List` or `ListScheduled`.

Purpose: this is the phase's headline risk. The adjacent `superseded_by` / `archived_at` `IsEmpty`
idiom has **inverted cardinality** here — absence is the rare state for those fields and the
majority state for `schema_version` at adoption — so copying the idiom would silently exclude every
pre-migration record from its owner's recall. Plan 02-01 added a Qdrant payload index on the field,
which makes it *easier* for a future filter to reach for it; from now on this gate, not
inconvenience, is the only thing holding that line.

Output: a recursive filter-key walker with proven recursion, a derived-and-classified builder
enumeration, a negative gate over the filter objects actually transmitted to Qdrant, and three
recorded prove-RED-then-revert cycles against real source.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-record-schema-versioning-foundation/02-CONTEXT.md
@.planning/phases/02-record-schema-versioning-foundation/02-RESEARCH.md
@.planning/phases/02-record-schema-versioning-foundation/02-PATTERNS.md
@.planning/phases/02-record-schema-versioning-foundation/02-01-SUMMARY.md
@CLAUDE.md
</context>

## Why this walker is new code

RESEARCH.md and PATTERNS.md both confirm by grep that **no filter-condition walker exists anywhere
in this repository**. Every existing filter-related test in `internal/store` asserts on query
*results* — does `Search` return record X — never on the shape of the `*qdrant.Filter` object
itself. A result-shaped assertion is exactly what D-13 rejects: a filter could reference
`schema_version` and still pass if the test data happens to satisfy the condition. Build the walker
from scratch. The only thing to reuse from `collectionprefix_conformance_test.go` is its *discipline*
— derive the set, assert completeness, guard against scanning nothing — not its traversal, whose
target is source AST rather than a runtime proto struct graph.

## Artifacts this phase produces

See the table in `02-01-PLAN.md` § "Artifacts this phase produces" — every symbol listed there is
created by this phase and must be excluded from drift/source-grounding verification. This plan
contributes: `internal/store/schemaversion_recallgate_test.go`, `walkFilterKeys`,
`dialCapturingTestClient`, `TestSchemaVersionNeverGatesRecall`,
`TestRecallBuilderEnumerationIsComplete`.

<tasks>

<task type="auto">
  <name>Task 1: The recursive filter-key walker and its positive/negative controls</name>
  <files>internal/store/schemaversion_recallgate_test.go</files>
  <read_first>
    - `internal/store/store.go` lines 880-1000 — `ownerScopeFilter` (`:885`, the `&qdrant.Filter{Must: must}` top-level shape), `tagMatchConditions` (`:902`), `categoryMatchCondition` (`:924`, which wraps a nested `&qdrant.Filter{Should: ...}` via `qdrant.NewFilterAsCondition` — the shape the recursion exists for), `activeWindowConditions` (`:943`).
    - `internal/store/store.go` lines 760-870 — `ownerOrSharedCondition` (`:769`), `ownerOnlyCondition` (`:796`), `decideBucket` (`:819`): the Cedar-derived authz conditions that compile into the same filter.
    - `internal/store/store.go` lines 1000-1060 — `Search`'s filter assembly, which MUTATES `f.Must` by append after `ownerScopeFilter` returns.
    - The `qdrant` Go client's `Filter` and `Condition` types (`Must`, `Should`, `MustNot` slices; `Condition`'s `GetField()`, `GetIsEmpty()`, `GetIsNull()`, `GetHasId()`, `GetNested()`, `GetFilter()` accessors) — read the generated types in the module cache before writing the traversal so no oneof variant is missed.
    - `.planning/phases/02-record-schema-versioning-foundation/02-PATTERNS.md` § "D-13/D-14 negative recall-gate test: filter-object walker".
  </read_first>
  <action>
    Create `internal/store/schemaversion_recallgate_test.go` (SPDX header, `package store`) with an
    unexported, test-file-local recursive walker. Keep it unexported: nothing outside
    `internal/store` constructs a `*qdrant.Filter`, so exported introspection surface would be
    speculative API (CONTEXT.md leaves this to planning judgment; this is the resolution).

    **`walkFilterKeys`.** Takes a `*qdrant.Filter` and accumulates into a `map[string]bool` (or a
    sorted-set helper) every payload field key the filter references. It must:
    - Handle a nil filter by returning without panicking.
    - Iterate `Must`, `Should` AND `MustNot` — all three, since a `MustNot` reference is just as
      capable of narrowing recall as a `Must` one.
    - For each `Condition`, collect the key from every key-bearing oneof variant the client exposes
      — the field condition, the is-empty condition, the is-null condition, and the nested-object
      condition — reading the generated type to enumerate them rather than guessing.
    - **Recurse** into `Condition.GetFilter()` when the condition is itself a wrapped filter. This
      recursion is load-bearing, not defensive: `categoryMatchCondition` produces exactly that shape
      via `qdrant.NewFilterAsCondition`, so a walker that only scans the top level would miss any key
      buried in an OR-composed group and the whole gate would pass vacuously.
    - Also recurse through nested-condition sub-filters if the client's nested variant carries one.

    Return the collected keys as a **sorted slice** from a small helper, so every assertion built on
    it is order-independent: reordering the `f.Must` appends in `Search` must not be able to change
    any verdict.

    **Walker controls (`TestFilterWalkerSeesEveryPosition`).** Hand-build synthetic filters and
    assert the walker's output as a set, not a count:
    - `top-level Must`: a filter with a match condition on the `schema_version` key → the key set
      contains it.
    - `MustNot`: the same condition placed in `MustNot` → still found.
    - `is-empty`: `qdrant.NewIsEmpty(schemaVersionKey)` in `Must` → still found. This is the exact
      condition shape the inverted-cardinality trap would introduce.
    - `nested Should group`: `qdrant.NewFilterAsCondition(&qdrant.Filter{Should: [...]})` wrapping a
      `schema_version` condition → still found. Comment this subtest with why it is the load-bearing
      one.
    - `doubly nested`: a nested filter inside a nested filter → still found, proving the recursion
      is unbounded rather than one level deep.
    - **Negative control — adjacency:** a filter referencing only the keys `scope`, `owner`,
      `schema`, and `schema_version_legacy` → the key set does NOT contain `schema_version`. Matching
      is exact string equality, so a key that merely contains or is contained by the target is not a
      hit and cannot produce a false RED.
    - **Negative control — clean filter:** a realistic recall-shaped filter with no version
      reference → the key set is non-empty (it has `scope`, `owner`, etc.) but excludes
      `schema_version`. A walker that returned an empty set for every input would pass a naive
      absence check; asserting non-emptiness here is what stops that.

    Every subtest compares sorted sets with an explicit want-list, never `len(...) > 0` as the
    positive assertion and never a bare "contains at least one".
  </action>
  <verify>
    <automated>go test -v -run 'TestFilterWalkerSeesEveryPosition$' ./internal/store/... 2>&1 | rg -c -- '--- PASS: TestFilterWalkerSeesEveryPosition/'</automated>
  </verify>
  <acceptance_criteria>
    - The command above prints `7` — all seven named subtests (top-level Must, MustNot, is-empty, nested Should group, doubly nested, adjacency negative control, clean-filter negative control) ran and passed.
    - `walkFilterKeys` iterates `Must`, `Should` and `MustNot`, and recurses through the wrapped-filter condition variant; the nested and doubly-nested subtests prove the recursion.
    - The walker returns keys as a sorted slice (or is compared through a sorting helper), so no assertion in the file depends on condition order.
    - Key matching is exact string equality; the adjacency subtest asserts that `schema`, `schema_version_legacy` and `scope` do NOT register as `schema_version`.
    - The clean-filter subtest asserts the returned key set is BOTH non-empty AND free of `schema_version` — an all-empty walker cannot pass this file.
    - `walkFilterKeys` returns without panicking on a nil filter (asserted).
    - No subtest in this file uses a bare non-zero-length assertion as its positive proof.
    - `gofmt -l internal/store` prints nothing and `task license:check` exits 0.
  </acceptance_criteria>
  <done>A recursive walker exists whose ability to see every filter position — including keys buried inside nested OR groups — is proven by hand-built controls in both directions.</done>
</task>

<task type="auto">
  <name>Task 2: Derive the filter-builder set from source and assert the classification is complete</name>
  <files>internal/store/schemaversion_recallgate_test.go, internal/store/store.go</files>
  <read_first>
    - `internal/store/collectionprefix_conformance_test.go` lines 159-238 — `scanConstructions` and `scanPackageDir`, for the `go/ast` scan and the "a scan that sees nothing must not report clean" guard shape.
    - `internal/store/schemaversion_stamp_gate_test.go` — the sibling scanner from plan 02-02; reuse its directory-walking helper rather than writing a third one, if its signature allows a different classifier.
    - `internal/store/store.go` lines 880-1520 — every `&qdrant.Filter{` construction on the recall path: `ownerScopeFilter` (`:885`), `categoryMatchCondition` (`:924`), `listFilter` (`:1200`), and the inline constructions inside `SearchDiscovery` (`:1118-1132`) and `ListScheduled` (`:1490-1507`).
    - `internal/store/spine.go` — the operator-tier (`spine-review`) filter construction, if any, which D-16 deliberately excludes.
    - `.planning/phases/02-record-schema-versioning-foundation/02-RESEARCH.md` § "Criterion-4 Filter-Builder Enumeration" and the finding that `decideRecord` does NOT build a filter (anchor the scan on the filter construction, not on an authz-action parameter, or `decideRecord` is falsely swept in).
  </read_first>
  <action>
    Add `TestRecallBuilderEnumerationIsComplete` to the same file. This is D-14: the builder set is
    **derived and asserted complete, never hand-listed**.

    **Derive.** Using `go/ast`, scan every non-test `.go` file in `internal/store` for composite
    literals of type `qdrant.Filter` (both `&qdrant.Filter{...}` and the bare form) and collect the
    set of enclosing function names (receiver-qualified, e.g. `Store.ownerScopeFilter`). Anchor on
    the *filter construction*, never on an authz-action parameter — RESEARCH.md shows that anchoring
    on the authz signature would falsely sweep in `decideRecord`, which decides in memory over an
    already-fetched record and never touches a filter. Fail loudly on a scan error and on a zero
    scanned-file count, exactly as plan 02-02's gate does.

    **Classify.** Author two explicitly named lists in the test file:
    - `recallFilterBuilders` — the functions on the recall/authz path, whose transmitted filters
      Task 3 walks. Run the scan first and populate this from what the scan actually reports rather
      than from RESEARCH.md's table, then cross-check the two agree and note any divergence in the
      SUMMARY.
    - `operatorFilterBuilders` — the operator-tier constructions D-16 deliberately excludes
      (`spine-review`, prune, reindex and any sibling). Every entry carries a justification string
      naming why it is out of criterion-4 scope. The justification must state the reason the
      exclusion exists at all: Phase 3's sweep MUST filter by `schema_version` to find its backlog,
      so a blanket ban across every Qdrant query in the package would make Phase 3 unimplementable.
      The recall/authz boundary is the line criterion 4 actually draws.

    **Assert.** The union of the two lists is set-equal to the derived scan result. Print, on
    failure, both the derived-but-unclassified names and the classified-but-underived names. This is
    the stale-classification guard: adding a new filter-building function anywhere in the package
    fails the suite until someone decides, on the record, whether it is recall or operator.

    **Prove RED.** Temporarily add to `internal/store/store.go` — real source, not a fixture — a small
    unexported function constructing a `&qdrant.Filter{}`. Run the test: it MUST fail naming that
    function as derived-but-unclassified. Record the output. Revert with an exact `git checkout --`
    and re-run: it MUST pass. Confirm `git status --porcelain internal/store/store.go` is empty.
    Record the cycle in this plan's SUMMARY.
  </action>
  <verify>
    <automated>go test -v -run 'TestRecallBuilderEnumerationIsComplete$' ./internal/store/... 2>&1 | rg -q -- '--- PASS: TestRecallBuilderEnumerationIsComplete'</automated>
  </verify>
  <acceptance_criteria>
    - The command above matches; a bare package-level `ok` is a FALSE GREEN and a failure.
    - The derived set comes from a `go/ast` scan of non-test `internal/store` source for `qdrant.Filter` composite literals — not from a hand-typed list and not from a regex.
    - The scan fails loudly on a read error and on a zero scanned-file count; both branches exist as separate `t.Fatalf` calls.
    - `decideRecord` does NOT appear in the derived set (the scan is anchored on filter construction, not on an authz-action parameter).
    - `operatorFilterBuilders` entries each carry a non-empty justification string, and at least one of them states that Phase 3's sweep must be able to filter by `schema_version`.
    - The assertion is set equality between the derived set and the union of the two classified lists, compared as sorted sets; the failure message prints both difference directions by name.
    - Prove-RED is recorded in `02-03-SUMMARY.md`: the injected unclassified filter constructor, the observed failing output naming it, the revert command, and the observed pass.
    - `git status --porcelain internal/store/store.go` is empty at task end.
  </acceptance_criteria>
  <done>The set of filter-building functions is derived from source, every member is explicitly classified as recall-walked or operator-excluded with a reason, and an unclassified newcomer has been observed turning the suite RED.</done>
</task>

<task type="auto">
  <name>Task 3: Walk the filter actually transmitted to Qdrant by all five recall entry points, and prove RED nested</name>
  <precondition>A Qdrant instance is reachable for `internal/store`'s TestMain: either the Docker daemon responds to `docker info` or `ENGRAM_QDRANT_TEST_ADDR` is set. The gate captures real gRPC requests, so it needs a live endpoint to dial.</precondition>
  <files>internal/store/schemaversion_recallgate_test.go, internal/store/store.go</files>
  <read_first>
    - `internal/store/store_test.go` lines 160-200 — `dialTestClient`, the client-construction shape this task's capturing sibling mirrors (`qdrant.NewClient(&qdrant.Config{Host, Port})`).
    - The `qdrant` client's `Config` struct in the module cache — specifically its `GrpcOptions []grpc.DialOption` field, which is how the interceptor is attached without touching production code.
    - `internal/store/store.go` lines 1001-1080 (`Search`), 1081-1098 (`SearchReranked`, which delegates to `Search` and builds no independent filter), 1099-1170 (`SearchDiscovery`, inline `must` slice — it does NOT call `ownerScopeFilter`), 1200-1290 (`listFilter` and `List`), 1443-1520 (`scheduledStateCondition` and `ListScheduled`, inline — it calls neither `listFilter` nor `ownerScopeFilter`). Read each signature before writing the invocations.
    - `internal/store/subject.go` — how a `Subject` is constructed, for the two representative subjects.
    - `internal/store/store_test.go` — an existing integration test's Subject/authz setup, to reuse rather than re-derive.
    - `.planning/phases/02-record-schema-versioning-foundation/02-RESEARCH.md` § "Pitfall 2" — `SearchDiscovery` and `ListScheduled` are the two most likely to be silently skipped by a shared-helper-only implementation.
  </read_first>
  <action>
    Add `TestSchemaVersionNeverGatesRecall` — the criterion-4 gate proper. It asserts on the object
    actually sent to Qdrant, which is what makes it evidence rather than inference.

    **`dialCapturingTestClient`.** A sibling of `dialTestClient` that builds the client with
    `GrpcOptions` carrying a `grpc.WithUnaryInterceptor`. The interceptor records every outgoing
    request that carries a filter: type-switch the request over the Qdrant request types that have a
    `Filter` field (query, scroll, count, and any sibling the client exposes), append the
    `*qdrant.Filter` plus the method name to a capture slice guarded by a mutex, then call the
    invoker. Provide a reset so seeding writes and index creation do not pollute the recall capture.

    **The invocation table.** Enumerate rows of (entry point, representative subject). The five entry
    points named by criterion 4 — `Search`, `SearchReranked`, `SearchDiscovery`, `List`,
    `ListScheduled` — crossed with two representative subjects: an anonymous subject (empty owner,
    the single anonymous bucket) and an owner-bearing subject. Ten rows. Each row declares how many
    filter-carrying requests it expects to transmit, normally 1; if a row legitimately short-circuits
    before transmitting (an authz denial that never reaches Qdrant), declare its expectation as 0
    **with a justification string** so the short-circuit is explicit and reviewable rather than a
    silent hole.

    For each row: reset the capture, invoke the entry point with representative options (include tag
    and category options on `Search`/`List` so `categoryMatchCondition`'s nested OR group is actually
    present in the transmitted filter), then assert:
    - the number of filters captured equals the row's declared expectation;
    - for each captured filter, `walkFilterKeys` returns a **non-empty** key set — a filter that
      collapsed to zero conditions would make the absence assertion vacuous, and this is the guard
      that stops it;
    - `schema_version` is **absent** from that key set.

    Assertions are about the transmitted filter, not about returned records, so an empty result set
    is fine and the collection does not need elaborate seeding. Route any collection name through
    `testCollection("schemaversion_recallgate")` and construct via `newTestStore`.

    After the loop, assert the total number of filters walked across all rows equals the sum of the
    declared per-row expectations, and that this total is greater than zero. Log the total.

    **Prove RED — shape 1 (top level).** Temporarily add `qdrant.NewIsEmpty(schemaVersionKey)` to
    `Search`'s `f.Must` appends in `internal/store/store.go` — real source, because a fixture-only
    proof is what missed the Phase 1 bypass. Run the gate: it MUST fail naming `Search` and the key.
    Record the output. Revert exactly, re-run: it MUST pass.

    **Prove RED — shape 2 (nested).** Temporarily inject a `schema_version` condition wrapped in
    `qdrant.NewFilterAsCondition(&qdrant.Filter{Should: ...})` — the `categoryMatchCondition` shape —
    into a real recall builder. Run the gate: it MUST fail. This is the direction that proves the
    walker's recursion is load-bearing in the real pipeline and not only against a synthetic control:
    a non-recursive walker would report clean here. Record the output, revert exactly, re-run: it
    MUST pass.

    Confirm `git status --porcelain internal/store/store.go` is empty after both reverts, and record
    both cycles in this plan's SUMMARY under "Prove-RED evidence", carrying the same evidence into
    the phase `VERIFICATION.md` per D-15.
  </action>
  <verify>
    <automated>go test -v -run 'TestSchemaVersionNeverGatesRecall$' ./internal/store/... 2>&1 | rg -q -- '--- PASS: TestSchemaVersionNeverGatesRecall'</automated>
  </verify>
  <acceptance_criteria>
    - The command above matches; a bare `ok ... [no tests to run]` is a FALSE GREEN and a failure.
    - The filter under assertion is obtained from a gRPC unary interceptor attached via `qdrant.Config.GrpcOptions` — the test contains no hand-reconstruction of a filter by calling `ownerScopeFilter`/`listFilter` directly and re-appending conditions.
    - The invocation table enumerates exactly ten rows: all five criterion-4 entry points crossed with two representative subjects (anonymous and owner-bearing).
    - `SearchDiscovery` and `ListScheduled` each appear as their own rows and are invoked directly — not assumed to be covered by `ownerScopeFilter`/`listFilter`.
    - Any row declaring zero expected transmitted filters carries a non-empty justification string.
    - For every captured filter the test asserts the walked key set is non-empty BEFORE asserting `schema_version` is absent.
    - The test asserts the total filters walked equals the sum of declared expectations and that the total is greater than zero, and logs the total.
    - `Search` and `List` are invoked with tag and category options so `categoryMatchCondition`'s nested group is present in at least one captured filter (assert that at least one captured filter's key set contains `category`, proving the nested group was really traversed in the live pipeline).
    - Prove-RED shape 1 (top-level condition) is recorded in `02-03-SUMMARY.md` with the injection, the observed failure naming `Search`, the revert, and the observed pass.
    - Prove-RED shape 2 (nested inside a `NewFilterAsCondition` group) is recorded in `02-03-SUMMARY.md` with the same four elements, and the SUMMARY states explicitly that a non-recursive walker would have reported clean on this shape.
    - `git status --porcelain internal/store/store.go` is empty at task end and `rg -c 'payload\(m\)' internal/store/store.go` still reports 1.
    - On failure, sort failures by timestamp and read the EARLIEST: a leading `connection refused` is the known testcontainer cascade (#497), not an assertion failure.
  </acceptance_criteria>
  <done>`schema_version` is proven absent from every filter the five recall entry points actually transmit to Qdrant under two representative subjects, with non-vacuity guarded by a non-empty key-set assertion per filter and a totals check, and the gate observed RED against both a top-level and a nested real-source injection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `internal/store` → Qdrant recall queries | The `*qdrant.Filter` crossing this boundary decides what a caller can see. A condition added here silently narrows recall for everyone. |
| Cedar PDP → Qdrant filter | `ownerOrSharedCondition` / `ownerOnlyCondition` compile authz decisions into the same filter, so authz and recall share one crossing. |
| Test ↔ real source under prove-RED | Temporary injections into `internal/store/store.go` must revert completely; a stray injection would ship a real recall-narrowing condition. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-09 | Denial of Service | a `schema_version` condition in any recall or authz filter | high | mitigate | The gate walks the filter actually transmitted to Qdrant for all five entry points under two subjects and asserts the key is absent; the walked-set is asserted non-empty per filter so absence cannot be vacuous. Proven RED against real source in both top-level and nested shapes. |
| T-02-10 | Repudiation | a walker that cannot see nested conditions, making the gate pass vacuously | high | mitigate | Seven walker controls including nested and doubly-nested positive cases plus two negative controls; the live pipeline assertion additionally requires at least one captured filter to expose the `category` key, proving the nested OR group was traversed in production shape. |
| T-02-11 | Tampering | a new filter-building function slipping in unclassified | medium | mitigate | The builder set is derived by AST scan and asserted set-equal to the union of the recall and operator classifications; an unclassified newcomer fails the suite, proven RED. |
| T-02-12 | Tampering | a prove-RED injection left behind in real source | high | mitigate | Every prove-RED cycle ends with an exact `git checkout --` and an asserted-empty `git status --porcelain internal/store/store.go`; the single-door invariant (`payload(m)` call-site count of 1) is re-checked at task end. |
| T-02-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task in this phase; RESEARCH.md's Package Legitimacy Audit records N/A. |
</threat_model>

<verification>
- `go test -v ./internal/store/... -run 'TestFilterWalkerSeesEveryPosition$|TestRecallBuilderEnumerationIsComplete$|TestSchemaVersionNeverGatesRecall$'` shows an explicit `--- PASS:` line for every named test and subtest.
- `git status --porcelain internal/store/store.go` is empty — all three prove-RED injections reverted.
- `task` (lint + test) is green.
</verification>

<success_criteria>
- Criterion 4 is proven against the filter objects actually transmitted to Qdrant by all five named recall entry points, under two representative subjects.
- The walker's recursion into nested OR groups is proven by synthetic controls AND observed in the live pipeline.
- The builder enumeration is derived from source and completely classified, with the operator-tier exclusion carrying its Phase-3 justification.
- Three prove-RED-then-revert cycles are recorded, covering the top-level condition, the nested condition, and the unclassified builder.
</success_criteria>

<output>
Create `.planning/phases/02-record-schema-versioning-foundation/02-03-SUMMARY.md` when done, including the "Prove-RED evidence" section covering all three cycles.
</output>
