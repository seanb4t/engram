---
phase: 02-record-schema-versioning-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - internal/store/schemaversion_stamp_gate_test.go
  - internal/store/testdata/schemaversionstamp/good_pkg.go.txt
  - internal/store/testdata/schemaversionstamp/bad_pkg.go.txt
  - internal/store/schemaversion_stamp_test.go
  - internal/store/store.go
autonomous: true
requirements:
  - REQ-schema-version-stamped

estimate:
  tokens: 68000
  raw_tokens: 68000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A source-level gate proves every Qdrant point-write in internal/store non-test source routes its payload through payload(), with exactly one named, justified exception: Store.Reindex's verbatim raw-map copy."
    - "The gate is anchored on call-site identity — which function the Payload field value calls — never on argument shape, so replacing payload() with a hand-built map at the legitimate call site turns the gate RED."
    - "The gate reports the number of non-test files it scanned and FAILS when that number is zero, and it fails loudly on a nonexistent scan directory rather than reporting clean."
    - "The bad fixture yields a finding for BOTH bypass shapes — an inline map literal and the same map bound to a local variable first — asserted by set equality over the enumerated finding set, never by a non-zero count."
    - "The real-package finding set is compared for set equality against a named exception allowlist, so both an added bypass and a stale allowlist entry turn the gate RED."
    - "Every full-write method (Store.Upsert, the scheduled-window variant, Store.Update, Store.Supersede) is separately proven behaviorally to leave schema_version at max(migrate.CurrentVersion, the record's prior version) against a real Qdrant, and the executed row count is asserted equal to the enumerated row count."
    - "A partial-write path (Store.SetVisibility) is proven NOT to stamp: a raw-injected v0 record touched by it stays at v0 — the D-02 accepted limitation is asserted, not assumed."
    - "RED was proven in BOTH directions against real source, not only a fixture: injecting a payload-bypassing point write into internal/store non-test source turns the gate RED and reverting turns it green, and removing the Reindex allowlist entry turns it RED and restoring it turns it green. Both cycles are recorded in the plan SUMMARY."
  prohibitions:
    - statement: "MUST NOT ship a gate that can report clean while having inspected nothing. A green gate over zero subjects is a false assurance to the operator, and this project has a recorded instance of exactly that defect shipping and being passed by a goal-backward verifier as having no gaps."
      requirement_id: REQ-schema-version-stamped
      category: transparency
  artifacts:
    - path: "internal/store/schemaversion_stamp_gate_test.go"
      provides: "AST call-site-identity gate proving payload() is the only door for full point writes"
      contains: "TestEveryPointWriteRoutesThroughPayload"
    - path: "internal/store/testdata/schemaversionstamp/good_pkg.go.txt"
      provides: "Known-good fixture: the conforming Payload field value expression"
    - path: "internal/store/testdata/schemaversionstamp/bad_pkg.go.txt"
      provides: "Known-bad fixture carrying both bypass shapes"
    - path: "internal/store/schemaversion_stamp_test.go"
      provides: "Behavioral per-write-method stamping proof plus the partial-write negative"
      contains: "TestEveryFullWriteMethodStampsSchemaVersion"
  key_links:
    - from: "internal/store/schemaversion_stamp_gate_test.go"
      to: "internal/store/store.go"
      via: "AST scan of non-test source for qdrant.PointStruct composite literals"
      pattern: 'qdrant\.PointStruct'
    - from: "internal/store/schemaversion_stamp_test.go"
      to: "internal/migrate/migrate.go"
      via: "expected stamps derived from migrate.CurrentVersion, never hard-coded"
      pattern: 'migrate\.CurrentVersion'
---

<objective>
Prove ROADMAP success criterion 1 — "every write path stamps the current `schema_version`, proven by
a test asserting 100% of write paths stamp, not a sample" — structurally and behaviorally.

Purpose: the structural half is the load-bearing one because it catches the write path that does not
exist yet. A behavioral test can only assert about the three methods someone remembered to list; an
AST gate over every `qdrant.PointStruct` construction in the package fails the moment a fourth
appears. This project has a recorded instance (durable memory `x6v6qxqd6f`) of a Phase 1 gate that
shipped while scanning nothing, was bypassed by a local variable holding the literal, and was passed
3/3 "no gaps" by a goal-backward verifier — every design choice below is a direct response to that.

Output: an AST conformance gate with good/bad fixtures and a named exception allowlist, a behavioral
per-method stamping proof, and recorded prove-RED-then-revert evidence in both directions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-record-schema-versioning-foundation/02-CONTEXT.md
@.planning/phases/02-record-schema-versioning-foundation/02-RESEARCH.md
@.planning/phases/02-record-schema-versioning-foundation/02-PATTERNS.md
@.planning/phases/02-record-schema-versioning-foundation/02-01-SUMMARY.md
@CLAUDE.md
</context>

## Correction this plan is built on

CONTEXT.md's D-01 states that `Upsert`, `Update`, `Supersede` **and `Reindex`** all funnel through
`payload()`. RESEARCH.md disproves the fourth by reading the code: `payload()` has exactly one call
site — `internal/store/store.go:749`, inside `Store.Upsert` — and `Store.Reindex`'s per-point write
calls the raw Qdrant client directly with `Payload: p.Payload`, the payload map scrolled verbatim
off the *source* collection. `Update` and `Supersede` reach `payload()` transitively through
`s.Upsert`; `Reindex` never reaches it at all.

**Consequence this gate must encode:** Reindex's per-point write is a *named, justified exception*
in the allowlist, mirroring the existing `embedder_identity` exception comment already at that
write. It is NOT a conforming site and must NOT be forced to route through `payload()` — refactoring
Reindex is outside this phase's additive scope. A gate written on D-01's stated premise would red on
a write path that was never broken.

## Artifacts this phase produces

See the table in `02-01-PLAN.md` § "Artifacts this phase produces" — every symbol listed there is
created by this phase and must be excluded from drift/source-grounding verification. This plan
contributes: `internal/store/schemaversion_stamp_gate_test.go`,
`internal/store/schemaversion_stamp_test.go`,
`internal/store/testdata/schemaversionstamp/{good_pkg.go.txt,bad_pkg.go.txt}`,
`TestEveryPointWriteRoutesThroughPayload`, `TestEveryFullWriteMethodStampsSchemaVersion`.

<tasks>

<task type="auto">
  <name>Task 1: The AST scanner and its known-good / known-bad fixtures</name>
  <files>internal/store/schemaversion_stamp_gate_test.go, internal/store/testdata/schemaversionstamp/good_pkg.go.txt, internal/store/testdata/schemaversionstamp/bad_pkg.go.txt</files>
  <read_first>
    - `internal/store/collectionprefix_conformance_test.go` — the whole file. Specifically `scanConstructions` (`:159`), `isStoreConstructorCall` (`:140`, the call-site-identity anchor to mirror), `scanPackageDir` (`:216`, including its comment on why `err != nil` and `filesScanned == 0` are the same failure class), and the four subtests of `TestEveryStoreConstructionRoutesThroughSeam` (`:244-325`) — the good-fixture, both-bypass-shapes set-equality, zero-applicability and real-package structure.
    - `internal/store/testdata/collectionprefix/good_pkg_test.go.txt` and `bad_pkg_test.go.txt` — the fixture file convention (compiled-out `.txt` suffix, self-contained package text).
    - `internal/store/store.go` lines 731-770 — `Store.Upsert`'s conforming `qdrant.PointStruct` construction with `Payload: qdrant.NewValueMap(payload(m))`.
    - `internal/store/store.go` lines 3200-3230 — Reindex's non-conforming `Payload: p.Payload` write and its existing `embedder_identity` exception comment.
    - `.planning/phases/02-record-schema-versioning-foundation/02-RESEARCH.md` § "Pattern 2" and § "Pitfall 1".
  </read_first>
  <action>
    Create `internal/store/schemaversion_stamp_gate_test.go` (SPDX header, `package store`) holding
    the scanner, and the two fixtures under `internal/store/testdata/schemaversionstamp/`.

    **What the scanner looks for.** Using `go/ast`, `go/parser` and `go/token` — never a regex or
    string match, which the Don't-Hand-Roll table forbids for exactly this — walk each parsed file
    for composite literals whose type is `qdrant.PointStruct` (both `&qdrant.PointStruct{...}` and
    the bare form). For each such literal, locate the `Payload` key-value element and classify its
    value expression:

    - **Conforming** when, and only when, the value is a call whose callee is the selector
      `qdrant.NewValueMap` AND that call's single argument is itself a call whose callee is the
      identifier `payload`. This is call-site *identity*: which functions are invoked. Do not inspect
      what is inside `payload(...)`'s own argument list — an argument-shape rule is precisely the
      check that a local variable defeated in Phase 1.
    - **A finding** in every other case, including a missing `Payload` element.

    Each finding records the enclosing function name (from the innermost enclosing `ast.FuncDecl`,
    prefixed with the receiver type name when there is one, e.g. `Store.Reindex`), the display path
    and line, and a short reason string naming the non-conforming shape. The enclosing **function
    name** is the finding's identity key — not the line number, which drifts every time `store.go`
    is edited and would make the allowlist stale by accident.

    **Directory scanning.** Provide the sibling of `scanPackageDir`: read the directory's entries
    non-recursively, select files by a suffix parameter so the same function serves both the real
    package (`.go`, skipping any name ending `_test.go`) and the fixture directory (`.go.txt`),
    return `(findings, filesScanned, err)`, and surface a missing directory as an error rather than
    a silent zero. Copy `scanPackageDir`'s doc-comment reasoning verbatim in spirit: a scan that
    cannot see what it was told to check must never report clean.

    **`good_pkg.go.txt`.** A self-contained package text with at least two conforming point writes:
    one written inline as `qdrant.NewValueMap(payload(m))`, and one where the `Memory` value is
    first bound to a local variable and then passed — proving the gate accepts a legitimate
    refactor of the *argument* while still requiring the *callee* identity. Expect zero findings.

    **`bad_pkg.go.txt`.** A self-contained package text carrying BOTH bypass shapes in two
    distinctly named functions so the finding set has two distinguishable members:
    (a) an inline hand-built map — `Payload: qdrant.NewValueMap(map[string]any{...})` — a shape that
    can carry a `schema_version` key by hand and still never call `payload()`; and (b) the Phase 1
    shape, the same hand-built map assigned to a local variable first and the variable passed to
    `qdrant.NewValueMap`. Expect exactly two findings.

    **Fixture subtests.** Mirror `TestEveryStoreConstructionRoutesThroughSeam`'s structure inside a
    new `TestEveryPointWriteRoutesThroughPayload`:
    - `good fixture yields zero findings` — fail with the finding list on any non-zero count.
    - `bad fixture yields a finding for BOTH bypass shapes` — collect the findings into a set keyed
      by enclosing function name, assert every expected member is present AND that the set's size
      equals the expected size. Carry the collectionprefix comment's reasoning: an "at least one
      finding" assertion passes while catching only one of two bypass shapes, which is the exact
      state the Phase 1 gate shipped in.
    - `zero-applicability guard: nonexistent directory fails loudly` — the directory scanner over a
      path that does not exist must return a non-nil error.
    - `zero-applicability guard: a directory with no matching files fails loudly` — point the
      scanner at an existing directory containing no file with the requested suffix and assert the
      caller treats `filesScanned == 0` as a failure, not as clean.
  </action>
  <verify>
    <automated>go test -v -run 'TestEveryPointWriteRoutesThroughPayload$/(good_fixture|bad_fixture|zero-applicability)' ./internal/store/... 2>&1 | rg -c -- '--- PASS: TestEveryPointWriteRoutesThroughPayload/'</automated>
  </verify>
  <acceptance_criteria>
    - The command above prints `4` — all four subtests ran and passed. Fewer means a subtest name drifted and the `-run` pattern matched nothing (durable record `bsbsvn4hbc`).
    - `internal/store/schemaversion_stamp_gate_test.go` imports `go/ast`, `go/parser` and `go/token`, and contains no `regexp` import and no `strings.Contains` over source text for the classification decision.
    - The conformance decision inspects only the callee identity of the `Payload` value expression and of its single argument; the test file contains no inspection of `payload(...)`'s own arguments.
    - Findings are keyed by enclosing function name (receiver-qualified where applicable), not by line number.
    - `good_pkg.go.txt` contains two conforming point writes, one of which passes a locally-bound `Memory` variable, and the good-fixture subtest asserts exactly zero findings.
    - `bad_pkg.go.txt` contains exactly two non-conforming point writes in distinctly named functions, and the bad-fixture subtest asserts the finding set both CONTAINS both names AND has size exactly 2 — a `len(findings) > 0` assertion anywhere in this file is a failure of this task.
    - Both zero-applicability subtests exist: one for a nonexistent directory (error expected) and one for an existing directory with no matching files (`filesScanned == 0` treated as failure).
    - `task license:check` exits 0 and `gofmt -l internal/store` prints nothing.
  </acceptance_criteria>
  <done>The scanner classifies point writes by call-site identity, the two fixtures pin its behavior in both directions with set equality, and a scan that sees nothing fails loudly.</done>
</task>

<task type="auto">
  <name>Task 2: Run the gate over the real package, with the named Reindex exception, and prove RED both ways</name>
  <files>internal/store/schemaversion_stamp_gate_test.go, internal/store/store.go</files>
  <read_first>
    - `internal/store/schemaversion_stamp_gate_test.go` — the scanner written in Task 1.
    - `internal/store/collectionprefix_conformance_test.go` lines 294-325 — the `real packages` subtest with its `n == 0` and `totalFiles == 0` guards and its `t.Logf` of the scanned count.
    - `internal/store/store.go` lines 3200-3230 — the exact enclosing function and the existing exception comment that this allowlist entry's justification must agree with.
    - `.planning/phases/02-record-schema-versioning-foundation/02-RESEARCH.md` § "D-07 Research Flag — Resolved" points 1-4.
    - `.planning/phases/01-gate-ci-integrity/01-06-SUMMARY.md` — how Phase 1 recorded its own prove-RED evidence, for the format to reuse in this plan's SUMMARY.
  </read_first>
  <action>
    Add the `real package` subtest to `TestEveryPointWriteRoutesThroughPayload`, then prove the gate
    goes RED against real source in both directions and revert.

    **The allowlist.** Declare a package-level table of named exceptions: each entry pairs an
    enclosing function name with a justification string. Exactly one entry:
    `Store.Reindex` — justified as the intentional verbatim-payload exception, because Reindex copies
    the raw payload map scrolled off the source collection rather than rebuilding it from a decoded
    `Memory`, which is what makes it preserve every key including ones this binary does not know
    about; routing it through `payload()` would defeat that contract. Reference the existing
    `embedder_identity` exception comment at the same write so the two explanations cannot drift.

    **The real-package subtest.** Scan `internal/store`'s own directory for non-test `.go` files.
    Fail loudly if the scan errors OR if `filesScanned` is zero, log the scanned count, then compare
    the finding set against the allowlist by **set equality**: every finding must be an allowlisted
    function name, AND every allowlisted name must appear in the findings. The second half is the
    stale-allowlist guard — if Reindex is ever refactored to route through `payload()`, the allowlist
    entry becomes a lie and this test says so instead of silently permitting a hole. The failure
    messages must print both the unexpected findings and the unmatched allowlist entries by name.

    **Prove RED — direction 1 (an added bypass).** Temporarily edit `internal/store/store.go` — real
    package source, not a fixture, because a fixture-only proof is what missed the Phase 1 bypass —
    adding a small unexported function that constructs a `qdrant.PointStruct` whose `Payload` value
    is a hand-built map bound to a local variable. Run the gate: it MUST fail, naming the new
    function. Record the exact failure output. Revert the edit with `git checkout --
    internal/store/store.go` (or an equivalent exact revert), re-run: it MUST pass. Confirm
    `git status --porcelain internal/store/store.go` is empty after the revert.

    **Prove RED — direction 2 (a stale allowlist).** Temporarily remove the `Store.Reindex` entry
    from the allowlist. Run the gate: it MUST fail, naming `Store.Reindex` as an unexpected finding.
    Restore the entry, re-run: it MUST pass. This proves the set-equality assertion has teeth on the
    side that a "findings ⊆ allowlist" check alone would miss.

    Record both cycles — command, observed failure text, observed pass — in this plan's SUMMARY under
    a "Prove-RED evidence" heading, and carry the same evidence into the phase `VERIFICATION.md`
    when it is written. A gate whose RED was never observed is not shippable (durable record
    `v5q7jdbw43`).
  </action>
  <verify>
    <automated>go test -v -run 'TestEveryPointWriteRoutesThroughPayload$' ./internal/store/... 2>&1 | rg -q -- '--- PASS: TestEveryPointWriteRoutesThroughPayload/real_package'</automated>
  </verify>
  <acceptance_criteria>
    - The command above matches — the real-package subtest ran and passed by that exact name.
    - The real-package subtest fails when the scan returns an error AND when `filesScanned` is zero; both guards are present as separate `t.Fatalf` branches.
    - The subtest logs the number of non-test files scanned, and that number is greater than 1 when the suite runs.
    - The allowlist has exactly one entry, `Store.Reindex`, carrying a justification string that names the verbatim raw-map copy as the reason.
    - The comparison is set equality in both directions: an unexpected finding fails, and an allowlist entry with no corresponding finding fails. The file contains no subset-only check.
    - Prove-RED direction 1 is recorded in `02-02-SUMMARY.md`: the injected bypass, the observed failing output naming the injected function, the revert command, and the observed pass.
    - Prove-RED direction 2 is recorded in `02-02-SUMMARY.md`: the removed allowlist entry, the observed failing output naming `Store.Reindex`, the restore, and the observed pass.
    - `git status --porcelain internal/store/store.go` is empty at task end — the injected bypass was fully reverted and no stray edit shipped.
    - `rg -c 'payload\(m\)' internal/store/store.go` reports 1 — the single-door invariant is intact after the revert.
  </acceptance_criteria>
  <done>The gate runs over real `internal/store` source, reports what it scanned, matches the finding set to a one-entry named allowlist by set equality, and has been observed RED in both the added-bypass and stale-allowlist directions with clean reverts.</done>
</task>

<task type="auto">
  <name>Task 3: Behavioral proof — every full-write method stamps, and no partial-write method does</name>
  <precondition>A Qdrant instance is reachable for `internal/store`'s TestMain: either the Docker daemon responds to `docker info` or `ENGRAM_QDRANT_TEST_ADDR` is set.</precondition>
  <files>internal/store/schemaversion_stamp_test.go</files>
  <read_first>
    - `internal/store/store.go` lines 731-770 (`Store.Upsert`), 1860-1900 (`Store.Update`), 2270-2320 (`Store.Supersede`) — the three full-write methods and how each reaches `payload()`.
    - `internal/store/store.go` lines 367-420 — the `setPayloadKeys` / `deletePayloadKeys` function-var seams and the list of partial-write callers (`UpdatePayload`, `SetVisibility`, `IncrementAccess`, the supersede back-stamp, archive/restore, `RemapOwner`, `BackfillShortIDs`).
    - `internal/store/store_test.go` lines 50-200 — `testCollection`, `newTestStore`, `dialTestClient`, `TestMain`.
    - `internal/store/store_test.go` — an existing `Store.Supersede` integration test, for the setup shape (subject construction, owner wiring) to reuse rather than re-derive.
    - `internal/migrate/migrate.go` — `CurrentVersion`, so every expectation is derived from it.
  </read_first>
  <action>
    Create `internal/store/schemaversion_stamp_test.go` (SPDX header, `package store`) holding
    `TestEveryFullWriteMethodStampsSchemaVersion` against real Qdrant, routed through
    `testCollection("schemaversion_stamp")` and `newTestStore` — never a raw `New`, and never an
    unprefixed collection name.

    Drive it from an enumerated table of rows, each naming the write method it exercises and the
    expected resulting version **expressed in terms of `migrate.CurrentVersion`**, never a literal:

    1. `upsert-fresh` — `Store.Upsert` of a new record → stored version is `CurrentVersion`.
    2. `upsert-scheduled` — `Store.Upsert` of a record carrying `NotBefore`/`NotAfter` → stored
       version is `CurrentVersion`. This is the ROADMAP criterion-1 "schedule" write path; it shares
       `Store.Upsert` but must be asserted rather than assumed.
    3. `update` — `Store.Update` of the record from row 1 → still `CurrentVersion`.
    4. `update-preserves-newer` — raw-inject a record whose payload carries `schema_version` at
       `CurrentVersion + 2` via the Qdrant client's `SetPayload`, then `Store.Update` it → stored
       version is still `CurrentVersion + 2`. This is D-05's monotonic rule observed at the write
       path, and it is the behavioral half of criterion 5.
    5. `supersede` — `Store.Supersede` → the NEW correcting record's version is `CurrentVersion`.
    6. `partial-write-does-not-stamp` — raw-inject a record with the `schema_version` key ABSENT,
       call `Store.SetVisibility` on it (a `setPayloadKeys` partial write), then read the raw payload
       back and assert the `schema_version` key is STILL ABSENT. This asserts D-02's accepted
       limitation rather than leaving it as an untested claim: a one-key merge cannot honestly claim
       currency, and a v0 record touched by `SetVisibility` stays v0 until the sweep reaches it.

    Read the resulting version back through `Store.Get` for rows 1-5 and through a raw Qdrant point
    read for row 6 (the assertion there is about key *presence*, which the decoded struct cannot
    distinguish from a zero value).

    Assert the executed row count equals the enumerated row count (6) at the end of the test, so a
    row that silently fails to run fails the suite. Failure messages name the row, the observed
    version and the wanted version.
  </action>
  <verify>
    <automated>go test -v -run 'TestEveryFullWriteMethodStampsSchemaVersion$' ./internal/store/... 2>&1 | rg -q -- '--- PASS: TestEveryFullWriteMethodStampsSchemaVersion'</automated>
  </verify>
  <acceptance_criteria>
    - The command above matches; a bare `ok ... [no tests to run]` is a FALSE GREEN and a failure.
    - The test enumerates exactly six named rows covering: fresh upsert, scheduled upsert, update, update-preserves-newer, supersede, and the partial-write negative.
    - No expected version anywhere in the file is a bare integer literal; every one is written in terms of `migrate.CurrentVersion`.
    - The `update-preserves-newer` row raw-injects `CurrentVersion + 2` and asserts the post-`Update` value is still `CurrentVersion + 2` (monotonic, not downgraded).
    - The `partial-write-does-not-stamp` row asserts the payload KEY is absent via a raw point read, not that a decoded field equals zero.
    - The test asserts its executed row count equals 6 before returning.
    - The collection name is produced by `testCollection(...)` and the store by `newTestStore(...)`.
    - On failure, sort failures by timestamp and read the EARLIEST: a leading `connection refused` is the known testcontainer cascade (#497), not an assertion failure.
  </acceptance_criteria>
  <done>Each full-write method is observed stamping against real Qdrant with monotonicity preserved, and the partial-write path is observed deliberately not stamping.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `internal/store` source ↔ the conformance gate | The gate reads source text as untrusted input; a bypass shape it cannot see is a hole. |
| `internal/store` → Qdrant point writes | Every payload map crossing this boundary must have been produced by `payload()` or be a named exception. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Tampering | a future Qdrant point write that bypasses `payload()` | high | mitigate | The AST gate keyed on call-site identity plus set equality against a one-entry allowlist; a new bypass in any non-test file of the package fails the suite. Proven RED by injecting a real bypass into real source, not only a fixture. |
| T-02-06 | Repudiation | a gate that reports clean while inspecting nothing | high | mitigate | Two zero-applicability guards (missing directory → error; `filesScanned == 0` → failure), a logged scanned-file count, and set-equality assertions in every fixture subtest. No `len(findings) > 0` assertion is permitted in the gate file. |
| T-02-07 | Tampering | a stale exception allowlist silently permitting a real hole | medium | mitigate | Set equality in both directions: an allowlist entry with no matching finding fails the gate. Proven RED by removing the `Store.Reindex` entry and observing the failure. |
| T-02-08 | Tampering | a partial write falsely claiming currency | medium | mitigate | Row 6 asserts `Store.SetVisibility` leaves the `schema_version` key absent on a v0 record, pinning D-02's accepted limitation as observed behaviour. |
| T-02-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task in this phase; RESEARCH.md's Package Legitimacy Audit records N/A. |
</threat_model>

<verification>
- `go test -v ./internal/store/... -run 'TestEveryPointWriteRoutesThroughPayload$|TestEveryFullWriteMethodStampsSchemaVersion$'` shows an explicit `--- PASS:` line for every named subtest.
- `git status --porcelain internal/store/store.go` is empty — both prove-RED injections were reverted.
- `task` (lint + test) is green.
</verification>

<success_criteria>
- Criterion 1 is proven structurally: an AST gate keyed on call-site identity, with derived findings compared by set equality against a one-entry named allowlist, over a non-zero scanned-file count.
- Criterion 1 is proven behaviorally: six enumerated rows covering every full-write method plus the partial-write negative, all expectations derived from `migrate.CurrentVersion`.
- Prove-RED-then-revert is recorded in both directions against real package source.
</success_criteria>

<output>
Create `.planning/phases/02-record-schema-versioning-foundation/02-02-SUMMARY.md` when done, including the "Prove-RED evidence" section.
</output>
