---
phase: 02-record-schema-versioning-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - internal/store/schemaversion_stamp_gate_test.go
  - internal/store/testdata/schemaversionstamp/good_pkg.go.txt
  - internal/store/testdata/schemaversionstamp/bad_pkg.go.txt
  - internal/store/schemaversion_stamp_test.go
  - internal/store/store.go
autonomous: true
requirements:
  - REQ-schema-version-stamped

estimate:
  tokens: 74000
  raw_tokens: 74000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The gate derives its subject set from the WRITE BOUNDARY, not from payload-construction syntax: it scans internal/store non-test source for every call site whose callee method name is Upsert, keyed by enclosing function, so a bypass that transmits a request built by a helper, a clone, a parameter or a generated builder is still surfaced even though it constructs no new qdrant.PointStruct literal."
    - "The derived Upsert call-site set is asserted set-equal to a two-entry classification — Store.Upsert (conforming) and Store.Reindex (named verbatim-raw-copy exception) — so both a new bypassing write and a stale classification entry turn the gate RED."
    - "For the site classified CONFORMING, a second assertion proves its transmitted payload derives from payload(): the qdrant.PointStruct it builds carries Payload whose value is qdrant.NewValueMap applied to a call of payload. This is the conformance check, applied to a site the boundary scan already found — never the completeness derivation."
    - "The partial-write boundary is derived the same way and classified explicitly: every SetPayload, DeletePayload and OverwritePayload call site in internal/store non-test source is set-equal to an enumerated non-stamping classification carrying the D-02 justification, so a new partial-write path must be classified before the suite goes green."
    - "The gate reports the number of non-test files it scanned and FAILS when that number is zero, and it fails loudly on a nonexistent scan directory rather than reporting clean."
    - "Both fixtures contain REAL client.Upsert calls, not bare struct literals: the bad fixture's two bypass shapes each transmit a request, and one of them transmits a request bound to a local variable first, so the fixtures pin the property the gate actually claims rather than a shape adjacent to it."
    - "Every full-write mechanism reaching Qdrant (direct Store.Upsert including its scheduled-window argument shape, Store.Update, Store.Supersede) is separately proven behaviorally to leave schema_version at max(migrate.CurrentVersion, the record's prior version) against a real Qdrant, with the executed row count asserted equal to the enumerated row count. The table is a behavioral enumeration; method-completeness is the structural gate's job, not this table's."
    - "A partial-write path (Store.SetVisibility) is proven NOT to stamp: a raw-injected v0 record touched by it stays at v0 — the D-02 accepted limitation is asserted, not assumed."
    - "RED was proven in BOTH directions against real source, not only a fixture: injecting a payload-bypassing Upsert call into internal/store non-test source turns the gate RED and reverting turns it green, and removing the Reindex classification entry turns it RED and restoring it turns it green. Both cycles are recorded in the plan SUMMARY."
    - "Every temporary source mutation is reverted by an EXACT INVERSE PATCH and verified with git diff --exit-code scoped to the touched file — never by git checkout --, which would discard unrelated uncommitted work in a dirty or concurrently-shared worktree."
  prohibitions:
    - statement: "MUST NOT ship a gate that can report clean while having inspected nothing. A green gate over zero subjects is a false assurance to the operator, and this project has a recorded instance of exactly that defect shipping and being passed by a goal-backward verifier as having no gaps."
      requirement_id: REQ-schema-version-stamped
      category: transparency
  artifacts:
    - path: "internal/store/schemaversion_stamp_gate_test.go"
      provides: "Write-boundary AST gate: derives the Upsert call-site set, classifies it, and proves the conforming site routes through payload(); plus the partial-write classification gate"
      contains: "TestEveryPointWriteRoutesThroughPayload"
    - path: "internal/store/testdata/schemaversionstamp/good_pkg.go.txt"
      provides: "Known-good fixture: real client.Upsert calls whose request payload derives from payload()"
    - path: "internal/store/testdata/schemaversionstamp/bad_pkg.go.txt"
      provides: "Known-bad fixture: two real client.Upsert calls that bypass payload(), one transmitting a request bound to a local variable first"
    - path: "internal/store/schemaversion_stamp_test.go"
      provides: "Behavioral per-write-method stamping proof plus the partial-write negative"
      contains: "TestEveryFullWriteMethodStampsSchemaVersion"
  key_links:
    - from: "internal/store/schemaversion_stamp_gate_test.go"
      to: "internal/store/store.go"
      via: "AST scan of non-test source for call sites whose callee method name is Upsert — the transmitted write boundary"
      pattern: 'client\.Upsert'
    - from: "internal/store/schemaversion_stamp_test.go"
      to: "internal/migrate/migrate.go"
      via: "expected stamps derived from migrate.CurrentVersion, never hard-coded"
      pattern: 'migrate\.CurrentVersion'
---

<objective>
Prove ROADMAP success criterion 1 — "every write path stamps the current `schema_version`, proven by
a test asserting 100% of write paths stamp, not a sample" — structurally and behaviorally.

This plan is the whole of **D-03**: the structural half is the source-level conformance gate in the
`internal/surfaces` / `internal/keylinks` idiom, establishing that `payload()` is the only door for
full-record writes — and it is anchored on the **transmitted write boundary**, the set of call sites
that actually hand a request to Qdrant, exactly as D-03's "anchor on identity, not argument shape"
planner note requires; the behavioral half is the round-trip assertion per full-write method that
D-03 pairs with it (Task 3).

Purpose: the structural half is the load-bearing one because it catches the write path that does not
exist yet. A behavioral test can only assert about the three methods someone remembered to list; a
gate over the write boundary fails the moment a fourth transmission appears, however its payload was
built. This project has a recorded instance (durable memory `x6v6qxqd6f`) of a Phase 1 gate that
shipped while scanning nothing, was bypassed by a local variable holding the literal, and was passed
3/3 "no gaps" by a goal-backward verifier — every design choice below is a direct response to that.

Output: a write-boundary AST gate with good/bad fixtures that transmit real requests, a two-entry
classification, a partial-write classification gate, a behavioral per-method stamping proof, and
recorded prove-RED-then-revert evidence in both directions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-record-schema-versioning-foundation/02-CONTEXT.md
@.planning/phases/02-record-schema-versioning-foundation/02-RESEARCH.md
@.planning/phases/02-record-schema-versioning-foundation/02-PATTERNS.md
@.planning/phases/02-record-schema-versioning-foundation/02-01-SUMMARY.md
@CLAUDE.md
</context>

## Correction this plan is built on

CONTEXT.md's D-01 states that `Upsert`, `Update`, `Supersede` **and `Reindex`** all funnel through
`payload()`. RESEARCH.md disproves the fourth by reading the code: `payload()` has exactly one call
site — `internal/store/store.go:749`, inside `Store.Upsert` — and `Store.Reindex`'s per-point write
calls the raw Qdrant client directly with `Payload: p.Payload`, the payload map scrolled verbatim
off the *source* collection. `Update` and `Supersede` reach `payload()` transitively through
`s.Upsert`; `Reindex` never reaches it at all.

**Consequence this gate must encode:** Reindex's per-point write is a *named, justified exception*
in the classification, mirroring the existing `embedder_identity` exception comment already at that
write. It is NOT a conforming site and must NOT be forced to route through `payload()` — refactoring
Reindex is outside this phase's additive scope. A gate written on D-01's stated premise would red on
a write path that was never broken.

## The gate redesign this revision makes (review findings H1 + H2)

Cross-AI review (`02-REVIEWS.md`) landed two HIGH findings on this plan's previous design, and both
are correct. The previous gate scanned `qdrant.PointStruct` **composite literals** and classified the
`Payload` field's value expression. That is the wrong surface, twice over:

1. **A bypass need not construct a literal.** `s.client.Upsert(ctx, req)` where `req` — or the points
   inside it — came from a helper, a clone, a function parameter or a generated builder adds no new
   `qdrant.PointStruct{...}` to the scanned package. The gate stays green while a raw point write
   ships. Scanning the construction is scanning one *possible* upstream of the write, not the write.
2. **A literal need not be transmitted.** A fixture containing only a `PointStruct` literal was
   classified as a "point write" even though nothing ever sends it, so the fixtures did not pin the
   property the gate advertised.

**The corrected anchor is the transmitted boundary: call sites of `Upsert`.** Note this is MORE
faithful to CONTEXT.md D-03's instruction — "anchor this gate on identity (does this call site route
through `payload()`) rather than on argument shape" — not less. The previous design anchored on the
identity of the wrong thing.

The redesign **replaces** machinery rather than adding to it. The literal classifier survives, but
demoted: it is no longer the completeness derivation, it is the *conformance check applied to the one
call site the boundary scan classifies as conforming*. The two halves are now complementary and each
does one job:

| Half | Question it answers | Failure it catches |
|---|---|---|
| Boundary scan (`Upsert` call sites → enclosing function) | *Which functions transmit a full point write?* | A new write path, however its payload was built |
| Conformance check on the conforming site | *Does that site's payload come from `payload()`?* | Replacing `payload(m)` with a hand-built map at the legitimate call site |

### Verified against source at revision time

`rg -n 'client\.Upsert\(' internal/store/*.go --glob '!*_test.go'` returns exactly two hits:
`store.go:744` (inside `Store.Upsert`) and `store.go:3213` (inside `Store.Reindex`). The two-entry
classification below is therefore complete as of today — and now it is complete *at the boundary the
invariant is actually about*.

## Scope decision (review MEDIUM: "the phase may be over-engineered")

Codex raised, and Kimi disputed, whether five plans and thirteen tasks are disproportionate for one
additive field. **Decision: keep the task count; the redesign above is what makes it proportionate.**
The reasoning, recorded here so it is not relitigated at execution:

- The redesign is net-neutral in machinery, not additive. One scanner replaces another; the fixtures
  change shape rather than multiply; Task count in this plan stays at 3.
- Codex's own ranking of the five highest-value proofs — codec round trip and absent decode, Qdrant
  Upsert emission-site classification, transmitted recall-filter capture, raw future-payload
  compatibility, MCP wire visibility — maps one-to-one onto plans 02-01, 02-02, 02-03, 02-04, 02-05.
  There is no plan in this phase outside his own top five. Cutting a plan means cutting a proof he
  ranked as load-bearing.
- The one genuinely additive item in this revision is the partial-write classification gate in Task 2.
  It is ~15 lines on a scanner that already exists, and it converts D-02's "partial writes never
  stamp" from a claim proven for one method behaviorally into a property a newcomer must classify.
- The cost this phase is guarding against is not hypothetical. Phase 1 shipped gates carrying the
  exact defect they were built to eliminate, and a goal-backward verifier passed them 3/3 with "no
  gaps" (durable memory `x6v6qxqd6f`). The verification weight here is calibrated to that record.

## Artifacts this phase produces

See the table in `02-01-PLAN.md` § "Artifacts this phase produces" — every symbol listed there is
created by this phase and must be excluded from drift/source-grounding verification. This plan
contributes: `internal/store/schemaversion_stamp_gate_test.go`,
`internal/store/schemaversion_stamp_test.go`,
`internal/store/testdata/schemaversionstamp/{good_pkg.go.txt,bad_pkg.go.txt}`,
`scanQdrantCalls`, `scanPackageDirForCalls`, `payloadDerivesFromCodec`,
`TestEveryPointWriteRoutesThroughPayload`, `TestPartialWritePathsAreClassifiedNonStamping`,
`TestEveryFullWriteMethodStampsSchemaVersion`.

Plan 02-03 **reuses** `scanQdrantCalls` and `scanPackageDirForCalls` for its own emission-site scan —
build them with a caller-supplied method-name set so a second scanner never has to exist.

<tasks>

<task type="auto">
  <name>Task 1: The write-boundary scanner, the conformance predicate, and fixtures that transmit real requests</name>
  <files>internal/store/schemaversion_stamp_gate_test.go, internal/store/testdata/schemaversionstamp/good_pkg.go.txt, internal/store/testdata/schemaversionstamp/bad_pkg.go.txt</files>
  <read_first>
    - `internal/store/collectionprefix_conformance_test.go` — the whole file. Specifically `scanConstructions` (`:159`), `isStoreConstructorCall` (`:140`, the call-site-identity anchor to mirror), `scanPackageDir` (`:216`, including its comment on why `err != nil` and `filesScanned == 0` are the same failure class), and the four subtests of `TestEveryStoreConstructionRoutesThroughSeam` (`:244-325`) — the good-fixture, both-bypass-shapes set-equality, zero-applicability and real-package structure.
    - `internal/store/testdata/collectionprefix/good_pkg_test.go.txt` and `bad_pkg_test.go.txt` — the fixture file convention (compiled-out `.txt` suffix, self-contained package text).
    - `internal/store/store.go` lines 731-770 — `Store.Upsert`'s conforming `qdrant.PointStruct` construction with `Payload: qdrant.NewValueMap(payload(m))`.
    - `internal/store/store.go` lines 3200-3230 — Reindex's non-conforming `Payload: p.Payload` write and its existing `embedder_identity` exception comment.
    - `.planning/phases/02-record-schema-versioning-foundation/02-RESEARCH.md` § "Pattern 2" and § "Pitfall 1".
  </read_first>
  <action>
    Create `internal/store/schemaversion_stamp_gate_test.go` (SPDX header, `package store`) holding
    the scanner, and the two fixtures under `internal/store/testdata/schemaversionstamp/`.

    Read the "gate redesign" section of this plan before writing a line. The scan surface is the
    **transmitted write boundary**, not payload construction; a scanner built on the latter is the
    defect this revision exists to remove.

    **`scanQdrantCalls` — the boundary scanner.** Using `go/ast`, `go/parser` and `go/token` — never
    a regex or string match over source text, which the Don't-Hand-Roll table forbids for exactly
    this — walk each parsed file for `ast.CallExpr` nodes whose callee is an `ast.SelectorExpr` whose
    `Sel` name is in a caller-supplied method-name set. The same function serves both the full-write
    scan (method set `{"Upsert"}`) and the partial-write scan in Task 2 (method set
    `{"SetPayload", "DeletePayload", "OverwritePayload"}`).

    Match on the **method name alone**, deliberately ignoring the receiver expression. This
    over-approximates: it catches `s.client.Upsert(...)`, a local alias `c := s.client; c.Upsert(...)`,
    a parameter `cl.Upsert(...)`, and a same-named method on some other type. Over-approximation is
    the correct bias for a gate — a false positive costs one line of explicit classification, a false
    negative is the hole. Do NOT narrow this to a receiver-expression pattern to reduce noise; that
    reintroduces the syntax-fragility the redesign removed. State this reasoning in the scanner's doc
    comment so a future maintainer does not "tighten" it back.

    Each call site records the enclosing function name (innermost enclosing `ast.FuncDecl`, prefixed
    with the receiver type name when there is one, e.g. `Store.Reindex`), the display path and line,
    and the matched method name. The enclosing **function name** is the identity key — not the line
    number, which drifts every time `store.go` is edited and would make the classification stale by
    accident.

    **`payloadDerivesFromCodec` — the conformance check.** A separate, smaller predicate, applied
    ONLY to a call site the boundary scan has already classified as conforming. Given the enclosing
    `ast.FuncDecl`, find the `qdrant.PointStruct` composite literal within it (both `&qdrant.PointStruct{...}`
    and the bare form), locate the `Payload` key-value element, and report conforming when and only
    when its value is a call whose callee is the selector `qdrant.NewValueMap` AND that call's single
    argument is itself a call whose callee is the identifier `payload`. Do not inspect what is inside
    `payload(...)`'s own argument list — an argument-shape rule is precisely the check that a local
    variable defeated in Phase 1. Report non-conforming in every other case, including a missing
    `Payload` element and a missing `PointStruct` literal.

    This predicate is a **conformance check, not a completeness derivation**, and its doc comment
    must say so: it answers "does the site the boundary scan found route its payload through the
    codec", never "which sites exist". Conflating the two is what the review found.

    **`scanPackageDirForCalls` — directory scanning.** The sibling of `scanPackageDir`: read the
    directory's entries non-recursively, select files by a suffix parameter so the same function
    serves both the real package (`.go`, skipping any name ending `_test.go`) and the fixture
    directory (`.go.txt`), return `(sites, filesScanned, err)`, and surface a missing directory as an
    error rather than a silent zero. Copy `scanPackageDir`'s doc-comment reasoning verbatim in
    spirit: a scan that cannot see what it was told to check must never report clean.

    **`good_pkg.go.txt`.** A self-contained package text containing **real `Upsert` calls** — this is
    the H2 correction; a fixture holding only a struct literal proves nothing about a boundary the
    gate defines by transmission. Two conforming functions: one building the point inline and calling
    `client.Upsert(ctx, req)` where the point's `Payload` is `qdrant.NewValueMap(payload(m))`; one
    where the `Memory` value is first bound to a local variable and then passed to `payload` — proving
    the conformance check accepts a legitimate refactor of the *argument* while still requiring the
    *callee* identity. Expect two Upsert call sites, both classified conforming.

    **`bad_pkg.go.txt`.** A self-contained package text with BOTH bypass shapes, each **transmitting
    an actual request**, in two distinctly named functions so the site set has two distinguishable
    members:
    (a) a function that builds a hand-built payload map inline into the point and calls
        `client.Upsert` with it — a shape that can carry a `schema_version` key by hand and still
        never call `payload()`;
    (b) the shape the previous design could not see at all: the request is assembled by a **separate
        helper function** and passed to `client.Upsert` through a local variable, so the enclosing
        function of the write contains no `qdrant.PointStruct` literal whatsoever. This is the H1
        bypass, and the fixture exists to prove the new scan surface sees it. Comment it in the
        fixture as such.
    Expect exactly two Upsert call sites, both classified non-conforming.

    **Fixture subtests.** Mirror `TestEveryStoreConstructionRoutesThroughSeam`'s structure inside a
    new `TestEveryPointWriteRoutesThroughPayload`:
    - `good fixture: every Upsert site is conforming` — assert the derived site set has size 2, the
      expected two function names, and that both pass the conformance predicate. Fail with the site
      list on any mismatch.
    - `bad fixture yields a finding for BOTH bypass shapes` — collect the sites into a set keyed by
      enclosing function name, assert every expected member is present AND that the set's size equals
      the expected size, AND that both fail the conformance predicate. Carry the collectionprefix
      comment's reasoning: an "at least one finding" assertion passes while catching only one of two
      bypass shapes, which is the exact state the Phase 1 gate shipped in.
    - `helper-built request is still seen` — a dedicated subtest naming shape (b) explicitly and
      asserting it appears in the derived set. This is the regression guard for the H1 defect: a
      scanner that reverted to construction-literal scanning fails HERE, by name, rather than
      silently going quiet.
    - `zero-applicability guard: nonexistent directory fails loudly` — the directory scanner over a
      path that does not exist must return a non-nil error.
    - `zero-applicability guard: a directory with no matching files fails loudly` — point the
      scanner at an existing directory containing no file with the requested suffix and assert the
      caller treats a zero scanned-file count as a failure, not as clean.
  </action>
  <verify>
    <automated>go test -v -run 'TestEveryPointWriteRoutesThroughPayload$' ./internal/store/... 2>&1 | rg -c -- '--- PASS: TestEveryPointWriteRoutesThroughPayload/'</automated>
  </verify>
  <acceptance_criteria>
    - The command above prints `5` — all five subtests of this task ran and passed. Fewer means a subtest name drifted and the `-run` pattern matched nothing (durable record `bsbsvn4hbc`). Task 2 adds a sixth; re-check the count after that task.
    - `internal/store/schemaversion_stamp_gate_test.go` imports `go/ast`, `go/parser` and `go/token`, and contains no `regexp` import and no `strings.Contains` over source text for any classification decision.
    - The derived subject set comes from `ast.CallExpr` nodes matched on callee method name — the test file derives no subject set from `qdrant.PointStruct` composite literals. A scan whose subject set is derived from composite literals is a failure of this task regardless of what it asserts.
    - `scanQdrantCalls` matches on the selector's method name alone and its doc comment states that the receiver expression is deliberately ignored, that this over-approximates, and why over-approximation is the correct bias for a gate.
    - `payloadDerivesFromCodec`'s doc comment states that it is a conformance check applied to an already-derived site, never a completeness derivation.
    - The conformance decision inspects only the callee identity of the `Payload` value expression and of its single argument; the test file contains no inspection of `payload(...)`'s own arguments.
    - Call sites are keyed by enclosing function name (receiver-qualified where applicable), not by line number.
    - Both fixtures contain real calls whose callee method name is `Upsert`; neither fixture relies on a bare struct literal to be counted as a write.
    - `good_pkg.go.txt` contains two conforming write sites, one of which passes a locally-bound `Memory` variable to `payload`, and the good-fixture subtest asserts a derived set of size exactly 2 with both members conforming.
    - `bad_pkg.go.txt` contains exactly two non-conforming write sites in distinctly named functions, one of which obtains its request from a separate helper and contains no point-struct literal in the writing function at all; the bad-fixture subtest asserts the derived set both CONTAINS both names AND has size exactly 2. A non-zero-length assertion used as the positive proof anywhere in this file is a failure of this task.
    - The `helper-built request is still seen` subtest exists by that name and asserts the helper-built shape appears in the derived set.
    - Both zero-applicability subtests exist: one for a nonexistent directory (error expected) and one for an existing directory with no matching files (a zero scanned-file count treated as failure).
    - `task license:check` exits 0 and `gofmt -l internal/store` prints nothing.
  </acceptance_criteria>
  <done>The scanner derives its subject set from the transmitted write boundary, the conformance predicate is applied to that derived set rather than standing in for it, the two fixtures transmit real requests and pin both behaviors with set equality, and a scan that sees nothing fails loudly.</done>
</task>

<task type="auto">
  <name>Task 2: Classify the real package's write boundary — full writes and partial writes — and prove RED both ways</name>
  <files>internal/store/schemaversion_stamp_gate_test.go, internal/store/store.go</files>
  <read_first>
    - `internal/store/schemaversion_stamp_gate_test.go` — the scanner and conformance predicate written in Task 1.
    - `internal/store/collectionprefix_conformance_test.go` lines 294-325 — the `real packages` subtest with its `n == 0` and `totalFiles == 0` guards and its `t.Logf` of the scanned count.
    - `internal/store/store.go` lines 3200-3230 — the exact enclosing function and the existing exception comment that this classification entry's justification must agree with.
    - The output of `rg -n 'client\.(Upsert|SetPayload|DeletePayload|OverwritePayload)\(' internal/store/*.go --glob '!*_test.go'` — run it BEFORE writing the classification lists and populate them from what the scan reports, not from this plan's prose. This plan's table below is the expectation to cross-check against, not the source of truth.
    - `internal/store/summarize.go` around line 84 and `internal/store/spine.go` around line 750 — the two partial-write call sites outside `store.go`, so the partial-write classification is not silently `store.go`-only.
    - `.planning/phases/02-record-schema-versioning-foundation/02-RESEARCH.md` § "D-07 Research Flag — Resolved" points 1-4.
    - `.planning/phases/01-gate-ci-integrity/01-06-SUMMARY.md` — how Phase 1 recorded its own prove-RED evidence, for the format to reuse in this plan's SUMMARY.
  </read_first>
  <action>
    Add the `real package` and `partial writes are classified non-stamping` subtests, then prove the
    gate goes RED against real source in both directions and revert safely.

    **The full-write classification.** Declare a package-level table with exactly two entries, each
    pairing an enclosing function name with a disposition and a justification string:

    | Enclosing function | Disposition | Justification |
    |---|---|---|
    | `Store.Upsert` | conforming | Its request payload derives from `payload()`, so every full record it transmits carries the monotonic stamp. Subject to the conformance predicate below. |
    | `Store.Reindex` | named raw-copy exception | It copies the raw payload map scrolled off the source collection rather than rebuilding it from a decoded `Memory`, which is what makes it preserve every key including ones this binary does not know about. Routing it through `payload()` would defeat that contract. |

    Reference the existing `embedder_identity` exception comment at Reindex's write in the Reindex
    entry's justification so the two explanations cannot drift.

    **The real-package subtest.** Scan `internal/store`'s own directory for non-test `.go` files with
    method-name set `{"Upsert"}`. Fail loudly if the scan errors OR if the scanned-file count is
    zero, log the scanned count, then assert two things:

    1. **Completeness by set equality.** The derived set of enclosing function names is set-equal to
       the two classification entries. Every derived site must be classified, AND every classified
       entry must appear in the derived set. The second half is the stale-classification guard — if
       Reindex is ever refactored to route through `payload()`, its entry becomes a lie and this test
       says so instead of silently permitting a hole. The failure messages must print both
       difference directions by name.
    2. **Conformance of the conforming site.** Apply `payloadDerivesFromCodec` to `Store.Upsert`'s
       enclosing declaration and assert it passes. This is what catches someone replacing
       `payload(m)` with a hand-built map at the legitimate call site — a change the boundary scan
       alone cannot see, because the call site count would not move.

    **The partial-write classification subtest** (`TestPartialWritePathsAreClassifiedNonStamping`).
    Run the same scanner over the same directory with method-name set
    `{"SetPayload", "DeletePayload", "OverwritePayload"}`, and assert the derived enclosing-function
    set is set-equal to an enumerated classification list, every entry carrying the D-02
    justification: a one-key merge cannot honestly claim currency, so a partial write that stamped
    the current version would assert "every key of this version is present" while having written
    only one — a false-currency claim that would make Phase 3's sweep skip records still needing
    migration. Populate the list from the scan, cross-check against the `rg` output read above, and
    note any divergence in the SUMMARY. Same fail-loud guards: scan error and zero scanned files are
    the same failure class.

    This subtest is what turns D-02 from a property proven for one method behaviorally (Task 3's
    `SetVisibility` row) into a property a new partial-write path must be classified against before
    the suite goes green.

    **Prove RED — direction 1 (an added bypass at the boundary).** Temporarily edit
    `internal/store/store.go` — real package source, not a fixture, because a fixture-only proof is
    what missed the Phase 1 bypass — adding a small unexported function that builds its request
    through a **local variable assigned from a helper** and calls `s.client.Upsert` with it. Use the
    helper-built shape specifically: this is the shape the previous gate design could not see, so
    observing RED on it is the evidence that the redesign works on real source and not only on a
    fixture. Run the gate: it MUST fail, naming the new function as derived-but-unclassified. Record
    the exact failure output.

    **Reverting a temporary source mutation — the required procedure** (review finding, both
    reviewers). Do NOT use `git checkout -- internal/store/store.go`. That discards every uncommitted
    change to the file, not only the injection, and this repo has a standing rule about concurrent
    agents sharing one git index — an unrelated in-flight edit would be destroyed silently. Instead:

    1. Before injecting, capture the exact injected hunk (write the patch, or keep the exact inserted
       text) so an inverse is available.
    2. Revert by applying the exact inverse — remove precisely the injected lines, or
       `git apply -R` the captured patch.
    3. Verify with `git diff --exit-code -- internal/store/store.go`. This is the sharper check than
       `git status --porcelain` for an edited tracked file: it fails on any residual content
       difference rather than merely reporting a modified path. If the file legitimately carried
       other uncommitted work before the injection, verify instead that `git diff -- internal/store/store.go`
       is byte-identical to the diff captured before the injection, and record that comparison in the
       SUMMARY.

    **Prove RED — direction 2 (a stale classification).** Temporarily remove the `Store.Reindex`
    entry from the classification table. Run the gate: it MUST fail, naming `Store.Reindex` as an
    unclassified derived site. Restore the entry, re-run: it MUST pass. This proves the set-equality
    assertion has teeth on the side that a subset-only check would miss. This injection is in the
    test file, so the same inverse-patch discipline applies to it.

    Record both cycles — command, observed failure text, revert method, observed pass — in this
    plan's SUMMARY under a "Prove-RED evidence" heading, and carry the same evidence into the phase
    `VERIFICATION.md` when it is written. A gate whose RED was never observed is not shippable
    (durable record `v5q7jdbw43`).
  </action>
  <verify>
    <automated>go test -v -run 'TestEveryPointWriteRoutesThroughPayload$|TestPartialWritePathsAreClassifiedNonStamping$' ./internal/store/... 2>&1 | rg -c -- '--- PASS: (TestEveryPointWriteRoutesThroughPayload/real_package|TestPartialWritePathsAreClassifiedNonStamping)'</automated>
  </verify>
  <acceptance_criteria>
    - The command above prints `2` — the real-package subtest and the partial-write gate both ran and passed by those exact names.
    - Both subtests fail when the scan returns an error AND when the scanned-file count is zero; both guards are present as separate `t.Fatalf` branches in each.
    - Each subtest logs the number of non-test files scanned, and that number is greater than 1 when the suite runs.
    - The full-write classification table has exactly two entries — `Store.Upsert` (conforming) and `Store.Reindex` (named raw-copy exception) — each carrying a non-empty justification string; Reindex's names the verbatim raw-map copy as the reason.
    - The real-package subtest applies the conformance predicate to `Store.Upsert` and asserts it passes, so replacing `payload(m)` with a hand-built map at the legitimate call site fails the gate even though the derived site count is unchanged.
    - The partial-write classification covers call sites outside `store.go`: `internal/store/summarize.go` and `internal/store/spine.go` each contribute at least one classified entry, so the scan is provably not `store.go`-only.
    - Every comparison is set equality in both directions: an unclassified derived site fails, and a classified entry with no corresponding derived site fails. The file contains no subset-only check.
    - Prove-RED direction 1 is recorded in `02-02-SUMMARY.md`: the injected bypass (helper-built request through a local variable, transmitting via `Upsert`), the observed failing output naming the injected function, the exact-inverse revert, and the observed pass.
    - Prove-RED direction 2 is recorded in `02-02-SUMMARY.md`: the removed classification entry, the observed failing output naming `Store.Reindex`, the restore, and the observed pass.
    - Neither prove-RED cycle used `git checkout --` on a tracked file. The SUMMARY names the revert method used for each cycle.
    - `git diff --exit-code -- internal/store/store.go` succeeds at task end (or, if the worktree legitimately carried prior uncommitted work in that file, the SUMMARY records the before/after diff comparison instead) — the injected bypass was fully reverted and no unrelated edit was destroyed.
    - `rg -c 'payload\(m\)' internal/store/store.go` reports 1 — the single-door invariant is intact after the revert.
  </acceptance_criteria>
  <done>The gate derives both write boundaries from real `internal/store` source, reports what it scanned, matches each derived set to an explicit classification by set equality, separately proves the conforming site still routes through the codec, and has been observed RED in both the added-bypass and stale-classification directions with reverts that cannot destroy unrelated work.</done>
</task>

<task type="auto">
  <name>Task 3: Behavioral proof — every full-write method stamps, and no partial-write method does</name>
  <precondition>A Qdrant instance is reachable for `internal/store`'s TestMain: either the Docker daemon responds to `docker info` or `ENGRAM_QDRANT_TEST_ADDR` is set.</precondition>
  <files>internal/store/schemaversion_stamp_test.go</files>
  <read_first>
    - `internal/store/store.go` lines 731-770 (`Store.Upsert`), 1860-1900 (`Store.Update`), 2270-2320 (`Store.Supersede`) — the three full-write methods and how each reaches `payload()`.
    - `internal/store/store.go` lines 367-420 — the `setPayloadKeys` / `deletePayloadKeys` function-var seams and the list of partial-write callers (`UpdatePayload`, `SetVisibility`, `IncrementAccess`, the supersede back-stamp, archive/restore, `RemapOwner`, `BackfillShortIDs`).
    - `internal/store/store_test.go` lines 50-200 — `testCollection`, `newTestStore`, `dialTestClient`, `TestMain`.
    - `internal/store/store_test.go` — an existing `Store.Supersede` integration test, for the setup shape (subject construction, owner wiring) to reuse rather than re-derive.
    - `internal/migrate/migrate.go` — `CurrentVersion`, so every expectation is derived from it.
  </read_first>
  <action>
    Create `internal/store/schemaversion_stamp_test.go` (SPDX header, `package store`) holding
    `TestEveryFullWriteMethodStampsSchemaVersion` against real Qdrant, routed through
    `testCollection("schemaversion_stamp")` and `newTestStore` — never a raw `New`, and never an
    unprefixed collection name.

    **What this table is and is not** (review finding, Codex MEDIUM). It is a *behavioral*
    enumeration: it observes what each caller-facing write mechanism actually leaves on disk. It is
    NOT a claim of method-completeness on its own, and no comment or SUMMARY line in this task may
    describe it as one. The distinct full-write mechanisms are direct `Store.Upsert`, `Store.Update`
    and `Store.Supersede`; the "scheduled" row is the same `Store.Upsert` method invoked with
    temporal fields set, kept as a row because the ROADMAP names it as a write path, not because it
    is a fourth mechanism. Method-completeness is Tasks 1-2's job — the boundary scan is what fails
    when a fifth transmission appears.

    Drive it from an enumerated table of rows, each naming the write method it exercises and the
    expected resulting version **expressed in terms of `migrate.CurrentVersion`**, never a literal:

    1. `upsert-fresh` — `Store.Upsert` of a new record → stored version is `CurrentVersion`.
    2. `upsert-scheduled` — `Store.Upsert` of a record carrying `NotBefore`/`NotAfter` → stored
       version is `CurrentVersion`. Same method, temporal argument shape; asserted rather than
       assumed because the ROADMAP names the schedule path explicitly.
    3. `update` — `Store.Update` of the record from row 1 → still `CurrentVersion`.
    4. `update-preserves-newer` — raw-inject a record whose payload carries `schema_version` at
       `CurrentVersion + 2` via the Qdrant client's `SetPayload`, then `Store.Update` it → stored
       version is still `CurrentVersion + 2`. This is D-05's monotonic rule observed at the write
       path, and it is the behavioral half of criterion 5.
    5. `supersede` — `Store.Supersede` → the NEW correcting record's version is `CurrentVersion`.
    6. `partial-write-does-not-stamp` — raw-inject a record with the `schema_version` key ABSENT,
       call `Store.SetVisibility` on it (a `setPayloadKeys` partial write), then read the raw payload
       back and assert the `schema_version` key is STILL ABSENT. This asserts D-02's accepted
       limitation rather than leaving it as an untested claim: a one-key merge cannot honestly claim
       currency, and a v0 record touched by `SetVisibility` stays v0 until the sweep reaches it.

    Read the resulting version back through `Store.Get` for rows 1-5 and through a raw Qdrant point
    read for row 6 (the assertion there is about key *presence*, which the decoded struct cannot
    distinguish from a zero value).

    Assert the executed row count equals the enumerated row count (6) at the end of the test, so a
    row that silently fails to run fails the suite. Failure messages name the row, the observed
    version and the wanted version.
  </action>
  <verify>
    <automated>go test -v -run 'TestEveryFullWriteMethodStampsSchemaVersion$' ./internal/store/... 2>&1 | rg -q -- '--- PASS: TestEveryFullWriteMethodStampsSchemaVersion'</automated>
  </verify>
  <acceptance_criteria>
    - The command above matches; a bare `ok ... [no tests to run]` is a FALSE GREEN and a failure.
    - The test enumerates exactly six named rows covering: fresh upsert, scheduled upsert, update, update-preserves-newer, supersede, and the partial-write negative.
    - Neither the test's comments nor `02-02-SUMMARY.md` describes this table as method-complete; both point at Tasks 1-2's boundary scan as the completeness mechanism and state that the scheduled row is the same `Store.Upsert` method with temporal fields.
    - No expected version anywhere in the file is a bare integer literal; every one is written in terms of `migrate.CurrentVersion`.
    - The `update-preserves-newer` row raw-injects `CurrentVersion + 2` and asserts the post-`Update` value is still `CurrentVersion + 2` (monotonic, not downgraded).
    - The `partial-write-does-not-stamp` row asserts the payload KEY is absent via a raw point read, not that a decoded field equals zero.
    - The test asserts its executed row count equals 6 before returning.
    - The collection name is produced by `testCollection(...)` and the store by `newTestStore(...)`.
    - On failure, sort failures by timestamp and read the EARLIEST: a leading `connection refused` is the known testcontainer cascade (#497), not an assertion failure.
  </acceptance_criteria>
  <done>Each full-write method is observed stamping against real Qdrant with monotonicity preserved, and the partial-write path is observed deliberately not stamping.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `internal/store` source ↔ the conformance gate | The gate reads source text as untrusted input; a write shape it cannot see is a hole. The scan surface, not the assertion rigour, is what determines what it can see. |
| `internal/store` → Qdrant point writes | Every full-record payload crossing this boundary must have been produced by `payload()` or be transmitted from a named exception. |
| `internal/store` → Qdrant partial payload writes | `SetPayload` / `DeletePayload` / `OverwritePayload` cross here and must NOT stamp; a stamping partial write is a false-currency claim. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Tampering | a future Qdrant point write that bypasses `payload()` | high | mitigate | **Revised after review (H1).** The gate derives its subject set from the transmitted write boundary — call sites whose callee method name is `Upsert`, keyed by enclosing function — so a bypass whose request came from a helper, clone, parameter or generated builder is surfaced even though it constructs no point-struct literal. Set equality against a two-entry classification; proven RED by injecting exactly that helper-built shape into real source. |
| T-02-05b | Tampering | the legitimate call site quietly dropping `payload()` while the site count stays the same | high | mitigate | The conformance predicate is applied to `Store.Upsert`'s declaration in the real-package subtest: its point's `Payload` value must be `qdrant.NewValueMap` applied to a `payload` call. A boundary scan alone cannot see this, which is why both halves exist. |
| T-02-06 | Repudiation | a gate that reports clean while inspecting nothing | high | mitigate | **Revised after review (H2).** Two zero-applicability guards (missing directory → error; zero scanned files → failure), a logged scanned-file count, set-equality assertions in every subtest, and fixtures that transmit real `Upsert` requests rather than holding inert literals — so the fixtures pin the property the gate claims rather than one adjacent to it. |
| T-02-07 | Tampering | a stale classification entry silently permitting a real hole | medium | mitigate | Set equality in both directions: a classification entry with no matching derived site fails the gate. Proven RED by removing the `Store.Reindex` entry and observing the failure. |
| T-02-08 | Tampering | a partial write falsely claiming currency | medium | mitigate | **Strengthened after review.** Structurally: Task 2's partial-write scan derives every `SetPayload`/`DeletePayload`/`OverwritePayload` site and requires each to be classified non-stamping with the D-02 justification, across `store.go`, `summarize.go` and `spine.go`. Behaviorally: Task 3 row 6 asserts `Store.SetVisibility` leaves the `schema_version` key absent on a v0 record. |
| T-02-20 | Tampering | a prove-RED injection reverted with `git checkout --`, destroying unrelated uncommitted work | medium | mitigate | Every temporary source mutation is reverted by exact inverse patch and verified with `git diff --exit-code` scoped to the touched file; `git checkout --` on a tracked file is forbidden by acceptance criteria and the revert method is recorded per cycle in the SUMMARY. |
| T-02-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task in this phase; RESEARCH.md's Package Legitimacy Audit records N/A. |
</threat_model>

<verification>
- `go test -v ./internal/store/... -run 'TestEveryPointWriteRoutesThroughPayload$|TestPartialWritePathsAreClassifiedNonStamping$|TestEveryFullWriteMethodStampsSchemaVersion$'` shows an explicit `--- PASS:` line for every named test and subtest.
- `git diff --exit-code -- internal/store/store.go` succeeds — every prove-RED injection was reverted by exact inverse patch, and no unrelated uncommitted work was destroyed.
- `task` (lint + test) is green.
</verification>

<success_criteria>
- Criterion 1 is proven structurally at the transmitted write boundary: the subject set is derived from `Upsert` call sites keyed by enclosing function, compared by set equality against a two-entry classification, over a non-zero scanned-file count — and the conforming site is separately proven to route its payload through `payload()`.
- D-02's partial-write exclusion is proven structurally as well: every partial-payload call site in the package is derived and classified non-stamping with its justification.
- Criterion 1 is proven behaviorally: six enumerated rows covering every full-write mechanism plus the partial-write negative, all expectations derived from `migrate.CurrentVersion`, with the table described as behavioral rather than method-complete.
- Prove-RED-then-revert is recorded in both directions against real package source, using exact inverse patches rather than `git checkout --`.
</success_criteria>

<output>
Create `.planning/phases/02-record-schema-versioning-foundation/02-02-SUMMARY.md` when done, including the "Prove-RED evidence" section.
</output>
