---
phase: 20-correctness-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - proto/engram/v1/engram.proto
  - internal/server/connectapi.go
  - internal/server/connectapi_test.go
  - internal/server/tools_test.go
  - gen/go/
  - gen/ts/
  - ui/src/lib/gen/
  - internal/webauth/static/
autonomous: true
requirements:
  - REQ-discovery-proto-fidelity
  - REQ-discovery-shortid-schema
user_setup: []

must_haves:
  truths:
    - "SearchDiscoveries returns kind and citations over the Connect wire (summary already worked pre-phase — D-03) — ROADMAP SC1"
    - "storeDiscoveryArgs.ID jsonschema advertises short_id support, pinned by a regression test — ROADMAP SC5"
    - "proto + gen drift stays clean (buf lint + buf breaking + gen/ diff)"
  artifacts:
    - "proto/engram/v1/engram.proto with additive Memory.kind (field 21) + Memory.citations (field 22)"
    - "internal/server/connectapi.go citationsToProto helper + extended memoryToProto"
    - "internal/server/connectapi_test.go Kind/Citations round-trip assertion"
    - "internal/server/tools_test.go TestStoreDiscoveryArgsIDSchemaAdvertisesShortID"
    - "regenerated gen/go, gen/ts, ui/src/lib/gen, rebuilt internal/webauth/static"
  key_links:
    - "store.Memory.Kind/.Citations -> memoryToProto -> engramv1.Memory.Kind/.Citations -> SearchDiscoveries response"
    - "proto:gen regenerates gen/ts -> re-vendored ui/src/lib/gen -> ui:build embeds SPA (CI ui-drift gate)"
---

<objective>
Close #307 (discovery proto fidelity) and #303 (discovery short_id schema) — the proto + discovery subsystem slice of Phase 20 (per D-10 subsystem split).

#307 extends the shared `Memory` proto message additively so `SearchDiscoveries` carries discovery `kind` and `citations` over the Connect wire instead of silently dropping them (`summary` is already mapped — D-03 — no change). Scope is wire-only per D-02: no console rendering this phase.

#303 is VERIFICATION-ONLY: `storeDiscoveryArgs.ID`'s jsonschema already advertises `short_id` (since commit 92a6f610, PR #288 — predates this phase). No production code change; add a regression test that pins the tag string so a future silent drop is caught, then close GitHub #303.

Purpose: Discovery records recalled over Connect stop losing their `kind`/`citations`, and the short_id schema advertisement gains a guard against silent regression.
Output: additive proto fields 21/22, regenerated gen trees, a `citationsToProto` read-path mapper, and two new regression tests.
</objective>

<artifacts_produced>
## Artifacts this phase produces (Plan A)

New / changed symbols:
- `Memory.kind` — proto field 21 (`string`), additive
- `Memory.citations` — proto field 22 (`repeated Citation`), additive, reuses existing `Citation` message
- `citationsToProto` — new store->proto helper in `internal/server/connectapi.go` (beside `memoryToProto`)
- `TestMemoryToProto` Kind/Citations round-trip case (extends `internal/server/connectapi_test.go`)
- `TestStoreDiscoveryArgsIDSchemaAdvertisesShortID` — new pin in `internal/server/tools_test.go`
- Regenerated `gen/go/`, `gen/ts/`, re-vendored `ui/src/lib/gen/`, rebuilt `internal/webauth/static/`

No new proto message. No new CLI flag. No console rendering (deferred — see 20-CONTEXT.md Deferred Ideas).
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-correctness-polish/20-CONTEXT.md
@.planning/phases/20-correctness-polish/20-RESEARCH.md
@.planning/phases/20-correctness-polish/20-PATTERNS.md
@.planning/phases/20-correctness-polish/20-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Memory proto additively (kind=21, citations=22) and regenerate</name>
  <files>proto/engram/v1/engram.proto, gen/go/, gen/ts/, ui/src/lib/gen/, internal/webauth/static/</files>
  <read_first>
    - proto/engram/v1/engram.proto (Memory message L13-37 ending at `last_accessed_at = 20`; existing `Citation` message L122-128)
    - 20-PATTERNS.md §"proto/engram/v1/engram.proto (Memory message)" — additive-append rule (Phase-15 discipline)
    - 20-RESEARCH.md Pattern 1 + Pitfall 2 (append strictly after field 20, touch no other line)
    - Taskfile.yaml `proto:gen` target (L145) — regenerates gen/go + gen/ts AND re-vendors ui/src/lib/gen; `ui:build` target (L21)
  </read_first>
  <action>
    Append exactly two additive fields to the `Memory` message in `proto/engram/v1/engram.proto`, strictly after `last_accessed_at = 20`, per D-01: `string kind = 21;` and `repeated Citation citations = 22;`. Reuse the existing `Citation` message (engram.proto:122) — do NOT declare a new message and do NOT renumber/retype any existing field (Phase-15 additive discipline). Add a one-line field comment noting these are discovery-only (empty on plain memories). Touch no other line in the message (Pitfall 2 — the git diff on this file must be a ~3-line addition only).
    Then run `task proto:gen` (regenerates gen/go + gen/ts and re-vendors ui/src/lib/gen) followed by `task ui:build` (rebuilds the embedded SPA under internal/webauth/static so the CI ui-drift job stays green — run defensively even though D-02 means no behavioral SPA change). Do NOT hand-edit anything under gen/ or ui/src/lib/gen — those are generated.
  </action>
  <verify>
    <automated>go tool buf lint && go tool buf breaking --against 'https://github.com/seanb4t/engram.git#branch=main' && go tool buf generate && git diff --exit-code -- gen/</automated>
  </verify>
  <acceptance_criteria>
    - `go tool buf lint` passes; `go tool buf breaking --against main` reports no breaking change (additive-only)
    - `go tool buf generate && git diff --exit-code -- gen/` is clean (committed gen tree matches regenerated output)
    - `git diff proto/engram/v1/engram.proto` shows only the two appended field lines (+ their comment), no other edits
    - `rg -n 'kind = 21|citations = 22' proto/engram/v1/engram.proto` matches both new fields
  </acceptance_criteria>
  <done>Memory carries additive fields 21/22 reusing Citation; gen/go, gen/ts, ui/src/lib/gen, and internal/webauth/static are regenerated and drift-clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Populate kind/citations in memoryToProto via new citationsToProto helper</name>
  <files>internal/server/connectapi.go, internal/server/connectapi_test.go</files>
  <read_first>
    - internal/server/connectapi.go (`memoryToProto` L32-52; `Summary:` already mapped at L45 — D-03 confirms no change there; `memoriesToProto` L54)
    - internal/server/connectapi_test.go (existing memoryToProto/memoriesToProto tests — extend, don't replace)
    - internal/store/store.go (`store.Memory.Kind` L130, `.Citations` L131; `store.Citation` struct L184-190: Kind/Ref/Locator/Pin/Excerpt)
    - proto Citation message (engram.proto:122-128: kind=1, ref=2, locator=3, pin=4, excerpt=5 — 1:1 with store.Citation)
    - 20-PATTERNS.md §"internal/server/connectapi.go" — helper lives in connectapi.go (read path), NOT protoconv.go (write path, where citationToArg lives)
  </read_first>
  <behavior>
    - A store.Memory with Kind="map" and two Citations round-trips through memoryToProto: the proto Memory has Kind=="map" and len(Citations)==2 with each field (Kind/Ref/Locator/Pin/Excerpt) mapped 1:1
    - A store.Memory with empty Kind and nil Citations produces a proto Memory with Kind=="" and Citations==nil (no empty slice) — the non-discovery zero-value path
    - Summary continues to map unchanged (D-03) — no assertion regression on the existing summary case
  </behavior>
  <action>
    Add a `citationsToProto(cs []store.Citation) []*engramv1.Citation` helper in `internal/server/connectapi.go` beside `memoryToProto` (naming-symmetric with `memoriesToProto`), returning `nil` for empty input and otherwise mapping each `store.Citation` field to the proto `Citation` (Kind->Kind, Ref->Ref, Locator->Locator, Pin->Pin, Excerpt->Excerpt). Extend `memoryToProto`'s returned struct literal with `Kind: m.Kind` and `Citations: citationsToProto(m.Citations)`. Copy unconditionally — no `if m.Category == "discovery"` guard (Kind/Citations are Go zero-value on every non-discovery write path already). Do NOT touch the existing `Summary:` mapping (D-03 — already correct). Extend connectapi_test.go with the round-trip and zero-value cases from the behavior block.
  </action>
  <verify>
    <automated>go test ./internal/server/... -run 'TestMemoryToProto|TestMemoriesToProto' -v</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/server/... -run 'TestMemoryToProto|TestMemoriesToProto' -v` passes with the new Kind/Citations assertions
    - `rg -n 'func citationsToProto' internal/server/connectapi.go` matches; `rg -n 'func citationsToProto' internal/server/protoconv.go` does NOT match (helper is on the read path)
    - `go build ./...` clean
  </acceptance_criteria>
  <done>SearchDiscoveries responses carry populated kind/citations (summary unchanged); round-trip is asserted in connectapi_test.go.</done>
</task>

<task type="auto">
  <name>Task 3: Pin storeDiscoveryArgs.ID jsonschema short_id advertisement (verification-only, #303)</name>
  <files>internal/server/tools_test.go</files>
  <read_first>
    - internal/server/tools.go (`storeDiscoveryArgs` L543; `.ID` L550 already reads "...supply the full UUID or short_id to replace in place" — DO NOT edit this file)
    - internal/server/tools_test.go (imports "strings" at L17; uses storeDiscoveryArgs — add a "reflect" import if absent)
    - 20-PATTERNS.md §"internal/server/tools_test.go (jsonschema-string assertion)" — reflect-based tag read
    - 20-RESEARCH.md Pitfall 1 — this is verification-only; a diff to tools.go:550 without a preceding failing test is a warning sign
  </read_first>
  <action>
    Add `TestStoreDiscoveryArgsIDSchemaAdvertisesShortID` to `internal/server/tools_test.go`: use `reflect.TypeOf(storeDiscoveryArgs{}).FieldByName("ID")` to read the `jsonschema` struct tag and assert (via `strings.Contains`) that it mentions `short_id`, failing loudly if not. This pins the already-correct wording so a future silent drop (exactly how #303 was originally filed) is caught. Make NO change to `internal/server/tools.go` (Pitfall 1 — the field is already correct as of commit 92a6f610). After the test is green, close GitHub #303 with a comment citing commit 92a6f610 / PR #288 as the original fix and this test as the new guard.
  </action>
  <verify>
    <automated>go test ./internal/server/... -run TestStoreDiscoveryArgsIDSchemaAdvertisesShortID -v</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/server/... -run TestStoreDiscoveryArgsIDSchemaAdvertisesShortID -v` passes
    - `git diff internal/server/tools.go` is empty (no production code change — verification-only)
    - `gh issue view 303` shows the issue closed with a comment citing 92a6f610 and the new test
  </acceptance_criteria>
  <done>The storeDiscoveryArgs.ID short_id advertisement is regression-pinned; #303 is closed without touching production code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Connect client -> SearchDiscoveries RPC | Authenticated caller reads discovery records; authz (per-actor + shared-read rules) is upstream of memoryToProto and unchanged |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-01 | Information Disclosure | new Memory.kind/citations proto fields on SearchDiscoveries | low | accept | Not a new risk — kind/citations ride the same per-record authz-filtered Memory conversion every existing field uses; the SearchDiscoveries authz gate is entirely upstream of memoryToProto and untouched (RESEARCH §Security Domain: "no new read surface, just more fields on an already-authorized response") |
| T-20-SC | Tampering | npm/pip/cargo/go module installs | low | accept | No new package installs this phase — proto:gen/ui:build use already-vendored toolchain (go tool buf, pnpm) and add no dependency; Package Legitimacy Gate is N/A (RESEARCH §Package Legitimacy Audit) |

ASVS L1, block-on-high: no high-severity threat — nothing blocks.
</threat_model>

<verification>
- `go tool buf lint && go tool buf breaking --against main && go tool buf generate && git diff --exit-code -- gen/` clean
- `go test ./internal/server/... -run 'TestMemoryToProto|TestMemoriesToProto|TestStoreDiscoveryArgsIDSchemaAdvertisesShortID' -v` passes
- `task ui:build` produces no uncommitted drift under internal/webauth/static
- `git diff internal/server/tools.go` empty
</verification>

<success_criteria>
- ROADMAP SC1: SearchDiscoveries carries kind + citations (summary already worked) over the Connect wire
- ROADMAP SC5: storeDiscoveryArgs.ID jsonschema advertises short_id, now regression-pinned
- proto/gen/ui drift-clean; #307 and #303 closed
</success_criteria>

<output>
Create `.planning/phases/20-correctness-polish/20-01-SUMMARY.md` when done
</output>
