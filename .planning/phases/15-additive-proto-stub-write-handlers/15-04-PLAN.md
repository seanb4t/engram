---
phase: 15-additive-proto-stub-write-handlers
plan: 04
type: execute
wave: 3
depends_on: ["15-01", "15-03"]
files_modified:
  - internal/server/connectdescriptor_test.go
  - internal/server/connectapi_negative_test.go
autonomous: true
requirements: [REQ-connect-write-rpcs]
must_haves:
  truths:
    - "A descriptor test walks the generated EngramService FileDescriptor and asserts exactly 11 RPCs, and every RPC's IdempotencyLevel is IDEMPOTENCY_UNKNOWN"
    - "SC4 identical-wire-format proof (finding #6): the descriptor test pins a per-field table for every read request/response message (and the Memory + ScopeCount payloads) — field number, name, kind, cardinality, and referenced message type — not merely the message NAMES, so an accidental additive field on a read message fails the test"
    - "The descriptor test also asserts the six write RPCs by name with their exact request/response message types (StoreMemory→StoreMemoryRequest/Response, …, ScheduleMemory→ScheduleMemoryRequest/Response)"
    - "For each of the six write RPCs: an authenticated POST with a valid payload returns exactly CodeUnimplemented"
    - "For each of the six write RPCs: an unauthenticated POST returns exactly CodeUnauthenticated (auth before validate — even with an invalid payload)"
    - "For each of the six write RPCs: a raw HTTP GET against the generated EngramService…Procedure path returns HTTP 405"
    - "For each of the six write RPCs: an authenticated POST with an invalid payload returns exactly CodeInvalidArgument (proves the protovalidate interceptor is wired)"
    - "UpdateMemory mask cells (finding #1/#3): authenticated POST with (a) absent update_mask, (b) non-nil update_mask with empty paths, and (c) update_mask with an unknown path (e.g. 'owner') each return exactly CodeInvalidArgument — proving D-03 end-to-end"
    - "Category-injection cell (finding #2): authenticated StoreMemory (and ScheduleMemory) POST with category='rule' returns exactly CodeInvalidArgument — proving the string.in allowlist"
    - "The five existing read RPCs remain unaffected (existing connectapi/cookie suites stay green; descriptor test pins their field tables)"
  artifacts:
    - internal/server/connectdescriptor_test.go
    - internal/server/connectapi_negative_test.go
  key_links:
    - "The descriptor test references engramv1.File_engram_v1_engram_proto (present in gen/go after Plan 01)"
    - "The negative matrix reuses the httptest.NewServer + real mountConnect chain from connectapi_cookie_test.go; the 405 case uses raw http.Get against the generated engramv1connect.EngramService…Procedure constants (a Connect unary RPC cannot express GET via the typed client)"
    - "The InvalidArgument cells (invalid payload, mask cells, category-injection cell) depend on the protovalidate annotations from Plan 01 + the interceptor wired in Plan 03"
  prohibitions:
    - statement: "No write RPC may report an idempotency level other than IDEMPOTENCY_UNKNOWN"
      status: resolved
      verification: "TestEngramServiceDescriptor asserts opts.GetIdempotencyLevel() == IDEMPOTENCY_UNKNOWN for all 11 methods"
---

<objective>
Add the two regression tests that prove Phase 15's success criteria in Go: (1) a
descriptor-walking test (D-12) pinning the read lane and the no-side-effects invariant
post-codegen, and (2) the full negative-path matrix (D-11) asserting the exact Connect
code for every write RPC across the four request shapes.

Purpose: Turn SC2/SC3/SC4 into automated proof that survives codegen churn and catches a
future regression where the proto ban is bypassed but codegen still emits the enum, an
interceptor is reordered, or a read RPC's shape drifts.
Output: `internal/server/connectdescriptor_test.go` and
`internal/server/connectapi_negative_test.go`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-additive-proto-stub-write-handlers/15-CONTEXT.md
@.planning/phases/15-additive-proto-stub-write-handlers/15-RESEARCH.md
@.planning/phases/15-additive-proto-stub-write-handlers/15-PATTERNS.md
@internal/server/connectapi_cookie_test.go
@internal/server/connectapi.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Descriptor-walking regression test (D-12)</name>
  <files>internal/server/connectdescriptor_test.go</files>
  <read_first>
    - .planning/phases/15-additive-proto-stub-write-handlers/15-RESEARCH.md § "Pattern 5: Descriptor-walking regression test" (verified, ready-to-adapt code; checked against gen/go/engram/v1/engram.pb.go:891 File_engram_v1_engram_proto)
    - .planning/phases/15-additive-proto-stub-write-handlers/15-PATTERNS.md § "internal/server/connectdescriptor_test.go"
    - proto/engram/v1/engram.proto (the final RPC/message names to pin — read the Plan 01 output, not assumptions)
  </read_first>
  <action>
    Create `internal/server/connectdescriptor_test.go` in package `server`. Add
    `TestEngramServiceDescriptor_ReadLaneUnaffectedAndNoSideEffectsRPCs` that reflects over
    `engramv1.File_engram_v1_engram_proto`, gets the first service, and asserts its full name
    is `engram.v1.EngramService`. Assert `svc.Methods().Len() == 11`. Import the generated
    `engramv1` package, `google.golang.org/protobuf/reflect/protoreflect`, and
    `google.golang.org/protobuf/types/descriptorpb`.

    (1) Read-lane req/resp type pins — map the five read RPCs to their exact `{request,
    response}` full message names: ListScopes → {engram.v1.ListScopesRequest,
    engram.v1.ListScopesResponse}, ListMemories → {ListMemoriesRequest, ListMemoriesResponse},
    SearchMemories → {SearchMemoriesRequest, SearchMemoriesResponse}, GetMemory →
    {GetMemoryRequest, GetMemoryResponse}, SearchDiscoveries → {SearchDiscoveriesRequest,
    SearchDiscoveriesResponse}; for each assert `md.Input().FullName()` / `md.Output().FullName()`.

    (2) Write-lane RPC pins (finding #6, codex — prove the six write RPCs explicitly, not just
    via count): map the six write RPCs to their exact `{request, response}` full message names —
    StoreMemory → {StoreMemoryRequest, StoreMemoryResponse}, StoreDiscovery →
    {StoreDiscoveryRequest, StoreDiscoveryResponse}, UpdateMemory → {UpdateMemoryRequest,
    UpdateMemoryResponse}, DeleteMemory → {DeleteMemoryRequest, DeleteMemoryResponse},
    SetVisibility → {SetVisibilityRequest, SetVisibilityResponse}, ScheduleMemory →
    {ScheduleMemoryRequest, ScheduleMemoryResponse} — and assert each method exists with those
    Input/Output full names.

    (3) SC4 identical-wire-format field table (finding #6, codex MEDIUM — pins field
    numbers/kinds/cardinality/type, not just message names, so an accidental additive read field
    fails). Resolve the read messages via `fd.Messages().ByName(...)` and, for each, assert
    `msg.Fields().Len()` equals the expected count AND, per field (looked up by number via
    `Fields().ByNumber(n)`), assert its `Name()`, `Kind()` (protoreflect.Kind), `Cardinality()`
    (Optional/Repeated), and — when `Kind()==protoreflect.MessageKind` — `Message().FullName()`.
    Cover these payload + read request/response messages with the field counts read from
    proto/engram/v1/engram.proto (do NOT invent — read the Plan 01 output):
      - Memory (20 fields; pin at least the message-typed/timestamp fields created_at=14,
        last_accessed_at=20, and the scalars id=1, score=17 float, short_id=18, access_count=19 uint64)
      - ScopeCount (2: scope=1 string, count=2 uint64)
      - ListScopesRequest (0), ListScopesResponse (2: scopes=1 repeated ScopeCount, approximate=2 bool)
      - ListMemoriesRequest (11), ListMemoriesResponse (4: memories=1 repeated Memory, total=2 uint64,
        approximate=3 bool, next_page_token=4 string)
      - SearchMemoriesRequest (7), SearchMemoriesResponse (1: memories=1 repeated Memory)
      - GetMemoryRequest (1: id=1 string), GetMemoryResponse (1: memory=1 Memory)
      - SearchDiscoveriesRequest (3), SearchDiscoveriesResponse (1: discoveries=1 repeated Memory)
    A helper that takes (messageName, expectedCount, map[fieldNumber]{name,kind,cardinality,msgType})
    keeps this compact; this is semantic reflection, not a golden file (no wire snapshot).

    (4) No-side-effects invariant — for EVERY method (all 11) assert
    `md.Options().(*descriptorpb.MethodOptions).GetIdempotencyLevel() == descriptorpb.MethodOptions_IDEMPOTENCY_UNKNOWN`
    (SC2/D-12 guard). Handle a nil `Options()` — a method with no options still reports
    IDEMPOTENCY_UNKNOWN via the getter on a nil `*descriptorpb.MethodOptions`.
  </action>
  <verify>
    <automated>go test ./internal/server/... -run TestEngramServiceDescriptor -v</automated>
  </verify>
  <acceptance_criteria>
    - The test asserts `svc.FullName() == "engram.v1.EngramService"` and `methods.Len() == 11`
    - The five read RPCs' request/response full message names are pinned and unchanged
    - The six write RPCs are asserted by name with their exact request/response message types
    - A per-field table pins number/name/kind/cardinality (and referenced message type for message-kind fields) for the read request/response messages plus Memory and ScopeCount, including each message's field count (finding #6 — catches an accidental additive read field)
    - Every one of the 11 methods asserts `IDEMPOTENCY_UNKNOWN`
    - `go test ./internal/server/... -run TestEngramServiceDescriptor -v` passes
    - `connectdescriptor_test.go` opens with the repo's Apache-2.0 SPDX header (CLAUDE.md MUST; `task license:check` exits 0)
  </acceptance_criteria>
  <done>The descriptor test pins the 11-RPC shape, the read-lane field tables (not just names), the six write RPC types, and the no-side-effects invariant; it is green.</done>
</task>

<task type="auto">
  <name>Task 2: Full negative-path matrix test across all six write RPCs (D-11)</name>
  <files>internal/server/connectapi_negative_test.go</files>
  <read_first>
    - internal/server/connectapi_cookie_test.go (TestConnectCookieLaneIsolation harness lines 44-90: stub resolver keyed off X-Test-Actor, http.NewServeMux + d.mountConnect(mux, resolve) + httptest.NewServer + engramv1connect.NewEngramServiceClient; TestConnectNoCORSHeaders lines 96-122: &deps{} no-Qdrant pattern + raw http request + connect.CodeOf(err) assertion idiom)
    - .planning/phases/15-additive-proto-stub-write-handlers/15-RESEARCH.md § "Phase Requirements → Test Map" (SC3 row) and § D-11
    - .planning/phases/15-additive-proto-stub-write-handlers/15-PATTERNS.md § "internal/server/connectapi_negative_test.go"
    - proto/engram/v1/engram.proto (final write message field requirements, to build valid vs invalid payloads per RPC)
  </read_first>
  <action>
    Create `internal/server/connectapi_negative_test.go` in package `server`, named
    `TestWriteRPCNegativeMatrix`. Use `d := &deps{}` (no Qdrant — none of these cells reach
    store code, since stubs return CodeUnimplemented and the interceptors run first) with a
    stub resolver that returns a valid TokenInfo when the `X-Test-Actor` header is set and
    `connect.NewError(connect.CodeUnauthenticated, ...)` otherwise (mirror
    connectapi_cookie_test.go). Mount the REAL chain via `d.mountConnect(mux, resolve)` and
    serve with `httptest.NewServer`.

    Drive a table over the six write RPCs (StoreMemory, StoreDiscovery, UpdateMemory,
    DeleteMemory, SetVisibility, ScheduleMemory), each entry supplying a valid request and one
    or more invalid requests (violating a buf.validate rule — e.g. empty required
    content/scope/id, zero Visibility, or an empty schedule window). For each RPC assert these
    cells with `connect.CodeOf(err)` equality via the generated `engramv1connect` client:
    - authenticated POST + valid payload → exactly `connect.CodeUnimplemented`
    - unauthenticated POST (no X-Test-Actor header) → exactly `connect.CodeUnauthenticated`
      (send the invalid payload here too, to prove auth precedes validation)
    - authenticated POST + invalid payload → exactly `connect.CodeInvalidArgument`

    Add these targeted InvalidArgument cells proving the Plan 01 CEL/allowlist fixes end-to-end
    (findings #1/#2/#3 — the matrix is where the mask + category gaps are proven closed):
    - UpdateMemory, authenticated, with (a) NO update_mask set (absent), (b) a non-nil
      `&fieldmaskpb.FieldMask{}` with empty `Paths`, and (c)
      `&fieldmaskpb.FieldMask{Paths: []string{"owner"}}` (unknown path) — each → exactly
      `connect.CodeInvalidArgument` (D-03). Import
      `google.golang.org/protobuf/types/known/fieldmaskpb`. Populate id + at least one masked
      field so only the mask rule is under test.
    - StoreMemory (and ScheduleMemory), authenticated, with a well-formed request whose only
      violation is `category: "rule"` → exactly `connect.CodeInvalidArgument` (string.in allowlist).

    For the GET cell, do NOT use the typed client (a Connect unary RPC cannot express GET
    through it): issue a raw `http.Get(srv.URL + engramv1connect.EngramService<Rpc>Procedure)`
    using the GENERATED procedure constants (finding #6, codex LOW — do not hardcode
    `/engram.v1.EngramService/<Rpc>` strings; the constants tie the path to codegen), and
    assert the response status is `http.StatusMethodNotAllowed` (405). Close the response body.
    Keep the six RPCs in a table so adding/removing an RPC is a one-line change; the mask cells
    and category cells attach to their specific RPC entries.
  </action>
  <verify>
    <automated>go test ./internal/server/... -run TestWriteRPCNegativeMatrix -v</automated>
  </verify>
  <acceptance_criteria>
    - The test covers all six write RPCs, each with the core cells (Unimplemented / Unauthenticated / 405 / InvalidArgument)
    - Authenticated valid → `connect.CodeUnimplemented`; unauthenticated (invalid payload) → `connect.CodeUnauthenticated`; authenticated invalid → `connect.CodeInvalidArgument`
    - UpdateMemory has three explicit mask cells — absent mask, empty-paths mask, and unknown-path mask — each asserting `connect.CodeInvalidArgument` (finding #3, proving D-03)
    - StoreMemory and ScheduleMemory each have a `category:"rule"` cell asserting `connect.CodeInvalidArgument` (finding #2)
    - The GET cell uses raw `http.Get` against the generated `engramv1connect.EngramService…Procedure` constants (not hardcoded path strings) and asserts HTTP 405
    - The harness uses `&deps{}` (no Qdrant) and the real `mountConnect` interceptor chain
    - `go test ./internal/server/... -run TestWriteRPCNegativeMatrix -v` passes for every RPC/cell
    - `connectapi_negative_test.go` opens with the repo's Apache-2.0 SPDX header (CLAUDE.md MUST; `task license:check` exits 0)
  </acceptance_criteria>
  <done>The negative matrix proves the exact Connect code for every write RPC across all request shapes, including auth-before-validate ordering, GET-405 via generated procedure constants, and the D-03 mask + category-allowlist rejections.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 04)

New/extended symbols introduced here (excluded from source-drift verification):
- Test: `TestEngramServiceDescriptor_ReadLaneUnaffectedAndNoSideEffectsRPCs` (internal/server/connectdescriptor_test.go)
- Test: `TestWriteRPCNegativeMatrix` (internal/server/connectapi_negative_test.go)
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Connect mux | The negative matrix drives real untrusted request shapes through the full interceptor chain and asserts the exact rejection codes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-01 | Tampering/Spoofing | write RPC GET-reachability | high | mitigate | Descriptor test pins IDEMPOTENCY_UNKNOWN on all 11 RPCs; the negative matrix's raw-GET cell asserts HTTP 405 for every write RPC — proving none is GET-reachable. |
| T-15-02 | Information Disclosure | interceptor ordering | medium | mitigate | The "unauthenticated + invalid payload → CodeUnauthenticated" cell proves auth runs before validation (D-10), so anonymous callers never see field-level contract detail. |
</threat_model>

<review_notes>
## Cross-AI review dispositions (15-REVIEWS.md)

Incorporated in this plan:
- **Finding #3 (AGREED): mask-specific negative cells.** Task 2 now has explicit UpdateMemory
  cells for absent mask, empty-paths mask, and unknown-path mask (each → CodeInvalidArgument),
  plus StoreMemory/ScheduleMemory `category:"rule"` cells — proving the Plan 01 CEL + string.in
  fixes end-to-end.
- **Finding #6 (codex MEDIUM): SC4 "identical wire format" under-proved.** Task 1's descriptor
  test now pins a per-field table (number/name/kind/cardinality/message-type) for every read
  request/response message plus Memory and ScopeCount — not just message names — and asserts the
  six write RPCs by name with their exact req/resp types.
- **Finding #6 (codex LOW): hardcoded HTTP procedure strings.** Task 2's GET cells now use the
  generated `engramv1connect.EngramService…Procedure` constants instead of hand-written paths.

Accepted with rationale (codex MEDIUM — documented limitation, no full fix in scope):
- **Codex MEDIUM: existing read-behavior tests may be skipped without Qdrant.** Acknowledged.
  The SC4 concern this raises about *wire format* is now fully covered by the field-table pins
  above (finding #6), which run with no Qdrant. The remaining *behavioral* read regression proof
  still relies on the existing `connectapi_test.go`/cookie suites (green when Qdrant is available)
  plus `buf breaking --against main` for wire compatibility. A hermetic Qdrant-less authenticated
  read test is deferred: every read handler dereferences the store, so proving read behavior
  without Qdrant would require injecting a fake store boundary — that belongs with Phase 17, where
  the handlers gain real logic and a store seam. This phase's read guarantee is structural
  (field table + buf breaking + gen-drift), which is the correct proof for a stub-only phase.
</review_notes>

<verification>
- `go test ./internal/server/... -run 'TestEngramServiceDescriptor|TestWriteRPCNegativeMatrix' -v` green
- `task test` (full Go + python suite) green
- Existing `TestConnect*` read-lane suite unaffected
</verification>

<success_criteria>
- SC2: descriptor test pins IDEMPOTENCY_UNKNOWN (11 RPCs)
- SC3: six write RPCs → CodeUnimplemented; GET → 405 (via generated procedure constants); unauth → CodeUnauthenticated; invalid → CodeInvalidArgument, including the D-03 mask cells and the category-allowlist cells
- SC4: read lane unaffected — per-field table pins number/name/kind/cardinality/type for every read message (not just names) + existing suite green + buf breaking
</success_criteria>

<output>
Create `.planning/phases/15-additive-proto-stub-write-handlers/15-04-SUMMARY.md` when done
</output>
