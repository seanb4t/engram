<!--
SPDX-License-Identifier: Apache-2.0
Copyright 2026 Sean Brandt
-->

# v0.12.x Phase 1 — Spec-less Probe Coverage Report

> Generated by `/gsd-plan-phase` §7.95 (spec-less probe fallback) on 2026-07-31.
> This phase has no `SPEC.md`, so both `## Edge Coverage` and `## Prohibitions` were absent
> (`EDGE_ABSENT=1`, `PROHIB_ABSENT=1`) and the deterministic edge probe ran over the four phase
> requirement IDs.
>
> **Why this file exists:** the plan-checker flagged that it could not reconcile the probe-edge
> total against `must_haves` because the coverage report lived only in the planner's prompt. This
> is the durable reconciliation artifact. Re-verify against it at execute and verify time.

## Probe result

```json
{"coverage":{"applicable":9,"resolved":0,"unresolved":9,"byVerification":{"explicit":0,"backstop":0}}}
```

9 applicable edges surfaced, 0 auto-resolved by the engine. Resolution below was performed by the
planner per §A `--auto` rules.

## Reconciliation — 9 in, 9 accounted for

| # | Requirement | Category | Disposition | Where authored |
|---|-------------|----------|-------------|----------------|
| 1 | `REQ-connect-bearer-identity` | `unclassified` | **unresolved → flagged assumption** | 01-01 Flagged Assumption #1 |
| 2 | `REQ-connect-token-expiry` | `unclassified` | **unresolved → flagged assumption** | 01-01 Flagged Assumption #2 |
| 3 | `REQ-connect-lane-provenance` | `adjacency` | **covered** | 01-01 `must_haves.truths[1]` — non-Bearer scheme / bare token / empty credential / absent header all bypass the bearer lane (the D-02 boundary) |
| 4 | `REQ-connect-lane-provenance` | `empty` | **covered** | 01-01 `must_haves.truths[3]` — absent/unrecognized `auth.Lane` on a write RPC → `CodePermissionDenied` (paired with `truths[2]`'s zero-`Expiration` reject) |
| 5 | `REQ-connect-lane-provenance` | `ordering` | **covered** | 01-01 `must_haves.truths[5]` — interceptor order unchanged, so the subject interceptor stamps strictly before the CSRF interceptor reads |
| 6 | `REQ-connect-lane-provenance` | `concurrency` | **covered** | 01-01 `must_haves.truths[4]` — provenance carried only on per-request `context.Context`; no shared mutable state |
| 7 | `REQ-connect-headless-mount` | `adjacency` | **covered** | 01-03 `must_haves.truths[2]` — mounted iff UI enabled OR headless set; both/either/neither all specified |
| 8 | `REQ-connect-headless-mount` | `empty` | **covered** | 01-03 `must_haves.truths[1]` — unset or empty `connect.headless` resolves to off |
| 9 | `REQ-connect-headless-mount` | `ordering` | **unresolved → flagged assumption** | 01-03 Flagged Assumption #1 — judged inapplicable with reason (no ordered collection exists; the key is a boolean) |

**Totals:** 6 `covered` + 3 flagged assumptions = **9**. No silent drops. Neither `unclassified`
row was auto-`backstop`ped, per §A.

### Not probe-derived (do not count toward the 9)

Two `backstop` markers exist in the plan set. They are independently-authored phase invariants, not
probe output — counting them against the probe total is the arithmetic error that made the original
planner summary read as 11:

- 01-01 `must_haves.truths[6]` — "The CSRF exemption decision is derived from the stamped lane value
  alone and from no request-supplied content." (`verification: backstop`)
- 01-03 `must_haves.truths[5]` — "`mountConnect`'s capability gate is byte-for-byte unchanged, so
  there is no boolean inside it that an `OR` could loosen." (`verification: backstop`)

At verify time a `backstop` truth the verifier cannot confirm with explicit evidence must abstain to
`human_needed` (reason `insufficient_spec`) — never a silent pass.

## Prohibitions (§B — `PROHIB_ABSENT=1`)

Authored **descriptor-less** (no `check_*` scalar) so each disposes flagged-unverified. Three kept
across the plan set:

| Prohibition | Plan |
|---|---|
| MUST NOT derive the CSRF exemption from any caller-controlled request content — credential-header presence, cookie presence, content type, or HTTP method | 01-01 |
| MUST NOT write a bearer credential, session cookie value, or CSRF token into a log record, error message, or wire response | 01-01 |
| MUST NOT cause any existing deployment to gain a reachable Connect surface on upgrade without the operator explicitly setting `connect.headless` | 01-03 |

Generic OWASP/CSRF canon candidates were breadcrumbed to `/gsd-secure-phase` and dropped rather than
minted, per the ADR-550 D6 canon-referral rule.
