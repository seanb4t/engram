---
phase: 01-shared-auth-chain-connect-bearer-identity
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - internal/config/registry.go
  - internal/config/config.go
  - internal/config/validate.go
  - internal/config/connect_test.go
  - cmd/engram/serve.go
  - cmd/engram/serve_test.go
  - internal/server/connectmount_test.go
autonomous: true
requirements:
  - REQ-connect-headless-mount
  - REQ-connect-bearer-identity

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The verifier chain is constructed exactly once per process, in `buildAuthChain`, and the same value is handed to the MCP wrapper and to the Connect bearer half; `withAuth` accepts a verifier and can no longer build one (D-06)."
    - "`connect.headless` is registered as `ENGRAM_CONNECT_HEADLESS` with the `--connect-headless` flag, no `Legacy:` key, and a `false` default; an unset or empty value resolves to off (D-10)."
    - "Connect is mounted iff the UI is enabled OR `connect.headless` is set; with both set, exactly one composed resolver is built and mounted once; with neither, no composed resolver is built and `mountConnect` registers no handler (D-12)."
    - "A deployment with a configured auth lane but no UI and no headless flag gains no Connect surface on upgrade — the composed resolver is nil, byte-for-byte today's behavior."
    - "`connect.headless` set with zero configured auth lanes fails startup with a config error; `withAuth`'s existing no-lane behavior and the MCP anonymous bucket are unchanged, so no existing deployment can fail to boot because of this phase (D-11)."
    - statement: "`mountConnect`'s capability gate is byte-for-byte unchanged, so there is no boolean inside it that an `OR` could loosen."
      verification: backstop
    - "Mounting and bearer-inclusion are independent decisions: whenever Connect is mounted and an auth chain is configured, that chain is the bearer half — including on a UI-enabled deployment with `connect.headless` unset. A token accepted on MCP is therefore accepted on Connect on every deployment where Connect exists at all (REQ-connect-bearer-identity / SC1; REVIEWS.md HIGH-3)."
  prohibitions:
    - "MUST NOT cause any existing deployment to gain a reachable Connect surface on upgrade without the operator explicitly setting `connect.headless`."
  artifacts:
    - path: "internal/config/registry.go"
      provides: "the connect.headless field registration"
      contains: "ENGRAM_CONNECT_HEADLESS"
    - path: "cmd/engram/serve.go"
      provides: "buildAuthChain, connectHeadlessGuard, connectResolverFor, and the refactored withAuth"
      contains: "func buildAuthChain"
    - path: "internal/server/connectmount_test.go"
      provides: "the byte-for-byte default-off mount assertion"
      contains: "TestMountConnectDefaultOffWithoutUIOrHeadlessFlag"
  key_links:
    - from: "cmd/engram/serve.go"
      to: "internal/server/connectbearer.go"
      via: "connectResolverFor hands the one built chain to NewConnectResolver as the bearer half"
      pattern: "server\\.NewConnectResolver\\("
    - from: "cmd/engram/serve.go"
      to: "internal/config/registry.go"
      via: "runServe reads cfg.Connect.Headless, populated from the connect.headless registry field"
      pattern: "cfg\\.Connect\\.Headless"
---

<!-- planner-discipline-allow: auth.New( -->
<!-- planner-discipline-allow: auth.NewService -->
<!-- planner-discipline-allow: auth.NewStaticTokenVerifier -->
<!-- planner-discipline-allow: headless -->
<!-- planner-discipline-allow: chain == nil -->
<!-- planner-discipline-allow: TestBuildAuthChainWrapsWithExpiry -->

<objective>
Make the shared verifier real at the process level and give an operator an explicit, default-off
switch to mount Connect on a headless deployment.

Three things ship together here because they are one wiring decision: the chain is built **once**
and injected into both mount sites so drift is impossible by construction (D-06); `connect.headless`
becomes a first-class `ENGRAM_` registry key (D-10); and the mount decision is composed from two
independent booleans upstream of `mountConnect`, whose capability gate is never touched (D-12),
with a startup refusal when headless is set and no auth lane exists (D-11).

Purpose: closes RESEARCH.md Pitfall 4 (two independently-constructed chains drift) and Pitfall 5
(a headless mount silently flips a deployment's exposure on upgrade).
Output: the `connect.headless` config key, `buildAuthChain`, `connectHeadlessGuard`,
`connectResolverFor`, and the refactored `withAuth`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-shared-auth-chain-connect-bearer-identity/01-CONTEXT.md
@.planning/phases/01-shared-auth-chain-connect-bearer-identity/01-RESEARCH.md
@.planning/phases/01-shared-auth-chain-connect-bearer-identity/01-PATTERNS.md
@.planning/phases/01-shared-auth-chain-connect-bearer-identity/01-VALIDATION.md
@.planning/phases/01-shared-auth-chain-connect-bearer-identity/01-01-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Created by **this plan** — none of these exist on `main`:

| Symbol | File | Shape |
|---|---|---|
| `config.ConnectConfig` | `internal/config/config.go` | `struct { Headless string \`koanf:"headless"\` }` |
| `Config.Connect` | `internal/config/config.go` | `Connect ConnectConfig \`koanf:"connect"\`` |
| registry field `connect.headless` | `internal/config/registry.go` | `Env: "ENGRAM_CONNECT_HEADLESS"`, `Flag: "connect-headless"`, `Default: "false"`, **no** `Legacy` |
| cobra flag `--connect-headless` | `cmd/engram/serve.go` `init()` | `f.String("connect-headless", config.FlagDefault("connect-headless"), …)` |
| `buildAuthChain` | `cmd/engram/serve.go` | `func(oidc config.OIDCConfig, svcAuth config.ServiceAuthConfig, ownerClaims []string) (mcpauth.TokenVerifier, error)` |
| `composeAuthChain` | `cmd/engram/serve.go` | `func(human, service, static mcpauth.TokenVerifier) mcpauth.TokenVerifier` — pure composition, no config; the sole `auth.EnforceExpiry` site and the unit-testable seam (REVIEWS.md MED-9) |
| `connectHeadlessGuard` | `cmd/engram/serve.go` | `func(headless bool, chain mcpauth.TokenVerifier) error` |
| `connectResolverFor` | `cmd/engram/serve.go` | `func(chain mcpauth.TokenVerifier, headless bool, cookieResolve func(context.Context, connect.AnyRequest) (*mcpauth.TokenInfo, error)) func(context.Context, connect.AnyRequest) (*mcpauth.TokenInfo, auth.Lane, error)` |

**Changed signature:** `withAuth` becomes
`func(handler http.Handler, chain mcpauth.TokenVerifier, resourceMetadataURL string) http.Handler`
— it loses both config parameters and its error return.

**New files:** `internal/config/connect_test.go`, `internal/server/connectmount_test.go`.

**Consumed from Plan 01 (also new this phase, not on `main`):** `auth.Lane`, `auth.LaneBearer`,
`auth.LaneCookie`, `auth.LaneUnknown`, `auth.EnforceExpiry`, `server.NewConnectResolver`, and the
three-return `connectResolver` type.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register `connect.headless` as a first-class ENGRAM_ config key</name>

  <reversibility rating="costly">D-10 publishes an operator-facing config key; renaming `ENGRAM_CONNECT_HEADLESS` or `--connect-headless` after release breaks deployments and Helm values that already set it.</reversibility>

  <read_first>
    - `internal/config/registry.go:13-20` (the `field` struct and what each column means), `:54-62` (the `service_auth.*` block and its "No Legacy value: these are brand-new vars" comment convention), `:63-66` (the `ui.*` Env+Flag block D-10 mirrors), and `:90-115` (how `Flag` feeds `flagToKey` / `FlagDefault`).
    - `internal/config/config.go:23-34` (the `Config` struct and its koanf tags), `:90-100` and `:180-190` (the "values are kept as strings, parsed with `strconv.ParseBool` at point of use" convention), `:162-167` (`UIConfig`, the analog struct).
    - `internal/config/validate.go:190-205` — the existing `strconv.ParseBool` validation for `summarize.on_write` and `usage.signals`; the new key joins this list.
    - `cmd/engram/serve.go:43-63` — the `init()` cobra flag block where the new flag is declared.
    - `internal/config/service_auth_test.go:74` — `TestServiceAuthNoFlag`, the precedent for asserting a field's flag binding (or absence of one).
    - `.planning/phases/01-shared-auth-chain-connect-bearer-identity/01-PATTERNS.md` § `internal/config/registry.go` and § `internal/config/config.go`.
  </read_first>

  <files>internal/config/registry.go, internal/config/config.go, internal/config/validate.go, internal/config/connect_test.go, cmd/engram/serve.go</files>

  <behavior>
    - `TestConnectHeadlessDefault`: loading config with no `ENGRAM_CONNECT_HEADLESS` set yields
      `cfg.Connect.Headless == "false"`, and `strconv.ParseBool` of it yields `false`.
    - `TestConnectHeadlessFromEnv`: `ENGRAM_CONNECT_HEADLESS=true` yields `"true"`.
    - `TestConnectHeadlessHasNoLegacyVar`: the registry entry for `connect.headless` has an empty
      `Legacy` field, so no `MEM_*` name can satisfy it and the retired-var guard stays intact.
    - `TestConnectHeadlessHasFlagBinding`: `config.FlagDefault("connect-headless")` returns
      `"false"` and the flag name maps to the `connect.headless` key.
    - `TestConnectHeadlessRejectsNonBoolean`: a config whose `Connect.Headless` is `"yes"` fails
      validation with an error naming the key.
  </behavior>

  <action>
Add one registry entry to `internal/config/registry.go`, placed immediately after the `ui.*` block:
`{Key: "connect.headless", Env: "ENGRAM_CONNECT_HEADLESS", Flag: "connect-headless", Default: "false"}`.
Deliberately **omit** the `Legacy:` field — this key is brand new, and retired `MEM_*` vars are a
fatal startup guard (DEC-jgq / DEC-irq), so inventing a legacy name would create a var that must
never be set. Precede it with a short comment in the style of the `service_auth.*` block explaining
that the key names the *mode* (headless operation) rather than the surface, that it defaults off
independently of every `ui.*` and `service_auth.*` key, and that it carries no legacy name.

Add `type ConnectConfig struct { Headless string \`koanf:"headless"\` }` to
`internal/config/config.go`, mirroring `UIConfig`'s shape, and add `Connect ConnectConfig \`koanf:"connect"\``
to the `Config` struct beside `UI UIConfig`. Keep it a `string`, not a `bool` — this file's stated
convention is that values stay strings and are parsed with `strconv.ParseBool` at point of use, the
same treatment `summarize.on_write` and `usage.signals` already get.

Extend the validation function in `internal/config/validate.go` that already `strconv.ParseBool`-checks
`c.Summarize.OnWrite` and `c.Usage.Signals` with the identical check for `c.Connect.Headless`,
returning an error that names the key so a typo fails at startup rather than silently reading as
off.

Declare the cobra flag in `cmd/engram/serve.go`'s `init()` block, alongside the existing `ui-enabled`
declaration: `f.String("connect-headless", config.FlagDefault("connect-headless"), …)` with a help
string stating that it mounts the ConnectRPC lane on a headless deployment, defaults off, and
requires at least one configured auth lane. Use `f.String`, not `f.Bool`, to match every other flag
in this block and the string-valued registry.

Create `internal/config/connect_test.go` with the five cases above, following the existing
`internal/config` test conventions for env manipulation and loader invocation.

Run `task license:add` for the new file, then `task fmt`.
  </action>

  <verify>
    <automated>go test ./internal/config/... -run 'TestConnectHeadless' -v 2>&1 | grep -E '^(=== RUN|--- PASS|--- FAIL|ok|FAIL)'</automated>
    <automated>go test ./internal/config/... 2>&1 | tail -10</automated>
    <automated>go run ./cmd/engram serve --help 2>&1 | grep -- '--connect-headless'</automated>
  </verify>

  <acceptance_criteria>
    - All five named tests print a matched `=== RUN` / `--- PASS` pair. A `[no tests to run]` line
      with no `=== RUN` fails this criterion.
    - `grep -n 'connect.headless' internal/config/registry.go` returns exactly one line, and that
      line contains `ENGRAM_CONNECT_HEADLESS`, `connect-headless`, and `"false"`.
    - That same line contains no `Legacy:` field:
      `grep 'connect.headless' internal/config/registry.go | grep -c 'Legacy'` prints `0`.
    - `go run ./cmd/engram serve --help` lists `--connect-headless` with a default of `false`.
    - `grep -c 'Connect.Headless' internal/config/validate.go` is at least `1` — the boolean is
      validated at load, not only at point of use.
    - `git diff --exit-code go.mod go.sum` is clean.
    - `task lint` and `task license:check` are clean.
  </acceptance_criteria>

  <done>
`ENGRAM_CONNECT_HEADLESS` and `--connect-headless` resolve through the single `ENGRAM_` registry to
`cfg.Connect.Headless`, default to `"false"` when unset, carry no legacy name, and fail startup on a
non-boolean value.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the verifier chain once and inject it into both mount sites</name>

  <reversibility rating="costly">D-06 makes `withAuth`'s signature the injection seam; reverting means re-inlining chain construction at two call sites and reintroducing the drift surface Pitfall 4 describes. D-05's reject-zero-`Expiration` rule ships with it and changes acceptance behavior for any token lacking an expiration in every deployment at once.</reversibility>

  <read_first>
    - `cmd/engram/serve.go:286-344` — the whole current `withAuth`, including the three per-lane construction blocks, the "no lane configured" warning at `:335-338`, and the `mcpauth.RequireBearerToken` wrap at `:341`. This is the extraction source; the per-lane logic moves verbatim.
    - `cmd/engram/serve.go:66-138` — `runServe` up to the `ownerClaimGuard` call, so the new `buildAuthChain` call lands in the right place.
    - `cmd/engram/serve.go:199-205` — the current `withAuth` call site and its error handling.
    - `cmd/engram/serve_test.go:106-230` — the three existing `TestWithAuth_*` tests that call the old four-argument signature. They must be re-pointed, not deleted.
    - `internal/auth/chain.go` — `ChainVerifier`'s composition and its nil-lane behavior.
    - `internal/auth/static_token.go:22,:79-80` — the 100-year sentinel `Expiration` that keeps the static-token lane passing `EnforceExpiry`.
    - `internal/auth/bearer.go` — `EnforceExpiry`, created in Plan 01.
    - `.planning/phases/01-shared-auth-chain-connect-bearer-identity/01-PATTERNS.md` § `cmd/engram/serve.go (MODIFIED)` — the verbatim extraction source and the D-06 target split.
    - `.planning/phases/01-shared-auth-chain-connect-bearer-identity/01-RESEARCH.md` § Architecture Pattern 1 and § Pitfall 4.
  </read_first>

  <files>cmd/engram/serve.go, cmd/engram/serve_test.go</files>

  <behavior>
    - `TestBuildAuthChainNoLaneConfiguredReturnsNil`: empty OIDC and service-auth config →
      `(nil, nil)`, no error.
    - `TestBuildAuthChainStaticLaneAcceptsConfiguredToken`: a static-token map only → a non-nil
      verifier that accepts the configured token and resolves its owner.
    - `TestComposeAuthChainWrapsWithExpiry` (REVIEWS.md MED-9): `composeAuthChain(stub, nil, nil)`
      where `stub` returns a `TokenInfo` with a past `Expiration` and a nil error → the composed
      verifier rejects with an error satisfying `errors.Is(err, auth.ErrTokenExpired)`. This
      replaces the originally-planned `TestBuildAuthChainWrapsWithExpiry`, which was **not
      feasible**: `buildAuthChain` accepts only configuration and constructs real OIDC/static
      verifiers, so a test cannot make it return a past-expiration `TokenInfo` without an
      injection seam nobody planned. Extracting the pure composition step gives the same
      guarantee with a real behavioral assertion instead of an unwritable one.
    - `TestComposeAuthChainAllNilReturnsNil`: `composeAuthChain(nil, nil, nil)` returns nil, so
      "no lane configured" still propagates as a nil chain.
    - `TestBuildAuthChainDelegatesComposition`: a structural assertion that `buildAuthChain` is
      the only caller of `composeAuthChain` and applies no expiry wrapping of its own — see the
      source gates in the acceptance criteria. Recorded as a named test so the obligation is
      visible in the suite even though its evidence is a source check.
    - `TestAuthChainSharedBetweenLanes`: one `buildAuthChain` call produces one verifier; the same
      variable is handed to `withAuth` and to `server.NewConnectResolver(chain, nil)`, and the same
      static token is accepted through both — the MCP handler returns non-401 and the composed
      resolver returns a `TokenInfo` with `auth.LaneBearer`.
    - The three existing `TestWithAuth_*` tests keep asserting the same observable outcomes through
      the new two-step call (`buildAuthChain` then `withAuth`): no lane configured returns the
      handler unchanged, the static-token lane authenticates its configured token, and a
      human-only config rejects an unauthenticated request.
  </behavior>

  <action>
Split `withAuth` in `cmd/engram/serve.go` into a pure chain builder and a thin MCP wrapper.

Move everything from the current `withAuth`'s first `var humanVerifier, serviceVerifier, staticVerifier`
declaration through `chain := auth.ChainVerifier(...)` into a new
`buildAuthChain(oidc config.OIDCConfig, svcAuth config.ServiceAuthConfig, ownerClaims []string) (mcpauth.TokenVerifier, error)`.
Keep the three per-lane construction blocks byte-for-byte, including their `slog.Info` lines and
their error wrapping — the D-03 rule that each lane is built only when its own config is present is
preserved, not rewritten.

Split the final composition step out into
`composeAuthChain(human, service, static mcpauth.TokenVerifier) mcpauth.TokenVerifier`, a pure
function taking three verifiers and no config: it returns nil when all three are nil (the caller
decides what "no auth lane" means for its transport), and otherwise
`auth.EnforceExpiry(auth.ChainVerifier(human, service, static))`, so expiry is a property of the
verifier that every present and future lane inherits (D-04). `buildAuthChain` ends with
`return composeAuthChain(humanVerifier, serviceVerifier, staticVerifier), nil`.

This split exists for a testability reason worth writing down (REVIEWS.md MED-9): `buildAuthChain`
takes configuration and builds real OIDC and static verifiers from it, so no test can make it
return a past-expiration `TokenInfo` and therefore no test can prove the expiry wrapping is
present. `composeAuthChain` takes verifiers, so a stub returning a past `Expiration` proves it in
one line. D-06 is unaffected — `buildAuthChain` remains the sole site where a lane verifier is
*constructed*, and `composeAuthChain` constructs nothing. Say this in `composeAuthChain`'s doc
comment.

Reduce `withAuth` to
`func withAuth(handler http.Handler, chain mcpauth.TokenVerifier, resourceMetadataURL string) http.Handler`:
a nil `chain` returns the handler unchanged after emitting the existing
"bearer-token validation DISABLED" `slog.Warn` verbatim; otherwise it wraps with
`mcpauth.RequireBearerToken(chain, &mcpauth.RequireBearerTokenOptions{ResourceMetadataURL: resourceMetadataURL})`.
It no longer returns an error, because it no longer does anything that can fail. Give it a doc
comment recording that **the compiler is the D-06 assertion**: because `withAuth` receives a
verifier and takes no config, there is no code path by which the MCP lane can construct a second
chain, so the two mount sites cannot drift.

In `runServe`, call `buildAuthChain` exactly once, immediately after the `ownerClaimGuard` call and
before the `connectResolve` declaration, into a single local variable. Store its error and return it
with the existing `slog.Error("oidc verifier init failed", …)` message. Update the call at `:201` to
`handler = withAuth(handler, chain, cfg.OIDC.ResourceMetadata)` and drop the now-absent error branch.
Note in the SUMMARY that OIDC discovery now happens slightly earlier in startup than before; the
failure mode is unchanged, only its position in the boot sequence.

Re-point the three existing `TestWithAuth_*` tests in `cmd/engram/serve_test.go` to the two-step
call. They are the regression guard that this refactor is behavior-preserving, so keep every
assertion they already make and change only how the verifier reaches `withAuth`. If any existing
test now fails because its stub verifier returns a zero `Expiration`, fix the **fixture** to carry a
real future expiration — that failure is D-05 working as designed and is not a reason to weaken
`EnforceExpiry`.

Add the new tests above. `TestAuthChainSharedBetweenLanes` must call `buildAuthChain` exactly
once and pass the resulting variable to both consumers; a version that builds two verifiers cannot
detect the drift class it exists to detect.

Run `task fmt`.
  </action>

  <verify>
    <automated>go test ./cmd/engram/... -run 'TestBuildAuthChain|TestComposeAuthChain|TestAuthChainSharedBetweenLanes|TestWithAuth_' -v 2>&1 | grep -E '^(=== RUN|--- PASS|--- FAIL|ok|FAIL)'</automated>
    <automated>go build ./... && go vet ./...</automated>
    <automated>go test ./cmd/engram/... ./internal/auth/... ./internal/server/... 2>&1 | tail -12</automated>
  </verify>

  <acceptance_criteria>
    - `TestBuildAuthChainNoLaneConfiguredReturnsNil`, `TestBuildAuthChainStaticLaneAcceptsConfiguredToken`,
      `TestComposeAuthChainWrapsWithExpiry`, `TestComposeAuthChainAllNilReturnsNil`,
      `TestBuildAuthChainDelegatesComposition`, `TestAuthChainSharedBetweenLanes`, and all three
      pre-existing `TestWithAuth_*` tests print a matched `=== RUN` / `--- PASS` pair. A
      `[no tests to run]` line with no `=== RUN` fails this criterion.
    - No infeasible test survives: `grep -c 'TestBuildAuthChainWrapsWithExpiry' cmd/engram/serve_test.go`
      prints `0`. That test could not be written against a config-only builder (REVIEWS.md MED-9);
      `TestComposeAuthChainWrapsWithExpiry` carries the obligation instead.
    - The three verifier constructors live in exactly one function.
      `grep -c 'auth\.New(\|auth\.NewService(\|auth\.NewStaticTokenVerifier(' internal/server/*.go`
      prints `0` for every file, and in `cmd/engram/serve.go` every occurrence returned by
      `grep -n 'auth\.New(\|auth\.NewService(\|auth\.NewStaticTokenVerifier(' cmd/engram/serve.go`
      falls between the line numbers printed by
      `grep -n 'func buildAuthChain' cmd/engram/serve.go` and the closing `}` of that function.
    - `grep -n 'func withAuth' cmd/engram/serve.go` prints exactly:
      `func withAuth(handler http.Handler, chain mcpauth.TokenVerifier, resourceMetadataURL string) http.Handler {`
      — proving by signature that `withAuth` cannot build a chain.
    - `grep -c ':= buildAuthChain(\|= buildAuthChain(' cmd/engram/serve.go` prints `1` — one
      construction site.
    - `grep -v '^\s*//' cmd/engram/serve.go | grep -c 'auth.EnforceExpiry('` prints `1`, and the
      line number returned by `grep -n 'auth.EnforceExpiry(' cmd/engram/serve.go` falls between
      `grep -n 'func composeAuthChain' cmd/engram/serve.go` and the closing `}` of that function —
      expiry wraps the chain once, in the pure composition step, not per lane and not at a call
      site.
    - `composeAuthChain` has exactly one caller:
      `grep -v '^\s*//' cmd/engram/serve.go | grep -c 'composeAuthChain('` prints `2` (the
      declaration plus the single call inside `buildAuthChain`), and that call's line number falls
      inside `buildAuthChain`'s body.
    - `withAuth`'s doc comment contains the words "compiler" and "drift", recording that the
      compiler is the D-06 assertion (permitted by CONTEXT.md's test-first obligations).
    - The "bearer-token validation DISABLED" warning text is unchanged:
      `grep -c 'bearer-token validation DISABLED' cmd/engram/serve.go` prints `1`.
    - `git diff --exit-code go.mod go.sum` is clean; `go build ./...` and `go vet ./...` exit 0.
  </acceptance_criteria>

  <done>
One `buildAuthChain` call per process produces one expiry-wrapped verifier, and the same value
reaches the MCP wrapper and the Connect bearer half. `withAuth` cannot construct a chain because its
signature does not admit the config to build one. The three pre-existing `withAuth` behaviors are
unchanged.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Separate mounting from bearer-inclusion, and refuse to start headless without an auth lane</name>

  <reversibility rating="reversible">D-11's startup guard ships tight from the start; relaxing a refusal later is safe, and D-12 leaves `mountConnect` untouched so the mount decision lives entirely in one `serve.go` function.</reversibility>

  <read_first>
    - `cmd/engram/serve.go:139-178` — the `connectResolve` / `connectCSRFVerify` / `connectReseal` declarations, the `uiCfg.Enabled` block, the `server.NewConnectResolver(nil, …)` call Plan 01 left there, and the `server.Register` call.
    - `cmd/engram/serve.go:262-284` — `ownerClaimGuard`, the fail-closed-at-boot precedent this task's guard mirrors in shape and call position.
    - `cmd/engram/uiconfig.go` — `resolveUIConfig` and the `UIConfig.Enabled bool` it produces.
    - `internal/server/connectapi.go:356-398` — `mountConnect` and the `resolve == nil` guard that must stay byte-for-byte.
    - `internal/server/connectapi_test.go` — the `deps` construction and mux-mount helpers the new mount test reuses.
    - `internal/server/connectbearer.go` — `NewConnectResolver`'s four-arm composition, created in Plan 01.
    - `.planning/phases/01-shared-auth-chain-connect-bearer-identity/01-RESEARCH.md` § Architecture Pattern 3 and § Pitfall 5.
    - `.planning/REQUIREMENTS.md:197` — the exact false-pass this requirement must avoid.
  </read_first>

  <files>cmd/engram/serve.go, cmd/engram/serve_test.go, internal/server/connectmount_test.go</files>

  <behavior>
    - `TestHeadlessRefusesStartWithoutAuthLane` (D-11): `connectHeadlessGuard(true, nil)` returns a
      non-nil error whose message names `ENGRAM_CONNECT_HEADLESS`; `connectHeadlessGuard(true, chain)`
      returns nil; `connectHeadlessGuard(false, nil)` returns nil, so no existing deployment can
      fail to boot because of this phase.
    - `TestConnectResolverForDefaultOff` (D-12 / SC5, the Pitfall-5 regression guard):
      `connectResolverFor(nil, false, nil)` is nil; **`connectResolverFor(chain, false, nil)` is
      also nil** — a configured auth lane alone never mounts Connect.
    - `TestConnectResolverForHeadlessOnly`: `connectResolverFor(chain, true, nil)` is non-nil and
      resolves a well-formed bearer credential to `auth.LaneBearer`.
    - `TestConnectResolverForUIOnlyNoChain`: `connectResolverFor(nil, false, cookieResolve)` is
      non-nil and stamps `auth.LaneCookie` for every request, byte-for-byte today's UI-only
      behavior on a deployment with no auth lane configured at all.
    - `TestConnectResolverForUIEnabledIncludesBearerHalf` (**REVIEWS.md HIGH-3 / SC1**):
      `connectResolverFor(chain, false, cookieResolve)` — UI on, headless *unset*, chain
      configured — is non-nil **and** routes a well-formed bearer credential to `auth.LaneBearer`
      through `chain`, while everything else still stamps `auth.LaneCookie`. This is the case the
      previous plan got wrong: it would have left such a deployment cookie-only, so a token
      accepted on MCP would be rejected on Connect.
    - `TestConnectResolverForBothLanes`: `connectResolverFor(chain, true, cookieResolve)` is non-nil,
      routes a well-formed bearer credential to `auth.LaneBearer` and everything else to
      `auth.LaneCookie`.
    - `TestConnectResolverForHeadlessWithoutChainIsNil`: `connectResolverFor(nil, true, nil)` is
      nil. Unreachable in `runServe` because `connectHeadlessGuard` refuses startup first; kept as
      the defense-in-depth assertion that the composition itself never mounts an
      unauthenticatable surface.
    - `TestMountConnectDefaultOffWithoutUIOrHeadlessFlag` (in `internal/server`, reshaped per
      REVIEWS.md LOW-11): the composition-level assertion, not another direct nil-resolver test —
      `server.NewConnectResolver(nil, nil)` returns nil, and passing **that** result to
      `d.mountConnect` registers no handler, so a POST to the EngramService procedure path gets a
      404. `TestMountConnectSkipsWhenResolverNil` (`connectapi_test.go:776-789`) already covers a
      hand-passed literal nil; this test's value is the composition→mount link, so it must not
      restate that one.
  </behavior>

  <action>
Add `connectHeadlessGuard(headless bool, chain mcpauth.TokenVerifier) error` to `cmd/engram/serve.go`,
shaped like `ownerClaimGuard`: it returns an error only when `headless` is true and `chain` is nil,
with a message stating that `ENGRAM_CONNECT_HEADLESS` (or `--connect-headless`) is set while no auth
lane is configured, that mounting would expose every write RPC unauthenticated into the anonymous
empty-owner bucket, and naming `ENGRAM_OIDC_ISSUER` and the `ENGRAM_SERVICE_AUTH_*` vars as the ways
to configure one. Every other combination returns nil. This constrains only the new flag —
`withAuth`'s existing no-lane behavior and the MCP lane's anonymous bucket are deliberately
untouched.

Add `connectResolverFor(chain mcpauth.TokenVerifier, headless bool, cookieResolve func(context.Context, connect.AnyRequest) (*mcpauth.TokenInfo, error)) func(context.Context, connect.AnyRequest) (*mcpauth.TokenInfo, auth.Lane, error)`.

**Mounting and bearer-inclusion are two separate decisions. Do not let one flag answer both
(REVIEWS.md HIGH-3).** The function is exactly:

  - **Mount decision** — return nil when `cookieResolve == nil && !headless`. Those are the two
    independent activation booleans: a non-nil `cookieResolve` means the UI is on, `headless`
    means the operator asked for the lane explicitly. A configured `chain` on its own is *not* an
    activation signal and must never appear in this condition; that is what keeps
    `REQUIREMENTS.md:197`'s false-pass ("loosening the existing mount guard with an OR keeps every
    'UI enabled → mounted' test green even if the new condition is wrong") impossible, and it is
    what `TestConnectResolverForDefaultOff` pins.
  - **Bearer decision** — whenever the lane *is* mounted, pass `chain` through as the bearer half:
    `return server.NewConnectResolver(chain, cookieResolve)`. Unconditionally. Not
    `if headless { chain } else { nil }`.

The rule the previous draft got wrong: gating the bearer half on `headless` leaves an existing
deployment with the UI enabled, a configured MCP verifier, and `connect.headless` unset serving
Connect cookie-only — so a token the MCP lane accepts is rejected on Connect, directly
contradicting success criterion 1 and `REQ-connect-bearer-identity`. Enabling bearer there exposes
no new route: `serve.go:143-178` already mounts the Connect surface whenever the UI is on, so the
only change is which credential families that already-reachable surface accepts, for principals
who already hold equivalent access through MCP.

Check this against the locked decisions before implementing, and note in the doc comment that it
was checked: **D-10 and D-12 govern mounting only.** D-10 names the key for the *mode* and says it
defaults off "independently of every `ui.*` and `service_auth.*` flag" — a statement about when
the lane is mounted. D-12 says `serve.go` decides "whether to build a composed resolver at all —
UI enabled OR `connect.headless` set", which this preserves exactly. Neither decision says the
bearer half is conditional on `headless`; D-06 says the opposite, that one chain value is handed
to both mount sites. There is no conflict to escalate.

When `headless` is true, `chain` must be non-nil — `connectHeadlessGuard` guarantees it upstream.
`NewConnectResolver(nil, nil)` returning nil is the defense-in-depth backstop if that guarantee is
ever broken, and `TestConnectResolverForHeadlessWithoutChainIsNil` pins it.

Wire it in `runServe`: parse `cfg.Connect.Headless` with `strconv.ParseBool` right after the
`buildAuthChain` call from Task 2, returning a startup error on a parse failure rather than
defaulting to off. Call `connectHeadlessGuard(headless, chain)` immediately after that parse and
before anything mounts, returning its error with a `slog.Error`. Keep the `uiCfg.Enabled` block
exactly as it is for the cookie half — it still assigns `webauth.NewResolver(codec).Resolve`,
`csrfSigner.Verify`, and `webHandler.Reseal` only when the UI is on — but replace Plan 01's
`server.NewConnectResolver(nil, …)` call with a single `connectResolverFor(chain, headless, cookieResolve)`
call placed **after** the `uiCfg.Enabled` block, so both halves are known before composition.
Extend the existing "web UI disabled (headless); Connect API not mounted" log line so it reports the
actual outcome for both booleans rather than asserting the UI-only conclusion.

Do not modify `internal/server/connectapi.go` in this task. `mountConnect`'s `resolve == nil` guard
and its interceptor list stay byte-for-byte as Plan 01 left them (D-12): whether Connect is mounted
remains a pure capability question, decided upstream by whether `connectResolverFor` returned a
non-nil resolver.

Create `internal/server/connectmount_test.go` with `TestMountConnectDefaultOffWithoutUIOrHeadlessFlag`,
reusing `connectapi_test.go`'s `deps` construction. **Assert the composition→mount link, not the
nil-resolver behavior (REVIEWS.md LOW-11):** build the resolver with
`server.NewConnectResolver(nil, nil)` — the shape `connectResolverFor` produces when neither
activation boolean is set — pass that value to `d.mountConnect`, and assert the 404.
`TestMountConnectSkipsWhenResolverNil` at `connectapi_test.go:776-789` already asserts the
hand-passed-literal-nil case; restating it here would add a duplicate rather than coverage. Say so
in the test's doc comment so a later reader does not "simplify" the two back together.

Add the guard and composition tests to `cmd/engram/serve_test.go`.

Run `task license:add` for the new file, then `task fmt`.
  </action>

  <verify>
    <automated>go test ./cmd/engram/... -run 'TestHeadlessRefusesStartWithoutAuthLane|TestConnectResolverFor' -v 2>&1 | grep -E '^(=== RUN|--- PASS|--- FAIL|ok|FAIL)'</automated>
    <automated>go test ./internal/server/... -run TestMountConnectDefaultOffWithoutUIOrHeadlessFlag -v 2>&1 | grep -E '^(=== RUN|--- PASS|--- FAIL|ok|FAIL)'</automated>
    <automated>task</automated>
  </verify>

  <acceptance_criteria>
    - `TestHeadlessRefusesStartWithoutAuthLane`, `TestConnectResolverForDefaultOff`,
      `TestConnectResolverForHeadlessOnly`, `TestConnectResolverForUIOnlyNoChain`,
      `TestConnectResolverForUIEnabledIncludesBearerHalf`, `TestConnectResolverForBothLanes`,
      `TestConnectResolverForHeadlessWithoutChainIsNil`, and
      `TestMountConnectDefaultOffWithoutUIOrHeadlessFlag`
      each print a matched `=== RUN` / `--- PASS` pair. A `[no tests to run]` line with no `=== RUN`
      fails this criterion.
    - `TestConnectResolverForDefaultOff` asserts `connectResolverFor(chain, false, nil)` is nil with
      a **non-nil** chain — confirmed by reading the test body. A version that only tests the
      all-nil case does not close `REQUIREMENTS.md:197`'s false-pass.
    - **Bearer-inclusion is not gated on `headless` (HIGH-3).**
      `TestConnectResolverForUIEnabledIncludesBearerHalf` passes `headless=false` with a **non-nil**
      chain and a non-nil `cookieResolve`, and asserts a well-formed bearer credential resolves to
      `auth.LaneBearer` — confirmed by reading the test body. Independently, the source contains no
      conditional bearer half:
      `grep -v '^\s*//' cmd/engram/serve.go | grep -cE 'headless *\? *chain|if +headless *\{[^}]*chain'`
      prints `0`, and the single `server.NewConnectResolver(` call in the file passes `chain`
      directly as its first argument —
      `grep -v '^\s*//' cmd/engram/serve.go | grep -c 'server.NewConnectResolver(chain,'` prints `1`.
    - The mount condition names only the two activation booleans:
      `sed -n '/func connectResolverFor/,/^}$/p' cmd/engram/serve.go | grep -c 'chain == nil'`
      prints `0` — a configured chain is never an activation signal.
    - `mountConnect` is byte-for-byte unchanged by this task:
      `git diff --exit-code internal/server/connectapi.go` is clean relative to the Plan 01 commit,
      and
      `sed -n '/func (d \*deps) mountConnect/,/^}$/p' internal/server/connectapi.go | grep -c 'if resolve == nil'`
      still prints `1`.
    - The two activation booleans are never combined into one:
      `grep -cE 'uiCfg\.Enabled *(\|\||&&) *headless|headless *(\|\||&&) *uiCfg\.Enabled' cmd/engram/serve.go`
      prints `0`.
    - `grep -c 'connectHeadlessGuard(' cmd/engram/serve.go` is at least `2` (declaration plus one
      call site), and `grep -n 'connectHeadlessGuard(\|mountConnect\|server.Register' cmd/engram/serve.go`
      shows the guard's call-site line number strictly lower than the `server.Register` line number.
    - `git diff --exit-code internal/webauth/` and `git diff --exit-code go.mod go.sum` are clean.
    - `task` (lint + test) exits 0.
  </acceptance_criteria>

  <done>
An operator can set `ENGRAM_CONNECT_HEADLESS=true` to mount Connect with the UI off; setting it
without any configured auth lane refuses startup with a message naming the fix. With the UI disabled
and the flag unset, no composed resolver is built and no Connect handler is registered — including
on a deployment that already has service-auth configured. And wherever Connect *is* mounted, a
configured verifier chain is its bearer half — including a UI-enabled deployment that never sets
the headless flag — so a token accepted on MCP is accepted on Connect everywhere the lane exists.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator config → network surface | `ENGRAM_CONNECT_HEADLESS` decides whether six write RPCs become reachable on a deployment that had no Connect surface before. |
| process startup → request serving | Once mounted, the surface is live; a misconfiguration is only correctable by a restart, so it must be caught at boot. |
| chain construction → both transports | One `mcpauth.TokenVerifier` value crosses from `cmd/engram` into both `mcpauth.RequireBearerToken` and the Connect composed resolver. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Elevation of Privilege | `connectHeadlessGuard` | high | mitigate | Headless with zero configured auth lanes refuses to start, mirroring the `ownerClaimGuard` fail-closed-at-boot precedent. `TestHeadlessRefusesStartWithoutAuthLane` covers all three combinations, including the two that must NOT refuse. |
| T-03-02 | Information Disclosure | `connectResolverFor` mount decision | high | mitigate | The mount condition names only the two activation booleans (`cookieResolve == nil && !headless`) and never the chain, and `connectResolverFor(chain, false, nil)` is asserted nil — a deployment with service-auth configured but no UI and no flag gains nothing on upgrade. `mountConnect`'s guard is gated byte-for-byte, so there is no boolean there to loosen. |
| T-03-07 | Elevation of Privilege | bearer half on an already-mounted UI deployment | medium | accept | Passing the chain as the bearer half whenever Connect is mounted widens the credential families an *already-reachable* surface accepts (REVIEWS.md HIGH-3). No new route is exposed — `serve.go:143-178` mounts Connect on UI-enabled deployments today — and the principals it newly admits already hold equivalent access via MCP with the same verifier, the same `SubjectFromTokenInfo` derivation, and the same `internal/store` owner isolation. Accepted as the intended meaning of `REQ-connect-bearer-identity`; the alternative (gating on `headless`) is the defect this row replaces. |
| T-03-08 | Elevation of Privilege | CSRF exemption reach on UI-enabled deployments | medium | mitigate | Because the bearer half is now live on UI-enabled deployments, the D-08 CSRF exemption is reachable there too. It is still keyed solely on a lane stamped after successful bearer *verification* — a browser cannot self-stamp it, and a malformed credential falls to `auth.LaneCookie` (D-02) — so the exemption remains unavailable to any cross-site caller. Gated by Plan 01 Task 2's end-to-end self-declaration and failed-bearer negatives, which run against the real composed resolver. |
| T-03-03 | Tampering | drifted verifier chains | high | mitigate | `buildAuthChain` is the sole construction site; `withAuth`'s signature admits no config, so the MCP lane cannot build a second chain. Gated by a source check that the three constructors occur only inside `buildAuthChain` and nowhere in `internal/server`. |
| T-03-04 | Spoofing | non-boolean `ENGRAM_CONNECT_HEADLESS` | medium | mitigate | Validated with `strconv.ParseBool` at config load and again at point of use in `runServe`, which returns a startup error rather than defaulting to off, so a typo cannot silently read as either value. |
| T-03-05 | Information Disclosure | startup log lines | low | mitigate | The mount-decision log reports the resolved outcome of both booleans; no credential, cookie key, or static token value is logged (prohibition P2 from Plan 01). |
| T-03-06 | Denial of Service | earlier OIDC discovery in boot | low | accept | `buildAuthChain` moves ahead of the UI block, so a slow issuer delays boot marginally earlier than today. The 15-second timeout and the failure mode are unchanged; only the position in the boot sequence moves. |
| T-03-SC | Tampering | Go module supply chain | high | mitigate | Zero packages installed. Enforced by the `git diff --exit-code go.mod go.sum` acceptance criterion on all three tasks. |
</threat_model>

## Flagged Assumptions

1. **Probe edge `REQ-connect-headless-mount` / `ordering` — unresolved, judged inapplicable.** The
   probe asks "when elements compare equal, is output order specified and stable?" A single boolean
   mount decision produces no multi-element output whose tie-break order could vary. The one
   ordering-shaped question that *is* real for this requirement — what happens when `ui.enabled` and
   `connect.headless` are both set — is the `adjacency` probe, resolved above as the
   `TestConnectResolverForBothLanes` criterion. Surfaced rather than dropped; a reviewer who
   disagrees should read the adjacency criterion as its answer.
2. **The Helm chart value IS added in this phase — reversed after review.** The previous draft
   deferred `connect.headless` to a follow-up issue on the reasoning that no source artifact asked
   for a chart entry. REVIEWS.md MED-10 correctly points out that deferral leaves a Helm-deployed
   operator unable to satisfy `REQ-connect-headless-mount` through the supported chart at all, which
   is a requirement gap and not a documentation nicety. `charts/engram/values.yaml` has no generic
   extra-env escape hatch, so there is no workaround. Plan 04 Task 2 now adds the value, renders
   `ENGRAM_CONNECT_HEADLESS`, re-pins the `engram.containerEnv` drift checksum, and runs
   `task chart:validate`. It stays out of *this* plan only because it shares no file with any task
   here and belongs with the other operator-facing surface work.
3. **`buildAuthChain` returning `(nil, nil)` is not an error case.** Two callers read that nil
   differently — `withAuth` treats it as "MCP validation disabled, log loudly" (unchanged behavior)
   while `connectHeadlessGuard` treats it as fatal when the headless flag is set. The nil is the
   shared signal; if a future lane needs to distinguish "no lane configured" from "construction
   failed", the second return value already carries that.
4. **A UI-enabled deployment with a configured chain changes behavior on upgrade.** After HIGH-3's
   fix, such a deployment's Connect lane begins accepting bearer credentials it previously ignored
   (they fell through to the cookie lane and were rejected). This is intended — it is what
   `REQ-connect-bearer-identity` asks for and what T-03-07 accepts — but it *is* an observable
   change to an existing deployment, unlike the mount decision, which is strictly opt-in. A human
   should confirm that reading before the phase seals. The change is credential-family only: no new
   route, no new principal, no change to `internal/store` authz.

## Review Findings Disposition (01-REVIEWS.md, Codex)

| Finding | Disposition | Where |
|---|---|---|
| HIGH-3 — `connect.headless` wrongly doubles as a bearer-auth enablement switch | **incorporated** | Task 3 renamed and rewritten: mount decision is `cookieResolve == nil && !headless`, bearer half is `chain` unconditionally whenever mounted; `TestConnectResolverForUIEnabledIncludesBearerHalf`; source gates; `must_haves.truths[6]`; T-03-07 / T-03-08. Checked against D-10/D-12 — **no conflict**, both govern mounting only, and D-06 requires one chain reaching both mount sites. |
| MED-9 (second half) — `TestBuildAuthChainWrapsWithExpiry` is infeasible against a config-only builder | **incorporated** | Task 2 extracts `composeAuthChain(human, service, static)`; `TestComposeAuthChainWrapsWithExpiry` replaces it; a negative gate asserts the infeasible test name does not appear |
| MED-10 — the official Helm chart cannot enable the feature | **incorporated, relocated** | Reversed the prior deferral (Flagged Assumption 2 above); implemented in Plan 04 Task 2 with `task chart:validate` and the `engram.containerEnv` checksum re-pin |
| LOW-11 — `TestMountConnectDefaultOffWithoutUIOrHeadlessFlag` duplicates `TestMountConnectSkipsWhenResolverNil` | **incorporated** | Reshaped to assert the composition→mount link via `NewConnectResolver(nil, nil)` rather than a second hand-passed nil; the CONTEXT.md-named test survives with real coverage |
