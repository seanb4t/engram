diff --git a/internal/setup/plugin.go b/internal/setup/plugin.go
index edaf22f0..6a7615d1 100644
--- a/internal/setup/plugin.go
+++ b/internal/setup/plugin.go
@@ -410,6 +410,6 @@ func classifyPluginVersion(installed string, isInstalled bool, binary string) (P
 	case 0:
 		return PluginCurrent, ""
 	default:
-		return PluginCurrent, fmt.Sprintf("plugin %s is newer than this binary %s", boundCapture(installed), binary)
+		return PluginOutdated, fmt.Sprintf("plugin %s is newer than this binary %s", boundCapture(installed), binary)
 	}
 }
