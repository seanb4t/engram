---
phase: 01-test-harness-fixture-helper
plan: 04
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - internal/store/store_test.go
  - internal/store/export_test.go
  - internal/store/main_test.go
  - internal/store/instrument_test.go
  - internal/store/listscopes_oversized_test.go
autonomous: true
requirements:
  - REQ-oversized-fixture-helper
  - REQ-test-client-parity

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "internal/store's TestMain lives in `main_test.go` (`package store_test`): it installs `storetest.SkipOrFailNoQdrant` as the in-package no-Qdrant handler via `store.SetNoQdrantHandler` and exits with `storetest.Run(m)`; `store_test.go` no longer declares TestMain, an ENGRAM_REQUIRE_QDRANT parser, a container terminator, the image-tag constant, or the address/booted variables (D-07, D-10)"
    - "In-package tests read the Qdrant address from `ENGRAM_QDRANT_TEST_ADDR` (the variable `storetest.Run` exports after booting a container) and take their skip-or-fail decision from the installed handler, so storetest's parser is the only ENGRAM_REQUIRE_QDRANT parser in internal/store; `qdrantTOCTOUVerifiedVersion` stays a separate in-package constant from `storetest.QdrantImage` (D-10)"
    - "`TestSharedQdrantAddressHonored` is defined exactly once in the internal/store directory, in `main_test.go`, and PASSes when ENGRAM_QDRANT_TEST_ADDR is set, so CI's shared-address step reads 3 PASS + 1 SKIP across the four consumer packages (RESEARCH Pitfall 2)"
    - "`dialTestClient` still skips when no Qdrant is available and ENGRAM_REQUIRE_QDRANT is unset, and still FAILS with `no Qdrant available and ENGRAM_REQUIRE_QDRANT is set: failing instead of skipping` when it is set; the failing half's re-exec'd child now gets a placeholder shared address so it never boots a container"
    - "`TestListScopesFullPayloadsOverGRPCLimit` runs in `package store_test` over BOTH fixture shapes; each subtest dials with `storetest.Dial(t, storetest.RecvLimit)` and seeds with `storetest.SeedOversized` at `Limit: storetest.RecvLimit`, and asserts every seeded record is counted by ListScopes (D-04, D-05, REQ-oversized-fixture-helper)"
    - "Reverting ListScopes' scope-only payload selector to full payloads turns BOTH subtests RED (observed and recorded here; packaged as red-evidence by plan 01-05), so the migrated #583 test keeps, and widens, its detection power"
    - "`storetest.ManySmallRecords` equals internal/store's `maxListLimit`, pinned by `TestManySmallShapeFitsOneListPage`, so a many-small fixture always fits in one List page (D-05)"
  artifacts:
    - path: "internal/store/main_test.go"
      provides: "internal/store's TestMain (package store_test) and its single TestSharedQdrantAddressHonored"
      contains: "store.SetNoQdrantHandler(storetest.SkipOrFailNoQdrant)"
    - path: "internal/store/export_test.go"
      provides: "SetNoQdrantHandler, NewTestStore, PrefixedTestCollection, MaxListLimit exposed to package store_test"
      contains: "func SetNoQdrantHandler(f func(testing.TB))"
    - path: "internal/store/listscopes_oversized_test.go"
      provides: "the migrated #583 regression test over both shapes, plus TestManySmallShapeFitsOneListPage"
      contains: "TestListScopesFullPayloadsOverGRPCLimit"
    - path: "internal/store/store_test.go"
      provides: "in-package dialTestClient reading ENGRAM_QDRANT_TEST_ADDR and the installed no-Qdrant handler"
      contains: "noQdrantHandler(t)"
  key_links:
    - from: "internal/store/main_test.go"
      to: "internal/store/export_test.go"
      via: "the external TestMain hands storetest's skip-or-fail decision to in-package tests that cannot import storetest (D-07)"
      pattern: "store[.]SetNoQdrantHandler[(]storetest[.]SkipOrFailNoQdrant[)]"
    - from: "internal/store/main_test.go"
      to: "internal/store/storetest/storetest.go"
      via: "internal/store's container lifecycle is storetest's (D-10)"
      pattern: "os[.]Exit[(]storetest[.]Run[(]m[)][)]"
    - from: "internal/store/store_test.go"
      to: "internal/store/export_test.go"
      via: "in-package dialTestClient calls the handler the external TestMain installed"
      pattern: "noQdrantHandler[(]t[)]"
    - from: "internal/store/listscopes_oversized_test.go"
      to: "internal/store/storetest/seed.go"
      via: "the migrated #583 test seeds through the shared oversized seeder at the named limit (D-04)"
      pattern: "storetest[.]SeedOversized[(]t, st, storetest[.]Spec[{]Limit: storetest[.]RecvLimit"
    - from: "internal/store/listscopes_oversized_test.go"
      to: "internal/store/storetest/storetest.go"
      via: "the regression test names its receive limit to the dial helper (D-04)"
      pattern: "storetest[.]Dial[(]t, storetest[.]RecvLimit[)]"
  prohibitions:
    - statement: "MUST NOT let an oversized regression test's pass depend on the production receive-limit backstop: every such test names storetest.RecvLimit (4 MiB) explicitly to both Dial and SeedOversized, so a green result can only mean the bounded-read mechanism kept the response under the limit"
      category: transparency
      status: resolved
      verification: test
      reason: "Task 2 acceptance requires both storetest.RecvLimit arguments in the migrated test; the RED observation with the reverted payload selector proves the named limit is what the test measures against. No wired check descriptor by design (spec-less fallback, flagged-unverified)"
    - statement: "MUST NOT weaken internal/store's fail-closed Qdrant gate while relocating its harness: with ENGRAM_REQUIRE_QDRANT truthy a missing Qdrant must still FAIL an in-package dial rather than skip"
      category: transparency
      status: resolved
      verification: test
      reason: "TestDialTestClientFailsWhenRequiredAndUnavailable (re-exec) asserts the exact fail-closed message through the relocated handler; Task 1 gate runs it. No wired check descriptor by design (spec-less fallback, flagged-unverified)"
---

<objective>
Finish internal/store's convergence (D-07, D-10): move its TestMain into a new external-package file
`main_test.go` (`package store_test`, Go's sanctioned import-cycle breaker — storetest imports store, so
in-package tests cannot import storetest) that delegates to `storetest.Run`; hand storetest's
skip-or-fail decision to in-package tests through an `export_test.go` hook; have in-package tests read
the address from `ENGRAM_QDRANT_TEST_ADDR` (the existing contract `storetest.Run` exports); move
`TestSharedQdrantAddressHonored` into the same external file (RESEARCH Pitfall 2); and delete
internal/store's duplicated parser, terminator, image tag and address variables. Then migrate
`TestListScopesFullPayloadsOverGRPCLimit` (#583) onto `storetest.Dial` + `storetest.SeedOversized`
over both fixture shapes, keeping its name (it is plan 01-05's red-evidence target).

Purpose: REQ-oversized-fixture-helper (the first real read-path regression test on the shared seeder,
RED against the pre-fix code) and REQ-test-client-parity (the last duplicated harness collapses).

Output: `main_test.go`, `export_test.go`, `listscopes_oversized_test.go`; a slimmer `store_test.go`.

Expected red, do NOT fix: `TestRedEvidencePatchesAreLive` stays RED until plan 01-05 Task 3. It must be
the ONLY failing test in internal/store after each task.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-test-harness-fixture-helper/01-CONTEXT.md
@.planning/phases/01-test-harness-fixture-helper/01-RESEARCH.md
@.planning/phases/01-test-harness-fixture-helper/01-PATTERNS.md
@.planning/phases/01-test-harness-fixture-helper/01-01-SUMMARY.md
@.planning/phases/01-test-harness-fixture-helper/01-02-SUMMARY.md
@CLAUDE.md

<interfaces>
From plan 01-01 (`internal/store/storetest`): `RecvLimit`, `QdrantImage`, `Run(m, opts...) int`,
`RequireQdrant`, `SkipOrFailNoQdrant(t testing.TB)`, `Addr`, `EnvAddr`, `ContainerBooted`,
`AssertSharedAddressHonored(t testing.TB)`, `Dial(t testing.TB, recvLimit int, opts ...grpc.DialOption) *qdrant.Client`,
`Shape` (`FewLarge`, `ManySmall`, `String()` → `few-large` / `many-small`), `ManySmallRecords`,
`Spec{Limit, Shape, Vector, Template}`, `Fixture{Shape, Scope, Owner, IDs, RecordBytes, TotalBytes}`,
`SeedOversized(t testing.TB, st *store.Store, spec Spec) Fixture` (skips under -short; cleans up via DeleteAll).
`Run` exports a booted container's address with `os.Setenv("ENGRAM_QDRANT_TEST_ADDR", ...)`.

From plan 01-02 (`internal/store/store_test.go`): `dialTestClient(t *testing.T, opts ...grpc.DialOption) *qdrant.Client`,
still reading package var `testQdrantAddr` and calling in-package `requireQdrant()` — this plan replaces both.

Existing in-package code (verified at planning time):
- `store_test.go:29-57` — `qdrantImageTag`, `qdrantTOCTOUVerifiedVersion`, `testQdrantAddr`, `testQdrantContainerBooted`.
- `store_test.go:59-88` — `testCollectionPrefix = "store_"`, `testCollection`, `newTestStore(t testing.TB, c *qdrant.Client, name string, opts ...Option) *Store`.
- `store_test.go:98-164` — `requireQdrant`, `TestMain`, `terminateQdrant`; `:199-232` `TestRequireQdrant`; `:241-253` `TestSharedQdrantAddressHonored`; `:255-270` `TestDialTestClientSkipsWhenNotRequired`; `:272-303` `TestDialTestClientFailsWhenRequiredAndUnavailable` (re-exec via `os.Args[0]`, asserts the substring `no Qdrant available and ENGRAM_REQUIRE_QDRANT is set: failing instead of skipping`).
- `store_test.go:1160-1180, 4052, 4159, 4295` — TOCTOU messages naming the image-tag constant.
- `store_test.go:1788-1840` — `TestListScopesFullPayloadsOverGRPCLimit` (in-package, `testStore` + `DeleteAllRaw`).
- `store.go:1458` `const maxListLimit = 1000`; `store.go:1673-1705` `ListScopes` with `WithPayload: qdrant.NewWithPayloadInclude("scope")` (the #583 fix).
- `instrument_test.go:88` — comment referencing the address variable being deleted.
- `internal/store/spine_forgery_test.go:1-19` — existing `package store_test` file precedent.
- `collectionprefix_conformance_test.go` scans every `_test.go` in this directory (both packages): a qualified `store.New(` call outside a function named `newTestStore` is a finding, and exactly one `const testCollectionPrefix` must be found here (the in-package `store_`).
- go vet's `tests` analyzer rejects a `_test.go` function whose name starts with `Test` but is not a test — exported helpers in `export_test.go` must not start with `Test`.
</interfaces>
</context>

## Artifacts this phase produces (plan 01-04's share)

| Kind | Artifact | Created by |
|------|----------|------------|
| Go file | `internal/store/main_test.go` (`package store_test`): `TestMain`, `TestSharedQdrantAddressHonored` | Task 1 |
| Go file | `internal/store/export_test.go` (`package store`) | Task 1 |
| Test hook | `func SetNoQdrantHandler(f func(testing.TB))` (export_test.go) and in-package `var noQdrantHandler func(testing.TB)` (store_test.go) | Task 1 |
| Signature change | in-package `dialTestClient` reads `ENGRAM_QDRANT_TEST_ADDR` and calls `noQdrantHandler(t)` | Task 1 |
| Deleted | in-package `qdrantImageTag`, `testQdrantAddr`, `testQdrantContainerBooted`, `requireQdrant`, `TestMain`, `terminateQdrant`, `TestRequireQdrant`, and the in-package `TestSharedQdrantAddressHonored` | Task 1 |
| Test exports | `func NewTestStore(t testing.TB, c *qdrant.Client, name string, opts ...Option) *Store`, `func PrefixedTestCollection(name string) string`, `const MaxListLimit = maxListLimit` (export_test.go) | Task 2 |
| Go file | `internal/store/listscopes_oversized_test.go` (`package store_test`) | Task 2 |
| Tests | `TestListScopesFullPayloadsOverGRPCLimit` (subtests `few-large`, `many-small`), `TestManySmallShapeFitsOneListPage` | Task 2 |

## Edge coverage and prohibitions

No edge-probe rows are allocated to this plan (plans 01-01 and 01-02 author all ten). This plan owns the
fourth kept prohibition (regression tests never depend on the production backstop) and re-asserts the
fail-closed prohibition across the harness relocation; both are in `must_haves.prohibitions`.

<tasks>

<task type="tracer">
  <name>Task 1: internal/store's harness runs on storetest end to end: external TestMain, in-package handler hook, env-var address, single shared-address test</name>
  <precondition>Docker is reachable (`docker info` exits 0) or ENGRAM_QDRANT_TEST_ADDR names a live Qdrant gRPC endpoint: this task's verify runs real-Qdrant tests under ENGRAM_REQUIRE_QDRANT=1, which fails rather than skips without one.</precondition>
  <files>internal/store/store_test.go, internal/store/export_test.go, internal/store/main_test.go, internal/store/instrument_test.go</files>
  <read_first>
    - `internal/store/store_test.go` lines 1-310 (imports, constants, variables, seam, parser, TestMain, terminator, dialTestClient, TestRequireQdrant, TestSharedQdrantAddressHonored, both dial skip/fail tests) and lines 1155-1185, 4045-4060, 4150-4165, 4288-4300 (TOCTOU messages).
    - `internal/store/spine_forgery_test.go` lines 1-19 — external package header precedent.
    - `internal/store/instrument_test.go` lines 85-92.
    - `internal/store/storetest/storetest.go` (plan 01-01) — `Run`, `SkipOrFailNoQdrant`, `AssertSharedAddressHonored` exact behavior.
    - `.planning/phases/01-test-harness-fixture-helper/01-RESEARCH.md` Pitfall 2 and the "Go-language confirmation" paragraph (package-level vars do not cross the store/store_test boundary; the env var is the only channel).
    - `.planning/phases/01-test-harness-fixture-helper/01-CONTEXT.md` D-07, D-10.
  </read_first>
  <action>
NEVER run any git stash subcommand — to inspect a pristine file use `git show HEAD:<path>` into the scratchpad instead. Commit with an explicit pathspec (rule n6m4as49mr). `TestRedEvidencePatchesAreLive` is the single expected red until plan 01-05 Task 3. All four file edits land in ONE commit: two TestMain functions in one test binary do not compile.

(1) `internal/store/store_test.go` (D-07, D-10):
- Delete the image-tag constant (its value now lives in `storetest.QdrantImage`), `testQdrantAddr`, `testQdrantContainerBooted`, `requireQdrant`, `TestMain`, `terminateQdrant`, `TestRequireQdrant` (storetest's copy is now the single pin), and `TestSharedQdrantAddressHonored` (it moves to main_test.go — define it in exactly one of the two packages). Keep `qdrantTOCTOUVerifiedVersion` as a SEPARATE constant and rewrite every remaining mention of the deleted constant — its doc comment, the TOCTOU test comments near lines 1160-1170, and the four TOCTOU failure messages (lines ~1179, 4052, 4159, 4295) — to name `storetest.QdrantImage` instead.
- Add `var noQdrantHandler func(testing.TB)` with a doc comment: installed by internal/store's TestMain (main_test.go, package store_test) via `SetNoQdrantHandler` before any test runs; it is storetest's skip-or-fail decision, handed across the package boundary because package store cannot import storetest (import cycle, D-07), so storetest's parser stays the only ENGRAM_REQUIRE_QDRANT parser.
- `dialTestClient(t *testing.T, opts ...grpc.DialOption)`: read `addr := os.Getenv("ENGRAM_QDRANT_TEST_ADDR")` (D-10's existing contract — storetest.Run exports a booted container's address through it); when empty, `t.Fatal` with a message naming the missing hook if `noQdrantHandler` is nil, otherwise call exactly `noQdrantHandler(t)`; the host/port split and `NewQdrantClient(host, port, opts...)` stay as plan 01-02 left them. Update its doc comment.
- `TestDialTestClientSkipsWhenNotRequired`: drop the save/restore of the deleted variable; use `t.Setenv("ENGRAM_QDRANT_TEST_ADDR", "")` and `t.Setenv("ENGRAM_REQUIRE_QDRANT", "")`; the inner run must still report skipped-not-failed.
- `TestDialTestClientFailsWhenRequiredAndUnavailable`: in the helper branch replace the variable assignment with `t.Setenv("ENGRAM_QDRANT_TEST_ADDR", "")` before `dialTestClient(t)`; in the parent append `ENGRAM_QDRANT_TEST_ADDR=127.0.0.1:1` to the child env (after `os.Environ()`, so it wins) alongside the two existing helper variables, so the child's TestMain always takes storetest's fast path and never boots a container — the helper clears it before dialing, so the placeholder is never dialed. Keep every existing output assertion, including the exact fail-closed substring.
- Remove imports left unused (`tcqdrant`, possibly others).

(2) New `internal/store/export_test.go` (package `store`, SPDX header): `func SetNoQdrantHandler(f func(testing.TB)) { noQdrantHandler = f }` with a doc comment explaining the D-07 boundary. Nothing else in this task.

(3) New `internal/store/main_test.go` (package `store_test`, SPDX header, a package-doc paragraph in the `spine_forgery_test.go` style explaining why TestMain lives in the external package: storetest imports store, so only the external test package can import storetest; both packages compile into one test binary with exactly one TestMain; in-package tests receive the address via ENGRAM_QDRANT_TEST_ADDR and the skip-or-fail decision via the hook):
- `func TestMain(m *testing.M)`: `store.SetNoQdrantHandler(storetest.SkipOrFailNoQdrant)` then `os.Exit(storetest.Run(m))`.
- `func TestSharedQdrantAddressHonored(t *testing.T)`: doc comment carried over (CI pins this name; D-20 of the prior milestone); body `storetest.AssertSharedAddressHonored(t)`.

(4) `internal/store/instrument_test.go`: reword the line-88 comment to say `testStore` skips (or fails under ENGRAM_REQUIRE_QDRANT) when no Qdrant is available. Comment-only.

Behavior preserved verbatim through storetest (D-10): fail-closed parsing, 3-minute bounded start, 30-second bounded terminate, skip-with-message, the `store_` collection prefix, `qdrantTOCTOUVerifiedVersion` separate from the image tag.

Commit as `test(store): run internal/store's suite on the storetest harness` with pathspec `internal/store/store_test.go internal/store/export_test.go internal/store/main_test.go internal/store/instrument_test.go`, ending the message with the session's attribution trailer lines.
  </action>
  <verify>
    <automated>out=$(ENGRAM_REQUIRE_QDRANT=1 go test ./internal/store/ -count=1 -v 2>&amp;1); fails=$(printf '%s\n' "$out" | rg -o '^--- FAIL: \S+' | sort -u); printf 'top-level failures:\n%s\n' "$fails"; test "$fails" = "--- FAIL: TestRedEvidencePatchesAreLive" &amp;&amp; printf '%s\n' "$out" | rg -q -e '^--- PASS: TestDialTestClientFailsWhenRequiredAndUnavailable' &amp;&amp; printf '%s\n' "$out" | rg -q -e '^--- PASS: TestDialTestClientSkipsWhenNotRequired'</automated>
    <fails_when>the set of top-level `--- FAIL:` lines is anything other than exactly `--- FAIL: TestRedEvidencePatchesAreLive` (empty means a build failure, e.g. two TestMains), or either dial skip/fail test is not reported `--- PASS`</fails_when>
    <automated>out=$(ENGRAM_QDRANT_TEST_ADDR=127.0.0.1:6334 ENGRAM_REQUIRE_QDRANT=1 go test -run '^TestSharedQdrantAddressHonored$' -v -count=1 ./internal/store/... ./internal/server/... ./internal/e2e/... ./internal/retrievaleval/... 2>&amp;1); rc=$?; pass=$(printf '%s\n' "$out" | rg -o '^--- PASS: TestSharedQdrantAddressHonored' | wc -l | tr -d '[:space:]'); skip=$(printf '%s\n' "$out" | rg -o '^--- SKIP: TestSharedQdrantAddressHonored' | wc -l | tr -d '[:space:]'); echo "PASS=$pass SKIP=$skip"; test "$rc" -eq 0 &amp;&amp; test "$pass" = 3 &amp;&amp; test "$skip" = 1</automated>
    <fails_when>non-zero exit, or counts other than exactly `PASS=3 SKIP=1` — CI's pinned invariant (`.github/workflows/ci.yaml` shared-address step); 4 PASS means the test is defined in both packages, 2 PASS means internal/store lost it</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n '^func (TestMain|TestSharedQdrantAddressHonored)[(]' internal/store/*_test.go` prints exactly two lines, both in `internal/store/main_test.go`.
    - `rg -n 'func (requireQdrant|terminateQdrant|TestRequireQdrant)[(]' internal/store/*_test.go | wc -l` prints `0`; `rg -n -w 'testQdrant(Addr|ContainerBooted)|qdrantImageTag' internal/store/*_test.go | wc -l` prints `0`; `rg -n 'qdrant/qdrant[:]v' internal/store/*_test.go | wc -l` prints `0`.
    - `rg -n -F 'qdrantTOCTOUVerifiedVersion = "1.19.1"' internal/store/store_test.go` prints exactly one line (kept separate, D-10).
    - `rg -n 'Getenv[(]"ENGRAM_REQUIRE_QDRANT"[)]|strconv[.]ParseBool' internal/store/*_test.go | wc -l` prints `0` (no ENGRAM_REQUIRE_QDRANT parser left in internal/store's tests; storetest owns the only one).
    - `head -2` of `main_test.go` and `export_test.go` matches `head -2 internal/store/store.go`; `rg -n '^package store_test$' internal/store/main_test.go` prints one line.
    - `rg -n '^func Test' internal/store/export_test.go | wc -l` prints `0`, and `go vet ./internal/store/` exits 0.
    - `ENGRAM_REQUIRE_QDRANT=treu go test ./internal/store/ -run '^TestSharedQdrantAddressHonored$' -count=1` exits non-zero with `invalid value "treu"` in its output.
    - `golangci-lint run ./internal/store/...` reports no findings.
  </acceptance_criteria>
  <done>internal/store's suite runs on storetest.Run from an external TestMain; in-package tests get the address from the env var and the skip-or-fail decision from the installed hook; the duplicated harness is gone; the fail-closed and skip behaviors and CI's 3 PASS + 1 SKIP hold; committed with an explicit pathspec.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate the #583 regression test onto storetest over both shapes, observed RED against the pre-fix ListScopes</name>
  <precondition>Docker is reachable (`docker info` exits 0) or ENGRAM_QDRANT_TEST_ADDR names a live Qdrant gRPC endpoint: this task's verify runs real-Qdrant tests under ENGRAM_REQUIRE_QDRANT=1, which fails rather than skips without one.</precondition>
  <files>internal/store/store_test.go, internal/store/export_test.go, internal/store/listscopes_oversized_test.go</files>
  <read_first>
    - `internal/store/store_test.go` lines 1785-1845 — the in-package `TestListScopesFullPayloadsOverGRPCLimit` being moved (its doc comment is the rationale to carry over).
    - `internal/store/store.go` lines 1663-1705 — `ListScopes` and its scope-only `WithPayload` selector (the #583 fix); line 1458 `maxListLimit`.
    - `internal/store/storetest/seed.go` (plan 01-01) — `SeedOversized`, `Spec`, `Fixture`, `ManySmallRecords`, `Shape.String()`.
    - `internal/store/export_test.go` and `internal/store/main_test.go` (Task 1).
    - `internal/store/collectionprefix_conformance_test.go` lines 100-200 — why the external file must build its store through an exported seam, never a qualified `store.New(`.
    - `.planning/phases/01-test-harness-fixture-helper/01-CONTEXT.md` D-04, D-05, D-07, D-12 and "Specific Ideas".
  </read_first>
  <behavior>
    - `TestListScopesFullPayloadsOverGRPCLimit` (package `store_test`, real Qdrant): one subtest per shape, FewLarge then ManySmall, named by `Shape.String()`. Each: `c := storetest.Dial(t, storetest.RecvLimit)`; a fresh collection `store.PrefixedTestCollection("oversized_listscopes_" + uuid.NewString())` built with `store.NewTestStore(t, c, name)`, `EnsureCollection(ctx, 3)`, deleted in `t.Cleanup` via `c.DeleteCollection`; `fx := storetest.SeedOversized(t, st, storetest.Spec{Limit: storetest.RecvLimit, Shape: shape, Vector: []float32{0.1, 0.2, 0.3}})`; `scopes, _, err := st.ListScopes(ctx, store.Authenticated(fx.Owner))` must not error (message names the receive limit); the count for `fx.Scope` equals `len(fx.IDs)` (40 or 1000).
    - `TestManySmallShapeFitsOneListPage` (package `store_test`, no Qdrant): `storetest.ManySmallRecords == store.MaxListLimit`.
    - RED observation (recorded in the SUMMARY; plan 01-05 packages it as a patch): change ListScopes' `WithPayload` selector to request full payloads, run `ENGRAM_REQUIRE_QDRANT=1 go test ./internal/store/ -run '^TestListScopesFullPayloadsOverGRPCLimit$' -count=1 -v`, observe BOTH `--- FAIL: TestListScopesFullPayloadsOverGRPCLimit/few-large` and `--- FAIL: TestListScopesFullPayloadsOverGRPCLimit/many-small`, then restore store.go by writing `git diff -- internal/store/store.go` to a scratchpad patch and running `git apply -R` on it; `git status --porcelain -- internal/store/store.go` must print nothing.
  </behavior>
  <action>
NEVER run any git stash subcommand — to inspect a pristine file use `git show HEAD:<path>` into the scratchpad instead. Commit with an explicit pathspec (rule n6m4as49mr). `TestRedEvidencePatchesAreLive` is the single expected red until plan 01-05 Task 3.

(1) `internal/store/export_test.go`: add `func NewTestStore(t testing.TB, c *qdrant.Client, name string, opts ...Option) *Store` returning `newTestStore(t, c, name, opts...)` (the in-package prefix-asserting seam, D-16 of the prior milestone — the collection-prefix gate only permits live construction through a `newTestStore` seam, and a qualified `store.New(` in an external file would be a finding), `func PrefixedTestCollection(name string) string` returning `testCollection(name)` (named without a `Test` prefix: go vet's `tests` analyzer rejects non-test functions named `Test...` in `_test.go` files), and `const MaxListLimit = maxListLimit`. Doc comment on each: exposed only to package store_test (D-07).

(2) `internal/store/store_test.go`: delete the in-package `TestListScopesFullPayloadsOverGRPCLimit` (the name must exist exactly once in the directory).

(3) New `internal/store/listscopes_oversized_test.go` (package `store_test`, SPDX header): the two tests in `<behavior>`. Carry the original doc comment's substance: the test proves ListScopes' request shape against real Qdrant with the receive limit NAMED by the test (storetest.RecvLimit, D-04), covering our request shape, never grpc-go's or Qdrant's behavior (rule m45p2b4bp7); it now covers both fixture shapes; the `-short` skip is inherited from the seeder (D-12). Pass `storetest.RecvLimit` to BOTH `storetest.Dial` and the `Spec` literal, written exactly as `storetest.SeedOversized(t, st, storetest.Spec{Limit: storetest.RecvLimit, Shape: shape, Vector: []float32{0.1, 0.2, 0.3}})`. No test or subtest opts into parallel execution (D-12). Imports: `context`, `testing`, `github.com/google/uuid`, internal/store, internal/store/storetest.

Run the RED observation from `<behavior>` before committing and record the two failing subtest lines in the SUMMARY.

Commit as `test(store): migrate the ListScopes 4 MiB regression onto storetest` with pathspec `internal/store/store_test.go internal/store/export_test.go internal/store/listscopes_oversized_test.go`, ending the message with the session's attribution trailer lines.
  </action>
  <verify>
    <automated>out=$(ENGRAM_REQUIRE_QDRANT=1 go test ./internal/store/ -run '^(TestListScopesFullPayloadsOverGRPCLimit|TestManySmallShapeFitsOneListPage|TestEveryStoreConstructionRoutesThroughSeam|TestCollectionPrefixesAreDisjoint)$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^\s*--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; printf '%s\n' "$out" | rg -q -e '--- PASS: TestListScopesFullPayloadsOverGRPCLimit/few-large' &amp;&amp; printf '%s\n' "$out" | rg -q -e '--- PASS: TestListScopesFullPayloadsOverGRPCLimit/many-small' &amp;&amp; printf '%s\n' "$out" | rg -q -e '^--- PASS: TestManySmallShapeFitsOneListPage' &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e '--- SKIP' | wc -l)" -eq 0</automated>
    <fails_when>non-zero exit, any `--- FAIL:` or `--- SKIP` line, or either shape subtest / the one-page parity test missing its `--- PASS` line</fails_when>
    <automated>out=$(ENGRAM_REQUIRE_QDRANT=1 go test ./internal/store/ -count=1 -v 2>&amp;1); fails=$(printf '%s\n' "$out" | rg -o '^--- FAIL: \S+' | sort -u); printf 'top-level failures:\n%s\n' "$fails"; test "$fails" = "--- FAIL: TestRedEvidencePatchesAreLive"</automated>
    <fails_when>the set of top-level `--- FAIL:` lines is anything other than exactly `--- FAIL: TestRedEvidencePatchesAreLive`</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n '^func TestListScopesFullPayloadsOverGRPCLimit[(]' internal/store/*_test.go` prints exactly one line, in `internal/store/listscopes_oversized_test.go`.
    - `rg -o 'storetest[.]Dial[(]t, storetest[.]RecvLimit[)]' internal/store/listscopes_oversized_test.go | wc -l` prints `1` and `rg -o 'Limit: storetest[.]RecvLimit' internal/store/listscopes_oversized_test.go | wc -l` prints `1` (the named limit reaches both the dial and the seeder — D-04).
    - `rg -o 'store[.]New[(]' internal/store/listscopes_oversized_test.go | wc -l` prints `0` and `rg -o 't[.]Parallel' internal/store/listscopes_oversized_test.go | wc -l` prints `0`.
    - `rg -o '4 *<< *20|4194304' internal/store/listscopes_oversized_test.go | wc -l` prints `0` (the limit is named via storetest, never re-typed).
    - The SUMMARY records the RED observation: both `--- FAIL: TestListScopesFullPayloadsOverGRPCLimit/few-large` and `/many-small` with the full-payload selector, and a clean `git status --porcelain -- internal/store/store.go` after restoring.
    - `go vet ./internal/store/` exits 0; `golangci-lint run ./internal/store/...` reports no findings.
  </acceptance_criteria>
  <done>The #583 regression runs in package store_test over both shapes with the named 4 MiB limit, is observed RED for both shapes against the pre-fix selector, the many-small shape is pinned to one List page, and internal/store's only failure remains TestRedEvidencePatchesAreLive; committed with an explicit pathspec.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external TestMain → in-package tests | The skip-or-fail decision and the Qdrant address cross the store/store_test package boundary through a hook and an env var; a miswired hook would turn a required Qdrant into a silent skip. |
| test process → Qdrant | Oversized fixtures (about 5 MiB each) land in the shared CI Qdrant. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Repudiation | internal/store's fail-closed gate lost in the relocation (required Qdrant silently skipped) | high | mitigate | `dialTestClient` t.Fatal's when the hook is missing; the re-exec test asserts the exact fail-closed message through the relocated handler; `treu` still exits non-zero at TestMain. |
| T-04-02 | Repudiation | TestSharedQdrantAddressHonored duplicated or lost, breaking CI's pinned count or proving nothing | medium | mitigate | Acceptance requires exactly one definition (main_test.go); Task 1 runs CI's exact command and requires PASS=3 SKIP=1. |
| T-04-03 | Repudiation | the migrated regression passing because of a client-side accident rather than the bounded request shape | medium | mitigate | The test names `storetest.RecvLimit` to Dial and the seeder; the observed RED with the full-payload selector proves it measures the request shape; plan 01-05 registers that patch. |
| T-04-04 | Denial of Service | oversized fixtures accumulating in the shared CI Qdrant (#497) | medium | mitigate | One fresh collection per subtest, deleted in `t.Cleanup`; the seeder's DeleteAll cleanup is registered before its first write; no parallel subtests (D-12). |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | Not applicable: no installs; `github.com/google/uuid` is already a direct dependency. |
</threat_model>

<verification>
- `ENGRAM_REQUIRE_QDRANT=1 go test ./internal/store/ -count=1 -v` fails only `TestRedEvidencePatchesAreLive`; both `TestListScopesFullPayloadsOverGRPCLimit` subtests pass.
- `ENGRAM_QDRANT_TEST_ADDR=127.0.0.1:6334 ENGRAM_REQUIRE_QDRANT=1 go test -run '^TestSharedQdrantAddressHonored$' -v -count=1 ./internal/store/... ./internal/server/... ./internal/e2e/... ./internal/retrievaleval/...` reports exactly 3 PASS + 1 SKIP.
- `go test -short ./internal/store/... -count=1` exits 0 (the red-evidence harness and the oversized fixtures both skip under -short).
- `task lint` exits 0; `git diff --exit-code main...HEAD -- go.mod go.sum` exits 0.
- After this plan's SUMMARY is written: `go test ./internal/keylinks/ -count=1` exits 0.
</verification>

<success_criteria>
- All four duplicated TestMains, all three duplicated parsers and all four image-tag copies now collapse into storetest (the last internal/store copies go here; D-10).
- The first read-path regression test of this milestone uses the shared seeder at the named limit and is RED against the pre-fix code (REQ-oversized-fixture-helper).
- CI's pinned shared-address invariant (3 PASS + 1 SKIP) holds.
</success_criteria>

<output>
Create `.planning/phases/01-test-harness-fixture-helper/01-04-SUMMARY.md` when done.
</output>
