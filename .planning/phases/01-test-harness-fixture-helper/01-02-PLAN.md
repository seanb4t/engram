---
phase: 01-test-harness-fixture-helper
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - internal/store/store_test.go
  - internal/store/qdrantclient_test.go
  - internal/store/migrate_status_test.go
  - internal/store/revert_test.go
  - internal/store/migrate_converge_test.go
  - internal/store/schemaversion_recallgate_test.go
  - internal/store/migrate_faultinject_test.go
autonomous: true
requirements:
  - REQ-test-client-parity
coupling_justified:
  - "01-03: internal/store's TestEveryStoreConstructionRoutesThroughSeam and TestCollectionPrefixesAreDisjoint read 01-03's server/e2e/retrievaleval test files as source text; 01-03 keeps every newTestStore seam and testCollectionPrefix constant unchanged, so either execution order leaves both gates green"

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every in-package `internal/store` test client is built by one helper, `dialTestClient(t, opts ...grpc.DialOption)`, which calls `NewQdrantClient(host, port, opts...)`; the five interceptor helpers pass their interceptor as a caller option and no longer carry their own address/skip/parse block (D-07, D-10)"
    - "A test client built through the shared constructor with a caller `grpc.WithUnaryInterceptor` BOTH fires that interceptor AND records the production otelgrpc client span for the same RPC: base options first, caller options after, neither displacing the other (edge probe `ordering`, REQ-test-client-parity, resolved explicit)"
    - "The six in-package dial sites keep their skip and fail-closed semantics unchanged in this plan: the address still comes from the existing in-package harness variable and `ENGRAM_REQUIRE_QDRANT` from the existing in-package parser; relocating the harness is plan 01-04's job"
    - "Every interceptor-driven test family (facet counts, count side effects, mid-sweep hooks, recall-filter capture, SetPayload fault injection) still passes against real Qdrant, and the only failing top-level test in `internal/store` is `TestRedEvidencePatchesAreLive`"
  artifacts:
    - path: "internal/store/store_test.go"
      provides: "dialTestClient(t, opts...) dialing through NewQdrantClient"
      contains: "NewQdrantClient(host, port, opts...)"
    - path: "internal/store/qdrantclient_test.go"
      provides: "TestNewQdrantClientAppliesBaseAndCallerOptions: the production-parity proof for test clients"
      contains: "TestNewQdrantClientAppliesBaseAndCallerOptions"
  key_links:
    - from: "internal/store/store_test.go"
      to: "internal/store/store.go"
      via: "the in-package dialing primitive builds every in-package client through the shared constructor (D-07)"
      pattern: "NewQdrantClient[(]host, port, opts[.][.][.][)]"
    - from: "internal/store/migrate_status_test.go"
      to: "internal/store/store_test.go"
      via: "the facet-intercepting helper delegates to dialTestClient"
      pattern: "dialTestClient[(]t, grpc[.]WithUnaryInterceptor[(]interceptor[)][)]"
    - from: "internal/store/revert_test.go"
      to: "internal/store/store_test.go"
      via: "the count-side-effect helper delegates to dialTestClient"
      pattern: "dialTestClient[(]t, grpc[.]WithUnaryInterceptor[(]countSideEffectInterceptor[(]inj[)][)][)]"
    - from: "internal/store/migrate_converge_test.go"
      to: "internal/store/store_test.go"
      via: "the mid-sweep helper delegates to dialTestClient"
      pattern: "dialTestClient[(]t, grpc[.]WithUnaryInterceptor[(]midSweepInterceptor[(]h[)][)][)]"
    - from: "internal/store/schemaversion_recallgate_test.go"
      to: "internal/store/store_test.go"
      via: "the recall-filter capturing helper delegates to dialTestClient"
      pattern: "dialTestClient[(]t, grpc[.]WithUnaryInterceptor[(]recallCaptureInterceptor[(]t, capture[)][)][)]"
    - from: "internal/store/migrate_faultinject_test.go"
      to: "internal/store/store_test.go"
      via: "the SetPayload fault-injecting helper delegates to dialTestClient"
      pattern: "dialTestClient[(]t, grpc[.]WithUnaryInterceptor[(]setPayloadFaultInterceptor[(]inj[)][)][)]"
    - from: "internal/store/qdrantclient_test.go"
      to: "internal/store/instrument_test.go"
      via: "the parity test reads the package's single process-wide span recorder"
      pattern: "withSpanRecorder[(]t[)]"
---

<objective>
Converge all six in-package `internal/store` test dial sites onto the shared constructor from plan
01-01: `dialTestClient` gains a `...grpc.DialOption` pass-through and calls `NewQdrantClient`
directly by its bare name (D-07: package `store` tests cannot import storetest), and the five
interceptor-wrapping siblings (`dialFacetInterceptingTestClient`, `dialCountSideEffectTestClient`,
`dialMidSweepTestClient`, `dialCapturingTestClient`, `dialFaultInjectingTestClient`) collapse to one
line each that hands their interceptor to `dialTestClient` (D-10). A new parity test proves a test
client now carries the production dial option (the otelgrpc stats handler) AND the caller's option at
once — the substance of REQ-test-client-parity's "same dial options as the production client".

This plan deliberately leaves the harness itself (TestMain, the in-package address variable and
`requireQdrant`) where it is; plan 01-04 relocates it. Keeping the two steps apart keeps every commit
compiling.

Purpose: REQ-test-client-parity — six of the eleven independent `qdrant.NewClient` test call sites
converge here.

Output: `dialTestClient(t, opts...)`, five one-line helpers, `internal/store/qdrantclient_test.go`.

Expected red, do NOT fix: `TestRedEvidencePatchesAreLive` (internal/store) stays RED until plan 01-05
Task 3 registers this phase's red-evidence patches (RESEARCH Pitfall 1). It must be the ONLY failing
test in the package after each task.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-test-harness-fixture-helper/01-CONTEXT.md
@.planning/phases/01-test-harness-fixture-helper/01-RESEARCH.md
@.planning/phases/01-test-harness-fixture-helper/01-PATTERNS.md
@.planning/phases/01-test-harness-fixture-helper/01-01-SUMMARY.md
@CLAUDE.md

<interfaces>
From plan 01-01 (internal/store/store.go):
- `func NewQdrantClient(host string, port int, opts ...grpc.DialOption) (*qdrant.Client, error)` — base `grpc.WithStatsHandler(otelgrpc.NewClientHandler())` first, caller options appended.

Existing in-package test code (verified at planning time):
- `internal/store/store_test.go:170` `dialTestClient(t *testing.T) *qdrant.Client` — reads package var `testQdrantAddr`, calls `requireQdrant()` on empty address, splits host/port, then `qdrant.NewClient(&qdrant.Config{Host: host, Port: port})`.
- `internal/store/instrument_test.go:33` `withSpanRecorder(t *testing.T) *tracetest.SpanRecorder` — installs ONE process-wide recorder-backed TracerProvider (sync.Once) and returns it; the recorder accumulates, so a test must snapshot `before := len(sr.Ended())` and search `sr.Ended()[before:]`.
- otelgrpc v0.71.0 names a client span after the full method without its leading slash (`internal/parse.go:21-28`), so a Qdrant health check span is named `qdrant.Qdrant/HealthCheck`.
- `(*qdrant.Client).HealthCheck(ctx) (*qdrant.HealthCheckReply, error)`.
- The five helpers and their interceptor expressions:
  - `migrate_status_test.go:181` `dialFacetInterceptingTestClient(t, interceptor grpc.UnaryClientInterceptor)` → interceptor
  - `revert_test.go:536` `dialCountSideEffectTestClient(t, inj *countSideEffectInjector)` → `countSideEffectInterceptor(inj)`
  - `migrate_converge_test.go:425` `dialMidSweepTestClient(t, h *midSweepHook)` → `midSweepInterceptor(h)`
  - `schemaversion_recallgate_test.go:919` `dialCapturingTestClient(t, capture *recallCapture)` → `recallCaptureInterceptor(t, capture)`
  - `migrate_faultinject_test.go:228` `dialFaultInjectingTestClient(t, inj *setPayloadFaultInjector)` → `setPayloadFaultInterceptor(inj)`
</interfaces>
</context>

## Artifacts this phase produces (plan 01-02's share)

| Kind | Artifact | Created by |
|------|----------|------------|
| Signature change | `dialTestClient(t *testing.T, opts ...grpc.DialOption) *qdrant.Client` dialing through `NewQdrantClient` | Task 1 |
| Go file | `internal/store/qdrantclient_test.go` (package `store`) | Task 1 |
| Test | `TestNewQdrantClientAppliesBaseAndCallerOptions` | Task 1 |
| Body change | `dialFacetInterceptingTestClient`, `dialCountSideEffectTestClient`, `dialMidSweepTestClient`, `dialCapturingTestClient`, `dialFaultInjectingTestClient` each reduced to a `dialTestClient(t, grpc.WithUnaryInterceptor(...))` delegation | Task 2 |

## Edge coverage (spec-less fallback)

This plan authors the tenth and last edge-probe row (plan 01-01 authored nine): REQ-test-client-parity
`ordering` — resolved explicit, as the second `must_haves.truths` entry, proven by
`TestNewQdrantClientAppliesBaseAndCallerOptions`. Phase accounting stays 10 authored + 0 flagged.
Prohibitions: none owned by this plan (see plans 01-01 and 01-04).

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: In-package tests dial through the shared constructor, proven by a production-span plus caller-interceptor parity test</name>
  <precondition>Docker is reachable (`docker info` exits 0) or ENGRAM_QDRANT_TEST_ADDR names a live Qdrant gRPC endpoint: this task's verify runs real-Qdrant tests under ENGRAM_REQUIRE_QDRANT=1, which fails rather than skips without one.</precondition>
  <files>internal/store/store_test.go, internal/store/qdrantclient_test.go</files>
  <read_first>
    - `internal/store/store_test.go` lines 1-27 (imports) and 166-195 (`dialTestClient`).
    - `internal/store/instrument_test.go` lines 20-60 — `withSpanRecorder`, `spanByName`, and the before/after scoping rule.
    - `internal/store/store.go` — `NewQdrantClient` (added by plan 01-01, directly above `New`).
    - `.planning/phases/01-test-harness-fixture-helper/01-01-SUMMARY.md` — confirm the constructor's final signature and doc.
    - `.planning/phases/01-test-harness-fixture-helper/01-CONTEXT.md` D-03 (tests gain the otelgrpc handler; no-op without a tracer provider) and D-07.
  </read_first>
  <behavior>
    - `TestNewQdrantClientAppliesBaseAndCallerOptions` (package `store`, real Qdrant): `sr := withSpanRecorder(t)`; `before := len(sr.Ended())`; a counting `grpc.UnaryClientInterceptor` that increments an `atomic.Int64` when the method ends in `/HealthCheck` and then calls the invoker; `c := dialTestClient(t, grpc.WithUnaryInterceptor(counting))`; `c.HealthCheck(context.Background())` succeeds; the counter is at least 1 (the caller option took effect); among `sr.Ended()[before:]` there is a span whose `Name()` ends in `/HealthCheck` and whose `SpanKind()` is `trace.SpanKindClient` (the production base option took effect on the same client).
    - RED first: add the variadic parameter to `dialTestClient` but keep constructing with `qdrant.NewClient(&qdrant.Config{Host: host, Port: port, GrpcOptions: opts})`; the new test FAILS on the missing client span while the interceptor count passes (proves the span assertion discriminates). Then switch to `NewQdrantClient` and observe GREEN. Record both in the SUMMARY.
  </behavior>
  <action>
NEVER run any git stash subcommand — to inspect a pristine file use `git show HEAD:<path>` into the scratchpad instead. Commit with an explicit pathspec (rule n6m4as49mr). `TestRedEvidencePatchesAreLive` is the single expected red until plan 01-05 Task 3.

(1) `internal/store/store_test.go`: change `dialTestClient` to `func dialTestClient(t *testing.T, opts ...grpc.DialOption) *qdrant.Client`. Keep its address/skip/fail-closed/host-port logic exactly as it is (plan 01-04 relocates the harness); replace only the client construction with exactly `c, err := NewQdrantClient(host, port, opts...)` (bare name — same package, D-07), keeping the `client: %v` fatal. Add the `google.golang.org/grpc` import. Rewrite its doc comment: it is the single in-package dialing primitive; every in-package test client flows through it and therefore through the shared constructor production uses, so an in-package client carries production's dial options plus whatever the caller appends (interceptors for fault injection, capture, counting). Existing zero-argument callers compile unchanged.

(2) New `internal/store/qdrantclient_test.go` (package `store`, two-line SPDX header byte-identical to store.go's) with `TestNewQdrantClientAppliesBaseAndCallerOptions` per `<behavior>`, plus a doc comment: this is REQ-test-client-parity's direct proof that a test client applies the same dial option as the production client (the otelgrpc stats handler) while still honoring caller options, asserted through the observable span and interceptor rather than by inspecting grpc internals; it asserts our constructor's composition, never grpc-go's or Qdrant's own behavior (rule m45p2b4bp7). Use `go.opentelemetry.io/otel/trace` for `SpanKindClient` and `sync/atomic` for the counter. The test does not opt into parallel execution (the span recorder is process-wide).

Follow the RED-then-GREEN sequence in `<behavior>` and record it.

Commit as `test(store): dial in-package test clients through NewQdrantClient` with pathspec `internal/store/store_test.go internal/store/qdrantclient_test.go`, ending the message with the session's attribution trailer lines.
  </action>
  <verify>
    <automated>out=$(ENGRAM_REQUIRE_QDRANT=1 go test ./internal/store/ -run '^(TestNewQdrantClientAppliesBaseAndCallerOptions|TestUpsertGetDeleteRoundtrip|TestListScopes|TestDialTestClientSkipsWhenNotRequired)$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e '^--- PASS: Test' | wc -l)" -eq 4</automated>
    <fails_when>non-zero exit, a `--- FAIL:` line, or fewer than four top-level `--- PASS: Test` lines (the parity test or an existing dialTestClient caller regressed)</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n -F 'func dialTestClient(t *testing.T, opts ...grpc.DialOption) *qdrant.Client' internal/store/store_test.go` prints exactly one line.
    - `rg -n -F 'c, err := NewQdrantClient(host, port, opts...)' internal/store/store_test.go` prints exactly one line.
    - `head -2 internal/store/qdrantclient_test.go` matches `head -2 internal/store/store.go`.
    - `rg -o 'SpanKindClient' internal/store/qdrantclient_test.go | wc -l` prints at least `1`, and `rg -o 'withSpanRecorder[(]t[)]' internal/store/qdrantclient_test.go | wc -l` prints `1`.
    - The SUMMARY records the observed RED (missing client span with the plain-constructor variant) and the GREEN after switching to `NewQdrantClient`.
    - `go vet ./internal/store/` exits 0 and `golangci-lint run ./internal/store/...` reports no findings.
  </acceptance_criteria>
  <done>dialTestClient accepts caller dial options and builds through NewQdrantClient; a real-Qdrant parity test proves a test client records the production otelgrpc span and fires a caller interceptor on the same RPC; committed with an explicit pathspec.</done>
</task>

<task type="auto">
  <name>Task 2: The five interceptor dial helpers delegate to dialTestClient, and every interceptor-driven test family still passes</name>
  <precondition>Docker is reachable (`docker info` exits 0) or ENGRAM_QDRANT_TEST_ADDR names a live Qdrant gRPC endpoint: this task's verify runs real-Qdrant tests under ENGRAM_REQUIRE_QDRANT=1, which fails rather than skips without one.</precondition>
  <files>internal/store/migrate_status_test.go, internal/store/revert_test.go, internal/store/migrate_converge_test.go, internal/store/schemaversion_recallgate_test.go, internal/store/migrate_faultinject_test.go</files>
  <read_first>
    - `internal/store/migrate_status_test.go` lines 175-210 (`dialFacetInterceptingTestClient`).
    - `internal/store/revert_test.go` lines 525-565 (`countSideEffectInterceptor`, `dialCountSideEffectTestClient`).
    - `internal/store/migrate_converge_test.go` lines 405-455 (`midSweepInterceptor`, `dialMidSweepTestClient`).
    - `internal/store/schemaversion_recallgate_test.go` lines 900-950 (`recallCaptureInterceptor`, `dialCapturingTestClient`).
    - `internal/store/migrate_faultinject_test.go` lines 215-260 (`dialFaultInjectingTestClient`).
    - `internal/store/store_test.go` — `dialTestClient(t, opts...)` as changed by Task 1.
    - Each file's import block (the `net` and `strconv` imports may become unused).
  </read_first>
  <action>
NEVER run any git stash subcommand — to inspect a pristine file use `git show HEAD:<path>` into the scratchpad instead. Commit with an explicit pathspec (rule n6m4as49mr). `TestRedEvidencePatchesAreLive` is the single expected red until plan 01-05 Task 3.

In each of the five helpers keep the function name, parameters and return type exactly as they are, and replace the whole body after `t.Helper()` with a single return (D-07, D-10):
- `dialFacetInterceptingTestClient`: `return dialTestClient(t, grpc.WithUnaryInterceptor(interceptor))`
- `dialCountSideEffectTestClient`: `return dialTestClient(t, grpc.WithUnaryInterceptor(countSideEffectInterceptor(inj)))`
- `dialMidSweepTestClient`: `return dialTestClient(t, grpc.WithUnaryInterceptor(midSweepInterceptor(h)))`
- `dialCapturingTestClient`: `return dialTestClient(t, grpc.WithUnaryInterceptor(recallCaptureInterceptor(t, capture)))`
- `dialFaultInjectingTestClient`: `return dialTestClient(t, grpc.WithUnaryInterceptor(setPayloadFaultInterceptor(inj)))`

Rewrite each helper's doc comment so it no longer claims to carry its own "identical dial/skip/parse boilerplate": it wires its interceptor as a caller option into `dialTestClient`, which owns the address, the skip/fail-closed behavior, and the shared-constructor dial (so this client also carries production's dial options). Remove any import these edits leave unused (`net`, `strconv` — check each file; keep them where other code still uses them). Leave every interceptor and every test that calls these helpers untouched. Do not edit `schemaversion_recallgate_test.go`'s scan scope, `recallTransmitters`, or any gate classification (RESEARCH Pitfall 3: those scans never see test helpers).

Commit as `test(store): route interceptor dial helpers through dialTestClient` with pathspec `internal/store/migrate_status_test.go internal/store/revert_test.go internal/store/migrate_converge_test.go internal/store/schemaversion_recallgate_test.go internal/store/migrate_faultinject_test.go`, ending the message with the session's attribution trailer lines.
  </action>
  <verify>
    <automated>out=$(ENGRAM_REQUIRE_QDRANT=1 go test ./internal/store/ -count=1 -v 2>&amp;1); fails=$(printf '%s\n' "$out" | rg -o '^--- FAIL: \S+' | sort -u); printf 'top-level failures:\n%s\n' "$fails"; test "$fails" = "--- FAIL: TestRedEvidencePatchesAreLive"</automated>
    <fails_when>the set of top-level `--- FAIL:` lines is anything other than exactly `--- FAIL: TestRedEvidencePatchesAreLive` — an extra failure means a converged helper broke an interceptor-driven family; an empty set means the package did not compile or the run never reached the harness</fails_when>
  </verify>
  <acceptance_criteria>
    - Each of the five helpers is a one-statement delegation: `rg -o 'return dialTestClient[(]t, grpc[.]WithUnaryInterceptor[(]' internal/store/migrate_status_test.go internal/store/revert_test.go internal/store/migrate_converge_test.go internal/store/schemaversion_recallgate_test.go internal/store/migrate_faultinject_test.go | wc -l` prints `5`.
    - No in-package test code constructs a client except `dialTestClient`: `rg -n 'qdrant[.]NewClient[(]' internal/store/*_test.go | rg -v '^[^:]+:[0-9]+:\s*//' | wc -l` prints `0` (comment lines excluded).
    - `rg -o 'testQdrantAddr' internal/store/migrate_status_test.go internal/store/revert_test.go internal/store/migrate_converge_test.go internal/store/schemaversion_recallgate_test.go internal/store/migrate_faultinject_test.go | wc -l` prints `0` (the address lives only in dialTestClient now).
    - `go vet ./internal/store/` exits 0 and `golangci-lint run ./internal/store/...` reports no findings.
  </acceptance_criteria>
  <done>All six in-package dial sites build through NewQdrantClient via dialTestClient, every interceptor-driven family passes against real Qdrant, and TestRedEvidencePatchesAreLive is the package's only failure; committed with an explicit pathspec.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in-package test → Qdrant | Fault-injection, capture and counting interceptors sit on the test client's request path; after convergence they share the connection with the production otelgrpc stats handler. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | an in-package test client silently diverging from production dial options | medium | mitigate | All six sites build through `NewQdrantClient`; `TestNewQdrantClientAppliesBaseAndCallerOptions` proves the production span and the caller interceptor both fire, with an observed RED against the plain-constructor variant. |
| T-02-02 | Repudiation | a converged helper dropping its interceptor, so a fault-injection or capture test passes vacuously | medium | mitigate | Helpers keep their signatures and pass the interceptor as the caller option; Task 2's gate requires the whole Qdrant-backed package green under `ENGRAM_REQUIRE_QDRANT=1`, and those families assert on what their interceptors observed. |
| T-02-03 | Information Disclosure | spans from test RPCs leaking into span-count assertions | low | accept | The store package's recorder is process-wide by design and every span test scopes its lookup to `sr.Ended()[before:]` by name; no store test asserts a total span count. |
| T-02-SC | Tampering | npm/pip/cargo installs | low | accept | Not applicable: no package installs; only stdlib `sync/atomic` and the already-direct otel trace module are newly imported by a test file. |
</threat_model>

<verification>
- `ENGRAM_REQUIRE_QDRANT=1 go test ./internal/store/ -count=1 -v` fails only `TestRedEvidencePatchesAreLive`; `TestNewQdrantClientAppliesBaseAndCallerOptions` passes.
- `rg -n 'qdrant[.]NewClient[(]' internal/store/*_test.go | rg -v '^[^:]+:[0-9]+:\s*//' | wc -l` prints `0`.
- `task lint` exits 0; `git diff --exit-code main...HEAD -- go.mod go.sum` exits 0.
- After this plan's SUMMARY is written: `go test ./internal/keylinks/ -count=1` exits 0.
</verification>

<success_criteria>
- Six of eleven test dial sites converge on the shared constructor (REQ-test-client-parity, D-07, D-10).
- A test client provably applies the production dial option and caller options together (edge `ordering`).
- No interceptor-driven test family regresses.
</success_criteria>

<output>
Create `.planning/phases/01-test-harness-fixture-helper/01-02-SUMMARY.md` when done.
</output>
