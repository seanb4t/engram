---
phase: 09-retrieval-eval-harness-ranking-precision
plan: 03
type: execute
wave: 2
depends_on:
  - 09-01
  - 09-02
files_modified:
  - internal/store/rerank.go
  - internal/store/rerank_test.go
  - internal/server/tools.go
  - internal/retrievaleval/retrieval_eval_test.go
  - internal/retrievaleval/fixtures.go
autonomous: false
requirements:
  - REQ-ranking-precision

must_haves:
  truths:
    - "A dependency-free, in-process heuristic reranker (D-06) reorders search hits deterministically — lexical-overlap boost (and/or MMR / score-gap re-scoring), pure Go over already-fetched store.Memory results, negligible latency, no new dependency."
    - "The reranker is exercised by BOTH the real search path (search_memory handler) and the eval, so the eval measures exactly what a real MCP client experiences after the fix."
    - "The #261 eval fixture is upgraded from report-only to a hard PASS/FAIL acceptance bar: Record T surfaces within default `k` for both Query A and Query B, and Record T's score is strictly above every seeded sticky-neighbor distractor (D-03 score separation)."
    - "The ranking approach shipped is the one the eval numbers select (D-05): D-06 light levers first; hybrid (D-07) and cross-encoder (D-08) are escalated ONLY if the eval shows D-06 is insufficient."
    - "The eval-gated accept/escalate decision is made against recorded recall@k/MRR + the #261 bar, at or above the post-#262 prod baseline (D-05a)."
  artifacts:
    - "internal/store/rerank.go — exported heuristic reranker over []store.Memory (D-06)."
    - "internal/store/rerank_test.go — hermetic unit test (no Qdrant/gateway) proving deterministic reordering; runs in CI's required `test` job."
    - "Updated internal/server/tools.go searchMemory path (over-fetch + rerank + truncate to effective k; default-k lever if the eval selects it)."
    - "Updated internal/retrievaleval eval (hard #261 assertion + rerank in the measured path)."
  key_links:
    - "search_memory handler → store rerank helper → truncate to k: the shipped ranking path."
    - "eval → same store rerank helper → recall@k/MRR + #261 hard bar: the acceptance gate that selects the approach (D-05)."
  prohibitions:
    - "Ranking MUST NOT introduce or depend on per-memory usage signals — usage signals are Phase 12 scope and must never affect ranking (explicit Phase 12 constraint). Only content / lexical / vector signals may influence ordering."
    - "Do NOT unconditionally implement the D-07 sparse-vector reindex or the D-08 cross-encoder — both are gated on eval evidence (D-05). They are represented here as conditional escalations, NOT committed work; implementing them speculatively violates D-05/D-06."
    - "Do NOT add a new dependency for D-06 — the heuristic rerank is pure Go stdlib string/slice ops."
    - "Do NOT make the rerank asymmetric between query and document sides in a way that breaks determinism — the reranker is a pure function of (query, hits)."
    - "Do NOT add a new required CI status check; the eval remains env-gated (Plan 01's constraint carries forward)."
---

<objective>
Close REQ-ranking-precision using the eval as the decision mechanism (D-05).
The #261 failure is phrasing-sensitive: near-verbatim restatements (Query A/B) of
Record T fail to surface T within default `k` because sticky topical neighbors
crowd it out. Because near-verbatim restatements have high lexical overlap with
their target, the light lever most likely to fix this — and the one D-06 mandates
trying first — is an in-process, dependency-free heuristic rerank (lexical-overlap
boost / MMR / score-gap), optionally with a higher default `k`.

This plan (1) implements the D-06 heuristic reranker wired into both the real
search path and the eval, (2) upgrades the #261 fixture to a hard acceptance bar
and runs the eval to get real numbers, and (3) makes the eval-gated accept/escalate
decision. Hybrid dense+lexical fusion (D-07) and a cross-encoder reranker (D-08)
are IN SCOPE only if the eval shows D-06 is insufficient — they are represented
here as conditional escalations with their RESEARCH mechanics, not as committed
work (D-05: nothing added speculatively).

Purpose: eliminate phrasing-sensitive misses so search-before-store stops
producing duplicates (the #261 goal), proven by the eval not intuition.
Output: the D-06 reranker + the eval-selected ranking decision.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-retrieval-eval-harness-ranking-precision/09-CONTEXT.md
@.planning/phases/09-retrieval-eval-harness-ranking-precision/09-RESEARCH.md
@.planning/phases/09-retrieval-eval-harness-ranking-precision/09-PATTERNS.md
@.planning/phases/09-retrieval-eval-harness-ranking-precision/09-VALIDATION.md
@.planning/phases/09-retrieval-eval-harness-ranking-precision/09-01-SUMMARY.md
</context>

<conditional_escalation>
## D-07 / D-08 are conditional on eval evidence (D-05) — NOT committed in this plan

These are represented so the phase is honest about scope, but they are executed
ONLY if Task 3's eval-gated checkpoint decides D-06 is insufficient. If triggered,
each becomes a follow-up plan (e.g. `/gsd-plan-phase 09 --gaps` or a new
`09-04`/`09-05`), planned within its own context budget. Do not implement here.

**D-07 — Hybrid dense+lexical (BM25) fusion** (RESEARCH Pattern 3/4, Pitfall 2):
- Qdrant server-side `qdrant/bm25` `Document` inference is Cloud-Inference-only and
  unavailable on self-hosted/OSS Qdrant (the Helm-deployed backend) — do NOT use it.
- Instead: generate sparse vectors client-side in Go (deterministic tokenize →
  hash terms to indices → term-frequency values, SAME tokenizer document- and
  query-side) and let Qdrant's `Modifier_Idf` on `SparseVectorParams` compute IDF
  server-side. Query via `QueryPoints.Prefetch` (dense + sparse) fused by
  `qdrant.NewQueryFusion(qdrant.Fusion_RRF)`.
- Converting the current single UNNAMED dense vector to a named-vector map
  (`"dense"` + `"sparse"`) is a breaking, immutable schema change → requires the
  existing `engram reindex --target <new-collection>` flow (re-embed AND
  re-tokenize every record, `--dry-run`, cut over `ENGRAM_QDRANT_COLLECTION`), NOT
  an in-place mutation. Model operator ergonomics on `cmd/engram/reindex.go`.

**D-08 — Cross-encoder reranker** (RESEARCH Pattern 5):
- Opt-in only; off the default hot path unless the eval justifies default-on.
- A second-stage HTTP call to a Cohere/Jina-compatible `/v1/rerank` endpoint
  (vLLM-hosted cross-encoder), mirroring `internal/embed`'s `net/http` client — NO
  new Go dependency; plain stdlib.
- New opt-in `ENGRAM_RERANK_*` koanf fields in `internal/config/registry.go` (no
  Default = disabled when empty; no Legacy). Any proposed rerank client LIBRARY
  (instead of hand-rolled `net/http`) MUST pass the Package Legitimacy Gate + a
  blocking human checkpoint before adoption — none is pre-approved.
- Data-flow note: rerank sends the FULL candidate record content (not just the
  query) to the external gateway — same trust boundary as existing embed/summarize
  calls, but a broader payload; document this for operators choosing D-08.
</conditional_escalation>

<artifacts_this_phase_produces>
## Artifacts this plan produces (source-grounding for drift verification)

New paths (committed, D-06):
- `internal/store/rerank.go` (exported heuristic reranker)
- `internal/store/rerank_test.go` (hermetic unit test)

Modified:
- `internal/server/tools.go` (searchMemory over-fetch + rerank + truncate; default-k lever iff eval-selected)
- `internal/retrievaleval/retrieval_eval_test.go`, `internal/retrievaleval/fixtures.go` (#261 hard bar + rerank in measured path + calibrated score-gap threshold)

New exported symbol (D-06):
- a reranker entrypoint in `internal/store` (e.g. `RerankHits(query string, hits []Memory, k int) []Memory`) — exact name at executor discretion, referenced by both the handler and the eval.

Conditional (NOT produced unless Task 3 escalates — see <conditional_escalation>):
- D-07: `SparseVectorsConfig` in `ensureCollection`, sparse tokenizer, `Prefetch`+`Fusion` in `Store.Search`, reindex backfill in `cmd/engram/reindex.go`, new target collection.
- D-08: `ENGRAM_RERANK_BASE_URL` / `ENGRAM_RERANK_MODEL` (and siblings) koanf fields, a `/v1/rerank` net/http client.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Implement the D-06 in-process heuristic reranker (dependency-free, hermetic-tested)</name>
  <files>internal/store/rerank.go, internal/store/rerank_test.go, internal/server/tools.go</files>
  <read_first>
    - internal/store/store.go (Memory struct incl. Content/Tags/Score ~L136-151, Store.Search ~L544-573, EmbedText tag-folding — the fields the reranker scores over)
    - internal/server/tools.go lines 704-728 (deps.searchMemory: default `if a.K == 0 { a.K = 8 }`, EmbedQuery → Store.Search → shapeRecall — where over-fetch + rerank + truncate hook in)
    - internal/summarize/fidelity_test.go (hermetic table-test style with no assertion library — the convention for rerank_test.go)
    - 09-RESEARCH.md "Don't Hand-Roll" (recall/MRR + rerank are the parts that genuinely SHOULD be hand-rolled) and D-06 in CONTEXT.md
  </read_first>
  <action>
    Add `internal/store/rerank.go`: an exported, pure reranker that takes the search query and the fetched `[]Memory` (each with `Score`, `Content`, `Tags`) and returns a deterministically reordered top-k slice. Implement at least a lexical-overlap boost (tokenize query and candidate content/tags the same way, up-weight candidates whose terms overlap the query — the direct lever for near-verbatim restatements) and structure it so score-gap re-scoring and/or MMR diversification can be layered per the eval. No new dependency: stdlib `strings`/`sort` only. Determinism: equal-scored ties break on a stable key (e.g. id) so output is reproducible.
    Wire it into `deps.searchMemory` (`internal/server/tools.go`): over-fetch from `Store.Search` (a limit ≥ the effective `k`, giving the reranker candidates to promote from), apply the reranker, then truncate to the effective `k`. Keep the default-k logic intact for now; only raise the default (the D-06 "higher default k" lever) if Task 2's eval numbers select it. Ensure ownership/scope filtering is unchanged — the reranker runs strictly AFTER the existing owner-scope filter, never widening visibility. Do not read or consult any usage-signal field (Phase 12 constraint).
    Add `internal/store/rerank_test.go`: a hermetic unit test (no Qdrant, no gateway) that feeds synthetic `[]Memory` and asserts the reranker promotes a high-lexical-overlap candidate above a topical-but-distinct one and is deterministic across runs.
  </action>
  <verify>
    <automated>go test ./internal/store/ -run Rerank && gofmt -l internal/store/rerank.go internal/store/rerank_test.go && go vet ./internal/store/... ./internal/server/...</automated>
  </verify>
  <acceptance_criteria>
    - `internal/store/rerank.go` exports a reranker function over `[]Memory` using only stdlib (grep: no new import beyond `strings`/`sort`/stdlib).
    - `go test ./internal/store/ -run Rerank` passes hermetically (no `ENGRAM_*` gateway or Qdrant env required) and runs inside the required `test` job.
    - The reranker is deterministic: repeated calls on identical input yield identical order (test asserts this).
    - `deps.searchMemory` over-fetches, reranks, and truncates to the effective `k`; owner/scope filtering is unchanged; no usage-signal field is referenced.
    - `task lint` and `task license:check` pass on the new files.
  </acceptance_criteria>
  <done>
    A dependency-free heuristic reranker exists, is unit-tested in CI, and is on the real search path — the D-06 lever, ready to be measured by the eval.
  </done>
</task>

<task type="auto">
  <name>Task 2: Upgrade the #261 fixture to a hard acceptance bar, exercise the reranker in the eval, and run it</name>
  <files>internal/retrievaleval/retrieval_eval_test.go, internal/retrievaleval/fixtures.go</files>
  <read_first>
    - internal/retrievaleval/retrieval_eval_test.go (the measured path from Plan 01 — where the reranker must be inserted so the eval measures the post-fix ordering)
    - internal/retrievaleval/fixtures.go (retrievalCase.wantScoreGap — the score-separation threshold to calibrate from the Plan 01 baseline)
    - internal/store/rerank.go (Task 1's reranker entrypoint the eval will call)
    - .planning/phases/09-retrieval-eval-harness-ranking-precision/09-01-SUMMARY.md (the recorded post-#262 baseline: recall@k/MRR and the #261 target ranks to improve against — D-05a)
  </read_first>
  <action>
    Update the eval's measured path so it applies the same `internal/store` reranker after `Store.Search` (over-fetch → rerank → truncate to default `k`), exactly mirroring the handler — so the eval measures the shipped path.
    Flip the #261 fixture from report-only to a HARD acceptance check: `t.Errorf` if Record T does NOT surface within default `k` for BOTH Query A and Query B, and `t.Errorf` if Record T's similarity score is not strictly greater than every seeded sticky-neighbor distractor's score (D-03 score separation). Set `wantScoreGap` to a defensible floor calibrated from the Plan 01 baseline (at minimum a positive margin; tighten to the observed post-fix separation).
    Run `ENGRAM_RETRIEVAL_EVAL=1 task eval:retrieval` (live gateway + Docker) and record the numbers: recall@k, MRR, per-query T rank, T-vs-distractor score gap — at or above the D-05a baseline. If the lexical-overlap lever alone does not clear the bar, iterate WITHIN D-06 first (add score-gap re-scoring / MMR / raise default `k`) before concluding D-06 is insufficient. Capture the final recorded result (PASS, or "D-06 insufficient — residual gap") for Task 3's decision.
  </action>
  <verify>
    <automated>go test ./internal/retrievaleval/...</automated>
  </verify>
  <acceptance_criteria>
    - The #261 fixture is now a hard `t.Errorf` bar (T within default `k` for A and B; T's score strictly above all seeded distractors), not `t.Logf`-only.
    - The eval's measured path calls the same `internal/store` reranker used by `deps.searchMemory` (grep: reranker symbol referenced in the eval source).
    - `go test ./internal/retrievaleval/...` with the gate unset still exits 0 (skip preserved).
    - The final `ENGRAM_RETRIEVAL_EVAL=1 task eval:retrieval` run is recorded (recall@k, MRR, #261 result) at or above the D-05a baseline; the recorded outcome is captured in the SUMMARY for Task 3.
  </acceptance_criteria>
  <done>
    The eval now enforces the #261 acceptance bar against the shipped rerank path, and a real numbers-backed result exists for the accept/escalate decision.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Eval-gated ranking decision — accept D-06 or escalate to D-07/D-08</name>
  <files>.planning/phases/09-retrieval-eval-harness-ranking-precision/09-03-SUMMARY.md</files>
  <action>
    Present Task 2's recorded eval numbers (recall@k, MRR, per-query #261 T rank,
    T-vs-distractor score gap) against the D-05a post-#262 baseline, then decide per
    D-05 whether the D-06 heuristic rerank clears the bar or escalation is warranted.
    Do NOT implement D-07 or D-08 here — selecting an escalate option spawns a
    follow-up plan. If accept-d06, confirm the #261 fixture is a green hard bar and
    record in the SUMMARY that hybrid/cross-encoder were evaluated and not needed.
  </action>
  <decision>Does the D-06 heuristic rerank clear the eval bar for REQ-ranking-precision, or must we escalate to D-07 (hybrid) and/or D-08 (cross-encoder)?</decision>
  <context>
    Per D-05, the eval numbers — not intuition — select the ranking approach. Task 2
    recorded recall@k/MRR and the #261 result (T within default `k` for A/B, score
    separation) against the post-#262 prod baseline (D-05a). Escalation to D-07 costs
    a breaking Qdrant schema change + full reindex; D-08 costs a new opt-in gateway
    round-trip and config surface. Neither should be undertaken unless the eval shows
    D-06 is insufficient. See <conditional_escalation> for the exact mechanics.
  </context>
  <options>
    <option id="accept-d06">
      <name>Accept D-06 — the eval bar is met</name>
      <pros>No schema change, no reindex, no new gateway; negligible latency; REQ-ranking-precision closed by the lightest lever. #261 fixture green as a permanent regression guard.</pros>
      <cons>None if the numbers clear the bar. Record that hybrid/cross-encoder were evaluated and NOT needed (honest #261 traceability).</cons>
    </option>
    <option id="escalate-d07">
      <name>Escalate to D-07 — hybrid dense+lexical fusion</name>
      <pros>Adds true lexical (BM25/IDF) signal via Qdrant RRF fusion; strongest for exact-term restatements the dense model still misses.</pros>
      <cons>Breaking, immutable collection schema change (unnamed → named dense+sparse) requiring a full `engram reindex --target` re-embed + re-tokenize + cutover. Becomes a follow-up plan; not implemented here.</cons>
    </option>
    <option id="escalate-d08">
      <name>Escalate to D-08 — cross-encoder reranker</name>
      <pros>Highest precision second-stage reranking when heuristics + hybrid still miss.</pros>
      <cons>Extra per-search gateway round-trip; sends FULL candidate content to an external endpoint; new opt-in ENGRAM_RERANK_* config; must stay off the default hot path unless the eval justifies default-on. Follow-up plan; not implemented here.</cons>
    </option>
  </options>
  <resume-signal>Select: accept-d06, escalate-d07, or escalate-d08 (escalation spawns a follow-up plan via /gsd-plan-phase 09 --gaps). If accept-d06, confirm the recorded eval numbers and #261 fixture are green.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| search request → in-process reranker | The D-06 reranker operates on already-owner-scoped, already-authorized `[]Memory` — strictly after the existing owner/scope filter; it reorders, never widens visibility. |
| (conditional D-07) reindex → live Qdrant collection | An operator-run backfill re-embeds/re-tokenizes into a NEW target collection, then cuts over — the existing reindex trust boundary. |
| (conditional D-08) reranker → external `/v1/rerank` gateway | Candidate record CONTENT leaves the process — same boundary as existing embed/summarize, broader payload. |

## STRIDE Threat Register (ASVS L1, block-on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-03-01 | Elevation of Privilege / Information Disclosure | D-06 reranker ordering | high | mitigate | Reranker runs strictly AFTER `ownerScopeFilter`; it only reorders/truncates an already-authorized result set and never re-queries or widens scope. Enforced by acceptance criterion (owner/scope filtering unchanged) + hermetic test on synthetic in-scope data. |
| T-09-03-02 | Tampering (scope integrity) | ranking inputs | medium | mitigate | Ranking consumes only content/lexical/vector signals; the plan prohibits reading any usage-signal field (Phase 12 constraint) so a future usage lane cannot silently influence ordering. |
| T-09-03-03 | Denial of Service | required CI `test` job | medium | mitigate | The D-06 reranker unit test is hermetic (in the required `test` job); the eval stays env-gated (no new required check). No skipped-required-workflow deadlock. |
| T-09-03-D07 | Tampering / Denial of Service | (conditional) reindex + schema change | medium | mitigate | ONLY if escalated: route through `engram reindex --target <new-collection>` with `--dry-run`, new collection + cutover (never in-place mutation of the live collection); operator-gated, follows the shipped reindex precedent. |
| T-09-03-D08 | Information Disclosure | (conditional) cross-encoder gateway call | high | mitigate | ONLY if escalated: opt-in, default-off `ENGRAM_RERANK_*`; document that full candidate content is sent to the external gateway (same trust boundary as embed/summarize). Rerank output only reorders already-authorized results — a malicious gateway cannot surface another actor's records (authz runs first). |
| T-09-03-SC | Tampering | supply chain | high | mitigate | D-06 adds NO dependency (stdlib only). D-08 (conditional) uses plain `net/http` — no new dep. Any proposed rerank/tokenizer LIBRARY is `[ASSUMED]` by default and MUST pass the Package Legitimacy Gate + a blocking human checkpoint before install (never auto-approved). |

V4 Access Control is the load-bearing category here and is preserved: all ranking
operates post-authorization. V2/V3/V6 N/A.
</threat_model>

<verification>
- `go test ./internal/store/ -run Rerank` passes hermetically in the required `test` job.
- `go test ./internal/retrievaleval/...` (gate unset) exits 0 — skip preserved.
- `ENGRAM_RETRIEVAL_EVAL=1 task eval:retrieval` recorded at/above the D-05a baseline; #261 fixture is a hard bar and green under the accepted option.
- `task` (lint + `go test ./...`) green; `task license:check` clean.
- The accept/escalate decision is recorded in the SUMMARY with the eval numbers.
</verification>

<success_criteria>
- REQ-ranking-precision: Query A/B (near-verbatim restatements) surface Record T within default `k`, proven by the eval, via the eval-selected approach (D-05) — D-06 first, D-07/D-08 only on eval evidence.
- The #261 fixture is a permanent green regression guard under the accepted approach.
- No usage signals influence ranking (Phase 12 boundary preserved).
- No new required CI status check; no speculative D-07/D-08 implementation.
</success_criteria>

<output>
Create `.planning/phases/09-retrieval-eval-harness-ranking-precision/09-03-SUMMARY.md` when done, recording the eval numbers and the accept/escalate decision.
</output>
