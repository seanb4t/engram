---
phase: 09-retrieval-eval-harness-ranking-precision
plan: 03
type: execute
wave: 2
depends_on:
  - 09-01
  - 09-02
files_modified:
  - internal/store/rerank.go
  - internal/store/rerank_test.go
  - internal/store/store.go
  - internal/server/tools.go
  - internal/server/connectapi.go
  - internal/retrievaleval/retrieval_eval_test.go
  - internal/retrievaleval/fixtures.go
autonomous: false
requirements:
  - REQ-ranking-precision

must_haves:
  truths:
    - "A dependency-free, in-process heuristic reranker (D-06) reorders search hits deterministically — lexical-overlap boost (and/or MMR / score-gap re-scoring), pure Go over already-fetched store.Memory results, negligible latency, no new dependency."
    - "The reranker is wired behind a SHARED exported store helper (e.g. store.SearchReranked) invoked by BOTH recall surfaces — MCP `deps.searchMemory` (tools.go) AND Connect `engramAPI.SearchMemories` (connectapi.go) — so neither recall path is left unfixed (review finding 2), and the eval calls the SAME helper so it measures exactly the shipped path (review finding 5)."
    - "The over-fetch is defined explicitly in the shared helper: `candidateK = min(max(k*4, 32), 100)` — never collapsing to no over-fetch, never unbounded (review finding 7). Store.Search's `Limit` is set to candidateK; the reranker promotes from that candidate pool, then the helper truncates to the caller's effective `k`."
    - "The #261 eval fixture is upgraded from report-only to a hard PASS/FAIL acceptance bar that is RANK-based: Record T surfaces within default `k` (position) for both Query A and Query B. Raw-score separation is a DIAGNOSTIC, not a hard gate — a lexical reranker can promote T's RANK without raising its raw Qdrant score above a sticky neighbor, so a hard raw-separation gate would reject a valid D-06 fix (review finding 3, reconciling D-03: score separation stays asserted/reported as the score-meaningfulness proof, but RANK is the acceptance bar)."
    - "No assertion assumes results stay score-descending after rerank — reranking changes order, so the post-rerank result set is NOT guaranteed non-increasing in raw score (review finding 3)."
    - "The ranking approach shipped is the one the eval numbers select (D-05): D-06 light levers first; hybrid (D-07) and cross-encoder (D-08) are escalated ONLY if the eval shows D-06 is insufficient. If strict raw-score separation is genuinely required and D-06 cannot deliver it, that is itself eval evidence D-06 is insufficient → escalate to D-07/D-08 (never redefine `score` semantics — review finding 3)."
    - "The eval-gated accept/escalate decision is made against recorded recall@k/MRR + the #261 RANK bar, at or above the post-#262 prod baseline (D-05a)."
  artifacts:
    - "internal/store/rerank.go — exported PURE heuristic reranker over []store.Memory (D-06), e.g. `RerankHits(query string, hits []Memory, k int) []Memory`."
    - "internal/store/store.go — a new SHARED exported search-with-rerank helper (e.g. `SearchReranked(ctx, scope, subj, query string, vec []float32, k uint64, tags, after, before) ([]Memory, error)`) that over-fetches candidateK, applies RerankHits, and truncates to k — the single path both handlers and the eval call (review finding 2/5/7)."
    - "internal/store/rerank_test.go — hermetic unit test (no Qdrant/gateway) proving deterministic reordering; runs in CI's required `test` job."
    - "Updated internal/server/tools.go `deps.searchMemory` — calls the shared SearchReranked helper (default-k lever if the eval selects it)."
    - "Updated internal/server/connectapi.go `engramAPI.SearchMemories` — calls the SAME shared SearchReranked helper (review finding 2)."
    - "Updated internal/retrievaleval eval (hard RANK-based #261 assertion + shared rerank helper in the measured path; raw-score gap reported as diagnostic)."
  key_links:
    - "MCP `deps.searchMemory` AND Connect `engramAPI.SearchMemories` → shared `store.SearchReranked` (over-fetch candidateK → RerankHits → truncate to k): the ONE shipped ranking path for both recall surfaces (review finding 2)."
    - "eval → same `store.SearchReranked` helper → recall@k/MRR + #261 RANK bar: the acceptance gate that selects the approach (D-05) and cannot drift from the handler (review finding 5)."
  prohibitions:
    - "Ranking MUST NOT introduce or depend on per-memory usage signals — usage signals are Phase 12 scope and must never affect ranking (explicit Phase 12 constraint). Only content / lexical / vector signals may influence ordering."
    - "Do NOT wire the reranker into MCP `searchMemory` only — Connect `SearchMemories` embeds and calls `Store.Search` directly (connectapi.go:153) and would be left unreranked; both surfaces MUST route through the shared helper (review finding 2)."
    - "Do NOT gate the #261 acceptance on raw-score separation (T's score above every neighbor) — the hard bar is RANK (T within default `k`); raw-score separation is diagnostic only (review finding 3). Do NOT redefine `score` to force separation."
    - "Do NOT assert the reranked result set is score-descending / non-increasing — rerank changes order (review finding 3)."
    - "Do NOT let over-fetch collapse to `Limit == k` (no over-fetch) or become unbounded — use `candidateK = min(max(k*4, 32), 100)` (review finding 7)."
    - "Do NOT unconditionally implement the D-07 sparse-vector reindex or the D-08 cross-encoder — both are gated on eval evidence (D-05). They are represented here as conditional escalations, NOT committed work; implementing them speculatively violates D-05/D-06."
    - "Do NOT add a new dependency for D-06 — the heuristic rerank is pure Go stdlib string/slice ops."
    - "Do NOT make the rerank asymmetric between query and document sides in a way that breaks determinism — the reranker is a pure function of (query, hits)."
    - "Do NOT add a new required CI status check; the eval remains env-gated (Plan 01's constraint carries forward)."
---

<objective>
Close REQ-ranking-precision using the eval as the decision mechanism (D-05).
The #261 failure is phrasing-sensitive: near-verbatim restatements (Query A/B) of
Record T fail to surface T within default `k` because sticky topical neighbors
crowd it out. Because near-verbatim restatements have high lexical overlap with
their target, the light lever most likely to fix this — and the one D-06 mandates
trying first — is an in-process, dependency-free heuristic rerank (lexical-overlap
boost / MMR / score-gap), optionally with a higher default `k`.

This plan (1) implements the D-06 heuristic reranker wired into both the real
search path and the eval, (2) upgrades the #261 fixture to a hard acceptance bar
and runs the eval to get real numbers, and (3) makes the eval-gated accept/escalate
decision. Hybrid dense+lexical fusion (D-07) and a cross-encoder reranker (D-08)
are IN SCOPE only if the eval shows D-06 is insufficient — they are represented
here as conditional escalations with their RESEARCH mechanics, not as committed
work (D-05: nothing added speculatively).

Purpose: eliminate phrasing-sensitive misses so search-before-store stops
producing duplicates (the #261 goal), proven by the eval not intuition.
Output: the D-06 reranker + the eval-selected ranking decision.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-retrieval-eval-harness-ranking-precision/09-CONTEXT.md
@.planning/phases/09-retrieval-eval-harness-ranking-precision/09-RESEARCH.md
@.planning/phases/09-retrieval-eval-harness-ranking-precision/09-PATTERNS.md
@.planning/phases/09-retrieval-eval-harness-ranking-precision/09-VALIDATION.md
@.planning/phases/09-retrieval-eval-harness-ranking-precision/09-01-SUMMARY.md
</context>

<conditional_escalation>
## D-07 / D-08 are conditional on eval evidence (D-05) — NOT committed in this plan

These are represented so the phase is honest about scope, but they are executed
ONLY if Task 3's eval-gated checkpoint decides D-06 is insufficient. If triggered,
each becomes a follow-up plan (e.g. `/gsd-plan-phase 09 --gaps` or a new
`09-04`/`09-05`), planned within its own context budget. Do not implement here.

**D-07 — Hybrid dense+lexical (BM25) fusion** (RESEARCH Pattern 3/4, Pitfall 2):
- Qdrant server-side `qdrant/bm25` `Document` inference is Cloud-Inference-only and
  unavailable on self-hosted/OSS Qdrant (the Helm-deployed backend) — do NOT use it.
- Instead: generate sparse vectors client-side in Go (deterministic tokenize →
  hash terms to indices → term-frequency values, SAME tokenizer document- and
  query-side) and let Qdrant's `Modifier_Idf` on `SparseVectorParams` compute IDF
  server-side. Query via `QueryPoints.Prefetch` (dense + sparse) fused by
  `qdrant.NewQueryFusion(qdrant.Fusion_RRF)`.
- Converting the current single UNNAMED dense vector to a named-vector map
  (`"dense"` + `"sparse"`) is a breaking, immutable schema change → requires the
  existing `engram reindex --target <new-collection>` flow (re-embed AND
  re-tokenize every record, `--dry-run`, cut over `ENGRAM_QDRANT_COLLECTION`), NOT
  an in-place mutation. Model operator ergonomics on `cmd/engram/reindex.go`.

**D-08 — Cross-encoder reranker** (RESEARCH Pattern 5):
- Opt-in only; off the default hot path unless the eval justifies default-on.
- A second-stage HTTP call to a Cohere/Jina-compatible `/v1/rerank` endpoint
  (vLLM-hosted cross-encoder), mirroring `internal/embed`'s `net/http` client — NO
  new Go dependency; plain stdlib.
- New opt-in `ENGRAM_RERANK_*` koanf fields in `internal/config/registry.go` (no
  Default = disabled when empty; no Legacy). Any proposed rerank client LIBRARY
  (instead of hand-rolled `net/http`) MUST pass the Package Legitimacy Gate + a
  blocking human checkpoint before adoption — none is pre-approved.
- Data-flow note: rerank sends the FULL candidate record content (not just the
  query) to the external gateway — same trust boundary as existing embed/summarize
  calls, but a broader payload; document this for operators choosing D-08.
</conditional_escalation>

<artifacts_this_phase_produces>
## Artifacts this plan produces (source-grounding for drift verification)

New paths (committed, D-06):
- `internal/store/rerank.go` (exported PURE heuristic reranker)
- `internal/store/rerank_test.go` (hermetic unit test)

Modified:
- `internal/store/store.go` (new shared exported search-with-rerank helper — over-fetch candidateK → RerankHits → truncate)
- `internal/server/tools.go` (`deps.searchMemory` calls the shared helper; default-k lever iff eval-selected)
- `internal/server/connectapi.go` (`engramAPI.SearchMemories` calls the SAME shared helper — review finding 2)
- `internal/retrievaleval/retrieval_eval_test.go`, `internal/retrievaleval/fixtures.go` (RANK-based #261 hard bar + shared rerank helper in measured path + raw-score gap reported as diagnostic)

New exported symbols (D-06 — referenced by BOTH handlers AND the eval, review finding 2/5):
- `internal/store` PURE reranker entrypoint — e.g. `RerankHits(query string, hits []Memory, k int) []Memory` (exact name at executor discretion).
- `internal/store` SHARED search-with-rerank helper — e.g. `SearchReranked(ctx context.Context, scope string, subj Subject, query string, vec []float32, k uint64, tags []string, after, before time.Time) ([]Memory, error)` that computes `candidateK = min(max(k*4, 32), 100)` (review finding 7), runs the existing owner/scope-filtered Query at candidateK, applies `RerankHits`, and truncates to `k`. Invoked by `deps.searchMemory` (MCP), `engramAPI.SearchMemories` (Connect), and the eval.

Both signatures are illustrative; the load-bearing contract is: ONE exported store helper, over-fetch bounded by the candidateK formula, called by all three sites.

Conditional (NOT produced unless Task 3 escalates — see <conditional_escalation>):
- D-07: `SparseVectorsConfig` in `ensureCollection`, sparse tokenizer, `Prefetch`+`Fusion` in `Store.Search`, reindex backfill in `cmd/engram/reindex.go`, new target collection.
- D-08: `ENGRAM_RERANK_BASE_URL` / `ENGRAM_RERANK_MODEL` (and siblings) koanf fields, a `/v1/rerank` net/http client.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Implement the D-06 reranker + shared over-fetch helper wired into BOTH MCP and Connect</name>
  <files>internal/store/rerank.go, internal/store/rerank_test.go, internal/store/store.go, internal/server/tools.go, internal/server/connectapi.go</files>
  <read_first>
    - internal/store/store.go (Memory struct incl. Content/Tags/Score ~L136-151; Store.Search ~L544-576 — note `Limit: qdrant.PtrOf(k)` passes k straight to Qdrant, so over-fetch must live in the new wrapper; `ownerScopeFilter` ~L562 runs BEFORE results return; memoriesFromPoints m.Score = p.Score ~L579-587 — proves Score is raw Qdrant similarity, unchanged by rerank)
    - internal/server/tools.go lines 704-728 (deps.searchMemory: default `if a.K == 0 { a.K = 8 }`, EmbedQuery → Store.Search → shapeRecall — swap Store.Search for the shared helper)
    - internal/server/connectapi.go lines 144-172 (engramAPI.SearchMemories: EmbedQuery → `a.d.st.Search(...)` with default `k = 20`, then shapeProtoMemories — the SECOND recall surface that MUST also route through the shared helper, review finding 2)
    - internal/summarize/fidelity_test.go (hermetic table-test style with no assertion library — the convention for rerank_test.go)
    - 09-RESEARCH.md "Don't Hand-Roll" (recall/MRR + rerank are the parts that genuinely SHOULD be hand-rolled) and D-06 in CONTEXT.md
  </read_first>
  <action>
    Add `internal/store/rerank.go`: an exported, PURE reranker (e.g. `RerankHits(query string, hits []Memory, k int) []Memory`) that takes the search query and the fetched `[]Memory` (each with `Score`, `Content`, `Tags`) and returns a deterministically reordered top-k slice. Implement at least a lexical-overlap boost (tokenize query and candidate content/tags the same way, up-weight candidates whose terms overlap the query — the direct lever for near-verbatim restatements) and structure it so score-gap re-scoring and/or MMR diversification can be layered per the eval. No new dependency: stdlib `strings`/`sort` only. Determinism: equal-scored ties break on a stable key (e.g. id) so output is reproducible.
    Add a SHARED exported search-with-rerank helper in `internal/store/store.go` (e.g. `SearchReranked`) that: computes `candidateK = min(max(k*4, 32), 100)` (review finding 7 — never `Limit == k`, never unbounded); runs the existing owner/scope-filtered vector Query at `candidateK` (reuse the exact filter construction in `Store.Search`; the reranker runs strictly AFTER `ownerScopeFilter`, never widening visibility); applies `RerankHits(query, hits, int(k))`; and returns the truncated top-`k`. This helper is the single ranking path.
    Wire BOTH recall surfaces to call it (review finding 2): in `deps.searchMemory` (`internal/server/tools.go`) replace the `d.st.Search(...)` call with the shared helper (passing `a.Query` as the query text); in `engramAPI.SearchMemories` (`internal/server/connectapi.go`) replace the `a.d.st.Search(...)` call with the SAME shared helper (passing `req.Msg.Query`). Keep each surface's existing default-k (MCP 8, Connect 20) intact for now; only raise a default (the D-06 "higher default k" lever) if Task 2's eval numbers select it. Do not read or consult any usage-signal field (Phase 12 constraint).
    Add `internal/store/rerank_test.go`: a hermetic unit test (no Qdrant, no gateway) that feeds synthetic `[]Memory` to `RerankHits` and asserts it promotes a high-lexical-overlap candidate above a topical-but-distinct one and is deterministic across runs.
  </action>
  <verify>
    <automated>go test ./internal/store/ -run Rerank && gofmt -l internal/store/rerank.go internal/store/rerank_test.go && go vet ./internal/store/... ./internal/server/...</automated>
  </verify>
  <acceptance_criteria>
    - `internal/store/rerank.go` exports a PURE reranker function over `[]Memory` using only stdlib (grep: no new import beyond `strings`/`sort`/stdlib).
    - `internal/store/store.go` exports a shared search-with-rerank helper computing `candidateK = min(max(k*4, 32), 100)` (grep: the formula, or an equivalent named constant set, is present) (review finding 7).
    - BOTH `deps.searchMemory` (tools.go) AND `engramAPI.SearchMemories` (connectapi.go) call the shared helper — neither calls `st.Search` directly for the ranked search path anymore (grep: the shared helper symbol appears in both files) (review finding 2).
    - `go test ./internal/store/ -run Rerank` passes hermetically (no `ENGRAM_*` gateway or Qdrant env required) and runs inside the required `test` job.
    - The reranker is deterministic: repeated calls on identical input yield identical order (test asserts this).
    - Owner/scope filtering is unchanged (the helper reuses `ownerScopeFilter`, reranks strictly after it); no usage-signal field is referenced.
    - `task lint` and `task license:check` pass on the new/changed files.
  </acceptance_criteria>
  <done>
    A dependency-free heuristic reranker + a shared bounded-over-fetch helper exist, are unit-tested in CI, and are on BOTH the MCP and Connect search paths — the D-06 lever with no recall surface left behind, ready to be measured by the eval.
  </done>
</task>

<task type="auto">
  <name>Task 2: Upgrade the #261 fixture to a hard acceptance bar, exercise the reranker in the eval, and run it</name>
  <files>internal/retrievaleval/retrieval_eval_test.go, internal/retrievaleval/fixtures.go</files>
  <read_first>
    - internal/retrievaleval/retrieval_eval_test.go (the measured path from Plan 01 — where the shared helper replaces the raw Store.Search call so the eval measures the post-fix ordering)
    - internal/retrievaleval/fixtures.go (retrievalCase.wantScoreGap — now a DIAGNOSTIC threshold reported, not a hard gate, per review finding 3)
    - internal/store/store.go (Task 1's shared `SearchReranked` helper the eval will call — the exact shipped path, review finding 2/5) and internal/store/rerank.go (RerankHits)
    - .planning/phases/09-retrieval-eval-harness-ranking-precision/09-01-SUMMARY.md (the recorded post-#262 baseline: recall@k/MRR and the #261 target PRE-FIX ranks to improve against — D-05a)
  </read_first>
  <action>
    Update the eval's measured path so it calls the SAME shared `internal/store` `SearchReranked` helper the handlers call (over-fetch candidateK → rerank → truncate to default `k`) instead of raw `Store.Search` — so the eval measures exactly the shipped path and cannot drift (review finding 2/5). Drop any assertion that the reranked result set is score-descending / non-increasing — rerank changes order (review finding 3).
    Flip the #261 fixture from report-only to a HARD, RANK-based acceptance check: `t.Errorf` if Record T does NOT surface within default `k` (by POSITION) for BOTH Query A and Query B — this is the acceptance bar (review finding 3/4). Do NOT `t.Errorf` on raw-score separation: compute and `t.Logf` Record T's raw score versus the best distractor's score as a DIAGNOSTIC (keep `wantScoreGap` as a reported threshold, not a hard gate). Reconcile D-03: score separation stays ASSERTED as reported diagnostic evidence the score is meaningful, but a lexical reranker can promote T's rank without raising its raw Qdrant score, so RANK is the pass/fail bar. If T reaches within default `k` but strict raw separation is genuinely required by a consumer and D-06 cannot deliver it, treat that as eval evidence D-06 is insufficient → carry it to the Task 3 escalation decision (do NOT redefine `score`).
    Run `ENGRAM_RETRIEVAL_EVAL=1 task eval:retrieval` (live gateway + Docker) and record the numbers: recall@k, MRR, per-query T rank (pre- and post-fix), T-vs-distractor score gap (diagnostic) — at or above the D-05a baseline. If the lexical-overlap lever alone does not clear the RANK bar, iterate WITHIN D-06 first (add score-gap re-scoring / MMR / raise default `k`) before concluding D-06 is insufficient. Capture the final recorded result (PASS, or "D-06 insufficient — residual gap") for Task 3's decision.
  </action>
  <verify>
    <automated>go test ./internal/retrievaleval/...</automated>
  </verify>
  <acceptance_criteria>
    - The #261 fixture is now a hard `t.Errorf` bar that is RANK-based (T within default `k` by position for BOTH A and B), not `t.Logf`-only (review finding 3/4).
    - Raw-score separation is reported via `t.Logf` as a diagnostic; there is NO `t.Errorf` gating on T's raw score exceeding neighbors (review finding 3).
    - No assertion in the reranked measured path asserts non-increasing / score-descending order (review finding 3).
    - The eval's measured path calls the SAME shared `internal/store` helper used by `deps.searchMemory` AND `engramAPI.SearchMemories` (grep: the shared helper symbol referenced in the eval source) (review finding 2/5).
    - `go test ./internal/retrievaleval/...` with the gate unset still exits 0 (skip preserved).
    - The final `ENGRAM_RETRIEVAL_EVAL=1 task eval:retrieval` run is recorded (recall@k, MRR, per-query T rank, diagnostic score gap) at or above the D-05a baseline; the recorded outcome is captured in the SUMMARY for Task 3.
  </acceptance_criteria>
  <done>
    The eval now enforces the #261 acceptance bar against the shipped rerank path, and a real numbers-backed result exists for the accept/escalate decision.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Eval-gated ranking decision — accept D-06 or escalate to D-07/D-08</name>
  <files>.planning/phases/09-retrieval-eval-harness-ranking-precision/09-03-SUMMARY.md</files>
  <action>
    Present Task 2's recorded eval numbers (recall@k, MRR, per-query #261 T rank,
    T-vs-distractor score gap) against the D-05a post-#262 baseline, then decide per
    D-05 whether the D-06 heuristic rerank clears the bar or escalation is warranted.
    Do NOT implement D-07 or D-08 here — selecting an escalate option spawns a
    follow-up plan. If accept-d06, confirm the #261 fixture is a green hard bar and
    record in the SUMMARY that hybrid/cross-encoder were evaluated and not needed.
  </action>
  <decision>Does the D-06 heuristic rerank clear the eval bar for REQ-ranking-precision, or must we escalate to D-07 (hybrid) and/or D-08 (cross-encoder)?</decision>
  <context>
    Per D-05, the eval numbers — not intuition — select the ranking approach. Task 2
    recorded recall@k/MRR and the #261 result against the post-#262 prod baseline
    (D-05a). The hard acceptance bar is RANK (T within default `k` by position for
    A/B); raw-score separation is a DIAGNOSTIC (review finding 3). Two escalation
    triggers: (1) T does not reach within default `k` under D-06; OR (2) T reaches
    within `k` but a consumer genuinely requires strict raw-score separation that the
    order-only reranker cannot produce — either is eval evidence D-06 is insufficient
    (do NOT redefine `score`). Both recall surfaces (MCP + Connect) already route
    through the shared helper (Task 1), so an accepted D-06 fix covers both; an
    escalation likewise lands behind the same shared helper. Escalation to D-07 costs
    a breaking Qdrant schema change + full reindex; D-08 costs a new opt-in gateway
    round-trip and config surface. See <conditional_escalation> for the exact mechanics.
  </context>
  <options>
    <option id="accept-d06">
      <name>Accept D-06 — the eval RANK bar is met</name>
      <pros>No schema change, no reindex, no new gateway; negligible latency; REQ-ranking-precision closed by the lightest lever on BOTH recall surfaces (MCP + Connect via the shared helper). #261 fixture green (rank-based) as a permanent regression guard.</pros>
      <cons>None if T reaches within default `k` for A and B. Raw-score separation may remain a diagnostic (not guaranteed by an order-only reranker) — that is acceptable unless a consumer requires strict separation (which would itself be an escalation trigger). Record that hybrid/cross-encoder were evaluated and NOT needed (honest #261 traceability).</cons>
    </option>
    <option id="escalate-d07">
      <name>Escalate to D-07 — hybrid dense+lexical fusion</name>
      <pros>Adds true lexical (BM25/IDF) signal via Qdrant RRF fusion; strongest for exact-term restatements the dense model still misses.</pros>
      <cons>Breaking, immutable collection schema change (unnamed → named dense+sparse) requiring a full `engram reindex --target` re-embed + re-tokenize + cutover. Becomes a follow-up plan; not implemented here.</cons>
    </option>
    <option id="escalate-d08">
      <name>Escalate to D-08 — cross-encoder reranker</name>
      <pros>Highest precision second-stage reranking when heuristics + hybrid still miss.</pros>
      <cons>Extra per-search gateway round-trip; sends FULL candidate content to an external endpoint; new opt-in ENGRAM_RERANK_* config; must stay off the default hot path unless the eval justifies default-on. Follow-up plan; not implemented here.</cons>
    </option>
  </options>
  <resume-signal>Select: accept-d06, escalate-d07, or escalate-d08 (escalation spawns a follow-up plan via /gsd-plan-phase 09 --gaps). If accept-d06, confirm the recorded eval numbers and #261 fixture are green.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP + Connect search request → shared in-process reranker | BOTH `deps.searchMemory` and `engramAPI.SearchMemories` route through the shared `SearchReranked` helper, which runs the SAME `ownerScopeFilter`-constrained Query (store.go:562) and reranks strictly AFTER results return — for both surfaces it reorders already-owner-scoped, already-authorized `[]Memory` and never widens visibility (review finding 2; auth posture unchanged for both recall paths). |
| (conditional D-07) reindex → live Qdrant collection | An operator-run backfill re-embeds/re-tokenizes into a NEW target collection, then cuts over — the existing reindex trust boundary. |
| (conditional D-08) reranker → external `/v1/rerank` gateway | Candidate record CONTENT leaves the process — same boundary as existing embed/summarize, broader payload. |

## STRIDE Threat Register (ASVS L1, block-on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-03-01 | Elevation of Privilege / Information Disclosure | shared D-06 reranker ordering (MCP + Connect) | high | mitigate | The shared `SearchReranked` helper runs strictly AFTER `ownerScopeFilter` (store.go:562) for BOTH recall surfaces; it only reorders/truncates an already-authorized result set and never re-queries or widens scope. Over-fetch raises the candidate `Limit` (candidateK) within the SAME owner/scope filter, so it cannot surface out-of-scope records. Enforced by acceptance criterion (owner/scope filtering unchanged; both handlers call the shared helper) + hermetic test on synthetic in-scope data. |
| T-09-03-02 | Tampering (scope integrity) | ranking inputs | medium | mitigate | Ranking consumes only content/lexical/vector signals; the plan prohibits reading any usage-signal field (Phase 12 constraint) so a future usage lane cannot silently influence ordering. |
| T-09-03-03 | Denial of Service | required CI `test` job | medium | mitigate | The D-06 reranker unit test is hermetic (in the required `test` job); the eval stays env-gated (no new required check). No skipped-required-workflow deadlock. |
| T-09-03-D07 | Tampering / Denial of Service | (conditional) reindex + schema change | medium | mitigate | ONLY if escalated: route through `engram reindex --target <new-collection>` with `--dry-run`, new collection + cutover (never in-place mutation of the live collection); operator-gated, follows the shipped reindex precedent. |
| T-09-03-D08 | Information Disclosure | (conditional) cross-encoder gateway call | high | mitigate | ONLY if escalated: opt-in, default-off `ENGRAM_RERANK_*`; document that full candidate content is sent to the external gateway (same trust boundary as embed/summarize). Rerank output only reorders already-authorized results — a malicious gateway cannot surface another actor's records (authz runs first). |
| T-09-03-SC | Tampering | supply chain | high | mitigate | D-06 adds NO dependency (stdlib only). D-08 (conditional) uses plain `net/http` — no new dep. Any proposed rerank/tokenizer LIBRARY is `[ASSUMED]` by default and MUST pass the Package Legitimacy Gate + a blocking human checkpoint before install (never auto-approved). |

V4 Access Control is the load-bearing category here and is preserved: all ranking
operates post-authorization. V2/V3/V6 N/A.
</threat_model>

<review_disposition>
## Codex review findings addressed in this plan (09-03)

| Finding | Severity | Disposition | Where |
|---------|----------|-------------|-------|
| 2 — Connect recall surface left unfixed | HIGH | INCORPORATED (shared helper) | frontmatter `files_modified` (+connectapi.go, +store.go); Task 1 wires BOTH `deps.searchMemory` AND `engramAPI.SearchMemories` to the shared `SearchReranked` helper; `must_haves` truth #2, key_links, prohibition; threat T-09-03-01 + trust boundary. |
| 3 — raw-score-separation assertion conflicts with rerank | HIGH | INCORPORATED (reframed to RANK) | Task 2 hard bar is RANK (T within default `k` by position); raw-score gap is a `t.Logf` DIAGNOSTIC; score-descending assertion dropped post-rerank; strict-separation-required → escalation evidence (not `score` redefinition). `must_haves` truths #4/#5/#6, prohibitions. Reconciles locked D-03 (separation still asserted as reported evidence). |
| 4 — #261 fixture floor too weak (shared with 09-01) | HIGH | INCORPORATED | 09-01 seeds `≥ defaultK+1` distractors + records pre-fix miss/rank; this plan's RANK bar proves promotion from outside default `k` to inside. |
| 5 — package boundary / drift | MEDIUM | INCORPORATED | The eval calls the SAME exported shared helper the handlers call (Task 2 acceptance grep) — no drift; eval stays `package retrievaleval`. |
| 7 — over-fetch underspecified | MEDIUM | INCORPORATED | `candidateK = min(max(k*4, 32), 100)` defined in the shared helper (Task 1 action + acceptance + `must_haves` truth #3 + prohibition). |
</review_disposition>

<verification>
- `go test ./internal/store/ -run Rerank` passes hermetically in the required `test` job.
- `go test ./internal/retrievaleval/...` (gate unset) exits 0 — skip preserved.
- Both `deps.searchMemory` (tools.go) and `engramAPI.SearchMemories` (connectapi.go) call the shared `SearchReranked` helper (grep) — no recall surface left unreranked (review finding 2).
- `ENGRAM_RETRIEVAL_EVAL=1 task eval:retrieval` recorded at/above the D-05a baseline; the #261 fixture is a hard RANK-based bar and green under the accepted option; raw-score gap reported as diagnostic (review finding 3).
- `task` (lint + `go test ./...`) green; `task license:check` clean.
- The accept/escalate decision is recorded in the SUMMARY with the eval numbers.
</verification>

<success_criteria>
- REQ-ranking-precision: Query A/B (near-verbatim restatements) surface Record T within default `k` (by RANK) on BOTH the MCP and Connect recall paths, proven by the eval, via the eval-selected approach (D-05) — D-06 first, D-07/D-08 only on eval evidence.
- The #261 fixture is a permanent green (rank-based) regression guard under the accepted approach.
- No usage signals influence ranking (Phase 12 boundary preserved).
- No new required CI status check; no speculative D-07/D-08 implementation.
</success_criteria>

<output>
Create `.planning/phases/09-retrieval-eval-harness-ranking-precision/09-03-SUMMARY.md` when done, recording the eval numbers and the accept/escalate decision.
</output>
