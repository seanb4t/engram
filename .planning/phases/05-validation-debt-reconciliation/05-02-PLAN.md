---
phase: 05-validation-debt-reconciliation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/retrievaleval/retrieval_eval_test.go
  - docs-site/src/content/docs/guides/embedding-instructions.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [REQ-citation-fixture-355, REQ-nyquist-reconciled]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`internal/retrievaleval/retrieval_eval_test.go` cites `deps.searchMemory`'s `a.K = 8` default and `store.EmbedText` by symbol name, and carries no `tools.go` line-number anchor that can drift again (#355 IN-01)"
    - "The OpenRouter row in `docs-site/src/content/docs/guides/embedding-instructions.md` links to the guide that actually carries the referenced row instead of pointing at a row that is not on that page (#355 IN-02)"
    - "`REQ-citation-fixture-355` no longer claims the repair exercises `spine-review verify`, a claim that was never true because `verify` reads stored `Citation.Excerpt` payloads out of Qdrant and cannot see a Go comment or a docs cross-ref (D-05)"
    - "`REQ-nyquist-reconciled` and the ROADMAP Phase 5 entry no longer assert the disproven inherited-debt premise; they state what the live re-resolution actually found (D-06)"
    - "The package still compiles after the comment edit — `go vet ./internal/retrievaleval/...` is clean, so the citation repair did not become a code change"
    - "`.planning/ROADMAP.md` gained no new heading, section or block shape; only the text of prose GSD already writes was changed (repo rule `8dfdhfs5nn`)"
  artifacts:
    - path: "internal/retrievaleval/retrieval_eval_test.go"
      provides: "Symbol-named citations for the production defaults the eval mirrors"
      contains: "store.EmbedText"
    - path: "docs-site/src/content/docs/guides/embedding-instructions.md"
      provides: "OpenRouter row cross-referencing the embedding-models guide"
      contains: "/guides/embedding-models/"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Corrected Validation Debt requirement text"
      contains: "REQ-nyquist-reconciled"
    - path: ".planning/ROADMAP.md"
      provides: "Corrected Phase 5 goal, dependency line and success criteria"
      contains: "Phase 5: Validation Debt Reconciliation"
  key_links:
    - from: "internal/retrievaleval/retrieval_eval_test.go"
      to: "internal/server/tools.go"
      via: "the comment names the production default the eval mirrors, by symbol rather than by line"
      pattern: "deps\\.searchMemory"
    - from: "internal/retrievaleval/retrieval_eval_test.go"
      to: "internal/store/store.go"
      via: "the comment names the doc-embed helper the eval reproduces, by symbol rather than by line"
      pattern: "store\\.EmbedText"
    - from: "docs-site/src/content/docs/guides/embedding-instructions.md"
      to: "docs-site/src/content/docs/guides/embedding-models.md"
      via: "OpenRouter row links to the guide that carries the referenced row"
      pattern: "/guides/embedding-models/"
  prohibitions:
    - requirement_id: REQ-citation-fixture-355
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT replace a drifted line-number citation with a fresh line-number citation. Re-anchoring to today's line numbers reproduces the defect on the next refactor; the repair is to cite the symbol, which is what makes the reference survive movement"
      resolution: null
      reason: null
    - requirement_id: REQ-nyquist-reconciled
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT edit the shipped v0.12.x milestone summary line, the Progress table's status or count columns, or any other ROADMAP bookkeeping outside Phase 5's own entry. The Progress-table drift was seen and explicitly deferred by the user; correcting a shipped milestone's record of what was true when it shipped is rewriting history, not reconciling it"
      resolution: null
      reason: null
    - requirement_id: REQ-nyquist-reconciled
      category: values
      status: resolved
      verification: judgment
      statement: "MUST NOT soften the corrected requirement text into something vaguer than what was found. The correction replaces a false specific with a true specific — it does not turn a checkable claim into an unfalsifiable one"
      resolution: null
      reason: null
---

<objective>
Fix #355's two drifted anchors as the plain documentation change they are, and correct the two
requirement statements this phase proved false.

Purpose: a citation that names a line number is a claim with an expiry date; a requirement built
on a premise nobody re-checked is the same defect one level up. Both are in scope precisely
because this phase is about records that stopped being true.
Output: two source/docs edits and two planning-text corrections. No behaviour change — nothing
here can affect the binary.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-validation-debt-reconciliation/05-CONTEXT.md
</context>

<flagged_assumptions>
The deterministic edge probe returned six items for this phase, all `unresolved`. None is silently
dropped; none is auto-resolved with a backstop. Each is surfaced here as an explicit assumption.
Two belong to `REQ-citation-fixture-355` and are recorded below; the other four belong to
`REQ-nyquist-reconciled` and are recorded in `05-01-PLAN.md`.

- **empty / single-element / null input.** Inapplicable as posed: this requirement changes three
  comment and prose strings in two files. There is no input, no collection and no call site — the
  "empty case" would have to be the absence of the files themselves, which the acceptance criteria
  already cover by asserting a positive match in each. **Assumed:** no empty-input behaviour is
  specified or needed. Not converted into a predicate, because inventing one would assert
  something the requirement does not claim.
- **encoding — whose definition of length or equality applies.** Genuinely touches this work,
  though not in the way the probe means. The prose being edited contains non-ASCII characters
  (an em dash in the docs table row, and the eval comment sits in a file `task fmt` has previously
  rewritten a straight quote into a curly one inside a Go comment). **Assumed:** byte-level
  equality after the edit, verified by `git diff` review and `gofmt -l` rather than by any
  normalisation rule. Task 1 carries an explicit instruction to diff after formatting for exactly
  this reason.
</flagged_assumptions>

<tasks>

<!-- planner-discipline-allow: tools.go: -->
<!-- planner-discipline-allow: see its row above -->
<!-- planner-discipline-allow: calibrat -->
<!-- planner-discipline-allow: six at `status: draft` -->

<task type="auto">
  <name>Task 1: Repair #355's two drifted anchors (IN-01 and IN-02)</name>
  <files>internal/retrievaleval/retrieval_eval_test.go, docs-site/src/content/docs/guides/embedding-instructions.md</files>
  <read_first>
    - `internal/retrievaleval/retrieval_eval_test.go` lines 20-30 and 88-100 (both comments being edited, in place)
    - `docs-site/src/content/docs/guides/embedding-instructions.md` lines 95-115 (the params table containing the OpenRouter row)
    - `internal/server/tools.go` around line 1617 and `internal/store/store.go` around line 333 (confirm the two symbols the comments will name still exist under those names)
  </read_first>
  <behavior>
    - The eval's two comments name the production symbols they mirror, with no numeric file anchor
    - The OpenRouter row resolves to a page that actually carries the row it refers to
    - The package still compiles and is gofmt-clean
  </behavior>
  <action>
    Implements D-04: #355 repaired as the plain docs fix it is, following the issue's own
    prescription. Nothing about `verify` is exercised here (D-05).

    IN-01, two comments in `internal/retrievaleval/retrieval_eval_test.go`.

    The comment at line 23-24 introduces `defaultK` and currently anchors the production default
    to a `tools.go` line number. That anchor has drifted: the `a.K = 8` assignment now sits at
    `internal/server/tools.go:1617`. Rewrite the comment to name the symbol instead — it mirrors
    the `a.K = 8` default inside `deps.searchMemory` — and remove the numeric anchor entirely. Do
    not substitute the current line number; a fresh number re-arms the same trap on the next
    refactor, which is the whole point of the issue's own prescribed fix.

    The comment at line 94-95 documents the doc-embed sequence and anchors it to a `tools.go` line
    range. That range now holds an unrelated struct. Rewrite it to name `store.EmbedText` as the
    helper the sequence reproduces, and keep the substantive claim the comment is making: the
    sequence folds tags in via `store.EmbedText`, then embeds, then upserts the precomputed vector,
    never a bespoke shortcut. Preserve the existing `round-2 finding 2` provenance note. Remove the
    numeric anchor.

    IN-02, one table row in `docs-site/src/content/docs/guides/embedding-instructions.md:106`. The
    OpenRouter row currently defers to a row on the same page that is not there — the OpenRouter
    row it means lives on `embedding-models.md`. Replace that trailing deferral with an explicit
    markdown link reading `[Embedding model recipes](/guides/embedding-models/)`, keeping the rest
    of the cell's wording (OpenRouter forwards whichever field name and value the backend model
    expects). Keep the em dash and the surrounding table formatting byte-identical apart from the
    replaced clause; `/guides/embedding-models/` already appears twice elsewhere in this file, so
    the new link must be inside the OpenRouter row for the check to mean anything.

    Formatting gotcha, and it applies directly here. `task fmt` has been observed rewriting a
    straight quote into a curly quote inside a Go comment in this repo, and CI's `gofmt -l .` job
    already fails on pre-existing drift in `cmd/engram/client_list_test.go` and
    `cmd/engram/migrate_test.go`. If you run `task fmt`, `git diff` before committing and revert
    any change outside these two files. Do not "fix" the two pre-existing offenders; they are not
    in this phase's scope.
  </action>
  <verify>
    <automated>T=internal/retrievaleval/retrieval_eval_test.go; E=docs-site/src/content/docs/guides/embedding-instructions.md; rg -q 'deps\.searchMemory' "$T" && rg -q 'store\.EmbedText' "$T" && ! rg -q 'tools\.go:[0-9]' "$T" && rg -q 'OpenRouter.*\(/guides/embedding-models/\)' "$E" && ! rg -q 'see its row above' "$E" && go vet ./internal/retrievaleval/... && test -z "$(gofmt -l internal/retrievaleval/)"</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c 'tools\.go:[0-9]' internal/retrievaleval/retrieval_eval_test.go` reports no match
    - The same file matches both `deps\.searchMemory` and `store\.EmbedText` — the paired positive proving the negative above is not vacuous
    - `rg -c 'OpenRouter.*\(/guides/embedding-models/\)' docs-site/src/content/docs/guides/embedding-instructions.md` reports exactly 1
    - `rg -c 'see its row above' docs-site/src/content/docs/guides/embedding-instructions.md` reports no match
    - `go vet ./internal/retrievaleval/...` exits 0
    - `gofmt -l internal/retrievaleval/` prints nothing
    - `git diff --stat -- cmd/engram/` is empty (no collateral formatting churn)
  </acceptance_criteria>
  <done>Both #355 anchors cite something that cannot silently move, the package compiles, and no unrelated file was reformatted.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the two requirement statements this phase disproved (D-05, D-06)</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 160-175 (the `### Validation Debt` section being rewritten)
    - `.planning/ROADMAP.md` line 186 (the Phases-checklist entry) and lines 506-537 (the Phase 5 detail block)
    - `.planning/phases/05-validation-debt-reconciliation/05-CONTEXT.md` (D-05, D-06, and the `<deferred>` block naming what is explicitly NOT folded in)
  </read_first>
  <behavior>
    - Both requirement statements assert only things that are true at HEAD
    - The corrected text is as specific as the text it replaces — a true specific, not a vague generality
    - No structural change lands in either file
  </behavior>
  <action>
    Implements D-06 (correct the disproven inherited-debt premise, in both files) and D-05 (drop
    the retired `verify` clause and the matching half of SC-3). No mechanism, gate or report
    artifact comes out of this task either (D-09, D-11), and nothing under
    `.planning/milestones/**` is touched (D-10).

    Use `Edit` with scoped replacements throughout. Never `Write` either of these files: a
    whole-file write destroys every entry outside the diff window, and both are parsed by
    scope-sensitive readers. Add no heading, no section and no new block shape — in particular no
    version-bearing or check-marked `###` heading in ROADMAP.md, which truncates the active
    milestone's scope for `extractCurrentMilestone` and silently drops phases (repo rule
    `8dfdhfs5nn`). Changing the text of prose GSD already writes is a value fix and is permitted;
    everything else is not.

    `.planning/REQUIREMENTS.md`, `### Validation Debt` (lines 164-170). Replace
    `REQ-nyquist-reconciled`'s body. Its current premise — six inherited drafts plus one phase with
    no file — was disproven by a live re-resolution: that debt was already cleared on the v0.12.x
    branch after the milestone audit was written and before the squash merge. The replacement
    asserts what is now required and true: every v0.13.x `VALIDATION.md` row is re-resolved against
    `go test -list` with a nonzero match count per pattern element rather than being re-run and
    checked for exit 0, and each phase's frontmatter states what that resolution found, leaving a
    genuinely unproven requirement visibly unproven. Record the disproven premise in the same
    entry, with the number: 89 of 90 real rows resolve at that milestone's own merge commit
    `906a5cf6`, and the single miss is v0.12.x Phase 7's own already-documented `ScopeGuard` drift,
    left as-is because archived milestone artifacts are immutable.

    Then replace `REQ-citation-fixture-355`'s body. Keep the repair clause and drop the trailing
    clause about the repair exercising `verify`'s false-positive rate. State the repair concretely:
    the drifted `tools.go` citation anchors are repaired by citing symbol names instead of line
    numbers, and the dangling cross-ref points at the guide that carries the referenced row. Add
    one sentence recording why the dropped clause went: `spine-review verify` reads stored
    `Citation.Excerpt` payloads out of Qdrant via `EnumerateCitations`
    (`internal/store/spine.go:296-360`), so it cannot see a Go source comment or a docs cross-ref,
    and the premise was never true. Do not use any word beginning `calibrat` in the replacement
    text — the point is that the claim is retired, and leaving the word behind leaves the claim
    findable.

    `.planning/ROADMAP.md`, exactly four edits, all inside Phase 5's own text:

    Line 186, the `## Phases` checklist entry. Keep the leading `- [ ] **Phase 5: Validation Debt
    Reconciliation** - ` prefix byte-identical and replace only the description after it, so it
    describes this milestone's own five phases being re-resolved against `go test -list` with each
    record stating what it found, and #355's drifted citations repaired as the plain docs fix they
    are.

    Lines 510-514, the `**Goal:**` paragraph. Rewrite it to the same corrected claim: every
    v0.13.x phase's validation record reflects tests that actually run rather than a stale false
    green, with the one requirement that was never proven left visibly unproven. Drop the sentence
    explaining that the phase runs last so #355 can serve as a live acceptance fixture.

    Lines 518-520, the `**Depends on:**` line. The Phase 3 dependency existed only to give #355
    something to exercise, and that premise did not hold. Replace it with a statement that the
    phase has no technical dependency on Phases 1-4, noting in one clause why the original ordering
    rationale was retired.

    Lines 522-533, the three success criteria. SC-1 becomes: the v0.12.x premise is corrected to
    what a live re-resolution found — that debt was already cleared before the squash merge — and
    archived milestone artifacts are not edited. SC-2 becomes: every v0.13.x phase (1-4) has its
    `VALIDATION.md` rows re-resolved against `go test -list` with a nonzero match count per
    element, and its frontmatter set to what that found, including leaving Phase 4 partial because
    `REQ-consent-adversarial-proof` is genuinely unproven. SC-3 becomes the #355 repair only, with
    the over-flagging half dropped for the same reason as D-05.

    Leave `**Requirements:**` and `**Plans:**` alone — the plan list was already filled in when
    these plans were written.

    Out of bounds, deliberately. Do not touch the Progress table's rows, statuses or counts; its
    drift was seen during the discussion and explicitly deferred. Do not touch the shipped v0.12.x
    milestone summary line, which records what was true when that milestone shipped. Do not touch
    the v0.13.x milestone summary line that still mentions carrying the debt forward — same class,
    same deferral. Note them in the SUMMARY as observed-and-not-folded rather than fixing them.

    Do not add a licence or SPDX header to either file (repo rule `2rjnv8sc9a`).
  </action>
  <verify>
    <automated>! rg -q 'six at .status: draft.' .planning/REQUIREMENTS.md .planning/ROADMAP.md && ! rg -q 'calibrat' .planning/REQUIREMENTS.md .planning/ROADMAP.md && rg -q 'REQ-nyquist-reconciled' .planning/REQUIREMENTS.md && rg -q 'REQ-citation-fixture-355' .planning/REQUIREMENTS.md && rg -q '^### Phase 5: Validation Debt Reconciliation$' .planning/ROADMAP.md && test "$(rg -c '^### Phase ' .planning/ROADMAP.md)" = "$(git show HEAD:.planning/ROADMAP.md | rg -c '^### Phase ')"</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c 'six at .status: draft.' .planning/REQUIREMENTS.md .planning/ROADMAP.md` reports no match
    - `rg -c 'calibrat' .planning/REQUIREMENTS.md .planning/ROADMAP.md` reports no match
    - `rg -c 'REQ-nyquist-reconciled' .planning/REQUIREMENTS.md` and `rg -c 'REQ-citation-fixture-355' .planning/REQUIREMENTS.md` each report at least 1 — the paired positives proving the two negatives above are not vacuous
    - `rg -c '^### Phase ' .planning/ROADMAP.md` equals the count in `git show HEAD:.planning/ROADMAP.md` — no heading was added or removed
    - `git diff .planning/ROADMAP.md` touches no line inside the Progress table and no milestone summary line
    - `node gsd-core/bin/gsd-tools.cjs query roadmap.validate` (or the installed equivalent) reports no new warnings
    - The v0.12.x shipped-milestone summary line is byte-identical to HEAD
  </acceptance_criteria>
  <done>Both requirement statements say only what is true, ROADMAP gained no structure, and the deferred bookkeeping drift is recorded as observed rather than quietly fixed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning artifact → GSD parser | `ROADMAP.md` is read by scope-sensitive parsers; an invented heading changes what the tool sees with no error and no warning |
| published docs → operator | `embedding-instructions.md` is user-facing configuration guidance; a wrong cross-ref sends an operator to a page that does not answer the question |

No runtime trust boundary is crossed. The only source edit is a comment inside a test file, so
nothing here reaches the binary. No package-manager install task exists, so RESEARCH.md's Package
Legitimacy Audit is not required.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05 | Tampering | `.planning/ROADMAP.md` structure | high | mitigate | Scoped `Edit` only, never `Write`; acceptance criteria assert the `### Phase ` heading count is unchanged from HEAD and that no Progress-table or milestone-summary line moved |
| T-05-06 | Tampering | `internal/retrievaleval/retrieval_eval_test.go` | medium | mitigate | The edit is comment-only; `go vet` and `gofmt -l` are asserted clean, and `git diff --stat -- cmd/engram/` is asserted empty to catch `task fmt` collateral |
| T-05-07 | Information disclosure | `docs-site` guidance | low | accept | The edited row names a public provider and links a public guide page |
| T-05-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task in this plan; nothing to audit |
</threat_model>

<verification>
- `go vet ./internal/retrievaleval/...` clean and `gofmt -l internal/retrievaleval/` empty
- No `tools.go` numeric anchor remains in the eval, and both replacement symbols are present
- The OpenRouter row links to `/guides/embedding-models/` and defers to nothing on its own page
- Neither `REQUIREMENTS.md` nor `ROADMAP.md` still asserts the disproven premise or the retired verify claim
- `ROADMAP.md`'s `### Phase ` heading count is unchanged; the Progress table and both milestone summary lines are byte-identical to HEAD
- `task` (lint + test) is green
</verification>

<success_criteria>
#355's anchors cite symbols rather than line numbers, its cross-ref resolves, and the two
requirement statements this phase disproved now say what is true — with no structural change to
either planning artifact and no behaviour change to the binary.
</success_criteria>

<output>
Create `.planning/phases/05-validation-debt-reconciliation/05-02-SUMMARY.md` when done
</output>
