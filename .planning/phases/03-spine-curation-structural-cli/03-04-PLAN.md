---
phase: 03-spine-curation-structural-cli
plan: 04
type: execute
wave: 4
depends_on: ["03-03"]
files_modified:
  - cmd/engram/spine_review_verify.go
  - cmd/engram/spine_review_verify_test.go
  - internal/store/spine.go
  - internal/store/spine_test.go
  - internal/surfaces/rules.go
  - internal/surfaces/rules_test.go
  - internal/surfaces/toolclass.go
  - internal/surfacesgen/main.go
  - docs-site/src/content/docs/guides/cli.md
  - cmd/engram/testdata/help.golden
  - cmd/engram/testdata/catalog.golden
autonomous: true
requirements: [REQ-citation-drift-verify]

estimate:
  tokens: 88000
  raw_tokens: 88000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`engram spine-review verify` classifies every stored citation as valid, moved, broken, or unverifiable, and reports the moved tier separately from the broken tier"
    - "An excerpt found at a DIFFERENT byte offset in the SAME file classifies as moved — not valid and not broken — which is the drift class an edit above the cited range produces"
    - "A file citation whose `Excerpt` is empty classifies as unverifiable with reason `no cached excerpt`, never as valid"
    - "Tier evaluation is ordered file-missing, then no-excerpt, then at-locator, then same-file search, then excerpt-gone; a missing file therefore never reports `excerpt gone`"
    - "A citation whose `Kind` is commit, url, or repo classifies as unverifiable with a per-entry reason, never as silently skipped"
    - "A citation whose owning record's scope names a different repo than the working tree classifies as unverifiable with reason `different repo`, never as a confident wrong verdict"
    - "`verify` exits 0 by default even when broken citations are found; a nonzero exit requires `--fail-on`"
    - "`--fail-on` is a registered `internal/surfaces` conditional rule with an enum hint, bound on every surface that advertises the flag"
    - "`verify` never searches outside the file a citation's `Ref` names — no whole-tree fallback and no fuzzy matching"
  artifacts:
    - path: "cmd/engram/spine_review_verify.go"
      provides: "the verify leaf, the pure tier classifier, and the repo-identity gate"
      contains: "func verifyFileCitation"
    - path: "internal/store/spine.go"
      provides: "Subject-less citation enumeration feeding verify"
      contains: "func (s *Store) EnumerateCitations"
    - path: "internal/surfaces/rules.go"
      provides: "RuleVerifyFailOnValues declared in the single rule registry"
      contains: "RuleVerifyFailOnValues"
  key_links:
    - from: "cmd/engram/spine_review_verify.go"
      to: "internal/store/spine.go"
      via: "leaf RunE enumerates citations through the Subject-less store method"
      pattern: "\\.EnumerateCitations\\("
    - from: "cmd/engram/spine_review_verify.go"
      to: "internal/surfaces/rules.go"
      via: "the --fail-on flag's Usage composes the declared rule Sentence"
      pattern: "RuleByID\\(surfaces\\.RuleVerifyFailOnValues\\)"
  prohibitions:
    - requirement_id: REQ-citation-drift-verify
      category: transparency
      status: resolved
      verification: test
      statement: "MUST NOT report a citation as `valid` that the verifier did not actually check — every unchecked citation lands in the unverifiable tier carrying the reason it was not checked, so a clean report can never be read as coverage the run did not have"
---

<objective>
Ship `engram spine-review verify`: excerpt-anchored citation drift classification into four tiers
(valid / moved / broken / unverifiable), bounded to the file the citation names and to the repo the
command runs in, exiting 0 by default with an opt-in `--fail-on` for CI.

Purpose: an operator reading a clean report must be able to tell "checked and fine" from "never
looked at". The moved tier exists so ordinary refactoring does not train an operator to ignore the
verifier — GitHub issue #355 is the live drift fixture this classification is chosen to get right.

Output: a pure tier classifier, a Subject-less citation enumeration, the leaf command with its
`--output` and `--fail-on` flags, and a registered conditional rule anchored on its surfaces.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-spine-curation-structural-cli/03-CONTEXT.md
@.planning/phases/03-spine-curation-structural-cli/03-RESEARCH.md
@.planning/phases/03-spine-curation-structural-cli/03-PATTERNS.md
@.planning/phases/03-spine-curation-structural-cli/03-03-SUMMARY.md
</context>

## Artifacts this phase produces

See `03-01-PLAN.md` § "Artifacts this phase produces". This plan contributes:
`cmd/engram/spine_review_verify.go`, `spineReviewVerifyCmd`, `verifyFileCitation`,
`citationVerdict`, `excerptOffsetAt`, `repoIdentityFromCWD`, `verifySummary`,
`store.EnumerateCitations`, `store.CitationRecord`, `surfaces.RuleVerifyFailOnValues`, the
`spine-review verify` blast-radius row, and the `--fail-on` flag.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The pure four-tier citation classifier</name>
  <files>cmd/engram/spine_review_verify.go, cmd/engram/spine_review_verify_test.go</files>
  <read_first>
    - internal/store/store.go lines 286-292 (the Citation struct: Kind, Ref, Locator, Pin, Excerpt — D-05 through D-08 are entirely determined by these five fields)
    - internal/server/tools.go lines 856-896 (validateCitations and validCitationKind: the four accepted kinds and the excerpt-length bound this classifier reads against)
    - .planning/phases/03-spine-curation-structural-cli/03-RESEARCH.md § Code Examples, "Excerpt-anchored citation verification" (the sketched signature and tier order)
    - .planning/phases/03-spine-curation-structural-cli/03-CONTEXT.md § D-05, D-06, D-08 and § Specific Ideas (the instruction not to widen the search radius)
    - cmd/engram/reindex.go lines 87-107 (the pure-formatter discipline the classifier and its report both follow)
  </read_first>
  <behavior>
    - Kind `commit`, `url` and `repo` each classify unverifiable with a reason naming the kind, never skipped and never valid.
    - Kind `file`, file absent from the tree: broken, reason `file missing`.
    - Kind `file`, file present, `Excerpt` empty: unverifiable, reason `no cached excerpt`.
    - Kind `file`, excerpt present at the byte range the `Locator` resolves to: valid.
    - Kind `file`, excerpt absent at the locator but present elsewhere in the same file: moved, reason naming the byte offset where it was found.
    - Kind `file`, excerpt absent from the file entirely: broken, reason `excerpt gone`.
    - A `Locator` that does not parse, or that resolves past the end of the file, falls through to the same-file search rather than erroring — a stale line range is drift, not a malformed citation.
    - The same excerpt appearing at both the locator and elsewhere still classifies valid; the locator match wins.
  </behavior>
  <action>
In `cmd/engram/spine_review_verify.go` declare a `citationVerdict` struct carrying `Tier string`,
`Reason string`, and enough identity for a report row (record id, short id, citation index, `Ref`).
Declare the four tier names as unexported string constants so no call site re-types them, and split
the broken tier by cause with two distinct reason constants — the tier name stays one value, the
reason distinguishes a missing file from a vanished excerpt (D-08).

Write `verifyFileCitation(c store.Citation, fileContent string, fileExists bool) citationVerdict` as
a pure function over already-read content — the caller reads each unique `Ref` once and passes the
bytes in, so the classifier is unit-testable with zero filesystem access. Evaluate the tiers in
exactly the order the behavior list gives; that order is what keeps a missing file from ever
reporting a vanished excerpt.

Write `excerptOffsetAt(content string, locator string) int` returning the byte offset the locator's
line range begins at, or -1 when the locator is empty, unparseable, or beyond the content. Support
the `Locator` forms the repo already writes: a bare line number and a `start-end` line range. Do not
invent additional forms.

For the moved tier use a single in-file search for the excerpt and report the byte offset found.
Do NOT fall back to a whole-tree search and do NOT fuzzy-match: a short excerpt risking a confident
wrong match is the tradeoff D-06 deliberately accepted in exchange for staying tight and cheap, and
CONTEXT.md § Specific Ideas explicitly forbids widening this during planning or implementation.

Write `cmd/engram/spine_review_verify_test.go` as a table test over `verifyFileCitation` covering
every behavior-list row, plus a case per non-file kind. Include a case reproducing GitHub issue
#355's shape — an excerpt whose cited line range drifted because lines were inserted above it — and
assert it classifies moved, not broken.
  </action>
  <verify>
    <automated>go test ./cmd/engram/... -run 'TestVerifyFileCitation|TestExcerptOffsetAt' -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd/engram/... -run TestVerifyFileCitation -count=1` passes with no Qdrant and no filesystem access from the test body (the test builds content strings inline).
    - The table covers at least nine rows: three non-file kinds, file-missing, empty-excerpt, at-locator, moved, excerpt-gone, and the unparseable-locator fallthrough.
    - A test case named for issue #355 asserts the moved tier for a drifted line range.
    - `rg -n 'filepath.Walk|filepath.WalkDir|os.ReadDir' cmd/engram/spine_review_verify.go` returns no matches — there is no tree walk in the classifier's file.
    - `verifyFileCitation`'s signature takes no `*store.Store`, no `context.Context`, and no path: `rg -n 'func verifyFileCitation' cmd/engram/spine_review_verify.go` shows only the citation, content and existence parameters.
    - `task license:check` green.
  </acceptance_criteria>
  <done>The four-tier classifier is pure, order-defined, bounded to one file, and proven against the #355 drift shape.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The `verify` leaf — citation enumeration, repo identity, and the report</name>
  <files>internal/store/spine.go, internal/store/spine_test.go, cmd/engram/spine_review_verify.go, cmd/engram/spine_review_verify_test.go, internal/surfaces/toolclass.go, cmd/engram/testdata/help.golden, cmd/engram/testdata/catalog.golden</files>
  <precondition>A reachable Qdrant (Docker for testcontainers, or `ENGRAM_QDRANT_TEST_ADDR`) is required for the enumeration integration test; without it the harness's existing skip path applies and the pure tests still run.</precondition>
  <read_first>
    - internal/store/spine.go (ScanSpine's Scroll sweep and telemetry boilerplate from plan 03-01; CountExpired from plan 03-03)
    - internal/store/store.go lines 286-292 (Citation) and lines 480-600 (toPayload / fromPayload, to see how citations round-trip through the payload)
    - cmd/engram/spine_review_scan.go (the leaf skeleton, --scope/--all-scopes handling, renderOperator wiring)
    - cmd/engram/operator_output.go (addOperatorOutputFlag, operatorOutputFormat, renderOperator from plan 03-02)
    - cmd/engram/operror.go (classifyOperatorErr and classifyOperatorErrConstruction)
    - skill/engram/skills/discovering/SKILL.md lines 45-70 (the `discovery:repo:<repo>` scope convention — the only place the repo-identity shape is written down)
    - internal/store/bench_test.go lines 20-30 (a live scope value in the `repo:<host>/<path>` form)
    - .planning/phases/03-spine-curation-structural-cli/03-RESEARCH.md § Assumptions Log row A1
  </read_first>
  <behavior>
    - `EnumerateCitations` returns every record carrying at least one citation across the requested scope, with no `Subject` and no owner filter, including records that recall hides (superseded, expired).
    - `EnumerateCitations` returns an empty, non-nil slice for a scope with no citations.
    - `verify --scope <s>` reads each unique `Ref` at most once even when several citations name it.
    - A citation whose record scope names a repo other than the working tree's identity classifies unverifiable with reason `different repo`.
    - A `Ref` that is absolute, or that escapes the working tree via parent-directory segments, classifies unverifiable with a reason naming the escape — the verifier never reads outside the tree it was run in.
    - The report groups counts by tier and lists per-entry rows for every non-valid tier.
    - Both `--output` modes carry the same tier counts and the same per-entry reasons.
  </behavior>
  <action>
Add `EnumerateCitations(ctx context.Context, opts SpineScanOptions) ([]CitationRecord, error)` to
`internal/store/spine.go`, where `CitationRecord` is `{ID, ShortID, Scope, Category string;
Citations []Citation}`. Implement it as a Subject-less `Scroll` requesting payload only, filtered to
records whose citations key is non-empty, following `ScanSpine`'s span and telemetry boilerplate with
op name `EnumerateCitations`. No `Subject` parameter, no owner or shared condition, and no reuse of
`Search`/`List`/`SearchDiscovery` — those are Subject-gated and would silently narrow the sweep to
one actor's bucket.

Add `repoIdentityFromCWD() (string, error)` to `cmd/engram/spine_review_verify.go`. Derive the
identity from `git remote get-url origin`, normalised to the `host/path` form the repo's live scope
values already use (strip the scheme and any user info, strip a trailing `.git`, strip a trailing
slash). Fall back to the working directory's base name when there is no origin remote or git is
unavailable. Compare it against the citation's owning record scope by checking whether the scope
carries a `repo:` segment whose value equals the derived identity; a scope with no repo segment, or
one naming a different repo, yields unverifiable with reason `different repo`. This derivation is a
recorded assumption (RESEARCH.md A1) — it is chosen to fail SAFE: a mis-derivation produces
`unverifiable`, never a confident wrong verdict.

Add the path-safety gate before any `os.ReadFile`: resolve the citation `Ref` against the working
directory, reject an absolute path and reject any resolved path that does not remain under the
working directory, classifying either as unverifiable with a reason naming the rejection. A citation
`Ref` is client-authored data and must never be able to make the verifier read an arbitrary file.

Write the leaf: `spineReviewVerifyCmd` (`Use: "verify"`) registered on `spineReviewCmd`, with
`--scope` / `--all-scopes` / `--timeout` / `--output` matching `scan`'s registrations, following
`prune.go`'s RunE skeleton and routing errors through `classifyOperatorErr` /
`classifyOperatorErrConstruction`. Read each unique `Ref` exactly once into a map before classifying,
so a file cited by fifty records is read once.

Add `verifySummary(res verifyReport) string` as a pure formatter and a matching JSON struct, both
projected from the same report value. The report carries per-tier counts and a per-entry list for
every non-valid tier, each entry naming the record id, short id, the `Ref`, and the reason. Never
include the `Excerpt` text itself in the report: the excerpt is stored record content, and the
operator tier reads every actor's private records.

Add the `spine-review verify` row to `internal/surfaces/toolclass.go`'s `operations` with
`ReadOnly: true, Destructive: false, Idempotent: true, OpenWorld: false`.

Extend `internal/store/spine_test.go` with integration coverage for `EnumerateCitations`, including
the empty-scope case and a case seeding a superseded record with citations to prove recall-hidden
records still appear. Extend `cmd/engram/spine_review_verify_test.go` with pure coverage for the
repo-identity comparison, the path-safety gate, the read-once behaviour, and both output modes.

Regenerate both goldens.
  </action>
  <verify>
    <automated>go test ./internal/store/... -run 'TestEnumerateCitations' -count=1 && go test ./cmd/engram/... -run 'TestSpineReviewVerify|TestRepoIdentity|TestHelpGolden|TestCatalogGolden' -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c '^## engram spine-review verify$' cmd/engram/testdata/help.golden` outputs `1`.
    - `rg -o '"spine-review verify"' cmd/engram/testdata/catalog.golden | wc -l` outputs `1`, with `"readOnly": true`.
    - `rg -n 'func (s \*Store) EnumerateCitations' internal/store/spine.go` shows no `Subject` parameter, and `rg -n 'ownerOrSharedCondition|ownerOnlyCondition' internal/store/spine.go` returns no matches.
    - A test asserts a `Ref` beginning with `/` classifies unverifiable, and a test asserts a `Ref` containing parent-directory traversal that escapes the working directory classifies unverifiable.
    - A test asserts a `Ref` cited by three records triggers exactly one file read, using an injected read function that counts calls.
    - A test asserts the rendered report contains no `Excerpt` substring for a citation whose excerpt is a distinctive sentinel string supplied by the test.
    - `go test ./internal/store/... -run TestEnumerateCitations -count=1` passes or skips with the harness's existing "no Qdrant available" message.
    - `task lint` clean; `task license:check` green.
  </acceptance_criteria>
  <done>`engram spine-review verify --all-scopes` enumerates every citation through a Subject-less path, classifies each into one of the four tiers, refuses to read outside its own tree, and renders the same facts in text and JSON.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: `--fail-on` as a registered conditional rule with default exit 0</name>
  <files>internal/surfaces/rules.go, internal/surfaces/rules_test.go, internal/surfacesgen/main.go, cmd/engram/spine_review_verify.go, cmd/engram/spine_review_verify_test.go, docs-site/src/content/docs/guides/cli.md, cmd/engram/testdata/help.golden, cmd/engram/testdata/catalog.golden</files>
  <read_first>
    - internal/surfaces/rules.go (the `rules` literal, the `declared` marker, ValidateRules' constraints)
    - internal/server/argerror.go lines 20-40 (the HintCode vocabulary — `enum` already exists for a value outside an accepted set; register no new hint code)
    - internal/surfacesgen/main.go lines 40-110 (ruleTargets and render)
    - internal/surfaces/conformance_test.go lines 100-160 (checkProseSurface's requirement that an applicable prose surface carries an anchored region)
    - cmd/engram/destructive.go (plan 03-03's pattern for composing a flag Usage string from the registry)
    - cmd/engram/client_common.go lines 215-235 (the exit-code constants and cliError)
    - .planning/phases/03-spine-curation-structural-cli/03-CONTEXT.md § D-14 and § Specific Ideas (D-14 is the one place the user chose the weaker default, deliberately)
  </read_first>
  <behavior>
    - `verify` with no `--fail-on` exits 0 even when broken citations are reported.
    - `--fail-on broken` exits nonzero when at least one broken entry is reported and 0 otherwise.
    - `--fail-on moved` and `--fail-on unverifiable` behave analogously for their own tiers.
    - `--fail-on any` exits nonzero when any non-valid entry is reported.
    - `--fail-on nonsense` exits 2 with a message naming the flag and the accepted values.
    - The nonzero exit for findings is distinguishable from a command failure: it does not reuse the exit code a store/transport error produces.
  </behavior>
  <action>
Declare `RuleVerifyFailOnValues = "verify-fail-on-accepted-values"` in `internal/surfaces/rules.go`
and add its `ConditionalRule` to the `rules` literal with `declared: true`,
`Fields: []string{"fail-on"}`, `Hint: "enum"` (the existing `HintEnum` code — do not add a new hint
code), and an ASCII Sentence naming the accepted values. Leave `TagForm` empty; no MCP arg struct
carries this field.

Add a `ruleTargets` entry for `docs-site/src/content/docs/guides/cli.md` (`kindMarkdown`), place the
anchor pair by hand in a new `spine-review` section of that guide, and run `task surfaces:gen` to
fill the region. Confirm applicability empirically rather than assuming: after adding the section,
`--fail-on` appears in the guide, so the docs-site surface resolves applicable and the anchor is
required there. `fail-on` appears in no other prose target today, so no other prose anchor is needed
— re-check with a grep rather than trusting this sentence.

Register the flag on the verify leaf with `addOperatorOutputFlag`'s sibling shape: a string flag whose
Usage composes `surfaces.RuleByID(surfaces.RuleVerifyFailOnValues)`'s Sentence, so the cobra-usage
conformance gate is satisfied by construction. Validate the value against the accepted set and return
`usageErrorf` on rejection so an illegal value exits 2 through Phase 1's taxonomy.

Wire the exit: after rendering the report, if `--fail-on` was given and the named tier has at least
one entry, return a `cliError` carrying a findings exit code distinct from the store/transport codes.
Choose it from the existing constants rather than inventing a new one if a suitable one exists;
if none fits, add one constant to the taxonomy in `client_common.go` AND add it to `buildCatalog`'s
`ExitCodes` list in the same change, since that list is derived from the constants and a new code
that is not advertised breaks the derivation's whole point.

Document the contract in the same `spine-review` section of the CLI guide: `verify` exits 0 by
default because "the command worked" and "the data is healthy" are different questions, and
`--fail-on` is how CI asks the second one without parsing output.

Extend `cmd/engram/spine_review_verify_test.go` with a table covering every behavior-list row,
asserting exit codes via `exitCodeFromError`. Extend `internal/surfaces/rules_test.go` to assert the
new rule validates and resolves to a non-empty applicable surface set.

Regenerate both goldens.
  </action>
  <verify>
    <automated>go test ./internal/surfaces/... ./cmd/engram/... -run 'TestValidateRules|TestSurfaceConformance|TestSpineReviewVerifyFailOn|TestHelpGolden|TestCatalogGolden' -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/surfaces/... -count=1` passes, including `TestSurfaceConformanceProseFiles`.
    - `rg -c 'engram:rule:start verify-fail-on-accepted-values' docs-site/src/content/docs/guides/cli.md` outputs `1`.
    - A test asserts that a run reporting broken citations with no `--fail-on` returns a nil error and exit code 0.
    - A test asserts `--fail-on broken` on the same report returns the findings exit code, and that this code differs from `exitUnavailable` and from `exitGeneric`.
    - A test asserts `--fail-on nonsense` exits 2.
    - If a new exit-code constant was added, `TestCatalogExitCodesMatchMapper` still passes and `cmd/engram/testdata/catalog.golden` lists the new code.
    - `go run ./internal/surfacesgen` is idempotent (a second run produces no diff).
    - `task lint` clean; `task license:check` green.
  </acceptance_criteria>
  <done>`verify` separates "the command worked" from "the data is healthy": exit 0 by default, an opt-in `--fail-on` gate for CI, declared once in the rule registry and anchored on every advertising surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored citation `Ref` → local filesystem | client-authored path data reaches `os.ReadFile` here |
| stored record payload → operator terminal | citation excerpts are stored record content belonging to every actor |
| CI pipeline → `verify` exit code | an automated gate acts on the process exit status |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04 | Information Disclosure | citation `Ref` resolution in `cmd/engram/spine_review_verify.go` | medium | mitigate | Every `Ref` is resolved against the working directory and rejected if absolute or if it escapes that directory, classifying as unverifiable rather than reading the file. Asserted by explicit absolute-path and traversal tests. Canon path-traversal coverage otherwise belongs to `/gsd-secure-phase` plus lint — not minted as a bespoke prohibition. |
| T-03-14 | Information Disclosure | verify report rendering | medium | mitigate | The report never includes the `Excerpt` text; a sentinel-string test asserts the rendered output does not contain it. |
| T-03-07 | Spoofing (of coverage claims) | `EnumerateCitations` | high | mitigate | Subject-less by signature with no owner condition; an integration test seeds a recall-hidden (superseded) record and asserts it is enumerated, so a future refactor onto `Search`/`List` would fail. |
| T-03-15 | Repudiation | `verify` exit semantics | low | mitigate | Default exit 0 keeps a healthy-data failure from being confused with a command failure; the findings code is distinct from the store/transport codes and is advertised in the catalog's exit-code list. |
| T-03-SC | Tampering | package-manager installs | low | accept | No package-manager install occurs in this phase — zero new Go module dependencies per RESEARCH.md § Package Legitimacy Audit. |
</threat_model>

<verification>
- `go test ./cmd/engram/... ./internal/store/... ./internal/surfaces/... -count=1` passes.
- `go test ./cmd/engram/... -count=1 -shuffle=<seed>` passes for three distinct seeds.
- `engram spine-review verify --all-scopes --output json | jq '.tiers'` against a live spine emits tier counts.
- `task` (lint + test) green.
</verification>

<success_criteria>
- Four tiers, moved reported separately from broken, broken split by cause.
- No whole-tree fallback, no fuzzy matching, no read outside the working tree.
- Exit 0 by default with an opt-in `--fail-on` registered as a conditional rule.
- REQ-citation-drift-verify satisfied.
</success_criteria>

<output>
Create `.planning/phases/03-spine-curation-structural-cli/03-04-SUMMARY.md` when done. Record the
repo-identity derivation actually implemented and whether a new exit-code constant was added.
</output>
