---
phase: 03-spine-curation-structural-cli
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/engram/cmdwalk.go
  - cmd/engram/cmdwalk_test.go
  - cmd/engram/catalog.go
  - cmd/engram/catalog_test.go
  - cmd/engram/golden_test.go
  - cmd/engram/surfaces_test.go
  - cmd/engram/spine_review.go
  - cmd/engram/spine_review_scan.go
  - cmd/engram/spine_review_test.go
  - cmd/engram/testdata/help.golden
  - cmd/engram/testdata/catalog.golden
  - internal/surfaces/toolclass.go
  - internal/surfaces/toolclass_test.go
  - internal/store/spine.go
  - internal/store/spine_test.go
autonomous: true
requirements: [REQ-spine-scan, REQ-operator-output-flag]

estimate:
  tokens: 92000
  raw_tokens: 92000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`engram spine-review scan --scope <s>` prints an inventory report and exits 0 without performing any mutating Qdrant RPC"
    - "`engram spine-review scan --all-scopes` reports counts broken down by scope and by category across the whole collection"
    - "`engram spine-review scan` reads through a Subject-less store method and never calls Search, List, SearchDiscovery, or ListScheduled"
    - "`engram --help` lists `spine-review`, and `engram spine-review --help` lists its leaves"
    - "`cmd/engram/testdata/help.golden` carries a `## engram spine-review` section and a `## engram spine-review scan` section"
    - "`cmd/engram/testdata/catalog.golden` carries a command entry whose name is the qualified path `spine-review scan`, with readOnly true and destructive false"
    - "`surfaces.ClassForCommand` resolves a qualified command path such as `spine-review scan`, and a nested leaf with no operations row makes buildCatalog panic"
    - "TestHelpGolden and TestCatalogGolden pass across at least three distinct `-shuffle=<seed>` values, not one green run"
    - "`engram spine-review scan --output json` emits a single JSON document on stdout with no interleaved text, and `--output text` emits the human line"
    - statement: "A scan run against a collection containing records owned by more than one owner reports every one of them, so the reported totals equal the collection totals rather than one owner's bucket"
      verification: backstop
  artifacts:
    - path: "cmd/engram/spine_review.go"
      provides: "spine-review cobra group command registered on rootCmd"
      contains: "spineReviewCmd"
    - path: "cmd/engram/spine_review_scan.go"
      provides: "scan leaf command plus its pure report formatter"
      contains: "spineReviewScanCmd"
    - path: "cmd/engram/cmdwalk.go"
      provides: "recursive cobra tree walk shared by buildCatalog, goldenCommands and nonHiddenCommands"
      contains: "func walkCommands"
    - path: "internal/store/spine.go"
      provides: "Subject-less spine scan aggregation"
      contains: "func (s *Store) ScanSpine"
  key_links:
    - from: "cmd/engram/catalog.go"
      to: "internal/surfaces/toolclass.go"
      via: "ClassForCommand keyed on the qualified command path, not the bare leaf name"
      pattern: "ClassForCommand\\(commandKey\\("
    - from: "cmd/engram/golden_test.go"
      to: "cmd/engram/cmdwalk.go"
      via: "goldenCommands delegates to the shared recursive walker"
      pattern: "walkCommands\\("
    - from: "cmd/engram/spine_review_scan.go"
      to: "internal/store/spine.go"
      via: "leaf RunE calls the Subject-less ScanSpine method"
      pattern: "\\.ScanSpine\\("
  prohibitions:
    - requirement_id: REQ-spine-scan
      category: transparency
      status: resolved
      verification: test
      statement: "MUST NOT report a spine-review sweep as whole-spine coverage while reading through a Subject-gated path (Search/List/SearchDiscovery/ListScheduled), which would silently narrow the report to one owner's bucket while the wording still claims the whole spine"
    - requirement_id: REQ-spine-scan
      category: privacy
      status: resolved
      verification: test
      statement: "MUST NOT print stored record content or summary text in any spine-review report; the operator tier reads every actor's private records, so reports carry ids, short_ids, counts, scopes, categories, citation locators and scores only"
---

<objective>
Land the phase's architectural spine end-to-end: the operator tier's first nested cobra
subcommand tree (`engram spine-review`), the depth-aware traversal and qualified-path blast-radius
classification that nesting forces, and the first Subject-less store method behind it —
proven by one real leaf, `spine-review scan`.

Purpose: nesting breaks three single-level walkers and one bare-name classification map at once
(RESEARCH.md Pitfall 5). Proving that whole path with one leaf, before five more land on top of
it, is what keeps a traversal mistake from being discovered after six commits.

Output: `engram spine-review scan` working against a live spine, with `--output json|text`,
pinned in both goldens, plus `internal/store/spine.go` established as this phase's store-side home.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-spine-curation-structural-cli/03-CONTEXT.md
@.planning/phases/03-spine-curation-structural-cli/03-RESEARCH.md
@.planning/phases/03-spine-curation-structural-cli/03-PATTERNS.md
@.planning/phases/03-spine-curation-structural-cli/03-VALIDATION.md
</context>

<planner_assumptions>
**Flagged planner assumption (unresolved edge, REQ-spine-scan).** The edge probe classified this
requirement's edge as `unclassified`; per the spec-less fallback protocol it is carried forward as an
explicit assumption rather than auto-resolved into a backstop predicate.

The assumption: "inventory and health signals" has no enumerated content in the requirement, in the
roadmap, or in CONTEXT.md — CONTEXT.md explicitly leaves the signal set to Claude's discretion. There
is therefore no boundary, empty, ordering or encoding edge derivable from the requirement text
itself, because the requirement does not say what is being counted.

This plan resolves the discretion concretely (total, distinct owners, summary coverage,
superseded / expired / scheduled counts, citation counts, and the scope-by-category breakdown; the
archived bucket arrives with plan 03-06). If a reviewer expected a specific signal that is absent —
citation AGE distribution is the most likely candidate, named in CONTEXT.md's discretion list but not
implemented here because "age" has no defensible bucketing without a threshold — that is a gap in the
requirement, not a regression, and it should be raised as a follow-up rather than treated as a
verification failure.
</planner_assumptions>

## Artifacts this phase produces

Everything below is **new surface created by phase 3** — none of it exists in the codebase today,
so drift verification must not flag these as unacknowledged symbols.

**Commands / subcommands (new):** `engram spine-review` (group), `spine-review scan`,
`spine-review verify`, `spine-review consolidate`, `spine-review purge`, `spine-review archive`,
`spine-review restore`.

**CLI flags (new):** `--output` on `reindex`, `migrate-remap-owner`, `migrate-set-owner`,
`prune-expired`, `summarize-missing`, `backfill-short-ids` and every `spine-review` leaf;
`--apply` on `prune-expired` and `spine-review purge`; `--fail-on` on `spine-review verify`;
`--min-score` and `--top-k` on `spine-review consolidate`; `--class`, `--tags`, `--token` on
`spine-review purge`; `--id` on `spine-review archive` / `restore`; `--scope` / `--all-scopes` /
`--timeout` on each `spine-review` leaf that sweeps.

**Go files (new):** `cmd/engram/cmdwalk.go`, `cmd/engram/spine_review.go`,
`cmd/engram/spine_review_scan.go`, `cmd/engram/spine_review_verify.go`,
`cmd/engram/spine_review_consolidate.go`, `cmd/engram/spine_review_purge.go`,
`cmd/engram/spine_review_archive.go`, `cmd/engram/operator_output.go`,
`cmd/engram/destructive.go`, `internal/store/spine.go`, and their `_test.go` siblings.

**Exported Go symbols (new, `internal/store`):** `ScanSpine`, `SpineScanResult`,
`ScopeCategoryCount`, `EnumerateCitations`, `CitationRecord`, `NearDuplicates`, `DuplicatePair`,
`NearDuplicateOptions`, `PurgeManifest`, `PurgeManifest.IsVerified`, `PurgeManifest.IDs`,
`PurgeManifest.DerivedAt`, `PurgeManifest.Token`, `PurgeOptions`, `PurgeClass`, `PreviewPurge`,
`ApplyPurge`, `ParsePurgeToken`, `PurgeResult`, `Archive`, `Restore`, `ArchiveResult`, and the new
`Memory.ArchivedAt` struct field.

**Exported Go symbols (new, elsewhere):** `surfaces.RuleDestructiveRequiresApply`,
`surfaces.RulePurgeFilterRequiresScope`, `surfaces.RuleVerifyFailOnValues`,
`config.ValidateOutputFormat`.

**Unexported Go symbols (new, `cmd/engram`):** `walkCommands`, `commandKey`, `spineReviewCmd`,
`spineReviewScanCmd`, `spineReviewVerifyCmd`, `spineReviewConsolidateCmd`, `spineReviewPurgeCmd`,
`spineReviewArchiveCmd`, `spineReviewRestoreCmd`, `spineScanSummary`, `verifySummary`,
`consolidateSummary`, `purgeSummary`, `archiveSummary`, `verifyFileCitation`, `citationVerdict`,
`excerptOffsetAt`, `repoIdentityFromCWD`, `addOperatorOutputFlag`, `operatorOutputFormat`,
`addApplyFlag`, `destructiveByClassification`.

**Payload keys (new):** `archived_at` (epoch-second integer on the Qdrant point payload).

**Blast-radius rows (new, `internal/surfaces/toolclass.go` `operations`):** `spine-review`,
`spine-review scan`, `spine-review verify`, `spine-review consolidate`, `spine-review purge`,
`spine-review archive`, `spine-review restore` — keyed by qualified path.

**Docs sections (new):** a `spine-review` section and a destructive-command section in
`docs-site/src/content/docs/guides/cli.md`; a `prune-expired` migration entry in
`docs-site/src/content/docs/guides/upgrade.md`.

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end `engram spine-review scan` — one leaf, every layer</name>
  <files>cmd/engram/cmdwalk.go, cmd/engram/catalog.go, cmd/engram/golden_test.go, cmd/engram/surfaces_test.go, internal/surfaces/toolclass.go, cmd/engram/spine_review.go, cmd/engram/spine_review_scan.go, internal/store/spine.go, cmd/engram/testdata/help.golden, cmd/engram/testdata/catalog.golden</files>
  <read_first>
    - cmd/engram/catalog.go (buildCatalog's command loop and its panic backstop, collectFlags)
    - cmd/engram/golden_test.go (goldenCommands, buildHelpGoldenContent's InitDefaultHelpFlag discipline, buildCatalogGoldenContent)
    - cmd/engram/surfaces_test.go (nonHiddenCommands and TestSurfaceConformanceCobraUsage — a THIRD single-level walker RESEARCH.md's Pitfall 5 did not enumerate)
    - internal/surfaces/toolclass.go (Operation, the operations slice, classByCommand, ClassForCommand, validateOperationSet's duplicate-CLICommand rejection)
    - cmd/engram/prune.go (the operator RunE skeleton: StoreFromEnv, classifyOperatorErrConstruction, signal.NotifyContext, optional WithTimeout, classifyOperatorErr)
    - cmd/engram/summarize.go (the --scope / --all-scopes sweep flag pattern)
    - cmd/engram/reindex.go lines 87-120 (reindexSummary: the pure-formatter discipline to copy)
    - cmd/engram/client_common.go lines 185-215 (outputFormatFromConfig, isTerminal)
    - internal/store/store.go lines 2079-2140 (PruneExpired and CountOwnerless: the Subject-less collection-wide precedent, tracer/telemetry boilerplate)
    - .planning/phases/03-spine-curation-structural-cli/03-PATTERNS.md (Pattern Assignments for spine_review.go and internal/store/spine.go)
    - .planning/phases/03-spine-curation-structural-cli/03-RESEARCH.md § Common Pitfalls, Pitfall 5 and Pitfall 6
  </read_first>
  <action>
Create `cmd/engram/cmdwalk.go` with two unexported helpers: `walkCommands(root *cobra.Command, skip func(*cobra.Command) bool) []*cobra.Command`, a depth-first pre-order traversal that returns every descendant of `root` for which `skip` is false and does not descend into a skipped command; and `commandKey(cmd *cobra.Command) string`, which returns `cmd.CommandPath()` with the root binary name and its following space trimmed (so `engram spine-review purge` becomes `spine-review purge`, and a top-level `engram reindex` becomes `reindex`). Give `walkCommands` a deterministic order: sort each level's children lexicographically by `Name()` before descending, so the traversal is independent of registration order (memory `jb33frww29`).

Export the existing skip predicate as a single named function in `cmdwalk.go` — hidden commands plus cobra's own `help` and `completion` scaffolding — and have `buildCatalog` (`cmd/engram/catalog.go`), `goldenCommands` (`cmd/engram/golden_test.go`) and `nonHiddenCommands` (`cmd/engram/surfaces_test.go`) all obtain their command set from `walkCommands` with that one predicate. All three currently iterate `root.Commands()` a single level; after this change none of them does. Keep `buildCatalog`'s existing panic backstop verbatim in behavior — only its lookup key changes.

In `buildCatalog`, replace the bare-name blast-radius lookup with a lookup on `commandKey(cmd)`, and set `catalogCommand.Name` to `commandKey(cmd)` as well, so the catalog's `Commands` array stays a FLAT list whose names are qualified paths. Do not change `catalogDoc`'s JSON schema — this is a name-derivation change, additive for existing consumers whose top-level command names are unchanged. Update the panic message so it names the qualified path.

In `internal/surfaces/toolclass.go`, update `Operation.CLICommand`'s doc comment: it is now the command's qualified path relative to the binary (a nested leaf is `spine-review scan`), not the cobra `Use` name. Update `ClassForCommand`'s doc comment to match. Add two `operations` rows: `{MCPTool: "", CLICommand: "spine-review", Class: {ReadOnly: true, Destructive: false, Idempotent: true, OpenWorld: false}}` — the group command runs nothing but help — and `{MCPTool: "", CLICommand: "spine-review scan", Class: {ReadOnly: true, Destructive: false, Idempotent: true, OpenWorld: false}}`. `validateOperationSet`'s duplicate-CLICommand rejection needs no change: qualified paths are unique by construction.

Create `internal/store/spine.go` with a Subject-less `ScanSpine(ctx context.Context, opts SpineScanOptions) (SpineScanResult, error)`. `SpineScanOptions` carries `Scope string` (empty means all scopes) and nothing else this task. `SpineScanResult` carries `Total uint64` and `ByScopeCategory []ScopeCategoryCount` where `ScopeCategoryCount` is `{Scope, Category string; Count uint64}`. Implement it with `client.Scroll` over the collection requesting payload keys only (`WithVectors` false), aggregating in Go — never `Search`, `List`, `SearchDiscovery` or `ListScheduled`, and never an owner or shared condition. Copy `PruneExpired`'s `tracer.Start` span plus deferred `telemetry.RecordStoreOp` boilerplate exactly, with op name `ScanSpine`. Sort `ByScopeCategory` by (Scope, Category) before returning so output is deterministic.

Create `cmd/engram/spine_review.go` declaring `spineReviewCmd` as a `*cobra.Command` with `Use: "spine-review"`, a `Short` describing inventory/verification/curation of the memory spine, and no `RunE` (so a bare invocation prints its own help and exits 0 — cobra's default for a parent with children). Its `init()` registers `rootCmd.AddCommand(spineReviewCmd)`.

Create `cmd/engram/spine_review_scan.go` declaring `spineReviewScanCmd` (`Use: "scan"`) with package-level flag vars `spineScanScope string`, `spineScanAllScopes bool`, `spineScanTimeout time.Duration`, `spineScanOutput string`. Its `RunE` follows `prune.go`'s skeleton exactly. It rejects a run with neither `--scope` nor `--all-scopes` via `usageErrorf`, reusing the existing scope-or-all-scopes wording `summarize.go` already uses so the already-registered conditional rule keeps binding. It resolves the rendering choice with `outputFormatFromConfig(spineScanOutput, isTerminal(os.Stdout))` and writes to `cmd.OutOrStdout()`. Register the leaf on `spineReviewCmd` in `init()`, and register `--timeout` with the operator-tier semantics (default 5m, help text stating 0 disables) — do NOT copy the client tier's `--timeout` wording or its rejection of 0 (memory `x37kbpw0xq`).

Add a pure formatter `spineScanSummary(res store.SpineScanResult, scope string) string` in the same file, taking value types only — no `*store.Store`, no `context.Context` — mirroring `reindexSummary`. For JSON, marshal a small exported-field struct built from the same `SpineScanResult`; emit `[]` not `null` for an empty breakdown by initialising the slice non-nil.

Report ids, counts, scopes and categories only. No record content, no summary text, ever, on any spine-review output path.

Regenerate both goldens with the repo's existing golden-update convention and commit them.
  </action>
  <verify>
    <automated>go test ./cmd/engram/... ./internal/surfaces/... -run 'TestHelpGolden|TestCatalogGolden|TestSurfaceConformanceCobraUsage|TestCatalogExitCodesMatchMapper' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` succeeds and `go vet ./cmd/engram/... ./internal/store/... ./internal/surfaces/...` is clean.
    - `rg -c '^## engram spine-review$' cmd/engram/testdata/help.golden` outputs `1`.
    - `rg -c '^## engram spine-review scan$' cmd/engram/testdata/help.golden` outputs `1`.
    - `rg -o '"spine-review scan"' cmd/engram/testdata/catalog.golden | wc -l` outputs `1`, and the object containing it has `"readOnly": true` and `"destructive": false`.
    - `go test ./cmd/engram/... -run 'TestHelpGolden|TestCatalogGolden' -count=1 -shuffle=1` and the same with `-shuffle=2` and `-shuffle=3` all pass.
    - A Go test constructs a throwaway nested `*cobra.Command` with no matching `operations` row and asserts `buildCatalog` panics with a message containing that command's qualified path.
    - `rg -n 'root\.Commands\(\)' cmd/engram/catalog.go cmd/engram/golden_test.go cmd/engram/surfaces_test.go` returns no matches (all three now go through the shared walker).
    - `internal/store/spine.go`'s `ScanSpine` takes no `Subject` parameter: `rg -n 'func \(s \*Store\) ScanSpine' internal/store/spine.go` shows a signature whose parameters are only `ctx` and an options struct.
    - `rg -n 'ownerOrSharedCondition|ownerOnlyCondition|s\.Search\(|s\.List\(' internal/store/spine.go` returns no matches.
    - Every new `.go` file carries the Apache-2.0 SPDX header; `task license:check` is green.
  </acceptance_criteria>
  <done>`engram spine-review scan --all-scopes` runs against a live spine, prints a deterministic inventory line (text) or one JSON document (json), performs zero mutating RPCs, and both goldens pin the group command and the leaf.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Full health-signal set for `scan` and its JSON contract</name>
  <files>internal/store/spine.go, internal/store/spine_test.go, cmd/engram/spine_review_scan.go, cmd/engram/spine_review_test.go, cmd/engram/testdata/help.golden, cmd/engram/testdata/catalog.golden</files>
  <read_first>
    - internal/store/spine.go (the ScanSpine written in Task 1)
    - internal/store/store.go lines 843-872 (activeWindowConditions — how expiry and deferred reveal are expressed) and lines 930-940, 1155-1165 (the superseded_by soft-hide sites)
    - internal/store/store.go lines 170-200 (the Memory struct fields ScanSpine aggregates over) and lines 286-292 (the Citation struct)
    - internal/store/store_test.go (the testcontainers / ENGRAM_QDRANT_TEST_ADDR harness and its skip path)
    - cmd/engram/spine_review_scan.go (the formatter written in Task 1)
    - .planning/phases/03-spine-curation-structural-cli/03-CONTEXT.md § Claude's Discretion (which health signals scan reports is explicitly discretionary)
  </read_first>
  <behavior>
    - ScanSpine over an empty collection returns Total 0 with non-nil, zero-length slices.
    - ScanSpine over a seeded collection returns Total equal to the point count and a per-(scope, category) breakdown summing to Total.
    - The breakdown includes records that recall would hide: a superseded record and a record whose not_after has lapsed both appear in the totals, and each is additionally counted in its own health bucket.
    - Summary coverage counts records whose summary is empty separately from those with one.
    - Citation counts report how many records carry at least one citation and the total citation count.
    - Records belonging to two different owners are both counted (the Subject-less proof).
  </behavior>
  <action>
Widen `SpineScanResult` with the health signals CONTEXT.md leaves to discretion, each a plain
counter so the struct stays pure data: `WithoutSummary uint64`, `WithSummary uint64`,
`Superseded uint64`, `Expired uint64` (records whose `not_after` is at or before the scan instant),
`Scheduled uint64` (records whose `not_before` is after the scan instant), `WithCitations uint64`,
`Citations uint64`, and `Owners uint64` (distinct non-empty owner values — the signal that makes the
Subject-less claim observable). Keep `Archived uint64` OUT of this task: it lands with the
`archived_at` key in plan 03-06, which owns that field.

Aggregate all of these in the SAME single `Scroll` sweep `ScanSpine` already performs — do not add a
second pass or a per-signal `Count` RPC. Take the comparison instant once at the top of the method
via the store's existing `now()` hook so the result is deterministic under test.

Extend the text formatter to print a two-part report: a header line with `Total`, `Owners`, and the
scan instant, then one line per health signal, then the (scope, category) breakdown sorted as in
Task 1. Extend the JSON struct with the same fields using snake_case JSON tags matching the store
field names, and keep every slice non-nil so an empty spine marshals arrays as `[]`.

Write `internal/store/spine_test.go` covering the behaviors above against the existing
testcontainers/`ENGRAM_QDRANT_TEST_ADDR` harness, using the same skip path `internal/store/store_test.go`
already uses when neither is available. Write the two-owner case explicitly: seed records under two
distinct owner values and assert `Total` equals their sum and `Owners` equals 2.

Write `cmd/engram/spine_review_test.go` covering the pure formatter for both output modes with a
hand-built `SpineScanResult` — no Qdrant. Include an empty-result case asserting the JSON form
contains `[]` and not `null`, and asserting the text form is a single trailing-newline-terminated
report.

Regenerate both goldens (the leaf's flag set is unchanged by this task, but regenerate and confirm
no diff so a stray flag registration cannot slip through).
  </action>
  <verify>
    <automated>go test ./internal/store/... -run 'TestScanSpine' -count=1 -v && go test ./cmd/engram/... -run 'TestSpineReviewScan|TestHelpGolden|TestCatalogGolden' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/store/... -run TestScanSpine -count=1` passes, or skips with the harness's existing "no Qdrant available" message when neither Docker nor `ENGRAM_QDRANT_TEST_ADDR` is present.
    - A test named for the two-owner case exists and asserts `Owners == 2` and `Total` equal to the seeded record count across both owners.
    - `go test ./cmd/engram/... -run TestSpineReviewScan -count=1` passes with no Qdrant reachable (the formatter tests are pure).
    - The JSON emitted for a zero-record result parses via `json.Unmarshal` and its breakdown field is an empty array, asserted in a test.
    - `rg -n 'ScanSpine' internal/store/spine.go | rg -c 'Scroll'` confirms a single scroll-based sweep; no `client.Count` call is added by this task.
    - `git diff --exit-code cmd/engram/testdata/` is clean after regeneration.
  </acceptance_criteria>
  <done>`scan` reports total, owner count, summary coverage, superseded/expired/scheduled counts, citation counts, and the scope-by-category breakdown, in both text and JSON, proven by integration tests against a seeded two-owner collection.</done>
</task>

<task type="auto">
  <name>Task 3: Order-independence and conformance hardening for the nested tree</name>
  <files>cmd/engram/cmdwalk_test.go, cmd/engram/catalog_test.go, cmd/engram/golden_test.go, cmd/engram/spine_review_test.go, internal/surfaces/toolclass_test.go</files>
  <read_first>
    - cmd/engram/cmdwalk.go (the walker written in Task 1)
    - cmd/engram/clienttest_test.go lines 100-200 (resetClientFlags and resetCommandFlagState, including its doc comment on shared command singletons and the parent PersistentFlags walk)
    - cmd/engram/golden_test.go (the InitDefaultVersionFlag / InitDefaultHelpCmd / InitDefaultCompletionCmd forcing in buildHelpGoldenContent, and the stripLazyHelpFlag rationale)
    - cmd/engram/catalog_test.go (existing catalog assertions)
    - internal/surfaces/toolclass_test.go (existing ValidateOperations coverage)
    - .planning/phases/03-spine-curation-structural-cli/03-VALIDATION.md § Sampling Rate (the determinism requirement is a phase-level gate, not a per-task nicety)
  </read_first>
  <action>
Write `cmd/engram/cmdwalk_test.go` with table-driven coverage of `walkCommands` and `commandKey`
over a throwaway `*cobra.Command` tree built in the test (never `rootCmd`): a two-level tree returns
parent-then-children in lexicographic order; a hidden parent removes its whole subtree from the
result; `help` and `completion` are skipped at every depth, not just the top; `commandKey` returns
the bare name for a direct child of root and the space-joined path for a grandchild.

Add a `cmd/engram/catalog_test.go` case asserting that `buildCatalog` over the live `rootCmd`
produces a `Commands` entry for every command `walkCommands` returns, with no entry whose `Name` is a
bare leaf name that also exists as a nested leaf — i.e. the catalog's command-name set equals the
`commandKey` set exactly. Assert set equality, not a substring or a count.

Add the negative case as its own test: build a throwaway root with a nested command carrying no
`operations` row and assert `buildCatalog` panics, recovering and asserting the panic string contains
the qualified path. This proves the backstop still fires at depth.

Extend `internal/surfaces/toolclass_test.go` with a case asserting `ClassForCommand` resolves each of
the qualified paths added in Task 1 and returns `ok == false` for the bare leaf name `scan` — the
qualification is load-bearing, not cosmetic.

In `cmd/engram/spine_review_test.go`, add `resetCommandFlagState(t, spineReviewScanCmd)` to every
table row that executes the leaf, and add a two-row regression case that runs the leaf twice in one
test binary with different flag values, asserting the second row does not inherit the first row's
flag state (memory `k66tenzbhy`, RESEARCH.md Pitfall 6).

Run the golden and catalog tests under several shuffle seeds and record the seeds used in the plan
summary. A single green run is not evidence (memory `jb33frww29`).
  </action>
  <verify>
    <automated>go test ./cmd/engram/... ./internal/surfaces/... -count=1 -shuffle=1 && go test ./cmd/engram/... ./internal/surfaces/... -count=1 -shuffle=7 && go test ./cmd/engram/... ./internal/surfaces/... -count=1 -shuffle=13</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd/engram/... -run TestWalkCommands -count=1 -v` passes and covers all four table cases named in the action.
    - The catalog set-equality test fails if a single command is dropped from either side — proven by temporarily skipping one command in a local run, then restoring.
    - `go test ./cmd/engram/... ./internal/surfaces/... -count=1 -shuffle=<seed>` passes for at least three distinct seeds, all recorded in the plan SUMMARY.
    - A test asserts `surfaces.ClassForCommand("scan")` returns `ok == false` while `surfaces.ClassForCommand("spine-review scan")` returns `ok == true`.
    - `task lint` is clean and `task license:check` is green.
  </acceptance_criteria>
  <done>The nested tree's traversal, classification and flag state are order-independent and proven so across multiple shuffle seeds; the catalog panic backstop demonstrably still fires at depth.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell → `cmd/engram` | operator-supplied flags cross here; no bearer identity is involved, the tier is Subject-less by design |
| `internal/store` → Qdrant | collection-wide reads with no owner filter cross here |
| stored record payload → operator terminal / pipe | every actor's records, including `private` ones, become readable to whoever runs the binary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-05 | Information Disclosure | `spine-review scan` report rendering (`cmd/engram/spine_review_scan.go`) | medium | mitigate | Reports carry ids, counts, scopes, categories only — never record `content` or `summary`. Enforced by the JSON struct's explicit field list (no embedded `store.Memory`) and asserted in `cmd/engram/spine_review_test.go`. |
| T-03-07 | Spoofing (of coverage claims) | `internal/store/spine.go` `ScanSpine` | high | mitigate | `ScanSpine` takes no `Subject` and adds no owner/shared condition; the two-owner integration test in Task 2 proves the report spans owners, so a future refactor that composed `Search`/`List` would fail rather than silently narrow. |
| T-03-08 | Tampering | `internal/surfaces` blast-radius classification at depth | high | mitigate | `buildCatalog`'s panic backstop is preserved and re-proven at depth (Task 3), so a nested leaf can never ship unclassified — which is what plan 03-03's derived `--apply` gate depends on. |
| T-03-SC | Tampering | package-manager installs | low | accept | No package-manager install occurs in this phase — zero new Go module dependencies, confirmed in RESEARCH.md § Package Legitimacy Audit. No install checkpoint is warranted. |
</threat_model>

<verification>
- `go test ./cmd/engram/... ./internal/store/... ./internal/surfaces/... -count=1` passes.
- `go test ./cmd/engram/... ./internal/surfaces/... -count=1 -shuffle=<seed>` passes for three distinct seeds.
- `task lint` clean, `task license:check` green.
- `engram spine-review scan --all-scopes --output json | jq .` against a live spine emits one parseable document.
</verification>

<success_criteria>
- `engram spine-review` and `engram spine-review scan` exist, are pinned in both goldens, and are classified by qualified path.
- `buildCatalog`, `goldenCommands` and `nonHiddenCommands` share one recursive walker; none iterates a single level.
- `internal/store/spine.go` exists with a Subject-less `ScanSpine` proven to span owners.
- REQ-spine-scan is satisfied for inventory and health signals with no mutation on any path.
</success_criteria>

<output>
Create `.planning/phases/03-spine-curation-structural-cli/03-01-SUMMARY.md` when done. Record the
`-shuffle` seeds used.
</output>
