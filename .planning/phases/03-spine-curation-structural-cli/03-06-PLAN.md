---
phase: 03-spine-curation-structural-cli
plan: 06
type: execute
wave: 6
depends_on: ["03-05"]
files_modified:
  - internal/store/store.go
  - internal/store/store_test.go
  - internal/store/spine.go
  - internal/store/spine_test.go
  - cmd/engram/spine_review_archive.go
  - cmd/engram/spine_review_archive_test.go
  - cmd/engram/spine_review_scan.go
  - cmd/engram/spine_review_test.go
  - cmd/engram/clienttest_test.go
  - internal/surfaces/toolclass.go
  - internal/server/tools.go
  - docs-site/src/content/docs/guides/cli.md
  - docs-site/src/content/docs/reference/memory-record.md
  - docs-site/src/content/docs/reference/tools.md
  - cmd/engram/testdata/help.golden
  - cmd/engram/testdata/catalog.golden
autonomous: false
requirements: [REQ-archive-tier]

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A record can be archived and restored through `engram spine-review archive` / `restore`, and an archived record is retained in the collection rather than deleted"
    - "An archived record is hidden from `search_memory`, `list_memory`, `search_discovery` and `list_scheduled`, and remains fetchable by id through `get_memory`"
    - "`archived_at` is a new payload key orthogonal to both `not_after` and `superseded_by`; archiving never writes `not_after` and never writes `superseded_by`"
    - "An operator can distinguish an archived record from a naturally-expired one and from a superseded one by reading `get_memory` alone"
    - "Archiving an already-archived record leaves `archived_at` at its original value and exits 0; restoring a record that is not archived is a no-op success"
    - "A record that is superseded and archived carries both keys and stays hidden from recall under either condition independently — removing one does not resurface it while the other still holds"
    - "`prune-expired` and `scan`'s expired counter exclude archived records from the naturally-expired population, by checking `archived_at` absence and never by changing the `not_after` predicate"
    - "`archived_at` survives every sibling write path: a record updated or superseded after being archived is still archived afterwards"
    - "`archived_at` survives a CONCURRENT update: `Archive` and `Restore` take the same `TargetLocker` lock on the record that `Update` takes, so an archive landing between `Update`'s in-lock re-read and its whole-payload `Upsert` cannot be erased — asserted by a DETERMINISTIC interleaving driven through the `updateAfterReadHook` seam, which forces the vulnerable window open on a single iteration, not by repeating an unsynchronised goroutine race and hoping to hit it"
    - "`restore --id <unknown>` and `archive --id <unknown>` behave IDENTICALLY: both resolve the target first and both report not-found with the same exit code, so a typo is never silently reported as success"
    - "`scan` reports an `archived` bucket alongside its other health signals"
    - "The repeatable `--id` flag does not latch across table rows: its backing `stringSlice` var is nilled in `resetClientFlags`' explicit cleanup list, not only reset via `resetCommandFlagState` — which deliberately skips value reset for `stringSlice` flags"
  artifacts:
    - path: "internal/store/spine.go"
      provides: "Subject-less archive and restore operations"
      contains: "func (s *Store) Archive"
    - path: "cmd/engram/spine_review_archive.go"
      provides: "the archive and restore leaf commands"
      contains: "spineReviewArchiveCmd"
    - path: "docs-site/src/content/docs/reference/memory-record.md"
      provides: "the archived_at field's published contract"
      contains: "archived_at"
  key_links:
    - from: "internal/store/store.go"
      to: "internal/store/store.go"
      via: "the archived_at soft-hide is appended at the SAME four recall call sites as the superseded_by soft-hide, keeping the two structurally paired"
      pattern: "NewIsEmpty\\(\"archived_at\"\\)"
    - from: "cmd/engram/spine_review_archive.go"
      to: "internal/store/spine.go"
      via: "leaf RunE calls the Subject-less Archive / Restore methods"
      pattern: "\\.(Archive|Restore)\\("
  prohibitions:
    - requirement_id: REQ-archive-tier
      category: safety
      status: resolved
      verification: test
      statement: "MUST NOT implement archive as a delete, a content erasure, or a vector removal — an archived record must remain byte-recoverable through `get_memory`, because an operator reaching for `archive` is deliberately choosing the reversible option over `purge`"
---

<objective>
Make archive a first-class record state (D-12): a new orthogonal `archived_at` payload key,
soft-hidden at exactly the same recall call sites `superseded_by` already is, exposed through
`get_memory`, surfaced as an `archived` bucket in `scan`, and driven by `archive` / `restore` verbs.

Purpose: REQ-archive-tier demands a state distinct from BOTH supersession's soft-hide and expiry.
Overloading `not_after` would make a naturally-expired record indistinguishable from an archived one
at three independent read sites — the failure D-12 rejected that option to avoid.

Output: the `archived_at` key with its round-trip and soft-hide wiring, two Subject-less store
methods, two leaf commands, and the published field contract.
</objective>

<review_response>
## Cross-AI review findings this plan answers (03-REVIEWS.md, cycle 1)

- **MEDIUM (review #13) — the archive/update race is open on the WRITE side.** `Update` takes
  `s.locker.Lock(ctx, cur.ID)` and re-reads only `Supersedes`/`SupersededBy` inside the lock before a
  whole-payload `Upsert` (`store.go:1694-1700`, `:1732`). The previous revision correctly extended
  that in-lock re-read to `ArchivedAt` but had `Archive` copy `SetVisibility`'s **lock-free** targeted
  `SetPayload` (`store.go:1872-1892`). An `Archive` landing between `Update`'s in-lock `Get` and its
  `Upsert` is erased: the sequential must-have passes, the concurrent case loses the archive. Task 2
  now requires BOTH sides to take the same lock, and adds interleaving tests.
- **MEDIUM (review #16) — `restore` on a nonexistent id silently succeeded, asymmetrically with
  `archive`.** `defaultDeletePayloadKeys` (`store.go:1855-1862`) is a bare `DeletePayload` with an id
  selector and NO existence check, while `SetVisibility`'s documented TOCTOU note (`store.go:1866-1871`)
  records that point-id `SetPayload` DOES return NotFound. So `archive --id <bogus>` errored and
  `restore --id <bogus>` exited 0. Task 2 resolves the target first in BOTH verbs.
- **MEDIUM (review #11) — the new repeatable `--id` flag will latch across `-shuffle` rows.**
  `resetCommandFlagState` deliberately skips VALUE reset for `stringSlice` flags
  (`clienttest_test.go:155-197`); the reset lives in `resetClientFlags`' explicit nil-list
  (`:100-153`), added after a real CR-01 incident whose comment warns the bug "turns real under
  `-count=2` or `-shuffle=on`" — exactly the evidence this phase leans on. Task 3 extends that list.
  `cmd/engram/clienttest_test.go` is now in `files_modified`.
- **MEDIUM (review #23) — `archived_at` will be an UNINDEXED filter key.** `ensureIndexes`
  (`store.go:429-457`) indexes `owner`, `scope`, `created_at`, `short_id` only. `superseded_by` is
  likewise unindexed and filtered with `IsEmpty`, so the cost is pre-accepted — but the previous
  revision mentioned indexing nowhere, making it an assumption. Task 2 records it as an explicit
  DECISION with its rationale.
- **LOW (review #27) — cross-plan file mismatch on the shared expiry filter.** Plan 03-03 places
  `CountExpired` and the single filter constructor in `internal/store/spine.go`; this plan's grep
  now spans BOTH `internal/store/store.go` and `internal/store/spine.go`, so the two expectations
  agree instead of one of them yielding 4 where 5 was required.
- **LOW / Divergent view — Connect-lane `archived_at` parity.** Codex called the proto omission a
  HIGH parity violation; orchestrator verification of `proto/engram/v1/engram.proto`'s full field
  list (1-22) shows the Connect `Memory` carries **no `superseded_by`, `supersedes`, `not_before` or
  `not_after` at all** — the lane already omits the entire supersession and scheduling state.
  Omitting `archived_at` is therefore CONSISTENT with the shipped contract, not a regression. Task 3
  adds the documentation line and requires a follow-up issue for the pre-existing gap; no proto,
  codegen or `memoryToProto` work lands in this phase.
- **LOW (review #30) — `reference/tools.md` and the `get_memory` tool description enumerate the
  recall-hidden record states and go stale** when `archived` becomes a fourth one.
  `docs-site/src/content/docs/reference/tools.md` and `internal/server/tools.go` are now in
  `files_modified` and in Task 3.
</review_response>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-spine-curation-structural-cli/03-CONTEXT.md
@.planning/phases/03-spine-curation-structural-cli/03-RESEARCH.md
@.planning/phases/03-spine-curation-structural-cli/03-PATTERNS.md
@.planning/phases/03-spine-curation-structural-cli/03-05-SUMMARY.md
</context>

## Artifacts this phase produces

See `03-01-PLAN.md` § "Artifacts this phase produces". This plan contributes: the `archived_at`
payload key, `store.Memory.ArchivedAt`, `store.Archive`, `store.Restore`, `store.ArchiveResult`,
`cmd/engram/spine_review_archive.go`, `spineReviewArchiveCmd`, `spineReviewRestoreCmd`,
`archiveSummary`, the `--id` flag, the `spine-review archive` and `spine-review restore`
blast-radius rows, `SpineScanResult.Archived`, and the unexported `updateAfterReadHook` interleaving
seam in `internal/store/store.go` (nil in production; the mechanism that makes the archive/update
race gate deterministic).

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm archive as a new observable record state</name>
  <decision>
D-12 makes archive a first-class state observable through `get_memory` and therefore through the MCP
surface. Agents will begin branching on it. Confirm the storage mechanism and the observable shape
before the key is written anywhere.
  </decision>
  <context>
**D-12 (one-way).** Once `archived_at` appears on a record returned by `get_memory`, it is part of
the published memory contract: agents read it, skills branch on it, and removing it later is a
breaking change to the MCP surface, not just to the CLI.

RESEARCH.md verified the two candidate mechanisms concretely:

- **A new orthogonal `archived_at` payload key**, soft-hidden by appending
  `NewIsEmpty("archived_at")` at the same four recall call sites `superseded_by` already occupies.
  Archived and expired stay independently readable predicates.
- **Reusing `not_after` plus a second marker** — the option D-12 already rejected. RESEARCH.md
  Pitfall 3 confirms why by reading the code: `activeWindowConditions` and `PruneExpired`
  independently match on `not_after`, so three call sites would each need to also consult the marker,
  and one future read path that forgets misreads an archived record as naturally expired.

Two shape questions this checkpoint settles:

1. **Value type.** An epoch-second integer (matching `not_before` / `not_after`'s stored form) versus
   an RFC3339 string (matching `created_at` / `last_accessed_at`'s stored form). The recall filter
   needs `IsEmpty` only, so either works for hiding; the integer form matches the other two window
   keys and makes a future retention-window range filter (plan 03-07's third structural purge class)
   a direct `Range` query rather than a parse-and-compare.
2. **Whether `archive` / `restore` accept ids only, or also a filter.** CONTEXT.md leaves this to
   discretion. Ids only keeps the blast radius of a mutating verb explicitly enumerated; a filter form
   would need its own scope gate, mirroring plan 03-07's purge filter path.
  </context>
  <options>
    <option id="option-a">
      <name>Orthogonal `archived_at` as an epoch-second integer; `archive` / `restore` take explicit ids only</name>
      <pros>Matches `not_before` / `not_after`'s stored form, so a retention-window range filter needs no parsing. Ids-only keeps a mutating verb's blast radius explicitly enumerated by the operator. Smallest new surface.</pros>
      <cons>Archiving a large set means many invocations or a shell loop. Epoch seconds are less legible than RFC3339 when read raw out of `get_memory`.</cons>
    </option>
    <option id="option-b">
      <name>Orthogonal `archived_at` as an RFC3339 string; `archive` / `restore` take explicit ids only</name>
      <pros>Legible in `get_memory` output without conversion; matches `created_at` / `last_accessed_at`.</pros>
      <cons>A retention-window filter (plan 03-07's archived-past-retention purge class) must then parse strings client-side or store a second numeric key — reintroducing the two-fields-for-one-fact shape D-12 rejected.</cons>
    </option>
    <option id="option-c">
      <name>Orthogonal `archived_at`; `archive` / `restore` accept ids OR a scope-gated filter</name>
      <pros>Bulk archiving in one invocation, which is the realistic milestone-close workflow. Reuses the explicit-scope gate plan 03-07 builds for purge.</pros>
      <cons>Adds a second conditional rule and a second filter path in the same phase as purge's. Widens a mutating verb's blast radius before anyone has asked for it.</cons>
    </option>
  </options>
  <resume-signal>Select: option-a, option-b, or option-c.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The `archived_at` key — round-trip, soft-hide, and the store verbs</name>
  <files>internal/store/store.go, internal/store/store_test.go, internal/store/spine.go, internal/store/spine_test.go</files>
  <precondition>A reachable Qdrant (Docker for testcontainers, or `ENGRAM_QDRANT_TEST_ADDR`) is required for the recall-gate and round-trip integration tests; without it the harness's existing skip path applies.</precondition>
  <read_first>
    - internal/store/store.go lines 165-200 (the Memory struct's NotBefore / NotAfter / Supersedes / SupersededBy field docs — the shape and comment discipline ArchivedAt follows)
    - internal/store/store.go lines 475-500 and 570-600 (toPayload and fromPayload — where every payload key is written and read)
    - internal/store/store.go lines 843-872 (activeWindowConditions — the helper archived_at must NOT be folded into)
    - internal/store/store.go lines 925-940, 1022-1035, 1152-1168, 1385-1398 (the four call sites appending the superseded_by IsEmpty soft-hide — archived_at pairs with each of them)
    - internal/store/store.go lines 1850-1900 (defaultDeletePayloadKeys and SetVisibility — the targeted single-key SetPayload / DeletePayload pattern Archive and Restore copy, minus the getWritable owner gate)
    - internal/store/store.go lines 1670-1700 and around 1960-1980 (the whole-payload Upsert paths and the TargetLocker discipline — the STATE.md gotcha that a new payload key must survive every sibling write path)
    - internal/store/store.go lines 2079-2140 (PruneExpired — its expiry filter must exclude archived records)
    - internal/store/spine.go (ScanSpine's aggregation, CountExpired from plan 03-03)
    - .planning/phases/03-spine-curation-structural-cli/03-RESEARCH.md § Common Pitfalls, Pitfall 3
  </read_first>
  <behavior>
    - `Archive(ctx, id)` on a live record stamps `archived_at` and returns success; `get_memory` on that id still returns the full record with the field populated.
    - `Archive` on an already-archived record leaves the original stamp value unchanged and returns success.
    - `Restore(ctx, id)` removes the key; `get_memory` then shows the field absent.
    - `Restore` on a record that was never archived returns success and changes nothing.
    - An archived record does not appear in `Search`, `List`, `SearchDiscovery` or `ListScheduled` results.
    - An archived record's `not_after` is untouched by archiving, and its `superseded_by` is untouched.
    - A record that is both superseded and archived stays hidden after `Restore` (the supersession still hides it) and stays hidden after the supersession link is cleared while it is still archived.
    - `PruneExpired` does not delete an archived record whose `not_after` has lapsed.
    - A record archived and then updated through the whole-payload write path is still archived afterwards.
    - `Archive` and `Update` run CONCURRENTLY against the same record leave the record archived, regardless of interleaving: both take the same `TargetLocker` lock, so `Update` cannot re-read a pre-archive payload and then overwrite the stamp.
    - `Restore` and `Update` run concurrently against the same archived record leave the record restored, by the same locking.
    - `Archive(ctx, unknownID)` and `Restore(ctx, unknownID)` BOTH return a not-found-class error; neither reports success.
  </behavior>
  <action>
Add `ArchivedAt *time.Time` to the `Memory` struct in `internal/store/store.go` with a
`json:"archived_at,omitempty"` tag and a doc comment in the same voice as `SupersededBy`'s: it is
server-set only, non-nil excludes the record from recall (soft-hide) but never from `Get`, and it is
orthogonal to `NotAfter` — an archived record and a naturally-expired record are different states.
Use the value form Task 1's checkpoint selected.

Write the key in `toPayload` and read it in `fromPayload`, mirroring exactly how `not_after` is
handled for a pointer-typed optional field. Confirm every whole-payload `Upsert` path round-trips it
the way it already round-trips `superseded_by`, `supersedes`, `idempotency_fingerprint` and
`citations`; if a path re-reads current out-of-band keys inside a `TargetLocker` block, add
`archived_at` to that re-read set in the same change.

Append `qdrant.NewIsEmpty("archived_at")` at each of the FOUR recall call sites that already append
the `superseded_by` IsEmpty condition — as a sibling condition, not folded into
`activeWindowConditions`, and not touching `not_after`'s semantics anywhere. Mirror the existing
comment at those sites explaining why the condition sits beside the window helper rather than inside
it, so the two soft-hides stay structurally paired for the next reader.

Exclude archived records from the naturally-expired population: add an `archived_at` IsEmpty
condition to the filter `PruneExpired` and `CountExpired` share. Plan 03-03 made that a single
construction site and placed it in `internal/store/spine.go`, so this task edits `spine.go` for the
expiry filter and `store.go` for the four recall sites — the acceptance grep below spans both files
for exactly that reason. Do not change the `not_after` predicate itself.

**Record the indexing decision explicitly, do not leave it an assumption.** `ensureIndexes`
(`store.go:429-457`) creates payload indexes for `owner`, `scope`, `created_at` and `short_id` only.
`archived_at` is deliberately NOT added: `superseded_by` is likewise unindexed and filtered with
`IsEmpty` at the same four sites, so this pairs with the established shape and the cost is already
accepted for an identical predicate at identical cardinality. Adding an index for a key that is
`IsEmpty`-filtered rather than range-queried would buy little and would change `ensureIndexes` for
every existing deployment on next start. Write this reasoning into a comment beside the new
condition, so the next reader finds a decision rather than an oversight. If plan 03-07's
archived-past-retention class later needs a `Range` query over the key, revisit it there.

Add `Archive(ctx context.Context, id string) (ArchiveResult, error)` and
`Restore(ctx context.Context, id string) (ArchiveResult, error)` to `internal/store/spine.go`,
Subject-less. **Both verbs take `s.locker.Lock(ctx, id)` for the duration of their read-then-write**,
the SAME lock `Update` takes at `store.go:1694`. This is not optional hygiene: `Update` re-reads the
record inside its lock and then issues a whole-payload `Upsert` (`:1732`), so a lock-free targeted
`SetPayload` — the shape `SetVisibility` uses at `store.go:1872-1892` — can land between `Update`'s
re-read and its `Upsert` and be silently erased. Copying `SetVisibility` verbatim, as the previous
revision specified, satisfies the sequential must-have while losing the archive in the concurrent case.

**Both verbs resolve the target FIRST.** `Archive` reads the record (by id, no `Subject`, no
`getWritable` — the operator tier carries none), returns a not-found-class error when it does not
exist, and returns without writing when `archived_at` is already set, so the operation is idempotent
by value rather than by re-stamping; otherwise it issues a targeted `SetPayload` for that single key
against a point-id selector. `Restore` performs the same existence resolution before calling the
existing `defaultDeletePayloadKeys` helper with the single key. That helper
(`store.go:1855-1862`) is a bare `DeletePayload` with an id selector and NO existence check, while
point-id `SetPayload` DOES return NotFound (`store.go:1866-1871`'s documented TOCTOU note) — so
without the explicit resolution `archive --id <bogus>` would error while `restore --id <bogus>` exited
0, an asymmetry an operator would read as "it was already restored".

`ArchiveResult` reports which of three outcomes occurred — changed, already in that state, or not
found — so the CLI can say each honestly without inferring from an error string. Both carry
`ScanSpine`'s span and telemetry boilerplate.

Add an `Archived uint64` counter to `SpineScanResult` and populate it in the existing single `Scroll`
sweep. Keep the archived population OUT of the expired counter, matching the store-side exclusion.

Extend `internal/store/store_test.go` and `internal/store/spine_test.go` with integration coverage
for every behavior-list row. The recall-gate rows must assert absence from all four read paths, not
just one. The both-states row must assert the record stays hidden under each condition independently.
The write-path row must archive a record, drive a whole-payload update through the existing update
path, and assert `archived_at` survives.

**Write the CONCURRENCY rows as a DETERMINISTIC interleaving, not a repeated coin flip.** The
previous revision specified an unsynchronised two-goroutine race run "some tens of iterations". Both
review lanes flagged the same structural problem in cycle 2: nothing forces `Archive` into `Update`'s
re-read→upsert window, so a lock-free implementation can pass a hundred runs in a row and a green
result is not evidence the vulnerable interleaving was ever reached. Replace the loop with an
injected seam that makes the window reachable on demand.

Add an unexported package-level test seam to `internal/store/store.go`:
`var updateAfterReadHook func()`, nil in production, called by `Update` exactly once — INSIDE the
`s.locker.Lock` block, AFTER the in-lock re-read at `store.go:1694-1700` and BEFORE the whole-payload
`Upsert` at `:1732` — guarded by a nil check so the production path costs one branch. Its doc comment
must say it exists to make the archive/update window addressable from a test and must never be set
outside one, mirroring how `spineScrollBatch` is documented.

`TestArchiveSurvivesConcurrentUpdate` then runs ONE deterministic iteration rather than tens:

1. Seed a record and set `updateAfterReadHook` with a `t.Cleanup` restore to nil.
2. The hook closes an `inWindow` channel to announce that `Update` has re-read and is now holding
   the lock inside the vulnerable window, then blocks on an `archiveDone` channel with a BOUNDED
   `select` timeout (2s is ample) and returns when either fires.
3. The test launches `Update` in a goroutine, waits for `inWindow`, then calls `Archive` in a second
   goroutine and closes `archiveDone` when it returns.
4. Wait for both goroutines, re-read the record, and assert `archived_at` is set.

The bound on step 2 is what makes both outcomes deterministic rather than one of them a hang. Against
a LOCK-FREE `Archive` (the `SetVisibility` shape), `Archive` completes inside the window,
`archiveDone` fires immediately, `Update`'s whole-payload `Upsert` then erases the stamp, and the
assertion fails on the FIRST run — a reliably reachable RED, not a probabilistic one. Against the
LOCKING `Archive` this task specifies, `Archive` blocks on `s.locker.Lock` because `Update` holds it;
the hook's timeout expires, `Update` completes and releases, `Archive` then acquires and stamps, and
the record is archived. Green, also on the first run. Add the mirror case for `Restore`
(`TestRestoreSurvivesConcurrentUpdate`), asserting the record is NOT archived afterwards.

Observe the RED half first by temporarily reverting `Archive` to a lock-free targeted `SetPayload`,
and record in the SUMMARY both the failure line and the fact that it reproduced on a single
iteration — that single-iteration reproduction is the evidence the gate is deterministic, and it is
the specific claim the previous revision could not make.
  </action>
  <verify>
    <automated>go test ./internal/store/... -run 'TestArchive|TestRestore|TestScanSpine|TestPruneExpired|TestRecallGate|TestSupersede' -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `rg -v '^\s*//' internal/store/store.go internal/store/spine.go | rg -o 'NewIsEmpty\("archived_at"\)' | wc -l` outputs `5` — the four recall call sites in `store.go` plus the ONE shared expiry filter, wherever plan 03-03 placed it, with comments filtered. Exactly five, not "at least": a sixth means the condition was duplicated instead of appended to the single construction site.
    - A test asserts each of the four recall sites carries the condition, by archiving one record and asserting its absence from `Search`, `List`, `SearchDiscovery` AND `ListScheduled` — the set-level proof behind the count.
    - A test asserts `activeWindowConditions` returns conditions that do not reference `archived_at` — the soft-hide is a SIBLING of that helper, never folded into it. Assert on the helper's returned conditions, not on a grep for two tokens sharing a line, which any comment could satisfy or defeat.
    - A test seeds a record whose `not_after` is exactly at and just past the cutoff and asserts `CountExpired` and `PruneExpired` agree — proving the expiry range predicate itself is unchanged and still shared.
    - A test archives a record and asserts `Get` still returns it with the field populated.
    - A test archives a record twice and asserts the second call reports the already-archived outcome and the stamp value is unchanged.
    - A test restores a never-archived record and asserts the already-restored outcome with no mutation.
    - **A test asserts `Archive(ctx, "unknown-id")` and `Restore(ctx, "unknown-id")` BOTH return a not-found-class error**, and that neither returns a success outcome. Observe the `Restore` half RED first against the bare `defaultDeletePayloadKeys` call and record the failure line in the SUMMARY.
    - A test seeds an archived record with a lapsed `not_after`, runs `PruneExpired`, and asserts the record survives.
    - A test archives a record, drives a whole-payload update, and asserts `archived_at` is still present afterwards.
    - **`TestArchiveSurvivesConcurrentUpdate` and `TestRestoreSurvivesConcurrentUpdate` pass on a SINGLE deterministic iteration**, driven through the `updateAfterReadHook` seam rather than by repeating an unsynchronised race. `rg -v '^\s*//' internal/store/spine.go | rg -o 's\.locker\.Lock\(' | wc -l` is at least `2` — both verbs take the lock.
    - **The seam exists and is production-inert.** `rg -v '^\s*//' internal/store/store.go | rg -o 'updateAfterReadHook' | wc -l` is at least `2` (declaration plus the nil-guarded call inside `Update`'s lock), and `rg -v '^\s*//' --glob '!*_test.go' internal/ cmd/ | rg -o 'updateAfterReadHook\s*=' | wc -l` outputs `0` — nothing outside a test ever assigns it. (`-v` is invert-match; rg recurses by default, so no `-r`.)
    - **The RED half reproduced on one iteration, not eventually.** Temporarily revert `Archive` to a lock-free targeted `SetPayload`, run the test once, and record in the SUMMARY both the failure line and that a single iteration was sufficient. "Failed after N tries" would mean the barrier is not doing its job and the gate is still probabilistic.
    - A comment beside the new expiry condition records the deliberate decision NOT to index `archived_at`, with the `superseded_by` precedent as its rationale; `git diff -- internal/store/store.go | rg -o '^[-+].*ensureIndexes|^[-+].*FieldTypeKeyword' | wc -l` outputs `0` — the index list is untouched in this diff, which is a stronger statement than any grep over the final file.
    - `rg -n 'func (s \*Store) Archive|func (s \*Store) Restore' internal/store/spine.go` shows no `Subject` parameter, and `rg -v '^\s*//' internal/store/spine.go | rg -o 'getWritable' | wc -l` outputs `0` (comments filtered, so the doc comment explaining why the operator tier skips the owner gate cannot defeat its own gate).
    - `task license:check` green.
  </acceptance_criteria>
  <done>`archived_at` is a live, orthogonal record state: soft-hidden at the same four sites as supersession, excluded from the expired population, surviving every sibling write path, idempotent to set, and reversible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The `archive` and `restore` leaves, the scan bucket, and the published field contract</name>
  <files>cmd/engram/spine_review_archive.go, cmd/engram/spine_review_archive_test.go, cmd/engram/spine_review_scan.go, cmd/engram/spine_review_test.go, cmd/engram/clienttest_test.go, internal/surfaces/toolclass.go, internal/server/tools.go, docs-site/src/content/docs/guides/cli.md, docs-site/src/content/docs/reference/memory-record.md, docs-site/src/content/docs/reference/tools.md, cmd/engram/testdata/help.golden, cmd/engram/testdata/catalog.golden</files>
  <read_first>
    - internal/store/spine.go (Archive, Restore, ArchiveResult from Task 2)
    - cmd/engram/spine_review_scan.go and cmd/engram/spine_review_verify.go (the established leaf skeleton and renderOperator wiring)
    - cmd/engram/destructive.go (plan 03-03's derived gate and `registerDestructive` — read it to confirm whether the blast-radius rows chosen here pull these leaves into the `--apply` requirement, and therefore into the choke point)
    - cmd/engram/clienttest_test.go lines 100-200 IN FULL (resetClientFlags' explicit nil-list and the CR-01 comment explaining why `stringSlice` value reset lives there and NOT in resetCommandFlagState — the `--id` flag added here is exactly that shape)
    - internal/surfaces/toolclass.go (the Class doc comments: Destructive is true only if the operation may remove or overwrite existing data under some valid invocation)
    - docs-site/src/content/docs/reference/memory-record.md (the existing field table this adds a row to)
    - docs-site/src/content/docs/reference/tools.md lines 193-255 (the passages stating fetch-by-id is not recall-gated and ENUMERATING the hidden record states, and the superseded soft-hide description — `archived` becomes a fourth hidden state and both passages go stale)
    - internal/server/tools.go around line 2005 (the `get_memory` tool description, which enumerates the same states in agent-facing prose)
    - proto/engram/v1/engram.proto (`message Memory`'s field list — confirm for yourself that it carries no `superseded_by`, `supersedes`, `not_before` or `not_after`, which is why omitting `archived_at` is consistent with the shipped Connect contract rather than a new parity gap)
    - docs-site/src/content/docs/guides/cli.md (the `spine-review` section from plans 03-04 and 03-05)
  </read_first>
  <behavior>
    - `spine-review archive --id <id>` archives the record and prints a confirmation naming what changed.
    - `spine-review archive --id <id>` on an already-archived record prints an "already archived" line and exits 0.
    - `spine-review restore --id <id>` reverses it; on a non-archived record it prints a no-change line and exits 0.
    - Both commands accept `--id` more than once and process each id, reporting per-id outcomes.
    - An unknown id produces the same not-found exit code the rest of the CLI uses, and processing of the remaining ids is reported explicitly rather than silently abandoned.
    - Both output modes carry the same per-id outcomes.
    - `scan` reports the `archived` count as its own bucket, separate from the expired count.
  </behavior>
  <action>
Create `cmd/engram/spine_review_archive.go` declaring both `spineReviewArchiveCmd` (`Use: "archive"`)
and `spineReviewRestoreCmd` (`Use: "restore"`), registered on `spineReviewCmd`, each with a
repeatable `--id` string-slice flag plus `--timeout` and `--output`, following the established leaf
skeleton and routing errors through `classifyOperatorErr` and
`classifyOperatorErrConstruction`. Per Task 1's checkpoint outcome, add the filter form only if
option-c was selected — and if it was, gate it behind the same explicit-scope conditional rule plan
03-07 registers for purge rather than a second bespoke rule.

Write `archiveSummary(results []store.ArchiveResult, verb string) string` as a pure formatter with a
matching JSON struct projected from the same value, reporting per-id outcomes as three explicit
states — changed, already in that state, not found — so a json consumer never has to infer an
outcome from prose. Never print record content.

Add the `spine-review archive` and `spine-review restore` rows to `internal/surfaces/toolclass.go`'s
`operations`. Classify each deliberately against the `Class` doc comments and record the reasoning in
the row's comment as the existing rows do: archive and restore overwrite no existing data and remove
nothing — they set and clear a single additive key and each is reversible by the other verb — while
being non-read-only and idempotent. Getting this row wrong pulls these leaves into or out of plan
03-03's derived `--apply` requirement, so run `TestDestructiveCommandsRequireApply` immediately after
adding the rows and reconcile rather than adjusting the test.

Extend `SpineScanResult`'s rendering in `cmd/engram/spine_review_scan.go` with the archived bucket in
both the text report and the JSON struct.

**Document the state everywhere it is enumerated — four surfaces, not one.**

1. `docs-site/src/content/docs/reference/memory-record.md`: add an `archived_at` row to the field
   table stating it is server-set, that a non-nil value hides the record from recall but not from
   `get_memory`, and that it is distinct from both `not_after` expiry and `superseded_by`
   supersession.
2. `docs-site/src/content/docs/reference/tools.md`: update the passages that state fetch-by-id is not
   recall-gated and ENUMERATE the hidden states, and the superseded soft-hide description, so
   `archived` appears as a fourth hidden state. Leaving them is a silent contradiction of the very
   observability D-12 exists to provide.
3. `internal/server/tools.go`: update the `get_memory` tool description, which enumerates the same
   states in the prose an AGENT reads. This is the surface most likely to cause an agent to reason
   incorrectly about a record it can fetch but cannot recall.
4. `docs-site/src/content/docs/guides/cli.md`: add an `archive` / `restore` subsection to the
   `spine-review` section covering the verbs, `--id`, the three per-id outcomes, and the
   retained-not-deleted guarantee.

**Connect-lane note, plus a follow-up (not phase-3 work).** Add one sentence to the
`memory-record.md` row stating that `archived_at` is visible on the MCP lane (`get_memory` returns
the `store.Memory` struct directly as structured output, `internal/server/tools.go:2011-2014`, so the
new `json:"archived_at,omitempty"` field appears with zero server changes) and NOT on the Connect
lane. This is consistent with the shipped contract rather than a new gap: `proto/engram/v1/engram.proto`'s
`Memory` carries no `superseded_by`, `supersedes`, `not_before` or `not_after` either — the Connect
lane already omits the whole supersession and scheduling state. Do NOT touch `proto/`, `gen/go/`,
`gen/ts/` or `memoryToProto` in this phase. File a follow-up GitHub issue for the PRE-EXISTING
Connect-lane omission covering all five fields together and record its number in the SUMMARY.

**Close the `stringSlice` latch on `--id`.** Add the `--id` backing var to `resetClientFlags`'
explicit nil-list in `cmd/engram/clienttest_test.go`, beside `storeTags` / `searchTags` / `listTags`.
`resetCommandFlagState` deliberately SKIPS value reset for `stringSlice` flags — its doc comment
explains that `DefValue` for such a flag is the bracketed display string and that
`stringSliceValue.Set` APPENDS once its internal changed bit has latched — so calling it alone leaves
row N's `--id a,b` in place for row N+1. That is the exact CR-01 contamination the repo has already
burned itself on once, and its own comment warns it "turns real under `-count=2` or `-shuffle=on`",
which is precisely the evidence this phase leans on.

Write `cmd/engram/spine_review_archive_test.go` covering the pure formatter for both modes with
hand-built results — no Qdrant — including the already-archived and not-found outcomes and the
multi-id case. Call `resetCommandFlagState` for each leaf in every table row that executes one, and
add a two-row regression case that supplies `--id` in row 1 and a different `--id` in row 2, asserting
row 2 sees only its own value.

Regenerate both goldens.
  </action>
  <verify>
    <automated>go test ./cmd/engram/... ./internal/surfaces/... -run 'TestSpineReviewArchive|TestSpineReviewScan|TestDestructiveCommandsRequireApply|TestHelpGolden|TestCatalogGolden' -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c '^## engram spine-review archive$' cmd/engram/testdata/help.golden` outputs `1`, and the same for `restore`.
    - `rg -o '"spine-review archive"' cmd/engram/testdata/catalog.golden | wc -l` outputs `1`, and the same for `"spine-review restore"`.
    - `TestDestructiveCommandsRequireApply` AND `TestDestructiveCommandsRouteThroughGate` both pass unmodified after the two new rows are added; if either fails, the row's classification is reconsidered rather than the test.
    - A test asserts the JSON form reports the three outcome states as distinct enumerated values, not as differing prose.
    - A test asserts a multi-id invocation with one unknown id still reports outcomes for the known ids, and that the unknown id's outcome is the not-found state — the same state `archive` reports for it.
    - `rg -o 'archived_at' docs-site/src/content/docs/reference/memory-record.md | wc -l` is at least `1`, and a reviewer confirms the row states the MCP-visible / Connect-omitted split.
    - **The fourth hidden state is documented everywhere the others are:** a test or a reviewer confirms `archived` appears alongside the existing hidden-state enumerations in `docs-site/src/content/docs/reference/tools.md` and in the `get_memory` tool description in `internal/server/tools.go`. Verify by reading those passages, not by a bare token count — the token `archived` may already appear elsewhere in both files.
    - **`--id` does not latch:** the two-row `--id` regression case passes under `go test ./cmd/engram/... -run TestSpineReviewArchive -count=2 -shuffle=on`, and the backing var appears in `resetClientFlags`' cleanup list (`rg -o 'spineArchiveIDs *= *nil' cmd/engram/clienttest_test.go | wc -l` outputs `1`). Observe the regression case RED first with the nil-list entry removed and record the failure line in the SUMMARY.
    - `go test ./cmd/engram/... -count=1 -shuffle=on` passes on three separate invocations.
    - `go clean -testcache && task test` is green. The flush matters here because plan 03-03 added the first `internal/e2e` test that execs a changed operator command; before that, `internal/e2e` execed only `serve`, `search` and `list` (memory `p1vqxqhxrm`, corrected scope).
    - `task lint` clean; `task license:check` green (`docs-site/**` takes no SPDX header).
  </acceptance_criteria>
  <done>`engram spine-review archive` / `restore` move a record in and out of the archived state through the CLI, `scan` reports the archived bucket, and the field's contract is published in the memory-record reference.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `internal/store` recall filters → every read path | a missing soft-hide condition resurfaces intentionally hidden content |
| operator shell → a mutating verb | `--id` values decide which records change state |
| `archived_at` → the published MCP memory contract | agents will branch on this field once it is observable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-03 | Information Disclosure | recall filters in `internal/store/store.go` | high | mitigate | The `archived_at` IsEmpty condition is appended at exactly the four call sites `superseded_by`'s already occupies, keeping the two structurally paired so a future read path cannot pick up one without the other. A test asserts absence from all four read paths, not one. |
| T-03-17 | Tampering | whole-payload `Upsert` write paths | high | mitigate | `archived_at` joins `Update`'s in-lock re-read set, AND `Archive`/`Restore` take the SAME `TargetLocker` lock `Update` takes, so a targeted stamp cannot land between `Update`'s re-read and its `Upsert`. Proven by an interleaving test run over tens of iterations, not only by the sequential archive-then-update case — a lock-free targeted `SetPayload` (the `SetVisibility` shape) passes the sequential test while losing the archive under concurrency. |
| T-03-29 | Repudiation | `restore` on a nonexistent id | medium | mitigate | Both verbs resolve the target before writing, so a typo reports not-found identically on `archive` and `restore`. `defaultDeletePayloadKeys` (`store.go:1855-1862`) has no existence check while point-id `SetPayload` returns NotFound, so without the explicit resolution the two verbs would disagree and `restore --id <typo>` would exit 0 — read by an operator as "already restored". |
| T-03-18 | Repudiation | `prune-expired` treating an archived record as naturally expired | medium | mitigate | The shared expiry filter gains an `archived_at` IsEmpty condition, and a test seeds an archived record with a lapsed `not_after` and asserts it survives a prune. `not_after`'s own predicate is unchanged. |
| T-03-19 | Denial of Service (of the operator's own data) | `archive` misclassified as non-reversible | medium | mitigate | `Restore` uses the existing `defaultDeletePayloadKeys` helper; a test proves round-trip recovery through `get_memory`. Archive never deletes a point, erases content, or drops a vector. |
| T-03-SC | Tampering | package-manager installs | low | accept | No package-manager install occurs in this phase — zero new Go module dependencies per RESEARCH.md § Package Legitimacy Audit. |
</threat_model>

<verification>
- `go clean -testcache && task test` green.
- `go test ./cmd/engram/... ./internal/surfaces/... -count=1 -shuffle=<seed>` passes for three distinct seeds.
- Against a live spine: archive a record, confirm it disappears from `engram search` and `engram list`, confirm `get_memory` still returns it with `archived_at` set, then restore and confirm it returns to recall.
</verification>

<success_criteria>
- Archive is a genuine fourth record state, observably distinct from expiry and supersession, and documented as such on all four surfaces that enumerate the states.
- Retained, not deleted; reversible through `restore`; unknown ids report not-found symmetrically.
- Soft-hidden at the same four sites as supersession; excluded from the expired population through the single shared filter constructor.
- The stamp survives concurrent updates, proven by an interleaving test.
- The no-index decision is recorded in code, not assumed.
- REQ-archive-tier satisfied.
</success_criteria>

<output>
Create `.planning/phases/03-spine-curation-structural-cli/03-06-SUMMARY.md` when done. Record: the
checkpoint decision (value type and whether a filter form was added); the blast-radius classification
chosen for the two new leaves with its reasoning; the observed RED failure lines from
`TestArchiveSurvivesConcurrentUpdate` against a lock-free `Archive`, from the unknown-id `Restore`
case, and from the `--id` latch regression with the nil-list entry removed; and the follow-up issue
number filed for the pre-existing Connect-lane field omission.
</output>
