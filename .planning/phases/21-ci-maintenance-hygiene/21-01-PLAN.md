---
phase: 21-ci-maintenance-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .rumdl.toml
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - REQ-lint-planning-exclude

must_haves:
  truths:
    - "`task lint:markdown` exits 0 on a clean tree (D-10) — which unblocks `task` default (lint + test), blocked since Phase 20."
    - "Markdown outside `.planning/` is still linted: a deliberate violation in a repo-root `.md` file is still reported by rumdl (SC3's second half)."
    - "ROADMAP SC2 no longer mislabels IN-01 as 'duplicate depth-gauge registration' (D-00a, D-12) — it names the real finding: the `storeMemory`/`scheduleMemory` duplicated Upsert-then-enqueue block."
    - "ROADMAP SC3 no longer carries the stale 331-failure figure (D-00b, D-12); no new hard-coded count replaces it (the real count drifts daily — measured 1505 → 1514 → 1566 across three reads on 2026-07-15/16)."
  artifacts:
    - ".rumdl.toml — `exclude` array contains a plain `.planning` entry with a trailing why-comment (D-09)"
    - ".planning/ROADMAP.md — Phase 21 SC2 + SC3 corrected (D-12)"
    - ".planning/REQUIREMENTS.md — REQ-p11-review-residuals + REQ-lint-planning-exclude wording corrected to match"
  key_links:
    - "`.rumdl.toml` `exclude` → `task lint:markdown` (`rumdl check .`) → `task` default (`lint` + `test`). The exclude is the single lever that unblocks the whole local gate; `respect-gitignore = true` cannot do it because `.planning/` is committed, not gitignored."
---

<objective>
Add `.planning` to `.rumdl.toml`'s `exclude` array so `task lint:markdown` — and therefore
`task` default (lint + test) — stops failing on agent-generated GSD planning artifacts, while
shipped Markdown outside `.planning/` stays linted. Also correct the two factual errors in the
Phase 21 acceptance list that scouting found (D-00a, D-00b) so the downstream verifier checks
against a true ROADMAP (D-12).

Purpose: `task` default is the local quality gate every other plan in this phase (and every
future phase) runs. It has been blocked since Phase 20 by 1566 lint issues, **100% of which are
inside `.planning/`** and none of which are shipped prose. This plan is the smallest possible
change that restores the gate. **Land this plan first** (D-11) — it is a soft ordering
dependency for Plans 02 and 03, which can use granular subtargets (`task test:go`,
`task lint:actions`) until it lands.

Output: a one-line `.rumdl.toml` exclude entry plus two docs corrections. No code, no runtime
surface, no new dependency.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-ci-maintenance-hygiene/21-CONTEXT.md
@.planning/phases/21-ci-maintenance-hygiene/21-PATTERNS.md
@.rumdl.toml
</context>

<artifacts_this_phase_produces>
This plan creates **no new symbols and no new files**. It produces:

| Artifact | Kind | Location |
|---|---|---|
| `".planning"` exclude entry | TOML array element | `.rumdl.toml` `[global].exclude` |
| Corrected Phase 21 SC2 / SC3 prose | docs | `.planning/ROADMAP.md` §"Phase 21: CI / Maintenance Hygiene" |
| Corrected REQ-p11-review-residuals / REQ-lint-planning-exclude prose | docs | `.planning/REQUIREMENTS.md` §"CI / Maintenance Hygiene" |

No new secrets, no new workflow steps, no new Go symbols. (Those come from Plans 02 and 03.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Exclude .planning from rumdl (D-09, D-10)</name>

  <files>.rumdl.toml</files>

  <read_first>
    - `.rumdl.toml` (whole file, 46 lines) — the `[global].exclude` array is lines 17-29.
    - `.planning/phases/21-ci-maintenance-hygiene/21-PATTERNS.md` §"`.rumdl.toml` (config)" —
      carries the verbatim current array and the convention to copy.
  </read_first>

  <action>
    Append one entry to the end of the `exclude` array in `.rumdl.toml`, immediately after the
    existing `"docs-site"` entry, per D-09:

    - Use the **plain directory name** `.planning` — NOT a glob. Every existing entry is a plain
      name or a literal filename; no glob syntax appears anywhere in the array. ROADMAP SC3's
      prose implies the glob form; that prose is wrong and Task 2 corrects it. If the plain form
      unexpectedly does not take (verify below fails), stop and report rather than silently
      switching to a glob — the plain form is confirmed correct by the convention of its
      neighbours.
    - Add a trailing same-line why-comment matching the density and style of `.beads`, `.agents`,
      `CHANGELOG.md`, and `docs-site` — explaining that these are GSD planning artifacts:
      agent-generated working documents, not shipped prose.
    - Append chronologically at the end; do NOT alphabetize the array (existing entries are in
      append order).

    Do NOT touch `respect-gitignore = true` (line 31). It is already true, and it is precisely why
    `.planning/` is linted today: `.planning/` is committed, not gitignored, so `.gitignore` is the
    wrong lever and the `exclude` array is the right one.

    Change nothing else in the file — no rule toggles, no other exclude entries. Widening the
    exclude beyond `.planning` would silently de-scope shipped prose and break SC3's second half.
  </action>

  <verify>
    <automated>task lint:markdown</automated>
    <automated>printf '## x\n## x\n' > rumdl-scope-probe.md; rumdl check rumdl-scope-probe.md; test $? -ne 0; rc=$?; rm -f rumdl-scope-probe.md; exit $rc</automated>
  </verify>

  <done>`task lint:markdown` exits 0, and a deliberate violation in a repo-root `.md` file is still reported by rumdl.</done>

  <acceptance_criteria>
    - `task lint:markdown` exits 0 (D-10 — acceptance is mechanical and binary).
    - The scope probe exits non-zero: a repo-root `.md` file with a deliberate MD022/MD024/MD041
      violation is still reported. This proves the exclude did not de-scope Markdown outside
      `.planning/` (SC3's second half). Confirmed working against the current tree: the probe
      reports 4 issues and exits 1. The probe file MUST be removed — it must never be committed.
    - `git diff .rumdl.toml` shows exactly one added line inside the `exclude` array.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Correct the Phase 21 acceptance list (D-00a, D-00b, D-12)</name>

  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>

  <read_first>
    - `.planning/ROADMAP.md` lines 459-471 — §"Phase 21: CI / Maintenance Hygiene", the
      Success Criteria list. SC2 is line 467, SC3 is line 468.
    - `.planning/REQUIREMENTS.md` lines 64-68 — §"CI / Maintenance Hygiene". REQ-p11-review-residuals
      is line 67, REQ-lint-planning-exclude is line 68.
    - `.planning/phases/21-ci-maintenance-hygiene/21-CONTEXT.md` §D-00a / §D-00b — the authority
      for both corrections.
  </read_first>

  <action>
    Two factual errors in the phase's acceptance list must be corrected (D-12). The ROADMAP is what
    the downstream verifier checks against, so leaving it wrong guarantees a verifier mismatch.

    **Correction 1 — ROADMAP SC2 mislabels IN-01 (D-00a).** SC2 currently describes IN-01 as
    "duplicate depth-gauge registration". That defect does not exist: the depth gauge is registered
    exactly once, at `internal/server/tools.go:255` via `telemetry.RegisterSummaryQueueDepth`. Both
    authoritative sources — the archived review
    (`.planning/milestones/v0.9.x-phases/11-async-on-write-summaries/11-REVIEW.md:265`) and GitHub
    issue #335 — define IN-01 as: `storeMemory` and `scheduleMemory` duplicate the
    Upsert-then-enqueue block verbatim. Rewrite SC2's IN-01 parenthetical to name that real finding.
    Leave SC2's WR-03 and IN-02 parentheticals untouched — both are accurate.

    **Correction 2 — ROADMAP SC3 carries a stale figure and the wrong exclude form (D-00b).**
    Replace the "331-failure" figure. Do NOT substitute a new hard-coded count: the real number
    drifts upward every time a planning doc lands (measured 1505 → 1514 → 1566 across three reads
    on 2026-07-15/16 — it was already stale again before this plan was written). Describe the noise
    qualitatively instead (e.g. "the systemic planning-doc noise"), optionally with an
    order-of-magnitude and an as-of date, never a precise count presented as durable fact. Also
    change SC3's `.planning/**` glob form to the plain `.planning` that Task 1 actually shipped, so
    the criterion matches the code. Keep SC3's "shipped Markdown outside `.planning/` is still
    linted" clause — it is accurate and is the half the Task 1 scope probe proves.

    **Correction 3 — mirror both fixes into REQUIREMENTS.md.** `REQUIREMENTS.md` carries the
    identical two errors (the same IN-01 mislabel on the REQ-p11-review-residuals line, and the same
    stale figure + glob form on the REQ-lint-planning-exclude line). D-12 names only the ROADMAP,
    but fixing one document and leaving its source-of-truth twin wrong just relocates the
    contradiction — apply the same two corrections there, keeping each line's existing structure and
    its `*(GitHub #NNN)*` citation intact.

    Do NOT change any requirement ID, any GitHub issue reference, the phase Goal line, the
    Depends-on line, or the Requirements line. Do NOT check off any requirement checkbox — those are
    closed by Plans 02/03 and the phase ship step, not here.
  </action>

  <verify>
    <automated>task lint:markdown</automated>
    <automated>grep -n 'depth-gauge' .planning/ROADMAP.md .planning/REQUIREMENTS.md; test $? -ne 0</automated>
    <automated>grep -n '331-failure' .planning/ROADMAP.md .planning/REQUIREMENTS.md; test $? -ne 0</automated>
  </verify>

  <done>ROADMAP SC2 names the real IN-01 finding, SC3 carries no stale count and the plain exclude form, and REQUIREMENTS.md mirrors both corrections — with every requirement ID, issue citation, and checkbox unchanged.</done>

  <acceptance_criteria>
    - Neither `.planning/ROADMAP.md` nor `.planning/REQUIREMENTS.md` contains the stale IN-01
      mislabel or the stale failure figure (both negative greps find no match).
    - ROADMAP SC2's IN-01 parenthetical names the `storeMemory`/`scheduleMemory` duplication.
    - ROADMAP SC3 names the plain `.planning` exclude form matching what Task 1 shipped, and
      carries no hard-coded issue count presented as durable fact.
    - `task lint:markdown` still exits 0 (both files are inside `.planning/`, so they are now
      excluded — this re-confirms Task 1 rather than testing Task 2; the greps are Task 2's real gate).
    - The three requirement IDs, both GitHub issue citations, and every checkbox state are unchanged.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer/CI → lint gate | `.rumdl.toml` defines what the `task lint:markdown` quality gate inspects. Weakening it silently reduces assurance on shipped prose. No untrusted input crosses this boundary; no runtime surface exists. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-01-01 | Tampering | `.rumdl.toml` `exclude` array | low | mitigate | An over-broad exclude (e.g. a glob, or excluding a shipped docs tree) would silently stop linting shipped prose while still exiting 0 — a green gate that checks nothing. Mitigated by the Task 1 scope probe, which proves a repo-root `.md` violation is still reported, and by the acceptance criterion that the diff is exactly one line inside the array. |
| T-21-01-02 | Repudiation | `.planning/ROADMAP.md` acceptance list | low | mitigate | An incorrect acceptance list lets a verifier mark a requirement green against a defect that does not exist (SC2's phantom depth-gauge duplication), or red against a stale figure. Mitigated by Task 2's corrections and its negative greps. |
| T-21-01-03 | Information Disclosure | `.planning/` contents | low | accept | Excluding `.planning/` from lint does not change its visibility — it was already committed and public. Lint scope has no bearing on disclosure. No mitigation warranted. |

No `T-21-01-SC` supply-chain row: this plan installs no npm/pip/cargo packages and adds no
dependency (RESEARCH.md §"Package Legitimacy Audit" — not applicable to this phase). ASVS L1,
block-on-high: no high or critical threats identified for this plan.
</threat_model>

<verification>
1. `task lint:markdown` exits 0.
2. The scope probe (repo-root `.md` with a deliberate violation) still exits non-zero, and the
   probe file is not left behind (`git status --porcelain` clean of `rumdl-scope-probe.md`).
3. `task` default (lint + test) is now runnable — it was blocked before this plan. It may still
   fail for unrelated reasons (e.g. Qdrant testcontainer availability); the gate being *reachable*
   is what this plan delivers, not the full suite being green.
4. Neither `depth-gauge` nor `331-failure` appears in `.planning/ROADMAP.md` or
   `.planning/REQUIREMENTS.md`.
</verification>

<success_criteria>
- REQ-lint-planning-exclude is satisfied: `task lint:markdown` exits 0 and Markdown outside
  `.planning/` is provably still linted.
- The Phase 21 acceptance list in ROADMAP.md and REQUIREMENTS.md is factually correct (D-12).
- Exactly one atomic commit, Conventional Commits format. Suggested:
  `chore(lint): exclude .planning from rumdl and correct phase 21 acceptance list`
- REQ-lint-planning-exclude has **no GitHub issue** (roadmap-only) — this commit closes nothing.
  Do not invent an issue reference.
</success_criteria>

<output>
Create `.planning/phases/21-ci-maintenance-hygiene/21-01-SUMMARY.md` when done.
</output>
