---
phase: 05-connect-record-state-parity
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - internal/server/connectapi_boundary_second_test.go
  - cmd/engram/client_schemaversion_json_test.go
autonomous: true
requirements:
  - REQ-connect-record-state-parity
  - REQ-connect-parity-roundtrip-proof
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    # edge:boundary (REQ-connect-parity-roundtrip-proof)
    - "A not_before submitted with a sub-second component comes back floored to the containing whole second and a not_after comes back ceiled to it; a bound already exactly on a whole second comes back unchanged; and both results are identical on the MCP and Connect read lanes."
    # edge:precision (REQ-connect-parity-roundtrip-proof)
    - "Rounding is OUTWARD, never nearest or half-up: not_before never advances the reveal time and not_after never truncates an expiry into the past. It happens on the write path only. Separately, schema_version crosses as uint32 and renders as a JSON NUMBER (not a quoted string, which is how protojson renders uint64); a Memory whose schema_version is ASSIGNED zero renders `\"schema_version\": 0`, and — per D-14, verified against protobuf-go v1.36.11 — a Memory whose schema_version is left UNSET omits the key entirely even under EmitDefaultValues. Both halves are asserted, the second as a permanent negative fixture, because the second is what makes D-14 §3's assign-always rule on memoryToProto load-bearing."
    # edge:idempotency (REQ-connect-parity-roundtrip-proof)
    - "Reading the same scheduled record twice through the same read lane returns byte-identical bounds — the stored value is already whole-second when it leaves the store, so no re-rounding accumulates across reads and no read-path rounding call exists to accumulate it. This is a SUPPORTING assertion, not central SC3 evidence: with no read-side rounding code today its mutation sensitivity is limited to newly introduced stateful or non-idempotent read-path conversion logic, so treat it as a tripwire for a future read-path rounding call (which D-09 forbids) rather than as proof of the current property."
    # edge:concurrency (REQ-connect-parity-roundtrip-proof)
    - "Each boundary-second sub-test seeds into a per-run UNIQUE scope (the fixed iso-test:project:boundary-second- prefix plus a suffix derived from the sub-test name and a process-unique token), computed once and used for both the write and the deferred DeleteAll. This minimizes cross-test contamination in the shared mem_eval_test collection and cleans up on normal completion. It does NOT claim isolation against a process killed before defers run, nor against a sibling that queries the whole collection — Qdrant collection membership is shared, and the plan does not assert otherwise."
    - "No read-path rounding code is added anywhere. The property holds because the store encodes scheduling bounds at whole-second granularity, and the proto field comments on not_before/not_after say so."
    - "The four new Timestamp fields are NOT asserted to be present-as-null in CLI JSON output for an unset bound — the CLI's marshal options emit zero scalars and empty repeated fields but leave unset message fields absent."
  artifacts:
    - "internal/server/connectapi_boundary_second_test.go — TestBoundarySecondReadLaneIdentity"
    - "cmd/engram/client_schemaversion_json_test.go — TestClientJSONSchemaVersionZeroVisible"
  key_links:
    - "The write path's outward rounding lives in formatWindowBound (internal/server/protoconv.go) and is DRIVEN by this test, never reimplemented — a second rounding call site is exactly what D-09 rejects."
    - "The MCP read lane returns store.Memory verbatim through its json tags; the Connect read lane goes through memoryToProto. The identity assertion is what proves plan 05-01's mapping did not introduce a lane divergence for the scheduling bounds. Because the named MCP mechanism IS the json tags, the MCP-side assertion must TRAVERSE them: json.Marshal the returned store.Memory and decode into map[string]json.RawMessage before asserting, mirroring internal/server/schemaversion_wire_test.go:246. Comparing Go struct fields directly does not exercise the mechanism the truth names, and stays green through a json:\"-\", omitempty, or tag-rename regression on NotBefore/NotAfter."
    - "renderJSON's UseProtoNames option is what makes schema_version reach engram list/search JSON output under its proto name. Under D-14 the KEY's PRESENCE no longer depends on EmitDefaultValues at all — verified: with the field assigned, EmitDefaultValues=false renders \"schema_version\":0 exactly as true does, and with the field unset neither setting emits it. Presence now depends entirely on memoryToProto assigning the field, which this plan does NOT exercise (its stub server builds the Memory by hand). The mapper-side gate is plan 05-02's zero-value sub-test; this plan gates the renderer half and the failure shape, and the SUMMARY must not merge the two into one claim."
---

<objective>
Prove the two read-side properties the roadmap's SC3 and CONTEXT's D-03 name, without adding a
single line of read-path rounding code (D-09) or a second rounding call site.

Purpose: SC3's honest property is not "the read path applies the write path's rounding" — it is
that the store's whole-second encoding makes read-side rounding a no-op by construction, so a
read-path rounding call would be a branch that can never change an outcome (the constant-gate shape
recorded in `k000pn14qp`). This plan makes that property observable instead of asserted. In
parallel it anchors Phase 2's D-10 promise — an operator asking what version a record is always
gets an answer — to the one CLI renderer whose marshal options actually deliver it.

Output: a boundary-second read-lane-identity test in `internal/server`, and a `renderJSON`
`schema_version: 0` anchor test in `cmd/engram`.
</objective>

<review_cycle_1_incorporation>
Cross-AI review cycle 1 raised one HIGH and three lower findings against this plan. All four are
incorporated below rather than left in `05-REVIEWS.md` (which the executor does not read).

1. **HIGH — the "MCP read lane" assertion did not traverse the mechanism the plan names.** The
   key_link says the MCP lane returns `store.Memory` verbatim *through its json tags*, but the
   cycle-1 task compared Go struct fields off `d.getMemory` with no marshal step, so a `json:"-"`,
   `omitempty`, or tag-rename regression on `NotBefore`/`NotAfter` left it green. Fixed in task 1:
   the MCP-side value is `json.Marshal`ed and decoded into `map[string]json.RawMessage` before any
   assertion, mirroring `internal/server/schemaversion_wire_test.go:246` — the precedent this plan
   already cited but applied only to the CLI sub-task. A RED proof against a tag rename is now
   mandatory.
2. **MEDIUM — the fixture-isolation truth overstated the mechanism.** Fixed scope strings over the
   shared `mem_eval_test` collection do not make a concurrent or killed sibling unable to observe
   or corrupt the fixture. The truth is narrowed to what the mechanism gives, and the scope now
   carries a per-run unique suffix used for both write and cleanup.
3. **LOW — repeat-read idempotency framing.** Demoted in the truth and in the task text to a
   supporting tripwire; it is not cited as central SC3 evidence.
4. **LOW — requirement routing too broad.** Addressed as a partial accept; see
   `<requirement_attribution>` below for what changed and the rationale for what did not.
</review_cycle_1_incorporation>

<requirement_attribution>
Cycle 1 asked that `REQ-connect-parity-roundtrip-proof` be attributed to 05-02 and this plan be
described as supporting SC3 and D-03. Adopted at the task level; the frontmatter keeps both IDs.

- **Task 1 (boundary-second read-lane identity)** — serves `REQ-connect-record-state-parity` and
  contributes SUPPORTING evidence to `REQ-connect-parity-roundtrip-proof`: it is a genuine
  write-then-read round trip across two lanes, which is why the ID stays on this plan and why the
  edge-case markers on this plan's truths remain tagged to it.
- **Task 2 (CLI `renderJSON` anchor)** — serves `REQ-connect-record-state-parity`, D-03 and SC1
  ONLY. It contributes NOTHING to `REQ-connect-parity-roundtrip-proof` and must not be cited as
  round-trip evidence in the SUMMARY.
- **OWNERSHIP:** the exhaustive field-by-field round-trip proof that requirement names is owned
  entirely by **05-02**. Nothing in this plan is exhaustive over the field set, and no verification
  of 05-02's requirement should look here for it.

Partial-accept rationale for keeping both IDs in frontmatter rather than dropping one: this plan's
edge-case-marked truths (boundary, precision, idempotency, concurrency) are tagged to
`REQ-connect-parity-roundtrip-proof`, and dropping the ID would leave those requirement tags
dangling while gaining only what the per-task attribution above already states explicitly.
</requirement_attribution>

<review_cycle_3_incorporation>
Cycle 3 raised one HIGH (systemic across all three plans) against this plan, in BOTH of its forms.

1. **HIGH (C3-H1), inverse form — `git diff --name-only` at task end could never be SATISFIED.**
   `execute-plan.md:194` commits the task, `:197-203` THEN runs the acceptance HARD GATE, and
   `:158-159` re-runs ALL acceptance criteria while writing the SUMMARY. The working tree is clean
   at every one of those points, so a bare `git diff --name-only` lists ZERO files — and a criterion
   reading "lists only the new test file" is not vacuous but UNSATISFIABLE. A diligent executor
   burns its two fix attempts on already-correct work; a lax one rubber-stamps it. Reproduced:
   post-commit, `git status --porcelain` and `git diff --name-only` both returned empty.

2. **HIGH (C3-H1), vacuous form — `git status --porcelain <production file>` cannot detect an
   already-committed production edit.** Cycle 3 judged this plan's two `git status` criteria still
   CORRECT for their primary purpose (proving the transient json-tag RED-proof mutation was
   reverted, observed in-task before the commit), and that judgement is accepted. Both are KEPT as
   in-task revert evidence and PAIRED with a commit-range assertion, rather than replaced.

   Decision for both forms: `<commit_range_protocol>` below, spelled as an ALLOWLIST over PLAN_BASE.
   Two shape choices, each established by running it rather than reasoning about it:
   - **Allowlist, not "only file X".** A base-to-working-tree diff always spans FORWARD to the
     moment it is evaluated, so at the SUMMARY re-run a task-1-scoped "only the new test file"
     criterion would also see task 2's file and fail on correct work. An allowlist over this plan's
     own `files_modified` is stable at every evaluation point and proves the property that actually
     matters, which `<verification>` already states outright: no production file was touched.
   - **`git diff <PLAN_BASE>`, not `<PLAN_BASE>..HEAD`.** The two-dot form compares two COMMITS and
     cannot see uncommitted work; the base-to-working-tree form is a strict superset and covers the
     in-task window as well, so one command holds at every evaluation point.

   Non-emptiness is asserted FIRST in every case: verified that on empty input the allowlist check
   exits 1 and would "pass" without having examined anything — the same defect one level up.
</review_cycle_3_incorporation>

<d14_amendment>
D-14 (2026-08-15, Sean) SUPERSEDES D-01 and D-02: `superseded_by` (23), `schema_version` (28) and
`summary_model` (29) all take explicit presence — `optional`. Task 1 (boundary-second) is
UNAFFECTED: the scheduling bounds are `google.protobuf.Timestamp` message fields, which already
carried explicit presence in proto3. **Task 2 is materially affected and is restructured below.**

**What changed, observed against `google.golang.org/protobuf v1.36.11` in a throwaway module —
including generated code from this repo's own pinned `buf.build/protocolbuffers/go:v1.36.11`:**

| message state | `{UseProtoNames, EmitDefaultValues:true}` | `{UseProtoNames}` only |
|---|---|---|
| `&Memory{}` | `schema_version` key **ABSENT** | key ABSENT |
| `&Memory{SchemaVersion: proto.Uint32(0)}` | `"schema_version":0` | `"schema_version":0` |

Two consequences, and the second is the reason task 2 could not be left as written:

1. **`EmitDefaultValues` is now IRRELEVANT to this field.** It forces zero values for
   IMPLICIT-presence fields only; an ASSIGNED `optional` renders with or without it, and an UNSET
   one renders under neither (`EmitUnpopulated:true` does not rescue it either — also observed).
   D-03's original framing — gating "the coupling between D-10's promise and one renderer's marshal
   options" — describes a coupling that D-14 dissolved for `schema_version`. What remains coupled
   to `renderJSON`'s options is `UseProtoNames` (the key's SPELLING) and `EmitDefaultValues` for
   the implicit-presence fields around it. Say that, not the old thing.

2. **Task 2's cycle-1..3 instruction is now actively wrong and would produce a FAILING test.** It
   said: *"the stub's `Memory` leaves `SchemaVersion` at its Go zero value (do NOT assign a literal
   `0`)"*. Under D-14 the Go zero value is a nil `*uint32`, so the key would be absent and the
   assertion would fail. Worse, the naive repair — assign `proto.Uint32(0)` and move on — makes the
   sub-test a tautology: the stub author decides whether to assign, and the test asserts what the
   stub author decided. That is exactly the vacuous-gate class this phase exists to eliminate.

**Restructure, stated once here so the task text below is unambiguous.** Task 2 keeps its stub
harness and gains a PERMANENT NEGATIVE FIXTURE (per `k000pn14qp`: prove the FILTER can match, not
merely that the producer emitted rows). The assigned case asserts the key is PRESENT; the sibling
unassigned case asserts the key is ABSENT. The pair is what makes the presence assertion measure
something real, and the absent case is the executable record of the failure mode D-14 §3 exists to
prevent.

**Scope, stated plainly and not to be blurred in the SUMMARY.** The stub builds its `Memory` by
hand, so this task never invokes `memoryToProto`. It characterizes the RENDERER given a message in
a known state. The MAPPER half — that `memoryToProto` assigns `schema_version` even for a v0
record — is gated by plan 05-02's zero-value sub-test and its RED PROOF 3. The operator-visible
guarantee is the CONJUNCTION of the two, and no test in this phase spans the seam between them.
Tracked as **#499** — a known, accepted gap for this phase, not an oversight. Closing it needs a
real-server CLI harness in `cmd/engram`, which is out of scope here.
</d14_amendment>

<commit_range_protocol>
Two acceptance criteria below and one plan-level `<verification>` line assert that THE CHANGE THIS
PLAN MADE touches no production file. They must not be spelled over the bare working tree.

Why: `execute-plan.md:194` commits the task as part of completing it; only THEN does the HARD GATE
at `:197-203` run the acceptance loop, and `:158-159` re-runs ALL acceptance criteria later still.
At both evaluation points the working tree is CLEAN — so `git diff --name-only` lists nothing (a
criterion demanding it list a file can never be met) and `git status --porcelain <path>` is empty
regardless of whether that path was committed with a mutation in it.

Protocol, performed by you at the START of task 1, BEFORE your first edit:

1. Run `git rev-parse HEAD` and record the 40-character SHA. Call it PLAN_BASE.
2. Shell variables do NOT persist between tool calls in this runtime (verified). Do not write
   `$PLAN_BASE` and expect it to be set — substitute the LITERAL SHA into every command below.
3. Quote the recorded PLAN_BASE SHA in the SUMMARY so the criteria you ran are reproducible.

Spelling: `git diff --name-only <PLAN_BASE>` — base against the WORKING TREE, with NO `..HEAD`, so
it sees committed AND uncommitted changes alike. NON-EMPTY IS MANDATORY: assert `[ -s ... ]` on the
captured output FIRST, before any negative check runs over it.

This plan's allowlist is exactly its two owned test files plus `.planning/`:
`internal/server/connectapi_boundary_second_test.go` and
`cmd/engram/client_schemaversion_json_test.go`.
</commit_range_protocol>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-connect-record-state-parity/05-CONTEXT.md
@.planning/phases/05-connect-record-state-parity/05-PATTERNS.md
@.planning/phases/05-connect-record-state-parity/05-01-SUMMARY.md
</context>

<roadmap_status>
ROADMAP SC3 was ALREADY corrected in commit `4ff2df5b` to state the property that actually ships
(no read-path rounding; the store's whole-second encoding makes it a no-op). The `/gsd-phase edit`
D-09 called for at plan time is DONE. Do not re-edit it.
</roadmap_status>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Boundary-second read-lane identity (D-09)</name>
  <files>internal/server/connectapi_boundary_second_test.go</files>
  <precondition>A Qdrant instance is reachable for the `internal/server` suite — `ENGRAM_QDRANT_TEST_ADDR` is set or Docker is running for testcontainers. This test must traverse the real store encode/decode path (`.Unix()` flooring), so it is run with `ENGRAM_REQUIRE_QDRANT=1`: an absent Qdrant FAILS rather than silently skipping, because a skipped run proves nothing about the store's granularity.</precondition>
  <read_first>
    - internal/server/protoconv.go:123-196 (`scheduleMemoryRequestToArgs`, `windowBoundFloor`, `windowBoundCeil`, `formatWindowBound` — the write-path outward rounding this test DRIVES and must not reimplement, and its comment naming the store's `.Unix()` floor sites)
    - internal/store/store.go:320,323,406,410 (the `.Unix()` encode/decode flooring that makes read-side rounding a no-op)
    - internal/server/connectapi.go:298-332 (`GetMemory`) and :425-446 (`ScheduleMemory`)
    - internal/server/schemaversion_wire_test.go:1-40 and :213-330 (the seed-then-assert-on-a-real-lane shape, the `iso-test:project:*` scope + `defer cleanupErr(... DeleteAll ...)` isolation idiom, and `dialRawQdrantClient` at :150-170 if a hand-shaped record proves necessary). Read :238-266 CLOSELY — the `json.Marshal(got)` → `map[string]json.RawMessage` → key-presence → decode sequence at :246 is the exact MCP-lane discipline this task must copy, and the doc comment at :213-220 states why `d.getMemory` is the served MCP path.
    - internal/server/tools.go — the `get_memory` `mcp.AddTool` registration (search for `"get_memory"`): confirm for yourself that the registered handler wraps `d.getMemory` and returns its `store.Memory` as the structured result, i.e. that serialization happens AFTER the call this test makes. That is why the marshal step below is required rather than optional.
    - internal/server/connectapi_write_parity_test.go:60-80 (`parityConnectCtx`)
    - internal/server/tools_test.go:364-400 (`testDepsWithStore`), :452-507 (`cleanupErr`, `authedContext`, `callerFor`)
    - .planning/phases/05-connect-record-state-parity/05-CONTEXT.md §"Round-Trip and Rounding" (D-09 in full)
  </read_first>
  <behavior>
    - A `not_before` submitted as `T + 500ms` comes back as `T` (floored) on BOTH read lanes.
    - A `not_after` submitted as `T + 500ms` comes back as `T + 1s` (ceiled) on BOTH read lanes.
    - A bound submitted exactly on a whole second comes back unchanged on both lanes — one step either side of the threshold behaves as stated, and the exact-threshold case does not shift.
    - The MCP-lane value and the Connect-lane value for the same record and the same bound are equal to the instant, not merely to the second.
    - The MCP-lane value is read out of the record's SERIALIZED json form (`json.Marshal` → `map[string]json.RawMessage` → `not_before`/`not_after`), not off Go struct fields — so the json tags the MCP lane actually ships through are exercised.
    - Renaming or `json:"-"`-ing `store.Memory.NotBefore`'s tag makes this test FAIL rather than pass silently.
    - Reading the same record twice on the same lane returns the same value — no accumulating re-rounding. (Supporting tripwire, not central evidence.)
  </behavior>
  <action>
**FIRST, before any edit:** run `git rev-parse HEAD` and record the literal SHA as PLAN_BASE, per
`<commit_range_protocol>`. Two acceptance criteria and this plan's `<verification>` are unrunnable
without it, and it cannot be recovered after the fact once work has committed.

Create `internal/server/connectapi_boundary_second_test.go` (package `server`, Apache-2.0 SPDX
header matching adjacent test files) with `TestBoundarySecondReadLaneIdentity`.

**Harness.** `d, _ := testDepsWithStore(t)` for a real store; `api := &engramAPI{d: d}`. Choose ONE
owner string and use it for both `parityConnectCtx(owner)` (Connect lane) and
`authedContext(t, owner)` (MCP lane) — both helpers write the same `owner_claim` key, so the same
owner resolves on both lanes and both can read the one seeded record.

Scope isolation, stated at the strength the mechanism actually provides (review cycle 1, MEDIUM):
compute the scope ONCE per sub-test as the fixed prefix `iso-test:project:boundary-second-` plus a
per-run unique suffix — the sub-test name plus a process-unique token (e.g. a package-level
`atomic.AddUint64` counter combined with `os.Getpid()`, or a UUID) — hold it in a local, and use
that SAME value for the write and for `defer cleanupErr(t, ..., d.st.DeleteAll(...))`. Never
recompute the scope string at the cleanup site. What this buys: a concurrently-running sibling or a
re-run after a crash writes to a different scope, so leftover points from a previous run cannot be
read as this run's fixture. What it does NOT buy, and what the plan therefore does not claim: the
`mem_eval_test` collection is shared, defers do not run when a process is killed, and a sibling
that queries the whole collection still sees these points.

**Write once, through the real Connect lane.** Call `api.ScheduleMemory` with `NotBefore` and
`NotAfter` `*timestamppb.Timestamp` values carrying a deliberate sub-second component. Pick a base
instant well clear of "now" so the handler's own window validation does not reject the record
(`ScheduleMemory` validates the window against the deps clock — read the handler before choosing
the offsets). This is the only write; do not seed a second copy for the second lane.

Compute the expected values from D-09's stated semantics, spelled out literally rather than by
calling the production rounding helper: `not_before` floors to the containing whole second,
`not_after` ceils to it. Rounding is OUTWARD — `not_before` never advances the reveal time and
`not_after` never truncates an expiry into the past. Do NOT reach for the production window-bound
rounding helpers declared in `protoconv.go` (read them in `read_first`) from the assertion side:
an expected value computed by the same function under test is a tautology.

**Read twice, once per lane.**

MCP lane — **and this must traverse the json tags, not just the struct** (review cycle 1, HIGH).
`d.getMemory(mcpCtx, callerFor(mcpCtx, t), idArgs{ID: id})` returns the `store.Memory` that the
registered `get_memory` tool handler hands back as its structured result; the MCP wire shape is
produced by marshalling THAT value through its json tags. So do what
`internal/server/schemaversion_wire_test.go` already does at its marshal site (read it in
`read_first`): `json.Marshal` the returned value, `json.Unmarshal` into
`map[string]json.RawMessage`, assert the `not_before` and `not_after` keys are PRESENT, and decode
each into a `time.Time`. Assert on those decoded values. Do NOT assert on
`got.NotBefore`/`got.NotAfter` struct fields as the lane's evidence — a `json:"-"`, an added
`omitempty` misfire, or a tag rename on either field would leave a struct-field comparison green
while the MCP lane silently stopped shipping the bound. Reading the struct field additionally, as a
sanity check on top of the serialized assertion, is fine.

Connect lane: `api.GetMemory` returns the proto message — read `not_before`/`not_after` and decode
with `.AsTime()`.

Assert:
1. each lane's value equals the expected outward-widened instant exactly;
2. the two lanes' values are equal to each other (this is SC3's "both lanes" — MCP and Connect),
   comparing the MCP lane's DECODED-FROM-JSON instant against the Connect lane's `.AsTime()`;
3. calling each lane's read a second time returns the identical value. Frame this in the SUMMARY as
   a supporting tripwire (review cycle 1, LOW): there is no read-path rounding code today, so its
   sensitivity is to a FUTURE stateful or non-idempotent read-side conversion — it is not evidence
   for the current property, which comes from (1) and (2).

**RED PROOF (mandatory, performed by you, recorded in the SUMMARY).** With the test green,
temporarily rename `store.Memory.NotBefore`'s json tag from `not_before` to
`not_before_tmp_probe` in `internal/store/store.go` (the store persists through its own manual
`payload()`/`fromPayload()` codec, not through json tags, so this changes the wire shape without
changing what is stored). Run
`ENGRAM_REQUIRE_QDRANT=1 go test ./internal/server/ -run '^TestBoundarySecondReadLaneIdentity$' -count=1 -v`
and confirm it FAILS on the missing `not_before` key. Capture the output verbatim. Record in the
SUMMARY that a struct-field comparison — the cycle-1 shape — would have stayed GREEN under this
exact mutation. Revert the tag and confirm green; `git status --porcelain internal/store/store.go`
must be empty at task end.

**Sub-test for the exact-threshold case.** Repeat with bounds already exactly on a whole second
(nanosecond component zero) in a second dedicated scope, and assert both bounds come back unchanged
on both lanes. This is the "one step either side of the threshold" half of the boundary edge.

**No production code changes.** D-09 is explicit: this phase adds NO read-path rounding. The test
drives the existing write-path helper and observes the store's existing whole-second encoding. If
the test fails, the correct response is to investigate the store/mapper, not to add a rounding call
on the read side.
  </action>
  <verify>
    <automated>ENGRAM_REQUIRE_QDRANT=1 go test ./internal/server/ -run '^TestBoundarySecondReadLaneIdentity$' -count=1 -v</automated>
    <automated>go test ./internal/server/ -count=1</automated>
    <automated>go test ./internal/store/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `ENGRAM_REQUIRE_QDRANT=1 go test ./internal/server/ -run '^TestBoundarySecondReadLaneIdentity$' -count=1 -v` passes and its output contains no `--- SKIP` line.
    - `rg -c 'formatWindowBound|windowBoundFloor|windowBoundCeil' internal/server/connectapi_boundary_second_test.go` returns no matches (exit status 1) — the expected values are computed independently of the function under test, so the assertion is not a tautology.
    - COMMIT-RANGE ALLOWLIST (see `<commit_range_protocol>` — substitute the literal PLAN_BASE SHA): run `git diff --name-only <PLAN_BASE> > /tmp/05-03-names.txt`, then assert ALL THREE: (a) `[ -s /tmp/05-03-names.txt ]` holds — an EMPTY list is a FAILURE, not a pass; (b) `rg -q '^internal/server/connectapi_boundary_second_test\.go$' /tmp/05-03-names.txt` succeeds — this task's file is actually in the change; (c) `rg -v '^(internal/server/connectapi_boundary_second_test\.go|cmd/engram/client_schemaversion_json_test\.go|\.planning/)' /tmp/05-03-names.txt` returns no matches (exit 1) — no file under `internal/store/`, no `internal/server/protoconv.go`, and no production file anywhere appears in this plan's change, so no read-path rounding call was added (D-09) and the RED proof's transient json-tag rename in `internal/store/store.go` was genuinely reverted rather than committed. The bare `git diff --name-only` spelling was REJECTED as UNSATISFIABLE: reproduced post-commit, it lists ZERO files, so "lists only the new test file" can never be met and an executor would burn its two fix attempts on already-correct work. The sibling test file is inside the allowlist deliberately — a base-to-working-tree diff spans forward to the moment it is evaluated, so at the SUMMARY re-run this criterion also sees task 2's file, and an "only file X" phrasing would fail on correct work.
    - The test writes the record exactly once (a single `api.ScheduleMemory` call per sub-test) and reads it on both lanes, so a lane divergence cannot be masked by two independently-seeded fixtures.
    - The MCP-lane assertion is made against values decoded from `json.Unmarshal` of `json.Marshal(got)` into `map[string]json.RawMessage`, with `not_before` and `not_after` key PRESENCE asserted before value comparison — `rg -c 'json.Marshal\(' internal/server/connectapi_boundary_second_test.go` is at least `1` and `rg -c 'map\[string\]json.RawMessage' internal/server/connectapi_boundary_second_test.go` is at least `1`. A struct-field-only comparison does NOT satisfy this criterion: it leaves the test green through a `json:"-"`, `omitempty`, or tag-rename regression, which is the cycle-1 HIGH finding.
    - RED PROOF RECORDED: with `store.Memory.NotBefore`'s json tag temporarily renamed to `not_before_tmp_probe`, the captured `-v` output shows `--- FAIL` on the missing `not_before` key; the SUMMARY quotes it and states that a struct-field comparison would have stayed green. After revert, the command passes and `git status --porcelain internal/store/store.go` is empty. This criterion is NOT satisfied by "the test passes".
    - The MCP-vs-Connect assertion compares instants for equality directly, not truncated-to-second values on either side — truncating before comparing would make the assertion pass regardless of the property.
    - Each sub-test computes ONE unique scope string (fixed `iso-test:project:boundary-second-` prefix + sub-test name + a process-unique token) into a local and uses that same local for both the write and the deferred `DeleteAll` — the cleanup never recomputes the scope.
    - `go test ./internal/server/ -count=1` and `go test ./internal/store/ -count=1` both pass unfiltered.
  </acceptance_criteria>
  <done>A sub-second bound written once through Connect comes back outward-widened and identical on both the MCP and Connect read lanes — with the MCP side read out of the record's serialized json form, proven RED against a json-tag rename; a whole-second bound comes back unchanged; repeat reads are stable (supporting); and no read-path rounding code was added.</done>
  <reversibility rating="reversible">Test-only; no production change.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate the operator-visible schema_version rendering, presence and absence (D-03, D-14)</name>
  <files>cmd/engram/client_schemaversion_json_test.go</files>
  <read_first>
    - cmd/engram/client_common.go:369-391 (`renderJSON` — `UseProtoNames: true`, `EmitDefaultValues: true`, `Multiline: false`; the comment explaining why both options are load-bearing)
    - cmd/engram/client_list_test.go:18-64 (`TestClientListEndToEndJSON` — the exact stub-service + `startStubServer` + `runClient` + JSON-decode shape to copy, including the note that protojson renders uint64 as a JSON string)
    - cmd/engram/clienttest_test.go:28-130 (`stubEngramService`, its `listFn`/`searchFn`/`storeFn` hooks, `startStubServer`, `resetClientFlags`, `resetCommandFlagState`, `runClient`)
    - cmd/engram/client_list.go:80-95 (where `renderJSON` is called for `--output json`)
    - .planning/phases/05-connect-record-state-parity/05-PATTERNS.md § the renderJSON section (the `EmitDefaultValues` vs `EmitUnpopulated` pitfall, stated in full)
    - internal/store/store.go:315-353 (`SchemaVersion`'s comment — Phase 2's D-10 promise this test anchors)
  </read_first>
  <behavior>
    - `engram list --output json` against a stub returning a `Memory` whose `SchemaVersion` is ASSIGNED zero (`proto.Uint32(0)` — the state `memoryToProto` produces for a v0 record under D-14 §3) renders a `schema_version` key whose value is the JSON number `0` — not an omitted key, not a quoted `"0"`.
    - The same command against a stub returning a `Memory` whose `SchemaVersion` is left NIL renders NO `schema_version` key at all. This is the permanent negative fixture: it proves the presence assertion above can fail, and it is the executable record of what D-14 §3's assign-always rule on `memoryToProto` prevents.
    - A non-zero `SchemaVersion` renders as the same JSON number type, confirming `uint32` (number) and not `uint64` (string) semantics.
    - The four new Timestamp fields are ABSENT from the JSON when unset — asserted as absent, never as present-and-null.
  </behavior>
  <action>
Create `cmd/engram/client_schemaversion_json_test.go` (package `main`, Apache-2.0 SPDX header
matching adjacent test files) with `TestClientJSONSchemaVersionZeroVisible`.

Requirement attribution for THIS task (review cycle 1, LOW): it serves D-03 and SC1 only. It
contributes nothing to `REQ-connect-parity-roundtrip-proof` — that requirement's exhaustive
field-by-field proof is owned by plan 05-02 — so do not describe this test as round-trip evidence
in the SUMMARY. See `<requirement_attribution>` above.

Phase 2's D-10 recorded the promise "an operator asking what version is this record always gets
one", and rated it *costly* precisely because this phase freezes it onto a proto field number.

**Read `<d14_amendment>` before writing a line of this.** D-14 moved where that promise is kept.
It is no longer an emergent side effect of `renderJSON`'s `EmitDefaultValues` — observed, that flag
makes no difference to an `optional` field either way. The promise now rests on `memoryToProto`
assigning the field, which this task cannot see: the stub builds its `Memory` by hand. This task
gates the RENDERER half and pins the FAILURE SHAPE; plan 05-02 gates the mapper half. Do not write
a SUMMARY sentence that merges them.

Copy `TestClientListEndToEndJSON`'s harness exactly: `resetClientFlags(t)`,
`resetCommandFlagState(t, listCmd)`, a `stubEngramService` whose `listFn` returns a
`ListMemoriesResponse` carrying one `*engramv1.Memory`, `startStubServer(t, svc)`, then
`runClient(t, "list", "--server", url, "--scope", ..., "--output", "json")`.

Sub-tests:
- `assigned-zero schema_version renders as 0`: the stub's `Memory` sets
  `SchemaVersion: proto.Uint32(0)` EXPLICITLY. This is not a shortcut — it is the state
  `memoryToProto` produces for a v0 record under D-14 §3, so the fixture mirrors production rather
  than a convenient literal. (The cycle-1..3 instruction here was the opposite — "do NOT assign a
  literal 0" — and D-14 makes that instruction produce a failing test, because the Go zero value is
  now a nil `*uint32`. It is reversed deliberately.) Decode stdout into
  `map[string]json.RawMessage`, take `memories`, decode element 0 into another
  `map[string]json.RawMessage`, assert the `schema_version` key EXISTS, and decode its raw value
  into a `uint32` (or `json.Number`) asserting it equals 0. Assert on key presence in a decoded
  map, not on a substring of stdout: a value containing the text would inflate a substring count,
  and a duplicate key cannot survive decoding into a map. This mirrors the discipline
  `TestSchemaVersionOnRecallWire` already uses on the MCP side.
- `unassigned schema_version is OMITTED — the permanent negative fixture`: an otherwise identical
  stub whose `Memory` leaves `SchemaVersion` nil. Assert the `schema_version` key is ABSENT from
  the decoded memory object (`_, ok := decoded["schema_version"]`; `ok` must be false). Document
  directly above it what it is for, because on its own it reads like a test of nothing: paired with
  the sub-test above it is what proves that presence assertion can FAIL — per `k000pn14qp`, an
  anti-vacuity guard must show the FILTER can match, not merely that the producer emitted rows.
  Standing alone, "the key is present" is satisfiable by a renderer that emits every key
  unconditionally. Document also that `EmitDefaultValues: true` does NOT rescue the unset field —
  verified against protobuf-go v1.36.11, and neither does `EmitUnpopulated` — which is exactly why
  `memoryToProto` must assign unconditionally (D-14 §3) and why plan 05-02 carries a RED proof
  against making that assignment conditional. Deleting or skipping this sub-test converts its
  sibling into a gate that can only ever pass.
- `schema_version renders as a JSON number, not a string`: with a NON-ZERO
  `SchemaVersion: proto.Uint32(N)`, assert the raw value does not begin with a double-quote byte.
  This pins D-04's `uint32`-over-`uint64` choice, which D-14 did NOT disturb —
  `client_list_test.go` already documents that protojson renders `uint64` as a JSON string, so a
  future widening to `uint64` would silently change the CLI's output type and this catches it.
- `unset scheduling bounds are absent, not null`: with the stub's `Memory` leaving `NotBefore`,
  `NotAfter`, `ArchivedAt` and `SummaryEgressAt` unset, assert those four keys are ABSENT from the
  decoded memory object. `renderJSON` sets `EmitDefaultValues` and NOT `EmitUnpopulated`: default
  values covers zero scalars and empty repeated/map fields, but an unset singular MESSAGE field
  (which every `google.protobuf.Timestamp` is) stays out of the document entirely. Asserting these
  keys are present-and-null would be testing behavior this project has not opted into.

Do not modify `renderJSON` or any production file — this task targets an existing function
unchanged.
  </action>
  <verify>
    <automated>go test ./cmd/engram/ -run '^TestClientJSONSchemaVersionZeroVisible$' -count=1 -v</automated>
    <automated>go test ./cmd/engram/ -count=1</automated>
    <automated>task lint:go</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd/engram/ -run '^TestClientJSONSchemaVersionZeroVisible$' -count=1 -v` passes and its output contains no `--- SKIP` line.
    - `git status --porcelain cmd/engram/client_common.go` is empty — `renderJSON` was not modified. On its own this is silent post-commit (a `client_common.go` committed with an edit in it also shows a clean status), so it is paired with the next criterion rather than trusted alone.
    - COMMIT-RANGE ALLOWLIST (see `<commit_range_protocol>` — substitute the literal PLAN_BASE SHA): `git diff --name-only <PLAN_BASE>` is non-empty, contains `cmd/engram/client_schemaversion_json_test.go`, and `rg -v '^(internal/server/connectapi_boundary_second_test\.go|cmd/engram/client_schemaversion_json_test\.go|\.planning/)'` over it returns no matches (exit 1) — `cmd/engram/client_common.go` is provably absent from this plan's whole change, committed or not.
    - The presence assertion is made against a decoded `map[string]json.RawMessage` key, and `rg -c 'strings.Contains' cmd/engram/client_schemaversion_json_test.go` returns no matches (exit status 1) — no substring-on-stdout assertion is used.
    - The assigned-zero sub-test's stub `Memory` sets `SchemaVersion` to a non-nil pointer addressing zero, mirroring what `memoryToProto` produces for a v0 record under D-14 §3. Leaving it at the Go zero value does NOT satisfy this criterion: under D-14 that value is nil, the key is omitted, and the sub-test fails. (This criterion INVERTS the cycle-1..3 one it replaces, which required the opposite and was made unsatisfiable by D-14.)
    - The permanent negative sub-test exists, its stub `Memory` leaves `SchemaVersion` nil, and it asserts the `schema_version` key is ABSENT via `_, ok := decoded["schema_version"]` with `ok` false. Both sub-tests run on every CI run. Without this one its sibling is satisfiable by a renderer that emits every key unconditionally, so a presence-only assertion would never have been shown capable of failing — the `k000pn14qp` rule, applied to the renderer.
    - The two sub-tests differ ONLY in the stub's `SchemaVersion` assignment. If any other field, flag, or harness detail differs between them, the pair no longer isolates presence and does not satisfy this criterion.
    - `rg -c 'proto\.Uint32\(' cmd/engram/client_schemaversion_json_test.go` is at least `2` — the assigned-zero and the non-zero number-rendering fixtures both assign through a pointer helper. The negative fixture is the one that must NOT.
    - The four Timestamp keys are asserted ABSENT (a `_, ok := decoded[k]; ok` failure), never asserted present-and-null. This remains correct under D-14 and was re-verified rather than assumed: an unset singular message field is absent under `EmitDefaultValues:true` and renders `null` only under `EmitUnpopulated:true`, which this project does not set.
    - `go test ./cmd/engram/ -count=1` passes unfiltered and `task lint:go` is clean.
  </acceptance_criteria>
  <done>An ASSIGNED-zero `schema_version` renders as `0` in `engram list --output json`, an UNASSIGNED one is provably omitted (permanent negative fixture), the number-not-string rendering is pinned, and the unset-Timestamp behavior is asserted as this project's marshal options actually provide it. The mapper-side half of the operator guarantee is 05-02's and is not claimed here.</done>
  <reversibility rating="reversible">Test-only; no production change.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Connect write RPC → `formatWindowBound` → store `.Unix()` encode → both read lanes | A scheduling bound crosses three representations; an inconsistency between the two read lanes means two callers disagree about when a record reveals or expires. |
| CLI stdout → operator | The rendered JSON is what an operator reads to answer "what version is this record"; an omitted key reads as "no answer" rather than "v0". |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-09 | Tampering | scheduling-window bounds across lanes | high | mitigate | One write, two reads, exact-instant equality asserted between the MCP and Connect lanes; the expected value is computed independently of the production rounding helper (asserted by a source criterion) so the check cannot be a tautology. The MCP side is read out of the record's SERIALIZED json form (`json.Marshal` → `map[string]json.RawMessage` → key presence → decode), so the json tags that lane actually ships through are traversed rather than bypassed; proven RED against a `not_before` tag rename. |
| T-05-14 | Tampering | `store.Memory` json tags on the MCP read lane | high | mitigate | A `json:"-"`, an `omitempty` misfire, or a tag rename on `NotBefore`/`NotAfter` silently removes a scheduling bound from every MCP consumer while every Go-struct-level assertion stays green. Caught by asserting key PRESENCE in the decoded json map before comparing values, mirroring `internal/server/schemaversion_wire_test.go`'s marshal-then-decode discipline, and carrying its own RED proof. |
| T-05-10 | Information Disclosure | `not_after` truncation | high | mitigate | Rounding is asserted OUTWARD: a sub-second `not_after` is widened to the containing whole second, never floored — a floor would silently collapse a scheduled expiry to immediate-expiry and drop a record out of recall early. Both the sub-second and the exact-threshold cases are covered. |
| T-05-11 | Repudiation | operator's view of record version | medium | mitigate | `schema_version: 0` presence is gated on a decoded map key rather than a stdout substring, so a value merely containing the text cannot register as a pass and a duplicate key cannot survive the decode; and the presence assertion is paired with a permanent negative fixture (unassigned → key absent) proving it can fail. Scope limit, stated rather than implied: under D-14 the key's presence is decided by `memoryToProto`, which this stub-served test does not invoke — that half is T-05-15 in plan 05-02. |
| T-05-12 | Tampering | test fixture isolation in the shared Qdrant collection | medium | mitigate | Each boundary sub-test computes one per-run UNIQUE scope (fixed prefix + sub-test name + process-unique token) and uses that same value for the write and the deferred `DeleteAll`, so a concurrent sibling or a re-run after a crash reads a different scope and cannot be mistaken for this run's fixture. Residual risk ACCEPTED and stated rather than papered over: `mem_eval_test` is a shared collection, defers do not run on a killed process, and a whole-collection query still sees these points. |
| T-05-13 | Denial of Service | silent test skip with no Qdrant | medium | mitigate | The boundary test runs under `ENGRAM_REQUIRE_QDRANT=1`, which turns the absent-Qdrant path into a failure rather than a skip; the verify asserts no skip line in the `-v` output. |
| T-05-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task exists in this plan; no new external dependency is added. `05-RESEARCH.md` § Package Legitimacy Audit records this. |
</threat_model>

<artifacts_this_phase_produces>
Symbols and paths created by THIS plan (new — not pre-existing, not drift):

**New files:**
- `internal/server/connectapi_boundary_second_test.go`
- `cmd/engram/client_schemaversion_json_test.go`

**New Go test functions:**
- `TestBoundarySecondReadLaneIdentity` (package `server`)
- `TestClientJSONSchemaVersionZeroVisible` (package `main`)

**No production symbol, no new file path, no new CLI flag, and no new struct field is added by this
plan.** `renderJSON`, `formatWindowBound`, `windowBoundFloor`, `windowBoundCeil` and the store's
`.Unix()` encoding are all pre-existing and are DRIVEN, never modified (D-09).

Sibling plans add: proto fields 23-30, the extended `memoryToProto`, and
`TestConnectRecordStateOnGetMemoryHandler` (05-01); `storeJSONVisibleFields`,
`storeToProtoFieldAlias`, `unmappedStoreFields`, `negativeFixtureMemory`, `autoFillMemory`,
`assertDecodeBackCoversAllFields`, `TestConnectMemoryParityDetector`,
`TestConnectMemoryFieldsPopulated` (05-02).
</artifacts_this_phase_produces>

<verification>
- `ENGRAM_REQUIRE_QDRANT=1 go test ./internal/server/ -count=1` passes with no skipped tests among the new functions.
- `go test ./cmd/engram/ -count=1` and `go test ./internal/store/ -count=1` pass.
- `git status --porcelain internal/server/protoconv.go internal/store/store.go cmd/engram/client_common.go` is empty — the RED proof's transient json-tag rename was reverted (in-task evidence).
- Commit-range allowlist over PLAN_BASE (see `<commit_range_protocol>`): `git diff --name-only <PLAN_BASE>` is non-empty and `rg -v '^(internal/server/connectapi_boundary_second_test\.go|cmd/engram/client_schemaversion_json_test\.go|\.planning/)'` over it returns no matches — THIS is the assertion that no production file was touched by this plan. The `git status` line above cannot carry it: post-commit the working tree is clean whether or not a production file went in with the change.
- `task lint:go` and `task license:check` clean.
</verification>

<success_criteria>
- A sub-second `not_before` comes back floored and a sub-second `not_after` comes back ceiled, both identical to the instant on the MCP and Connect read lanes, from a single write.
- The MCP lane's values are read out of the record's serialized json (marshal → decode → key presence → value), and a `not_before` json-tag rename has been observed making that test FAIL.
- A whole-second bound comes back unchanged; repeat reads on the same lane are stable (recorded as a supporting tripwire, not as central SC3 evidence).
- Zero lines of read-path rounding code were added; the expected values in the assertion are computed independently of the production rounding helper.
- `engram list --output json` renders `"schema_version": 0` as a JSON number for a record whose field is ASSIGNED zero, and OMITS the key for one where it is unassigned — both asserted, the second as a permanent negative fixture, so the presence assertion is shown capable of failing.
- The SUMMARY states that this task never invokes `memoryToProto` and therefore covers the renderer half only; the mapper half (D-14 §3 assign-always) is plan 05-02's.
</success_criteria>

<output>
Create `.planning/phases/05-connect-record-state-parity/05-03-SUMMARY.md` when done.
Record in it: the PLAN_BASE SHA (per `<commit_range_protocol>`, so the commit-range criteria are
reproducible), the observed sub-second input and both lanes' returned instants for each sub-test,
the verbatim RED-proof failure output from the `not_before` json-tag rename together with the note
that a struct-field comparison would have stayed green under it, and BOTH no-production-change
results: the in-task `git status --porcelain` on the three production files named above (plus
`internal/store/store.go`, mutated transiently by the RED proof), AND the commit-range allowlist
output over PLAN_BASE.
Describe task 2 as D-03/SC1 evidence only — never as round-trip evidence.
For task 2 also record both rendered JSON documents verbatim (assigned-zero and unassigned), and
state explicitly that the stub builds the `Memory` by hand so `memoryToProto` is never invoked:
this task proves the renderer's behavior and the failure shape, NOT that the mapper assigns the
field. That half is plan 05-02's zero-value sub-test and its RED PROOF 3. Do not write a sentence
claiming the operator guarantee is proven end to end.
</output>
