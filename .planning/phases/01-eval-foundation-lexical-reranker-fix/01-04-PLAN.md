---
phase: 01-eval-foundation-lexical-reranker-fix
plan: 04
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
  - 01-03
files_modified:
  - internal/retrievaleval/fixtures.go
  - internal/retrievaleval/rankers.go
  - internal/retrievaleval/rankers_test.go
  - internal/retrievaleval/retrieval_eval_test.go
  - internal/retrievaleval/paraphrase_fixture.go
  - internal/retrievaleval/paraphrase_fixture_test.go
autonomous: true
requirements:
  - RANK-01
  - RANK-02

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Each `retrievalQuery` carries its own `wantKey`. An empty value marks a no-answer query, and `retrievalCase.wantKey` no longer exists, so no second source of truth remains (RESEARCH Pitfall 3). `gh261Case`'s record and query text is byte-identical to `main`, and only its struct shape changes (D-04)"
    - "`TestRetrievalEval` iterates `evalRankers()`, a pluggable named list: `vector-only`, `lexical`, `overlap-gate-t0.90`/`t0.75`/`t0.60`, `cosine-blend-a0.05`/`a0.10`/`a0.20`/`a0.30`, and a disabled `jev` stub that renders `Jev: disabled`. Phase 4 enables Jev by filling the stub's rank function, with no refactor (D-06, D-07, D-11)"
    - "For every query, every enabled variant ranks the SAME candidate pool `SearchReranked` would fetch (`st.Search` at `store.CandidateK(defaultK)` with `Full: true`). The shipped path is measured separately through `st.SearchReranked` itself and reported as the `shipped (SearchReranked)` row (CONTEXT discretion clause)"
    - "The eval logs one Markdown table (`| variant |` header) with per-variant #261 ranks, recall@8 and MRR on the paraphrase case, and D-05 eligibility, followed by a `D-05 decision: winner=<name>` line and a `shipped (SearchReranked) matches: …` diagnostic line (D-09, D-10)"
    - "Exactly two hard gates exist, both prefixed `D-10 gate FAILED:`: the shipped ranking puts #261's target at rank 1 for both #261 queries, and shipped paraphrase MRR is at least vector-only paraphrase MRR. Each logs `D-10 gate PASS:` when it holds. No absolute recall or MRR threshold is asserted anywhere (D-10)"
    - "`decideRanking` applies D-05 mechanically, and `TestDecideRanking` pins its behavior before any live numbers exist. A variant is eligible only if it keeps #261 at rank 1 for both queries AND its paraphrase MRR is at least vector-only's. When vector-only is eligible, a tuned variant (blend or gate) must beat vector-only by at least 0.05. The best eligible MRR wins, and ties go to the simplest (vector-only, then lexical, then gate from highest theta, then blend from lowest alpha, then name). Disabled and shipped rows never compete (D-05, D-07)"
    - "No-answer queries are excluded from recall@k and MRR and produce only `no-answer` log lines (top hit key and score per variant) (D-12)"
    - "`paraphraseCase` (role `roleParaphrase`) wraps `paraphraseSeeds` with 24 queries transcribed verbatim from `01-BLIND-QUERIES.md`, mapped to targets by topic ID. Its doc comment records the D-01 procedure: labels-only prompt, fresh author, date, prompt commit, no edits, and the separate mapping pass (D-01, RANK-01)"
    - "Harness-correctness failures (empty result, zero score, unseeded `wantKey`, target missing at the ceiling, embed error) are all prefixed `harness:`, so a run can distinguish a broken harness from a failed ranking gate"
    - "FLAGGED ASSUMPTION (spec-less edge probe: RANK-01/RANK-02 returned unclassified/unresolved): the eval-level edges assumed here are a variant tie in MRR, vector-only itself failing #261, no eligible variant, a disabled variant, a no-answer query, and a missing paraphrase case. TestDecideRanking, TestVariantMetrics and TestRetrievalCasesRoles cover them, but the probe did not classify them"
  artifacts:
    - path: "internal/retrievaleval/rankers.go"
      provides: "rankerFunc, namedRanker, evalRankers, blendAlphaGrid, gateThetaGrid, d05TunedMargin, mrrEpsilon, shippedRowName, variantMetrics, variantSummary, buildSummaries, rankingDecision, decideRanking, formatVariantTable"
      contains: "func decideRanking(rows []variantSummary) rankingDecision"
    - path: "internal/retrievaleval/rankers_test.go"
      provides: "TestVariantMetrics, TestFormatVariantTable, TestEvalRankersRoster, TestDecideRanking"
      contains: "func TestDecideRanking("
    - path: "internal/retrievaleval/fixtures.go"
      provides: "query-level wantKey, caseRole, retrievalCases including paraphraseCase"
      contains: "roleRegressionGuard"
    - path: "internal/retrievaleval/paraphrase_fixture.go"
      provides: "paraphraseCase with the blind queries"
      contains: "var paraphraseCase = retrievalCase{"
  key_links:
    - from: "internal/retrievaleval/retrieval_eval_test.go"
      to: "internal/retrievaleval/rankers.go"
      via: "the eval iterates the pluggable named-ranker roster (D-11)"
      pattern: "evalRankers[(][)]"
    - from: "internal/retrievaleval/retrieval_eval_test.go"
      to: "internal/retrievaleval/rankers.go"
      via: "the D-05 rule is applied in code, not by hand"
      pattern: "decideRanking[(]"
    - from: "internal/retrievaleval/retrieval_eval_test.go"
      to: "internal/store/store.go"
      via: "the shipped row is measured through the real shared seam"
      pattern: "st[.]SearchReranked[(]"
    - from: "internal/retrievaleval/retrieval_eval_test.go"
      to: "internal/store/rerank.go"
      via: "every variant ranks the exact pool SearchReranked fetches"
      pattern: "store[.]CandidateK[(]defaultK[)]"
    - from: "internal/retrievaleval/fixtures.go"
      to: "internal/retrievaleval/paraphrase_fixture.go"
      via: "the paraphrase case is part of the labeled dataset"
      pattern: "paraphraseCase"
  prohibitions:
    - statement: "MUST NOT let a human or agent pick the shipped ranking from the table by judgment. The winner is whatever `decideRanking` returns, and the rule is fixed and unit-tested before the numbers exist"
      category: integrity-of-evidence
      status: resolved
      verification: test
      reason: "TestDecideRanking pins every D-05 clause hermetically in this plan, before plan 01-05's live run. Plan 01-05's checkpoint offers no option to choose a different variant. No wired check descriptor by design (spec-less fallback, flagged-unverified)"
    - statement: "MUST NOT remove, weaken or re-tune the #261 regression guard to make a variant pass. `gh261Case` text stays byte-identical, and its bar only tightens (rank 1)"
      category: integrity-of-evidence
      status: resolved
      verification: test
      reason: "TestRetrievalCasesRoles asserts the guard exists with its two queries. The acceptance criteria diff gh261 text against main. No wired check descriptor by design (spec-less fallback, flagged-unverified)"
---

<objective>
Turn `TestRetrievalEval` into the pluggable, multi-variant eval RANK-01 and RANK-02 need:

- Per-query targets and no-answer queries (D-03, D-12).
- A named ranker roster including a disabled Jev stub (D-06, D-07, D-11), measured over `SearchReranked`'s exact candidate pool.
- A per-variant recall@k/MRR table (D-09), with the shipped path measured through `SearchReranked` itself.
- D-05 applied mechanically by a unit-tested `decideRanking`, and D-10's two hard gates.
- The independent paraphrase case wired from plan 01-03's blind queries, with D-01's mapping pass and provenance comment.

Purpose: plan 01-05 runs this live and gets a winner by rule, not by eye. Phase 4 appends Jev to the same roster.
Output: `rankers.go` and its test, the restructured eval loop, the query-level fixture shape, and `paraphraseCase`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-eval-foundation-lexical-reranker-fix/01-CONTEXT.md
@.planning/phases/01-eval-foundation-lexical-reranker-fix/01-RESEARCH.md
@.planning/phases/01-eval-foundation-lexical-reranker-fix/01-PATTERNS.md
@.planning/phases/01-eval-foundation-lexical-reranker-fix/01-01-SUMMARY.md
@.planning/phases/01-eval-foundation-lexical-reranker-fix/01-02-SUMMARY.md
@.planning/phases/01-eval-foundation-lexical-reranker-fix/01-03-SUMMARY.md
@internal/retrievaleval/fixtures.go
@internal/retrievaleval/retrieval_eval_test.go
</context>

<interfaces>
Available from wave 1 (verify each exists before starting):

- Plan 01-01: `requireEvalEnabled(t)`, `retrievalEvalEnabled()`, `cosineDistance`, and `server.StoreAndEmbedderFromEnvNoEnsure()`, which now returns six values (`_, dim, em, _, _, err :=`).
- Plan 01-02: `store.CandidateK(k uint64) uint64` and `store.VectorOrder(hits []store.Memory, k int) []store.Memory`. Eval-local: `lexicalRerank(query, hits, k)`, `cosineBlendRerank(query, hits, k, alpha)`, `overlapGateRerank(query, hits, k, theta)`.
- Plan 01-03: `paraphraseSeeds []seedRecord` (96), `paraphraseTopics []paraphraseTopic{id, label, wantKey}` (24, of which 4 have an empty wantKey), and `.planning/phases/01-eval-foundation-lexical-reranker-fix/01-BLIND-QUERIES.md` (24 lines `- Tnn: <query>` under a provenance header).
- Existing: `recallAtK(results []string, wantID string) bool`, `reciprocalRank(results []string, wantID string) float64`, `defaultK = 8`, `newTestcontainerStore`, `store.Memory.Score float32`, and `store.SearchOptions{Full bool, …}`.
- Stable log markers that plans 01-05 and 01-06 grep for, so emit them exactly: `harness:`, `D-10 gate FAILED:`, `D-10 gate PASS:`, `D-05 decision: winner=`, `| variant |`, `shipped (SearchReranked) matches:`, `no-answer`, and `Jev: disabled`.
</interfaces>

## Artifacts this phase produces (plan 01-04's share)

| Kind | Artifact | Task |
|------|----------|------|
| Field | `retrievalQuery.wantKey string` (moved from `retrievalCase`, which loses the field) | 1 |
| Type / consts | `type caseRole int`, `roleRegressionGuard`, `roleParaphrase`, and the field `retrievalCase.role caseRole` (`fixtures.go`) | 1 |
| Go file | `internal/retrievaleval/rankers.go` | 1 |
| Types | `type rankerFunc func(query string, pool []store.Memory, k int) []store.Memory`; `type namedRanker struct { name, family string; simplicity int; tuned bool; rank rankerFunc; disabledReason string }` | 1 |
| Func | `evalRankers() []namedRanker` (T1: vector-only, lexical and jev; T2: full grid) | 1, 2 |
| Consts | `shippedRowName = "shipped (SearchReranked)"`, `jevDisabledReason = "Jev: disabled"` | 1 |
| Type + methods | `variantMetrics` with `add(ids []string, wantID string)`, `recall() float64`, `mrr() float64`, `allRank1() bool`, `ranks []int` | 1 |
| Types / funcs | `variantSummary`, `buildSummaries(...) []variantSummary`, `rankingDecision{winner string; eligible []string; reason string}`, `formatVariantTable(rows []variantSummary, d rankingDecision) string` | 1 |
| Tests | `TestVariantMetrics`, `TestFormatVariantTable`, `TestEvalRankersRoster` (`rankers_test.go`) | 1, 2 |
| Consts / vars | `d05TunedMargin = 0.05`, `mrrEpsilon = 1e-9`, `blendAlphaGrid = []float64{0.05, 0.10, 0.20, 0.30}`, `gateThetaGrid = []float64{0.90, 0.75, 0.60}` | 2 |
| Func | `decideRanking(rows []variantSummary) rankingDecision` | 2 |
| Test | `TestDecideRanking` | 2 |
| Var | `paraphraseCase retrievalCase` (`paraphrase_fixture.go`), `retrievalCases = []retrievalCase{gh261Case, paraphraseCase}` | 3 |
| Tests | `TestParaphraseCorpusIntegrity/queries` subtest, `TestRetrievalCasesRoles` (`paraphrase_fixture_test.go`) | 3 |

## Flagged assumptions (spec-less edge probe)

RANK-01 and RANK-02 came back `unclassified` / `unresolved`. This plan's eval-level edge set is carried as a `FLAGGED ASSUMPTION` truth above.

## Executor notes (every task)

- NEVER run any git stash subcommand. Commit with an explicit pathspec, using Conventional Commits, and end the message with the session's attribution trailer lines. Record any use of the signer fallback.
- New Go files carry the SPDX header. New hermetic tests must NOT use the `TestRetrievalEval` name prefix, because they must run in ordinary CI with the gate off.
- The live eval (`task eval:retrieval`) needs Qdrant and the embedding gateway, and it runs in plan 01-05. This plan proves every non-I/O piece hermetically and proves the eval compiles and skips cleanly when the gate is off.
- Every string in the `<interfaces>` marker list must appear exactly as written, because downstream verify commands grep for them.
- In comments and messages, refer to today's shipped ranking only as `SearchReranked` or "the shipped rank step", never by the Go name of the store's lexical function. Plan 01-06 may delete that function and gates a repo-wide zero count of its name.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Named-ranker eval end to end on the #261 case: per-query targets, vector-only, lexical and the Jev stub over SearchReranked's pool, a shipped row, the rank-1 gate and the table</name>
  <files>internal/retrievaleval/fixtures.go, internal/retrievaleval/rankers.go, internal/retrievaleval/rankers_test.go, internal/retrievaleval/retrieval_eval_test.go</files>
  <read_first>
    - `internal/retrievaleval/fixtures.go` and `internal/retrievaleval/retrieval_eval_test.go` (whole files, post wave 1)
    - `internal/retrievaleval/comparison_rankers.go` (plan 01-02) and `internal/store/rerank.go` (`CandidateK`, `VectorOrder`)
    - `.claude/skills/spike-findings-engram/sources/004-jev-rerank-eval/main.go` lines 150-175 (the `metrics` accumulator to generalize)
    - `01-CONTEXT.md` D-04, D-06, D-09, D-10, D-11, D-12. `01-RESEARCH.md` "Fixture shape change", "Pluggable ranker design" and Pitfall 3
  </read_first>
  <behavior>
    - `TestVariantMetrics`: after `add` of an id list where the target is at rank 1, then rank 3, then absent: `ranks == [1 3 0]`, `recall() == 2.0/3`, `mrr() == (1+1.0/3)/3` (within 1e-9), and `allRank1() == false`. A metrics value with two rank-1 adds has `allRank1() == true`. An empty metrics value returns `recall() == 0`, `mrr() == 0` (never NaN) and `allRank1() == false`.
    - `TestFormatVariantTable`: the output's first line starts with `| variant |`. It has one row per summary. A disabled row renders `Jev: disabled` in place of its numbers. Numbers render with 3 decimals.
    - `TestEvalRankersRoster` (T1 form): the names in order are `vector-only`, `lexical` and `jev`. `jev` has a nil `rank` and `disabledReason == "Jev: disabled"`. On a 3-hit sample, vector-only's output equals `store.VectorOrder` and lexical's equals `lexicalRerank`.
    - RED first: the tests reference the new types before they exist. Record the failing build.
  </behavior>
  <action>
Implements D-04 (shape only), D-06, D-09, D-10 (gate 1), D-11 and D-12.

(1) `fixtures.go`:
- Add `wantKey string` to `retrievalQuery`, with a comment that an empty value marks a no-answer query (D-12), excluded from recall@k/MRR and logged only.
- DELETE `retrievalCase.wantKey` entirely (Pitfall 3: the compiler must force every literal onto the new shape).
- Add `type caseRole int` with `roleRegressionGuard` (#261: rank-1 hard bar and D-05 eligibility clause (a)) and `roleParaphrase` (D-05 clause (b) and the MRR gate), plus the field `retrievalCase.role caseRole`.
- `gh261Case`: set `role: roleRegressionGuard`. Each query gains `wantKey: recordTKey`. Delete the case-level line. Do not change a byte of any `content:` or `text:` string (D-04).
- Update the `retrievalCase` doc comment.

(2) New `internal/retrievaleval/rankers.go` (SPDX header, imports `fmt`, `sort`, `strings` and `internal/store`) with the artifacts listed for Task 1:
- `evalRankers()` returns vector-only (family `vector-only`, simplicity 0, rank = a closure over `store.VectorOrder(pool, k)`), lexical (family `lexical`, simplicity 10, rank = `lexicalRerank`), and jev (family `jev`, simplicity 99, nil rank, `disabledReason: jevDisabledReason`). Doc comment: this is the pluggable roster (D-11), and Phase 4 enables Jev by giving the stub a rank function.
- `variantMetrics` wraps the existing `recallAtK`/`reciprocalRank`. Do not reimplement them.
- `variantSummary` carries name, family, simplicity, tuned, disabled, disabledReason, guardRanks, guardAllRank1, paraphraseRecall, paraphraseMRR and paraphraseN.
- `buildSummaries` takes the roster, a per-variant, per-role `variantMetrics` map and the shipped row's metrics. It returns summaries in roster order, then the shipped row (family `shipped`).
- `formatVariantTable` renders the columns `variant`, `#261 ranks`, `#261 all rank 1`, `paraphrase recall@8`, `paraphrase MRR` and `D-05 eligible`. The eligible cell is `yes`/`no` from `rankingDecision.eligible`, or `—` for disabled and shipped rows. In Task 1 the decision value is always the zero value, so every cell reads `no` or `—`.

(3) New `internal/retrievaleval/rankers_test.go` (SPDX header): the three tests per `<behavior>`.

(4) `retrieval_eval_test.go`, `TestRetrievalEval`: restructure. Keep `requireEvalEnabled`, the `storetest.Addr()` skip, the prod-parity embedder build and the per-case fresh collection plus exact prod seeding. Build `idByKey` and `keyByID`. For each query:
- (a) `vec := em.EmbedQuery`, or `t.Fatalf("harness: …")`.
- (b) Shipped: `st.SearchReranked(ctx, scope, subj, q.text, vec, defaultK, store.SearchOptions{})`. Keep the empty-result and zero-score checks as `t.Errorf("harness: …")`.
- (c) Pool: `st.Search(ctx, scope, subj, vec, store.CandidateK(defaultK), store.SearchOptions{Full: true})`.
- (d) For each roster entry with a non-nil rank, rank the pool at `defaultK`.
- (e) If `q.wantKey == ""`, emit one `no-answer <case>/<query>: <variant> top=<key> score=<score>` log line per variant and for shipped, then continue.
- (f) Otherwise resolve `wantID`, or `t.Fatalf("harness: fixture bug: …")`. Add to the per-variant, per-role metrics and to the shipped metrics. Record every variant's and shipped's id list keyed `case/query`.
- (g) Keep the ceiling check as `t.Errorf("harness: … not found even at ceiling …")`.
- (h) For `roleRegressionGuard` cases, apply D-10 gate 1 to shipped: rank 1, else `t.Errorf("D-10 gate FAILED: #261 target at rank %d (want 1) for %s under the shipped ranking", …)`.
- (i) Keep the raw-score gap log as diagnostic only.

After the case loop, `t.Logf("\n%s", formatVariantTable(buildSummaries(…), rankingDecision{}))`. If both guard queries hit rank 1, log `D-10 gate PASS: #261 target at rank 1 for both queries under the shipped ranking`. Also log `shipped (SearchReranked) matches: <comma-separated variant names whose id list equals shipped's on every recorded query, or none>`. Rewrite the doc comment for the new shape (D-06, D-09, D-10, D-11, D-12, and the pool rationale).

Commit as `test(retrievaleval): pluggable named-ranker eval with per-query targets (D-06, D-10..D-12)` with pathspec `internal/retrievaleval/fixtures.go internal/retrievaleval/rankers.go internal/retrievaleval/rankers_test.go internal/retrievaleval/retrieval_eval_test.go`.
  </action>
  <verify>
    <automated>out=$(env -u ENGRAM_RETRIEVAL_EVAL go test ./internal/retrievaleval/ -run '^(TestVariantMetrics|TestFormatVariantTable|TestEvalRankersRoster)$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e '^--- PASS: ' | wc -l)" -eq 3</automated>
    <fails_when>non-zero exit or fewer than three top-level `--- PASS:` lines</fails_when>
    <automated>out=$(env -u ENGRAM_RETRIEVAL_EVAL go test ./internal/retrievaleval/ -count=1 -v 2>&amp;1); rc=$?; test "$rc" -eq 0 &amp;&amp; printf '%s\n' "$out" | rg -q -e '^--- SKIP: TestRetrievalEval ' &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e '^--- FAIL: ' | wc -l)" -eq 0 &amp;&amp; go vet ./internal/retrievaleval/</automated>
    <fails_when>non-zero exit, any `--- FAIL:`, the gated eval not reported as SKIP with the gate off, or a vet error</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -o 'wantKey +string' internal/retrievaleval/fixtures.go | wc -l` prints `1` (query-level only), and `awk '/^type retrievalCase struct/,/^}/' internal/retrievaleval/fixtures.go | rg -o '^[[:space:]]+wantKey[[:space:]]' | wc -l` prints `0` (no case-level field).
    - D-04 byte-identity: `base=$(git show main:internal/retrievaleval/fixtures.go) || exit 1; diff <(printf '%s\n' "$base" | rg -o 'content: +"[^"]*"|text: +"[^"]*"') <(rg -o 'content: +"[^"]*"|text: +"[^"]*"' internal/retrievaleval/fixtures.go)` exits 0 and prints nothing.
    - `rg -o 'store[.]CandidateK[(]defaultK[)]' internal/retrievaleval/retrieval_eval_test.go | wc -l` prints `1`, and `rg -o 'st[.]SearchReranked[(]' internal/retrievaleval/retrieval_eval_test.go | wc -l` prints `1`.
    - `rg -o 'D-10 gate FAILED:' internal/retrievaleval/retrieval_eval_test.go | wc -l` prints `1` (Task 2 adds the second), and `rg -o '"harness: ' internal/retrievaleval/retrieval_eval_test.go | wc -l` prints at least `5`.
    - `golangci-lint run ./internal/retrievaleval/...` reports no findings.
  </acceptance_criteria>
  <done>The eval measures a named roster over SearchReranked's own pool plus a shipped row. The gh261 guard is tightened to rank 1. No-answer queries are handled by the generic path. The table and match diagnostics are logged. Everything non-I/O is unit-tested. RED then GREEN, committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-05 applied in code: decideRanking, the full D-07 grid, and the shipped-versus-vector-only MRR gate</name>
  <files>internal/retrievaleval/rankers.go, internal/retrievaleval/rankers_test.go, internal/retrievaleval/retrieval_eval_test.go</files>
  <read_first>
    - `internal/retrievaleval/rankers.go`, `rankers_test.go` and `retrieval_eval_test.go` (post-Task-1)
    - `01-CONTEXT.md` D-05 (verbatim rule), D-07, D-09, D-10
  </read_first>
  <behavior>
    `TestDecideRanking` (table-driven, hermetic). Each row gives summaries and expects a winner (an empty string means none) plus a reason substring:
    - vector-only has the best MRR: vector-only wins.
    - vo 0.90 and blend 0.94 (below the margin): vector-only wins, and the reason names the 0.05 margin.
    - vo 0.80 and blend 0.86: the blend wins.
    - The margin boundary, vo 0.80 and blend exactly 0.85: the blend wins (epsilon-inclusive).
    - Lexical has MRR 0.99 but guardAllRank1 false: lexical is ineligible, and the best other eligible variant wins.
    - A gate variant with MRR below vo's is ineligible.
    - The untuned lexical at vo+0.01, with vo 0.90 and lexical 0.91: lexical wins. D-05's margin applies only to tuned variants (blend and gate), and this row pins that literal reading.
    - An exact tie, vo 0.90 and gate-t0.90 0.90: vector-only wins (the simplest).
    - A tie inside the gate family, t0.90 and t0.75 both at vo+0.10: t0.90 wins (the lower simplicity).
    - vector-only fails the guard, with vo MRR 0.80, blend 0.82 and gate 0.81 eligible: the blend wins, since there is no margin when vector-only is ineligible.
    - Nothing is eligible: the winner is empty and the reason starts with `no eligible variant`.
    - A disabled jev row with a fake high MRR is ignored.
    - A shipped row is ignored.
    - There is no vector-only row: the winner is empty and the reason names the missing baseline.
    `TestEvalRankersRoster` (T2 form): the names in exact order are `vector-only`, `lexical`, `overlap-gate-t0.90`, `overlap-gate-t0.75`, `overlap-gate-t0.60`, `cosine-blend-a0.05`, `cosine-blend-a0.10`, `cosine-blend-a0.20`, `cosine-blend-a0.30` and `jev`. `tuned` is true exactly for the gate and blend rows. Simplicity strictly increases across the enabled rows.
    RED first against a stub that always returns `vector-only`.
  </behavior>
  <action>
Implements D-05 (mechanically), D-07 (fixed grid) and D-10 (gate 2).

(1) `rankers.go`:
- Add `const d05TunedMargin = 0.05` and `const mrrEpsilon = 1e-9`.
- Add `var blendAlphaGrid = []float64{0.05, 0.10, 0.20, 0.30}` and `var gateThetaGrid = []float64{0.90, 0.75, 0.60}`. Doc comment: these are fixed BEFORE any live number exists (D-07). Alpha spans a barely-nudging to an overlap-dominant blend given spike 004's small cosine gaps between sticky neighbours. Theta spans near-verbatim-only to moderate promotion, since #261-style restatements match most query terms and paraphrases far fewer. The 0.05 margin in D-05 is the anti-overfitting guard.
- Extend `evalRankers()` with gate rows `overlap-gate-t%.2f` (family `overlap-gate`, tuned, simplicity 20 plus the index in `gateThetaGrid`, so the highest theta is simplest) and blend rows `cosine-blend-a%.2f` (family `cosine-blend`, tuned, simplicity 30 plus the index in `blendAlphaGrid`). The order is vector-only, lexical, the gates, the blends, then jev.
- Add `func decideRanking(rows []variantSummary) rankingDecision`, implementing D-05 exactly:
  1. The baseline is the single row with family `vector-only`. If it is missing, return no winner, with a reason naming the missing baseline.
  2. The candidates are rows that are neither disabled nor family `shipped`.
  3. A candidate is eligible when `guardAllRank1` holds and `paraphraseMRR >= vo.paraphraseMRR - mrrEpsilon`.
  4. When vector-only itself is eligible, drop every tuned candidate whose `paraphraseMRR` is below `vo.paraphraseMRR + d05TunedMargin - mrrEpsilon`.
  5. The winner is the maximum `paraphraseMRR`. Values within `mrrEpsilon` tie, and ties go to the lowest simplicity, then the lexically smallest name.
  6. No eligible candidate means an empty winner, with a reason starting `no eligible variant`.
  The `eligible` field lists every row that passed step 3, which is what the table shows. The `reason` names the deciding clause in words (for example "best eligible paraphrase MRR", "tuned variant below D-05's 0.05 margin over vector-only", "tie broken toward the simpler variant"). Doc comment: quote D-05's rule, state that the margin applies only to tuned (grid) variants, which is D-05's literal text, and state that the rule was committed before any live measurement.

(2) `rankers_test.go`: add `TestDecideRanking` and update `TestEvalRankersRoster` per `<behavior>`.

(3) `retrieval_eval_test.go`: after building the summaries, compute `d := decideRanking(summaries)`, pass `d` to `formatVariantTable`, and log `D-05 decision: winner=<name or none> reason=<reason> eligible=<comma list>`. Then apply D-10 gate 2. If no paraphrase queries were measured, fail with `t.Errorf("harness: no paraphrase-role queries measured — D-05 and the MRR gate cannot be evaluated")`. Else if shipped paraphrase MRR is below vector-only paraphrase MRR by more than `mrrEpsilon`, fail with `t.Errorf("D-10 gate FAILED: shipped paraphrase MRR %.3f < vector-only %.3f", …)`. Else log `D-10 gate PASS: shipped paraphrase MRR %.3f >= vector-only %.3f`. The shipped row is never a D-05 candidate. It is the thing the rule's winner must become (plan 01-06).

Commit as `test(retrievaleval): apply the D-05 decision rule in code with the D-07 grid (D-05, D-07, D-10)` with pathspec `internal/retrievaleval/rankers.go internal/retrievaleval/rankers_test.go internal/retrievaleval/retrieval_eval_test.go`.
  </action>
  <verify>
    <automated>out=$(env -u ENGRAM_RETRIEVAL_EVAL go test ./internal/retrievaleval/ -run '^(TestDecideRanking|TestEvalRankersRoster|TestVariantMetrics|TestFormatVariantTable)$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e '^--- PASS: ' | wc -l)" -eq 4 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e '    --- PASS: TestDecideRanking/' | wc -l)" -ge 14</automated>
    <fails_when>non-zero exit, fewer than four top-level PASS lines, or fewer than fourteen `TestDecideRanking` subtest PASS lines (a D-05 clause is untested)</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -o 'd05TunedMargin += 0[.]05' internal/retrievaleval/rankers.go | wc -l` prints `1`, and `rg -o 'blendAlphaGrid += [[][]]float64[{]0[.]05, 0[.]10, 0[.]20, 0[.]30[}]' internal/retrievaleval/rankers.go | wc -l` prints `1`.
    - `rg -o 'D-10 gate FAILED:' internal/retrievaleval/retrieval_eval_test.go | wc -l` prints `2`, and `rg -o 'D-10 gate PASS:' internal/retrievaleval/retrieval_eval_test.go | wc -l` prints `2`.
    - `rg -o 'decideRanking[(]' internal/retrievaleval/retrieval_eval_test.go | wc -l` prints `1`.
    - No absolute threshold: `rg -o '(recall|mrr|MRR)[^\n]{0,40}[<>]=? *0[.][0-9]' internal/retrievaleval/retrieval_eval_test.go | wc -l` prints `0`.
    - `golangci-lint run ./internal/retrievaleval/...` reports no findings.
  </acceptance_criteria>
  <done>D-05 is a pure, table-tested function covering every clause and tie rule. The D-07 grid is fixed and documented. The eval logs the decision line, and gate 2 guards shipped against vector-only. Committed.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the paraphrase case from the blind queries (D-01 mapping pass) and pin the dataset's roles</name>
  <files>internal/retrievaleval/paraphrase_fixture.go, internal/retrievaleval/paraphrase_fixture_test.go, internal/retrievaleval/fixtures.go</files>
  <read_first>
    - `.planning/phases/01-eval-foundation-lexical-reranker-fix/01-BLIND-QUERIES.md` (the whole file, including its provenance header)
    - `.planning/phases/01-eval-foundation-lexical-reranker-fix/01-BLIND-QUERY-PROMPT.md` and `01-03-SUMMARY.md` (prompt commit SHA, author type)
    - `internal/retrievaleval/paraphrase_fixture.go` and `paraphrase_fixture_test.go` (plan 01-03), and `fixtures.go` (post-Task-1)
    - `01-CONTEXT.md` D-01, D-03, D-12
  </read_first>
  <action>
Implements D-01 (the separate mapping pass plus the provenance comment), D-03 and D-12.

(1) `paraphrase_fixture.go`: add `var paraphraseCase = retrievalCase{ name: "paraphrase-blind-multidomain", role: roleParaphrase, seedRecords: paraphraseSeeds, queries: []retrievalQuery{ … } }` with exactly 24 queries. Each has `name` equal to the topic ID, `text` equal to that ID's line from `01-BLIND-QUERIES.md` transcribed VERBATIM (as a Go interpreted string, never edited or trimmed beyond the `- Tnn: ` prefix), and `wantKey` equal to the matching `paraphraseTopics` entry's `wantKey` (empty for the 4 no-answer topics). The mapping is by topic ID only. Write a doc comment recording the D-01 procedure: the corpus and labels were authored in plan 01-03, and the prompt carried labels only (cite the prompt's commit SHA from `01-03-SUMMARY.md`). Name who wrote the queries (fresh subagent or user, per the file's header) and on what date, with no tools or file access, never seeing `paraphraseSeeds`. The mapping to targets by topic ID was this separate, non-blind pass (plan 01-04). No query text was edited after the blind pass. The case is written independently of #261's targets (RANK-01), and the no-answer queries serve Phase 4's per-hit relevance signal (D-12).

(2) `fixtures.go`: set `var retrievalCases = []retrievalCase{gh261Case, paraphraseCase}`.

(3) `paraphrase_fixture_test.go`:
- Add a `queries` subtest to `TestParaphraseCorpusIntegrity`: `paraphraseCase.queries` has exactly `len(paraphraseTopics)` entries; each query `name` is a unique topic ID; each `wantKey` equals that topic's `wantKey`; each `text` is non-empty and contains no quote or backtick.
- Add `TestRetrievalCasesRoles` (hermetic). `retrievalCases` holds exactly one `roleRegressionGuard` case, and it is `gh261Case.name`, with two queries both targeting `recordTKey`. It holds exactly one `roleParaphrase` case, and it is `paraphraseCase.name`.

Commit as `test(retrievaleval): wire blind paraphrase queries into the eval (D-01, D-03, D-12, #605)` with pathspec `internal/retrievaleval/paraphrase_fixture.go internal/retrievaleval/paraphrase_fixture_test.go internal/retrievaleval/fixtures.go`.
  </action>
  <verify>
    <automated>out=$(env -u ENGRAM_RETRIEVAL_EVAL go test ./internal/retrievaleval/ -run '^(TestParaphraseCorpusIntegrity|TestRetrievalCasesRoles)$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e '^--- PASS: ' | wc -l)" -eq 2 &amp;&amp; printf '%s\n' "$out" | rg -q -e '--- PASS: TestParaphraseCorpusIntegrity/queries'</automated>
    <fails_when>non-zero exit, fewer than two top-level PASS lines, or no `queries` subtest PASS</fails_when>
    <automated>q=.planning/phases/01-eval-foundation-lexical-reranker-fix/01-BLIND-QUERIES.md; missing=$(rg '^- T[0-9]{2}: ' "$q" | sed -E 's/^- T[0-9]{2}: //' | while IFS= read -r line; do rg -qF -- "$line" internal/retrievaleval/paraphrase_fixture.go || printf 'MISSING: %s\n' "$line"; done); printf '%s' "$missing"; test -z "$missing"</automated>
    <fails_when>any `MISSING:` line: a blind query was not transcribed verbatim</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -o 'retrievalCases = [[][]]retrievalCase[{]gh261Case, paraphraseCase[}]' internal/retrievaleval/fixtures.go | wc -l` prints `1`.
    - `awk '/^var paraphraseCase/,/^}/' internal/retrievaleval/paraphrase_fixture.go | rg -o 'name: +"T[0-9]{2}"' | wc -l` prints `24`.
    - The `paraphraseCase` doc comment mentions `D-01`, the prompt commit SHA and the author type: `rg -o 'D-01' internal/retrievaleval/paraphrase_fixture.go | wc -l` prints at least `2`.
    - `env -u ENGRAM_RETRIEVAL_EVAL go test ./internal/retrievaleval/ -count=1` exits 0, and `golangci-lint run ./internal/retrievaleval/...` reports no findings.
  </acceptance_criteria>
  <done>The independent paraphrase case is in the dataset with verbatim blind queries mapped by topic ID and its provenance recorded. The dataset's two roles are pinned by a test. Committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live measurements → shipped-ranking decision | numbers from a gated eval decide which ranking every recall surface ships |
| eval → third-party embedding API | seeds and queries (synthetic) are embedded during the live run in plan 01-05 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-09 | Tampering (decision integrity) | the ranking choice | medium | mitigate | `decideRanking` encodes D-05 and is unit-tested before any live number exists. The live eval logs its output. Plan 01-05's checkpoint offers no option to choose a different variant |
| T-01-10 | Repudiation (false-green evidence) | eval output | medium | mitigate | Harness failures carry a distinct `harness:` prefix, separate from `D-10 gate FAILED:`, so a broken harness can never be read as a ranking result. The shipped row is measured through `SearchReranked` itself, and the "matches" diagnostic ties it to a roster row |
| T-01-11 | Information disclosure | paraphrase queries sent to the embedder | low | accept | The queries are synthetic prose about a fictional project, and the corpus denylist from plan 01-03 already constrains the neighbouring content |
</threat_model>

<verification>
- `env -u ENGRAM_RETRIEVAL_EVAL go test ./internal/retrievaleval/ -count=1 -v` exits 0: all hermetic tests PASS and every gated test SKIPs.
- `task lint` exits 0.
- The live table and decision are produced by plan 01-05.
</verification>

<success_criteria>
- RANK-01's eval shape is complete: vector-only, lexical, blend and gate grids, a Jev stub (disabled), the shipped row, and a paraphrase case written independently of #261.
- RANK-02's gates are in place: #261 at rank 1 and shipped MRR at least vector-only's, with the D-05 winner computed in code.
</success_criteria>

<output>
Create `.planning/phases/01-eval-foundation-lexical-reranker-fix/01-04-SUMMARY.md` when done.
</output>
