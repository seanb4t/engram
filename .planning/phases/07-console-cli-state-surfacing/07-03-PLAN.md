---
phase: 07-console-cli-state-surfacing
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - proto/engram/v1/engram.proto
  - gen/go/engram/v1/engram.pb.go
  - gen/ts/engram/v1/engram_pb.ts
  - ui/src/lib/gen/engram/v1/engram_pb.ts
  - internal/store/store.go
  - internal/store/store_test.go
  - internal/store/migratebacklog.go
  - internal/store/schemaversion_recallgate_test.go
  - internal/server/tools.go
  - internal/server/connectapi.go
  - cmd/engram/client_search.go
  - cmd/engram/client_search_test.go
  - cmd/engram/testdata/help.golden
  - cmd/engram/testdata/catalog.golden
autonomous: true
requirements: [REQ-cli-record-state, REQ-console-record-state]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`Store.Search` honours the same three orthogonal opt-in bools as `Store.List`, each mapping 1:1 onto one gate condition, with `IncludeScheduled` relaxing both halves of `activeWindowConditions` as one unit."
    - "`SearchOptions` reaches `Store.Search` through `Store.SearchReranked` unchanged, so `engram search` and the Connect `SearchMemories` lane both inherit the relaxation without a second gate site."
    - "The relaxation is deliberately 2-of-4: `Store.Search` and `Store.List` are in scope; `Store.SearchDiscovery` and `Store.ListScheduled` are NOT, per D-01. Both exclusions are stated in source, not merely omitted."
    - "`Store.ListScheduled` continues to hide superseded and archived records EVEN when handed a `store.ListOptions` value with all three include bools set to true — the behavioural gate that goes red if a later change 'completes' the 2-of-4 by accident."
    - "`Store.SearchDiscovery` continues to hide superseded and archived discoveries; it takes no options struct, so it cannot inherit the new fields without a signature change."
    - "Authorization stays orthogonal to state (D-04): a caller setting all three flags still sees exactly the owner/shared set they could see before, proven by a cross-owner store test on both `Search` and `List`."
    - "The `schemaversion_recallgate_test.go` reachability derivation is RE-RUN after the gates become conditional, and its result is recorded in the plan summary — not assumed to survive."
    - "`backlogFilter`'s doc-comment reachability claim is re-derived and repaired: the claim's substance (never called from a recall entry point) is restated on grounds that survive conditional gating, since the original grounds ('Search/List all apply activeWindowConditions') no longer hold unconditionally."
    - "With all three bools false, `Store.Search` builds the byte-identical filter set it builds today, so the MCP lane is behaviourally unchanged (D-03)."
  artifacts:
    - path: "proto/engram/v1/engram.proto"
      provides: "SearchMemoriesRequest fields 10/11/12 — the Search-lane half of the published opt-in"
      contains: "include_archived = 10"
    - path: "internal/store/store.go"
      provides: "SearchOptions.IncludeArchived/IncludeSuperseded/IncludeScheduled and the conditional Store.Search gate"
      contains: "IncludeScheduled"
    - path: "internal/store/migratebacklog.go"
      provides: "A re-derived backlogFilter reachability claim that survives conditional recall gates"
      contains: "backlogFilter"
  key_links:
    - from: "internal/server/connectapi.go"
      to: "internal/server/tools.go"
      via: "SearchMemories reads the three request bools into the coreSearchRequest literal"
      pattern: "IncludeSuperseded:\\s+req\\.Msg\\.IncludeSuperseded"
    - from: "internal/server/tools.go"
      to: "internal/store/store.go"
      via: "deps.searchMemory copies the three bools into store.SearchOptions before SearchReranked"
      pattern: "IncludeArchived:\\s+req\\.IncludeArchived"
    - from: "cmd/engram/client_search.go"
      to: "proto/engram/v1/engram.proto"
      via: "engram search --include-* populate SearchMemoriesRequest's three new bools"
      pattern: "IncludeScheduled:\\s+searchIncludeScheduled"
---

<objective>
Mirror the tracer's List-lane opt-in onto the Search lane, then discharge the two proof obligations
the relaxation creates: prove the flags behave as three independent 1:1 reveals with authorization
untouched, and re-derive the two existing reachability claims that rested on the recall gates being
unconditional.

The second half is not bookkeeping. `internal/store/schemaversion_recallgate_test.go`'s entry-point
derivation and `migratebacklog.go`'s "never reachable from any recall entry point" doc claim both
took the gates' unconditionality as a premise. This phase falsifies that premise. Re-running them is
a task, not a note.

Purpose: complete `REQ-cli-record-state`'s reachability half and keep this milestone's own Phase 2
proofs honest.
Output: `engram search --include-*` reaching hidden records, plus a repaired claim and a re-run
derivation whose result is recorded.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-console-cli-state-surfacing/07-CONTEXT.md
@.planning/phases/07-console-cli-state-surfacing/07-PATTERNS.md
@.planning/phases/07-console-cli-state-surfacing/07-01-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Newly created in THIS plan (they do not exist on `main`; do not verify them as drift):

**Protobuf (`proto/engram/v1/engram.proto`)**
- `SearchMemoriesRequest.include_archived` (field 10, `bool`)
- `SearchMemoriesRequest.include_superseded` (field 11, `bool`)
- `SearchMemoriesRequest.include_scheduled` (field 12, `bool`)

**Go — `internal/store`**
- `SearchOptions.IncludeArchived`, `.IncludeSuperseded`, `.IncludeScheduled` (`bool`)

**Go — `internal/server`**
- `coreSearchRequest.IncludeArchived`, `.IncludeSuperseded`, `.IncludeScheduled` (`bool`)

**Go — `cmd/engram`**
- package vars `searchIncludeArchived`, `searchIncludeSuperseded`, `searchIncludeScheduled`
- CLI flags `engram search --include-archived`, `--include-superseded`, `--include-scheduled`

**Generated / regenerated (never hand-edited)**
- `gen/go/engram/v1/engram.pb.go`, `gen/ts/engram/v1/engram_pb.ts`, `ui/src/lib/gen/engram/v1/engram_pb.ts`
- `cmd/engram/testdata/help.golden`, `cmd/engram/testdata/catalog.golden`

Symbols named in this plan that already exist on `main` and are only EXTENDED here:
`Store.Search`, `Store.SearchReranked`, `Store.ListScheduled`, `Store.SearchDiscovery`,
`backlogFilter`, `recallEntryPointSeeds`, `TestSupersedeRecallGate`, `TestSupersedeMultiRecallGate`,
`TestArchiveRecallGateSearchAndList`, `TestArchiveRecallGateListScheduled`,
`TestArchiveRecallGateSearchDiscovery`, `TestListScheduledSupersededHidden`.

## Reversibility gates — deliberate absence of a decision checkpoint

This plan carries a one-way reversibility rating on the task(s) that publish wire contract, and
deliberately does NOT insert a preceding `checkpoint:decision`. That omission is the documented
rule, not an oversight: `gsd-core/references/planner-reversibility.md` lists "gating a decision
already made" as an anti-pattern — "if the phase CONTEXT.md records the human choosing this exact
option, the door is already walked through. Keep the rating for the record; do not insert a
checkpoint to re-ask."

07-CONTEXT.md records the developer choosing each of these doors explicitly, with the reversibility
already stated on the decision itself (D-01, D-02 and D-05 each carry a "Reversibility: one-way"
line). Re-asking would produce checkpoint fatigue and would flip an otherwise-autonomous plan for no
new information. The ratings stay on the tasks so the signal survives into the phase record.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mirror the three-flag opt-in onto the Search lane</name>
  <files>proto/engram/v1/engram.proto, internal/store/store.go, internal/store/store_test.go, internal/server/tools.go, internal/server/connectapi.go, cmd/engram/client_search.go, cmd/engram/client_search_test.go, cmd/engram/testdata/help.golden, cmd/engram/testdata/catalog.golden, gen/go/engram/v1/engram.pb.go (regenerated), gen/ts/engram/v1/engram_pb.ts (regenerated), ui/src/lib/gen/engram/v1/engram_pb.ts (re-vendored)</files>
  <precondition>07-01 has landed: `ListMemoriesRequest` carries fields 13/14/15 and `store.ListOptions` carries the three include bools, so this task extends an established shape rather than inventing one.</precondition>
  <reversibility rating="one-way">`SearchMemoriesRequest` fields 10/11/12 are published wire fields; removal is breaking under `buf breaking` FILE mode. Rating recorded per D-02; the developer already chose this door in 07-CONTEXT.md, so no decision checkpoint is inserted.</reversibility>
  <read_first>
    - `internal/store/store.go` — `SearchOptions` (around line 1049) and `Store.Search`'s gate block (the `activeWindowConditions` / `superseded_by` / `archived_at` appends after `s.ownerScopeFilter(...)`, around lines 1086-1097)
    - `internal/store/store.go` — `Store.SearchReranked` (around line 1144), which forwards `opts` to `Store.Search` and builds no filter of its own
    - `internal/store/store.go` — `ListOptions` and `Store.List` as left by 07-01, for the exact comment and guard shape to mirror
    - `internal/server/tools.go` — `coreSearchRequest` (around line 1429) and `deps.searchMemory`'s `store.SearchOptions{...}` literal (around line 1539)
    - `internal/server/connectapi.go` — `SearchMemories` and its `coreSearchRequest{...}` literal (around line 306)
    - `cmd/engram/client_search.go` — the flag vars, the `SearchMemoriesRequest{...}` literal, and `init()`
    - `proto/engram/v1/engram.proto` — `SearchMemoriesRequest` (fields 1-9; `cross_spine = 9` is the current highest)
  </read_first>
  <behavior>
    - `Store.Search` with `SearchOptions{}`: archived, superseded and window-inactive records are all absent. Today's behaviour.
    - `SearchOptions{IncludeArchived: true}` returns the archived record and nothing else new.
    - `SearchOptions{IncludeSuperseded: true}` returns the superseded record and nothing else new.
    - `SearchOptions{IncludeScheduled: true}` returns BOTH a not-yet-active record and an already-expired record from one call.
    - `Store.SearchReranked` with the same options produces the same membership as `Store.Search`, confirming it adds no second gate.
  </behavior>
  <action>
Add `bool include_archived = 10;`, `bool include_superseded = 11;` and `bool include_scheduled = 12;`
to `SearchMemoriesRequest`, with comments matching the wording already committed on
`ListMemoriesRequest` — including the note on `include_scheduled` that it relaxes both window bounds
and that a separate `include_expired` bool was considered and rejected upstream. Do not add
`idempotency_level = NO_SIDE_EFFECTS`. Run `task proto:lint` then `task proto:gen`; commit the three
regenerated trees.

Add `IncludeArchived`, `IncludeSuperseded`, `IncludeScheduled bool` to `store.SearchOptions`, and in
`Store.Search` wrap each of the three existing appends in its own `if !opts.Include…` guard, in the
same shape 07-01 used for `Store.List`. Gate the whole `activeWindowConditions(...)` append as one
unit. Keep three independent guards — no combined branch, no shared helper taking all three bools.
Preserve and extend the existing sibling-condition comments; they already refuse the fold and must
keep refusing it.

Restate the deliberate 2-of-4 scope in `SearchOptions`' doc comment the same way 07-01 stated it in
`ListOptions`: `Store.Search` and `Store.List` are in scope; `Store.SearchDiscovery` and
`Store.ListScheduled` are excluded by D-01 and must not be wired. Name both excluded sites so a later
reader sees a decision rather than a half-applied invariant.

`Store.SearchReranked` needs no change — it already forwards `opts` to `Store.Search` and builds no
filter of its own. Confirm this by reading it rather than by editing it.

Add the three bools to `coreSearchRequest`, copy them into `deps.searchMemory`'s
`store.SearchOptions{...}` literal, and read them from `req.Msg` in `connectapi.go`'s
`SearchMemories`. The MCP search closure stays untouched — its `coreSearchRequest{...}` literal
never mentions the fields, so the zero value reproduces today's gated behaviour.

Add `--include-archived`, `--include-superseded` and `--include-scheduled` to `engram search`, backed
by `searchIncludeArchived` / `searchIncludeSuperseded` / `searchIncludeScheduled`, passed into the
request literal. Help text must match `engram list`'s wording verbatim so the two verbs read as one
family — the CLI is correct-by-reading (standing invariant `4aksmneehh`). Regenerate the pinned
surfaces with `task surfaces:gen`.

Add the store tests in `<behavior>` to `internal/store/store_test.go`, plus the all-false default-path
assertion over a mixed corpus.
  </action>
  <verify>
    <automated>go test ./internal/store/... -run 'Search' -count=1 && go test ./internal/server/... ./cmd/engram/... -count=1 && task proto:lint && git diff --exit-code -- gen/ ui/src/lib/gen/ cmd/engram/testdata/</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'include_archived = 10|include_superseded = 11|include_scheduled = 12' proto/engram/v1/engram.proto` matches exactly three lines.
    - `Store.Search` contains three independent `if !opts.Include…` guards, one per condition.
    - A store test asserts each of the three flags reveals ONLY its own hidden state on `Store.Search`.
    - A store test seeds one already-expired and one not-yet-active record and asserts a single `SearchOptions{IncludeScheduled: true}` call returns both.
    - A store test asserts `Store.SearchReranked` and `Store.Search` return the same membership under identical options.
    - A default-path test asserts `Store.Search` with `SearchOptions{}` over a mixed corpus returns exactly the live records.
    - `engram search --help` (via the regenerated golden) lists all three flags with wording identical to `engram list`'s.
    - `git diff --exit-code -- gen/ ui/src/lib/gen/ cmd/engram/testdata/` exits 0.
  </acceptance_criteria>
  <done>Both in-scope gate sites now honour the same three orthogonal opt-in bools, exposed on Connect and the CLI and invisible to MCP.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove the 2-of-4 scope and the authorization orthogonality</name>
  <files>internal/store/store_test.go</files>
  <read_first>
    - `internal/store/store.go` — `Store.ListScheduled`'s inline filter (the `qdrant.NewIsEmpty("superseded_by")` / `NewIsEmpty("archived_at")` entries around lines 1563/1568) and its `store.ListOptions` parameter
    - `internal/store/store.go` — `Store.SearchDiscovery`'s inline `must` slice (the same idiom, around lines 1191/1195)
    - `internal/store/store_test.go` — `TestArchiveRecallGateListScheduled` (around line 6685), `TestArchiveRecallGateSearchDiscovery` (around line 6647), `TestListScheduledSupersededHidden` (around line 2450), `TestSupersedeRecallGate` (around line 3195), `TestSupersedeMultiRecallGate` (around line 3784)
    - `internal/store/store_test.go` — an existing cross-owner isolation test (for example `TestListScheduledOwnerIsolation` around line 2505) for the two-subject seeding idiom
  </read_first>
  <behavior>
    - `Store.ListScheduled` handed `store.ListOptions{IncludeArchived: true, IncludeSuperseded: true, IncludeScheduled: true}` STILL omits archived and superseded records. If someone later wires the fields into `ListScheduled`'s inline filter, this assertion goes red.
    - `Store.SearchDiscovery` still omits archived and superseded discoveries (it takes no options struct, so the existing negative assertions already gate it; extend their comments to record why no options-based variant exists).
    - A caller who sets all three include bools on `Store.Search` and `Store.List` receives no record belonging to another owner that they could not already read, and no other owner's `private` record.
    - A `shared` record that is archived or superseded becomes readable, under the flags, by exactly the callers its live predecessor was readable by — D-04's stated property, asserted directly.
  </behavior>
  <action>
Extend `TestArchiveRecallGateListScheduled` and `TestListScheduledSupersededHidden` so each calls
`Store.ListScheduled` with a `store.ListOptions` value carrying all three include bools set to true
and still asserts the archived / superseded records are absent. Add a comment on each stating that
`ListScheduled` is one of the two deliberately-excluded gate sites (D-01) and that this assertion is
the gate which goes red if a later change "completes" the 2-of-4 by accident. This repo has a
recurring, expensive defect class of half-applied N-site invariants; an unlabelled 2-of-4 is
indistinguishable from that defect, so the label and the gate are both load-bearing.

Extend `TestArchiveRecallGateSearchDiscovery`'s comment to record that `Store.SearchDiscovery`
builds its filter from an inline `must` slice and accepts no options struct, so it cannot inherit
the new fields without a signature change that would break every call site — which is why no
options-carrying variant of this assertion exists.

Add authorization-orthogonality coverage to `internal/store/store_test.go`: seed two subjects, give
each a private record and one shared record, archive and supersede a subset, then assert for BOTH
`Store.Search` and `Store.List` that setting all three include bools returns the caller's own records
plus shared records from the other owner, and never the other owner's private records. Assert the
D-04 property explicitly: a shared record that has since been archived or superseded is returned to
the non-owning authenticated caller under the flags, because sharing is unchanged by state.

Extend `TestSupersedeRecallGate` and `TestSupersedeMultiRecallGate` with positive-relaxation
sub-cases: the record each already proves is hidden must be returned when `IncludeSuperseded` is set,
on both `Search` and `List`.
  </action>
  <verify>
    <automated>go test ./internal/store/... -run 'RecallGate|OwnerIsolation|Supersede' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/store/... -run TestArchiveRecallGateListScheduled -count=1` passes and its body constructs a `store.ListOptions` with all three include bools true.
    - Temporarily wiring `ListScheduled`'s inline `archived_at` condition to `opts.IncludeArchived` makes `TestArchiveRecallGateListScheduled` FAIL; revert the experiment and record the observed red in the plan summary.
    - `go test ./internal/store/... -run TestSupersedeRecallGate -count=1` passes with both the existing hidden-by-default assertion and a new revealed-under-`IncludeSuperseded` assertion.
    - A cross-owner test asserts that with all three flags set, `Store.Search` and `Store.List` each return zero records owned by the other subject and marked `private`.
    - A cross-owner test asserts that an archived `shared` record IS returned to the non-owning authenticated caller when `IncludeArchived` is set.
  </acceptance_criteria>
  <done>The deliberate 2-of-4 scope is enforced by a test that goes red on accidental completion, and D-04's orthogonality is a proven property rather than a stated intention.</done>
</task>

<task type="auto">
  <name>Task 3: Re-derive the two reachability claims the relaxation invalidates</name>
  <files>internal/store/migratebacklog.go, internal/store/schemaversion_recallgate_test.go</files>
  <read_first>
    - `internal/store/schemaversion_recallgate_test.go` — `recallEntryPointSeeds` (around line 357), the surrounding reachability derivation, and the filter-key walker assertions around lines 280-310
    - `internal/store/migratebacklog.go` — `backlogFilter`'s doc comment, specifically the sentence claiming it "is operator-tier only and is never reachable from any recall entry point — Search/List/SearchDiscovery/ListScheduled all apply activeWindowConditions or no temporal gate at all, never this filter"
    - `internal/store/store.go` — `Store.Search` and `Store.List` as left by Task 1, so the re-derivation reasons about what actually shipped
    - `.planning/phases/07-console-cli-state-surfacing/07-CONTEXT.md` — the "The recall gate (D-01/D-02 change this)" canonical-refs block
  </read_first>
  <action>
Run the existing derivation and record what it actually reports — do not assume it survives.

Execute `go test ./internal/store/... -run TestSchemaVersionNeverGatesRecall -count=1` (resolve the
exact test name from the file if it differs) and capture the outcome. The derivation is an
AST-reachability proof scoped to `schema_version`: this phase removes conditions from recall filters
and never adds a `schema_version` condition, and it introduces no new store method, so the seed list
should need no edit. Confirm that by running it, and record the run and its result in the plan
summary. If the seed list DOES need an edit, the edit is in scope here.

Repair `backlogFilter`'s doc-comment claim in `internal/store/migratebacklog.go`. The claim's
substance still holds — `backlogFilter` is still called only from the operator-tier migration path
and from no recall entry point — but its stated GROUNDS no longer do: `Store.Search` and `Store.List`
now apply `activeWindowConditions` conditionally, so "Search/List/SearchDiscovery/ListScheduled all
apply activeWindowConditions" is false as written. Restate the claim on grounds that survive
conditional gating: `backlogFilter` is not referenced by any of the six functions in
`recallEntryPointSeeds`, and a recall entry point relaxing its own gate removes conditions from its
filter rather than adopting a different one. Name this phase and the three include bools in the
comment so the next reader can see when and why the grounds changed.

Do not weaken the claim to a vaguer statement, and do not delete it. A doc comment that Phase 2's
reachability proof depends on is a load-bearing artifact; the correct repair is a more durable
justification, not a softer one.
  </action>
  <verify>
    <automated>go test ./internal/store/... -run 'SchemaVersion|Recall|Backlog' -count=1 && go test ./internal/store/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - The `schemaversion_recallgate_test.go` derivation was executed after Task 1 landed, and the plan summary records the exact command run and its pass/fail result.
    - `rg -n 'activeWindowConditions' internal/store/migratebacklog.go` no longer matches inside the reachability sentence, or matches only in a form that is true under conditional gating.
    - `backlogFilter`'s doc comment names `recallEntryPointSeeds` as the derivation its claim is grounded in.
    - `backlogFilter`'s doc comment names this phase and the three include bools as the change that forced the re-derivation.
    - `go test ./internal/store/... -count=1` passes in full.
  </acceptance_criteria>
  <done>Both claims that rested on unconditional recall gates have been re-run or re-derived against what actually shipped, with the evidence recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Connect client → `SearchMemories` handler | An authenticated caller supplies three new request bools that relax recall conditions |
| `internal/server` typed core → `internal/store` | The bools become Qdrant filter-condition decisions on the vector-search path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-02 | Elevation of Privilege | `Store.Search` gate block, `internal/store/store.go` | high | mitigate | The three flags gate ONLY the state `Must` conditions. `s.ownerScopeFilter(ctx, scope, subj)` builds the owner/shared authz conditions BEFORE the state appends and is never itself made conditional. Task 2 adds a cross-owner store test on both `Search` and `List` asserting a flag-setting caller reads nothing new that is `private` to another owner. |
| T-07-11 | Elevation of Privilege | `Store.ListScheduled` / `Store.SearchDiscovery` | high | mitigate | The two out-of-scope gate sites must not inherit the relaxation. `ListScheduled` accepts a `store.ListOptions` value and could silently pick the fields up; Task 2 lands a behavioural assertion that goes red if it ever does, and the experiment proving that red is a required acceptance criterion. |
| T-07-08 | Information Disclosure | `SearchMemoriesRequest` include bools, Connect lane | medium | accept | Relaxing the state gates widens what an already-authorized caller can see within their own readable set; the authz boundary does not move (D-04). Accepted per the developer's locked D-04, with the property asserted rather than assumed. |
| T-07-12 | Repudiation | `backlogFilter` reachability claim | medium | mitigate | A stale doc claim that a future reader trusts is how a half-applied invariant survives review. Task 3 re-derives the claim on grounds that hold under conditional gating and names the change that forced the re-derivation. |
| T-07-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no packages. `07-RESEARCH.md` records the Package Legitimacy Audit as not applicable and the repo's zero-new-Go-dependency invariant is preserved; `git diff --exit-code go.mod go.sum` is expected to exit 0. |
</threat_model>

<verification>
- `task` (lint + full Go suite) is green.
- `task proto:lint` passes and `git diff --exit-code -- gen/ ui/src/lib/gen/` exits 0.
- `git diff --exit-code -- cmd/engram/testdata/` exits 0 after `task surfaces:gen`.
- `git diff --exit-code go.mod go.sum` exits 0.
- The `buf breaking` check against `main` passes (all six request fields are additive).
</verification>

<success_criteria>
- `engram search --include-archived --include-superseded --include-scheduled` reaches records
  `engram search` alone cannot, with the same flag names and help wording as `engram list`.
- The 2-of-4 scope is enforced by a test proven to go red on accidental completion.
- D-04's authorization orthogonality is asserted on both in-scope gate sites.
- The `schemaversion_recallgate_test.go` derivation was re-run, and `backlogFilter`'s claim is
  re-grounded on reasoning that survives conditional recall gates.
</success_criteria>

<output>
Create `.planning/phases/07-console-cli-state-surfacing/07-03-SUMMARY.md` when done
</output>
