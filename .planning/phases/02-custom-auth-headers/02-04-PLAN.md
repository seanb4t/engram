---
phase: 02-custom-auth-headers
plan: 04
type: execute
wave: 4
depends_on:
  - 02-03
files_modified:
  - internal/setupgen/setupgen.go
  - internal/setupgen/setupgen_test.go
  - cmd/engram/setup_delegation_test.go
  - skill/engram/commands/engram-setup.md
  - docs-site/src/content/docs/guides/agent-setup.md
autonomous: true
requirements:
  - REQ-header-documented
  - REQ-header-bearer-unchanged
  - REQ-header-codex-declined
  - REQ-header-name-parameter

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`internal/setupgen.Cases()` returns FIVE cases: the four shipped auth-only cases (labels `oauth`, `oauth-client`, `bearer`, `none`, byte-identical `Options` and `DelegationArgs`) plus a fifth labeled `bearer+header` whose Options carry `Auth` bearer and exactly one `HeaderSpec` (Name `x-litellm-api-key`, EnvVar `LITELLM_KEY`), and whose `DelegationArgs` end `--auth bearer --header x-litellm-api-key=LITELLM_KEY` (D-01, REQ-header-documented)"
    - "`Render()` labels each row's Mode cell with `Case.Label`, so the four shipped rows of both generated tables are byte-identical to the committed `skill/engram/commands/engram-setup.md` region and a fifth `bearer+header` row is appended to each: the delegation table shows the `engram setup … --header x-litellm-api-key=LITELLM_KEY` preview and the fallback table shows `claude mcp add … --header 'Authorization: Bearer ${ENGRAM_TOKEN}' --header 'x-litellm-api-key: ${LITELLM_KEY}'` — the auth header first, the extra after (D-04, D-08, REQ-header-bearer-unchanged)"
    - "`Render()` still finds exactly ONE `claude mcp add` action per case — extra headers are more `--header` pairs on the same action, never a second action (RESEARCH Pitfall 3)"
    - "`skill/engram/commands/engram-setup.md`'s anchored `setup-commands` region is regenerated by `go run ./internal/surfacesgen` (the setupgen step of `task surfaces:gen`) in the SAME commit as the `Cases()` change, and `go run ./internal/surfacesgen --check-setup` exits 0 afterwards (CI drift gate)"
    - "`TestSetupGeneratedInvocations` runs the real CLI for all five cases: the fifth case's preview lane exits 0 with claude-code and opencode `would-write` (their `command` carrying the header in each runtime's own syntax) and codex `failed` with the exact plan-02-01 decline reason; its apply lane exits `exitPartial` (codex fails, the other two write) — exactly the exit the existing `oauth-client` case already produces for opencode (D-09, REQ-header-codex-declined)"
    - "`assertGeneratedPreviewArgs` accepts `--header` only when its value equals a `Name=EnvVar` rendering of one of `c.Options.Headers` (each header exactly once) and rejects `--header` on a case with no headers, and `assertGeneratedAuthWiring` asserts claude-code's `command` contains `--header 'x-litellm-api-key: ${LITELLM_KEY}'` and opencode's contains `--header 'x-litellm-api-key={env:LITELLM_KEY}'` for the header case"
    - "`skill/engram/commands/engram-setup.md`'s hand-authored prose (outside the anchored region) tells the agent to collect a gateway header's NAME and the NAME of its environment variable — never the value — to pass `--header NAME=ENVVAR` with any auth mode, to keep the `${ENVVAR}` reference literal in the fallback command, and that codex cannot express an extra header (its row reports `failed`; drop `--header` or exclude codex via `--runtime`), with `x-litellm-api-key`/`LITELLM_KEY` as the example (REQ-header-documented, D-09)"
    - "`docs-site/src/content/docs/guides/agent-setup.md` gains a `### Gateway headers` subsection under `## Choose authentication` showing the canonical `engram setup --url https://engram.example.com/mcp --auth oauth --header x-litellm-api-key=LITELLM_KEY` example, the per-runtime rendering (Claude Code `'x-litellm-api-key: ${LITELLM_KEY}'`, opencode `'x-litellm-api-key={env:LITELLM_KEY}'`, generic's `headers` object mapping `x-litellm-api-key` to `${LITELLM_KEY}`), the env-var-NAME-never-a-value rule, the `Authorization`-is-owned-by-`--auth` rule, the deterministic order, and the Codex limitation naming `--bearer-token-env-var`; the runtime×auth table gains a `--header` column (`claude-code` Supported, `codex` Unsupported, `opencode` Supported, `generic` Manual config); and `ENGRAM_HEADERS` is documented next to `ENGRAM_RUNTIME` in the Scripts section with the comma-list shape and the flag-replaces-env rule (REQ-header-documented, D-02, D-07, D-08)"
    - "No documentation example pairs a header name with a literal secret value — only env var names appear, matching the existing `${ENGRAM_TOKEN}` convention (REQ-header-value-env-ref-only)"
    - "No test in this plan invokes a third-party CLI write verb or touches the operator's `$HOME`; `TestSetupGeneratedInvocations` keeps its fake `Environment` whose `Getenv` fails on any key other than `XDG_CONFIG_HOME`, and `setupgen.Render()`'s synthetic environment still fails generation on any environment read (rule m45p2b4bp7, gotcha ryr82bf2s2); `go.mod`/`go.sum` are byte-unchanged"
  artifacts:
    - path: "internal/setupgen/setupgen.go"
      provides: "Case.Label field, fifth bearer+header Case, Render keyed on Label"
      contains: "bearer+header"
    - path: "internal/setupgen/setupgen_test.go"
      provides: "TestRenderRealPlans over five labeled cases including the header row"
      contains: "want five"
    - path: "cmd/engram/setup_delegation_test.go"
      provides: "TestSetupGeneratedInvocations header-aware conformance (Label subtests, --header arg mapping, ErrHeaderUnsupported codex branch, exitPartial for the header case)"
      contains: "setup.ErrHeaderUnsupported"
    - path: "skill/engram/commands/engram-setup.md"
      provides: "regenerated tables with the bearer+header row plus hand-authored gateway-header and codex-limitation prose"
      contains: "| `bearer+header` |"
    - path: "docs-site/src/content/docs/guides/agent-setup.md"
      provides: "Gateway headers subsection, --header table column, ENGRAM_HEADERS"
      contains: "### Gateway headers"
  key_links:
    - from: "internal/setupgen/setupgen.go"
      to: "internal/setup/claudecode.go"
      via: "Render's exactly-one-claude-mcp-add filter is why extra headers must ride on the same action's Args — the fifth case exercises it against the real ClaudeCode.Plan"
      pattern: 'action[.]Args[[]2[]] == "add"'
    - from: "cmd/engram/setup_delegation_test.go"
      to: "internal/setupgen/setupgen.go"
      via: "the conformance test iterates every Case, so the fifth case runs the real CLI end-to-end (preview + apply lanes) without a hand-written invocation"
      pattern: 'for _, c := range setupgen[.]Cases[(][)]'
    - from: "internal/surfacesgen/main.go"
      to: "skill/engram/commands/engram-setup.md"
      via: "surfacesgen's run() calls setupgen.Write(setupgen.Path) — the regeneration step that rewrites the anchored region; --check-setup is the CI drift gate"
      pattern: 'setupgen[.]Write[(]setupgen[.]Path[)]'
    - from: "docs-site/src/content/docs/guides/agent-setup.md"
      to: "cmd/engram/setup.go"
      via: "the Scripts section already documents ENGRAM_RUNTIME as an env default; ENGRAM_HEADERS is documented beside it (D-07)"
      pattern: 'ENGRAM_RUNTIME'
  prohibitions:
    - statement: "MUST NOT hand-edit the anchored `setup-commands` region of `skill/engram/commands/engram-setup.md` — it is regenerated from the real `ClaudeCode.Plan` by surfacesgen in the same commit as the `Cases()` change; only the prose OUTSIDE the anchors is hand-authored"
      category: safety
      status: resolved
      verification: test
      reason: "`go run ./internal/surfacesgen --check-setup` (the CI drift gate) and TestRenderRealPlans both compare the committed region against Render()'s output — no wired check descriptor by design (spec-less fallback, flagged-unverified)"
    - statement: "MUST NOT document, suggest, or generate a Codex header workaround inside engram's own writer or generated tables — the docs may point at Codex's own configuration as something the operator does themselves; engram never writes it (PROJECT.md Out of Scope, 02-CONTEXT.md deferred note)"
      category: transparency
      status: resolved
      verification: judgment
      reason: "The generated tables carry only claude mcp add and engram setup invocations by construction; the one-line docs pointer is scoped to agent-setup.md and reviewed by reading — no wired check descriptor by design (spec-less fallback, flagged-unverified)"
    - statement: "MUST NOT place a literal secret value in any doc, prose, fixture, or generated table — every example carries an environment variable NAME only"
      category: safety
      status: resolved
      verification: test
      reason: "Acceptance greps assert the docs contain the env var NAMEs and no sk-/Bearer-prefixed literal beside a header name; Render()'s synthetic environment fails generation on any environment read — no wired check descriptor by design (spec-less fallback, flagged-unverified)"
---

<objective>
Document the gateway header everywhere REQ-header-documented names, using the real code paths so the
prose cannot drift: `internal/setupgen.Cases()` gains a fifth case (`bearer` + `--header
x-litellm-api-key=LITELLM_KEY`, labeled `bearer+header`) so the regenerated `/engram-setup` tables show
the header in both the delegation preview and the Claude Code fallback (auth header first, extra after
— D-04, D-08); `TestSetupGeneratedInvocations` runs that case end-to-end through the CLI in both lanes,
proving the codex decline surfaces exactly as `oauth-client` on opencode does (D-09); the hand-authored
`/engram-setup` prose and `guides/agent-setup.md` explain the shape, the env-var-NAME-never-a-value rule,
`ENGRAM_HEADERS` (D-07), the `Authorization` ownership rule (D-02), and the Codex limitation.

Purpose: REQ-header-documented is the last success criterion — `--help` landed in plan 02-03; this plan
covers the regenerated prose and the docs site. REQ-header-bearer-unchanged is pinned again here at the
generated-prose level: the four shipped table rows must not move by a byte.

Output: `Case.Label`, the fifth `Case`, `Render()` keyed on `Label`, the regenerated
`skill/engram/commands/engram-setup.md` region plus new prose, `TestRenderRealPlans` over five cases,
the header-aware `TestSetupGeneratedInvocations`, and the `agent-setup.md` additions. Depends on 02-03
(the `--header` flag the fifth case's `DelegationArgs` exercise).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-custom-auth-headers/02-CONTEXT.md
@.planning/phases/02-custom-auth-headers/02-RESEARCH.md
@.planning/phases/02-custom-auth-headers/02-PATTERNS.md
@.planning/phases/02-custom-auth-headers/02-03-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces (plan 02-04's share)

| Kind | Artifact | Created by |
|------|----------|------------|
| Go field | `setupgen.Case.Label string` — the Mode-column label (`oauth`, `oauth-client`, `bearer`, `none`, `bearer+header`) | 02-04 Task 1 |
| Go value | fifth `Case{Label: "bearer+header", Options: {Auth: "bearer", Headers: {{x-litellm-api-key, LITELLM_KEY}}}, DelegationArgs: […, "--header", "x-litellm-api-key=LITELLM_KEY"]}` | 02-04 Task 1 |
| Generated rows | one `bearer+header`-labeled row in each of the two tables of `skill/engram/commands/engram-setup.md` | 02-04 Task 1 (regenerated) |
| Test edits | `TestRenderRealPlans` five-case loop; `TestSetupGeneratedInvocations` Label subtests + `--header` mapping + `ErrHeaderUnsupported` branch + `exitPartial` for the header case; `assertGeneratedAuthWiring` header assertions | 02-04 Task 1 |
| Prose | `/engram-setup` step 2 gateway-header paragraph and step 3 env-var sentence; Codex limitation | 02-04 Task 2 |
| Docs | `### Gateway headers` subsection, `--header` table column, `ENGRAM_HEADERS` in Scripts, Codex limitation and one-line pointer in `agent-setup.md` | 02-04 Task 2 |

## Flagged Assumptions (edge probe, spec-less fallback)

This plan accounts for **1 flagged** of the phase's 12 edge rows (02-01: 5 authored + 1 flagged; 02-02:
1 authored; 02-03: 4 authored; total 10 authored + 2 flagged = 12, no silent drop).

| Requirement | Probe category | Disposition |
|---|---|---|
| REQ-header-documented | `concurrency` | **Flagged assumption (unresolved — not applicable).** Documentation is static text with no concurrent readers or writers at runtime; the only "race" is a stale generated region when code and prose are committed separately, which is exactly what the same-commit regeneration rule and `surfacesgen --check-setup` (CI drift gate) close. Recorded rather than resolved with a backstop so the row is not silently dropped. |

## Prohibition probe (spec-less fallback, in-prompt recall)

Stage 1 raw candidates for the documentation surfaces: must not hand-edit the generated region; must not
show a literal secret; must not tell Codex users engram will write their config; must not present the
header as an `--auth` mode; must not omit the Codex limitation; must not let the four shipped rows drift;
must not describe `--header` as replacing `Authorization`; must not add a second `claude mcp add` action.
Stage 2 keeps three (authored above, descriptor-less): **never-hand-edit-the-region** (safety — the
drift gate exists because a hand edit silently lies), **never-generate-a-Codex-workaround**
(transparency — PROJECT.md Out of Scope), and **never-a-literal-secret-in-docs** (safety). The rest are
pinned as truths (D-01 framing, D-08 order) or are routine prose review.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Fifth setupgen case `bearer+header` — regenerated tables + header-aware CLI conformance in both lanes</name>
  <reversibility rating="reversible">One additive struct field, one appended case, a label lookup in Render, test extensions, and a regenerated region; undo is deletion plus regeneration.</reversibility>
  <files>internal/setupgen/setupgen.go, internal/setupgen/setupgen_test.go, cmd/engram/setup_delegation_test.go, skill/engram/commands/engram-setup.md</files>
  <read_first>
    - `internal/setupgen/setupgen.go` lines 24-47 (`Case`, `Cases()` — the `make([]Case, 0, 4)` and the auth loop), 49-86 (`Render()`: the synthetic env whose `Getenv` fails generation, the exactly-one-`claude mcp add` filter, and the two `fmt.Fprintf` row writers keyed on `c.Options.Auth` — both become `c.Label`; the two error messages that interpolate `c.Options.Auth` may also switch to `c.Label`).
    - `internal/setupgen/setupgen_test.go` lines 19-53 (`TestRenderRealPlans`: `len(cases) != 4`, the four-mode loop asserting `| \`mode\` | \`command\` |` rows, the bearer quoting assertion, the shared-mutable-data check), 239-289 (`TestPlanMutationChangesRegion` — it finds the `oauth-client` case by `Options.Auth`; unchanged by a fifth case but re-run).
    - `cmd/engram/setup_delegation_test.go` — all of it: `TestSetupGeneratedInvocations` (subtests named `c.Options.Auth`; the `wantExit` branch for `oauth-client`; the `errors.Is(planErr, setup.ErrAuthModeUnsupported)` unsupported-row branch; the `env.Getenv` that fails on any key other than `XDG_CONFIG_HOME`), `assertGeneratedPreviewArgs` (the `map[string]string{"--url":…, "--auth":…, "--client-id":…}` that rejects any other flag), `assertGeneratedAuthWiring`.
    - `internal/surfacesgen/main.go` lines 180-215 — `run()` calls `setupgen.Write(setupgen.Path)`; `--check-setup` is the drift check.
    - `Taskfile.yaml` lines 261-281 — `surfaces:gen` runs `go run ./internal/surfacesgen`, then `proto:gen` (buf remote plugins — needs network), then the golden regen. The setupgen half is the first command; the goldens do not move in this plan.
    - `skill/engram/commands/engram-setup.md` lines 55-75 — the anchored `setup-commands` region (`<!-- engram:rule:start setup-commands -->` … `end`) with the two four-row tables that will gain a fifth row each.
    - `internal/setup/codex.go` (plan 02-01) — the exact decline string; `cmd/engram/setup.go` (plan 02-03) — the `--header` flag the fifth case's `DelegationArgs` exercise.
  </read_first>
  <behavior>
    Write the test changes FIRST. Observe: `TestRenderRealPlans` fails (`got 4 cases, want five`); `TestSetupGeneratedInvocations` — once `Cases()` has the fifth entry but before the test is header-aware — fails in `assertGeneratedPreviewArgs` on the unmapped `--header` flag. Then implement and observe green.

    `internal/setupgen/setupgen_test.go` — `TestRenderRealPlans`: `len(cases) != 5` → `t.Fatalf("got %d cases, want five", …)`; loop over `[]string{"oauth", "oauth-client", "bearer", "none", "bearer+header"}` asserting `c.Label == label`; for the fifth also `c.Options.Auth == "bearer"`, `len(c.Options.Headers) == 1`, `c.Options.Headers[0] == setup.HeaderSpec{Name: "x-litellm-api-key", EnvVar: "LITELLM_KEY"}`, and `slices.Contains(c.DelegationArgs, "--header")` with `x-litellm-api-key=LITELLM_KEY` as the following element; each case's two rows appear as `| \`<label>\` | \`<command>\` |` (the existing check, now keyed on Label). Additionally assert the body contains `'Authorization: Bearer ${ENGRAM_TOKEN}' --header 'x-litellm-api-key: ${LITELLM_KEY}'` (auth first, extra after, both single-quoted — D-08). Assert `strings.Count(body, "| \`bearer\` |") == 2` (the shipped bearer rows are still exactly the two four-case rows — the fifth case's label is distinct).

    `cmd/engram/setup_delegation_test.go` — `TestSetupGeneratedInvocations`: subtest name `c.Label`; `wantExit := 0; if lane == "apply" && (c.Options.Auth == "oauth-client" || len(c.Options.Headers) > 0) { wantExit = exitPartial }` with a comment: opencode declines oauth-client; codex declines every custom header (D-09). In the per-runtime loop add, before the auth-mode branch: `if errors.Is(planErr, setup.ErrHeaderUnsupported) { if row.Name != "codex" || len(c.Options.Headers) == 0 || row.Outcome != "failed" || row.Command != "" || !strings.Contains(row.Reason, "x-litellm-api-key") || !strings.Contains(row.Reason, "--bearer-token-env-var") { t.Fatalf("header decline row lost: %+v", row) }; continue }`. `assertGeneratedPreviewArgs`: build `wantHeaders := map[string]bool` from `c.Options.Headers` as `Name+"="+EnvVar`; when `args[i] == "--header"`, require `wantHeaders[args[i+1]]` and mark it seen (each exactly once), else fall through to the existing map; after the loop require every wanted header was seen, and require that a case with no headers carries no `--header` (mirror the client-ID symmetry check). `assertGeneratedAuthWiring`: when `len(c.Options.Headers) > 0`, claude-code `row.Command` must contain `--header 'x-litellm-api-key: ${LITELLM_KEY}'` and opencode's must contain `--header 'x-litellm-api-key={env:LITELLM_KEY}'`; for the bearer+header case claude-code's must contain `'Authorization: Bearer ${ENGRAM_TOKEN}' --header 'x-litellm-api-key: ${LITELLM_KEY}'` (order). Keep the fake `Getenv` that fails on any key other than `XDG_CONFIG_HOME` — it is the structural proof that no layer reads `LITELLM_KEY`. Keep the `unknown-flag` subtest as is.
  </behavior>
  <action>
Implement the fifth case in `internal/setupgen/setupgen.go`. Add `Label string` to `Case` with a doc
comment: the Mode-column label in both generated tables; the four auth-only cases use their auth mode
verbatim (so the shipped rows stay byte-identical), and a case that adds a header is labeled
`<auth>+header`. In `Cases()`, grow `make([]Case, 0, 4)` to `5`, set `Label: auth` on each existing
case, and append after the loop: `opts := setup.Options{URL: "https://engram.example.com/mcp", Auth:
"bearer", Headers: []setup.HeaderSpec{{Name: "x-litellm-api-key", EnvVar: "LITELLM_KEY"}}}` and `args :=
[]string{"engram", "setup", "--url", opts.URL, "--auth", opts.Auth, "--header",
"x-litellm-api-key=LITELLM_KEY"}` as `Case{Label: "bearer+header", Options: opts, DelegationArgs: args}`
— the canonical gateway example from 02-CONTEXT.md (a bearer server behind a LiteLLM gateway that also
wants `x-litellm-api-key`), chosen because it shows the auth header AND the extra header on one line,
in D-08 order. Update `Cases()`'s doc comment (published auth-choice order, then the gateway case). In
`Render()`, replace `c.Options.Auth` with `c.Label` in both `fmt.Fprintf` row writers and in the two
error messages, and add one sentence to `Render()`'s doc: extra headers are additional `--header` pairs
on the SAME `claude mcp add` action (D-04/D-08), which is what keeps the exactly-one-add filter true for
the header case.

Update the two test files per `<behavior>`.

Regenerate the anchored region: run `go run ./internal/surfacesgen` (the setupgen step of `task
surfaces:gen`; prefer the full `task surfaces:gen` when buf's remote plugins are reachable — it must be a
no-op for `gen/` and the goldens in this plan). Inspect `git diff -- skill/engram/commands/engram-setup.md`:
the ONLY changes are one appended row labeled `bearer+header` in each table; the four shipped rows
are byte-identical. Then `go run ./internal/surfacesgen --check-setup` must exit 0.

Commit everything from this task in ONE commit — the `Cases()` change, the tests, and the regenerated
region, per the CI drift gate — as `feat(setupgen): add the bearer+header gateway case to the generated
setup tables` with the explicit pathspec `internal/setupgen/setupgen.go internal/setupgen/setupgen_test.go
cmd/engram/setup_delegation_test.go skill/engram/commands/engram-setup.md`.
  </action>
  <verify>
    <automated>out=$(go test ./internal/setupgen/ -count=1 -v 2>&amp;1 &amp;&amp; go test ./cmd/engram -run '^TestSetupGeneratedInvocations$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^(--- |    --- )(PASS|FAIL)' | tail -n 30; test "$rc" -eq 0 &amp;&amp; printf '%s\n' "$out" | rg -q -F -e '--- PASS: TestSetupGeneratedInvocations/bearer+header' &amp;&amp; printf '%s\n' "$out" | rg -q -F -e '--- PASS: TestRenderRealPlans/bearer+header' &amp;&amp; go run ./internal/surfacesgen --check-setup</automated>
    <fails_when>non-zero `go test` exit, any `--- FAIL:` line, the line `testing: warning: no tests to run`, no `--- PASS: TestSetupGeneratedInvocations/bearer+header` or `--- PASS: TestRenderRealPlans/bearer+header` subtest line, or `surfacesgen --check-setup` reporting `generated setup region has drifted`</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n -F 'Label: "bearer+header"' internal/setupgen/setupgen.go` prints exactly one line; `rg -n -F 'make([]Case, 0, 5)' internal/setupgen/setupgen.go` prints exactly one line; `rg -c -F 'c.Label' internal/setupgen/setupgen.go` prints at least `2` (both row writers).
    - `rg -n -F '"--header", "x-litellm-api-key=LITELLM_KEY"' internal/setupgen/setupgen.go` prints exactly one line.
    - `rg -c -F '| `bearer+header` |' skill/engram/commands/engram-setup.md` prints `2` (one row per table); `rg -c -F '| `bearer` |' skill/engram/commands/engram-setup.md` prints `2` (the shipped rows are unchanged).
    - `rg -n -F "--header 'Authorization: Bearer \${ENGRAM_TOKEN}' --header 'x-litellm-api-key: \${LITELLM_KEY}'" skill/engram/commands/engram-setup.md` prints exactly one line (the fallback row — auth header first).
    - `rg -n -F 'engram setup --url https://engram.example.com/mcp --auth bearer --header x-litellm-api-key=LITELLM_KEY' skill/engram/commands/engram-setup.md` prints exactly one line (the delegation row).
    - `diff <(git show ceabd8e9:skill/engram/commands/engram-setup.md | rg -F '| `oauth` |' ) <(rg -F '| `oauth` |' skill/engram/commands/engram-setup.md)` prints nothing, and the same holds for the `oauth-client`, `bearer`, and `none` rows (the four shipped rows of both tables are byte-identical to the pre-phase file).
    - `rg -n -F 'want five' internal/setupgen/setupgen_test.go` prints exactly one line; `rg -n -F 'setup.ErrHeaderUnsupported' cmd/engram/setup_delegation_test.go` prints exactly one line; `rg -n -F 'len(c.Options.Headers) > 0' cmd/engram/setup_delegation_test.go` prints at least two lines.
    - `rg -n -F 'if key != "XDG_CONFIG_HOME"' cmd/engram/setup_delegation_test.go` still prints exactly one line (the environment-read guard is intact).
    - RED observed (record in SUMMARY): with the tests edited and `setupgen.go` at plan-02-03 HEAD, `go test ./internal/setupgen/ -run '^TestRenderRealPlans$' -count=1` fails with `got 4 cases, want five`; with `Cases()` extended but `setup_delegation_test.go` at HEAD, `TestSetupGeneratedInvocations` fails in `assertGeneratedPreviewArgs`; after both edits and regeneration everything passes.
    - `go test ./internal/setupgen/ ./internal/surfacesgen/ -count=1` and `go test ./cmd/engram -count=1` exit 0; `gofmt -l internal/setupgen/ cmd/engram/` prints nothing.
  </acceptance_criteria>
  <done>`Cases()` returns five labeled cases, both generated tables carry the `bearer+header` row with the auth header first and the four shipped rows byte-identical, `TestRenderRealPlans` and the header-aware `TestSetupGeneratedInvocations` pass in both lanes (codex declined, `exitPartial` under apply), the drift check is green, and the change is in one commit with an explicit pathspec.</done>
</task>

<task type="auto">
  <name>Task 2: Hand-authored prose — `/engram-setup` steps and `guides/agent-setup.md` gateway headers, `ENGRAM_HEADERS`, Codex limitation</name>
  <reversibility rating="reversible">Markdown prose outside any generated region; undo is a revert.</reversibility>
  <files>skill/engram/commands/engram-setup.md, docs-site/src/content/docs/guides/agent-setup.md</files>
  <read_first>
    - `skill/engram/commands/engram-setup.md` lines 1-54 (frontmatter, Steps 1-4 — step 2's auth-mode list and step 3's "Gather auth inputs without collecting secrets" paragraph are the insertion points) and 76-113 (the two "When the binary is …" sections). Do NOT touch lines between the `engram:rule:start setup-commands` and `engram:rule:end setup-commands` anchors.
    - `docs-site/src/content/docs/guides/agent-setup.md` — all 208 lines: the `## Choose authentication` section (the four-command block and the runtime×auth table at lines 72-82), `### Bearer token` (lines 99-108 — the `${ENGRAM_TOKEN}` literal-reference convention to mirror), `### Scripts and JSON output` (lines 161-171 — `ENGRAM_URL`, `ENGRAM_AUTH`, and `ENGRAM_RUNTIME` sentence).
    - `.rumdl.toml` — `docs-site` and `.agents`/`.claude` are excluded from markdown lint; `skill/engram/commands/*.md` IS linted by `task lint` (MD013 line length is disabled; MD029 ordered-list style is `ordered`). `internal/skills/data` is a vendored copy of `skill/engram/skills/**` (NOT `commands/`), so no mirror needs updating.
    - `.planning/phases/02-custom-auth-headers/02-CONTEXT.md` §"Specific Ideas" (the canonical example and its four per-runtime renderings), D-02, D-07, D-09, and §"Deferred Ideas" second bullet — the planner MAY add a one-line doc pointer to Codex's own header configuration in `agent-setup.md` (never in engram's writer or generated tables).
    - `cmd/engram/setup.go` (plan 02-03) — the `--header` help paragraph, so the docs use the same vocabulary; `cmd/engram/testdata/help.golden` for the exact flag line.
  </read_first>
  <action>
Edit `skill/engram/commands/engram-setup.md` OUTSIDE the anchored region only. In step 2, after the
`**None**` bullet, add a paragraph: **Gateway header (optional).** If the deployment sits behind a
gateway that requires an additional header — for example LiteLLM's `x-litellm-api-key` — ask for the
header **name** and the **name of the environment variable** that will hold its value, never the value
itself. `--header NAME=ENVVAR` is repeatable and valid with every auth mode; it never replaces the
`Authorization` header, which stays owned by the auth mode (use `bearer` for that). Codex cannot express
an extra header (`codex mcp add` exposes only `--bearer-token-env-var`): its delegation row reports
`failed` naming the header — either drop `--header` or exclude codex with `--runtime`; never hand-edit
Codex's configuration to add one. In step 3, after the `ENGRAM_TOKEN` sentence, add: For a gateway header,
require the named variable (for example `LITELLM_KEY`) in the runtime's environment and keep the
generated `${LITELLM_KEY}` reference literal, including its single quotes. Update the "Generated command
reference" paragraph to also say: replace `LITELLM_KEY` with the user's own variable name when using the
`bearer+header` rows. Keep MD029 ordered-list numbering intact; no line-length constraint applies.

Edit `docs-site/src/content/docs/guides/agent-setup.md`. (1) In `## Choose authentication`, add a
sixth column to the runtime table headed `--header` (in backticks, exactly like the four mode columns),
with cells `claude-code` Supported, `codex` Unsupported, `opencode` Supported, `generic` Manual config;
extend the sentence under the table: "Unsupported combinations — including
`--header` on `codex` — appear as failed rows with a reason." (2) Add a new `### Gateway headers`
subsection after `### Bearer token` and before `### No authentication` containing: the canonical example
in a `sh` fence — `engram setup --url https://engram.example.com/mcp --auth oauth --header
x-litellm-api-key=LITELLM_KEY`; a paragraph: `--header NAME=ENVVAR` adds a header alongside whatever
`--auth` produces, is repeatable (or comma-separated), and works with every mode; `ENVVAR` is the NAME
of an environment variable the runtime resolves at connection time — never a value — so a right-hand
side containing `$`, `{`, whitespace, or `:` is rejected, as is any `Authorization` name (that header is
owned by `--auth`; use `--auth bearer`); a short table or list of the per-runtime rendering — Claude
Code `--header 'x-litellm-api-key: ${LITELLM_KEY}'`, opencode `--header
'x-litellm-api-key={env:LITELLM_KEY}'`, generic `"headers": {"x-litellm-api-key": "${LITELLM_KEY}"}` —
noting the auth header renders first and extra headers sort by name, identically in the preview, the
JSON `headers` field (a comma-separated `NAME=ENVVAR` string), and the generated `/engram-setup` prose;
a Codex paragraph: Codex has no custom-header flag (`codex mcp add` exposes only
`--bearer-token-env-var`), so a `--header` run reports a `failed` row for `codex` naming the header —
drop `--header` or select the other runtimes with `--runtime claude-code,opencode`; setup never writes
Codex's configuration for you; plus exactly one sentence, permitted by 02-CONTEXT.md's deferred note:
Codex documents its own per-server header configuration in its config file — configure it there
yourself if you need it. Keep the `${LITELLM_KEY}` reference literal, including its single quotes, and
never paste the value. (3) In `### Scripts and JSON output`, change the env-defaults sentence to name
`ENGRAM_HEADERS` beside `ENGRAM_RUNTIME`: `ENGRAM_URL`, `ENGRAM_AUTH`, `ENGRAM_RUNTIME`, and
`ENGRAM_HEADERS` (a comma-separated `NAME=ENVVAR` list) provide environment defaults; the corresponding
flags override them — `--header` on the command line replaces the whole `ENGRAM_HEADERS` list. Mention
that each present runtime's JSON row carries a `headers` string. No literal secret anywhere — env var
NAMEs only, matching the existing `${ENGRAM_TOKEN}` convention.

Run `task lint` (rumdl covers the skill file) and `go run ./internal/surfacesgen --check-setup` (the
prose edits must not touch the anchored region).

Commit as `docs(setup): document --header gateway headers, ENGRAM_HEADERS, and the codex limitation`
with the explicit pathspec `skill/engram/commands/engram-setup.md
docs-site/src/content/docs/guides/agent-setup.md`.
  </action>
  <verify>
    <automated>test "$(rg -c -F -e 'x-litellm-api-key=LITELLM_KEY' docs-site/src/content/docs/guides/agent-setup.md)" -ge 1 &amp;&amp; rg -q -F -e '### Gateway headers' docs-site/src/content/docs/guides/agent-setup.md &amp;&amp; rg -q -F -e 'ENGRAM_HEADERS' docs-site/src/content/docs/guides/agent-setup.md &amp;&amp; rg -q -F -e '--bearer-token-env-var' docs-site/src/content/docs/guides/agent-setup.md &amp;&amp; rg -q -F -e '| `codex` | Supported | Supported | Supported | Supported | Unsupported |' docs-site/src/content/docs/guides/agent-setup.md &amp;&amp; rg -q -F -e 'Gateway header (optional)' skill/engram/commands/engram-setup.md &amp;&amp; rg -q -F -e 'LITELLM_KEY' skill/engram/commands/engram-setup.md &amp;&amp; go run ./internal/surfacesgen --check-setup &amp;&amp; task lint</automated>
    <fails_when>non-zero exit from any link: a required substring (`### Gateway headers`, `ENGRAM_HEADERS`, `--bearer-token-env-var`, the codex table row, `Gateway header (optional)`, `LITELLM_KEY`) absent from its file, `surfacesgen --check-setup` reporting drift (the anchored region was touched), or `task lint` printing a rumdl/golangci-lint/yamlfmt/actionlint finding</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n -F '### Gateway headers' docs-site/src/content/docs/guides/agent-setup.md` prints exactly one line, and its line number is greater than that of `### Bearer token` and smaller than that of `### No authentication`.
    - `rg -n -F 'engram setup --url https://engram.example.com/mcp --auth oauth --header x-litellm-api-key=LITELLM_KEY' docs-site/src/content/docs/guides/agent-setup.md` prints exactly one line.
    - `rg -n -F "'x-litellm-api-key: \${LITELLM_KEY}'" docs-site/src/content/docs/guides/agent-setup.md` prints at least one line; `rg -n -F "'x-litellm-api-key={env:LITELLM_KEY}'" docs-site/src/content/docs/guides/agent-setup.md` prints at least one line; `rg -n -F '"x-litellm-api-key": "${LITELLM_KEY}"' docs-site/src/content/docs/guides/agent-setup.md` prints at least one line.
    - `rg -n -F '| Runtime | `oauth` | `oauth-client` | `bearer` | `none` | `--header` |' docs-site/src/content/docs/guides/agent-setup.md` prints exactly one line; the `claude-code`, `codex`, `opencode`, `generic` table rows end with `Supported |`, `Unsupported |`, `Supported |`, `Manual config |` respectively.
    - `rg -n -F 'ENGRAM_HEADERS' docs-site/src/content/docs/guides/agent-setup.md` prints at least one line inside the Scripts section (line number greater than that of `### Scripts and JSON output`).
    - `rg -n -F 'owned by `--auth`' docs-site/src/content/docs/guides/agent-setup.md` prints at least one line; `rg -n -F '--bearer-token-env-var' docs-site/src/content/docs/guides/agent-setup.md` prints at least one line.
    - `rg -n -e 'sk-[A-Za-z0-9]|Bearer [A-Za-z0-9]{8}' docs-site/src/content/docs/guides/agent-setup.md skill/engram/commands/engram-setup.md` prints nothing (no literal secret-shaped value in either doc).
    - `rg -n -F 'Gateway header (optional)' skill/engram/commands/engram-setup.md` prints exactly one line, with a line number smaller than that of `<!-- engram:rule:start setup-commands -->`; `rg -n -F '${LITELLM_KEY}' skill/engram/commands/engram-setup.md` prints at least two lines (the prose sentence and the generated fallback row).
    - `git diff --numstat ceabd8e9 -- skill/engram/commands/engram-setup.md | awk '{print $2}'` prints exactly `0` (zero deleted lines — additions only; a failed `git` prints nothing, which is not `0`), and `go run ./internal/surfacesgen --check-setup` exits 0.
    - `task lint` exits 0.
  </acceptance_criteria>
  <done>Both documents describe the gateway header shape with the canonical example, the env-var-NAME-never-a-value rule, `ENGRAM_HEADERS`, the `Authorization` ownership rule, the per-runtime rendering and order, and the Codex limitation; no literal secret appears; the anchored region is untouched; lint is green; committed with an explicit pathspec.</done>
</task>

<task type="auto">
  <name>Task 3: Plan gate — full `task`, license headers, zero go.mod drift, key-links gate, generated-surface drift</name>
  <files>none modified — verification-only; if `task fmt` or `task license:add` rewrites anything it must be one of this plan's five files_modified, and the rewrite is folded into a `chore(setup)` commit with an explicit pathspec</files>
  <read_first>
    - `Taskfile.yaml` — `default`, `lint`, `test:go`, `license:check`, `surfaces:gen`.
    - `CLAUDE.md` §Conventions — SPDX headers: `skill/**/SKILL.md`, slash-command markdown, and `docs-site/**` are EXCLUDED by `.licenserc.yaml`; this plan adds no Go file, so `license:check` needs no new header anywhere.
    - `.planning/STATE.md` §Blockers/Concerns — the three Phase 1 gates.
  </read_first>
  <action>
Run `task`, then `task license:check`, then `git diff --exit-code -- go.mod go.sum`, then `go test
./internal/keylinks/ -count=1` (this plan's key_links patterns anchor on text present at HEAD:
`Render()`'s add filter, the delegation test's `Cases()` loop, surfacesgen's `setupgen.Write` call, and
`ENGRAM_RUNTIME` in the docs), then the generated-surface drift checks: `go run ./internal/surfacesgen
--check-setup` and `go test ./cmd/engram -run 'TestHelpGolden|TestCatalogGolden' -count=1` (no golden
moves in this plan), and `git status --porcelain` must print nothing for `skill/`, `docs-site/`, and
`cmd/engram/testdata/`.

This is the phase's LAST plan: after it the orchestrator registers this phase's red-evidence patches
(`internal/store/redevidence_harness_test.go`) — do not edit that file and do not chase
`TestRedEvidencePatchesAreLive`. `cmd/engram` tests only with `-count=1`.

No commit is expected unless a formatter or the license fixer rewrote one of this plan's files; then
commit `chore(setup): gate fixups for the header docs` with the explicit pathspec of exactly those
file(s).
  </action>
  <verify>
    <automated>task &amp;&amp; task license:check &amp;&amp; git diff --exit-code -- go.mod go.sum &amp;&amp; go test ./internal/keylinks/ -count=1 &amp;&amp; go run ./internal/surfacesgen --check-setup &amp;&amp; go test ./cmd/engram -run 'TestHelpGolden|TestCatalogGolden' -count=1 &amp;&amp; test -z "$(git status --porcelain -- skill/ docs-site/ cmd/engram/testdata/)"</automated>
    <fails_when>non-zero exit from any link of the chain: `task` prints `task: Failed to run task` (or a golangci-lint / rumdl finding / `--- FAIL` line), `license-eye` lists a file as missing its header, `git diff --exit-code -- go.mod go.sum` prints a hunk, `go test ./internal/keylinks/` prints `FAIL`, `surfacesgen --check-setup` reports drift, a golden test prints a diff, or `git status --porcelain` prints a path under `skill/`, `docs-site/`, or `cmd/engram/testdata/`</fails_when>
  </verify>
  <acceptance_criteria>
    - `task` exits 0 with no `--- FAIL:` line and no lint finding.
    - `task license:check` exits 0.
    - `git diff --exit-code -- go.mod go.sum` exits 0.
    - `go test ./internal/keylinks/ -count=1` prints `ok`.
    - `go run ./internal/surfacesgen --check-setup` exits 0; `go test ./cmd/engram -run 'TestHelpGolden|TestCatalogGolden' -count=1` prints `ok`.
    - `git status --porcelain -- skill/ docs-site/ cmd/engram/testdata/ internal/setupgen/` prints nothing after the plan's commits.
  </acceptance_criteria>
  <done>All gate links exit 0, every generated surface is committed and non-drifting, and the working tree is clean under the plan's directories.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated tables → agent following `/engram-setup` | The agent copies commands from the tables; a drifted or hand-edited row would make it run something the real `Plan()` does not author. |
| docs → operator | Prose tells operators what to type; an example carrying a literal secret would be pasted verbatim. |
| `setupgen.Render()` → synthetic `Environment` | Generation must consult nothing on the generating machine (no environment read, no process). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-13 | Tampering | hand-edited or stale generated tables diverging from the real `ClaudeCode.Plan` | medium | mitigate | The region is regenerated by `surfacesgen` in the same commit as `Cases()` (Task 1); `surfacesgen --check-setup` and `TestRenderRealPlans` compare committed bytes against `Render()`; Task 2 asserts additions-only against the pre-phase file. |
| T-02-14 | Information Disclosure | a doc or table example carrying a literal secret beside a header name | medium | mitigate | Every example uses env var NAMEs (`LITELLM_KEY`, `ENGRAM_TOKEN`); Task 2's acceptance grep rejects `sk-…`/`Bearer <token>`-shaped literals in both docs; `Render()`'s synthetic env fails generation on any environment read, so no real value can leak into a table. |
| T-02-04 | Tampering | documentation implying engram writes Codex's config for a header | high | mitigate | The `/engram-setup` prose and `agent-setup.md` state the decline and the remedy (drop `--header` / `--runtime`), and the single permitted pointer says the operator configures Codex themselves; the generated tables carry only `claude mcp add` and `engram setup` invocations by construction; `TestSetupGeneratedInvocations` asserts codex's row is `failed` with `command == ""` for the header case. |
| T-02-08 | Tampering | conformance test invoking a real third-party CLI or the operator's `$HOME` | medium | mitigate | `TestSetupGeneratedInvocations` keeps `fakeSetupEnvWithRun` + `fakeSkillsEnv` + the `Getenv` guard that fails on any key other than `XDG_CONFIG_HOME`; `Render()` uses a synthetic `HomeDir`. |
| T-02-SC | Tampering | npm/pip/cargo installs | low | accept | Not applicable — no new packages; `docs-site` is not built or installed by this plan (no `task` gate exists for it; 02-RESEARCH.md Open Question 2 — treated as manual, consistent with the milestone's docs-requirement pattern); Task 3 asserts `git diff --exit-code -- go.mod go.sum`. |
</threat_model>

<verification>
- `go test ./internal/setupgen/ -count=1 -v` and `go test ./cmd/engram -run '^TestSetupGeneratedInvocations$' -count=1 -v` show `--- PASS` for every subtest including `bearer+header` in both lanes.
- `go run ./internal/surfacesgen --check-setup` exits 0; the four shipped table rows are byte-identical to `git show ceabd8e9:skill/engram/commands/engram-setup.md`.
- `agent-setup.md` contains `### Gateway headers`, the canonical example, the three per-runtime renderings, `ENGRAM_HEADERS`, the `--header` table column, and the Codex limitation; neither doc contains a secret-shaped literal.
- `task`, `task license:check`, `git diff --exit-code -- go.mod go.sum`, and `go test ./internal/keylinks/ -count=1` exit 0.
- `git diff --stat FIRST^..LAST` touches only the five files in `files_modified`.
- Manual (not gated — 02-RESEARCH.md Open Question 2, 02-VALIDATION.md Manual-Only Verifications): build the docs site locally and read the `Gateway headers` subsection for rendering; record the observation in the SUMMARY if performed.
</verification>

<success_criteria>
- The regenerated `/engram-setup` tables show the `bearer+header` gateway case with the auth header first and the extra header after, in both the delegation preview and the Claude Code fallback; the four shipped rows are byte-identical.
- The real CLI runs the fifth case end-to-end in both lanes: claude-code and opencode carry the header in their own syntax, codex declines with the exact reason, and the apply lane exits `exitPartial`.
- `/engram-setup` prose and `guides/agent-setup.md` document the gateway shape, the env-var-NAME rule, `ENGRAM_HEADERS`, the `Authorization` ownership rule, and the Codex limitation with no literal secret anywhere.
- `go.mod`/`go.sum` unchanged; `task`, `task license:check`, the key-links gate, and the generated-surface drift checks green.
</success_criteria>

<output>
Create `.planning/phases/02-custom-auth-headers/02-04-SUMMARY.md` when done
</output>
