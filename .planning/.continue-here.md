---
phase: 05-connect-record-state-parity
task: 1
total_tasks: 7
status: blocked
last_updated: 2026-08-15T20:58:21Z
---

<current_state>
Phase 05 planning is COMPLETE and converged (4 cross-AI review cycles, 0 HIGH / 0 actionable
remaining). Execution was dispatched and halted immediately at the phase's first gate: the
blocking `checkpoint:decision` at `05-01-PLAN.md:199`.

Sean selected **revisit** — do NOT commit `engramv1.Memory` field numbers 23-30 or the two
presence-model choices yet. Nothing has been dispatched and nothing has been mutated: no
executor ran, `proto/engram/v1/engram.proto` is untouched, `gen/` is untouched, and the working
tree is clean.
</current_state>

<completed_work>
- Roadmap SC1/SC3 corrected via edit-phase (commit `4ff2df5b`) — SC1 widened six fields/23-28 to
  eight/23-30 per D-04; SC3 rewritten to the no-read-path-rounding property per D-09.
- Research (`69339c60`), validation strategy (`825c6766`), pattern map + API-coverage
  declaration (committed with the plans).
- Plans authored and converged: `66239b66` (initial), `1a95f2fb` (cycle 1), `1e0398be` (cycle 2),
  `5e974c4b` (cycle 3). Reviews at `3e76501e`, `38408698`, `bee60eaf`, `6f4a7a79`.
- State recorded planned at `e298f347`.
- Checkpoint presented to Sean. Outcome: **revisit**.
</completed_work>

<remaining_work>
Everything. No plan task has run.

- 05-01 checkpoint — ANSWERED "revisit"; the decision it gates is now re-opened.
- 05-01 task 1 (tracer), task 2, task 3 — not started.
- 05-02 (exhaustive parity proof), 2 tasks — not started, wave 2.
- 05-03 (boundary-second identity + renderJSON), 2 tasks — not started, wave 2.
</remaining_work>

<decisions_made>
- Roadmap SC1/SC3 were corrected BEFORE planning, because `05-CONTEXT.md` (D-04, D-09) records
  that both were factually wrong as written and planning reads the roadmap, not CONTEXT.md.
  Durable record `fbgyfq6j6j`.
- The `api-coverage` detector's hit on this phase is a false positive (its signals matched
  engram's own Connect/MCP lanes, not a third-party service). Reasoned declaration written to
  `COVERAGE.md` so the seal-time gate accepts it without a capability matrix.
- Convergence ran 4 cycles, not 3: cycles 1 and 2 each produced a HIGH created by the PREVIOUS
  cycle's own repair, so Sean authorized one confirmation cycle. Cycle 4 came back clean and
  confirmed cycle 3's repair was the first that did not introduce a new defect.
- Gate spelling settled on `git diff <BASE> --` (base vs working tree), NOT `"$TASK_BASE"..HEAD`:
  `..HEAD` is blind to an uncommitted RED probe, which would force a criterion and its own
  GATE-RED PROOF to be different commands. Verified empirically, not reasoned. Durable record
  `0hza9gg6fw` covers the underlying post-commit-evaluation gotcha.
</decisions_made>

<blockers>
- **The 23-30 field-number commitment is re-opened.** `buf.yaml` sets `breaking: use: [FILE]`, so
  once these numbers merge to `main` they are a permanent published wire contract — a number
  cannot be reused even by a `deprecated = true` field, and neither number nor type can change
  without a breaking-change waiver.

  Three sub-decisions are bundled in this checkpoint and any of them may be what Sean wants to
  revisit:
  1. **The field SET** — eight fields (D-04) vs the roadmap's original six. The two extras are
     `summary_model` (29) and `summary_egress_at` (30). Including them REVERSES durable record
     `zyaa3m2fvd` under its own "reconsider BOTH together" clause.
  2. **`schema_version` presence model** — plain `uint32` at 28, where zero IS v0 IS absent
     (D-01, consistent with Phase 2 D-09). Moving to `optional uint32` later is FILE-mode
     breaking.
  3. **`superseded_by` presence model** — plain `string` at 23, nil collapsing to `""` (D-02).
     Moving to `optional string` later is likewise breaking.

- Consequence of dropping the two extra fields: D-05's inclusion rule is derived from the
  `json:"-"` tag as the SOLE exclusion mechanism, with no exemption list. Excluding
  `summary_model`/`summary_egress_at` from the wire while leaving them json-visible on
  `store.Memory` reintroduces exactly the exemption list D-05 exists to avoid, and
  `REQ-connect-parity-roundtrip-proof` cannot be written without classifying them somehow.
  Whatever is decided, that interaction has to be resolved with it — it is not separable.
</blockers>

<context>
This phase exists because the same gap recurred THREE times (v0.8.x, v0.11.x, v0.13.x): a green
`buf breaking` run plus compiling code was mistaken for evidence the Go-side field mapping
existed. `buf breaking` sees only the .proto schema and has zero visibility into
`memoryToProto`'s Go body. The plans are built around that: two independent detectors (name and
population), each with its own executor-performed RED proof against a mutation the other detector
cannot see.

The plans are in good shape and should NOT need rework unless the field set itself changes. If
only the presence models change, the edits are localized to 05-01's proto task and D-01/D-02
citations. If the field SET changes to six, 05-02's inclusion rule needs redesign and the roadmap
SC1 correction at `4ff2df5b` must be reverted or re-edited via `/gsd-edit-phase 5` — never by hand
(rule `8dfdhfs5nn`).
</context>

<next_action>
Ask Sean which of the three bundled sub-decisions he wants to revisit (field set / `schema_version`
presence / `superseded_by` presence), and whether the concern is the permanence itself or a
specific choice. Then reopen the corresponding decision in `05-CONTEXT.md` — via
`/gsd-discuss-phase 05` if the scope changes materially — and re-run
`/gsd-plan-review-convergence 05` only if the field set moves.

Delete this file once the decision is resolved.
</next_action>
