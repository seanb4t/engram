---
milestone: 2026-08-12.01
milestone_name: Record State & Schema Evolution
audited: 2026-08-22T02:20:00Z
status: tech_debt
scores:
  requirements: 27/27
  phases: 8/8
  integration: 5/5
  flows: 4/5
gaps: {}
nyquist:
  compliant_phases: [01, 02, 03, 04, 05]
  partial_phases: []
  not_validated_phases: [06, 07, 08]
  missing_phases: []
  overall: partial-coverage
tech_debt:
  - phase: 08-registry-docs-tail
    items:
      - "W3: guides/migrate.md:279 claims the CLI 'derives the equivalent number from absent and buckets directly'. No such derivation exists in migrate_family.go — the operator does the arithmetic by hand. The doc describes behavior the code does not have. (REQ-docs-record-state)"
      - "W4: CLAUDE.md's 'Every surface renders a record's derived state as up to four words' overstates. Two surfaces derive them (cmd/engram/memory_state.go, ui/src/lib/memorystate.ts); the MCP lane emits raw fields and leaves derivation to the caller — internal/server renders zero state words. (REQ-claude-md-migrations-convention)"
  - phase: 04-migration-cli-first-customer
    items:
      - "W2: engram migrate status omits `pending`, the value the milestone declared canonical. migrateStatusReportDoc (migrate_family.go:329-337) carries buckets/absent/future/future_total/total/current_version and no pending; statusSummary never computes it. The offline operator verb is the one surface that does not report it. Documented as deliberate at guides/migrate.md:279. (REQ-migrate-status-histogram, REQ-migration-state-visible)"
      - "W1: no full-stack E2E for the flagship operator flow. internal/e2e has zero migrate coverage. CLI layer tested against fakeMigrateFamilyStore; store layer against real Qdrant. Both halves strong, the join inferred. (REQ-migrate-command, REQ-migrate-preview-apply-parity, REQ-migrate-revert)"
  - phase: 05-connect-record-state-parity
    items:
      - "dprint drift, 4 files, task fmt:check exits 20: .claude/settings.json, docs-site/package.json, internal/webauth/static/_app/version.json, ui/tsconfig.json. NOTE: the version.json file is a vendored build artifact regenerated by task ui:build — it wants a dprint exclusion, not a format pass."
  - phase: cross-cutting
    items:
      - "Minor: schema version typed three ways in one proto file — Memory.schema_version is optional uint32 (engram.proto:52) while SchemaVersionBucket.version and MigrateStatusResponse.current_version are int32 (:186,203), against a Go-side migrate.Version int. No live break; a trap for a future signed sentinel."
      - "Nyquist: phases 06, 07, 08 have VALIDATION.md at status: draft (never reconciled by validate-phase). Per #2117 this is a coverage TODO, not a compliance failure."
resolved_during_audit:
  - "WR-01 (phase 04): --apply's single --timeout covering two full passes was undocumented. Now closed — migrateCmd.Long:257-259 states it; guides/migrate.md:214-218,229,245 covers both --apply verbs."
  - "SA1019 on deprecated Approximate (phase 07 deferred): closed. task lint:go reports 0 issues."
  - "TestNoEscapedPatternsRepoWide 19 findings (phase 07 deferred): closed. Test passes."
  - "rumdl MD041 on keylinks testdata (phase 01 deferred): closed by orchestrator at commit 51d0269e via .rumdl.toml exclude."
---

# Milestone Audit — 2026-08-12.01 (Record State & Schema Evolution)

**Status: `tech_debt`** — all 27 requirements satisfied, zero blockers, zero broken
cross-phase connections. Accumulated non-blocking items need a disposition decision
before the milestone is archived.

## Requirements Coverage — 27/27

Verified by mechanical 3-source cross-reference: the REQUIREMENTS.md traceability
table, the `requirements_completed` frontmatter of all 44 SUMMARY.md files, and all
8 VERIFICATION.md files. All three sets are identical — no orphans, no gaps, no
extras.

The coverage gate was RED-defeat-proved rather than merely observed passing:
removing `REQ-migrate-revert` from the summary set made the gate name exactly that
requirement; the unmodified control is empty. The empty result is therefore
evidence, not a vacuous pass.

| Phase | Requirements | VERIFICATION | Result |
|-------|--------------|--------------|--------|
| 01 Gate & CI Integrity | 3 | passed | satisfied |
| 02 Record Schema Versioning Foundation | 4 | passed | satisfied |
| 03 Migration Foundation | 5 | passed | satisfied |
| 04 Migration CLI: First Customer | 6 | passed | satisfied |
| 05 Connect Record-State Parity | 2 | passed | satisfied |
| 06 Typed Operator Renderer | 1 | passed | satisfied |
| 07 Console & CLI State Surfacing | 3 | passed | satisfied |
| 08 Registry & Docs Tail | 3 | passed | satisfied |

## Cross-Phase Integration — 5/5 seams wired

| Seam | Span | Verdict |
|------|------|---------|
| `schema_version` end-to-end | P2→P3→P4→P5→P7 | WIRED — no lane re-derives the value; Connect assigns unconditionally (`connectapi.go:103`), so a v0 record is not silently dropped |
| Record-state vocabulary | P5→P6→P7→P8 | WIRED — Go, TS, docs, and CLAUDE.md agree on word set, canonical order, and the half-open boundary rule |
| Migration status surfacing | P4→P7 | WIRED — one `Pending()` definition, four consumers; startup probe is read-only and never gates startup |
| Surfaces rule registry | P8 | WIRED — enforcement derived from the live cobra tree, not a hand list; fixed point CI-enforced at `ci.yaml:294-298` |
| Flagship operator E2E flow | P4→P2 | PARTIAL — see W1; both halves verified, the join is inferred |

## What Was Fixed Between Phase Verification and This Audit

Four items recorded as open in earlier phase artifacts were re-checked against the
live tree and are now closed. They are listed in the frontmatter's
`resolved_during_audit` block. The lesson worth keeping: a deferred item's status
goes stale while it sits, in both directions — three of these four closed
themselves through later phases' work, and nothing updated the note.

## Open Items

See the frontmatter `tech_debt` block. None blocks archival. The two that touch
shipped prose (W3, W4) are the ones most worth closing, because CLAUDE.md and the
operator guides are normative — a doc that describes behavior the code lacks is the
same defect class this milestone's Phase 8 existed to fix.
