---
milestone: 2026-08-12.01
audited: 2026-08-16T00:00:00Z
scope: checkpoint
scope_note: >-
  Mid-milestone checkpoint covering phases 1-5 only. Phases 6, 7 and 8 are not
  started (no plan directories exist) and their 9 requirements are out of scope,
  recorded as not-yet-built rather than unsatisfied. This is deliberately NOT
  named `2026-08-12.01-MILESTONE-AUDIT.md`: `gsd-complete-milestone` archives
  that exact filename and treats its presence as evidence the whole milestone
  was audited.
status: tech_debt
scores:
  requirements: 18/18
  phases: 5/5
  integration: 7/7
  flows: 3/3
  nyquist: 1/5
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: cross-cutting
    items:
      - "Nyquist: phases 1-4 VALIDATION.md still `status: draft` with unfilled plan-phase template placeholders — never reconciled by validate-phase (#2117 NOT-VALIDATED, not PARTIAL). Re-accrues the exact debt shape v0.12.x carried and v0.13.x cleared."
      - "ROADMAP.md progress table lists all eight 2026-08-12.01 phases as `Not started` / `0/N` while the checklist above it marks phases 1-5 `[x]` — the two halves of the same file disagree."
      - "UPSTREAM GAP (reported, not worked around): `gsd-core/bin/lib/artifacts.cjs:34` pins `/^v\\d+\\.\\d+(?:\\.\\d+)?-MILESTONE-AUDIT\\.md$/i`, which cannot match a CalVer milestone label. A `2026-08-12.01-MILESTONE-AUDIT.md` is invisible to the artifact-integrity checker."
  - phase: 01-gate-ci-integrity
    items:
      - "`TestSharedQdrantAddressHonored`'s address-equality half compares a value against itself; only the `testQdrantContainerBooted == false` half is discriminating. Disclosed in the code's own doc comment — a documented design choice, not a hidden vacuous gate."
  - phase: 02-record-schema-versioning-foundation
    items:
      - "CLOSED at this audit: the deferred `older-explicit` fixture row self-activated once Phase 4 raised `migrate.CurrentVersion` 0->1, and `olderThanCovered` is a checked value with `t.Fatalf` (`internal/store/schemaversion_compat_test.go:89-117`), not a comment. The deferral armed itself rather than rotting."
      - "CLOSED at this audit: `deferred-items.md` logged escaped `pattern:` fields in four `02-*-PLAN.md` files; Phase 1's repo-wide gate is green on the current tree."
  - phase: 03-migration-foundation-registry-invariants-sweep
    items:
      - "Criterion 4's no-persisted-cursor property is proven structurally (`Offset: nil` on every scroll call), not by a reddened test — plan 03-04's mutation did not reliably red the target subtest because Qdrant's own scroll-exhaustion incidentally recovers a skipped record. Disclosed, non-blocking."
      - "03-REVIEW.md IN-01 (registry-placement gate scoped to a hardcoded filename) and IN-02 (sealed-interface probe temp-dir location) remain open as cosmetic/maintenance notes."
  - phase: 04-migration-cli-first-customer
    items:
      - "WR-01 (`cmd/engram/migrate_family.go:187-213`): `--apply`'s single `--timeout` budget covers TWO full-backlog `Store.Migrate` passes (mint-heavy preview + apply), undocumented in `migrateCmd.Long` and `cli.md`. Large-collection robustness/doc gap."
      - "WR-02 (`internal/store/revert.go:160-181`): `RevertRefusalError` can concatenate two `field=`/`hint=` envelopes into one error string when a range holds both an irreversible-chain record and an unsupported-version record. Machine-parseability nuance on an already-correct refusal."
  - phase: 05-connect-record-state-parity
    items:
      - "#499: no single test spans CLI -> live Qdrant -> both wires. `migrate_family_test.go` drives a fake store; `connectapi_parity_test.go` calls `memoryToProto` directly rather than through a served endpoint. Accepted at plan time; Phase 7 builds the harness that closes it."
      - "#500: `ui/src/routes/+page.svelte`'s `recentQ` calls `listMemories({scope:''})` with no `cross_spine`, broken since 2026-08-12. Pre-existing, out of phase-5 scope, found by the first run of the new chromedp gate. `WINDOWS.md` id 4."
      - "dprint drift on four files untouched by this phase: `.claude/settings.json`, `docs-site/package.json`, `internal/webauth/static/_app/version.json`, `ui/tsconfig.json`. Needs a `dprint fmt .` pass."
resolved_since_verification:
  - "05-SECURITY.md staleness (T-05-SC premise falsified by chromedp) — folded in by commit `12cc3e0d`; T-05-SC now `accept`->`mitigate` closed, R-05-04 struck as withdrawn."
  - "05-VALIDATION.md — promoted to `status: validated` / `nyquist_compliant: true` by commit `ef76b9d1`."
---

# Checkpoint Audit — Milestone `2026-08-12.01` (Record State & Schema Evolution)

**Scope:** phases 1–5 of 8. **Status:** `tech_debt` — no blockers.

Phases 6 (Typed Operator Renderer), 7 (Console & CLI State Surfacing) and 8 (Registry &
Docs Tail) have no plan directories and are excluded. Their nine requirements are *not yet
built*, which is not the same as *unsatisfied*, and this report does not score them.

## Requirements Coverage — 3-Source Cross-Reference

Every in-scope requirement was resolved against three independent sources: the phase's
`VERIFICATION.md` status, the `requirements-completed` frontmatter across all 24 plan
SUMMARYs, and `REQUIREMENTS.md`'s traceability table.

| REQ-ID | Phase | VERIFICATION | SUMMARY | Traceability | Final |
|---|---|---|---|---|---|
| REQ-keylink-pattern-matchable | 1 | passed | 01-01, 01-02 | ⚠ Pending | **satisfied** ¹ |
| REQ-keylink-past-gates-reassessed | 1 | passed | 01-03 | ⚠ Pending | **satisfied** ¹ |
| REQ-ci-qdrant-container-stability | 1 | passed | 01-04, 01-05 | Complete | satisfied |
| REQ-schema-version-stamped | 2 | passed | 02-01, 02-02 | Complete | satisfied |
| REQ-schema-version-never-gates-recall | 2 | passed | 02-03 | Complete | satisfied |
| REQ-schema-version-wire-visible | 2 | passed | 02-01, 02-05 | Complete | satisfied |
| REQ-schema-version-forward-compatible | 2 | passed | 02-04 | Complete | satisfied |
| REQ-migration-step-registry | 3 | passed | 03-01, 03-02 | Complete | satisfied |
| REQ-migration-additive-only-gated | 3 | passed | 03-01, 03-03 | Complete | satisfied |
| REQ-migration-step-reversibility | 3 | passed | 03-01, 03-02 | Complete | satisfied |
| REQ-migrate-partial-failure-resume | 3 | passed | 03-04 | Complete | satisfied |
| REQ-migrate-converges-without-lock | 3 | passed | 03-01, 03-05 | Complete | satisfied |
| REQ-migrate-command | 4 | passed | 04-01, 04-03 | Complete | satisfied |
| REQ-migrate-status-histogram | 4 | passed | 04-02, 04-03 | Complete | satisfied |
| REQ-migrate-preview-apply-parity | 4 | passed | 04-01, 04-03 | Complete | satisfied |
| REQ-backfill-shortids-first-step | 4 | passed | 04-01, 04-04 | Complete | satisfied |
| REQ-migrate-revert | 4 | passed | 04-02, 04-03 | Complete | satisfied |
| REQ-migrate-never-automatic | 4 | passed | 04-02, 04-04 | Complete | satisfied |
| REQ-connect-record-state-parity | 5 | passed | 05-01, 05-02, 05-03 | Complete | satisfied ² |
| REQ-connect-parity-roundtrip-proof | 5 | passed | 05-02, 05-03, 05-04 | Complete | satisfied ² |

**18/18 satisfied. 0 unsatisfied, 0 partial, 0 orphaned.** (20 rows above; `REQ-schema-version-*`
and `REQ-connect-*` each appear once per REQ-ID — the count is of distinct IDs.)

¹ **Documentation drift, not a gap.** `01-VERIFICATION.md` records both as `✓ VERIFIED` with
live fail-first re-proofs (a corrupted pattern reintroduced into an archived plan reddened
`TestNoEscapedPatternsRepoWide` naming file:line, then reverted clean), and the reassessment
table carries 30/30 verdicts. Per the audit status matrix (`passed` + listed + `[ ]` →
satisfied, update checkbox), the checkboxes are stale, not the work. **Corrected by this audit.**

² Satisfied at mapper and renderer; PARTIAL across the mapper→renderer seam — see #499 under
tech debt. Accepted at plan time, tracked in three places.

**Orphan detection:** zero. Every requirement in the traceability table for phases 1–5 appears
in at least one phase VERIFICATION.md.

## Phase Verification

| Phase | Status | Score | Re-verified | Blocking gaps |
|---|---|---|---|---|
| 01 Gate & CI Integrity | passed | 3/3 | no | none |
| 02 Record Schema Versioning Foundation | passed | 5/5 | no | none |
| 03 Migration Foundation | passed | 5/5 | yes (stale-artifact trigger) | none |
| 04 Migration CLI & First Customer | passed | 6/6 | no | none |
| 05 Connect Record-State Parity | passed | 9/9 | yes (post G-05-9 closure) | none |

**5/5 passed.** No phase is missing a VERIFICATION.md. All five report zero anti-patterns
(`TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER`) across every phase-touched file.

## Cross-Phase Integration

Verified against source, not SUMMARY prose.

| # | Seam | Status | Evidence |
|---|---|---|---|
| 1 | P2→P3 dependency direction + leaf purity | ✓ WIRED | `internal/store/migrate.go:14` imports `internal/migrate`; no file under `internal/migrate/` imports any repo path. P4's `v1_step.go:6-9` imports only `fmt`+`maps`. Enforced by `leafpurity_test.go`, which derives the module path from `go.mod` so a rename cannot silently disable it. |
| 2 | P2 write stamp ↔ P4 `CurrentVersion=1` | ✓ WIRED | `store.go:646` `max(migrate.CurrentVersion, m.SchemaVersion)` — monotonic, never downgrades. `schemaversion_stamp_test.go:122-138` proves non-downgrade under `Update`'s in-lock re-read; `:208` proves partial writes never stamp unearned currency. |
| 3 | P3 registry → P4 sweep | ✓ WIRED | `Store.Migrate` drives P3 primitives directly — `Validate` (`migrate.go:235`), `Registry` (`:207`), `StepsFrom` (`:262`), `CheckAdditive` (`:288`). No re-implementation, no duplicate constant. The minter is a *parameter* to the step, so the leaf never captures store state. |
| 4 | P4 value → P5 wire | ✓ WIRED | `connectapi.go:102` emits the **stored** `m.SchemaVersion`, not a re-derived constant; no second `CurrentVersion` literal exists in `internal/server/`. Presence gated at `Has(fd)` (`connectapi_parity_test.go:667-684`) — the regression a getter-based test would miss. |
| 5 | P1 gate → P2–P5 artifacts | ✓ WIRED | `go test ./internal/keylinks/` green on the current tree; `TestGateScopesAreDistinct` prevents scope collapse. The bare-prose `key_links` defect (gotcha `chwrfp8q0v`) is closed for phases 2–5. CI runs the four Qdrant packages against the pinned service, byte-locked to `store_test.go`'s `qdrantImageTag`. |
| 6 | P4 `backfill-short-ids` alias | ✓ WIRED | `cmd/engram/backfill.go:36-42` — one-line adapters onto the shared sweep funcs, so the alias runs the identical DryRun→manifest-intersection apply. No parallel `MigrateOptions` construction. |
| 7 | Recall-gate blast radius | ✓ WIRED | `schemaversion_recallgate_test.go:1222-1223` walks every captured recall filter's key set and fails if `schema_version` appears. `IsEmpty(schemaVersionKey)` is confined to migration paths (`migrate_status.go:145`, `migratebacklog.go:63`), never recall. |

**7/7 seams wired.** This is the milestone's single highest-risk finding held: `schema_version`
never reaches a recall or authz filter, so no pre-migration record is silently excluded.

## End-to-End Flows

| Flow | Status | Notes |
|---|---|---|
| **A** legacy record → current, readable on both lanes | ✓ complete (1 warning) | Every link proven individually: absent-key detection, sweep mint+stamp (`migrate.go:414-422`), histogram move, MCP readability (`SchemaVersion` has no `omitempty`, pinned by `schemaversion_wire_test.go:76`), Connect readability. **Warning:** no single test spans CLI→live Qdrant→both wires — #499. |
| **B** revert refusal names snapshot recovery | ✓ complete | v0→v1 declared `Irreversible(...)` at `registry.go:30-32`. CLI refuses the **whole** range before any write: `migrate_family.go:510-522` calls `PreviewRevert`, and on `!plan.Reversible` returns `usageErrorf` **without ever calling `st.Revert`**. The mid-run `RevertRefusedError` path reports real counters, so an operator is never told "reverted: 0" for a run that mutated records. |
| **C** startup never migrates | ✓ complete | `warnPendingMigrations` inline at `tools.go:211`, calls only `MigrateStatus` under a 10s timeout. **Zero `.Migrate(` call sites anywhere in `internal/server/`** — the only callers are `migrate_family.go:169,200,206`. Pending predicate excludes `Future`, so an all-current collection does not spuriously warn. |

## Nyquist Coverage

| Phase | VALIDATION.md | status | nyquist_compliant | Verdict | Action |
|---|---|---|---|---|---|
| 1 | exists | `draft` | false | NOT-VALIDATED | `/gsd-validate-phase 1` |
| 2 | exists | `draft` | false | NOT-VALIDATED | `/gsd-validate-phase 2` |
| 3 | exists | `draft` | false | NOT-VALIDATED | `/gsd-validate-phase 3` |
| 4 | exists | `draft` | false | NOT-VALIDATED | `/gsd-validate-phase 4` |
| 5 | exists | `validated` | true | **COMPLIANT** | — |

Phases 1–4 still carry the unfilled `plan-phase` seed template (`{pytest 7.x / jest 29.x / ...}`
in the Test Infrastructure block). Per #2117 that is NOT-VALIDATED — a coverage TODO — not a
compliance failure: `nyquist_compliant: false` is not authoritative on a `draft` file.

## Open Seams for Phases 6–8

1. **P5 → P7:** the eight record-state fields are on the wire and nothing consumes them.
   Outside generated code, `schema_version` appears only in the `.proto`, the generated TS,
   and `guides/upgrade.md`. No hand-written `ui/src/` code reads it. Correct state for a
   checkpoint.
2. **#499** (mapper→renderer seam) is Phase 7's to close — it needs the real-server CLI
   harness Phase 7 would build anyway.
3. **`internal/migrate.Decoder`** is declared with zero implementors *by design* (D-11);
   the extension path is documented. Not an orphan.
4. **`MigrateOptions.Steps`/`Target`** are test-only injection points today. A future phase
   adding a second step must raise `CurrentVersion` and register the step in the same change —
   pinned as a failing test with an explanatory message at `internal/migrate/migrate_test.go:19-21`.

---

*Checkpoint audit: 2026-08-16 · phases 1–5 of 8 · 0 blockers*
