---
phase: 21-ci-maintenance-hygiene
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/server/summaryqueue.go
  - internal/server/usagequeue.go
  - internal/server/queue_export_test.go
  - internal/server/tools.go
  - internal/server/tools_test.go
autonomous: true
requirements:
  - REQ-p11-review-residuals

must_haves:
  truths:
    - "WR-03 (D-04): `Wait()` cannot be called from production code — not by convention or doc-comment, but because the method no longer exists in any file `go build` compiles. Applies to BOTH `summaryQueue` and `usageQueue` (D-00c: `usageQueue` is a verbatim mirror carrying the identical hazard)."
    - "IN-01 (D-05, D-00a): `storeMemory` and `scheduleMemory` no longer duplicate the MintShortID → Upsert → enqueue block; both delegate to one shared `persistAndEnqueue` helper. The real IN-01 — NOT ROADMAP SC2's phantom 'duplicate depth-gauge registration' (the gauge is registered exactly once at `internal/server/tools.go:255`)."
    - "IN-01 invariant preserved: an enqueue happens only after a confirmed-successful Upsert, exactly once per successful write — and provably does NOT happen when Upsert fails. A new regression test pins this for both handlers; it would fail if the refactor inverted the ordering."
    - "`storeDiscovery` and `storeRule` still never enqueue (D-05 negative space) — neither is folded into the helper."
    - "IN-02 (D-06): `TestBuildDepsFromEnvLoadsConfigOnce` cannot start a real summary queue from ambient `ENGRAM_*` env, so it leaks no worker goroutines."
  artifacts:
    - "internal/server/queue_export_test.go — NEW test-only file holding both relocated `Wait()` methods (SPDX header required)"
    - "internal/server/tools.go — new `(*deps).persistAndEnqueue` helper; both call sites collapsed to it"
    - "internal/server/tools_test.go — new enqueue-ordering regression test + two added `t.Setenv` calls in `TestBuildDepsFromEnvLoadsConfigOnce`"
  key_links:
    - "`persistAndEnqueue` → `d.st.MintShortID` → `d.st.Upsert` → `d.summaryQueue.tryEnqueue`. The ordering of these three is the whole invariant; the enqueue must be unreachable when Upsert returns an error."
    - "`Wait()` in `queue_export_test.go` → the 10 existing in-package `_test.go` call sites. In-package tests (`package server`) resolve a `_test.go`-defined method fine; `go build` excludes it entirely."
---

<objective>
Close the three deferred Phase-11 async-summary review residuals (#335): WR-03 (`Wait()` misuse
hazard), IN-01 (duplicated Upsert-then-enqueue block), IN-02 (test hermeticity). All three are
mechanical, self-contained, and confined to `internal/server/`.

Purpose: these are the last open findings from the Phase-11 review. Each is small; together they
remove a foot-gun (WR-03), a divergence risk (IN-01 — two copies of a write-path invariant that
must stay identical), and a goroutine leak (IN-02).

Output: `Wait()` relocated out of production reach for both queues, one shared `persistAndEnqueue`
helper with a regression test pinning its ordering invariant, and a hermetic
`TestBuildDepsFromEnvLoadsConfigOnce`. No public API change, no proto/wire/Helm change, no new
dependency, no behavior change visible to any client.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-ci-maintenance-hygiene/21-CONTEXT.md
@.planning/phases/21-ci-maintenance-hygiene/21-PATTERNS.md
@.planning/codebase/TESTING.md
@internal/server/summaryqueue.go
@internal/server/usagequeue.go
@internal/server/tools.go
</context>

<scope_corrections>
Read these before touching anything — three authoritative corrections that override the ROADMAP
and the issue text:

1. **ROADMAP SC2 mislabels IN-01** as "duplicate depth-gauge registration" (D-00a). That defect
   does not exist — the gauge is registered exactly once at `internal/server/tools.go:255`. Do NOT
   go looking for a duplicate registration; there is none. The real IN-01 is Task 2's target. Plan
   01 corrects the ROADMAP wording; this plan implements the real finding.
2. **Line numbers in `11-REVIEW.md` and issue #335 have DRIFTED** (D-08). Re-locate every target by
   symbol, never by the cited line number. This has already bitten twice: RESEARCH.md recorded
   `Wait()` at `summaryqueue.go:313`/`usagequeue.go:198`, but as of this plan they are at `:309`
   and `:194`. Treat every line number in this plan as a hint, not an address.
3. **WR-01 stays closed** (D-07). The worker pool's `context.Background()` was reviewed and
   **accepted as a non-issue** in #335. Do not reopen, refactor, or "improve" it.
</scope_corrections>

<artifacts_this_phase_produces>
Every symbol and file this plan creates:

| Artifact | Kind | Location |
|---|---|---|
| `internal/server/queue_export_test.go` | NEW file (test-only) | `internal/server/` |
| `func (q *summaryQueue) Wait()` | method — **relocated**, test-only | `internal/server/queue_export_test.go` (deleted from `summaryqueue.go`) |
| `func (q *usageQueue) Wait()` | method — **relocated**, test-only | `internal/server/queue_export_test.go` (deleted from `usagequeue.go`) |
| `func (d *deps) persistAndEnqueue(ctx context.Context, m store.Memory, vec []float32) (id, shortID string, err error)` | NEW unexported method | `internal/server/tools.go` |
| `upsertFailStore` (or equivalent) | NEW test-only type — `memStore` wrapper that fails `Upsert` | `internal/server/tools_test.go` |
| `TestPersistAndEnqueueSkipsEnqueueOnUpsertFailure` (name is Claude's call) | NEW test | `internal/server/tools_test.go` |

No new package, no new dependency, no new exported symbol, no new file outside `internal/server/`.
`queue_export_test.go` is the only new file and it is test-only.
</artifacts_this_phase_produces>

<repo_gates>
- **`gofmt -l .` must be clean before pushing.** CI's `test` job runs a `gofmt -l .` precheck
  (`.github/workflows/ci.yaml:47-52`) that is **stricter than golangci-lint**. Phase 20 lost a CI
  run to exactly this trap: local golangci and `task test:go` were both green while a
  gofmt-unclean test file reddened CI. Run `gofmt -l .` (or `task fmt`) before every push.
- **Every new Go file needs the Apache-2.0 SPDX header** (`task license:check` enforces it,
  `task license:add` applies it). `queue_export_test.go` is a new file — it needs one.
- **`task` default (lint + test) is BLOCKED** until Plan 01 lands (the rumdl failure). Until then
  use the granular subtargets: `task test:go`, `task lint:go`, `task fmt`. This is a soft ordering
  dependency only — Plan 01 changes no code this plan touches.
- Conventional Commits required; PR titles are CI-validated.
</repo_gates>

<tasks>

<task type="auto">
  <name>Task 1: Make Wait() unreachable from production for both queues (D-04 / WR-03)</name>

  <files>internal/server/summaryqueue.go, internal/server/usagequeue.go, internal/server/queue_export_test.go</files>

  <read_first>
    - `internal/server/summaryqueue.go` — locate the `Wait` method **by symbol** (near `:309`; the
      line has already drifted once, see scope correction 2). Read its full doc-comment and body.
    - `internal/server/usagequeue.go` — same, near `:194`. Confirm for yourself that it is the
      verbatim mirror D-00c claims.
    - `.planning/phases/21-ci-maintenance-hygiene/21-PATTERNS.md` §"`internal/server/summaryqueue.go`
      / `usagequeue.go` — WR-03 `Wait()` relocation" — carries both method bodies verbatim, the SPDX
      header text, and the ID-citing doc-comment convention. Copy from there rather than re-deriving.
  </read_first>

  <action>
    WR-03's hazard: `Wait()` blocks on an in-flight WaitGroup and is safe only in tests; a future
    production caller could deadlock the write path. D-04 rejects the doc-comment option outright —
    make the misuse structurally impossible instead. The evidence supports it: every one of the 10
    call sites in the repo is in a `_test.go` file (verified again this session across
    `summaryqueue_test.go`, `usagequeue_test.go`, `tools_test.go`, `connectapi_test.go`), so there
    is **no production caller to preserve**.

    Create a new file `internal/server/queue_export_test.go`, `package server`, carrying the
    Apache-2.0 SPDX header (copy the exact two-line form from `summaryqueue.go`'s top — a new file
    without it fails `task license:check`). Move BOTH `Wait` methods into it verbatim — bodies and
    existing doc-comments unchanged. Then delete both method definitions from `summaryqueue.go` and
    `usagequeue.go`.

    Apply to `usageQueue` as well as `summaryQueue` (D-00c). WR-03 was written during Phase 11,
    before `usageQueue` existed, so the issue text names only `summaryQueue` — but the mirror
    carries the identical hazard and is explicitly in scope. Fixing only one leaves the foot-gun.

    Mechanism notes:
    - **No build tag is needed.** The `_test.go` suffix alone is a compiler-level exclusion:
      `go build` never compiles `_test.go` files, so the method is structurally unreachable from
      `serve.go` or any other production caller. Do not add `//go:build` anything.
    - The package's tests are **in-package** (`package server`, not `package server_test`), so a
      `_test.go`-defined method resolves fine from every existing call site. **No call site needs to
      change** — do not edit `summaryqueue_test.go`, `usagequeue_test.go`, or `connectapi_test.go`.
    - The repo has **no existing `export_test.go` convention** — this file establishes one. Add a
      short file-level comment explaining the file's purpose, citing WR-03 by ID (the package's
      comments cite finding IDs inline — `CR-01`, `SC#1`, `SC#2` — match that style).

    Leave the `itemDone()` / `Shutdown()` shutdown kernel and the RWMutex closed-guard that fixed
    CR-01 completely untouched. WR-03 concerns only `Wait()`'s reachability.
  </action>

  <verify>
    <automated>go build ./...</automated>
    <automated>rg --glob '!*_test.go' -n 'func \(q \*(summaryQueue|usageQueue)\) Wait' internal/server/; test $? -ne 0</automated>
    <automated>go test ./internal/server/... -run 'TestSummaryQueue|TestUsageQueue' -v</automated>
    <automated>task license:check</automated>
    <automated>gofmt -l internal/server/</automated>
  </verify>

  <done>Neither queue defines a `Wait` method in any file `go build` compiles; both live in the new test-only `queue_export_test.go`; all existing queue tests pass unmodified; SPDX and gofmt clean.</done>

  <acceptance_criteria>
    - `go build ./...` succeeds — the production binary compiles with no `Wait` method on either queue.
    - No non-test file under `internal/server/` defines a `Wait` method on either queue type (the
      glob-filtered search finds no match).
    - `internal/server/queue_export_test.go` exists, is `package server`, carries the Apache-2.0
      SPDX header, and defines both relocated methods.
    - All existing `TestSummaryQueue*` / `TestUsageQueue*` tests pass **unchanged** — no call site
      was edited.
    - `task license:check` passes; `gofmt -l internal/server/` prints nothing.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract persistAndEnqueue and pin its ordering invariant (D-05 / IN-01)</name>

  <files>internal/server/tools.go, internal/server/tools_test.go</files>

  <read_first>
    - `internal/server/tools.go` — `storeMemory` (near `:650`) and `scheduleMemory` (near `:679`).
      Locate **by symbol**. The duplicated region runs from `MintShortID` to the `return`.
    - `internal/server/tools.go` — `storeDiscovery`'s doc-comment (near `:705`), which states why
      discoveries never enqueue. This is the negative space the helper must not absorb.
    - `internal/server/tools_test.go:715-744` — `TestStoreMemoryEnqueuesOnSuccess`, the existing
      positive-path test. `:785` — `TestDiscoveryAndRuleNeverEnqueue`, the existing negative-space test.
    - `internal/server/store_iface.go:25-43` — the `memStore` interface `deps.st` is typed as.
    - `internal/server/fakestore_test.go:43-119` — `spyStore` and `newSpyDeps()`, the Qdrant-free
      deps constructor the new test builds on. Note `spyStore.Upsert` always returns nil — it has no
      error-injection hook, which is why the new test needs a wrapper.
    - `.planning/phases/21-ci-maintenance-hygiene/21-PATTERNS.md` §"`internal/server/tools.go` —
      IN-01 `persistAndEnqueue` extraction" — both call sites verbatim plus the suggested helper.
  </read_first>

  <behavior>
    The new regression test must pin the invariant the refactor has to preserve. Wave 0 enumeration
    (done this session) found the existing coverage is **positive-path only** — nothing asserts the
    ordering fails safe:

    - `TestStoreMemoryEnqueuesOnSuccess` (`tools_test.go:718`) — covers `storeMemory` enqueue-on-success.
    - `TestDiscoveryAndRuleNeverEnqueue` (`tools_test.go:785`) — covers the negative space.
    - **Gap 1:** no `scheduleMemory` enqueue test exists at all.
    - **Gap 2:** no test asserts that a **failed** `Upsert` results in **no** enqueue — i.e. nothing
      would fail if the refactor moved the enqueue above the Upsert, or outside its error branch.
      That is precisely the invariant D-05 says to "preserve exactly", so it needs a guard.

    New test expectations (one test, table-driven over both handlers):
    - Test 1: `storeMemory` with a store whose `Upsert` returns an error → the handler returns that
      error AND zero ids are enqueued.
    - Test 2: `scheduleMemory` with the same failing store → same: error returned, zero enqueued.
    - Test 3 (may reuse the existing `TestStoreMemoryEnqueuesOnSuccess` rather than duplicating it):
      a successful write enqueues exactly one id, and it is the record's id.

    Ordering discipline for this task — this is a **behavior-preserving refactor**, so the test is a
    characterization test, not a RED→GREEN cycle:
    1. Write the test FIRST, against the **un-refactored** code. It must **PASS** immediately. If it
       fails before the refactor, the invariant is already broken — stop and report; that is a real
       bug, not a refactor concern.
    2. Then perform the extraction.
    3. Re-run. It must still pass. A test that only ever ran green after the refactor proves nothing.
  </behavior>

  <action>
    IN-01: `storeMemory` and `scheduleMemory` duplicate the MintShortID → Upsert → enqueue block
    verbatim, so the write-path invariant is stated twice and can silently diverge.

    **Step 1 — add the characterization test to `tools_test.go`** (per `<behavior>`, before the
    refactor). `spyStore` has no error-injection hook and `deps.st` is a `memStore` interface, so the
    cheapest correct approach is a small test-only wrapper that embeds `memStore` and overrides only
    `Upsert` to return an injected error — every other method delegates to the embedded `spyStore`.
    Build deps directly in the Qdrant-free style `newSpyDeps()` uses (a `deps` literal with the
    wrapper as `st`, `fakeEmbedder{}` as `em`, and a live `summaryQueue` whose fill records ids), so
    the test needs no testcontainer. Assert the handler returns the injected error and the recorder
    saw zero ids. Cover both `storeMemory` and `scheduleMemory`; `scheduleMemory` needs a valid
    window argument — mirror an existing `TestScheduleMemory*` test's args shape (`:448`, `:484`,
    `:614`) rather than inventing one.

    Note the queue's `tryEnqueue` is non-blocking and its fill is asynchronous, so assert absence
    deterministically via the `Wait()` drain seam (Task 1 relocated it but did not change it — it is
    still callable from `_test.go`), exactly as `TestDiscoveryAndRuleNeverEnqueue` does. Do not use
    `time.Sleep`.

    Confirm the test passes on the un-refactored code before continuing.

    **Step 2 — extract the helper into `tools.go`.** Add an unexported method on `deps` with the
    shape D-05 specifies: it takes the context, the fully-built memory, and the vector, and returns
    `(id, shortID string, err error)`. RESEARCH confirmed this signature fits both call sites with no
    adaptation — `m` and `vec` are already fully built when each handler reaches the duplicated
    region. Move the block into it verbatim, preserving the existing explanatory comment about
    enqueueing only after a confirmed-successful Upsert (it cites SC#1/SC#2 — keep the citations).
    Then collapse both call sites to delegate to it.

    **Preserve exactly:**
    - The ordering. MintShortID, then Upsert, then — and only if Upsert returned no error — enqueue.
      Both existing early-return error branches stay. Never enqueue on an error path.
    - Each handler's own pre-block logic, untouched: `storeMemory`'s embed-first ordering,
      `scheduleMemory`'s `parseWindow` + `NotBefore`/`NotAfter` assignment. Only the shared tail moves.

    **Do NOT fold in `storeDiscovery` or `storeRule`** (D-05 negative space). Both deliberately never
    enqueue — discoveries own their own summaries. Verified again this session: `rules.go` contains no
    reference to the queue or to enqueueing at all. `TestDiscoveryAndRuleNeverEnqueue` guards this;
    it must keep passing untouched.

    **Do not touch the depth-gauge registration** (`tools.go:255`). It is registered exactly once and
    is not a defect — see scope correction 1.
  </action>

  <verify>
    <automated>go test ./internal/server/... -run 'TestStoreMemory|TestScheduleMemory|TestDiscoveryAndRuleNeverEnqueue|TestPersistAndEnqueue' -v</automated>
    <automated>go test ./internal/server/... -count=1</automated>
    <automated>task lint:go</automated>
    <automated>gofmt -l internal/server/</automated>
  </verify>

  <done>The MintShortID→Upsert→enqueue sequence appears exactly once in `tools.go`; both handlers delegate to `persistAndEnqueue`; a failing Upsert produces zero enqueues from both; the new test passed before AND after the extraction; `storeDiscovery`/`storeRule` still never enqueue.</done>

  <acceptance_criteria>
    - The new test passed against the un-refactored code (characterization confirmed) AND passes
      after the extraction. The SUMMARY must record that both runs happened — a test first run only
      after the refactor is not a valid guard.
    - `internal/server/tools.go` defines exactly one `persistAndEnqueue` method on `deps`; both
      `storeMemory` and `scheduleMemory` delegate to it; the MintShortID/Upsert/enqueue sequence
      appears exactly once in the file.
    - A failing `Upsert` produces zero enqueues from both handlers.
    - `TestStoreMemoryEnqueuesOnSuccess`, `TestStoreMemoryReturnsWhenSummarizerHangs`, and
      `TestDiscoveryAndRuleNeverEnqueue` all pass **unmodified**.
    - Neither `storeDiscovery` nor `storeRule` calls the new helper.
    - `task lint:go` passes; `gofmt -l internal/server/` prints nothing.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Make TestBuildDepsFromEnvLoadsConfigOnce hermetic (D-06 / IN-02)</name>

  <files>internal/server/tools_test.go</files>

  <read_first>
    - `internal/server/tools_test.go` — `TestBuildDepsFromEnvLoadsConfigOnce`, near `:1624`. Locate
      by symbol (D-08). Read its existing `t.Setenv` block and the comment above it.
    - `internal/config/config_test.go:13-20` — `TestLoadDefaults`, the established in-repo
      env-clearing pattern this copies. Note its comment explaining that an empty value preserves
      the registry default rather than unsetting.
  </read_first>

  <action>
    IN-02: `TestBuildDepsFromEnvLoadsConfigOnce` calls `buildDepsFromEnv`, which reads the summary
    config straight from the process env. If a developer's or CI shell has the summary-on-write env
    set, the test starts a real summary queue it never shuts down — leaking 2 worker goroutines for
    the lifetime of the test binary.

    Per D-06, add two `t.Setenv` calls setting the summary model and summary-on-write variables to
    the empty string, alongside the three existing `t.Setenv` calls in this test. The exact variable
    names are given verbatim in `21-CONTEXT.md` §D-06 and `21-PATTERNS.md` §"IN-02 test hermeticity"
    — copy them from there; do not guess. Extend the existing comment block above the `t.Setenv`
    group to explain the isolation intent, matching the style of `config_test.go`'s `TestLoadDefaults`
    comment.

    Use the empty string, not an unset — that is the pattern `TestLoadDefaults` establishes and it
    preserves the registry default (the documented empty-env invariant) rather than leaving the
    variable's behavior ambiguous. `t.Setenv` restores the prior value at test end automatically; do
    not add manual cleanup.

    Change nothing else in the test — not the `configLoad` spy, not the `t.Cleanup`, not the
    assertions, not the Qdrant guard. This is a two-line hermeticity fix, not a rewrite.
  </action>

  <verify>
    <automated>go test ./internal/server/... -run TestBuildDepsFromEnvLoadsConfigOnce -v -count=1</automated>
    <automated>go test ./internal/server/... -count=1</automated>
    <automated>gofmt -l internal/server/</automated>
  </verify>

  <done>`TestBuildDepsFromEnvLoadsConfigOnce` passes with the summary env vars ambiently set and starts no queue; exactly two `t.Setenv` calls were added; nothing else in the test changed.</done>

  <acceptance_criteria>
    - `TestBuildDepsFromEnvLoadsConfigOnce` passes both with a clean env and with the summary
      env vars ambiently set — verify the second case explicitly, since that is the whole point of
      the fix (e.g. run the test once with the summary-on-write variable exported in the shell; it
      must still pass and must not start a queue).
    - The test's `t.Setenv` group has exactly two new entries; the `configLoad` spy, `t.Cleanup`, and
      all assertions are unchanged.
    - `gofmt -l internal/server/` prints nothing.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP/Connect client → write handlers (`storeMemory`, `scheduleMemory`) | Untrusted client input crosses here. **This plan does not touch that boundary** — it refactors the post-validation, post-authz tail of the write path only. Owner/actor stamping, authz, and validation all happen upstream of the extracted region and are unchanged. |
| in-process write path → async summary worker pool | The enqueue seam. A refactor error here could enqueue records that were never persisted, or block the write path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-02-01 | Tampering | `persistAndEnqueue` (`tools.go`) | medium | mitigate | A botched extraction could enqueue on a **failed** Upsert, feeding the summarizer ids for records that do not exist, or could enqueue twice per write. Mitigated by Task 2's characterization test, which asserts zero enqueues on a failing Upsert for both handlers and is required to pass **before** the refactor as well as after. |
| T-21-02-02 | Denial of Service | `persistAndEnqueue` / `tryEnqueue` | medium | mitigate | If the extraction made the enqueue blocking or synchronous, the write path would stall behind a slow/hung summarizer (the exact hazard SC#2 exists to prevent). Mitigated by preserving `tryEnqueue`'s non-blocking semantics verbatim and by `TestStoreMemoryReturnsWhenSummarizerHangs` passing unmodified. |
| T-21-02-03 | Denial of Service | `Wait()` on both queues | low | mitigate | The WR-03 hazard itself: a future production caller of `Wait()` blocks on the in-flight WaitGroup and can deadlock the serving path. Mitigated structurally by Task 1 — the method ceases to exist in any file `go build` compiles, so no production caller can be written. This is the disposition D-04 demands over a doc-comment. |
| T-21-02-04 | Elevation of Privilege | write-path authz (owner/actor stamping) | low | accept | The extracted region sits entirely downstream of owner/actor stamping and authz; `m` is fully built before it. No authz logic is moved, read, or re-derived. Accepted: no mitigation needed beyond the existing `TestStoreMemoryStampsOwnerHandler` continuing to pass. |
| T-21-02-05 | Information Disclosure | test env handling (IN-02) | low | mitigate | Ambient `ENGRAM_*` env leaking into a test could point a leaked worker at a real summarizer endpoint with real credentials from the dev/CI shell. Mitigated by Task 3's `t.Setenv` clearing. |

No `T-21-02-SC` supply-chain row: this plan installs no npm/Go/pip packages and adds no dependency
(RESEARCH.md §"Package Legitimacy Audit" — not applicable). ASVS L1, block-on-high: no high or
critical threats identified. T-21-02-01 and -02 are medium and both mitigated regardless.
</threat_model>

<verification>
1. `go build ./...` succeeds with no `Wait` method on either queue in production code.
2. `go test ./internal/server/... -count=1` fully green (needs Qdrant via testcontainers; set
   `ENGRAM_REQUIRE_QDRANT=1` to match CI's fail-closed gate if you want parity locally).
3. `task lint:go` and `task license:check` pass.
4. **`gofmt -l .` prints nothing** — mandatory before push. CI's `test` job precheck is stricter
   than golangci-lint and Phase 20 lost a run to exactly this.
5. Once Plan 01 has landed: `task` (full default gate) green.
</verification>

<success_criteria>
- REQ-p11-review-residuals satisfied: WR-03, IN-01, IN-02 all resolved, with `usageQueue` covered
  alongside `summaryQueue` (D-00c).
- No behavior change observable to any client: no proto change, no wire change, no public API
  change, no config change.
- One atomic commit closing #335. Suggested:
  `refactor(server): resolve phase-11 review residuals — test-only Wait, shared persistAndEnqueue, hermetic env test`
  with `Closes #335` in the body.
</success_criteria>

<output>
Create `.planning/phases/21-ci-maintenance-hygiene/21-02-SUMMARY.md` when done.

The SUMMARY MUST record:
- The chosen `Wait()` relocation mechanism and the new file path.
- The final `persistAndEnqueue` signature as shipped.
- The new test's name, and **explicit confirmation that it passed against the un-refactored code
  before the extraction** (the characterization step — see Task 2 acceptance criteria).
</output>
