---
phase: 20-correctness-polish
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - charts/engram/templates/_helpers.tpl
  - charts/engram/templates/memory-mcp.yaml
  - charts/engram/templates/summarize-cronjob.yaml
  - charts/engram/values.yaml
  - Taskfile.yaml
autonomous: true
requirements:
  - REQ-summarize-cronjob
user_setup: []

must_haves:
  truths:
    - "The Helm chart ships engram summarize-missing as a batch/v1 CronJob reusing the Deployment's image/env via a shared _helpers.tpl block — ROADMAP SC6"
    - "The CronJob is opt-in / disabled by default (cronjob.enabled: false) — D-07"
    - "When enabled, defaults are daily schedule + concurrencyPolicy Forbid + restartPolicy OnFailure + successfulJobsHistoryLimit 3 + failedJobsHistoryLimit 1, all surfaced as values overrides — D-08"
    - "The Deployment's rendered env is byte-identical before/after the _helpers.tpl factor-out (no-op diff) — D-09"
  artifacts:
    - "charts/engram/templates/_helpers.tpl — new engram.containerEnv named template (verbatim extraction of memory-mcp.yaml env L30-163)"
    - "charts/engram/templates/summarize-cronjob.yaml — new batch/v1 CronJob (opt-in, args summarize-missing --all-scopes)"
    - "charts/engram/values.yaml — new memory.summarize.cronjob block"
    - "Taskfile.yaml chart:validate target — proportional bash grep/diff over helm template + a re-runnable engram.containerEnv block drift pin (no helm-unittest plugin)"
  key_links:
    - "engram.containerEnv named template is include-d by BOTH memory-mcp.yaml Deployment AND summarize-cronjob.yaml — single env source (D-09)"
    - "CronJob container runs the existing `engram summarize-missing --all-scopes` (no new CLI flag; --all-scopes required per cmd/engram/summarize.go:38)"
---

<objective>
Close #269 (summarize-missing CronJob) — the Helm subsystem slice of Phase 20 (per D-10 subsystem split).

Factor the Deployment's inline container env block into a new `charts/engram/templates/_helpers.tpl` named template (`engram.containerEnv`) and `include` it from both the existing Deployment and a new `batch/v1` CronJob template that invokes `engram summarize-missing --all-scopes`. Per D-09 the env block is a byte-identical extraction — the rendered Deployment env must be a no-op diff. Per D-07 the CronJob is opt-in / disabled by default (`cronjob.enabled: false`). Per D-08 the enabled defaults are daily schedule, `concurrencyPolicy: Forbid`, `restartPolicy: OnFailure`, `successfulJobsHistoryLimit: 3`, `failedJobsHistoryLimit: 1`, all surfaced as `values.yaml` overrides under a new `cronjob:` block. Add a proportional chart-validation step (bash grep/diff over `helm template` in a Taskfile target — NOT a `helm-unittest` plugin dependency).

Purpose: Operators run the summarize-missing sweep on a schedule from the chart itself, eliminating the out-of-chart CronJob drift incidents cited in #269, with zero env duplication.
Output: `_helpers.tpl`, a CronJob template, a `cronjob:` values block, and a `chart:validate` Taskfile target.
</objective>

<artifacts_produced>
## Artifacts this phase produces (Plan D)

New / changed symbols:
- `charts/engram/templates/_helpers.tpl` — new `{{ define "engram.containerEnv" }}` named template (byte-identical extraction of memory-mcp.yaml env L30-163)
- `charts/engram/templates/summarize-cronjob.yaml` — new `batch/v1` CronJob `memory-mcp-summarize` (opt-in; args `summarize-missing --all-scopes`)
- `charts/engram/values.yaml` — new `memory.summarize.cronjob` block (enabled/schedule/concurrencyPolicy/restartPolicy/successfulJobsHistoryLimit/failedJobsHistoryLimit)
- `Taskfile.yaml` `chart:validate` target — bash grep/diff over `helm template` + a re-runnable `engram.containerEnv` block drift pin (checksum/line-count; no new tooling dependency)

No new CLI flag (the CronJob invokes the existing `engram summarize-missing`). No console change. No SPDX header needed (charts/** and Taskfile.yaml are outside the .licenserc.yaml paths).
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-correctness-polish/20-CONTEXT.md
@.planning/phases/20-correctness-polish/20-RESEARCH.md
@.planning/phases/20-correctness-polish/20-PATTERNS.md
@.planning/phases/20-correctness-polish/20-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract the Deployment container env into _helpers.tpl (byte-identical, D-09)</name>
  <files>charts/engram/templates/_helpers.tpl, charts/engram/templates/memory-mcp.yaml</files>
  <read_first>
    - charts/engram/templates/memory-mcp.yaml (env: key at L30; full env list L30-163 through the conditional SSL_CERT_FILE block at L159-162; containers: L24; nindent context)
    - 20-PATTERNS.md §"charts/engram/templates/_helpers.tpl" — the exact define-block shape and the "copy every line L31-163 verbatim, only the indentation context changes" constraint
    - 20-RESEARCH.md Pattern 4 — Deployment render must be byte-identical before/after; verify with diff, not visual review
  </read_first>
  <action>
    First, capture the pre-refactor render for the no-op check into the EXACT scratch path `"${TMPDIR:-/tmp}/engram-dep-before.yaml"` (before editing anything): `helm template charts/engram --show-only templates/memory-mcp.yaml > "${TMPDIR:-/tmp}/engram-dep-before.yaml"`. Use that identical path string — the `<verify>` command diffs against it. Create `charts/engram/templates/_helpers.tpl` with a single `{{- define "engram.containerEnv" -}} ... {{- end -}}` block containing the CURRENT inline `env:` list from memory-mcp.yaml (every var from ENGRAM_LISTEN_ADDR through the Downward-API POD_*/OTEL_RESOURCE_ATTRIBUTES block and the conditional SSL_CERT_FILE var) copied VERBATIM (D-09) — do not reword, reorder, or reformat any line's content; only the surrounding indentation context differs. Then replace memory-mcp.yaml's inline env list with `env: {{- include "engram.containerEnv" . | nindent 12 }}` (preserving the Deployment's existing nindent of 12). Finally diff the post-refactor Deployment render against the scratch file at `"${TMPDIR:-/tmp}/engram-dep-before.yaml"` — it MUST be empty (no-op). No SPDX header (charts/** is outside .licenserc.yaml paths).
  </action>
  <verify>
    <automated>diff <(cat "${TMPDIR:-/tmp}/engram-dep-before.yaml") <(helm template charts/engram --show-only templates/memory-mcp.yaml) && helm lint charts/engram</automated>
  </verify>
  <acceptance_criteria>
    - The pre-refactor vs post-refactor `helm template --show-only templates/memory-mcp.yaml` diff is EMPTY (Deployment env byte-identical — D-09)
    - `rg -n 'define "engram.containerEnv"' charts/engram/templates/_helpers.tpl` matches
    - `rg -n 'include "engram.containerEnv"' charts/engram/templates/memory-mcp.yaml` matches (Deployment now consumes the named template)
    - `helm lint charts/engram` passes
  </acceptance_criteria>
  <done>The Deployment container env lives in _helpers.tpl as engram.containerEnv and is include-d by the Deployment with a byte-identical rendered result.</done>
</task>

<task type="auto">
  <name>Task 2: Add the batch/v1 summarize CronJob template + values cronjob block (D-07/D-08)</name>
  <files>charts/engram/templates/summarize-cronjob.yaml, charts/engram/values.yaml</files>
  <read_first>
    - charts/engram/templates/memory-mcp.yaml (image/imagePullPolicy construction; imagePullSecrets L22; caBundle volumeMounts/volumes L164-184 — copy these conventions verbatim)
    - charts/engram/templates/_helpers.tpl (engram.containerEnv from Task 1 — the CronJob include-s it at nindent 16)
    - charts/engram/values.yaml (existing memory.summarize block L95-99 — add a nested cronjob: block in the same commented-inline style)
    - cmd/engram/summarize.go (Use "summarize-missing" L34; requires --scope OR --all-scopes L36-38; --all-scopes flag L85)
    - 20-PATTERNS.md §"charts/engram/templates/summarize-cronjob.yaml" + §"charts/engram/values.yaml" — the CronJob skeleton and the values block shape
    - 20-RESEARCH.md Pitfall 3 — the CronJob MUST hardcode --all-scopes (an unattended sweep with no --scope errors out); do NOT add a --scope values knob this phase
  </read_first>
  <action>
    Create `charts/engram/templates/summarize-cronjob.yaml` gated entirely behind `{{- if .Values.memory.summarize.cronjob.enabled }}` (D-07: opt-in, off by default) — a `batch/v1` CronJob named memory-mcp-summarize whose `schedule`, `concurrencyPolicy`, `successfulJobsHistoryLimit`, and `failedJobsHistoryLimit` read from `.Values.memory.summarize.cronjob.*` and whose jobTemplate pod sets `restartPolicy` from values too (D-08: all policy knobs surfaced as overrides). The single container reuses the Deployment's `.Values.image.*` (repository/tag default AppVersion/pullPolicy) and imagePullSecrets/caBundle volume+volumeMount conventions verbatim from memory-mcp.yaml, sets `env: {{- include "engram.containerEnv" . | nindent 16 }}`, and passes `args: ["summarize-missing", "--all-scopes"]` (Pitfall 3 — hardcode --all-scopes; no --scope knob). Add the `cronjob:` block under `memory.summarize` in values.yaml (D-08 defaults): `enabled: false`, `schedule: "0 3 * * *"` (daily), `concurrencyPolicy: Forbid`, `restartPolicy: OnFailure`, `successfulJobsHistoryLimit: 3`, `failedJobsHistoryLimit: 1`, each with an inline comment. No SPDX header (charts/** outside license paths).
  </action>
  <verify>
    <automated>helm template charts/engram --set memory.summarize.cronjob.enabled=true --show-only templates/summarize-cronjob.yaml | rg -n 'batch/v1|memory-mcp-summarize|concurrencyPolicy: Forbid|summarize-missing|--all-scopes|schedule:' && helm lint charts/engram</automated>
  </verify>
  <acceptance_criteria>
    - `helm template charts/engram --set memory.summarize.cronjob.enabled=true --show-only templates/summarize-cronjob.yaml` renders a `batch/v1` CronJob containing `concurrencyPolicy: Forbid`, `restartPolicy: OnFailure`, `args` `summarize-missing`/`--all-scopes`, and the schedule
    - `helm template charts/engram --show-only templates/summarize-cronjob.yaml` (default values, cronjob disabled) renders empty output (no resource emitted — D-07)
    - `rg -n 'cronjob:' charts/engram/values.yaml` matches under the summarize block with enabled:false default
    - `helm lint charts/engram` passes
  </acceptance_criteria>
  <done>An opt-in batch/v1 CronJob runs `engram summarize-missing --all-scopes` reusing the Deployment image+env via engram.containerEnv; its policy knobs default per D-08 and are values-overridable.</done>
</task>

<task type="auto">
  <name>Task 3: Add a proportional chart:validate Taskfile target (grep/diff, no helm-unittest)</name>
  <files>Taskfile.yaml</files>
  <read_first>
    - Taskfile.yaml (`chart:lint` target L112-116 — `helm lint` + `helm template`; proto:lint L136 for the repo's inline-bash-in-Taskfile convention with grep + exit 1)
    - charts/engram/templates/_helpers.tpl (the `engram.containerEnv` block from Task 1 — the block this target pins against drift; assert its extent/checksum, D-09)
    - 20-RESEARCH.md §Validation Architecture "Wave 0 Gaps" — add a LIGHTWEIGHT validation (bash grep/diff over helm template), NOT a full helm-unittest plugin; keep it proportional to one chart
    - 20-VALIDATION.md Per-Task Verification Map (Plan D row) + Manual-Only Verifications (Deployment env byte-identical pre/post render diff stays manual)
  </read_first>
  <action>
    Add a `chart:validate` target to Taskfile.yaml using inline bash (mirroring proto:lint's inline grep+exit-1 convention — no new tooling dependency, no helm-unittest plugin). It must assert four things and exit non-zero on any failure: (a) the default render (`helm template charts/engram`) emits NO CronJob resource (disabled by default — D-07); (b) the enabled render (`helm template charts/engram --set memory.summarize.cronjob.enabled=true`) DOES emit a CronJob and contains `concurrencyPolicy: Forbid` and the daily schedule; (c) the enabled render is valid (`helm lint charts/engram` clean); (d) the `engram.containerEnv` named-template block in `charts/engram/templates/_helpers.tpl` has not drifted — extract the block bounded by the `define "engram.containerEnv"` line and its matching closing `end` (e.g. an `awk` range or `sed` between the two markers) and assert its shape against a constant recorded here at implementation time (a `sha256`/`shasum` checksum of the extracted block, or its exact line count via `wc -l`), so any later re-touch of that env block — which would silently break the D-09 byte-identical guarantee — fails this re-runnable target. Record the expected constant from the block created in Task 1; do NOT use a bare `== 0` negative grep. Keep the Deployment env byte-identical pre/post render diff as the documented manual git-diff sign-off (20-VALIDATION.md Manual-Only) — that one needs a pre/post render comparison and does not belong in a re-runnable target; assertion (d) is the complementary re-runnable structural drift guard. Wire nothing into CI beyond the existing chart job; this target is local/proportional validation.
  </action>
  <verify>
    <automated>task chart:validate</automated>
  </verify>
  <acceptance_criteria>
    - `task chart:validate` exits 0 with the current chart (disabled render has no CronJob; enabled render has one with Forbid + daily schedule; `engram.containerEnv` block matches its recorded checksum/line-count constant)
    - `rg -n 'chart:validate' Taskfile.yaml` matches; `rg -n 'helm-unittest' Taskfile.yaml` returns nothing (no plugin dependency added)
    - `rg -n 'engram.containerEnv' Taskfile.yaml` matches (the target pins the shared env block against drift — D-09)
    - The target fails (non-zero) if the CronJob is emitted while disabled OR missing while enabled, OR if the `engram.containerEnv` block's checksum/line-count no longer matches the recorded constant (all proven by a quick manual toggle/edit during execution)
  </acceptance_criteria>
  <done>A lightweight, re-runnable `task chart:validate` guards the CronJob's disabled-by-default and enabled-shape invariants AND pins the shared `engram.containerEnv` block against future drift (D-09), without adding a helm-unittest dependency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kubernetes scheduler -> summarize CronJob pod | Scheduled batch pod runs the same engram binary/image/env as the Deployment; no exposed network port (no Service/Ingress) |
| operator values.yaml -> CronJob spec | Operator-trusted schedule/policy knobs; not end-user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-04 | Elevation of Privilege (blast radius) | CronJob pod reuses the full Deployment env (incl. OIDC/UI secrets summarize-missing never reads) | low | accept | Accepted per D-09: reusing the full env via engram.containerEnv guarantees zero drift at the cost of the pod holding secrets it doesn't use — mirrors the Deployment's own existing secret-mount posture; no new secret is introduced. Tightening is a separate future hardening phase (RESEARCH §Security Domain) |
| T-20-05 | Denial of Service | overlapping summarize sweeps | low | mitigate | concurrencyPolicy: Forbid (D-08) prevents overlapping runs; restartPolicy: OnFailure + failedJobsHistoryLimit: 1 bound retry/history growth |
| T-20-SC | Tampering | Helm subchart / plugin installs | low | accept | No new Helm subchart, no helm-unittest plugin — validation is bash+grep over helm template; batch/v1 CronJob is a core Kubernetes kind, not a dependency. Package Legitimacy Gate N/A (RESEARCH §Package Legitimacy Audit) |

ASVS L1, block-on-high: no high-severity threat — nothing blocks.
</threat_model>

<verification>
- Deployment env render byte-identical pre/post _helpers.tpl factor-out (empty diff — D-09)
- `helm template charts/engram --set memory.summarize.cronjob.enabled=true` renders a valid batch/v1 CronJob (Forbid, daily schedule, --all-scopes); default render emits none
- `helm lint charts/engram` and `task chart:validate` pass
</verification>

<success_criteria>
- ROADMAP SC6: the chart ships summarize-missing as a batch/v1 CronJob reusing the Deployment image/env via a shared _helpers.tpl block
- D-07 (disabled by default) / D-08 (daily + Forbid + OnFailure + history limits, values-overridable) / D-09 (byte-identical Deployment env) all satisfied
- #269 closed
</success_criteria>

<output>
Create `.planning/phases/20-correctness-polish/20-04-SUMMARY.md` when done
</output>
