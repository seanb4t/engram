---
phase: 06-rule-capture-investigation-fix
plan: 03
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-02
files_modified:
  - .planning/phases/06-rule-capture-investigation-fix/06-DEMONSTRATION.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: false
requirements:
  - REQ-rule-capture-intervention
  - REQ-rule-curation-hygiene
user_setup: []

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Rule capture fires in a scenario where it previously did not: the sweep enumerates the already-stored normatively-phrased gotchas and proposes each one, inline, to the user (roadmap criterion 3)."
    - "Every rule that lands does so after an explicit user yes for that specific candidate, recorded per candidate — no batched approval, no inferred consent (criterion 4)."
    - "Every candidate the user declines produces a `rule-declined` record, and no source record is deleted without its own separate yes."
    - "The demonstration record cites what actually happened — per-candidate answers and resulting `short_id`s — not a summary verdict."
    - "All three phase requirements are closed against artifacts that exist, and the ROADMAP progress row reflects it."
  artifacts:
    - ".planning/phases/06-rule-capture-investigation-fix/06-DEMONSTRATION.md — the criterion-3 record"
    - ".planning/REQUIREMENTS.md — REQ-rule-capture-intervention and REQ-rule-curation-hygiene closed; traceability rows updated"
    - ".planning/ROADMAP.md — Phase 6 checkbox, plan checkboxes, and the progress-table row"
  key_links:
    - "The live sweep → `### One-time rule backfill sweep` in the shipped skill. The executor follows the shipped procedure as written; if the procedure is unclear or unfollowable in practice, that is a finding about 06-02's prose, not a reason to improvise."
    - "The demonstration record → roadmap criterion 3. It is the only artifact that shows rule capture firing where it previously did not; a record that asserts success without per-candidate detail does not satisfy the criterion."
  prohibitions:
    - statement: "No path stores a rule without explicit user instruction."
      status: required
      verification: "The demonstration record names, per candidate, the user's verbatim answer and the resulting `short_id`. A rule present in `list_rules` after this plan with no corresponding recorded yes is a breach and must be deleted and reported."
    - statement: "No path deletes a rule without explicit user instruction."
      status: required
      verification: "This plan proposes no rule deletion. Any `delete_memory` call against a `rule:*` scope during execution is out of scope and must be reported, not performed."
    - statement: "The intervention adds no Go dependency and changes no Go code."
      status: required
      verification: "`git diff --exit-code <phase-base> -- '*.go' go.mod go.sum` and `git diff --exit-code <phase-base> -- internal/ cmd/` across the whole phase, run at close."
    - statement: "No evadable prose gate is presented as an automated check."
      status: required
      verification: "This plan's automated gates are the phase-close command set and three fixed-heading existence checks, each labelled weak. The demonstration itself is manual by nature and is declared so."
---

<objective>
Prove the fix in the world rather than on the page: run the shipped backfill sweep once, live,
against this repo's spine, and let the user bless or decline each normatively-phrased gotcha it
surfaces. Then close the phase's requirements against what actually exists.

Purpose: roadmap criterion 3 asks that rule capture demonstrably fires in a scenario where it
previously did not. The three records named in D-03 are exactly that scenario — they were written as
constraints, filed as gotchas, and never proposed, because until 06-01 nothing told the agent to
notice.

Output: `06-DEMONSTRATION.md` recording the sweep candidate-by-candidate, the two remaining
requirements closed, and the ROADMAP row updated.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-rule-capture-investigation-fix/06-CONTEXT.md
@.planning/phases/06-rule-capture-investigation-fix/06-01-SUMMARY.md
@.planning/phases/06-rule-capture-investigation-fix/06-02-SUMMARY.md
</context>

<precondition>
The engram MCP server is reachable and authenticated for this session — `mcp__engram__list_rules`
over `rule:repo:github.com/seanb4t/engram` returns without 401/403. The sweep is a live-store
operation and cannot be simulated. On an auth failure, halt and tell the user to authenticate via
`/mcp` (engram → Authenticate); do not fabricate the demonstration from the repo files.
</precondition>

<artifacts_this_phase_produces>
| Artifact | Kind | Introduced by |
|----------|------|---------------|
| `### Proposing a rule` | subsection heading, `curating-memory/SKILL.md` | 06-01 Task 1 |
| Amended `description` frontmatter | skill load surface, `curating-memory/SKILL.md` | 06-01 Task 1 |
| Decline record (`category: decision`, tag `rule-declined`) | documented record shape | 06-01 Task 1 |
| Corrected `store_rule` prose | `docs-site/src/content/docs/reference/tools.md` | 06-01 Task 2 |
| Corrected `Rule tools` paragraph | `CLAUDE.md` | 06-01 Task 2 |
| `### Rule hygiene` | subsection heading, `curating-memory/SKILL.md` | 06-02 Task 1 |
| Rule correction table (refine / replace-in-place / retire / cannot-unshare) | decision table | 06-02 Task 1 |
| `### One-time rule backfill sweep` | subsection heading + named procedure | 06-02 Task 2 |
| Hygiene mirror in `store_rule` / `list_rules` / `delete_memory` | `docs-site/.../reference/tools.md` | 06-02 Task 3 |
| `06-DEMONSTRATION.md` | phase artifact (criterion-3 record) | 06-03 Task 2 |
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Run the sweep for real — the criterion-3 demonstration</name>

  <what-built>
    `### Proposing a rule` (trigger, inline protocol, decline record), `### Rule hygiene`
    (duplicates, contradictions, rot, correction table, user-blessed deletion), and
    `### One-time rule backfill sweep` (the run-once per-item procedure) — all shipped in
    `skill/engram/skills/curating-memory/SKILL.md`, mirrored into the tool reference and `CLAUDE.md`.
    Nothing has yet been exercised against the live store.
  </what-built>

  <how-to-verify>
    The executing agent runs the sweep exactly as `### One-time rule backfill sweep` specifies. Read
    that subsection from the shipped file first and follow it — do not work from this plan's summary
    of it. If the shipped procedure turns out to be ambiguous or unfollowable in practice, stop and
    report that: it is a finding about 06-02's prose and worth more than a completed sweep.

    **Before proposing anything**, capture the starting state so the demonstration has a baseline:
    call `list_rules` over `rule:repo:github.com/seanb4t/engram` and record every `short_id` and
    `summary` returned. The orchestrator confirmed three rules at planning time — `7smp8vy9hr`,
    `rvmts69cz1`, `0v4249kc9d`. If the live count differs, record what you actually see; the count
    is context, not a gate.

    Then run the sweep. The three candidates named in D-03 are known to be in the store and are the
    demonstration's core; the sweep may legitimately surface others, and those are proposed on the
    same terms:

    - `r3bjakymtz` — "NEVER run two GSD executor agents concurrently in the SAME working directory"
    - `z4mgz3a4ab` — "ALWAYS `git diff .planning/ROADMAP.md` and hand-correct before committing"
    - `478rhhmhb0` — gate store tests on `--- PASS:`, never exit status

    For each candidate the sweep surfaces, present the proposal as `### Proposing a rule` specifies:
    what you noticed and why it reads as normative, the exact one-line `summary` you would store,
    and the scope. Then ask, once, and wait.

    **The user's decision is the demonstration.** A decline is as successful an outcome as a bless —
    what criterion 3 asks is that the proposal *happened*, not that rules were created. Three
    declines would still prove the trigger fires. Do not steer toward yes.

    On a bless: call `store_rule`, cite the new `short_id`, then ask *separately* whether the source
    gotcha should be deleted as redundant or kept. Do not delete it on the strength of the bless.

    On a decline: store the decline record (`category: decision`, tag `rule-declined`,
    `source: user-said`) and leave the source record alone.

    Record, per candidate, as you go: the candidate `short_id`, the proposed summary, the user's
    answer in their own words, the resulting `short_id` (new rule or decline record), and whether the
    source record was deleted and on what separate answer.
  </how-to-verify>

  <resume-signal>
    The sweep completes when every candidate has an answer. Type "done" when the last candidate is
    answered, or "stop" at any point — a partial sweep is a valid outcome and gets recorded as one.
    If the shipped procedure was unclear at any step, say where; that feeds back into 06-02's prose
    rather than being papered over.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Record the demonstration and close the phase</name>

  <read_first>
    - The per-candidate notes taken during Task 1 — the demonstration record is built from those,
      not reconstructed afterwards.
    - `.planning/ROADMAP.md`, the `### Phase 6:` section and the progress table row
      `| 6. Rule Capture — Investigation & Fix | v0.12.x | 0/3 | Pending | — |`.
    - `.planning/REQUIREMENTS.md` lines 96-119 (the three `REQ-rule-*` entries) and lines 253-255
      (the traceability rows).
    - `.planning/STATE.md`'s standing operating notes, specifically the Phase 5 warning that
      `gsd-tools`' `roadmap.update-plan-progress` and `state.advance-plan` corrupted this repo's
      flat-ROADMAP / hand-maintained-STATE shape in prior phases. **Hand-edit both files and
      `git diff` them before committing.** This is not optional caution; it has already gone wrong
      once and required a repair commit.
    - `.planning/phases/06-rule-capture-investigation-fix/06-01-SUMMARY.md` and `06-02-SUMMARY.md`
      — the cold-read answers and the paper trace, both of which the demonstration record cites.
  </read_first>

  <files>.planning/phases/06-rule-capture-investigation-fix/06-DEMONSTRATION.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>

  <reversibility rating="costly">A rule blessed during Task 1 is mechanically reversible —
  `delete_memory` has no rule guard — but it is shared, normative, MUST-follow ground truth that
  every session reads until someone notices and the user blesses its removal, and no supersession
  history survives the delete. Reversible, not free.</reversibility>

  <action>
Write `.planning/phases/06-rule-capture-investigation-fix/06-DEMONSTRATION.md`, opening with the
Apache-2.0 SPDX comment header the other phase artifacts in this directory carry (`06-CONTEXT.md`
and `06-RESEARCH.md` both begin with it).

Structure it as: the starting state, the per-candidate table, and what it does and does not prove.

The starting state records the `list_rules` baseline captured before the sweep — every `short_id`
and `summary` in `rule:repo:github.com/seanb4t/engram` — so the delta is visible rather than
asserted.

The per-candidate table is the substance. One row per candidate the sweep surfaced, with: the
candidate's `short_id`, the proposed one-line summary, the user's answer quoted in their own words,
the outcome (`blessed` with the new rule's `short_id`, or `declined` with the decline record's
`short_id`), and the separate answer on the source record where a bless occurred. **A row that
records an outcome but not the user's own words does not satisfy criterion 4** — it is
indistinguishable from an agent that acted and reported afterwards.

Then state plainly what the demonstration establishes and what it does not. It establishes that the
trigger fires and that a proposal is made where none previously was — criterion 3. It does not
establish that the trigger is well-calibrated over time; that needs accept/decline rates the phase
has no data path for, and CONTEXT.md defers it explicitly. Say so rather than overclaiming.

If any candidate was declined, note that a decline is a successful demonstration of the same
mechanism and that the resulting `rule-declined` record is what keeps it from re-firing.

Then close the requirements. In `.planning/REQUIREMENTS.md`, flip REQ-rule-capture-intervention and
REQ-rule-curation-hygiene from `[ ]` to `[x]`, each with a one-line closure note naming the artifact
that satisfies it — `### Proposing a rule` plus the three-surface reconciliation for the
intervention, `### Rule hygiene` plus `### One-time rule backfill sweep` for the hygiene discipline.
Update the three traceability rows at lines 253-255 from `Pending` to `Complete`.
REQ-rule-capture-investigation was already closed in 06-01; leave its entry alone and only update its
traceability row.

In `.planning/ROADMAP.md`, update the `### Phase 6:` section's plan checkboxes and the progress
table row to `3/3`, `Complete`, and today's date. Hand-edit. Diff before committing.

Finally run the phase-close gate set and record every result in the SUMMARY. Since this phase should
have touched no Go, the Go-side gates are expected to be trivially green — **run them anyway, and
treat any Go diff as a signal the phase drifted out of scope**, not as an incidental change to wave
through.
  </action>

  <verify>
    <automated>task</automated>
    <automated>go vet ./...</automated>
    <automated>task license:check</automated>
    <automated>task chart:validate</automated>
    <automated>task proto:lint</automated>
    <automated>task proto:gen && git diff --exit-code -- gen/</automated>
    <automated>task ui:build && git diff --exit-code -- web/</automated>
    <automated>git diff --exit-code dc98ec0c -- go.mod go.sum</automated>
    <automated>git diff --exit-code dc98ec0c -- '*.go' internal/ cmd/</automated>
    <automated>rg -n '^### Proposing a rule$|^### Rule hygiene$|^### One-time rule backfill sweep$' skill/engram/skills/curating-memory/SKILL.md</automated>
    <automated>rg -n 'errRuleImmutable' internal/server/tools.go</automated>
  </verify>

  <acceptance_criteria>
    - `task`, `go vet ./...`, `task license:check`, `task chart:validate`, and `task proto:lint` all
      exit 0.
    - `task proto:gen` followed by `git diff --exit-code -- gen/` exits 0 (zero drift). Same for
      `task ui:build` and `web/`.
    - `git diff --exit-code dc98ec0c -- go.mod go.sum` exits 0. **This is the phase's strongest
      prohibition gate** and the only non-evadable proof that the intervention added no Go
      dependency. Substitute the actual phase-base commit if `dc98ec0c` is not the base at execution
      time — read it from `06-01-SUMMARY.md` rather than guessing.
    - `git diff --exit-code <phase-base> -- '*.go' internal/ cmd/` exits 0. A non-zero exit means a
      Go file changed during a phase whose entire deliverable is prose — stop and investigate before
      closing anything.
    - `rg -n '^### Proposing a rule$|^### Rule hygiene$|^### One-time rule backfill sweep$'
      skill/engram/skills/curating-memory/SKILL.md` returns exactly three lines. **Weak by
      construction**, as at every prior appearance: it proves the three subsections survived the
      phase and nothing about whether their content is correct.
    - `rg -n 'errRuleImmutable' internal/server/tools.go` returns three hits — the rule guards on
      `updateMemory`'s un-share path, `setVisibility`, and `supersedeMemory` are intact. Criterion
      4's "the gate is proven intact" rests on this plus the two `git diff` gates above.
    - **Manual, by reading `06-DEMONSTRATION.md`:** every candidate row carries the user's answer in
      their own words. A row without it does not satisfy criterion 4.
    - **Manual, by reading:** the record states what the demonstration does not prove (calibration
      over time), rather than overclaiming.
    - **Manual, by `git diff`:** `.planning/ROADMAP.md` and `.planning/REQUIREMENTS.md` show only the
      intended edits — checkbox flips, closure notes, traceability rows, and the progress row. Any
      structural churn elsewhere in `ROADMAP.md` means a tool rewrote the file and the diff must be
      discarded and redone by hand. This has happened before in this repo and required a repair
      commit.
    - **Manual, by calling `list_rules`:** every rule present in `rule:repo:github.com/seanb4t/engram`
      that was not in the pre-sweep baseline has a corresponding recorded user yes in
      `06-DEMONSTRATION.md`. Any rule without one is a breach of the gate — delete it and report it.
  </acceptance_criteria>

  <done>
    `06-DEMONSTRATION.md` records the sweep candidate-by-candidate with the user's own words, all
    three requirements are closed against artifacts that exist, the ROADMAP row reads `3/3 Complete`,
    and every phase-close gate is green with results recorded. Committed as
    `docs(06-03): record the rule-capture demonstration and close phase 6` with an explicit
    pathspec.
  </done>
</task>

</tasks>

<threat_model>
This plan is the only one that touches the live store, so it is the only one where a gate breach
leaves real state behind rather than bad prose. The security property is unchanged — the
user-blessed gate — but here it is being exercised rather than described.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executing agent → live `rule:repo:*` scope | The agent holds a working `store_rule` and an unguarded `delete_memory`. Only the checkpoint's consent loop stands between them and the normative ground truth every future session reads. |
| demonstration narrative → the record | The agent writes the record of its own compliance. A record that asserts consent without quoting it is unfalsifiable. |
| planning-doc edits → project history | `ROADMAP.md` is hand-maintained here and has been corrupted once by tooling; a bad write loses shipped history. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-06 | Spoofing (of consent) | the demonstration record | high | mitigate | Every candidate row must quote the user's own answer, and a post-sweep `list_rules` must reconcile against the pre-sweep baseline — any rule with no recorded yes is a breach, must be deleted and reported. The agent cannot satisfy the criteria by asserting success. |
| T-06-12 | Elevation of Privilege | the sweep's Bless branch, live | high | mitigate | Task 1 is a blocking checkpoint: the agent proposes and waits. Source-record deletion after a bless requires its own separate answer, stated in both the shipped procedure and this plan. |
| T-06-13 | Coercion | proposal framing during the live sweep | medium | mitigate | "The user's decision is the demonstration. A decline is as successful an outcome as a bless — do not steer toward yes." Criterion 3 is satisfied by the proposal happening, not by rules being created, so the agent has no incentive to press. |
| T-06-14 | Destruction of ground truth | any `delete_memory` against a `rule:*` scope | high | mitigate | This plan proposes no rule deletion at all. Any such call during execution is out of scope and must be reported rather than performed — stated as a `must_haves` prohibition. |
| T-06-15 | Tampering (of project history) | hand-edits to `ROADMAP.md` / `REQUIREMENTS.md` | medium | mitigate | Hand-edit only; `git diff` both files before committing; structural churn beyond the intended edits means a tool rewrote the file and the diff is discarded. Carried forward from the Phase 5 incident recorded in `STATE.md` (repair commit `e5e9ce4c`). |
| T-06-SC | Tampering | npm/pip/cargo installs | high | mitigate | No installs. Pinned by `git diff --exit-code <phase-base> -- go.mod go.sum` at phase close. |
</threat_model>

<verification>
Phase-close gate set, all recorded in the SUMMARY with their actual output:

- `task` — lint + full test suite.
- `go vet ./...`
- `task license:check`
- `task chart:validate`
- `task proto:lint`
- `task proto:gen` then `git diff --exit-code -- gen/` — zero drift.
- `task ui:build` then `git diff --exit-code -- web/` — zero drift.
- `git diff --exit-code <phase-base> -- go.mod go.sum` — zero diff.
- `git diff --exit-code <phase-base> -- '*.go' internal/ cmd/` — zero diff. Any hit is scope drift.
- `rg -n '^### Proposing a rule$|^### Rule hygiene$|^### One-time rule backfill sweep$'
  skill/engram/skills/curating-memory/SKILL.md` — three lines (weak: existence only).
- `rg -n 'errRuleImmutable' internal/server/tools.go` — three hits.
- Manual: the four items marked **Manual** in Task 2's acceptance criteria, including the live
  `list_rules` reconciliation against the pre-sweep baseline.
</verification>

<success_criteria>
- The sweep ran live and every candidate it surfaced received an inline proposal and a user answer.
- `06-DEMONSTRATION.md` records the baseline, every candidate, and the user's own words per
  candidate.
- Every rule added during the sweep reconciles to a recorded yes; nothing was stored or deleted
  unasked.
- All three requirements are closed and the ROADMAP progress row reads `3/3 Complete`.
- Every phase-close gate is green, with the two `git diff` gates proving the phase changed no Go and
  added no dependency.
</success_criteria>

<output>
Create `.planning/phases/06-rule-capture-investigation-fix/06-03-SUMMARY.md` when done, with a
Phase-Close Gate Results table carrying each gate's actual output — matching the format 05-03 used.
</output>
