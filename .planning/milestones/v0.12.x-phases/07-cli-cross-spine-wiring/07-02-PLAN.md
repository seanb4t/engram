---
phase: 07-cli-cross-spine-wiring
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - cmd/engram/client_list.go
  - cmd/engram/clienttest_test.go
  - cmd/engram/client_list_test.go
  - cmd/engram/client_common_test.go
autonomous: true
requirements: [D-00, D-01, D-02, D-04, D-05, D-06]
user_setup: []

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-01: `engram list --cross-spine` issues a ListMemories RPC whose request carries cross_spine=true."
    - "D-01: `engram list` with neither `--scope` nor `--cross-spine` exits 2 and issues zero RPCs."
    - "D-04: `engram list --scope repo:x --cross-spine` exits 2 and issues zero RPCs."
    - "D-02: listCmd calls the same validateScopeCrossSpine helper searchCmd calls — no second copy of the rule."
    - "D-05: a cross-spine text-mode `engram list` prints the count-based coverage footer after its existing total: line."
    - "D-06: `engram list` text output without `--cross-spine` is byte-identical to the pre-phase baseline, including the existing total: / next_page_token: footer."
    - "D-00: on listCmd, `--scope`'s Usage names `--cross-spine` and `--cross-spine`'s Usage names `--scope`."
  artifacts:
    - "cmd/engram/client_list.go declares the listCrossSpine package var and a --cross-spine BoolVar"
    - "cmd/engram/client_list.go calls validateScopeCrossSpine and renderCoverageFooter"
    - "cmd/engram/client_list_test.go contains TestClientListCrossSpineEndToEnd, TestClientListMissingScopeIsUsageErrorBeforeDialing, TestClientListScopeWithCrossSpineIsUsageErrorBeforeDialing, TestClientListFooterUnchangedWithoutCrossSpine"
    - "cmd/engram/client_common_test.go's TestScopeCrossSpineFlagsNameEachOther table covers listCmd"
  key_links:
    - "listCmd.RunE calls validateScopeCrossSpine before resolveOutputFormat/clientFromFlags — the guard must fire before any dialing."
    - "ListMemoriesRequest.CrossSpine is populated from listCrossSpine in the request literal."
    - "renderCoverageFooter is appended after the existing total: line, never interleaved with it and never modifying it."
    - "resetClientFlags zeroes listCrossSpine alongside searchCrossSpine."
---

<objective>
Expand the proven tracer slice to the second recall command. `engram list` gains the same
`--cross-spine` flag, the same shared guard, the same request field, and the same coverage footer —
appended after the footer it already prints.

Purpose: after this plan both recall verbs reach cross-spine, which is the whole capability the
v0.12.x milestone audit found unreachable from the CLI. Plan 07-03 then makes it discoverable.

Output: `--cross-spine` on `listCmd` with D-00 help text on both it and `--scope`, the guard and
footer call sites, and `engram list`'s guard/footer/baseline test set.

**Traceability note.** No `REQ-*` IDs are mapped to this phase; CONTEXT.md's `D-00` through `D-09`
decision IDs are the traceability keys. No `07-SPEC.md` exists and the spec-less probe fallback was
skipped visibly, not silently, so no probe-derived edge predicates were generated this run — the
`must_haves` above come from CONTEXT.md's locked decisions and 07-VALIDATION.md's criterion map.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-cli-cross-spine-wiring/07-CONTEXT.md
@.planning/phases/07-cli-cross-spine-wiring/07-PATTERNS.md
@.planning/phases/07-cli-cross-spine-wiring/07-VALIDATION.md
@.planning/phases/07-cli-cross-spine-wiring/07-01-SUMMARY.md
</context>

<concurrency_contract>
This plan is wave 2 and shares a working directory with the rest of the phase. Every commit must
name its files with an explicit pathspec (`git add <paths>` then `git commit`). A bare `git commit -m`
sweeps whatever a sibling agent left staged, because the git index is shared process-wide — repo rule
`n6m4as49mr`, memory `r3bjakymtz`.

`cmd/engram/clienttest_test.go` is touched by both plan 07-01 and this plan, which is why this plan
is wave 2 and not parallel with 07-01.
</concurrency_contract>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire --cross-spine through engram list</name>
  <files>cmd/engram/client_list.go, cmd/engram/clienttest_test.go, cmd/engram/client_common_test.go</files>
  <read_first>
    - `cmd/engram/client_list.go` (whole file, 97 lines) — var block :16-28, `RunE` :35-79 with the request literal :44-56 and the existing text branch and footer :64-77, `init()` :82-96
    - `cmd/engram/client_list.go:86` and `:94` — the bidirectional flag-naming precedent between `--offset` and `--cursor-mode`, whose wording shape D-00 copies
    - `cmd/engram/client_search.go` as landed by plan 07-01 — the guard call site, request field, and footer call this task mirrors
    - `cmd/engram/client_common.go` as landed by plan 07-01 — `validateScopeCrossSpine` and `renderCoverageFooter` signatures and doc comments
    - `cmd/engram/clienttest_test.go:99-129` — `resetClientFlags`, already carrying the `searchCrossSpine` reset from 07-01
    - `.planning/phases/07-cli-cross-spine-wiring/07-PATTERNS.md` section for `cmd/engram/client_list.go`
  </read_first>
  <behavior>
    - `engram list --server U --cross-spine --output text` — the stub receives one ListMemories request with `CrossSpine` true and an empty `Scope`, exit 0, and stdout reads table, then the existing total line, then the coverage footer line.
    - `engram list --server U --scope repo:x --output text` — stdout is byte-identical to the pre-change baseline, including the existing total line and its next_page_token variant.
    - `engram list --server U` with no scope and no cross-spine — exit 2, zero ListMemories calls.
    - `engram list --server U --scope repo:x --cross-spine` — exit 2, zero ListMemories calls.
  </behavior>
  <action>
    Mirror plan 07-01's proven slice onto the second command. Reuse both helpers exactly; write no
    second copy of the rule and no second footer formatter — D-02's whole point is that one helper
    serves both commands so a third command cannot forget the guard.

    **`cmd/engram/client_list.go`.** Add `listCrossSpine bool` to the var block (:16-28). In `init()`
    (:82-96) add a `BoolVar` binding it to a `cross-spine` flag defaulting to false, and rewrite the
    Usage string on the existing scope `StringVar` (:84), which today reads only as a bare filter
    description. Use the same two Usage strings plan 07-01 wrote for `searchCmd`, verbatim: the
    cross-spine flag's Usage says it spans every scope you can read and is mutually exclusive with the
    scope flag; the scope flag's Usage says it limits recall to one scope, is required unless the
    cross-spine flag is set, and is mutually exclusive with it. Identical strings across both commands
    are what makes the self-describe catalog coherent to an agent reading it once (D-00, D-07).

    In `RunE`, call `validateScopeCrossSpine(listScope, listCrossSpine)` and return its error directly.
    Place the call at the very top of `RunE`, before `resolveOutputFormat` (:36) and therefore before
    `clientFromFlags` — unlike `searchCmd` there is no pre-existing guard here to sequence after, so
    this is the first client-side pre-flight check `listCmd` has ever had. Add a `CrossSpine` field
    populated from `listCrossSpine` to the `ListMemoriesRequest` literal (:44-56) without reordering
    existing fields.

    In the text branch (:64-77), leave the existing total and next_page_token footer block
    (:68-75) exactly as it is — do not restructure it, do not merge the new line into it, do not
    change its spacing. Append only: after that block computes its error, call
    `renderCoverageFooter(cmd.OutOrStdout(), listCrossSpine, resp.Msg.GetSearchedScopes(), resp.Msg.GetScopesTruncated())`
    so text output reads table, then total footer, then coverage footer, as two separate lines.
    Preserve the existing block's error before returning: if the total line failed to write, return
    that error rather than shadowing it with the footer's. Rendering the coverage footer as its own
    line rather than extending the total line is the Claude's-Discretion call CONTEXT.md left open,
    chosen because append-only is what makes D-06's byte-identical guarantee structural rather than a
    property someone has to re-verify by eye.

    Leave the JSON branch (:78) untouched — `renderJSON` already emits both provenance fields.

    **`cmd/engram/clienttest_test.go`.** Add a reset of `listCrossSpine` to false in
    `resetClientFlags`' cleanup body, beside the `searchCrossSpine` reset plan 07-01 added. Without
    it, a true value leaking out of this plan's end-to-end test makes every later scope-only list
    invocation in the binary exit 2 against the new guard.

    **`cmd/engram/client_common_test.go`.** Add `listCmd` as a second row to
    `TestScopeCrossSpineFlagsNameEachOther`'s table and raise its count assertion from one command to
    two, so the D-00 bidirectional-naming contract is now asserted on both commands.

    No change is needed to any existing `client_list_test.go` invocation: all eight already pass an
    explicit scope, verified during planning. This is the opposite of `client_search_test.go`'s
    situation and is why this plan carries no migration task.
  </action>
  <verify>
    <automated>go test ./cmd/engram/... -run 'TestClientList|TestScopeCrossSpineFlagsNameEachOther' -v 2>&1 | rg '^--- (PASS|FAIL|SKIP)'; go test ./cmd/engram/... 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd/engram/...` exits 0 — every pre-existing list test still passes unmodified, proving the guard did not regress the scope-confined path.
    - `rg -c 'listCrossSpine' cmd/engram/client_list.go` returns at least 3 (var declaration, flag binding, request field).
    - `rg -c 'validateScopeCrossSpine' cmd/engram/client_list.go` returns 1.
    - `rg -c 'renderCoverageFooter' cmd/engram/client_list.go` returns 1.
    - `rg -c 'listCrossSpine' cmd/engram/clienttest_test.go` returns 1.
    - `rg -c 'func validateScopeCrossSpine\(' cmd/engram/` returns 1 — one definition across the package, confirming D-02's single helper rather than a per-command copy.
    - `go test ./cmd/engram/... -run TestScopeCrossSpineFlagsNameEachOther -v 2>&1 | rg -c '^--- PASS'` returns 1, with the table now covering two commands.
    - `go test ./cmd/engram/... -run TestClientFilesImportBoundary -v 2>&1 | rg -c '^--- PASS'` returns 1.
    - `go build ./...` and `go vet ./...` both exit 0.
  </acceptance_criteria>
  <done>
    `engram list` reaches cross-spine with the same guard and the same footer as `engram search`, and
    both commands' help text names the sibling flag. Stage with an explicit pathspec naming
    `cmd/engram/client_list.go`, `cmd/engram/clienttest_test.go`, and
    `cmd/engram/client_common_test.go`; commit as
    `feat(07-02): wire --cross-spine through engram list`.
  </done>
  <reversibility rating="reversible">
    Local to `cmd/engram`; no proto, wire, or server change. The footer is appended after an untouched
    existing block, so reverting restores the prior output exactly.
  </reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove the list guard fires before dialing and the existing footer is untouched</name>
  <files>cmd/engram/client_list_test.go</files>
  <read_first>
    - `cmd/engram/client_list_test.go` (whole file) — especially `TestClientListMissingServerURLIsUsageError` (:206-225) for the before-dialing assertion shape, `TestClientListTextOutput` (:250-270) for the text-mode stdout assertion shape, and `TestClientListCursorModeReachesRequest` (:139-160) for the captured-request assertion shape
    - `cmd/engram/client_list.go:64-77` as landed by Task 1 — the two-line footer sequence under test
    - `cmd/engram/clienttest_test.go:29-97` — `stubEngramService`'s `listCalls` counter and `listFn` hook, and `startStubServer`
    - `cmd/engram/client_search_test.go` as landed by plan 07-01 — the four search-side tests this task mirrors for list
  </read_first>
  <behavior>
    - Cross-spine list: captured request has `CrossSpine` true and empty `Scope`; `listCalls` is 1; stdout carries the table, the existing total line, and a coverage footer line reporting the count of scopes the stub reported, with no scope name present.
    - Missing scope: exit 2 via `ExitCode()`, `listCalls` is 0.
    - Scope plus cross-spine: exit 2 via `ExitCode()`, `listCalls` is 0.
    - Scope-confined list with provenance fields populated on the stub response: stdout equals the pre-phase baseline exactly — table plus the single total line — with no third line.
  </behavior>
  <action>
    Add four tests to `cmd/engram/client_list_test.go`, mirroring the search-side set plan 07-01
    landed so the two commands are held to the same evidence standard.

    `TestClientListCrossSpineEndToEnd` — start the stub with a `listFn` that captures the received
    request and returns a `ListMemoriesResponse` carrying two memories, a non-zero `Total`, an empty
    `NextPageToken`, a `SearchedScopes` slice of three entries, and `ScopesTruncated` false. Run the
    list verb with a server URL, the cross-spine flag, and text output. Assert exit 0, that the
    captured request has `CrossSpine` true and an empty `Scope`, that `svc.listCalls` is 1, that
    stdout contains both the existing total line and a coverage footer reporting the count 3, that the
    coverage line appears after the total line, and that none of the three stub scope names appears
    anywhere in stdout — the D-05 count-not-names assertion.

    `TestClientListMissingScopeIsUsageErrorBeforeDialing` — run the list verb with only a server URL.
    Assert a non-nil error whose `ExitCode()` reached through `errors.As` equals `exitUsage`, and
    assert `svc.listCalls` is zero. The stub is started but never reached, exactly as
    `TestClientListMissingServerURLIsUsageError` (:206-225) arranges it.

    `TestClientListScopeWithCrossSpineIsUsageErrorBeforeDialing` — same shape, run with both a scope
    and the cross-spine flag. This is D-04's rejection, the row where the client is deliberately
    stricter than the server.

    `TestClientListFooterUnchangedWithoutCrossSpine` — D-06's measured baseline for list. Run the list
    verb with a scope and text output against a stub whose response populates `SearchedScopes` with
    entries and sets `ScopesTruncated` true. Assert stdout equals exactly the table plus the single
    pre-existing total line, and that it carries no third line. Populating the provenance fields on
    the stub response is what makes the test meaningful: it proves the footer is gated on the caller's
    own flag rather than on what the server returned, so the byte-identical guarantee holds even
    against a server that reports coverage on every response.

    Run both baseline tests together with their search-side counterparts at the end, since D-06's
    claim spans both commands.
  </action>
  <verify>
    <automated>go test ./cmd/engram/... -run 'TestClientListCrossSpineEndToEnd|TestClientListMissingScopeIsUsageErrorBeforeDialing|TestClientListScopeWithCrossSpineIsUsageErrorBeforeDialing|TestClientListFooterUnchangedWithoutCrossSpine|TestClientSearchNoFooterWithoutCrossSpine' -v 2>&1 | rg '^--- (PASS|FAIL|SKIP)'</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints exactly five `--- PASS` lines and no `--- FAIL` or `--- SKIP` line.
    - `rg -c '^func TestClientList(CrossSpineEndToEnd|MissingScopeIsUsageErrorBeforeDialing|ScopeWithCrossSpineIsUsageErrorBeforeDialing|FooterUnchangedWithoutCrossSpine)' cmd/engram/client_list_test.go` returns 4.
    - `go test ./cmd/engram/... 2>&1 | tail -3` exits 0.
    - `task` (lint plus the full repo test suite) exits 0.
    - `git diff --exit-code b4544d47 -- go.mod go.sum` exits 0 — zero new dependencies across the phase so far.
    - `git diff --name-only b4544d47 -- internal/ | wc -l` returns 1 — still only `internal/server/tools.go` under `internal/`, the sole authorized exception.
  </acceptance_criteria>
  <done>
    Both recall verbs now carry identical, independently-proven evidence: the guard fires before
    dialing on both rejected rows, the cross-spine field reaches the wire, the coverage footer prints
    only on cross-spine calls, and non-cross-spine output is unchanged. Stage with an explicit
    pathspec naming `cmd/engram/client_list_test.go`; commit as
    `test(07-02): pin the list guard, footer, and non-cross-spine baseline`.
  </done>
</task>

</tasks>

<threat_model>
Same calibration as plan 07-01: this plan adds no new threat class, it applies the identical
client-side guard and stdout footer to a second command against the same already-authenticated,
already-authorized read path. Phase 3's server-side authz analysis is not re-opened.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell → `engram` CLI process | Flag values are operator-supplied; the guard reads them first |
| `engram` CLI → remote Connect server | Already authenticated and authorized server-side; this plan changes one request field and reads two response fields |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-04 | Tampering | `listCmd.RunE` guard call site (`cmd/engram/client_list.go`) | low | mitigate | A second command wiring its own copy of the rule would let the two drift apart, and only one copy is pinned by the parity test. Task 1 calls the single shared `validateScopeCrossSpine`; the acceptance criterion `rg -c 'func validateScopeCrossSpine\(' cmd/engram/` returning 1 proves no second definition exists. |
| T-07-05 | Information Disclosure | `renderCoverageFooter` call site in `listCmd` | low | mitigate | Inherits plan 07-01's count-based footer with no scope names rendered. `TestClientListCrossSpineEndToEnd` asserts none of the stub's three scope names appears in stdout. |
| T-07-06 | Denial of Service (self-inflicted) | `listCmd` guard ordering | low | mitigate | A guard placed after `clientFromFlags` would dial before rejecting, wasting a round trip and leaking the request to the server on a call the client already knows is malformed. Task 1 places it at the top of `RunE`; `TestClientListMissingScopeIsUsageErrorBeforeDialing` and `TestClientListScopeWithCrossSpineIsUsageErrorBeforeDialing` both assert a zero call counter. |
| T-07-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no packages and adds no Go module dependency; the `go.mod`/`go.sum` zero-drift gate in Task 2's acceptance criteria enforces it rather than asserting it. |
</threat_model>

<verification>
Run after every task: `go test ./cmd/engram/...` (about 10 seconds; pure in-process, no Qdrant
dependency — `ENGRAM_REQUIRE_QDRANT`'s silent-skip trap from memory `478rhhmhb0` does not reach this
package).

Run once at plan close:
- `task` — lint plus the full repo test suite
- `go vet ./...`
- `task license:check`
- `git diff --exit-code b4544d47 -- go.mod go.sum`
- `git diff --name-only b4544d47 -- internal/` names exactly `internal/server/tools.go`
- `go test ./cmd/engram/... -run TestCatalogEnumeratesEveryFlag -v 2>&1 | rg '^--- (PASS|FAIL|SKIP)'` — the pre-existing catalog gate now covers `--cross-spine` on both commands with no catalog code written, which is D-07's evidence and plan 07-03's starting point.
</verification>

<success_criteria>
- `engram list --cross-spine` sends `cross_spine` true and prints the coverage footer after its existing total line.
- `engram list` with neither scope nor cross-spine exits 2 with zero RPCs issued.
- `engram list --scope repo:x --cross-spine` exits 2 with zero RPCs issued.
- `engram list` text output without the cross-spine flag is byte-identical to the pre-phase baseline, existing footer included.
- One `validateScopeCrossSpine` definition serves both commands.
- Both commands' `--scope` and `--cross-spine` Usage strings name each other, asserted by one table test covering two commands.
- `task` is green; `go.mod` and `go.sum` unchanged; `internal/server/tools.go` still the only file touched under `internal/`.
</success_criteria>

<output>
Create `.planning/phases/07-cli-cross-spine-wiring/07-02-SUMMARY.md` when done. Record the exact
coverage-footer line as rendered for both commands, so plan 07-03's docs-site task documents what
actually prints rather than what was planned.
</output>
