---
phase: 05-operator-config-reindex-correctness
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
files_modified:
  - docs-site/src/content/docs/guides/configure.md
  - docs-site/src/content/docs/guides/reindex.md
  - docs-site/src/content/docs/guides/upgrade.md
autonomous: true
requirements:
  - REQ-per-lane-api-key
  - REQ-reindex-stale-repair

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "configure.md no longer asserts that the API key is shared across both lanes and that there is no separate key for the chat base URL — the statement this phase makes false is CORRECTED, not merely supplemented and not deleted (D-06)."
    - "The residual credential-exposure risk the old shared-key callout explained survives the correction in its new form: setting the chat base URL without the chat key still sends the embedder's key to the new gateway, and the corrected prose says so (D-06, threat T-05-03)."
    - "An operator who ran an unpatched --resume can find, in the reindex guide, the documented path that heals the records it skipped: re-run engram reindex --resume, sized first with --dry-run --resume (D-13, D-14, D-16, REQ-reindex-stale-repair)."
    - "The repair section states the MECHANISM — the source collection is re-scrolled fresh on every invocation, the target does not self-correct — so the D-15 limit follows from it rather than being an unexplained caveat."
    - "The repair section states D-15's limit plainly: if the source collection was deleted after cutover the correct tags are unrecoverable, and re-embedding from the live target payload would produce a vector consistent with the STALE tags — silently wrong while looking healed. No command is offered for this case."
    - "guides/upgrade.md's existing v0.12.0 section gains two numbered subsections continuing its numbering, not a new top-level heading (D-06, D-16)."
    - "PHASE LEDGER — EDGE 1: a tags-differ/content-same record re-embeds (owned and proven by 05-02 Task 1, step 1)."
    - "PHASE LEDGER — EDGE 2: a content-and-tags-both-same record still SKIPS, the paired positive control (05-02 Task 1, step 2, with its own RED reading)."
    - "PHASE LEDGER — EDGE 3: same tag elements in a different order skip — order-independent comparison (05-02 Task 1, step 3)."
    - "PHASE LEDGER — EDGE 4: nil tags and an empty tag slice compare equal, so untagged records do not re-embed forever (05-02 Task 1, step 4, plus the tagsEqual table test)."
    - "PHASE LEDGER — EDGE 5: --dry-run still writes nothing and still creates no target collection after D-14 (05-02 Task 2, and TestReindexDryRunWritesNothing passing unmodified)."
    - "PHASE LEDGER — EDGE 6: with ENGRAM_OPENAI_CHAT_API_KEY unset, the argument reaching summarize.New is byte-identical to today's (05-01 Task 1, fallback subtest)."
    - "PHASE LEDGER — EDGE 7: go.mod and go.sum are unchanged from the phase base commit dc98ec0c across the whole phase."
  artifacts:
    - "docs-site/src/content/docs/guides/configure.md :: the ENGRAM_OPENAI_CHAT_API_KEY environment-table row and the corrected per-lane-credential callout replacing the shared-key callout"
    - "docs-site/src/content/docs/guides/reindex.md :: the `## Repairing a pre-patch resume` section, plus the corrected skip-test description in `## Resuming an interrupted run` and the --dry-run --resume note in `## Flags`/`## Output`"
    - "docs-site/src/content/docs/guides/upgrade.md :: two new numbered subsections under the existing `## v0.12.0` heading"
  key_links:
    - "configure.md's environment table row -> the corrected callout -> guides/upgrade.md's entry. All three must name the same env var and the same inherit-when-empty semantic, or an operator reading one and acting on another gets a different mental model."
    - "reindex.md's repair section -> the D-15 limit. The limit is a CONSEQUENCE of the stated mechanism (source re-scrolled fresh), not a floating caveat. If the mechanism is omitted, an operator who deleted the source runs --resume expecting a heal and gets silent skips."
    - "reindex.md's existing `## Resuming an interrupted run` prose -> the patched predicate. It currently says the skip test is content equality and that equal content re-embeds to an equal vector. That is now false and is the same class of stale-prose defect as D-06's."
  prohibitions:
    - statement: "The ChatAPIKey value MUST NOT appear in any log line, error string, or slog attribute."
      status: enforced
      verification: "Phase-close gate in this plan's <verification>: no non-test Go line referencing the field also references slog., fmt.Errorf, Printf, or Println. Owned by 05-01 Task 1; re-run here at phase close."
    - statement: "The embedder lane MUST NOT change credential — it keeps using cfg.OpenAI.APIKey regardless of the new key."
      status: enforced
      verification: "Phase-close gate: embedderFromConfig still passes cfg.OpenAI.APIKey to embed.New, and TestSummarizerFromConfigChatAPIKey's first subtest asserts the embeddings request carries the shared key while the chat request carries the chat key."
    - statement: "Tag ordering MUST NOT be normalized at write time — explicitly deferred in CONTEXT.md and out of scope this phase."
      status: enforced
      verification: "Phase-close gate: `git diff dc98ec0c -- internal/store/store.go` yields no hunk header naming `func payload(` or `func EmbedText(`, and the body of `func payload(m Memory) map[string]any` contains no ordering call. The docs must describe the reordering residual as intentional, never promise normalization."
    - statement: "internal/authz and internal/store authorization behavior MUST NOT change."
      status: enforced
      verification: "Phase-close gate: `internal/authz/` appears in no plan's files_modified across the phase; `go test ./internal/authz/... -count=1` and the full suite stay green."
    - statement: "No new one-time reconciliation command is added for stale-tag repair (D-13). The docs MUST NOT reference a command that does not exist."
      status: enforced
      verification: "`cmd/engram/` gains no new file this phase, and `engram --help` lists exactly the operator commands it listed before. The repair section names only `engram reindex --resume` and `engram reindex --dry-run --resume`."
---

<objective>
Make both fixes findable and correctly understood by the operator who needs them, and run the phase's
closing gates.

This plan is expansion work built on the two tracer slices that landed in wave 1 — it adds no new
code path, so it carries no tracer of its own. Its whole job is that a shipped behavior nobody can
find, and a shipped behavior described by prose that is now false, are both indistinguishable from
not having shipped.

Three pieces of prose in the tree become false the moment wave 1 lands:

1. `configure.md`'s shared-key callout asserts there is no separate key for the chat base URL. D-06
   is explicit that this must be **corrected**, not merely supplemented. It is equally explicit,
   through RESEARCH's security review, that the callout is explaining a **real residual risk** — a key
   intended for gateway A silently reaching gateway B when the chat base URL is set but the chat key
   is not. That risk still exists after the split; it is now an opt-out rather than a hard constraint.
   Correct the prose; do not simply delete the warning.
2. `reindex.md`'s resume section says the skip test is content equality and that equal content
   re-embeds to an equal vector. Both statements are now false in the same way.
3. `upgrade.md`'s v0.12.0 section describes three changes and stops. It gains two more.

The repair path itself (D-13) is documentation, not code: `Reindex` writes vector and payload in one
upsert from one source read and re-scrolls the source fresh on every invocation, so a plain re-run of
the patched `--resume` heals every record the buggy predicate skipped. What makes that a usable
operator tool rather than a hopeful instruction is D-14's `--dry-run --resume` sizing, which landed in
plan 05-02. The guide must state the mechanism, because D-15's limit is a direct consequence of it:
the correct tags live only in the source, so if the source was deleted after cutover they are
unrecoverable, and re-embedding from the live target payload would produce a vector consistent with
the stale tags — silently wrong while looking healed.

Purpose: close the discoverability half of REQ-per-lane-api-key and the whole of
REQ-reindex-stale-repair.
Output: three corrected and extended guides, and a green phase-close gate run.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-operator-config-reindex-correctness/05-CONTEXT.md
@.planning/phases/05-operator-config-reindex-correctness/05-RESEARCH.md
@.planning/phases/05-operator-config-reindex-correctness/05-01-SUMMARY.md
@.planning/phases/05-operator-config-reindex-correctness/05-02-SUMMARY.md
</context>

## Concurrency contract

This plan runs alone in wave 2, after both wave-1 plans have landed, so the shared-index hazard is
gone. Explicit-pathspec commits are still preferred for consistency with the rest of the phase, and
the phase-close gate below checks that no wave-1 file was accidentally swept into a docs commit.

Both wave-1 SUMMARYs are referenced above and are genuinely needed here: this plan's prose must name
the exact identifiers those plans shipped (the env var, the Helm value path, the counter names, the
summary-line wording), and RESEARCH warns that planner premises in this repo get falsified during
execution. Read what actually landed before describing it.

## Artifacts this phase produces

See the table in `05-01-PLAN.md` — it is the single phase-wide list. This plan owns the three
`docs-site` rows: the `configure.md` table row and corrected callout, the `reindex.md`
`## Repairing a pre-patch resume` section, and the two new numbered subsections in `upgrade.md`.

<tasks>

<task type="auto">
  <name>Task 1: Correct configure.md's shared-key assertion and document the new per-lane credential</name>
  <precondition>`05-01-SUMMARY.md` exists and records the shipped env var name and Helm value path — this task's prose must name what actually landed, not what was planned.</precondition>
  <files>docs-site/src/content/docs/guides/configure.md</files>
  <read_first>
    - `docs-site/src/content/docs/guides/configure.md:28-40` — the environment-variable table, specifically the `ENGRAM_OPENAI_API_KEY` row (line 32) and the `ENGRAM_OPENAI_CHAT_BASE_URL` row (line 34). The chat-base-URL row's structure — what the setting does, what empty means, what is validated — is the template for the new row. Note it ends by pointing at the Auto-summary section "for the URL-shape rule and the shared-API-key constraint"; that cross-reference becomes stale.
    - `docs-site/src/content/docs/guides/configure.md:52-63` — the Embedder section's split-deployment paragraph, which sets up the local-embedder-plus-hosted-chat scenario the new key completes.
    - `docs-site/src/content/docs/guides/configure.md:64-73` — the shared-key callout in full. Read every sentence: the assertion to correct, the local-servers-ignore-the-header reassurance, the "your embedding API key travels to that host too" warning, and the closing "not supported this milestone" statement. The warning is the part that survives; the constraint statements are the parts that become false.
    - `docs-site/src/content/docs/guides/configure.md:86-95` — the Auto-summary section, including the cross-reference back to the Embedder section.
    - `.planning/phases/05-operator-config-reindex-correctness/05-01-SUMMARY.md` — the shipped env var name, the Helm value path, and whether any premise check changed the plan.
  </read_first>
  <action>
Add an environment-variable table row for `ENGRAM_OPENAI_CHAT_API_KEY`, placed directly after the
`ENGRAM_OPENAI_CHAT_BASE_URL` row so the two chat-lane settings sit together. Mirror that row's
structure: no flag, empty default, and a description stating that it is the API key for the
chat/summarize lane only, that the embedder always uses `ENGRAM_OPENAI_API_KEY` regardless, that
empty means the summarizer **inherits** `ENGRAM_OPENAI_API_KEY`, and — mirroring the base-URL row's
validation note but inverted — that it is never validated at startup, because an API key has no
verifiable shape and empty is meaningful, so a wrong key fails at the provider rather than at boot
(D-05).

Rewrite the shared-key callout. Three things must be true of the result:

1. **The false statements are gone.** Three claims in the current callout have been falsified by this
   phase and must not survive in any form: its opening assertion about a single credential covering
   both lanes, its statement that the chat base URL has no credential of its own, and its closing
   statement about per-lane credentials being unavailable in this release cycle. Replace all three
   with the actual semantic: each lane can carry its own credential, and the chat lane falls back to
   the shared one when its own is unset. The verify gates for this task are negative greps on the
   exact phrasing currently in the file — read the file, then remove those sentences.
2. **The residual risk survives.** The callout's real content — your embedding API key travels to the
   chat host too — is still accurate whenever an operator points the chat base URL at a different
   gateway and leaves the chat key unset. State it as the default-inherit consequence it now is: an
   opt-out, not a hard constraint. Name the fix in the same breath: set `ENGRAM_OPENAI_CHAT_API_KEY`.
   Do not delete this warning; an operator who upgrades and only skims will hit exactly this.
3. **The reassurance stays scoped.** The observation that local embedding servers ignore an
   `Authorization` header they do not expect is still true and still useful for the local-embedder-
   plus-hosted-chat split. Keep it, attached to the case it actually covers.

Retitle the callout so its heading no longer announces a shared key — it now describes how the two
lanes' credentials relate.

Fix the two now-stale cross-references: the `ENGRAM_OPENAI_CHAT_BASE_URL` table row's pointer to the
"shared-API-key constraint" and the Auto-summary section's pointer back to the Embedder section. Both
should name the per-lane credential behavior instead.

Mention the Helm surface once, naming the exact value path plan 05-01 shipped, so a chart user is not
left to guess. Take the path from `05-01-SUMMARY.md` rather than from this plan.

Commit: `docs(05-03): per-lane chat credential in the configuration guide` with an explicit pathspec
limited to `docs-site/src/content/docs/guides/configure.md`.
  </action>
  <verify>
    <automated>rg -q 'ENGRAM_OPENAI_CHAT_API_KEY' docs-site/src/content/docs/guides/configure.md</automated>
    <automated>! rg -qi 'no separate key for' docs-site/src/content/docs/guides/configure.md</automated>
    <automated>! rg -qi 'not supported this milestone' docs-site/src/content/docs/guides/configure.md</automated>
    <automated>! rg -qi 'key is shared across both lanes' docs-site/src/content/docs/guides/configure.md</automated>
    <automated>rg -qi 'inherit' docs-site/src/content/docs/guides/configure.md</automated>
    <automated>test "$(rg -c 'ENGRAM_OPENAI_CHAT_API_KEY' docs-site/src/content/docs/guides/configure.md)" -ge "2"</automated>
    <automated>rg -q 'chatApiKeySecret' docs-site/src/content/docs/guides/configure.md</automated>
    <automated>task lint:markdown</automated>
  </verify>
  <acceptance_criteria>
    - The environment table contains a row whose first cell names `ENGRAM_OPENAI_CHAT_API_KEY`, positioned directly after the `ENGRAM_OPENAI_CHAT_BASE_URL` row, stating an empty default and that empty means inherit.
    - That row states the key is never validated at startup and explains why (no verifiable shape; empty is meaningful).
    - None of the three now-false statements survives anywhere in the file: that there is no separate key for the chat base URL, that the key is shared across both lanes, and that per-lane credentials are unsupported this milestone.
    - The residual-risk warning survives in corrected form: the file still tells an operator that leaving the chat key unset sends the embedder's key to the chat gateway, and names the env var that fixes it.
    - The local-servers-ignore-the-header reassurance is still present and still scoped to the local-embedder case.
    - The callout heading no longer announces a shared key.
    - The `ENGRAM_OPENAI_CHAT_BASE_URL` row's cross-reference and the Auto-summary section's back-reference both describe per-lane credentials rather than a shared-key constraint.
    - The Helm value path named in the file matches the path recorded in `05-01-SUMMARY.md` exactly.
    - `task lint:markdown` passes.
  </acceptance_criteria>
  <done>An operator reading `configure.md` learns the chat lane can carry its own key, how to set it in env and in Helm, what happens when it is unset, and what credential leaks to the chat gateway if they set the base URL and forget the key.</done>
</task>

<task type="auto">
  <name>Task 2: Document the stale-record repair path and its hard limit in the reindex guide</name>
  <precondition>`05-02-SUMMARY.md` exists and records the shipped `reindexSummary` dry-run-with-resume wording and counter names — this task's prose must quote the output an operator will actually see.</precondition>
  <files>docs-site/src/content/docs/guides/reindex.md</files>
  <read_first>
    - `docs-site/src/content/docs/guides/reindex.md:87-113` — `## Resuming an interrupted run` in full. Two statements become false: that engram "skips any record whose content is unchanged", and that "the skip test is content equality — equal content re-embeds to an equal vector under the same embedder". Both need correcting, and the second is the same class of stale-prose defect as D-06's.
    - `docs-site/src/content/docs/guides/reindex.md:54-86` — `## Flags` and `## Output`. The `--dry-run` flag description and the summary-line documentation both need to account for the new dry-run-with-resume behavior.
    - `docs-site/src/content/docs/guides/reindex.md:114-120` — `## See also`. The new section goes before it.
    - `.planning/phases/05-operator-config-reindex-correctness/05-02-SUMMARY.md` — the exact shipped `--dry-run --resume` summary wording and counter names, and any premise-check deviation from the plan.
  </read_first>
  <action>
Correct `## Resuming an interrupted run` first. The predicate is now a three-part conjunction: content
equality, tag-set equality compared without regard to order, and the embedder-identity guard. The
paragraph that currently describes the predicate as a single content comparison, and the follow-on
claim that equal content therefore yields an equal vector, are both falsified — rewrite them to
describe the conjunction. Keep the true parts: that the copied payload stays byte-for-byte identical
and that no extra bookkeeping is stored.
State the deliberate residual plainly: because the embedded text folds tags in slice order, a record
whose tags were only reordered embeds to different text yet is intentionally treated as unchanged.
Frame it as a decision, not an oversight, and do not promise a future normalization.

Update `## Flags` and `## Output` so `--dry-run` documents its resume-aware behavior: combined with
`--resume` against a target that already exists, it reports how many records would be re-embedded and
how many would be skipped, still writing nothing and still creating no collection; used alone, its
output is unchanged. Quote the actual summary line from `05-02-SUMMARY.md` rather than paraphrasing
it, so an operator can match what they see on their terminal.

Add `## Repairing a pre-patch resume` directly before `## See also`. It must contain, in this order:

**What went wrong.** A `--resume` run on a version before this fix compared only content, so a record
whose tags were edited while its content stayed the same was reported unchanged and kept the vector it
had before the tag edit. Nothing errored; the counts looked healthy.

**The mechanism, stated explicitly — this is the load-bearing paragraph.** `reindex` re-scrolls the
source collection fresh on every invocation and compares the source's current content and tags against
what the target holds. Vector and payload are written together by one upsert built from that one source
read, so a target record can never hold a vector from one revision and a payload from another. The
target does not self-correct and has no memory of what it should look like; the source is authoritative
and re-read every time. That is why re-running the patched `--resume` heals the affected records, and
it is exactly why the limit below exists.

**The procedure.** Size it first with `engram reindex --target <target> --resume --dry-run` and read
the would-re-embed count — that is the blast radius. Then run the same command without `--dry-run`.
There is no separate repair command and none is needed: the patched resume is the repair path. Do not
name any command that does not exist.

**The limit (D-15).** If the source collection was deleted after a previous cutover, the correct tags
are gone — they live only in the source. Re-embedding from the live target payload would produce a
vector consistent with the *stale* tags: silently wrong while appearing healed. There is deliberately
no command for this case. The recovery is to re-derive the records from wherever they were authored,
or to accept the stale vectors. Make it unambiguous that an operator who has deleted their source and
runs `--resume` will get silent skips, not a heal.

Commit: `docs(05-03): repairing a pre-patch reindex resume` with an explicit pathspec limited to
`docs-site/src/content/docs/guides/reindex.md`.
  </action>
  <verify>
    <automated>rg -q '^## Repairing a pre-patch resume' docs-site/src/content/docs/guides/reindex.md</automated>
    <automated>! rg -q 'The skip test is content equality' docs-site/src/content/docs/guides/reindex.md</automated>
    <automated>rg -qi 'order' docs-site/src/content/docs/guides/reindex.md</automated>
    <automated>rg -q -- '--dry-run --resume' docs-site/src/content/docs/guides/reindex.md</automated>
    <automated>! rg -qi 'engram repair' docs-site/src/content/docs/guides/reindex.md</automated>
    <automated>awk '/^## Repairing a pre-patch resume/{f=1} f && /^## See also/{exit} f{print}' docs-site/src/content/docs/guides/reindex.md | rg -qi 'source collection'</automated>
    <automated>awk '/^## Repairing a pre-patch resume/{f=1} f && /^## See also/{exit} f{print}' docs-site/src/content/docs/guides/reindex.md | rg -qi 'deleted'</automated>
    <automated>task lint:markdown</automated>
  </verify>
  <acceptance_criteria>
    - `## Repairing a pre-patch resume` exists and is positioned directly before `## See also`.
    - The section contains all four required parts in order: what went wrong, the mechanism, the procedure, and the D-15 limit.
    - The mechanism paragraph explicitly states that the source is re-scrolled fresh on every run and that the target does not self-correct.
    - The procedure names only `engram reindex --resume` and its `--dry-run` form; no non-existent repair command appears anywhere in the file.
    - The D-15 limit states that a deleted source makes the correct tags unrecoverable and that re-embedding from the live payload would produce a vector consistent with the stale tags.
    - `## Resuming an interrupted run` no longer claims the skip test is content equality, and describes the three-part conjunction instead.
    - The reordering residual is described as deliberate, with no promise of future write-time normalization.
    - The `--dry-run --resume` summary wording quoted in `## Output` matches the wording recorded in `05-02-SUMMARY.md`.
    - `task lint:markdown` passes.
  </acceptance_criteria>
  <done>An operator who ran an unpatched resume can size the damage with one command, heal it with a second, and knows before they start whether their deleted source makes the heal impossible.</done>
</task>

<task type="auto">
  <name>Task 3: Add the two v0.12.0 upgrade entries and run the phase-close gates</name>
  <files>docs-site/src/content/docs/guides/upgrade.md</files>
  <read_first>
    - `docs-site/src/content/docs/guides/upgrade.md:110-160` — the `## v0.12.0` section, its lead paragraph, and its three existing numbered subsections (`### 1.` through `### 3.`). Read the lead paragraph carefully: it currently announces that the release ships *three* wire-visible changes to argument rejection. Two entries are being appended that are not about argument rejection, so that framing needs widening.
    - The existing subsections' internal structure — how each states what changed, who is affected, and what action if any is required. Match it.
    - `.planning/phases/05-operator-config-reindex-correctness/05-01-SUMMARY.md` and `05-02-SUMMARY.md` — the shipped identifiers and behaviors these entries describe.
  </read_first>
  <action>
Append two subsections to the existing `## v0.12.0` section, continuing its numbering as `### 4.` and
`### 5.`. Do not create a new top-level heading and do not renumber the existing three.

Widen the section's lead paragraph so it no longer promises exactly three changes all concerning
argument rejection. Keep its pointer to the error-envelope reference attached to the first three.

`### 4.` covers the per-lane chat credential: the chat/summarize lane can now carry its own API key
via `ENGRAM_OPENAI_CHAT_API_KEY`, or the Helm value plan 05-01 shipped. State clearly that no action
is required — leaving it unset is byte-identical to previous behavior, with the embedder's key
inheriting to the chat lane exactly as before. State who should act: an operator who has pointed
`ENGRAM_OPENAI_CHAT_BASE_URL` at a different gateway and would rather that gateway not receive the
embedder's credential.

`### 5.` covers the reindex resume defect. Name the defect: before this release, `reindex --resume`
compared only content, so a record whose tags changed while its content did not was reported
unchanged and kept a stale vector. Name the action: operators who have run `--resume` on an earlier
version should re-run it, sizing first with `--dry-run --resume`, and link to the reindex guide's
repair section. Name the limit in one sentence with a pointer to the same section for the full
explanation — do not restate the whole D-15 argument here.

Then run the phase-close gates listed in this plan's `<verification>` block and record every result
in the SUMMARY. If any gate is red, fix it or report it; do not seal the phase around a red gate.

Commit: `docs(05-03): v0.12.0 upgrade notes for the chat key and the resume fix` with an explicit
pathspec limited to `docs-site/src/content/docs/guides/upgrade.md`.
  </action>
  <verify>
    <automated>rg -q '^### 4\.' docs-site/src/content/docs/guides/upgrade.md</automated>
    <automated>rg -q '^### 5\.' docs-site/src/content/docs/guides/upgrade.md</automated>
    <automated>test "$(rg -c '^## v0\.12\.0' docs-site/src/content/docs/guides/upgrade.md)" = "1"</automated>
    <automated>rg -q 'ENGRAM_OPENAI_CHAT_API_KEY' docs-site/src/content/docs/guides/upgrade.md</automated>
    <automated>rg -q -- '--resume' docs-site/src/content/docs/guides/upgrade.md</automated>
    <automated>! rg -q 'three wire-visible changes' docs-site/src/content/docs/guides/upgrade.md</automated>
    <automated>task</automated>
    <automated>go vet ./...</automated>
    <automated>task license:check</automated>
    <automated>task chart:validate</automated>
    <automated>task proto:lint</automated>
    <automated>git diff --exit-code dc98ec0c -- go.mod go.sum</automated>
  </verify>
  <acceptance_criteria>
    - `## v0.12.0` appears exactly once and now contains subsections numbered 1 through 5, with 1-3 unmodified.
    - The section's lead paragraph no longer claims the release ships exactly three changes, all about argument rejection.
    - `### 4.` names `ENGRAM_OPENAI_CHAT_API_KEY` and the Helm value path from `05-01-SUMMARY.md`, states that no action is required, and states who should act.
    - `### 5.` names the tags-only resume defect, prescribes the `--dry-run --resume` then `--resume` re-run, links to the reindex guide's repair section, and states the deleted-source limit in one sentence.
    - `task` (lint plus full suite) exits 0.
    - `go vet ./...`, `task license:check`, `task chart:validate`, and `task proto:lint` all exit 0.
    - `git diff --exit-code dc98ec0c -- go.mod go.sum` exits 0.
    - `task proto:gen` and `task ui:build` leave the working tree clean — `git status --porcelain` is empty after each.
    - Every phase-close gate result, including the two zero-drift codegen checks, is recorded in the SUMMARY.
  </acceptance_criteria>
  <done>`guides/upgrade.md` tells a v0.12.0 upgrader what changed for the chat credential and what to re-run for the reindex defect, and every phase-close gate is green and recorded.</done>
</task>

</tasks>

<threat_model>
ASVS level is not configured in `.planning/config.json`. Per RESEARCH's `## Security Domain` mapping,
V2, V3, and V4 do not apply to this phase; V5 is partial and is D-05's deliberate decline, already
dispositioned in plan 05-01; V6 secrets handling applies and this plan is where its residual is
communicated. This plan writes no code, so its threats are documentation-integrity threats — a
correct system described incorrectly produces the same operator outcome as an incorrect system.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation -> operator's mental model -> production configuration | Prose that is false, or that omits a residual risk, causes real misconfiguration. This is the boundary this plan defends. |
| Documentation -> operator's data | A documented repair procedure that does not apply to the operator's situation can cause an irreversible loss of correctness that looks like a successful heal. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03 | Information Disclosure | `guides/configure.md`'s credential callout | medium | mitigate | The residual accepted in plan 05-01 — the embedder's key reaching the chat gateway when the chat base URL is set and the chat key is not — is now an opt-out rather than a hard constraint. Deleting the old warning along with the false constraint would silently drop the only place this risk is explained. Task 1 requires the warning to survive in corrected form, gated by an explicit acceptance criterion, not merely by the negative greps that remove the false sentences. |
| T-05-09 | Tampering | `guides/reindex.md`'s repair procedure | medium | mitigate | An operator who deleted their source collection after a previous cutover and follows the repair procedure gets silent skips, and may conclude the records were healed when their vectors are still stale. Mitigated by Task 2's requirement to state the mechanism (the source is authoritative and re-read every time) before the procedure, so D-15's limit reads as a consequence rather than a footnote, plus an explicit statement of what such an operator will actually observe. |
| T-05-10 | Repudiation | `guides/reindex.md` naming a non-existent command | low | mitigate | D-13 declines a dedicated repair command. Documentation that invents one produces an operator dead end at exactly the moment they are trying to fix data. Gated by a negative check that no `engram repair`-shaped command name appears, and by the prohibition that `cmd/engram/` gains no new file this phase. |
| T-05-SC | Tampering | go / npm / pip / cargo package installs | high | mitigate | This plan installs no packages and modifies no manifest. RESEARCH's Package Legitimacy Audit records zero new packages phase-wide, so there is no `[ASSUMED]`/`[SUS]`/`[SLOP]` entry to gate on. Enforced phase-wide by `git diff --exit-code dc98ec0c -- go.mod go.sum` in this plan's phase-close gate. |
</threat_model>

<verification>
## Phase-close gates

Run all of these after Task 3's edits and record every result in the SUMMARY. These close the whole
phase, not just this plan.

- `task` — lint plus the full suite
- `go vet ./...`
- `task license:check`
- `task chart:validate` — the D-04b checksum guard, green only if plan 05-01 re-pinned it
- `task proto:lint`
- `task proto:gen` then `git status --porcelain` is empty — zero codegen drift
- `task ui:build` then `git status --porcelain` is empty — zero UI drift
- `git diff --exit-code dc98ec0c -- go.mod go.sum` — the milestone's zero-new-dependency proof

## Phase-wide prohibition gates

- `! rg --type go -n 'ChatAPIKey' internal cmd | rg -v '_test\.go:' | rg -q 'slog\.|fmt\.Errorf|Printf|Println'`
- `rg -q 'embed\.New\(cfg\.OpenAI\.BaseURL, cfg\.OpenAI\.APIKey' internal/server/tools.go`
- `! git diff dc98ec0c -- internal/store/store.go | rg -q '^@@.*func (payload|EmbedText)\('`
- `git diff --exit-code dc98ec0c -- internal/authz`
- `git diff --exit-code dc98ec0c -- internal/config/validate.go`
- `git diff --name-only dc98ec0c -- cmd/engram/` lists only `cmd/engram/reindex.go` and `cmd/engram/reindex_test.go` — no new command file

## Phase-wide edge gates

Each of the seven edges in this plan's `must_haves.truths` is proven by a named test or command that
already ran in wave 1. Re-run them here as the phase seal:

- `go test ./internal/store/... -run 'TestReindexResumeTags$' -v -count=1 2>&1 | rg -q '^--- PASS: TestReindexResumeTags \('` (edges 1-4)
- `go test ./internal/store/... -run 'TestReindexDryRun' -v -count=1 2>&1 | rg -q '^--- PASS: TestReindexDryRunWritesNothing \('` (edge 5)
- `go test ./internal/server/... -run 'TestSummarizerFromConfigChatAPIKey$' -v -count=1 2>&1 | rg -q '^--- PASS: TestSummarizerFromConfigChatAPIKey \('` (edge 6)
- `git diff --exit-code dc98ec0c -- go.mod go.sum` (edge 7)

A `--- SKIP:` result is a FAILED gate, not a pass — `internal/store`'s `dialTestClient` skips silently
when no Qdrant is reachable and does not honor `ENGRAM_REQUIRE_QDRANT`.
</verification>

<success_criteria>
- `configure.md` no longer asserts a shared key, still warns about the residual, and documents the new env var and Helm value.
- `reindex.md` carries a repair section that states the mechanism, the procedure, and the deleted-source limit, and no longer describes the skip test as content equality.
- `upgrade.md`'s v0.12.0 section runs 1 through 5 with the original three unmodified.
- Every phase-close gate, prohibition gate, and edge gate is green and recorded.
</success_criteria>

<output>
Create `.planning/phases/05-operator-config-reindex-correctness/05-03-SUMMARY.md` when done. Include a
table of every phase-close, prohibition, and edge gate with its result, and note any wave-1 identifier
that differed from what this plan anticipated.
</output>
