---
phase: 03-migration-foundation-registry-invariants-sweep
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - internal/migrate/additive_test.go
  - internal/migrate/additive.go
  - .planning/phases/03-migration-foundation-registry-invariants-sweep/red-evidence/03-03-red-1-superset-not-equality.patch
  - .planning/phases/03-migration-foundation-registry-invariants-sweep/red-evidence/03-03-red-2-removal-check-dropped.patch
  - .planning/phases/03-migration-foundation-registry-invariants-sweep/red-evidence/03-03-red-3-zero-fixtures.patch
autonomous: true
requirements:
  - REQ-migration-additive-only-gated

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Additive-only behavior is proven by a key-set diff asserted in BOTH directions: before is a subset of after (nothing removed or renamed), AND after minus before is SET-EQUAL to the step's declared addsKeys."
    - "The second direction is set equality, never a subset or a superset relation. A superset check passes a step that quietly adds an undeclared key; a subset check passes a step that declares keys it never writes. Either way the declaration stops describing the behavior, which is the failure the declaration exists to prevent (D-04)."
    - "The fixture table asserts a non-zero row count AND that both verdict classes are represented — a table of only-conforming rows would pass a checker that always returns nil, and a table of only-violating rows would pass one that always returns an error (D-05)."
    - "The set of rows classified non-conforming is asserted SET-EQUAL to an expected name set, so neither an always-nil nor an always-error checker can pass this file."
    - "The fixtures are test-only steps built through migrate.NewStep, never the production Registry — which is empty this phase and would make the scan pass for the wrong reason (D-06)."
    - "The key-removing row and the declared-drift row are each proven to FAIL by observation, not by absence: weakening the corresponding half of CheckAdditive flips exactly that row and leaves the others green."
    - "CheckAdditive compares KEY SETS only and makes no claim about values; a step that overwrites an existing key's value in place is outside what this check can see. That limit is written down rather than left to be inferred, AND its consequence is contained elsewhere: Store.Migrate builds its SetPayload map from AddedKeys only, proven by TestMigrateWritesOnlyAddedKeys in plan 03-01."
    - "A fixture step whose ApplyFunc mutates the map it was handed IN PLACE and returns that same map is still classified non-conforming, because the driver clones before every application. An implementation that cloned once and let before/after alias one backing map would call that row conforming — the exact vacuous pass PA-5a names."
    - "Every CONFORMING row is additionally proven step-level IDEMPOTENT: applying the same step a second time to its own output yields a deep-equal payload. This is one of the two executable halves of SC1's `idempotency` word (the other is the sweep-level rerun proof in plan 03-01); migrate.Validate's transition-uniqueness rule is the structural precondition for both and is not itself a proof of either."
  artifacts:
    - path: "internal/migrate/additive_test.go"
      provides: "The eight-row fixture table proving both diff directions, the aliasing row, the per-row step-level idempotence assertion, and the non-vacuity and both-classes-present guards"
      contains: "TestAdditiveOnlyKeySetDiff"
  key_links:
    - from: "internal/migrate/additive_test.go"
      to: "internal/migrate/additive.go"
      via: "the fixture table drives the same two-direction diff function the sweep drives, so the test and production cannot disagree"
      pattern: 'CheckAdditive'
    - from: "internal/migrate/additive_test.go"
      to: "internal/migrate/step.go"
      via: "every fixture step is constructed through the only exported constructor, so no fixture can carry a declaration shape a real step could not"
      pattern: 'NewStep'
---

<objective>
Prove the additive-only invariant is real: an eight-row fixture table over test-only steps, asserting
the key-set diff in BOTH directions, with guards that make a vacuous pass impossible, and three
observed RED cycles — one per half of the check plus one on the vacuity guard itself.

Purpose: D-04 names the load-bearing half explicitly. A subset or superset comparison would pass a
step whose declaration has drifted from its behavior, and the declaration is the entire mechanism
REQ-migration-additive-only-gated rests on. D-05 exists because this milestone has already shipped a
gate that exercised zero fixtures and reported green (durable record `x6v6qxqd6f`); the guard here is
mandatory, and this plan proves it fires.

Output: `internal/migrate/additive_test.go` plus three committed, reviewer-reproducible patches, one
of which flips only the drift row, one only the removal row, and one only the vacuity guard.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-migration-foundation-registry-invariants-sweep/03-CONTEXT.md
@.planning/phases/03-migration-foundation-registry-invariants-sweep/03-RESEARCH.md
@.planning/phases/03-migration-foundation-registry-invariants-sweep/03-PATTERNS.md
@.planning/phases/03-migration-foundation-registry-invariants-sweep/03-VALIDATION.md
@.planning/phases/03-migration-foundation-registry-invariants-sweep/03-01-SUMMARY.md
@CLAUDE.md
</context>

<planner_assumptions>

**PA-1 (fixtures live in `_test.go`, not `testdata/`).** CONTEXT.md leaves this to discretion. The
fixtures are Go `ApplyFunc` closures, not data — expressing them as `testdata/` JSON would require a
second interpreter layer whose bugs would be indistinguishable from the checker's. They stay in the
test file.

**PA-2 (`CheckAdditive` sees keys, not values — and where the consequence is contained).** The check
compares key SETS. A step that overwrites an existing key's VALUE in place is additive by key set and
passes. That is a real limit of D-04's chosen mechanism, not a defect in this implementation — the
restricted-writer alternative that would close it (a `PayloadAdder` with no set/delete surface) is in
CONTEXT.md's Deferred Ideas, explicitly not chosen for this phase.

Review cycle 1 noted that a documented limit is still a limit. It is contained, but NOT here: plan
03-01 builds `Store.Migrate`'s `SetPayload` map from `migrate.AddedKeys` only, so an overwritten value
has no added key and never crosses the wire, and `TestMigrateWritesOnlyAddedKeys` proves it against a
real Qdrant. Write BOTH halves into this test file's doc comment — the checker's blindness and the
write-shaping that contains it, naming that test — so a future reader neither infers a stronger
guarantee from `CheckAdditive` nor concludes the gap is unhandled.

**PA-3 (this table does not run against `Registry`).** D-06 is explicit: the production registry is
empty this phase, so a table iterating it would scan nothing and pass. Any acceptance criterion here
that mentions `Registry` is about asserting it is EMPTY, never about iterating it for coverage.

**PA-4 (step-level idempotence belongs here).** SC1's word "idempotency" is NOT discharged by
`migrate.Validate`, whose first rule checks transition uniqueness — a structural precondition, not a
behavioral property (review cycle 1 HIGH finding; see `Validate`'s corrected doc comment in 03-01).
The behavioral halves are proven executably in two places, and this table owns one of them: for every
CONFORMING row, applying the step a second time to its own output must yield a deep-equal payload. The
other half — re-running the whole sweep over a converged collection — is `TestMigrateTracer\
LegacyRecordEndToEnd`'s second run in plan 03-01. Non-conforming rows are excluded from the
apply-twice assertion on purpose: a step that already violates the declaration contract has no defined
second-application behavior, and asserting one would invent a requirement D-04 does not state.

**PA-15 (wave-2 RED-cycle git isolation — verified at plan time).** Identical to plan 03-02's PA-15
and binding here: `workflow.use_worktrees` is unset in `.planning/config.json` (GSD defaults it to
`true`), `.claude/settings.local.json` sets `worktree.baseRef: "head"` so this branch's divergence from
`origin/HEAD` does not force the sequential auto-degrade, and there is no `.gitmodules`. Wave-2
executors therefore run in separate git worktrees, each with its own working tree and index, so this
plan's captured hunks cannot collide with 03-02/03-04/03-05's. Record the observed
`git rev-parse --show-toplevel` and `git branch --show-current` in the SUMMARY before the first
injection. If the branch is NOT an `agent-*`/`worktree-agent-*`/`worktree-wf_*` branch — i.e. this
executor is on the shared main working tree — then the fallback protocol is mandatory: strictly one
captured patch at a time, `git apply --check` immediately before each injection with a RE-CAPTURE (not
a force) on context mismatch, and every `git apply`/`git apply -R`/`git diff`/`git commit` scoped by an
explicit pathspec naming only this plan's files (the hazard class this repo's rule `n6m4as49mr`
already addresses for commits). `git checkout --` on a tracked file is forbidden in either mode.

</planner_assumptions>

## Artifacts this phase produces

See the table in `03-01-PLAN.md` § "Artifacts this phase produces" — every symbol listed there is
created by this phase and must be excluded from drift/source-grounding verification. This plan
contributes: `internal/migrate/additive_test.go`, `TestAdditiveOnlyKeySetDiff`, and the three
committed RED patches under
`.planning/phases/03-migration-foundation-registry-invariants-sweep/red-evidence/`. It reuses (does
not re-create) `migrate.AddedKeys`, `migrate.RemovedKeys` and `migrate.CheckAdditive` from plan 03-01.

<tasks>

<task type="auto">
  <name>Task 1: The eight-row fixture table, with both diff directions, the aliasing row, per-row step idempotence, and both anti-vacuity guards</name>
  <files>internal/migrate/additive_test.go</files>
  <read_first>
    - `internal/migrate/additive.go` as written by plan 03-01 — `AddedKeys`, `RemovedKeys`, and the exact wording of `CheckAdditive`'s two error directions. The table asserts against that wording, so read it rather than guessing.
    - `internal/migrate/step.go` — `NewStep`'s parameter order and the two `Reversibility` constructors; every fixture is built through `NewStep`.
    - `.planning/phases/03-migration-foundation-registry-invariants-sweep/03-CONTEXT.md` D-04, D-05, D-06 — the two directions, the mandatory non-zero fixture count, and the four minimum fixture shapes. D-04's second paragraph names which direction is load-bearing and why.
    - `.planning/phases/03-migration-foundation-registry-invariants-sweep/03-RESEARCH.md` § "Code Examples > Additive-only key-set diff test" — the concrete table shape, including the `if len(fixtures) == 0 { t.Fatal(...) }` guard to copy literally.
    - `internal/store/schemaversion_recallgate_test.go` around line 174 — the fail-loudly-on-an-unrecognized-shape reflex; the table's default path must be a `t.Fatalf`, never a silent skip.
    - `.planning/phases/03-migration-foundation-registry-invariants-sweep/03-VALIDATION.md` § "Non-Vacuity Requirements" — bullets one and two are binding on this task.
  </read_first>
  <action>
    Create `internal/migrate/additive_test.go` (SPDX header, `package migrate`) with
    `TestAdditiveOnlyKeySetDiff`.

    **Table shape.** Each row carries: a name, a `before` payload map, a declared `addsKeys` slice, a
    `Reversibility`, an `ApplyFunc`, a `wantConforming` bool, and — for non-conforming rows — the list
    of key names the error must mention. Build each row's step with `NewStep(0, 1, declared, rev,
    apply)` so no fixture can carry a declaration shape a real registered step could not.

    **The eight rows.** The first three must be classified conforming, the last five non-conforming:
    1. `conforming additive` — `before` has `content`; declares `schema_version`; the apply sets
       exactly that one key. Reversible with a real inverse.
    2. `conforming additive, irreversible with a stated reason` — same shape, but carrying
       `Irreversible("...")` with a concrete reason string. This row exists to pin that reversibility
       and additive compliance are ORTHOGONAL: an irreversible step is still held to the same key-set
       discipline, and a conforming verdict here must not depend on which `Reversibility` variant the
       step carries.
    3. `no payload-key additions (version transition still advances)` — declares nothing and adds
       nothing. Legal: CONTEXT.md states steps may have side effects, and a pure-side-effect step
       touches no key. This row exercises the empty-set against empty-set edge, where a naive
       implementation using a nil-vs-empty-slice comparison breaks. Review cycle 1 flagged the earlier
       name `conforming no-op` as misleading and it must not be reused: such a step is NOT operationally
       a no-op — the sweep still advances the record's `schema_version` to the target, which is a real
       mutation. Name the row for what it asserts (no added keys), not for a property it does not have.
    4. `removes a key` — `before` has `content` and `legacy_field`; declares nothing; the apply
       deletes `legacy_field` from a COPY and returns that copy. Non-conforming: `before` is not a
       subset of `after`. The error must name `legacy_field`.
    5. `adds an undeclared key` — declares `schema_version`; the apply sets `schema_version` AND
       `undeclared_extra`. Non-conforming by set equality even though it is additive and even though
       the declared key IS among those added. This is D-04's load-bearing row: a superset check would
       call it conforming. The error must name `undeclared_extra`.
    6. `declares a key it never adds` — declares `schema_version` and `never_written`; the apply sets
       only `schema_version`. Non-conforming by set equality — the mirror of row 5, which a subset
       check would call conforming. The error must name `never_written`.
    7. `removes one and adds an undeclared one` — both directions violated at once. The error must
       name BOTH the vanished key and the undeclared key, proving the two directions are reported
       independently rather than collapsed into one generic message.
    8. `removes a key by mutating its input map in place` — identical in intent to row 4, but the
       `ApplyFunc` deletes `legacy_field` from THE MAP IT WAS HANDED and returns that same map rather
       than a copy. Still non-conforming; the error must still name `legacy_field`. This row exists
       because it is the one an aliasing bug makes pass: if the driver clones once and lets `before`
       and `after` reference one backing map, the deletion is invisible to the diff, `AddedKeys` and
       `RemovedKeys` are both empty, and `CheckAdditive` returns nil. Comment it with exactly that
       sentence, naming PA-5a from plan 03-01 — it is the table-driver mirror of the sweep-side
       sub-case in `TestMigrateRefusesNonAdditiveStep`, and it is not a duplicate of row 4: the two
       differ only in whether the step copies, and that difference is the entire point.

    **Drive each row identically, with TWO independent clones per application** — the same discipline
    `Store.Migrate` follows (plan 03-01, PA-5a), and the reason row 8 can fail:

        beforeRow := maps.Clone(row.before)
        after, err := row.step.Apply(maps.Clone(row.before))
        // ... err handling ...
        gotErr := CheckAdditive(row.step, beforeRow, after)

    Cloning once and passing that one map to both `Apply` and `CheckAdditive` is a failure of this
    task. `gotErr == nil` is the conforming verdict. For non-conforming rows assert that every expected
    key name appears in the error text, using `strings.Contains`.

    **Per-row step-level idempotence, for CONFORMING rows only** (PA-4). After a conforming verdict,
    apply the step a SECOND time to its own output — `secondAfter, err := row.step.Apply(maps.Clone(after))`
    — and assert `reflect.DeepEqual(secondAfter, after)`, printing the differing keys on failure. This
    is one of the two executable halves of SC1's "idempotency" word; `migrate.Validate`'s
    transition-uniqueness rule is a structural precondition and proves nothing about re-application,
    and the sweep-level half lives in plan 03-01's tracer. Skip the assertion for non-conforming rows
    and say why in a comment: a step that already violates the declaration contract has no defined
    second-application behavior, and asserting one would invent a requirement D-04 does not state.

    **The two anti-vacuity guards, both before the loop:**
    - `if len(fixtures) == 0 { t.Fatal(...) }` — D-05, copied literally from RESEARCH.md's example.
    - Count the rows by expected verdict and fail unless BOTH classes are non-empty. A table of only
      conforming rows passes a checker that always returns nil; a table of only violating rows passes
      one that always returns an error. Neither guard alone is sufficient, so assert both.

    **One more assertion after the loop, and it is the strongest one here:** collect the names of the
    rows that were actually classified non-conforming and assert that set is SET-EQUAL to the expected
    non-conforming name set, with both difference directions printed on failure. This single assertion
    is what an always-nil checker and an always-error checker both fail, and it does not depend on any
    row's error text.

    **Assert the shipped registry is empty** (PA-3): `if len(Registry) != 0 { t.Fatalf(...) }`, with a
    message stating that this phase ships an empty production registry and that this table deliberately
    runs against fixtures instead, because iterating an empty registry would pass for the wrong reason.

    Write PA-2's limit AND its containment into the test file's doc comment: this check compares key
    sets only and cannot see a step that overwrites an existing key's value in place; the
    restricted-writer alternative that would close that gap at the source is a recorded Deferred Idea,
    not an oversight; and the consequence is contained downstream because `Store.Migrate` builds its
    `SetPayload` map from `migrate.AddedKeys` only, proven by `TestMigrateWritesOnlyAddedKeys` in plan
    03-01. Naming that test is required — a limit stated without its containment reads as an
    unhandled hole.
  </action>
  <verify>
    <automated>go test -count=1 -v -run 'TestAdditiveOnlyKeySetDiff$' ./internal/migrate/ 2>&1 | rg -c -- '    --- PASS: TestAdditiveOnlyKeySetDiff/'</automated>
  </verify>
  <acceptance_criteria>
    - The command above prints `8` — all eight rows ran as passing subtests. A package-level `ok` is a FALSE GREEN and a failure of this task.
    - `go test -list '.*' ./internal/migrate/ | rg -c 'TestAdditiveOnlyKeySetDiff'` prints `1`, confirming the `-run` expression resolves to a real test.
    - Exactly three rows are expected conforming and exactly five are expected non-conforming, and both counts are asserted to be non-zero before the loop runs.
    - The post-loop assertion compares the OBSERVED non-conforming name set to the EXPECTED one for set equality, printing both difference directions on failure.
    - Row 5 (`adds an undeclared key`) and row 6 (`declares a key it never adds`) are both present. A table missing either one cannot distinguish set equality from a subset or superset relation, and is a failure of this task.
    - Row 7 asserts BOTH the vanished key name and the undeclared key name appear in the error text.
    - Row 8 (`removes a key by mutating its input map in place`) is present, its `ApplyFunc` returns the very map it was handed, and it is classified non-conforming. A table without it cannot distinguish the correct per-application cloning from a single shared clone.
    - The driver makes TWO independent clones per application — one kept as `before`, one passed to `Apply`. `rg -c 'maps\.Clone' internal/migrate/additive_test.go` prints at least `3` (the two per-application clones plus the idempotence re-application). A driver that passes one map value to both `Apply` and `CheckAdditive` is a failure of this task regardless of whether row 8 happens to pass.
    - Every CONFORMING row additionally asserts step-level idempotence: applying the step a second time to its own output yields a `reflect.DeepEqual` payload, with differing keys printed on failure. Non-conforming rows skip that assertion and a comment states why.
    - Row 3's name states the property it asserts: `rg -c 'no payload-key additions' internal/migrate/additive_test.go` prints at least `1`, and `go test -count=1 -v -run 'TestAdditiveOnlyKeySetDiff$' ./internal/migrate/ 2>&1 | rg -ci -- '--- PASS: TestAdditiveOnlyKeySetDiff/.*no.?op'` prints `0`. A step that adds no key still advances `schema_version` when the sweep writes it, so naming the row for an absence of effect would be wrong.
    - Every fixture step is constructed via `NewStep`; `rg -c 'NewStep\(' internal/migrate/additive_test.go` prints at least `8`.
    - `if len(fixtures) == 0 { t.Fatal(...) }` is present verbatim in spirit — a zero-row table fails loudly rather than passing.
    - The test asserts `len(Registry) == 0` with a message explaining why the table uses fixtures instead.
    - The test file's doc comment states that the check compares key sets only, names the deferred restricted-writer alternative, AND names `TestMigrateWritesOnlyAddedKeys` as where the consequence is contained.
    - `task lint` exits 0, `gofmt -l internal/migrate` prints nothing, `task license:check` exits 0.
  </acceptance_criteria>
  <done>An eight-row fixture table proves the key-set diff in both directions, distinguishes set equality from both a subset and a superset relation via mirrored rows, catches a step that mutates its input in place, proves every conforming step idempotent under re-application, and cannot pass vacuously — neither with zero rows, nor with one verdict class, nor with a checker that ignores its input.</done>
</task>

<task type="auto">
  <name>Task 2: Three RED cycles — one per diff direction, one on the vacuity guard</name>
  <files>internal/migrate/additive.go, internal/migrate/additive_test.go, .planning/phases/03-migration-foundation-registry-invariants-sweep/red-evidence/03-03-red-1-superset-not-equality.patch, .planning/phases/03-migration-foundation-registry-invariants-sweep/red-evidence/03-03-red-2-removal-check-dropped.patch, .planning/phases/03-migration-foundation-registry-invariants-sweep/red-evidence/03-03-red-3-zero-fixtures.patch</files>
  <read_first>
    - Task 1's `internal/migrate/additive_test.go` as written, and `internal/migrate/additive.go` from plan 03-01.
    - `.planning/phases/03-migration-foundation-registry-invariants-sweep/03-VALIDATION.md` § "Non-Vacuity Requirements", fourth bullet — the RED-evidence procedure: an exact reversible patch, committed under `red-evidence/`, with `git diff --exit-code` clean after revert.
    - `.planning/phases/02-record-schema-versioning-foundation/red-evidence/02-02-red-1-bypass.patch` — the committed-patch format and granularity this repo established.
    - `.planning/phases/02-record-schema-versioning-foundation/02-03-SUMMARY.md` — how a RED cycle is recorded: injection, observed failing output, three-command reproduce recipe, observed pass after revert.
  </read_first>
  <action>
    Three cycles. In each, capture the hunk BEFORE injecting, revert by applying the exact inverse with
    `git apply -R`, and verify with `git diff --exit-code` scoped to the touched file. Do NOT use
    `git checkout --` on a tracked file: it discards unrelated uncommitted work. PA-15's git-isolation
    protocol governs all three cycles — record the observed `git rev-parse --show-toplevel` and
    `git branch --show-current` in the SUMMARY before the first injection, hold at most one captured
    patch at a time, and use pathspec-scoped git commands throughout.

    What makes these cycles evidence rather than ceremony is **selectivity**: each patch must flip a
    named subset of rows and leave the rest green. A patch that turns the whole table red proves only
    that the table runs. Record the per-row verdict in the SUMMARY for each cycle, not just "it failed".

    **Cycle 1 — `03-03-red-1-superset-not-equality.patch`.** Weaken `CheckAdditive`'s second direction
    from set equality to the one-way relation `declared ⊆ added` — "every declared key is among the
    added keys". Note the direction precisely: the weakened predicate STOPS CHECKING `added − declared`
    and KEEPS CHECKING `declared − added`.

    The expected selective outcome follows mechanically, and review cycle 1 required it be stated
    unambiguously rather than hedged:
    - Row 5 (`adds an undeclared key`): declared `{schema_version}`, added `{schema_version,
      undeclared_extra}`. `declared ⊆ added` HOLDS, so the row FLIPS to conforming and its subtest
      FAILS. This is the only row that moves.
    - Row 6 (`declares a key it never adds`): declared `{schema_version, never_written}`, added
      `{schema_version}`. `declared ⊄ added`, so it is STILL non-conforming and STAYS GREEN. The
      earlier prediction that row 6 might also fail was wrong and has been corrected.
    - Rows 1-3 stay green (their declared and added sets are equal, so `⊆` and `==` agree). Rows 4, 7
      and 8 stay green (they are caught by the untouched removal direction).
    - The PARENT test also fails, on the post-loop set-equality assertion: the observed non-conforming
      name set has lost row 5.

    So the signature of a correct cycle 1 is exactly two failures — subtest row 5 and the parent's
    post-loop assertion — and six green subtests. Record what you OBSERVE; if it differs from this,
    write the difference down rather than reconciling it by editing the test, because a divergence here
    is a finding about `CheckAdditive`, not about the plan. The SUMMARY entry must say in plain terms
    that under this patch a step whose declaration has drifted from its behavior is accepted.

    **Cycle 2 — `03-03-red-2-removal-check-dropped.patch`.** Delete `CheckAdditive`'s first direction
    (the removed-keys check) entirely. Run the table. Expected: rows 4 and 8 (`removes a key`, and its
    in-place-mutating twin) FLIP to conforming and FAIL — with the removal check gone, each has an
    empty added set and an empty declared set, so the surviving direction calls it conforming. Row 7
    must also be checked and its observed verdict recorded: it violates both directions, so it should
    STAY non-conforming and green on the strength of the surviving added-key check alone. Rows 1-3, 5
    and 6 must stay green; if any of them moves, the two directions are not as independent as the
    implementation claims — stop and record that, because it is a finding about the production code,
    not about the test. The parent's post-loop set-equality assertion is expected to fail too, having
    lost rows 4 and 8.

    **Cycle 3 — `03-03-red-3-zero-fixtures.patch`.** Patch `additive_test.go` itself: empty the fixture
    table. The test MUST fail on the `len(fixtures) == 0` guard. This is the cycle that proves D-05's
    guard is load-bearing rather than decorative — it is the exact defect this milestone shipped in
    Phase 01 and had to prove by injection (durable record `x6v6qxqd6f`). Confirm the failure names the
    zero-fixture condition specifically, not some downstream nil dereference: a guard that "fails" only
    because the code panicked afterwards is not a guard.

    After all three cycles, re-run the full table and confirm `8` passing subtests, and confirm
    `git diff --exit-code -- internal/migrate/additive.go internal/migrate/additive_test.go` is clean.
  </action>
  <verify>
    <automated>go test -count=1 -v -run 'TestAdditiveOnlyKeySetDiff$' ./internal/migrate/ 2>&1 | rg -c -- '    --- PASS: TestAdditiveOnlyKeySetDiff/'</automated>
  </verify>
  <acceptance_criteria>
    - The command above prints `8` after all three cycles are reverted.
    - All three patch files exist under `.planning/phases/03-migration-foundation-registry-invariants-sweep/red-evidence/`, each applies cleanly with `git apply` and reverses cleanly with `git apply -R`.
    - `03-03-SUMMARY.md` records, per cycle: the injected change in one sentence, the OBSERVED per-row verdicts (which subtests failed and which stayed green), the exact three-command reproduce recipe (`git apply` -> `go test` expecting FAIL -> `git apply -R`), and the observed pass after revert.
    - Each cycle is SELECTIVE: the SUMMARY names which rows moved and which did not, and compares them against the expectation this plan states. A cycle that turned the entire table red is recorded as such and treated as weaker evidence, with an explanation.
    - Cycle 1's recorded outcome is compared against this plan's stated signature — exactly row 5's subtest plus the parent's post-loop assertion failing, six subtests green. Any divergence is written down as a finding about `CheckAdditive`, not reconciled by editing the test.
    - Cycle 1's entry states plainly that under `declared ⊆ added` a step whose declaration has drifted from its behavior is accepted, and names the direction the weakened predicate stopped checking (`added − declared`).
    - Cycle 3's entry states that the failure came from the zero-fixture guard itself, not from a downstream panic, and quotes the guard's message.
    - If cycle 2 moves any row other than 4 and 8 (plus the parent's post-loop assertion), that is recorded as a finding about `CheckAdditive`'s direction independence rather than silently accepted.
    - `git diff --exit-code -- internal/migrate/additive.go internal/migrate/additive_test.go` succeeds at task end. `git checkout --` was not run on any tracked file.
    - The SUMMARY records the observed git isolation mode (PA-15): `git rev-parse --show-toplevel` and `git branch --show-current` at the start of the first cycle, and whether the isolated-worktree path or the shared-working-tree fallback protocol was in force.
    - `.planning/**` files carry NO SPDX header — do not run `task license:add` over the red-evidence directory.
    - `task lint` exits 0 and `task license:check` exits 0.
  </acceptance_criteria>
  <done>Both halves of the additive-only check and the non-vacuity guard itself have each been observed turning a named subset of the table RED, with three committed patches a reviewer can re-run in three commands.</done>
</task>

</tasks>

<threat_model>
Security enforcement is active. ASVS level: L1 (default). Blocking threshold: high. No new external
package is introduced. This plan adds only test code; its threat surface is the integrity of the gate.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| step author -> `internal/migrate` | A step is trusted same-binary developer code. The additive-only check is the correctness control on that boundary — a badly-written step can silently destroy payload keys across the whole collection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | a step that removes or renames a payload key | high | mitigate | The `before` subset of `after` direction, with row 4 proving it fires and RED cycle 2 proving the direction is load-bearing. |
| T-03-11 | Tampering | a step whose declared `addsKeys` has drifted from what it actually writes | high | mitigate | Set equality on the added-key set, with mirrored rows 5 and 6 distinguishing it from both a subset and a superset relation, and RED cycle 1 proving the distinction matters. |
| T-03-08 | Tampering | a gate that checks nothing and reports green | high | mitigate | Two pre-loop guards (non-zero rows, both verdict classes present) plus the post-loop set-equality assertion; RED cycle 3 observes the zero-row guard firing. |
| T-03-12 | Tampering | a step that overwrites an existing key's VALUE in place | medium | mitigate | Out of what a key-set diff can see (PA-2), so it is NOT mitigated here — it is contained downstream: `Store.Migrate` builds its `SetPayload` map from `migrate.AddedKeys` only, so an overwritten value never reaches Qdrant (plan 03-01, `TestMigrateWritesOnlyAddedKeys`). The source-level mechanism that would close it — a restricted writer with no set/delete surface — remains a recorded Deferred Idea. Both the limit and its containment are documented in this test file's doc comment. |
| T-03-20 | Tampering | an `ApplyFunc` that mutates its input map in place, aliasing `before` and `after` into one backing map and making the diff vacuously empty | high | mitigate | The driver makes two independent clones per application (PA-5a, mirroring `Store.Migrate`), grep-pinned in the acceptance criteria and proven by row 8, whose `ApplyFunc` returns the very map it was handed. |
| T-03-23 | Tampering | two wave-2 executors' RED-cycle patches colliding in one git index | low | mitigate | PA-15: verified at plan time that wave-2 runs in isolated worktrees, plus a mandatory serialize / `git apply --check` / re-capture / pathspec-scoping fallback if the executor observes it is on the shared working tree. |
</threat_model>

<verification>
- `go test -count=1 -v ./internal/migrate/` is green, with `--- PASS: TestAdditiveOnlyKeySetDiff/`
  observed eight times — never a package-level `ok`.
- `go test -list '.*' ./internal/migrate/ | rg -c 'TestAdditiveOnlyKeySetDiff'` prints `1`.
- All three red-evidence patches apply and reverse cleanly; `git diff --exit-code` is clean for both
  touched files afterwards.
- `task` (lint + test) is green.
</verification>

<success_criteria>
- The key-set diff is asserted in both directions over test-only fixture steps, never the production
  registry.
- Set equality is distinguished from both a subset and a superset relation by mirrored fixture rows.
- A step that mutates its input map in place is still classified non-conforming, proving the driver's
  per-application cloning is real rather than assumed.
- Every conforming step is proven idempotent under re-application — the step-level half of SC1's
  "idempotency" word, which `Validate` does not cover.
- The table cannot pass with zero rows, with one verdict class, or against a checker that ignores its
  input.
- Each half of the check and the vacuity guard itself have been observed turning a named subset of
  rows RED, with committed reproducible patches and an unambiguous stated expectation to compare
  against.
</success_criteria>

<output>
Create `.planning/phases/03-migration-foundation-registry-invariants-sweep/03-03-SUMMARY.md` when done.
Record all three prove-RED cycles with observed per-row verdicts, patch paths and three-command
reproduce recipes; record the observed git isolation mode per PA-15; and record any deviation from
PA-1 … PA-4 and PA-15 explicitly.

Also record, for the next review cycle, the four cycle-1 findings this plan resolves and how:
(1) RED cycle 1's predicted outcome made mathematically unambiguous (only row 5 flips; row 6 stays
green); (2) the misleading `no-op` row name replaced with one naming the property actually asserted;
(3) the value-overwrite limitation now stated together with its containment in
`TestMigrateWritesOnlyAddedKeys`; (4) the aliasing hazard closed by two-clones-per-application and
proven by row 8. Note also that the per-conforming-row apply-twice assertion is one half of the
idempotency proof the HIGH finding on `Validate` required.
</output>
