---
phase: 08-registry-docs-tail
plan: 04
type: execute
wave: 3
depends_on: ["08-01", "08-02", "08-03"]
files_modified:
  - CLAUDE.md
autonomous: true
requirements: [REQ-claude-md-migrations-convention]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "CLAUDE.md's `Not used here` bullet no longer excludes database migrations outright. It states what this milestone actually ships — schema-version-driven payload migrations through a registered step chain — and names the deliberate boundary: the owner-remap, summary-fill, and embedder-reindex sweeps are NOT part of that mechanism and do not appear in its status histogram (D-05.1, SC3, REQ-claude-md-migrations-convention)."
    - "That bullet states the automation contract with both halves: no migration ever APPLIES automatically (never on startup, and the mutating verbs preview by default), and what IS automatic is a read-only startup status probe that may warn and never invokes the sweep. A bullet saying only 'nothing runs automatically' contradicts a warning an agent will actually see in the server log."
    - "CLAUDE.md's `cmd/engram/` layout row names every command the live cobra tree exposes, and the gate that proves it derives the expected set from the generated self-describe catalog AND extracts the observed set from THAT ROW rather than from the whole file — so a command name that happens to appear in the Memory contract prose cannot satisfy it. The gate's per-word split is explicit (`tr ' ' '\\n'`), never implicit scalar word splitting, so it behaves identically under bash, zsh and sh and grouped family notation genuinely satisfies it (D-05.2)."
    - "The row marks `migrate-set-owner` as the DEPRECATED ALIAS of `migrate-remap-owner`, not as a peer verb. It is in the catalog and so must appear, but a routing row that presents an alias as a first-class command routes an agent to the wrong verb — which is the one failure the row exists to prevent. The gate asserts the row carries the marker (D-05.2)."
    - "The row distinguishes the client tier from the operator tier. `get`, `search`, `list`, `store` and `migration-status` reach a running server over Connect; the sweep and maintenance verbs act on Qdrant directly. A row that files them all under `operator commands` is factually misleading even when every token is present, and misrouting an agent by tier is exactly the failure this row exists to prevent."
    - "CLAUDE.md's Memory contract section names `schema_version` and `archived_at`, which appear nowhere in the file today, and states the record-state vocabulary — `archived`, `superseded`, `expired`, `scheduled` — that Phases 5 through 7 shipped, in the canonical order and with the `expired`-suppresses-`scheduled` precedence (D-05.3)."
    - "Every addition states the CURRENT contract. CLAUDE.md is normative routing, not a changelog: no addition narrates what changed, in which release, or which phase shipped it (D-05 constraint)."
    - "The file keeps its existing table-and-bullet idiom and density. This is an edit to a routing doc, not an expansion of it — the Layout table stays a table, the Conventions list stays a bullet list, and the Memory contract stays prose paragraphs with no bullet list and no table introduced into it."
    - "CLAUDE.md's existing SPDX header — the HTML comment at lines 1-4 — is PRESERVED. The file is in `.licenserc.yaml` scope via `*.md` and already carries one; removing it, or reading the constraint as 'must not have a header', breaks `task license:check`."
    - statement: "An agent reading only CLAUDE.md forms an accurate picture of which command to reach for AND which tier it acts on, without needing to open `cmd/engram/` to discover a command the routing doc omitted — verified by reading the revised file against the catalog, not by an assertion."
      verification: backstop
  artifacts:
    - path: "CLAUDE.md"
      provides: "The project's normative routing doc, brought into agreement with the shipped command surface and record-state vocabulary"
      contains: "schema_version"
  key_links:
    - from: "CLAUDE.md"
      to: "docs-site/src/content/docs/guides/migrate.md"
      via: "the migration convention routing the reader to the operator procedure, mirroring how the reindex entry already routes"
      pattern: "guides[/]migrate"
  prohibitions:
    - statement: "The revised migrations line MUST NOT overclaim scope. Stating or implying that the owner-remap, summary-fill, or embedder-reindex sweeps are part of the schema-version mechanism would tell an agent that the migration status report accounts for work it deliberately does not track, and would send it to the wrong command for a recovery it needs to perform. The boundary is as load-bearing as the claim."
    - statement: "The revised migrations line MUST NOT say that nothing about migrations happens automatically. Server startup runs a read-only status probe and may log a pending-migrations warning. The accurate claim is that migrations never APPLY automatically; the broader claim would make an agent treat a real, expected log line as a defect."
    - statement: "CLAUDE.md MUST NOT become a changelog. It is the instruction set every agent working in this repo reads first; narrating history there displaces routing signal with archive, and an agent that has to date-reason about which sentence is current is reading an unreliable doc."
---

<!-- planner-discipline-allow: database migrations, viper, cocogitto -->

<objective>
Bring CLAUDE.md into agreement with what this milestone shipped: the migrations convention that
currently contradicts the code, the command inventory that omits four families and blurs two tiers,
and the Memory contract section that never mentions the schema-version stamp or the archived state
(D-05, REQ-claude-md-migrations-convention).

Purpose: CLAUDE.md is the normative routing doc every agent reads before touching this repo. A line
asserting the project does not use database migrations, in a repo that just shipped a migration
registry, a sweep, a status histogram, and a revert, is not a stale detail — it actively routes
agents away from the mechanism.

Output: a routing doc that describes the current surface, at its existing density, with a gate that
checks the row rather than the file.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-registry-docs-tail/08-CONTEXT.md
@CLAUDE.md
</context>

## Artifacts this phase produces

This plan creates no new file and no new code symbol. It edits three regions of one existing file:
the `cmd/engram/` row of the Layout table, the final bullet of the Conventions list, and the Memory
contract section.

## Note on editing your own instruction file

CLAUDE.md is both an instruction you are following and the artifact you are editing. Read it as
subject here. Its own rules still bind you while you work — Conventional Commits, branch-and-PR,
`task` before claiming done — and nothing in this plan changes any of them.

## Why this plan runs last

It depends on all three siblings, and the edges are real, not ceremonial:

- **`08-01`** regenerates `cmd/engram/testdata/catalog.golden`, which Task 1's inventory gate derives
  its expected command set from. Running before it would derive from a stale catalog.
- **`08-02`** writes the record-state contract on `reference/memory-record.md` that Task 2 compresses.
  Concurrent execution would let two surfaces settle on different wording for the same vocabulary
  with nothing forcing agreement.
- **`08-03`** creates `docs-site/src/content/docs/guides/migrate.md`, which Task 1's convention bullet
  links to.

All three edges survived cycle-2 review unchallenged. The phase's shape is now
`(08-01 ‖ 08-03) → 08-02 → 08-04` — `08-03` dropped its own undeclared edge on `08-01` (cycle-2
finding M-2.5), so this plan is wave **3**, not wave 4. Its own dependency set is unchanged.

## Cycle-2 review findings addressed here

| Finding | Where |
|---|---|
| **M-2.1** (supersedes cycle-1 **F-1.26**, which is closed via this successor) — Task 1's gate split each command name with `for w in $name`, relying on implicit scalar word splitting. bash does it; zsh and fish do not. Under zsh the gate searched for the single token `` `spine-review scan` ``, which the grouped row the plan itself mandates can never satisfy — the gate became unsatisfiable. | The `<verify>` block now splits explicitly with `tr ' ' '\n'` and drops `< <(…)` process substitution for a plain pipeline, so it carries no shell-dependent construct. Measured identical under **bash, zsh and sh**: 24 misses against `main`, exit 0 against a grouped row. |
| **L-2.1** — the plan told the executor to record 26 before-misses; the gate reports 24 | Corrected to 24, measured, and restated as a floor so a stale number cannot masquerade as a regression. |
| **L-2.2** — the gate compelled the row to name `migrate-set-owner`, the DEPRECATED alias of `migrate-remap-owner`, as a first-class command | The row must now mark it as an alias, and the `<verify>` block asserts the marker. Both halves of the gate were shown to fire independently. |

## What actually gates this file

Unlike the two docs-site plans in this phase, `task lint` IS meaningful here: `.rumdl.toml` excludes
`docs-site` but NOT the repo root, so `rumdl check .` reads CLAUDE.md. `task license:check` is
meaningful here too — CLAUDE.md is in `.licenserc.yaml` scope via `*.md` and already carries an SPDX
header at lines 1-4. Both are real gates for this plan; treat them as such.

<tasks>

<task type="auto">
  <name>Task 1: The migrations convention and the tier-aware command inventory</name>
  <reversibility rating="reversible">Documentation only; a single-file revert restores it (D-05).</reversibility>
  <files>CLAUDE.md</files>
  <read_first>
    - `CLAUDE.md` — the whole file, as subject: the SPDX header (1-4), the Layout table (11-23) and its `cmd/engram/` row (14), the Conventions bullet list (25-70) and its final bullet (70), and the Memory contract section that follows at 72
    - `cmd/engram/testdata/catalog.golden` — the generated self-describe catalog; its top-level `"name"` entries at six-space indentation are the authoritative command inventory. There are 23 of them.
    - `cmd/engram/migrate_family.go:248-286` and `:604-618` — the three migrate verbs and their one-line summaries
    - `cmd/engram/client_migration_status.go:18-30` — the Connect-lane status sibling, and what makes it client-tier rather than operator-tier
    - `internal/server/tools.go:490-535` — `warnPendingMigrations`: the read-only startup probe, its 10-second budget, its two distinct warnings, and its doc comment stating it MUST NOT invoke `Store.Migrate`. This is the authority for the automation half of the bullet; do not assert startup behavior without it.
    - `.planning/REQUIREMENTS.md` — the Migration Mechanism section and the Out of Scope table, particularly the row explaining why the owner-remap, summary-fill, and embedder-reindex sweeps are deliberately excluded from the registry
    - `internal/migrate/migrate.go` and `internal/migrate/step.go` — what the mechanism actually is: an ordered registry of additive-only steps, each declaring reversibility
    - `docs-site/src/content/docs/guides/migrate.md` — the guide `08-03` wrote, which this file should route to exactly as the existing row routes to the reindex guide
  </read_first>
  <action>
Two edits, both in the top third of the file.

**The Conventions bullet.** The final bullet of the Conventions list currently lists three things the
project does not use, the first of which is now false. Revise it so it states what this milestone
ships and where its boundary lies. It must convey five things, in the file's own compressed voice:

1. This project DOES have payload migrations, and they are schema-version-driven: an ordered registry
   of additive-only steps in `internal/migrate`, each of which must declare its reversibility, swept
   by `engram migrate`.
2. Migrations never APPLY automatically — the mutating verbs preview by default and mutate only under
   `--apply`, and nothing applies on startup.
3. What IS automatic: server startup runs a read-only `MigrateStatus` probe and may log a
   pending-migrations warning (and a separate future-version warning). It never invokes the sweep and
   never gates startup. Say this. A bullet claiming nothing automatic happens would make an agent
   treat a real, expected log line as a defect, and would contradict `internal/server/tools.go`.
4. The boundary, stated positively where possible: the registry covers version-driven payload
   evolution only. The three other sweep commands key off an IdP claim change, ongoing async summary
   fill, and embedder config identity respectively, none of which is version-driven, so none is in
   the registry and none appears in the status histogram. Naming them is fine and useful; implying
   they are part of the mechanism is the failure this bullet exists to prevent.
5. A pointer to the operator guide, in the same shape the `cmd/engram/` row already uses for the
   reindex guide.

The other two items the bullet excludes are still accurate and should survive in whatever form the
revision takes. If the result is too long for one bullet, split it — a migrations entry alongside a
shorter not-used-here entry is fine, and matches how the surrounding bullets are sized. (Splitting is
expected to add `- ` lines to the Conventions list; that is permitted work, and Task 2's structural
gate is scoped to the Memory contract section precisely so it cannot false-RED on it.)

**The `cmd/engram/` Layout row.** It names three core verbs plus a parenthesized operator-command
list that predates four families, and it files everything under `operator commands`. Bring it current
against the generated catalog AND make it tier-accurate.

Missing from the row today, measured with the gate below: `get`, `search`, `list`, `store`,
`migrate`, `migrate status`, `migrate revert`, `migrate-set-owner`, `migration-status`,
`spine-review`, and all six `spine-review` leaves (`scan`, `verify`, `consolidate`, `purge`,
`archive`, `restore`).

Two structural requirements:

- **Group the two families by their parent verb** so the row stays readable at its current length —
  the way the existing text groups by responsibility. The gate below is designed for grouped
  notation: it requires every WORD of each catalog command name to appear as a backticked token
  somewhere in the row, so `` `spine-review` (`scan`, `verify`, …) `` satisfies `spine-review scan`
  without the row having to spell the two-word path out.
- **Distinguish the tiers.** `get`, `search`, `list`, `store` and `migration-status` are client-tier:
  they reach a running server over Connect with a URL and a token. The sweep and maintenance verbs
  act on Qdrant directly. Say which is which. `migration-status` in particular exists precisely
  because it is the client-tier sibling of the operator-tier `migrate status`, and a row that hides
  that distinction sends an agent to the wrong one.

`migrate-set-owner` is in the catalog and must appear, but it is the DEPRECATED alias of
`migrate-remap-owner` — the file already describes it as one in the Memory contract section. **Mark
it as an alias in the row**, attached to the verb it aliases, rather than listing it as a peer. A row
that presents an alias as a first-class command cuts against the row's entire purpose of routing an
agent to the right verb. The gate below enforces this: the row must carry the word `deprecated`, and
the read-and-quote criterion checks that the marker attaches to `migrate-set-owner` and not to
something else.

Do not expand the row into a table of its own or into a per-command description list — it is one
cell in a Layout table and its job is routing, not reference. Where a command has an operator guide,
point at it in the same shape the row already uses for reindex.

Derive the inventory from `cmd/engram/testdata/catalog.golden`, not from memory or from the previous
row. That file is regenerated from the live cobra tree by `task surfaces:gen`, so it cannot disagree
with the binary. `08-01` regenerated it in this same phase; read the current file, not a remembered
list.

State the current contract. Do not write which release or phase added anything, and do not mark
anything as new. Do not touch the SPDX header at lines 1-4.
  </action>
  <verify>
    <automated>row=$(rg -N '^\| `cmd/engram/` \|' CLAUDE.md); misses=$(rg -o '^      "name": "[a-z][a-z -]*"' cmd/engram/testdata/catalog.golden | rg -o '"[a-z][a-z -]*"$' | tr -d '"' | sort -u | while IFS= read -r name; do printf '%s\n' "$name" | tr ' ' '\n' | while IFS= read -r w; do printf '%s' "$row" | rg -qF -- "\`$w\`" || printf '%s/%s\n' "$name" "$w"; done; done); printf '%s\n' "$misses"; test -z "$misses" && printf '%s' "$row" | rg -q 'deprecated'</automated>
  </verify>
  <acceptance_criteria>
    - **The inventory gate is scoped to the ROW, not the file.** Run the `<verify>` command and require `$misses` empty. The observed set is extracted from the `cmd/engram/` Layout row alone — checking the whole file is satisfied by `get`, `search`, `list` and `store`, which already occur 14, 12, 19 and 11 times respectively in the Memory contract prose, so a whole-file gate proves nothing about the row. The comparison is per-WORD against backticked tokens, so grouped family notation satisfies it while a bare mention in surrounding prose does not.
    - **The per-word split is EXPLICIT, and must stay explicit** (cycle-2 review finding M-2.1; closes cycle-1 F-1.26). An earlier draft used `for w in $name` and `< <(…)`. Implicit word splitting of a scalar is a bash behavior that zsh and fish do not share, so under zsh that gate searched for the single token `` `spine-review scan` `` — which the grouped row this very task mandates can never contain, making the gate unsatisfiable by its own required output. The command above splits with `tr ' ' '\n'` and uses a plain pipeline instead of process substitution, so it carries no shell-dependent construct at all. It was executed under **bash, zsh and sh** during replanning and produced byte-identical results in all three. Do not reintroduce `for w in $name`.
    - **The before-value is 24 name/word misses, measured — and it is a FLOOR, not an equality** (cycle-2 review finding L-2.1). An earlier draft said 26; the gate reports 24 against the current tree, and an executor told to expect 26 while observing 24 has no way to separate a stale number from a real regression. Record the observed count and the full miss list in the summary, and treat anything `-ge 24` as the expected before-state. The 24 are: `get/get`, `list/list`, `migrate/migrate`, `migrate revert/migrate`, `migrate revert/revert`, `migrate status/migrate`, `migrate status/status`, `migrate-set-owner/migrate-set-owner`, `migration-status/migration-status`, `search/search`, `spine-review/spine-review`, and the two-word `spine-review` leaves `archive`, `consolidate`, `purge`, `restore`, `scan`, `verify` (each contributing both its parent and its leaf word), plus `store/store`.
    - **The deprecated-alias half of the gate.** The trailing `rg -q 'deprecated'` over the row is the machine half of the L-2.2 fix: the inventory gate alone would be satisfied by a row that lists `migrate-set-owner` as a peer verb. The word `deprecated` does not appear in the row today, so this clause is RED before the work and was verified to fire independently — a scratch row with a complete inventory but no marker exits 1. Read-and-quote half: confirm by reading that the marker attaches to `migrate-set-owner` specifically (it is the only deprecated alias in the catalog), and quote the row in the summary.
    - The gate goes RED for the right reason: remove one leaf name from the finished row and confirm the failure names exactly that command and word; restore. Verified during replanning against a scratch grouped row — dropping `restore` produced exactly `spine-review restore/restore` and nothing else.
    - **The gate accepts grouped notation — prove it, do not assume it.** The row is required to group `migrate` and `spine-review` by parent verb, so the gate is only useful if grouped notation actually satisfies it. A scratch row of the required shape (`` `spine-review` (`scan`, `verify`, …) ``) was run through this exact command under all three shells and exited 0. Run the finished row through it and record the exit in the summary.
    - `rg -o 'database migrations, viper, cocogitto' CLAUDE.md | wc -l` reports `0`. It reports `1` against `main`.
    - `rg -o 'guides[/]migrate' CLAUDE.md | wc -l` reports at least `1`, and the link target exists: `test -f docs-site/src/content/docs/guides/migrate.md`.
    - The revised convention names the boundary AND the automation contract, both checked by reading and quoted in the summary: (a) the sentence saying the excluded sweeps are absent from the status report, not merely absent from the registry; (b) the sentence distinguishing "never applies automatically" from the read-only startup probe that does run. A grep cannot tell a correct scoping sentence from an overclaiming one — that is why these are read-and-quote criteria.
    - The tiers are distinguished. Confirm by reading that the row places `get`, `search`, `list`, `store` and `migration-status` on the client tier and the sweeps on the operator tier, and quote the row in the summary.
    - No date, release number, phase number, or "new in" phrasing was added: `git diff -U0 -- CLAUDE.md | rg -o '^\+.*(\bnew in\b|\bas of\b|shipped in|this release)' | wc -l` reports `0`. The `\b` word boundaries are load-bearing, not decoration: without them the `as of` alternative matches inside **`alias of`** — the exact phrase the deprecated-alias requirement above now compels the Layout row to carry — so the unbounded form was a self-invalidating gate that would go red on correct work. Verified: `printf 'deprecated alias of x' | rg -o '(new in|as of|shipped in|this release)'` prints `as of`, while the bounded form prints nothing and still catches `as of this release ... new in v2`.
    - **The SPDX header is intact:** `head -4 CLAUDE.md | rg -c 'SPDX-License-Identifier: Apache-2.0'` reports `1`. CLAUDE.md already carries one and is in `.licenserc.yaml` scope via `*.md` — this criterion asserts preservation, not absence.
    - The Memory contract section is untouched by this task (Task 2 owns it): `git diff -U0 -- CLAUDE.md | rg -o '^@@ -[0-9]+' | rg -o '[0-9]+'` — every hunk start line for THIS task falls above the `## Memory contract (stable)` heading's line number. `git diff --stat` cannot show this; it emits only file and insertion/deletion counts.
    - `task lint` is green, including `rumdl check .` over CLAUDE.md — which, unlike the docs-site pages in this phase, IS actually read by rumdl. `task license:check` passes.
  </acceptance_criteria>
  <done>An agent reading CLAUDE.md's routing surface finds every shipped command, knows which tier each acts on, and gets an accurate statement of what the migration mechanism covers, what it deliberately does not, and what the server does automatically.</done>
</task>

<task type="auto">
  <name>Task 2: The record-state vocabulary in the Memory contract section</name>
  <reversibility rating="reversible">Documentation only.</reversibility>
  <files>CLAUDE.md</files>
  <read_first>
    - `CLAUDE.md` — the Memory contract section in full (from `## Memory contract (stable)` at 72 to `## Auth` at 173), especially the opening record-shape paragraph that enumerates a record's fields, the supersession paragraph, and the scheduled-tools paragraph, for where each addition belongs and at what density
    - `internal/store/store.go:315-353` — the `SchemaVersion` field, `schemaVersionKey`, and the stamp site, for what is actually server-set
    - `internal/store/store.go:1004-1021` — `activeWindowConditions` — and the four recall-gate sites pairing `IsEmpty("superseded_by")` with `IsEmpty("archived_at")`
    - `cmd/engram/memory_state.go:1-70` — `memoryStateWords`, the canonical order and the precedence rule
    - `docs-site/src/content/docs/reference/memory-record.md` — the sections `08-02` wrote in this same phase. CLAUDE.md compresses that contract; it must not restate it in different words. Read it before writing, and match its vocabulary.
    - `cmd/engram/spine_review_archive.go` — the archive/restore verbs, for the vocabulary the section must name
  </read_first>
  <action>
Neither `schema_version` nor `archived_at` appears anywhere in CLAUDE.md today. The section already
documents supersession and the scheduling window in real depth, so the gap is a genuine omission
rather than stale text.

Add, at the section's existing density:

**The schema-version stamp.** Fold `schema_version` into the record-shape enumeration in the opening
paragraph, alongside the other server-set keys it already names. State that it is server-set on every
write, that absence reads as version 0, and that it never gates recall. One clause each; the field
contract lives on the reference page and the procedure on the operator guide.

**The archived state.** Add `archived_at` and the `spine-review archive` / `restore` pair. The
section already explains supersession's soft-hide-but-still-fetchable-by-id contract in a way that
`archived_at` shares exactly, so state the shared property once and note that archiving is an
orthogonal key: archiving never writes an expiry and never writes a supersession link, and each state
clears independently of the others.

**The state vocabulary.** State the four derived words — `archived`, `superseded`, `expired`,
`scheduled` — their canonical order by descending finality, and that `expired` and `scheduled` are
mutually exclusive with `expired` winning. This is the vocabulary every surface renders, so an agent
that knows it can read any of them. Do not restate the window boundary rule here — that belongs on
the reference page `08-02` wrote, and a second statement of it is a second thing to keep correct.
Link or refer to that page rather than paraphrasing its boundary sentences.

Keep the paragraph idiom. Do not introduce a table or a bulleted list into a section built from prose
paragraphs, and do not let the additions push it past the density of what is already there. If
something has to give, compress the additions, not the existing content.

State the current contract. No release narration, no phase numbers, no "new" markers. Do not touch
the SPDX header.
  </action>
  <verify>
    <automated>sec=$(sed -n '/^## Memory contract/,/^## Auth/p' CLAUDE.md); printf '%s\n' "$sec" | rg -q 'schema_version' && printf '%s\n' "$sec" | rg -q 'archived_at' && printf '%s\n' "$sec" | rg -q 'spine-review archive' && test "$(printf '%s\n' "$sec" | rg -c '^([-*]|[0-9]+\.) ' || echo 0)" = "0" && test "$(printf '%s\n' "$sec" | rg -c '^\|[ :-]*-{3,}' || echo 0)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - The three new terms land INSIDE the Memory contract section, not merely somewhere in the file. Extract the section with `sed -n '/^## Memory contract/,/^## Auth/p' CLAUDE.md` and confirm it matches `schema_version`, `archived_at`, and `spine-review archive`. Whole-file greps for these report `0` against `main`, so section-scoping costs nothing today and is what keeps the criterion honest after Task 1 has edited the Layout row.
    - All four state words appear in that same section: `for w in archived superseded expired scheduled; do sed -n '/^## Memory contract/,/^## Auth/p' CLAUDE.md | rg -qF -- "$w" || echo "missing: $w"; done` prints nothing. Quote the sentence that carries the canonical order in the summary so a reviewer can check it against `cmd/engram/memory_state.go` without re-deriving.
    - **No table or list was introduced into the section**, checked region-scoped rather than by diff: within the extracted section, `rg -c '^([-*]|[0-9]+\.) '` reports `0` and `rg -c '^\|[ :-]*-{3,}'` (a markdown table separator row) reports `0`. Both report `0` against `main`. The earlier draft used `git diff -- CLAUDE.md | rg -o '^\+[|-] '` over the whole file, which would RED on Task 1's own permitted bullet split; and a naive `^\s*[-*|] ` count over the section matches a wrapped prose line that happens to begin with `| ` (there is one today), so it cannot be used as an invariant either.
    - The section's vocabulary agrees with `reference/memory-record.md` as `08-02` wrote it. Confirm by reading both and record in the summary any place where CLAUDE.md's compression uses a different word for the same thing than the reference page does — there should be none.
    - No date, release number, phase number, or "new in" phrasing was added: `git diff -U0 -- CLAUDE.md | rg -o '^\+.*(\bnew in\b|\bas of\b|shipped in|this release)' | wc -l` reports `0`. The `\b` word boundaries are load-bearing, not decoration: without them the `as of` alternative matches inside **`alias of`** — the exact phrase the deprecated-alias requirement above now compels the Layout row to carry — so the unbounded form was a self-invalidating gate that would go red on correct work. Verified: `printf 'deprecated alias of x' | rg -o '(new in|as of|shipped in|this release)'` prints `as of`, while the bounded form prints nothing and still catches `as of this release ... new in v2`.
    - **The SPDX header is intact:** `head -4 CLAUDE.md | rg -c 'SPDX-License-Identifier: Apache-2.0'` reports `1`.
    - `task lint` is green (rumdl does read CLAUDE.md) and `task license:check` passes.
  </acceptance_criteria>
  <done>CLAUDE.md's Memory contract names the schema-version stamp, the archived state, and the four-word state vocabulary an agent will meet on every surface — in the same words the reference page uses, and without turning a prose section into a table.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLAUDE.md → every agent acting in this repo | This file is loaded ahead of the task and treated as normative; a wrong statement here propagates into agent behavior with no further review step |
| shipped command surface → documented inventory | The Layout row asserts what commands exist and which tier they act on, and nothing recompiles it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-13 | Spoofing | the revised migrations convention | medium | mitigate | A convention line that folds the owner-remap, summary-fill, or embedder-reindex sweeps into the schema-version mechanism would send an agent to `engram migrate` for a recovery that command cannot perform, and would make it read the status histogram as accounting for work it does not track. Both failures are silent. Task 1 requires the boundary stated explicitly in the bullet, with the summary quoting the sentence, and the prohibition block carries the same constraint into the goal-backward pass. |
| T-08-14 | Repudiation | the `cmd/engram/` inventory row | medium | mitigate | A hand-kept inventory is what produced the current gap, and re-writing it by hand would only reset the clock. The acceptance gate derives the expected set from `cmd/engram/testdata/catalog.golden` — regenerated from the live cobra tree — and extracts the observed set from THE ROW rather than the whole file. That second half is the mitigation that actually matters: a whole-file gate is already satisfied by `get`/`search`/`list`/`store` occurring in the Memory contract prose, so it would have reported green over an incomplete row. |
| T-08-15 | Spoofing | the client-tier / operator-tier distinction in the row | medium | mitigate | Filing `migration-status` under `operator commands` alongside `migrate status` tells an agent the two are interchangeable. They are not: one reaches a running server over Connect with a URL and a token, the other requires direct Qdrant access. An agent routed to the wrong one fails in an environment-dependent way that looks like a configuration problem. Task 1 requires the tiers distinguished in the row and the row quoted in the summary. |
| T-08-16 | Repudiation | the automation half of the migrations bullet | medium | mitigate | "Nothing runs automatically" is broader than the code: `internal/server/tools.go`'s `warnPendingMigrations` runs a read-only `MigrateStatus` probe on every startup and logs warnings. An agent told nothing is automatic will treat that expected log line as a defect and go looking for a bug that is not there — or, worse, "fix" it. Task 1 requires both halves stated and cites the implementation in `read_first`. |
| T-08-17 | Information Disclosure | the Memory contract additions | low | accept | Everything added is already public in `docs-site` and in the tool schemas; CLAUDE.md compresses it for routing. No credential, endpoint, or internal-only detail is introduced. |
| T-08-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package, runs no package manager, and touches no dependency manifest. `08-RESEARCH.md` records the Package Legitimacy Audit as not applicable; `git diff --exit-code go.mod go.sum` is expected to exit 0. |
</threat_model>

<verification>
- The row-scoped derived command-coverage gate passes: every word of every top-level command name in `cmd/engram/testdata/catalog.golden` appears as a backticked token in the `cmd/engram/` Layout row, split explicitly with `tr ' ' '\n'` so the result does not vary by shell. It fails against `main` with **24** name/word misses (measured under bash, zsh and sh alike).
- The same gate's trailing clause passes: the row carries the word `deprecated`, marking `migrate-set-owner` as the alias of `migrate-remap-owner` rather than as a peer verb. That clause is RED against `main`.
- `rg -o 'database migrations, viper, cocogitto' CLAUDE.md | wc -l` reports `0` (it reports `1` against `main`).
- Within the Memory contract section, `schema_version`, `archived_at` and `spine-review archive` all match; all three report `0` file-wide against `main`.
- Within that same section, the bullet-line count and the table-separator count are both `0`, as they are against `main`.
- `head -4 CLAUDE.md | rg -c 'SPDX-License-Identifier: Apache-2.0'` reports `1` — the existing header was preserved.
- `test -f docs-site/src/content/docs/guides/migrate.md` — the guide this file routes to exists.
- `task lint` is green, including `rumdl` over CLAUDE.md. Unlike this phase's docs-site plans, this IS a real gate for this file: `.rumdl.toml` excludes `docs-site`, not the repo root.
- `task license:check` passes with the existing SPDX header unchanged.
- `git diff --exit-code go.mod go.sum` exits 0.
- Manual check, per `08-VALIDATION.md`: read the revised Layout row and Conventions bullet against the live command tree and confirm the migration boundary matches the Out of Scope table in `.planning/REQUIREMENTS.md`, and that the tier split matches which commands take a server URL and token.
</verification>

<success_criteria>
- The migrations convention describes the schema-version mechanism this milestone ships, names its deliberate boundary, and distinguishes "never applies automatically" from the read-only startup probe that does run.
- The `cmd/engram/` inventory agrees with the generated catalog and distinguishes the client tier from the operator tier, and the gate that proves the first derives the expected set from that catalog and the observed set from the row itself.
- The Memory contract section names `schema_version`, `archived_at`, and the four-word state vocabulary, in the same words `reference/memory-record.md` uses, and stays prose.
- The existing SPDX header survives, and nothing added reads as a changelog entry.
</success_criteria>

<output>
Create `.planning/phases/08-registry-docs-tail/08-04-SUMMARY.md` when done
</output>
