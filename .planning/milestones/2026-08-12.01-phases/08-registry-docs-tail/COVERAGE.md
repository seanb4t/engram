# API Coverage — Phase 08 (Registry & Docs Tail)

No external API integration: internal `surfaces` rule registration, one new test, and `docs-site/` + `CLAUDE.md` prose only. The detector's `wire`/`connect` + `mcp` signals are engram's own MCP/Connect lanes named in plan prose.
