---
phase: 08-registry-docs-tail
plan: 05
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - CLAUDE.md
autonomous: true
requirements: [REQ-claude-md-migrations-convention]

estimate:
  tokens: 25000
  raw_tokens: 25000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "CLAUDE.md's `cmd/engram/` Layout row marks EVERY deprecated command in the live cobra tree as deprecated, not just one of them. The gate derives the deprecated set from the committed goldens (a top-level command present in `catalog.golden` but absent from `help.golden`'s root `Available Commands:` block is deprecated — cobra's `IsAvailableCommand()` excludes a command with a non-empty `Deprecated` from that listing) and requires ZERO of them to be unannotated. It never names a command literally, so a third command deprecated later fails the gate instead of slipping past a hardcoded pair (repairs 08-VERIFICATION.md truth 11, D-05.2)."
    - "CLAUDE.md's Archived-state paragraph names the SAME set of soft-hide recall surfaces its own adjacent Supersession paragraph names — proven by a two-directional set difference between the two paragraphs, not by a presence grep — and that set's size equals the number of live `qdrant.NewIsEmpty(\"archived_at\")` gate sites in `internal/store/store.go` with comment lines stripped. The paragraph's own claim is that `archived_at` \"shares supersession's soft-hidden-but-still-fetchable-by-id contract\"; the gate makes that claim executable rather than asserted (repairs 08-VERIFICATION.md truth 12, D-05.3)."
    - "Runtime behavior is unchanged: this plan touches exactly one file, `CLAUDE.md`, and no Go, proto, docs-site, skill, or generated artifact. `git diff --name-only` over the whole change reports `CLAUDE.md` and nothing else."
    - "Scope discipline: the two corrections are sentence-level, not section rewrites. Everything 08-04 landed and 08-VERIFICATION.md verified — the Conventions migrations bullet (truth 10), the row's 23-command inventory (truth 11's inventory half), the four-word canonical state vocabulary, and the `migrate-set-owner` alias wording that 08-VERIFICATION.md records as info-level-only and CORRECT — survives byte-identical apart from the two edits named in the tasks. 08-04's own two committed gates (the row-scoped inventory gate and the no-changelog-phrasing gate) are re-run as regression checks and stay green."
    - statement: "Both corrected sentences read as natural prose in CLAUDE.md's existing routing-doc idiom and density — a compression of the current contract, not a changelog entry and not an expansion of the section. Verified by reading the two paragraphs whole, not by an assertion."
      verification: backstop
  artifacts:
    - path: "CLAUDE.md"
      provides: "A routing doc whose deprecation marking is complete over the live command tree and whose Archived-state recall gate agrees with the store's own gate sites and with its sibling Supersession paragraph"
      contains: "deprecated"
  key_links:
    - from: "CLAUDE.md"
      to: "cmd/engram/backfill.go"
      via: "the Layout row names backfill-short-ids, the command whose backfillShortIDsCmd.Deprecated field is live in the tree, so the row's deprecation marking can be checked against the code"
      pattern: "backfill[-]short[-]ids"
    - from: "CLAUDE.md"
      to: "docs-site/src/content/docs/reference/memory-record.md"
      via: "both pages state the same four soft-hide recall surfaces for a hidden record, so the compression in CLAUDE.md cannot diverge from the reference page"
      pattern: "list_scheduled"
---

<objective>
Repair the two confirmed factual defects 08-VERIFICATION.md found in `CLAUDE.md` — the incomplete
deprecation marking in the `cmd/engram/` Layout row (truth 11, corroborated by 08-REVIEW.md WR-09)
and the understated recall gate in the Archived-state paragraph (truth 12, corroborated by
08-REVIEW.md WR-07).

Purpose: `CLAUDE.md` is normative routing. Both defects are *positive false assertions* to the agent
reading it: the Layout row demonstrates a deprecation annotation on one command and withholds it
from an equally-deprecated sibling (so the sibling reads as current), and the Archived paragraph
names two of four surfaces a record disappears from (so an agent believes `search_discovery` and
`list_scheduled` still surface archived records). Both are one-sentence corrections; neither is a
licence to rewrite a section.

Output: `CLAUDE.md`, two sentences corrected, plus two derived gates that are RED against `HEAD`
today and stay meaningful afterward.

**On tracer-first:** this plan has no tracer task and deliberately so. A tracer proves an
architecture end-to-end through the layers a phase touches; this plan touches one markdown file and
adds no layer, no call path, and no runtime behavior. The honest analogue of the tracer's value here
is that both gates are proven RED against `HEAD` before the edit (each task's `<verify>` records the
pre-state), which is what "observed failing" buys.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-registry-docs-tail/08-VERIFICATION.md
@.planning/phases/08-registry-docs-tail/08-CONTEXT.md
@.planning/phases/08-registry-docs-tail/08-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mark every deprecated command in the cmd/engram/ Layout row, derived from the goldens</name>
  <files>CLAUDE.md</files>
  <read_first>
    - `CLAUDE.md` line 15 — the single-line `| \`cmd/engram/\` | ... |` table row. Read the whole
      row before editing; it is long and one edit must not disturb the rest.
    - `cmd/engram/backfill.go` around line 58 — `backfillShortIDsCmd.Deprecated = "use: engram migrate"`,
      the live fact the row currently contradicts by omission.
    - `cmd/engram/migrate.go` around line 270 — `migrateSetOwnerCmd.Deprecated = ...`, the sibling
      whose annotation in the row is the pattern to match.
    - `.planning/phases/08-registry-docs-tail/08-VERIFICATION.md` — the `gaps:` entry for truth 11,
      including its `missing[]` item, which is the acceptance wording.
  </read_first>
  <action>
    Repairs 08-VERIFICATION.md truth 11 (gap 1). Implements D-05.2's "deprecated aliases marked
    consistently" clause as it was originally intended — 08-04 delivered the annotation for one
    command only.

    In `CLAUDE.md` line 15's `cmd/engram/` Layout row, the operator-tier list currently contains the
    bare item `` `backfill-short-ids` ``. Annotate it so it carries a deprecation marker, matching
    the density and idiom the row already uses for its sibling:

        `backfill-short-ids` (deprecated, use `migrate`)

    That exact form is required for two reasons the gate depends on, so do not paraphrase it into a
    different shape:

    1. The annotation must sit within the same row cell and must reach the word `deprecated` from
       the closing backtick of the command name without crossing another backtick or a `|` cell
       boundary. The gate's window is 40 characters of non-backtick, non-pipe text; ` (` is two.
    2. The command name must remain wrapped in backticks exactly as `` `backfill-short-ids` ``,
       because 08-04's committed row-scoped inventory gate derives its expected name set from
       `cmd/engram/testdata/catalog.golden` and looks each name up as a backticked token in this
       row. Breaking the backticks would turn that green gate red.

    Change nothing else in the row. In particular, leave the `migrate-remap-owner` item and its
    `(alias: \`migrate-set-owner\`, deprecated)` annotation exactly as it stands. 08-VERIFICATION.md
    records the loose use of the word "alias" there as info-level only and explicitly NOT a gap: the
    deprecated-supersession relationship is real, is declared by `migrateSetOwnerCmd.Deprecated`, and
    is pinned by `TestMigrateSetOwnerEquivalentToRemapOwnerMissing` in
    `internal/store/store_test.go`. Two prior reviewers wrongly flagged that line by comparing flag
    shapes. Editing it introduces a defect; do not.

    Also leave `CLAUDE.md` line 97's Memory-contract mention of `engram backfill-short-ids`
    untouched. It is outside this gap and outside this plan.

    Do not add any date, release number, phase number, or "new in"-style phrasing to the row.
    `CLAUDE.md` states the current contract; it never narrates what changed or when (D-05's closing
    constraint). 08-04 committed a gate for exactly this, re-run in `<verify>` below.
  </action>
  <verify>
    <automated>set -e; T=$(mktemp -d); rg -o '^      "name": "[a-z][a-z-]*"' cmd/engram/testdata/catalog.golden | rg -o '"[a-z][a-z-]*"$' | tr -d '"' | sort -u > "$T/catalog_top.txt"; awk 'f && /^$/{exit} f && /^  [a-z]/{print $1} /^Available Commands:$/{f=1}' cmd/engram/testdata/help.golden | sort -u > "$T/help_listed.txt"; comm -23 "$T/catalog_top.txt" "$T/help_listed.txt" > "$T/deprecated.txt"; test -s "$T/deprecated.txt"; row=$(rg -N '^[|] \x60cmd/engram/\x60 [|]' CLAUDE.md); test -n "$row"; misses=""; while IFS= read -r name; do printf '%s' "$row" | rg -q "\x60$name\x60[^\x60|]{0,40}deprecated" || misses="$misses $name"; done < "$T/deprecated.txt"; printf 'deprecated set:'; tr '\n' ' ' < "$T/deprecated.txt"; printf '\nunannotated:%s\n' "$misses"; test -z "$misses"</automated>
    <automated>set -e; row=$(rg -N '^[|] \x60cmd/engram/\x60 [|]' CLAUDE.md); misses=$(rg -o '^      "name": "[a-z][a-z -]*"' cmd/engram/testdata/catalog.golden | rg -o '"[a-z][a-z -]*"$' | tr -d '"' | sort -u | while IFS= read -r name; do printf '%s\n' "$name" | tr ' ' '\n' | while IFS= read -r w; do printf '%s' "$row" | rg -q -- "\x60$w\x60" || printf '%s/%s\n' "$name" "$w"; done; done); printf 'inventory misses: [%s]\n' "$misses"; test -z "$misses"</automated>
    <automated>test "$(git diff -U0 -- CLAUDE.md | rg -o '^[+].*(\bnew in\b|\bas of\b|shipped in|this release)' | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <done>
    - The first gate's `deprecated set:` line prints exactly `backfill-short-ids migrate-set-owner`
      (derived, never typed) and its `unannotated:` line is empty. This gate is RED against `HEAD`
      before the edit — record the observed pre-state (`unannotated: backfill-short-ids`) in the
      SUMMARY, because a gate whose failing state was never observed is a claim, not evidence.
    - 08-04's row-scoped inventory gate (second clause) still reports an empty miss list, proving the
      edit did not disturb the 23-command inventory.
    - 08-04's no-changelog-phrasing gate (third clause) reports `0`.
    - `git diff --name-only` reports `CLAUDE.md` alone.
    - The `migrate-remap-owner` / `migrate-set-owner` item is byte-identical to `HEAD`.
  </done>
</task>

<task type="auto">
  <name>Task 2: State all four soft-hide recall surfaces in the Archived-state paragraph</name>
  <files>CLAUDE.md</files>
  <read_first>
    - `CLAUDE.md` lines 150-184 — the Supersession paragraph (which already names all four surfaces
      correctly) immediately followed by the Archived-state paragraph (which names two). Read both
      together; the second is being brought into agreement with the first.
    - `internal/store/store.go` lines 1120-1135, 1220-1235, 1390-1405, 1625-1640 — the four
      `qdrant.NewIsEmpty("archived_at")` recall-gate sites. This is the authority. Do not re-reason
      the surface list from prose.
    - `docs-site/src/content/docs/reference/memory-record.md` — its `### Archiving` section states
      `soft-hidden from recall (\`search_memory\`, \`list_memory\`, \`search_discovery\`,
      \`list_scheduled\`)`. That page is correct; CLAUDE.md is the one that diverged.
    - `.planning/phases/08-registry-docs-tail/08-VERIFICATION.md` — the `gaps:` entry for truth 12.
  </read_first>
  <action>
    Repairs 08-VERIFICATION.md truth 12 (gap 3). Implements D-05.3, whose acceptance criterion in
    08-04 was explicit cross-page agreement with `reference/memory-record.md`.

    In `CLAUDE.md`'s Archived-state paragraph, the sentence currently reading (across two wrapped
    source lines, around lines 175-176):

        but-still-fetchable-by-id contract: an archived record drops out of
        `search_memory`/`list_memory` but stays reachable by id via `get_memory`.

    must name all four surfaces the store actually gates, in the same slash-joined backticked form
    and the same order the adjacent Supersession paragraph already uses for the identical
    `superseded_by` gate:

        `search_memory`/`list_memory`/`search_discovery`/`list_scheduled`

    Two mechanical constraints on how you write it:

    1. Keep the four-item slash-run on ONE source line. The gate normalizes the paragraph's
       newlines to spaces before matching the run, so a line break falling between two items breaks
       the run into fragments and the set difference goes red on correct prose. `MD013` is disabled
       in `.rumdl.toml`, so a long line is fine here.
    2. Re-wrap only the tail of that sentence. Leave the paragraph's opening sentences and its
       closing sentences — "Archiving is an orthogonal key ..." through "... lives on
       `reference/memory-record.md`, not restated here." — as they stand.

    Do not restate the window-boundary rule, do not add the canonical four state words again, and do
    not import `reference/memory-record.md`'s longer phrasing wholesale. This paragraph deliberately
    compresses; the defect is that it compressed away two of four surfaces, not that it compresses.

    Do not add any date, release number, phase number, or "new in"-style phrasing (D-05's closing
    constraint; gated below).
  </action>
  <verify>
    <automated>set -e; T=$(mktemp -d); awk '/^Archived state:/{f=1} f{print} f&&/^$/{exit}' CLAUDE.md | tr '\n' ' ' | rg -o '\x60[a-z_]+\x60(/\x60[a-z_]+\x60)+' | rg -o '[a-z_]+' | sort -u > "$T/archived.txt"; awk '/A superseded record is soft-hidden/{f=1} f{print} f&&/^$/{exit}' CLAUDE.md | tr '\n' ' ' | rg -o '\x60[a-z_]+\x60(/\x60[a-z_]+\x60)+' | rg -o '[a-z_]+' | sort -u > "$T/superseded.txt"; printf 'archived paragraph: '; tr '\n' ' ' < "$T/archived.txt"; printf '\nsupersession paragraph: '; tr '\n' ' ' < "$T/superseded.txt"; printf '\nsymmetric difference: ['; comm -3 "$T/archived.txt" "$T/superseded.txt" | tr -d '\t\n'; printf ']\n'; test -z "$(comm -3 "$T/archived.txt" "$T/superseded.txt")"; gates=$(rg -v '^\s*//' internal/store/store.go | rg -o 'qdrant[.]NewIsEmpty[(]"archived_at"[)]' | wc -l | tr -d ' '); named=$(wc -l < "$T/archived.txt" | tr -d ' '); printf 'store gate sites: %s / surfaces named: %s\n' "$gates" "$named"; test "$gates" = "$named"</automated>
    <automated>test "$(git diff -U0 -- CLAUDE.md | rg -o '^[+].*(\bnew in\b|\bas of\b|shipped in|this release)' | wc -l | tr -d ' ')" = "0"</automated>
    <automated>test "$(git diff --name-only | tr -d ' ')" = "CLAUDE.md"</automated>
  </verify>
  <done>
    - The set-difference gate prints an empty symmetric difference: the Archived paragraph's
      slash-run names exactly the set the Supersession paragraph names.
    - `store gate sites` and `surfaces named` both print `4`, from independent derivations — the
      count comes from the comment-stripped `qdrant.NewIsEmpty("archived_at")` occurrences in
      `internal/store/store.go`, not from a literal in this plan. The comment filter is load-bearing:
      the unfiltered grep returns 6, because `store.go` mentions the gate in prose at two places.
    - This gate is RED against `HEAD` before the edit — record the observed pre-state
      (`symmetric difference: [list_scheduled search_discovery]`, `4 / 2`) in the SUMMARY.
    - `git diff --name-only` reports `CLAUDE.md` alone; no Go, generated, or docs-site file moved.
  </done>
</task>

</tasks>

<threat_model>
No threat model applies. This plan's entire change surface is two sentences in a repository-local
markdown routing document. It introduces no trust boundary, no input path, no dependency, no
credential, and no code — `git diff --name-only` reporting `CLAUDE.md` alone is gated in Task 2.
Manufacturing a STRIDE register for it would be noise, not analysis.
</threat_model>

<verification>
After both tasks:

1. `task lint` is clean (rumdl covers `CLAUDE.md`).
2. `git diff --name-only` reports exactly `CLAUDE.md`.
3. Both derived gates from Task 1 and Task 2 pass, and both were observed RED against `HEAD` first.
4. 08-04's two committed gates (row inventory, no-changelog-phrasing) still pass.
5. Read the `cmd/engram/` Layout row and the Archived-state paragraph whole and confirm they read as
   routing prose in the file's existing idiom — the backstop truth. Quote both in the SUMMARY.
</verification>

<success_criteria>
- Every command the goldens derive as deprecated is annotated `deprecated` in the Layout row; the
  gate's unannotated list is empty and the gate names no command literally.
- The Archived-state paragraph's soft-hide surface set equals the Supersession paragraph's set and
  its size equals the store's live `archived_at` gate-site count.
- `CLAUDE.md` is the only file changed.
- The `migrate-set-owner` alias wording and `CLAUDE.md` line 97 are byte-identical to `HEAD`.
</success_criteria>

<output>
Create `.planning/phases/08-registry-docs-tail/08-05-SUMMARY.md` when done.

Record in it: the observed RED pre-state of both gates (verbatim output), the derived deprecated set,
the two corrected sentences quoted whole, and confirmation that the `migrate-set-owner` item was left
untouched on purpose.
</output>
