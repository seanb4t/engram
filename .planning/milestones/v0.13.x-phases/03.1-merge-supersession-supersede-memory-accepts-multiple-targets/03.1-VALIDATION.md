---
phase: 03.1
slug: merge-supersession-supersede-memory-accepts-multiple-targets
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: validated
nyquist_compliant: true
wave_0_complete: true
created: 2026-08-10
validated: 2026-08-11
---

# Phase 03.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Go stdlib `testing` + `testcontainers-go/modules/qdrant` |
| **Config file** | none — table/function-based Go tests; harness bootstrap is `TestMain` (`internal/store/store_test.go:74-112`) |
| **Quick run command** | `go test ./internal/store/... ./internal/server/... -run Supersede -v` |
| **Full suite command** | `go test ./... -count=1` (per `Taskfile.yaml:54`) |
| **Estimated runtime** | ~90 seconds full suite; quick run dominated by Qdrant container startup |

---

## Sampling Rate

- **After every task commit:** Run `go test ./internal/store/... ./internal/server/... -run Supersede -v`
- **After every plan wave:** Run `go test ./... -count=1`
- **Before `/gsd-verify-work`:** Full suite must be green, PLUS the REQ-merge-atomicity TOCTOU proof must be run explicitly and observed to run — not skipped — against the pinned `qdrant/qdrant:v1.18.2` image. Mirror the existing version-guard discipline at `store_test.go:34-39` (`qdrantTOCTOUVerifiedVersion`).
- **Max feedback latency:** 120 seconds

---

## Per-Task Verification Map

Task IDs are assigned when PLAN.md files are written; this map is seeded at requirement
granularity and MUST be refilled with real task IDs and real shipped test names by
`/gsd-validate-phase`. Test names below are the planner's intent, not yet facts on disk.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | Tests Matched | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD | TBD | TBD | REQ-merge-supersession | — | Multi-target merge stores one survivor and back-stamps every target; single-live-head still rejects an already-superseded target | integration (real Qdrant) | `go test ./internal/store/... -run TestSupersedeMulti -v` | 15 | ✅ green |
| TBD | TBD | TBD | REQ-merge-supersession | — | Handler rejects a rule target, echoes original input on not-found (404-indistinguishability), and dedupes duplicate/short_id-resolved targets before locking | unit | `go test ./internal/server/... -run TestSupersedeMemory -v` | 29 | ✅ green |
| TBD | TBD | TBD | REQ-merge-atomicity | — | A target deleted between preflight and the multi-ID `SetPayload` leaves NO stamped-but-orphaned target and no dangling `superseded_by` after compensation (D-15) | integration (real Qdrant, forced TOCTOU) | `go test ./internal/store/... -run TestSupersedeMultiTOCTOU -v` | 2 | ✅ green |
| TBD | TBD | TBD | REQ-merge-idempotency | — | Same key + same content + same target set replays; same key + different target set conflicts; replay check precedes the already-superseded preflight (D-14) | unit + integration | `go test ./internal/server/... -run TestSupersedeMemoryIdempotency -v`; `go test ./internal/store/... -run 'TestSupersedeConcurrentKeyedDisjointTargetsCannotBothLand\|TestPayloadRoundTripsIdempotencyFingerprint' -v` | 5 | ✅ green |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `internal/store/store_test.go` — `TestSupersedeMulti*` family: multi-target happy path, per-target already-superseded rejection, N-target interaction with `List`/`Search` soft-hide
- [ ] `internal/store/store_test.go` — `TestSupersedeMultiTOCTOU`: the direct REQ-merge-atomicity proof. Generalizes `TestSupersedeTOCTOU` (`store_test.go:3527-3579`) to N≥3 with one target deleted mid-window, and MUST additionally assert no orphaned `superseded_by` survives compensation
- [ ] `internal/server/tools_test.go` — `TestSupersedeMemoryMulti*` family: dedup/self-deadlock guard, per-target rule and ownership gates, INV-01 404-indistinguishable multi-fault echo, staged-by-class preflight ordering (D-11)
- [ ] `internal/server/idempotency_test.go` — merge-fingerprint tests: order-independence over the target set, content-fingerprint reuse, key-reuse-with-different-target-set conflict
- [ ] Framework install: none — `testcontainers-go/modules/qdrant` and stdlib `testing` are already vendored and used by the exact test files this phase extends

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Cross-chunk partial failure (target set > `PAYLOAD_OP_BATCH_SIZE` = 32) | REQ-merge-atomicity | Provoking a mid-sequence failure on a *later* chunk requires either >32 real targets or server-side fault injection Qdrant does not expose. D-15 accepts this class as covered-by-reconciliation but not proven-by-construction | Construct a >32-target merge against a live Qdrant, delete a target whose index falls in chunk 2, confirm the reconciliation pass clears every dangling `superseded_by` across both chunks and that the documented limitation matches observed behavior |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 120s
- [x] **Every `-run` pattern above matched a real test.** `go test -run <pattern>` exits 0 with "no tests to run" when the pattern matches nothing, so exit status alone certifies a fictional test as green. For each command, count `--- PASS` lines under `-v` and assert the count is non-zero and equals the expected test count — never trust the exit code. (Repo gotcha `gfh6q1ack4`: v0.13.x Phase 3 seeded three test names that never shipped and the audit reported all green.) Re-resolved 2026-08-11 against `go test -list '.*' ./...` at HEAD; see the Validation Audit below for method.
- [ ] `TestSupersedeMultiTOCTOU` was observed to RUN, not skip, against `qdrant/qdrant:v1.18.2` — not re-executed during this reconciliation pass; only re-resolved via `go test -list`, per the audit note below
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-08-11

---

## Validation Audit 2026-08-11

Every one of this file's five pattern elements was re-resolved against `go test -list '.*' ./...`
run fresh at HEAD, not trusted from a prior transcript or from exit status. Each element's
resolved count:

| Element | Resolved count |
|---|---|
| `TestSupersedeMulti` | 15 |
| `TestSupersedeMemory` | 29 |
| `TestSupersedeMultiTOCTOU` | 2 |
| `TestSupersedeMemoryIdempotency` | 3 |
| `TestSupersedeConcurrentKeyedDisjointTargetsCannotBothLand` | 1 |
| `TestPayloadRoundTripsIdempotencyFingerprint` | 1 |

The REQ-merge-idempotency row's store-side command named a test that never shipped — a plan-time
seed nobody repointed when the phase closed. It has been repointed to the two funcs that did ship,
both confirmed present in `internal/store/store_test.go`. Per D-08 the bar asserted for every
element is at least one match, never an exact count, so the counts above are a cross-check only,
not a pinned assertion.

Only the top-level element of a slash-separated `TestX/subtest` pattern is verifiable by this
method — `go test -list` does not enumerate subtests. None of this file's patterns use that form,
so it does not apply here, but the limitation is recorded for future reconciliation passes.

The seeded `Task ID` / `Plan` / `Wave` cells (all `TBD`) were deliberately left as-is rather than
backfilled with reconstructed values; assigning them now would be the same class of unverified
claim as the row this audit repairs.
