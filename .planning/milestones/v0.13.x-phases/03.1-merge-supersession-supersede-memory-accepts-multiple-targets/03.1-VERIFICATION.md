---
phase: 03.1-merge-supersession-supersede-memory-accepts-multiple-targets
verified: 2026-08-11T16:49:33Z
status: passed
score: 11/11 must-haves verified
behavior_unverified: 0
overrides_applied: 0
---

# Phase 03.1: Merge Supersession Verification Report

**Phase Goal:** `supersede_memory` accepts multiple `supersedes` targets, so consolidating a
duplicate set links every predecessor to one surviving record — history preserved for all of
them, and no `delete_memory` anywhere in the merge path.
**Verified:** 2026-08-11T16:49:33Z
**Status:** passed
**Re-verification:** No — initial verification

## Method

Read all 6 PLAN/SUMMARY pairs, `03.1-00-SUMMARY.md`'s binding developer ruling, `03.1-REVIEW.md`
(1 Critical + 2 Warning findings), and `03.1-REVIEW-FIX.md`. Cross-checked every commit hash cited
in the SUMMARYs against `git log`/`git show`. Ran the full `internal/store` + `internal/server`
suite once, under `-race`, against a real dockerized Qdrant `v1.18.2` (`go test
./internal/store/... ./internal/server/... -race -count=1 -v`): **543 `--- PASS`, 0 `--- FAIL`, 0
`--- SKIP`** across both packages (19.6s store, 8.1s server). Individually re-ran
`TestSupersedeConcurrentKeyedDisjointTargetsCannotBothLand` (the CR-01 regression test) standalone
under `-race` and confirmed 1 PASS. Grepped every named test cited as evidence in the SUMMARYs
against the saved full-run log and confirmed a non-zero `--- PASS` count for each (never trusting
`go test -run` exit status alone). Ran `go build ./...`, `go vet ./...`, and `golangci-lint run
./...` (0 issues). Read the actual diffs of the three review-fix commits (`9ef8148f`, `b18c11b6`,
`a8ab6b69`) rather than trusting the fix report's prose. Read the shipped documentation content
directly (never relying on `task lint:markdown`, which is excluded from `docs-site/**`).

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `supersede_memory` accepts a set of targets and, in one logical operation, stores one new record and back-stamps `superseded_by` on every target — additive, no breaking MCP schema change (SC1) | ✓ VERIFIED | `internal/store/store.go:2184` `Store.Supersede(ctx, newMem, vec, targets []string, subj)`; `TestSupersedeMultiStamp`, `TestSupersedeMemoryMultiTarget` PASS. Schema: `supersedes` is `array of string`, `minItems: 1`, no `maxItems` — additive over the pre-phase scalar form (`internal/server/tools.go:2223` `supersedeMemoryInputSchema`). |
| 2 | Single-live-head rule enforced per target; rejection names every offending target in one response, not just the first, without breaking 404-indistinguishability (SC2) | ✓ VERIFIED | `TestSupersedeMultiAlreadySuperseded`, `TestSupersedeMemoryMultiNamesAllNotFoundTargets`, `TestSupersedeMemoryMultiIndistinguishable` (whole-rejection string comparison after normalizing the target substring — not a sentinel-only check, `tools_test.go:3429-3432`), `TestSupersedeMemoryAmbiguousShortIDIndistinguishable` — all PASS. |
| 3 | A partially-applied merge does not survive in the terminal state, proven against a real Qdrant with a forced mid-sequence failure; claim explicitly scoped to NOT "never observable at any instant"; cross-chunk class documented, not left implicit (SC3) | ✓ VERIFIED | `TestSupersedeMultiTOCTOU` / `TestSupersedeMultiTOCTOUNoDanglingLink` — real pinned `qdrant/qdrant:v1.18.2`, forced partial-application via a real second raw `SetPayload`, both PASS. `Store.Supersede`'s doc comment (`store.go:2129-2142`) states the terminal-state-only scope verbatim, with no "never observable" language anywhere in the package. `const qdrantPayloadOpBatchSize = 32` is documented and `reconcileSupersedeFailure` re-reads the FULL target set across every chunk (`store.go:2368-2384`), not one chunk. |
| 4 | `idempotency_key` is supported, replay fingerprint keyed on content AND target set, replay check ordered before the already-superseded preflight (SC4) | ✓ VERIFIED | `mergeFingerprint` composes `contentFingerprint` (`idempotency.go`); `checkIdempotentMergeReplay` sits between `resolveAndAuthorizeSupersedeTargets` and `validateSupersedeTargetState` as two separate statements (`tools.go:2054-2084`); `TestSupersedeMemoryIdempotencyReplay`, `TestSupersedeMemoryIdempotencyDifferentTargetSetConflicts`, `TestSupersedeMemoryReplayPrecedesAlreadySupersededPreflight` — all PASS. |
| 5 | No `delete_memory` (or equivalent point deletion of a live/linked record) anywhere in the caller-reachable merge path — the reconciliation compensating-delete only ever targets the survivor this SAME call just minted, never a target | ✓ VERIFIED | `reconcileSupersedeFailure` removes `survivorID` via `defaultDeletePoint` (`store.go:2019-2029`), a raw ungated `client.Delete` — explicitly NOT `Store.Delete` (the `delete_memory`-backing, authz-gated verb) — with a doc comment stating "D-09's 'no delete_memory in the merge path' governs the caller-facing verb, not this internal cleanup." `internal/server/tools.go`'s `supersedeMemory` handler contains no call to `Store.Delete` or any delete verb. |
| 6 | Binding ruling PD-07 (target-set cap DROPPED) implemented exactly | ✓ VERIFIED | `rg -o 'maxSupersedeTargets\b' internal/` → 0 hits (only a comment in a test file referencing its absence). No `maxItems` in schema. No count branch in Class 1 (`tools.go:1874-1885`, checks only emptiness and per-entry blankness/length — never a `len(inputs)` bound branch). `docs-site/.../tools.md:259` states "There is no maximum target count." |
| 7 | Binding ruling PD-06 (collapse-ambiguous) implemented exactly | ✓ VERIFIED | `resolveAndAuthorizeSupersedeTargets` Class 2 collapses resolution-not-found, resolution-ambiguous, and fetch-not-found/not-owned into one `store.ErrNotFound`-sentinel bucket, evaluated before any class that can report on a specific record. `TestSupersedeMemoryStagedByClassOrdering`, `TestSupersedeMemoryAmbiguousShortIDIndistinguishable` PASS. |
| 8 | Binding ruling PD-01 (dedupe-silently) implemented exactly | ✓ VERIFIED | Dedupe runs over resolved UUIDs inside `resolveAndAuthorizeSupersedeTargets` (never caller spellings — required because the per-target lock is a non-reentrant `sync.Mutex`); `lockTargets` sorts+dedupes. `TestSupersedeMultiDuplicateTargets`, `TestSupersedeMemoryIdempotencyReorderedSetReplays` (covers a duplicate in the same case) PASS. |
| 9 | Binding ruling PD-02 (allow-heterogeneous) implemented exactly | ✓ VERIFIED | No homogeneity check exists anywhere in `resolveAndAuthorizeSupersedeTargets`/`validateSupersedeTargetState`; `docs-site/.../tools.md` states "A merge may span different scopes, categories, and visibilities." |
| 10 | CR-01 (code review, Critical): a keyed multi-target merge sharing an `idempotency_key` but naming disjoint target sets can no longer silently corrupt the supersession graph (both calls landing, one target's `superseded_by` dangling with no error/log) | ✓ VERIFIED | Fix (`9ef8148f`) locks the union of `targets` + `newMem.ID` for keyed calls while keeping the back-stamp/getWritable stamp set targets-only, plus a fingerprint re-check under the lock before `Upsert`. `TestSupersedeConcurrentKeyedDisjointTargetsCannotBothLand` asserts GRAPH STATE (exactly 1 success + 1 conflict; survivor's `Supersedes` equals only the winner's set; loser's target `SupersededBy` stays nil) — independently re-run standalone under `-race`: 1 PASS. |
| 11 | WR-01/WR-02 (code review, Warning): `supersedes` entries carry a per-entry byte bound without reintroducing a target-count cap; `errors.md`'s "No value echo" self-contradiction resolved; `contentFingerprint`'s field-sensitivity test covers `Citations` | ✓ VERIFIED | `maxSupersedeTargetBytes = 256` is a per-ENTRY bound only (`tools.go:736-748`) — no `maxSupersedeTargets` count constant added, no `maxItems`. `errors.md:163-175` now states the one bounded exception rather than contradicting the multi-target section. `TestContentFingerprintFieldSensitivity` now has a `"citations"` case. `TestSupersedeMemoryOversizedTargetRejected` PASS. |

**Score:** 11/11 truths verified (0 present, behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `internal/store/store.go` | `Memory.Supersedes []string`, `Store.Supersede` (multi-target), `lockTargets`, `reconcileSupersedeFailure`, `defaultDeletePoint`, `setPayloadKeys`/`deletePoint` seams, `MultiTargetError` | ✓ VERIFIED | All present; build/vet/lint clean |
| `internal/server/tools.go` | `resolveAndAuthorizeSupersedeTargets`, `validateSupersedeTargetState`, `renderTargetRejection`, `checkIdempotentMergeReplay`, `resolveLostMergeRace`, `supersedeMemoryInputSchema` | ✓ VERIFIED | All present, each with exactly 1 definition site |
| `internal/server/idempotency.go` | `mergeFingerprint` | ✓ VERIFIED | Present, composes `contentFingerprint` |
| `internal/server/supersededocs_test.go` | `TestSupersedeDocsMatchShippedContract` drift gate | ✓ VERIFIED | 4/4 sub-tests PASS |
| `docs-site/src/content/docs/reference/{errors,tools,memory-record}.md`, `CLAUDE.md`, `skill/engram/skills/curating-memory/SKILL.md` | Published multi-target/idempotency contract | ✓ VERIFIED | Content read directly; matches shipped code and the PD-01/02/06/07 ruling |

### Key Link Verification

| From | To | Via | Status |
|------|-----|-----|--------|
| `tools.go` handler | `store.go` `Store.Supersede` | `d.st.Supersede(ctx, m, vec, targetIDs, c.Subj)` | ✓ WIRED |
| `tools.go` `deps.supersedeMemory` | `checkIdempotentMergeReplay` | called between authorize and state stages (two statements, `tools.go:2064-2084`) | ✓ WIRED |
| `store.go` `Store.Supersede` | `reconcileSupersedeFailure` | invoked in the `setKeys(...)` error branch, before returning | ✓ WIRED |
| `internal/server/supersededocs_test.go` | `renderTargetRejection` | worked examples asserted byte-for-byte against production output | ✓ WIRED (non-vacuous — verified by regression checks recorded in the plan 05 SUMMARY, spot-checked one myself) |

### Behavioral Spot-Checks / Test Execution Evidence

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full store+server suite | `go test ./internal/store/... ./internal/server/... -race -count=1 -v` | 543 `--- PASS`, 0 FAIL, 0 SKIP; `ok` both packages | ✓ PASS |
| CR-01 regression (graph-state assertions) | `go test ./internal/store/... -run '^TestSupersedeConcurrentKeyedDisjointTargetsCannotBothLand$' -race -v` | 1 `--- PASS` (0.07s) | ✓ PASS |
| `go build ./...` | — | clean | ✓ PASS |
| `go vet ./...` | — | clean | ✓ PASS |
| `golangci-lint run ./...` | — | 0 issues | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plans | Status | Evidence |
|-------------|-------------|--------|----------|
| REQ-merge-supersession | 03.1-01, 03.1-03, 03.1-05 (00) | ✓ SATISFIED | Truths 1, 2, 6-9 above |
| REQ-merge-atomicity | 03.1-02, 03.1-05 | ✓ SATISFIED | Truth 3, plus CR-01 fix (truth 10) closes the one gap that would have undermined it |
| REQ-merge-idempotency | 03.1-04, 03.1-05 | ✓ SATISFIED | Truth 4, plus CR-01 fix (truth 10) |

No orphaned requirements: `REQUIREMENTS.md`'s "Phase 03.1" rows (`REQ-merge-supersession`,
`REQ-merge-atomicity`, `REQ-merge-idempotency`) exactly match the union of `requirements:` fields
declared across all 5 implementing plans' frontmatter. (`REQUIREMENTS.md`'s checkbox/table rows
still show `[ ]`/`Pending` — this is tracking metadata the GSD workflow updates in a later step,
not evidence of an implementation gap.)

### Anti-Patterns Found

None blocking. `rg` for `TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER` across all phase-touched files found
one false-positive hit (`CLAUDE.md:165`, "Do not use markdown TODO lists" — a prohibition
statement, not a debt marker). No stub returns, no hardcoded-empty data flowing to output, no
console-log-only implementations.

**ℹ️ Info (not a gap):** `gofmt -l` flags `internal/server/tools_test.go` (plus two unrelated,
non-phase files `cmd/engram/client_list_test.go`, `cmd/engram/migrate_test.go`) for a minor
line-wrap difference. `golangci-lint run ./...` (the repo's actual lint gate) reports 0 issues, and
`03.1-05-SUMMARY.md` already documents this exact class of repo-wide `task fmt` churn/drift as
observed-and-declined-to-file by the developer. Not phase-introduced, not build/lint-gating, no
functional effect.

### Human Verification Required

None. The one checkpoint this phase carried (`03.1-05` Task 4, "verify the shipped contract
matches plan 00's ruling") was already presented to and approved by the developer on 2026-08-11,
with the SUMMARY recording the specific check evidence (anchor counts, `rg` greps) rather than
just an approval stamp. I independently re-derived the same four checks (PD-07/06/01/02) against
the current tree and got matching results — see truths 6-9 above.

### Gaps Summary

None. All four ROADMAP success criteria are met with real, behaviorally-run tests (not presence
checks) against a real pinned Qdrant. The one Critical finding from the post-execution code review
(CR-01: a genuine data-integrity race between the newly-added keyed-multi-target interaction) was
fixed with a graph-state-asserting regression test that I independently re-ran and confirmed green
under `-race`. Both Warnings were fixed and independently spot-checked. The negative half of the
phase goal — no `delete_memory` in the merge path — holds: the only deletion in the entire code
path is a compensating removal of the record the SAME call just created, routed through an internal
ungated primitive explicitly documented as distinct from the `delete_memory`-backing `Store.Delete`
verb.

---

*Verified: 2026-08-11T16:49:33Z*
*Verifier: Claude (gsd-verifier)*
