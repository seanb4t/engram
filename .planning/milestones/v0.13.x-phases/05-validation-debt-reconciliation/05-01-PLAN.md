---
phase: 05-validation-debt-reconciliation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/03.1-merge-supersession-supersede-memory-accepts-multiple-targets/03.1-VALIDATION.md
  - .planning/phases/01-interface-enforceability/01-VALIDATION.md
  - .planning/phases/02-interface-discoverability/02-VALIDATION.md
  - .planning/phases/04-spine-curation-semantic-skill/04-VALIDATION.md
autonomous: false
requirements: [REQ-nyquist-reconciled]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "No `-run` pattern in any v0.13.x VALIDATION.md names a test that does not exist at HEAD: every top-level pattern element in 01, 02 and 03.1 resolves to at least one name in `go test -list '.*' ./...` (D-01, D-08)"
    - "03.1's REQ-merge-idempotency store-side command names tests that shipped, and each reconciled row records the number of tests its pattern resolved to — the asserted bar is >=1, never an exact count (D-08)"
    - "01, 02 and 03.1 read `status: validated` with `nyquist_compliant: true`, and each carries a dated audit note recording how that was established (D-02)"
    - "04 reads the documented PARTIAL state — `status: validated` with `nyquist_compliant: false` — and its REQ-consent-adversarial-proof row still reads `pending`, so the one genuinely unproven requirement in this milestone stays visibly unproven (D-03)"
    - "A future reader of any reconciled file can tell that a command was repointed and why, because the drift is written down rather than quietly corrected (07-VALIDATION.md:137 model)"
    - "Nothing under `.planning/milestones/**` is modified (D-10)"
  artifacts:
    - path: ".planning/phases/03.1-merge-supersession-supersede-memory-accepts-multiple-targets/03.1-VALIDATION.md"
      provides: "Repointed REQ-merge-idempotency command, four reconciled rows with resolved counts, validated frontmatter"
      contains: "Validation Audit 2026-08-11"
    - path: ".planning/phases/01-interface-enforceability/01-VALIDATION.md"
      provides: "Five reconciled rows with resolved counts, validated frontmatter"
      contains: "Validation Audit 2026-08-11"
    - path: ".planning/phases/02-interface-discoverability/02-VALIDATION.md"
      provides: "Reconciled rows with resolved counts, validated frontmatter"
      contains: "Validation Audit 2026-08-11"
    - path: ".planning/phases/04-spine-curation-semantic-skill/04-VALIDATION.md"
      provides: "Two structural rows reconciled; cold-read row deliberately left open"
      contains: "nyquist_compliant: false"
  key_links:
    - from: ".planning/phases/03.1-merge-supersession-supersede-memory-accepts-multiple-targets/03.1-VALIDATION.md"
      to: "internal/store/store_test.go"
      via: "the REQ-merge-idempotency store-side -run pattern must name a func that exists in this file"
      pattern: "TestSupersedeConcurrentKeyedDisjointTargetsCannotBothLand"
    - from: ".planning/phases/03.1-merge-supersession-supersede-memory-accepts-multiple-targets/03.1-VALIDATION.md"
      to: "internal/store/store_test.go"
      via: "the second store-side element of the same pattern"
      pattern: "TestPayloadRoundTripsIdempotencyFingerprint"
  prohibitions:
    - requirement_id: REQ-nyquist-reconciled
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT flip the REQ-consent-adversarial-proof row, or 04's nyquist_compliant flag, to a passing state. That requirement's cold read terminated at NOT-OBTAINED, the user accepted the non-result, and 04-VERIFICATION.md's override explicitly does not assert it was proven. Making the record look complete here would manufacture the exact false green this phase exists to close"
      resolution: null
      reason: null
    - requirement_id: REQ-nyquist-reconciled
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT modify anything under `.planning/milestones/**`. Archived milestone artifacts are an immutable audit record; v0.12.x Phase 7's unresolvable command stays exactly as written, because that file already documents its own drift in prose and rewriting it would destroy the evidence that the drift happened"
      resolution: null
      reason: null
    - requirement_id: REQ-nyquist-reconciled
      category: values
      status: resolved
      verification: judgment
      statement: "MUST NOT ship any durable mechanism that audits planning artifacts — no CI gate, no `task` target, no committed script, no Go test that reads `.planning/**`, and no reconciliation report artifact. The user cut this scope explicitly; a plan that reintroduces it has misread the phase"
      resolution: null
      reason: null
    - requirement_id: REQ-nyquist-reconciled
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT quietly repoint a drifted command. Every repoint records that the original command was drifted, what it was repointed to, and how the replacement was resolved — the record must let a later reader see the correction, not just its result"
      resolution: null
      reason: null
---

<objective>
Reconcile this milestone's four `status: draft` validation records so each states what is
actually true at HEAD — reconciled where the evidence supports it, still open where it does not.

Purpose: a validation record that certifies a test which never shipped is worse than no record,
because it reads as coverage. One of this milestone's records does exactly that today.
Output: four edited `VALIDATION.md` files under `.planning/phases/`. No code changes, no new
mechanism (D-09), no report artifact (D-11).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-validation-debt-reconciliation/05-CONTEXT.md
@.planning/phases/03-spine-curation-structural-cli/03-VALIDATION.md
</context>

<planner_notes>
## Discretion items resolved (05-CONTEXT.md `### Claude's Discretion`)

1. **Re-run the sweep, narrowly.** The sweep was re-run at plan time over the active
   milestone's five `VALIDATION.md` files against HEAD, and it reproduces 05-CONTEXT.md's
   numbers exactly: 01 = 5/5 elements resolve, 02 = 14/14, 03 = 14/14, 03.1 = 4/5 with the
   one stale name. `go test -list '.*' ./...` reports 1,023 names at HEAD. Each task below
   re-runs the narrow half it depends on, because `status: validated` is itself an unverified
   claim and certifying one from a transcript is the failure mode this phase exists to close.
   The archived v0.12.x half is NOT re-run: D-10 makes it immutable, so a finding there would
   be unactionable, and D-07's per-milestone reference tree means resolving it against HEAD
   would manufacture phantom drift.

2. **Row marker: `green`, in the shape `/gsd-validate-phase` already used on this
   milestone.** `03-VALIDATION.md` is the in-repo precedent for a reconciled record: its map
   carries a `Tests Matched` count column with `green` statuses, and a dated
   `## Validation Audit` section records the method in prose. Reusing that shape means no
   invented marker glyph and no new structure — and its count column is exactly where D-08's
   resolved count belongs.

3. **Scope held to the frontmatter, the evidence column and a dated audit note.** Task
   IDs / Plan / Wave cells that were seeded `TBD` stay `TBD`, and each audit note says so.
   Backfilling them is real work nobody asked for, and inventing them would be the same class
   of unverified claim as the row this phase is repairing.

## A finding that contradicts D-03's premise — read before Task 3

D-03 says 04's two structural rows "have commands and passed". One of them does not pass at
HEAD. `git diff --stat 72a32c58..HEAD -- internal/ cmd/ proto/ gen/` returns
`internal/server/verbtabledocs_test.go | 79 +++++`, landed by commit `a2599027`
("test(04): IN-01 bind curating-spine verb table to curating-memory's") during phase 04's own
code-review fix pass. The row is red as written, and it will stay red — plan 05-02 edits a file
under `internal/`, which widens the same range further.

The forbidden-tool row genuinely passes: its negative half returns 0 and its positive half
finds 13 references across 6 distinct allowed tools.

Task 3 is a blocking decision because a locked user decision rests on a premise that turned out
to be false, and choosing how to record it is precisely the false-green judgment this phase
exists to protect. Reversibility: reversible — one row in one planning document.
</planner_notes>

<artifacts_produced>
## Artifacts this phase produces

**New file (authored by the planner, not the executor):**

- `.planning/phases/05-validation-debt-reconciliation/05-VALIDATION.md` — Phase 5's own validation
  record. It exists because research was skipped this run, so nothing else would have created one,
  and a Phase 5 with no validation record would be the exact defect this phase closes. It is a
  record, not a mechanism: it ships no CI gate, no `task` target and no committed script.

**No new symbols.** This phase creates no Go type, function, method, CLI flag, config field, proto
message, MCP tool or struct field. Every code-tree edit in the phase is a comment inside an
existing test file. Nothing here changes the binary, and the `EngramService` and MCP tool contracts
are untouched.

**Files modified across both plans:** four `VALIDATION.md` records under `.planning/phases/`
(01, 02, 03.1, 04), `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`,
`internal/retrievaleval/retrieval_eval_test.go` (comments only) and
`docs-site/src/content/docs/guides/embedding-instructions.md` (one table cell).
</artifacts_produced>

<flagged_assumptions>
The deterministic edge probe returned six items for this phase, all `unresolved`. Per the no-silent-
drop rule none is dropped and none is auto-resolved with a backstop. The four belonging to
`REQ-nyquist-reconciled` are recorded here; the two belonging to `REQ-citation-fixture-355` are in
`05-02-PLAN.md`.

- **adjacency — when two things are exactly equal or just touch, do they merge, collide, or
  separate?** Inapplicable as posed: there is no data structure in this phase. The nearest real
  analogue is pattern adjacency — `TestSupersedeMemoryIdempotency` and a hypothetical
  `TestSupersedeMemoryIdempotencyExtra` both match a prefix pattern. **Assumed:** prefix matching
  is intended and correct, and per D-08 the asserted bar is at least one match rather than an exact
  count, so an adjacent sibling test is a feature and not a collision.
- **empty / single-element / null input.** The real empty case is a pattern element that resolves
  to zero tests, which is this phase's entire subject. **Assumed:** zero is always a halt-and-report
  condition, never something to route around. This one is not left as a bare assumption — both
  Tasks 1 and 2 carry it as an explicit instruction, and it is the paired-positive discipline the
  acceptance criteria enforce.
- **ordering — when elements compare equal, is output order specified and stable?** Inapplicable:
  rows are edited in place and no output is sorted or emitted. **Assumed:** row order in every
  edited table stays exactly as it is; no task reorders a row.
- **concurrency — if interrupted or run in parallel, what is guaranteed?** Partly real. The two
  plans in this phase are both wave 1 and may run in parallel, and their `files_modified` sets are
  disjoint, so neither can clobber the other. **Assumed:** if a task is interrupted mid-file, the
  recovery is to re-read the file and redo the edit — every edit is idempotent and no task depends
  on a partially-applied prior state. Any `git commit` in this phase must carry an explicit
  pathspec, because another agent may share the working directory (repo rule `n6m4as49mr`).
</flagged_assumptions>

<tasks>

<!-- planner-discipline-allow: TestSupersedeIdempotency -->
<!-- planner-discipline-allow: status: draft -->

<task type="tracer">
  <name>Task 1: Repoint 03.1's fictional test name and reconcile the file end-to-end</name>
  <files>.planning/phases/03.1-merge-supersession-supersede-memory-accepts-multiple-targets/03.1-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/03.1-merge-supersession-supersede-memory-accepts-multiple-targets/03.1-VALIDATION.md` (the file being edited — read all of it, the four rows are lines 47-50)
    - `.planning/phases/05-validation-debt-reconciliation/05-CONTEXT.md` (D-01, D-08, and the four traps in `<code_context>`)
    - `.planning/phases/03-spine-curation-structural-cli/03-VALIDATION.md` lines 56-72 and 156-180 (the reconciled-record shape this task copies: a `Tests Matched` column plus a dated `## Validation Audit` section)
  </read_first>
  <behavior>
    - The store-side half of the REQ-merge-idempotency row names two funcs that exist in `internal/store/store_test.go`
    - Every one of the file's five pattern elements resolves to at least one name reported by `go test -list`
    - The four rows carry the count each pattern resolved to, and none of them reads pending
    - A reader can see that the original command was drifted and what replaced it
  </behavior>
  <action>
    Implements D-01 (repoint the fictional name), D-02's `03.1` half (flip the frontmatter) and
    D-08 (record the count, assert at least one).

    This is the thin end-to-end slice: one file, carried through every step the other three
    files will repeat — resolve, repoint, record the count, flip the frontmatter, write the
    drift down. Do not start Task 2 until this file is committed.

    First re-resolve, do not trust these numbers. Run `go test -list '.*' ./... > /tmp/head-tests.txt`
    once (it builds every package; allow a few minutes) and check each element with
    `rg -q "^<element>" /tmp/head-tests.txt`. Expected at plan time: `TestSupersedeMulti` 15,
    `TestSupersedeMemory` 29, `TestSupersedeMultiTOCTOU` 2, `TestSupersedeMemoryIdempotency` 3,
    `TestSupersedeConcurrentKeyedDisjointTargetsCannotBothLand` 1,
    `TestPayloadRoundTripsIdempotencyFingerprint` 1. Counts are a cross-check only — per D-08 the
    asserted bar is at least one, never an exact number, because pinning a count red-builds on an
    ordinary sibling-test addition. If an element resolves to zero, stop and report it; that is a
    second instance of this task's own defect, not something to route around.

    Row 50 (REQ-merge-idempotency) is the defect. Its second command is
    `go test ./internal/store/... -run TestSupersedeIdempotency -v`, and that name has never
    existed — it is a plan-time seed nobody repointed when the phase closed. Replace that second
    command with
    `go test ./internal/store/... -run 'TestSupersedeConcurrentKeyedDisjointTargetsCannotBothLand\|TestPayloadRoundTripsIdempotencyFingerprint' -v`.
    Write the alternation with a backslash before the pipe, matching the existing convention at
    `02-VALIDATION.md:52` — inside a markdown table cell an unescaped pipe ends the cell. Leave the
    first (server-side) command alone: `TestSupersedeMemoryIdempotency` resolves to three real tests.

    Then reconcile the table. Rename the `File Exists` column header to `Tests Matched` and put in
    each cell the number that row's pattern(s) resolved to; set all four Status cells to the green
    check marker already defined in the legend beneath the table. Do not introduce a new marker
    glyph — the legend line directly under the table is the vocabulary.

    Frontmatter: `status` becomes `validated`, `nyquist_compliant` becomes `true`,
    `wave_0_complete` becomes `true` (every Wave 0 file listed in the section below now exists),
    and add a `validated: 2026-08-11` key directly after `created:`, matching
    `03-VALIDATION.md`'s key order. Leave the two lifecycle comment lines above `status` untouched.
    Under Validation Sign-Off, tick the boxes that are now true and set Approval to
    `approved 2026-08-11`.

    Finally append a `## Validation Audit 2026-08-11` section at the end of the file, after a `---`
    rule, following `03-VALIDATION.md:156`'s shape. It records: that every element was re-resolved
    against `go test -list` at HEAD rather than by exit status; the count each resolved to; that one
    store-side command named a test that never shipped and has been repointed to the two funcs that
    did; that only the top-level element of a slash-separated pattern is verifiable this way,
    because `go test -list` does not enumerate subtests; and that the seeded Task ID / Plan / Wave
    cells were deliberately left as-is rather than backfilled with reconstructed values.

    Write that section WITHOUT spelling the retired test name. Say "a store-side command named a
    test that never shipped" and give the replacement. Spelling the dead name would reintroduce it
    into the very file whose acceptance criterion asserts it is gone.

    Do not add a licence or SPDX header — GSD parses the YAML frontmatter from line 1, and a header
    above it makes a passed record read as missing (repo rule `2rjnv8sc9a`).
  </action>
  <verify>
    <automated>V=.planning/phases/03.1-merge-supersession-supersede-memory-accepts-multiple-targets/03.1-VALIDATION.md; rg -q 'TestSupersedeConcurrentKeyedDisjointTargetsCannotBothLand' "$V" && rg -q 'TestPayloadRoundTripsIdempotencyFingerprint' "$V" && ! rg -q 'TestSupersedeIdempotency' "$V" && rg -q '^status: validated' "$V" && rg -q '^nyquist_compliant: true' "$V"</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c 'TestSupersedeIdempotency' .planning/phases/03.1-*/03.1-VALIDATION.md` reports no match (exit 1)
    - The same file matches both `TestSupersedeConcurrentKeyedDisjointTargetsCannotBothLand` and `TestPayloadRoundTripsIdempotencyFingerprint` — the paired positive proving the matcher works on this file
    - `go test -list 'TestSupersedeConcurrentKeyedDisjointTargetsCannotBothLand' ./internal/store/...` prints that name; likewise for `TestPayloadRoundTripsIdempotencyFingerprint`
    - Frontmatter contains `status: validated`, `nyquist_compliant: true`, `wave_0_complete: true`, `validated: 2026-08-11`
    - `rg -c '⬜ pending' .planning/phases/03.1-*/03.1-VALIDATION.md` reports no match
    - The table header row contains `Tests Matched` and every one of the four rows carries a non-empty integer in that column
    - The file contains a `## Validation Audit 2026-08-11` heading
    - `git status --porcelain .planning/milestones/` is empty
  </acceptance_criteria>
  <done>03.1's record names only tests that exist, every row carries the count its pattern resolved to, the frontmatter says validated, and the repoint is written down rather than silently applied.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile 01 and 02 the same way</name>
  <files>.planning/phases/01-interface-enforceability/01-VALIDATION.md, .planning/phases/02-interface-discoverability/02-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/01-interface-enforceability/01-VALIDATION.md` (being edited; the map is at lines 42-49)
    - `.planning/phases/02-interface-discoverability/02-VALIDATION.md` (being edited; the map is at lines 48-69)
    - `.planning/phases/03.1-merge-supersession-supersede-memory-accepts-multiple-targets/03.1-VALIDATION.md` as committed by Task 1 (the shape to repeat)
  </read_first>
  <behavior>
    - Every `-run` element in both files resolves to at least one real test at HEAD
    - Both files read validated, with the resolved counts recorded per row
    - Rows that carry no `-run` pattern at all are marked as such, not given a fabricated count
  </behavior>
  <action>
    Implements D-02's `01` and `02` half, under D-08's count-but-assert-at-least-one rule. The
    reference tree is HEAD, because these are active-milestone phases (D-07 — an archived
    milestone would resolve against its own merge commit instead, and this task touches none).

    Repeat Task 1's shape on these two files. Reuse `/tmp/head-tests.txt` from Task 1 rather than
    rebuilding it.

    Resolve only rows of the Per-Task Verification Map — table rows starting with a pipe. Do NOT
    resolve the `-run` strings that appear in the Test Infrastructure and Sampling Rate prose:
    `01-VALIDATION.md:24` carries a `<TestName>` placeholder and every file's Sign-Off block
    carries `go test -run X ./pkg/...` as its own cautionary example of a false green. A sweep
    that flags the warning against false greens as a false green is trap 2 in
    05-CONTEXT.md `<code_context>`.

    Undo markdown table escaping before matching: a cell containing `TestA\|TestB` is an
    alternation of two elements, not one literal. Read as written it matches nothing and reports
    a phantom failure — trap 1. And validate only the top-level element of any
    `TestX/subtest` pattern, because `go test -list` does not enumerate subtests — trap 3.

    Expected element counts at HEAD, as a cross-check only. 01: `TestExitCodeBaseline` 4,
    `TestFlagGroup` 4, `TestExitCode` 7, `TestTimeout` 4, `TestClientConfig` 3. 02:
    `TestValidateRules` 4, `TestRegisterToolsEnumerable` 1, `TestNormalizeFieldRoundTrip` 1,
    `TestEveryRuleResolvesToNonEmptySurfaceSet` 1, `TestNoUnregisteredConditionalRejection` 1,
    `TestValidateOperations` 4, `TestOperationsCoverEveryTool` 1,
    `TestToolAnnotationsBothDirections` 1, `TestCatalogBlastRadiusMatchesToolClasses` 1,
    `TestCatalogExitCodesMatchMapper` 1, `TestClientFilesImportBoundary` 1, `TestHelpGolden` 1,
    `TestCatalogGolden` 2. A row's count is the sum over its own elements. Stop and report any
    element that resolves to zero.

    Several of 02's rows carry no `-run` pattern — they are whole-package runs, regeneration plus
    `git diff --exit-code` drift checks, a `checkpoint:decision` row and two manual rows. Put
    `n/a` in the `Tests Matched` column for those and say why in the audit note; do not invent a
    number, and do not mark a checkpoint or manual row with the automated-test marker.

    Rename `File Exists` to `Tests Matched` in both maps, fill the counts, set Status to the green
    marker from each file's own legend line, and apply the same frontmatter change as Task 1
    (`status: validated`, `nyquist_compliant: true`, `wave_0_complete: true`, add
    `validated: 2026-08-11`). Tick the now-true Sign-Off boxes and set Approval to
    `approved 2026-08-11`.

    Append a `## Validation Audit 2026-08-11` section to each file recording the same four facts
    Task 1 recorded, plus which rows were marked `n/a` and why. Note explicitly in 02's that the
    seeded Task ID cells were already real (`02-01-01` and so on) while 01's are `TBD` and were
    left that way.
  </action>
  <verify>
    <automated>for V in .planning/phases/01-interface-enforceability/01-VALIDATION.md .planning/phases/02-interface-discoverability/02-VALIDATION.md; do rg -q '^status: validated' "$V" && rg -q '^nyquist_compliant: true' "$V" && rg -q 'Tests Matched' "$V" && rg -q 'Validation Audit 2026-08-11' "$V" || { echo "FAIL $V"; exit 1; }; done</automated>
  </verify>
  <acceptance_criteria>
    - Both files' frontmatter contains `status: validated`, `nyquist_compliant: true`, `wave_0_complete: true`, `validated: 2026-08-11`
    - Both files' verification-map header row contains `Tests Matched`
    - `rg -c '⬜ pending' .planning/phases/01-*/01-VALIDATION.md` and the same over `02-*/02-VALIDATION.md` both report no match
    - Both files contain a `## Validation Audit 2026-08-11` heading
    - Running the per-element loop over all 18 elements listed in the action against `go test -list '.*' ./...` prints no `UNRESOLVED` line
    - `git status --porcelain .planning/milestones/` is empty
  </acceptance_criteria>
  <done>01 and 02 read validated, every row carries either a resolved count or an explicit `n/a`, and each file records how that was established.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>How to record 04's "zero new server-side code" row, whose command does not pass at HEAD</decision>
  <context>
D-03 authorised resolving 04's two structural rows on the stated premise that both "have commands
and passed". The forbidden-tool row does pass. The other one does not:
`git diff --stat 72a32c58..HEAD -- internal/ cmd/ proto/ gen/` returns
`internal/server/verbtabledocs_test.go | 79 +++++`, added by commit `a2599027` during phase 04's
own code-review fix pass. That file is a doc-binding test — it binds the `curating-spine` verb
table to `curating-memory`'s — so no server behaviour changed, but the command as written is red
and will get redder: plan 05-02 edits `internal/retrievaleval/retrieval_eval_test.go`, which is
inside the same open-ended range.

This is the one place in the phase where the honest answer is not obvious, which is why it stops
here instead of being decided silently.
  </context>
  <options>
    <option id="narrow-and-record">
      <name>Re-anchor the range and exclude test files, then mark it green, recording the correction</name>
      <pros>Makes the row a durable, checkable, true statement: `git diff --stat 72a32c58..b992929b -- internal/ cmd/ proto/ gen/ ':(exclude)*_test.go'` is empty and stays empty, because both ends are pinned. Matches what the criterion meant (no new server behaviour) and what 04-VERIFICATION.md already passed the phase on. Directly continues this file's own numbered-corrections convention, and is the same class of repair as its correction 1, which re-anchored this very row once before.</pros>
      <cons>Narrows a criterion after the fact. Needs the prose note to carry its own weight, or it reads as moving the goalposts.</cons>
    </option>
    <option id="mark-red">
      <name>Mark the row red and leave the command as written</name>
      <pros>Zero interpretation; the command's literal result is recorded.</pros>
      <cons>Asserts phase 04 shipped new server-side code, which is false — it shipped a test. A red that misdescribes what happened trains a reader to ignore reds, the over-flagging failure SC-3 warns about.</cons>
    </option>
    <option id="leave-pending">
      <name>Leave the row pending and record the finding in prose only</name>
      <pros>Claims nothing.</pros>
      <cons>Leaves a row whose command is known-broken sitting in the record with no verdict — the drift this phase exists to clear, restated.</cons>
    </option>
  </options>
  <resume-signal>Select: narrow-and-record, mark-red, or leave-pending. Recommended: narrow-and-record.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Partially reconcile 04 — resolve the structural rows, leave the open requirement open</name>
  <files>.planning/phases/04-spine-curation-semantic-skill/04-VALIDATION.md</files>
  <precondition>Commits `72a32c58` and `b992929b` are reachable in this clone (`git cat-file -e 72a32c58` and `git cat-file -e b992929b` both succeed). Both are needed to pin the diff range; a shallow clone will not have them.</precondition>
  <read_first>
    - `.planning/phases/04-spine-curation-semantic-skill/04-VALIDATION.md` (being edited; rows at 45-48, and the corrections prose at 52-61 that this task extends)
    - `.planning/phases/04-spine-curation-semantic-skill/04-VERIFICATION.md` lines 1-55 (the SC-3 override, and why the cold-read row is genuinely open)
    - `.planning/phases/05-validation-debt-reconciliation/05-CONTEXT.md` (D-03)
  </read_first>
  <behavior>
    - The forbidden-tool row's negative half returns zero AND its positive half returns at least six — the pairing is what makes the zero mean anything
    - The no-new-server-code row is recorded per the Task 3 decision, with the reason written down
    - The REQ-consent-adversarial-proof row is untouched and still reads pending
    - `nyquist_compliant` is still false
  </behavior>
  <action>
    Implements D-03 (partial reconciliation only) under D-10 (nothing under
    `.planning/milestones/**` is touched) and D-09/D-11 (no gate, no script, no report artifact
    comes out of this).

    Re-run the forbidden-tool row's command before recording anything. Both halves, remembering
    that the pipes inside the cell are markdown table escaping and must be unescaped to real
    alternation before running (trap 1). Negative half: search
    `skill/engram/skills/curating-spine/SKILL.md` for the three tool-name prefix forms against the
    seven forbidden tool names; it must return zero. Positive half: the same prefix shape against
    the six allowed tools must return at least six. At plan time this was 0 and 13 references
    across 6 distinct allowed tools. If the positive half ever drops below six, the negative half
    proves nothing and the row is not green — that is the vacuous-negative failure this file's own
    correction 2 already caught once.

    Apply the Task 3 decision to the no-new-server-code row. If `narrow-and-record` was chosen,
    replace the row's command with
    `git diff --stat 72a32c58..b992929b -- internal/ cmd/ proto/ gen/ ':(exclude)*_test.go'`,
    confirm it prints nothing, and mark the row green. `b992929b` is phase 04's final commit
    (`docs(04): add code review fix report`); pinning both ends is what stops a later phase's edit
    under `internal/` from silently reddening a closed phase's record.

    Extend this file's existing numbered corrections list rather than starting a new convention.
    It currently reads "Three corrections applied 2026-08-11" over three numbered items; make it
    four and add the fourth in the same voice: the row's range was open-ended at `HEAD`, one
    doc-binding test file (`internal/server/verbtabledocs_test.go`, commit `a2599027`) landed
    inside it during the phase's own review-fix pass, and both ends are now pinned with test files
    excluded so the row states the property that was actually meant. Say plainly that phase 05
    edits a file under `internal/`, so an open-ended range would have gone red for a reason having
    nothing to do with phase 04.

    Fill the `File Exists` column for the two structural rows: `skill/engram/skills/curating-spine/SKILL.md`
    now exists, so the first row is satisfied; keep the second row's `N/A (CI-level)` marker.

    Frontmatter: set `status: validated`, add `validated: 2026-08-11` after `created:`, and set
    `wave_0_complete: true` — both Wave 0 artefacts (`SKILL.md` and `04-COLD-READ.md`) exist.
    Leave `nyquist_compliant: false` exactly as it is. That pairing is the documented PARTIAL
    state, spelled out in this file's own frontmatter comment: draft means NOT-VALIDATED, while
    validated with `nyquist_compliant: false` means PARTIAL. Draft would under-report the two rows
    that are genuinely resolved; `nyquist_compliant: true` would over-report the one that is not.

    Do not touch the REQ-consent-adversarial-proof row, the paragraph beneath the table explaining
    why it carries no automated command, or the Manual-Only row for the same requirement. That
    requirement's cold read terminated at NOT-OBTAINED, the user accepted the non-result on
    2026-08-11, and `04-VERIFICATION.md` records an override that explicitly does not assert it was
    proven. It is tracked as an open broken window. It stays open here.

    Leave the Sign-Off `nyquist_compliant: true` box unticked and Approval at `pending` — the
    phase is partially reconciled, and the sign-off block should say so.
  </action>
  <verify>
    <automated>V=.planning/phases/04-spine-curation-semantic-skill/04-VALIDATION.md; rg -q '^nyquist_compliant: false' "$V" && rg -q 'REQ-consent-adversarial-proof.*⬜ pending' "$V" && rg -q '^status: validated' "$V" && test -z "$(git diff --stat 72a32c58..b992929b -- internal/ cmd/ proto/ gen/ ':(exclude)*_test.go')"</automated>
  </verify>
  <acceptance_criteria>
    - `04-VALIDATION.md` frontmatter contains `status: validated`, `nyquist_compliant: false`, `wave_0_complete: true`, `validated: 2026-08-11`
    - `rg -c 'REQ-consent-adversarial-proof.*⬜ pending' .planning/phases/04-*/04-VALIDATION.md` reports exactly 1
    - `rg -c 'nyquist_compliant: true' .planning/phases/04-*/04-VALIDATION.md` matches only the Sign-Off checklist line, never the frontmatter
    - The forbidden-tool negative search over `skill/engram/skills/curating-spine/SKILL.md` returns 0 matches and the paired positive search returns at least 6
    - `git diff --stat 72a32c58..b992929b -- internal/ cmd/ proto/ gen/ ':(exclude)*_test.go'` prints nothing
    - The corrections list in the file names four corrections, the fourth citing `internal/server/verbtabledocs_test.go` and commit `a2599027`
    - `git status --porcelain .planning/milestones/` is empty
  </acceptance_criteria>
  <done>04 records the two structural rows honestly, records why one of them had to be re-anchored, and still shows its one unproven requirement as unproven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning record → future reader | A `VALIDATION.md` is the only durable evidence that a shipped requirement was actually tested; a reader (auditor, `/gsd-audit-milestone`, a future maintainer) trusts it without re-deriving it |

No runtime trust boundary is crossed: this plan edits markdown under `.planning/` only. No
package-manager install task exists, so RESEARCH.md's Package Legitimacy Audit is not required.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | `03.1`/`01`/`02`-VALIDATION.md rows | high | mitigate | Every reconciled row's pattern is re-resolved against `go test -list` at HEAD before its status changes; a zero-resolving element halts the task instead of being routed around |
| T-05-02 | Repudiation | `04-VALIDATION.md` REQ-consent-adversarial-proof row | high | mitigate | The row, its explanatory paragraph and `nyquist_compliant: false` are explicitly out of bounds for every task; the plan's acceptance criteria assert the pending marker is still present after the edit |
| T-05-03 | Tampering | `.planning/milestones/**` | medium | mitigate | No task lists an archived path in `<files>`; every task's acceptance criteria assert `git status --porcelain .planning/milestones/` is empty |
| T-05-04 | Information disclosure | validation prose | low | accept | Records name test functions and commit SHAs already public in this repo |
| T-05-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task in this plan; nothing to audit |
</threat_model>

<verification>
- Every `-run` element in 01, 02 and 03.1 resolves to at least one name in `go test -list '.*' ./...`
- 01, 02, 03.1 read `status: validated` / `nyquist_compliant: true`; 04 reads `status: validated` / `nyquist_compliant: false`
- 04's REQ-consent-adversarial-proof row still reads pending
- `git status --porcelain .planning/milestones/` is empty
- No file outside `.planning/phases/0{1,2,3.1,4}-*/` was modified by this plan
</verification>

<success_criteria>
Four validation records state what is true at HEAD. The one requirement in this milestone that
was never proven is still visibly unproven. Nothing in the archived milestone tree moved, and no
mechanism, gate or report artifact was created.
</success_criteria>

<output>
Create `.planning/phases/05-validation-debt-reconciliation/05-01-SUMMARY.md` when done
</output>
