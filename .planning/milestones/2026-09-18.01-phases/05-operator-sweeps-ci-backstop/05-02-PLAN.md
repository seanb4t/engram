---
phase: 05-operator-sweeps-ci-backstop
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - internal/store/boundedread.go
  - internal/store/revert.go
  - internal/store/spine.go
  - internal/store/export_test.go
  - internal/store/boundedread_test.go
  - internal/store/spine_test.go
  - internal/store/orderedpage_oversized_test.go
  - internal/store/revertpreview_oversized_test.go
autonomous: true
requirements:
  - REQ-bounded-read-mechanism
  - REQ-sweeps-bounded

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`Store.previewRevertWithSteps` reads only each point's schema version, so it walks the above-target range on a one-field projection and its per-RPC record count is capped by `spineScrollBatch` rather than by a full-payload ceiling"
    - "`engram migrate revert`'s preview completes over a scope whose 256-record page would have exceeded `storetest.RecvLimit`, on both fixture shapes, and its `RevertPlan.Candidates` equals the seeded above-target record count"
    - "The zero-ceiling view constructor no longer exists anywhere in `internal/store`, production or test, and the inventory's closing check (b) prints `0`"
    - "The four test-file callers that the inventory's production-only closing check cannot see — the exported shim, the sweep-limit branch test, the collection snapshot helper and the ordered-page boundary table row — are all retargeted in the SAME change as the deletion, so no commit in this phase leaves the package unbuildable"
    - "The `internal/store` suite is green apart from the single expected red-evidence failure, and the 9 in-place `revert_test.go` tests are untouched and passing (D-02)"
    - statement: "After this plan every non-test `Store.scrollAllPoints` caller in the package passes a budgeted view, so no sweep can request a full payload at 256 records per RPC"
      verification: backstop
  artifacts:
    - path: "internal/store/boundedread.go"
      provides: "the one-field schema-version projection, and the removal of the zero-ceiling wrapper"
      contains: "schemaVersionOnlyView"
    - path: "internal/store/revert.go"
      provides: "the revert preflight reading the above-target range on a budgeted projection"
      contains: "schemaVersionOnlyView"
    - path: "internal/store/revertpreview_oversized_test.go"
      provides: "the revert-preview real-Qdrant oversized regression (D-03)"
      contains: "storetest.SeedOversized"
  key_links:
    - from: "internal/store/revert.go"
      to: "internal/store/boundedread.go"
      via: "the revert preflight takes its projection from the phase's one-field schema-version view"
      pattern: "schemaVersionOnlyView[(][)]"
    - from: "internal/store/boundedread_test.go"
      to: "internal/store/boundedread.go"
      via: "the sweep-limit branch test constructs a zero-ceiling readView value directly now that no constructor mints one"
      pattern: "sweepLimit[(]readView[{]"
    - from: "internal/store/revertpreview_oversized_test.go"
      to: "internal/store/storetest/seed.go"
      via: "the revert-preview regression seeds through the shared oversized fixture helper at the named receive limit"
      pattern: "storetest[.]SeedOversized[(]"
  prohibitions:
    - "MUST NOT split the constructor's deletion from the retargeting of its four test-file callers across two commits — a commit that does not build is a broken bisect point in the history of the most safety-critical operator command in the repo"
    - "MUST NOT change what the revert preflight reports (its candidate count, its unsupported-version tally or its refusal typing) while changing how it reads — the preflight is what decides whether a destructive revert is allowed to run at all"
    - "MUST NOT treat the inventory's production-only zero-count check as proof the deletion is complete; it is scoped to exclude test files by construction and is silent about exactly the callers that would break the build"
---

<objective>
Move the last production caller of the zero-ceiling view onto the narrowest projection in the phase,
then delete that constructor outright — together with the four test-file callers the inventory's own
closing check cannot see.

Decisions covered: D-01 (the constructor is deleted once its last caller is gone; the inventory's
closing check (b) printing `0` is this phase's completion signal for it), D-03 (the preview gets its
own real-Qdrant regression), D-04 (per-sweep projection — this one reads a single field), and the
CONTEXT's discretion note that the deletion may ride in the same plan as the last migration.

Purpose: every non-test sweep in `internal/store` now travels on a budgeted view, and the count-only
escape hatch that made an unbudgeted sweep representable is gone.

Output: one new view constructor and its ceiling, one migrated call site, one new real-Qdrant
regression, one deleted constructor, one deleted export shim, and three retargeted test call sites.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-operator-sweeps-ci-backstop/05-CONTEXT.md
@.planning/phases/05-operator-sweeps-ci-backstop/05-PATTERNS.md
@.planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/03-INVENTORY.md
@.planning/phases/05-operator-sweeps-ci-backstop/05-01-SUMMARY.md
@CLAUDE.md

<interfaces>
`internal/store/revert.go`:
`func (s *Store) previewRevertWithSteps(ctx context.Context, to migrate.Version, steps []migrate.Step) (RevertPlan, error)`
— a single exhaustive read-only walk of `aboveTargetFilter(to)` building a version-keyed chain cache.
Its callback increments `plan.Candidates`, calls `versionOf(p.Payload)` and memoises
`preflightRecordVersionSupport(steps, v, to)` per distinct version. `versionOf` is the ONLY payload
read in the whole callback. `func (s *Store) Revert(ctx context.Context, to migrate.Version) (RevertResult, error)`
is the exported entry point; `RevertPlan` carries `To`, `Candidates`, `Reversible`, `Unsupported`
and `Irreversible`, and `RevertRefusedError` wraps a plan.

`internal/store/boundedread.go` (as shipped by plan 05-01): `scanView`/`scanRecordCeiling`,
`citationsView`/`citationsRecordCeiling`, `nearDuplicateIdentityView`/`nearDuplicateIdentityRecordCeiling`,
plus the pre-existing `fullView`, `summaryView`, `keysView`/`keysRecordCeiling` and
`sweepLimit(v readView) uint32`. `readView`'s zero value has `maxRecordBytes: 0`, for which
`budgeted()` is already `false`.

The FOUR test-file callers of the zero-ceiling constructor, confirmed live against the tree — the
inventory's closing check (b) is scoped `--glob '!*_test.go'` and is silent about every one of them:
- `internal/store/export_test.go:81-82` — an exported shim returning `ReadView`, itself consumed by:
- `internal/store/orderedpage_oversized_test.go:654` — one row of a `scrollOrderedPage` argument-validation
  table, named `"unbudgeted view"`, which expects `store.ErrInvalidArgument`.
- `internal/store/boundedread_test.go:86-91` — `TestSweepLimit`'s first assertion, that a zero-ceiling
  view yields `spineScrollBatch`.
- `internal/store/spine_test.go:81` — `snapshotCollection`'s full-payload digest walk.

`internal/store/storetest`: unchanged from plan 05-01's interfaces block — `RecvLimit`, `Shape`
(`FewLarge` = 40 records of `RecvLimit/32` bytes, `ManySmall` = 1000 records), `Dial`, `Spec`,
`SeedOversized`, `Fixture`. `store.PrefixedTestCollection`, `store.NewTestStore`,
`st.EnsureCollection(ctx, 3)` and the `t.Cleanup` deleting the collection are the fixed preamble;
`internal/store/spinesweeps_oversized_test.go` (plan 05-01) is now the nearest in-repo example.

`internal/migrate`: `const CurrentVersion Version = 1`, and `var Registry` holds exactly ONE step —
a minting v0 to v1 step declared `Irreversible`. This is load-bearing for the regression's shape:
`Store.Revert` runs against that production registry, so a revert to version 0 ALWAYS ends in a
refusal, and the refusal carries the completed plan.

The seeder writes records through the public write path, so every seeded record carries the CURRENT
schema version. A revert preview needs records ABOVE its target: reverting to version 0 makes
`aboveTargetFilter(0)` match the whole fixture, and the preflight's candidate count is observable
through the refusal `Store.Revert` returns — `RevertRefusedError` carries the `RevertPlan` the
preflight built. No new `export_test.go` shim is needed for this plan.
</interfaces>
</context>

## Artifacts this phase produces

The full phase ledger is in `05-01-PLAN.md`. This plan owns:

| Kind | Symbol or path |
|------|----------------|
| New constructor | `schemaVersionOnlyView()` in `internal/store/boundedread.go` |
| New constant | `schemaVersionOnlyRecordCeiling` |
| New test file | `internal/store/revertpreview_oversized_test.go` |
| DELETED | the zero-ceiling view constructor in `internal/store/boundedread.go` |
| DELETED | its exported shim in `internal/store/export_test.go` |
| DELETED or retargeted | the unbudgeted-view row of the `scrollOrderedPage` table in `internal/store/orderedpage_oversized_test.go` |
| Retargeted | `TestSweepLimit`'s zero-ceiling assertion in `internal/store/boundedread_test.go` |
| Retargeted | `snapshotCollection`'s view in `internal/store/spine_test.go` |
| Rewritten | the sweep-iterator doc-comment paragraph naming the removed constructor, in `internal/store/spine.go` |

## Executor notes (every task)

- NEVER run any `git stash` subcommand. To inspect a pristine file use `git show HEAD:<path>`.
- Commit with an explicit pathspec (rule `n6m4as49mr`), Conventional Commits, and the session's
  attribution trailer.
- Never add an SPDX header to any `.planning/**` file. Every new `.go` file gets one.
- `TestRedEvidencePatchesAreLive` is the SINGLE expected red for this whole phase until plan 05-06.
- Real-Qdrant tests need `ENGRAM_REQUIRE_QDRANT=1`; a skipped test is not a pass.
- Any test importing `internal/store/storetest` MUST be `package store_test` (memory `y02a9ft3gy`).
- After any `roadmap update-plan-progress` or `state.advance-plan`, audit
  `git diff -U0 -- .planning/ROADMAP.md` and revert any hunk touching a milestone other than
  `2026-09-18.01`.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: The revert preflight bounded on a one-field projection, proven over an oversized above-target range</name>
  <files>internal/store/boundedread.go, internal/store/revert.go, internal/store/revertpreview_oversized_test.go</files>
  <precondition>Plan 05-01 is executed (`05-01-SUMMARY.md` exists), Docker is reachable (`docker info` exits 0) or `ENGRAM_QDRANT_TEST_ADDR` names a live Qdrant gRPC endpoint, and the working tree is clean.</precondition>
  <read_first>
    - `internal/store/revert.go` lines 255-350 — `previewRevertWithSteps` in full, `aboveTargetFilter`, and `Revert`'s wiring into it.
    - `internal/store/migrate.go` around `versionOf` and `schemaVersionKey` — the one payload key the preflight reads and the constant naming it.
    - `internal/store/boundedread.go` after plan 05-01 — `keysView`/`keysRecordCeiling` as the fixed-allowance precedent to copy, and the three constructors 05-01 added as the doc-comment convention.
    - `internal/store/spinesweeps_oversized_test.go` — plan 05-01's regression file, the nearest example of the preamble and both-shapes table.
    - `internal/store/revert_test.go` — the names of the 9 in-place tests that must stay green.
  </read_first>
  <behavior>
    - `TestRevertPreviewBoundedOverGRPCLimit/few-large`: over a `FewLarge` fixture at `storetest.RecvLimit`, calling `Revert` with target version 0 returns a refusal (matched with `errors.As` against `*store.RevertRefusedError`, never by parsing the message) whose plan's `Candidates` equals the seeded record count. Before the migration the same call fails with a receive-limit error instead of a refusal, because one 256-record page returns all 40 large records at once.
    - `TestRevertPreviewBoundedOverGRPCLimit/many-small`: the same over a `ManySmall` fixture, with `Candidates` equal to 1000 across many pages.
    - Both subtests assert the refusal's plan reports the seeded records' schema version as the unsupported one and reports `To` as 0 — proving the projection still delivered the one field the preflight reads, on every page.
    - A test failing with a receive-limit error rather than a refusal, or with a refusal whose `Candidates` is short of the seeded count, is the RED this regression exists to catch.
  </behavior>
  <action>
Author `schemaVersionOnlyRecordCeiling` beside `keysRecordCeiling` in `internal/store/boundedread.go`
as a small documented fixed allowance — one small integer plus point and protobuf map framing, no
capped field left in the selector to derive from, exactly the rationale `keysRecordCeiling`'s doc
comment already states for its own case. It must be less than or equal to `keysRecordCeiling`,
because a schema version is an integer where `keysRecordCeiling` budgets an RFC3339 string.

Add `func schemaVersionOnlyView() readView` — a package-level function, not a method, because it
depends on no `Store` state — pairing `qdrant.NewWithPayloadInclude(schemaVersionKey)` with that
ceiling. Use the existing key constant rather than a string literal so the selector can never drift
from the key `versionOf` reads. The doc comment must name the single field the view carries, name
`previewRevertWithSteps` as its caller, and state that the view is unsafe for any caller decoding a
whole record.

Swap `previewRevertWithSteps`' view argument to `schemaVersionOnlyView()`. Its filter, its callback
body, its memoisation and its error handling all stay byte-identical: `scrollAllPoints` returns the
raw error and the existing `if err != nil` handling is untouched.

Write `internal/store/revertpreview_oversized_test.go` as `package store_test` with the Apache-2.0
SPDX header and `TestRevertPreviewBoundedOverGRPCLimit` per the behaviour block, following plan
05-01's regression preamble. Reach the preflight through the exported `Store.Revert` entry point with
target version 0: the production registry's single step is irreversible, so the call always ends in
a `*store.RevertRefusedError` carrying the plan the preflight built, and no new `export_test.go`
shim is needed. Match the refusal with `errors.As`, never by reading its message.

Commit as `refactor(store): bound the revert preflight on a schema-version projection` with an
explicit pathspec naming the three files.
  </action>
  <verify>
    <automated>out=$(ENGRAM_REQUIRE_QDRANT=1 go test ./internal/store/ -run '^TestRevertPreviewBoundedOverGRPCLimit$' -count=1 -v 2&gt;&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^\s*--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e '--- PASS: TestRevertPreviewBoundedOverGRPCLimit/(few-large|many-small)' | wc -l)" -eq 2</automated>
    <fails_when>non-zero exit, a `--- FAIL:` or `--- SKIP:` line, or a PASS count other than 2 — one of the two shapes did not run</fails_when>
    <automated>out=$(ENGRAM_REQUIRE_QDRANT=1 go test ./internal/store/ -run '^TestMigrateRevert|^TestRevert' -count=1 -v 2&gt;&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^\s*--- |^ok|^FAIL'; test "$rc" -eq 0</automated>
    <fails_when>non-zero exit or a `--- FAIL:` line — an in-place revert characterisation test regressed (D-02: the existing suite is the characterisation)</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -c -e 'func schemaVersionOnlyView[(][)] readView' internal/store/boundedread.go` prints `1`.
    - `rg -c -e 'schemaVersionOnlyRecordCeiling' internal/store/boundedread.go` prints at least `2` — the declaration and its single use.
    - `rg -c -e 'schemaVersionOnlyView[(][)]' internal/store/revert.go` prints `1`.
    - `rg -o -e 'NewWithPayloadInclude[(]schemaVersionKey[)]' internal/store/boundedread.go | wc -l` prints `1` — the selector uses the shared key constant, not a literal.
    - `sed -n '1,2p' internal/store/revertpreview_oversized_test.go` shows the Apache-2.0 SPDX line and `rg -c -e '^package store_test$' internal/store/revertpreview_oversized_test.go` prints `1`.
    - `go build ./...` exits 0 and `task license:check` exits 0.
    - `rg -c -e 'errors[.]As[(]' internal/store/revertpreview_oversized_test.go` prints at least `1` — the refusal is matched by type. No assertion in the file branches on the error's message text; confirm by reading and say so in the SUMMARY.
    - The SUMMARY records the observed `RevertPlan.Candidates` for each shape and the per-RPC record count `schemaVersionOnlyRecordCeiling` yields.
  </acceptance_criteria>
  <done>`engram migrate revert`'s preflight reads the above-target range one field at a time, completes over an oversized scope on both fixture shapes, and reaches exactly the same verdict it reached before; the in-place revert suite is green; committed with an explicit pathspec.</done>
</task>

<task type="auto">
  <name>Task 2 (owns the deletion trap): The zero-ceiling view constructor removed, with all four test-file callers retargeted in the same change</name>
  <files>internal/store/boundedread.go, internal/store/spine.go, internal/store/export_test.go, internal/store/boundedread_test.go, internal/store/spine_test.go, internal/store/orderedpage_oversized_test.go</files>
  <precondition>Task 1 is committed, the working tree is clean, and `go build ./...` exits 0.</precondition>
  <read_first>
    - `internal/store/boundedread.go` lines 255-285 — the constructor being removed, its doc comment (which already names its own deletion condition), and `sweepLimit`'s branch on `budgeted()`.
    - `internal/store/spine.go` lines 35-70 — `scrollAllPoints`' doc comment, which names the removed constructor in prose and must be rewritten to describe a zero-value view instead.
    - `internal/store/export_test.go` lines 60-95 — the shim block and its doc-comment convention, plus the neighbouring `FullView`/`KeysView`/`PerRPCLimit` shims.
    - `internal/store/boundedread_test.go` lines 80-110 — `TestSweepLimit` in full, including the `spineScrollBatch` override and its `t.Cleanup`.
    - `internal/store/spine_test.go` lines 65-100 — `snapshotCollection` in full.
    - `internal/store/orderedpage_oversized_test.go` lines 640-680 — the argument-validation table, the row being retargeted, and what the surrounding subtest asserts.
    - `.planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/03-INVENTORY.md` — the closing-check section, so the exact check (b) command is run verbatim rather than paraphrased.
  </read_first>
  <action>
<!-- planner-discipline-allow: unbudgetedView -->
<!-- planner-discipline-allow: UnbudgetedView -->
<!-- planner-discipline-allow: nbudgetedView -->
One commit. Deleting `unbudgetedView` from `internal/store/boundedread.go` without moving every
caller in the same change breaks the build, and four of those callers live in `_test.go` files that
the inventory's closing check (b) is scoped to exclude — so a `0` from that check is necessary but
not sufficient, and the whole-package grep below is what actually proves the removal.

Do all of the following in one change:

1. Delete `unbudgetedView` and its doc comment from `internal/store/boundedread.go`. Keep
   `sweepLimit`'s unbudgeted branch and `readView.budgeted()`: a zero-value `readView` is still
   representable and still means "no byte-derived ceiling"; what is being removed is the constructor
   that made minting one convenient.
2. Delete the `UnbudgetedView` shim from `internal/store/export_test.go`.
3. In `internal/store/boundedread_test.go`, retarget `TestSweepLimit`'s first assertion to construct
   the zero-ceiling view directly as a `readView` composite literal rather than through a
   constructor — the branch under test is `sweepLimit`'s, and that branch still exists. Update the
   test's own doc comment so it describes the value it now builds.
4. In `internal/store/spine_test.go`, retarget `snapshotCollection`'s view to `s.fullView()`. It
   digests the whole payload, which is exactly what `fullView` selects; note at the call site that
   the digest is unchanged because the selector is the same, only the ceiling is now budgeted.
5. In `internal/store/orderedpage_oversized_test.go`, resolve the `"unbudgeted view"` row of the
   `scrollOrderedPage` argument-validation table. Read what the surrounding subtest asserts first:
   if the row exists to prove `scrollOrderedPage` rejects a view carrying no byte ceiling, keep the
   row and build that view as a `store.ReadView` composite literal through whatever `export_test.go`
   already exposes, adding a minimal shim only if the type's fields are not reachable from
   `package store_test`; if the row's premise was that such a view could arrive from production code,
   that premise is now false and the row is deleted with a one-line comment in the test recording
   why. Either resolution is acceptable; guessing between them without reading the subtest is not.
   This file belongs to Phase 4 — touching it here is a ripple this deletion causes, not scope creep,
   and the SUMMARY must say which resolution was chosen and why.
6. Rewrite the sentence in `scrollAllPoints`' doc comment (`internal/store/spine.go`, the paragraph
   describing what a budgeted versus a count-only view does) that names the removed constructor by
   identifier. Describe the behaviour in terms of a `readView` whose `maxRecordBytes` is zero, which
   is what `budgeted()` still tests. The prose is load-bearing here for a different reason than
   usual: the whole-package grep below is exactly what a stale doc-comment reference defeats.

Then run the inventory's closing check (b) verbatim from
`.planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/03-INVENTORY.md` and record
its output in the SUMMARY alongside the whole-package grep, stating explicitly that the first is
production-scoped and the second is what proves the constructor is gone.

Commit as `refactor(store)!: drop the count-only sweep view constructor` with an explicit pathspec
naming the six files.
  </action>
  <verify>
    <automated>test "$(rg -o -e 'nbudgetedView' internal/store | wc -l)" -eq 0</automated>
    <fails_when>non-zero exit — a reference to the removed constructor or its exported shim survives somewhere in `internal/store`, including in a test file or a doc comment</fails_when>
    <automated>go build ./... &amp;&amp; go vet ./internal/store/...</automated>
    <fails_when>non-zero exit, or any `undefined:` line in the output — a caller was left behind</fails_when>
    <automated>out=$(ENGRAM_REQUIRE_QDRANT=1 go test ./internal/store/ -count=1 -v 2&gt;&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^\s*--- FAIL|^ok|^FAIL'; test "$(printf '%s\n' "$out" | rg -o -e '^\s*--- FAIL: ' | wc -l)" -eq 1 &amp;&amp; printf '%s\n' "$out" | rg -q -e '--- FAIL: TestRedEvidencePatchesAreLive'</automated>
    <fails_when>more than one `--- FAIL:` line, or the single failure is not `TestRedEvidencePatchesAreLive` — that one red is expected until plan 05-06; anything else is a real regression</fails_when>
    <automated>task lint</automated>
    <fails_when>non-zero exit from golangci-lint, yamlfmt, actionlint or rumdl</fails_when>
  </verify>
  <acceptance_criteria>
    - The inventory's closing check (b), run verbatim, prints `0`, and its output is quoted in the SUMMARY.
    - `rg -o -e 'nbudgetedView' internal/store | wc -l` prints `0`, and the SUMMARY states that this whole-package grep — not check (b) — is what proves the four test-file callers were moved.
    - `rg -c -e 'func sweepLimit' internal/store/boundedread.go` prints `1` and `rg -c -e 'func [(]v readView[)] budgeted' internal/store/boundedread.go` prints `1` — the unbudgeted BRANCH survives; only the constructor is gone.
    - `rg -c -e 's[.]fullView[(][)]' internal/store/spine_test.go` prints `1`.
    - `rg -o -e 'scrollAllPoints[(]ctx, s[.]collection,' internal/store | wc -l` prints `6`, unchanged from plan 05-01.
    - `git diff --exit-code HEAD -- go.mod go.sum` exits 0.
    - The SUMMARY names which resolution was chosen for the ordered-page table row and quotes the subtest assertion that decided it.
  </acceptance_criteria>
  <done>The count-only sweep view constructor no longer exists in production or test source, all five of its former callers build and pass in the same commit, the inventory's closing check (b) prints `0`, and the only failing test in `internal/store` is the expected red-evidence harness.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| revert preflight → destructive apply | The preflight's verdict is the only gate deciding whether a schema revert is allowed to mutate records; a preflight that reads the range differently must still decide identically. |
| production-scoped gate → whole-package truth | The inventory's closing check is scoped to non-test files and cannot observe the callers that break the build. |
| deleted constructor → build integrity | A partial deletion leaves a commit in the history of an operator command that does not compile. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-02-01 | Tampering | `previewRevertWithSteps`' verdict under a narrowed projection | high | mitigate | The one-field selector is built from the same `schemaVersionKey` constant `versionOf` reads, so selector and reader cannot drift; the regression asserts the oversized plan's reversibility verdict and unsupported tally match a small-scope run seeded identically. |
| T-05-02-02 | Repudiation | a zero-count production gate read as proof of a complete deletion | high | mitigate | Task 2's first verify is a whole-package grep including test files, and an acceptance criterion requires the SUMMARY to state which of the two checks is the proof. |
| T-05-02-03 | Denial of Service | a broken build committed between the deletion and a caller's retarget | medium | mitigate | A prohibition forbids splitting the deletion across commits; `go build ./...` and `go vet` are verify commands on the same task that performs it. |
| T-05-02-04 | Tampering | a Phase 4 test file edited beyond the ripple this deletion causes | medium | mitigate | Task 2 names the single row that may change, requires the surrounding subtest to be read before choosing a resolution, and requires the SUMMARY to quote the assertion that decided it. |
| T-05-02-05 | Information Disclosure | the revert preview's filter widened while its view narrows | high | mitigate | The action states the filter and callback stay byte-identical; an acceptance criterion counts the single changed call-site token. |
| T-05-02-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install and no manifest change: `git diff --exit-code HEAD -- go.mod go.sum` exits 0. |
</threat_model>

<verification>
- `ENGRAM_REQUIRE_QDRANT=1 go test ./internal/store/ -run '^TestRevertPreviewBoundedOverGRPCLimit$' -count=1 -v` shows two `--- PASS` subtest lines and exits 0.
- The inventory's closing check (b), run verbatim, prints `0`.
- `rg -o -e 'nbudgetedView' internal/store | wc -l` prints `0`.
- `ENGRAM_REQUIRE_QDRANT=1 go test ./internal/store/ -count=1` fails only on `TestRedEvidencePatchesAreLive`.
- `task lint` exits 0; `task license:check` exits 0; `git diff --exit-code HEAD -- go.mod go.sum` exits 0.
</verification>

<success_criteria>
- Every non-test `scrollAllPoints` caller in `internal/store` passes a budgeted, per-sweep-sized view.
- `engram migrate revert`'s preview completes over a scope whose 256-record page would have exceeded the named receive limit, reaching the identical verdict.
- The count-only view constructor and its exported shim are gone, with all four test-file callers moved in the same commit, and the inventory's completion signal for D-01 prints `0`.
</success_criteria>

<output>
Create `.planning/phases/05-operator-sweeps-ci-backstop/05-02-SUMMARY.md` when done.
</output>
