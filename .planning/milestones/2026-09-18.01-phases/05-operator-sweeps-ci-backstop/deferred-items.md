## Deferred Items

- Pre-existing `TestActiveMilestoneKeyLinksSatisfiable` failure in `internal/keylinks` against `.planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/03-02-PLAN.md:72`'s `key_links` entry (`pattern: "unbudgetedView[(]qdrant[.]NewWithPayload[(]true[)][)]"`, expected against `internal/store/revert.go` or `internal/store/boundedread.go`).
  status: acknowledged
  **What:** The pattern no longer matches either file. Confirmed via `git log --all -S 'unbudgetedView(qdrant.NewWithPayload(true))' -- internal/store/revert.go`, which shows the last call site removed by commit `af0aee9e` ("refactor(store): bound the revert preflight on a schema-version projection") — an ancestor of `2372b65a`, the commit this plan's own work (05-05) started from. The pattern went stale during an earlier Phase 5 plan's own-loop/view migration (D-01/D-04), before plan 05-05 touched anything. Out of scope for this plan per the executor's scope boundary rule: plan 05-05's `files_modified` is `internal/store/store.go`, `internal/store/storetest/storetest.go`, `internal/store/qdrantbackstop_test.go` — it never touches `revert.go`, `boundedread.go`, or `03-02-PLAN.md`, and none of those files appear in this plan's diff.
