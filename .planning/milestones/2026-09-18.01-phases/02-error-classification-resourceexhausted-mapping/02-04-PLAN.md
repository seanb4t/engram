---
phase: 02-error-classification-resourceexhausted-mapping
plan: 04
type: execute
wave: 4
depends_on:
  - 02-01
  - 02-02
  - 02-03
files_modified:
  - internal/store/redevidence_harness_test.go
  - .planning/phases/02-error-classification-resourceexhausted-mapping/red-evidence/02-02-connect-arm-removed.patch
  - .planning/phases/02-error-classification-resourceexhausted-mapping/red-evidence/02-01-classifier-not-in-base-options.patch
  - .planning/phases/02-error-classification-resourceexhausted-mapping/red-evidence/02-01-classifier-requires-two-number-shape.patch
  - .planning/phases/02-error-classification-resourceexhausted-mapping/red-evidence/02-01-classifier-relabels-any-resource-exhausted.patch
  - .planning/phases/02-error-classification-resourceexhausted-mapping/red-evidence/02-02-mcp-mapper-unregistered.patch
  - .planning/phases/02-error-classification-resourceexhausted-mapping/red-evidence/02-03-exit-mapping-reverted.patch
  - .planning/phases/02-error-classification-resourceexhausted-mapping/red-evidence/02-03-errors-doc-drops-too-large.patch
  - .planning/phases/02-error-classification-resourceexhausted-mapping/red-evidence/02-03-operator-arm-removed.patch
autonomous: true
requirements:
  - REQ-exhausted-sentinel
  - REQ-exhausted-connect
  - REQ-exhausted-mcp
  - REQ-exhausted-cli-docs

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`redEvidenceDirs` gains exactly one new entry, keyed `.planning/phases/02-error-classification-resourceexhausted-mapping/red-evidence`, mapping eight patches to their target tests; Phase 1's entry is unchanged"
    - "Each Phase 2 patch is one behavioral mutation that compiles and makes its named target test itself report `--- FAIL:` (never a build break), hand-verified with git apply --check / apply / go test / apply -R before registration"
    - "The registered directions cover every lane: classifier absent from the base options, classifier blind to the real single-number shape (RESEARCH Pitfall 1), classifier relabeling every ResourceExhausted (D-02), Connect arm removed (D-06), MCP mapper unregistered (D-08), CLI exit mapping reverted (D-07), operator-tier arm removed (D-07), and errors.md dropping the too_large row (D-05)"
    - "`TestRedEvidencePatchesAreLive` passes with twelve `confirmed RED:` log lines (Phase 1's four plus Phase 2's eight) and leaves every touched source and doc file clean"
    - "`task` (lint + test) exits 0 for the whole repository, and `go test ./internal/keylinks/ -count=1` exits 0 with every executed Phase 2 plan's key_links satisfiable"
  artifacts:
    - path: "internal/store/redevidence_harness_test.go"
      provides: "redEvidenceDirs entry for Phase 2's eight patches"
      contains: "02-error-classification-resourceexhausted-mapping/red-evidence"
    - path: ".planning/phases/02-error-classification-resourceexhausted-mapping/red-evidence/02-02-connect-arm-removed.patch"
      provides: "RED evidence for the Connect arm (D-06)"
      contains: "ErrResponseTooLarge"
    - path: ".planning/phases/02-error-classification-resourceexhausted-mapping/red-evidence/02-01-classifier-requires-two-number-shape.patch"
      provides: "RED evidence that real traffic's single-number shape must classify (Pitfall 1)"
      contains: "isRecvLimitMessage"
  key_links:
    - from: "internal/store/redevidence_harness_test.go"
      to: ".planning/phases/02-error-classification-resourceexhausted-mapping/red-evidence/02-02-connect-arm-removed.patch"
      via: "the harness applies every registered Phase 2 patch and requires its target test to fail"
      pattern: "02-error-classification-resourceexhausted-mapping/red-evidence"
    - from: "internal/store/redevidence_harness_test.go"
      to: "internal/server/responsetoolarge_test.go"
      via: "removing the Connect arm must turn the Connect-lane regression RED"
      pattern: '"TestConnectListMemoriesResponseTooLarge"'
    - from: "internal/store/redevidence_harness_test.go"
      to: "internal/store/responsetoolarge_oversized_test.go"
      via: "a classifier blind to the real single-number shape must turn the real-overflow regression RED"
      pattern: '"TestStoreListOverflowIsResponseTooLarge"'
---

<objective>
Close the phase with its evidence: package each RED direction this phase's tests exist to catch as a
registered red-evidence patch, so `TestRedEvidencePatchesAreLive` keeps every lane's regression test
provably able to fail for as long as the milestone is open. The harness is GREEN today (Phase 1's entry
keeps `redEvidenceDirs` non-empty, RESEARCH Pitfall 7) — this plan does not "fix" it; it adds Phase 2's
own entry. This is the phase's LAST plan (orchestrator guidance 5).

Eight directions, one per failure the phase guards against:

| Patch | Mutation | Target test | Guards |
|---|---|---|---|
| `02-01-classifier-not-in-base-options.patch` | `NewQdrantClient` stops appending the classifier | `TestResponseTooLargeClassifierSitsInsideCallerChain` | D-01 |
| `02-01-classifier-requires-two-number-shape.patch` | `isRecvLimitMessage` additionally requires the two-number form | `TestStoreListOverflowIsResponseTooLarge` | Pitfall 1 on real traffic |
| `02-01-classifier-relabels-any-resource-exhausted.patch` | the classifier drops its message-shape check | `TestClassifyResponseTooLarge` | D-02 |
| `02-02-connect-arm-removed.patch` | `connectError` loses its `ErrResponseTooLarge` case | `TestConnectListMemoriesResponseTooLarge` | D-06 |
| `02-02-mcp-mapper-unregistered.patch` | `addToolMiddleware` registers only `instrumentTools` | `TestMCPListMemoryResponseTooLarge` | D-08 |
| `02-03-exit-mapping-reverted.patch` | `exitCodeForConnectErr` loses its `CodeResourceExhausted` case | `TestExitCodeBaseline` | D-07 |
| `02-03-operator-arm-removed.patch` | `classifyOperatorErr` loses its `ErrResponseTooLarge` case | `TestClassifyOperatorErrCodesAreDistinct` | D-07 (operator tier) |
| `02-03-errors-doc-drops-too-large.patch` | errors.md loses its `too_large` table row | `TestErrorsDocHintCodesMatchArgErrorConstants` | D-05 |

Purpose: every requirement of the phase (REQ-exhausted-sentinel, -connect, -mcp, -cli-docs) gets a live,
harness-checked RED proof, per the milestone's Phase 1 gate.

Output: eight `.patch` files under this phase's `red-evidence/` directory and one `redEvidenceDirs` entry.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-error-classification-resourceexhausted-mapping/02-CONTEXT.md
@.planning/phases/02-error-classification-resourceexhausted-mapping/02-01-SUMMARY.md
@.planning/phases/02-error-classification-resourceexhausted-mapping/02-02-SUMMARY.md
@.planning/phases/02-error-classification-resourceexhausted-mapping/02-03-SUMMARY.md
@.planning/phases/01-test-harness-fixture-helper/01-05-SUMMARY.md
@CLAUDE.md

<interfaces>
Red-evidence harness (`internal/store/redevidence_harness_test.go`, read the whole file doc comment): `redEvidenceDirs map[string]map[string]string` (line 106) currently holds ONE entry, `.planning/phases/01-test-harness-fixture-helper/red-evidence` → four patches, each with a trailing `// reverts: …` comment. For every patch under every registered directory (discovered by glob) the harness requires: a clean touched tree, `git apply --check`, apply, then `go test -run '^<Target>$' -count=1 ./...` exits non-zero, then `git apply -R` restores exactly. It fails if a directory's glob matches zero patches or if a discovered patch has no mapping. A BUILD failure also exits non-zero, so every patch must compile and the executor must see the target's own `--- FAIL:` line. It is skipped under `-short`, must never run concurrently with itself, and its child `go test` inherits the parent's environment (so the booted Qdrant's address reaches every package).

Patch shape precedent: `.planning/phases/01-test-harness-fixture-helper/red-evidence/01-04-listscopes-full-payload-selector.patch` — a plain `git diff -U3 -- <one file>` of one behavioral mutation, named `<plan>-<slug>.patch`, no SPDX header (`.planning/**` is excluded from license scope and GSD parses it).

The code each patch mutates (all shipped by plans 02-01..02-03):
- `internal/store/store.go` `NewQdrantClient`: the line appending `grpc.WithChainUnaryInterceptor(classifyResponseTooLarge)`.
- `internal/store/responsetoolarge.go`: `isRecvLimitMessage` (prefix + `larger than max` + receive marker) and `classifyResponseTooLarge` (its `!isRecvLimitMessage(...)` pass-through condition).
- `internal/server/connecterror.go`: the `case errors.Is(err, store.ErrResponseTooLarge):` arm (case line, log line, return line).
- `internal/server/instrument.go` `addToolMiddleware`: `s.AddReceivingMiddleware(instrumentTools(record), mapResponseTooLarge())`.
- `cmd/engram/client_common.go` `exitCodeForConnectErr`: the `case connect.CodeResourceExhausted:` line and its `return exitTooLarge` line.
- `cmd/engram/operror.go` `classifyOperatorErr`: the `case errors.Is(err, store.ErrResponseTooLarge):` arm and its return line.
- `docs-site/src/content/docs/reference/errors.md`: the `too_large` row of the eleven-code table.
</interfaces>
</context>

## Artifacts this phase produces (plan 02-04's share)

| Kind | Artifact | Created by |
|------|----------|------------|
| Patch | `red-evidence/02-02-connect-arm-removed.patch` → `TestConnectListMemoriesResponseTooLarge` | Task 1 |
| Gate data | `redEvidenceDirs[".planning/phases/02-error-classification-resourceexhausted-mapping/red-evidence"]` (first mapping) | Task 1 |
| Patches | `02-01-classifier-not-in-base-options.patch`, `02-01-classifier-requires-two-number-shape.patch`, `02-01-classifier-relabels-any-resource-exhausted.patch`, `02-02-mcp-mapper-unregistered.patch`, `02-03-exit-mapping-reverted.patch`, `02-03-operator-arm-removed.patch`, `02-03-errors-doc-drops-too-large.patch` | Task 2 |
| Gate data | the remaining seven mappings in the same entry | Task 2 |

## Executor notes (every task)

- NEVER run any git stash subcommand — to inspect a pristine file use `git show HEAD:<path>` into the scratchpad instead.
- Commit with an explicit pathspec (rule n6m4as49mr), Conventional Commits, ending the message with the session's attribution trailer lines.
- Commit signing: if a commit fails because the 1Password SSH signer is locked or errors, you MAY retry that ONE commit with `git -c commit.gpgsign=false commit …` (never change persistent git config) and must record it in the SUMMARY.
- Never run two red-evidence harness runs at once, and check `git status --porcelain` before trusting the tree after any interrupted run.
- Patches under `.planning/**` carry NO SPDX header.

## Per-patch procedure (both tasks)

For EACH patch: start from a clean tree for the touched file; make exactly the one mutation; write
`git diff -U3 -- <file>` to the patch path; `git apply -R <patch>`; confirm `git status --porcelain -- <file>`
prints nothing. Then prove it by hand: `git apply --check <patch>`, `git apply <patch>`,
`ENGRAM_REQUIRE_QDRANT=1 go test -run '^<Target>$' -count=1 -v ./...` and confirm the output contains
`--- FAIL: <Target>` (not merely a non-zero exit — a compile error would also exit non-zero), then
`git apply -R <patch>` and confirm the file is clean. Record each target's `--- FAIL:` line in the SUMMARY.

<tasks>

<task type="tracer">
  <name>Task 1: One lane's RED proof end to end: the Connect-arm patch, registered and confirmed by the harness</name>
  <files>internal/store/redevidence_harness_test.go, .planning/phases/02-error-classification-resourceexhausted-mapping/red-evidence/02-02-connect-arm-removed.patch</files>
  <precondition>Plans 02-01, 02-02 and 02-03 are executed (their SUMMARYs exist), and Docker is reachable (`docker info` exits 0) or ENGRAM_QDRANT_TEST_ADDR names a live Qdrant gRPC endpoint.</precondition>
  <read_first>
    - `internal/store/redevidence_harness_test.go` (whole file).
    - `.planning/phases/01-test-harness-fixture-helper/red-evidence/01-04-listscopes-full-payload-selector.patch` (shape precedent).
    - `internal/server/connecterror.go` (the arm to remove) and `internal/server/responsetoolarge_test.go` (`TestConnectListMemoriesResponseTooLarge`).
    - `.planning/phases/01-test-harness-fixture-helper/01-05-SUMMARY.md` Task 3 (how the prior registration was verified).
  </read_first>
  <action>
Create `.planning/phases/02-error-classification-resourceexhausted-mapping/red-evidence/` and author `02-02-connect-arm-removed.patch` by the per-patch procedure: in `internal/server/connecterror.go` delete the whole `ErrResponseTooLarge` arm (its `case` line, its log line, its return line) so the error falls to the default arm; target `TestConnectListMemoriesResponseTooLarge` (expected failure: code `internal` instead of `resource_exhausted`).

Register it in `internal/store/redevidence_harness_test.go`: add ONE new top-level entry to `redEvidenceDirs`, keyed `".planning/phases/02-error-classification-resourceexhausted-mapping/red-evidence"`, mapping `"02-02-connect-arm-removed.patch"` → `"TestConnectListMemoriesResponseTooLarge"` with a trailing comment `// reverts: connectError's ErrResponseTooLarge → resource_exhausted arm (D-06)`. Leave Phase 1's entry and everything else in the harness untouched.

Run the harness once, alone (see verify). Commit as `test(store): register phase 2 red-evidence for the Connect arm` with pathspec `internal/store/redevidence_harness_test.go .planning/phases/02-error-classification-resourceexhausted-mapping/red-evidence`.
  </action>
  <verify>
    <automated>out=$(ENGRAM_REQUIRE_QDRANT=1 go test ./internal/store/ -run '^TestRedEvidencePatchesAreLive$' -count=1 -v -timeout 20m 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e 'confirmed RED|^\s*--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o 'confirmed RED: ' | wc -l)" -eq 5 &amp;&amp; test -z "$(git status --porcelain -- internal/server/connecterror.go)"</automated>
    <fails_when>non-zero exit, a `--- FAIL:` line, a `confirmed RED:` count other than 5 (Phase 1's four plus this one), or `internal/server/connecterror.go` left dirty</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -c '^diff --git a/' .planning/phases/02-error-classification-resourceexhausted-mapping/red-evidence/02-02-connect-arm-removed.patch` prints `1`, and the patch's first line is `diff --git a/internal/server/connecterror.go b/internal/server/connecterror.go`.
    - `rg -n -F '".planning/phases/02-error-classification-resourceexhausted-mapping/red-evidence"' internal/store/redevidence_harness_test.go` prints exactly one line; Phase 1's four mappings are unchanged: with `C` set to this task's commit SHA (record it in the SUMMARY), `d=$(git diff "$C^..$C" -- internal/store/redevidence_harness_test.go) || exit 1; printf '%s\n' "$d" | rg '^-[^-]' | wc -l` prints `0` (the task only adds lines).
    - The SUMMARY records the hand-verified `--- FAIL: TestConnectListMemoriesResponseTooLarge` line.
  </acceptance_criteria>
  <done>The Connect-arm RED direction is a registered, harness-confirmed patch; the harness passes with five confirmed REDs and leaves the tree clean; committed with an explicit pathspec.</done>
</task>

<task type="auto">
  <name>Task 2 (LAST task of the phase): The remaining seven RED directions registered, and the full quality gate green</name>
  <files>internal/store/redevidence_harness_test.go, .planning/phases/02-error-classification-resourceexhausted-mapping/red-evidence/02-01-classifier-not-in-base-options.patch, .planning/phases/02-error-classification-resourceexhausted-mapping/red-evidence/02-01-classifier-requires-two-number-shape.patch, .planning/phases/02-error-classification-resourceexhausted-mapping/red-evidence/02-01-classifier-relabels-any-resource-exhausted.patch, .planning/phases/02-error-classification-resourceexhausted-mapping/red-evidence/02-02-mcp-mapper-unregistered.patch, .planning/phases/02-error-classification-resourceexhausted-mapping/red-evidence/02-03-exit-mapping-reverted.patch, .planning/phases/02-error-classification-resourceexhausted-mapping/red-evidence/02-03-errors-doc-drops-too-large.patch, .planning/phases/02-error-classification-resourceexhausted-mapping/red-evidence/02-03-operator-arm-removed.patch</files>
  <precondition>Docker is reachable (`docker info` exits 0) or ENGRAM_QDRANT_TEST_ADDR names a live Qdrant gRPC endpoint.</precondition>
  <read_first>
    - `internal/store/store.go` `NewQdrantClient`; `internal/store/responsetoolarge.go`; `internal/server/instrument.go` `addToolMiddleware`; `cmd/engram/client_common.go` `exitCodeForConnectErr`; `cmd/engram/operror.go` `classifyOperatorErr`; `docs-site/src/content/docs/reference/errors.md` (the eleven-code table).
    - The target tests: `internal/store/responsetoolarge_test.go`, `internal/store/responsetoolarge_oversized_test.go`, `internal/server/responsetoolarge_test.go`, `cmd/engram/exitcode_baseline_test.go`, `cmd/engram/operror_test.go`, `internal/server/hintcodedocs_test.go`.
    - `internal/store/redevidence_harness_test.go` (after Task 1).
  </read_first>
  <action>
Author seven patches in the same directory by the per-patch procedure, each the smallest mutation that makes its target test itself fail while the tree still compiles:

1. `02-01-classifier-not-in-base-options.patch` → `TestResponseTooLargeClassifierSitsInsideCallerChain` (D-01): in `internal/store/store.go`'s `NewQdrantClient`, delete the line that appends the classifier to the base options. The unused interceptor function still compiles.
2. `02-01-classifier-requires-two-number-shape.patch` → `TestStoreListOverflowIsResponseTooLarge` (RESEARCH Pitfall 1): in `internal/store/responsetoolarge.go`'s `isRecvLimitMessage`, additionally require that the message contain ` vs. ` — so real traffic's single-number shape no longer classifies and both real-Qdrant shape subtests fail.
3. `02-01-classifier-relabels-any-resource-exhausted.patch` → `TestClassifyResponseTooLarge` (D-02): in `classifyResponseTooLarge`, drop the `isRecvLimitMessage` condition so every `ResourceExhausted` is relabeled; the pass-through rows fail.
4. `02-02-mcp-mapper-unregistered.patch` → `TestMCPListMemoryResponseTooLarge` (D-08): in `internal/server/instrument.go`'s `addToolMiddleware`, register only `instrumentTools(record)`; the tool result carries raw text again.
5. `02-03-exit-mapping-reverted.patch` → `TestExitCodeBaseline` (D-07): in `cmd/engram/client_common.go`'s `exitCodeForConnectErr`, delete the `CodeResourceExhausted` case and its return line; both `response-too-large` rows report exit 1.
6. `02-03-operator-arm-removed.patch` → `TestClassifyOperatorErrCodesAreDistinct` (D-07, operator tier): in `cmd/engram/operror.go`'s `classifyOperatorErr`, delete the `ErrResponseTooLarge` case and its return line; the sentinel falls to the passthrough default and the distinct-set test reports it unclassified.
7. `02-03-errors-doc-drops-too-large.patch` → `TestErrorsDocHintCodesMatchArgErrorConstants` (D-05): in `docs-site/src/content/docs/reference/errors.md`, delete the `too_large` row of the hint-code table.

Register all seven in the Phase 2 entry Task 1 created, each with a trailing `// reverts: …` comment naming what the mutation undoes and its decision id. Change nothing else in the harness.

Run the harness once, alone, then the full gate (see verify). If `task` shows any failure other than one this plan caused, stop and report it rather than editing unrelated code.

Commit as `test(store): register phase 2 red-evidence patches` with pathspec `internal/store/redevidence_harness_test.go .planning/phases/02-error-classification-resourceexhausted-mapping/red-evidence`.
  </action>
  <verify>
    <automated>out=$(ENGRAM_REQUIRE_QDRANT=1 go test ./internal/store/ -run '^TestRedEvidencePatchesAreLive$' -count=1 -v -timeout 30m 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e 'confirmed RED|^\s*--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o 'confirmed RED: ' | wc -l)" -eq 12 &amp;&amp; test -z "$(git status --porcelain -- internal/store/store.go internal/store/responsetoolarge.go internal/server/connecterror.go internal/server/instrument.go cmd/engram/client_common.go cmd/engram/operror.go docs-site/src/content/docs/reference/errors.md)"</automated>
    <fails_when>non-zero exit, a `--- FAIL:` line, a `confirmed RED:` count other than 12, or any mutated source or doc file left dirty after the run</fails_when>
    <automated>task</automated>
    <fails_when>non-zero exit from `task` (lint or test), including any `--- FAIL:` or `FAIL` package line</fails_when>
    <automated>go test ./internal/keylinks/ -count=1 -v 2>&amp;1 | rg -e '^--- |^ok|^FAIL'; go test ./internal/keylinks/ -count=1</automated>
    <fails_when>non-zero exit from the second command, or a `--- FAIL:` line naming TestNoEscapedPatternsRepoWide or TestActiveMilestoneKeyLinksSatisfiable (a Phase 2 key_links pattern is escaped or no longer matches the shipped code)</fails_when>
  </verify>
  <acceptance_criteria>
    - `ls .planning/phases/02-error-classification-resourceexhausted-mapping/red-evidence/*.patch | wc -l` prints `8`, and each patch contains exactly one `diff --git a/` header (`rg -c '^diff --git a/' <file>` prints `1` for each).
    - `rg -o '"Test[A-Za-z]+"' internal/store/redevidence_harness_test.go | sort -u` includes all eight Phase 2 target names alongside Phase 1's four.
    - The SUMMARY records, per patch, the hand-verified `--- FAIL: <Target>` line and the harness's `confirmed RED:` line.
    - No red-evidence patch or `.planning/**` file gained an SPDX header; `task license:check` exits 0.
    - `git status --porcelain` after the final commit shows no modified tracked source file.
  </acceptance_criteria>
  <done>Eight hand-verified Phase 2 patches are registered; the harness confirms twelve REDs and restores the tree; `task` and the keylinks gates are green; committed with an explicit pathspec.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| red-evidence harness → working tree | The harness applies real patches to Go source and a published doc and must always restore them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04-01 | Repudiation | a patch counted RED because it broke the build, not the test | medium | mitigate | Each patch hand-verified by its target's own `--- FAIL:` line before registration; mutations chosen to keep the tree compiling. |
| T-02-04-02 | Tampering | the harness leaving source or errors.md mutated | medium | mitigate | Harness refuses dirty files and always reverts; the verify requires all seven touched files clean afterward; runs are never concurrent. |
| T-02-04-03 | Repudiation | a lane's regression test silently losing its teeth later in the milestone | medium | mitigate | Each lane has a registered patch; `TestRedEvidencePatchesAreLive` runs in `task test` for the rest of the open milestone. |
| T-02-04-SC | Tampering | npm/pip/cargo installs | low | accept | Not applicable: no installs, no new modules. |
</threat_model>

<verification>
- `ENGRAM_REQUIRE_QDRANT=1 go test ./internal/store/ -run '^TestRedEvidencePatchesAreLive$' -count=1 -v -timeout 30m` passes with twelve `confirmed RED:` lines.
- `task` exits 0; `task license:check` exits 0; `git diff --exit-code main...HEAD -- go.mod go.sum` exits 0.
- After this plan's SUMMARY is written: `go test ./internal/keylinks/ -count=1` exits 0.
</verification>

<success_criteria>
- Every Phase 2 lane (store classifier, Connect, MCP, CLI client and operator tiers, errors.md) has a registered, harness-confirmed RED proof.
- The whole repository's quality gate is green at the end of the phase.
</success_criteria>

<output>
Create `.planning/phases/02-error-classification-resourceexhausted-mapping/02-04-SUMMARY.md` when done.
</output>
