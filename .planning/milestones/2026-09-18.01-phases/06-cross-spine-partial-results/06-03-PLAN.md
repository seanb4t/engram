---
phase: 06-cross-spine-partial-results
plan: 03
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-02
files_modified:
  - internal/store/redevidence_harness_test.go
  - .planning/phases/06-cross-spine-partial-results/red-evidence/06-01-helper-swallows-listscopes-error.patch
  - .planning/phases/06-cross-spine-partial-results/red-evidence/06-01-connect-search-discards-hits.patch
  - .planning/phases/06-cross-spine-partial-results/red-evidence/06-01-mcp-list-discards-hits.patch
  - .planning/phases/06-cross-spine-partial-results/red-evidence/06-01-empty-scopes-substituted-for-absence.patch
  - .planning/phases/06-cross-spine-partial-results/red-evidence/06-02-footer-drops-unknown-form.patch
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - REQ-cross-spine-partial

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`redEvidenceDirs` gains exactly one new entry, keyed by this phase's red-evidence directory, mapping five patches to their target tests; the Phase 1, 2, 3, 4 and 5 entries are byte-unchanged"
    - "Each patch is ONE behavioral mutation that still compiles and makes its named target test itself report a failure — never a build break — hand-verified by apply, run, revert before it is registered"
    - "The registered directions cover every guarantee this phase adds: the helper swallowing its own error into a zero-value coverage claim, a Connect handler discarding already-computed hits, an MCP closure doing the same, an empty searched-scopes list substituted for absence, and the CLI dropping its third footer form"
    - "`TestRedEvidencePatchesAreLive` passes with fifty-eight confirmed RED lines (the earlier phases' fifty-three plus this phase's five) and leaves every mutated file clean"
    - "Every earlier phase's patch still applies at this phase's HEAD: this phase edits `internal/server/tools.go` and `cmd/engram/client_common.go`, both of which earlier patches mutate"
    - "REQ-cross-spine-partial is ticked in the checklist and its traceability row reads complete; NO other requirement's checkbox or row moves"
    - "`task` exits 0 for the whole repository and `go test ./internal/keylinks/ -count=1` exits 0 with every executed plan's key links satisfiable"
    - "No red-evidence patch carries a licence header, and no planning artifact gains a version-bearing heading"
    - statement: "The phase's own recorded state is internally consistent at close: the roadmap's plan list, the requirement checkbox and the traceability row all say the same thing about Phase 6, and no other milestone's rows moved"
      verification: backstop
  artifacts:
    - path: "internal/store/redevidence_harness_test.go"
      provides: "the redEvidenceDirs entry for this phase's five patches"
      contains: "06-cross-spine-partial-results/red-evidence"
    - path: ".planning/phases/06-cross-spine-partial-results/red-evidence/06-01-helper-swallows-listscopes-error.patch"
      provides: "RED evidence that the coverage helper must not degrade its own failure into a zero-value claim"
      contains: "searchedScopes"
    - path: ".planning/phases/06-cross-spine-partial-results/red-evidence/06-01-empty-scopes-substituted-for-absence.patch"
      provides: "RED evidence that an empty scope list is never substituted for absence on the unknown path"
      contains: "recallResultMap"
    - path: ".planning/REQUIREMENTS.md"
      provides: "this phase's single requirement marked complete in both the checklist and the traceability table"
      contains: "REQ-cross-spine-partial"
  key_links:
    - from: "internal/store/redevidence_harness_test.go"
      to: ".planning/phases/06-cross-spine-partial-results/red-evidence/06-01-helper-swallows-listscopes-error.patch"
      via: "the harness applies the phase's highest-value patch and requires its target test to fail"
      pattern: "06-01-helper-swallows-listscopes-error[.]patch"
    - from: "internal/store/redevidence_harness_test.go"
      to: ".planning/phases/06-cross-spine-partial-results/red-evidence/06-01-empty-scopes-substituted-for-absence.patch"
      via: "the absence-versus-empty-list direction, the failure mode that would otherwise report a confident wrong answer"
      pattern: "TestCrossSpineCoverageUnknownMCPSearch"
  prohibitions:
    - "MUST NOT register a patch that has not been hand-proven to make its own target test fail — a patch that breaks the build, or whose target passes with it applied, is evidence of nothing and makes the gate a false comfort"
    - "MUST NOT edit another phase's verification document, context document or requirement checkbox to make this phase's gate green"
    - "MUST NOT tick a requirement belonging to another phase, and MUST NOT tick this one before the whole repository gate is green"
    - "MUST NOT raise a timeout in the task runner or in CI to make the red-evidence gate pass"
    - "MUST NOT add a licence header to any file under .planning, and MUST NOT add a version-bearing heading to any planning artifact"
---

<objective>
Make every guarantee this phase added a live, harness-checked RED proof, confirm the earlier phases'
patches still apply over this phase's edits, and close the phase on a green full gate with its one
requirement marked complete.

This is the phase's LAST plan. It registers the phase's entry in the red-evidence map after every
other plan has shipped, exactly as plans 03-06, 04-08 and 05-06 did for the previous three phases.

Decisions covered: D-01 gets two patches reverting the "keep the hits" contract, one per lane; D-02
and the roadmap's explicit warning get the swallow-the-error patch; D-03 gets the empty-list
substitution patch; D-05 gets the footer patch. REQ-cross-spine-partial is ticked here and nowhere
else.

Purpose: this phase's requirement proven RED-able for the rest of the open milestone.

Output: five patch files, one harness entry, one ticked requirement, and a green gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-cross-spine-partial-results/06-CONTEXT.md
@.planning/phases/06-cross-spine-partial-results/06-01-SUMMARY.md
@.planning/phases/06-cross-spine-partial-results/06-02-SUMMARY.md
@.planning/phases/05-operator-sweeps-ci-backstop/05-06-SUMMARY.md
@CLAUDE.md

<interfaces>
`internal/store/redevidence_harness_test.go`: `var redEvidenceDirs = map[string]map[string]string{…}`
(`:106-170`) — one top-level entry per phase directory (repo-root-relative), each mapping
`"<patch file>": "<TargetTest>"` with a trailing `// reverts: …` comment naming what the mutation
undoes and its decision ids. There are five entries today, holding fifty-three patches between them.

`TestRedEvidencePatchesAreLive` (`:250`, skipped under `-short`) globs `<dir>/*.patch`, requires set
equality with the mapping in BOTH directions, and per patch: refuses to run if a touched file is
already dirty, `git apply --check`, `git apply`, `go test -run '^Target$' -count=1 ./...` (which must
exit NON-zero and must be a test failure, not an abnormal error), `git apply -R`, then asserts the
touched files are clean. It logs
`confirmed RED: <patch> applied -> <Target> failed as expected`. It must never run concurrently with
itself (its doc comment records a 2026-08-16 incident). A directory whose glob matches zero patches
fails ITS OWN subtest.

Per-patch authoring procedure (Phase 1/2/3/4/5 precedent): make the ONE mutation on a clean tree,
write `git diff -- <file> > <dir>/<name>.patch`, then prove it —
`git apply --check <patch> && git apply <patch> && go test -run '^Target$' -count=1 ./<pkg>/ -v`
shows the target's OWN failure line (a compile error is NOT acceptable) — then `git apply -R <patch>`
and `git status --porcelain -- <file>` prints nothing. Each patch has exactly one `diff --git` header
and no licence header. Never use a git stash; to inspect a pristine file use `git show HEAD:<path>`
into the scratchpad.

Import constraints for the mutations: `internal/server/connectapi.go` imports `fmt` but NOT `errors`,
and `internal/server/tools.go` imports `fmt`. A mutation that needs to fabricate an error must
therefore build it with `fmt.Errorf` and a constant string, so the patch stays a single-file diff
with no import block change.

Earlier phases' patches that mutate files THIS phase edited — these are the ones most likely to have
gone stale and are why the whole harness is re-run here, not just this phase's subtest:
`03-01-content-cap-removed.patch`, `03-01-tags-cap-removed.patch`, `03-03-record-caps-not-wired.patch`,
`03-03-update-content-cap-removed.patch`, `03-03-update-gates-on-presence-not-change.patch`,
`04-06-maximum-check-runs-after-embed.patch`, `04-06-surface-maximum-check-removed.patch` (all
`internal/server/tools.go`), and `02-03-exit-mapping-reverted.patch`
(`cmd/engram/client_common.go`).

`.planning/REQUIREMENTS.md`: the `### Cross-Spine` checklist entry for REQ-cross-spine-partial
(`:55`) and the `## Traceability` table row `| REQ-cross-spine-partial | Phase 6 | Pending |`
(`:103`). At the start of this plan seventeen requirement boxes are ticked and three are not; exactly
one of those three belongs to this phase — the other two belong to Phase 7 and must not move.

Requirement readiness helper, run BEFORE ticking anything:
`node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" query requirements.ready-ids
.planning/phases/06-cross-spine-partial-results/06-03-PLAN.md REQ-cross-spine-partial` — it reports
`ready` versus `blocked`; a blocked id means an earlier plan's evidence is missing and must be
resolved rather than ticked over.
</interfaces>

<test_names_this_phase_shipped>
Every target below must be re-resolved with `go test -list` before it is relied on: a `-run` pattern
matching nothing exits 0 with `no tests to run` and reports a permanent false green (durable records
`bsbsvn4hbc`, `gfh6q1ack4`; Phase 4 hit this with two of five seeded names).

- `TestCrossSpineCoverageUnknownConnectSearch`, `TestCrossSpineCoverageUnknownConnectList`,
  `TestCrossSpineCoverageUnknownMCPSearch`, `TestCrossSpineCoverageUnknownMCPList`,
  `TestCrossSpineCoverageThreeStates` — all in `internal/server/crossspinecoverage_test.go` (06-01).
- `TestClientListCoverageUnknownFooter`, `TestClientSearchCoverageUnknownFooter` — `cmd/engram`
  (06-02).
</test_names_this_phase_shipped>
</context>

## Artifacts this phase produces (plan 06-03's share)

The full phase ledger is in `06-01-PLAN.md`. This plan owns:

| Kind | Artifact | Created by |
|------|----------|------------|
| Directory | `.planning/phases/06-cross-spine-partial-results/red-evidence/` | Task 1 |
| Patch | `06-01-helper-swallows-listscopes-error.patch` | Task 1 |
| Patches | the remaining four, listed in Task 2 | Task 2 |
| Harness entry | this phase's `redEvidenceDirs` entry | Tasks 1-2 |
| Checkbox | REQ-cross-spine-partial ticked in the checklist and the traceability table | Task 3 |

## Executor notes (every task)

- NEVER run any `git stash` subcommand. To inspect a pristine file use `git show HEAD:<path>` into
  the scratchpad instead.
- Commit with an explicit pathspec (rule `n6m4as49mr`), Conventional Commits, and the session's
  attribution trailer.
- Every patch is authored and proven on a CLEAN tree, one at a time; never run two harness or patch
  cycles concurrently, and never against a dirty tree — the harness refuses to run and reports a
  spurious failure (every Phase 5 plan hit this).
- Patches under `.planning/**` carry NO licence header, and no planning artifact gains a
  version-bearing heading.
- If `task` shows a failure this plan did not cause, STOP and report it rather than editing
  unrelated code — and never edit another phase's verification or context document to make it green.
- After any `roadmap update-plan-progress` or `state.advance-plan`, audit
  `git diff -U0 -- .planning/ROADMAP.md` and revert any hunk touching a milestone other than
  `2026-09-18.01`. This has recurred in most plans of Phases 4 and 5 — in Phase 5,
  `update-plan-progress "05"` matched a same-numbered row in a shipped milestone.
- This plan has no outward-facing step: nothing here touches GitHub or any other external system, so
  everything it does is recoverable from the git diff alone.

<tasks>

<task type="tracer">
  <name>Task 1: One RED proof end to end — the forbidden swallow-the-error fix, registered and confirmed by the harness</name>
  <files>internal/store/redevidence_harness_test.go, .planning/phases/06-cross-spine-partial-results/red-evidence/06-01-helper-swallows-listscopes-error.patch</files>
  <precondition>Plans 06-01 and 06-02 are executed (both SUMMARY files exist), the working tree is clean (`git status --porcelain` prints nothing), and Docker is reachable (`docker info` exits 0) or `ENGRAM_QDRANT_TEST_ADDR` names a live Qdrant gRPC endpoint — several of the fifty-three already-registered targets need real Qdrant, and a skipped target is not a RED.</precondition>
  <read_first>
    - `internal/store/redevidence_harness_test.go` in full — the map, the harness, its tree-safety,
      set-equality and empty-map guards, and its concurrency warning.
    - `.planning/phases/05-operator-sweeps-ci-backstop/05-06-SUMMARY.md` and one of that phase's
      patches — the shape precedent.
    - `internal/server/tools.go` after plan 06-01 — the shipped helper body and the shipped names,
      before writing any mutation.
    - `internal/server/crossspinecoverage_test.go` — the target test's own assertions, so the
      mutation is aimed at an assertion that exists.
    - `.planning/phases/06-cross-spine-partial-results/06-01-SUMMARY.md` — the shipped identifier
      names.
  </read_first>
  <action>
Create `.planning/phases/06-cross-spine-partial-results/red-evidence/` and author
`06-01-helper-swallows-listscopes-error.patch` by the per-patch procedure: in
`internal/server/tools.go`, make the coverage helper's failure path degrade into a zero-value
coverage claim — it still logs, still returns normally, but no longer reports that coverage is
unknown, so the caller cannot tell a failed enumeration from a successful one. The function still
compiles and every call site is unchanged.

Target `TestCrossSpineCoverageThreeStates`, whose third row then fails. Prove it RED by hand and
record the observed failure line.

This is the phase's highest-value direction, and the reason it goes first: it is the exact fix the
roadmap and 06-CONTEXT forbid by name, it is the one a future contributor is most likely to reach
for while "simplifying" the helper, and with it applied the whole phase silently reverts to the
ambiguity `searched_scopes` exists to remove while every hit still comes back looking fine.

Register it: add ONE new top-level entry to `redEvidenceDirs`, keyed by this phase's red-evidence
directory path, mapping the patch name to its target test with a trailing `// reverts:` comment
naming what the mutation undoes and its decision ids. Leave the Phase 1, 2, 3, 4 and 5 entries and
everything else untouched.

Run the harness once, alone (verify). Commit as
`test(store): register phase 6 red-evidence for the coverage-unknown signal` with pathspec
`internal/store/redevidence_harness_test.go .planning/phases/06-cross-spine-partial-results/red-evidence`.
  </action>
  <verify>
    <automated>out=$(ENGRAM_REQUIRE_QDRANT=1 go test ./internal/store/ -run '^TestRedEvidencePatchesAreLive$' -count=1 -v -timeout 180m 2&gt;&amp;1); rc=$?; printf '%s\n' "$out" | rg -e 'confirmed RED|^\s*--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e 'confirmed RED: ' | wc -l)" -eq 54 &amp;&amp; { dirty=$(git status --porcelain -- internal/server/tools.go) || exit 1; test -z "$dirty"; }</automated>
    <fails_when>non-zero exit, a `--- FAIL:` line, a `confirmed RED:` count other than 54 (the earlier phases' fifty-three plus this one), or `internal/server/tools.go` left dirty after the run</fails_when>
    <automated>n=$(rg -o -e '^diff --[g]it a/' .planning/phases/06-cross-spine-partial-results/red-evidence/06-01-helper-swallows-listscopes-error.patch | wc -l); test "$n" -eq 1</automated>
    <fails_when>non-zero exit — the patch touches a number of files other than one, so it is not one behavioral mutation</fails_when>
  </verify>
  <acceptance_criteria>
    - The patch's only `diff --git` line names `internal/server/tools.go`.
    - `rg -n -F '".planning/phases/06-cross-spine-partial-results/red-evidence"' internal/store/redevidence_harness_test.go` prints exactly one line.
    - With `C` the commit SHA of this task (record it in the SUMMARY): `d=$(git diff "$C^..$C" -- internal/store/redevidence_harness_test.go) || exit 1; printf '%s\n' "$d" | rg -o -e '^-[^-]' | wc -l` prints `0` — the task only adds lines to the harness.
    - `rg -l 'SPDX-License-Identifier' .planning/phases/06-cross-spine-partial-results/red-evidence/` prints nothing.
    - The target name was re-resolved with `go test -list` before the mapping was written, and the SUMMARY records both that resolution and the hand-verified failure line for `TestCrossSpineCoverageThreeStates`.
  </acceptance_criteria>
  <done>The phase's highest-value RED direction — the fix the roadmap forbids by name — is a registered, harness-confirmed patch; the harness passes with fifty-four confirmed REDs and leaves the tree clean; committed with an explicit pathspec.</done>
</task>

<task type="auto">
  <name>Task 2: The remaining four RED directions registered and the harness green at fifty-eight</name>
  <files>internal/store/redevidence_harness_test.go, .planning/phases/06-cross-spine-partial-results/red-evidence/06-01-connect-search-discards-hits.patch, .planning/phases/06-cross-spine-partial-results/red-evidence/06-01-mcp-list-discards-hits.patch, .planning/phases/06-cross-spine-partial-results/red-evidence/06-01-empty-scopes-substituted-for-absence.patch, .planning/phases/06-cross-spine-partial-results/red-evidence/06-02-footer-drops-unknown-form.patch</files>
  <precondition>Task 1 is committed, the working tree is clean, and Docker is reachable (`docker info` exits 0) or `ENGRAM_QDRANT_TEST_ADDR` names a live Qdrant gRPC endpoint.</precondition>
  <read_first>
    - `internal/store/redevidence_harness_test.go` after Task 1.
    - Each mutation site in the shipped source: `internal/server/connectapi.go`,
      `internal/server/tools.go` (both the MCP `list_memory` closure and `recallResultMap`), and
      `cmd/engram/client_common.go`.
    - Each target test's own assertions, in `internal/server/crossspinecoverage_test.go` and
      `cmd/engram/client_list_test.go`, so every mutation is aimed at an assertion that exists.
    - This plan's `<interfaces>` note on which imports each file already has — a mutation must not
      need a new import.
    - Every target name re-resolved with `go test -list` before it is relied on.
  </read_first>
  <action>
Author four more patches in the same directory by the per-patch procedure, each the smallest
mutation that makes its target test itself fail while the tree still compiles.

1. `06-01-connect-search-discards-hits.patch` → `TestCrossSpineCoverageUnknownConnectSearch` (D-01):
   in the Connect search handler, abort with a mapped error when the coverage value reports unknown,
   so the already-computed hits are discarded again. Build the error with `fmt.Errorf` and a
   constant string so the diff stays single-file with no import change.
2. `06-01-mcp-list-discards-hits.patch` → `TestCrossSpineCoverageUnknownMCPList` (D-01): the same
   re-introduced abort in the MCP list closure, so the tool call returns an error result instead of
   the memories it already has.
3. `06-01-empty-scopes-substituted-for-absence.patch` → `TestCrossSpineCoverageUnknownMCPSearch`
   (D-03): make the result-map assembler add the searched-scopes key on the unknown path as well,
   carrying the empty slice — the "searched nothing" reading the phase exists to prevent. The
   unknown key stays set, so only the absence property breaks.
4. `06-02-footer-drops-unknown-form.patch` → `TestClientListCoverageUnknownFooter` (D-05): remove the
   shared renderer's unknown branch so a coverage-unknown response falls through to the count-bearing
   form and prints a count of zero. The parameter stays in the signature, so both call sites and the
   tree still compile.

Register all four in the phase entry Task 1 created, each with a trailing `// reverts:` comment
naming what the mutation undoes and its decision ids. Change nothing else in the harness.

If any named direction turns out not to make its target fail — for example because the target's
fixture never reaches the mutated branch — STOP and report which one and why, rather than dropping it
silently or retargeting it to a test that fails for an unrelated reason. A direction that cannot be
proven is a finding, not a rounding error.

Run the harness once, alone, then the full gate (verify). The whole harness matters here, not just
this phase's subtest: this phase edited `internal/server/tools.go` and `cmd/engram/client_common.go`,
and eight earlier patches mutate those same two files. If one of them no longer applies, report it
with the patch name and the `git apply` output rather than rewriting the earlier phase's patch as
part of this task.

Commit as `test(store): register phase 6's remaining red-evidence` with pathspec
`internal/store/redevidence_harness_test.go .planning/phases/06-cross-spine-partial-results/red-evidence`.
  </action>
  <verify>
    <automated>out=$(ENGRAM_REQUIRE_QDRANT=1 go test ./internal/store/ -run '^TestRedEvidencePatchesAreLive$' -count=1 -v -timeout 180m 2&gt;&amp;1); rc=$?; printf '%s\n' "$out" | rg -e 'confirmed RED|^\s*--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e 'confirmed RED: ' | wc -l)" -eq 58 &amp;&amp; { dirty=$(git status --porcelain -- internal/server cmd/engram internal/store) || exit 1; test -z "$dirty"; }</automated>
    <fails_when>non-zero exit, a `--- FAIL:` line, a `confirmed RED:` count other than 58, a subtest reporting that a target test PASSED with its patch applied, or any mutated source file left dirty after the run</fails_when>
    <automated>ls .planning/phases/06-cross-spine-partial-results/red-evidence/*.patch | wc -l | rg -x '5'</automated>
    <fails_when>non-zero exit — the directory holds a number of patches other than five, so a named direction was dropped or an extra one added without a mapping</fails_when>
    <automated>for p in .planning/phases/06-cross-spine-partial-results/red-evidence/*.patch; do n=$(rg -o -e '^diff --[g]it a/' "$p" | wc -l); test "$n" -eq 1 || { echo "MULTI-FILE PATCH: $p ($n)"; exit 1; }; done; echo "all single-file"</automated>
    <fails_when>non-zero exit, or a `MULTI-FILE PATCH:` line — a patch touches more than one file and is therefore not one behavioral mutation</fails_when>
  </verify>
  <acceptance_criteria>
    - `ls .planning/phases/06-cross-spine-partial-results/red-evidence/*.patch | wc -l` prints `5`.
    - `rg -l 'SPDX-License-Identifier' .planning/phases/06-cross-spine-partial-results/red-evidence/` prints nothing; `task license:check` exits 0.
    - The phase entry maps exactly the five patch names, and the earlier phases' entries are unchanged: with `C` the Task 1 commit SHA recorded in the SUMMARY, `d=$(git diff "$C^" -- internal/store/redevidence_harness_test.go) || exit 1; printf '%s\n' "$d" | rg -o -e '^-\s+"0[12345]-' | wc -l` prints `0`.
    - Every target name in the phase entry appears in `go test -list '.*' ./internal/store/... ./internal/server/... ./cmd/engram/...` output; record the resolution in the SUMMARY.
    - The SUMMARY carries a table of all five patches with each target's hand-verified failure line, and states explicitly that the eight earlier patches over the two files this phase edited still applied.
    - `git status --porcelain` after the final commit shows no modified tracked file.
  </acceptance_criteria>
  <done>Five hand-verified patches are registered, the harness confirms fifty-eight REDs with a clean tree, and every guarantee this phase added has a live failing direction.</done>
</task>

<task type="auto">
  <name>Task 3 (LAST task of the phase): The requirement marked complete on a green gate, with no other phase's state disturbed</name>
  <files>.planning/REQUIREMENTS.md</files>
  <precondition>Task 2 is committed, the whole red-evidence harness passes at fifty-eight, and the working tree is clean.</precondition>
  <read_first>
    - `.planning/REQUIREMENTS.md` in full — both the Cross-Spine checklist entry and the traceability
      table, its Status vocabulary, and any trailing coverage counts.
    - `.planning/phases/06-cross-spine-partial-results/06-01-SUMMARY.md` and `06-02-SUMMARY.md` — the
      evidence the tick rests on.
    - `.planning/ROADMAP.md` Phase 6's entry — its plan list and success criteria, to confirm both
      criteria are actually met before ticking anything.
  </read_first>
  <action>
Query the requirement readiness helper first, with the exact invocation recorded in this plan's
`<interfaces>` block. If it reports the id blocked rather than ready, resolve the missing evidence or
report it — do not tick over a blocked id.

Then tick exactly REQ-cross-spine-partial in the Cross-Spine checklist, and change that same row's
Status cell in the traceability table from pending to the completed value the table already uses.
Leave every other box and row exactly as it is: the two Phase 7 requirements stay unticked, and no
other phase's verification document, context document or checkbox is touched anywhere in this task.
Update any trailing last-updated line the way the previous phase's close did. Add no heading, no
version marker and no licence header.

Re-run the full gate and the key-links gates as the final act of the phase. Then run the roadmap
plan-progress update for this phase and immediately audit `git diff -U0 -- .planning/ROADMAP.md`,
reverting any hunk that touches a milestone other than `2026-09-18.01` — a same-numbered row in a
shipped milestone has been matched before.

If anything is red that this plan did not cause, STOP and report it rather than editing unrelated
code.

Commit as `docs(06): mark the cross-spine partial-results requirement complete` with an explicit
pathspec.
  </action>
  <verify>
    <automated>rg -c -e '^- \[x\] \*\*REQ-cross-spine-partial\*\*' .planning/REQUIREMENTS.md | rg -x '1'</automated>
    <fails_when>non-zero exit — this phase's requirement is still unticked, or the checklist line was reshaped so the gate cannot see it</fails_when>
    <automated>test "$(rg -o -e '^- \[x\] \*\*REQ-' .planning/REQUIREMENTS.md | wc -l)" -eq 18 &amp;&amp; test "$(rg -o -e '^- \[ \] \*\*REQ-' .planning/REQUIREMENTS.md | wc -l)" -eq 2</automated>
    <fails_when>non-zero exit — the ticked total is not the seventeen already complete plus this phase's one, or the remaining unticked total is not the two that belong to Phase 7, meaning another phase's box moved</fails_when>
    <automated>test "$(rg -c -e 'REQ-cross-spine-partial [|] Phase 6 [|] Complete' .planning/REQUIREMENTS.md)" -eq 1 &amp;&amp; test "$(rg -o -e 'Phase 6 [|] Pending' .planning/REQUIREMENTS.md | wc -l)" -eq 0</automated>
    <fails_when>non-zero exit — the traceability row still reads pending, or its Status cell does not use the completed value the table already uses elsewhere</fails_when>
    <automated>test "$(rg -o -e '✅|📋|🚧' .planning/REQUIREMENTS.md | wc -l)" -eq 0 &amp;&amp; test "$(rg -o -e 'SPDX-License-Identifier' .planning/REQUIREMENTS.md | wc -l)" -eq 0</automated>
    <fails_when>non-zero exit — a milestone marker or a licence header was introduced into a parsed planning artifact, which makes GSD misread it</fails_when>
    <automated>git diff --exit-code HEAD -- .planning/phases/01-test-harness-fixture-helper .planning/phases/02-error-classification-resourceexhausted-mapping .planning/phases/03-shared-bounded-read-mechanism-content-cap-decision .planning/phases/04-list-listscheduled-search-bounded-reads .planning/phases/05-operator-sweeps-ci-backstop</automated>
    <fails_when>non-zero exit — an earlier phase's recorded state was edited, which this plan forbids outright</fails_when>
    <automated>task</automated>
    <fails_when>non-zero exit from `task` (lint or test), including any `--- FAIL:` line, a `FAIL` package line, or a lint diagnostic</fails_when>
    <automated>go test ./internal/keylinks/ -count=1 -v 2&gt;&amp;1 | rg -e '^--- |^ok|^FAIL'; go test ./internal/keylinks/ -count=1</automated>
    <fails_when>non-zero exit from the second command, or a `--- FAIL:` line naming the repo-wide escaping gate or the active-milestone satisfiability gate — a key-links pattern in one of this phase's plans is escaped or no longer matches the shipped code</fails_when>
  </verify>
  <acceptance_criteria>
    - The readiness helper's output is quoted in the SUMMARY, and it reported the id ready before the tick was made.
    - The SUMMARY names both of ROADMAP Phase 6's success criteria and the shipped evidence for each.
    - `git diff -U0 -- .planning/ROADMAP.md` was audited after the plan-progress update; the SUMMARY states whether any hunk touched a milestone other than `2026-09-18.01` and, if so, that it was reverted.
    - With `R` the SHA of this task's requirement commit (record it in the SUMMARY): `git diff --name-only "$R^..$R"` lists only `.planning/REQUIREMENTS.md` (the roadmap progress update, if it produces its own commit, is recorded separately in the SUMMARY).
    - `git status --porcelain` after the final commit shows no modified tracked file.
  </acceptance_criteria>
  <done>The phase's single requirement is ticked in both places on evidence that actually shipped, no other phase's recorded state moved, and the full quality gate and both key-links gates are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness → working tree | The harness applies real patches to real source files and must always restore them. |
| this phase's plans → other phases' recorded state | A cross-phase edit made to turn a gate green would erase another phase's evidence. |
| a registered patch → the guarantee it claims to prove | A stale or mis-targeted patch reports confidence while proving nothing. |
| a ticked requirement → the project's record of what shipped | A tick with no evidence is a false claim that survives the milestone. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-03-01 | Tampering | a patch left applied after a failed run | medium | mitigate | The harness reverts via its deferred and cleanup paths and asserts a clean porcelain status; both harness verifies additionally assert the mutated source trees are clean. |
| T-06-03-02 | Repudiation | a stale patch that no longer proves its gate RED | high | mitigate | Each patch is hand-verified against its target's own failure line before registration, the harness re-proves all fifty-eight on every full run, and the verify rejects a run in which a target passed with its patch applied. |
| T-06-03-03 | Repudiation | a target name that resolves to no test, reporting a permanent false green | high | mitigate | Every target is re-resolved with `go test -list` before it is relied on, an acceptance criterion requires that resolution to be recorded, and the harness's own set-equality check rejects an unmapped patch. |
| T-06-03-04 | Tampering | an earlier phase's patch silently broken by this phase's edits to `tools.go` and `client_common.go` | high | mitigate | The whole harness — not just this phase's subtest — is run and required to confirm fifty-eight; the eight at-risk patches are named in the plan, and an acceptance criterion requires the SUMMARY to state they still applied. |
| T-06-03-05 | Repudiation | a requirement ticked without shipped evidence | high | mitigate | The readiness helper is queried before ticking, the totals are asserted by count so no extra box can move, both roadmap success criteria must be named with their evidence, and the whole gate must be green first. |
| T-06-03-06 | Tampering | another phase's recorded state edited to make this gate green | high | mitigate | A verify diffs all five earlier phase directories and requires them unchanged; the executor notes and a prohibition forbid the edit outright. |
| T-06-03-07 | Tampering | the roadmap's progress update rewriting a shipped milestone's rows | medium | mitigate | The plan requires a `git diff -U0` audit of the roadmap immediately after the update, with any hunk outside the active milestone reverted and the audit's result recorded. |
| T-06-03-08 | Denial of Service | the harness outgrowing its timeout at fifty-eight patches | medium | mitigate | The verifies pass an explicit generous timeout; a prohibition forbids raising a timeout in the task runner or CI to hide it, and a genuine overrun is reported rather than papered over. |
| T-06-03-SC | Tampering | npm/pip/cargo installs | low | accept | Not applicable: no installs, no manifest change. |
</threat_model>

<verification>
- `ENGRAM_REQUIRE_QDRANT=1 go test ./internal/store/ -run '^TestRedEvidencePatchesAreLive$' -count=1 -v -timeout 180m` shows 58 `confirmed RED:` lines and exits 0, leaving the tree clean.
- The red-evidence directory holds exactly five patches, each a single-file diff with no licence header, each mapped to a target resolved with `go test -list`.
- `.planning/REQUIREMENTS.md` shows eighteen ticked and two unticked requirements, with this phase's traceability row complete and no other row moved.
- `git diff --exit-code HEAD` over the five earlier phase directories exits 0.
- `task` exits 0; `task license:check` exits 0; `go test ./internal/keylinks/ -count=1` exits 0.
- `git status --porcelain` prints nothing after the final commit.
</verification>

<success_criteria>
- Every guarantee this phase added — the helper reporting its failure rather than swallowing it, each lane keeping its already-authorized hits, absence never degraded to an empty list, and the CLI's third form — has a registered, live RED proof.
- The earlier phases' patches over the two files this phase edited are confirmed still live rather than assumed.
- REQ-cross-spine-partial is marked complete on the evidence that actually shipped, and the phase closes on a green `task` with no other phase's recorded state disturbed.
</success_criteria>

<output>
Create `.planning/phases/06-cross-spine-partial-results/06-03-SUMMARY.md` when done.
</output>

