---
phase: 03-shared-bounded-read-mechanism-content-cap-decision
plan: 06
type: execute
wave: 4
depends_on:
  - 03-01
  - 03-02
  - 03-03
  - 03-04
  - 03-05
files_modified:
  - internal/store/redevidence_harness_test.go
  - .planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/red-evidence/03-01-content-cap-removed.patch
  - .planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/red-evidence/03-01-tags-cap-removed.patch
  - .planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/red-evidence/03-01-config-accepts-zero.patch
  - .planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/red-evidence/03-02-sweep-count-not-byte-derived.patch
  - .planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/red-evidence/03-02-ceiling-drops-citations.patch
  - .planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/red-evidence/03-02-sweep-fallback-removed.patch
  - .planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/red-evidence/03-02-sweep-swallows-single-overflow.patch
  - .planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/red-evidence/03-03-update-content-cap-removed.patch
  - .planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/red-evidence/03-03-update-gates-on-presence-not-change.patch
  - .planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/red-evidence/03-03-record-caps-not-wired.patch
  - .planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/red-evidence/03-04-ordered-page-ignores-page-budget.patch
  - .planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/red-evidence/03-04-budget-cut-reported-exhausted.patch
  - .planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/red-evidence/03-04-ordered-page-tie-exclusion-removed.patch
autonomous: true
requirements:
  - REQ-byte-budget-pages
  - REQ-content-cap-decided

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`redEvidenceDirs` gains exactly one new entry, keyed `.planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/red-evidence`, mapping thirteen patches to their target tests; the Phase 1 and Phase 2 entries are byte-unchanged"
    - "Each Phase 3 patch is one behavioral mutation that compiles and makes its named target test itself report `--- FAIL:` (never a build break), hand-verified with `git apply --check` / `apply` / `go test -run '^Target$'` / `apply -R` before registration"
    - "The registered directions cover both requirements: content cap removed from the create paths (D-01) and from `deps.updateMemory` (D-09), tags cap removed (D-10), config accepting `0` (D-09), the update gate widened from `contentChanged` to presence (legacy lockout, D-07), the production store not receiving the configured caps (D-02/D-09), the sweep count not derived from the byte budget (D-02), the ceiling dropping citations (the content-only pitfall), the sweep fallback removed and a single-record overflow swallowed (D-07), the ordered page ignoring its page budget (D-02), a budget-cut page reported as exhausted (REQ-list-contract-unchanged precondition), and the tie exclusion removed (D-03)"
    - "`TestRedEvidencePatchesAreLive` passes with twenty-five `confirmed RED:` lines (Phase 1's four, Phase 2's eight, Phase 3's thirteen) and leaves every mutated file clean"
    - "`task` (lint + test, default per-package timeout) exits 0 for the whole repository, and `go test ./internal/keylinks/ -count=1` exits 0 with every executed Phase 3 plan's key_links satisfiable"
    - "No red-evidence patch carries an SPDX header"
  artifacts:
    - path: "internal/store/redevidence_harness_test.go"
      provides: "redEvidenceDirs entry for Phase 3's thirteen patches"
      contains: "03-shared-bounded-read-mechanism-content-cap-decision/red-evidence"
    - path: ".planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/red-evidence/03-03-update-content-cap-removed.patch"
      provides: "RED evidence that Connect's UpdateMemory lane is capped inside deps.updateMemory (D-09)"
      contains: "checkContentBytes"
    - path: ".planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/red-evidence/03-04-ordered-page-ignores-page-budget.patch"
      provides: "RED evidence that the ordered page stops on accumulated bytes (D-02)"
      contains: "pageByteBudget"
  key_links:
    - from: "internal/store/redevidence_harness_test.go"
      to: ".planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/red-evidence/03-01-content-cap-removed.patch"
      via: "the harness applies every registered patch and requires its target test to fail"
      pattern: "03-01-content-cap-removed[.]patch"
    - from: "internal/store/redevidence_harness_test.go"
      to: ".planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/red-evidence/03-04-ordered-page-tie-exclusion-removed.patch"
      via: "the last registered direction of the phase"
      pattern: "TestScrollOrderedPageTiesAcrossRPCBoundaries"
---

<objective>
Make every Phase 3 guarantee a live, harness-checked RED proof, then close the phase on a green full
gate. This is the phase's LAST plan: its final task registers Phase 3's entry in `redEvidenceDirs`
after every other plan has shipped, exactly as plan 02-04 did for Phase 2.

Decisions covered: every lane of D-01, D-02, D-03, D-07, D-09 and D-10 gets one patch that reverts
it; D-04/D-06/D-08 are exercised through the same target tests. Framing (D-00): the existing harness
and its one-mutation-per-patch discipline, unchanged unless its runtime forces the pre-authorized
narrowing below.

Harness runtime contingency (pre-authorized, applied only if needed): the harness's nested
`go test -run '^Target$' -count=1 ./...` recompiles and starts every package per patch; with
twenty-five patches the `internal/store` test binary may exceed `go test`'s default 10-minute timeout
under `task`. If — and only if — `ENGRAM_REQUIRE_QDRANT=1 go test ./internal/store/ -count=1` (no
`-timeout` flag) panics with `test timed out after 10m0s` inside `TestRedEvidencePatchesAreLive`,
narrow the nested run to the one package that declares the target test (details in Task 2). Never
raise a timeout in `Taskfile.yaml` or CI to make the gate pass.

Purpose: REQ-byte-budget-pages and REQ-content-cap-decided proven RED-able for the rest of the open
milestone.

Output: thirteen `.patch` files and one harness entry.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-error-classification-resourceexhausted-mapping/02-04-SUMMARY.md
@.planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/03-01-SUMMARY.md
@.planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/03-02-SUMMARY.md
@.planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/03-03-SUMMARY.md
@.planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/03-04-SUMMARY.md
@.planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/03-05-SUMMARY.md
@CLAUDE.md

<interfaces>
`internal/store/redevidence_harness_test.go`: `var redEvidenceDirs = map[string]map[string]string{…}` — one entry per phase directory (repo-root-relative), each mapping `"<patch file>": "<TargetTest>"` with a trailing `// reverts: …` comment. `TestRedEvidencePatchesAreLive` (skipped under `-short`) globs `<dir>/*.patch`, requires set equality with the mapping, and per patch: `git apply --check`, `git apply`, `go test -run '^Target$' -count=1 ./...` (must exit non-zero), `git apply -R`, then asserts the touched files are clean; it logs `confirmed RED: <patch> applied -> <Target> failed as expected`. It refuses to run if a file a patch touches is already dirty, and must never run concurrently with itself.

Per-patch authoring procedure (Phase 1/2 precedent, 01-05 and 02-04 SUMMARYs): make the ONE mutation on a clean tree, write `git diff -- <file> > <dir>/<name>.patch`, `git checkout -- <file>` is NOT used — revert with `git apply -R <patch>`; then prove it: `git apply --check <patch> && git apply <patch> && go test -run '^Target$' -count=1 ./<pkg>/ -v` shows the target's OWN `--- FAIL: Target` line (a compile error is not acceptable), then `git apply -R <patch>` and `git status --porcelain -- <file>` prints nothing. Each patch has exactly one `diff --git` header and no SPDX header.

Target tests (created by plans 03-01..03-04; re-resolve with `go test -list` before relying on a name, durable record `bsbsvn4hbc`): `internal/server`: `TestMemoryWriteCapsRejectOnEveryCreateLane`, `TestMemoryWriteCapBoundaries`, `TestUpdateMemoryContentCap`, `TestUpdateMemoryLegacyOversizedRecord`, `TestStoreFromConfigCarriesRecordCaps`; `internal/config`: `TestMemoryCapsRejectZeroAndNonPositive`; `internal/store`: `TestScrollAllPointsByteBudget`, `TestRecordCeilingHoldsForMaxCapRecord`, `TestScrollAllPointsBatchOfOneFallback`, `TestScrollAllPointsSingleOversizedRecordFailsNamed`, `TestScrollOrderedPageByteBudget`, `TestScrollOrderedPageTiesAcrossRPCBoundaries`.

Mutation sites (created by plans 03-01..03-04): `internal/server/tools.go` — the `checkContentBytes(a.Content, caps.contentBytes)` and `checkTags(a.Tags, caps.tags, caps.tagBytes)` call blocks in `validateStoreArgs`; the `checkContentBytes(*a.Content, caps.contentBytes)` block and its `contentChanged` gate in `deps.updateMemory`; `storeFromConfig`'s `store.WithRecordCaps(recordCapsFromConfig(cfg))` argument. `internal/config/validate.go` — `validatePositiveCap`'s zero arm. `internal/store/boundedread.go` — `sweepLimit`'s budgeted branch; `fullRecordCeiling`'s `Citations*(...)` term. `internal/store/spine.go` — `scrollAllPoints`' batch-of-1 branch and its count-1 error return. `internal/store/orderedpage.go` — the remaining-page-budget clamp in the per-RPC count, the `CutByBudget = true` assignment, and `excludeSeen`.
</interfaces>
</context>

## Artifacts this phase produces (plan 03-06's share)

Nothing below exists on HEAD today.

| Kind | Artifact | Created by |
|------|----------|------------|
| Directory | `.planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/red-evidence/` | Task 1 |
| Patches | `03-01-content-cap-removed.patch` (Task 1); `03-01-tags-cap-removed.patch`, `03-01-config-accepts-zero.patch`, `03-02-sweep-count-not-byte-derived.patch`, `03-02-ceiling-drops-citations.patch`, `03-02-sweep-fallback-removed.patch`, `03-02-sweep-swallows-single-overflow.patch`, `03-03-update-content-cap-removed.patch`, `03-03-update-gates-on-presence-not-change.patch`, `03-03-record-caps-not-wired.patch`, `03-04-ordered-page-ignores-page-budget.patch`, `03-04-budget-cut-reported-exhausted.patch`, `03-04-ordered-page-tie-exclusion-removed.patch` (Task 2) | Tasks 1-2 |
| Harness entry | `redEvidenceDirs[".planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/red-evidence"]` | Tasks 1-2 |
| Conditional | `packageOfTest` helper and the narrowed nested run in the harness — only if the runtime contingency fires | Task 2 |

## Executor notes (every task)

- NEVER run any git stash subcommand — to inspect a pristine file use `git show HEAD:<path>` into the scratchpad instead.
- Commit with an explicit pathspec (rule `n6m4as49mr`), Conventional Commits, ending the message with the session's attribution trailer lines. Commit signing is off: commit normally and never add signing configuration.
- Every patch is authored and proven on a CLEAN tree, one at a time; never run two harness or patch cycles concurrently (the harness doc comment records a 2026-08-16 incident).
- Patches under `.planning/**` carry NO SPDX header.
- Real-Qdrant targets run with `ENGRAM_REQUIRE_QDRANT=1`; a skipped target is not a RED.

<tasks>

<task type="tracer">
  <name>Task 1: One lane's RED proof end to end: the create-path content-cap patch, registered and confirmed by the harness</name>
  <files>internal/store/redevidence_harness_test.go, .planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/red-evidence/03-01-content-cap-removed.patch</files>
  <precondition>Plans 03-01 through 03-05 are executed (their SUMMARYs exist), the working tree is clean (`git status --porcelain` shows no modified tracked file), and Docker is reachable (`docker info` exits 0) or ENGRAM_QDRANT_TEST_ADDR names a live Qdrant gRPC endpoint.</precondition>
  <read_first>
    - `internal/store/redevidence_harness_test.go` (whole file).
    - `.planning/phases/02-error-classification-resourceexhausted-mapping/02-04-SUMMARY.md` and one of its patches (shape precedent).
    - `internal/server/tools.go` `validateStoreArgs` and `internal/server/contentcap_test.go` `TestMemoryWriteCapsRejectOnEveryCreateLane`.
  </read_first>
  <action>
Create `.planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/red-evidence/` and author `03-01-content-cap-removed.patch` by the per-patch procedure: in `internal/server/tools.go`'s `validateStoreArgs`, delete the `checkContentBytes(a.Content, caps.contentBytes)` check block (its `if` line, `return` line and closing brace) — the helper stays, so the tree compiles. Target `TestMemoryWriteCapsRejectOnEveryCreateLane` (expected: every `content-too-long` subtest fails because the write is stored). Prove it RED by hand and record the `--- FAIL:` line.

Register it: add ONE new top-level entry to `redEvidenceDirs`, keyed `".planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/red-evidence"`, mapping `"03-01-content-cap-removed.patch"` → `"TestMemoryWriteCapsRejectOnEveryCreateLane"` with the trailing comment `// reverts: validateStoreArgs' content cap on store/schedule/supersede, MCP and Connect (D-01)`. Leave the Phase 1 and Phase 2 entries and everything else untouched.

Run the harness once, alone (verify). Commit as `test(store): register phase 3 red-evidence for the create-path content cap` with pathspec `internal/store/redevidence_harness_test.go .planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/red-evidence`.
  </action>
  <verify>
    <automated>out=$(ENGRAM_REQUIRE_QDRANT=1 go test ./internal/store/ -run '^TestRedEvidencePatchesAreLive$' -count=1 -v -timeout 30m 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e 'confirmed RED|^\s*--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o 'confirmed RED: ' | wc -l)" -eq 13 &amp;&amp; test -z "$(git status --porcelain -- internal/server/tools.go)"</automated>
    <fails_when>non-zero exit, a `--- FAIL:` line, a `confirmed RED:` count other than 13 (Phases 1-2's twelve plus this one), or `internal/server/tools.go` left dirty</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -o '^diff --[g]it a/' .planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/red-evidence/03-01-content-cap-removed.patch | wc -l` prints `1`, and its first line is `diff --git a/internal/server/tools.go b/internal/server/tools.go`.
    - `rg -n -F '".planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/red-evidence"' internal/store/redevidence_harness_test.go` prints exactly one line; with `C` set to the commit SHA of this task (record it in the SUMMARY), `d=$(git diff "$C^..$C" -- internal/store/redevidence_harness_test.go) || exit 1; printf '%s\n' "$d" | rg '^-[^-]' | wc -l` prints `0` (the task only adds lines).
    - The SUMMARY records the hand-verified `--- FAIL: TestMemoryWriteCapsRejectOnEveryCreateLane` line.
  </acceptance_criteria>
  <done>The create-path content-cap RED direction is a registered, harness-confirmed patch; the harness passes with thirteen confirmed REDs and leaves the tree clean; committed with an explicit pathspec.</done>
</task>

<task type="auto">
  <name>Task 2 (LAST task of the phase): The remaining twelve RED directions registered, and the full quality gate green</name>
  <files>internal/store/redevidence_harness_test.go, .planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/red-evidence/03-01-tags-cap-removed.patch, .planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/red-evidence/03-01-config-accepts-zero.patch, .planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/red-evidence/03-02-sweep-count-not-byte-derived.patch, .planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/red-evidence/03-02-ceiling-drops-citations.patch, .planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/red-evidence/03-02-sweep-fallback-removed.patch, .planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/red-evidence/03-02-sweep-swallows-single-overflow.patch, .planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/red-evidence/03-03-update-content-cap-removed.patch, .planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/red-evidence/03-03-update-gates-on-presence-not-change.patch, .planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/red-evidence/03-03-record-caps-not-wired.patch, .planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/red-evidence/03-04-ordered-page-ignores-page-budget.patch, .planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/red-evidence/03-04-budget-cut-reported-exhausted.patch, .planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/red-evidence/03-04-ordered-page-tie-exclusion-removed.patch</files>
  <precondition>Task 1 is committed, the working tree is clean, and Docker is reachable (`docker info` exits 0) or ENGRAM_QDRANT_TEST_ADDR names a live Qdrant gRPC endpoint.</precondition>
  <read_first>
    - `internal/server/tools.go` (`validateStoreArgs`, `deps.updateMemory`, `storeFromConfig`), `internal/config/validate.go` (`validatePositiveCap`), `internal/store/boundedread.go` (`sweepLimit`, `fullRecordCeiling`), `internal/store/spine.go` (`scrollAllPoints`), `internal/store/orderedpage.go` (`scrollOrderedPage`, `excludeSeen`).
    - The target test files: `internal/server/contentcap_test.go`, `internal/server/updatecap_test.go`, `internal/server/recordcaps_test.go`, `internal/config/validate_test.go`, `internal/store/boundedread_oversized_test.go`, `internal/store/orderedpage_oversized_test.go`.
    - `internal/store/redevidence_harness_test.go` (after Task 1).
  </read_first>
  <action>
Author twelve patches in the same directory by the per-patch procedure, each the smallest mutation that makes its target test itself fail while the tree still compiles:

1. `03-01-tags-cap-removed.patch` → `TestMemoryWriteCapBoundaries` (D-10): delete the `checkTags(a.Tags, caps.tags, caps.tagBytes)` check block in `validateStoreArgs`.
2. `03-01-config-accepts-zero.patch` → `TestMemoryCapsRejectZeroAndNonPositive` (D-09): delete the zero arm of `validatePositiveCap` so `0` passes validation.
3. `03-02-sweep-count-not-byte-derived.patch` → `TestScrollAllPointsByteBudget` (D-02): make `sweepLimit` return `spineScrollBatch` for budgeted views too (the recorded `Limit` becomes 256).
4. `03-02-ceiling-drops-citations.patch` → `TestRecordCeilingHoldsForMaxCapRecord` (RESEARCH Pitfall "content alone"): drop the `Citations*(CitationExcerptBytes+citationEntryAllowance)` term from `fullRecordCeiling`; the max-cap record's `proto.Size` then exceeds the full-view ceiling.
5. `03-02-sweep-fallback-removed.patch` → `TestScrollAllPointsBatchOfOneFallback` (D-07): delete the batch-of-1 branch in `scrollAllPoints` so the first overflow is returned.
6. `03-02-sweep-swallows-single-overflow.patch` → `TestScrollAllPointsSingleOversizedRecordFailsNamed` (D-07, no silent skip): make an overflow at count 1 end the sweep with `return nil`.
7. `03-03-update-content-cap-removed.patch` → `TestUpdateMemoryContentCap` (D-09): delete the `checkContentBytes(*a.Content, caps.contentBytes)` block in `deps.updateMemory`.
8. `03-03-update-gates-on-presence-not-change.patch` → `TestUpdateMemoryLegacyOversizedRecord` (D-07/D-09): replace that block's `contentChanged` gate with `a.Content != nil`, so resending a legacy record's unchanged content is rejected.
9. `03-03-record-caps-not-wired.patch` → `TestStoreFromConfigCarriesRecordCaps` (D-02/D-09): drop the `store.WithRecordCaps(recordCapsFromConfig(cfg))` argument from `storeFromConfig` (`recordCapsFromConfig` stays referenced by its unit test, so the tree still compiles and lints).
10. `03-04-ordered-page-ignores-page-budget.patch` → `TestScrollOrderedPageByteBudget` (D-02): remove the remaining-page-budget term from the per-RPC count in `scrollOrderedPage`.
11. `03-04-budget-cut-reported-exhausted.patch` → `TestScrollOrderedPageByteBudget` (REQ-list-contract-unchanged precondition): also set `Exhausted = true` where the page sets `CutByBudget = true`.
12. `03-04-ordered-page-tie-exclusion-removed.patch` → `TestScrollOrderedPageTiesAcrossRPCBoundaries` (D-03): make `excludeSeen` return `f` unchanged, so ids tied at the boundary come back again.

Register all twelve in the Phase 3 entry Task 1 created, each with a trailing `// reverts: …` comment naming what the mutation undoes and its decision id. Change nothing else in the harness.

Run the harness once, alone, then the default-timeout package run, then the full gate (verify). Runtime contingency: if the default-timeout run panics with `test timed out after 10m0s` inside `TestRedEvidencePatchesAreLive`, narrow the harness's nested run — add `func packageOfTest(t *testing.T, root, target string) string` that walks the module (skipping `.git`, `node_modules`, `docs-site`, `ui`, `.planning`) for `_test.go` files containing a line matching `^func <target>[(]t [*]testing[.]T[)]` and returns `./<that dir>`, failing the subtest when zero or more than one package matches; pass its result instead of `./...`; update the doc comment's "Nested `go test`" paragraph to describe the scoping; re-run all three verifies; record the before/after durations in the SUMMARY. Never raise a timeout in `Taskfile.yaml` or CI. If `task` shows any failure this plan did not cause, stop and report it rather than editing unrelated code.

Commit as `test(store): register phase 3's remaining red-evidence and close the phase gate` with pathspec `internal/store/redevidence_harness_test.go .planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/red-evidence`.
  </action>
  <verify>
    <automated>out=$(ENGRAM_REQUIRE_QDRANT=1 go test ./internal/store/ -run '^TestRedEvidencePatchesAreLive$' -count=1 -v -timeout 60m 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e 'confirmed RED|^\s*--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o 'confirmed RED: ' | wc -l)" -eq 25 &amp;&amp; test -z "$(git status --porcelain -- internal/server/tools.go internal/config/validate.go internal/store/boundedread.go internal/store/spine.go internal/store/orderedpage.go)"</automated>
    <fails_when>non-zero exit, a `--- FAIL:` line, a `confirmed RED:` count other than 25, or any mutated source file left dirty after the run</fails_when>
    <automated>ENGRAM_REQUIRE_QDRANT=1 go test ./internal/store/ -count=1</automated>
    <fails_when>non-zero exit, including a `panic: test timed out after 10m0s` (the runtime contingency applies)</fails_when>
    <automated>task</automated>
    <fails_when>non-zero exit from `task` (lint or test), including any `--- FAIL:` or `FAIL` package line</fails_when>
    <automated>go test ./internal/keylinks/ -count=1 -v 2>&amp;1 | rg -e '^--- |^ok|^FAIL'; go test ./internal/keylinks/ -count=1</automated>
    <fails_when>non-zero exit from the second command, or a `--- FAIL:` line naming TestNoEscapedPatternsRepoWide or TestActiveMilestoneKeyLinksSatisfiable (a Phase 3 key_links pattern is escaped or no longer matches the shipped code)</fails_when>
  </verify>
  <acceptance_criteria>
    - `ls .planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/red-evidence/*.patch | wc -l` prints `13`, and each patch's `rg -o '^diff --[g]it a/' <patch> | wc -l` prints `1`.
    - `rg -l 'SPDX-License-Identifier' .planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/red-evidence/` prints nothing; `task license:check` exits 0.
    - The Phase 3 `redEvidenceDirs` entry maps exactly the thirteen patch names; the Phase 1 and Phase 2 entries are unchanged: with `C` the Task 1 commit SHA recorded in the SUMMARY, `d=$(git diff "$C^" -- internal/store/redevidence_harness_test.go) || exit 1; printf '%s\n' "$d" | rg -o '^-\s+"0[12]-' | wc -l` prints `0`.
    - The SUMMARY carries a table of all thirteen patches with each target's hand-verified `--- FAIL:` line, and states whether the runtime contingency fired (with durations if it did).
    - `git status --porcelain` after the final commit shows no modified tracked file.
  </acceptance_criteria>
  <done>Thirteen hand-verified Phase 3 patches are registered, the harness confirms twenty-five REDs with a clean tree, `task` and the keylinks gates are green; committed with an explicit pathspec.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Harness → working tree | The harness applies real patches to real source files and must always restore them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-06-01 | Tampering | A patch left applied after a failed run | medium | mitigate | The harness reverts via `defer` and `t.Cleanup` and asserts a clean `git status --porcelain`; the verify also checks every mutated file is clean. |
| T-03-06-02 | Repudiation | A stale patch that no longer proves its gate RED | medium | mitigate | Each patch is hand-verified against its target's own `--- FAIL:` line before registration, and the harness re-proves all twenty-five on every full run. |
| T-03-06-03 | Denial of Service | The harness outgrowing the default test timeout and turning `task` red | low | mitigate | Measured under the default timeout; the pre-authorized per-package narrowing applies only if it fires; timeouts are never raised to hide it. |
| T-03-06-SC | Tampering | npm/pip/cargo installs | low | accept | Not applicable: no installs, no new modules. |
</threat_model>

<verification>
- `ENGRAM_REQUIRE_QDRANT=1 go test ./internal/store/ -run '^TestRedEvidencePatchesAreLive$' -count=1 -v -timeout 60m` shows 25 `confirmed RED:` lines and exits 0.
- `task` exits 0; `task license:check` exits 0; `go test ./internal/keylinks/ -count=1` exits 0; `git diff --exit-code HEAD -- go.mod go.sum` exits 0.
</verification>

<success_criteria>
- Every Phase 3 guarantee (both primitives' byte bounds, the batch-of-1 fallback and named failure, the page contract, the tie-safe keyset, the caps on every write lane, the always-enforced config, the legacy-record contract, the production ceiling wiring) has a registered, live RED proof.
- The phase closes on a green `task`.
</success_criteria>

<output>
Create `.planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/03-06-SUMMARY.md` when done.
</output>
