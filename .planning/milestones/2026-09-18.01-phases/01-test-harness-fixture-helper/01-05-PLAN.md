---
phase: 01-test-harness-fixture-helper
plan: 05
type: execute
wave: 4
depends_on:
  - 01-03
  - 01-04
files_modified:
  - internal/store/qdrant_client_convergence_test.go
  - internal/store/testdata/qdrantclient/good_store.go.txt
  - internal/store/testdata/qdrantclient/bad_test.go.txt
  - internal/store/testdata/qdrantclient/bad_aliased_test.go.txt
  - internal/store/testdata/qdrantclient/bad_store.go.txt
  - internal/store/storetest/cipin_test.go
  - .github/workflows/ci.yaml
  - internal/store/redevidence_harness_test.go
  - .planning/phases/01-test-harness-fixture-helper/red-evidence/01-01-storetest-raw-client-write.patch
  - .planning/phases/01-test-harness-fixture-helper/red-evidence/01-04-listscopes-full-payload-selector.patch
  - .planning/phases/01-test-harness-fixture-helper/red-evidence/01-05-bare-qdrant-newclient-in-test.patch
  - .planning/phases/01-test-harness-fixture-helper/red-evidence/01-05-ci-qdrant-image-drift.patch
autonomous: true
requirements:
  - REQ-test-client-parity
  - REQ-oversized-fixture-helper

estimate:
  tokens: 66000
  raw_tokens: 66000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`TestQdrantClientConstructedOnlyByNewQdrantClient` walks the whole module INCLUDING `_test.go` files and fails on any Qdrant client construction (`NewClient`, `NewGrpcClient`, `NewDefaultGrpcClient`, `NewGrpcClientFromConn` called on the qdrant import under any local name, or a dot-import of it) anywhere except inside the receiver-less `NewQdrantClient` in `internal/store/store.go` (D-11)"
    - "On the converged tree the gate finds zero violations and exactly one sanctioned construction, and logs non-zero counts of both non-test and test files scanned, proving `_test.go` files are in scope (D-11, REQ-test-client-parity)"
    - "The gate's fixture subtests catch every bypass shape by set equality: a direct call in a test helper, a sibling constructor, an aliased import, a dot-import, and a second construction in another function of `internal/store/store.go`, while the good fixture yields exactly one sanctioned site and no violation (RESEARCH Pitfall 5)"
    - "`TestQdrantImageMatchesCIService` fails when any `qdrant/qdrant:v...` reference in `.github/workflows/ci.yaml` differs from `storetest.QdrantImage` or the `services.qdrant` image line is missing, so the byte-identical pin is gate-enforced rather than comment-enforced (D-10)"
    - "The CI comments that pointed at `store_test.go`'s image constant, at a package-level address variable, and at server's parser now point at storetest, and `.github/workflows/ci.yaml` stays yamlfmt- and actionlint-clean with its pinned `3 PASS + 1 SKIP` step unchanged (D-10)"
    - "`redEvidenceDirs` maps `.planning/phases/01-test-harness-fixture-helper/red-evidence` to four patches, each proven RED against its named test by `TestRedEvidencePatchesAreLive`, which now passes (RESEARCH Pitfall 1 closed)"
    - "`task` (lint + test) exits 0 for the whole repository, and CI's exact shared-address command reports 3 PASS + 1 SKIP"
  artifacts:
    - path: "internal/store/qdrant_client_convergence_test.go"
      provides: "D-11 convergence gate: scanQdrantClientConstructions, scanRepoForClientConstructions, TestQdrantClientConstructedOnlyByNewQdrantClient"
      contains: "TestQdrantClientConstructedOnlyByNewQdrantClient"
      min_lines: 120
    - path: "internal/store/storetest/cipin_test.go"
      provides: "TestQdrantImageMatchesCIService"
      contains: "TestQdrantImageMatchesCIService"
    - path: "internal/store/redevidence_harness_test.go"
      provides: "redEvidenceDirs entry for this phase's four patches"
      contains: "01-test-harness-fixture-helper/red-evidence"
    - path: ".planning/phases/01-test-harness-fixture-helper/red-evidence/01-04-listscopes-full-payload-selector.patch"
      provides: "RED evidence for the migrated #583 regression test"
      contains: "NewWithPayload(true)"
  key_links:
    - from: "internal/store/qdrant_client_convergence_test.go"
      to: "internal/store/store.go"
      via: "the single sanctioned construction site is keyed by file path and enclosing function (D-11)"
      pattern: '"internal/store/store[.]go"'
    - from: "internal/store/qdrant_client_convergence_test.go"
      to: "internal/store/testdata/qdrantclient/good_store.go.txt"
      via: "the gate proves itself against good and bad fixtures before scanning the module"
      pattern: '"good_store[.]go[.]txt"'
    - from: "internal/store/storetest/cipin_test.go"
      to: ".github/workflows/ci.yaml"
      via: "the pinned CI service image must equal storetest.QdrantImage (D-10)"
      pattern: "image: qdrant/qdrant:v1[.]19[.]1"
    - from: "internal/store/redevidence_harness_test.go"
      to: ".planning/phases/01-test-harness-fixture-helper/red-evidence/01-04-listscopes-full-payload-selector.patch"
      via: "the harness applies every registered phase-1 patch and requires its target test to fail"
      pattern: "01-test-harness-fixture-helper/red-evidence"
    - from: "internal/store/redevidence_harness_test.go"
      to: "internal/store/listscopes_oversized_test.go"
      via: "the reverted payload selector must turn the migrated #583 test RED"
      pattern: '"TestListScopesFullPayloadsOverGRPCLimit"'
---

<objective>
Close the phase with its enforcement and its evidence. (1) Land D-11's convergence gate: a repo-wide
AST scan, `_test.go` files included, that permits a Qdrant client construction only inside
`store.NewQdrantClient` — now satisfiable because plans 01-02 and 01-03 converged all eleven test sites
and plan 01-01 converged production. (2) Turn D-10's "CI's `services.qdrant.image` stays byte-identical to
the tag" from a comment into a gate, and update the CI comments that still name the deleted in-package
harness. (3) As the phase's LAST task, author one red-evidence patch per new RED direction and register
them in `redEvidenceDirs`, which turns `TestRedEvidencePatchesAreLive` green (RESEARCH Pitfall 1).

Purpose: REQ-test-client-parity's "the independent `qdrant.NewClient` test call sites converge" becomes
mechanically checkable; REQ-oversized-fixture-helper's "each is RED against the pre-fix code" becomes a
live gate.

Output: `qdrant_client_convergence_test.go` + four fixtures, `storetest/cipin_test.go`, CI comment
updates, four patches, the `redEvidenceDirs` entry.

`TestRedEvidencePatchesAreLive` is RED until Task 3 of THIS plan; Tasks 1 and 2 must leave it as the only
failing test in internal/store, and Task 3 must turn it green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-test-harness-fixture-helper/01-CONTEXT.md
@.planning/phases/01-test-harness-fixture-helper/01-RESEARCH.md
@.planning/phases/01-test-harness-fixture-helper/01-PATTERNS.md
@.planning/phases/01-test-harness-fixture-helper/01-01-SUMMARY.md
@.planning/phases/01-test-harness-fixture-helper/01-04-SUMMARY.md
@CLAUDE.md

<interfaces>
Existing gate helpers in package `store` (`internal/store/schemaversion_stamp_gate_test.go`, verified at planning time):
- `func findModuleRoot() (string, error)` (line 768) — walks up to go.mod.
- `func enclosingFuncDisplayName(fn *ast.FuncDecl) string` (line 108) — `Name` for a function, `Type.Name` for a method.
- `scanRepoForQdrantClientRefs` (line 826) — the WalkDir skeleton (skips `gen`, `vendor`, dot-dirs) but it EXCLUDES `_test.go`; D-11's scan must not.
- `fileRefsQdrantClient`'s CallExpr branch (line 796) — the selector-matching shape.

Existing fixture convention: `internal/store/testdata/collectionprefix/{good,bad}_pkg_test.go.txt` — `.go.txt` suffix (never compiled), two-line SPDX header, a doc comment naming the gate they exercise.

qdrant-go-client v1.19.2 client constructors (verified in the module cache): `NewClient(*Config) (*Client, error)` (client.go:26), `NewDefaultGrpcClient() (*GrpcClient, error)` (grpc_client.go:28), `NewGrpcClient(*Config) (*GrpcClient, error)` (grpc_client.go:33), `NewGrpcClientFromConn(*grpc.ClientConn) *GrpcClient` (grpc_client.go:83). The generated `NewQdrantClient(grpc.ClientConnInterface)` / `NewPointsClient(...)` family wraps an already-dialed conn and is out of this gate's scope.

Red-evidence harness (`internal/store/redevidence_harness_test.go`): `redEvidenceDirs map[string]map[string]string` (line 106, empty); per patch it requires a clean touched tree, `git apply --check`, apply, then `go test -run '^<Target>$' -count=1 ./...` must exit non-zero, then `git apply -R` restores exactly. A BUILD failure also exits non-zero, so every patch must compile — the executor verifies the target's own `--- FAIL:` line appears. The harness subprocess inherits the parent's environment, so after `storetest.Run` exported a booted container's address every child package takes the shared fast path.

Prior patch shape: `.planning/milestones/2026-09-13.01-phases/01-executor-correctness-man-pages/red-evidence/01-01-osrun-ctx-err-first.patch` — a plain `git diff -U3` of one behavioral mutation, named `<plan>-<slug>.patch`.

CI (`.github/workflows/ci.yaml`): `services.qdrant.image: qdrant/qdrant:v1.19.1` with a comment (lines ~53-56) pointing at `internal/store/store_test.go`'s image constant; the shared-participation comment (~lines 92-99) naming the retrievaleval address variable; the `Test` step env comments (~111-120) naming server's parser and "All four TestMain chains"; two `docker ps --filter "ancestor=qdrant/qdrant:v1.19.1"` references in the single-container assertion.
</interfaces>
</context>

## Artifacts this phase produces (plan 01-05's share)

| Kind | Artifact | Created by |
|------|----------|------------|
| Go file | `internal/store/qdrant_client_convergence_test.go` (package `store`) | Task 1 |
| Test data | `qdrantClientConstructors` set, `qdrantImportPath` const, `clientConstruction` struct | Task 1 |
| Go funcs | `scanQdrantClientConstructions(fset, src, displayPath)`, `scanRepoForClientConstructions(fset)`, `isSanctionedConstruction(c)` | Task 1 |
| Test | `TestQdrantClientConstructedOnlyByNewQdrantClient` (subtests: good fixture, bad test-helper fixture, bad aliased/dot-import fixture, bad second-construction-in-store.go fixture, real module) | Task 1 |
| Fixtures | `internal/store/testdata/qdrantclient/{good_store.go.txt, bad_test.go.txt, bad_aliased_test.go.txt, bad_store.go.txt}` | Task 1 |
| Go file / test | `internal/store/storetest/cipin_test.go`: `TestQdrantImageMatchesCIService` (+ unexported `moduleRoot` helper) | Task 2 |
| CI comments | `.github/workflows/ci.yaml` comments now name storetest | Task 2 |
| Patches | `red-evidence/01-01-storetest-raw-client-write.patch`, `01-04-listscopes-full-payload-selector.patch`, `01-05-bare-qdrant-newclient-in-test.patch`, `01-05-ci-qdrant-image-drift.patch` | Task 3 |
| Gate data | `redEvidenceDirs[".planning/phases/01-test-harness-fixture-helper/red-evidence"]` (four mappings) | Task 3 |

## Edge coverage and prohibitions

No edge-probe rows are allocated to this plan (plans 01-01 and 01-02 author all ten). Task 3's patches
make three of the phase's prohibitions live gates: `01-01-storetest-raw-client-write.patch` (no raw-client
write path in test support) and `01-04-listscopes-full-payload-selector.patch` (the regression test
measures the request shape under the named limit, not the backstop).

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: D-11 convergence gate end to end: repo-wide scan including test files, proven on fixtures, green on the converged tree</name>
  <files>internal/store/qdrant_client_convergence_test.go, internal/store/testdata/qdrantclient/good_store.go.txt, internal/store/testdata/qdrantclient/bad_test.go.txt, internal/store/testdata/qdrantclient/bad_aliased_test.go.txt, internal/store/testdata/qdrantclient/bad_store.go.txt</files>
  <read_first>
    - `internal/store/schemaversion_stamp_gate_test.go` lines 1-130 (house style, `receiverText`, `enclosingFuncDisplayName`) and 748-998 (`findModuleRoot`, `fileRefsQdrantClient`, `scanRepoForQdrantClientRefs`, `TestQdrantClientIsHeldOnlyByStorePackage`).
    - `internal/store/collectionprefix_conformance_test.go` lines 205-270 — fixture-subtest house style (good fixture, bad fixture by SET EQUALITY, zero-applicability guard).
    - `internal/store/testdata/collectionprefix/good_pkg_test.go.txt` — fixture header/doc shape.
    - `internal/store/store.go` — `NewQdrantClient` (the one sanctioned site).
    - `.planning/phases/01-test-harness-fixture-helper/01-RESEARCH.md` Pitfall 5 (narrower predicate than `fileRefsQdrantClient`; allowed site keyed on the enclosing receiver-less FuncDecl in store.go, not merely the file).
    - `.planning/phases/01-test-harness-fixture-helper/01-CONTEXT.md` D-11.
  </read_first>
  <behavior>
    - Fixture subtests (write first; RED because the scanner does not exist yet):
      - `good fixture yields one sanctioned site`: `good_store.go.txt` scanned under display path `internal/store/store.go` → exactly one construction, `isSanctionedConstruction` true.
      - `bad test-helper fixture`: `bad_test.go.txt` (plain `qdrant` import) scanned as `internal/example/example_test.go` → the set of (enclosing function, callee) pairs equals exactly {(`dialDirect`, `NewClient`), (`dialGrpc`, `NewGrpcClient`)}, none sanctioned.
      - `bad aliased fixture`: `bad_aliased_test.go.txt` (import aliased `qc`, plus a separate dot-import spec of the same path) scanned as `internal/example/aliased_test.go` → exactly {(`dialAliased`, `NewClient`), (`<import>`, `dot-import`)}.
      - `bad second construction in store.go`: `bad_store.go.txt` scanned as `internal/store/store.go` → one sanctioned (`NewQdrantClient`) plus exactly one violation (`sneakyDial`, `NewClient`) — a second construction in the right FILE still trips the gate (Pitfall 5).
    - `real module` subtest: zero violations (each reported with path:line, enclosing function and callee), exactly one sanctioned site, and a `t.Logf` of `scanned N non-test and M test .go files` where both N and M are non-zero (the zero-applicability guard fails the test otherwise).
  </behavior>
  <action>
NEVER run any git stash subcommand — to inspect a pristine file use `git show HEAD:<path>` into the scratchpad instead. Commit with an explicit pathspec (rule n6m4as49mr). `TestRedEvidencePatchesAreLive` is the single expected red until Task 3 of this plan.

Create `internal/store/qdrant_client_convergence_test.go` (package `store`, SPDX header) implementing D-11 with a NEW, narrower walker (do not reuse `fileRefsQdrantClient`/`scanRepoForQdrantClientRefs` — they conflate type references with calls and exclude `_test.go`; naming the `*qdrant.Client` type stays legal everywhere):
- File doc comment: what the gate proves (every Qdrant client construction in the module, tests included, goes through `store.NewQdrantClient`, so a regression test cannot silently dial with options production does not use — REQ-test-client-parity) and what it does not (an interface-carried or reflection-built client, and the generated conn-wrapping service-client constructors, which need an already-dialed connection).
- `const qdrantImportPath = "github.com/qdrant/go-client/qdrant"`; `var qdrantClientConstructors = map[string]bool{"NewClient": true, "NewGrpcClient": true, "NewDefaultGrpcClient": true, "NewGrpcClientFromConn": true}` with a comment citing the v1.19.2 source lines listed in this plan's interfaces.
- `type clientConstruction struct { path string; line int; enclosingFunc string; callee string }` with a `String()` rendering `path:line: callee in enclosingFunc`.
- `func scanQdrantClientConstructions(fset *token.FileSet, src []byte, displayPath string) ([]clientConstruction, error)`: parse; collect the local name of EVERY import spec whose path is `qdrantImportPath` (default name `qdrant`, an explicit alias, `_` ignored); a `.` import is itself recorded as a construction with enclosing function `<import>` and callee `dot-import` (it would make unqualified calls invisible); no qdrant import → return nil. Walk each top-level decl like `scanQdrantCalls` does — `enclosingFuncDisplayName` for FuncDecls, `<package-level>` otherwise — and record every `*ast.CallExpr` whose `Fun` is a selector on one of the collected local names with `Sel.Name` in `qdrantClientConstructors`.
- `func isSanctionedConstruction(c clientConstruction) bool`: true only when `c.path == "internal/store/store.go"` AND `c.enclosingFunc == "NewQdrantClient"` (a method named NewQdrantClient renders as `Type.NewQdrantClient` and is NOT sanctioned).
- `func scanRepoForClientConstructions(fset *token.FileSet) (sites []clientConstruction, nonTestFiles, testFiles int, err error)`: `findModuleRoot`, then `filepath.WalkDir` skipping directories named `gen`, `vendor`, `testdata`, and dot-prefixed directories (except the root), scanning EVERY `.go` file including `_test.go`, with the module-root-relative forward-slash path as display path; count test and non-test files separately.
- `TestQdrantClientConstructedOnlyByNewQdrantClient` with the subtests in `<behavior>`, fixtures read from `filepath.Join("testdata", "qdrantclient", "<name>")`, bad-fixture assertions by set equality of (enclosingFunc, callee) pairs — never a count-only or contains check.

Create the four fixtures under `internal/store/testdata/qdrantclient/` (two-line SPDX header, a doc comment naming this gate and what the fixture proves, a synthetic package name; parse-only, never compiled): `good_store.go.txt` (a receiver-less `NewQdrantClient(host string, port int, opts ...grpc.DialOption)` returning a qdrant client construction); `bad_test.go.txt` (`dialDirect` calling `NewClient`, `dialGrpc` calling `NewGrpcClient`, on the plain `qdrant` import); `bad_aliased_test.go.txt` (import aliased `qc` with `dialAliased` calling `qc.NewClient`, plus a separate dot-import spec of the same path); `bad_store.go.txt` (the sanctioned `NewQdrantClient` plus a `sneakyDial` function calling `qdrant.NewClient`).

Commit as `test(store): gate every Qdrant client construction through NewQdrantClient (D-11)` with pathspec `internal/store/qdrant_client_convergence_test.go internal/store/testdata/qdrantclient`, ending the message with the session's attribution trailer lines.
  </action>
  <verify>
    <automated>out=$(go test ./internal/store/ -run '^TestQdrantClientConstructedOnlyByNewQdrantClient$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^\s*--- |scanned [0-9]+ non-test|^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e '    --- PASS: TestQdrantClientConstructedOnlyByNewQdrantClient/' | wc -l)" -eq 5 &amp;&amp; printf '%s\n' "$out" | rg -q 'scanned [1-9][0-9]* non-test and [1-9][0-9]* test [.]go files'</automated>
    <fails_when>non-zero exit, a `--- FAIL:` line, fewer than five passing subtests, or the scan-count log line missing or showing zero test files (the scan did not include `_test.go`)</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n -F '"internal/store/store.go"' internal/store/qdrant_client_convergence_test.go` prints at least one line and `rg -n -F '"NewQdrantClient"' internal/store/qdrant_client_convergence_test.go` prints at least one line.
    - `rg -o 'HasSuffix[(][^)]*_test[.]go' internal/store/qdrant_client_convergence_test.go | wc -l` prints `0` (the walker never excludes test files).
    - `ls internal/store/testdata/qdrantclient/` lists exactly the four fixture files, each starting with the SPDX header (`head -1` prints `// SPDX-License-Identifier: Apache-2.0`).
    - RED proof recorded in the SUMMARY: temporarily add one `qdrant.NewClient` call inside any existing `_test.go` helper outside internal/store that already imports the qdrant package, run the gate, observe `--- FAIL: TestQdrantClientConstructedOnlyByNewQdrantClient` naming that file, and restore it via `git diff` to a scratchpad patch plus `git apply -R` (clean `git status --porcelain` for that file afterward).
    - `go vet ./internal/store/` exits 0; `golangci-lint run ./internal/store/...` reports no findings; `task license:check` exits 0.
  </acceptance_criteria>
  <done>The D-11 gate scans every .go file in the module including tests, proves each bypass shape on fixtures by set equality, is green on the converged tree with exactly one sanctioned site, and was observed RED against a test-file bypass; committed with an explicit pathspec.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CI's pinned Qdrant image is gate-enforced against storetest.QdrantImage, and CI comments name storetest</name>
  <files>internal/store/storetest/cipin_test.go, .github/workflows/ci.yaml</files>
  <read_first>
    - `.github/workflows/ci.yaml` lines 20-160 (the `test` job: `services.qdrant` and its pin comment, the single-container assertion, the shared-participation step and its comment, the `Test` step env comments).
    - `internal/store/storetest/storetest.go` — `QdrantImage` and its doc comment.
    - `.planning/phases/01-test-harness-fixture-helper/01-CONTEXT.md` D-10; `01-RESEARCH.md` Runtime State Inventory (the stale CI comment pointer).
  </read_first>
  <behavior>
    - `TestQdrantImageMatchesCIService` (package `storetest`, no Qdrant needed): resolves the module root by walking up to `go.mod`; reads `filepath.Join(root, ".github", "workflows", "ci.yaml")`; finds every match of `qdrant/qdrant:v[0-9][0-9A-Za-z.+-]*`; fails if there are zero matches (zero-applicability guard), if any match differs from `QdrantImage`, or if the text lacks `image: ` followed by `QdrantImage`.
    - RED proof: change only the `services.qdrant` `image:` line to `qdrant/qdrant:v1.19.2`, observe `--- FAIL: TestQdrantImageMatchesCIService`, restore via `git diff` to a scratchpad patch plus `git apply -R` (Task 3 re-creates this as a registered patch).
  </behavior>
  <action>
NEVER run any git stash subcommand — to inspect a pristine file use `git show HEAD:<path>` into the scratchpad instead. Commit with an explicit pathspec (rule n6m4as49mr). `TestRedEvidencePatchesAreLive` is the single expected red until Task 3.

(1) New `internal/store/storetest/cipin_test.go` (package `storetest`, SPDX header): an unexported `moduleRoot(t *testing.T) string` that walks up from the working directory to the directory holding `go.mod` and fails loudly if none; `TestQdrantImageMatchesCIService` per `<behavior>`, with a doc comment: D-10 requires CI's shared service and every testcontainer fallback to run the same Qdrant version because `qdrantTOCTOUVerifiedVersion`'s guard exists to force a conscious re-verification on a bump; this test makes that pin a gate instead of a comment.

(2) `.github/workflows/ci.yaml` — comment text only; do not change any key, value, step, or the pinned `3 PASS + 1 SKIP` logic (D-10):
- The `services.qdrant` pin comment: keep byte-identical to `storetest.QdrantImage` in `internal/store/storetest/storetest.go` — enforced by `TestQdrantImageMatchesCIService`, not by this comment; keep the reason (every testcontainer fallback and this service must stay version-locked for the TOCTOU re-verification guard).
- The shared-participation comment: internal/retrievaleval's TestMain returns before calling `storetest.Run` unless `ENGRAM_RETRIEVAL_EVAL=1` (replacing the reference to the deleted package variable); the counts stay pinned as today.
- The `Test` step env comments: point the fail-closed gate reference at `internal/store/storetest` (`RequireQdrant` / `Run`) instead of server's deleted parser, and say every Qdrant-backed package's TestMain delegates to `storetest.Run`, which honors `ENGRAM_QDRANT_TEST_ADDR`, instead of "All four TestMain chains".
Keep line lengths and indentation yamlfmt-clean.

Commit as `ci: pin the shared Qdrant image to storetest.QdrantImage by test` with pathspec `internal/store/storetest/cipin_test.go .github/workflows/ci.yaml`, ending the message with the session's attribution trailer lines.
  </action>
  <verify>
    <automated>out=$(go test ./internal/store/storetest/ -run '^TestQdrantImageMatchesCIService$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; printf '%s\n' "$out" | rg -q -e '^--- PASS: TestQdrantImageMatchesCIService'</automated>
    <fails_when>non-zero exit, or no `--- PASS: TestQdrantImageMatchesCIService` line</fails_when>
    <automated>yamlfmt -lint .github/workflows/ci.yaml &amp;&amp; actionlint .github/workflows/ci.yaml</automated>
    <fails_when>non-zero exit from yamlfmt (formatting drift) or any actionlint finding printed</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'store_test[.]go|testQdrantAddr|tools_test[.]go requireQdrant' .github/workflows/ci.yaml | wc -l` prints `0`, and `rg -o 'storetest' .github/workflows/ci.yaml | wc -l` prints at least `3`.
    - Only comment lines changed: with `C` set to this task's commit SHA (record it in the SUMMARY), `d=$(git diff "$C^..$C" -- .github/workflows/ci.yaml) || exit 1; printf '%s\n' "$d" | rg '^[-+] ' | rg -v '^[-+]\s*#' | wc -l` prints `0`, and `printf '%s\n' "$d" | rg -c '^[-+] '` prints a non-zero count (the diff is not empty).
    - `rg -o 'qdrant/qdrant[:]v1[.]19[.]1' .github/workflows/ci.yaml | wc -l` is unchanged from HEAD before this task.
    - The SUMMARY records the observed RED of `TestQdrantImageMatchesCIService` against a drifted `image:` line and the clean restore.
    - `head -2 internal/store/storetest/cipin_test.go` matches `head -2 internal/store/store.go`; `golangci-lint run ./internal/store/storetest/...` reports no findings.
  </acceptance_criteria>
  <done>The CI service image pin is enforced by TestQdrantImageMatchesCIService (observed RED on drift), CI comments point at storetest with no functional change, and the workflow stays yamlfmt/actionlint clean; committed with an explicit pathspec.</done>
</task>

<task type="auto">
  <name>Task 3 (LAST task of the phase): Author and register this phase's red-evidence patches so TestRedEvidencePatchesAreLive goes green</name>
  <precondition>Docker is reachable (`docker info` exits 0) or ENGRAM_QDRANT_TEST_ADDR names a live Qdrant gRPC endpoint: this task's verify runs real-Qdrant tests under ENGRAM_REQUIRE_QDRANT=1, which fails rather than skips without one.</precondition>
  <files>internal/store/redevidence_harness_test.go, .planning/phases/01-test-harness-fixture-helper/red-evidence/01-01-storetest-raw-client-write.patch, .planning/phases/01-test-harness-fixture-helper/red-evidence/01-04-listscopes-full-payload-selector.patch, .planning/phases/01-test-harness-fixture-helper/red-evidence/01-05-bare-qdrant-newclient-in-test.patch, .planning/phases/01-test-harness-fixture-helper/red-evidence/01-05-ci-qdrant-image-drift.patch</files>
  <read_first>
    - `internal/store/redevidence_harness_test.go` (whole file) — the harness contract, the `redEvidenceDirs` SCOPE note, the empty-map guard.
    - `.planning/milestones/2026-09-13.01-phases/01-executor-correctness-man-pages/red-evidence/01-01-osrun-ctx-err-first.patch` — patch shape precedent.
    - `internal/store/store.go` lines 1685-1695 (`ListScopes`' scope-only `WithPayload`).
    - `internal/store/storetest/storetest.go` — `Dial`'s `c, err := store.NewQdrantClient(...)` binding.
    - `internal/e2e/spine_review_test.go` — `spineReviewQdrantClient` (returns a `*qdrant.Client`, so the file imports the qdrant package).
    - `.github/workflows/ci.yaml` — the `services.qdrant` `image:` line.
    - The SUMMARYs of plans 01-01 (Task 3), 01-04 (Task 2) and this plan's Tasks 1-2, which each recorded the RED these patches package.
  </read_first>
  <action>
NEVER run any git stash subcommand — to inspect a pristine file use `git show HEAD:<path>` into the scratchpad instead. Commit with an explicit pathspec (rule n6m4as49mr). Never run two red-evidence harness runs at once (the harness mutates the shared working tree).

Author four patches in `.planning/phases/01-test-harness-fixture-helper/red-evidence/` (create the directory). For EACH: start from a clean tree for the touched file; make exactly the one mutation below; write `git diff -U3 -- <file>` to the patch path; run `git apply -R <patch>`; confirm `git status --porcelain -- <file>` prints nothing. Then prove it by hand: `git apply --check <patch>`, `git apply <patch>`, run `ENGRAM_REQUIRE_QDRANT=1 go test -run '^<Target>$' -count=1 -v ./...` and confirm the output contains `--- FAIL: <Target>` (a build failure also exits non-zero, which the harness would miscount as RED — so the patched tree must compile and the named test itself must fail), then `git apply -R <patch>` and confirm the file is clean. Record each target's failing line in the SUMMARY.

1. `01-01-storetest-raw-client-write.patch` → target `TestQdrantClientIsHeldOnlyByStorePackage` (D-13): in `internal/store/storetest/storetest.go`, right after `Dial`'s client binding and its error check, add one statement discarding both results of `c.Upsert(context.Background(), &qdrant.UpsertPoints{})`.
2. `01-04-listscopes-full-payload-selector.patch` → target `TestListScopesFullPayloadsOverGRPCLimit`: in `internal/store/store.go`'s `ListScopes`, change the scope-only `WithPayload` selector to request full payloads (`qdrant.NewWithPayload(true)`) — reverting the #583 fix; both subtests fail against the named 4 MiB limit.
3. `01-05-bare-qdrant-newclient-in-test.patch` → target `TestQdrantClientConstructedOnlyByNewQdrantClient` (D-11): in `internal/e2e/spine_review_test.go`'s `spineReviewQdrantClient`, add as the first statement after `t.Helper()` one statement discarding both results of a qdrant `NewClient` call with a `&qdrant.Config{Host: "127.0.0.1", Port: 1}` argument (a bypass in a `_test.go` file of another package — exactly what D-11 exists to catch).
4. `01-05-ci-qdrant-image-drift.patch` → target `TestQdrantImageMatchesCIService` (D-10): in `.github/workflows/ci.yaml`, change only the `services.qdrant` `image:` value to `qdrant/qdrant:v1.19.2`.

Then register them in `internal/store/redevidence_harness_test.go`: replace the empty `redEvidenceDirs` body (and its "Empty: no milestone is open" comment) with one entry keyed `".planning/phases/01-test-harness-fixture-helper/red-evidence"` mapping `"01-01-storetest-raw-client-write.patch"` → `"TestQdrantClientIsHeldOnlyByStorePackage"`, `"01-04-listscopes-full-payload-selector.patch"` → `"TestListScopesFullPayloadsOverGRPCLimit"`, `"01-05-bare-qdrant-newclient-in-test.patch"` → `"TestQdrantClientConstructedOnlyByNewQdrantClient"`, `"01-05-ci-qdrant-image-drift.patch"` → `"TestQdrantImageMatchesCIService"`, each with a trailing comment naming what the mutation reverts. Change nothing else in the harness.

Run the harness once, alone, then the full quality gate (see verify). If `task` shows any failure other than one this plan caused, stop and report it rather than editing unrelated code.

Commit as `test(store): register phase 1 red-evidence patches` with pathspec `internal/store/redevidence_harness_test.go .planning/phases/01-test-harness-fixture-helper/red-evidence`, ending the message with the session's attribution trailer lines.
  </action>
  <verify>
    <automated>out=$(ENGRAM_REQUIRE_QDRANT=1 go test ./internal/store/ -run '^TestRedEvidencePatchesAreLive$' -count=1 -v -timeout 20m 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e 'confirmed RED|^\s*--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o 'confirmed RED: ' | wc -l)" -eq 4 &amp;&amp; test -z "$(git status --porcelain -- internal/store/store.go internal/store/storetest/storetest.go internal/e2e/spine_review_test.go .github/workflows/ci.yaml)"</automated>
    <fails_when>non-zero exit, a `--- FAIL:` line, fewer than four `confirmed RED:` log lines, or any of the four touched source files left dirty after the run</fails_when>
    <automated>task</automated>
    <fails_when>non-zero exit from `task` (lint or test), including any `--- FAIL:` or `FAIL` package line in its output</fails_when>
    <automated>out=$(ENGRAM_QDRANT_TEST_ADDR=127.0.0.1:6334 ENGRAM_REQUIRE_QDRANT=1 go test -run '^TestSharedQdrantAddressHonored$' -v -count=1 ./internal/store/... ./internal/server/... ./internal/e2e/... ./internal/retrievaleval/... 2>&amp;1); pass=$(printf '%s\n' "$out" | rg -o '^--- PASS: TestSharedQdrantAddressHonored' | wc -l | tr -d '[:space:]'); skip=$(printf '%s\n' "$out" | rg -o '^--- SKIP: TestSharedQdrantAddressHonored' | wc -l | tr -d '[:space:]'); echo "PASS=$pass SKIP=$skip"; test "$pass" = 3 &amp;&amp; test "$skip" = 1</automated>
    <fails_when>counts other than exactly `PASS=3 SKIP=1` (CI's pinned shared-address invariant)</fails_when>
  </verify>
  <acceptance_criteria>
    - `ls .planning/phases/01-test-harness-fixture-helper/red-evidence/*.patch | wc -l` prints `4`, and each file contains exactly one `diff --git a/` header (`rg -c '^diff --git a/' <file>` prints `1` for each).
    - `rg -n -F '".planning/phases/01-test-harness-fixture-helper/red-evidence"' internal/store/redevidence_harness_test.go` prints exactly one line, and `rg -o '"Test[A-Za-z0-9]+"' internal/store/redevidence_harness_test.go | sort -u` includes all four target names.
    - The SUMMARY records, per patch, the `--- FAIL: <Target>` line observed by hand and the harness's `confirmed RED:` line.
    - No red-evidence patch or `.planning/**` file gained an SPDX header (rule 2rjnv8sc9a); `task license:check` exits 0.
    - `git status --porcelain` after the final commit shows no modified tracked source file.
  </acceptance_criteria>
  <done>Four red-evidence patches, each hand-verified to fail its named test and revert cleanly, are registered for this phase; TestRedEvidencePatchesAreLive passes; `task` is green and CI's shared-address count is 3 PASS + 1 SKIP; committed with an explicit pathspec.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository source → test clients | Any Go file, test files included, could construct a Qdrant client with options production does not use; the D-11 gate is the enforcement point. |
| red-evidence harness → working tree | The harness applies real patches to source and CI files and must always restore them. |
| repository → CI | `.github/workflows/ci.yaml` pins the shared Qdrant service; drift from the testcontainer image voids the TOCTOU re-verification guard. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | a new test (or production) file dialing Qdrant outside the shared constructor | medium | mitigate | D-11 gate scans every `.go` file including tests; fixtures prove direct, sibling-constructor, aliased, dot-import and second-in-store.go shapes are caught; patch `01-05-bare-qdrant-newclient-in-test.patch` keeps it live. |
| T-05-02 | Repudiation | a gate that scans nothing reporting clean | medium | mitigate | Zero-applicability guards: the D-11 gate fails unless both test and non-test file counts are non-zero; the CI pin test fails on zero image references; the harness keeps its own per-directory guard. |
| T-05-03 | Repudiation | a red-evidence patch counted RED because it broke the build, not the gate | medium | mitigate | Task 3 requires each patch to be hand-verified with the target's own `--- FAIL:` line in the output before registration. |
| T-05-04 | Tampering | the harness leaving store.go, storetest.go, spine_review_test.go or ci.yaml mutated | medium | mitigate | The harness refuses dirty files and always reverts; Task 3's verify requires all four files clean afterward; runs are never concurrent. |
| T-05-05 | Tampering | CI's Qdrant image drifting from the testcontainer image | low | mitigate | `TestQdrantImageMatchesCIService` plus patch `01-05-ci-qdrant-image-drift.patch`. |
| T-05-SC | Tampering | npm/pip/cargo installs | low | accept | Not applicable: no installs, no new modules. |
</threat_model>

<verification>
- `go test ./internal/store/ -run '^TestQdrantClientConstructedOnlyByNewQdrantClient$' -count=1 -v` passes with five subtests and non-zero test/non-test scan counts.
- `ENGRAM_REQUIRE_QDRANT=1 go test ./internal/store/ -run '^TestRedEvidencePatchesAreLive$' -count=1 -v -timeout 20m` passes with four `confirmed RED:` lines.
- `task` exits 0; `task license:check` exits 0; `git diff --exit-code main...HEAD -- go.mod go.sum` exits 0.
- CI's exact shared-address command yields 3 PASS + 1 SKIP.
- After this plan's SUMMARY is written: `go test ./internal/keylinks/ -count=1` exits 0.
</verification>

<success_criteria>
- Every Qdrant client construction in the module, tests included, provably goes through `store.NewQdrantClient` (REQ-test-client-parity, D-11).
- CI's pinned image is gate-enforced against storetest's single tag and CI comments name storetest (D-10).
- The phase's RED claims are live gates: the migrated #583 test, the D-11 gate, the D-13 write check and the CI pin each have a registered patch; `TestRedEvidencePatchesAreLive` passes (REQ-oversized-fixture-helper's "RED against the pre-fix code").
</success_criteria>

<output>
Create `.planning/phases/01-test-harness-fixture-helper/01-05-SUMMARY.md` when done.
</output>
