---
phase: 01-test-harness-fixture-helper
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - internal/server/tools_test.go
  - internal/server/schemaversion_wire_test.go
  - internal/e2e/harness_test.go
  - internal/e2e/spine_review_test.go
  - internal/e2e/console_browser_test.go
  - internal/retrievaleval/retrieval_eval_test.go
autonomous: true
requirements:
  - REQ-test-client-parity
coupling_justified:
  - "01-02: this plan's files are read as source text by internal/store's seam and prefix gates, which 01-02's verification runs; every commit here keeps each newTestStore seam and testCollectionPrefix constant unchanged, so either execution order leaves both gates green"

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "internal/server, internal/e2e and internal/retrievaleval each delegate Qdrant container lifecycle to `storetest.Run`; none of them still declares its own ENGRAM_REQUIRE_QDRANT parser, container terminator, Qdrant image literal, skip-or-fail helper, or address/booted package variables (D-10)"
    - "The five cross-package dial sites (`testDepsWithStore`, `dialWarnPendingMigrationsTestClient`, `dialRawQdrantClient`, `spineReviewQdrantClient`, `newTestcontainerStore`) obtain their client with `storetest.Dial(t, storetest.RecvLimit)`; what each does with the client afterward (raw SetPayload injection, codec bypass, DeleteCollection) is unchanged (D-10; D-08 governs only the new seeder)"
    - "internal/retrievaleval keeps its `ENGRAM_RETRIEVAL_EVAL` opt-in as TestMain's first statement and then runs `storetest.Run(m, storetest.IgnoreRequireQdrant())`, so it still never parses ENGRAM_REQUIRE_QDRANT, a missing Qdrant still only skips, and its eval store is still built only from the test Qdrant address, never the ambient production one (RESEARCH Pitfall 6; decision recorded in this plan)"
    - "internal/e2e still parses ENGRAM_REQUIRE_QDRANT before building the engram binary (via `storetest.RequireQdrant`), builds the binary itself, then runs `storetest.Run` and removes its temp dir before exiting; it newly inherits storetest's post-boot empty-address fail-closed check, a deliberate normalization recorded in this plan"
    - "Each package's `TestSharedQdrantAddressHonored` delegates to `storetest.AssertSharedAddressHonored`: with ENGRAM_QDRANT_TEST_ADDR set, internal/server and internal/e2e PASS and internal/retrievaleval SKIPs unless ENGRAM_RETRIEVAL_EVAL=1, so CI's pinned 3 PASS + 1 SKIP holds (D-10)"
    - "ENGRAM_REQUIRE_QDRANT=1 still fails a server or e2e run that cannot reach Qdrant instead of skipping it, and an unparseable value (e.g. `treu`) still exits non-zero for server and e2e while retrievaleval ignores it"
  artifacts:
    - path: "internal/server/tools_test.go"
      provides: "server TestMain on storetest.Run; testDepsWithStore and dialWarnPendingMigrationsTestClient on storetest.Dial"
      contains: "storetest.Run(m)"
    - path: "internal/server/schemaversion_wire_test.go"
      provides: "dialRawQdrantClient on storetest.Dial"
      contains: "storetest.Dial(t, storetest.RecvLimit)"
    - path: "internal/e2e/harness_test.go"
      provides: "e2e TestMain: early storetest.RequireQdrant, local binary build, storetest.Run, temp-dir cleanup"
      contains: "storetest.Run(m)"
    - path: "internal/e2e/spine_review_test.go"
      provides: "spineReviewQdrantClient on storetest.Dial; storetest.Addr for the subprocess env"
      contains: "storetest.Dial(t, storetest.RecvLimit)"
    - path: "internal/retrievaleval/retrieval_eval_test.go"
      provides: "opt-in-gated TestMain on storetest.Run with IgnoreRequireQdrant; newTestcontainerStore on storetest.Dial"
      contains: "storetest.IgnoreRequireQdrant()"
  key_links:
    - from: "internal/server/tools_test.go"
      to: "internal/store/storetest/storetest.go"
      via: "server's TestMain delegates container lifecycle to storetest (D-10)"
      pattern: "os[.]Exit[(]storetest[.]Run[(]m[)][)]"
    - from: "internal/server/tools_test.go"
      to: "internal/store/storetest/storetest.go"
      via: "testDepsWithStore dials through storetest with the named limit"
      pattern: "storetest[.]Dial[(]t, storetest[.]RecvLimit[)]"
    - from: "internal/e2e/harness_test.go"
      to: "internal/store/storetest/storetest.go"
      via: "e2e's TestMain builds its binary, then delegates Qdrant lifecycle and keeps its own temp-dir cleanup"
      pattern: "code := storetest[.]Run[(]m[)]"
    - from: "internal/retrievaleval/retrieval_eval_test.go"
      to: "internal/store/storetest/storetest.go"
      via: "retrievaleval opts out of ENGRAM_REQUIRE_QDRANT, preserving its pre-phase behavior (Pitfall 6)"
      pattern: "storetest[.]Run[(]m, storetest[.]IgnoreRequireQdrant[(][)][)]"
    - from: "internal/e2e/spine_review_test.go"
      to: "internal/store/storetest/storetest.go"
      via: "spineReviewQdrantClient dials through storetest with the named limit"
      pattern: "storetest[.]Dial[(]t, storetest[.]RecvLimit[)]"
---

<objective>
Converge the three Qdrant-backed packages outside `internal/store` onto storetest (D-10): each
TestMain delegates container lifecycle to `storetest.Run`, the five cross-package dial sites dial
through `storetest.Dial(t, storetest.RecvLimit)`, the duplicated `requireQdrant` / `failOrSkipNoQdrant`
/ `skipOrFailNoQdrant` / `terminateQdrant` / address-and-booted variables / inline image literals are
deleted, and every `TestSharedQdrantAddressHonored` delegates to `storetest.AssertSharedAddressHonored`.

Recorded decision (orchestrator guidance 10, RESEARCH Pitfall 6 and Open Question 2): internal/retrievaleval
keeps its `ENGRAM_RETRIEVAL_EVAL` opt-in gate as TestMain's first statement and calls
`storetest.Run(m, storetest.IgnoreRequireQdrant())`. Its pre-phase harness never parsed
`ENGRAM_REQUIRE_QDRANT` and only ever skipped on a missing Qdrant; the option preserves exactly that —
no fail-closed behavior is silently added to it. internal/e2e keeps its binary build local (orthogonal to
storetest) and keeps parsing `ENGRAM_REQUIRE_QDRANT` before the build; it gains storetest's post-boot
"required but empty address" check, which its own copy lacked — a deliberate normalization toward the
store/server harness, recorded here rather than slipped in.

Purpose: REQ-test-client-parity — the remaining five of the eleven independent `qdrant.NewClient`
test call sites converge here (six converged in plan 01-02).

Output: three migrated harnesses and five dial sites on storetest.

Expected red, do NOT fix: `TestRedEvidencePatchesAreLive` (internal/store) stays RED until plan 01-05
Task 3. It is outside this plan's packages; do not run the internal/store package as this plan's gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-test-harness-fixture-helper/01-CONTEXT.md
@.planning/phases/01-test-harness-fixture-helper/01-RESEARCH.md
@.planning/phases/01-test-harness-fixture-helper/01-01-SUMMARY.md
@CLAUDE.md

<interfaces>
From plan 01-01 (`github.com/seanb4t/engram/internal/store/storetest`):
- `const RecvLimit = 4 << 20`; `const QdrantImage = "qdrant/qdrant:v1.19.1"`
- `func Run(m *testing.M, opts ...Option) int` — returns the exit code (never calls os.Exit); captures ENGRAM_QDRANT_TEST_ADDR first; fast path when set; else boots a testcontainer (3 min bound), exports its address back into ENGRAM_QDRANT_TEST_ADDR, terminates it (30 s bound) after `m.Run`.
- `func IgnoreRequireQdrant() Option` — Run never parses ENGRAM_REQUIRE_QDRANT; SkipOrFailNoQdrant always skips.
- `func RequireQdrant() (bool, error)`; `func SkipOrFailNoQdrant(t testing.TB)`
- `func Addr() string`; `func EnvAddr() string`; `func ContainerBooted() bool`
- `func AssertSharedAddressHonored(t testing.TB)`
- `func Dial(t testing.TB, recvLimit int, opts ...grpc.DialOption) *qdrant.Client` — skips/fails itself when no address resolved; registers no Close.

Sites in this plan (verified at planning time):
- internal/server/tools_test.go: vars `testQdrantAddr` (125), `testQdrantContainerBooted` (134); `requireQdrant` (174); `failOrSkipNoQdrant` (192); `TestMain` (211, inline image literal 224); `terminateQdrant` (254); `TestSharedQdrantAddressHonored` (267); `TestRequireQdrant` (287); `testDepsWithStore` (364-386, dial site 377); `TestBuildDepsFromEnvLoadsConfigOnce` (5334, uses the address at 5345 via `t.Setenv("ENGRAM_QDRANT_ADDR", ...)`); comment at 5385 naming the direct constructor; `dialWarnPendingMigrationsTestClient` (6057-6074, dial site 6070).
- internal/server/schemaversion_wire_test.go: `dialRawQdrantClient` (~176-200, dial site 195).
- internal/e2e/harness_test.go: vars (48, 57); `requireQdrant` (96); `TestMain` (108-164, inline image literal 139, binary build 118-125); `terminateQdrant` (167); `skipOrFailNoQdrant` (173); `TestSharedQdrantAddressHonored` (192); `startServer` uses the address (321, 329).
- internal/e2e/spine_review_test.go: `spineReviewQdrantClient` (63-78, dial site 73); `newSpineReviewStore` (90-104); address checks at 169, 221, 315; `pruneEnv` (109-115).
- internal/e2e/console_browser_test.go: comments at 61 and 109 naming the e2e helpers this plan deletes.
- internal/retrievaleval/retrieval_eval_test.go: `qdrantImageTag` (33), vars (41, 50); `TestRetrievalEval` address use (93, 116; comment 104); comment 253; `newTestcontainerStore` (325-348, dial site 336); `TestMain` (353-381); `terminateQdrant` (385); `TestSharedQdrantAddressHonored` (404).
</interfaces>
</context>

## Artifacts this phase produces (plan 01-03's share)

No new symbols; this plan deletes duplicates and rewires call sites.

| Kind | Artifact | Changed by |
|------|----------|------------|
| TestMain | `internal/server` → `os.Exit(storetest.Run(m))` | Task 1 |
| TestMain | `internal/e2e` → early `storetest.RequireQdrant()`, local build, `storetest.Run(m)`, temp-dir cleanup | Task 2 |
| TestMain | `internal/retrievaleval` → opt-in gate, then `storetest.Run(m, storetest.IgnoreRequireQdrant())` | Task 2 |
| Dial sites | `testDepsWithStore`, `dialWarnPendingMigrationsTestClient`, `dialRawQdrantClient` → `storetest.Dial(t, storetest.RecvLimit)` | Task 1 |
| Dial sites | `spineReviewQdrantClient`, `newTestcontainerStore` (signature narrows to `(t testing.TB, dim uint64)`) → `storetest.Dial(t, storetest.RecvLimit)` | Task 2 |
| Deleted | server/e2e/retrievaleval copies of the ENGRAM_REQUIRE_QDRANT parser, skip-or-fail helpers, terminators, address/booted vars, image literal/const, and server's `TestRequireQdrant` (storetest owns the one copy) | Tasks 1-2 |

## Edge coverage and prohibitions

No edge-probe rows are allocated to this plan (plans 01-01 and 01-02 author all ten). The fail-closed
prohibition authored in plan 01-01 is preserved here by construction and re-checked by Task 1 and Task 2
acceptance (`treu` still exits non-zero for server and e2e).

<tasks>

<task type="tracer">
  <name>Task 1: internal/server end to end on storetest: TestMain, three dial sites, shared-address test</name>
  <precondition>Docker is reachable (`docker info` exits 0) or ENGRAM_QDRANT_TEST_ADDR names a live Qdrant gRPC endpoint: this task's verify runs real-Qdrant tests under ENGRAM_REQUIRE_QDRANT=1, which fails rather than skips without one.</precondition>
  <files>internal/server/tools_test.go, internal/server/schemaversion_wire_test.go</files>
  <read_first>
    - `internal/server/tools_test.go` lines 1-60 (imports), 118-320 (harness block), 355-390 (`testDepsWithStore`), 5325-5395 (`TestBuildDepsFromEnvLoadsConfigOnce` and the constructor comment), 6050-6080 (`dialWarnPendingMigrationsTestClient`).
    - `internal/server/schemaversion_wire_test.go` lines 170-205 (`dialRawQdrantClient`).
    - `internal/store/storetest/storetest.go` (plan 01-01) — exact exported API and messages.
    - `.planning/phases/01-test-harness-fixture-helper/01-CONTEXT.md` D-10; `01-RESEARCH.md` Pitfall 2 and the 4-TestMain comparison table.
  </read_first>
  <action>
NEVER run any git stash subcommand — to inspect a pristine file use `git show HEAD:<path>` into the scratchpad instead. Commit with an explicit pathspec (rule n6m4as49mr). `TestRedEvidencePatchesAreLive` (internal/store) is the single expected red until plan 01-05 Task 3; it is not part of this plan's gate.

In `internal/server/tools_test.go` (D-10):
- Delete the `testQdrantAddr` and `testQdrantContainerBooted` variables with their doc comments, `requireQdrant`, `failOrSkipNoQdrant`, `terminateQdrant`, and `TestRequireQdrant` (storetest's own `TestRequireQdrant` is now the single pin of that parse contract). Keep `testCollectionPrefix`, `testCollection` and `newTestStore` byte-for-byte (the collection-prefix gates read them).
- Replace `TestMain`'s body with `os.Exit(storetest.Run(m))`; its doc comment says container lifecycle, the ENGRAM_QDRANT_TEST_ADDR fast path and the ENGRAM_REQUIRE_QDRANT fail-closed gate all live in storetest (plan 01-01) and CI's `test` job relies on that gate.
- `TestSharedQdrantAddressHonored`: keep the name and a trimmed doc comment; the body becomes `storetest.AssertSharedAddressHonored(t)`.
- `testDepsWithStore`: replace the address check, host/port split and client construction with `c := storetest.Dial(t, storetest.RecvLimit)`; the rest of the function is unchanged.
- `TestBuildDepsFromEnvLoadsConfigOnce`: replace the address check with `addr := storetest.Addr()`, calling `storetest.SkipOrFailNoQdrant(t)` when it is empty, and feed `addr` to the existing `t.Setenv` of the store address.
- Every remaining call of the deleted skip-or-fail helper becomes `storetest.SkipOrFailNoQdrant(t)`.
- `dialWarnPendingMigrationsTestClient`: body becomes `return storetest.Dial(t, storetest.RecvLimit)`; update its doc comment (it dials through the shared constructor via storetest; the caller's raw SetPayload injection afterward is unchanged).
- The comment above `TestStoreAndEmbedderFromEnvNoEnsureLoadsConfigOnce` that says the direct qdrant constructor fires a one-shot version HealthCheck: say `store.NewQdrantClient` (which wraps it) instead.
- Imports: add `github.com/seanb4t/engram/internal/store/storetest`; remove `tcqdrant` and any import left unused (`net`, `strconv`, `fmt` — check each; keep what other code still uses).
- Side effect to watch, not to pre-empt: server test clients now carry the production otelgrpc stats handler (D-03 accepted this), so Qdrant RPCs emit client spans. `internal/server/instrument_test.go`'s `TestInstrumentToolsSetsOwnerSpanAttribute` asserts exactly one span while it holds a temporary tracer provider; it is serial and its handler touches no Qdrant, so it should stay green. If it fails, report the observed span names in the SUMMARY and stop — do not loosen that assertion.

In `internal/server/schemaversion_wire_test.go`: `dialRawQdrantClient`'s body becomes `return storetest.Dial(t, storetest.RecvLimit)`; its doc comment drops the reference to the deleted address variable and says the codec-bypassing legacy-shape fixture use afterward is unchanged; fix imports.

Commit as `test(server): run the server suite on the storetest harness` with pathspec `internal/server/tools_test.go internal/server/schemaversion_wire_test.go`, ending the message with the session's attribution trailer lines.
  </action>
  <verify>
    <automated>out=$(ENGRAM_REQUIRE_QDRANT=1 go test ./internal/server/ -count=1 2>&amp;1); rc=$?; printf '%s\n' "$out" | tail -n 5; test "$rc" -eq 0</automated>
    <fails_when>non-zero exit, or output containing `FAIL` / `--- FAIL:` / `[build failed]` (the server suite regressed or did not compile on the storetest harness)</fails_when>
    <automated>out=$(ENGRAM_QDRANT_TEST_ADDR=127.0.0.1:6334 ENGRAM_REQUIRE_QDRANT=1 go test ./internal/server/ -run '^TestSharedQdrantAddressHonored$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e '^--- PASS: TestSharedQdrantAddressHonored' | wc -l)" -eq 1</automated>
    <fails_when>non-zero exit, or not exactly one `--- PASS: TestSharedQdrantAddressHonored` line with the shared address set (a SKIP or FAIL means the fast path or the delegation is wrong)</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'func (requireQdrant|failOrSkipNoQdrant|terminateQdrant|TestRequireQdrant)[(]' internal/server/*_test.go | wc -l` prints `0`.
    - `rg -n -w 'testQdrant(Addr|ContainerBooted)' internal/server/*_test.go | wc -l` prints `0`, and `rg -n 'qdrant/qdrant[:]v' internal/server/*_test.go | wc -l` prints `0`.
    - `rg -n 'qdrant[.]NewClient[(]' internal/server/*_test.go | rg -v '^[^:]+:[0-9]+:\s*//' | wc -l` prints `0`.
    - `rg -o 'storetest[.]Dial[(]t, storetest[.]RecvLimit[)]' internal/server/tools_test.go internal/server/schemaversion_wire_test.go | wc -l` prints `3`.
    - `rg -n -F 'os.Exit(storetest.Run(m))' internal/server/tools_test.go` prints exactly one line.
    - Fail-closed parse preserved: `ENGRAM_REQUIRE_QDRANT=treu go test ./internal/server/ -run '^TestSharedQdrantAddressHonored$' -count=1` exits non-zero and its output contains `invalid value "treu"`.
    - `go vet ./internal/server/` exits 0; `golangci-lint run ./internal/server/...` reports no findings.
  </acceptance_criteria>
  <done>The server suite runs on storetest's lifecycle with all three dial sites on storetest.Dial, its duplicated harness deleted, the shared-address test passing on the fast path, and fail-closed parsing intact; committed with an explicit pathspec.</done>
</task>

<task type="auto">
  <name>Task 2: internal/e2e and internal/retrievaleval on storetest, each preserving its own harness quirks</name>
  <precondition>Docker is reachable (`docker info` exits 0) or ENGRAM_QDRANT_TEST_ADDR names a live Qdrant gRPC endpoint: this task's verify runs real-Qdrant tests under ENGRAM_REQUIRE_QDRANT=1, which fails rather than skips without one.</precondition>
  <files>internal/e2e/harness_test.go, internal/e2e/spine_review_test.go, internal/e2e/console_browser_test.go, internal/retrievaleval/retrieval_eval_test.go</files>
  <read_first>
    - `internal/e2e/harness_test.go` lines 1-210 (imports, vars, `requireQdrant`, `TestMain`, `terminateQdrant`, `skipOrFailNoQdrant`, `TestSharedQdrantAddressHonored`) and 310-335 (`startServer`).
    - `internal/e2e/spine_review_test.go` lines 55-120 and the address checks near lines 169, 221 and 315.
    - `internal/e2e/console_browser_test.go` lines 55-70 and 105-115 (comments naming the helpers this task deletes).
    - `internal/retrievaleval/retrieval_eval_test.go` lines 1-130, 245-265, 320-420.
    - `internal/store/storetest/storetest.go` (plan 01-01).
    - `.planning/phases/01-test-harness-fixture-helper/01-RESEARCH.md` Pitfall 6 and Open Question 2; this plan's `<objective>` decision paragraph.
  </read_first>
  <action>
NEVER run any git stash subcommand — to inspect a pristine file use `git show HEAD:<path>` into the scratchpad instead. Commit with an explicit pathspec (rule n6m4as49mr). `TestRedEvidencePatchesAreLive` (internal/store) is the single expected red until plan 01-05 Task 3.

internal/e2e (D-10):
- `harness_test.go`: delete the `testQdrantAddr` and `testQdrantContainerBooted` variables, `requireQdrant`, `terminateQdrant`, and `skipOrFailNoQdrant`. Keep `testCollectionPrefix`, `testCollection`, `newTestStore` and `engramBin` untouched. Rewrite `TestMain` in this order: (1) `if _, err := storetest.RequireQdrant(); err != nil` print `fatal: <err>` and `os.Exit(1)` — still BEFORE the build, preserving today's ordering; (2) the temp dir and `go build` of the engram binary exactly as today; (3) `code := storetest.Run(m)`; (4) `_ = os.RemoveAll(tmp)`; (5) `os.Exit(code)`. Drop the dead deferred RemoveAll. Update TestMain's doc comment: binary build stays here (orthogonal to storetest); Qdrant lifecycle, fast path and fail-closed gate are storetest's; this package now also inherits storetest's post-boot empty-address fail-closed check (recorded normalization). `TestSharedQdrantAddressHonored` → body `storetest.AssertSharedAddressHonored(t)`. `startServer`: `addr := storetest.Addr()`, `storetest.SkipOrFailNoQdrant(t)` when empty, and `addr` as the subprocess's store address. Imports: add storetest, drop `tcqdrant` and anything left unused.
- `spine_review_test.go`: `spineReviewQdrantClient`'s body becomes `return storetest.Dial(t, storetest.RecvLimit)` (doc comment updated; the raw `DeleteCollection` setup/teardown in `newSpineReviewStore` stays); every address check becomes `storetest.Addr() == ""` with `storetest.SkipOrFailNoQdrant(t)`; `pruneEnv` takes its store address from `storetest.Addr()`; fix imports.
- `console_browser_test.go`: reword the two comments that name the deleted e2e helpers so they point at `storetest.RequireQdrant` / `storetest.SkipOrFailNoQdrant` as the shape `requireBrowser` / `skipOrFailNoBrowser` mirror. Comment-only change.

internal/retrievaleval (recorded decision, Pitfall 6):
- Delete the `qdrantImageTag` constant, the `testQdrantAddr` / `testQdrantContainerBooted` variables and `terminateQdrant`. Keep `testCollectionPrefix`, `testCollection`, `newTestStore`.
- `TestMain`: keep `if os.Getenv("ENGRAM_RETRIEVAL_EVAL") != "1" { os.Exit(m.Run()) }` as the FIRST statement, then `os.Exit(storetest.Run(m, storetest.IgnoreRequireQdrant()))`. Doc comment: the opt-in gate stays first so CI's `go test ./...` pays no Docker cost here; `IgnoreRequireQdrant` preserves this package's pre-phase behavior exactly — it never consulted ENGRAM_REQUIRE_QDRANT and a missing Qdrant only skips the eval.
- `TestRetrievalEval`: skip with the existing message when `storetest.Addr()` is empty; call `newTestcontainerStore(t, dim)`.
- `newTestcontainerStore` narrows to `(t testing.TB, dim uint64) *store.Store` and gets its client from `storetest.Dial(t, storetest.RecvLimit)`; keep the uuid collection, the seam, the 15 s EnsureCollection bound. Its doc comment keeps the round-2 finding 1 guarantee in words: the eval store is built only from the test Qdrant storetest resolved, never from the ambient production address a developer's environment might set.
- `TestSharedQdrantAddressHonored`: keep the ENGRAM_RETRIEVAL_EVAL skip as its first statement, then `storetest.AssertSharedAddressHonored(t)`.
- Update the remaining comments that name the deleted address variable (near lines 104 and 253). Fix imports (drop `tcqdrant`, `net`, `strconv`, `fmt` if unused).

Commit as `test(e2e,retrievaleval): run both suites on the storetest harness` with pathspec `internal/e2e/harness_test.go internal/e2e/spine_review_test.go internal/e2e/console_browser_test.go internal/retrievaleval/retrieval_eval_test.go`, ending the message with the session's attribution trailer lines.
  </action>
  <verify>
    <automated>out=$(ENGRAM_REQUIRE_QDRANT=1 go test ./internal/e2e/ ./internal/retrievaleval/ -count=1 2>&amp;1); rc=$?; printf '%s\n' "$out" | tail -n 6; test "$rc" -eq 0</automated>
    <fails_when>non-zero exit, or output containing `FAIL` / `--- FAIL:` / `[build failed]`</fails_when>
    <automated>out=$(ENGRAM_QDRANT_TEST_ADDR=127.0.0.1:6334 ENGRAM_REQUIRE_QDRANT=1 go test -run '^TestSharedQdrantAddressHonored$' -v -count=1 ./internal/server/... ./internal/e2e/... ./internal/retrievaleval/... 2>&amp;1); rc=$?; pass=$(printf '%s\n' "$out" | rg -o '^--- PASS: TestSharedQdrantAddressHonored' | wc -l | tr -d '[:space:]'); skip=$(printf '%s\n' "$out" | rg -o '^--- SKIP: TestSharedQdrantAddressHonored' | wc -l | tr -d '[:space:]'); echo "PASS=$pass SKIP=$skip"; test "$rc" -eq 0 &amp;&amp; test "$pass" = 2 &amp;&amp; test "$skip" = 1</automated>
    <fails_when>non-zero exit, or the counts are not exactly `PASS=2 SKIP=1` over server, e2e and retrievaleval (internal/store's third PASS is plan 01-04's)</fails_when>
    <automated>out=$(ENGRAM_RETRIEVAL_EVAL=1 ENGRAM_REQUIRE_QDRANT=treu ENGRAM_QDRANT_TEST_ADDR=127.0.0.1:6334 go test ./internal/retrievaleval/ -run '^TestSharedQdrantAddressHonored$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; printf '%s\n' "$out" | rg -q -e '^--- PASS: TestSharedQdrantAddressHonored'</automated>
    <fails_when>non-zero exit, or no `--- PASS: TestSharedQdrantAddressHonored` line: retrievaleval parsed the invalid ENGRAM_REQUIRE_QDRANT value (IgnoreRequireQdrant not applied) or did not take the shared path</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n 'func (requireQdrant|skipOrFailNoQdrant|terminateQdrant)[(]' internal/e2e/*_test.go internal/retrievaleval/*_test.go | wc -l` prints `0`.
    - `rg -n -w 'testQdrant(Addr|ContainerBooted)|qdrantImageTag' internal/e2e/*_test.go internal/retrievaleval/*_test.go | wc -l` prints `0`, and `rg -n 'qdrant/qdrant[:]v' internal/e2e/*_test.go internal/retrievaleval/*_test.go | wc -l` prints `0`.
    - `rg -n 'qdrant[.]NewClient[(]' internal/e2e/*_test.go internal/retrievaleval/*_test.go | rg -v '^[^:]+:[0-9]+:\s*//' | wc -l` prints `0`.
    - `rg -n -F 'storetest.Run(m, storetest.IgnoreRequireQdrant())' internal/retrievaleval/retrieval_eval_test.go` prints exactly one line, and in `TestMain` the `ENGRAM_RETRIEVAL_EVAL` check precedes it (`rg -n 'ENGRAM_RETRIEVAL_EVAL|storetest[.]Run' internal/retrievaleval/retrieval_eval_test.go` lists the gate line inside TestMain before the Run line).
    - In `internal/e2e/harness_test.go`, `storetest.RequireQdrant()` appears on a line number smaller than the `go build` command's line, which is smaller than `storetest.Run(m)`'s line.
    - e2e fail-closed parse preserved: `ENGRAM_REQUIRE_QDRANT=treu go test ./internal/e2e/ -run '^TestSharedQdrantAddressHonored$' -count=1` exits non-zero and its output contains `invalid value "treu"`.
    - `go vet ./internal/e2e/ ./internal/retrievaleval/` exits 0; `golangci-lint run ./internal/e2e/... ./internal/retrievaleval/...` reports no findings.
  </acceptance_criteria>
  <done>e2e and retrievaleval run on storetest with their quirks preserved (e2e's early parse and local build; retrievaleval's opt-in gate and no fail-closed parse), both dial sites on storetest.Dial, duplicates deleted, and the shared-address counts at 2 PASS + 1 SKIP across these three packages; committed with an explicit pathspec.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → Qdrant | Which Qdrant each package dials is now decided by storetest; retrievaleval's eval previously guaranteed it never touched the ambient production address. |
| e2e test → engram subprocess | The built binary receives its store address through an environment map built from scratch; it must keep receiving only the test Qdrant address. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | retrievaleval or e2e dialing an operator's production Qdrant after the rewire | high | mitigate | Both packages take their address only from `storetest.Addr()`/`storetest.Dial` (test instance or booted container); e2e's `childEnv` stays built from scratch; `newTestcontainerStore`'s doc keeps the round-2 finding 1 guarantee. |
| T-03-02 | Repudiation | a package silently skipping its Qdrant tier in CI (or retrievaleval silently gaining fail-closed) | medium | mitigate | Task verifies: shared-address counts PASS=2 SKIP=1 here (3+1 with internal/store in 01-04); `treu` still exits non-zero for server and e2e; retrievaleval proven to ignore it via IgnoreRequireQdrant. |
| T-03-03 | Denial of Service | CI booting extra Qdrant containers (#497 regression) | medium | mitigate | Every TestMain now takes storetest's ENGRAM_QDRANT_TEST_ADDR fast path first, exactly as before; the fast-path PASS counts prove no package booted its own container when the shared address is set. |
| T-03-SC | Tampering | npm/pip/cargo installs | low | accept | Not applicable: no installs, no new modules; only an in-repo import of storetest is added. |
</threat_model>

<verification>
- `ENGRAM_REQUIRE_QDRANT=1 go test ./internal/server/ ./internal/e2e/ ./internal/retrievaleval/ -count=1` exits 0.
- `ENGRAM_QDRANT_TEST_ADDR=127.0.0.1:6334 ENGRAM_REQUIRE_QDRANT=1 go test -run '^TestSharedQdrantAddressHonored$' -v -count=1 ./internal/server/... ./internal/e2e/... ./internal/retrievaleval/...` reports 2 PASS + 1 SKIP.
- `rg -n 'qdrant[.]NewClient[(]' internal/server internal/e2e internal/retrievaleval | rg -v '^[^:]+:[0-9]+:\s*//' | wc -l` prints `0`.
- `task lint` exits 0; `git diff --exit-code main...HEAD -- go.mod go.sum` exits 0.
- After this plan's SUMMARY is written: `go test ./internal/keylinks/ -count=1` exits 0.
</verification>

<success_criteria>
- The remaining five test dial sites converge on the shared constructor through storetest (REQ-test-client-parity, D-10).
- Three of the four duplicated TestMains, two of the three duplicated parsers, and three of the four image-tag copies collapse into storetest (the internal/store copies are plan 01-04's).
- retrievaleval's and e2e's harness quirks are preserved and the retrievaleval decision is recorded.
</success_criteria>

<output>
Create `.planning/phases/01-test-harness-fixture-helper/01-03-SUMMARY.md` when done.
</output>
