---
phase: 01-test-harness-fixture-helper
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/store/store.go
  - internal/store/storetest/storetest.go
  - internal/store/storetest/storetest_test.go
  - internal/store/storetest/seed.go
  - internal/store/storetest/seed_test.go
  - internal/store/schemaversion_stamp_gate_test.go
  - internal/store/collectionprefix_conformance_test.go
  - internal/server/tools.go
autonomous: true
requirements:
  - REQ-oversized-fixture-helper
  - REQ-test-client-parity

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`store.NewQdrantClient(host, port, opts...)` is the one exported constructor for a `*qdrant.Client`: it applies `grpc.WithStatsHandler(otelgrpc.NewClientHandler())` first and appends caller options after it, and `internal/server/tools.go`'s `storeFromConfig` builds the production client through it with no extra option, so the production dial option set is exactly the pre-phase one (D-01, D-03)"
    - "A test outside `internal/store` obtains a Qdrant client only via `storetest.Dial(t, storetest.RecvLimit)`: the named receive limit reaches the connection as `grpc.WithDefaultCallOptions(grpc.MaxCallRecvMsgSize(recvLimit))` through `store.NewQdrantClient`'s pass-through, and no wrapper option type or helper exists (D-02, D-04, D-06)"
    - "`storetest.RecvLimit` is exactly `4 << 20` (4194304 bytes); `dialOptions` rejects a receive limit of 0 and of -1 with an error instead of defaulting, and accepts 1 (edge probe `boundary`, REQ-test-client-parity, resolved explicit)"
    - "`dialOptions` returns the caller's options unchanged and in their original order, followed by exactly one named-limit option placed LAST, so no caller-supplied option can widen the named limit, asserted by interface identity on the returned slice (edge probe `adjacency`, REQ-test-client-parity, resolved explicit)"
    - "With no resolved Qdrant address `storetest.Dial` never dials: it skips, or fails when `ENGRAM_REQUIRE_QDRANT` is truthy or unparseable (an invalid value is an error, never coerced to false); `splitAddr` rejects an empty address, a missing port, port 0, a negative port, a non-numeric port and a port above 65535 (edge probe `empty`, REQ-test-client-parity, resolved explicit)"
    - "`storetest.RecvLimit` is an exact `int` byte count handed unconverted to `grpc.MaxCallRecvMsgSize`: no unit conversion, rounding, or narrowing sits between the named constant and the dial option (edge probe `precision`, REQ-test-client-parity, resolved explicit)"
    - "`storetest.SeedOversized` seeds, through public `Store.Upsert` only, a fresh scope whose logical content bytes exceed the caller-named limit in either shape: FewLarge = 40 records of 131072 bytes and ManySmall = 1000 records of 5243 bytes at `RecvLimit`, each at least 1.25 times the limit, every single record well under it (D-05, D-08)"
    - "`checkOversized` rejects a fixture whose logical total EQUALS the limit and accepts a total of limit+1 (strictly-greater contract), and rejects any single record at or above the limit (edge probe `boundary`, REQ-oversized-fixture-helper, resolved explicit)"
    - "Two `SeedOversized` calls never share a scope, an owner, or a record id: each fixture identity is freshly uuid-derived unless the caller's Template supplies it, so two fixtures in one collection never merge (edge probe `adjacency`, REQ-oversized-fixture-helper, resolved explicit)"
    - "`validateSpec` rejects a nil store, an empty Vector, a non-positive limit, the zero Shape, an unknown Shape, a FewLarge limit below 32 bytes and a ManySmall limit below 2 bytes, and `SeedOversized` fails the test on any of them BEFORE writing a single record (edge probe `empty`, REQ-oversized-fixture-helper, resolved explicit)"
    - "Seeded records carry strictly increasing `created_at` values exactly one second apart, distinct at the store's RFC3339 second precision, in write order, and `Fixture.IDs` lists the ids in that same order (edge probe `ordering`, REQ-oversized-fixture-helper, resolved explicit)"
    - "`layout` targets limit + ceil(limit/4) and sizes with ceiling division, so records times recordBytes is at least 1.25 times the limit for every accepted row of TestLayout's table (2, 32, 1000, 4<<20, 64<<20); the self-assertion counts logical content bytes actually written, a lower bound on the decompressed payload, never a compressed size (edge probe `precision`, REQ-oversized-fixture-helper, resolved explicit)"
    - "`SeedOversized` holds the single `testing.Short()` skip every oversized test inherits, registers its `Store.DeleteAll` cleanup before its first write so a partial seed is reclaimed, and the storetest integration test seeds one shape per subtest in its own collection so at most one fixture is live per package (D-12)"
    - "`TestQdrantClientIsHeldOnlyByStorePackage` derives exactly three holders (`internal/store/store.go`, `internal/server/tools.go`, `internal/store/storetest/storetest.go`) and runs its never-issues-a-write check on EVERY allowlist entry except `internal/store/store.go`, logging each checked holder (D-09, D-13)"
    - "`qdrantClientLocalNames` and `fileRefsQdrantClient` recognize `store.NewQdrantClient` as a client construction alongside `qdrant.NewClient`, so the composition root stays a derived holder after it stops naming the qdrant package (D-11)"
    - "`internal/store/storetest` is registered in `qdrantBackedPackages` with its own `testCollectionPrefix` (`storetest_`) and `newTestStore` seam; the seam and prefix-disjointness gates now cover five packages (20 ordered pairs)"
    - "The storetest package declares no test named after CI's shared-address participation check, so CI's pinned count over `./internal/store/...` is unchanged"
    - "`go.mod` and `go.sum` are byte-unchanged, and `internal/store/storetest` is absent from `go list -deps ./cmd/engram`"
  artifacts:
    - path: "internal/store/store.go"
      provides: "NewQdrantClient: the shared base-plus-caller-options Qdrant client constructor"
      contains: "func NewQdrantClient(host string, port int, opts ...grpc.DialOption) (*qdrant.Client, error)"
    - path: "internal/store/storetest/storetest.go"
      provides: "RecvLimit, QdrantImage, Run, IgnoreRequireQdrant, RequireQdrant, SkipOrFailNoQdrant, Addr, EnvAddr, ContainerBooted, AssertSharedAddressHonored, Dial"
      contains: "const RecvLimit = 4 << 20"
      min_lines: 150
    - path: "internal/store/storetest/seed.go"
      provides: "Shape, ManySmall, FewLarge, ManySmallRecords, Spec, Fixture, SeedOversized"
      contains: "func SeedOversized(t testing.TB, st *store.Store, spec Spec) Fixture"
      min_lines: 120
    - path: "internal/store/storetest/storetest_test.go"
      provides: "storetest's own TestMain, collection-prefix seam, lifecycle/dial unit tests and the real-Qdrant TestDialRoundTrip"
      contains: "TestDialRoundTrip"
    - path: "internal/store/storetest/seed_test.go"
      provides: "layout/checkOversized/validateSpec table tests and the real-Qdrant TestSeedOversizedShapes"
      contains: "TestSeedOversizedShapes"
    - path: "internal/server/tools.go"
      provides: "storeFromConfig building the production client via store.NewQdrantClient"
      contains: "store.NewQdrantClient(host, port)"
    - path: "internal/store/schemaversion_stamp_gate_test.go"
      provides: "three-entry qdrantClientHolderAllowlist and the D-13 generalized write check"
      contains: "internal/store/storetest/storetest.go"
    - path: "internal/store/collectionprefix_conformance_test.go"
      provides: "qdrantBackedPackages including internal/store/storetest"
      contains: "internal/store/storetest"
  key_links:
    - from: "internal/server/tools.go"
      to: "internal/store/store.go"
      via: "storeFromConfig builds the production client through the shared constructor with no caller option (D-01, D-03)"
      pattern: "store[.]NewQdrantClient[(]host, port[)]"
    - from: "internal/store/storetest/storetest.go"
      to: "internal/store/store.go"
      via: "Dial binds its client through the same constructor production uses, so the holder gate has a client-bound identifier to write-check (D-01, D-13)"
      pattern: "c, err := store[.]NewQdrantClient[(]host, port, dialOpts[.][.][.][)]"
    - from: "internal/store/storetest/storetest.go"
      to: "internal/store/storetest/storetest.go"
      via: "dialOptions appends the named receive limit in upstream grpc vocabulary (D-02)"
      pattern: "grpc[.]WithDefaultCallOptions[(]grpc[.]MaxCallRecvMsgSize[(]recvLimit[)][)]"
    - from: "internal/store/store.go"
      to: "internal/store/store.go"
      via: "NewQdrantClient's base dial option is the production otelgrpc stats handler (D-01)"
      pattern: "grpc[.]WithStatsHandler[(]otelgrpc[.]NewClientHandler[(][)][)]"
    - from: "internal/store/storetest/seed.go"
      to: "internal/store/store.go"
      via: "the seeder writes only through the public Store.Upsert write path (D-08)"
      pattern: "st[.]Upsert[(]ctx, m, spec[.]Vector[)]"
    - from: "internal/store/schemaversion_stamp_gate_test.go"
      to: "internal/store/storetest/storetest.go"
      via: "storetest's dial file is an allowlisted, write-checked client holder (D-09, D-13)"
      pattern: "internal/store/storetest/storetest[.]go"
    - from: "internal/store/collectionprefix_conformance_test.go"
      to: "internal/store/storetest/storetest_test.go"
      via: "storetest joins the Qdrant-backed package set the seam and prefix gates scan"
      pattern: 'name: "internal/store/storetest", dir: "storetest"'
  prohibitions:
    - statement: "MUST NOT resolve, dial, or write to any Qdrant other than the test instance: storetest reads only ENGRAM_QDRANT_TEST_ADDR or the address of the container it booted itself, never the production ENGRAM_QDRANT_ADDR that storeFromConfig reads, so a developer's configured production or dev Qdrant is never touched by a test"
      category: safety
      status: resolved
      verification: test
      reason: "Task 1 acceptance asserts the production variable name is absent from storetest's non-test sources; Addr() is sourced only from Run. No wired check descriptor by design (spec-less fallback, flagged-unverified)"
    - statement: "MUST NOT add a raw-client write path to test-support code: SeedOversized writes only through Store.Upsert and cleans up only through Store.DeleteAll, and storetest never calls a write method on a *qdrant.Client it holds"
      category: safety
      status: resolved
      verification: test
      reason: "Enforced by Task 3's generalized write check (D-13) over storetest.go; proven live by red-evidence patch 01-01-storetest-raw-client-write.patch, registered by plan 01-05. No wired check descriptor by design (spec-less fallback, flagged-unverified)"
    - statement: "MUST NOT let a Qdrant-backed test report green by skipping when ENGRAM_REQUIRE_QDRANT is truthy, and MUST NOT coerce an unparseable ENGRAM_REQUIRE_QDRANT value to false"
      category: transparency
      status: resolved
      verification: test
      reason: "TestRequireQdrant pins the parse contract; Task 1 acceptance runs storetest with ENGRAM_REQUIRE_QDRANT=treu and requires a non-zero exit naming the invalid value. No wired check descriptor by design (spec-less fallback, flagged-unverified)"
---

<objective>
Build the shared foundation every later task and phase of milestone 2026-09-18.01 reuses: one exported
Qdrant client constructor in `internal/store` (`store.NewQdrantClient`, D-01), the new
`internal/store/storetest` test-support package (D-06) that owns the named 4 MiB receive limit (D-04),
the single Qdrant image tag, the container lifecycle (`storetest.Run`), the one `ENGRAM_REQUIRE_QDRANT`
parser, the dial-with-required-limit helper (`storetest.Dial`, D-02), and the two-shape oversized
fixture seeder (`storetest.SeedOversized`, D-05/D-08/D-12). The production composition root switches
onto the shared constructor with zero behavior change (D-03), and the client-holder gate learns the new
call shape and generalizes its never-writes check to every allowlisted holder except `store.go`
(D-09, D-11 composition-root half, D-13).

Framing (D-00): every choice here is made by idiom and long-term maintenance. The receive limit travels
in upstream grpc vocabulary, not a wrapper; the constructor lives in the package that owns the client;
storetest follows the `net/http/httptest` / `internal/testhttp` shape.

Purpose: REQ-oversized-fixture-helper and REQ-test-client-parity both depend on a fixture seeder and a
shared constructor existing before any call site converges (plans 01-02, 01-03, 01-04) or the
convergence gate lands (plan 01-05).

Output: `store.NewQdrantClient`; `internal/store/storetest/{storetest.go,seed.go}` plus their tests; a
three-entry client-holder allowlist with the D-13 generalized write check; storetest registered with the
collection-prefix gates; `storeFromConfig` on the shared constructor.

Expected red, do NOT fix: `TestRedEvidencePatchesAreLive` (internal/store) is RED on HEAD because this
phase directory exists while `redEvidenceDirs` is empty (RESEARCH Pitfall 1). It is the single expected
red until plan 01-05 Task 3 registers this phase's red-evidence patches. Every other test must stay green.
One transient exception, resolved by this plan itself: `internal/keylinks`' `TestActiveMilestoneKeyLinksSatisfiable`
is red on HEAD because no active-milestone plan has a SUMMARY yet (it scans zero plan files); it turns green
once `01-01-SUMMARY.md` exists and must then pass, which requires this plan's `must_haves.key_links` patterns
to match the code exactly as the actions below dictate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-test-harness-fixture-helper/01-CONTEXT.md
@.planning/phases/01-test-harness-fixture-helper/01-RESEARCH.md
@.planning/phases/01-test-harness-fixture-helper/01-PATTERNS.md
@CLAUDE.md

<interfaces>
Existing API this plan builds on (verified at planning time; do not re-derive):

From internal/store/store.go:
- `func New(c *qdrant.Client, collection string, opts ...Option) *Store` (line ~503)
- `func (s *Store) EnsureCollection(ctx context.Context, dim uint64) error` (line 515)
- `func (s *Store) Upsert(ctx context.Context, m Memory, vec []float32) error` (line 795): one `UpsertPoints` RPC per call with `Wait: true`; no batch variant exists (RESEARCH Pitfall 7)
- `func (s *Store) Get(ctx context.Context, id string) (Memory, error)` (line 1711); `created_at` is stored as RFC3339 (second precision, line 618) and parsed back at line 717
- `func (s *Store) ListScopes(ctx context.Context, subj Subject) ([]ScopeCount, bool, error)` (line 1673): scans at most 1000 readable points requesting ONLY the `scope` payload key (the #583 fix)
- `func (s *Store) DeleteAll(ctx context.Context, scope string, subj Subject) error` (line 2626)
- `const maxListLimit = 1000` (line 1458, unexported)
- `Memory` fields used here: `ID, Content, Scope, Owner, Actor string; CreatedAt time.Time`

From internal/store/subject.go:
- `func Authenticated(sub string) Subject` — PANICS on an empty string

From internal/server/tools.go (lines 110-134): `storeFromConfig` currently calls `qdrant.NewClient(&qdrant.Config{Host: host, Port: port, GrpcOptions: []grpc.DialOption{grpc.WithStatsHandler(otelgrpc.NewClientHandler())}})` and wraps the error as `qdrant client: %w`. Its otelgrpc import is aliased `otelgrpc "go.opentelemetry.io/contrib/instrumentation/google.golang.org/grpc/otelgrpc"`.

From google.golang.org/grpc v1.83.2: `grpc.WithDefaultCallOptions(cos ...CallOption) DialOption` (additive), `grpc.MaxCallRecvMsgSize(bytes int) CallOption`, `grpc.WithStatsHandler(h stats.Handler) DialOption`. Every `DialOption` these return is a `*funcDialOption` pointer, so two `grpc.DialOption` interface values compare equal only when they are the same option value.

From github.com/qdrant/go-client v1.19.2: `qdrant.NewClient(config *Config) (*Client, error)`; `Config.GrpcOptions` is APPENDED after the client's own defaults (`grpc_client.go:33-53`); `(*Client).Close() error`; `(*Client).DeleteCollection(ctx, name) error`.

From github.com/testcontainers/testcontainers-go/modules/qdrant v0.44.0 (imported as `tcqdrant`): `tcqdrant.Run(ctx, image string) (*tcqdrant.QdrantContainer, error)`, `(*QdrantContainer).GRPCEndpoint(ctx) (string, error)`, `(*QdrantContainer).Terminate(ctx) error`.

From internal/store/schemaversion_stamp_gate_test.go: `qdrantClientHolderAllowlist` (line 756), `fileRefsQdrantClient` (790), `scanRepoForQdrantClientRefs` (826), `qdrantClientLocalNames` (873), `TestQdrantClientIsHeldOnlyByStorePackage` (927), `scanQdrantCalls(fset, src, displayPath, methods)` (137), `fullWriteMethods`/`partialWriteMethods` (295/300), `findModuleRoot()` (768), `keysOf` (store_test.go:3169, unsorted).

From internal/store/collectionprefix_conformance_test.go: `qdrantBackedPackages` (line 72), the seam rule keyed on the enclosing function name `newTestStore` (line 106), and `extractTestCollectionPrefix` reading a package-level `const testCollectionPrefix = "..."`.
</interfaces>
</context>

## Artifacts this phase produces (plan 01-01's share)

Nothing below exists on HEAD today.

| Kind | Artifact | Created by |
|------|----------|------------|
| Go func (production) | `store.NewQdrantClient(host string, port int, opts ...grpc.DialOption) (*qdrant.Client, error)` in `internal/store/store.go` | Task 1 |
| Go package | `internal/store/storetest` (non-`_test.go` test-support package, import path `github.com/seanb4t/engram/internal/store/storetest`) | Task 1 |
| Go file | `internal/store/storetest/storetest.go` | Task 1 |
| Go const | `storetest.RecvLimit = 4 << 20` | Task 1 |
| Go const | `storetest.QdrantImage = "qdrant/qdrant:v1.19.1"` | Task 1 |
| Go type / func | `storetest.Option`, `storetest.IgnoreRequireQdrant() Option` | Task 1 |
| Go func | `storetest.Run(m *testing.M, opts ...Option) int` | Task 1 |
| Go func | `storetest.RequireQdrant() (bool, error)` | Task 1 |
| Go func | `storetest.SkipOrFailNoQdrant(t testing.TB)` | Task 1 |
| Go func | `storetest.Addr() string`, `storetest.EnvAddr() string`, `storetest.ContainerBooted() bool` | Task 1 |
| Go func | `storetest.AssertSharedAddressHonored(t testing.TB)` | Task 1 |
| Go func | `storetest.Dial(t testing.TB, recvLimit int, opts ...grpc.DialOption) *qdrant.Client` | Task 1 |
| Go func (unexported) | `dialOptions(recvLimit int, opts []grpc.DialOption) ([]grpc.DialOption, error)`, `splitAddr(addr string) (string, int, error)`, `terminate(*tcqdrant.QdrantContainer)` | Task 1 |
| Go file | `internal/store/storetest/storetest_test.go` | Task 1 |
| Test | `TestMain` (storetest), `TestRequireQdrant`, `TestSplitAddr`, `TestDialOptions`, `TestRecvLimitIsFourMiB`, `TestSkipOrFailNoQdrantSkipsWhenNotRequired`, `TestDialRoundTrip` | Task 1 |
| Test helper | storetest's `const testCollectionPrefix = "storetest_"`, `testCollection`, `newTestStore` | Task 1 |
| Gate data | third `qdrantClientHolderAllowlist` entry `internal/store/storetest/storetest.go` | Task 1 |
| Gate data | `qdrantBackedPackages` entry `internal/store/storetest` | Task 1 |
| Go file | `internal/store/storetest/seed.go` | Task 2 |
| Go type / consts | `storetest.Shape`, `storetest.ManySmall`, `storetest.FewLarge`, `(Shape).String()`, `storetest.ManySmallRecords = 1000` | Task 2 |
| Go types | `storetest.Spec{Limit, Shape, Vector, Template}`, `storetest.Fixture{Shape, Scope, Owner, IDs, RecordBytes, TotalBytes}` | Task 2 |
| Go func | `storetest.SeedOversized(t testing.TB, st *store.Store, spec Spec) Fixture` | Task 2 |
| Go func (unexported) | `layout`, `checkOversized`, `validateSpec`, `fixtureIdentity`, `createdAtFor` | Task 2 |
| Go file / tests | `internal/store/storetest/seed_test.go`: `TestLayout`, `TestCheckOversized`, `TestValidateSpec`, `TestFixtureIdentityIsUnique`, `TestCreatedAtStrictlyIncreasing`, `TestSeedOversizedShapes` | Task 2 |
| Gate change | `fileRefsQdrantClient` + `qdrantClientLocalNames` recognize `store.NewQdrantClient`; D-13 loop in `TestQdrantClientIsHeldOnlyByStorePackage` | Task 3 |

## Edge coverage (spec-less fallback)

The deterministic edge probe surfaced 10 rows across this phase's two requirements. Accounting for the
whole phase: **10 authored + 0 flagged = 10**. This plan authors nine of them (all `resolved`, verification
`explicit`) as `must_haves.truths`; the tenth (REQ-test-client-parity `ordering`) is authored by plan 01-02,
where the parity test that proves it lives.

| Requirement | Probe category | Disposition |
|---|---|---|
| REQ-test-client-parity | `boundary` | Authored here: `RecvLimit == 4<<20`; `dialOptions` rejects 0/-1, accepts 1. `TestRecvLimitIsFourMiB`, `TestDialOptions`. |
| REQ-test-client-parity | `adjacency` | Authored here: caller options kept in order, named limit appended last, checked by interface identity. `TestDialOptions`. |
| REQ-test-client-parity | `empty` | Authored here: no address → skip/fail, never dial; `splitAddr` rejection rows. `TestSplitAddr`, `TestSkipOrFailNoQdrantSkipsWhenNotRequired`. |
| REQ-test-client-parity | `ordering` | Authored by plan 01-02 (base otelgrpc option before caller options, both effective). |
| REQ-test-client-parity | `precision` | Authored here: exact `int` byte count, no conversion. Task 1 source assertion. |
| REQ-oversized-fixture-helper | `boundary` | Authored here: strictly-greater total; single record at/over limit rejected. `TestCheckOversized`. |
| REQ-oversized-fixture-helper | `adjacency` | Authored here: fresh uuid identity per fixture. `TestFixtureIdentityIsUnique`. |
| REQ-oversized-fixture-helper | `empty` | Authored here: invalid spec rejected before any write. `TestValidateSpec`, `TestLayout`. |
| REQ-oversized-fixture-helper | `ordering` | Authored here: strictly increasing, second-distinct `created_at` in write order. `TestCreatedAtStrictlyIncreasing`, `TestSeedOversizedShapes`. |
| REQ-oversized-fixture-helper | `precision` | Authored here: ceiling division, logical-byte self-assertion. `TestLayout`. |

## Prohibition probe (spec-less fallback, in-prompt recall)

Stage 1 raw candidates (over-produced on purpose) for both requirements: must not dial the operator's
production Qdrant; must not write through a raw client in test support; must not let a required-Qdrant run
skip green; must not coerce an invalid `ENGRAM_REQUIRE_QDRANT` to false; must not make a regression test
depend on the production backstop; must not leave fixture records behind; must not seed one record larger
than the limit; must not assert grpc-go's default; must not run oversized fixtures in parallel; must not
change production dial options. Stage 2 keeps four values/safety/transparency items (three authored in
this plan's `must_haves.prohibitions`, the backstop-independence one in plan 01-04's), all
descriptor-less; the rest are routine correctness owned by the edge probe, D-12, or rule `m45p2b4bp7`.
Canon breadcrumb: none applies (no injection, traversal, or PII surface).

<!-- planner-discipline-allow: ENGRAM_QDRANT_ADDR -->

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end shared client: store.NewQdrantClient, storetest lifecycle and Dial, one real-Qdrant round trip</name>
  <precondition>Docker is reachable (`docker info` exits 0) or ENGRAM_QDRANT_TEST_ADDR names a live Qdrant gRPC endpoint: this task's verify runs real-Qdrant tests under ENGRAM_REQUIRE_QDRANT=1, which fails rather than skips without one.</precondition>
  <reversibility rating="costly">D-01 (CONTEXT.md): once the composition root and all eleven test dial sites import store.NewQdrantClient (plans 01-01 to 01-03), undoing it touches every call site; no data or wire contract is involved, so it is not one-way.</reversibility>
  <files>internal/store/store.go, internal/store/storetest/storetest.go, internal/store/storetest/storetest_test.go, internal/store/schemaversion_stamp_gate_test.go, internal/store/collectionprefix_conformance_test.go</files>
  <read_first>
    - `internal/store/store.go` lines 1-45 (import block and package doc) and lines 494-512 (`New`) — the new constructor goes directly above `New`.
    - `internal/server/tools.go` lines 108-134 — `storeFromConfig`, the literal dial code being extracted (do NOT edit tools.go in this task; Task 3 switches it).
    - `internal/store/store_test.go` lines 29-305 — the harness being ported into storetest: `qdrantImageTag`, `requireQdrant`, `TestMain`, `terminateQdrant`, `dialTestClient`, `TestRequireQdrant`, `TestSharedQdrantAddressHonored`, and the two dial skip/fail tests. Copy behavior and message strings verbatim; do NOT delete anything from store_test.go (plan 01-04 does).
    - `internal/retrievaleval/retrieval_eval_test.go` lines 349-381 — the one TestMain with no fail-closed check (RESEARCH Pitfall 6), which `IgnoreRequireQdrant` exists to serve.
    - `internal/testhttp/reuse.go` lines 1-19 — the non-`_test.go` test-support package doc-comment precedent.
    - `internal/store/schemaversion_stamp_gate_test.go` lines 748-765 — `qdrantClientHolder` and `qdrantClientHolderAllowlist`.
    - `internal/store/collectionprefix_conformance_test.go` lines 50-80 and 85-130 — `qdrantPackage`, `qdrantBackedPackages`, and the `newTestStore` seam rule.
    - `.planning/phases/01-test-harness-fixture-helper/01-CONTEXT.md` decisions D-01, D-02, D-03, D-04, D-06, D-09, D-10.
    - `.planning/phases/01-test-harness-fixture-helper/01-RESEARCH.md` Pattern 1, Pattern 2, Pitfalls 2 and 6, and the "4 TestMains" comparison table.
  </read_first>
  <behavior>
    Write `internal/store/storetest/storetest_test.go` alongside the implementation; the pure tests go RED
    first (package does not compile / functions missing), then GREEN.
    - `TestRecvLimitIsFourMiB`: `RecvLimit == 4<<20` and `RecvLimit == 4194304` (our named constant, never grpc-go's default — rule m45p2b4bp7).
    - `TestRequireQdrant`: the exact six-row table from `store_test.go` (`""`→false, `true`/`1`→true, `false`/`0`→false, `treu`→error, never coerced to false).
    - `TestSplitAddr`: `localhost:6334` → (`localhost`, 6334, nil); `127.0.0.1:1` → ok; errors for `""`, `localhost`, `localhost:0`, `localhost:-1`, `localhost:x`, `localhost:65536`.
    - `TestDialOptions`: limit 0 and -1 → error; limit 1 → one option; with two sentinel caller options `grpc.WithUserAgent("storetest-a")` and `grpc.WithAuthority("storetest-b")` the result has length 3, `got[0] == opts[0]`, `got[1] == opts[1]` (interface identity; order preserved), and the named limit is the third element (neither sentinel).
    - `TestSkipOrFailNoQdrantSkipsWhenNotRequired`: subtest A sets `ENGRAM_REQUIRE_QDRANT` to empty via `t.Setenv`, runs an inner `t.Run` calling `SkipOrFailNoQdrant`, and requires the inner run to report passed (skipped, not failed); subtest B saves and restores the package's ignore-require flag, sets it true, sets `ENGRAM_REQUIRE_QDRANT=1`, and requires the inner run to be skipped as well.
    - `TestDialRoundTrip` (real Qdrant): `c := Dial(t, RecvLimit)`; a store from the `newTestStore` seam over `testCollection("roundtrip_" + uuid.NewString())`; `EnsureCollection(ctx, 3)`; `t.Cleanup` deleting that collection via `c.DeleteCollection`; one `Upsert` of a small record under a fresh owner; `Get` returns the same Content; `ListScopes(ctx, store.Authenticated(owner))` counts exactly 1 for the record's scope.
  </behavior>
  <action>
NEVER run any git stash subcommand — to inspect a pristine file use `git show HEAD:<path>` into the scratchpad instead. Commit with an explicit pathspec (rule n6m4as49mr). `TestRedEvidencePatchesAreLive` is the single expected red until plan 01-05 Task 3; do not touch `redevidence_harness_test.go`.

(1) `internal/store/store.go` (D-01, D-02, D-03): add exported `NewQdrantClient(host string, port int, opts ...grpc.DialOption) (*qdrant.Client, error)` directly above `New`. Doc comment: it is the single constructor for every `*qdrant.Client` in this module (production composition root and every test); it applies the shared base dial options — today exactly `grpc.WithStatsHandler(otelgrpc.NewClientHandler())`, the option production has always used — FIRST, then appends caller options, mirroring qdrant-go-client's own base-then-`Config.GrpcOptions` order so caller options take precedence; a receive limit is passed by the caller in upstream vocabulary (`grpc.WithDefaultCallOptions(grpc.MaxCallRecvMsgSize(n))`), never through a wrapper; it adds no production behavior (the MaxCallRecvMsgSize backstop is REQ-recv-limit-backstop, a later phase). Body: allocate a fresh `dialOpts` slice with capacity `1+len(opts)`, append the base option then `opts...`, and `return qdrant.NewClient(&qdrant.Config{Host: host, Port: port, GrpcOptions: dialOpts})` — the error is returned unwrapped (callers wrap). Add imports `google.golang.org/grpc` and `otelgrpc "go.opentelemetry.io/contrib/instrumentation/google.golang.org/grpc/otelgrpc"`. No other store.go change.

(2) New `internal/store/storetest/storetest.go` (package `storetest`, D-04, D-06, D-10): the two-line SPDX header byte-identical to store.go's, then a package doc in the `internal/testhttp/reuse.go` shape stating: shared test support for every Qdrant-backed test package; a normal non-`_test.go` package because Go cannot share `_test.go` across packages; it deliberately imports `testing` and testcontainers (unlike testhttp) and must never be imported by production code; it owns the named receive limit, the one Qdrant image tag, the container lifecycle, the one `ENGRAM_REQUIRE_QDRANT` parser, and the dial helper. It must reference the production Qdrant address variable only conceptually — never by its literal name — and it reads exactly two env vars: `ENGRAM_QDRANT_TEST_ADDR` and `ENGRAM_REQUIRE_QDRANT`. Contents, each exported identifier with a doc comment starting with its name (revive):
- `const RecvLimit = 4 << 20` — the receive limit every oversized regression test in this milestone names explicitly and passes to both `Dial` and the seeder (D-04); the ceiling production effectively runs at today; tests keep pinning it after the production backstop lands so a passing test proves the bounded-read mechanism, not the backstop; never derived from grpc-go's default (rule m45p2b4bp7).
- `const QdrantImage = "qdrant/qdrant:v1.19.1"` — the one pinned image for every testcontainer fallback; CI's `services.qdrant.image` must stay byte-identical (gated by plan 01-05); bumping it requires re-verifying the TOCTOU contract guarded by internal/store's separate `qdrantTOCTOUVerifiedVersion`.
- Unexported package state set only by `Run`: `ran`, `ignoreRequire` (bools), `envAddr`, `addr` (strings), `booted` (bool). Unexported `runConfig` struct; exported `type Option func(*runConfig)`; `func IgnoreRequireQdrant() Option` — for a package whose pre-existing harness never consulted `ENGRAM_REQUIRE_QDRANT` (internal/retrievaleval): `Run` then never parses it and `SkipOrFailNoQdrant` always skips.
- `func RequireQdrant() (bool, error)` — the single parser: unset/empty → (false, nil); otherwise `strconv.ParseBool`; any invalid value → `fmt.Errorf("ENGRAM_REQUIRE_QDRANT: invalid value %q: %w", v, err)`, never coerced to false.
- `func SkipOrFailNoQdrant(t testing.TB)` — `t.Helper()`; unless `ignoreRequire`: a parse error → `t.Fatalf("%v", err)`; required → `t.Fatal("no Qdrant available and ENGRAM_REQUIRE_QDRANT is set: failing instead of skipping")` (this exact text is asserted by internal/store's re-exec test); otherwise `t.Skip("no Qdrant available: set ENGRAM_QDRANT_TEST_ADDR or start Docker (testcontainers)")`.
- `func Run(m *testing.M, opts ...Option) int` — returns the exit code and never calls os.Exit itself, so a caller can clean up (internal/e2e removes its temp dir) before exiting. Steps, preserving the store harness verbatim (D-10): set `ran = true` and apply opts; capture `envAddr = os.Getenv("ENGRAM_QDRANT_TEST_ADDR")` BEFORE anything mutates it; unless `ignoreRequire`, call `RequireQdrant` and on error print `fatal: <err>` to stderr and return 1; if `envAddr != ""`, set `addr = envAddr` and return `m.Run()` (shared fast path, no container); otherwise run `tcqdrant.Run` under a `context.WithTimeout` of `3*time.Minute`, cancelling explicitly on every path; on boot error print `qdrant testcontainer unavailable (<err>); integration tests will skip — set ENGRAM_QDRANT_TEST_ADDR or start Docker`, and if required also print `fatal: ENGRAM_REQUIRE_QDRANT is set — failing instead of skipping` and return 1, else return `m.Run()`; resolve `GRPCEndpoint` (on error terminate, print `qdrant grpc endpoint: <err>`, return 1); set `booted = true`; if required and the address is empty, terminate, print `fatal: ENGRAM_REQUIRE_QDRANT is set but no Qdrant address resolved`, return 1; export the booted address with `os.Setenv("ENGRAM_QDRANT_TEST_ADDR", addr)` (D-10: this is how internal/store's in-package tests, which cannot import storetest, receive it; on error terminate and return 1); then `code := m.Run()`, terminate, return code. `terminate` uses a `30*time.Second` context and discards `Terminate`'s error explicitly (`_ =`).
- `func Addr() string`, `func EnvAddr() string` (the value the process started with, captured before `Run` exports a booted address), `func ContainerBooted() bool`.
- `func AssertSharedAddressHonored(t testing.TB)` — the body every package's CI-counted shared-address test delegates to: `t.Fatal` if `Run` has not run; `t.Skip("ENGRAM_QDRANT_TEST_ADDR not set: this test only asserts the shared-instance path")` when `EnvAddr()` is empty; `t.Errorf` when `Addr() != EnvAddr()` (shared CI Qdrant address not honored); `t.Error` when `ContainerBooted()` (booted its own testcontainer although the shared address was set). Declare no test with the CI-counted name inside storetest: `.github/workflows/ci.yaml` pins 3 PASS + 1 SKIP over `./internal/store/...`, which includes this package.
- `func Dial(t testing.TB, recvLimit int, opts ...grpc.DialOption) *qdrant.Client` (D-02, D-04): `t.Helper()`; `t.Fatal` with a message naming storetest.Run when `Run` has not run; build `dialOpts, err := dialOptions(recvLimit, opts)` and `t.Fatalf` on error; if `addr == ""` call `SkipOrFailNoQdrant(t)`; `host, port, err := splitAddr(addr)` (`t.Fatalf` on error); then bind exactly `c, err := store.NewQdrantClient(host, port, dialOpts...)` — an assignment, not a direct return, because the client-holder gate's write check needs a client-bound identifier in this file (D-13); `t.Fatalf` on error; return `c`. Dial registers no Close: every pre-convergence dial site left its client open for the process lifetime, and closing is a lifecycle change this phase does not make.
- `func dialOptions(recvLimit int, opts []grpc.DialOption) ([]grpc.DialOption, error)`: `recvLimit` of 0 or less → an error saying a positive byte count naming the limit under test is required; otherwise a fresh slice of the caller's options in order followed LAST by `grpc.WithDefaultCallOptions(grpc.MaxCallRecvMsgSize(recvLimit))` — `recvLimit` passed through as the same `int`, no conversion.
- `func splitAddr(addr string) (string, int, error)`: `net.SplitHostPort` then `strconv.Atoi`; reject a port outside 1..65535.

(3) New `internal/store/storetest/storetest_test.go` (package `storetest`; SPDX header): `func TestMain(m *testing.M) { os.Exit(Run(m)) }`; `const testCollectionPrefix = "storetest_"` (disjoint from `store_`, `server_`, `e2e_`, `retrievaleval_`); `testCollection(name string) string`; `newTestStore(t testing.TB, c *qdrant.Client, name string) *store.Store` — the prefix-asserting seam in the exact shape of internal/server's, the ONLY function in this package's tests that calls `store.New`. Then the tests in `<behavior>`. Real-Qdrant tests use a fresh uuid-named collection each and delete it in `t.Cleanup` through their own client. No test in this file opts into parallel execution.

(4) `internal/store/schemaversion_stamp_gate_test.go` (D-09): append a third `qdrantClientHolderAllowlist` entry `{file: "internal/store/storetest/storetest.go", justification: "Test-support dialer (D-09): Dial constructs a client via store.NewQdrantClient and hands it to the caller; storetest never transmits a write on a client it holds, and its seeder writes only through *store.Store (D-08)."}`, and update the allowlist's doc comment from "exactly TWO members" to three, describing the storetest entry. Leave `fileRefsQdrantClient`, `qdrantClientLocalNames` and the write-check block untouched in this task (Task 3 changes them).

(5) `internal/store/collectionprefix_conformance_test.go`: append `{name: "internal/store/storetest", dir: "storetest", allowUnqualified: false},` to `qdrantBackedPackages`, and replace the fixed "four packages" wording in the `qdrantPackage` / `qdrantBackedPackages` / file-header doc comments with count-free wording (every Qdrant-backed package, now including storetest). No logic change.

Commit as `refactor(store): add NewQdrantClient and the storetest harness package` with pathspec `internal/store/store.go internal/store/storetest/storetest.go internal/store/storetest/storetest_test.go internal/store/schemaversion_stamp_gate_test.go internal/store/collectionprefix_conformance_test.go`, ending the message with the session's attribution trailer lines.
  </action>
  <verify>
    <automated>out=$(ENGRAM_REQUIRE_QDRANT=1 go test ./internal/store/storetest/ -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | tail -n 20; test "$rc" -eq 0 &amp;&amp; printf '%s\n' "$out" | rg -q -e '^--- PASS: TestDialRoundTrip' &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e '^--- SKIP: ' | wc -l)" -eq 0</automated>
    <fails_when>non-zero `go test` exit, any `--- FAIL:` line, any TOP-LEVEL `--- SKIP:` line (a test never reached Qdrant or skipped instead of failing under ENGRAM_REQUIRE_QDRANT=1; the deliberately skipping inner subtests of TestSkipOrFailNoQdrantSkipsWhenNotRequired are indented and not counted), or no `--- PASS: TestDialRoundTrip` line</fails_when>
    <automated>out=$(go test ./internal/store/ -run '^(TestQdrantClientIsHeldOnlyByStorePackage|TestEveryStoreConstructionRoutesThroughSeam|TestCollectionPrefixesAreDisjoint)$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^(--- |ok|FAIL)|ordered pairs'; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e '^--- PASS: Test' | wc -l)" -eq 3 &amp;&amp; printf '%s\n' "$out" | rg -q 'compared 20 ordered pairs across 5 packages'</automated>
    <fails_when>non-zero exit, fewer than three top-level `--- PASS: Test` lines, or the log line `compared 20 ordered pairs across 5 packages` is absent (storetest not registered with the prefix gate)</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n -F 'func NewQdrantClient(host string, port int, opts ...grpc.DialOption) (*qdrant.Client, error)' internal/store/store.go` prints exactly one line, and `rg -o 'otelgrpc[.]NewClientHandler' internal/store/store.go | wc -l` prints `1`.
    - `head -2 internal/store/storetest/storetest.go` and `head -2 internal/store/storetest/storetest_test.go` each print the two SPDX lines byte-identical to `head -2 internal/store/store.go`.
    - `rg -n -F 'const RecvLimit = 4 << 20' internal/store/storetest/storetest.go` and `rg -n -F 'const QdrantImage = "qdrant/qdrant:v1.19.1"' internal/store/storetest/storetest.go` each print exactly one line.
    - `rg -n -F 'grpc.MaxCallRecvMsgSize(recvLimit)' internal/store/storetest/storetest.go` prints exactly one line (the limit reaches grpc as the same int, unconverted — edge `precision`).
    - `rg -o 'ENGRAM_QDRANT_ADDR' internal/store/storetest/storetest.go | wc -l` prints `0` (the production address variable is never read or named).
    - `rg -n '^func Test[A-Za-z]*Shared' internal/store/storetest/ | wc -l` prints `0` (no CI-counted shared-address test in this package).
    - Fail-closed parse preserved: `ENGRAM_REQUIRE_QDRANT=treu go test ./internal/store/storetest/ -run '^TestRecvLimitIsFourMiB$' -count=1` exits non-zero and its output contains `ENGRAM_REQUIRE_QDRANT: invalid value "treu"`.
    - `go test ./internal/store/storetest/ -run '^(TestRecvLimitIsFourMiB|TestRequireQdrant|TestSplitAddr|TestDialOptions|TestSkipOrFailNoQdrantSkipsWhenNotRequired)$' -count=1 -v` shows `--- PASS` for all five.
    - `go list -deps ./cmd/engram | rg -o 'store/storetest$' | wc -l` prints `0`, and `git diff --exit-code main...HEAD -- go.mod go.sum` exits 0.
    - `go vet ./internal/store/...` exits 0 and `golangci-lint run ./internal/store/...` reports no findings.
  </acceptance_criteria>
  <done>`store.NewQdrantClient` exists with the production base option; storetest provides the named limit, image tag, lifecycle, the one ENGRAM_REQUIRE_QDRANT parser, and Dial; storetest's own suite (including the real-Qdrant round trip) passes under ENGRAM_REQUIRE_QDRANT=1; storetest is an allowlisted holder and a registered Qdrant-backed package; committed with an explicit pathspec.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Two-shape oversized seeder: SeedOversized writes many-small or few-large fixtures that self-assert they exceed the named limit</name>
  <precondition>Docker is reachable (`docker info` exits 0) or ENGRAM_QDRANT_TEST_ADDR names a live Qdrant gRPC endpoint: this task's verify runs real-Qdrant tests under ENGRAM_REQUIRE_QDRANT=1, which fails rather than skips without one.</precondition>
  <files>internal/store/storetest/seed.go, internal/store/storetest/seed_test.go</files>
  <read_first>
    - `internal/store/storetest/storetest.go` and `internal/store/storetest/storetest_test.go` (Task 1) — `RecvLimit`, `Dial`, `newTestStore`, `testCollection`, the SPDX header shape.
    - `internal/store/store_test.go` lines 1793-1840 — `TestListScopesFullPayloadsOverGRPCLimit` (#583), the fixture shape and `n*contentBytes` self-check being generalized (40 x 128 KiB).
    - `internal/store/store.go` lines 795-817 (`Upsert`, one RPC per record), 2619-2654 (`DeleteAll` owner-scoped), 1458 (`maxListLimit = 1000`), 612-625 (`created_at` written as RFC3339 seconds).
    - `internal/store/subject.go` lines 36-47 — `Authenticated` panics on an empty owner.
    - `.planning/phases/01-test-harness-fixture-helper/01-CONTEXT.md` decisions D-05, D-08, D-12 and the Deferred list (a single record larger than the limit is out of scope).
    - `.planning/phases/01-test-harness-fixture-helper/01-RESEARCH.md` Pitfall 7 and Assumptions A1/A2.
  </read_first>
  <behavior>
    Write `seed_test.go` first; run it and observe RED (undefined symbols), then implement `seed.go`.
    - `TestLayout` table (limit, shape → records, recordBytes, or error): (4<<20, FewLarge) → (40, 131072); (4<<20, ManySmall) → (1000, 5243); (64<<20, FewLarge) → (40, 2097152); (64<<20, ManySmall) → (1000, 83887); (32, FewLarge) → (40, 1); (1000, ManySmall) → (1000, 2); (2, ManySmall) → (1000, 1); errors for (31, FewLarge), (1, ManySmall), (0, FewLarge), (-1, ManySmall), (4<<20, Shape(0)), (4<<20, Shape(99)). For every accepted row also assert records*recordBytes is at least limit + ceil(limit/4), strictly greater than limit, and recordBytes is below limit.
    - `TestCheckOversized` table: (100, 101, 1) → nil; (100, 100, 1) → error (equal is not oversized); (4<<20, 32, 131072) → error (exactly the limit); (100, 1, 100) → error (single record at the limit); (100, 1, 101) → error (single record over it); (100, 0, 50) → error; (100, 5, 0) → error; (0, 10, 10) → error.
    - `TestValidateSpec`: a nil store → error; an empty Vector → error; each invalid limit/shape row from TestLayout → error; a valid FewLarge spec at `RecvLimit` against a nil-client store built with `newTestStore(t, nil, testCollection("validate"))` (a nil client never dials) → (40, 131072, nil).
    - `TestFixtureIdentityIsUnique`: two `fixtureIdentity(FewLarge)` calls return different scopes and different owners; each scope starts with `storetest-oversized:project:few-large-` and each owner with `storetest-owner-`.
    - `TestCreatedAtStrictlyIncreasing`: for a base truncated to the second, `createdAtFor(base, i+1)` is exactly one second after `createdAtFor(base, i)` for i in 0..9, and formatting each with `time.RFC3339` yields ten distinct strings.
    - `TestSeedOversizedShapes` (real Qdrant, D-12): one subtest per shape in the order FewLarge, ManySmall (subtest names from `Shape.String()`: `few-large`, `many-small`); each dials with `Dial(t, RecvLimit)`, builds its own uuid-named collection through `newTestStore`, ensures dim 3, deletes the collection in `t.Cleanup`, calls `SeedOversized(t, st, Spec{Limit: RecvLimit, Shape: shape, Vector: []float32{0.1, 0.2, 0.3}})`, then asserts: `TotalBytes == len(IDs)*RecordBytes`; `TotalBytes` greater than `RecvLimit`; `RecordBytes` below `RecvLimit`; `len(IDs)` is 40 (few-large) or 1000 (many-small); `ListScopes(ctx, store.Authenticated(fx.Owner))` counts exactly `len(IDs)` for `fx.Scope`; `Get` of `IDs[0]`, `IDs[1]` and `IDs[len-1]` returns strictly increasing `CreatedAt`, the first two exactly one second apart.
  </behavior>
  <action>
NEVER run any git stash subcommand — to inspect a pristine file use `git show HEAD:<path>` into the scratchpad instead. Commit with an explicit pathspec (rule n6m4as49mr). `TestRedEvidencePatchesAreLive` is the single expected red until plan 01-05 Task 3.

Create `internal/store/storetest/seed.go` (SPDX header; D-05, D-08, D-12), every exported identifier doc-commented:
- `type Shape int` with `const ( ManySmall Shape = iota + 1; FewLarge )` — the zero value is deliberately invalid so a caller must choose; `func (s Shape) String() string` returning `many-small`, `few-large`, or `Shape(<n>)`.
- `const ManySmallRecords = 1000` — doc: mirrors internal/store's unexported `maxListLimit` so ONE List page of a ManySmall fixture overflows the limit (D-05); pinned against it by plan 01-04's `TestManySmallShapeFitsOneListPage`. Unexported `fewLargeDivisor = 32` (FewLarge records are limit/32, the #583 shape).
- `type Spec struct { Limit int; Shape Shape; Vector []float32; Template store.Memory }` — Limit is the caller-named receive limit (pass `RecvLimit`, D-04); Vector is stored on every record and must match the collection dimension; Template is copied for every record and lets later phases set Category, Tags, windows, Scope or Owner; the seeder always overwrites ID, Content and CreatedAt; an empty Template Scope or Owner gets a fresh generated value; an empty Actor defaults to the Owner.
- `type Fixture struct { Shape Shape; Scope, Owner string; IDs []string; RecordBytes, TotalBytes int }` — IDs in write order; TotalBytes is the logical content bytes actually written.
- `func layout(limit int, shape Shape) (records, recordBytes int, err error)`: reject a limit of 0 or less; `target := limit + (limit+3)/4` (limit plus ceil(limit/4), so at least 1.25 times the limit, D-05); FewLarge → `recordBytes = limit / fewLargeDivisor` (error when that is below 1), `records = ceil(target / recordBytes)`; ManySmall → `records = ManySmallRecords`, `recordBytes = ceil(target / records)`; any other shape → error; finally return `checkOversized(limit, records, recordBytes)`'s error if non-nil (so a ManySmall limit whose single record would reach the limit is rejected). Integer ceiling division only.
- `func checkOversized(limit, records, recordBytes int) error`: error when limit is 0 or less, records below 1, recordBytes below 1, recordBytes at or above limit (a single record at or over the limit is the deferred content-cap question — oversized fixtures must overflow by accumulation), or `records*recordBytes` not strictly greater than limit; the message names the logical byte total and the named limit.
- `func validateSpec(st *store.Store, spec Spec) (records, recordBytes int, err error)`: error when `st == nil` or `len(spec.Vector) == 0`, otherwise delegate to `layout(spec.Limit, spec.Shape)`.
- `func fixtureIdentity(shape Shape) (scope, owner string)`: scope `storetest-oversized:project:<shape>-<uuid>`, owner `storetest-owner-<uuid>` (`github.com/google/uuid`, already a direct dependency).
- `func createdAtFor(base time.Time, i int) time.Time`: `base.Add(time.Duration(i) * time.Second)`.
- `func SeedOversized(t testing.TB, st *store.Store, spec Spec) Fixture`: `t.Helper()`; `validateSpec` and `t.Fatalf` on error BEFORE any write; then the phase's single `testing.Short()` skip (D-12): `t.Skipf` stating the shape and that the fixture writes more than the named limit in bytes; resolve scope/owner (Template or `fixtureIdentity`); register `t.Cleanup` calling `st.DeleteAll(context.Background(), scope, store.Authenticated(owner))` and reporting a non-nil error with `t.Errorf` — registered BEFORE the first write so a partial seed is reclaimed (D-08: never `DeleteAllRaw`, which is package-internal to internal/store's tests); `ctx := context.Background()`; `base := time.Now().UTC().Truncate(time.Second).Add(-time.Duration(records) * time.Second)`; `content := strings.Repeat("x", recordBytes)`; loop `i` from 0 to records-1 SEQUENTIALLY (no goroutines: deterministic write order and created_at, and RESEARCH A2's concurrency option is declined): `m := spec.Template`, set `m.ID = uuid.NewString()`, `m.Content = content`, `m.Scope`, `m.Owner`, default `m.Actor`, `m.CreatedAt = createdAtFor(base, i)`, then write with exactly `if err := st.Upsert(ctx, m, spec.Vector); err != nil` → `t.Fatalf` naming the shape and index; append the id; add `len(m.Content)` to the logical total. After the loop re-run `checkOversized(spec.Limit, len(ids), recordBytes)` against what was actually written AND require the logical total to equal `len(ids)*recordBytes` (`t.Fatalf` otherwise). `t.Logf` one line: shape, records, record bytes, logical total, limit, elapsed wall time. Return the Fixture. The seeder only ever writes through `Store.Upsert` and never reaches for a raw client (D-08).

Create `internal/store/storetest/seed_test.go` (package `storetest`, SPDX header) with the tests in `<behavior>`; no test or subtest opts into parallel execution (D-12: at most one fixture live per package).

Record in the SUMMARY the seeding wall time `SeedOversized` logged for each shape (RESEARCH Assumption A1 was a one-data-point projection).

Commit as `test(storetest): add the two-shape oversized fixture seeder` with pathspec `internal/store/storetest/seed.go internal/store/storetest/seed_test.go`, ending the message with the session's attribution trailer lines.
  </action>
  <verify>
    <automated>out=$(ENGRAM_REQUIRE_QDRANT=1 go test ./internal/store/storetest/ -run '^(TestLayout|TestCheckOversized|TestValidateSpec|TestFixtureIdentityIsUnique|TestCreatedAtStrictlyIncreasing|TestSeedOversizedShapes)$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^\s*--- |storetest: seeded'; test "$rc" -eq 0 &amp;&amp; printf '%s\n' "$out" | rg -q -e '--- PASS: TestSeedOversizedShapes/few-large' &amp;&amp; printf '%s\n' "$out" | rg -q -e '--- PASS: TestSeedOversizedShapes/many-small' &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e '--- SKIP' | wc -l)" -eq 0</automated>
    <fails_when>non-zero exit, any `--- FAIL:` line, any `--- SKIP` line, or either `--- PASS: TestSeedOversizedShapes/few-large` or `--- PASS: TestSeedOversizedShapes/many-small` missing</fails_when>
    <automated>out=$(go test -short ./internal/store/storetest/ -run '^TestSeedOversizedShapes$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^\s*--- '; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e '--- SKIP: TestSeedOversizedShapes/' | wc -l)" -eq 2</automated>
    <fails_when>non-zero exit, or fewer than two `--- SKIP: TestSeedOversizedShapes/` subtest lines under `-short` (the D-12 skip does not live in the seeder)</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n -F 'func SeedOversized(t testing.TB, st *store.Store, spec Spec) Fixture' internal/store/storetest/seed.go` prints exactly one line.
    - `rg -o 'testing[.]Short[(][)]' internal/store/storetest/seed.go | wc -l` prints `1` and `rg -o 'testing[.]Short[(][)]' internal/store/storetest/seed_test.go | wc -l` prints `0` (the skip lives once, in the seeder — D-12).
    - `rg -o 'st[.]Upsert[(]ctx, m, spec[.]Vector[)]' internal/store/storetest/seed.go | wc -l` prints `1`; `rg -o '[.]DeleteAll[(]' internal/store/storetest/seed.go | wc -l` prints `1`; `rg -o '"github[.]com/qdrant/go-client/qdrant"' internal/store/storetest/seed.go | wc -l` prints `0` (D-08: seed.go does not even import the qdrant package, so it cannot reach a raw client).
    - `rg -o 't[.]Parallel' internal/store/storetest/seed_test.go internal/store/storetest/storetest_test.go | wc -l` prints `0`, and `rg -o 'go func' internal/store/storetest/seed.go | wc -l` prints `0`.
    - `head -2` of both new files matches `head -2 internal/store/store.go`.
    - The run log shows one `storetest: seeded` line per shape; the SUMMARY records both wall times.
    - `go vet ./internal/store/storetest/` exits 0 and `golangci-lint run ./internal/store/storetest/...` reports no findings.
  </acceptance_criteria>
  <done>SeedOversized seeds both shapes through Store.Upsert with strictly increasing second-distinct created_at, cleans up through Store.DeleteAll, self-asserts the logical total strictly exceeds the named limit, skips once under -short, and every pure and real-Qdrant seeder test passes; committed with an explicit pathspec.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Production composition root on the shared constructor, holder gate recognizes it, and the never-writes check covers every allowlisted holder (D-13)</name>
  <precondition>Docker is reachable (`docker info` exits 0) or ENGRAM_QDRANT_TEST_ADDR names a live Qdrant gRPC endpoint: this task's verify runs real-Qdrant tests under ENGRAM_REQUIRE_QDRANT=1, which fails rather than skips without one.</precondition>
  <files>internal/server/tools.go, internal/store/schemaversion_stamp_gate_test.go</files>
  <read_first>
    - `internal/server/tools.go` lines 1-37 (imports) and 108-134 (`storeFromConfig`).
    - `internal/store/schemaversion_stamp_gate_test.go` lines 137-190 (`scanQdrantCalls`), 290-305 (method sets), 748-998 (allowlist, `findModuleRoot`, `fileRefsQdrantClient`, `scanRepoForQdrantClientRefs`, `qdrantClientLocalNames`, `TestQdrantClientIsHeldOnlyByStorePackage`).
    - `internal/store/storetest/storetest.go` (Task 1) — `Dial`'s `c, err := store.NewQdrantClient(...)` binding the D-13 check reads.
    - `internal/store/store_test.go` line 3169 — `keysOf` (returns keys unsorted).
    - `.planning/phases/01-test-harness-fixture-helper/01-CONTEXT.md` decisions D-01, D-03, D-11, D-13; `01-RESEARCH.md` Pitfalls 4 and 5, Open Question 1.
  </read_first>
  <behavior>
    - With tools.go switched but the gate NOT yet updated, `go test ./internal/store/ -run '^TestQdrantClientIsHeldOnlyByStorePackage$' -count=1` goes RED (tools.go no longer names the qdrant package, so the derived holder set loses it and the allowlist entry reads as stale). Observe this RED, then update the gate and observe GREEN.
    - After the update the test logs `write-checked holder internal/server/tools.go` and `write-checked holder internal/store/storetest/storetest.go`, never one for `internal/store/store.go`.
    - D-13 RED observation (recorded in the SUMMARY; plan 01-05 packages it as a patch): temporarily add a discarded `c.Upsert(context.Background(), &qdrant.UpsertPoints{})` statement right after `Dial`'s client binding in storetest.go, run the same test, observe `--- FAIL: TestQdrantClientIsHeldOnlyByStorePackage` naming storetest.go and `Upsert`, then restore the file by capturing `git diff -- internal/store/storetest/storetest.go` into a scratchpad patch and running `git apply -R` on it; `git status --porcelain -- internal/store/storetest/storetest.go` must then print nothing.
  </behavior>
  <action>
NEVER run any git stash subcommand — to inspect a pristine file use `git show HEAD:<path>` into the scratchpad instead. Commit with an explicit pathspec (rule n6m4as49mr). `TestRedEvidencePatchesAreLive` is the single expected red until plan 01-05 Task 3.

(1) `internal/server/tools.go` (D-01, D-03): in `storeFromConfig` replace the direct client construction with exactly `qc, err := store.NewQdrantClient(host, port)`, keeping the `qdrant client: %w` wrap and everything else in the function unchanged. Remove the imports that become unused (the qdrant package, grpc, and the otelgrpc alias); keep `otelhttp` and every other import. Production dial options are unchanged: the same single base option now applied inside the shared constructor (D-03).

(2) `internal/store/schemaversion_stamp_gate_test.go`:
- `fileRefsQdrantClient`: add a `*ast.CallExpr` branch that also flags a call whose selector is package ident `store` with method `NewQdrantClient` (a construction through the shared constructor still makes the file a client holder); update its doc comment.
- `qdrantClientLocalNames`: bind the identifier for BOTH construction shapes — the existing `qdrant` + `NewClient` selector and `store` + `NewQdrantClient` (both return `(*Client, error)`, so Lhs index 0 is the client); update its doc comment accordingly (D-11: the composition-root check follows the new call shape).
- The tools.go allowlist justification becomes: composition root only — `storeFromConfig` constructs the client via `store.NewQdrantClient` and hands it straight to `store.New` without issuing a Qdrant operation on it.
- D-13: replace the tools.go-only write-check block at the end of `TestQdrantClientIsHeldOnlyByStorePackage` with a loop over `qdrantClientHolderAllowlist` that skips ONLY the entry `internal/store/store.go` (the one real holder, governed by the write-boundary gate above). For each other entry: read and parse `filepath.Join(root, filepath.FromSlash(e.file))`; `clientNames := qdrantClientLocalNames(file)`; `t.Fatalf` when empty (the entry's premise is stale — nothing to verify against); `scanQdrantCalls` with the union of `fullWriteMethods` and `partialWriteMethods`; `t.Errorf` for every site whose receiver is in `clientNames`, naming the file, method, receiver, line and enclosing function ("an allowlisted client holder must never itself transmit a write"); then `t.Logf("write-checked holder %s (client identifiers: %s)", e.file, <sorted names joined by ", ">)` and count it. After the loop require the count to equal `len(qdrantClientHolderAllowlist)-1`. Update the test's doc comment: the check now covers every allowlisted holder except store.go, so storetest's D-08 restriction is gate-enforced (D-13), not asserted by review.

Run the D-13 RED observation from `<behavior>` and record the failing line in the SUMMARY.

Commit as `refactor(server): build the production Qdrant client through store.NewQdrantClient` with pathspec `internal/server/tools.go internal/store/schemaversion_stamp_gate_test.go`, ending the message with the session's attribution trailer lines.
  </action>
  <verify>
    <automated>out=$(go test ./internal/store/ -run '^TestQdrantClientIsHeldOnlyByStorePackage$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e 'write-checked holder|^--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; printf '%s\n' "$out" | rg -q 'write-checked holder internal/server/tools[.]go' &amp;&amp; printf '%s\n' "$out" | rg -q 'write-checked holder internal/store/storetest/storetest[.]go' &amp;&amp; test "$(printf '%s\n' "$out" | rg -o 'write-checked holder internal/store/store[.]go' | wc -l)" -eq 0</automated>
    <fails_when>non-zero exit, a `--- FAIL:` line, either `write-checked holder` log line for tools.go or storetest.go missing, or a `write-checked holder internal/store/store.go` line present</fails_when>
    <automated>out=$(ENGRAM_REQUIRE_QDRANT=1 go test ./internal/server/ -run '^(TestStoreAndEmbedderFromEnvNoEnsureLoadsConfigOnce|TestBuildDepsFromEnvLoadsConfigOnce)$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e '^--- PASS: Test' | wc -l)" -eq 2</automated>
    <fails_when>non-zero exit, a `--- FAIL:` or `--- SKIP:` line, or fewer than two top-level `--- PASS: Test` lines (the production store builder broke)</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n -F 'qc, err := store.NewQdrantClient(host, port)' internal/server/tools.go` prints exactly one line.
    - `rg -n 'qdrant[.]NewClient[(]|otelgrpc[.]NewClientHandler' internal/server/tools.go | wc -l` prints `0`.
    - `rg -o '"NewQdrantClient"' internal/store/schemaversion_stamp_gate_test.go | wc -l` prints at least `2` (recognized by both `fileRefsQdrantClient` and `qdrantClientLocalNames`).
    - `go build ./... && go vet ./internal/server/ ./internal/store/...` exits 0; `golangci-lint run ./internal/server/... ./internal/store/...` reports no findings.
    - The SUMMARY records both observed REDs: the stale-allowlist red before the gate update, and the D-13 red (`--- FAIL: TestQdrantClientIsHeldOnlyByStorePackage` naming storetest.go) with the tree restored clean afterward.
    - `git diff --exit-code main...HEAD -- go.mod go.sum` exits 0.
  </acceptance_criteria>
  <done>Production builds its client through store.NewQdrantClient with unchanged options; the holder gate derives tools.go, storetest.go and store.go, recognizes the new call shape, and write-checks every holder except store.go, proven live by an observed RED; committed with an explicit pathspec.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → Qdrant | Every test client dials a Qdrant instance resolved from `ENGRAM_QDRANT_TEST_ADDR` or a testcontainer; which instance is dialed is the boundary a test harness must never let drift to an operator's real deployment. |
| test-support package → store write path | storetest now holds a `*qdrant.Client` (D-09); any write it transmitted directly would bypass `payload()`'s schema stamping and the write-boundary gate. |
| module → production binary | storetest is a non-`_test.go` package that imports `testing` and testcontainers; it must never enter the `cmd/engram` import graph. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | storetest writing to an operator's production/dev Qdrant | high | mitigate | `Addr()` is set only by `Run` from `ENGRAM_QDRANT_TEST_ADDR` or the booted container; Task 1 acceptance asserts the production address variable is absent from storetest.go; retrievaleval's "never the ambient address" property is preserved by plan 01-03 routing it through `Dial`. |
| T-01-02 | Tampering | raw Qdrant write path in test support bypassing schema stamping | high | mitigate | D-08: the seeder writes only via `Store.Upsert` and cleans up via `Store.DeleteAll` (Task 2 acceptance greps); D-13: Task 3 generalizes the holder write check to storetest.go and observes it RED against an injected `c.Upsert`; plan 01-05 registers that red-evidence patch. |
| T-01-03 | Repudiation | a Qdrant-backed suite reporting green by skipping under `ENGRAM_REQUIRE_QDRANT` | medium | mitigate | `RequireQdrant` is the single parser (invalid value is an error); `Run` fails closed on boot failure and on an empty resolved address; Task 1 acceptance proves `ENGRAM_REQUIRE_QDRANT=treu` exits non-zero. |
| T-01-04 | Tampering | test client dial options silently diverging from production | medium | mitigate | One constructor for both (D-01); Task 3 switches production onto it; the convergence gate (plan 01-05) makes any other construction loud. |
| T-01-05 | Denial of Service | oversized fixtures pressuring the shared CI Qdrant / runner (#497) | medium | mitigate | D-05 sizes at 1.25x the limit only (about 5 MiB per fixture); D-12: one shape per subtest, cleanup registered before the first write, no parallel tests, `-short` skip inside the seeder. |
| T-01-06 | Elevation of Privilege | test-only code (testing, testcontainers) entering the production binary | low | mitigate | Task 1 acceptance: `go list -deps ./cmd/engram` contains no storetest path; `go.mod`/`go.sum` unchanged. |
| T-01-SC | Tampering | npm/pip/cargo installs | low | accept | Not applicable: no package-manager install and no new Go module; every import (grpc, otelgrpc, qdrant go-client, testcontainers qdrant module, google/uuid) is already a direct dependency in go.mod. RESEARCH's Package Legitimacy Audit records zero new packages. |
</threat_model>

<verification>
- `ENGRAM_REQUIRE_QDRANT=1 go test ./internal/store/storetest/ -count=1 -v` passes with no top-level `--- SKIP:` line, including `TestDialRoundTrip` and both `TestSeedOversizedShapes` subtests.
- `go test ./internal/store/ -run '^(TestQdrantClientIsHeldOnlyByStorePackage|TestEveryStoreConstructionRoutesThroughSeam|TestCollectionPrefixesAreDisjoint)$' -count=1 -v` passes and logs both write-checked holders and `compared 20 ordered pairs across 5 packages`.
- `ENGRAM_REQUIRE_QDRANT=1 go test ./internal/store/ -count=1` fails ONLY `TestRedEvidencePatchesAreLive` (the expected red): `go test ./internal/store/ -count=1 -v 2>&1 | rg -o '^--- FAIL: \S+' | sort -u` prints exactly `--- FAIL: TestRedEvidencePatchesAreLive`.
- `ENGRAM_REQUIRE_QDRANT=1 go test ./internal/server/ -count=1` exits 0.
- `task lint` and `task license:check` exit 0; `git diff --exit-code main...HEAD -- go.mod go.sum` exits 0.
- After `01-01-SUMMARY.md` is written: `go test ./internal/keylinks/ -count=1` exits 0 (this plan's key_links are satisfiable against the code as built).
</verification>

<success_criteria>
- One exported constructor, `store.NewQdrantClient`, builds the production client and is the path storetest dials through (REQ-test-client-parity foundation, D-01, D-03).
- storetest provides the named 4 MiB limit, the image tag, the lifecycle, the single fail-closed parser, and a dial helper that requires a named limit (D-02, D-04, D-06, D-10).
- SeedOversized seeds many-small and few-large fixtures through the real write path that self-assert they exceed the named limit (REQ-oversized-fixture-helper, D-05, D-08, D-12).
- The client-holder gate derives three holders, recognizes the new constructor, and write-checks every holder except store.go (D-09, D-11, D-13).
</success_criteria>

<output>
Create `.planning/phases/01-test-harness-fixture-helper/01-01-SUMMARY.md` when done.
</output>
