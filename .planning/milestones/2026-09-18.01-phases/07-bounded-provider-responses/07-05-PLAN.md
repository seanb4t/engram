---
phase: 07-bounded-provider-responses
plan: 05
type: execute
wave: 4
depends_on:
  - 07-01
  - 07-02
  - 07-03
  - 07-04
files_modified:
  - internal/store/redevidence_harness_test.go
  - .planning/phases/07-bounded-provider-responses/red-evidence/07-01-drain-unbounded-by-bytes.patch
  - .planning/phases/07-bounded-provider-responses/red-evidence/07-01-drain-unbounded-by-time.patch
  - .planning/phases/07-bounded-provider-responses/red-evidence/07-01-drain-default-swallows-zero.patch
  - .planning/phases/07-bounded-provider-responses/red-evidence/07-03-timeout-zero-means-unbounded.patch
  - .planning/phases/07-bounded-provider-responses/red-evidence/07-03-error-body-read-unbounded.patch
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - REQ-provider-drain-bounded
  - REQ-provider-error-body-closed

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`redEvidenceDirs` gains exactly one new entry, keyed by this phase's red-evidence directory, mapping five patches to their target tests; the Phase 1 through 6 entries are byte-unchanged"
    - "Each patch is ONE behavioral mutation that still compiles and makes its named target test itself report a failure — never a build break — hand-verified by apply, run, revert before it is registered"
    - "The five directions cover every guarantee this phase adds: the byte axis, the time axis, the honored-zero defaults placement, the request-timeout ceiling, and the truncated error body — with both provider lanes and both requirements represented"
    - "Every earlier phase's patch still applies at this phase's HEAD; eleven of the fifty-eight mutate files this phase edited, so the whole harness is re-run, not only this phase's subtest"
    - "`TestRedEvidencePatchesAreLive` passes with sixty-three confirmed RED lines and leaves every mutated file clean"
    - "Both of this phase's requirements are ticked in the checklist and their traceability rows read complete; NO other requirement's checkbox or row moves"
    - "GitHub #347 is closed with a comment citing the three tests that pin it: the provider-snippet assertion, the drain-for-reuse test, and this phase's new truncation assertion"
    - "`task` exits 0 for the whole repository, and `go test ./internal/keylinks/ -count=1` exits 0 with every executed plan's key links satisfiable"
    - "No red-evidence patch carries a licence header, and no planning artifact gains a version-bearing heading"
    - statement: "The phase's recorded state is internally consistent at close: the roadmap's plan list, both requirement checkboxes and both traceability rows say the same thing about Phase 7, and no other milestone's rows moved"
      verification: backstop
  artifacts:
    - path: "internal/store/redevidence_harness_test.go"
      provides: "the redEvidenceDirs entry for this phase's five patches"
      contains: "07-bounded-provider-responses/red-evidence"
    - path: ".planning/phases/07-bounded-provider-responses/red-evidence/07-01-drain-unbounded-by-time.patch"
      provides: "RED evidence that the drain must be bounded by time, the axis a byte limit alone cannot close"
      contains: "httpdrain"
    - path: ".planning/phases/07-bounded-provider-responses/red-evidence/07-03-error-body-read-unbounded.patch"
      provides: "RED evidence that the provider error read stays bounded, the live proof behind REQ-provider-error-body-closed"
      contains: "maxErrorBodyBytes"
    - path: ".planning/REQUIREMENTS.md"
      provides: "this phase's two requirements marked complete in both the checklist and the traceability table"
      contains: "REQ-provider-drain-bounded"
  key_links:
    - from: "internal/store/redevidence_harness_test.go"
      to: ".planning/phases/07-bounded-provider-responses/red-evidence/07-01-drain-unbounded-by-time.patch"
      via: "the harness applies the phase's highest-value patch and requires its target test to fail"
      pattern: "07-01-drain-unbounded-by-time[.]patch"
    - from: "internal/store/redevidence_harness_test.go"
      to: ".planning/phases/07-bounded-provider-responses/red-evidence/07-03-error-body-read-unbounded.patch"
      via: "the error-body direction, the one that keeps the second requirement from resting on an assertion nobody can break"
      pattern: "TestSummarizeNon200ErrorBodyTruncated"
  prohibitions:
    - "MUST NOT register a patch that has not been hand-proven to make its own target test fail — a patch that breaks the build, or whose target passes with it applied, is evidence of nothing and makes the gate a false comfort"
    - "MUST NOT author or prove a patch against a dirty tree; the harness refuses to run when a file a patch touches is already modified, and reports a spurious failure instead"
    - "MUST NOT rewrite an earlier phase's patch to make it apply; if one has gone stale, report it with the patch name and the apply output"
    - "MUST NOT raise a timeout in the task runner or in CI to make the red-evidence gate pass"
    - "MUST NOT tick a requirement belonging to another phase, and MUST NOT tick either of this phase's before the whole repository gate is green"
    - "MUST NOT add a licence header to any file under `.planning`, and MUST NOT add a version-bearing heading to any planning artifact"
    - "MUST NOT close any GitHub issue other than the one this phase's decisions name"
---

<objective>
Make every guarantee this phase added a live, harness-checked RED proof; confirm the earlier phases'
patches still apply over this phase's edits; and close the phase — and the milestone — on a green
full gate with both requirements marked complete and the issue this phase's decisions promised to
close actually closed.

This is the phase's LAST plan, and Phase 7 is the milestone's last phase. It registers this phase's
entry in the red-evidence map after every other plan has shipped, exactly as plans 03-06, 04-08,
05-06 and 06-03 did for the previous four phases.

Decisions covered: D-12 (this phase's own red-evidence directory registered in `redEvidenceDirs`)
and D-10's second half (close the issue citing the tests that pin it). The five directions map onto
D-01 and D-05 (both drain axes), D-06 (the honored zero), D-07 and D-09 (the ceiling), and D-10 (the
truncated error body).

Purpose: both of this phase's requirements are proven RED-able, so the guarantees survive the next
contributor who finds them inconvenient.

Output: five patch files, one harness entry, two ticked requirements, one closed issue, and a green
gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-bounded-provider-responses/07-CONTEXT.md
@.planning/phases/07-bounded-provider-responses/07-01-SUMMARY.md
@.planning/phases/07-bounded-provider-responses/07-02-SUMMARY.md
@.planning/phases/07-bounded-provider-responses/07-03-SUMMARY.md
@.planning/phases/07-bounded-provider-responses/07-04-SUMMARY.md
@.planning/phases/06-cross-spine-partial-results/06-03-SUMMARY.md
@CLAUDE.md

<interfaces>
`internal/store/redevidence_harness_test.go`: `var redEvidenceDirs = map[string]map[string]string{…}`
— one top-level entry per phase directory (repo-root-relative), each mapping
`"<patch file>": "<TargetTest>"` with a trailing `// reverts: …` comment naming what the mutation
undoes and its decision ids. There are six entries today, holding fifty-eight patches between them.
The Phase 6 entry is the most recent and structurally the simplest; use it as the template.

`TestRedEvidencePatchesAreLive` (skipped under `-short`) globs `<dir>/*.patch`, requires set equality
with the mapping in BOTH directions, and per patch: refuses to run if a touched file is already
dirty, `git apply --check`, `git apply`, `go test -run '^Target$' -count=1 ./...` (which must exit
NON-zero and must be a test FAILURE, not an abnormal error), `git apply -R`, then asserts the touched
files are clean. It logs `confirmed RED: <patch> applied -> <Target> failed as expected`. It must
never run concurrently with itself. A directory whose glob matches zero patches fails its own
subtest. The map is non-empty, so the zero-applicability guard never fires — what an executor
actually hits when something is wrong is the dirty-tree refusal.

Per-patch authoring procedure (every prior phase's precedent): make the ONE mutation on a clean tree,
write `git diff -- <file> > <dir>/<name>.patch`, then prove it —
`git apply --check <patch> && git apply <patch> && go test -run '^Target$' -count=1 ./<pkg>/ -v`
shows the target's OWN failure line (a compile error is NOT acceptable) — then `git apply -R <patch>`
and `git status --porcelain -- <file>` prints nothing. Each patch has exactly one `diff --git` header
and no licence header. Never use a git stash; to inspect a pristine file use `git show HEAD:<path>`
into the scratchpad.

**The five directions, with the shape each mutation must take.** Each is chosen to be the smallest
change that still compiles and needs no import-block edit:

| Patch | Mutates | Mutation | Target test |
|---|---|---|---|
| `07-01-drain-unbounded-by-bytes.patch` | `internal/httpdrain/httpdrain.go` | the copy reads the body directly instead of through the byte-limited reader | `TestEmbedDrainBoundedByBytes` |
| `07-01-drain-unbounded-by-time.patch` | `internal/httpdrain/httpdrain.go` | the timer is armed with a hard-coded long duration instead of the caller's, so it never fires within a test — this keeps `t` declared and the tree compiling, unlike deleting the timer outright | `TestEmbedDrainBoundedByTimeUnderZeroRequestTimeout` |
| `07-01-drain-default-swallows-zero.patch` | `internal/embed/embed.go` | the drain byte default moves out of the struct literal into a post-options fallback, reintroducing the sibling convention D-06 deliberately diverges from, so an explicit zero is swallowed | `TestEmbedDrainOptionsHonorZero` |
| `07-03-timeout-zero-means-unbounded.patch` | `internal/summarize/summarize.go` | the post-options ceiling clamp in `New` is removed, so a non-positive request timeout is unbounded again | `TestSummarizeTimeoutCeiling` |
| `07-03-error-body-read-unbounded.patch` | `internal/summarize/summarize.go` | the error read drops its byte limit and reads the whole body, so an oversized provider body is surfaced in full | `TestSummarizeNon200ErrorBodyTruncated` |

Five is the proportionate count, not a floor to pad or a ceiling to squeeze under: one patch per
genuinely distinct RED direction, both provider lanes represented, and both of this phase's
requirements represented — the first four pin REQ-provider-drain-bounded, the fifth pins
REQ-provider-error-body-closed. The harness is already at fifty-eight patches and re-proves every one
on each run, so a sixth patch that duplicates an existing direction on the twin lane costs a full
re-proof for no new information.

**Earlier phases' patches that mutate files THIS phase edited** — these are why the whole harness is
re-run, not just this phase's subtest. Verified against the patch files themselves, not recalled:

| Patch | Mutates |
|---|---|
| `03-01-config-accepts-zero.patch` | `internal/config/validate.go` — plan 07-02 edited this file |
| `03-01-content-cap-removed.patch` | `internal/server/tools.go` |
| `03-01-tags-cap-removed.patch` | `internal/server/tools.go` |
| `03-03-record-caps-not-wired.patch` | `internal/server/tools.go` |
| `03-03-update-content-cap-removed.patch` | `internal/server/tools.go` |
| `03-03-update-gates-on-presence-not-change.patch` | `internal/server/tools.go` |
| `04-06-maximum-check-runs-after-embed.patch` | `internal/server/tools.go` |
| `04-06-surface-maximum-check-removed.patch` | `internal/server/tools.go` |
| `06-01-empty-scopes-substituted-for-absence.patch` | `internal/server/tools.go` |
| `06-01-helper-swallows-listscopes-error.patch` | `internal/server/tools.go` |
| `06-01-mcp-list-discards-hits.patch` | `internal/server/tools.go` |

Plan 07-04 added helpers and option wiring to `internal/server/tools.go`, and plan 07-02 added
validation blocks to `internal/config/validate.go`. Both are additive, so context drift is possible
but not certain. Phase 5 hit exactly this and regenerated one stale patch in place after
re-verifying it by hand; Phase 3 hit it too. If one has gone stale, report the patch name and the
`git apply` output — do not silently rewrite it.

`.planning/REQUIREMENTS.md`: the two checklist entries for this phase (`:59-60`) and their two
`## Traceability` rows (`:104-105`), which read `| REQ-provider-drain-bounded | Phase 7 | Pending |`
and the same for the second. At the start of this plan eighteen requirement boxes are ticked and two
are not; both of the two belong to this phase. No other checkbox may move.

Requirement readiness helper, run BEFORE ticking anything:
`node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" query requirements.ready-ids
.planning/phases/07-bounded-provider-responses/07-05-PLAN.md REQ-provider-drain-bounded
REQ-provider-error-body-closed` — it reports `ready` versus `blocked`; a blocked id means an earlier
plan's evidence is missing and must be resolved rather than ticked over.

**GitHub #347, and exactly what closing it must cite** (D-10): the requirement it carries was already
satisfied in substance by the bounded error read shipped in v0.12.x. Three tests now pin it, and the
closing comment must name all three per lane: the provider-snippet assertion
(`TestEmbedNon2xxIncludesStatusAndBody` / `TestSummarizeNon200IncludesStatusAndBody`), the
drain-for-reuse test (`TestEmbedNon2xxDrainsForReuse` / `TestSummarizeNon200DrainsForReuse`), and this
phase's new truncation assertion (`TestEmbedNon2xxErrorBodyTruncated` /
`TestSummarizeNon200ErrorBodyTruncated`). #457 is NOT closed here — it closes through the normal ship
flow with the rest of the phase's work.
</interfaces>

<test_names_this_phase_shipped>
Every target below must be re-resolved with `go test -list` before it is relied on: a `-run` pattern
matching nothing exits 0 with `no tests to run` and reports a permanent false green (durable records
`bsbsvn4hbc`, `gfh6q1ack4`; Phase 4 hit this with two of five seeded names).

- `internal/embed`: `TestEmbedDrainBoundedByBytes`,
  `TestEmbedDrainBoundedByTimeUnderZeroRequestTimeout`, `TestEmbedDrainOptionsHonorZero`,
  `TestEmbedTimeoutCeiling`, `TestEmbedNon2xxErrorBodyTruncated` (07-01)
- `internal/summarize`: `TestSummarizeDrainBoundedByBytes`, `TestSummarizeDrainOptionsHonorZero`,
  `TestSummarizeDrainBoundedByTimeUnderZeroRequestTimeout`, `TestSummarizeTimeoutCeiling`,
  `TestSummarizeNon200ErrorBodyTruncated` (07-03)
- `internal/httpdrain`: `TestDrainStopsAtByteBound`, `TestDrainClosesBodyAtTimeBound`,
  `TestDrainZeroSkipsEntirely` (07-01)
- `internal/config`: `TestProviderBoundRegistryEntries`, `TestValidateProviderBounds` (07-02)
- `internal/server`: `TestProviderBoundHelpersParseAndDefault`,
  `TestProviderBoundOptionsWiredIntoBothClients` (07-04)
</test_names_this_phase_shipped>
</context>

## Executor notes (every task)

- NEVER run any `git stash` subcommand. To inspect a pristine file use `git show HEAD:<path>` into
  the scratchpad instead.
- Conventional Commits, explicit pathspec on every commit, and the session's attribution trailer.
- Every patch is authored and proven on a CLEAN tree, one at a time; never run two harness or patch
  cycles concurrently, and never against a dirty tree — the harness refuses to run and reports a
  spurious failure. Every plan of Phase 5 hit this.
- Patches under `.planning/**` carry NO licence header, and no planning artifact gains a
  version-bearing heading.
- `internal/store`'s harness needs materially more than Go's default per-package timeout at this
  patch count: bare `task` timed out during Phase 6's close at fifty-eight patches. Every harness
  invocation in this plan carries an explicit `-timeout 180m` (WINDOWS entry 13/14). This is a
  measurement budget, never a way to make a failing gate pass.
- After any `roadmap update-plan-progress` or `state.advance-plan`, audit
  `git diff -U0 -- .planning/ROADMAP.md` and revert any hunk touching a milestone other than
  `2026-09-18.01`. This has recurred in most plans of Phases 4, 5 and 6; in Phase 6,
  `update-plan-progress "06"` matched a same-numbered row in a shipped milestone.
- Task 3 has this plan's only outward-facing step. Everything before it is recoverable from the git
  diff alone; the issue close is not, so it runs last and only once the gate is green.

<tasks>

<task type="tracer">
  <name>Task 1: One RED proof end to end — the time axis, the bound a byte limit alone cannot close</name>
  <files>internal/store/redevidence_harness_test.go, .planning/phases/07-bounded-provider-responses/red-evidence/07-01-drain-unbounded-by-time.patch</files>
  <precondition>Plans 07-01 through 07-04 are executed (all four SUMMARY files exist), the working tree is clean (`git status --porcelain` prints nothing), and Docker is reachable (`docker info` exits 0) or `ENGRAM_QDRANT_TEST_ADDR` names a live Qdrant gRPC endpoint — many of the fifty-eight already-registered targets need real Qdrant, and a skipped target is not a RED.</precondition>
  <read_first>
    - `internal/store/redevidence_harness_test.go` in full — the map, the harness, its tree-safety,
      set-equality and empty-glob guards, and its concurrency warning.
    - `.planning/phases/06-cross-spine-partial-results/06-03-SUMMARY.md` and one of that phase's
      patches — the most recent shape precedent.
    - `internal/httpdrain/httpdrain.go` as shipped by plan 07-01 — the real body, before writing any
      mutation, so the patch is aimed at the shipped code and not at the plan's description of it.
    - `internal/embed/embed_test.go`'s time-bound regression — the target's own assertion, so the
      mutation trips an assertion that exists rather than an incidental one.
    - `.planning/phases/07-bounded-provider-responses/07-01-SUMMARY.md` — the shipped identifier
      names and the observed elapsed time the assertion compares against.
  </read_first>
  <action>
Create `.planning/phases/07-bounded-provider-responses/red-evidence/` and author
`07-01-drain-unbounded-by-time.patch` by the per-patch procedure: in the shared drain helper, arm the
timer with a hard-coded long duration instead of the caller's, so it can never fire within a test.
Keep the mutation to that one argument — deleting the timer outright would leave its handle unused
and break the build, which is evidence of nothing.

Target the embed lane's time-bound regression, whose elapsed-time assertion then fails after the
trickle handler's own bounded budget rather than hanging. Prove it RED by hand and record the
observed failure line and the observed elapsed time.

This is the phase's highest-value direction, and the reason it goes first: the time axis is the one a
byte limit alone cannot close, it is the exact hole the roadmap and the issue name, and with it
reverted every other test in the phase still passes — a byte-bounded drain on a trickling body looks
entirely healthy right up until the goroutine never comes back.

Register it: add ONE new top-level entry to `redEvidenceDirs`, keyed by this phase's red-evidence
directory path, mapping the patch name to its target test with a trailing `// reverts:` comment
naming what the mutation undoes and its decision ids. Leave the Phase 1 through 6 entries and
everything else untouched.

Run the harness once, alone (verify). Commit as
`test(store): register phase 7 red-evidence for the drain time bound` with pathspec
`internal/store/redevidence_harness_test.go .planning/phases/07-bounded-provider-responses/red-evidence`.
  </action>
  <verify>
    <automated>out=$(ENGRAM_REQUIRE_QDRANT=1 go test ./internal/store/ -run '^TestRedEvidencePatchesAreLive$' -count=1 -v -timeout 180m 2&gt;&amp;1); rc=$?; printf '%s\n' "$out" | rg -e 'confirmed RED|^\s*--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e 'confirmed RED: ' | wc -l)" -eq 59 &amp;&amp; { dirty=$(git status --porcelain -- internal/httpdrain/httpdrain.go) || exit 1; test -z "$dirty"; }</automated>
    <fails_when>non-zero exit, a `--- FAIL:` line, a `confirmed RED:` count other than 59 (the earlier phases' fifty-eight plus this one), or the shared helper left dirty after the run</fails_when>
    <automated>n=$(rg -o -e '^diff --[g]it a/' .planning/phases/07-bounded-provider-responses/red-evidence/07-01-drain-unbounded-by-time.patch | wc -l); test "$n" -eq 1</automated>
    <fails_when>non-zero exit — the patch touches a number of files other than one, so it is not one behavioral mutation</fails_when>
    <automated>test "$(rg -o -e '07-bounded-provider-responses/red-evidence' internal/store/redevidence_harness_test.go | wc -l)" -eq 1</automated>
    <fails_when>a count other than 1 — the phase must have exactly one top-level entry in the map</fails_when>
  </verify>
  <acceptance_criteria>
    - The patch's only `diff --git` line names `internal/httpdrain/httpdrain.go`.
    - With `C` the commit SHA of this task (record it in the SUMMARY): `d=$(git diff "$C^..$C" -- internal/store/redevidence_harness_test.go) || exit 1; printf '%s\n' "$d" | rg -o -e '^-[^-]' | wc -l` prints `0` — the task only adds lines to the harness.
    - `rg -l 'SPDX-License-Identifier' .planning/phases/07-bounded-provider-responses/red-evidence/` prints nothing.
    - The target name was re-resolved with `go test -list` before the mapping was written, and the SUMMARY records both that resolution and the hand-verified failure line.
    - The SUMMARY states the observed wall-clock cost of the full harness run at fifty-nine patches, so the next phase can budget for it.
  </acceptance_criteria>
  <done>The phase's highest-value RED direction — the time axis no byte limit can close — is a registered, harness-confirmed patch; the harness passes with fifty-nine confirmed REDs and leaves the tree clean.</done>
</task>

<task type="auto">
  <name>Task 2: The remaining four RED directions registered and the harness green at sixty-three</name>
  <files>internal/store/redevidence_harness_test.go, .planning/phases/07-bounded-provider-responses/red-evidence/07-01-drain-unbounded-by-bytes.patch, .planning/phases/07-bounded-provider-responses/red-evidence/07-01-drain-default-swallows-zero.patch, .planning/phases/07-bounded-provider-responses/red-evidence/07-03-timeout-zero-means-unbounded.patch, .planning/phases/07-bounded-provider-responses/red-evidence/07-03-error-body-read-unbounded.patch</files>
  <precondition>Task 1 is committed and the working tree is clean (`git status --porcelain` prints nothing), and Docker is reachable (`docker info` exits 0) or `ENGRAM_QDRANT_TEST_ADDR` names a live Qdrant gRPC endpoint.</precondition>
  <read_first>
    - `internal/store/redevidence_harness_test.go` after Task 1.
    - Each mutation site in the shipped source: `internal/httpdrain/httpdrain.go`,
      `internal/embed/embed.go` (the struct literal in `New`), and `internal/summarize/summarize.go`
      (both the post-options clamp and the bounded error read).
    - Each target test's own assertions in `internal/embed/embed_test.go` and
      `internal/summarize/summarize_test.go`, so every mutation is aimed at an assertion that exists.
    - Every target name re-resolved with `go test -list` before it is relied on.
    - This plan's `<interfaces>` table of earlier-phase patches that touch files this phase edited.
  </read_first>
  <action>
Author the remaining four patches in the same directory by the per-patch procedure, each the smallest
mutation that makes its target test itself fail while the tree still compiles. Use the shapes in this
plan's `<interfaces>` table:

1. `07-01-drain-unbounded-by-bytes.patch` → the embed byte-bound regression (D-01, D-05): the shared
   helper's copy reads the body directly rather than through its byte-limited reader, so an oversized
   body is consumed whole and the connection becomes reusable again — the over-the-bound assertion
   fails while its control still passes, which is exactly what makes the pair meaningful.
2. `07-01-drain-default-swallows-zero.patch` → the embed honored-zero test (D-05, D-06): the drain
   byte default moves out of the struct literal into a post-options fallback, reintroducing the
   sibling convention this phase deliberately diverges from, so an explicit zero is swallowed back
   into the default.
3. `07-03-timeout-zero-means-unbounded.patch` → the summarize ceiling table (D-07, D-09): the
   post-options clamp in `New` is removed, so a non-positive request timeout is unbounded again. The
   ceiling field stays declared and every option still compiles.
4. `07-03-error-body-read-unbounded.patch` → the summarize truncation assertion (D-10): the error read
   drops its byte limit and reads the whole body, so an oversized provider body is surfaced in full
   and the truncation assertion fails while the snippet assertion still passes.

Register all four in the phase entry Task 1 created, each with a trailing `// reverts:` comment naming
what the mutation undoes and its decision ids. Change nothing else in the harness.

If any named direction turns out not to make its target fail — for example because the target's
fixture never reaches the mutated branch — STOP and report which one and why, rather than dropping it
silently or retargeting it to a test that fails for an unrelated reason. A direction that cannot be
proven is a finding, not a rounding error.

Run the harness once, alone, then the full gate (verify). The WHOLE harness matters here, not just
this phase's subtest: this phase edited `internal/server/tools.go` and `internal/config/validate.go`,
and eleven earlier patches mutate those two files. If one of them no longer applies, report it with
the patch name and the `git apply` output rather than rewriting the earlier phase's patch as part of
this task — Phase 5 regenerated one in place only after re-verifying it by hand, and recorded that it
had done so.

Commit as `test(store): register phase 7 red-evidence for the remaining four directions` with
pathspec `internal/store/redevidence_harness_test.go .planning/phases/07-bounded-provider-responses/red-evidence`.
  </action>
  <verify>
    <automated>out=$(ENGRAM_REQUIRE_QDRANT=1 go test ./internal/store/ -run '^TestRedEvidencePatchesAreLive$' -count=1 -v -timeout 180m 2&gt;&amp;1); rc=$?; printf '%s\n' "$out" | rg -e 'confirmed RED|^\s*--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e 'confirmed RED: ' | wc -l)" -eq 63 &amp;&amp; { dirty=$(git status --porcelain) || exit 1; test -z "$dirty"; }</automated>
    <fails_when>non-zero exit, a `--- FAIL:` line, a `confirmed RED:` count other than 63 (the earlier phases' fifty-eight plus this phase's five), or any file left dirty after the run</fails_when>
    <automated>n=$(ls .planning/phases/07-bounded-provider-responses/red-evidence/*.patch | wc -l); test "$n" -eq 5; for p in .planning/phases/07-bounded-provider-responses/red-evidence/*.patch; do h=$(rg -o -e '^diff --[g]it a/' "$p" | wc -l); test "$h" -eq 1 || { echo "$p has $h diff headers"; exit 1; }; done; echo five-single-file-patches</automated>
    <fails_when>a patch count other than five, or any patch touching more than one file — each must be exactly one behavioral mutation</fails_when>
    <automated>out=$(task 2&gt;&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^FAIL|Error:|task:'; test "$rc" -eq 0</automated>
    <fails_when>non-zero exit from the repository gate — lint, format or any package's tests failing</fails_when>
  </verify>
  <acceptance_criteria>
    - Each of the four patches' only `diff --git` line names the file its row in this plan's table says it mutates.
    - Every registered target name was re-resolved with `go test -list` before the mapping was written; the SUMMARY records each resolution and each hand-verified failure line.
    - `rg -l 'SPDX-License-Identifier' .planning/phases/07-bounded-provider-responses/red-evidence/` prints nothing.
    - With `C` the commit SHA of this task: `d=$(git diff "$C^..$C" -- internal/store/redevidence_harness_test.go) || exit 1; printf '%s\n' "$d" | rg -o -e '^-[^-]' | wc -l` prints `0`.
    - The SUMMARY states explicitly whether any of the eleven at-risk earlier patches went stale, and if one did, names it with the `git apply` output rather than describing the outcome in prose.
  </acceptance_criteria>
  <done>All five of this phase's RED directions are registered and harness-confirmed, the earlier fifty-eight still apply over this phase's edits, and the repository gate is green.</done>
</task>

<task type="auto">
  <name>Task 3: Both requirements ticked, the issue closed on the evidence, and the phase shut on a green gate</name>
  <files>.planning/REQUIREMENTS.md</files>
  <precondition>Task 2 is committed, the working tree is clean, `task` exits 0, and `gh auth status` exits 0 — the issue close is the only step in this phase that is not recoverable from the git diff, so it runs against an authenticated CLI and a green gate or not at all.</precondition>
  <read_first>
    - `.planning/REQUIREMENTS.md` in full — both checklist entries for this phase and both
      traceability rows, plus the eighteen entries that must not move.
    - `.planning/phases/07-bounded-provider-responses/07-01-SUMMARY.md` and
      `07-03-SUMMARY.md` — the shipped test names the issue-closing comment must cite verbatim.
    - `.planning/phases/07-bounded-provider-responses/07-CONTEXT.md` D-10 — what the closing comment
      is required to cite.
    - `.planning/ROADMAP.md`'s Phase 7 section — the plan list and success criteria the recorded
      state must agree with.
  </read_first>
  <action>
Run the requirement readiness helper named in this plan's `<interfaces>` block for both ids before
touching anything. If either reports `blocked`, STOP and report which evidence is missing rather than
ticking over it.

Tick both of this phase's requirements in `.planning/REQUIREMENTS.md`: the two checklist boxes and
the two traceability rows, moving each row's status from pending to complete. Touch no other
checkbox or row. Verify afterwards that exactly two boxes changed and that the ticked total went from
eighteen to twenty.

Close GitHub #347 with a comment that cites the evidence rather than asserting the outcome, per D-10.
Name all six tests by their resolved names — the provider-snippet assertion, the drain-for-reuse test
and this phase's new truncation assertion, on each of the two provider lanes — and state in one
sentence what each pins. Say plainly that the bounded read itself shipped earlier and that what was
missing was an assertion that the bound is a TRUNCATION rather than merely a present snippet. Do not
close any other issue; #457 closes through the normal ship flow with the rest of this phase's work.

Re-run the full gate and confirm the tree is clean. Record in the SUMMARY the issue number closed,
the comment's first line, and the six cited test names, so the close is recoverable from the planning
record even though it leaves no git diff — this exact gap is why an issue close was missed once
before and had to be reconciled retroactively.

Commit as `docs(07): mark the bounded-provider-response requirements complete` with pathspec
`.planning/REQUIREMENTS.md`.
  </action>
  <verify>
    <automated>out=$(node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" query requirements.ready-ids .planning/phases/07-bounded-provider-responses/07-05-PLAN.md REQ-provider-drain-bounded REQ-provider-error-body-closed 2&gt;&amp;1); printf '%s\n' "$out"; printf '%s\n' "$out" | rg -q -e 'blocked' &amp;&amp; exit 1; exit 0</automated>
    <fails_when>either requirement reports blocked — an earlier plan's evidence is missing and the tick would be ticking over a gap</fails_when>
    <automated>test "$(rg -o -e '^- \[x\] \*\*REQ-provider-(drain-bounded|error-body-closed)\*\*' .planning/REQUIREMENTS.md | wc -l)" -eq 2 &amp;&amp; test "$(rg -o -e '^- \[ \] \*\*REQ-' .planning/REQUIREMENTS.md | wc -l)" -eq 0 &amp;&amp; test "$(rg -o -e '\| Phase 7 \| Pending \|' .planning/REQUIREMENTS.md | wc -l)" -eq 0</automated>
    <fails_when>either of this phase's requirements is still unticked, any other requirement is left unticked, or a Phase 7 traceability row still reads pending</fails_when>
    <automated>C=$(git rev-parse HEAD) || exit 1; d=$(git diff "$C^..$C" -- .planning/REQUIREMENTS.md) || exit 1; test "$(printf '%s\n' "$d" | rg -o -e '^[-+]- \[' | wc -l)" -eq 4</automated>
    <fails_when>a count other than 4 — exactly two checklist lines changed, each appearing once removed and once added; a larger count means another phase's checkbox moved</fails_when>
    <automated>out=$(gh issue view 347 --json state,closed --jq '.state' 2&gt;&amp;1); printf '%s\n' "$out"; test "$out" = "CLOSED"</automated>
    <fails_when>the issue is not closed — the one step in this phase that leaves no git diff, and the one that was silently skipped once before</fails_when>
    <automated>out=$(task 2&gt;&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^FAIL|Error:|task:'; test "$rc" -eq 0; dirty=$(git status --porcelain) || exit 1; test -z "$dirty"</automated>
    <fails_when>non-zero exit from the repository gate, or a dirty tree after the final commit</fails_when>
    <automated>out=$(go test ./internal/keylinks/ -count=1 -v 2&gt;&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^\s*--- |^ok|^FAIL'; test "$rc" -eq 0</automated>
    <fails_when>non-zero exit — a key-links pattern in one of this phase's plans is unsatisfiable against the shipped code, which is a real gate failure and not a formatting nit</fails_when>
  </verify>
  <acceptance_criteria>
    - The issue-closing comment names all six tests by their `go test -list`-resolved names, not from memory, and states what each pins.
    - No issue other than #347 changed state; `gh issue view 457 --json state --jq '.state'` still prints `OPEN`.
    - `.planning/REQUIREMENTS.md`'s coverage block still reports twenty v1 requirements mapped to phases with zero unmapped.
    - The SUMMARY records the closed issue number, the comment's first line and the six cited test names, so the close is recoverable from the planning record alone.
    - No planning artifact gained a version-bearing heading and no file under `.planning` gained a licence header.
  </acceptance_criteria>
  <done>Both requirements read complete in both places, the issue this phase promised to close is closed on cited evidence, and the whole repository gate is green with a clean tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a future contributor → this phase's guarantees | Every bound this phase adds is one refactor away from being "simplified" out; a guarantee with no live RED proof is a comment, not a control. |
| a registered patch → the harness's verdict | A patch that breaks the build, or whose target passes with it applied, makes the gate report confidence it has not earned. |
| planning record → shipped reality | A requirement ticked ahead of its evidence, or an issue closed without it, records a state the code does not hold. |
| this phase's edits → earlier phases' patches | Eleven of the fifty-eight existing patches mutate files this phase edited; a stale one silently stops proving what it was registered to prove. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-05-01 | Tampering | a bound silently removed by a later refactor | high | mitigate | Five registered patches, one per distinct guarantee, each hand-proven apply → RED → revert before registration. `TestRedEvidencePatchesAreLive` re-proves all sixty-three on every run, so removing any bound turns the suite red rather than quiet. |
| T-07-05-02 | Spoofing | a patch that appears to prove something and does not | high | mitigate | The harness requires the target to exit non-zero AND to be a test failure rather than an abnormal error, so a compile-breaking patch is rejected. Acceptance criteria require the hand-verified failure line to be recorded per patch, and a verify pins each patch to a single `diff --git` header. |
| T-07-05-03 | Tampering | a stale earlier-phase patch silently no longer applying | high | mitigate | The whole harness is re-run, not this phase's subtest, and this plan names all eleven at-risk patches and the two files that put them at risk. A stale one must be reported with its `git apply` output, never rewritten as a side effect of this phase. |
| T-07-05-04 | Repudiation | a requirement ticked ahead of its evidence | high | mitigate | The readiness helper runs first and a blocked id stops the task. A verify pins the exact number of changed checklist lines at four, so a second phase's checkbox cannot move unnoticed. |
| T-07-05-05 | Repudiation | an issue close that leaves no recoverable record | medium | mitigate | The close runs last, behind a green gate and an authenticated CLI, and its number, first line and six cited test names are recorded in the SUMMARY — precisely because an issue close leaves no git diff, the gap that caused one to be missed and reconciled retroactively before. |
| T-07-05-06 | Denial of Service | the harness exceeding its timeout and being "fixed" by raising one | medium | mitigate | Every harness invocation carries an explicit measurement budget, and a prohibition forbids raising a timeout to make the gate pass. A run that needs more than the stated budget is a finding to report. |
| T-07-05-07 | Tampering | a patch authored against a dirty tree | medium | mitigate | Both patch tasks carry a clean-tree precondition the executor asserts before starting, and the harness itself refuses to run when a touched file is already modified. |
| T-07-05-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install and no manifest change in this plan; the only external tool invoked is the already-installed GitHub CLI, used read-only until the single authorized issue close. |
</threat_model>

<verification>
- `ENGRAM_REQUIRE_QDRANT=1 go test ./internal/store/ -run '^TestRedEvidencePatchesAreLive$' -count=1 -v -timeout 180m` exits 0 with sixty-three `confirmed RED:` lines and a clean tree afterwards.
- Exactly five patches exist under this phase's red-evidence directory, each with one `diff --git` header and no licence header.
- `internal/store/redevidence_harness_test.go` has exactly one top-level entry for this phase and its earlier six entries are unchanged.
- Both of this phase's requirements read complete in `.planning/REQUIREMENTS.md`, and no other checkbox moved.
- `gh issue view 347 --json state --jq '.state'` prints `CLOSED`; `gh issue view 457 --json state --jq '.state'` prints `OPEN`.
- `task` exits 0; `go test ./internal/keylinks/ -count=1` exits 0; `git status --porcelain` prints nothing after the final commit.
</verification>

<success_criteria>
- Every guarantee this phase added can be broken on demand, and breaking it turns the suite red — both drain axes, the honored zero, the timeout ceiling and the truncated error body.
- Both provider lanes and both of the phase's requirements are represented in the registered evidence, at a patch count proportionate to what the harness already carries.
- The earlier phases' fifty-eight patches still apply over this phase's edits, confirmed rather than assumed.
- The planning record and the shipped code agree: two requirements complete, one issue closed on cited evidence, one gate green.
</success_criteria>

<output>
Create `.planning/phases/07-bounded-provider-responses/07-05-SUMMARY.md` when done.
</output>