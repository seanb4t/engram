---
phase: 07-bounded-provider-responses
plan: 04
type: execute
wave: 3
depends_on:
  - 07-01
  - 07-02
  - 07-03
files_modified:
  - internal/server/tools.go
  - internal/server/providerbounds_test.go
  - docs-site/src/content/docs/guides/configure.md
  - docs-site/src/content/docs/guides/upgrade.md
autonomous: true
requirements:
  - REQ-provider-drain-bounded

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Each of the six configured bounds reaches the client it belongs to: three options on the embedder's construction and three on the summarizer's, sourced from config and never from a direct environment read"
    - "Each bound is parsed by a helper following the file's existing parse-warn-fall-back-to-default shape, so a value that bypassed startup validation still degrades to a documented default rather than to zero or to no bound"
    - "The two byte bounds are parsed by the SAME exported parser `Config.Validate` used, so the validated range and the enforced range cannot diverge"
    - "A source-level gate proves each of the six options is actually present in the right constructor — a helper that exists but is never passed would otherwise look wired"
    - "All eight provider transport variables are documented in the configuration guide: the six new ones and the two pre-existing per-request timeouts, which were never documented at all"
    - "The upgrade guide carries one new numbered subsection for the changed zero-timeout semantics, plus a row in its own act-on-this table pointing at it (D-07, D-13)"
    - "No documentation file gains a licence header, and no planning artifact gains a version-bearing heading"
    - "`task lint`, `task fmt:check` and `task license:check` exit 0"
  artifacts:
    - path: "internal/server/tools.go"
      provides: "six config-parsing helpers and the option wiring that carries each bound into its client"
      contains: "embed.WithDrainBytes"
    - path: "internal/server/providerbounds_test.go"
      provides: "helper parse/default/warn coverage and the source gate proving every option is passed"
      contains: "TestProviderBoundOptionsWiredIntoBothClients"
    - path: "docs-site/src/content/docs/guides/configure.md"
      provides: "operator documentation for all eight provider transport variables"
      contains: "ENGRAM_EMBED_DRAIN_TIMEOUT"
    - path: "docs-site/src/content/docs/guides/upgrade.md"
      provides: "the numbered breaking-change subsection and its act-on-this table row"
      contains: "ENGRAM_EMBED_MAX_TIMEOUT"
  key_links:
    - from: "internal/server/tools.go"
      to: "internal/embed/embed.go"
      via: "the embedder's option list carries the configured drain bounds and ceiling"
      pattern: "embed[.]WithMaxTimeout[(]embedMaxTimeout[(]cfg[)][)]"
    - from: "internal/server/tools.go"
      to: "internal/summarize/summarize.go"
      via: "the summarizer's option list carries the configured drain bounds and ceiling"
      pattern: "summarize[.]WithMaxTimeout[(]summaryMaxTimeout[(]cfg[)][)]"
    - from: "docs-site/src/content/docs/guides/upgrade.md"
      to: "docs-site/src/content/docs/guides/configure.md"
      via: "the breaking-change subsection points the reader at the ceiling knob's documented row"
      pattern: "ENGRAM_SUMMARY_MAX_TIMEOUT"
  prohibitions:
    - "MUST NOT read any of the six values from the environment directly; every one comes from the loaded config, which the registry owns"
    - "MUST NOT put the timeout ceiling in these wiring helpers instead of in the clients — it lives in the client so every consumer gets it, including tests and any future embedder of those packages (D-09)"
    - "MUST NOT write a second byte parser; the exported non-negative parser `Config.Validate` calls is the one to call, so validated and enforced ranges stay identical"
    - "MUST NOT add an SPDX or licence header to either documentation file — both begin with `---` frontmatter, and a header above it breaks the site build"
    - "MUST NOT renumber, reword or reorder any existing upgrade-guide subsection; the new one is appended with the next number"
    - "MUST NOT document any variable this phase did not add or change beyond the two pre-existing per-request timeouts the breaking-change note has to point at"
---

<objective>
Turn the six declared bounds into live ones: parsed from the loaded configuration, carried into both
provider clients at construction, proven wired by a gate that a mere helper cannot satisfy, and
documented where an operator will actually look.

The upgrade guide's obligation is the sharp edge here. Both per-request timeout variables changed
meaning in plans 07-01 and 07-03 — zero no longer means "forever" — and neither of those variables
was ever documented in the configuration guide at all. The breaking-change note has nothing to point
at unless this plan documents them alongside the six new ones, so all eight rows land together.

Decisions covered: D-04 and D-08 (the six knobs reach their clients), D-09 (the ceiling stays in the
client, not in this wiring), D-13 (both documentation obligations).

Purpose: an operator can set these bounds, read what they do, and be told plainly that a setting
they may be relying on has changed meaning.

Output: six helpers and their wiring, a source gate that cannot be satisfied by a dead helper, eight
documented variables, and one numbered upgrade note.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-bounded-provider-responses/07-CONTEXT.md
@.planning/phases/07-bounded-provider-responses/07-PATTERNS.md
@.planning/phases/07-bounded-provider-responses/07-01-SUMMARY.md
@.planning/phases/07-bounded-provider-responses/07-02-SUMMARY.md
@.planning/phases/07-bounded-provider-responses/07-03-SUMMARY.md
@CLAUDE.md

<interfaces>
**Client options to wire** (shipped by plans 07-01 and 07-03 — read both files for the authoritative
signatures before calling them):

- `embed.WithDrainBytes(int64)`, `embed.WithDrainTimeout(time.Duration)`,
  `embed.WithMaxTimeout(time.Duration)`
- `summarize.WithDrainBytes(int64)`, `summarize.WithDrainTimeout(time.Duration)`,
  `summarize.WithMaxTimeout(time.Duration)`

**Config fields to read** (shipped by plan 07-02): `cfg.Embed.DrainBytes`, `cfg.Embed.DrainTimeout`,
`cfg.Embed.MaxTimeout`, `cfg.Summarize.DrainBytes`, `cfg.Summarize.DrainTimeout`,
`cfg.Summarize.MaxTimeout` — all `string`, all registry-defaulted to `262144`, `2s` and `10m`
respectively.

**Helper template to replicate** (`internal/server/tools.go`) — `embedTimeout` (`:469-479`) and its
twin `summaryTimeout` (`:454-464`):

```go
// embedTimeout parses the per-request embed HTTP client timeout, defaulting to
// 30s on empty/invalid. 0 is honored (disables the timeout); negatives fall
// back to the default. Mirrors summaryTimeout.
func embedTimeout(cfg *config.Config) time.Duration {
	d, err := time.ParseDuration(cfg.Embed.Timeout)
	if err != nil || d < 0 {
		if cfg.Embed.Timeout != "" {
			slog.Warn("ENGRAM_EMBED_TIMEOUT is set but unparseable or negative; using default 30s",
				"value", cfg.Embed.Timeout)
		}
		return 30 * time.Second
	}
	return d
}
```

Note: `embedTimeout`'s own doc comment says "0 is honored (disables the timeout)". The first half
stays true — zero still passes through this helper unchanged — but the parenthetical is now wrong,
since the client resolves a zero to its ceiling. Correct that parenthetical on BOTH existing timeout
helpers while adding the new ones; do not change what either helper returns.

The integer variant to mirror for the byte bounds is `summaryMaxTokens` (`:437-449`), same shape
with `strconv.Atoi`. For these two, call `config.ParseNonNegativeIntCap` instead of a bare
`strconv.Atoi`: it is the exported parser `Config.Validate` itself calls, and using the same one on
both sides is what prevents a value that passes validation from silently falling back to a default
at runtime — the divergence class already recorded in `validatePositiveCap`'s own doc comment.

**Wiring call sites** (`internal/server/tools.go`):

- `embedderFromConfig` (`:483-525`) builds `opts := []embed.Option{…}` and appends a conditional
  one before calling `embed.New(...)`. The three new options join the literal beside
  `embed.WithTimeout(embedTimeout(cfg))`.
- `summarizerFromConfig` (`:527-539`) passes its options directly as arguments to
  `summarize.New(...)`. The three new options join that argument list beside
  `summarize.WithTimeout(summaryTimeout(cfg))`.

**Source-gate analogs in this package** — three existing tests parse this package's own source with
`go/parser` rather than trusting a hand-maintained list: `internal/server/hintcodedocs_test.go`,
`internal/server/conditionalsweep_test.go`, `internal/server/connectapi_write_parity_test.go`. Use
whichever is closest in shape; the requirement is that the gate reads the real call expressions in
`embedderFromConfig` and `summarizerFromConfig`, not a string search over the whole file, so a
mention inside a comment cannot satisfy it.

**Documentation targets:**

`docs-site/src/content/docs/guides/configure.md`

- The `## Embedder` table (`:30-36`) — four-column shape, `Flag` column present, every row uses `—`
  because all of these are env-only. Add four rows: `ENGRAM_EMBED_TIMEOUT` (`30s`),
  `ENGRAM_EMBED_DRAIN_BYTES` (`262144`), `ENGRAM_EMBED_DRAIN_TIMEOUT` (`2s`),
  `ENGRAM_EMBED_MAX_TIMEOUT` (`10m`).
- The `## Auto-summary` table (`:104-107`) — identical column shape. Add four rows:
  `ENGRAM_SUMMARY_TIMEOUT` (`30s`), `ENGRAM_SUMMARY_DRAIN_BYTES` (`262144`),
  `ENGRAM_SUMMARY_DRAIN_TIMEOUT` (`2s`), `ENGRAM_SUMMARY_MAX_TIMEOUT` (`10m`).
- Confirmed by `rg -n "TIMEOUT" docs-site/src/content/docs/guides/configure.md` at phase start:
  NEITHER pre-existing per-request timeout is documented anywhere in this file today. That is why
  eight rows land, not six — the upgrade note needs something to link to.
- Each section carries a `Source:` trailer line naming the code that owns it (`:38` and the
  auto-summary section's equivalent). Update each to name the new helpers.

`docs-site/src/content/docs/guides/upgrade.md`

- `## Unreleased` runs from `:11` to the next `## ` heading. Its highest numbered subsection is
  `### 17.` (`:448`), so the new one is `### 18.`.
- `### 6.` (`:161-172`) is the closest precedent in shape: an existing zero-means-unbounded escape
  hatch is removed. It states the prior behavior, the new behavior, and a bolded **Who should act:**
  paragraph naming exactly who must change something and what to change it to. Follow that shape.
- The act-on-this table at `:19-38` gets one new row in the existing `| If you… | Read |` shape,
  pointing at the new section.
- Both documentation files begin with `---` frontmatter. Neither may gain a licence header —
  `.licenserc.yaml` already excludes `docs-site/**`, and a header above the frontmatter breaks the
  site build.

**What the upgrade note must say** (D-07, stated once so it is not re-derived): setting either
per-request timeout variable to `0` previously meant no request deadline at all. It now resolves to
a ceiling — `ENGRAM_EMBED_MAX_TIMEOUT` / `ENGRAM_SUMMARY_MAX_TIMEOUT`, both defaulting to `10m` —
and there is deliberately no value that means unbounded. An explicit positive duration is still
honored uncapped, however large. Anyone who relied on `0` should set an explicit duration instead,
raising the ceiling if they need one longer than ten minutes.
</interfaces>

<test_names_this_plan_creates>
Re-resolve with `go test ./internal/server/ -list '.*'` before relying on either name — a `-run`
pattern matching nothing exits 0 and prints `ok`, a permanent false green (durable record
`bsbsvn4hbc`).

- `TestProviderBoundHelpersParseAndDefault`
- `TestProviderBoundOptionsWiredIntoBothClients`
</test_names_this_plan_creates>
</context>

## Executor notes (every task)

- Conventional Commits, explicit pathspec on every commit, and the session's attribution trailer.
- `task lint` (which includes `rumdl` over markdown) and `task fmt` must be clean before each commit.
- NEVER run any `git stash` subcommand; use `git show HEAD:<path>` into the scratchpad instead.
- `internal/server` is large but fast. Do NOT gate any task on `internal/store`, which this plan
  never touches and which needs `-timeout 180m` on this machine.
- If `task` shows a failure this plan did not cause, STOP and report it rather than editing
  unrelated code.

<tasks>

<task type="auto">
  <name>Task 1: Six config-parsing helpers, and both clients built with their configured bounds</name>
  <files>internal/server/tools.go</files>
  <read_first>
    - `internal/server/tools.go:430-540` — `summaryMaxChars`, `summaryMaxTokens`, `summaryTimeout`,
      `embedTimeout`, `embedderFromConfig` and `summarizerFromConfig` in full.
    - `internal/server/tools.go:135-150` and `:400-415` — the two existing comments explaining why a
      runtime parser must be the same one `Validate` used, and what happened the one time they
      diverged.
    - `internal/embed/embed.go` and `internal/summarize/summarize.go` as shipped by plans 07-01 and
      07-03 — the six option signatures and their argument types.
    - `internal/config/validate.go:295-316` — `ParseNonNegativeIntCap`, the parser both byte helpers
      must call.
  </read_first>
  <action>
Add six helpers beside the existing four, following their shape exactly: parse, warn on a set but
unparseable or negative value naming the variable, fall back to the documented default, and
otherwise return the parsed value. Name them for the values they carry on each lane, matching the
existing `embedTimeout`/`summaryTimeout` naming so the pairs read together.

The two byte helpers return `int64` and parse via `config.ParseNonNegativeIntCap` rather than a bare
integer parse, so the enforced range is the same one validation checked. The four duration helpers
parse durations. Defaults are the registry's own: 256 KiB, two seconds, ten minutes.

One asymmetry to get right and to comment: for the drain bounds, zero is a legitimate value that
must pass through untouched, so only a negative or unparseable value falls back. For the ceilings, a
non-positive value is not legitimate at all — validation rejects it — so a non-positive value falls
back to the default rather than being passed on, which keeps this defensive path from handing the
client a value the client would then have to ignore.

Correct the now-wrong parenthetical on both existing timeout helpers' doc comments: zero is still
passed through unchanged by the helper, but it no longer disables anything downstream — the client
resolves it to its ceiling. Do not change what either helper returns; the behavior change belongs to
the clients and to nothing else.

Wire all six into the two constructors, beside each lane's existing timeout option. Do not add a
conditional; every one of these is registry-defaulted, so each always has a value.

Note for the record in a short comment at the summarizer's construction: the ceiling is applied
inside the client, not here, so a caller that builds a client without going through this wiring —
tests, the summarize-missing command's own builder, any future embedder of the package — still gets
it (D-09).

Commit as `feat(server): wire configured provider drain bounds and timeout ceilings into both clients`
with pathspec `internal/server/tools.go`.
  </action>
  <verify>
    <automated>out=$(go build ./... 2&gt;&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '.' ; test "$rc" -eq 0</automated>
    <fails_when>non-zero exit — an option name or argument type does not match what plans 07-01 and 07-03 shipped</fails_when>
    <automated>test "$(rg -o -e 'embed[.]With(DrainBytes|DrainTimeout|MaxTimeout)[(]' internal/server/tools.go | wc -l)" -eq 3 &amp;&amp; test "$(rg -o -e 'summarize[.]With(DrainBytes|DrainTimeout|MaxTimeout)[(]' internal/server/tools.go | wc -l)" -eq 3</automated>
    <fails_when>either lane carries a number of new options other than three</fails_when>
    <automated>test "$(rg -o -e 'config[.]ParseNonNegativeIntCap[(]' internal/server/tools.go | wc -l)" -eq 2</automated>
    <fails_when>a count other than 2 — both byte helpers must call the same exported parser validation uses, and neither may hand-roll a second one</fails_when>
    <automated>out=$(go test ./internal/server/ -count=1 2&gt;&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^ok|^FAIL|^\s*--- FAIL'; test "$rc" -eq 0</automated>
    <fails_when>non-zero exit or any FAIL line — the existing wiring tests must still pass with the new options in place</fails_when>
  </verify>
  <acceptance_criteria>
    - Each of the six helpers warns through `slog.Warn` naming its own environment variable, and only when the configured value is non-empty — matching the two existing timeout helpers exactly.
    - The drain-bound helpers pass a configured zero through; the ceiling helpers do not.
    - Both existing timeout helpers' doc comments no longer claim a zero disables the timeout, and neither helper's return value changed.
    - `rg -n -e 'os.Getenv|EnvOr' internal/server/tools.go` shows no new call introduced by this task — every value comes from the loaded config.
    - The SUMMARY names all six helpers and states which default each falls back to.
  </acceptance_criteria>
  <done>Six configured bounds are parsed once, by the same parsers validation used, and carried into the two clients at construction.</done>
</task>

<task type="auto">
  <name>Task 2: A gate a dead helper cannot pass</name>
  <files>internal/server/providerbounds_test.go</files>
  <read_first>
    - `internal/server/tools.go` after Task 1 — the six helpers and both constructors.
    - `internal/server/hintcodedocs_test.go` and `internal/server/conditionalsweep_test.go` — the
      two existing `go/parser` source gates in this package, for the parse-and-walk shape and the
      failure-message style.
    - `internal/server/embed_wiring_test.go:100-240` — how this package builds a `config.Config`
      literal for a wiring test, so the helper table matches local convention.
  </read_first>
  <action>
Create `internal/server/providerbounds_test.go` with two tests.

`TestProviderBoundHelpersParseAndDefault` is a table over all six helpers. For each: the empty
string yields the documented default with no warning, a well-formed value is returned verbatim, a
malformed value yields the default, a negative value yields the default, and zero is returned
verbatim by the four drain helpers but replaced by the default by the two ceiling helpers. Assert on
the returned value for each case; the table's rows are the contract.

`TestProviderBoundOptionsWiredIntoBothClients` parses this package's own `tools.go` with `go/parser`
and walks the bodies of `embedderFromConfig` and `summarizerFromConfig`, collecting the option calls
each actually passes to its client constructor. Assert each function passes its lane's three new
options, and that each option's argument is a call to that lane's corresponding helper rather than a
literal or a different helper — a crossed pair (the summarize ceiling handed the embed helper) is
exactly the mistake a string search would not catch.

Say plainly in the test's doc comment why it parses source rather than asserting on a constructed
client: the client fields these options set are unexported and live in another package, so from here
a helper that exists but is never passed is indistinguishable from one that is wired, and a test
built on the helpers alone would pass while the bounds never reached the client at all.

Commit as `test(server): gate that every provider bound option is actually passed to its client`
with pathspec `internal/server/providerbounds_test.go`.
  </action>
  <verify>
    <automated>out=$(go test ./internal/server/ -run '^TestProviderBound(HelpersParseAndDefault|OptionsWiredIntoBothClients)$' -count=1 -v 2&gt;&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^\s*--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e '^--- PASS: TestProviderBound' | wc -l)" -eq 2</automated>
    <fails_when>non-zero exit, any `--- FAIL:` line, or a top-level PASS count other than 2</fails_when>
    <automated>out=$(go test ./internal/server/ -run '^TestProviderBoundHelpersParseAndDefault$' -count=1 -v 2&gt;&amp;1); rc=$?; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e '^\s+--- PASS: TestProviderBoundHelpersParseAndDefault/' | wc -l)" -ge 24</automated>
    <fails_when>fewer than 24 sub-test PASS lines — six helpers times at least four cases each; a smaller table is not covering the empty, well-formed, malformed and zero cases per helper</fails_when>
    <automated>test "$(rg -o -e '"go/parser"' internal/server/providerbounds_test.go | wc -l)" -eq 1 &amp;&amp; test "$(rg -o -e 'embedderFromConfig|summarizerFromConfig' internal/server/providerbounds_test.go | wc -l)" -ge 2</automated>
    <fails_when>the gate does not parse source, or does not name both constructors it is supposed to walk</fails_when>
    <automated>out=$(task 2&gt;&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^FAIL|Error:|task:'; test "$rc" -eq 0</automated>
    <fails_when>non-zero exit from the repository gate</fails_when>
  </verify>
  <acceptance_criteria>
    - The source gate fails if an option is present in the file but outside the two named constructors — verify this by hand once, moving one option call into a comment or a dead helper and observing the failure, and record the observed failure line in the SUMMARY.
    - The gate checks the ARGUMENT of each option call, not only the option name, so a crossed helper pairing fails.
    - `grep -c 'SPDX-License-Identifier' internal/server/providerbounds_test.go` prints `1`.
    - The helper table asserts a concrete returned value per case, never a range or a nil check.
  </acceptance_criteria>
  <done>A helper that is never passed, or passed with the wrong lane's value, fails a gate that reads the real call expressions rather than the file's text.</done>
</task>

<task type="auto">
  <name>Task 3: Eight documented variables and one numbered upgrade note</name>
  <files>docs-site/src/content/docs/guides/configure.md, docs-site/src/content/docs/guides/upgrade.md</files>
  <read_first>
    - `docs-site/src/content/docs/guides/configure.md:26-40` — the Embedder section, its table and
      its `Source:` trailer.
    - `docs-site/src/content/docs/guides/configure.md:100-140` — the Auto-summary section, its
      table, its `Source:` trailer, and the Async-on-write subsection below it, so the new rows land
      in the right table and not the worker-pool one.
    - `docs-site/src/content/docs/guides/upgrade.md:11-40` — the `## Unreleased` heading and the
      act-on-this table's row shape.
    - `docs-site/src/content/docs/guides/upgrade.md:161-172` — subsection `### 6.`, the closest
      precedent for a removed zero-means-unbounded escape hatch.
    - `docs-site/src/content/docs/guides/upgrade.md:448-470` — subsection `### 17.`, which
      establishes the number to increment from and the current formatting conventions.
    - `.planning/phases/07-bounded-provider-responses/07-CONTEXT.md` D-07 and D-13.
  </read_first>
  <action>
Add four rows to the Embedder table and four to the Auto-summary table, per this plan's
`<interfaces>` block. Each description says what the bound actually does in one sentence an operator
can act on: what is bounded, what happens when the bound is reached, and what zero means where zero
is meaningful. For the drain bounds, say plainly that the drain exists only so the connection can be
reused, that abandoning it costs one new connection and nothing else, and that zero skips it
entirely — for an operator who would rather burn a handshake than ever stall. For the ceilings, say
they bound the per-request timeout and that there is no value meaning unbounded.

Update each section's `Source:` trailer to name the new helpers alongside the existing ones.

Append `### 18.` to `## Unreleased` in the upgrade guide, following subsection `### 6.`'s shape:
state the prior behavior, state the new behavior including both ceiling variables and their `10m`
default, note that an explicit positive duration is still honored uncapped however large, and close
with a bolded **Who should act:** paragraph naming anyone who set either per-request timeout to zero
expecting no deadline and telling them exactly what to do instead. Link to the configuration guide's
newly documented rows so the reader can find the knob.

Add one row to the act-on-this table near the top, in its existing shape, pointing at the new
section.

Neither file gets a licence header. Do not renumber or reword any existing subsection, and do not
reorder the act-on-this table's existing rows.

Commit as `docs: document the provider drain bounds and the changed zero-timeout semantics` with
pathspec `docs-site/src/content/docs/guides/configure.md docs-site/src/content/docs/guides/upgrade.md`.
  </action>
  <verify>
    <automated>for v in ENGRAM_EMBED_TIMEOUT ENGRAM_EMBED_DRAIN_BYTES ENGRAM_EMBED_DRAIN_TIMEOUT ENGRAM_EMBED_MAX_TIMEOUT ENGRAM_SUMMARY_TIMEOUT ENGRAM_SUMMARY_DRAIN_BYTES ENGRAM_SUMMARY_DRAIN_TIMEOUT ENGRAM_SUMMARY_MAX_TIMEOUT; do n=$(rg -o -F -e "$v" docs-site/src/content/docs/guides/configure.md | wc -l); test "$n" -ge 1 || { echo "undocumented: $v"; exit 1; }; done; echo all-eight-documented</automated>
    <fails_when>any of the eight provider transport variables is still absent from the configuration guide</fails_when>
    <automated>test "$(rg -o -e '^### 18[.] ' docs-site/src/content/docs/guides/upgrade.md | wc -l)" -eq 1 &amp;&amp; test "$(rg -o -e '^### 19[.] ' docs-site/src/content/docs/guides/upgrade.md | wc -l)" -eq 0 &amp;&amp; test "$(rg -o -e '§18' docs-site/src/content/docs/guides/upgrade.md | wc -l)" -ge 1</automated>
    <fails_when>the new subsection is missing or numbered other than 18, a nineteenth subsection appeared, or the act-on-this table carries no pointer to the new section</fails_when>
    <automated>test "$(rg -o -e 'SPDX-License-Identifier' docs-site/src/content/docs/guides/configure.md docs-site/src/content/docs/guides/upgrade.md | wc -l)" -eq 0 &amp;&amp; head -1 docs-site/src/content/docs/guides/upgrade.md | rg -q -e '^---$'</automated>
    <fails_when>either documentation file gained a licence header, or the upgrade guide's first line is no longer its frontmatter delimiter — both break the site build</fails_when>
    <automated>out=$(task lint 2&gt;&amp;1); rc=$?; printf '%s\n' "$out" | rg -e 'rumdl|MD[0-9]{3}|Error:|task:'; test "$rc" -eq 0</automated>
    <fails_when>non-zero exit — the markdown linter rejects the new table rows or the new subsection</fails_when>
  </verify>
  <acceptance_criteria>
    - With `C` the commit SHA of this task (record it in the SUMMARY): `d=$(git diff "$C^..$C" -- docs-site/src/content/docs/guides/upgrade.md) || exit 1; printf '%s\n' "$d" | rg -o -e '^-[^-]' | wc -l` prints a number small enough to account for only the act-on-this table's closing rows being re-emitted; no existing numbered subsection's text is removed or renumbered. The diff's exit status is captured into `d` before the counting pipeline runs, so a failed invocation cannot read as a clean result.
    - Each of the eight new configuration rows states a default matching the registry's own default for that key, verified against `internal/config/registry.go` rather than from memory.
    - The new upgrade subsection contains a bolded **Who should act:** paragraph and names both ceiling variables.
    - Both `Source:` trailers name at least one of the helpers Task 1 added.
    - The SUMMARY records the eight rows added, the subsection number used, and confirms `rg -n "TIMEOUT" docs-site/src/content/docs/guides/configure.md` now matches where it previously matched nothing.
  </acceptance_criteria>
  <done>An operator can find every provider transport bound in the configuration guide, and anyone who relied on a zero timeout meaning "forever" is told so in the upgrade guide, in the shape that guide already uses for exactly this kind of change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| configuration → client construction | A bound that is parsed but never passed to the client is indistinguishable, from outside, from one that is enforced. |
| validated range → enforced range | Two parsers for one value is how a configuration that passed startup validation silently becomes a different number at runtime. |
| published documentation → operator behavior | An operator acts on what the guide says. A changed meaning that is not written down is a silent behavioral break in a deployed system. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-04-01 | Tampering | a bound parsed but never passed to its client | high | mitigate | `TestProviderBoundOptionsWiredIntoBothClients` parses `tools.go` with `go/parser` and walks the two constructors' real call expressions, asserting each of the six options is present AND carries its own lane's helper as its argument. A dead helper or a crossed pair fails; a text search would not catch either. |
| T-07-04-02 | Tampering | validated range diverging from enforced range on the byte bounds | high | mitigate | Both byte helpers call `config.ParseNonNegativeIntCap`, the same exported parser `Config.Validate` calls — the fix pattern already recorded in `validatePositiveCap`'s doc comment for exactly this failure. A verify pins the call-site count at two. |
| T-07-04-03 | Denial of Service | a ceiling helper passing a non-positive value through to the client | high | mitigate | The two ceiling helpers fall back to the ten-minute default on any non-positive value, so this defensive path cannot hand the client a value meaning unbounded even when `Validate` was bypassed. The helper table asserts the zero and negative cases explicitly. |
| T-07-04-04 | Repudiation | a changed timeout meaning shipped without notice | high | mitigate | D-07 is a documented-behavior change on a published escape hatch. The upgrade guide gains a numbered subsection in the shape that guide already uses for a removed zero-means-unbounded flag, plus an act-on-this table row, and the two previously undocumented variables are documented so the note has something to point at. |
| T-07-04-05 | Information Disclosure | a warning log echoing an operator value | low | accept | The warn shape copied from the two existing timeout helpers attaches the offending value. These are transport tuning numbers, not credentials; diverging from the established shape for these six alone would be inconsistent with no security gain. |
| T-07-04-06 | Tampering | a licence header above documentation frontmatter | medium | mitigate | `.licenserc.yaml` already excludes `docs-site/**`; a verify additionally asserts neither file carries a header and that the upgrade guide's first line is still its frontmatter delimiter. This is a recorded repository trap, not a hypothetical. |
| T-07-04-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install and no manifest change in this plan; the documentation site's own toolchain is untouched. |
</threat_model>

<verification>
- `go test ./internal/server/ -count=1 -v` shows both new tests PASS and no FAIL.
- `go test ./internal/server/ -list '.*'` lists `TestProviderBoundHelpersParseAndDefault` and `TestProviderBoundOptionsWiredIntoBothClients`.
- All eight provider transport variables appear in `docs-site/src/content/docs/guides/configure.md`.
- `docs-site/src/content/docs/guides/upgrade.md` carries exactly one `### 18.` subsection under `## Unreleased`, and its act-on-this table points at it.
- `task` exits 0; `task license:check` exits 0; `git status --porcelain` prints nothing after the final commit.
</verification>

<success_criteria>
- Every one of the six bounds is settable by an operator, parsed once, and demonstrably passed to the client it belongs to.
- The parser that validates a byte bound at startup is the same one that enforces it at runtime.
- An operator reading the configuration guide finds all eight provider transport variables, including two that were never documented before.
- Anyone relying on a zero per-request timeout meaning "no deadline" is told, in the guide they would consult on upgrade, what changed and what to do instead.
</success_criteria>

<output>
Create `.planning/phases/07-bounded-provider-responses/07-04-SUMMARY.md` when done.
</output>