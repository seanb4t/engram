## Deferred Items

- Pre-existing `TestActiveMilestoneKeyLinksSatisfiable` failure in `internal/keylinks` against `.planning/phases/04-list-listscheduled-search-bounded-reads/04-06-PLAN.md:62`'s `key_links` entry (`from: internal/server/tools.go`, `to: internal/store/store.go`, `pattern: "Full: req[.]Full"`).
  status: acknowledged
  **What:** The pattern requires exactly one space between `Full:` and `req.Full`, but `deps.listMemory`'s actual `store.ListOptions{...}` struct literal is gofmt-aligned (`Full:              req.Full,`, many spaces). Confirmed via `git show 557152c2:internal/server/tools.go` (04-06's own final commit, before any 04-07 change) that the pattern was already unsatisfiable then — this predates 04-07 entirely and is not caused by any file this plan touches (`internal/server/tools.go` and `internal/store/store.go` are outside 04-07's `files_modified`, and neither file appears in 04-07's diff). Likely surfaced now rather than at 04-06's own `task` run because Go's test cache returned a stale cached PASS for `internal/keylinks` at that time (the package's own source was unchanged, and the gate reads `.planning/*.md` via `os.ReadFile` at test time, which the build cache's `ActionID` does not track). Out of scope for this plan per the executor's scope boundary rule and the plan's own cross-plan note (04-06's surface, including its `PLAN.md`, is not 04-07's to edit).
