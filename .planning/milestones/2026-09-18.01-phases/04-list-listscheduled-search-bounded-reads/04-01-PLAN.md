---
phase: 04-list-listscheduled-search-bounded-reads
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/server/argerror.go
  - internal/server/connecterror.go
  - internal/server/responsetoolarge.go
  - internal/server/responsetoolarge_test.go
  - cmd/engram/exitcode_baseline_test.go
  - internal/store/redevidence_harness_test.go
  - docs-site/src/content/docs/reference/errors.md
  - docs-site/src/content/docs/guides/cli.md
  - docs-site/src/content/docs/guides/upgrade.md
  - .planning/phases/02-error-classification-resourceexhausted-mapping/red-evidence/02-03-errors-doc-drops-response-too-large.patch
autonomous: true
requirements:
  - REQ-list-limit-contract-decided

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Both server lanes render the SAME renamed overflow envelope for `store.ErrResponseTooLarge` — Connect `resource_exhausted` and the MCP tool result — with the Connect code and CLI exit 10 unchanged (D-11)"
    - "`internal/server/argerror.go` declares twelve `HintCode` constants and `docs-site/src/content/docs/reference/errors.md`'s published hint table is their exact transcription: heading count word, every count-word phrase on the page (frontmatter included), and every docs-site anchor link to that heading all agree (D-10, D-11)"
    - "The new numeric-maximum hint is classified `classMalformed` (Connect `invalid_argument`, CLI exit 2), NOT `classOutOfRange`, even though that class exists and reads closer — D-10 locks the class and this is not to be 'corrected'"
    - "`TestRedEvidencePatchesAreLive` stays green across the rename: Phase 2's errors-doc patch is renamed to match the new hint value and regenerated against the renamed page, and the Phase 1/2/3 mapping is otherwise byte-unchanged (D-11)"
    - "`internal/store/storetest/storetest_test.go`'s `port_too_large` case is about a TCP port number, not this hint — it is deliberately NOT renamed, and the retired-value sweep is written so it does not match it"
  artifacts:
    - path: "internal/server/argerror.go"
      provides: "the twelve-entry HintCode catalog: the renamed overflow hint (D-11) plus the new numeric-maximum hint (D-10)"
      contains: "HintResponseTooLarge"
    - path: "internal/server/responsetoolarge.go"
      provides: "the one shared overflow envelope, rendering the renamed hint on both lanes"
      contains: "HintResponseTooLarge"
    - path: "docs-site/src/content/docs/reference/errors.md"
      provides: "the twelve-code published hint table, the new hint's row, and the renamed overflow row"
      contains: "out_of_range"
    - path: ".planning/phases/02-error-classification-resourceexhausted-mapping/red-evidence/02-03-errors-doc-drops-response-too-large.patch"
      provides: "Phase 2's errors-doc RED direction, regenerated against the renamed hint table"
      contains: "errors.md"
  key_links:
    - from: "internal/server/argerror.go"
      to: "docs-site/src/content/docs/reference/errors.md"
      via: "hintcodedocs_test.go transcribes the HintCode constant set into the published table and pins the heading count word"
      pattern: "HintResponseTooLarge"
    - from: "internal/server/responsetoolarge.go"
      to: "cmd/engram/exitcode_baseline_test.go"
      via: "the CLI baseline reproduces the rendered envelope literally, so the rename must move both together"
      pattern: "response_too_large"
    - from: "internal/store/redevidence_harness_test.go"
      to: ".planning/phases/02-error-classification-resourceexhausted-mapping/red-evidence/02-03-errors-doc-drops-response-too-large.patch"
      via: "the harness applies the regenerated Phase 2 patch and requires its target test to fail"
      pattern: "02-03-errors-doc-drops-response-too-large[.]patch"
  prohibitions:
    - "MUST NOT put a byte ceiling, an observed size, upstream gRPC/Qdrant text, or the caller's rejected VALUE into any wire message — a rejection names the field and the documented bound only; the overflow envelope keeps carrying no digit at all"
---

<objective>
Settle the hint-code half of decision B's rejection contract before any read path moves: rename
Phase 2's overflow hint so it can never be read as the same failure as an over-long input field
(D-11), and add the new numeric-maximum hint the rest of this phase rejects with (D-10). Both are
published wire contracts, so they land first — every later plan in this phase then cites a hint that
already exists, is already documented, and is already proven by the mechanical docs gate.

Decisions covered: D-10 (the `out_of_range` hint code, its detail wording, and its deliberate
`classMalformed` classification), D-11 (the `too_large` → `response_too_large` rename across source,
tests, docs, and Phase 2's red-evidence patch).

Purpose: REQ-list-limit-contract-decided — the chosen contract's rejection vocabulary exists and is
documented before it is wired.

Output: a twelve-entry `HintCode` catalog, a twelve-code published hint table, and a renamed,
regenerated Phase 2 red-evidence patch.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-list-listscheduled-search-bounded-reads/04-CONTEXT.md
@.planning/phases/04-list-listscheduled-search-bounded-reads/04-RESEARCH.md
@.planning/phases/04-list-listscheduled-search-bounded-reads/04-PATTERNS.md
@.planning/phases/02-error-classification-resourceexhausted-mapping/02-03-SUMMARY.md
@.planning/phases/02-error-classification-resourceexhausted-mapping/02-04-SUMMARY.md
@CLAUDE.md

<interfaces>
`internal/server/argerror.go`: `type HintCode string`; a single `const (...)` block declaring the
catalog, today eleven entries ending at `HintTooLarge`. `type argClass int` with three values —
`classMalformed` (→ `connect.CodeInvalidArgument`), `classOutOfRange` (→ `connect.CodeOutOfRange`),
`classPrecondition` (→ `connect.CodeFailedPrecondition`).
`func argErrf(class argClass, hint HintCode, field, format string, a ...any) error`.
`func renderHintEnvelope(fields []string, hint HintCode, detail string) string`.

`internal/server/responsetoolarge.go`: `const responseTooLargeDetail = "..."` and
`func responseTooLargeEnvelope() string` — the ONE place the hint value reaches a wire string; both
`connectError` (connecterror.go) and `mapResponseTooLarge` (the MCP middleware, same file) call it.

`internal/server/hintcodedocs_test.go`: `TestErrorsDocHintCodesMatchArgErrorConstants` parses
`argerror.go`'s constants with `go/parser`, requires SET EQUALITY against the backticked first cells
of the table under the `## The <word> hint codes` heading, requires that heading's count word to be
`numberWords[len(constants)]`, requires EVERY count-word phrase anywhere on the page (frontmatter
included) matching `\b([a-z]+)-code\b` or `\b([a-z]+) hint codes\b` to read the current word,
requires `normalizeWS(responseTooLargeEnvelope())` to appear verbatim in the page, and walks every
`.md`/`.mdx` under `docs-site/src/content/docs` requiring each
`/reference/errors/#the-<word>-hint-codes` link to carry the current word. `numberWords` already maps
`12` to its word.

`internal/store/redevidence_harness_test.go`: `var redEvidenceDirs = map[string]map[string]string{…}`
— per phase directory, `"<patch file>": "<TargetTest>"` with a trailing `// reverts: …` comment.
`TestRedEvidencePatchesAreLive` globs `<dir>/*.patch`, requires set equality with the mapping, and per
patch runs `git apply --check`, `git apply`, `go test -run '^Target$' -count=1 ./...` (must exit
non-zero), `git apply -R`, then asserts the touched files are clean, logging
`confirmed RED: <patch> applied -> <Target> failed as expected`.

Per-patch authoring procedure (Phase 1/2/3 precedent): make the ONE mutation on a clean tree, write
`git diff -- <file> > <dir>/<name>.patch`, then prove it — `git apply --check <patch> &&
git apply <patch> && go test -run '^Target$' -count=1 ./<pkg>/ -v` shows the target's OWN
`--- FAIL: Target` line (a compile error is not acceptable) — then `git apply -R <patch>` and
`git status --porcelain -- <file>` prints nothing. Exactly one `diff --git` header per patch; no SPDX
header on a `.planning/**` file.
</interfaces>
</context>

## Artifacts this phase produces (plan 04-01's share)

Nothing below exists on HEAD today.

| Kind | Artifact | Created by |
|------|----------|------------|
| Renamed const | `HintResponseTooLarge` (replacing `HintTooLarge`) in `internal/server/argerror.go` | Task 1 |
| Renamed wire value | the overflow hint value carried by `responseTooLargeEnvelope()` | Task 1 |
| Renamed patch | `.planning/phases/02-…/red-evidence/02-03-errors-doc-drops-response-too-large.patch` (replaces the old file name, regenerated content) | Task 1 |
| New const | `HintOutOfRange` in `internal/server/argerror.go` | Task 2 |
| New doc rows | the new hint's row in errors.md's hint table and in its Connect-code/exit table | Task 2 |
| New doc prose | the paragraph distinguishing the numeric-maximum hint from `too_long`/`too_many` | Task 2 |

## Executor notes (every task)

- NEVER run any `git stash` subcommand — to inspect a pristine file use `git show HEAD:<path>` into
  the scratchpad instead.
- Commit with an explicit pathspec (rule `n6m4as49mr`), Conventional Commits, ending the message with
  the session's attribution trailer lines. Commit signing is off.
- `.planning/**` files (including every `.patch`) carry NO SPDX header.
- Red-evidence status: this phase registers its OWN patches only in plan 04-08. Until then,
  `TestRedEvidencePatchesAreLive` is the only failure `task` may legitimately report, and only for a
  Phase 1/2/3 patch that THIS plan's own source change made stale — that staleness must be repaired
  inside the same task (Task 1 does exactly this for Phase 2's errors-doc patch). Any other red is
  this plan's defect.
- Run the harness alone, never concurrently with another harness or patch cycle.
- Rule `xvqj44e5mk`: the rename rides the existing single-envelope mechanism; never hand-build a
  second envelope string.

<tasks>

<task type="tracer">
  <name>Task 1: The overflow hint renamed end to end — one constant, both lanes, the CLI baseline, the published page, and Phase 2's patch regenerated</name>
  <files>internal/server/argerror.go, internal/server/connecterror.go, internal/server/responsetoolarge.go, internal/server/responsetoolarge_test.go, cmd/engram/exitcode_baseline_test.go, internal/store/redevidence_harness_test.go, docs-site/src/content/docs/reference/errors.md, docs-site/src/content/docs/guides/cli.md, docs-site/src/content/docs/guides/upgrade.md, .planning/phases/02-error-classification-resourceexhausted-mapping/red-evidence/02-03-errors-doc-drops-response-too-large.patch</files>
  <precondition>The working tree is clean (`git status --porcelain` prints nothing) and `.planning/phases/02-error-classification-resourceexhausted-mapping/red-evidence/02-03-errors-doc-drops-too-large.patch` exists at HEAD.</precondition>
  <read_first>
    - `internal/server/argerror.go` (whole file — the `HintCode` catalog, its leading doc comment, `argClass`, `argErrf`, `renderHintEnvelope`).
    - `internal/server/responsetoolarge.go` (whole file) and `internal/server/connecterror.go` (the `store.ErrResponseTooLarge` arm and the doc comment above `connectError`).
    - `internal/server/hintcodedocs_test.go` (whole file — it is the gate every doc edit below must satisfy).
    - `internal/server/responsetoolarge_test.go` and `cmd/engram/exitcode_baseline_test.go`'s `substituteTooLargeServerURL` helper (both reproduce the rendered envelope literally).
    - `docs-site/src/content/docs/reference/errors.md` (whole file), `guides/cli.md`'s exit-code table, `guides/upgrade.md`'s §14 entry and its error-envelope link.
    - `internal/store/redevidence_harness_test.go` (whole file) and the existing Phase 2 errors-doc patch.
    - `.planning/phases/02-error-classification-resourceexhausted-mapping/02-03-SUMMARY.md` (how that patch was authored and proven).
  </read_first>
  <action>
Rename the overflow hint per D-11 — one Go identifier and one wire value, moved together.

1. `internal/server/argerror.go`: rename the catalog's final constant to `HintResponseTooLarge` and
   change its value to the self-describing form `response` + underscore + `too` + underscore +
   `large`. Update the const block's leading doc comment where it names the old identifier, keeping
   its "adding a code here requires the matching row in errors.md" sentence intact.
2. `internal/server/responsetoolarge.go`: update `responseTooLargeEnvelope()`'s `renderHintEnvelope`
   argument to the renamed constant, and update `responseTooLargeDetail`'s doc comment where it
   names the old identifier. `responseTooLargeDetail`'s VALUE does not change.
3. `internal/server/connecterror.go`: update the doc comment that spells the envelope's hint value.
   The `errors.Is(err, store.ErrResponseTooLarge)` arm itself is untouched.
4. `internal/server/responsetoolarge_test.go` and `cmd/engram/exitcode_baseline_test.go`: update the
   two envelope-prefix assertions and `substituteTooLargeServerURL`'s reproduced envelope string to
   the renamed value. Change nothing else about those tests.
5. `docs-site/src/content/docs/reference/errors.md`: rename the overflow row's code cell in the hint
   table, the same code in the Connect-code/exit table, the fenced envelope example (which
   `hintcodedocs_test.go` compares verbatim against the rendered envelope), the "opposite directions"
   disambiguation paragraph, and the bolded no-number sentence near the end of the page. The heading
   still reads eleven codes after this task — Task 2 is what makes it twelve.
6. `docs-site/src/content/docs/guides/cli.md`: update the exit-10 row's hint name.
   `docs-site/src/content/docs/guides/upgrade.md`: update §14's hint name. Both keep their existing
   anchor links unchanged (only the hint VALUE moves in this task).
7. Do NOT touch `internal/store/storetest/storetest_test.go` — its `port_too_large` case names a TCP
   port bound, not this hint, and the acceptance sweep below is written so it does not match.
8. Regenerate Phase 2's errors-doc red evidence, because the page it patches just changed:
   `git mv` the patch to `02-03-errors-doc-drops-response-too-large.patch`, re-author it by the
   per-patch procedure (delete the overflow row from errors.md's hint table, `git diff -- <errors.md>
   > <patch>`, prove `TestErrorsDocHintCodesMatchArgErrorConstants` reports its own `--- FAIL:`, then
   `git apply -R`), and update the Phase 2 entry in `redEvidenceDirs` to the new file name, keeping
   its target test and rewriting only the `// reverts:` comment's stale code name. Every other entry
   in that map stays byte-identical.

Commit as `refactor(server): rename the overflow hint to response_too_large` with an explicit
pathspec covering exactly the files above.
  </action>
  <verify>
    <automated>out=$(go test ./internal/server/ -run '^(TestErrorsDocHintCodesMatchArgErrorConstants|TestResponseTooLargeEnvelopeShape|TestConnectListMemoriesResponseTooLarge|TestMCPListMemoryResponseTooLarge)$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e '^--- PASS: ' | wc -l)" -eq 4</automated>
    <fails_when>non-zero exit, any `--- FAIL:` line, or a `--- PASS:` count other than 4 (a skipped real-Qdrant overflow test is not a pass here — it reduces the count)</fails_when>
    <automated>out=$(go test ./cmd/engram/ -run '^TestExitCodeBaseline$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^\s*--- |^ok|^FAIL'; test "$rc" -eq 0</automated>
    <fails_when>non-zero exit, or a `--- FAIL: TestExitCodeBaseline/` subtest line (the exit-10 row still expects the retired envelope text)</fails_when>
    <automated>out=$(ENGRAM_REQUIRE_QDRANT=1 go test ./internal/store/ -run '^TestRedEvidencePatchesAreLive$' -count=1 -v -timeout 60m 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e 'confirmed RED|^\s*--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e 'confirmed RED: ' | wc -l)" -eq 25 &amp;&amp; test -z "$(git status --porcelain -- docs-site/src/content/docs/reference/errors.md)"</automated>
    <fails_when>non-zero exit, a `confirmed RED:` count other than 25, a `git apply --check` staleness failure naming the Phase 2 errors-doc patch, or errors.md left dirty after the run</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -o -e 'hint=too[_]large' -e '"too[_]large"' -e 'HintTooLarge' internal cmd docs-site/src proto CLAUDE.md skill | wc -l` prints `0`.
    - `rg -c -e 'port[_]too[_]large' internal/store/storetest/storetest_test.go` prints `1` and `git diff --exit-code HEAD -- internal/store/storetest/storetest_test.go` exits 0 (the unrelated TCP-port case was not renamed).
    - `rg -o -e '^diff --[g]it a/' .planning/phases/02-error-classification-resourceexhausted-mapping/red-evidence/02-03-errors-doc-drops-response-too-large.patch | wc -l` prints `1`, and that patch's only `diff --git` line names `docs-site/src/content/docs/reference/errors.md`.
    - `ls .planning/phases/02-error-classification-resourceexhausted-mapping/red-evidence/*.patch | wc -l` prints `8`, and `rg -l 'SPDX-License-Identifier' .planning/phases/02-error-classification-resourceexhausted-mapping/red-evidence/` prints nothing.
    - With `C` the commit SHA of this task (record it in the SUMMARY): `d=$(git diff "$C^..$C" -- internal/store/redevidence_harness_test.go) || exit 1; printf '%s\n' "$d" | rg -o -e '^[-+]\s+"0[123]-' | wc -l` prints `2` (exactly one removed and one added mapping key — no other entry moved).
    - `rg -c 'HintResponseTooLarge' internal/server/argerror.go internal/server/responsetoolarge.go` prints a non-zero count for both files.
    - The SUMMARY records the hand-verified `--- FAIL: TestErrorsDocHintCodesMatchArgErrorConstants` line observed while proving the regenerated patch.
  </acceptance_criteria>
  <done>The overflow hint reads `response_too_large` on every surface and in every doc, the Connect code and CLI exit 10 are unchanged, `hintcodedocs_test.go` is green against an eleven-code page, and the red-evidence harness confirms 25 REDs with a clean tree.</done>
</task>

<task type="auto">
  <name>Task 2: The numeric-maximum hint added to the catalog and published as the twelfth code</name>
  <files>internal/server/argerror.go, docs-site/src/content/docs/reference/errors.md, docs-site/src/content/docs/guides/upgrade.md</files>
  <precondition>Task 1 is committed and the working tree is clean.</precondition>
  <reversibility rating="one-way">Publishing a rejection hint on the recall surfaces is a wire contract callers will branch on; the user chose reject-over-clamp explicitly in discuss-phase (2026-09-19, 04-CONTEXT D-10), so this checkpoint is PRE-APPROVED — record the rating and continue without stopping.</reversibility>
  <read_first>
    - `internal/server/argerror.go` after Task 1 (the catalog and the three `argClass` values with their Connect-code mapping comments).
    - `internal/server/hintcodedocs_test.go` (the count-word phrase patterns and the anchor walk).
    - `docs-site/src/content/docs/reference/errors.md` after Task 1 — specifically the hint table, the per-code disambiguation paragraphs beneath it, the operator-tier section that also cites the table's size, the Connect-code/exit table, and the frontmatter description line.
    - `docs-site/src/content/docs/guides/upgrade.md`'s link to the hint-code heading.
    - `cmd/engram/client_common.go`'s `exitCodeForConnectErr` (why `classMalformed` and `classOutOfRange` are behaviourally identical at the CLI).
  </read_first>
  <action>
Add the numeric-maximum hint per D-10 and make the published page a twelve-code transcription.

1. `internal/server/argerror.go`: add `HintOutOfRange` to the catalog with the value
   `out` + underscore + `of` + underscore + `range`. Give it a short comment stating it is raised when
   a numeric field exceeds its documented maximum, and that it is classified `classMalformed` (Connect
   `invalid_argument`, CLI exit 2) by decision, NOT `classOutOfRange` — both classes already group
   under the CLI's usage exit, and the class is locked by 04-CONTEXT D-10. Do not add a fourth
   `argClass` and do not re-point any existing call site at `classOutOfRange`.
2. `docs-site/src/content/docs/reference/errors.md`: add the new code's row to the hint table
   (meaning: a numeric field exceeds its documented maximum; what to do: resend at or below the
   maximum named in the detail text — the value is rejected, never clamped). Add its row to the
   Connect-code/CLI-exit table as `invalid_argument` / exit `2`. Add one short paragraph beneath the
   table distinguishing it from `too_long` (a length/byte bound on a string or blob) and `too_many`
   (a collection count bound): this one names a NUMERIC argument above its documented ceiling.
3. Change every count word on the page from the eleven-word to the twelve-word — the frontmatter
   description line, the heading itself, the operator-tier section's two references to the table's
   size, and the supersede-section reference. `hintcodedocs_test.go` scans the WHOLE page for both
   count-word phrase shapes, so grep the page and fix each occurrence rather than relying on the
   heading check alone.
4. Update `docs-site/src/content/docs/guides/upgrade.md`'s link to the hint-code heading so its
   anchor slug carries the new count word (the heading rename moves the slug). The docs gate walks
   every `.md`/`.mdx` under `docs-site/src/content/docs`, so re-run its sweep rather than assuming
   this is the only link.

No handler wires this hint yet — plan 04-05 adds the store backstop and plan 04-06 adds the
per-surface rejections. An unreferenced exported-style constant is intentional here and lints clean.

Commit as `feat(server): add the out_of_range hint for a numeric field above its maximum` with an
explicit pathspec covering exactly the files above.
  </action>
  <verify>
    <automated>out=$(go test ./internal/server/ -run '^(TestErrorsDocHintCodesMatchArgErrorConstants|TestParseHintCodeTable)$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; printf '%s\n' "$out" | rg -q -e '^--- PASS: TestErrorsDocHintCodesMatchArgErrorConstants'</automated>
    <fails_when>non-zero exit, or a `--- FAIL:` line naming a missing table row, a stale count word anywhere on the page, or a stale docs-site anchor slug</fails_when>
    <automated>out=$(go test ./internal/server/... ./cmd/engram/... -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^--- FAIL|^FAIL|^ok'; test "$rc" -eq 0</automated>
    <fails_when>non-zero exit or any `--- FAIL:`/`FAIL` line (the added constant broke an enumeration test that counts hints)</fails_when>
    <automated>task lint</automated>
    <fails_when>non-zero exit from golangci-lint, rumdl, yamlfmt, or actionlint</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -o -e '\| `out[_]of[_]range` \|' docs-site/src/content/docs/reference/errors.md | wc -l` prints `2` (the hint table row and the Connect-code/exit row).
    - `rg -o -e '\beleven\b' docs-site/src/content/docs/reference/errors.md | wc -l` prints `0`, and `rg -o -e '\btwelve\b' docs-site/src/content/docs/reference/errors.md | wc -l` prints `5`.
    - `rg -o -e 'the-eleven-hint-codes' docs-site/src | wc -l` prints `0`.
    - `rg -o -e 'HintCode = "' internal/server/argerror.go | wc -l` prints `12`.
    - `rg -n -e 'classOutOfRange' internal/server/argerror.go | wc -l` prints a non-zero count (the class still exists and was not deleted), and `rg -c -e 'HintOutOfRange' internal/server/argerror.go` prints `1` — the new hint is declared exactly once and no call site uses it yet.
    - With `C2` the commit SHA of this task (record it in the SUMMARY): `git diff --exit-code "$C2^..$C2" -- internal/server/responsetoolarge.go internal/server/connecterror.go internal/server/responsetoolarge_test.go` exits 0 (Task 1's files are untouched by Task 2).
  </acceptance_criteria>
  <done>`argerror.go` declares twelve hint codes, errors.md is their exact twelve-code transcription with correct count words and anchors everywhere, the new hint's class and exit code are documented, and `task lint` plus the server and CLI suites are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → client wire | Every rejection and overflow envelope crosses here; it must carry the field and the bound, never a value, a byte ceiling, or upstream transport text. |
| source → published docs | `hintcodedocs_test.go` is the only mechanical link; anything it does not check can silently drift. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01-01 | Information Disclosure | `responseTooLargeEnvelope()` | high | mitigate | The rename touches the hint value only; `responseTooLargeDetail`'s text is unchanged and `TestResponseTooLargeEnvelopeShape`'s no-digit and banned-substring assertions stay in force. |
| T-04-01-02 | Information Disclosure | the new numeric-maximum hint's detail text | high | mitigate | The detail names the field and the maximum via `argErrf`'s format arguments and never interpolates the caller's rejected value (argerror.go's D-12 discipline); plan 04-06's attribution matrix asserts it. |
| T-04-01-03 | Repudiation | Phase 2's errors-doc red-evidence patch | medium | mitigate | Regenerated and hand-proven RED against the renamed page in the same task, then re-proven by the harness over all 25 patches. |
| T-04-01-04 | Tampering | a patch left applied after a failed harness run | medium | mitigate | The harness reverts via `defer` + `t.Cleanup` and asserts a clean `git status --porcelain`; the verify re-checks errors.md is clean. |
| T-04-01-SC | Tampering | npm/pip/cargo installs | low | accept | Not applicable: this plan installs no package and changes no manifest. |
</threat_model>

<verification>
- `go test ./internal/server/... ./cmd/engram/... -count=1` exits 0.
- `ENGRAM_REQUIRE_QDRANT=1 go test ./internal/store/ -run '^TestRedEvidencePatchesAreLive$' -count=1 -v -timeout 60m` shows 25 `confirmed RED:` lines and exits 0.
- `task lint` and `task license:check` exit 0; `git diff --exit-code HEAD -- go.mod go.sum` exits 0.
- `rg -o -e 'hint=too[_]large' -e '"too[_]large"' -e 'HintTooLarge' internal cmd docs-site/src proto CLAUDE.md skill | wc -l` prints `0`.
</verification>

<success_criteria>
- The overflow hint and the numeric-maximum hint are two self-describing, separately documented codes that cannot be read as the same failure.
- The published hint table is a mechanically-proven twelve-code transcription of `argerror.go`, with every count word and every anchor in agreement.
- Phase 2's red evidence still proves its gate RED against the renamed page.
</success_criteria>

<output>
Create `.planning/phases/04-list-listscheduled-search-bounded-reads/04-01-SUMMARY.md` when done.
</output>
