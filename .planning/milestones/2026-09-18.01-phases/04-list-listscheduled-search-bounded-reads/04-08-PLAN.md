---
phase: 04-list-listscheduled-search-bounded-reads
plan: 08
type: execute
wave: 7
depends_on:
  - 04-01
  - 04-02
  - 04-03
  - 04-04
  - 04-05
  - 04-06
  - 04-07
files_modified:
  - internal/store/redevidence_harness_test.go
  - .planning/REQUIREMENTS.md
  - .planning/phases/04-list-listscheduled-search-bounded-reads/red-evidence/04-01-hint-code-value-drift.patch
  - .planning/phases/04-list-listscheduled-search-bounded-reads/red-evidence/04-01-overflow-envelope-hint-reverted.patch
  - .planning/phases/04-list-listscheduled-search-bounded-reads/red-evidence/04-02-cursor-reports-last-page-on-budget-cut.patch
  - .planning/phases/04-list-listscheduled-search-bounded-reads/red-evidence/04-02-zero-limit-fetches-whole-scope.patch
  - .planning/phases/04-list-listscheduled-search-bounded-reads/red-evidence/04-02-offset-overflow-guard-removed.patch
  - .planning/phases/04-list-listscheduled-search-bounded-reads/red-evidence/04-03-prefix-walk-uses-full-view.patch
  - .planning/phases/04-list-listscheduled-search-bounded-reads/red-evidence/04-03-assembly-loop-stops-after-one-page.patch
  - .planning/phases/04-list-listscheduled-search-bounded-reads/red-evidence/04-04-search-fetch-drops-caller-filter.patch
  - .planning/phases/04-list-listscheduled-search-bounded-reads/red-evidence/04-04-search-fetch-always-issues-an-rpc.patch
  - .planning/phases/04-list-listscheduled-search-bounded-reads/red-evidence/04-04-search-returns-reversed-rank-order.patch
  - .planning/phases/04-list-listscheduled-search-bounded-reads/red-evidence/04-05-no-summary-backfill-removed.patch
  - .planning/phases/04-list-listscheduled-search-bounded-reads/red-evidence/04-05-list-always-full-view.patch
  - .planning/phases/04-list-listscheduled-search-bounded-reads/red-evidence/04-05-store-clamps-instead-of-refusing.patch
  - .planning/phases/04-list-listscheduled-search-bounded-reads/red-evidence/04-06-surface-maximum-check-removed.patch
  - .planning/phases/04-list-listscheduled-search-bounded-reads/red-evidence/04-06-maximum-check-runs-after-embed.patch
  - .planning/phases/04-list-listscheduled-search-bounded-reads/red-evidence/04-06-full-not-threaded-to-rule-listing.patch
  - .planning/phases/04-list-listscheduled-search-bounded-reads/red-evidence/04-07-cli-help-drops-the-maximum.patch
autonomous: true
requirements:
  - REQ-list-bounded
  - REQ-list-scheduled-bounded
  - REQ-search-k-bounded
  - REQ-list-contract-unchanged
  - REQ-list-limit-contract-decided

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`redEvidenceDirs` gains exactly one new entry, keyed by this phase's red-evidence directory, mapping seventeen patches to their target tests; the Phase 1, Phase 2 and Phase 3 entries are byte-unchanged"
    - "Each patch is ONE behavioral mutation that still compiles and makes its named target test itself report a failure — never a build break — hand-verified by apply, run, revert before it is registered"
    - "The registered directions cover every guarantee this phase adds: the hint catalog's agreement with the published table and the overflow envelope's renamed hint (D-10, D-11); a budget-cut cursor page reported as the last page and a zero limit fetching the whole scope and the offset arithmetic's wrap guard (D-01, D-05, D-06); the prefix walk's keys-only projection and the assembly loop following its resume position (D-07, D-05); the search fetch's caller-filter wrap, its empty-batch short-circuit and its rank-order preservation (D-09); the no-summary backfill, the per-caller projection and the store's refusal-not-clamp (Phase 3 D-04, D-10); the surface rejection, its position ahead of the embedder, and the rule listing's projection threading (D-02, D-03, D-10); and the CLI help stating the maximum (D-01, D-04)"
    - "`TestRedEvidencePatchesAreLive` passes with forty-two confirmed RED lines (Phase 1's four, Phase 2's eight, Phase 3's thirteen, this phase's seventeen) and leaves every mutated file clean"
    - "This phase's five requirement checkboxes are ticked in the requirements document and their traceability rows read complete; NO other phase's checkbox, verification document or context document is touched"
    - "`task` exits 0 for the whole repository and `go test ./internal/keylinks/ -count=1` exits 0 with every executed plan's key links satisfiable"
    - "No red-evidence patch carries a licence header, and no planning artifact gains a version-bearing heading"
  artifacts:
    - path: "internal/store/redevidence_harness_test.go"
      provides: "the redEvidenceDirs entry for this phase's seventeen patches"
      contains: "04-list-listscheduled-search-bounded-reads/red-evidence"
    - path: ".planning/phases/04-list-listscheduled-search-bounded-reads/red-evidence/04-04-search-fetch-drops-caller-filter.patch"
      provides: "RED evidence that the two-phase search fetch re-applies the caller's authz filter rather than trusting the id batch"
      contains: "includeIDs"
    - path: ".planning/phases/04-list-listscheduled-search-bounded-reads/red-evidence/04-05-store-clamps-instead-of-refusing.patch"
      provides: "RED evidence that an over-maximum count is refused rather than silently clamped"
      contains: "MaxRecallLimit"
    - path: ".planning/REQUIREMENTS.md"
      provides: "this phase's five requirements marked complete in both the checklist and the traceability table"
      contains: "REQ-list-limit-contract-decided"
  key_links:
    - from: "internal/store/redevidence_harness_test.go"
      to: ".planning/phases/04-list-listscheduled-search-bounded-reads/red-evidence/04-04-search-fetch-drops-caller-filter.patch"
      via: "the harness applies every registered patch and requires its target test to fail"
      pattern: "04-04-search-fetch-drops-caller-filter[.]patch"
    - from: "internal/store/redevidence_harness_test.go"
      to: ".planning/phases/04-list-listscheduled-search-bounded-reads/red-evidence/04-07-cli-help-drops-the-maximum.patch"
      via: "the last registered direction of the phase, proving the correct-by-reading gate has teeth"
      pattern: "TestRecallMaximumIsStatedNumerically"
  prohibitions:
    - "MUST NOT register a patch that has not been hand-proven to make its own target test fail — a patch that breaks the build, or whose target passes with it applied, is evidence of nothing and makes the gate a false comfort"
    - "MUST NOT edit another phase's verification document, context document or requirement checkbox to make this phase's gate green — cross-phase staleness is the orchestrator's to re-fingerprint, not this plan's to paper over"
---

<objective>
Make every guarantee this phase added a live, harness-checked RED proof, then close the phase on a
green full gate. This is the phase's LAST plan: it registers the phase's entry in the red-evidence
map after every other plan has shipped, exactly as plan 03-06 did for the previous phase, and marks
the phase's five requirements complete.

Decisions covered: every decision this phase implemented gets at least one patch that reverts it —
D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-09, D-10 and D-11, plus Phase 3 D-04's projection read
forward. D-08 and D-12 are exercised through the same target tests.

Purpose: all five of this phase's requirements proven RED-able for the rest of the open milestone.

Output: seventeen patch files, one harness entry, and five ticked requirement boxes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/03-06-SUMMARY.md
@.planning/phases/04-list-listscheduled-search-bounded-reads/04-01-SUMMARY.md
@.planning/phases/04-list-listscheduled-search-bounded-reads/04-02-SUMMARY.md
@.planning/phases/04-list-listscheduled-search-bounded-reads/04-03-SUMMARY.md
@.planning/phases/04-list-listscheduled-search-bounded-reads/04-04-SUMMARY.md
@.planning/phases/04-list-listscheduled-search-bounded-reads/04-05-SUMMARY.md
@.planning/phases/04-list-listscheduled-search-bounded-reads/04-06-SUMMARY.md
@.planning/phases/04-list-listscheduled-search-bounded-reads/04-07-SUMMARY.md
@CLAUDE.md

<interfaces>
`internal/store/redevidence_harness_test.go`: `var redEvidenceDirs = map[string]map[string]string{…}`
— one entry per phase directory (repo-root-relative), each mapping `"<patch file>": "<TargetTest>"`
with a trailing `// reverts: …` comment. `TestRedEvidencePatchesAreLive` (skipped under `-short`)
globs `<dir>/*.patch`, requires set equality with the mapping, and per patch: refuses to run if a
touched file is already dirty, `git apply --check`, `git apply`,
`go test -run '^Target$' -count=1 ./...` (must exit non-zero), `git apply -R`, then asserts the
touched files are clean; it logs `confirmed RED: <patch> applied -> <Target> failed as expected`. It
must never run concurrently with itself (its doc comment records a 2026-08-16 incident).

Per-patch authoring procedure (Phase 1/2/3 precedent, and plan 03-06's own SUMMARY): make the ONE
mutation on a clean tree, write `git diff -- <file> > <dir>/<name>.patch`, then prove it —
`git apply --check <patch> && git apply <patch> && go test -run '^Target$' -count=1 ./<pkg>/ -v`
shows the target's OWN failure line (a compile error is NOT acceptable) — then `git apply -R <patch>`
and `git status --porcelain -- <file>` prints nothing. Each patch has exactly one `diff --git`
header and no licence header. Never use a git stash; to inspect a pristine file use
`git show HEAD:<path>` into the scratchpad.

Runtime contingency (pre-authorized, applies only if it fires): the harness's nested
`go test -run '^Target$' -count=1 ./...` recompiles and starts every package per patch. Plan 03-06
measured the whole `internal/store` package run at 111 seconds with twenty-five patches registered,
well under `go test`'s ten-minute default, and its narrowing contingency did NOT fire — no
`packageOfTest` helper exists today. If, and ONLY if, the default-timeout
`ENGRAM_REQUIRE_QDRANT=1 go test ./internal/store/ -count=1` (no `-timeout` flag) now panics with a
test-timeout inside `TestRedEvidencePatchesAreLive` at forty-two patches, add a helper that walks the
module (skipping the version-control, dependency, docs, console and planning directories) for the
`_test.go` file declaring the target function, return that package path, pass it instead of the
whole-module pattern, fail the subtest when zero or more than one package matches, and update the
harness doc comment's nested-run paragraph. NEVER raise a timeout in the task runner or in CI to make
the gate pass; record before/after durations in the SUMMARY if it fires.

Target tests created by plans 04-01..04-07 (re-resolve each with `go test -list` before relying on a
name — durable record `bsbsvn4hbc`):
`internal/server`: `TestErrorsDocHintCodesMatchArgErrorConstants`, `TestResponseTooLargeEnvelopeShape`,
`TestOutOfRangeRejectedOnEveryRecallSurface`, `TestOutOfRangeRejectedBeforeAnyBackend`,
`TestListRulesFullThreaded`, `TestRecallMaximumIsStatedNumerically`.
`internal/store`: `TestStoreListContractInvariant`, `TestStoreListOffsetBounded`,
`TestStoreListDeepOffsetBounded`, `TestListScheduledBounded`, `TestSearchTwoPhaseBounded`,
`TestSearchFetchSkipsEmptyBatch`, `TestSearchPreservesRankOrder`, `TestRecallViewSelection`,
`TestNoSummaryContentBackfill`, `TestStoreRejectsOverMaximumCount`.

Mutation sites created by plans 04-01..04-07: `internal/server/argerror.go` — the two hint constants'
values. `internal/server/responsetoolarge.go` — the envelope's hint argument. `internal/server/tools.go`
— the shared over-maximum check helper and its four call sites, and the typed list core's projection
field. `internal/server/rules.go` — the direct store call's projection field.
`internal/store/store.go` — the zero-limit resolution, the offset wrap guard, `recallView`, the
over-maximum guard helper, the assembly loop's resume-following branch, the prefix walk's view
argument, and the cursor page's next-cursor derivation. `internal/store/searchfetch.go` — the id-set
filter's caller-filter wrap, the empty-batch early return, and the no-summary backfill.
`cmd/engram/client_list.go` — the list limit flag's usage string.

`.planning/REQUIREMENTS.md`: the `### Bounded Qdrant Reads` and `### Decisions (resolved in
discuss-phase)` checklists, and the `## Traceability` table whose rows read
`| Requirement | Phase | Status |`.
</interfaces>
</context>

## Artifacts this phase produces (plan 04-08's share)

Nothing below exists on HEAD today.

| Kind | Artifact | Created by |
|------|----------|------------|
| Directory | `.planning/phases/04-list-listscheduled-search-bounded-reads/red-evidence/` | Task 1 |
| Patch | `04-04-search-fetch-drops-caller-filter.patch` | Task 1 |
| Patches | the remaining sixteen, listed in Task 2 | Task 2 |
| Harness entry | the phase's `redEvidenceDirs` entry | Tasks 1–2 |
| Checkboxes | this phase's five requirement boxes and their traceability rows | Task 3 |
| Conditional | the harness's package-narrowing helper, only if the runtime contingency fires | Task 2 |

## Executor notes (every task)

- NEVER run any git stash subcommand — to inspect a pristine file use `git show HEAD:<path>` into the
  scratchpad instead.
- Commit with an explicit pathspec (rule `n6m4as49mr`), Conventional Commits, ending the message with
  the session's attribution trailer lines.
- Every patch is authored and proven on a CLEAN tree, one at a time; never run two harness or patch
  cycles concurrently.
- Patches under `.planning/**` carry NO licence header, and no planning artifact gains a
  version-bearing heading.
- Real-Qdrant targets run with `ENGRAM_REQUIRE_QDRANT=1`; a skipped target is not a RED.
- If `task` shows a failure this plan did not cause, STOP and report it rather than editing unrelated
  code — and never edit another phase's verification or context document to make it green.

<tasks>

<task type="tracer">
  <name>Task 1: One RED proof end to end — the search fetch's caller-filter wrap, registered and confirmed by the harness</name>
  <files>internal/store/redevidence_harness_test.go, .planning/phases/04-list-listscheduled-search-bounded-reads/red-evidence/04-04-search-fetch-drops-caller-filter.patch</files>
  <precondition>Plans 04-01 through 04-07 are executed (their SUMMARY files exist), the working tree is clean (`git status --porcelain` prints nothing), and Docker is reachable (`docker info` exits 0) or `ENGRAM_QDRANT_TEST_ADDR` names a live Qdrant gRPC endpoint.</precondition>
  <read_first>
    - `internal/store/redevidence_harness_test.go` (whole file — the map, the harness, its tree-safety and empty-map guards, and its concurrency warning).
    - `.planning/phases/03-shared-bounded-read-mechanism-content-cap-decision/03-06-SUMMARY.md` and one of that phase's patches (the shape precedent).
    - `internal/store/searchfetch.go` (the id-set filter builder) and the cross-owner subtest inside `internal/store/searchtwophase_oversized_test.go`.
    - Each of this phase's SUMMARY files, to confirm the shipped identifier names before writing any mutation.
  </read_first>
  <action>
Create `.planning/phases/04-list-listscheduled-search-bounded-reads/red-evidence/` and author
`04-04-search-fetch-drops-caller-filter.patch` by the per-patch procedure: in
`internal/store/searchfetch.go`, make the id-set filter builder return a filter carrying ONLY the
id-set condition — drop the branch that wraps the caller's filter as a nested condition. The helper
still compiles and is still called. Target `TestSearchTwoPhaseBounded`, whose cross-owner subtest then
receives another owner's record. Prove it RED by hand and record the observed failure line.

Register it: add ONE new top-level entry to `redEvidenceDirs`, keyed by this phase's red-evidence
directory path, mapping the patch name to its target test with a trailing `// reverts:` comment
naming what the mutation undoes and its decision id. Leave the Phase 1, Phase 2 and Phase 3 entries
and everything else untouched.

Run the harness once, alone (verify). Commit as
`test(store): register phase 4 red-evidence for the search fetch's authz filter` with pathspec
`internal/store/redevidence_harness_test.go .planning/phases/04-list-listscheduled-search-bounded-reads/red-evidence`.
  </action>
  <verify>
    <automated>out=$(ENGRAM_REQUIRE_QDRANT=1 go test ./internal/store/ -run '^TestRedEvidencePatchesAreLive$' -count=1 -v -timeout 60m 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e 'confirmed RED|^\s*--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e 'confirmed RED: ' | wc -l)" -eq 26 &amp;&amp; { dirty=$(git status --porcelain -- internal/store/searchfetch.go) || exit 1; test -z "$dirty"; }</automated>
    <fails_when>non-zero exit, a `--- FAIL:` line, a `confirmed RED:` count other than 26 (the earlier phases' twenty-five plus this one), or `internal/store/searchfetch.go` left dirty after the run</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -o -e '^diff --[g]it a/' .planning/phases/04-list-listscheduled-search-bounded-reads/red-evidence/04-04-search-fetch-drops-caller-filter.patch | wc -l` prints `1`, and its only `diff --git` line names `internal/store/searchfetch.go`.
    - `rg -n -F '".planning/phases/04-list-listscheduled-search-bounded-reads/red-evidence"' internal/store/redevidence_harness_test.go` prints exactly one line.
    - With `C` the commit SHA of this task (record it in the SUMMARY): `d=$(git diff "$C^..$C" -- internal/store/redevidence_harness_test.go) || exit 1; printf '%s\n' "$d" | rg -c -e '^-[^-]'` prints `0` (the task only adds lines).
    - `rg -l 'SPDX-License-Identifier' .planning/phases/04-list-listscheduled-search-bounded-reads/red-evidence/` prints nothing.
    - The SUMMARY records the hand-verified failure line for `TestSearchTwoPhaseBounded`.
  </acceptance_criteria>
  <done>The phase's highest-value RED direction is a registered, harness-confirmed patch; the harness passes with twenty-six confirmed REDs and leaves the tree clean; committed with an explicit pathspec.</done>
</task>

<task type="auto">
  <name>Task 2: The remaining sixteen RED directions registered and the harness green at forty-two</name>
  <files>internal/store/redevidence_harness_test.go, .planning/phases/04-list-listscheduled-search-bounded-reads/red-evidence/04-01-hint-code-value-drift.patch, .planning/phases/04-list-listscheduled-search-bounded-reads/red-evidence/04-01-overflow-envelope-hint-reverted.patch, .planning/phases/04-list-listscheduled-search-bounded-reads/red-evidence/04-02-cursor-reports-last-page-on-budget-cut.patch, .planning/phases/04-list-listscheduled-search-bounded-reads/red-evidence/04-02-zero-limit-fetches-whole-scope.patch, .planning/phases/04-list-listscheduled-search-bounded-reads/red-evidence/04-02-offset-overflow-guard-removed.patch, .planning/phases/04-list-listscheduled-search-bounded-reads/red-evidence/04-03-prefix-walk-uses-full-view.patch, .planning/phases/04-list-listscheduled-search-bounded-reads/red-evidence/04-03-assembly-loop-stops-after-one-page.patch, .planning/phases/04-list-listscheduled-search-bounded-reads/red-evidence/04-04-search-fetch-always-issues-an-rpc.patch, .planning/phases/04-list-listscheduled-search-bounded-reads/red-evidence/04-04-search-returns-reversed-rank-order.patch, .planning/phases/04-list-listscheduled-search-bounded-reads/red-evidence/04-05-no-summary-backfill-removed.patch, .planning/phases/04-list-listscheduled-search-bounded-reads/red-evidence/04-05-list-always-full-view.patch, .planning/phases/04-list-listscheduled-search-bounded-reads/red-evidence/04-05-store-clamps-instead-of-refusing.patch, .planning/phases/04-list-listscheduled-search-bounded-reads/red-evidence/04-06-surface-maximum-check-removed.patch, .planning/phases/04-list-listscheduled-search-bounded-reads/red-evidence/04-06-maximum-check-runs-after-embed.patch, .planning/phases/04-list-listscheduled-search-bounded-reads/red-evidence/04-06-full-not-threaded-to-rule-listing.patch, .planning/phases/04-list-listscheduled-search-bounded-reads/red-evidence/04-07-cli-help-drops-the-maximum.patch</files>
  <precondition>Task 1 is committed, the working tree is clean, and Docker is reachable (`docker info` exits 0) or `ENGRAM_QDRANT_TEST_ADDR` names a live Qdrant gRPC endpoint.</precondition>
  <read_first>
    - `internal/store/redevidence_harness_test.go` after Task 1.
    - Each mutation site named in this plan's interfaces block, read in the shipped source: `internal/server/argerror.go`, `internal/server/responsetoolarge.go`, `internal/server/tools.go`, `internal/server/rules.go`, `internal/store/store.go`, `internal/store/searchfetch.go`, `cmd/engram/client_list.go`.
    - The target test files, so each mutation is aimed at an assertion that actually exists: `internal/store/listbounded_oversized_test.go`, `listcontract_oversized_test.go`, `listscheduled_oversized_test.go`, `searchtwophase_oversized_test.go`, `recallview_oversized_test.go`, `recallmax_oversized_test.go`; `internal/server/outofrange_test.go`, `recallfullthreading_test.go`, `recallmaxdocs_test.go`, `responsetoolarge_test.go`, `hintcodedocs_test.go`.
  </read_first>
  <action>
Author sixteen patches in the same directory by the per-patch procedure, each the smallest mutation
that makes its target test itself fail while the tree still compiles:

1. `04-01-hint-code-value-drift.patch` → `TestErrorsDocHintCodesMatchArgErrorConstants` (D-10): change
   the new numeric-maximum hint constant's VALUE so it no longer matches the published table's row.
   Do not delete the constant — that would break the build.
2. `04-01-overflow-envelope-hint-reverted.patch` → `TestResponseTooLargeEnvelopeShape` (D-11): make
   the shared overflow envelope render a DIFFERENT existing hint constant.
3. `04-02-cursor-reports-last-page-on-budget-cut.patch` → `TestStoreListContractInvariant` (D-06):
   in the cursor-mode adapter, return an empty next cursor whenever the page reports a budget cut.
4. `04-02-zero-limit-fetches-whole-scope.patch` → `TestStoreListOffsetBounded` (D-01): restore
   offset mode's zero-limit resolution to the exact matched total instead of the recall maximum.
5. `04-02-offset-overflow-guard-removed.patch` → `TestStoreListOffsetBounded` (D-01 precision):
   delete the guard that refuses an offset-plus-limit pair that would wrap.
6. `04-03-prefix-walk-uses-full-view.patch` → `TestStoreListDeepOffsetBounded` (D-07): make the
   prefix walk request the full view instead of the keys-only one.
7. `04-03-assembly-loop-stops-after-one-page.patch` → `TestListScheduledBounded` (D-05): make the
   page-assembly loop return after its first primitive page regardless of the requested count.
8. `04-04-search-fetch-always-issues-an-rpc.patch` → `TestSearchFetchSkipsEmptyBatch` (D-09,
   RESEARCH Pitfall 1): delete the empty-batch early return so a zero-id fetch still issues a Scroll.
9. `04-04-search-returns-reversed-rank-order.patch` → `TestSearchPreservesRankOrder` (D-09): reverse
   the order in which phase one's ids are walked when the result is rebuilt.
10. `04-05-no-summary-backfill-removed.patch` → `TestNoSummaryContentBackfill` (Phase 3 D-04): delete
    the backfill call from the list paths; the helper stays referenced by search so the tree compiles.
11. `04-05-list-always-full-view.patch` → `TestRecallViewSelection` (Phase 3 D-04): make the single
    projection selector always return the full view.
12. `04-05-store-clamps-instead-of-refusing.patch` → `TestStoreRejectsOverMaximumCount` (D-10): make
    the store's over-maximum guard lower the count to the maximum and return nil instead of refusing.
13. `04-06-surface-maximum-check-removed.patch` → `TestOutOfRangeRejectedOnEveryRecallSurface` (D-10):
    delete the shared over-maximum check's call from the typed list core, so the list surfaces fall
    through to the store's differently-shaped backstop error.
14. `04-06-maximum-check-runs-after-embed.patch` → `TestOutOfRangeRejectedBeforeAnyBackend` (D-10,
    the denial-of-service ordering): move the check in the memory-search core method to AFTER the
    embed call.
15. `04-06-full-not-threaded-to-rule-listing.patch` → `TestListRulesFullThreaded` (Pattern 6 step 4):
    drop the projection field from the rule listing's direct store-options literal.
16. `04-07-cli-help-drops-the-maximum.patch` → `TestRecallMaximumIsStatedNumerically` (D-01, D-04):
    revert the list limit flag's usage string to a form that does not name the number.

Register all sixteen in the phase entry Task 1 created, each with a trailing `// reverts:` comment
naming what the mutation undoes and its decision id. Change nothing else in the harness.

Run the harness once, alone, then the default-timeout package run, then the full gate (verify). Apply
the pre-authorized runtime contingency described in this plan's interfaces block only if the
default-timeout run genuinely times out inside the harness.

Commit as `test(store): register phase 4's remaining red-evidence` with pathspec
`internal/store/redevidence_harness_test.go .planning/phases/04-list-listscheduled-search-bounded-reads/red-evidence`.
  </action>
  <verify>
    <automated>out=$(ENGRAM_REQUIRE_QDRANT=1 go test ./internal/store/ -run '^TestRedEvidencePatchesAreLive$' -count=1 -v -timeout 90m 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e 'confirmed RED|^\s*--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e 'confirmed RED: ' | wc -l)" -eq 42 &amp;&amp; { dirty=$(git status --porcelain -- internal/server internal/store cmd/engram) || exit 1; test -z "$dirty"; }</automated>
    <fails_when>non-zero exit, a `--- FAIL:` line, a `confirmed RED:` count other than 42, a subtest reporting that a target test PASSED with its patch applied, or any mutated source file left dirty after the run</fails_when>
    <automated>ENGRAM_REQUIRE_QDRANT=1 go test ./internal/store/ -count=1</automated>
    <fails_when>non-zero exit, including a test-timeout panic (the pre-authorized narrowing contingency then applies)</fails_when>
    <automated>task</automated>
    <fails_when>non-zero exit from `task` (lint or test), including any `--- FAIL:` or `FAIL` package line</fails_when>
  </verify>
  <acceptance_criteria>
    - `ls .planning/phases/04-list-listscheduled-search-bounded-reads/red-evidence/*.patch | wc -l` prints `17`, and every patch's `rg -o -e '^diff --[g]it a/' <patch> | wc -l` prints `1`.
    - `rg -l 'SPDX-License-Identifier' .planning/phases/04-list-listscheduled-search-bounded-reads/red-evidence/` prints nothing; `task license:check` exits 0.
    - The phase entry maps exactly the seventeen patch names, and the earlier phases' entries are unchanged: with `C` the Task 1 commit SHA recorded in the SUMMARY, `d=$(git diff "$C^" -- internal/store/redevidence_harness_test.go) || exit 1; printf '%s\n' "$d" | rg -o -e '^-\s+"0[123]-' | wc -l` prints `0`.
    - The SUMMARY carries a table of all seventeen patches with each target's hand-verified failure line, and states whether the runtime contingency fired (with before/after durations if it did).
    - `git status --porcelain` after the final commit shows no modified tracked file.
  </acceptance_criteria>
  <done>Seventeen hand-verified patches are registered, the harness confirms forty-two REDs with a clean tree, and `task` is green.</done>
</task>

<task type="auto">
  <name>Task 3 (LAST task of the phase): The five requirements marked complete and the phase gate closed</name>
  <files>.planning/REQUIREMENTS.md</files>
  <precondition>Task 2 is committed, `task` exits 0, and the working tree is clean.</precondition>
  <read_first>
    - `.planning/REQUIREMENTS.md` in full — the `### Bounded Qdrant Reads` and `### Decisions (resolved in discuss-phase)` checklists, the `## Traceability` table and its Status vocabulary, and the coverage counts at the end.
    - Each of this phase's SUMMARY files, to confirm every requirement's evidence actually shipped before ticking its box.
  </read_first>
  <action>
Tick exactly this phase's five requirements and nothing else.

1. In the requirements checklists, change the unchecked box to a checked one for the list-bounded,
   scheduled-list-bounded, search-count-bounded, list-contract-unchanged and
   list-limit-contract-decided requirements. Leave every other box exactly as it is — in particular
   the Phase 5, 6 and 7 requirements stay unchecked, and no other phase's verification document,
   context document or checkbox is touched anywhere in this task.
2. In the traceability table, change those same five rows' Status cell from pending to the completed
   value the table already uses for the earlier phases' rows. Change no other row.
3. Update the file's trailing last-updated line the way the previous phase's close did. Do NOT add
   any heading to this file, and do NOT add a licence header.
4. Re-run the full gate as the final act of the phase. If anything is red that this plan did not
   cause, STOP and report it rather than editing unrelated code.

Commit as `docs(04): mark the phase's five requirements complete` with pathspec
`.planning/REQUIREMENTS.md`.
  </action>
  <verify>
    <automated>rg -c -e '^- \[x\] \*\*REQ-(list-bounded|list-scheduled-bounded|search-k-bounded|list-contract-unchanged|list-limit-contract-decided)\*\*' .planning/REQUIREMENTS.md</automated>
    <fails_when>the count printed is anything other than `5`, or `rg` exits non-zero because no line matched</fails_when>
    <automated>test "$(rg -o -e '^- \[x\] \*\*REQ-' .planning/REQUIREMENTS.md | wc -l)" -eq 12</automated>
    <fails_when>non-zero exit — the total number of ticked requirements is not the seven that were already complete plus this phase's five, meaning another phase's box moved</fails_when>
    <automated>task</automated>
    <fails_when>non-zero exit from `task` (lint or test), including any `--- FAIL:` or `FAIL` package line</fails_when>
    <automated>go test ./internal/keylinks/ -count=1 -v 2>&amp;1 | rg -e '^--- |^ok|^FAIL'; go test ./internal/keylinks/ -count=1</automated>
    <fails_when>non-zero exit from the second command, or a `--- FAIL:` line naming the repo-wide escaping gate or the active-milestone satisfiability gate (a key-links pattern in one of this phase's plans is escaped or no longer matches the shipped code)</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -c -e 'Phase 4 \| Complete' .planning/REQUIREMENTS.md` prints `5`, and `rg -c -e 'Phase 4 \| Pending' .planning/REQUIREMENTS.md` prints `0`.
    - `git diff --exit-code HEAD -- .planning/phases/02-error-classification-resourceexhausted-mapping .planning/phases/03-shared-bounded-read-mechanism-content-cap-decision` exits 0 apart from the Phase 2 red-evidence patch plan 04-01 legitimately regenerated — name that file in the SUMMARY and confirm nothing else under those directories moved in this plan.
    - `rg -c -e 'SPDX-License-Identifier' .planning/REQUIREMENTS.md` prints `0`.
    - `rg -o -e '✅|📋|🚧' .planning/REQUIREMENTS.md | wc -l` prints the same number it printed at HEAD before this task — no version-bearing or milestone-marker heading was introduced.
    - `git status --porcelain` after the final commit shows no modified tracked file.
  </acceptance_criteria>
  <done>The phase's five requirements are ticked in both the checklist and the traceability table with no other phase's state touched, the full quality gate and the key-links gates are green, and the phase closes clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness → working tree | The harness applies real patches to real source files and must always restore them. |
| this phase's plans → other phases' recorded state | A cross-phase edit made to turn a gate green would erase another phase's evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-08-01 | Tampering | a patch left applied after a failed run | medium | mitigate | The harness reverts via deferred and cleanup paths and asserts a clean porcelain status; both harness verifies additionally assert the mutated source trees are clean. |
| T-04-08-02 | Repudiation | a stale patch that no longer proves its gate RED | high | mitigate | Each patch is hand-verified against its target's own failure line before registration, the harness re-proves all forty-two on every full run, and the verify rejects a run in which a target passed with its patch applied. |
| T-04-08-03 | Repudiation | a requirement ticked without shipped evidence | high | mitigate | Task 3 reads every plan SUMMARY before ticking, the totals are asserted by count so no extra box can move, and the whole gate must be green first. |
| T-04-08-04 | Tampering | another phase's recorded state edited to make this gate green | high | mitigate | An acceptance criterion diffs the Phase 2 and Phase 3 directories and permits only the single patch plan 04-01 legitimately regenerated; the executor notes forbid the edit outright. |
| T-04-08-05 | Denial of Service | the harness outgrowing the default test timeout at forty-two patches | medium | mitigate | Measured under the default timeout; the pre-authorized per-package narrowing applies only if it fires; timeouts are never raised in the task runner or CI to hide it. |
| T-04-08-SC | Tampering | npm/pip/cargo installs | low | accept | Not applicable: no installs, no manifest change. |
</threat_model>

<verification>
- `ENGRAM_REQUIRE_QDRANT=1 go test ./internal/store/ -run '^TestRedEvidencePatchesAreLive$' -count=1 -v -timeout 90m` shows 42 `confirmed RED:` lines and exits 0.
- `task` exits 0; `task license:check` exits 0; `go test ./internal/keylinks/ -count=1` exits 0; `git diff --exit-code HEAD -- go.mod go.sum` exits 0.
- `git status --porcelain` prints nothing after the final commit.
</verification>

<success_criteria>
- Every guarantee this phase added — the bounded list in all three modes, the bounded scheduled listing, the two-phase bounded search, the unchanged list contract, and the decided-and-published count contract — has a registered, live RED proof.
- The phase's five requirements are marked complete on the evidence that actually shipped.
- The phase closes on a green `task` with no other phase's recorded state disturbed.
</success_criteria>

<output>
Create `.planning/phases/04-list-listscheduled-search-bounded-reads/04-08-SUMMARY.md` when done.
</output>
