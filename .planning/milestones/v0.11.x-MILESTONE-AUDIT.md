---
milestone: v0.11.x
name: Capture & Service Identity
audited: 2026-07-26T22:57:25Z
status: passed
scores:
  requirements: 11/11
  phases: 5/5
  integration: 6/6
  flows: 2/2
gaps:
  requirements: []
  integration: []
  flows: []
nyquist:
  compliant_phases: [22, 23, 25]
  partial_phases: []
  not_validated_phases: [24, 26]
  missing_phases: []
  overall: partial-coverage
tech_debt:
  - phase: roadmap-bookkeeping
    items:
      - "ROADMAP.md Progress table plan counts are stale: Phase 22 shows 2/2 (3 plans on disk), Phase 23 shows 4/4 (6), Phase 25 shows 0/1 (2), Phase 26 shows 3/3 (6). Milestone checkboxes and Complete/date columns are correct."
  - phase: 22-cedar-authz-foundation-store-enforcement
    items:
      - "ADR engram-cdr1 does not explicitly name the scoped-vs-blanket forbid distinction that the implementation deliberately adopted (documentation completeness, not a functional gap)."
  - phase: 24-idempotent-capture
    items:
      - "VALIDATION.md is status: draft — validate-phase never reconciled it. Nyquist coverage TODO."
  - phase: 26-structured-citations-category-filter-chat-base-url
    items:
      - "VALIDATION.md is status: draft — validate-phase never reconciled it. Nyquist coverage TODO."
coverage_notes:
  - "REQ-chat-base-url was not examined by the integration checker: it is Phase-26-internal (internal/openaiurl.Join shared by internal/embed and internal/summarize) with no cross-phase touchpoint to Phases 22-25. It IS verified by Phase 26's own VERIFICATION.md (SC3)."
  - "REQ-service-auth-chain wiring was confirmed from internal/auth/chain.go structure; cmd/engram/serve.go was not re-read this pass. Phase 23's own VERIFICATION.md covers it (SC1-SC5, all passed)."
---

# Milestone v0.11.x — Capture & Service Identity: Audit Report

**Status:** passed
**Audited:** 2026-07-26T22:57:25Z
**Phases:** 22-26 (5 phases, 19 plans)
**All phases merged to `main`** (Phase 26 via PR #432, squash `9980d945`)

**Milestone goal:** Make programmatic capture correct and re-runnable, and give headless service
principals a first-class, isolated identity — so agents can write memory mechanically and safely
into shared stores.

---

## Requirements Coverage (3-source cross-reference)

Each requirement is triangulated across three independent sources: the REQUIREMENTS.md
traceability table, the assigned phase's VERIFICATION.md requirements table, and the
`requirements-completed` frontmatter of the plan SUMMARY.md files.

| Requirement | Phase | REQUIREMENTS.md | VERIFICATION.md | SUMMARY frontmatter | Status |
|---|---|---|---|---|---|
| REQ-cedar-pdp-foundation | 22 | `[x]` Complete | passed | 22-01 | **satisfied** |
| REQ-cedar-store-enforcement | 22 | `[x]` Complete | passed | 22-02, 22-03 | **satisfied** |
| REQ-service-auth-chain | 23 | `[x]` Complete | passed | 23-01, 23-02, 23-03, 23-04, 23-06 | **satisfied** |
| REQ-static-token-auth | 23 | `[x]` Complete | passed | 23-02, 23-04 | **satisfied** |
| REQ-service-owner-failclosed | 23 | `[x]` Complete | passed | 23-01 | **satisfied** |
| REQ-service-principal-isolation | 23 | `[x]` Complete | passed | 23-05, 23-06 | **satisfied** |
| REQ-idempotent-capture | 24 | `[x]` Complete | passed | 24-01, 24-02 | **satisfied** |
| REQ-supersession-links | 25 | `[x]` Complete | passed | 25-01, 25-02 | **satisfied** |
| REQ-memory-citations | 26 | `[x]` Complete | passed | 26-05, 26-06 | **satisfied** |
| REQ-category-filter | 26 | `[x]` Complete | passed | 26-01, 26-02, 26-03, 26-06 | **satisfied** |
| REQ-chat-base-url | 26 | `[x]` Complete | passed | 26-04, 26-06 | **satisfied** |

**Score: 11/11 satisfied. 0 unsatisfied, 0 partial, 0 orphaned.**

Orphan check: every REQ-ID in the traceability table appears in at least one phase VERIFICATION.md
and at least one SUMMARY.md frontmatter. No requirement was assigned but left unverified.

---

## Phase Verification Summary

| Phase | Plans | Verification | Score | Gaps | Anti-patterns | Human verification | Threats open |
|---|---|---|---|---|---|---|---|
| 22. Cedar Authz Foundation & Store Enforcement | 3/3 | passed | 5/5 must-haves | none | none | none | 0 |
| 23. Service Auth Chain & Tenancy Isolation | 6/6 | passed | 12/12 must-haves | none | none | none | 0 |
| 24. Idempotent Capture | 2/2 | passed | 12/12 must-haves | none | none | none | 0 |
| 25. Supersession with History | 2/2 | passed | 9/9 must-haves | none | none | none | 0 |
| 26. Structured Citations, Category Filter & Chat Base URL | 6/6 | passed | 4/4 SCs + 6/6 plan truth sets | none blocking | 1 doc-staleness (resolved) | none | 0 |

**Score: 5/5 phases verified passed.**

Every phase verified against live code and executed tests — several against a real Qdrant instance
via testcontainers rather than mocks. Three phases carried a code review that found and fixed a
defect before completion (23: CR-01 static-token map orientation; 25: CR-04 cross-path lost write;
26: CR-01 idempotency fingerprint omission).

---

## Cross-Phase Integration

Six seams were checked against the composed system on `main`, reading source rather than phase
narratives.

| # | Seam | Phases | Status | Evidence |
|---|---|---|---|---|
| 1 | Authz spine — outer-`Must` invariant survives all newer filters | 22→23→24/25/26 | **WIRED** | `ownerScopeFilter`/`listFilter` build the authz condition first (`store.go:752-757`, `:1054-1058`); tags/categories/time-window/`superseded_by` are appended after at `store.go:889-899` (Search) and `:1113-1121` (List). `SearchDiscovery` follows the same pattern (`:977-987`). Never reordered ahead of or substituted for `ownerOrSharedCondition`. |
| 2 | Service identity → capture — idempotency keyed on the resolved owner | 23→24 | **WIRED** | `storeMemory` derives `owner := c.Subj.Owner()` (`tools.go:794`) → `checkIdempotentReplay` → `idempotencyPointID(owner, scope, key)` (`idempotency.go:39-43`). Phase 23's fail-closed gate (`auth.go:254-259`) plus per-token `namespacedOwner("static_token", ownerID)` make the owner non-empty and injective, so two service principals cannot collide on an identical key+scope. |
| 3 | Payload key coexistence — no whole-payload write erases a sibling key | 24→25→26 | **WIRED** | `Update`'s whole-payload `Upsert` (`store.go:1688`) operates on a `fromPayload`-decoded record that round-trips `IdempotencyFingerprint`/`Citations`/`SupersededBy` (`:592-637`), and re-reads `Supersedes`/`SupersededBy` inside the `TargetLocker` lock (`:1650-1659`, the CR-04 fix). `Supersede`'s back-stamp is a targeted single-key `SetPayload` (`:1927-1931`). |
| 4 | Recall gate composition — soft-hide composes with the category pre-filter on both lanes | 25→26 | **WIRED** | `qdrant.NewIsEmpty("superseded_by")` applied at **both** `store.go:893` (Search) and `:1118` (List), in the same `f.Must` slice as the category pre-filter (`:895-897`, `:1059-1061`). `SearchDiscovery` carries it too (`:987`). |
| 5 | MCP↔Connect parity — same core, same compact-view contract | 26 + all | **WIRED** | Both surfaces call the transport-neutral `deps.listMemory`/`deps.searchMemory` (`tools.go:1066`, `:1116`) from MCP (`:1465`, `:1498`) and Connect (`connectapi.go:161`, `:210`). Compact view: Connect clears `Citations`/`Kind` in the non-full branch (`connectapi.go:101-107`), mirroring MCP's `recallView` allow-list. |
| 6 | E2E flows | all | **WIRED** | See below. |

**Score: 6/6 seams wired. 0 orphaned, 0 missing.**

### End-to-End Flows

| Flow | Status | Path |
|---|---|---|
| (a) Authenticated service principal stores a memory with an idempotency key + citations, then recalls it filtered by category | **WIRED** | auth chain resolves owner (fail-closed) → `storeMemory` stamps `IdempotencyFingerprint` + `Citations` via `payload()` → `searchMemory` / Connect `SearchMemories` apply the same `Categories` OR-filter inside the authz-gated `Search` |
| (b) A memory is superseded, disappears from search/list, stays fetchable by id, authz still enforced on both | **WIRED** | `Search`/`List` both apply `NewIsEmpty("superseded_by")`; `getMemory` → `GetReadable` → `decideRecord` is ungated for soft-hide but still authz-gates per-record ownership/shared-read |

**Score: 2/2 flows complete.**

---

## Nyquist Coverage

| Phase | VALIDATION.md | status | nyquist_compliant | Classification | Action |
|---|---|---|---|---|---|
| 22 | exists | validated | true | **COMPLIANT** | — |
| 23 | exists | validated | true | **COMPLIANT** | — |
| 24 | exists | draft | false | **NOT-VALIDATED** | `/gsd-validate-phase 24` |
| 25 | exists | validated | true | **COMPLIANT** | — |
| 26 | exists | draft | false | **NOT-VALIDATED** | `/gsd-validate-phase 26` |

Phases 24 and 26 read `status: draft`, meaning validate-phase never reconciled the file that
plan-phase seeded. Per the NOT-VALIDATED-vs-PARTIAL rule, `nyquist_compliant: false` is **not
authoritative** on a draft file — this is a coverage TODO, not a compliance failure, and it does
not force `gaps_found`. Re-running `/gsd-validate-phase` on either promotes it to `validated` and
yields the real verdict.

---

## Tech Debt

Non-blocking, deferred. Nothing here affects shipped behavior or any requirement.

**ROADMAP bookkeeping**
- Progress-table plan counts are stale: Phase 22 shows `2/2` (3 plans on disk), Phase 23 shows
  `4/4` (6), Phase 25 shows `0/1` (2), Phase 26 shows `3/3` (6). The milestone-level checkboxes,
  Complete status, and completion dates are all correct. Worth correcting during milestone
  archival so the archived roadmap is accurate.

**Phase 22 — Cedar Authz Foundation**
- ADR `engram-cdr1` does not explicitly name the scoped-vs-blanket forbid distinction that the
  implementation deliberately adopted (`when { resource has owner && resource.owner != "" }
  unless { principal.owner != "" }` rather than a blanket forbid, so the anonymous `owner==""`
  bucket stays reachable). The reconciliation is verified correct and tested; only the ADR prose
  omits the distinction by name.

**Phases 24, 26 — Nyquist**
- `VALIDATION.md` left at `status: draft` (see Nyquist Coverage above).

**Resolved during the milestone (not outstanding)**
- Phase 24's audit flagged the Phase-24 ROADMAP milestone checkbox as `[ ]`; it is now `[x]` with
  a completion date.
- Phase 26's verification flagged a stale `26-05-SUMMARY.md` decision bullet asserting
  pre-CR-01 behavior; it and the corresponding PLAN entries were annotated SUPERSEDED in the same
  session.

---

## Coverage Caveats

Stated so the scores are not read as broader than the evidence supports:

- **REQ-chat-base-url** was not examined by the integration checker. It is Phase-26-internal
  (`internal/openaiurl.Join` shared by `internal/embed` and `internal/summarize`) with no
  cross-phase touchpoint to Phases 22-25, so it fell outside the six named seams. It is verified
  by Phase 26's own VERIFICATION.md (SC3, including
  `TestJoin/shipped_default_is_byte-identical_to_today`).
- **REQ-service-auth-chain** wiring was confirmed from `internal/auth/chain.go` structure;
  `cmd/engram/serve.go` was not re-read in the integration pass. Phase 23's own VERIFICATION.md
  covers the full chain (SC1-SC5, all passed).

---

## Verdict

**passed** — 11/11 requirements satisfied, 5/5 phases verified, 6/6 integration seams wired,
2/2 E2E flows complete, 0 critical gaps, 0 open threats across all five phases. Remaining items
are documentation, bookkeeping, and two Nyquist coverage TODOs — none blocking milestone closure.

---

_Audited by Claude (gsd-audit-milestone) — phase verifications read from artifacts, integration
seams verified against source on `main` by gsd-integration-checker._
