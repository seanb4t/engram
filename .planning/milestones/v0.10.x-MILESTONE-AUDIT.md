---
milestone: v0.10.x
audited: 2026-07-16
status: tech_debt
scores:
  requirements: 19/20 satisfied (1 partial/deferred — not failed)
  phases: 9/9 complete (7 verified passed, 2 human_needed by design)
  integration: all write-lane links WIRED (0 gaps)
  flows: 4/4 core E2E complete (2 live-observation UATs deferred by design)
  security: 0 open threats across all phases with a SECURITY.md
  nyquist: 9/9 compliant (16 & 19 reconciled 2026-07-16 — 0 gaps, no new tests)
gaps:
  requirements:
    - id: REQ-ci-renovate-spa-drift
      status: partial
      phase: 21-ci-maintenance-hygiene
      claimed_by_plans: ["21-03-PLAN.md"]
      completed_by_plans: []       # 21-03-SUMMARY requirements-completed: [] — deliberately not claimed complete
      verification_status: human_needed
      evidence: >-
        Code + infra + security + review all complete and merged (PR #371, ci.yaml self-heal).
        The self-heal path's live behaviour can only be observed on a real Renovate ui/ bump PR,
        and that is inherently POST-MERGE (Renovate PRs branch off main and only carry the workflow
        once it is on main). NOT a defect and NOT remediable pre-observation — tracked by GitHub #369.
  integration: []                  # none — integration checker found no wiring gaps
  flows: []                        # none — both incomplete flows are documented, by-design deferrals
tech_debt:
  - phase: 19-console-write-ux
    items:
      - "Live-stack UAT deferred (4 items, blocked_by: server) — full-stack e2e harness (compose + mock OIDC + Playwright) is candidate future work (GH #366). Automated component/logic coverage is complete (Nyquist reconciled 9/9 on 2026-07-16)."
  - phase: 21-ci-maintenance-hygiene
    items:
      - "REQ-ci-renovate-spa-drift live observation pending (#369) — post-merge only, see requirements gap above."
      - "Pre-existing Taskfile.yaml yamlfmt discrepancy (local task lint:yaml red, CI green — CI runs no yamlfmt). Tracked as #370. Not introduced by this milestone."
resolved_during_audit:
  - "Nyquist reconciliation for phases 16 & 19 (2026-07-16): both were unflipped/unfilled VALIDATION.md docs; retroactive audit found 0 coverage gaps (all behaviors already had real passing tests — 16: 8/8, 19: 10/10 automated + 2 documented manual-only), flipped nyquist_compliant to true. No new tests generated. Milestone now 9/9 Nyquist-compliant."
---

# Milestone v0.10.x — Hardening & Write Lane — Audit

**Audited:** 2026-07-16 · **Status:** `tech_debt` (no blockers; one deferred post-merge observation + minor bookkeeping)
**Phases:** 13–21 (9 phases, all merged to `main` @ `4c940339`)

## Verdict

The milestone is **code-complete and integration-clean**. Every one of the 20 requirements has its
implementation shipped and merged; 19 are fully verified. The single non-`passed` requirement,
**REQ-ci-renovate-spa-drift (#301)**, is **deferred, not failed** — its only outstanding step is a
live observation that is *impossible before merge* and is tracked by #369. The cross-phase write-lane
spine wires end-to-end with no gaps, and the milestone's #1 risk (MCP↔Connect authz parity) is backed
by a real parity-test artifact, not a claim.

Status is `tech_debt` rather than `gaps_found` because there are **no blockers, no broken wiring, and
no failed requirements** — only one deferred observation and two validation-signoff bookkeeping gaps.
(The FAIL gate forces `gaps_found` only for *unsatisfied* requirements — verification failed or
missing. #301 is `partial`: verification `human_needed`, code merged, SUMMARY explicitly defers it.)

## Requirements Coverage (3-source: VERIFICATION.md × SUMMARY frontmatter × REQUIREMENTS.md)

| Phase | Requirements | 3-source status |
|---|---|---|
| 13 | REQ-embed-timeout, REQ-embed-baseurl-join, REQ-embed-config-identity | ✅ satisfied (passed / listed / `[x]`) |
| 14 | REQ-embed-gemini-direct, REQ-embed-prod-parity-eval, REQ-embed-model-docs | ✅ satisfied |
| 15 | REQ-connect-write-rpcs, REQ-connect-write-authz-parity | ✅ satisfied |
| 16 | REQ-connect-csrf | ✅ satisfied |
| 17 | REQ-connect-write-authz-parity (wired) | ✅ satisfied |
| 18 | REQ-session-rotation | ✅ satisfied |
| 19 | REQ-console-write-ux | ✅ satisfied (code merged; live E2E UAT deferred) |
| 20 | REQ-discovery-proto-fidelity, REQ-shortid-mint-cap, REQ-embed-param-key-sharing, REQ-embed-body-build-collapse, REQ-discovery-shortid-schema, REQ-summarize-cronjob | ✅ satisfied (6/6) |
| 21 | REQ-p11-review-residuals, REQ-lint-planning-exclude | ✅ satisfied |
| 21 | **REQ-ci-renovate-spa-drift** | ⏸ **partial — deferred (#369, post-merge only)** |

**19/20 satisfied · 1 partial (deferred).** No orphaned requirements (every REQ-ID appears in its
phase's VERIFICATION.md and SUMMARY). No `unsatisfied` (failed) requirements.

## Phase Verification

| Phase | Verification | Security (threats_open) | Nyquist |
|---|---|---|---|
| 13 embedder-reliability-foundation | passed | (no SECURITY.md — low-surface embed knobs) | compliant |
| 14 embedder-model-options-eval | passed | 0 | compliant |
| 15 additive-proto-stub-write-handlers | passed | 0 | compliant |
| 16 csrf-interceptor | passed | 0 | ⚠ false (signoff bookkeeping) |
| 17 wired-write-handlers | passed | 0 | compliant |
| 18 stateless-session-rotation | passed | 0 | compliant |
| 19 console-write-ux | human_needed (live UAT deferred) | 0 | ⚠ false (signoff bookkeeping) |
| 20 correctness-polish | passed | 0 | compliant |
| 21 ci-maintenance-hygiene | human_needed (#369 live obs) | 0 | compliant |

**7 passed · 2 human_needed (both by-design deferrals).** Security: 0 open threats everywhere a
threat model was authored.

## Cross-Phase Integration (gsd-integration-checker)

**All write-lane links WIRED — no gaps, no BLOCKER/WARNING findings beyond the two documented deferrals.**

| Link | Path | Status |
|---|---|---|
| Proto → handlers (15→17) | `engram.proto:238-243` (6 RPCs, no `NO_SIDE_EFFECTS`, CI-gated) → `connectapi.go` handlers → `deps.*` | WIRED |
| CSRF → write RPCs (16→17) | `connectcsrf.go` allowlist keyed off generated `EngramService*Procedure` constants (regen-safe) | WIRED |
| MCP↔Connect authz parity (17) | `connectapi_write_parity_test.go` — real per-RPC parity test (the #1 milestone risk) | WIRED |
| Session rotation (18) | `webauth.Reseal` → `serve.go:171` → `mountConnect` → innermost interceptor, success-only | WIRED |
| Console → wire (19) | `client.ts engramWrite` (retryOnce+attachCsrf) → `mutations/memory.ts` → form sheets → `resume.ts` | WIRED (live E2E UAT deferred) |
| Discovery fidelity (20→19) | `memoryToProto` Kind/Citations → `SearchDiscoveries` → `DiscoveryFormSheet` | WIRED |

**E2E flows complete:** create/edit/delete/re-share/schedule (console → Connect → deps → store, CSRF +
retry + resume-envelope); read/search/get → summary-shaping → display incl. discovery fidelity;
session survival across a long read→write session.

**E2E flows deferred (by design, not gaps):** Phase 19 live browser↔server↔OIDC UAT (no harness yet);
Phase 21 live Renovate self-heal observation (#369).

## Nyquist Coverage — 9/9 compliant

All 9 phases `nyquist_compliant: true`. Phases 16 and 19 were reconciled during this audit
(2026-07-16): 16's VALIDATION.md was authored but never flipped from `draft`; 19's was an unfilled
template. A retroactive State-A audit found **0 coverage gaps** — every behavior already had a real,
passing test (16: 8/8 strategy behaviors, all green; 19: 10/10 automated behaviors + 2 documented
manual-only). **No auditor spawn, no new test code generated** — reconciliation only. Both re-run
green 2026-07-16.

## What Shipped (milestone deliverables)

- **Embedder reliability & options** (13–14): configurable timeout, correct base-URL join across
  provider shapes, embedder-config-identity stamping, Gemini-direct, prod-parity eval, model docs.
- **Connect write lane** (15–19): 6 additive write RPCs, CSRF (Origin + double-submit), MCP↔Connect
  authz-parity handlers, stateless sliding-expiry session rotation, full console write UX.
- **Correctness & polish** (20): discovery proto fidelity, MintShortID cap, embed cleanups, summarize
  CronJob.
- **CI/maintenance hygiene** (21): rumdl `.planning` exclude, Phase-11 review residuals, Renovate
  vendored-SPA self-heal.

## Remediation / next steps

The one open requirement is **not remediable now** — it awaits a real-world event (the next Renovate
`ui/` bump PR, now possible because the workflow is on `main`). No closure phase applies.

- **Proceed to close the milestone**, recording REQ-ci-renovate-spa-drift as a known deferred item
  (`/gsd-complete-milestone` with override closeout). This is the recommended path given the milestone
  is code-complete and integration-clean.
- Optionally run `/gsd-validate-phase 16` and `/gsd-validate-phase 19` first to flip the Nyquist
  signoff flags (bookkeeping).
- When the first live Renovate `ui/` bump PR is observed self-healing → `/gsd-verify-work 21` closes
  REQ-ci-renovate-spa-drift and #301.

**Repo-specific note for milestone close:** this repo uses release-please for semver package tags
(v0.8.x) and protects `main`. "v0.10.x" is a GSD milestone label, not a package version — do **not**
create a `v0.10.x` git tag, and run the archival through a branch + PR (not direct commits to `main`).
