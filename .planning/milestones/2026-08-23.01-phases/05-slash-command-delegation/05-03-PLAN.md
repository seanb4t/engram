---
phase: 05-slash-command-delegation
plan: 03
type: execute
wave: 3
depends_on: [05-02]
files_modified:
  - internal/setupgen/setupgen.go
  - internal/setupgen/setupgen_test.go
  - internal/surfacesgen/main.go
  - internal/surfacesgen/main_test.go
  - Taskfile.yaml
autonomous: true
requirements: [REQ-delegation-equivalence-derived]
estimate:
  tokens: 19000
  raw_tokens: 19000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - task lint renders and compares the setup command region without changing any repository file, and fails on drift or malformed/missing anchors.
    - CI's existing surfaces:gen followed by git diff detects a changed setup command region through the same renderer.
    - Changing one real Plan registration argv token changes generated bytes and makes both comparison lanes fail against the previous bytes.
    - Regenerating a drifted temporary command restores equality while preserving all bytes outside the region.
  artifacts:
    - path: internal/setupgen/setupgen.go
      provides: Read-only Check using the same Render as Write
    - path: internal/surfacesgen/main.go
      provides: Check-only dispatch that bypasses all writers
    - path: internal/setupgen/setupgen_test.go
      provides: Plan mutation and exact drift negative controls
    - path: Taskfile.yaml
      provides: lint:setup delegation drift check dependency
  key_links:
    - from: Taskfile.yaml
      to: internal/surfacesgen/main.go
      via: lint:setup runs check-only mode
      pattern: 'check-setup'
    - from: internal/setupgen/setupgen.go
      to: internal/surfaces/anchor.go
      via: Check compares existing region to rendered bytes
      pattern: 'surfaces.ReadRegion'
---

<objective>
Make generated registration drift fail locally and in CI and prove the gates can go red, per D-13 through D-15 and D-20. Preserve task surfaces:gen as the only production regeneration path and keep lint read-only.
</objective>

<execution_context>
@/Users/sean/.codex/gsd-core/workflows/execute-plan.md
@/Users/sean/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-slash-command-delegation/05-CONTEXT.md
@.planning/phases/05-slash-command-delegation/05-PATTERNS.md
@.planning/phases/05-slash-command-delegation/05-02-SUMMARY.md
</context>

## Artifacts this phase produces

- `setupgen.Check(path string) error` and internal shared render/compare helpers as needed for injected Plan fixtures.
- `--check-setup` check-only mode in the existing `internal/surfacesgen` executable; `lint:setup` Task dependency.
- `internal/surfacesgen/main_test.go` with check dispatch and subprocess exit integration tests.
- `TestPlanMutationChangesRegion` and `TestDriftChecks` in `internal/setupgen/setupgen_test.go`.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Wire a read-only local gate through the existing generator</name>
  <files>internal/setupgen/setupgen.go, internal/setupgen/setupgen_test.go, internal/surfacesgen/main.go, internal/surfacesgen/main_test.go, Taskfile.yaml</files>
  <read_first>internal/setupgen/setupgen.go; internal/setupgen/setupgen_test.go; internal/surfacesgen/main.go; internal/surfaces/anchor.go; Taskfile.yaml; .github/workflows/ci.yaml; .planning/phases/05-slash-command-delegation/05-CONTEXT.md</read_first>
  <behavior>
    - Check passes for an exact generated region, fails for one-byte drift, and leaves whole-file bytes unchanged on either result.
    - Missing, malformed, reversed, or ambiguous region anchors fail closed; a duplicate setup region must not hide a stale second copy.
    - Generator check-only dispatch bypasses all region writers and emits a nonzero exit on drift.
    - The lint task invokes check-only mode, not the mutating generation task.
  </behavior>
  <action>Implement Check(path) with the same production Render as Write, reading via surfaces.ReadRegion and returning contextual errors for missing/malformed regions or unequal bytes per D-14 and D-20. Preserve exact comparison semantics, including trailing newline handling shared with the writer. Ensure one canonical setup region is checked completely; reject duplicated setup anchors rather than validating only the first region (the shared reader supports other consumers with multiple regions). Add --check-setup to existing surfacesgen dispatch, selecting this read-only path before run() reaches any existing writer. Extract testable dispatch only as needed and have main report its error with nonzero status. Add lint:setup invoking go run ./internal/surfacesgen --check-setup, and add it to lint deps. The ordinary task surfaces:gen still invokes the existing generator and existing protobuf/golden sequence unchanged (D-12). CI already regenerates and diffs skill/: retain that lane without a duplicate invocation or workflow edit. Tests exercise the real check dispatch against a scratch repository-shaped fixture and compare a sentinel file plus target bytes before/after; no test calls a third-party runtime.</action>
  <verify><automated>go test ./internal/setupgen ./internal/surfacesgen -count=1 -v</automated></verify>
  <acceptance_criteria>Check dispatch passes for matching bytes, exits nonzero on drift, rejects bad anchors, and never changes target or sentinel bytes. task lint:setup succeeds against the committed generated command and is reachable from task lint.</acceptance_criteria>
  <done>A developer gets a failing local lint result for setup command drift without regeneration or repository mutation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove both drift lanes detect a changed authored argv</name>
  <files>internal/setupgen/setupgen_test.go, internal/surfacesgen/main_test.go</files>
  <read_first>internal/setupgen/setupgen.go; internal/setupgen/setupgen_test.go; internal/surfacesgen/main.go; internal/surfacesgen/main_test.go; internal/setup/claudecode.go; internal/surfaces/anchor_test.go; .github/workflows/ci.yaml; Taskfile.yaml</read_first>
  <behavior>
    - An injected Plan function copies a real add action and changes one argv token; output differs from baseline for that row.
    - The read-only check rejects baseline bytes when rendering mutated source and preserves the file byte-for-byte.
    - Writing mutated source into a committed scratch fixture makes git diff --exit-code fail; regenerating again is stable.
    - Writing the authoritative baseline back restores clean scratch-git comparison and preserves outside-anchor text.
  </behavior>
  <action>Per D-15, create TestPlanMutationChangesRegion using a real ClaudeCode.Plan, copying its Args before changing one registration token through the injected PlanFunc; assert the changed command appears in derived output and unrelated rows retain baseline bytes. Do not mutate source files or duplicate argv literals. Create TestDriftChecks connecting that mutant to the same check/write helpers used by production. In a t.TempDir git repository with local author identity, commit the baseline Markdown fixture, demonstrate read-only rejection without writes, then run the writer with mutant rendering and require a failing git diff --exit-code. Restore baseline through the writer and require a clean diff. Also corrupt the region independently to show the check and regeneration/diff lane detect checked-in artifact drift, preserving prefix/suffix bytes and failing closed on malformed anchors. This establishes source sensitivity and artifact sensitivity separately. Invoke only git and engram-owned code in scratch paths; production CI continues to use its existing regeneration command.</action>
  <verify><automated>go test ./internal/setupgen ./internal/surfacesgen -run 'Test(PlanMutationChangesRegion|DriftChecks)' -count=1 -v</automated></verify>
  <acceptance_criteria>Tests visibly run and assert both red controls: mutant source changes output and stale artifact fails read-only comparison; scratch git diff fails after mutation and passes after authoritative restoration. No committed golden or keyword-only equivalence assertion is introduced.</acceptance_criteria>
  <done>Both actual comparison mechanisms have negative controls proving the derived-equivalence gate is non-vacuous.</done>
</task>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Lint command to filesystem | Validation must not repair the evidence it checks. |
| Source Plan to published prose | A source change must invalidate stale generated bytes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-08 | Tampering | Check / check-only dispatch | high | mitigate | Exact ReadRegion comparison; fail closed for absent or malformed anchors; before/after byte assertions and writer-bypass tests. |
| T-05-09 | Tampering | derived gate | high | mitigate | Inject real-Plan argv mutation and require read-only failure plus scratch git-diff failure and clean restoration. |
| T-05-10 | Denial of service | lint failures | low | accept | Drift intentionally blocks lint until explicit regeneration; failures name the target and regeneration command. |

ASVS Level 1; high-severity findings block. No package or schema installation is involved.
</threat_model>

<verification>
Run task lint:setup, go test ./internal/setupgen ./internal/surfacesgen ./internal/setup ./cmd/engram -count=1, and task. Run task surfaces:gen explicitly after intended output is committed, then git diff --exit-code -- skill/engram/commands/engram-setup.md cmd/engram/testdata/help.golden cmd/engram/testdata/catalog.golden. Record the real test names and RUN/PASS evidence in 05-VALIDATION.md without treating the plan as completed verification.
</verification>

<success_criteria>D-13 through D-15 and D-20 hold: one renderer, one production regeneration path, a read-only local gate, existing CI drift enforcement, and negative controls for both source and artifact drift.</success_criteria>

<output>Create .planning/phases/05-slash-command-delegation/05-03-SUMMARY.md.</output>
