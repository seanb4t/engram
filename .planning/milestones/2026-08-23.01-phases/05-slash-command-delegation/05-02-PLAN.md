---
phase: 05-slash-command-delegation
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - internal/setupgen/setupgen.go
  - internal/setupgen/setupgen_test.go
  - internal/surfacesgen/main.go
  - skill/engram/commands/engram-setup.md
  - cmd/engram/setup_delegation_test.go
autonomous: true
requirements: [REQ-engram-setup-delegates, REQ-engram-setup-prose-fallback, REQ-delegation-equivalence-derived]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - The slash command selects delegation by command -v engram, offers oauth/oauth-client/bearer/none, shows preview rows, and runs the same invocation with --apply only after explicit confirmation.
    - When the binary is absent, the slash command provides four Claude Code registration choices and OAuth completion instructions, with an environment-based credential prerequisite.
    - Every fallback registration command is rendered from the single matching add Action in the real ClaudeCode.Plan using synthetic Options.
    - Delegation inputs use the same option cases as registration rendering, and all generated arguments are accepted by the real Cobra command under fake environments.
    - Generated content replaces only the setup-commands anchored region and repeated regeneration is byte-identical.
  artifacts:
    - path: internal/setupgen/setupgen.go
      provides: Pure Plan-derived renderer, shared input cases, and anchored writer
    - path: skill/engram/commands/engram-setup.md
      provides: Generated four-mode commands plus authored delegation and fallback instructions
    - path: cmd/engram/setup_delegation_test.go
      provides: Generated invocation conformance against the live Cobra command
  key_links:
    - from: internal/setupgen/setupgen.go
      to: internal/setup/claudecode.go
      via: Real Plan function supplied to renderer
      pattern: 'setup.ClaudeCode.Plan'
    - from: internal/surfacesgen/main.go
      to: internal/setupgen/setupgen.go
      via: Existing generation entry point calls setupgen
      pattern: 'setupgen.Write'
    - from: internal/setupgen/setupgen.go
      to: internal/surfaces/anchor.go
      via: Existing anchored replacement
      pattern: 'surfaces.WriteRegion'
---

<objective>
Deliver both entry points from one authored registration source, per D-01 through D-12 and D-16 through D-19. The approved client-ID input from 05-01 enables all four delegation choices. Equivalence means registration argv plus auth inputs; the binary retains its separate remove/add execution and skills distribution behavior.
</objective>

<execution_context>
@/Users/sean/.codex/gsd-core/workflows/execute-plan.md
@/Users/sean/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-slash-command-delegation/05-CONTEXT.md
@.planning/phases/05-slash-command-delegation/05-PATTERNS.md
@.planning/phases/05-slash-command-delegation/05-01-SUMMARY.md
</context>

## Artifacts this phase produces

- New importable package `internal/setupgen` with `PlanFunc`, `Case`, `Cases`, `Render`, and `Write`; `Case.Options` and `Case.DelegationArgs` expose shared synthetic inputs and preview argv to owned tests.
- Region ID `setup-commands` and canonical target `skill/engram/commands/engram-setup.md`.
- `TestSetupGeneratedInvocations` in `cmd/engram/setup_delegation_test.go`.
- Renderer, single-action selection, quoting, and anchored-write tests in `internal/setupgen/setupgen_test.go`.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Generate and publish the four-mode command flow from real Plans</name>
  <files>internal/setupgen/setupgen.go, internal/setupgen/setupgen_test.go, internal/surfacesgen/main.go, skill/engram/commands/engram-setup.md</files>
  <read_first>internal/setup/claudecode.go; internal/setup/runtime.go; internal/setup/plan.go; internal/setup/quote.go; internal/setup/environment.go; internal/surfaces/anchor.go; internal/surfaces/anchor_test.go; internal/surfacesgen/main.go; skill/engram/commands/engram-setup.md; Taskfile.yaml; .planning/phases/05-slash-command-delegation/05-CONTEXT.md</read_first>
  <behavior>
    - The real Plan for the none case renders through surfacesgen into the command's anchored table; repeat generation is identical and outside-region bytes are preserved.
    - The renderer covers exactly oauth, oauth-client, bearer, none with synthetic URL and client ID values; each row equals the structurally selected add Action.Command.
    - Plan error, missing add action, or ambiguous add actions cause an error instead of partial rendered content.
    - Rendering uses a synthetic HomeDir and makes unexpected environment access fail; no real credentials or runtime processes are accessed.
  </behavior>
  <action>Per D-09 through D-12 and D-19, create setupgen as an importable library. Define PlanFunc with the existing Environment/Options-to-Plan signature; Render accepts it, while production Write supplies setup.ClaudeCode.Plan. Cases returns fresh copies of four ordered synthetic Options and their corresponding engram setup preview argv. Use a synthetic example URL and non-secret client-ID placeholder; oauth-client alone includes --client-id. Delegation argv has no runtime selector; bearer documents ENGRAM_TOKEN rather than treating token-file as a native credential input, per D-03 and D-18. Select a unique add Action by argv structure, not position, and render with Action.Command; use that same quoter for delegation display. Render both the fallback command table and delegation invocation table inside one setup-commands anchor pair. Add Write(path) using surfaces.WriteRegion and call it from existing surfacesgen.run after existing outputs; update the generator's source-description comment because it now consumes setup Plans as well as surfaces rules. No new generation entry point or command embedding is introduced (D-12, D-16).

Hand-author the routing prose around the generated region per D-01 through D-08 and D-10: preserve URL gathering and four auth choices; run command -v engram; present means preview the matching generated invocation, display every row including unsupported/failed rows, explain limitations and obtain confirmation before appending --apply. Show the result and stop on a nonzero exit without automatically retrying or falling through to prose. Do not infer success from preview exit zero. Absent means follow the generated Claude-only add command and retain /mcp OAuth completion plus removal/reconfiguration instructions. Keep one nonblocking brew install seanb4t/tap/engram pointer line. Apart from that pointer, the fallback describes registration only. Gather the non-secret client ID for oauth-client; require the user to make MCP_CLIENT_SECRET available to the scripted Claude invocation and ENGRAM_TOKEN available to the runtime for bearer. Neither ask the user to paste a secret into the conversation nor expand it into argv. Identify synthetic values as values to replace, never an executable apply example. Keep YAML frontmatter first, existing license exclusions, and human prose outside the generated region. First prove the none path through the real renderer and anchored file in a temporary fixture, then add the remaining three cases in the same coherent task.</action>
  <verify><automated>go test ./internal/setupgen -count=1 -v</automated></verify>
  <acceptance_criteria>All four registration rows compare exactly to real Plan add actions; the none tracer proves rendering plus anchored replacement; missing/ambiguous/failed Plan cases reject. task surfaces:gen updates the existing command file deterministically. Source review confirms preview/show/confirm/apply routing and the first-class Claude fallback; automated artifact tests do not claim to prove an agent obeys prose.</acceptance_criteria>
  <done>The published slash command has a working generated command table for both branches, with four delegated modes and preserved Claude-only fallback scope.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove generated delegation values reach the actual Cobra entry point</name>
  <files>cmd/engram/setup_delegation_test.go, internal/setupgen/setupgen.go, internal/setupgen/setupgen_test.go</files>
  <read_first>cmd/engram/setup.go; cmd/engram/setup_test.go; cmd/engram/clienttest_test.go; internal/setupgen/setupgen.go; internal/setupgen/setupgen_test.go; internal/setup/claudecode.go; internal/setup/codex.go; .planning/phases/05-slash-command-delegation/05-CONTEXT.md</read_first>
  <behavior>
    - Every generated Case.DelegationArgs parses through the actual setup command and yields the real Plan-derived registration command for the case's auth inputs.
    - Preview contains no apply flag or runtime selector; appending apply in the fake environment executes the same authored registration inputs.
    - OAuth-client carries the synthetic client ID; bearer preserves the literal environment reference without reading a credential.
    - A mutated unknown flag is rejected, demonstrating that conformance is tied to real Cobra definitions.
  </behavior>
  <action>Add TestSetupGeneratedInvocations in cmd/engram, importing setupgen (never importing the main CLI package into the generator). Run each exported synthetic preview argv through runClient using fake setup and skills environments and compare its parsed report command to the real runtime Plan for those same Options. Check generated option names against setupCmd's actual flag lookup and add an unknown-flag negative control. Append --apply only inside the fake invocation case and compare captured argv to the authored Plan actions. Assert preservation of the default detected runtime selection and unsupported rows, not third-party support for every pair. Cases and Render must share data; do not copy an independent table into the test. Per D-19 this demonstrates engram flag conformance without claiming Plan authors engram's flag names. Keep the independent behavior expectation on auth-to-ID wiring and preview/apply differences so using shared cases cannot make the test self-fulfilling.</action>
  <verify><automated>go test ./cmd/engram -run '^TestSetupGeneratedInvocations$' -count=1 -v</automated></verify>
  <acceptance_criteria>Verbose output proves four mode subtests plus unknown-flag control ran; preview reports and fake apply argv match real Plans. Generated preview arguments cannot scope away other detected runtimes or include mutation permission.</acceptance_criteria>
  <done>The generated handoff is checked against the actual CLI input surface and runtime plan output, with no real setup effects.</done>
</task>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Generated instructions to shell input | Rendered placeholders and environment references must remain correctly quoted. |
| Runtime report to agent decision | External text is report data, not an instruction to retry or bypass confirmation. |
| Renderer to local environment | Synthetic Plan environment isolates generation from the user's home and credentials. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-04 | Tampering | slash delegation prose | high | mitigate | Preview rows precede user confirmation; apply is never automatic; failures stop with reported rows. |
| T-05-05 | Information disclosure | renderer / fallback | high | mitigate | Reuse Action.Command and literal environment references; credentials are never read during generation or supplied as argv values. |
| T-05-06 | Spoofing | external runtime output | medium | mitigate | Treat output as data; a failed or unsupported row cannot trigger an automatic apply retry or fallback. |
| T-05-07 | Tampering | Plan-to-render mapping | high | mitigate | Require exactly one matching registration action; compare real argv and validate invocation flags against Cobra with a negative control. |

ASVS Level 1; high-severity findings block. No dependencies or schema changes.
</threat_model>

<verification>
Run go test ./internal/setupgen ./internal/setup ./cmd/engram -count=1 and task. Run task surfaces:gen explicitly, inspect the command diff, commit intended generated output, then rerun generation and require git diff --exit-code -- skill/engram/commands/engram-setup.md. Report authored prose review separately from automated byte/CLI evidence.
</verification>

<success_criteria>D-01 through D-12 and D-16 through D-19 are covered. Both entry points expose four auth choices; fallback argv is source-derived and delegation inputs reach the actual CLI. No installation docs or third-party runtime behavior gate is introduced.</success_criteria>

<output>Create .planning/phases/05-slash-command-delegation/05-02-SUMMARY.md.</output>
