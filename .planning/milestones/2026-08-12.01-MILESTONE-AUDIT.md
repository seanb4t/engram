---
milestone: 2026-08-12.01
milestone_name: Record State & Schema Evolution
audited: 2026-08-22T23:05:53Z
reaudited: 2026-08-22T23:05:53Z  # deterministic checks re-run; code tree unchanged since 62c39c22
status: tech_debt
supersedes_audit: 2026-08-22T18:05:00Z
scores:
  requirements: 27/27
  phases: 9/9
  integration: 5/5
  flows: 3/5
gaps: {}
nyquist:
  compliant_phases: ["01", "02", "03", "04", "05", "06", "07", "08", "09"]
  partial_phases: []
  not_validated_phases: []
  missing_phases: []
  overall: compliant_9_of_9
tech_debt:
  - phase: 04-migration-cli-first-customer
    items:
      - "W1 no full-stack E2E for `engram migrate` — `rg -i migrate internal/e2e/` returns ZERO hits; the suite covers boot/tool-surface, CLI exit codes, console browser rendering, spine-review and prune-expired, but the status -> apply -> status reconvergence path has no live-Qdrant coverage at all. Backlog 999.2."
  - phase: 08-registry-docs-tail
    items:
      - "W4 CLAUDE.md's \"every surface renders a record's derived state\" overstates — the MCP lane exposes raw fields (SupersededBy, ArchivedAt) but derives no state words. Backlog 999.3."
  - phase: cross-cutting
    items:
      - "schema_version typed three ways in one proto file: `schema_version` uint32 (proto:52) vs `version` int32 (proto:186) vs `current_version` int32 (proto:204). Backlog 999.4."
resolved_since_prior_audit:
  - "W2 `engram migrate status` omitted `pending` — closed by Phase 9."
  - "W3 `guides/migrate.md` documented a CLI derivation that did not exist — closed by Phase 9."
  - "dprint drift on 4 files — resolved; `task fmt:check` exits 0, `internal/webauth/static` added as a dprint exclude."
  - "staticcheck SA1019 deferred by 07-01/07-03 — resolved; deprecated ListMemoriesResponse.Approximate is gone, `task lint` exits 0 across 126 files."
---

# Milestone Audit: 2026-08-12.01 — Record State & Schema Evolution

Re-run after Phase 9 landed. Supersedes the 18:05 pass, which predated Phase 9
and scored `phases: 8/8`. Phase 9 existed specifically to close that audit's W2
and W3, so the refresh was needed to reflect their closure.

## Status: `tech_debt`

All 27 requirements satisfied. No critical blockers. Three debt items remain,
all already parked in backlog phases 999.2 / 999.3 / 999.4.

## Requirements — 27/27

Verified by a mechanical 3-source cross-reference, proven by set equality rather
than by counting:

| Source | Count |
|---|---|
| `REQUIREMENTS.md` traceability table rows | 27 |
| `REQUIREMENTS.md` body checkboxes (all `[x]`) | 27 |
| `SUMMARY.md` `requirements_completed` frontmatter, deduped | 27 |
| REQ-IDs appearing in phase `VERIFICATION.md` files | 27 |

`comm` reports an empty symmetric difference in both directions: zero orphans
(in traceability, absent from every VERIFICATION) and zero extras.

**Non-vacuity controls, both observed firing** — a passing gate is not evidence
unless it can fail:

- Injected `REQ-frobnicate-nonexistent` into the traceability set → correctly
  reported as an orphan.
- Dropped a known member (`REQ-backfill-shortids-first-step`) → correctly
  reported as an extra.

The `1a854afsj4` under-report (body checkboxes exceeding traceability rows, which
bit v0.13.x) does not occur here — all three counts agree.

Extraction note: `summary-extract --pick` concatenates list values with no
separator, so the REQ-ID regex uses a lowercase-only body class
(`REQ-[a-z0-9-]+`); the next token's uppercase `REQ` prefix then terminates the
match. A `[A-Za-z0-9_-]` body class silently merges `REQ-aREQ-b` into one token
(gotcha `6jj4dn31f8`).

## Phases — 9/9

Every phase carries a `VERIFICATION.md` at `status: passed`.

| Phase | Verification |
|---|---|
| 01 Gate & CI Integrity | passed |
| 02 Record Schema Versioning Foundation | passed |
| 03 Migration Foundation (Registry, Invariants & Sweep) | passed |
| 04 Migration CLI & First Customer | passed |
| 05 Connect Record-State Parity | passed |
| 06 Typed Operator Renderer | passed |
| 07 Console & CLI State Surfacing | passed |
| 08 Registry & Docs Tail | passed |
| 09 Report pending in migrate status | passed |

## Integration — 5/5

Re-derived independently by an integration checker instructed not to restate the
prior pass. Every seam resolved to WIRED with file:line evidence and passing
tests.

| Seam | Verdict | Evidence |
|---|---|---|
| schema_version stamping → registry → CLI (2→3→4) | WIRED | `store.go:646` stamps from the single pinned `migrate.CurrentVersion` (`migrate.go:54`, test-pinned at `migrate_test.go:19`); `registry.go:105` drives the sweep |
| `MigrateStatusResult.Pending()` as one shared definition (3/9→4→5) | WIRED | Defined once at `migrate_status.go:76`; three call sites — `connectapi.go:212`, `tools.go:519`, `migrate_family.go:340,363`. No re-derivation found anywhere |
| proto fields → typed renderer → CLI/console (5→6→7) | WIRED (CLI+console) | `memory_state.go:39` is the single Go-side derivation; `ui/src/lib/memorystate.ts:35` is a deliberately independent twin with matching precedence |
| additive-only/reversibility → `migrate revert` refusal (3→4) | WIRED | Genuine double preflight: `migrate_family.go:576-600` refuses before mutation, and `Store.Revert` refuses again mid-loop via typed `*store.RevertRefusedError`. Whole-range refusal pinned by `revert_test.go:102` and `:662` |
| registered scope rule → live cobra tree (8) | WIRED | Single `requireSweepScope` (`sweep_scope.go:28`) wrapping the registered rule, composed onto exactly three sweep leaves. The other 74 `usageErrorf` sites were read, not counted, and are unrelated flag/config validation |

## Flows — 3/5

Scored below the prior pass's 4/5. The checker's rationale, recorded as given:
W1 is not partial credit but zero live-stack coverage of this milestone's
flagship CLI verb, and flow 3's "every lane" claim is measurably false rather
than cosmetically incomplete.

| Flow | Verdict |
|---|---|
| `migrate status` → `--apply` → status, pending reaches 0 | **UNVERIFIABLE end-to-end.** `rg -i migrate internal/e2e/` returns zero hits — not partial coverage, none. The suite's 11 tests span boot/tool-surface, CLI exit codes, console browser rendering, spine-review and prune-expired. Unit-level proof of the arithmetic, wiring and CLI/doc parity exists; the live sweep-then-reconverge path does not. Confirms W1 still true post-Phase-9 |
| Pre-migration (schema_version absent) record stays recallable; version never gates recall | WIRED at unit level; UNVERIFIABLE live (no contradicting evidence found) |
| Archived/superseded/scheduled soft-hidden yet fetchable by id, on every lane | **PARTIAL.** Hide/fetch mechanics are wired and tested. "Every lane" holds for CLI and console; the MCP lane renders no state words, so an agent must infer state from raw timestamps and links |

## Nyquist Coverage

| Phase | VALIDATION.md | Compliant | Action |
|---|---|---|---|
| 01–09 | exists, `status: validated` | true | — |

**9/9 compliant.** Phase 09 initially read NOT-VALIDATED (`status: draft` —
`plan-phase` seeded the file and `validate-phase` had never reconciled it, so
`nyquist_compliant: false` was not a compliance verdict, #2117).
`/gsd-validate-phase 9` was run on 2026-08-22 and found **0 gaps**: every row of
its Per-Task Verification Map was re-run against the merged tree, all 9
top-level tests and all 15 named subtests green, quick run 1.111s. The file now
reads `status: validated`, `nyquist_compliant: true` (commit `41618986`).

## Tech Debt — 3 items

**Phase 04 — Migration CLI**
- **W1** no full-stack E2E for `engram migrate`. Backlog **999.2**.

**Phase 08 — Registry & Docs Tail**
- **W4** CLAUDE.md's "every surface" record-state claim overstates; the MCP lane
  derives no state words. Backlog **999.3**. Independently reproduced this pass.

**Cross-cutting**
- `schema_version` typed three ways in one proto file. Backlog **999.4**.
  Confirmed: `uint32` at proto:52 vs `int32` at proto:186 and proto:204.

### Resolved since the 18:05 pass

- **W2** `engram migrate status` omitted `pending` — closed by Phase 9.
- **W3** `guides/migrate.md` documented a non-existent CLI derivation — closed by
  Phase 9 behind a self-tested zero-occurrence docs gate.
- **dprint drift**, 4 files — `task fmt:check` now exits 0.
- **staticcheck SA1019** deferred by 07-01/07-03 — `task lint` now exits 0 across
  126 files; the deprecated `ListMemoriesResponse.Approximate` is gone.

### Requirement notes

`REQ-operator-renderer-typed` and `REQ-migration-state-visible` are **satisfied**
by the 3-source matrix (VERIFICATION passed + SUMMARY listed + traceability
`[x]`). The checker separately marks their *integration reach* PARTIAL because
neither has an MCP consumer. That is the W4 surface gap, already tracked as
999.3 — not a requirement shortfall.

## Quality gates at audit time

`task lint` exits 0 (126 files, "All checks passed", 0 issues).
`task fmt:check` exits 0.

## Re-run note

This audit was re-run after `/gsd-validate-phase 9`. Every deterministic check was
recomputed from scratch — phase scope, all 9 `VERIFICATION.md` statuses, all 9
`VALIDATION.md` statuses, the full 3-source requirements cross-reference with both
non-vacuity controls, both quality gates, and each of the three open debt items.
All results were unchanged except Nyquist, which advanced to 9/9.

The integration and flow verdicts are carried forward rather than re-derived: every
commit since the checker read the tree (`80e25a01`, `41618986`, `3c99d917`) touches
`.planning/` only, so `git diff 62c39c22..HEAD -- . ':!.planning/'` is empty and the
analyzed code is byte-identical. Re-spawning the checker against an unchanged tree
could not change its verdict.

---

*For current project status, see .planning/ROADMAP.md*
