---
phase: 04-drift-detection-read-only
plan: 05
type: execute
wave: 3
depends_on:
  - "04-01"
  - "04-02"
  - "04-04"
files_modified:
  - internal/setup/claudecode.go
  - internal/setup/claudecode_test.go
  - internal/setup/codex.go
  - internal/setup/codex_test.go
  - internal/setup/drift_test.go
  - cmd/engram/setup_test.go
autonomous: true
requirements:
  - REQ-drift-observed-registration
  - REQ-drift-three-way
  - REQ-drift-redaction
  - REQ-drift-facet-naming
  - REQ-drift-preserved-outcome

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Claude Code implements `DriftRuntime` in its own file: `Observe` is a TOTAL parse of `claude mcp get engram`'s combined stdout+stderr in which every non-blank line lands in exactly one class — the entry-name line, a `Scope:` line, a `Status:` block (its prefix is chrome regardless of what follows, so an unreachable server never yields spurious `preserved` — D-11 / 04-RESEARCH.md Pitfall 4), a `Type:` line (its value must be the transport engram authors, `http`, compared case-insensitively; anything else is unrecognized content), the `URL:` line (the framing anchor: without it `ok == false`, D-09), the `Headers:` line and its indented `Name: value` lines, and any trailing hint line the observation record shows — with every leftover line reported as unrecognized content by its LABEL only (the text before its first colon, bounded to 40 bytes, quoteWord'ed; the fixed token `line` when it has no colon), never its remainder (D-11, REQ-drift-observed-registration)"
    - "A Claude Code header line `Name: value` is split at its first `: `; the raw value is compared in a `rawHeader` local against the reference this file's own `Plan()` would write for that name — `${ENVVAR}` for an extra header, `Bearer ${ENGRAM_TOKEN}` for `Authorization` (`claudeCodeBearerForm`, the SAME literal `Plan()`'s bearer arm uses) — and is never assigned to any field that survives the call; an `Authorization` header equal to the bearer form is `AuthBearer`, any other `Authorization` value is `AuthForeign`, absence is `AuthNone`; a second observed header with an already-matched name is unplanned (edge probe adjacency: duplicates collide into preserved, never merge)"
    - "Claude Code reaches all three states through `Preview` end-to-end with fixtures built from `04-OBSERVATIONS.md` and cited in a header comment (D-08): `already-correct` (URL, auth, header set all match), `would-write` (`url`; `auth-mode` in both directions; `header-value-ref` — observed `${OLD_VAR}` vs planned `${GATEWAY_KEY}`; `header-name` — a planned extra absent), and `preserved` (an unplanned header carrying the OBSERVED literal echo shape; an unrecognized label; a foreign `Authorization` when oauth is planned) — SC2's per-runtime three-state coverage for the second parsed runtime"
    - "The literal dummy value from `04-OBSERVATIONS.md` (`sk-DO-NOT-COMMIT-literal-test-abc123`), planted verbatim in the OBSERVED positions for both runtimes, appears nowhere in `json.Marshal(Result)`, in `Registered`, `Reason`, `Drift`, `Facets`, or `Notes`, nor in `engram setup`'s stdout/stderr in either output lane — the SC5 fixture proof on the observed shape (REQ-drift-redaction)"
    - "Codex's mirror struct matches the OBSERVED literal-header shape: if `04-OBSERVATIONS.md` shows `http_headers`/`env_http_headers` as an object of strings, the 04-01 map typing stands and the observed document is the fixture; if it shows another shape, the mirror is corrected to decode exactly that shape (values still discarded at the decode boundary) and the 04-01 `ASSUMED SHAPE (A3)` subtest is replaced by the observed one; either way an entry carrying a literal custom header reads `preserved` naming the header, with the codex whole-entry sentence in `Reason`"
    - "A `claude mcp get` not-found output (the post-remove capture in `04-OBSERVATIONS.md`), an empty output, and output with no `URL:` line all yield `ok == false` → `would-write` with `Registered` empty and `Drift` beginning `claude-code: not compared:` (D-09); the failed-dial exit code the record shows does NOT prevent parsing — `execute()` parses regardless of exit code (04-01), so an unreachable server with a matching registration still reads `already-correct`"
    - "`opencode.go` and `generic.go` still author no `Observe`; `TestDriftRuntimeIsOptional` now asserts `ClaudeCode.(DriftRuntime)` is true beside codex, and opencode/generic false (D-10 restated at the end of the phase); no test invokes a real CLI or touches `$HOME` (rule m45p2b4bp7); `internal/setup` stays stdlib-only; `go.mod`/`go.sum` unchanged"
  artifacts:
    - path: "internal/setup/claudecode.go"
      provides: "claudeCodeBearerForm, claudeCodeWholeEntryNote, claudeCodeLabel helpers, claudeCodeRuntime.Observe (total parse of claude mcp get)"
      contains: "func (claudeCodeRuntime) Observe(probeOutput string, opts Options) (Observation, bool)"
    - path: "internal/setup/claudecode_test.go"
      provides: "TestObserveClaudeCodeRegistration (three states, chrome, framing, duplicates, literal echo citing 04-OBSERVATIONS.md), TestPreviewClassifiesRegistration claude-code subtests"
      contains: "04-OBSERVATIONS.md"
    - path: "internal/setup/codex_test.go"
      provides: "TestObserveCodexRegistration/preserved-literal-header-observed citing 04-OBSERVATIONS.md"
      contains: "04-OBSERVATIONS.md"
    - path: "internal/setup/drift_test.go"
      provides: "TestRedactionUnconditional claude-code-observed-literal and codex-observed-literal subtests; TestDriftRuntimeIsOptional ClaudeCode assertion"
      contains: "claude-code-observed-literal"
    - path: "cmd/engram/setup_test.go"
      provides: "TestSetupJSONNeverLeaksProbeLiteral claude-code-observed-shape and codex-observed-shape subtests"
      contains: "claude-code-observed-shape"
  key_links:
    - from: "internal/setup/claudecode.go"
      to: "internal/setup/drift.go"
      via: "claude-code authors its own registration scanner in its own file (AUTHORED-HERE), producing the shared Observation type"
      pattern: 'func [(]claudeCodeRuntime[)] Observe[(]probeOutput string, opts Options[)] [(]Observation, bool[)]'
    - from: "internal/setup/claudecode.go"
      to: "internal/setup/drift.go"
      via: "header values are compared through the shared transient join, never stored"
      pattern: 'joinHeaders[(]'
    - from: "internal/setup/claudecode.go"
      to: "internal/setup/claudecode.go"
      via: "the bearer form compared against is the SAME literal Plan()'s bearer arm authors — one constant, two uses, no drift"
      pattern: 'claudeCodeBearerForm'
    - from: "internal/setup/claudecode_test.go"
      to: ".planning/phases/04-drift-detection-read-only/04-OBSERVATIONS.md"
      via: "every literal-echo fixture cites the dated observation record in a header comment (D-08)"
      pattern: '04-OBSERVATIONS[.]md'
  prohibitions:
    - statement: "MUST NOT retain, render, marshal, or log a header value read from `claude mcp get` or `codex mcp get --json`: the raw value lives in a rawHeader local inside Observe and in joinHeaders' argument only; the observed literal dummy is asserted absent from every rendered field and from the CLI's output streams (D-02/D-03)"
      category: safety
      status: resolved
      verification: test
      reason: "TestRedactionUnconditional's observed-literal subtests and TestSetupJSONNeverLeaksProbeLiteral's observed-shape subtests assert absence; the acceptance grep holds claudecode.go's Observe to zero assignments of a header value into Observation — no wired check descriptor by design (spec-less fallback, flagged-unverified)"
    - statement: "MUST NOT build a scanner fixture for the literal-echo case from an assumed shape: every such fixture quotes `04-OBSERVATIONS.md` and cites it in a header comment; if the record was marked `Not observed`, the literal-echo fixtures are deferred with a recorded blocker, never invented (D-05, D-08)"
      category: safety
      status: resolved
      verification: test
      reason: "Task 1's precondition checks the record's sections; the acceptance greps require the citation in both test files — no wired check descriptor by design (spec-less fallback, flagged-unverified)"
    - statement: "MUST NOT treat the `Status:` line's content as a facet, and MUST NOT let a nonzero probe exit alone prevent parsing: connection state is chrome, so an unreachable engram server never produces a spurious `preserved` or blocks a legitimate `already-correct` (D-11, Pitfall 4)"
      category: safety
      status: resolved
      verification: test
      reason: "TestObserveClaudeCodeRegistration's status-connected/status-failed subtests and the failed-dial-exit-code Preview subtest pin it — no wired check descriptor by design (spec-less fallback, flagged-unverified)"
    - statement: "MUST NOT invoke a real claude/codex/opencode binary or touch the operator's $HOME from any test (rule m45p2b4bp7); MUST NOT author an Observe for opencode or generic (D-10)"
      category: safety
      status: resolved
      verification: test
      reason: "Every fixture is a scripted string; the acceptance grep holds opencode.go/generic.go to zero Observe methods — no wired check descriptor by design (spec-less fallback, flagged-unverified)"
---

<objective>
Wire the second parsed runtime and close the observation-gated half of the phase: Claude Code's
`Observe` — a total parse of `claude mcp get engram`'s fixed-label text (D-11) built from the
verbatim framing `04-OBSERVATIONS.md` recorded — with three-state coverage, the `Status:`-line
chrome rule that keeps an unreachable server from reading `preserved`, and the literal-echo
fixtures for BOTH runtimes quoted from the record and cited in header comments (D-08), proven
leak-free from `internal/setup` through `engram setup`'s output streams (SC5).

Purpose: REQ-drift-three-way's "unit coverage of all three states per runtime" needs Claude Code
beside Codex; REQ-drift-redaction's proof must use "a fixture whose probe output carries a literal
value" from observed reality (Pitfall 2), not an assumed shape; REQ-drift-observed-registration
needs the bounded text scan for Claude Code. This plan is wave 3 because it consumes 04-01's
vocabulary, 04-02's record, and 04-04's CLI-boundary test.

Decision (Claude's discretion): `Scope:` is recognized chrome, not a facet — `claude mcp get`
resolves scope precedence itself and engram's write is always user scope; a project-scoped entry
is never destroyed by a user-scope write, so comparing scope would add a facet without a safety
gain. `Type:` IS a facet-bearing line (a transport engram does not set is unaccounted content,
D-01).

Output: `claudeCodeBearerForm`, `claudeCodeWholeEntryNote`, `claudeCodeRuntime.Observe`, the
Claude Code test table, the observed-shape fixtures for codex, and the extended redaction proofs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-drift-detection-read-only/04-CONTEXT.md
@.planning/phases/04-drift-detection-read-only/04-RESEARCH.md
@.planning/phases/04-drift-detection-read-only/04-OBSERVATIONS.md
@.planning/phases/04-drift-detection-read-only/04-01-SUMMARY.md
@.planning/phases/04-drift-detection-read-only/04-02-SUMMARY.md
@.planning/phases/04-drift-detection-read-only/04-04-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Contracts CONSUMED from plan 04-01 — implement against these verbatim (the 04-01 SUMMARY wins if it recorded a deviation). -->

From internal/setup/drift.go (04-01):
```go
type Observation struct { URL string; Auth AuthState; BearerForm string; Headers []ObservedHeader; Unrecognized []string; WholeEntryNote string }
type DriftRuntime interface { Observe(probeOutput string, opts Options) (Observation, bool) }
type rawHeader struct { Name, Value string }     // transient
type plannedHeader struct { Name, Value string }
func joinHeaders(observed []rawHeader, planned []plannedHeader) []ObservedHeader
const redactedValue = "<redacted>"
// AuthNone / AuthBearer / AuthForeign; HeaderMatches / HeaderDiffers / HeaderMissing / HeaderUnplanned
```
From internal/setup/claudecode.go (existing): `claudeCodeHeaderArgs` renders `NAME: ${ENVVAR}`; `Plan()`'s bearer arm authors `"Authorization: Bearer ${ENGRAM_TOKEN}"`; every Plan carries `Probe: []string{"claude", "mcp", "get", "engram"}`.
From cmd/engram/setup_test.go (04-04): `scriptedSetupRun(t, mcpGet)`, `TestSetupJSONNeverLeaksProbeLiteral`, `rowByName`.
</interfaces>
</context>

## Artifacts this phase produces (plan 04-05's share)

| Kind | Artifact | Created by |
|------|----------|------------|
| Go consts | `claudeCodeBearerForm = "Bearer ${ENGRAM_TOKEN}"`, `claudeCodeWholeEntryNote = "claude mcp remove then add replaces the whole entry: --apply would overwrite it or leave it untouched, never merge into it"` in `internal/setup/claudecode.go` | 04-05 Task 1 |
| Go func | `claudeCodeLineLabel(line string) string` (the D-11 unrecognized-label helper) and `func (claudeCodeRuntime) Observe(probeOutput string, opts Options) (Observation, bool)` | 04-05 Task 1 |
| Test funcs | `TestObserveClaudeCodeRegistration`, `TestPreviewClassifiesRegistration/claude-code/*` (in `claudecode_test.go`) | 04-05 Task 1 |
| Go edits | `codex.go` mirror reconciled to the observed literal-header shape (only if the record differs from A3) | 04-05 Task 2 |
| Test funcs | `TestObserveCodexRegistration/preserved-literal-header-observed`, `TestRedactionUnconditional/{claude-code-observed-literal,codex-observed-literal}`, `TestDriftRuntimeIsOptional` ClaudeCode assertion | 04-05 Task 2 |
| Test funcs | `TestSetupJSONNeverLeaksProbeLiteral/{claude-code-observed-shape,codex-observed-shape}` | 04-05 Task 3 |

## Flagged Assumptions (edge probe, spec-less fallback)

This plan flags **1** of the phase's 13 edge-probe rows (04-01 authored 10 and flagged 1; 04-04
authored 1):

| Requirement | Probe category | Disposition |
|---|---|---|
| REQ-drift-redaction | `unclassified` | **Flagged assumption (unresolved — kept visible).** Redaction is by construction (values never leave the scan frame) and is proven against the OBSERVED literal positions (`Headers:` block; the codex header key the record shows). What stays an assumption is that a runtime echoes a header value ONLY in those positions: a CLI that echoed a value elsewhere would meet D-11's totality (an unrecognized line contributes its LABEL only; an unknown JSON key contributes its NAME only), which bounds but does not eliminate the residual for a value placed inside a recognized chrome line's remainder (e.g. a `Status:` message quoting the header). The `Status:` remainder is never rendered, so that residual is closed for the one chrome line whose content varies. Recorded per the spec-less fallback rules; not auto-resolved. |

Recorded consequence (not a probe row): the `oauth-client` read-back shape stays unobserved (D-07,
04-01's flagged assumption); an oauth-client Claude Code entry whose `mcp get` prints an extra
label reads `preserved` naming that label — the accepted D-11 posture.

## Prohibition probe (spec-less fallback, in-prompt recall)

Stage 1 raw candidates: must not retain a header value; must not invent the literal-echo shape;
must not treat Status content as a facet; must not let a nonzero exit block parsing; must not
author an opencode/generic scanner; must not run a real CLI; must not compare scope; must not
share a parser with codex; must not string-match the Status text to decide an outcome; must not
name a vendor in fixtures. Stage 2 keeps four (authored above, descriptor-less): the
**never-retain-or-render-a-probe-value** privacy prohibition, the **fixtures-from-observed-reality**
provenance prohibition (D-05/D-08), the **Status-is-chrome / exit-code-never-blocks-parsing**
safety prohibition (Pitfall 4), and the **no-real-CLI / no-opencode-scanner** prohibition. The rest
are pinned by acceptance criteria (no shared parser: `rg` for a `claudeCodeRuntime` reference in
`codex.go` prints nothing). Canon breadcrumb: none beyond 04-01's.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Claude Code `Observe` — a total parse of `claude mcp get engram` built from the observation record, with three-state coverage and the Status-is-chrome rule</name>
  <precondition>`.planning/phases/04-drift-detection-read-only/04-OBSERVATIONS.md` exists, contains a `## Claude Code — literal value` section with a fenced verbatim capture that includes a `Headers:` line, a `## What this pins` section, and does NOT contain a `## Not observed` heading (read-only check: `rg -c`); if it is absent or marked not observed, halt — the literal-echo fixtures cannot be built honestly (D-05/D-08)</precondition>
  <files>internal/setup/claudecode.go, internal/setup/claudecode_test.go</files>
  <read_first>
    - `.planning/phases/04-drift-detection-read-only/04-OBSERVATIONS.md` whole file — the verbatim Claude Code captures (literal value, bare-reference control, post-remove not-found) and the `## What this pins` bullets: the ordered label set, the exact failed-dial `Status:` line(s), the failed-dial exit code, whether the literal appears verbatim inside `Headers:`. EVERY line class the scanner recognizes must be justified by a line in this record.
    - `internal/setup/claudecode.go` lines 64-92 (`claudeCodeHeaderArgs` — the `NAME: ${ENVVAR}` rendering the scanner's planned side reproduces; `claudeCodeRemoveAction`), 94-207 (`Plan()` — the bearer arm's `"Authorization: Bearer ${ENGRAM_TOKEN}"` literal to hoist into `claudeCodeBearerForm`; the probe line), 280-304 (`ParseMarketplaceList` — the `strings.Split` + line-prefix idiom).
    - `internal/setup/drift.go` (04-01) — `Observation`, `rawHeader`/`plannedHeader`/`joinHeaders`, `AuthState`, the 40-byte label bounding + `quoteWord` convention codex used for `Unrecognized`.
    - `internal/setup/codex.go` (04-01) `Observe` — the sibling's structure (framing → auth → headers → unrecognized) to mirror in SHAPE only; share no code with it (Pitfall 3).
    - `internal/setup/drift_test.go` (04-01) — `TestPreviewClassifiesRegistration`'s codex subtests (copy the assertion shape for claude-code), `TestRedactionUnconditional`.
    - `internal/setup/apply_test.go` lines 406-470 (`TestApplyConvergesClaudeCode` — the four-result scripted shape; preview needs only ONE scripted result).
    - `.planning/phases/04-drift-detection-read-only/04-CONTEXT.md` D-01, D-02, D-09, D-11, D-12 and §"Claude's Discretion" (the network-sensitivity item); 04-RESEARCH.md Pitfall 4.
  </read_first>
  <behavior>
    Write `TestObserveClaudeCodeRegistration` in `claudecode_test.go` FIRST; it references
    `ClaudeCode.(DriftRuntime)` and `claudeCodeWholeEntryNote` — the type assertion compiles but
    `ok` is false and the const is undefined, so the first run fails to compile (RED; record it).

    Fixture builder `claudeGetFixture(headers []string, status string) string` in
    `claudecode_test.go`: reproduces the EXACT framing `04-OBSERVATIONS.md` recorded for the
    `engram` entry — the name line, `Scope:` line, the `Status:` line (parameterized: the observed
    failed-dial text, or the observed/plausible connected text), `Type:` line, `URL:` line, and a
    `Headers:` block with one indented `Name: value` line per element (indentation copied from the
    record), plus any trailing hint line the record shows — with a header comment: `// Shape:
    .planning/phases/04-drift-detection-read-only/04-OBSERVATIONS.md §"Claude Code — literal
    value" (claude <version from the record>, <date>)`. Never a shape the record does not show.

    Subtests, each calling `ClaudeCode.(DriftRuntime).Observe(stdout, opts)` with opts `{URL:
    "https://engram.example.com/mcp", Auth: "bearer", Headers: [{x-gateway-api-key, GATEWAY_KEY}]}`
    unless stated, asserting the full `Observation` literally:
    - `already-correct-bearer`: headers `Authorization: Bearer ${ENGRAM_TOKEN}`, `x-gateway-api-key:
      ${GATEWAY_KEY}`, connected status → `ok`, `URL` matches, `Auth == AuthBearer`, `Headers ==
      [{x-gateway-api-key, HeaderMatches, "${GATEWAY_KEY}"}]`, `len(Unrecognized) == 0`,
      `BearerForm == "Bearer ${ENGRAM_TOKEN}"`, `WholeEntryNote == claudeCodeWholeEntryNote`.
    - `status-failed-dial-is-chrome`: same headers, the RECORD's failed-dial `Status:` text → an
      identical Observation (`reflect.DeepEqual` with the previous case's).
    - `oauth-shape`: no Authorization line, opts oauth, no extras → `AuthNone`, no headers.
    - `header-value-ref-differs`: `x-gateway-api-key: ${OLD_KEY}` → `[{x-gateway-api-key,
      HeaderDiffers, "${GATEWAY_KEY}"}]`.
    - `header-missing`: no extras line, opts with one header → `[{x-gateway-api-key, HeaderMissing,
      "${GATEWAY_KEY}"}]`.
    - `unplanned-header-literal-observed`: the RECORD's literal line verbatim (`x-litellm-api-key`
      as the record wrote it is the ONE place that vendor name may appear in a fixture — it quotes the
      observation; cite the record) with opts carrying no extras → `[{<observed name>,
      HeaderUnplanned, ""}]`, and the dummy literal absent from every Observation field (walk the
      struct via `json.Marshal` and `strings.Contains`).
    - `unplanned-header-reference-observed`: the bare-reference control from the record →
      byte-identical Observation to the literal case (D-02).
    - `foreign-authorization`: `Authorization: Bearer sk-DO-NOT-COMMIT-literal-test-abc123` with opts
      oauth → `Auth == AuthForeign`, value absent from the Observation.
    - `duplicate-header-names`: two `x-gateway-api-key:` lines (one `${GATEWAY_KEY}`, one `${OTHER}`)
      → first Matches, second Unplanned (adjacency).
    - `case-insensitive-name`: `X-Gateway-Api-Key: ${GATEWAY_KEY}` → Matches (EqualFold).
    - `type-not-http`: the `Type:` line's value replaced by `sse` → `Unrecognized == ["Type"]`.
    - `unrecognized-label`: an extra line `Proxy: http://p` inside the facet region → `["Proxy"]`;
      `unrecognized-unlabeled-line`: an extra line `something odd` → `["line"]`.
    - `not-found-after-remove`: the RECORD's post-remove output → `ok == false`; `empty` → false;
      `no-url-line` → false.
    Then `TestPreviewClassifiesRegistration` gains `claude-code/` subtests (in `claudecode_test.go`,
    driving `Preview` with ONE scripted Run result — exit code set to the RECORD's failed-dial exit
    code so the test proves parsing survives it): `already-correct` (Outcome already-correct,
    `Facets == ""`, `Registered == "url=https://engram.example.com/mcp auth=bearer
    headers=x-gateway-api-key=<redacted>"` — `Authorization` is folded into `auth=bearer`, never
    listed under `headers=`);
    `would-write-url`; `would-write-auth-mode` (opts oauth vs observed bearer → `Facets ==
    "auth-mode"`, `Drift == "auth-mode: observed bearer, would write oauth"`);
    `would-write-header-value-ref` (`Drift == "x-gateway-api-key: observed <redacted>, would write
    ${GATEWAY_KEY}"` — the D-02 endorsed shape); `preserved-unplanned-literal` (the record's line →
    preserved, `Facets == "header-name"`, `Reason == "claude-code: preserved: <name>: observed
    <redacted>, not authored by setup; " + claudeCodeWholeEntryNote`, dummy absent from
    `json.Marshal(res)`); `preserved-url-and-unplanned` (`Facets == "url,header-name"`, D-01);
    `not-found-would-write` (`Drift` begins `claude-code: not compared:`). Each asserts exactly one
    Run call with args `["mcp","get","engram"]`.
  </behavior>
  <action>
In `internal/setup/claudecode.go`: hoist `claudeCodeBearerForm = "Bearer ${ENGRAM_TOKEN}"` and
use it in `Plan()`'s bearer arm (`"Authorization: " + claudeCodeBearerForm`) so the compared form
and the authored form are one literal; add `claudeCodeWholeEntryNote = "claude mcp remove then add
replaces the whole entry: --apply would overwrite it or leave it untouched, never merge into it"`.
Implement `func (claudeCodeRuntime) Observe(probeOutput string, opts Options) (Observation, bool)`
as a TOTAL parse (D-11) over `strings.Split(probeOutput, "\n")`: (1) framing — locate the `URL:`
line (trimmed prefix match); none → `return Observation{}, false` (covers not-found, empty,
garbage — D-09); (2) classify EVERY other non-blank line by trimmed prefix into exactly one class,
each class justified by a line in `04-OBSERVATIONS.md`: the entry-name line (the record's first
line shape — e.g. `<name>:` — matched by SHAPE, never by the literal name `engram`, so the
scanner is name-agnostic); `Scope:` → chrome; `Status:` → chrome by PREFIX, plus any
continuation line(s) the record shows belonging to the status block (recognized by the exact
indentation/glyph shape the record shows — never by their text); `Type:` → facet: value
`strings.EqualFold` to `http` is accounted for, else unrecognized `Type`; `Headers:` → begins the
header block: every following line indented deeper than the `Headers:` label (or until the next
label-shaped line at the label's indentation, whichever the record shows) is a header line split
at its first `": "` into name and raw value; a trailing hint line → chrome only if the record
shows one (match its fixed prefix); every other non-blank line → `claudeCodeLineLabel(line)`
appended to unrecognized. `claudeCodeLineLabel`: text before the first `:` trimmed, bounded to 40
bytes at a rune boundary, `quoteWord`ed; the fixed token `line` when there is no colon — the
remainder after the colon is NEVER copied. (3) auth: an `Authorization` header (EqualFold) is
folded into `Auth` — value `== claudeCodeBearerForm` → AuthBearer, else AuthForeign; absent →
AuthNone; it is NOT added to the raw header list. (4) headers: build a local `[]rawHeader` from
the remaining header lines and `planned := []plannedHeader{{Name: h.Name, Value: "${" + h.EnvVar +
"}"}}` over `sortedHeaders(opts.Headers)` (the same rendering `claudeCodeHeaderArgs` authors —
state in the doc comment that the two must stay in lockstep and reference the helper by name);
`Headers = joinHeaders(observed, planned)`; the raw slice goes out of scope here. (5) return
`Observation{URL: <the URL: line's trimmed value>, Auth, BearerForm: claudeCodeBearerForm, Headers,
Unrecognized, WholeEntryNote: claudeCodeWholeEntryNote}, true`. Doc-comment the method: D-09,
D-11 (cosmetic output changes may cause spurious preserved until the scanner learns the shape —
accepted), D-12, Pitfall 4 (Status is chrome; the probe dials the URL), the AUTHORED-HERE rule
(no code shared with codex's JSON scan), and the record it was built from
(`04-OBSERVATIONS.md`, CLI version, date).

Write the tests per `<behavior>`. Commit as `feat(setup): claude-code observes its registration
with a total parse of claude mcp get` with the explicit pathspec `internal/setup/claudecode.go
internal/setup/claudecode_test.go`.
  </action>
  <verify>
    <automated>out=$(go test ./internal/setup/ -run '^(TestObserveClaudeCodeRegistration|TestPreviewClassifiesRegistration|TestClaudeCodePlan|TestNoSecretInArgs|TestApplyConvergesClaudeCode|TestSetupPackageIsStdlibOnlyLeaf)$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^(--- |    --- )(PASS|FAIL)' | tail -n 60; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -c -F -e '--- PASS: TestObserveClaudeCodeRegistration/')" -ge 15 &amp;&amp; test "$(printf '%s\n' "$out" | rg -c -F -e '--- PASS: TestPreviewClassifiesRegistration/claude-code/')" -ge 7 &amp;&amp; printf '%s\n' "$out" | rg -q -F -e '--- PASS: TestApplyConvergesClaudeCode' &amp;&amp; printf '%s\n' "$out" | rg -q -F -e '--- PASS: TestSetupPackageIsStdlibOnlyLeaf' &amp;&amp; rg -q -F -e '04-OBSERVATIONS.md' internal/setup/claudecode_test.go</automated>
    <fails_when>non-zero `go test` exit, a `--- FAIL:` line, the line `testing: warning: no tests to run`, fewer than 15 `TestObserveClaudeCodeRegistration/` or 7 `TestPreviewClassifiesRegistration/claude-code/` PASS lines, no PASS line for `TestApplyConvergesClaudeCode` (the mutate lane regressed) or `TestSetupPackageIsStdlibOnlyLeaf`, or no citation of `04-OBSERVATIONS.md` in `claudecode_test.go` (D-08 provenance missing)</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n -F 'func (claudeCodeRuntime) Observe(probeOutput string, opts Options) (Observation, bool)' internal/setup/claudecode.go` prints exactly one line; `rg -c -F 'claudeCodeBearerForm' internal/setup/claudecode.go` prints at least `3`; `rg -n -F 'claudeCodeWholeEntryNote' internal/setup/claudecode.go` prints at least two lines; `rg -n -F 'joinHeaders(' internal/setup/claudecode.go` prints exactly one line; `rg -n -F 'func claudeCodeLineLabel(line string) string' internal/setup/claudecode.go` prints exactly one line.
    - `awk '/claudeCodeRuntime[)] Observe[(]/,/^}/' internal/setup/claudecode.go | rg -n -F '"engram"'` prints nothing (region-scoped to `Observe`'s body: the name line is matched by shape, not by the literal name).
    - `rg -n -F 'codexRuntime' internal/setup/claudecode.go` prints nothing and `rg -n -F 'claudeCodeRuntime' internal/setup/codex.go` prints nothing (no shared parser).
    - `rg -c -F '04-OBSERVATIONS.md' internal/setup/claudecode_test.go` prints at least `2`; `rg -n -F 'sk-DO-NOT-COMMIT-literal-test-abc123' internal/setup/claudecode_test.go` prints at least two lines; `rg -n -e 'status-failed-dial-is-chrome|not-found-after-remove|duplicate-header-names|type-not-http' internal/setup/claudecode_test.go` prints four lines.
    - RED observed (record in SUMMARY): with the test file edited and `claudecode.go` at HEAD, `go test ./internal/setup/ -run '^TestObserveClaudeCodeRegistration$' -count=1` fails to compile (`claudeCodeWholeEntryNote` undefined) and, after stubbing only that const, fails at runtime on `ok == false` for every subtest (ClaudeCode does not yet satisfy DriftRuntime); GREEN after the production edit.
    - `go test ./internal/setup/ -count=1` exits 0; `gofmt -l internal/setup/` prints nothing.
  </acceptance_criteria>
  <done>Claude Code observes its registration through a total parse built from the record, reaches all three states through `Preview`, treats connection status as chrome and a failed-dial exit as parseable, and its literal-echo fixtures quote and cite `04-OBSERVATIONS.md`; committed with an explicit pathspec.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Codex's observed literal-header shape reconciles the mirror; the redaction proof covers both runtimes' observed shapes; ClaudeCode joins the optional-interface assertion</name>
  <precondition>`.planning/phases/04-drift-detection-read-only/04-OBSERVATIONS.md` contains a `## Codex — literal header (hand-edited)` section with a fenced verbatim `--json` capture (read-only `rg -c` check); absent → halt, the codex literal fixture cannot be built honestly</precondition>
  <files>internal/setup/codex.go, internal/setup/codex_test.go, internal/setup/drift_test.go</files>
  <read_first>
    - `.planning/phases/04-drift-detection-read-only/04-OBSERVATIONS.md` §"Codex — literal header (hand-edited)" and §"What this pins" (which key carried the literal, its JSON shape, any new key).
    - `internal/setup/codex.go` (04-01) `codexRegistrationDoc`/`codexRegistrationTransport` and `Observe` — the mirror to reconcile; `internal/setup/codex_test.go` (04-01) `TestObserveCodexRegistration`'s `http-headers-assumed-shape` subtest (marked `ASSUMED SHAPE (A3)`).
    - `internal/setup/drift_test.go` (04-01) `TestRedactionUnconditional`, `TestDriftRuntimeIsOptional`.
    - `internal/setup/claudecode_test.go` (Task 1) — `claudeGetFixture` and the literal line, reused for the drift_test.go subtest via a package-level helper (same package).
  </read_first>
  <behavior>
    - `TestObserveCodexRegistration/preserved-literal-header-observed` (codex_test.go): the RECORD's
      verbatim codex `--json` capture, with `"name": "probe-literal-04"` rewritten to `"engram"` (the
      only edit; state it in the comment) and a header comment citing the record section, CLI
      version, and date → `ok == true`, `Headers` contains `{<observed header name>,
      HeaderUnplanned, ""}`, `Auth` per the record's `bearer_token_env_var` (`DUMMY_04` → AuthForeign),
      the dummy literal absent from the Observation. If the record shows the literal under a key the
      04-01 mirror does not model, this subtest is RED (unrecognized-content instead of a header
      name) until the mirror is corrected; if the record's shape IS the A3 map, this subtest is GREEN
      on first run — record which, and in either case REPLACE the `http-headers-assumed-shape`
      subtest with this observed one (delete the ASSUMED marker).
    - `TestRedactionUnconditional/claude-code-observed-literal` and `/codex-observed-literal`
      (drift_test.go): drive `Preview` end-to-end for each runtime with the observed literal fixture
      (Claude Code: `claudeGetFixture` with the record's literal line; Codex: the observed capture)
      and opts `{URL: "https://engram.example.com/mcp", Auth: "oauth"}` → `Outcome ==
      OutcomePreserved`, and `sk-DO-NOT-COMMIT-literal-test-abc123` absent from `json.Marshal(res)`
      and from each of `Registered`/`Reason`/`Drift`/`Facets`/`Notes`; both cite the record.
    - `TestDriftRuntimeIsOptional`: add `_, ok := ClaudeCode.(DriftRuntime)` → true (beside codex);
      opencode/generic remain false.
  </behavior>
  <action>
Reconcile `internal/setup/codex.go`'s mirror against the record: if `04-OBSERVATIONS.md` shows the
literal header under `http_headers`/`env_http_headers` as an object of strings, keep the 04-01
typing and only update the struct's doc comment to cite the record (drop the "ASSUMPTION A3"
wording); if it shows a different shape (an array of pairs, a nested object, a different key),
retype exactly that field to decode the observed shape — still copying only header NAMES into
`rawHeader` and discarding values at the decode boundary — and add any newly observed key to the
mirror and to `codexKnownTransportKeys`/`codexKnownTopKeys` ONLY if the record shows engram's own
`mcp add` producing it (a key only a hand edit produces stays unknown → preserved, D-11). Record
the reconciliation outcome in the SUMMARY.

Write the tests per `<behavior>`. Commit as `test(setup): observed literal-echo fixtures prove
redaction for claude-code and codex` with the explicit pathspec `internal/setup/codex_test.go
internal/setup/drift_test.go` plus `internal/setup/codex.go` only if the mirror changed.
  </action>
  <verify>
    <automated>out=$(go test ./internal/setup/ -run '^(TestObserveCodexRegistration|TestRedactionUnconditional|TestDriftRuntimeIsOptional)$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^(--- |    --- )(PASS|FAIL)' | tail -n 40; test "$rc" -eq 0 &amp;&amp; printf '%s\n' "$out" | rg -q -F -e '--- PASS: TestObserveCodexRegistration/preserved-literal-header-observed' &amp;&amp; printf '%s\n' "$out" | rg -q -F -e '--- PASS: TestRedactionUnconditional/claude-code-observed-literal' &amp;&amp; printf '%s\n' "$out" | rg -q -F -e '--- PASS: TestRedactionUnconditional/codex-observed-literal' &amp;&amp; printf '%s\n' "$out" | rg -q -F -e '--- PASS: TestDriftRuntimeIsOptional' &amp;&amp; test "$(rg -c -F 'ASSUMED SHAPE (A3)' internal/setup/codex_test.go)" -eq 0 &amp;&amp; rg -q -F -e '04-OBSERVATIONS.md' internal/setup/codex_test.go</automated>
    <fails_when>non-zero `go test` exit, a `--- FAIL:` line, `testing: warning: no tests to run`, no PASS line for any of the four named (sub)tests, the `ASSUMED SHAPE (A3)` marker still present in `codex_test.go` (the assumed fixture was not replaced by the observed one), or no citation of `04-OBSERVATIONS.md` in `codex_test.go`</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n -F 'preserved-literal-header-observed' internal/setup/codex_test.go` prints exactly one line; `rg -c -F '04-OBSERVATIONS.md' internal/setup/codex_test.go` prints at least `1`; `rg -c -F 'sk-DO-NOT-COMMIT-literal-test-abc123' internal/setup/codex_test.go internal/setup/drift_test.go` prints at least `1` for each file.
    - `rg -n -e 'claude-code-observed-literal|codex-observed-literal' internal/setup/drift_test.go` prints two lines; `rg -n -F 'ClaudeCode.(DriftRuntime)' internal/setup/drift_test.go` prints exactly one line.
    - `rg -c -F 'ASSUMPTION A3' internal/setup/codex.go` prints `0` (the doc comment now cites the record).
    - RED/GREEN recorded in the SUMMARY per subtest with the `=== RUN` lines; the mirror reconciliation outcome (unchanged / retyped, and why) recorded.
    - `go test ./internal/setup/ -count=1` exits 0; `gofmt -l internal/setup/` prints nothing.
  </acceptance_criteria>
  <done>The codex mirror matches the observed literal-header shape and the assumed-shape subtest is replaced by the observed one; both runtimes' observed literal fixtures classify preserved with the dummy absent from every rendered field; ClaudeCode and Codex are the two DriftRuntime implementers; committed with explicit pathspecs.</done>
</task>

<task type="auto">
  <name>Task 3: The CLI-boundary leak proof on both observed shapes, then the phase gate</name>
  <files>cmd/engram/setup_test.go</files>
  <read_first>
    - `cmd/engram/setup_test.go` (04-04) — `TestSetupJSONNeverLeaksProbeLiteral`, `scriptedSetupRun`, `codexGetEngramBearerJSON`, `rowByName`.
    - `internal/setup/claudecode_test.go` (Task 1) `claudeGetFixture` — NOT importable across packages; re-type the record's Claude Code capture as a `cmd/engram` test const with the same header-comment citation.
    - `.planning/phases/04-drift-detection-read-only/04-OBSERVATIONS.md` — the two captures to quote.
    - `Taskfile.yaml`; `.planning/STATE.md` §Blockers/Concerns (key-links gate, `-count=1`, the ROADMAP progress-row trap — never run `roadmap update-plan-progress`).
  </read_first>
  <action>
Extend `TestSetupJSONNeverLeaksProbeLiteral` with two subtests, each run for BOTH `--output json`
and `--output text`: `claude-code-observed-shape` (claude present, `--runtime claude-code --auth
oauth`, mcpGet stdout = the record's Claude Code literal capture with the entry name rewritten to
`engram`, exit code = the record's failed-dial exit code) and `codex-observed-shape` (codex present,
`--runtime codex --auth oauth`, mcpGet stdout = the record's codex literal capture with `name`
rewritten to `engram`) — asserting the row's `Outcome == "preserved"`, `Facets` contains
`header-name`, and `sk-DO-NOT-COMMIT-literal-test-abc123` absent from `stdout` and `stderr`.
Header-comment both fixtures with the record citation (D-08).

Then run the PHASE gate: `task`, `task license:check`, `git diff --exit-code -- go.mod go.sum`,
`go test ./internal/keylinks/ -count=1` (every 04-0N plan's patterns are bracket-escaped; a red is
a planning-artifact defect to report), `go test ./internal/setup/ -count=1 -shuffle=on`, `go run
./internal/surfacesgen --check-setup`, `test -z "$(git status --porcelain -- skill/
internal/setupgen/ cmd/engram/testdata/ release-please-config.json)"`, and the D-08 provenance
check `test "$(rg -c -F '04-OBSERVATIONS.md' internal/setup/claudecode_test.go)" -ge 1 && test
"$(rg -c -F '04-OBSERVATIONS.md' internal/setup/codex_test.go)" -ge 1 && test "$(rg -c -F
'04-OBSERVATIONS.md' cmd/engram/setup_test.go)" -ge 1`. Red-evidence patches for this phase are
authored and registered by the orchestrator after this plan from each SUMMARY's RED record — do
not edit `internal/store/redevidence_harness_test.go`. Never run `cmd/engram` tests with
`-count>1`; never run `roadmap update-plan-progress`.

Commit as `test(setup): observed literal shapes never reach engram setup output` with the explicit
pathspec `cmd/engram/setup_test.go`.
  </action>
  <verify>
    <automated>out=$(go test ./cmd/engram -run '^TestSetupJSONNeverLeaksProbeLiteral$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^(--- |    --- )(PASS|FAIL)' | tail -n 20; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -c -F -e '--- PASS: TestSetupJSONNeverLeaksProbeLiteral/')" -ge 6 &amp;&amp; printf '%s\n' "$out" | rg -q -F -e 'claude-code-observed-shape' &amp;&amp; printf '%s\n' "$out" | rg -q -F -e 'codex-observed-shape' &amp;&amp; task &amp;&amp; task license:check &amp;&amp; git diff --exit-code -- go.mod go.sum &amp;&amp; go test ./internal/keylinks/ -count=1 &amp;&amp; go test ./internal/setup/ -count=1 -shuffle=on &amp;&amp; go run ./internal/surfacesgen --check-setup &amp;&amp; test -z "$(git status --porcelain -- skill/ internal/setupgen/ cmd/engram/testdata/ release-please-config.json)" &amp;&amp; test "$(rg -c -F '04-OBSERVATIONS.md' internal/setup/claudecode_test.go)" -ge 1 &amp;&amp; test "$(rg -c -F '04-OBSERVATIONS.md' internal/setup/codex_test.go)" -ge 1 &amp;&amp; test "$(rg -c -F '04-OBSERVATIONS.md' cmd/engram/setup_test.go)" -ge 1</automated>
    <fails_when>non-zero exit from any link: a `--- FAIL:` line or `no tests to run` in the focused run, fewer than 6 `TestSetupJSONNeverLeaksProbeLiteral/` PASS lines (two lanes × three shapes), a missing `claude-code-observed-shape`/`codex-observed-shape` subtest name, `task` printing `task: Failed to run task` (or a lint finding / `--- FAIL`), `license-eye` listing a file missing its header, a `go.mod`/`go.sum` hunk, `go test ./internal/keylinks/` printing `FAIL` naming any `04-0N-PLAN.md`, the shuffled `internal/setup` run printing `FAIL`, `surfacesgen --check-setup` reporting drift, a dirty generated file, or any of the three `04-OBSERVATIONS.md` citation counts below 1</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n -e 'claude-code-observed-shape|codex-observed-shape' cmd/engram/setup_test.go` prints at least two lines; `rg -c -F '04-OBSERVATIONS.md' cmd/engram/setup_test.go` prints at least `1`; `rg -c -F 'sk-DO-NOT-COMMIT-literal-test-abc123' cmd/engram/setup_test.go` prints at least `2`.
    - `task`, `task license:check`, `git diff --exit-code -- go.mod go.sum`, `go test ./internal/keylinks/ -count=1`, `go test ./internal/setup/ -count=1 -shuffle=on`, `go run ./internal/surfacesgen --check-setup` all exit 0.
    - `git status --porcelain -- internal/setup/ cmd/engram/ docs-site/ skill/ internal/setupgen/` prints nothing after the plan's commits.
    - RED/GREEN recorded: both new subtests are expected GREEN on first run (redaction by construction, 04-01/04-04) — record them as pinning shipped behavior with their `=== RUN` lines.
  </acceptance_criteria>
  <done>Both runtimes' observed literal-echo shapes pass through `engram setup` in both lanes with the dummy absent from stdout and stderr; every fixture cites the record; the full phase gate — `task`, license, dependency, key-links, shuffle, setupgen drift, clean generated files, provenance — is green; committed with an explicit pathspec.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `claude mcp get` stdout/stderr → `claudeCodeRuntime.Observe` | Untrusted fixed-label text (and a live connection-status line that varies with network state): line-classified by prefix, values compared in locals, never rendered verbatim. |
| `04-OBSERVATIONS.md` → test fixtures | A committed record quoted into fixtures; the fixtures' honesty depends on the record's provenance (D-08 citation). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-18 | Information Disclosure | a literal header value in `claude mcp get`'s `Headers:` block (the incident's exact shape, gotcha ryr82bf2s2) reaching `Registered`/`Reason`/`Drift`/JSON/text | high | mitigate | Values split off at the first `": "` are compared in a `rawHeader` local and never assigned to `Observation`; `TestObserveClaudeCodeRegistration/unplanned-header-literal-observed`, `TestRedactionUnconditional/claude-code-observed-literal`, and `TestSetupJSONNeverLeaksProbeLiteral/claude-code-observed-shape` assert the observed dummy is absent at every layer. |
| T-04-19 | Tampering | a Claude Code release adding a label/line, silently changing what "matches" means | high | mitigate | D-11 total parse: any unclassified line is unrecognized content → `preserved` naming its label, never a false `already-correct`; pinned by `unrecognized-label`/`unrecognized-unlabeled-line`/`type-not-http`. |
| T-04-20 | Denial of Service (of the comparison) | the live connection-status line varying with reachability, producing spurious `preserved` rows or blocking `already-correct` on an unreachable server | medium | mitigate | `Status:` is chrome by prefix; the failed-dial exit code the record shows does not block parsing (`execute()` parses regardless of exit code); pinned by `status-failed-dial-is-chrome` and the failed-dial-exit `Preview` subtests (Pitfall 4). |
| T-04-21 | Information Disclosure | an unrecognized line's remainder (potentially a value) copied into the reason | medium | mitigate | `claudeCodeLineLabel` copies only the text before the first colon, bounded to 40 bytes, `quoteWord`ed; a colon-less line contributes the fixed token `line`. |
| T-04-22 | Repudiation | a fixture claiming an echo shape the record does not support | medium | mitigate | Every literal-echo fixture cites `04-OBSERVATIONS.md` (gate: `rg -c` ≥ 1 in three files); the fixture builder's header comment names the record section, CLI version, and date. |
| T-04-23 | Tampering | a hand-edited codex key that only a hand edit produces being added to the known-key list, laundering a foreign field into `already-correct` | medium | mitigate | Task 2's rule: a key is added to the mirror/known lists ONLY if the record shows engram's own `mcp add` producing it; hand-edit-only keys stay unknown → preserved. |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | Not applicable — zero new packages; Task 3 asserts `git diff --exit-code -- go.mod go.sum`. |
</threat_model>

<verification>
- `go test ./internal/setup/ -run '^(TestObserveClaudeCodeRegistration|TestPreviewClassifiesRegistration|TestObserveCodexRegistration|TestRedactionUnconditional|TestDriftRuntimeIsOptional)$' -count=1 -v` shows `--- PASS` for every subtest named in the three `<behavior>` blocks, and `go test ./cmd/engram -run '^TestSetupJSONNeverLeaksProbeLiteral$' -count=1 -v` shows six lane×shape PASS lines.
- Every literal-echo fixture in `internal/setup/claudecode_test.go`, `internal/setup/codex_test.go`, and `cmd/engram/setup_test.go` cites `04-OBSERVATIONS.md` (D-08; the verifier checks fixture text against the record's fenced captures).
- `task`, `task license:check`, `git diff --exit-code -- go.mod go.sum`, `go test ./internal/keylinks/ -count=1`, `go test ./internal/setup/ -count=1 -shuffle=on`, `go run ./internal/surfacesgen --check-setup` exit 0; `git diff --stat FIRST^..LAST` touches only the six files in `files_modified`.
- Red-evidence handoff for the orchestrator (Phase 03 shape): suggested patches `04-05-claudecode-status-facet.patch` (treat the `Status:` line as unrecognized → `TestObserveClaudeCodeRegistration`), `04-05-claudecode-value-retained.patch` (copy a header value into `Observation.Unrecognized` → `TestRedactionUnconditional`), `04-05-codex-literal-header-tolerated.patch` (drop the header-key → unplanned mapping → `TestObserveCodexRegistration`).
- Scope exemption for the verifier (D-10): SC2's three-state coverage is complete for BOTH parsed runtimes (codex in 04-01, claude-code here); opencode authors no scanner by locked decision and is asserted only negatively (`TestDriftRuntimeIsOptional`, 04-04's `opencode-never-compared`). Not a gap.
- Not observed and accepted (D-07): the `oauth-client` read-back shape — an oauth-client entry may read `preserved` naming an extra label until Phase 5's OAuth read-back observation pins it.
</verification>

<success_criteria>
- Claude Code's existing registration is read through its own verb by a bounded, total text scan, normalized to the shape `Plan()` authors, and classified as exactly one of the three states with facets named (SC1, SC2, SC4 for claude-code).
- Header values from both runtimes' OBSERVED literal-echo shapes are redacted unconditionally before storage, rendering, JSON output, and the CLI's output streams, proven against fixtures quoted from `04-OBSERVATIONS.md` (SC5).
- The phase gate is green and every literal-echo fixture cites the record.
</success_criteria>

<output>
Create `.planning/phases/04-drift-detection-read-only/04-05-SUMMARY.md` when done
</output>
