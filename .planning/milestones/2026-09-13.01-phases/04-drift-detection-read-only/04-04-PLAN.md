---
phase: 04-drift-detection-read-only
plan: 04
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - cmd/engram/setup.go
  - cmd/engram/setup_test.go
  - cmd/engram/operator_view_setup_test.go
  - cmd/engram/testdata/help.golden
autonomous: true
requirements:
  - REQ-drift-preserved-outcome
  - REQ-drift-facet-naming
  - REQ-drift-redaction
  - REQ-drift-three-way

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`setupRuntimeRow` carries two new FLAT string fields, `Facets` (`json:\"facets,omitempty\"`) and `Drift` (`json:\"drift,omitempty\"`), copied field-for-field from `setup.Result.Facets`/`setup.Result.Drift` in `setupRuntimeRowFromResult` and back in `setupResultsFromRows` — never a slice, map, struct, or raw-message type, so `TestOperatorViewFixturesHaveNoUnsanitizedNesting` and `TestSetupViewIdentity` stay green with the new fixtures (D-12; edge probe `encoding` for REQ-drift-facet-naming, resolved explicit: `facets` is the closed lowercase-ASCII enum values comma-joined with no spaces in setup's fixed order, as one JSON string)"
    - "A `preserved` registration reaches `--output json` and `--output text` as a first-class row outcome: `outcome=preserved`, `registration=preserved`, `reason` beginning `<runtime>: preserved: ` and ending with the runtime's whole-entry sentence, `facets`/`drift` naming what is preserved, and `registered` carrying the normalized redacted rendering (REQ-drift-preserved-outcome)"
    - "The row's aggregated `Outcome` folds the registration facet through `setup.AggregateOutcome` unchanged: a `preserved` registration beside a `would-write` skills facet (every preview) shows `outcome=preserved`; an `already-correct` registration beside a `would-write` skills facet shows `outcome=already-correct` — both pinned as LITERAL rows in `setupOutcomeFoldTable`, never derived by calling the fold (REQ-drift-three-way's cmd/engram half)"
    - "`setupApplySummary` counts `preserved` as its own bucket — `apply: %d wrote, %d already correct, %d preserved, %d failed (of %d selected runtime(s))` — and `setupPreviewSummary` states that a present runtime's own CLI is read AND compared with what setup would write, and that opencode is not compared (REQ-setup-correct-by-reading discipline: the headline teaches the behavior)"
    - "`engram setup --help` (the `Long` text, pinned by `help.golden`) names the three classifications, the four differing facets, `preserved`'s meaning, that header values read from a runtime are never shown, that an unreadable registration reads would-write, and that opencode is not compared (rule 4aksmneehh: the CLI is correct-by-reading); `Short` and `catalog.golden` are byte-unchanged"
    - "`TestSetupPreviewNeverClassifiesAlreadyCorrect` — the test pinning the OLD preview invariant this phase overturns for parsed runtimes — is retargeted, not deleted: renamed `TestSetupPreviewNeverClassifiesAlreadyCorrectFromAmbiguousRead`, it asserts that an unparseable read on every runtime never yields `already-correct` or `preserved` and carries no facets (D-09), and that a present opencode with a convincing-looking `mcp list` table still reads `would-write` with `drift` = `opencode: not compared: runtime authors no registration scanner` (D-10)"
    - "No probe-read literal reaches the CLI's stdout or stderr in either output lane: `TestSetupJSONNeverLeaksProbeLiteral` scripts a codex `bearer_token_env_var` sentinel through `engram setup --output json` and `--output text` and asserts the sentinel is absent from both streams (REQ-drift-redaction at the process boundary); plan 04-05 extends it with the observed literal-echo shapes"
    - "No test in this plan invokes a real `claude`/`codex`/`opencode` binary or touches the operator's `$HOME`: every probe runs through `withFakeSetupEnv`/`fakeSetupEnvWithRun` with an args-keyed scripted `Run`, and `cmd/engram` tests run with `-count=1` only"
  artifacts:
    - path: "cmd/engram/setup.go"
      provides: "setupRuntimeRow.Facets/Drift, the two copy sites, setupApplySummary preserved bucket, setupPreviewSummary comparison wording, setupLongDescription drift paragraph"
      contains: "Facets string `json:\"facets,omitempty\"`"
    - path: "cmd/engram/setup_test.go"
      provides: "TestSetupPreviewJSONCarriesDriftFacets, TestSetupHelpNamesDriftOutcomes, TestSetupJSONNeverLeaksProbeLiteral, TestSetupApplySummaryCountsPreserved, TestSetupPreviewSummaryNamesComparison, TestSetupPreviewNeverClassifiesAlreadyCorrectFromAmbiguousRead, extended setupOutcomeFoldTable, the args-keyed scriptedSetupRun helper"
      contains: "func TestSetupPreviewNeverClassifiesAlreadyCorrectFromAmbiguousRead"
    - path: "cmd/engram/operator_view_setup_test.go"
      provides: "preservedGateway, driftURL, notCompared fixture rows in setupViewFixtures"
      contains: "Facets:"
    - path: "cmd/engram/testdata/help.golden"
      provides: "regenerated engram setup section naming preserved, facets, drift, not compared"
      contains: "preserved"
  key_links:
    - from: "cmd/engram/setup.go"
      to: "internal/setup/plan.go"
      via: "the row copies Result.Facets/Result.Drift field-for-field — no bespoke rendering, every new Result field is one more key=value the shared renderer picks up (D-15)"
      pattern: 'Facets: +r[.]Facets'
    - from: "cmd/engram/setup.go"
      to: "internal/setup/aggregate.go"
      via: "the row outcome stays the shared AggregateOutcome fold; preserved reaches the headline through the precedence 04-01 recorded"
      pattern: 'setup[.]AggregateOutcome[(]base, '
    - from: "cmd/engram/setup.go"
      to: "internal/setup/plan.go"
      via: "the apply headline tallies the sixth outcome as its own bucket"
      pattern: 'case setup[.]OutcomePreserved:'
  prohibitions:
    - statement: "MUST NOT add a facet-carrying row field typed as a slice, map, struct, or json.RawMessage: Facets and Drift are flat strings so sanitizeViewValue's scalar-only branch cannot be bypassed (TestOperatorViewFixturesHaveNoUnsanitizedNesting)"
      category: safety
      status: resolved
      verification: test
      reason: "The nesting gate walks every setupViewFixtures document including the three new rows; the acceptance grep pins both fields as `string` — no wired check descriptor by design (spec-less fallback, flagged-unverified)"
    - statement: "MUST NOT let a probe-read header value reach stdout or stderr in any output lane, and MUST NOT let a not-compared row render raw probe bytes: the CLI only ever copies internal/setup's already-redacted scalars"
      category: safety
      status: resolved
      verification: test
      reason: "TestSetupJSONNeverLeaksProbeLiteral asserts sentinel absence across both lanes and both streams; the ambiguous-read subtests assert the scripted probe bytes are absent from the row — no wired check descriptor by design (spec-less fallback, flagged-unverified)"
    - statement: "MUST NOT invoke a real claude/codex/opencode binary or the operator's $HOME from any test; MUST NOT run cmd/engram tests with -count>1; MUST NOT run `roadmap update-plan-progress`"
      category: safety
      status: resolved
      verification: test
      reason: "Every test drives withFakeSetupEnv; the verify commands carry -count=1; no task in this plan invokes the roadmap verb — no wired check descriptor by design (spec-less fallback, flagged-unverified)"
---

<objective>
Compose plan 04-01's classification into `engram setup`'s report: two flat row fields (`facets`,
`drift`) copied from `setup.Result`, `preserved` counted in the apply headline and named in the
preview headline, a `--help` paragraph that teaches the comparison (correct-by-reading), new view
fixtures for the three new row shapes, and the retargeting of the one existing test that pinned
the preview invariant this phase overturns — plus a process-boundary proof that no probe-read
literal reaches stdout/stderr in either output lane.

Purpose: REQ-drift-preserved-outcome requires `preserved` to be first-class in text AND JSON with
its reason, and consistent in aggregation; REQ-drift-facet-naming requires the facet set to reach
`--output json` in a fixed order consumers can branch on; REQ-drift-redaction's fixture proof must
hold at the CLI boundary, not only inside `internal/setup`. The orchestrator's note on
`TestSetupPreviewNeverClassifiesAlreadyCorrect` is honored here by retargeting it to D-09/D-10.

Composition decision (Claude's discretion, 04-CONTEXT.md): the plugin facet sits beside a
`preserved` registration exactly as it sits beside any other registration outcome — computed from
the same `Plan()` call, folded through `AggregateOutcome`, independent of registration drift
(03-CONTEXT.md D-12 posture). No routing change.

Output: `Facets`/`Drift` on `setupRuntimeRow`, the two copy sites, the summaries, the help
paragraph and regenerated `help.golden`, three fixture rows, six new/retargeted tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-drift-detection-read-only/04-CONTEXT.md
@.planning/phases/04-drift-detection-read-only/04-PATTERNS.md
@.planning/phases/04-drift-detection-read-only/04-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Contracts CONSUMED from plan 04-01 (internal/setup) — use verbatim; do not re-derive. -->

From internal/setup/plan.go (04-01):
```go
const OutcomePreserved Outcome = "preserved"
type Result struct {
	// … existing fields …
	Notes  string `json:"notes,omitempty"`
	Facets string `json:"facets,omitempty"` // comma-joined Facet values in facetOrder; "" when none
	Drift  string `json:"drift,omitempty"`  // "; "-joined detail lines, or "<runtime>: not compared: <why>"
	Skills SkillTarget `json:"-"`
}
```
From internal/setup/drift.go (04-01): facet values `url`, `auth-mode`, `header-name`, `header-value-ref`, `unrecognized-content`; `renderObservation` shape `url=<url> auth=<none|bearer|foreign> headers=<name>=<redacted>[,…]|none[ unrecognized=<label>[,…]]`; a preserved `Reason` is `<runtime>: preserved: <cause>[; <cause>]; <WholeEntryNote>`.
From internal/setup/codex.go (04-01): `codexWholeEntryNote = "codex mcp add replaces the whole entry: --apply would overwrite it or leave it untouched, never merge into it"`; the not-compared note for a scanner-less runtime is `<runtime>: not compared: runtime authors no registration scanner`.
Codex fixture base (04-01's `codexGetEngramBearer`) — recreate as a package-level const in `setup_test.go` (the two packages cannot share test code): the 04-RESEARCH.md verbatim JSON with `"name": "engram"`, URL `https://engram.example.com/mcp`, `"bearer_token_env_var": "ENGRAM_TOKEN"`.
</interfaces>
</context>

## Artifacts this phase produces (plan 04-04's share)

| Kind | Artifact | Created by |
|------|----------|------------|
| Go fields | `setupRuntimeRow.Facets`, `setupRuntimeRow.Drift` (flat strings, `omitempty`) in `cmd/engram/setup.go` | 04-04 Task 1 |
| Go edits | `setupRuntimeRowFromResult`/`setupResultsFromRows` copy sites; `setupApplySummary` preserved bucket; `setupPreviewSummary` wording; `setupLongDescription` drift paragraph | 04-04 Task 1 |
| Golden | `cmd/engram/testdata/help.golden` regenerated `engram setup` section | 04-04 Task 1 |
| Test helpers | `scriptedSetupRun(t, mcpGet setup.RunResult)` (args-keyed fake: `mcp get …` → mcpGet, everything else → exit 0 empty), `codexGetEngramBearerJSON` const | 04-04 Task 1 |
| Test funcs | `TestSetupPreviewJSONCarriesDriftFacets`, `TestSetupHelpNamesDriftOutcomes`, extended `setupOutcomeFoldTable` | 04-04 Task 1 |
| Test funcs | `TestSetupPreviewNeverClassifiesAlreadyCorrectFromAmbiguousRead` (retargeted), `TestSetupJSONNeverLeaksProbeLiteral`, `TestSetupApplySummaryCountsPreserved`, `TestSetupPreviewSummaryNamesComparison` | 04-04 Task 2 |
| Fixtures | `preservedGateway`, `driftURL`, `notCompared` rows + a new `setupReportDoc` entry in `setupViewFixtures` | 04-04 Task 2 |

## Flagged Assumptions (edge probe, spec-less fallback)

This plan authors **1** of the phase's 13 edge-probe rows (the remaining 11 are in 04-01, 1 flagged
in 04-05):

| Requirement | Probe category | Disposition |
|---|---|---|
| REQ-drift-facet-naming | `encoding` | **Authored (resolved, explicit)** — first truth: `facets` is one JSON string of lowercase-ASCII enum values, comma-joined, no spaces, fixed order; pinned by `TestSetupPreviewJSONCarriesDriftFacets` (`facets == "url"`) and by the `everything-differs` ordering already pinned in 04-01. |

Recorded consequence (not a probe row): under the shipped fold precedence, a PREVIEW row whose
registration is `already-correct` and whose skills facet is `would-write` shows the headline
`outcome=already-correct` (already-correct > would-write, the D-06 precedence of the setup
milestone). The `skills=would-write` facet and the headline's "run with --apply to register and
install skills" still say the skills would be written. This plan pins that fold literally rather
than re-litigating a shipped precedence; if it should change, that is a separate decision.

## Prohibition probe (spec-less fallback, in-prompt recall)

Stage 1 raw candidates: must not nest a facet field; must not leak a probe value through the CLI;
must not render raw probe bytes on a not-compared row; must not run a real CLI; must not run
cmd/engram tests with -count>1; must not touch the catalog golden (Short unchanged); must not
derive the fold table by calling AggregateOutcome; must not run roadmap update-plan-progress; must
not change opencode's outcome. Stage 2 keeps three (authored above, descriptor-less): the
**flat-scalar-only** safety prohibition, the **no-probe-value-at-the-process-boundary** privacy
prohibition, and the **no-real-CLI / -count=1 / no-roadmap-verb** hygiene prohibition (kept
because the roadmap verb has bitten this repo eight times — gotcha yzmfesbsg0). The rest are
pinned by acceptance criteria. Canon breadcrumb: none applicable.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Row fields, summaries, and `--help` teach the comparison — facets/drift/preserved reach `--output json` and the help golden</name>
  <files>cmd/engram/setup.go, cmd/engram/setup_test.go, cmd/engram/testdata/help.golden</files>
  <read_first>
    - `cmd/engram/setup.go` lines 432-531 (`setupRuntimeRow` doc + struct — add the fields as their own labeled block after `Notes`, mirroring the `Plugin*` block; extend the "Every one of these … is a plain string" discipline), 578-594 (`setupPreviewSummary`), 770-800 (`setupRuntimeRowFromResult` — the field-for-field copy), 802-820 (`setupResultsFromRows`), 822-836 (`setupApplySummary`), 935-1000 (`setupLongDescription` — insert the new paragraph after "for codex, it is a pure local read."), 1005-1029 (`init` — untouched).
    - `cmd/engram/setup_test.go` lines 30-70 (`fakeSetupEnvSucceedingRun`, `fakeSetupEnv`, `fakeSetupEnvWithRun`), 162-186 (`withFakeSetupEnv`, `withFakeSetupVersion`), 767-800 (`TestSetupPreviewNeverClassifiesAlreadyCorrect` — READ now, retargeted in Task 2), 860-880 (`TestSetupHelpNamesEveryRuntimeAndAuthMode` — the golden-section idiom `TestSetupHelpNamesDriftOutcomes` copies), 1803-1831 (`setupOutcomeFoldTable`/`assertFoldedOutcome` — extend the LITERAL table), 3457-3467 (`TestSetupHelpNamesPluginDelivery` — the `Long`-contains idiom).
    - `cmd/engram/golden_test.go` lines 18-30 and 295-330 (`-update` regenerates `help.golden`; `TestHelpGolden`/`TestCatalogGolden`).
    - `.planning/phases/04-drift-detection-read-only/04-01-SUMMARY.md` — the exact `Result` field names, `renderObservation` shape, and `codexWholeEntryNote` text as shipped (if 04-01 deviated from `<interfaces>`, the SUMMARY wins; update this plan's literals accordingly and record it).
    - `.planning/phases/04-drift-detection-read-only/04-CONTEXT.md` D-03, D-04, D-10, D-12; `.planning/STATE.md` §Accumulated Context "The CLI is correct-by-reading (4aksmneehh)".
  </read_first>
  <behavior>
    Write the tests FIRST; each fails against HEAD's `setup.go` (the row has no `facets`/`drift`
    keys, `Long` lacks the vocabulary, the fold table lacks the rows) — run each with `-run '^Name$'
    -v` and record the RED in the SUMMARY.

    Helper `scriptedSetupRun(t *testing.T, mcpGet setup.RunResult) func(context.Context, string,
    []string) (setup.RunResult, error)` in `setup_test.go`: returns `mcpGet` when `len(args) >= 2
    && args[0] == "mcp" && args[1] == "get"`, else `setup.RunResult{ExitCode: 0}` (the plugin
    probes then read as `unavailable`, exactly as every existing preview test's default fake does —
    no plugin facet folds). Const `codexGetEngramBearerJSON` = the 04-RESEARCH.md verbatim codex
    document (name `engram`, URL `https://engram.example.com/mcp`, bearer var `ENGRAM_TOKEN`, every
    other field `null`/`true` as captured).

    `TestSetupPreviewJSONCarriesDriftFacets(t)` — each subtest: `resetClientFlags`,
    `resetCommandFlagState(t, setupCmd)`, `withFakeSetupEnv(t, fakeSetupEnvWithRun(scriptedSetupRun(t,
    …), "codex"))`, `runClient(t, "setup", "--url", "https://engram.example.com/mcp", "--auth",
    "bearer", "--runtime", "codex", "--output", "json")`, unmarshal into `setupReportDoc`, take
    `rowByName(t, doc.Runtimes, "codex")`:
    - `codex-would-write-url`: mcpGet stdout = the const with the URL replaced by
      `https://old.example/mcp` → `row.Outcome == "would-write"`, `row.Registration == "would-write"`,
      `row.Facets == "url"`, `row.Drift == "url: observed https://old.example/mcp, would write
      https://engram.example.com/mcp"`, `row.Registered == "url=https://old.example/mcp auth=bearer
      headers=none"`, `row.Reason == ""`; `assertFoldedOutcome(t, row)`.
    - `codex-preserved-unrecognized-field`: stdout = the const plus a top-level
      `"oauth_client_id": "x"` → `row.Outcome == "preserved"`, `row.Registration == "preserved"`,
      `row.Facets == "unrecognized-content"`, `row.Drift == "unrecognized-content: oauth_client_id"`,
      `strings.HasPrefix(row.Reason, "codex: preserved: unrecognized-content: oauth_client_id; ")`
      and `strings.HasSuffix(row.Reason, "never merge into it")`, `row.Registered` ends with
      `unrecognized=oauth_client_id`; `assertFoldedOutcome(t, row)`; the raw JSON `stdout` string
      contains `"outcome":"preserved"` (first-class in the JSON lane).
    - `codex-already-correct`: stdout = the const → `row.Registration == "already-correct"`,
      `row.Outcome == "already-correct"` (the shipped fold: already-correct > would-write skills),
      `row.Facets == ""`, `row.Drift == ""`, `row.Registered == "url=https://engram.example.com/mcp
      auth=bearer headers=none"`; the JSON lacks a `facets` key for this row (omitempty —
      assert via a `map[string]any` decode of the row).
    - `text-lane-renders-flat-fields`: the preserved fixture with `--output text` → stdout contains
      `outcome=preserved`, `facets=unrecognized-content`, and `registration=preserved`.
    Extend `setupOutcomeFoldTable` with the LITERAL entries `{preserved, would-write} → preserved`
    and `{already-correct, would-write} → already-correct` (hand-pinned, never computed).

    `TestSetupHelpNamesDriftOutcomes(t)`: `lower := strings.ToLower(setupCmd.Long)` contains each
    of `preserved`, `already-correct`, `would-write`, `facets`, `drift`, `url`, `auth-mode`,
    `header-name`, `header-value-ref`, `never shown`, `not compared`, `cannot reproduce`; AND the
    `engram setup` section of `testdata/help.golden` (via `helpGoldenSection`) contains `preserved`
    and `not compared` — the golden proves the paragraph shipped, not only the in-memory `Long`.
  </behavior>
  <action>
In `cmd/engram/setup.go`:
- Add to `setupRuntimeRow`, after `Notes`, a new labeled block with a doc paragraph "Phase 4
  (Drift Detection, D-12/D-03)": `Facets string `json:"facets,omitempty"`` — the differing facets of
  a compared registration as ONE comma-joined string in internal/setup's fixed facet order (`url`,
  `auth-mode`, `header-name`, `header-value-ref`, `unrecognized-content`), empty for
  already-correct and for a not-compared read; `Drift string `json:"drift,omitempty"`` — the
  `"; "`-joined per-facet detail lines, or the `not compared` note. State that `Registered`'s
  content is now a normalized rendering with header values redacted (D-03) rather than a raw
  capture, and that both new fields take Config's "never a struct, map, slice, or raw-message
  type" constraint for the identical reason.
- `setupRuntimeRowFromResult`: add `Facets: r.Facets,` and `Drift: r.Drift,` to the row literal
  (gofmt aligns them). `setupResultsFromRows`: add the two reverse copies.
- `setupApplySummary`: add a `preserved` counter with `case setup.OutcomePreserved:` and render
  `"apply: %d wrote, %d already correct, %d preserved, %d failed (of %d selected runtime(s))"`;
  update its doc comment (D-04: preserved is reported, never counted as failed).
- `setupPreviewSummary`: render `"preview: %d/%d selected runtime(s) present; a present runtime's
  own CLI is read and compared with what setup would write (two of the three dial the configured
  URL; opencode is not compared); run with --apply to register and install skills"`; update its doc
  comment to name the comparison (REQ-setup-correct-by-reading).
- `setupLongDescription`: insert, after the paragraph ending "for codex, it is a pure local read.",
  this paragraph (wrap at the file's existing width): "For claude-code and codex, that read is
  compared with what setup would write — URL, auth mode, and header names with their
  environment-variable references — and the row is classified already-correct, would-write, or
  preserved. A would-write row names the differing facets (url, auth-mode, header-name,
  header-value-ref) and details each one; a preserved row means the registration carries something
  setup did not author and cannot reproduce — an extra header, an unrecognized field — so setup
  leaves it untouched and names it in the reason. Header values read from a runtime are never
  shown. A registration the read cannot parse reads would-write; opencode is not compared (its mcp
  list output is not parsed) and always reads would-write." Do NOT change `Short` (the catalog
  golden must stay byte-identical).
- Regenerate the help golden ONLY via `go test ./cmd/engram -run '^TestHelpGolden$' -update
  -count=1`; then confirm `git diff --stat -- cmd/engram/testdata/` lists `help.golden` only.

Write the helper, const, and tests per `<behavior>`. Commit as `feat(setup): report drift facets,
preserved rows, and the comparison in --help` with the explicit pathspec `cmd/engram/setup.go
cmd/engram/setup_test.go cmd/engram/testdata/help.golden`.
  </action>
  <verify>
    <automated>out=$(go test ./cmd/engram -run '^(TestSetupPreviewJSONCarriesDriftFacets|TestSetupHelpNamesDriftOutcomes|TestHelpGolden|TestCatalogGolden|TestSetupHelpNamesEveryRuntimeAndAuthMode|TestSetupHelpNamesPluginDelivery|TestSetupReportCoversEveryRuntimeShape)$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^(--- |    --- )(PASS|FAIL)' | tail -n 30; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -c -F -e '--- PASS: TestSetupPreviewJSONCarriesDriftFacets/')" -ge 4 &amp;&amp; printf '%s\n' "$out" | rg -q -F -e '--- PASS: TestSetupHelpNamesDriftOutcomes' &amp;&amp; printf '%s\n' "$out" | rg -q -F -e '--- PASS: TestHelpGolden' &amp;&amp; printf '%s\n' "$out" | rg -q -F -e '--- PASS: TestCatalogGolden' &amp;&amp; test -z "$(git status --porcelain -- cmd/engram/testdata/catalog.golden)"</automated>
    <fails_when>non-zero `go test` exit, a `--- FAIL:` line, the line `testing: warning: no tests to run`, fewer than 4 `TestSetupPreviewJSONCarriesDriftFacets/` PASS lines, no PASS line for `TestSetupHelpNamesDriftOutcomes`/`TestHelpGolden`/`TestCatalogGolden`, or `catalog.golden` showing as modified (Short moved)</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n -F 'Facets string `json:"facets,omitempty"`' cmd/engram/setup.go` and `rg -n -e 'Drift +string +`json:"drift,omitempty"`' cmd/engram/setup.go` each print exactly one line.
    - `rg -c -e 'Facets: +r[.]Facets' cmd/engram/setup.go` prints `2` and `rg -c -e 'Drift: +r[.]Drift' cmd/engram/setup.go` prints `2` (row-from-result and results-from-rows).
    - `rg -n -F 'case setup.OutcomePreserved:' cmd/engram/setup.go` prints exactly one line; `rg -n -F '%d preserved, %d failed' cmd/engram/setup.go` prints exactly one line; `rg -n -F 'read and compared with what setup would write' cmd/engram/setup.go` prints exactly one line; `rg -n -F 'opencode is not compared' cmd/engram/setup.go` prints at least two lines (headline + Long).
    - `rg -n -F 'not compared' cmd/engram/testdata/help.golden` prints at least one line; `rg -n -F 'preserved' cmd/engram/testdata/help.golden` prints at least one line; `git diff --exit-code -- cmd/engram/testdata/catalog.golden` exits 0.
    - `rg -n -F '{string(setup.OutcomePreserved), string(setup.OutcomeWouldWrite)}' cmd/engram/setup_test.go` prints exactly one line; `rg -n -F '{string(setup.OutcomeAlreadyCorrect), string(setup.OutcomeWouldWrite)}' cmd/engram/setup_test.go` prints exactly one line; `rg -n -F 'func scriptedSetupRun(' cmd/engram/setup_test.go` prints exactly one line.
    - RED observed (record in SUMMARY): with the tests in place and `setup.go` at HEAD, `go test ./cmd/engram -run '^TestSetupPreviewJSONCarriesDriftFacets$' -count=1` fails to compile (`row.Facets`/`row.Drift` undefined) and `go test ./cmd/engram -run '^TestSetupHelpNamesDriftOutcomes$' -count=1 -v` prints `--- FAIL`; both GREEN after the edits.
    - `gofmt -l cmd/engram/` prints nothing; `go vet ./cmd/engram/` exits 0.
  </acceptance_criteria>
  <done>`facets`/`drift` ride the row in both lanes, `preserved` is a first-class row outcome with its reason and fold pinned literally, the apply and preview headlines count/name it, `--help` teaches the comparison and the golden proves it shipped, the catalog golden is untouched; committed with an explicit pathspec.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Retarget the old preview invariant to D-09/D-10, prove no probe literal crosses the process boundary, pin the summaries, and add the three fixture rows</name>
  <files>cmd/engram/setup_test.go, cmd/engram/operator_view_setup_test.go</files>
  <read_first>
    - `cmd/engram/setup_test.go` lines 767-800 (the test to retarget — keep its body as the first subtest, extend its assertions), 721-765 (`TestSetupPreviewExitsZeroWhenProbeFails` — the nonzero/seam shapes; add `Facets == ""` assertions there too), Task 1's `scriptedSetupRun`/`codexGetEngramBearerJSON`.
    - `cmd/engram/operator_view_setup_test.go` lines 22-178 (`setupViewFixtures` — add the three rows and one `setupReportDoc` entry with a Phase 4 comment mirroring the Phase 3 block's), 180-190 (`TestSetupViewIdentity`).
    - `cmd/engram/operator_output_test.go` lines 163-180, 403-422 (`operatorViewFixtures`, `TestOperatorViewFixturesHaveNoUnsanitizedNesting` — the gate the new rows must pass).
    - `.planning/phases/04-drift-detection-read-only/04-CONTEXT.md` D-02, D-09, D-10.
  </read_first>
  <behavior>
    `TestSetupPreviewNeverClassifiesAlreadyCorrectFromAmbiguousRead(t)` — rename the existing
    function; rewrite its doc comment: a bare preview now DOES classify already-correct for a parsed
    claude-code/codex registration (Phase 4), so what this test pins is D-09/D-10: an AMBIGUOUS read
    never yields already-correct or preserved, and opencode is never compared. Subtests:
    - `ambiguous-read-every-runtime`: the existing body (identical `engram: https://… (HTTP)` stdout
      on every call, all three runtimes present) asserting for every row `Outcome` is neither
      `already-correct` nor `preserved`, `Registration == "would-write"`, `Facets == ""`, and
      `Registered == ""` (D-03: an unframeable read renders nothing).
    - `opencode-never-compared`: opencode present alone, mcpGet stdout a box-drawing-styled table
      naming `engram` and `https://engram.example.com/mcp` with a `connected` glyph line → the
      opencode row has `Outcome == "would-write"`, `Facets == ""`, `Registered == ""`, `Drift ==
      "opencode: not compared: runtime authors no registration scanner"`.
    Add `row.Facets == ""` and `row.Registered == ""` assertions to both
    `TestSetupPreviewExitsZeroWhenProbeFails` subtests.

    `TestSetupJSONNeverLeaksProbeLiteral(t)`: const `sentinel = "SENTINEL-VALUE-cmd-7c1d4b-DO-NOT-LEAK"`;
    mcpGet stdout = `codexGetEngramBearerJSON` with `"bearer_token_env_var": "ENGRAM_TOKEN"` replaced
    by the sentinel; for each of `--output json` and `--output text` (codex present, `--auth oauth`
    so the row is preserved): `stdout`/`stderr` from `runClient` must NOT contain `sentinel`
    (`strings.Contains` negative on both streams), the row's `Outcome == "preserved"`, and in the
    json lane the row's `Registered == "url=https://engram.example.com/mcp auth=foreign headers=none"`.
    Doc-comment it as the process-boundary mirror of internal/setup's `TestRedactionUnconditional`
    and note plan 04-05 adds the observed-shape subtests.

    `TestSetupApplySummaryCountsPreserved(t)`: `setupApplySummary` over rows with outcomes wrote,
    already-correct, preserved, failed, not-present returns exactly `apply: 1 wrote, 1 already
    correct, 1 preserved, 1 failed (of 5 selected runtime(s))`.
    `TestSetupPreviewSummaryNamesComparison(t)`: `setupPreviewSummary` output contains
    `compared with what setup would write` and `opencode is not compared`.

    Fixtures (`operator_view_setup_test.go`), all flat strings: `preservedGateway` (claude-code,
    Present, Outcome `preserved`, Registration `preserved`, Command the two-action claude argv
    with `--header 'x-gateway-api-key: ${GATEWAY_KEY}'`, Headers `x-gateway-api-key=GATEWAY_KEY`,
    Facets `header-name`, Drift `x-other-gateway-key: observed <redacted>, not authored by setup`
    (the unplanned header uses a vendor-neutral name), Reason
    `claude-code: preserved: x-other-gateway-key: observed <redacted>, not authored by setup; claude
    mcp remove then add replaces the whole entry: --apply would overwrite it or leave it untouched,
    never merge into it`, Registered `url=https://engram.example.com/mcp auth=none
    headers=x-gateway-api-key=<redacted>,x-other-gateway-key=<redacted>`, Skills `would-write`);
    `driftURL` (codex, Outcome `would-write`, Registration `would-write`, Facets `url`, Drift `url:
    observed https://old.example/mcp, would write https://engram.example.com/mcp`, Registered
    `url=https://old.example/mcp auth=bearer headers=none`, Skills `would-write`); `notCompared`
    (opencode, Outcome `would-write`, Registration `would-write`, Drift `opencode: not compared:
    runtime authors no registration scanner`, Skills `would-write`). Append one
    `setupReportDoc{Runtimes: {preservedGateway, driftURL, notCompared}}` entry with a Phase 4
    comment. Verify `TestSetupViewIdentity` and `TestOperatorViewFixturesHaveNoUnsanitizedNesting`
    pass over the new rows.
  </behavior>
  <action>
Apply the edits per `<behavior>` to `cmd/engram/setup_test.go` and
`cmd/engram/operator_view_setup_test.go`. No vendor-branded gateway header name (the one the
incident record ryr82bf2s2 names) may appear in any HAND-BUILT fixture in this plan (the
shipped-bundle privacy guard precedent, STATE.md Phase 2 learnings); use
`x-gateway-api-key`/`x-other-gateway-key`. (Plan 04-05's fixtures that quote the observation
record verbatim are the one sanctioned exception.) Commit as `test(setup): retarget the preview
invariant to ambiguity, prove no probe literal crosses the CLI boundary, add drift view fixtures`
with the explicit pathspec `cmd/engram/setup_test.go cmd/engram/operator_view_setup_test.go`.
  </action>
  <verify>
    <automated>out=$(go test ./cmd/engram -run '^(TestSetupPreviewNeverClassifiesAlreadyCorrectFromAmbiguousRead|TestSetupJSONNeverLeaksProbeLiteral|TestSetupApplySummaryCountsPreserved|TestSetupPreviewSummaryNamesComparison|TestSetupPreviewExitsZeroWhenProbeFails|TestSetupViewIdentity|TestOperatorViewFixturesHaveNoUnsanitizedNesting)$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^(--- |    --- )(PASS|FAIL)' | tail -n 40; test "$rc" -eq 0 &amp;&amp; printf '%s\n' "$out" | rg -q -F -e '--- PASS: TestSetupPreviewNeverClassifiesAlreadyCorrectFromAmbiguousRead/opencode-never-compared' &amp;&amp; test "$(printf '%s\n' "$out" | rg -c -F -e '--- PASS: TestSetupJSONNeverLeaksProbeLiteral/')" -ge 2 &amp;&amp; printf '%s\n' "$out" | rg -q -F -e '--- PASS: TestOperatorViewFixturesHaveNoUnsanitizedNesting/setup' &amp;&amp; test -z "$(git status --porcelain -- cmd/engram/testdata/)" &amp;&amp; test "$(rg -c -F 'TestSetupPreviewNeverClassifiesAlreadyCorrect(' cmd/engram/setup_test.go)" -eq 0</automated>
    <fails_when>non-zero `go test` exit, a `--- FAIL:` line, the line `testing: warning: no tests to run`, no PASS line for the `opencode-never-compared` subtest, fewer than 2 `TestSetupJSONNeverLeaksProbeLiteral/` PASS lines (one per output lane), no PASS line for the nesting gate's `setup` group, a dirty `testdata/`, or the OLD test name still declared (`TestSetupPreviewNeverClassifiesAlreadyCorrect(` present — the retarget did not happen)</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n -F 'func TestSetupPreviewNeverClassifiesAlreadyCorrectFromAmbiguousRead' cmd/engram/setup_test.go` prints exactly one line and `rg -n -F 'func TestSetupPreviewNeverClassifiesAlreadyCorrect(' cmd/engram/setup_test.go` prints nothing.
    - `rg -n -F 'SENTINEL-VALUE-cmd-7c1d4b-DO-NOT-LEAK' cmd/engram/setup_test.go` prints at least two lines; `rg -n -F 'auth=foreign' cmd/engram/setup_test.go` prints at least one line.
    - `rg -n -F 'apply: 1 wrote, 1 already correct, 1 preserved, 1 failed (of 5 selected runtime(s))' cmd/engram/setup_test.go` prints exactly one line.
    - `rg -c -e 'Facets: +"' cmd/engram/operator_view_setup_test.go` prints at least `2`; `rg -n -F 'not compared: runtime authors no registration scanner' cmd/engram/operator_view_setup_test.go` prints at least one line; `rg -c -F 'x-litellm' cmd/engram/operator_view_setup_test.go` prints `0` (hand-built view fixtures stay vendor-neutral; plan 04-05's fixtures that QUOTE the observation record are exempt from this and live in `setup_test.go`).
    - RED observed (record in SUMMARY): `TestSetupJSONNeverLeaksProbeLiteral` is GREEN on first run against Task 1's tree — record it as "pins existing behavior (04-01's redaction by construction)" with the `=== RUN` lines; the retargeted `opencode-never-compared` subtest is likewise GREEN on first run (D-10 held by construction) — record the same way; `TestSetupApplySummaryCountsPreserved` and `TestSetupPreviewSummaryNamesComparison` were RED before Task 1 (record from Task 1's RED run if authored there, else note they pin Task 1's shipped strings).
    - `go test ./cmd/engram -count=1` exits 0 (whole package); `gofmt -l cmd/engram/` prints nothing.
  </acceptance_criteria>
  <done>The old invariant test now pins D-09 (ambiguity never already-correct/preserved) and D-10 (opencode never compared); no probe-read literal reaches stdout/stderr in either lane; both summaries are pinned; three flat-scalar fixture rows pass the identity and nesting gates; committed with an explicit pathspec.</done>
</task>

<task type="auto">
  <name>Task 3: Plan gate — full `task`, license, dependency, key-links, setupgen drift, and clean goldens</name>
  <files>none modified — verification-only; a formatter/license rewrite of one of this plan's files is folded into a `chore(setup)` commit with an explicit pathspec</files>
  <read_first>
    - `Taskfile.yaml` — `default`, `lint`, `license:check`, and the `surfaces`/`setupgen` gate names.
    - `.planning/STATE.md` §Blockers/Concerns — the key-links gate, `-count=1`-only for `cmd/engram`, the ROADMAP progress-row trap (never run `roadmap update-plan-progress`).
  </read_first>
  <action>
Run `task` (lint + test across every package), `task license:check`, `git diff --exit-code --
go.mod go.sum`, `go test ./internal/keylinks/ -count=1` (this plan's key_links patterns use
bracket classes and `+` — no backslash; a red is a planning-artifact defect to report, never
something to fix by editing a tool-owned file's structure), `go run ./internal/surfacesgen
--check-setup` (the generated `/engram-setup` tables render argv, not outcome vocabulary — this
proves the help/summary edits did not disturb them), and `test -z "$(git status --porcelain --
skill/ internal/setupgen/ cmd/engram/testdata/ release-please-config.json)"`. Never run `cmd/engram`
tests with `-count>1`; never run `roadmap update-plan-progress`. No commit is expected unless a
formatter rewrote a file.
  </action>
  <verify>
    <automated>task &amp;&amp; task license:check &amp;&amp; git diff --exit-code -- go.mod go.sum &amp;&amp; go test ./internal/keylinks/ -count=1 &amp;&amp; go run ./internal/surfacesgen --check-setup &amp;&amp; test -z "$(git status --porcelain -- skill/ internal/setupgen/ cmd/engram/testdata/ release-please-config.json)"</automated>
    <fails_when>non-zero exit from any link of the chain: `task` prints `task: Failed to run task` (or a golangci-lint finding / `--- FAIL` line), `license-eye` lists a file missing its header, `git diff --exit-code -- go.mod go.sum` prints a hunk, `go test ./internal/keylinks/` prints `FAIL` naming a `04-04-PLAN.md` pattern, `surfacesgen --check-setup` reports drift, or `git status --porcelain` lists a generated file</fails_when>
  </verify>
  <acceptance_criteria>
    - `task` exits 0 with no `--- FAIL:` line and no lint finding; `task license:check` exits 0.
    - `git diff --exit-code -- go.mod go.sum` exits 0; `go test ./internal/keylinks/ -count=1` prints `ok`.
    - `go run ./internal/surfacesgen --check-setup` exits 0; `git status --porcelain -- skill/ internal/setupgen/ cmd/engram/testdata/ release-please-config.json` prints nothing.
    - `git status --porcelain -- cmd/engram/` prints nothing after the plan's commits.
  </acceptance_criteria>
  <done>`task`, the license check, the dependency diff, the key-links gate, the setupgen drift gate, and the clean-generated-files check all exit 0 with a clean tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `setup.Result` → `setupRuntimeRow` → `renderOperator` (text / `--output json`) | Already-redacted scalars from internal/setup cross into the operator's terminal and into scripts consuming JSON; nothing here parses or re-derives probe content. |
| `--help` / headlines → operator | Prose an operator reads to decide whether to `--apply`; a wrong claim here would be a correct-by-reading failure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-14 | Information Disclosure | a probe-read literal reaching stdout/stderr through the row renderer in either output lane | high | mitigate | The row copies only internal/setup's redacted scalars (`Facets`, `Drift`, `Registered`, `Reason`); `TestSetupJSONNeverLeaksProbeLiteral` asserts sentinel absence on both streams in both lanes; ambiguous-read subtests assert `Registered == ""`. |
| T-04-15 | Tampering | a nested facet field (slice/map) bypassing `sanitizeViewValue`'s scalar-only branch and rendering unsanitized control characters | medium | mitigate | `Facets`/`Drift` are `string`; the three new fixture rows are walked by `TestOperatorViewFixturesHaveNoUnsanitizedNesting`, which fails on any container two levels deep. |
| T-04-16 | Repudiation | a `preserved` registration laundered into `already-correct` at the row headline, so a script reads a declined write as convergence | medium | mitigate | The fold is `setup.AggregateOutcome` with 04-01's precedence (preserved > already-correct); `setupOutcomeFoldTable` pins `{preserved, would-write} → preserved` literally; `registration=preserved` rides beside the headline in both lanes. |
| T-04-17 | Spoofing | `--help`/headline claiming opencode is compared, inviting a false sense of safety before `--apply` | low | mitigate | Both texts state opencode is not compared; `TestSetupHelpNamesDriftOutcomes` and `TestSetupPreviewSummaryNamesComparison` pin the phrases; the docs gate (04-03) pins the guide. |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | Not applicable — zero new packages; Task 3 asserts `git diff --exit-code -- go.mod go.sum`. |
</threat_model>

<verification>
- `go test ./cmd/engram -run '^(TestSetupPreviewJSONCarriesDriftFacets|TestSetupHelpNamesDriftOutcomes|TestSetupPreviewNeverClassifiesAlreadyCorrectFromAmbiguousRead|TestSetupJSONNeverLeaksProbeLiteral|TestSetupApplySummaryCountsPreserved|TestSetupPreviewSummaryNamesComparison|TestSetupViewIdentity|TestOperatorViewFixturesHaveNoUnsanitizedNesting|TestHelpGolden|TestCatalogGolden)$' -count=1 -v` shows `--- PASS` for every subtest named in the two `<behavior>` blocks.
- `rg -n -F 'func TestSetupPreviewNeverClassifiesAlreadyCorrect(' cmd/engram/setup_test.go` prints nothing (the old-invariant test is retargeted, per the orchestrator's note).
- `task`, `task license:check`, `git diff --exit-code -- go.mod go.sum`, `go test ./internal/keylinks/ -count=1`, `go run ./internal/surfacesgen --check-setup` exit 0; `git diff --stat FIRST^..LAST` touches only the four files in `files_modified`.
- Red-evidence handoff for the orchestrator (Phase 03 shape): suggested patch `04-04-facets-not-copied.patch` (drop the `Facets: r.Facets` copy in `setupRuntimeRowFromResult` → `TestSetupPreviewJSONCarriesDriftFacets`) and `04-04-apply-summary-preserved.patch` (drop the preserved bucket → `TestSetupApplySummaryCountsPreserved`).
- Scope exemption for the verifier (D-10): opencode's only assertion at this layer is the negative one — a present opencode with a convincing table never yields already-correct or preserved and its `drift` says it was not compared. This is the locked decision, not a coverage gap.
</verification>

<success_criteria>
- `preserved` is a first-class row outcome in text and JSON with a reason naming what cannot be reproduced, consistent through the aggregate fold and the apply headline (SC3's CLI half).
- A `would-write` row for an existing parsed registration names its differing facets in fixed order through `facets`, with `drift` detailing them (SC4).
- No probe-read literal reaches the CLI's output streams (SC5 at the process boundary); `--help` and both headlines teach the comparison and the opencode exemption; the old preview invariant test now pins D-09/D-10.
</success_criteria>

<output>
Create `.planning/phases/04-drift-detection-read-only/04-04-SUMMARY.md` when done
</output>