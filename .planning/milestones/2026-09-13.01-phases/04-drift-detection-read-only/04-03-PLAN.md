---
phase: 04-drift-detection-read-only
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs-site/src/content/docs/guides/agent-setup.md
  - cmd/engram/agent_setup_docs_test.go
autonomous: true
requirements:
  - REQ-drift-preserved-outcome
  - REQ-drift-facet-naming
  - REQ-drift-observed-registration

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`docs-site/src/content/docs/guides/agent-setup.md`'s results table (§Read results and repeat safely) carries a sixth row whose first cell is `preserved`, stating that the existing registration carries something setup did not author and cannot reproduce (an extra header, an unrecognized field), that setup leaves it untouched and `reason` names it, that header values read from a runtime are never shown, and — in the same row — that Claude Code and Codex both replace the whole entry on write (no partial merge), so a later `--apply` either overwrites the entry or leaves it untouched and never merges into it (REQ-drift-preserved-outcome's Codex whole-entry sentence)"
    - "The guide states plainly, in one sentence that names `opencode` and the words `not compared`, that opencode's registration is not compared because its `mcp list` prints a table setup does not parse, so a present opencode always reads `would-write` (D-10, REQ-drift-observed-registration)"
    - "The `would-write` row states that for an existing Claude Code or Codex registration the row's `facets` field names what differs — `url`, `auth-mode`, `header-name`, `header-value-ref` — and `drift` details each difference (REQ-drift-facet-naming); the `already-correct` row states that in preview it is a real comparison of URL, auth mode, and header names/references read through the runtime's own CLI, and keeps its existing after-`--apply` caveat"
    - "The preview paragraph (§Register, after the `--auth oauth` example) states that Claude Code's and Codex's existing engram registration is compared against what setup would write; the Scripts-and-JSON paragraph names the three JSON row fields `registered` (a normalized rendering with header values redacted), `facets`, and `drift` beside the existing `headers`"
    - "The exit-code sentences of the guide are unchanged in meaning: a preview still exits zero while reporting `preserved`, and the 0/8/9 apply taxonomy text is not moved (D-04 keeps preserved a non-failed attempt)"
    - "`cmd/engram/agent_setup_docs_test.go` is a `migrate_docs_test.go`-shaped gate: a pure `agentSetupGuideDriftViolations(doc string) []error` checked against the LIVE guide (skip-on-`os.IsNotExist`, fail-on-empty) and proven to fire on each injected violation while staying silent on a clean fixture"
  artifacts:
    - path: "docs-site/src/content/docs/guides/agent-setup.md"
      provides: "the preserved results row, the opencode not-compared statement, facets/drift/registered field descriptions"
      contains: "| `preserved` |"
    - path: "cmd/engram/agent_setup_docs_test.go"
      provides: "agentSetupGuideRelPath, preservedRowPattern, agentSetupGuideDriftViolations, TestAgentSetupGuideDocumentsDrift, TestAgentSetupGuideDriftGateFiresOnInjectedViolation"
      contains: "func agentSetupGuideDriftViolations(doc string) []error"
  key_links:
    - from: "cmd/engram/agent_setup_docs_test.go"
      to: "docs-site/src/content/docs/guides/agent-setup.md"
      via: "the gate reads the live guide by the same relative path idiom migrate_docs_test.go uses"
      pattern: 'agentSetupGuideRelPath = "[.][.]/[.][.]/docs-site/src/content/docs/guides/agent-setup[.]md"'
    - from: "cmd/engram/agent_setup_docs_test.go"
      to: "docs-site/src/content/docs/guides/agent-setup.md"
      via: "the preserved row is located as a markdown table row whose first cell is the backticked outcome value"
      pattern: 'preservedRowPattern'
  prohibitions:
    - statement: "MUST NOT document opencode as compared, parsed, or able to reach `already-correct`/`preserved` in preview — the guide states the opposite (D-10), and no sentence may imply its `mcp list` table is read for structure"
      category: safety
      status: resolved
      verification: test
      reason: "The gate requires a line naming opencode together with `not compared`; the positive control's `opencode_claimed_compared` fixture proves the gate fires when that line is missing — no wired check descriptor by design (spec-less fallback, flagged-unverified)"
    - statement: "MUST NOT show or imply that a header value read from a runtime is ever displayed: every documented rendering of `registered`, `drift`, or `reason` describes names and references only, with observed values redacted"
      category: safety
      status: resolved
      verification: test
      reason: "The preserved row must contain `never shown` (gate leg); the docs prose is reviewed against 04-CONTEXT.md D-02 — no wired check descriptor by design (spec-less fallback, flagged-unverified)"
---

<objective>
Document the sixth outcome and the comparison it comes from where operators read results:
`guides/agent-setup.md` gains a `preserved` row (with the Codex/Claude Code whole-entry sentence
REQ-drift-preserved-outcome requires), an explicit "opencode is not compared" statement (D-10), a
`would-write` row that points at the `facets`/`drift` fields (REQ-drift-facet-naming), an
`already-correct` row that says preview now really compares, and JSON-field descriptions for
`registered`/`facets`/`drift` — pinned by a `migrate_docs_test.go`-shaped docs gate with a
positive control, so the row can never be silently deleted or the opencode statement dropped.

Purpose: SC3 requires `preserved` "documented in guides/agent-setup.md's results table, including
Codex's whole-entry semantics"; D-10 requires the guide to state plainly that opencode is not
compared so the verifier does not read the missing opencode scanner as a gap. D-06 lists the
results-table row among the shape-independent work, so this plan runs in wave 1 beside 04-01.

The JSON field names this plan documents (`registered`, `facets`, `drift`) are the names plans
04-01 and 04-04 implement (`Result.Facets` → `facets`, `Result.Drift` → `drift`); the three plans
were authored together against one vocabulary.

Output: the edited guide and one new docs-gate test file.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-drift-detection-read-only/04-CONTEXT.md
@.planning/phases/04-drift-detection-read-only/04-RESEARCH.md
@CLAUDE.md
</context>

## Artifacts this phase produces (plan 04-03's share)

| Kind | Artifact | Created by |
|------|----------|------------|
| Docs | `guides/agent-setup.md`: `preserved` results row; rewritten `would-write` and `already-correct` rows; comparison sentence in the preview paragraph; opencode not-compared sentence; `registered`/`facets`/`drift` JSON field sentences | 04-03 Task 1 |
| Test file | `cmd/engram/agent_setup_docs_test.go`: `agentSetupGuideRelPath`, `preservedRowPattern`, `wouldWriteRowPattern`, `agentSetupGuideDriftViolations`, `TestAgentSetupGuideDocumentsDrift`, `TestAgentSetupGuideDriftGateFiresOnInjectedViolation` | 04-03 Task 1 |

## Flagged Assumptions (edge probe, spec-less fallback)

No edge-probe row is assigned to this plan (plans 04-01, 04-04, 04-05 carry all 13). Nothing is
dropped: the docs describe the behaviors those plans' truths pin.

## Prohibition probe (spec-less fallback, in-prompt recall)

Stage 1 raw candidates: must not document opencode as compared; must not show a header value in
an example; must not restate exit codes wrongly; must not delete an existing row to dodge the
gate; must not describe `registered` as raw CLI output; must not promise `--apply` honors
`preserved` (that is Phase 5 — the row describes preview's classification and the whole-entry
consequence, not an apply-time guarantee); must not break rumdl. Stage 2 keeps two (authored
above, descriptor-less): the **opencode-never-documented-as-compared** prohibition and the
**no-displayed-header-value** privacy prohibition. "Must not promise the apply gate" is enforced
by the row wording below (it says what `--apply` WOULD do to a whole entry, not that it declines —
Phase 5 documents the gate). Canon breadcrumb: none applicable.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Docs gate RED against the live guide, then the `preserved` row, the opencode statement, and the facets/drift/registered sentences</name>
  <files>cmd/engram/agent_setup_docs_test.go, docs-site/src/content/docs/guides/agent-setup.md</files>
  <read_first>
    - `cmd/engram/migrate_docs_test.go` whole file — the exact gate shape to mirror: `migrateGuideRelPath` (two-levels-up relative path idiom), `pendingRowPattern` (`(?m)^\|\s*`pending`\s*\|.*\|\s*$` — write yours with bracket classes so no backslash-escaped pattern is needed in this plan's key_links), the `*Violations(doc) []error` function, the live-file test with the `os.IsNotExist` skip and empty-file fail, and the positive-control table with its load-bearing `clean` case.
    - `docs-site/src/content/docs/guides/agent-setup.md` lines 28-45 (the preview paragraph to extend), 176-196 (the five-row results table and the exit-code paragraph that must keep its meaning), 198-212 (Scripts and JSON output — the `headers` sentence to extend).
    - `.planning/phases/04-drift-detection-read-only/04-CONTEXT.md` D-01, D-02 (the endorsed `observed <redacted>, would write ${LITELLM_KEY}` shape — use a vendor-neutral name in docs, `x-gateway-api-key`/`GATEWAY_KEY`, per the shipped-bundle privacy guard precedent in STATE.md), D-03, D-04, D-10, D-12.
    - `.planning/REQUIREMENTS.md` lines 41-45 (the five REQ-drift-* texts the rows restate).
    - `CLAUDE.md` §Conventions: `task lint` runs rumdl over markdown; `docs-site/**` carries NO SPDX header; the new Go test file DOES.
  </read_first>
  <behavior>
    Write `cmd/engram/agent_setup_docs_test.go` FIRST (SPDX header, `package main`, imports `fmt`,
    `os`, `regexp`, `strings`, `testing`):
    - `const agentSetupGuideRelPath = "../../docs-site/src/content/docs/guides/agent-setup.md"`.
    - `var preservedRowPattern = regexp.MustCompile("(?m)^[|][ ]*`preserved`[ ]*[|].*[|][ ]*$")` and
      `var wouldWriteRowPattern` for the backticked `would-write` cell, same shape.
    - `func agentSetupGuideDriftViolations(doc string) []error` with these legs, one error each:
      (1) no `preserved` row → error naming the row; (2) the preserved row lacks `whole entry` →
      error ("Codex whole-entry sentence missing"); (3) the preserved row lacks `never merge` →
      error; (4) the preserved row lacks `never shown` → error (header values are never shown);
      (5) no `would-write` row, or the row lacks `facets` → error; (6) no line of `doc` contains
      BOTH `opencode` and `not compared` (case-sensitive, same line) → error; (7) `doc` lacks the
      token `drift` within the Scripts-and-JSON section — approximate as: no line contains both
      `registered` and `facets` and `drift` → error.
    - `TestAgentSetupGuideDocumentsDrift(t)`: read the live guide via `agentSetupGuideRelPath`;
      `os.IsNotExist` → `t.Skipf`; empty → `t.Fatalf`; every violation → `t.Error`. RED: run it
      individually with `-run '^TestAgentSetupGuideDocumentsDrift$' -v` BEFORE editing the guide;
      it must FAIL with at least legs 1, 5 (facets), 6, 7 reported.
    - `TestAgentSetupGuideDriftGateFiresOnInjectedViolation(t)`: in-memory fixtures — `clean`
      (a three-line fixture: a compliant `preserved` row, a compliant `would-write` row, one line
      `opencode's registration is not compared …`, and one line naming `registered`, `facets`,
      `drift`) expects NO violation; `row_deleted`, `whole_entry_sentence_missing`,
      `never_merge_missing`, `never_shown_missing`, `would_write_omits_facets`,
      `opencode_claimed_compared` (the opencode line removed), `json_fields_missing` each expect a
      violation. The `clean` case is load-bearing.
  </behavior>
  <action>
Edit `docs-site/src/content/docs/guides/agent-setup.md`:

1. Preview paragraph (after "Codex reads local registration state."): add — "For Claude Code and
   Codex, preview also compares the existing engram registration it reads — URL, auth mode, and
   header names with their environment-variable references — against what setup would write, and
   classifies the row as `already-correct`, `would-write`, or `preserved`. opencode's registration
   is not compared: its `mcp list` prints a table setup does not parse, so a present opencode always
   reads `would-write`." (That last sentence is the D-10 statement the gate requires — keep
   `opencode` and `not compared` on ONE line when wrapping.)
2. Results table — replace the `would-write` and `already-correct` rows and add `preserved` after
   `already-correct`:
   - `would-write`: "Proposed changes are shown. For an existing Claude Code or Codex registration,
     `facets` names what differs (`url`, `auth-mode`, `header-name`, `header-value-ref`) and `drift`
     details each difference, for example `x-gateway-api-key: observed <redacted>, would write
     ${GATEWAY_KEY}`. Generic also uses this outcome after `--apply` because it only prints output."
   - `already-correct`: "In preview, the registration read through the runtime's own CLI matches
     the requested URL, auth mode, and header names and references — a real comparison, not a
     guess. After `--apply` it means the observed state matches; it does not guarantee that no
     write commands ran."
   - `preserved`: "The existing registration carries something setup did not author and cannot
     reproduce — an extra header, an unrecognized field — so setup leaves it untouched and `reason`
     names it; header values read from a runtime are never shown. Claude Code and Codex both
     replace the whole entry on write (no partial merge): a later `--apply` either overwrites the
     entry or leaves it untouched and will never merge into it."
   Keep `not-present`, `wrote`, `failed` rows byte-identical; keep the exit-code paragraph below the
   table unchanged (a preview still exits zero; `preserved` is not a failure).
3. Scripts and JSON output: extend "Each present runtime's JSON row carries a `headers` string." to
   also name `registered` (a normalized rendering of the registration the runtime's CLI reported —
   URL, auth state, header names — with every header value redacted), `facets` (the comma-joined
   differing facets, in a fixed order), and `drift` (the per-facet detail lines, or a note that the
   registration was not compared). One sentence, all three tokens on one line.

Write the test file per `<behavior>`. Run `task lint` (rumdl over the guide; table cells may be
long — rumdl's line-length rule is already configured for this table's existing rows) and fix any
markdown finding in the guide, never by disabling a rule.

Commit as `docs(setup): document the preserved outcome, facet naming, and opencode not compared`
with the explicit pathspec `docs-site/src/content/docs/guides/agent-setup.md
cmd/engram/agent_setup_docs_test.go`.
  </action>
  <verify>
    <automated>out=$(go test ./cmd/engram -run '^(TestAgentSetupGuideDocumentsDrift|TestAgentSetupGuideDriftGateFiresOnInjectedViolation)$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^(--- |    --- )(PASS|FAIL|SKIP)' | tail -n 20; test "$rc" -eq 0 &amp;&amp; printf '%s\n' "$out" | rg -q -F -e '--- PASS: TestAgentSetupGuideDocumentsDrift' &amp;&amp; test "$(printf '%s\n' "$out" | rg -c -F -e '--- PASS: TestAgentSetupGuideDriftGateFiresOnInjectedViolation/')" -ge 8 &amp;&amp; rg -q -e '^[|][ ]*`preserved`[ ]*[|].*whole entry.*never merge.*[|]' docs-site/src/content/docs/guides/agent-setup.md</automated>
    <fails_when>non-zero `go test` exit, a `--- FAIL:` line, a `--- SKIP: TestAgentSetupGuideDocumentsDrift` line (the guide was not found — a wrong relative path, not a pass), the line `testing: warning: no tests to run`, fewer than 8 positive-control subtest PASS lines, or no table row in the live guide whose first cell is `preserved` and which carries both `whole entry` and `never merge`</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n -e '^[|][ ]*`preserved`[ ]*[|]' docs-site/src/content/docs/guides/agent-setup.md` prints exactly one line, and that line contains `never shown`, `whole entry`, and `never merge`.
    - `rg -n -e 'opencode.*not compared|not compared.*opencode' docs-site/src/content/docs/guides/agent-setup.md` prints at least one line.
    - `rg -n -e '^[|][ ]*`would-write`[ ]*[|]' docs-site/src/content/docs/guides/agent-setup.md` prints exactly one line containing `facets` and `drift` and `<redacted>`.
    - `rg -n -e 'registered.*facets.*drift' docs-site/src/content/docs/guides/agent-setup.md` prints at least one line; `rg -c -F 'x-litellm' docs-site/src/content/docs/guides/agent-setup.md` prints `0` (vendor-neutral example only).
    - `rg -n -e '^[|][ ]*`(not-present|wrote|failed)`[ ]*[|]' docs-site/src/content/docs/guides/agent-setup.md` prints three lines byte-identical to the pre-phase commit's. Capture the base first: `base=$(git show ff5a6f94:docs-site/src/content/docs/guides/agent-setup.md)` exits 0 (the pinned pre-phase SHA, never a relative HEAD anchor).
    - Then `diff <(printf '%s\n' "$base" | rg -e '^[|][ ]*`(not-present|wrote|failed)`') <(rg -e '^[|][ ]*`(not-present|wrote|failed)`' docs-site/src/content/docs/guides/agent-setup.md)` prints nothing.
    - `rg -n -F 'func agentSetupGuideDriftViolations(doc string) []error' cmd/engram/agent_setup_docs_test.go` prints exactly one line; `rg -n -F 'os.IsNotExist' cmd/engram/agent_setup_docs_test.go` prints exactly one line; `rg -c -e '"(clean|row_deleted|whole_entry_sentence_missing|never_merge_missing|never_shown_missing|would_write_omits_facets|opencode_claimed_compared|json_fields_missing)"' cmd/engram/agent_setup_docs_test.go` prints `8`.
    - `rg -n -F -e '\' cmd/engram/agent_setup_docs_test.go` prints nothing — no backslash anywhere in the file (bracket classes only in the two row patterns, the same discipline this plan's key_links follow).
    - RED observed (record in SUMMARY): `go test ./cmd/engram -run '^TestAgentSetupGuideDocumentsDrift$' -count=1 -v` FAILS against the unedited guide with the `preserved` row, `facets`, opencode, and JSON-fields legs reported; GREEN after the edit.
    - `task lint` exits 0; `head -2 cmd/engram/agent_setup_docs_test.go` shows the SPDX header; `head -1 docs-site/src/content/docs/guides/agent-setup.md` is `---` (frontmatter intact, no SPDX header).
  </acceptance_criteria>
  <done>The guide documents `preserved` (with the whole-entry sentence and the never-shown-values statement), the facet-naming behavior, the real preview comparison, the opencode exemption, and the three JSON fields; a docs gate with a positive control pins all of it; `task lint` is green; committed with an explicit pathspec.</done>
</task>

<task type="auto">
  <name>Task 2: Plan gate — full `task`, license check, key-links gate, and the docs gate in the whole-package run</name>
  <files>none modified — verification-only; if `task fmt` or the license fixer rewrites one of this plan's two files, fold it into a `chore(docs)` commit with an explicit pathspec</files>
  <read_first>
    - `Taskfile.yaml` — `default` (lint + test), `lint` (golangci-lint, yamlfmt, actionlint, rumdl), `license:check`.
    - `.planning/STATE.md` §Blockers/Concerns — the key-links gate and the `-count=1`-only rule for `cmd/engram` tests.
  </read_first>
  <action>
Run `task` (lint + test across every package — the new test file compiles in `cmd/engram` beside
plan 04-01's still-in-flight `internal/setup` changes only if this plan touched nothing there; it
did not), then `task license:check`, `git diff --exit-code -- go.mod go.sum`, and `go test
./internal/keylinks/ -count=1` (this plan's key_links patterns use bracket classes for every
literal dot and no backslash — a red here is a planning-artifact defect to report, never something
to fix by editing a tool-owned file's structure). Never run `cmd/engram` tests with `-count>1`.
No commit is expected unless a formatter rewrote a file.
  </action>
  <verify>
    <automated>task &amp;&amp; task license:check &amp;&amp; git diff --exit-code -- go.mod go.sum &amp;&amp; go test ./internal/keylinks/ -count=1 &amp;&amp; go test ./cmd/engram -run '^TestAgentSetupGuide' -count=1</automated>
    <fails_when>non-zero exit from any link: `task` prints `task: Failed to run task` (a golangci-lint, rumdl, or `--- FAIL` finding), `license-eye` lists a file missing its header, `git diff --exit-code -- go.mod go.sum` prints a hunk, `go test ./internal/keylinks/` prints `FAIL` naming a `04-03-PLAN.md` pattern, or the `TestAgentSetupGuide` run prints `FAIL` or `no tests to run`</fails_when>
  </verify>
  <acceptance_criteria>
    - `task` exits 0 with no `--- FAIL:` line and no lint finding; `task license:check` exits 0.
    - `git diff --exit-code -- go.mod go.sum` exits 0; `go test ./internal/keylinks/ -count=1` prints `ok`.
    - `git status --porcelain -- docs-site/ cmd/engram/` prints nothing after the plan's commits.
  </acceptance_criteria>
  <done>`task`, the license check, the dependency diff, the key-links gate, and the docs gate all exit 0 with a clean tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs-site → operator | Prose an operator acts on; a wrong claim (e.g. "opencode is compared") would cause a false sense of safety before `--apply`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-12 | Repudiation | the guide silently losing the `preserved` row or the opencode statement in a later edit, so operators believe every runtime is compared | medium | mitigate | `agentSetupGuideDriftViolations` fails the build on a missing preserved row, missing whole-entry/never-merge/never-shown clauses, a `would-write` row without `facets`, or no opencode-not-compared line; the positive control proves each leg fires. |
| T-04-13 | Information Disclosure | a docs example showing a header VALUE, teaching operators that values are displayed | low | mitigate | The only example is `x-gateway-api-key: observed <redacted>, would write ${GATEWAY_KEY}` (vendor-neutral, value redacted); the preserved row states values are never shown (gate leg 4). |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | Not applicable — no package installed; one markdown file and one Go test file. |
</threat_model>

<verification>
- `go test ./cmd/engram -run '^TestAgentSetupGuide' -count=1 -v` shows `--- PASS` for the live-guide test and every positive-control subtest, and NO `--- SKIP` (the relative path resolves).
- `rg` checks in Task 1's acceptance criteria hold against the live guide; the three untouched rows are byte-identical to HEAD.
- `task`, `task license:check`, `git diff --exit-code -- go.mod go.sum`, `go test ./internal/keylinks/ -count=1` exit 0.
- Not gated here: the `--help` text's own mention of `preserved` (plan 04-04, `TestSetupHelpNamesDriftOutcomes`) and the generated `/engram-setup` prose (unchanged — it names no outcome vocabulary; `setupgen --check-setup` stays green in 04-04/04-05's gates).
</verification>

<success_criteria>
- `guides/agent-setup.md` documents `preserved` in its results table with the whole-entry sentence, states opencode is not compared, and describes `facets`/`drift`/`registered` (SC3's documentation half, D-10's plain statement, SC4's field names).
- A docs gate with a positive control keeps every one of those statements from being silently removed.
</success_criteria>

<output>
Create `.planning/phases/04-drift-detection-read-only/04-03-SUMMARY.md` when done
</output>
