diff --git a/cmd/engram/setup.go b/cmd/engram/setup.go
index efc728a7..6cc1022e 100644
--- a/cmd/engram/setup.go
+++ b/cmd/engram/setup.go
@@ -791,7 +791,7 @@ func setupRuntimeRowFromResult(ctx context.Context, env setup.Environment, rt se
 			p = setup.PluginPreview(ctx, env, rt, r.Binary, setupVersion())
 		}
 		outcome := setupApplyPluginFacet(&row, r.Outcome, p)
-		if p.Delivered() {
+		if !p.Delivered() {
 			outcome = setupReportNativeSkills(&row, outcome, r.Skills)
 		} else {
 			outcome = setupApplySkillsFacet(&row, outcome, r.Skills, mutate, includeContent)
