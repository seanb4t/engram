---
phase: 03-plugin-first-delivery
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/skills/environment.go
  - internal/skills/presence.go
  - internal/skills/presence_test.go
autonomous: true
requirements:
  - REQ-plugin-skips-skills-copy

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`internal/skills.Environment` gains a fourth func field `Lstat func(name string) (os.FileInfo, error)` mirroring `os.Lstat`'s signature, with `OSEnvironment.Lstat = os.Lstat` in production — a purely additive struct-field change; `Install` never calls it and every existing `Install` test passes unchanged (03-RESEARCH.md Pitfall 4, D-08)"
    - "`skills.DetectPresence(env Environment, target Target, list []Skill) Presence` is READ-ONLY: the only `Environment` fields it touches are `Lstat` and `ReadFile`; it never creates, writes, renames, unlinks, or removes anything, so setup can never delete a native skills path without an explicit future verb (D-08, REQ-plugin-skips-skills-copy: never remove a skills path without first checking for a managed symlink — here, never remove at all)"
    - "`Presence.Skills` counts the entries of `list` whose directory `filepath.Join(target.Dir, skill.Name)` `Lstat`s successfully (any error — not-exist, permission — counts as absent, report-only); `Presence.Symlink` is true when `target.Dir` itself OR any counted skill entry has `os.ModeSymlink` set — the maintainer's own hand-made per-skill symlinks and a symlinked directory are both the symlink case (D-08's `(symlink)` vs `(copy)` distinction)"
    - "`Presence.IndexBlock` is true, for `FormatAgentsMD` only, when `ReadFile(target.IndexFile)` succeeds and the package's own `scanBlock` reports any state other than `blockAbsent` (a well-formed OR malformed engram skills block is 'present' — D-09's `index block present`); a missing or unreadable index file, or any other Format, yields false"
    - "`FormatNone` yields the zero `Presence` after zero `Environment` calls (edge: a runtime with no filesystem destination has nothing to report)"
    - "No test writes outside `t.TempDir()`; the real-filesystem subtest creates its symlinks with `os.Symlink` under a temp dir and drives `OSEnvironment` there, never the operator's home (rule m45p2b4bp7)"
  artifacts:
    - path: "internal/skills/environment.go"
      provides: "Lstat seam on Environment + OSEnvironment.Lstat = os.Lstat"
      contains: "Lstat func(name string) (os.FileInfo, error)"
    - path: "internal/skills/presence.go"
      provides: "Presence struct and DetectPresence — the D-08/D-09 report-only native-destination check"
      contains: "func DetectPresence(env Environment, target Target, list []Skill) Presence"
    - path: "internal/skills/presence_test.go"
      provides: "TestDetectPresence (fake Lstat table + real TempDir symlink subtest)"
      contains: "func TestDetectPresence"
  key_links:
    - from: "internal/skills/presence.go"
      to: "internal/skills/environment.go"
      via: "DetectPresence stats through the new Lstat seam, never os.Lstat directly, so a fake Environment can script symlink-vs-copy"
      pattern: 'env[.]Lstat[(]'
    - from: "internal/skills/environment.go"
      to: "internal/skills/presence.go"
      via: "the seam is a func field mirroring os.Lstat, added exactly like ReadFile/WriteFile/MkdirAll"
      pattern: 'Lstat:[ ]+os[.]Lstat'
    - from: "internal/skills/presence.go"
      to: "internal/skills/agentsmd.go"
      via: "D-09's index-block presence reuses the package's own scanBlock classifier instead of a second marker scan"
      pattern: 'scanBlock[(]'
  prohibitions:
    - statement: "MUST NOT delete, unlink, rename, truncate, overwrite, or create any native skills path or AGENTS.md content from the presence check — `DetectPresence` reads (Lstat/ReadFile) and reports; a future explicit prune verb is the only sanctioned place for removal"
      category: safety
      status: resolved
      verification: test
      reason: "The acceptance greps hold presence.go to zero write/remove/mkdir identifiers, and TestDetectPresence's fake Environment leaves WriteFile/MkdirAll nil so any write attempt panics the test — no wired check descriptor by design (spec-less fallback, flagged-unverified)"
---

<objective>
Give `internal/skills` the one seam D-08 needs and the one report-only function D-08/D-09 describe:
an `Lstat` func field on `Environment` (so a fake can script symlink-vs-regular without touching a
real home), and `DetectPresence`, which counts the shipped skills already present under a runtime's
native destination, says whether they are symlinks or copies, and says whether Codex's `AGENTS.md`
already carries an engram skills index block — without writing, creating, or removing anything.

Purpose: REQ-plugin-skips-skills-copy's second sentence ("setup never removes a skills path without
first checking for a managed symlink") is satisfied by construction — this milestone never removes at
all (D-08 report-only; the prune verb is deferred) — but the operator must still SEE the leftover
native copy or index block beside a plugin-delivered runtime so `curating-memory` appearing twice is
diagnosable. Plan 03-03 renders this report onto the row's `skills_native` field.

Output: `Environment.Lstat`, `Presence`, `DetectPresence`, `TestDetectPresence`. One new production
file, one new test file, zero new dependencies; `Install`'s behavior is untouched.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-plugin-first-delivery/03-CONTEXT.md
@.planning/phases/03-plugin-first-delivery/03-RESEARCH.md
@.planning/phases/03-plugin-first-delivery/03-PATTERNS.md
@CLAUDE.md
</context>

## Artifacts this phase produces (plan 03-02's share)

| Kind | Artifact | Created by |
|------|----------|------------|
| Go field | `Environment.Lstat func(name string) (os.FileInfo, error)` + `OSEnvironment.Lstat: os.Lstat` in `internal/skills/environment.go` | 03-02 Task 1 |
| Go type | `Presence struct { Skills int; Symlink bool; IndexBlock bool }` in `internal/skills/presence.go` | 03-02 Task 1 |
| Go func | `DetectPresence(env Environment, target Target, list []Skill) Presence` | 03-02 Task 1 |
| Test func | `TestDetectPresence` (`internal/skills/presence_test.go`) | 03-02 Task 1 |

## Flagged Assumptions (edge probe, spec-less fallback)

This plan carries no edge-probe rows of its own: REQ-plugin-skips-skills-copy's single row
(`concurrency`) is accounted for by plan 03-03, which owns the routing decision this plan only
reports into. Phase total stays 7 authored + 4 flagged = 11 (see 03-01's table).

## Prohibition probe (spec-less fallback, in-prompt recall)

Stage 1 raw candidates: must not delete a native copy; must not remove a symlink; must not rewrite
AGENTS.md; must not follow a symlink out of the destination; must not report a partial copy as
absent; must not stat the real home in tests; must not add a home-derivation field to
`Environment`; must not make `Install` depend on `Lstat`. Stage 2 keeps one (authored above,
descriptor-less): the **read-only, never-remove** safety prohibition — the rest are routine
correctness (the table test), D-10-of-the-skills-milestone's no-home-derivation rule (already
pinned by `Environment`'s own doc comment), or 03-03's routing. Canon breadcrumb: path traversal
via a skill name is canon — `list` comes from the embedded inventory, never from operator input;
covered by `/gsd-secure-phase`, not minted here.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Lstat seam + DetectPresence — count present skills, symlink vs copy, AGENTS.md index block, read-only</name>
  <reversibility rating="reversible">One additive struct field, one new file with one exported func; undo is deleting them. D-08 is rated reversible in 03-CONTEXT.md.</reversibility>
  <files>internal/skills/environment.go, internal/skills/presence.go, internal/skills/presence_test.go</files>
  <read_first>
    - `internal/skills/environment.go` whole file — the three func fields, `OSEnvironment`, and the doc comment's "deliberately NO home-directory field" rule (every path arrives absolute from a runtime's own Plan()).
    - `internal/skills/install.go` lines 13-71 (`Format`, `Target`, `Report`, `Install`'s switch) and 97-125 (`installFiles`'s walk-skills-then-stat-then-classify loop — the STRUCTURE to mirror, read-only).
    - `internal/skills/agentsmd.go` lines 24-27 (`BlockStartMarker`/`BlockEndMarker`), 42-57 (`blockState` — `blockAbsent`/`blockWellFormed`/`blockMalformed`), 109 (`scanBlock(content []byte) (state blockState, ...)` — reuse it; do not scan markers a second way).
    - `internal/skills/inventory.go` lines 26-56 — `Skill{Name, Files}` and `Inventory()`; the five shipped skill names are what `Presence.Skills` counts.
    - `internal/skills/install_test.go` lines 21-38 (`fakeInstallEnv(store)` — the map-backed fake to extend with an `Lstat` closure), 416-470 (`TestAgentsMdPreservesSymlink` — the ONE existing real-filesystem test: `t.TempDir()` + `os.Symlink` + `OSEnvironment`, the idiom the real-FS subtest copies), and the `testSkills()` helper.
    - `.planning/phases/03-plugin-first-delivery/03-RESEARCH.md` §"Runtime State Inventory" (the maintainer's `~/.agents/skills/` hand-made symlinks are a REAL case) and Pitfall 4.
    - `.planning/phases/03-plugin-first-delivery/03-CONTEXT.md` D-08, D-09.
  </read_first>
  <behavior>
    Write `internal/skills/presence_test.go` FIRST (SPDX header), observe it fail to compile
    (`Environment` has no field `Lstat`; `DetectPresence` undefined), then implement.

    `TestDetectPresence(t)`:
    - A `fakeLstatEnv(store map[string][]byte, entries map[string]os.FileMode)` helper: `ReadFile` from
      `store` (missing → `os.ErrNotExist`), `Lstat` returning a minimal `os.FileInfo` implementation
      (a small `fakeFileInfo{name string; mode os.FileMode}` type in the test file) for a path in
      `entries` and `os.ErrNotExist` otherwise; `WriteFile` and `MkdirAll` deliberately left nil so
      any write attempt panics the test (the read-only prohibition, mechanically).
    - `list := testSkills()` (or a literal five-skill list); `dir := "/home/fake/.agents/skills"`;
      `index := "/home/fake/.codex/AGENTS.md"`.
    - Subtests:
      - `none-present`: empty entries, `Target{Format: FormatNative, Dir: dir}` → `Presence{}`.
      - `copies`: entries for `dir` (mode `os.ModeDir`) and each skill dir (`os.ModeDir`) → `Skills == len(list)`, `Symlink == false`, `IndexBlock == false`.
      - `per-skill-symlinks` (the maintainer's case): `dir` is `os.ModeDir`, each skill entry is `os.ModeSymlink` → `Skills == len(list)`, `Symlink == true`.
      - `symlinked-dir`: `dir` is `os.ModeSymlink`, skill entries `os.ModeDir` → `Symlink == true`.
      - `partial`: only two skill entries exist → `Skills == 2`.
      - `agents-md-block-present`: `Target{Format: FormatAgentsMD, Dir: dir, IndexFile: index}`, store has `index` → `"# Mine\n" + BlockStartMarker + "\n...\n" + BlockEndMarker + "\n"` → `IndexBlock == true`.
      - `agents-md-block-malformed`: index content carries `BlockStartMarker` with no end marker → `IndexBlock == true` (present, not absent — report it).
      - `agents-md-no-block`: index content `"# Mine\n"` → `IndexBlock == false`.
      - `agents-md-missing-index`: no index in store → `IndexBlock == false`, no panic.
      - `native-ignores-index`: `FormatNative` with `IndexFile` set and a block in store → `IndexBlock == false`.
      - `format-none`: `Target{Format: FormatNone}` against an env whose `Lstat`/`ReadFile` call `t.Errorf("must not be called")` → zero `Presence`.
      - `lstat-permission-error-counts-absent`: `Lstat` returns `os.ErrPermission` for one skill → that skill is not counted; no error surfaces (report-only).
    - `real-filesystem` subtest: `root := t.TempDir()`; `real := filepath.Join(root, "real")` with a
      subdirectory per skill name (`os.MkdirAll`); `link := filepath.Join(root, "skills")` created via
      `os.Symlink(real, link)`; `DetectPresence(OSEnvironment, Target{Format: FormatNative, Dir: link}, list)`
      → `Skills == len(list)`, `Symlink == true`; then a second target whose `Dir` is `real` → `Symlink == false`.
      Also assert `OSEnvironment.Lstat != nil`.
  </behavior>
  <action>
Extend `internal/skills/environment.go`: add `Lstat func(name string) (os.FileInfo, error)` as the
fourth field of `Environment`, doc-commented as mirroring `os.Lstat` (never `os.Stat` — the whole
point is to see a symlink AS a symlink) and as a report-only seam consumed by `DetectPresence`
below, never by `Install` (Pitfall 4: the three existing fields were sized for Install's read-compare-
write; a presence check needs to distinguish link types without writing). Set `Lstat: os.Lstat` in
`OSEnvironment`. Preserve the doc comment's "deliberately NO home-directory field" rule verbatim.

Create `internal/skills/presence.go` (SPDX header). Declare `type Presence struct { Skills int;
Symlink bool; IndexBlock bool }` with a doc comment per field as stated in `must_haves.truths`
(what counts, what makes Symlink true, when IndexBlock is true), and that this is D-08/D-09's
REPORT-ONLY view: the operator is told what is there so they can remove it by hand; nothing here
removes it (the prune verb is deferred, 03-CONTEXT.md Deferred Ideas). Declare `func
DetectPresence(env Environment, target Target, list []Skill) Presence`: `FormatNone` → return the zero
value immediately (no env call); otherwise `if fi, err := env.Lstat(target.Dir); err == nil &&
fi.Mode()&os.ModeSymlink != 0 { p.Symlink = true }`; for each skill in `list`: `fi, err :=
env.Lstat(filepath.Join(target.Dir, s.Name))`; `err != nil` → continue (any error — not-exist or
otherwise — is "not counted"; this is a report, and D-08's ambiguity should under-report rather than
invent); else `p.Skills++` and set `p.Symlink` when the mode has `os.ModeSymlink`; then, only for
`FormatAgentsMD` with a non-empty `IndexFile`: `content, err := env.ReadFile(target.IndexFile)`; `err
== nil` → `state, _, _, _, _, _ := scanBlock(content)`; `p.IndexBlock = state != blockAbsent`. The
function's only `Environment` touches are `Lstat` and `ReadFile` — no create, write, rename, unlink,
or remove of any kind, by construction; say so in the doc comment in prose (name the two seams it
DOES use; do not name the write seams by identifier — the acceptance grep scans the whole file,
comments included) and cite REQ-plugin-skips-skills-copy's "never removes a skills path" clause.
Imports: `os`, `path/filepath` only (the import gate
`TestSkillsPackageImportsAreGated` forbids same-module imports and non-allowlisted third parties).

Write `presence_test.go` per `<behavior>`.

Commit as `feat(skills): add Lstat seam and read-only DetectPresence for native skills` with the
explicit pathspec `internal/skills/environment.go internal/skills/presence.go
internal/skills/presence_test.go`.
  </action>
  <verify>
    <automated>out=$(go test ./internal/skills/ -run '^(TestDetectPresence|TestInstallNativeConverges|TestInstallAgentsMdConverges|TestAgentsMdPreservesSymlink|TestSkillsPackageImportsAreGated)$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^(--- |    --- )(PASS|FAIL)' | tail -n 30; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -c -F -e '--- PASS: TestDetectPresence/')" -ge 12 &amp;&amp; printf '%s\n' "$out" | rg -q -F -e '--- PASS: TestDetectPresence/real-filesystem' &amp;&amp; printf '%s\n' "$out" | rg -q -F -e '--- PASS: TestSkillsPackageImportsAreGated'</automated>
    <fails_when>non-zero `go test` exit, any `--- FAIL:` line, the line `testing: warning: no tests to run`, fewer than 12 `--- PASS: TestDetectPresence/` subtest lines, no `--- PASS: TestDetectPresence/real-filesystem` line, or no `--- PASS: TestSkillsPackageImportsAreGated` line</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n -F 'Lstat func(name string) (os.FileInfo, error)' internal/skills/environment.go` prints exactly one line; `rg -n -e 'Lstat:[ ]+os[.]Lstat' internal/skills/environment.go` prints exactly one line.
    - `rg -n -F 'func DetectPresence(env Environment, target Target, list []Skill) Presence' internal/skills/presence.go` prints exactly one line; `rg -n -F 'type Presence struct' internal/skills/presence.go` prints exactly one line.
    - `rg -c -F 'env.Lstat(' internal/skills/presence.go` prints `2` (the directory and the per-skill stat); `rg -n -F 'env.ReadFile(' internal/skills/presence.go` prints exactly one line; `rg -n -F 'scanBlock(' internal/skills/presence.go` prints exactly one line; `rg -n -F 'blockAbsent' internal/skills/presence.go` prints exactly one line.
    - `rg -n -e 'WriteFile|MkdirAll|os[.]Remove|os[.]Rename|os[.]Create|os[.]Stat[(]|os[.]Lstat[(]' internal/skills/presence.go` prints nothing (read-only through the seam; no direct os call).
    - `rg -n -e 'Lstat' internal/skills/install.go` prints nothing (`Install` never depends on the new seam).
    - `rg -n -F 'func TestDetectPresence' internal/skills/presence_test.go` prints exactly one line; `rg -n -F 'os.Symlink(' internal/skills/presence_test.go` prints at least one line; `rg -n -F 't.TempDir()' internal/skills/presence_test.go` prints at least one line.
    - RED observed (record in SUMMARY): with `presence_test.go` in place and the two production files at HEAD, `go test ./internal/skills/ -count=1` fails to compile naming `Lstat`/`DetectPresence`; after the edit the verify command passes.
    - `go test ./internal/skills/ -count=1` exits 0 (every existing `Install` test unchanged and green); `gofmt -l internal/skills/` prints nothing; `go vet ./internal/skills/` exits 0; `head -2 internal/skills/presence.go internal/skills/presence_test.go` shows the SPDX header lines.
  </acceptance_criteria>
  <done>`Environment.Lstat` exists with `os.Lstat` in production; `DetectPresence` reports count, symlink-vs-copy, and index-block presence through `Lstat`/`ReadFile` only; twelve-plus subtests including a real temp-dir symlink pass; `Install` is untouched; the change is committed with an explicit pathspec.</done>
</task>

<task type="auto">
  <name>Task 2: Plan gate — full `task`, license headers, zero go.mod drift, key-links gate</name>
  <files>none modified — verification-only; if `task fmt` or `task license:add` rewrites anything it must be one of this plan's three files_modified, and the rewrite is folded into a `chore(skills)` commit with an explicit pathspec</files>
  <read_first>
    - `Taskfile.yaml` — `default` (lint + test), `license:check`, `license:add`; `skills:vendor` is NOT involved (this plan touches no file under `skill/engram/skills/` or `internal/skills/data/`).
    - `CLAUDE.md` §Conventions — SPDX header on new in-scope Go files; never on `.planning/**`.
    - `.planning/STATE.md` §Blockers/Concerns — the Phase 1 gates (key-links bracket classes; red-evidence registration is the orchestrator's step).
  </read_first>
  <action>
Run `task` (lint + test everywhere — proves the additive `Lstat` field breaks no `skills.Environment`
literal in `cmd/engram`'s tests, which simply leave the new field nil), `task license:check`, `git
diff --exit-code -- go.mod go.sum`, and `go test ./internal/keylinks/ -count=1`. Do not touch
`internal/store/redevidence_harness_test.go`; `TestRedEvidencePatchesAreLive` is green and not this
plan's to register. Never run `cmd/engram` tests with `-count>1`.

No commit is expected unless a formatter or the license fixer rewrote one of this plan's files; then
commit `chore(skills): gate fixups for the presence seam` with the explicit pathspec of exactly the
rewritten file(s).
  </action>
  <verify>
    <automated>task &amp;&amp; task license:check &amp;&amp; git diff --exit-code -- go.mod go.sum &amp;&amp; go test ./internal/keylinks/ -count=1 &amp;&amp; go test ./internal/skills/ -count=1 -shuffle=on</automated>
    <fails_when>non-zero exit from any link of the chain: `task` prints `task: Failed to run task` (or a golangci-lint finding / `--- FAIL` line), `license-eye` lists a file as missing its header, `git diff --exit-code -- go.mod go.sum` prints a hunk, `go test ./internal/keylinks/` prints `FAIL`, or the shuffled `internal/skills` run prints `FAIL`</fails_when>
  </verify>
  <acceptance_criteria>
    - `task` exits 0 with no `--- FAIL:` line and no golangci-lint finding.
    - `task license:check` exits 0.
    - `git diff --exit-code -- go.mod go.sum` exits 0 (no output).
    - `go test ./internal/keylinks/ -count=1` exits 0 and prints `ok`.
    - `git status --porcelain -- internal/skills/` prints nothing after the plan's commits.
  </acceptance_criteria>
  <done>`task`, `task license:check`, the empty go.mod/go.sum diff, and the key-links gate all exit 0, and the working tree under `internal/skills/` is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `internal/skills.DetectPresence` → operator filesystem | Reads (`Lstat`, `ReadFile`) an absolute destination authored by a runtime's own Plan(); never writes. |
| `Presence` → report row (plan 03-03) | Count, link kind, and index-block boolean are rendered as data; nothing acts on them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-03 | Tampering | setup deleting or rewriting a native skills path or an operator's `AGENTS.md` while switching a runtime to plugin delivery | high | mitigate | `DetectPresence` is structurally read-only (`Lstat`/`ReadFile` only — acceptance greps hold `presence.go` to zero write/remove/mkdir identifiers); the test fake leaves `WriteFile`/`MkdirAll` nil so any write panics; D-08 defers removal to a future explicit verb. |
| T-03-09 | Information Disclosure | a presence report leaking file CONTENT of the operator's `AGENTS.md` | low | mitigate | Only a boolean (`IndexBlock`) is derived from the index read; content never leaves the function. |
| T-03-10 | Tampering | test suite touching the operator's real home | medium | mitigate | The real-FS subtest is confined to `t.TempDir()`; every other subtest uses a map-backed fake (rule m45p2b4bp7). |
| T-03-SC | Tampering | npm/pip/cargo installs | low | accept | Not applicable — zero new packages; `TestSkillsPackageImportsAreGated` keeps the allowlist unchanged; Task 2 asserts `git diff --exit-code -- go.mod go.sum`. |
</threat_model>

<verification>
- `go test ./internal/skills/ -run '^TestDetectPresence$' -count=1 -v` shows `--- PASS` for every subtest including `real-filesystem`.
- `rg -n -e 'WriteFile|MkdirAll|Remove' internal/skills/presence.go` prints nothing.
- `task`, `task license:check`, `git diff --exit-code -- go.mod go.sum`, and `go test ./internal/keylinks/ -count=1` exit 0.
- `git diff --stat FIRST^..LAST` touches only the three files in `files_modified`.
</verification>

<success_criteria>
- `skills.Environment.Lstat` exists (production `os.Lstat`); `DetectPresence` reports how many shipped skills already exist under a native destination, whether as symlinks or copies, and whether Codex's `AGENTS.md` already carries an engram index block — reading only, never removing (D-08, D-09).
- `Install` is byte-unchanged in behavior; `go.mod`/`go.sum` unchanged; `task`, `task license:check`, and the key-links gate green.
</success_criteria>

<output>
Create `.planning/phases/03-plugin-first-delivery/03-02-SUMMARY.md` when done
</output>
