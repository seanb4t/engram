---
phase: 03-plugin-first-delivery
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified:
  - cmd/engram/setup.go
  - cmd/engram/setup_test.go
  - cmd/engram/setup_delegation_test.go
  - cmd/engram/operator_view_setup_test.go
  - cmd/engram/testdata/help.golden
  - cmd/engram/testdata/catalog.golden
autonomous: true
requirements:
  - REQ-plugin-facet-reported
  - REQ-plugin-skips-skills-copy
  - REQ-plugin-capability-detection
  - REQ-plugin-install-or-update
  - REQ-plugin-three-way-state

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "For every PRESENT runtime, `setupRuntimeRowFromResult` runs the plugin lane exactly once — `setup.PluginApply(ctx, env, rt, r.Binary, setupVersion())` under `--apply`, `setup.PluginPreview(...)` otherwise — AFTER the registration lane returned, reusing `Result.Binary` (no second `LookPath`; a runtime whose registration lane resolved no binary gets no plugin facet), with the binary version supplied by the `setupVersion` seam (`resolvedVersion` in production — the same function `engram version` reports; D-01)"
    - "The plugin facet is seven flat-scalar `omitempty` string fields on `setupRuntimeRow` — `Plugin` (`plugin`: the facet OUTCOME, one of would-write/wrote/already-correct/failed, EMPTY when unavailable), `PluginState` (`plugin_state`: absent/outdated/current/unavailable), `PluginInstalled` (`plugin_installed`), `PluginTarget` (`plugin_target`), `PluginSource` (`plugin_source`), `PluginCommand` (`plugin_command`: the exact argv preview), `PluginNote` (`plugin_note`) — rendered identically in text and `--output json` by the existing `renderOperator` pipeline with zero bespoke rendering; a not-present runtime, opencode, and generic carry none of them (REQ-plugin-facet-reported, REQ-plugin-three-way-state; edge probe `empty` for REQ-plugin-facet-reported, resolved explicit: the facet is omitted via `omitempty` for a runtime outside the lane, and an `unavailable` runtime carries `plugin_state`+`plugin_note` but no `plugin` outcome)"
    - "Every plugin facet value is a plain Go `string` that reaches the row only through `sanitizeViewValue`'s scalar branch — never a struct, map, slice, or `json.RawMessage` — and `TestOperatorViewFixturesHaveNoUnsanitizedNesting` stays green over a new plugin-facet fixture (edge probe `encoding` for REQ-plugin-facet-reported, resolved explicit: values are UTF-8 strings compared and rendered byte-wise after C0/DEL stripping; the `skills_native` em-dash is a printable rune, not a control; version strings are compared numerically in `internal/setup`, never here)"
    - "A `failed` plugin facet folds into the row through `setup.AggregateOutcome(base, p.Outcome)` exactly like the skills facet, its reason joined onto `Reason` as `plugin: ` plus the describeFailure text via `setupJoinReason`, so a row reads `registration=wrote plugin=failed outcome=failed` with both facets visible, and `setup.Classify` yields `exitPartial` (8) when another selected runtime succeeded (REQ-plugin-facet-reported's exit-taxonomy clause, 03-CONTEXT.md discretion: like any other failed facet)"
    - "An `unavailable` plugin facet (probe failed) contributes NOTHING to aggregation: `Plugin` stays empty, `PluginState` is `unavailable`, `PluginNote` carries the probe's exit/timeout reason, and registration plus the native skills copy proceed exactly as today — the row's outcome is never failed by the probe (D-12, REQ-plugin-capability-detection)"
    - "`PluginResult.Delivered()` (attempted AND not unavailable) routes the skills facet: a delivered runtime NEVER reaches `skills.Install` — zero `skills.Environment.WriteFile`/`MkdirAll` calls, no `AGENTS.md` write — and its row reports `skills=plugin-delivered` with `skills_dest`/`skills_index` still naming the native destination and a new `skills_native` field carrying the D-08/D-09 presence report from `skills.DetectPresence(skillsEnv, target, inv)`: `none` when nothing is there, else `N skills present at DIR (symlink|copy)` and/or `index block present at INDEXFILE`, joined by `; ` and suffixed ` — remove manually to avoid duplicates`; nothing is ever deleted (D-07, D-08, D-09, REQ-plugin-skips-skills-copy)"
    - "A non-delivered present runtime (unavailable probe, opencode, generic) takes the unchanged native path through `setupApplySkillsFacet`, whose first parameter is now the already-folded base outcome (registration then plugin) and which no longer writes `row.Registration` itself — `row.Registration` is set once, from `r.Outcome`, in `setupRuntimeRowFromResult` for every present row"
    - "Under `--apply` a `current` plugin reports `plugin=already-correct plugin_state=current`, runs zero plugin write verbs, and writes zero native skill files; under preview every plugin-capable state reports `plugin=would-write` with `plugin_command` equal to the exact argv the apply lane would run, and NO write verb of any kind runs (SC2, SC3; REQ-plugin-install-or-update)"
    - "`TestSetupGeneratedInvocations`'s captured-argv assertion stays EXACT: for each runtime implementing `setup.PluginRuntime`, the expected sequence gains that runtime's list probe (`PluginProbes()`'s first value) immediately after its registration sequence in both lanes — the shared succeeding fake returns empty stdout, which is `unavailable` after one probe, so every pre-existing native-path expectation (skills mutations under apply, outcomes, exit codes) holds unchanged"
    - "`engram setup --help`'s Long text describes plugin-first delivery — Claude Code and Codex with a working `plugin` CLI receive skills, hooks, and `/engram-setup` through engram's OWN marketplace plugin (added when absent, installed when absent, updated when outdated, untouched when current), every other runtime keeps the native copy carried inside the binary, the two are mutually exclusive per runtime, an existing native copy or index block is reported not removed — and the sentence claiming a separate plugin install is unnecessary is gone; `help.golden`/`catalog.golden` are regenerated in the SAME commit; `TestSetupHelpNamesSkillsInstallation`'s no-destination-path-segment rule still holds (REQ-plugin-facet-reported's text half; SC4)"
    - "No test invokes a real `claude`/`codex` verb or touches the operator's `$HOME`: every plugin response is scripted through `fakeSetupEnvWithRun` keyed on `filepath.Base(path)` and the args slice, every skills touch goes through a fake `skills.Environment` whose `Lstat` is a map-backed closure (rule m45p2b4bp7, gotcha ryr82bf2s2)"
  artifacts:
    - path: "cmd/engram/setup.go"
      provides: "setupVersion seam, seven Plugin* + SkillsNative row fields, setupApplyPluginFacet, setupReportNativeSkills, setupNativePresenceSummary, plugin-aware setupRuntimeRowFromResult, plugin-first Long help paragraph"
      contains: "func setupApplyPluginFacet(row *setupRuntimeRow, base setup.Outcome, p setup.PluginResult) setup.Outcome"
    - path: "cmd/engram/setup_test.go"
      provides: "fakeSkillsEnv Lstat, fakePluginRun scripting helper, withFakeSetupVersion, TestSetupApplyJSONEmitsPluginFacet, TestSetupPluginDeliveredRuntimeAuthorsZeroNativeWrites, TestSetupPreviewShowsPluginArgv, TestSetupPluginUnavailableFallsBackToNative, TestSetupHelpNamesPluginDelivery"
      contains: "func TestSetupPluginDeliveredRuntimeAuthorsZeroNativeWrites"
    - path: "cmd/engram/setup_delegation_test.go"
      provides: "TestSetupGeneratedInvocations expecting each PluginRuntime's list probe after its registration sequence"
      contains: "setup.PluginRuntime"
    - path: "cmd/engram/operator_view_setup_test.go"
      provides: "plugin-facet fixtures (delivered current, failed-install-beside-wrote, unavailable) for the flat-scalar identity gate"
      contains: "PluginState:"
    - path: "cmd/engram/testdata/help.golden"
      provides: "regenerated --help golden carrying the plugin-first paragraph"
      contains: "marketplace"
    - path: "cmd/engram/testdata/catalog.golden"
      provides: "regenerated catalog golden (moves only if the catalog carries the changed text; regenerated in the same commit either way)"
      contains: "\"name\": \"setup\""
  key_links:
    - from: "cmd/engram/setup.go"
      to: "internal/setup/plugin.go"
      via: "the apply lane runs the plugin lane once per present runtime on the registration lane's resolved binary and the seam-supplied binary version (D-01, Open Question 3)"
      pattern: 'setup[.]PluginApply[(]ctx, env, rt, r[.]Binary, setupVersion[(][)][)]'
    - from: "cmd/engram/setup.go"
      to: "cmd/engram/buildversion.go"
      via: "D-01's binary-side operand is resolvedVersion — the same function engram version reports — behind a t.Cleanup-overridable seam"
      pattern: 'var setupVersion = resolvedVersion'
    - from: "cmd/engram/setup.go"
      to: "internal/skills/presence.go"
      via: "the D-08/D-09 presence report is read through the skills package's read-only DetectPresence, never a direct os call"
      pattern: 'skills[.]DetectPresence[(]skillsEnv, target, inv[)]'
    - from: "cmd/engram/setup.go"
      to: "internal/setup/aggregate.go"
      via: "the plugin facet folds through the same authored precedence the skills facet uses (failed > wrote > already-correct > would-write)"
      pattern: 'setup[.]AggregateOutcome[(]base, p[.]Outcome[)]'
    - from: "cmd/engram/setup_delegation_test.go"
      to: "internal/setup/plugin.go"
      via: "the exact-argv test learns each runtime's list probe from the runtime itself, never a hardcoded literal"
      pattern: 'rt[.][(]setup[.]PluginRuntime[)]'
  prohibitions:
    - statement: "MUST NOT call `skills.Install`, or any `skills.Environment.WriteFile`/`MkdirAll`, for a runtime whose plugin facet reports `Delivered()`, and MUST NOT write, splice, or rewrite an `AGENTS.md` index block for it — plugin delivery and the native copy are mutually exclusive per runtime per run"
      category: safety
      status: resolved
      verification: test
      reason: "TestSetupPluginDeliveredRuntimeAuthorsZeroNativeWrites counts WriteFile/MkdirAll calls across an --apply with both runtimes current and asserts zero, and asserts the seeded AGENTS.md bytes are unchanged — no wired check descriptor by design (spec-less fallback, flagged-unverified)"
    - statement: "MUST NOT let a plugin probe failure produce a `failed` row, suppress registration, or suppress the native skills copy — `unavailable` is reported on `plugin_state`/`plugin_note` and contributes nothing to the aggregated outcome"
      category: transparency
      status: resolved
      verification: test
      reason: "TestSetupPluginUnavailableFallsBackToNative scripts a list probe exiting 1 under --apply and asserts outcome=wrote, plugin empty, plugin_state=unavailable, skills=wrote with mutations greater than zero — no wired check descriptor by design (spec-less fallback, flagged-unverified)"
    - statement: "MUST NOT collapse the registration and plugin outcomes into one field or silently launder a failed plugin install into a successful row — `registration`, `plugin`, and `skills` each keep their own value and the aggregate follows the authored precedence"
      category: transparency
      status: resolved
      verification: test
      reason: "TestSetupApplyJSONEmitsPluginFacet asserts registration=wrote beside plugin=failed on the same row with outcome=failed and exitPartial — no wired check descriptor by design (spec-less fallback, flagged-unverified)"
---

<objective>
Compose plan 03-01's plugin lane and plan 03-02's presence report into the `engram setup` report row:
a `plugin` facet of seven flat scalars beside `registration` and `skills` (text and JSON), folded
into the aggregated outcome so a `wrote` registration beside a `failed` plugin install stays visible
and reaches `exitPartial` (REQ-plugin-facet-reported); D-07 routing so a plugin-delivered runtime
never receives the native skills copy or an `AGENTS.md` block, with the leftover-native report on a
new `skills_native` field (D-08, D-09, REQ-plugin-skips-skills-copy); D-12's unavailable fallback
leaving registration and the native copy exactly as today; and `--help` telling the truth about
plugin-first delivery with the goldens regenerated in the same commit.

Purpose: SC3 and SC4 of the phase live here — the three-way state must be reported, the mutual
exclusion must be enforced at the one call site that invokes `skills.Install`, and the failed-plugin
case must be legible. Plan 03-04 renders the claude-code plugin argv into `/engram-setup`
independently of this plan.

Output: `setupVersion`, `setupApplyPluginFacet`, `setupReportNativeSkills`,
`setupNativePresenceSummary`, eight new row fields, the plugin-aware `setupRuntimeRowFromResult`,
the help paragraph, regenerated goldens, view fixtures, and five new tests plus the updated
`TestSetupGeneratedInvocations`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-plugin-first-delivery/03-CONTEXT.md
@.planning/phases/03-plugin-first-delivery/03-RESEARCH.md
@.planning/phases/03-plugin-first-delivery/03-PATTERNS.md
@.planning/phases/03-plugin-first-delivery/03-01-SUMMARY.md
@.planning/phases/03-plugin-first-delivery/03-02-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces (plan 03-03's share)

| Kind | Artifact | Created by |
|------|----------|------------|
| Go var | `setupVersion = resolvedVersion` (func seam, `cmd/engram/setup.go`) | 03-03 Task 1 |
| Go fields | `setupRuntimeRow.Plugin/PluginState/PluginInstalled/PluginTarget/PluginSource/PluginCommand/PluginNote/SkillsNative` (json: `plugin`, `plugin_state`, `plugin_installed`, `plugin_target`, `plugin_source`, `plugin_command`, `plugin_note`, `skills_native`) | 03-03 Task 1 |
| Go func | `setupApplyPluginFacet(row *setupRuntimeRow, base setup.Outcome, p setup.PluginResult) setup.Outcome` | 03-03 Task 1 |
| Go func | `setupReportNativeSkills(row *setupRuntimeRow, base setup.Outcome, planTarget setup.SkillTarget) setup.Outcome`; `setupNativePresenceSummary(p skills.Presence, dir, indexFile string) string` | 03-03 Task 1 |
| Go signature | `setupRuntimeRowFromResult(ctx context.Context, env setup.Environment, rt setup.Runtime, r setup.Result, headers string, mutate bool, includeContent bool) setupRuntimeRow`; `setupApplySkillsFacet(row, base setup.Outcome, planTarget, mutate, includeContent)` (first param renamed, no longer writes Registration) | 03-03 Task 1 |
| Go const | `setupSkillsPluginDelivered = "plugin-delivered"` (the `skills` value on the plugin path) | 03-03 Task 1 |
| Test helpers | `fakeSkillsEnv` gains `Lstat`; `fakeSkillsEnvWithEntries(entries map[string]os.FileMode, store map[string][]byte)`; `fakePluginRun(...)`; `withFakeSetupVersion(t, v string)` | 03-03 Task 1 |
| Test funcs | `TestSetupApplyJSONEmitsPluginFacet`, `TestSetupPluginDeliveredRuntimeAuthorsZeroNativeWrites`, `TestSetupPreviewShowsPluginArgv`; `TestSetupGeneratedInvocations` updated | 03-03 Task 1 |
| Test funcs | `TestSetupPluginUnavailableFallsBackToNative`, `TestSetupHelpNamesPluginDelivery`; three plugin fixtures in `setupViewFixtures()` | 03-03 Task 2 |
| Regenerated | `cmd/engram/testdata/help.golden`, `cmd/engram/testdata/catalog.golden` | 03-03 Task 2 |

## Flagged Assumptions (edge probe, spec-less fallback)

This plan accounts for **2 authored + 1 flagged = 3** of the phase's 11 rows (03-01: 5 authored +
2 flagged; 03-04: 2 flagged). Phase total 7 authored + 4 flagged = 11, no silent drop.

| Requirement | Probe category | Disposition |
|---|---|---|
| REQ-plugin-facet-reported | `empty` | **Authored (resolved, explicit)** — second truth: every facet field is `omitempty`; a not-present row, opencode, and generic carry no plugin field; an `unavailable` row carries `plugin_state` + `plugin_note` and an EMPTY `plugin` outcome. Pinned by `TestSetupPluginUnavailableFallsBackToNative` and the view fixtures. |
| REQ-plugin-facet-reported | `encoding` | **Authored (resolved, explicit)** — third truth: flat strings only, rendered through `sanitizeViewValue`; `TestOperatorViewFixturesHaveNoUnsanitizedNesting` over the new fixtures is the mechanical proof; the `skills_native` em-dash is a printable rune. |
| REQ-plugin-skips-skills-copy | `concurrency` | **Flagged assumption (unresolved — not applicable).** `engram setup` is a single-process CLI; per run, per runtime, exactly one of `skills.Install` or the presence report executes, decided by one `PluginResult` computed once. Two operators running `--apply` at the same time race inside the runtime's own plugin store and the filesystem — neither is a property this command can observe or lock. Recorded rather than resolved with a backstop so the row is not silently dropped. |

## Prohibition probe (spec-less fallback, in-prompt recall)

Stage 1 raw candidates: must not install native skills beside a plugin; must not write AGENTS.md for
a plugin-delivered runtime; must not delete a leftover native copy; must not fail the row on a probe
failure; must not hide a failed plugin behind a wrote registration; must not nest a struct into the
row; must not run a plugin write verb in preview; must not hardcode a runtime name in the composer;
must not read the real version in tests; must not run a real CLI. Stage 2 keeps three (authored
above, descriptor-less): the **never-install-natively-beside-a-plugin** safety prohibition, the
**probe-failure-never-fails-the-row** transparency prohibition, and the **facets-stay-separate**
transparency prohibition. The rest are routine correctness (flat-scalar gate, the fake seams) or
already pinned truths. Canon breadcrumb: vendor stderr reaching the terminal is canon
(`boundCapture` + `sanitizeViewValue` already bound and strip it); covered by `/gsd-secure-phase`.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end plugin facet in the report — one --apply run yields registration=wrote beside plugin=failed at exitPartial, a current plugin yields zero native writes with the leftover-native report</name>
  <reversibility rating="reversible">Additive row fields, one seam var, two composer functions, one signature widening; undo is deleting them and restoring the two call sites. Nothing one-way.</reversibility>
  <files>cmd/engram/setup.go, cmd/engram/setup_test.go, cmd/engram/setup_delegation_test.go</files>
  <read_first>
    - `cmd/engram/setup.go` lines 30-43 (`setupEnv`/`skillsEnv` seams — the shape `setupVersion` copies), 57-70 (`setupSkillsTarget`), 108-113 (`setupJoinReason`), 130-182 (`setupApplySkillsFacet` — the facet-composition pattern; its FIRST line writes `row.Registration`, which this task relocates), 306-376 (`setupRuntimeRow`'s doc comment and struct — the seven skills fields are the template), 405-412 (`setupBuildRows`), 587-609 (`setupRuntimeRowFromResult`), 611-628 (`setupResultsFromRows` — maps only `Outcome` into `Classify`; no change), 662-700 (`setupApplyRun`'s per-runtime loop and exit mapping).
    - `cmd/engram/buildversion.go` lines 141-166 — `resolvedVersion()`: the D-01 operand; `dev` or `X.Y.Z-dev.0+g` plus a short hash in a test binary, which is why tests MUST inject a version through the new seam.
    - `internal/setup/plugin.go` (03-01) — `PluginResult` fields, `Delivered()`, `PluginPreview`/`PluginApply` signatures, `PluginRuntime.PluginProbes()`; `internal/setup/claudecode.go`/`codex.go` (03-01) — the exact argv the assertions pin.
    - `internal/skills/presence.go` (03-02) — `Presence{Skills, Symlink, IndexBlock}`, `DetectPresence(env, target, list)`; `internal/skills/environment.go` — the `Lstat` field the cmd/engram fakes must now set.
    - `cmd/engram/setup_test.go` lines 23-110 (`fakeSetupEnvSucceedingRun`, `fakeSetupEnv`, `fakeSetupEnvWithRun`, `fakeSkillsEnv`, `withFakeSetupEnv`), 1244-1300 (`TestSetupPartialExitIsLiveProducible` — distinguishing runtimes by `path` inside the fake, the exitPartial assertion shape), 1414-1480 (`TestSetupSkillsFailureReachesPartialExit` — counting skills writes via a wrapped `skillsEnv`), 1625-1700 (`setupE2ERecorder.skillsEnv()` — a second `skills.Environment` literal that gains an `Lstat` returning `os.ErrNotExist`).
    - `cmd/engram/setup_delegation_test.go` lines 21-130 — `TestSetupGeneratedInvocations`'s `calls`/`wantCalls` exact-sequence assertion (the fake returns `setup.RunResult{}` — empty stdout — for everything, so every runtime's plugin lane is `unavailable` after ONE probe).
    - `internal/setup/apply.go` lines 296-383 — `execute`'s two `mcp get` reads: byte-IDENTICAL probe captures classify `already-correct`, DIFFERENT captures classify `wrote` — a test that wants `registration=wrote` must script a per-call-distinct `mcp get` stdout.
    - `.planning/phases/03-plugin-first-delivery/03-CONTEXT.md` D-07, D-08, D-09, D-12, §"Claude's Discretion" (facet field names; a failed plugin beside a wrote registration → exitPartial like any other failed facet), §"Specific Ideas" (the maintainer's own preview text; the negative-space tests).
    - `.planning/phases/03-plugin-first-delivery/03-RESEARCH.md` Pattern 2 (facet composition in cmd/engram), Pattern 3 resolution (routing decided from the plugin lane's outcome AFTER Plan() returned), Pitfall 6 (five-value Outcome vocabulary — `unavailable` is a string facet, never a sixth Outcome).
  </read_first>
  <behavior>
    Write the tests FIRST; observe compile failures (`setupVersion`, `Plugin` fields undefined) and
    the `TestSetupGeneratedInvocations` argv mismatch once the composer runs the probe; then implement.

    Test scaffolding in `cmd/engram/setup_test.go`:
    - `fakeSkillsEnv()` gains `Lstat: func(string) (os.FileInfo, error) { return nil, os.ErrNotExist }`.
      Add `fakeSkillsEnvWithEntries(entries map[string]os.FileMode, store map[string][]byte)
      skills.Environment` — same map-backed ReadFile/WriteFile/MkdirAll over `store`, plus an `Lstat`
      returning a minimal `os.FileInfo` (a `fakeFileInfo{name, mode}` type) for a path in `entries`,
      else `os.ErrNotExist`. Give `setupE2ERecorder.skillsEnv()`'s literal an `Lstat` returning
      `os.ErrNotExist` too.
    - `withFakeSetupVersion(t *testing.T, v string)`: sets `setupVersion = func() string { return v }`
      with `t.Cleanup` restoring the original (mirrors `withFakeSetupEnv`).
    - `fakePluginRun(base func(context.Context, string, []string) (setup.RunResult, error), script
      map[string]map[string]setup.RunResult) func(...)`: outer key `filepath.Base(path)` (`claude`/
      `codex`), inner key `strings.Join(args, " ")`; a scripted key returns its RunResult, anything else
      delegates to `base`. Plus a recording wrapper capturing `append([]string{filepath.Base(path)},
      args...)` into a `*[][]string` (the delegation test's own idiom). For `registration=wrote` cases,
      `base` returns a distinct stdout per `mcp get` call (a counter: `fmt.Sprintf("read %d", n)`), so
      execute's two reads differ.
    - Fixture literals: `claudeListEmptyJSON = "[]"`, `claudeListCurrentJSON =
      `[{"id":"engram@engram","version":"0.16.1","scope":"user"}]``, `claudeMarketplaceAbsentText =
      "❯ other\n    Source: GitHub (someone/other)\n"`, `claudeMarketplacePresentText = "❯ engram\n
      Source: GitHub (seanb4t/engram)\n"`, `codexListEmptyJSON = `{"installed":[],"available":[]}``,
      `codexListCurrentJSON = `{"installed":[{"pluginId":"engram@engram","name":"engram",
      "marketplaceName":"engram","version":"0.16.1"}],"available":[]}``, `codexMarketplacePresentText =
      "MARKETPLACE  ROOT\nengram  /home/fake/.codex/plugins/marketplaces/engram\n"`.

    `TestSetupApplyJSONEmitsPluginFacet(t)` — `resetClientFlags`, `resetCommandFlagState`,
    `withFakeSetupVersion(t, "0.16.1")`, `withFakeSetupEnv(t, fakeSetupEnvWithRun(recording(
    fakePluginRun(counterBase, script)), "claude", "codex"))` where script: claude `plugin list --json`
    → `claudeListEmptyJSON`; claude `plugin marketplace list` → `claudeMarketplaceAbsentText`; claude
    `plugin install engram@engram --scope user --json -y` → `{ExitCode: 1, Stderr: "boom: install
    refused"}`; codex `plugin list --json` → `codexListEmptyJSON`; codex `plugin marketplace list` →
    `codexMarketplacePresentText` (every unscripted call, including both `marketplace add`, `add`, and
    the registration verbs, exits 0). Wrap `skillsEnv.WriteFile`/`MkdirAll` to count mutations. Run
    `setup --url https://engram.example.com/mcp --runtime claude-code,codex --output json --apply`.
    Assert: err non-nil and `exitCodeFromError(err) == exitPartial`; stdout parses into
    `setupReportDoc` with 2 rows; claude row: `Outcome == "failed"`, `Registration == "wrote"`,
    `Plugin == "failed"`, `PluginState == "absent"`, `PluginTarget == "0.16.1"`, `PluginInstalled ==
    ""`, `PluginCommand == "claude plugin marketplace add seanb4t/engram --scope user; claude plugin
    install engram@engram --scope user --json -y"`, `Reason` contains `plugin: claude-code: claude
    plugin install engram@engram --scope user --json -y exited 1: 'boom: install refused'`, `Skills ==
    "plugin-delivered"`, `SkillsNative == "none"`; codex row: `Outcome == "wrote"`, `Registration ==
    "wrote"`, `Plugin == "wrote"`, `PluginState == "absent"`, `PluginSource ==
    "/home/fake/.codex/plugins/marketplaces/engram"`, `PluginCommand == "codex plugin add
    engram@engram --json"`, `Skills == "plugin-delivered"`. Skills mutations == 0 for the whole run
    (both runtimes delivered — a failed install still never falls back to a native copy in the same
    run, because the re-run that succeeds would then leave duplicates). Recorded calls contain
    `["claude","plugin","marketplace","add","seanb4t/engram","--scope","user"]` followed by
    `["claude","plugin","install","engram@engram","--scope","user","--json","-y"]`, and
    `["codex","plugin","add","engram@engram","--json"]` with NO `["codex","plugin","marketplace","add",...]`.

    `TestSetupPluginDeliveredRuntimeAuthorsZeroNativeWrites(t)` — both runtimes present, version
    `0.16.1`; script claude list → `claudeListCurrentJSON`, claude marketplace → present; codex list →
    `codexListCurrentJSON`, codex marketplace → present; `base` returns IDENTICAL `mcp get` output
    (`fakeSetupEnvSucceedingRun`) so registration is `already-correct`. `skillsEnv =
    fakeSkillsEnvWithEntries(entries, store)` where `entries` has `/home/fake/.agents/skills` as
    `os.ModeDir` and, for each `s` in `skills.Inventory()`, `/home/fake/.agents/skills/<s.Name>` as
    `os.ModeSymlink`; `store["/home/fake/.codex/AGENTS.md"]` = `"# Mine\n" + skills.BlockStartMarker +
    "\nold\n" + skills.BlockEndMarker + "\n"`; wrap WriteFile/MkdirAll to count. Run `--apply --output
    json`. Assert: err nil (exit 0); both rows `Outcome == "already-correct"`, `Plugin ==
    "already-correct"`, `PluginState == "current"`, `PluginInstalled == "0.16.1"`, `PluginTarget ==
    "0.16.1"`, `PluginCommand == ""`, `PluginNote == ""`, `Skills == "plugin-delivered"`; claude
    `SkillsDest == "/home/fake/.claude/skills"`, `SkillsNative == "none"`; codex `SkillsDest ==
    "/home/fake/.agents/skills"`, `SkillsIndex == "/home/fake/.codex/AGENTS.md"`, `SkillsNative ==
    fmt.Sprintf("%d skills present at /home/fake/.agents/skills (symlink); index block present at
    /home/fake/.codex/AGENTS.md — remove manually to avoid duplicates", len(inv))`; mutations == 0;
    `store["/home/fake/.codex/AGENTS.md"]` byte-identical to the seed (D-09: no AGENTS.md write);
    recorded calls contain NO sequence beginning `plugin install`, `plugin update`, `plugin add`,
    `plugin remove`, or `plugin marketplace add` for either binary; `SkillsDigest`, `SkillsBytes`,
    `SkillsContent` are empty on both rows (nothing was installed natively).

    `TestSetupPreviewShowsPluginArgv(t)` — claude only, version `0.16.1`, list `[]`, marketplace
    absent; preview `--output json` → err nil; row `Outcome == "would-write"`, `Registration ==
    "would-write"`, `Plugin == "would-write"`, `PluginState == "absent"`, `PluginCommand ==
    "claude plugin marketplace add seanb4t/engram --scope user; claude plugin install engram@engram
    --scope user --json -y"`, `Command` equals the real `setup.ClaudeCode.Plan(...).Display()` for
    oauth (registration argv untouched), `Skills == "plugin-delivered"`; recorded calls EXACTLY
    `[["claude","mcp","get","engram"],["claude","plugin","list","--json"],["claude","plugin","marketplace","list"]]`
    — no write verb; skills mutations == 0. Then the text lane: rerun with `--output text` and assert
    stdout contains `plugin=would-write`, `plugin_state=absent`, `skills=plugin-delivered`, and
    `plugin_command=` (the text row renders the same keys).

    `cmd/engram/setup_delegation_test.go` — in `TestSetupGeneratedInvocations`'s per-runtime loop,
    after the existing registration-sequence expectations (probe; under apply also actions + probe),
    append: `if pr, ok := rt.(setup.PluginRuntime); ok { list, _ := pr.PluginProbes(); wantCalls =
    append(wantCalls, list) }` — in BOTH lanes (the plugin lane runs in preview too, D-11; the fake's
    empty stdout is `unavailable` after that one probe, so no marketplace probe and no action
    follows). The decline branches (`ErrHeaderUnsupported`, `ErrAuthModeUnsupported`) `continue`
    BEFORE this append: a runtime whose Plan() failed resolved no binary, so its plugin lane never ran.
    Every other assertion in that test (mutations under apply, outcomes, exit codes) is unchanged.
  </behavior>
  <action>
In `cmd/engram/setup.go`:

Add `var setupVersion = resolvedVersion` beside `setupEnv`/`skillsEnv` with a doc comment: the
binary-side operand of D-01's plugin-version comparison, `resolvedVersion` (buildversion.go) in
production — the SAME function `engram version` reports, never the raw `version` ldflags var —
behind a package-level seam so a test injects a release core (a test binary resolves to `dev` or a
derived `-dev.0+g` form, which D-03 deliberately never compares).

Extend `setupRuntimeRow` with eight `string` fields after `SkillsContent`, each `omitempty`:
`Plugin` (`json:"plugin,omitempty"`), `PluginState` (`plugin_state`), `PluginInstalled`
(`plugin_installed`), `PluginTarget` (`plugin_target`), `PluginSource` (`plugin_source`),
`PluginCommand` (`plugin_command`), `PluginNote` (`plugin_note`), and `SkillsNative`
(`skills_native`). Extend the struct's doc comment with a paragraph: the plugin facet is the THIRD
facet the aggregate folds (D-06 of the skills milestone generalized); `Plugin` is the facet's
outcome (one of the five `Outcome` values, EMPTY when the probe found no working plugin CLI — D-12
reports `unavailable` on `PluginState` and the reason on `PluginNote` without ever producing an
outcome to fold); `PluginCommand` is the exact argv `--apply` runs (SC2); `PluginSource` is the
observed marketplace source so a fork stays visible (D-05); `SkillsNative` is D-08/D-09's report of
what already sits under the native destination beside a plugin-delivered runtime; every field takes
Config's "never a struct, map, slice, or raw-message" constraint for the same
`TestOperatorViewFixturesHaveNoUnsanitizedNesting` reason. Add `const setupSkillsPluginDelivered =
"plugin-delivered"` — the `Skills` value on the plugin path (not an `Outcome`: nothing was written
or compared natively; the plugin facet carries the delivery outcome).

Add `func setupApplyPluginFacet(row *setupRuntimeRow, base setup.Outcome, p setup.PluginResult)
setup.Outcome`: `!p.Attempted` → return `base` unchanged (opencode, generic, or no resolved binary —
no facet at all); otherwise write `row.PluginState = string(p.State)`, `row.PluginInstalled =
p.Installed`, `row.PluginTarget = p.Target`, `row.PluginSource = p.Source`, `row.PluginCommand =
p.Command`, `row.PluginNote = p.Note`; `p.Outcome == ""` (unavailable) → return `base` (nothing to
fold — D-12); else `row.Plugin = string(p.Outcome)`; when `p.Reason != ""` → `row.Reason =
setupJoinReason(row.Reason, "plugin: "+p.Reason)`; `return setup.AggregateOutcome(base, p.Outcome)`
— EXACTLY that expression. Doc-comment it as the third facet composed the way
`setupApplySkillsFacet` composes the second (03-RESEARCH.md Pattern 2), and that a `failed` plugin
folds to a failed row which `Classify` then places in `exitPartial` beside any succeeding runtime,
"like any other failed facet" (03-CONTEXT.md discretion).

Add `func setupNativePresenceSummary(p skills.Presence, dir, indexFile string) string`: collect
parts — when `p.Skills > 0`: `fmt.Sprintf("%d skills present at %s (%s)", p.Skills, dir, kind)` with
`kind` `symlink` when `p.Symlink` else `copy`; when `p.IndexBlock`: `fmt.Sprintf("index block present
at %s", indexFile)`; zero parts → return `"none"`; else `strings.Join(parts, "; ") + " — remove
manually to avoid duplicates"` (D-08's own example wording, D-09's `index block present`). Add `func
setupReportNativeSkills(row *setupRuntimeRow, base setup.Outcome, planTarget setup.SkillTarget)
setup.Outcome`: `target, err := setupSkillsTarget(planTarget)`; on error → `row.Skills =
string(setup.OutcomeFailed)`, join `skills: NAME: ERR` onto Reason, return
`setup.AggregateOutcome(base, setup.OutcomeFailed)` (an authoring bug stays visible exactly as on
the native path); `inv, invErr := skills.Inventory()`; on error → the same failed shape with `skills:
ERR`; else `pres := skills.DetectPresence(skillsEnv, target, inv)` — EXACTLY that call — then
`row.Skills = setupSkillsPluginDelivered`, `row.SkillsDest = target.Dir`, `row.SkillsIndex =
target.IndexFile`, `row.SkillsNative = setupNativePresenceSummary(pres, target.Dir,
target.IndexFile)`, and return `base` (the skills facet contributes nothing to the aggregate on the
plugin path: nothing was written or compared natively — the plugin facet already carries the
delivery outcome). Never call `skills.Install` here (if the doc comment names it, write it without
call parentheses — the acceptance criterion counts call sites); doc-comment D-07 (nothing is added to
the native directory), D-08 (never deleted — report with path, count, symlink-vs-copy), D-09 (an
existing index block is left in place and reported; no `AGENTS.md` write).

Change `setupApplySkillsFacet`'s first parameter from `registrationOutcome` to `base` (the fold of the
facets already composed — registration, then plugin) and DELETE its first statement (the
`row.Registration` assignment); every `return setup.AggregateOutcome(registrationOutcome, ...)`
becomes `setup.AggregateOutcome(base, ...)`. Update its doc comment accordingly (AggregateOutcome is
symmetric and associative — `aggregate.go` — so a three-way fold via two two-way folds is exact).

Widen `setupRuntimeRowFromResult` to `(ctx context.Context, env setup.Environment, rt setup.Runtime,
r setup.Result, headers string, mutate bool, includeContent bool)`. Inside `if r.Present`: set
`row.Headers = headers` and `row.Registration = string(r.Outcome)`; then `var p setup.PluginResult;
if mutate { p = setup.PluginApply(ctx, env, rt, r.Binary, setupVersion()) } else { p =
setup.PluginPreview(ctx, env, rt, r.Binary, setupVersion()) }` — EXACTLY those two calls (the lane
reuses the registration lane's resolved `Result.Binary`; Open Question 3 decided: no second
`LookPath`); `outcome := setupApplyPluginFacet(&row, r.Outcome, p)`; then `if p.Delivered() {
outcome = setupReportNativeSkills(&row, outcome, r.Skills) } else { outcome =
setupApplySkillsFacet(&row, outcome, r.Skills, mutate, includeContent) }`; `row.Outcome =
string(outcome)`. Doc-comment the routing rule: delivery is decided by CAPABILITY (a working plugin
CLI), not by the install's success — a plugin-capable runtime never receives the native copy in the
same run even when its install failed, because the re-run that succeeds would otherwise leave
`curating-memory` twice, the defect this phase exists to prevent; the failed facet is visible and
`--apply` is re-runnable. Update the two call sites: `setupBuildRows` passes `ctx, env, rt`;
`setupApplyRun` passes `ctx, setupEnv, rt`.

Write the tests and helpers per `<behavior>` in `cmd/engram/setup_test.go` (`fakeSkillsEnv` +
`Lstat`, `fakeSkillsEnvWithEntries`, `withFakeSetupVersion`, `fakePluginRun`, the three tests) and
update `cmd/engram/setup_delegation_test.go` per `<behavior>`. Every test in this file that touches
`setupVersion` must go through `withFakeSetupVersion` (t.Cleanup restore) — never assign the seam
directly.

Commit as `feat(setup): report plugin delivery as its own facet and skip the native copy for a
plugin-delivered runtime` with the explicit pathspec `cmd/engram/setup.go cmd/engram/setup_test.go
cmd/engram/setup_delegation_test.go`.
  </action>
  <verify>
    <automated>out=$(go test ./cmd/engram -run '^(TestSetupApplyJSONEmitsPluginFacet|TestSetupPluginDeliveredRuntimeAuthorsZeroNativeWrites|TestSetupPreviewShowsPluginArgv|TestSetupGeneratedInvocations|TestSetupPartialExitIsLiveProducible|TestSetupSkillsFailureReachesPartialExit|TestSetupIndexReadFailureReachesPartialExit|TestSetupReportCoversEveryRuntimeShape|TestSetupPreviewExecutesNoRuntimeCLI|TestSetupPreviewNeverClassifiesAlreadyCorrect)$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^(--- |    --- )(PASS|FAIL)' | tail -n 40; test "$rc" -eq 0 &amp;&amp; printf '%s\n' "$out" | rg -q -F -e '--- PASS: TestSetupApplyJSONEmitsPluginFacet' &amp;&amp; printf '%s\n' "$out" | rg -q -F -e '--- PASS: TestSetupPluginDeliveredRuntimeAuthorsZeroNativeWrites' &amp;&amp; printf '%s\n' "$out" | rg -q -F -e '--- PASS: TestSetupPreviewShowsPluginArgv' &amp;&amp; test "$(printf '%s\n' "$out" | rg -c -F -e '--- PASS: TestSetupGeneratedInvocations/')" -ge 5</automated>
    <fails_when>non-zero `go test` exit, any `--- FAIL:` line, the line `testing: warning: no tests to run`, no `--- PASS:` line for any of the three new tests, or fewer than 5 `--- PASS: TestSetupGeneratedInvocations/` case lines (the exact-argv test lost a case or its argv expectation no longer matches the composer's probe sequence)</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n -F 'var setupVersion = resolvedVersion' cmd/engram/setup.go` prints exactly one line.
    - `rg -c -e 'json:"(plugin|plugin_state|plugin_installed|plugin_target|plugin_source|plugin_command|plugin_note|skills_native),omitempty"' cmd/engram/setup.go` prints `8`; `rg -n -e 'Plugin[A-Za-z]* +(map|\[\]|json\.RawMessage|struct)' cmd/engram/setup.go` prints nothing (flat scalars only).
    - `rg -n -F 'func setupApplyPluginFacet(row *setupRuntimeRow, base setup.Outcome, p setup.PluginResult) setup.Outcome' cmd/engram/setup.go` prints exactly one line; `rg -n -F 'setup.AggregateOutcome(base, p.Outcome)' cmd/engram/setup.go` prints exactly one line; `rg -n -F '"plugin: "+p.Reason' cmd/engram/setup.go` prints exactly one line.
    - `rg -n -F 'setup.PluginApply(ctx, env, rt, r.Binary, setupVersion())' cmd/engram/setup.go` and `rg -n -F 'setup.PluginPreview(ctx, env, rt, r.Binary, setupVersion())' cmd/engram/setup.go` each print exactly one line; `rg -n -F 'p.Delivered()' cmd/engram/setup.go` prints exactly one line.
    - `rg -n -F 'skills.DetectPresence(skillsEnv, target, inv)' cmd/engram/setup.go` prints exactly one line; `rg -c -F 'skills.Install(' cmd/engram/setup.go` prints `1` (the single native-path call inside setupApplySkillsFacet — no second call site); `rg -n -F 'const setupSkillsPluginDelivered = "plugin-delivered"' cmd/engram/setup.go` prints exactly one line; `rg -n -F ' — remove manually to avoid duplicates' cmd/engram/setup.go` prints exactly one line.
    - `rg -n -F 'func setupApplySkillsFacet(row *setupRuntimeRow, base setup.Outcome, planTarget setup.SkillTarget, mutate bool, includeContent bool) setup.Outcome' cmd/engram/setup.go` prints exactly one line; `rg -c -F 'row.Registration = string(' cmd/engram/setup.go` prints `1` (set once, in setupRuntimeRowFromResult).
    - `rg -n -F 'func setupRuntimeRowFromResult(ctx context.Context, env setup.Environment, rt setup.Runtime, r setup.Result, headers string, mutate bool, includeContent bool) setupRuntimeRow' cmd/engram/setup.go` prints exactly one line.
    - `rg -n -F 'rt.(setup.PluginRuntime)' cmd/engram/setup_delegation_test.go` prints exactly one line; `rg -n -F 'func withFakeSetupVersion(' cmd/engram/setup_test.go` prints exactly one line; `rg -c -F 'Lstat:' cmd/engram/setup_test.go` prints at least `3` (fakeSkillsEnv, fakeSkillsEnvWithEntries, the E2E recorder).
    - `rg -n -F '"boom: install refused"' cmd/engram/setup_test.go` prints at least one line; `rg -n -F 'index block present at /home/fake/.codex/AGENTS.md' cmd/engram/setup_test.go` prints at least one line; `rg -n -F 'func fakePluginRun(' cmd/engram/setup_test.go` prints exactly one line.
    - RED observed (record in SUMMARY): with the test edits in place and `setup.go` at HEAD, `go test ./cmd/engram -count=1` fails to compile naming `setupVersion`/`Plugin`; after the production edit the verify command passes.
    - `go test ./cmd/engram -count=1` exits 0 (the WHOLE package — every pre-existing setup test still green with the probe now part of every present runtime's sequence); `gofmt -l cmd/engram/` prints nothing; `go vet ./cmd/engram/` exits 0.
  </acceptance_criteria>
  <done>A single `--apply` run renders `registration=wrote plugin=failed outcome=failed` beside a `wrote` codex row at `exitPartial`; a `current` plugin under `--apply` yields `already-correct` with zero native writes and the exact leftover-native report; preview shows the exact plugin argv and runs no write verb; `TestSetupGeneratedInvocations` pins the new probe in the exact sequence; the whole `cmd/engram` package is green and the change is committed with an explicit pathspec.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unavailable fallback proof, plugin-first help paragraph, flat-scalar view fixtures, golden regeneration</name>
  <reversibility rating="reversible">Help text and fixtures; the goldens are regenerated from the live cobra tree and can be regenerated again. Nothing one-way.</reversibility>
  <files>cmd/engram/setup.go, cmd/engram/setup_test.go, cmd/engram/operator_view_setup_test.go, cmd/engram/testdata/help.golden, cmd/engram/testdata/catalog.golden</files>
  <read_first>
    - `cmd/engram/setup.go` lines 729-790 — `setupLongDescription()`: the `--apply also installs the engram curation skills ...` paragraph (the one asserting the skills are carried inside the binary so a separate plugin install is unnecessary — now false for a plugin-capable runtime) and the `Each present runtime's row reports a registration result and a skills result as two separate fields` sentence (now three facets).
    - `cmd/engram/setup_test.go` lines 819-862 — `TestSetupHelpNamesSkillsInstallation`: Long must still contain `skill` and `binary`, and must contain NO destination path segment derived from any runtime's `Plan().Skills` (`/.claude`, `/skills`, `/.agents`, `/.codex`, `/AGENTS.md`) — so the new paragraph names no directory.
    - `cmd/engram/operator_view_setup_test.go` whole file — `setupViewFixtures()`'s row literals and the returned `map[string][]any{"setup": {...}}`; `TestSetupViewIdentity` and `TestOperatorViewFixturesHaveNoUnsanitizedNesting` (`operator_output_test.go`) consume it.
    - `cmd/engram/golden_test.go` lines 1-60, 263-300 — `TestHelpGolden`/`TestCatalogGolden` and the `-update` discipline (regeneration is an explicit, separately-reviewable step, never a side effect; `-count=1` is load-bearing).
    - `Taskfile.yaml` lines 261-281 — `surfaces:gen`'s golden step: `go test ./cmd/engram -run 'TestHelpGolden|TestCatalogGolden' -update -count=1` (run that one command directly; the `proto:gen` sibling needs network and is not this plan's concern).
    - `.planning/phases/03-plugin-first-delivery/03-CONTEXT.md` D-12 (probe failure → registration and native copy proceed exactly as today; facet reads `unavailable: REASON`), §"Specific Ideas" (a failed probe authors the full native path and the facet string contains the probe's exit/timeout reason).
  </read_first>
  <behavior>
    Write the two tests and the fixtures FIRST.

    `TestSetupPluginUnavailableFallsBackToNative(t)` — claude only, `withFakeSetupVersion(t,
    "0.16.1")`; script claude `plugin list --json` → `{ExitCode: 1, Stderr: "unknown command
    plugin"}`; `base` returns per-call-distinct `mcp get` stdout (so registration is `wrote`); wrap
    `skillsEnv` to count mutations. Run `--runtime claude-code --output json --apply`. Assert: err
    nil (exit 0 — never a failed row); row `Outcome == "wrote"`, `Registration == "wrote"`, `Plugin
    == ""`, `PluginState == "unavailable"`, `PluginNote == "claude-code: claude plugin list --json
    exited 1: 'unknown command plugin'"`, `PluginCommand == ""`, `Skills == "wrote"` (the native
    copy proceeded exactly as today), `SkillsNative == ""`, mutations greater than zero, and recorded
    calls contain exactly ONE `["claude","plugin","list","--json"]` and no `plugin marketplace list`
    and no plugin write verb. A second subtest `timeout`: the fake returns `(setup.RunResult{},
    context.DeadlineExceeded)` for the list probe → `PluginNote` contains `timed out after 20s`,
    everything else as above.

    `TestSetupHelpNamesPluginDelivery(t)` — `strings.ToLower(setupCmd.Long)` contains each of
    `plugin`, `marketplace`, `mutually exclusive`, `updated when outdated`, `never removed` (or
    `reported, never removed`), and does NOT contain `separate plugin install`; and
    `TestSetupHelpNamesSkillsInstallation`'s own rules are re-run implicitly by leaving that test
    untouched.

    `setupViewFixtures()` gains three rows appended as one more `setupReportDoc` document:
    `pluginDelivered` (claude-code, present, `Outcome: "already-correct"`, `Registration:
    "already-correct"`, `Plugin: "already-correct"`, `PluginState: "current"`, `PluginInstalled:
    "0.16.1"`, `PluginTarget: "0.16.1"`, `PluginSource: "GitHub (seanb4t/engram)"`, `Skills:
    "plugin-delivered"`, `SkillsNative: "none"`), `pluginFailedBesideWrote` (claude-code, `Outcome:
    "failed"`, `Registration: "wrote"`, `Plugin: "failed"`, `PluginState: "absent"`, `PluginCommand:
    "claude plugin marketplace add seanb4t/engram --scope user; claude plugin install engram@engram
    --scope user --json -y"`, `Reason: "plugin: claude-code: claude plugin install engram@engram
    --scope user --json -y exited 1: 'boom'"`, `Skills: "plugin-delivered"`), and `pluginUnavailable`
    (codex, `Outcome: "wrote"`, `Registration: "wrote"`, `PluginState: "unavailable"`, `PluginNote:
    "codex: codex plugin list --json exited 1: 'unknown subcommand'"`, `Skills: "wrote"`, `SkillsNative`
    absent, plus a codex `SkillsNative` example on a fourth row `pluginDeliveredWithLeftovers`:
    `"5 skills present at /home/u/.agents/skills (symlink); index block present at
    /home/u/.codex/AGENTS.md — remove manually to avoid duplicates"`). `TestSetupViewIdentity` and
    `TestOperatorViewFixturesHaveNoUnsanitizedNesting` must pass over them unchanged.
  </behavior>
  <action>
Rewrite the `--apply also installs ...` paragraph of `setupLongDescription()` in
`cmd/engram/setup.go` (leave every other paragraph — the `Accepted --auth modes` block, the headers
paragraph, the examples — byte-identical). New paragraph, naming NO directory path segment (the help
test forbids them): `--apply` also delivers the engram curation skills to each present runtime. A
Claude Code or Codex whose own `plugin` CLI works receives the skills, hooks, and the `/engram-setup`
command through its plugin system — engram's own marketplace and plugin only (`seanb4t/engram`,
`engram@engram`): the marketplace is added when absent, the plugin installed when absent, updated
when outdated, and left untouched when current, with the exact plugin commands shown in the preview
and no consent gate beyond `--apply` itself. Every other runtime — opencode, `generic`, or a Claude
Code/Codex without a working `plugin` CLI — receives the native skills copy carried inside the
binary itself. The two are mutually exclusive per runtime: a plugin-delivered runtime gets no native
copy and no index block, and an existing native copy or index block beside it is reported, never
removed. Each present runtime's row reports a registration result, a plugin result (absent, outdated,
current, or unavailable with a reason), and a skills result as separate fields under one aggregated
outcome, with the full skill content available in the `--output json` lane. Keep the words `skill`
and `binary` present. Delete the sentence claiming the bundled skills make a separate plugin
install unnecessary — and do not quote that retired sentence in any code comment, because the
acceptance grep for it scans `setup.go` and the golden in full. Extend `setupLongDescription()`'s doc
comment with one sentence citing D-07/D-12 and REQ-plugin-facet-reported.

Add the fixtures to `setupViewFixtures()` in `cmd/engram/operator_view_setup_test.go` per
`<behavior>` (a comment block explaining the four rows mirror the header fixtures' comment), and the
two tests to `cmd/engram/setup_test.go` per `<behavior>`.

Regenerate the goldens: `go test ./cmd/engram -run 'TestHelpGolden|TestCatalogGolden' -update
-count=1` (the golden step of `task surfaces:gen`, run directly), then `go test ./cmd/engram -run
'TestHelpGolden|TestCatalogGolden' -count=1` must pass and `git status --porcelain --
cmd/engram/testdata/` must show `help.golden` modified (the Long moved) and `catalog.golden` modified
only if the catalog carries the changed text — commit whichever moved IN THE SAME COMMIT as
`setup.go` (a golden committed apart from its cause is the drift this repo's D-14 forbids).

Commit as `feat(setup): describe plugin-first delivery in --help and pin the plugin facet fixtures`
with the explicit pathspec `cmd/engram/setup.go cmd/engram/setup_test.go
cmd/engram/operator_view_setup_test.go cmd/engram/testdata/help.golden` plus
`cmd/engram/testdata/catalog.golden` when it moved.
  </action>
  <verify>
    <automated>out=$(go test ./cmd/engram -run '^(TestSetupPluginUnavailableFallsBackToNative|TestSetupHelpNamesPluginDelivery|TestSetupHelpNamesSkillsInstallation|TestSetupViewIdentity|TestOperatorViewFixturesHaveNoUnsanitizedNesting|TestHelpGolden|TestCatalogGolden|TestSetupHelpNamesEveryRuntimeAndAuthMode|TestSetupHelpClientIDContract)$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^(--- |    --- )(PASS|FAIL)' | tail -n 30; test "$rc" -eq 0 &amp;&amp; printf '%s\n' "$out" | rg -q -F -e '--- PASS: TestSetupPluginUnavailableFallsBackToNative' &amp;&amp; printf '%s\n' "$out" | rg -q -F -e '--- PASS: TestOperatorViewFixturesHaveNoUnsanitizedNesting' &amp;&amp; printf '%s\n' "$out" | rg -q -F -e '--- PASS: TestHelpGolden' &amp;&amp; test -z "$(git status --porcelain -- cmd/engram/testdata/)"</automated>
    <fails_when>non-zero `go test` exit, any `--- FAIL:` line, the line `testing: warning: no tests to run`, no `--- PASS:` line for `TestSetupPluginUnavailableFallsBackToNative`, `TestOperatorViewFixturesHaveNoUnsanitizedNesting`, or `TestHelpGolden` (a golden mismatch prints a diff and `FAIL`), or `git status --porcelain -- cmd/engram/testdata/` printing a path (a regenerated golden left uncommitted)</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n -F 'separate plugin install is required' cmd/engram/setup.go cmd/engram/testdata/help.golden` prints nothing; `rg -n -i -F 'mutually exclusive' cmd/engram/setup.go` prints at least one line; `rg -n -F 'updated when outdated' cmd/engram/setup.go` prints at least one line; `rg -n -F 'engram@engram' cmd/engram/testdata/help.golden` prints at least one line.
    - `go test ./cmd/engram -run '^TestSetupHelpNamesSkillsInstallation$' -count=1 -v` prints `--- PASS: TestSetupHelpNamesSkillsInstallation` (the mechanical proof that the new paragraph names no destination path segment and still says `skill` and `binary`).
    - `rg -n -F 'func TestSetupPluginUnavailableFallsBackToNative' cmd/engram/setup_test.go` prints exactly one line; `rg -n -F 'func TestSetupHelpNamesPluginDelivery' cmd/engram/setup_test.go` prints exactly one line; `rg -n -F 'timed out after 20s' cmd/engram/setup_test.go` prints at least one line.
    - `rg -c -F 'PluginState:' cmd/engram/operator_view_setup_test.go` prints at least `4`; `rg -n -F 'SkillsNative:' cmd/engram/operator_view_setup_test.go` prints at least two lines.
    - RED observed (record in SUMMARY): `TestSetupHelpNamesPluginDelivery` fails against HEAD's Long text; `TestHelpGolden` fails (diff printed) after the Long edit and before `-update`; both pass after.
    - `go test ./cmd/engram -count=1` exits 0; `gofmt -l cmd/engram/` prints nothing; `git status --porcelain -- cmd/engram/testdata/` prints nothing after the commit.
  </acceptance_criteria>
  <done>A failed or timed-out plugin probe leaves the row `wrote` with the native copy written and the reason on `plugin_note`; `--help` describes plugin-first delivery with no path segments and no stale claim; the plugin facet's flat-scalar fixtures pass the identity and no-nesting gates; both goldens are regenerated and committed with their cause.</done>
</task>

<task type="auto">
  <name>Task 3: Plan gate — full `task`, license headers, zero go.mod drift, key-links gate, golden drift check</name>
  <files>none modified — verification-only; if `task fmt` or `task license:add` rewrites anything it must be one of this plan's six files_modified, and the rewrite is folded into a `chore(setup)` commit with an explicit pathspec</files>
  <read_first>
    - `Taskfile.yaml` — `default`, `lint`, `test:go`, `license:check`; `surfaces:gen`'s golden step (already run in Task 2).
    - `CLAUDE.md` §Conventions — no new Go files in this plan, so `license:check` should already be green; never a header on `.planning/**` or `.golden`.
    - `.planning/STATE.md` §Blockers/Concerns — the Phase 1 gates; `cmd/engram` tests only with `-count=1` (pre-existing `TestCollectFlagsIsDepthAware` flake under `-count>1`).
  </read_first>
  <action>
Run `task` (lint + test everywhere), `task license:check`, `git diff --exit-code -- go.mod go.sum`,
`go test ./internal/keylinks/ -count=1` (this plan's key_links anchor on the exact call expressions
Task 1 was instructed to write), `go test ./cmd/engram -run 'TestHelpGolden|TestCatalogGolden'
-count=1` with `git status --porcelain -- cmd/engram/testdata/` empty (no regenerated golden left
uncommitted), and `go run ./internal/surfacesgen --check-setup` (this plan touches neither
`setupgen` nor the anchored region, so the existing gate must still be green — plan 03-04 owns the
plugin table). Do not touch `internal/store/redevidence_harness_test.go`.

No commit is expected unless a formatter or the license fixer rewrote one of this plan's files; then
commit `chore(setup): gate fixups for the plugin facet` with the explicit pathspec of exactly the
rewritten file(s).
  </action>
  <verify>
    <automated>task &amp;&amp; task license:check &amp;&amp; git diff --exit-code -- go.mod go.sum &amp;&amp; go test ./internal/keylinks/ -count=1 &amp;&amp; go test ./cmd/engram -run 'TestHelpGolden|TestCatalogGolden' -count=1 &amp;&amp; test -z "$(git status --porcelain -- cmd/engram/testdata/)" &amp;&amp; go run ./internal/surfacesgen --check-setup</automated>
    <fails_when>non-zero exit from any link of the chain: `task` prints `task: Failed to run task` (or a golangci-lint finding / `--- FAIL` line), `license-eye` lists a file as missing its header, `git diff --exit-code -- go.mod go.sum` prints a hunk, `go test ./internal/keylinks/` prints `FAIL`, a golden test prints a diff and `FAIL`, `git status --porcelain` prints a testdata path, or `surfacesgen --check-setup` reports `generated setup region has drifted`</fails_when>
  </verify>
  <acceptance_criteria>
    - `task` exits 0 with no `--- FAIL:` line and no golangci-lint finding.
    - `task license:check` exits 0.
    - `git diff --exit-code -- go.mod go.sum` exits 0 (no output).
    - `go test ./internal/keylinks/ -count=1` exits 0 and prints `ok`.
    - `go test ./cmd/engram -run 'TestHelpGolden|TestCatalogGolden' -count=1` exits 0; `git status --porcelain -- cmd/engram/testdata/` prints nothing.
    - `go run ./internal/surfacesgen --check-setup` exits 0.
    - `git status --porcelain -- cmd/engram/` prints nothing after the plan's commits.
  </acceptance_criteria>
  <done>`task`, `task license:check`, the empty go.mod/go.sum diff, the key-links gate, the golden tests with a clean testdata tree, and the setupgen drift check all exit 0, and the working tree under `cmd/engram/` is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `setup.PluginResult` → `setupRuntimeRow` → terminal / `--output json` | Vendor stderr (bounded by `boundCapture`) and a marketplace source line become row values; rendered only through `sanitizeViewValue`'s scalar branch. |
| plugin facet → skills routing | One boolean (`Delivered()`) decides whether the native writer runs; a wrong value either duplicates skills or leaves none — never deletes. |
| `--help` text → operator | The help is the operator's model of what `--apply` does; a stale claim is a correctness-by-reading defect. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-03 | Tampering | native skills copy or `AGENTS.md` written (or deleted) for a plugin-delivered runtime | high | mitigate | `setupReportNativeSkills` never calls `skills.Install`; `TestSetupPluginDeliveredRuntimeAuthorsZeroNativeWrites` asserts zero WriteFile/MkdirAll and an unchanged seeded `AGENTS.md`; the presence check is 03-02's read-only `DetectPresence`. |
| T-03-04 | Repudiation | a failed plugin install laundered into a successful row, or an unavailable probe failing a healthy registration | high | mitigate | `setupApplyPluginFacet` folds `failed` through `AggregateOutcome` (row `failed`, reason `plugin: ...`) and folds NOTHING for `unavailable`; `TestSetupApplyJSONEmitsPluginFacet` (exitPartial, both facets visible) and `TestSetupPluginUnavailableFallsBackToNative` (exit 0, `wrote`) pin both directions. |
| T-03-11 | Information Disclosure | third-party stdout/stderr or a marketplace source string forging report structure | low | mitigate | Every facet value is a flat string through `sanitizeViewValue` (C0/DEL stripped) and `boundCapture` (4096-byte bound) — `TestOperatorViewFixturesHaveNoUnsanitizedNesting` over the new fixtures is the structural proof. |
| T-03-12 | Tampering | a test binary's own `dev` version driving assertions (nondeterministic across machines) | low | mitigate | `setupVersion` seam + `withFakeSetupVersion(t, "0.16.1")` in every plugin test; production keeps `resolvedVersion`. |
| T-03-08 | Tampering | test suite invoking a real `claude`/`codex` verb or the operator's `$HOME` | medium | mitigate | `fakePluginRun` scripts every plugin response by `filepath.Base(path)` + args; `fakeSkillsEnvWithEntries` scripts `Lstat`; no test constructs `setup.OSEnvironment`/`skills.OSEnvironment`. |
| T-03-SC | Tampering | npm/pip/cargo installs | low | accept | Not applicable — zero new packages; Task 3 asserts `git diff --exit-code -- go.mod go.sum`. |
</threat_model>

<verification>
- `go test ./cmd/engram -run '^(TestSetupApplyJSONEmitsPluginFacet|TestSetupPluginDeliveredRuntimeAuthorsZeroNativeWrites|TestSetupPreviewShowsPluginArgv|TestSetupPluginUnavailableFallsBackToNative|TestSetupHelpNamesPluginDelivery|TestSetupGeneratedInvocations|TestOperatorViewFixturesHaveNoUnsanitizedNesting|TestHelpGolden|TestCatalogGolden)$' -count=1 -v` shows `--- PASS` for every test and subtest.
- `rg -c -F 'skills.Install(' cmd/engram/setup.go` prints `1`; `rg -n -F 'separate plugin install is required' cmd/engram/setup.go` prints nothing.
- `task`, `task license:check`, `git diff --exit-code -- go.mod go.sum`, `go test ./internal/keylinks/ -count=1`, and `go run ./internal/surfacesgen --check-setup` exit 0.
- `git diff --stat FIRST^..LAST` touches only the six files in `files_modified`.
- Not gated here (rule m45p2b4bp7): running `engram setup --apply` against a real machine — the maintainer's own preview text from 03-CONTEXT.md §"Specific Ideas" is a post-merge observation.
</verification>

<success_criteria>
- A result row shows plugin delivery as its own facet in text and JSON beside registration and skills; `registration=wrote` next to `plugin=failed` stays visible on one row and reaches `exitPartial` (SC4).
- Plugin state is reported as absent / outdated / current (or unavailable with a reason); a plugin-delivered runtime never receives a native skills copy or an `AGENTS.md` block, and any leftover native copy or index block is reported with path, count, and symlink-vs-copy, never removed (SC3).
- A probe failure leaves registration and the native copy exactly as today with `plugin_state=unavailable` (SC1's fallback half); `--help` describes plugin-first delivery; goldens regenerated in the same commit; `task` and every gate green.
</success_criteria>

<output>
Create `.planning/phases/03-plugin-first-delivery/03-03-SUMMARY.md` when done
</output>
