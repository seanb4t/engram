---
phase: 03-plugin-first-delivery
plan: 04
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - skill/engram/.codex-plugin/plugin.json
  - release-please-config.json
  - internal/setupgen/manifest_test.go
  - internal/setupgen/setupgen.go
  - internal/setupgen/setupgen_test.go
  - skill/engram/commands/engram-setup.md
autonomous: true
requirements:
  - REQ-codex-plugin-manifest
  - REQ-plugin-setupgen-regenerated

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`skill/engram/.codex-plugin/plugin.json` exists, is git-tracked, and carries EXACTLY four top-level keys — `$schema` (`https://agent-plugins.org/schemas/1.0.0/plugin.schema.json`), `name`, `version`, `description` — with `name`, `version`, and `description` byte-equal to `skill/engram/.claude-plugin/plugin.json`'s (the minimal manifest the orchestrator decided on for Open Question 2; no `interface`, `hooks`, or `mcpServers` key — REQ-codex-plugin-manifest)"
    - "`release-please-config.json`'s `packages[\".\"][\"extra-files\"]` carries exactly one entry `{\"type\": \"json\", \"path\": \"skill/engram/.codex-plugin/plugin.json\", \"jsonpath\": \"$.version\"}` beside the unchanged `.claude-plugin/plugin.json` entry, so every release bumps both manifests' `version` together (REQ-codex-plugin-manifest: release-please-synced like `.claude-plugin/plugin.json`)"
    - "`TestPluginManifestIdentityMatches` (`internal/setupgen/manifest_test.go`) is the drift gate: it reads both manifests from the repository root, asserts the three identity fields are equal, asserts the codex manifest's key set is exactly the four above and its `version` is a bare `X.Y.Z` core, and asserts the release-please `extra-files` entry exists exactly once — a divergence in any of those fails `task` (REQ-codex-plugin-manifest's drift gate)"
    - "The shipped-bundle privacy guard (`skill/engram/hooks/tests/test_no_residual_memory_oauth.py`) scans the new manifest and stays green: its `description` is the vendor-neutral text already shipped in `.claude-plugin/plugin.json` (03-RESEARCH.md Security Domain)"
    - "`setupgen.Render(planFn PlanFunc, pluginFn PluginActionsFunc)` renders a THIRD table, `### Claude Code plugin delivery (--apply)`, after the two shipped tables, with exactly four rows — `absent` (marketplace absent), `absent` (marketplace present), `outdated`, `current` — whose Command cells are `setup.Plan{Actions: pluginFn(state, marketplacePresent)}.Display()` through `commandCell` (or the literal `(no action)` for zero actions); a nil `pluginFn` renders the two registration tables only; `Check`/`Write` always pass claude-code's real `PluginActions` (REQ-plugin-setupgen-regenerated; the orchestrator's Open Question 1 decision: the generated tables SHOW the plugin actions)"
    - "The two shipped tables (`### Delegation preview`, `### Claude Code fallback registration`) render byte-identical to HEAD — the five `Cases()` are untouched and the plugin table is appended after them — and `Render` returns an error (never a partial body) when a plugin row's action count contradicts its state (absent/outdated must author at least one action, current must author none) or when any plugin action's argv does not begin `claude plugin` (03-RESEARCH.md Pitfall 7's renderer-knows-its-shape assertion)"
    - "`skill/engram/commands/engram-setup.md`'s anchored `setup-commands` region is regenerated by `go run ./internal/surfacesgen` in the SAME commit as the `Render` change, and `go run ./internal/surfacesgen --check-setup` exits 0 afterwards (the existing CI drift gate stays green); the hand-authored prose OUTSIDE the anchors gains the plugin-first explanation (what `--apply` does for a plugin-capable Claude Code or Codex, the plugin facet's four states, and that codex's own `codex plugin` argv appear only in the live preview because setupgen renders claude-code's Plan only)"
    - "The `/engram-setup` prose still opens with YAML frontmatter on line 1 (`test_engram_setup_command_shape`), carries no SPDX header (CLAUDE.md: slash-command markdown is frontmatter-first), and `task lint` (rumdl) is clean over it"
    - "No task in this plan runs `codex plugin add`, `claude plugin install`, or any third-party write verb; Codex's loader accepting the minimal manifest is a post-release live observation recorded in 03-VALIDATION.md's Manual-Only table, not a phase gate (rule m45p2b4bp7)"
  artifacts:
    - path: "skill/engram/.codex-plugin/plugin.json"
      provides: "Codex plugin manifest — minimal four-key twin of .claude-plugin/plugin.json"
      contains: "\"$schema\": \"https://agent-plugins.org/schemas/1.0.0/plugin.schema.json\""
    - path: "release-please-config.json"
      provides: "extra-files $.version sync entry for the codex manifest"
      contains: "skill/engram/.codex-plugin/plugin.json"
    - path: "internal/setupgen/manifest_test.go"
      provides: "TestPluginManifestIdentityMatches — the two-manifest identity + release-please drift gate"
      contains: "func TestPluginManifestIdentityMatches"
    - path: "internal/setupgen/setupgen.go"
      provides: "PluginActionsFunc, claudeCodePluginActions, the third rendered table, Render/check/write threading the plugin func"
      contains: "### Claude Code plugin delivery (--apply)"
    - path: "internal/setupgen/setupgen_test.go"
      provides: "TestRenderPluginTable plus the existing tests threaded through the widened Render/check/write signatures"
      contains: "func TestRenderPluginTable"
    - path: "skill/engram/commands/engram-setup.md"
      provides: "regenerated anchored region with the plugin-delivery table + hand-authored plugin-first prose"
      contains: "### Claude Code plugin delivery (--apply)"
  key_links:
    - from: "internal/setupgen/setupgen.go"
      to: "internal/setup/claudecode.go"
      via: "the plugin table is rendered from claude-code's REAL PluginActions, reached through the exported PluginRuntime interface — never a re-typed argv"
      pattern: 'setup[.]ClaudeCode[.][(]setup[.]PluginRuntime[)]'
    - from: "internal/setupgen/setupgen.go"
      to: "internal/setup/plan.go"
      via: "each plugin row's Command cell is Plan.Display() over the authored actions — display stays a pure function of Args"
      pattern: 'setup[.]Plan[{]Actions: actions[}][.]Display[(][)]'
    - from: "internal/surfacesgen/main.go"
      to: "internal/setupgen/setupgen.go"
      via: "surfacesgen's run() calls setupgen.Write(setupgen.Path) — the regeneration step whose --check-setup twin is the CI drift gate"
      pattern: 'setupgen[.]Write[(]setupgen[.]Path[)]'
    - from: "release-please-config.json"
      to: "skill/engram/.codex-plugin/plugin.json"
      via: "the $.version sync entry duplicates the .claude-plugin entry's exact shape"
      pattern: '"path": "skill/engram/[.]codex-plugin/plugin[.]json"'
    - from: "internal/setupgen/manifest_test.go"
      to: "skill/engram/.claude-plugin/plugin.json"
      via: "the drift gate reads the claude manifest as the identity source of truth and the codex manifest as its twin"
      pattern: '"[.]claude-plugin", "plugin[.]json"'
  prohibitions:
    - statement: "MUST NOT hand-edit the anchored `setup-commands` region of `skill/engram/commands/engram-setup.md` — it is regenerated from the real `ClaudeCode.Plan` and the real claude-code `PluginActions` by surfacesgen in the same commit as the `Render` change; only the prose OUTSIDE the anchors is hand-authored"
      category: transparency
      status: resolved
      verification: test
      reason: "`go run ./internal/surfacesgen --check-setup` (the CI drift gate) and TestRenderPluginTable both compare the committed region against Render()'s output — no wired check descriptor by design (spec-less fallback, flagged-unverified)"
    - statement: "MUST NOT ship any vendor-specific or private-deployment substring in the new manifest or the regenerated prose — the shipped bundle stays neutral (the canonical gateway example is `x-gateway-api-key=GATEWAY_KEY`; the description is the already-shipped neutral text)"
      category: privacy
      status: resolved
      verification: test
      reason: "`uv run --with pytest pytest skill/engram/hooks/tests -q` scans every file under skill/engram/ including the new manifest — no wired check descriptor by design (spec-less fallback, flagged-unverified)"
    - statement: "MUST NOT let the two plugin manifests' identity fields diverge — `name`, `version`, and `description` are compared byte-for-byte by a Go test that runs under `task`, and release-please rewrites both `version` fields from one config"
      category: safety
      status: resolved
      verification: test
      reason: "TestPluginManifestIdentityMatches fails task on any divergence or on a missing extra-files entry — no wired check descriptor by design (spec-less fallback, flagged-unverified)"
---

<objective>
Ship the two remaining phase deliverables that live outside the Go setup lane: Codex's plugin
manifest (`skill/engram/.codex-plugin/plugin.json`, the minimal four-key twin of the Claude manifest,
release-please-synced and drift-gated — REQ-codex-plugin-manifest), and the regenerated `/engram-setup`
prose whose generated region now carries a third table showing the exact claude-code plugin argv per
observed state, with the existing `setupgen` CI drift gate green in the same commit
(REQ-plugin-setupgen-regenerated).

Purpose: SC5 of the phase. Codex's `codex plugin add engram@engram` needs a `.codex-plugin/plugin.json`
to accept the bundle at all; the drift gate keeps the two manifests from silently diverging; and the
slash command's generated tables must reflect what `--apply` now does so `/engram-setup`'s
delegation prose is not silently incomplete (03-RESEARCH.md Pitfall 7).

Decision applied (orchestrator, Open Question 2): minimal manifest (`$schema`, `name`, `version`,
`description`); whether Codex's loader wants an `interface` block is a post-release observation.
Decision applied (Open Question 1): the generated tables SHOW the plugin actions, rendered from
claude-code's real `PluginActions` via the exported `PluginRuntime` interface (plan 03-01).

Output: the manifest, the release-please entry, `TestPluginManifestIdentityMatches`,
`PluginActionsFunc` + the third table in `Render`, `TestRenderPluginTable`, and the regenerated
`engram-setup.md` with its hand-authored plugin-first prose.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-plugin-first-delivery/03-CONTEXT.md
@.planning/phases/03-plugin-first-delivery/03-RESEARCH.md
@.planning/phases/03-plugin-first-delivery/03-PATTERNS.md
@.planning/phases/03-plugin-first-delivery/03-01-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces (plan 03-04's share)

| Kind | Artifact | Created by |
|------|----------|------------|
| Manifest | `skill/engram/.codex-plugin/plugin.json` (`$schema`, `name`, `version`, `description`) | 03-04 Task 1 |
| Config entry | `release-please-config.json` `extra-files[]` → `{type: json, path: skill/engram/.codex-plugin/plugin.json, jsonpath: $.version}` | 03-04 Task 1 |
| Test func | `TestPluginManifestIdentityMatches` (`internal/setupgen/manifest_test.go`) | 03-04 Task 1 |
| Go type | `type PluginActionsFunc func(state setup.PluginState, marketplacePresent bool) []setup.Action` (`internal/setupgen`) | 03-04 Task 2 |
| Go func | `claudeCodePluginActions() PluginActionsFunc` (type-asserts `setup.ClaudeCode.(setup.PluginRuntime)`) | 03-04 Task 2 |
| Go signature | `Render(planFn PlanFunc, pluginFn PluginActionsFunc) (string, error)`; unexported `check(path, planFn, pluginFn)` / `write(path, planFn, pluginFn)`; `Check`/`Write` unchanged | 03-04 Task 2 |
| Rendered table | `### Claude Code plugin delivery (--apply)` with four rows | 03-04 Task 2 |
| Test func | `TestRenderPluginTable` | 03-04 Task 2 |
| Regenerated | `skill/engram/commands/engram-setup.md` anchored region + hand-authored prose | 03-04 Task 2 |

## Flagged Assumptions (edge probe, spec-less fallback)

This plan accounts for **0 authored + 2 flagged = 2** of the phase's 11 rows (03-01: 5 + 2; 03-03:
2 + 1). Phase total 7 authored + 4 flagged = 11, no silent drop.

| Requirement | Probe category | Disposition |
|---|---|---|
| REQ-codex-plugin-manifest | `unclassified` | **Flagged assumption (unresolved).** What this plan CAN pin: the file exists, its identity fields equal the Claude manifest's byte-for-byte, its key set is exactly four, release-please syncs `$.version` (all in `TestPluginManifestIdentityMatches`). What stays an assumption: that Codex's actual loader accepts a manifest without an `interface` block — the `agent-plugins.org` schema requires only `$schema` and `name` (03-RESEARCH.md Open Question 2), but a live `codex plugin add` is a write verb no test may run. Recorded in 03-VALIDATION.md's Manual-Only table as a post-release observation. |
| REQ-plugin-setupgen-regenerated | `unclassified` | **Flagged assumption (unresolved).** `setupgen` renders claude-code's Plan ONLY, by construction (`Check`/`Write` hardcode `setup.ClaudeCode.Plan`, 03-RESEARCH.md Pitfall 7); the plugin table therefore shows claude-code's argv only, and the hand-authored prose says so and points at the live preview for codex's `codex plugin` equivalents. Whether `/engram-setup`'s reader needs a codex table is a documentation question for Phase 5 (`guides/plugin.md`), not a gate here. |

## Prohibition probe (spec-less fallback, in-prompt recall)

Stage 1 raw candidates: must not hand-edit the generated region; must not leak a private host or
old branding into the manifest; must not let the manifests drift; must not add `hooks`/`mcpServers`
to the codex manifest (duplicate-hooks load failure, `test_plugin_config.py`); must not re-type argv
in the renderer; must not run a real `codex plugin add`; must not add an SPDX header to JSON or to
the frontmatter-first command file; must not change the five shipped `Cases()`. Stage 2 keeps three
(authored above, descriptor-less): the **never-hand-edit-the-region** transparency prohibition, the
**shipped-bundle-privacy** prohibition, and the **manifests-never-diverge** safety prohibition. The
rest are routine correctness owned by the tests or by CLAUDE.md's stated conventions. Canon
breadcrumb: supply-chain trust in the marketplace-declared install command is canon (Anthropic's
own "only install from sources you trust"); scoped to engram's own repo by 03-01's D-04 literals;
covered by `/gsd-secure-phase`.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Codex manifest + release-please sync + identity drift gate + privacy guard, end to end</name>
  <reversibility rating="reversible">One new JSON file, one config array entry, one test; undo is deleting them. Nothing one-way — the manifest ships with the next release and can be revised by a later release.</reversibility>
  <files>skill/engram/.codex-plugin/plugin.json, release-please-config.json, internal/setupgen/manifest_test.go</files>
  <read_first>
    - `skill/engram/.claude-plugin/plugin.json` — the whole 5-line file: the identity source of truth (`name`, `version`, `description`) the twin copies byte-for-byte.
    - `release-please-config.json` — the `extra-files` array; the `.claude-plugin/plugin.json` entry (`type: json`, `jsonpath: $.version`) is the exact shape to duplicate; `cmd/engram/buildversion.go`'s `generic` entry shows the array has mixed types.
    - `skill/engram/hooks/tests/test_plugin_config.py` — `test_plugin_manifest` asserts the Claude manifest carries no `hooks`/`mcpServers` key (a `hooks` key triggers a duplicate-hooks load failure); the codex twin must carry neither.
    - `skill/engram/hooks/tests/test_no_residual_memory_oauth.py` — the whole-bundle scan (every file under `skill/engram/` except `hooks/tests/`) for private hosts and pre-rebrand ids; the new manifest is in scope.
    - `internal/setupgen/setupgen_test.go` lines 1-20 (imports, package) and `fixtureGit`/`TestDriftChecks` (how these tests reach files by absolute path) — the new test file joins this package and locates the repo root by walking to the directory that contains `go.mod`.
    - `.licenserc.yaml` — `skill/**/*.json` is in `paths-ignore`: the manifest carries NO SPDX header; the new Go test file DOES (it is under `internal/**`).
    - `.planning/phases/03-plugin-first-delivery/03-RESEARCH.md` §"Code Examples" → "Minimal `.codex-plugin/plugin.json`" and Open Question 2 (the `agent-plugins.org` schema's `required: ["$schema","name"]`), §"Security Domain" (V14 configuration drift).
  </read_first>
  <behavior>
    Write `internal/setupgen/manifest_test.go` FIRST (SPDX header) and observe `TestPluginManifestIdentityMatches`
    fail (`.codex-plugin/plugin.json` missing), then create the manifest and the config entry.

    `TestPluginManifestIdentityMatches(t)`:
    - `repoRoot(t)`: starting from the test's working directory, walk parent directories until one
      contains `go.mod`; `t.Fatalf` if none (fail loud — never a vacuous pass on a moved tree).
    - Read `filepath.Join(root, "skill", "engram", ".claude-plugin", "plugin.json")` and
      `filepath.Join(root, "skill", "engram", ".codex-plugin", "plugin.json")`; unmarshal each into
      `map[string]any`.
    - Assert codex's key set is EXACTLY `{"$schema", "name", "version", "description"}` (sorted keys
      compared with `reflect.DeepEqual` or `slices.Equal`), and that `hooks` and `mcpServers` are absent
      (implied, but assert by name for legibility).
    - Assert `codex["$schema"] == "https://agent-plugins.org/schemas/1.0.0/plugin.schema.json"`.
    - For each of `name`, `version`, `description`: both values are strings and byte-equal; `name ==
      "engram"`.
    - Assert `version` matches `^(0|[1-9][0-9]*)[.](0|[1-9][0-9]*)[.](0|[1-9][0-9]*)$` (release-please
      writes a bare core; a `v` prefix or suffix here would break the D-01 comparison downstream).
    - Read `release-please-config.json`; navigate `packages["."]["extra-files"]` as `[]any`; count
      entries whose `path == "skill/engram/.codex-plugin/plugin.json"` — exactly one, with `type ==
      "json"` and `jsonpath == "$.version"`; also assert the `skill/engram/.claude-plugin/plugin.json`
      entry is still present exactly once (the twin did not replace it).
    - Subtest `description-is-vendor-neutral`: `strings.Contains(strings.ToLower(description), ...)` is
      false for the substrings the pytest guard bans — build them non-literally the way the guard does
      (`"fzy"+"mgc"`, `"lite"+"llm"`, `"memory"+"_oauth"`) so this test file is itself clean.
  </behavior>
  <action>
Create `skill/engram/.codex-plugin/plugin.json` with exactly four keys in this order — `$schema`:
`https://agent-plugins.org/schemas/1.0.0/plugin.schema.json`; `name`: `engram`; `version`: the value
currently in `skill/engram/.claude-plugin/plugin.json` (read it; do not type it from memory);
`description`: that manifest's `description` string byte-for-byte — two-space indentation and a
trailing newline like the Claude manifest. No `interface`, `author`, `hooks`, or `mcpServers` key
(Open Question 2 decided minimal; `hooks` would trigger Codex/Claude's duplicate-hooks load failure
per `test_plugin_config.py`). No SPDX header (JSON; `.licenserc.yaml` ignores `skill/**/*.json`).

Append to `release-please-config.json`'s `packages["."]["extra-files"]` array, immediately after the
existing `.claude-plugin/plugin.json` entry, the object `{"type": "json", "path":
"skill/engram/.codex-plugin/plugin.json", "jsonpath": "$.version"}` — same three keys, same order,
same indentation as its sibling. Nothing else in the file moves (`dprint`/`task fmt` must be clean).

Write `internal/setupgen/manifest_test.go` per `<behavior>` (package `setupgen`; imports
`encoding/json`, `os`, `path/filepath`, `regexp`, `slices` or `sort`, `strings`, `testing`).

Run `uv run --with pytest pytest skill/engram/hooks/tests -q` — the privacy guard now scans the new
manifest and must be green (the description is the already-shipped neutral text, so it is by
construction).

Commit as `feat(plugin): add Codex plugin manifest synced by release-please with an identity drift
gate` with the explicit pathspec `skill/engram/.codex-plugin/plugin.json release-please-config.json
internal/setupgen/manifest_test.go`. After committing, `git ls-files --
skill/engram/.codex-plugin/plugin.json` must print the path (tracked — `skill/engram/.gitignore`
ignores only Python caches).
  </action>
  <verify>
    <automated>out=$(go test ./internal/setupgen/ -run '^TestPluginManifestIdentityMatches$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^(--- |    --- )(PASS|FAIL)' | tail -n 10; test "$rc" -eq 0 &amp;&amp; printf '%s\n' "$out" | rg -q -F -e '--- PASS: TestPluginManifestIdentityMatches' &amp;&amp; uv run --with pytest pytest skill/engram/hooks/tests -q 2>&amp;1 | tail -n 3 | rg -q -e '^[0-9]+ passed' &amp;&amp; test -n "$(git ls-files -- skill/engram/.codex-plugin/plugin.json)"</automated>
    <fails_when>non-zero `go test` exit, any `--- FAIL:` line, the line `testing: warning: no tests to run`, no `--- PASS: TestPluginManifestIdentityMatches` line, pytest's summary line lacking `N passed` (a `failed` or `error` summary — the privacy guard or plugin-config test tripped), or `git ls-files` printing nothing (the manifest was not committed/tracked)</fails_when>
  </verify>
  <acceptance_criteria>
    - `test -f skill/engram/.codex-plugin/plugin.json` succeeds; `node -e 'const m=require("./skill/engram/.codex-plugin/plugin.json");const k=Object.keys(m).sort().join(",");if(k!=="$schema,description,name,version")process.exit(1)'` exits 0 (exactly four keys) — or the equivalent `python3 -c` one-liner.
    - `diff <(python3 -c 'import json;m=json.load(open("skill/engram/.claude-plugin/plugin.json"));print(m["name"],m["version"],m["description"])') <(python3 -c 'import json;m=json.load(open("skill/engram/.codex-plugin/plugin.json"));print(m["name"],m["version"],m["description"])')` prints nothing (identity fields equal).
    - `rg -n -F '"path": "skill/engram/.codex-plugin/plugin.json"' release-please-config.json` prints exactly one line; `rg -c -F '"jsonpath": "$.version"' release-please-config.json` prints `3` (Chart version, claude manifest, codex manifest).
    - `rg -n -F 'func TestPluginManifestIdentityMatches' internal/setupgen/manifest_test.go` prints exactly one line; `rg -n -F '".claude-plugin", "plugin.json"' internal/setupgen/manifest_test.go` prints at least one line; `rg -n -F '".codex-plugin", "plugin.json"' internal/setupgen/manifest_test.go` prints at least one line; `head -2 internal/setupgen/manifest_test.go` shows the SPDX header.
    - `head -1 skill/engram/.codex-plugin/plugin.json` prints `{` (no header above the JSON).
    - RED observed (record in SUMMARY): with the test in place and no manifest, `go test ./internal/setupgen/ -run '^TestPluginManifestIdentityMatches$' -count=1` FAILS naming the missing file; after creating the manifest and the config entry it passes.
    - `uv run --with pytest pytest skill/engram/hooks/tests -q` exits 0; `task fmt` leaves `release-please-config.json` unchanged (`git diff --exit-code -- release-please-config.json` after `task fmt` exits 0).
  </acceptance_criteria>
  <done>The codex manifest exists with exactly four keys, its identity fields equal the Claude manifest's, release-please syncs its version, the Go drift gate and the pytest privacy guard are green, and the change is committed with an explicit pathspec.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: setupgen renders the claude-code plugin-delivery table from the real PluginActions; regenerate the anchored region and author the plugin-first prose</name>
  <reversibility rating="reversible">A widened renderer signature, one appended table, regenerated region, prose additions; undo is reverting and regenerating.</reversibility>
  <files>internal/setupgen/setupgen.go, internal/setupgen/setupgen_test.go, skill/engram/commands/engram-setup.md</files>
  <read_first>
    - `internal/setupgen/setupgen.go` whole file — `PlanFunc`, `Case`/`Cases()` (UNTOUCHED: the five shipped rows must stay byte-identical), `Render`'s per-case env + exactly-one-`mcp add` filter, `commandCell`, `readRegion`, `Check`/`check`/`compare`, `Write`/`write`.
    - `internal/setupgen/setupgen_test.go` — every `Render(...)`, `check(path, planFn)`, `write(path, planFn)` call site (lines 21, 77, 99, 126, 192, 260, 264, 303, 381, 388, 396, 399) that the widened signatures touch; `TestRenderRealPlans`'s `strings.Contains(body, "| `label` | `command` |")` row-assertion idiom to copy for the plugin rows; `mutatedPlan`.
    - `internal/surfacesgen/main.go` lines 195-212 — `run()` calls `setupgen.Write(setupgen.Path)`; `--check-setup` calls `setupgen.Check(setupgen.Path)`; both exported signatures stay unchanged.
    - `internal/setup/plugin.go` (03-01) — `PluginRuntime.PluginActions(state, marketplacePresent)`, the four `PluginState` constants; `internal/setup/claudecode.go` (03-01) — the exact argv the table must show.
    - `skill/engram/commands/engram-setup.md` whole file — the frontmatter (line 1 `---`), the intro paragraph ("Never hand-edit settings files. The plugin ships no bundled MCP server."), §"Generated command reference"'s preamble, the anchored region, §"When the binary is present" steps 2 and 4 (which list what to show and explain).
    - `skill/engram/hooks/tests/test_plugin_config.py` `test_engram_setup_command_shape` — frontmatter on line 1 is load-bearing.
    - `.planning/phases/03-plugin-first-delivery/03-RESEARCH.md` Pitfall 7 and Open Question 1 (resolved by the orchestrator: the tables show plugin actions).
    - `Taskfile.yaml` lines 261-281 — `surfaces:gen`; run its first command `go run ./internal/surfacesgen` directly (the `proto:gen` sibling needs network and is out of scope; the goldens do not move in this plan).
  </read_first>
  <behavior>
    Write `TestRenderPluginTable` and thread the existing tests through the new signatures FIRST;
    observe compile failure (`Render` takes one argument today), then implement.

    `TestRenderPluginTable(t)`:
    - `body, err := Render(setup.ClaudeCode.Plan, claudeCodePluginActions())`; err nil.
    - `pr := setup.ClaudeCode.(setup.PluginRuntime)`; for each `(label, state, marketplacePresent)` in
      the four rows, `want := setup.Plan{Actions: pr.PluginActions(state, marketplacePresent)}.Display()`;
      assert `strings.Contains(body, "| "+label+" | "+commandCell(want)+" |")` for the three rows with
      actions and `strings.Contains(body, "| `current` | (no action) |")` for the fourth.
    - Assert the literal argv strings appear: `claude plugin marketplace add seanb4t/engram --scope user; claude plugin install engram@engram --scope user --json -y`, `claude plugin install engram@engram --scope user --json -y` (as a whole-cell match, i.e. preceded by a backtick and followed by a backtick), and `claude plugin update engram@engram --scope user --json -y`.
    - Assert `strings.Index(body, "### Claude Code plugin delivery (--apply)") > strings.Index(body, "### Claude Code fallback registration")` (the plugin table is appended AFTER the two shipped tables).
    - Assert the two shipped tables are byte-identical to `Render(setup.ClaudeCode.Plan, nil)`'s output: `strings.HasPrefix(body, twoTables)` where `twoTables, _ := Render(setup.ClaudeCode.Plan, nil)`.
    - Invariant subtests: a `pluginFn` returning nil for `PluginAbsent` → `Render` returns an error containing `plugin` and `absent` with an EMPTY body; a `pluginFn` returning a non-nil slice for `PluginCurrent` → error, empty body; a `pluginFn` returning `[]setup.Action{{Args: []string{"codex", "plugin", "add"}}}` for any state → error naming `claude plugin`, empty body (the renderer knows the shape it renders — Pitfall 7).
    - `Render(setup.ClaudeCode.Plan, nil)` succeeds and contains NO `plugin delivery` heading.

    Existing tests: `TestRenderRealPlans`, `TestCheckReadOnly`, `TestPlanMutationChangesRegion`, and
    `TestDriftChecks` compare against the REAL written region, so they pass `claudeCodePluginActions()`;
    `TestRenderSelectsActionAndQuotes` and `TestRenderRejectsInvalidPlans` exercise the registration
    filter in isolation and pass `nil` (`Render(nil, nil)` for the nil-plan case). Their existing
    assertions are otherwise unchanged and must still pass.
  </behavior>
  <action>
In `internal/setupgen/setupgen.go`: declare `type PluginActionsFunc func(state setup.PluginState,
marketplacePresent bool) []setup.Action` beside `PlanFunc`, doc-commented as the plugin-lane authoring
seam (rendering never executes its actions). Add `func claudeCodePluginActions() PluginActionsFunc`:
`pr, ok := setup.ClaudeCode.(setup.PluginRuntime)`; `!ok` → `panic("setupgen: setup.ClaudeCode does
not implement setup.PluginRuntime")` (a programming error at generation time, never a runtime path);
return `pr.PluginActions`. Widen `Render` to `Render(planFn PlanFunc, pluginFn PluginActionsFunc)
(string, error)`: keep the two existing tables byte-identical; when `pluginFn != nil`, append `"\n"`
plus a third table with header `### Claude Code plugin delivery (--apply)\n\n| Plugin state | Command
|\n| --- | --- |\n` and four rows in this fixed order — label `` `absent` (marketplace absent) `` →
`pluginFn(setup.PluginAbsent, false)`; `` `absent` (marketplace present) `` → `pluginFn(setup.PluginAbsent,
true)`; `` `outdated` `` → `pluginFn(setup.PluginOutdated, true)`; `` `current` `` →
`pluginFn(setup.PluginCurrent, true)`. For each row: the first three MUST return at least one action
and the fourth MUST return none, else return `"", fmt.Errorf("setupgen: plugin %s: expected %s, got
%d action(s)", label, "actions"/"no action", n)`; every action must satisfy `len(Args) >= 2 &&
Args[0] == "claude" && Args[1] == "plugin"` else `"", fmt.Errorf("setupgen: plugin %s: action %q is
not a claude plugin invocation", label, action.Command())`; the Command cell is
`commandCell(setup.Plan{Actions: actions}.Display())` — EXACTLY that expression — or the literal
`(no action)` for the fourth row. Doc-comment: the table shows what `--apply` runs for a
plugin-capable Claude Code by observed state (D-01/D-02/D-04/D-06 argv authored in claudecode.go),
that it exists so `/engram-setup`'s tables are not silently incomplete relative to what `--apply` does
(Pitfall 7), and that codex's `codex plugin` argv are deliberately not rendered here because this
package renders claude-code's Plan only (the prose points at the live preview). Widen the unexported
`check(path, planFn, pluginFn)` and `write(path, planFn, pluginFn)` (and `compare`) accordingly;
`Check(path)` and `Write(path)` keep their signatures and pass `setup.ClaudeCode.Plan,
claudeCodePluginActions()`.

Update every call site in `internal/setupgen/setupgen_test.go` per `<behavior>` and add
`TestRenderPluginTable`.

Regenerate the anchored region: `go run ./internal/surfacesgen` (the setupgen step of `task
surfaces:gen`), then confirm with `git diff -- skill/engram/commands/engram-setup.md` that the ONLY
change inside the anchors is the appended third table — the five delegation rows and five fallback
rows are byte-identical — and that `go run ./internal/surfacesgen --check-setup` exits 0.

Hand-author the prose OUTSIDE the anchors in `skill/engram/commands/engram-setup.md` (frontmatter on
line 1 untouched; wrap at the file's existing ~80-column width; rumdl-clean): (a) after "Never
hand-edit settings files. The plugin ships no bundled MCP server." add a sentence: under `--apply`, a
Claude Code or Codex whose own `plugin` CLI works receives the skills, hooks, and this command through
engram's own marketplace plugin (`engram@engram`) instead of a native skills copy — the two are
mutually exclusive per runtime — and every other runtime keeps the native copy carried inside the
binary; (b) in §"Generated command reference"'s preamble add: the third table lists the plugin
commands `--apply` issues for Claude Code by the plugin state the preview reports (`absent`,
`outdated`, `current`; `unavailable` means the runtime's `plugin` CLI did not answer and the native
copy is used); Codex's own `codex plugin` equivalents appear in the live preview's `plugin_command`
field, not in these synthetic tables; (c) in §"When the binary is present" step 2, list "plugin
results (absent, outdated, current, or unavailable with a reason)" beside registration details and
skills results, and in step 4 add "plugin install or update" to the changes the user must confirm,
plus one sentence: an existing native skills copy or index block reported beside a plugin-delivered
runtime is never removed by setup — tell the user, and let them remove it by hand if they want a
single copy. Keep every example vendor-neutral (the privacy guard scans this file).

Commit as `feat(setupgen): render the Claude Code plugin-delivery table and regenerate
/engram-setup` with the explicit pathspec `internal/setupgen/setupgen.go
internal/setupgen/setupgen_test.go skill/engram/commands/engram-setup.md` — the regenerated region
lands in the SAME commit as the `Render` change.
  </action>
  <verify>
    <automated>out=$(go test ./internal/setupgen/ -count=1 -v 2>&amp;1 &amp;&amp; go test ./internal/surfacesgen/ -count=1 -v 2>&amp;1 &amp;&amp; go test ./cmd/engram -run '^TestSetupGeneratedInvocations$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^(--- |    --- )(PASS|FAIL)' | tail -n 40; test "$rc" -eq 0 &amp;&amp; printf '%s\n' "$out" | rg -q -F -e '--- PASS: TestRenderPluginTable' &amp;&amp; printf '%s\n' "$out" | rg -q -F -e '--- PASS: TestRenderRealPlans/bearer+header' &amp;&amp; printf '%s\n' "$out" | rg -q -F -e '--- PASS: TestDriftChecks' &amp;&amp; go run ./internal/surfacesgen --check-setup &amp;&amp; rg -q -F -e '### Claude Code plugin delivery (--apply)' skill/engram/commands/engram-setup.md &amp;&amp; rg -q -F -e 'claude plugin update engram@engram --scope user --json -y' skill/engram/commands/engram-setup.md</automated>
    <fails_when>non-zero `go test` exit, any `--- FAIL:` line, the line `testing: warning: no tests to run`, no `--- PASS: TestRenderPluginTable`, `--- PASS: TestRenderRealPlans/bearer+header`, or `--- PASS: TestDriftChecks` line, `surfacesgen --check-setup` reporting `generated setup region has drifted`, or the committed command file lacking the plugin-delivery heading or the exact update argv</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -n -F 'type PluginActionsFunc func(state setup.PluginState, marketplacePresent bool) []setup.Action' internal/setupgen/setupgen.go` prints exactly one line; `rg -n -F 'func Render(planFn PlanFunc, pluginFn PluginActionsFunc) (string, error)' internal/setupgen/setupgen.go` prints exactly one line; `rg -n -F 'setup.ClaudeCode.(setup.PluginRuntime)' internal/setupgen/setupgen.go` prints exactly one line; `rg -n -F 'commandCell(setup.Plan{Actions: actions}.Display())' internal/setupgen/setupgen.go` prints exactly one line; `rg -n -F '"### Claude Code plugin delivery (--apply)' internal/setupgen/setupgen.go` prints exactly one line.
    - `rg -n -e '"codex", "plugin"|claude plugin install engram' internal/setupgen/setupgen.go` prints nothing (no argv is re-typed in the renderer — it comes from PluginActions).
    - `rg -n -F 'func Check(path string) error' internal/setupgen/setupgen.go` and `rg -n -F 'func Write(path string) error' internal/setupgen/setupgen.go` each print exactly one line (exported signatures unchanged; surfacesgen untouched).
    - `rg -c -F '| `oauth` |' skill/engram/commands/engram-setup.md` prints `2` and `rg -c -F '| `bearer+header` |' skill/engram/commands/engram-setup.md` prints `2` (the shipped rows are intact); `rg -c -e '^\| `(absent|outdated|current)`' skill/engram/commands/engram-setup.md` prints `4`; `rg -n -F '| `current` | (no action) |' skill/engram/commands/engram-setup.md` prints exactly one line.
    - `rg -n -F 'claude plugin marketplace add seanb4t/engram --scope user; claude plugin install engram@engram --scope user --json -y' skill/engram/commands/engram-setup.md` prints exactly one line.
    - `rg -n -i -F 'mutually exclusive' skill/engram/commands/engram-setup.md` prints at least one line; `rg -n -F 'plugin_command' skill/engram/commands/engram-setup.md` prints at least one line; `rg -n -F 'never removed' skill/engram/commands/engram-setup.md` prints at least one line; `head -1 skill/engram/commands/engram-setup.md` prints `---`.
    - `rg -n -F 'func TestRenderPluginTable' internal/setupgen/setupgen_test.go` prints exactly one line; `rg -c -F 'claudeCodePluginActions()' internal/setupgen/setupgen_test.go` prints at least `5`.
    - RED observed (record in SUMMARY): `go test ./internal/setupgen/ -count=1` fails to compile after the test edits and before the `Render` change; `go run ./internal/surfacesgen --check-setup` reports drift after the `Render` change and before regeneration; both green after.
    - `go run ./internal/surfacesgen --check-setup` exits 0; `task lint` exits 0 (rumdl over the command file, golangci-lint over setupgen); `uv run --with pytest pytest skill/engram/hooks/tests -q` exits 0.
  </acceptance_criteria>
  <done>`Render` appends a four-row plugin-delivery table sourced from claude-code's real `PluginActions` with the shipped tables byte-identical; the anchored region is regenerated and the drift gate is green; the prose explains plugin-first delivery, the four states, and the never-removed rule; every setupgen test passes and the change is committed with an explicit pathspec.</done>
</task>

<task type="auto">
  <name>Task 3: Plan gate — full `task`, license headers, zero go.mod drift, key-links gate, setupgen drift, privacy guard, clean tree</name>
  <files>none modified — verification-only; if `task fmt` or `task license:add` rewrites anything it must be one of this plan's six files_modified, and the rewrite is folded into a `chore(setupgen)` commit with an explicit pathspec</files>
  <read_first>
    - `Taskfile.yaml` — `default`, `lint` (rumdl covers `skill/engram/commands/engram-setup.md`; dprint/yamlfmt cover JSON/YAML), `test:python`, `license:check`.
    - `CLAUDE.md` §Conventions — SPDX on the new Go test file only; never on JSON or the frontmatter-first command file.
    - `.planning/STATE.md` §Blockers/Concerns — the Phase 1 gates; `TestRedEvidencePatchesAreLive` is the orchestrator's to register.
  </read_first>
  <action>
Run `task` (lint + test everywhere — includes `test:python`'s pytest run over the bundle), `task
license:check`, `git diff --exit-code -- go.mod go.sum`, `go test ./internal/keylinks/ -count=1`, `go
run ./internal/surfacesgen --check-setup`, `uv run --with pytest pytest skill/engram/hooks/tests -q`,
and confirm a clean tree under `skill/` and `internal/setupgen/`. Do not touch
`internal/store/redevidence_harness_test.go`. Never run `cmd/engram` tests with `-count>1`.

No commit is expected unless a formatter or the license fixer rewrote one of this plan's files; then
commit `chore(setupgen): gate fixups for the plugin-delivery table` with the explicit pathspec of
exactly the rewritten file(s).
  </action>
  <verify>
    <automated>task &amp;&amp; task license:check &amp;&amp; git diff --exit-code -- go.mod go.sum &amp;&amp; go test ./internal/keylinks/ -count=1 &amp;&amp; go run ./internal/surfacesgen --check-setup &amp;&amp; uv run --with pytest pytest skill/engram/hooks/tests -q &amp;&amp; test -z "$(git status --porcelain -- skill/ internal/setupgen/ release-please-config.json)"</automated>
    <fails_when>non-zero exit from any link of the chain: `task` prints `task: Failed to run task` (or a golangci-lint/rumdl finding / `--- FAIL` line), `license-eye` lists a file as missing its header, `git diff --exit-code -- go.mod go.sum` prints a hunk, `go test ./internal/keylinks/` prints `FAIL`, `surfacesgen --check-setup` reports `generated setup region has drifted`, pytest's summary line reads `failed` or `error`, or `git status --porcelain` prints a path under `skill/`, `internal/setupgen/`, or `release-please-config.json`</fails_when>
  </verify>
  <acceptance_criteria>
    - `task` exits 0 with no `--- FAIL:` line and no golangci-lint or rumdl finding.
    - `task license:check` exits 0.
    - `git diff --exit-code -- go.mod go.sum` exits 0 (no output).
    - `go test ./internal/keylinks/ -count=1` exits 0 and prints `ok`.
    - `go run ./internal/surfacesgen --check-setup` exits 0.
    - `uv run --with pytest pytest skill/engram/hooks/tests -q` exits 0 with a `passed` summary.
    - `git status --porcelain -- skill/ internal/setupgen/ release-please-config.json` prints nothing after the plan's commits.
  </acceptance_criteria>
  <done>`task`, `task license:check`, the empty go.mod/go.sum diff, the key-links gate, the setupgen drift check, and the pytest privacy guard all exit 0, and the working tree under `skill/`, `internal/setupgen/`, and `release-please-config.json` is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `skill/engram/**` → every consumer who installs the plugin | The shipped bundle (manifests, commands, skills, hooks) is public content; any private host or vendor id in it leaks to every installer. |
| `release-please-config.json` → both manifests | One config rewrites two `version` fields; a missing entry ships mismatched identities. |
| `setupgen.Render` → `/engram-setup` prose → an agent following it | The generated tables tell an agent what `--apply` will run; a stale or re-typed argv is what the AUTHORED-HERE invariant exists to prevent. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-05 | Information Disclosure | a vendor-specific or private-deployment substring shipped in `.codex-plugin/plugin.json` or the regenerated prose | high | mitigate | The description is byte-copied from the already-shipped neutral Claude manifest; `test_no_residual_memory_oauth.py` scans every file under `skill/engram/` (Task 1 and Task 3 run it); `TestPluginManifestIdentityMatches` re-checks the banned substrings non-literally. |
| T-03-13 | Tampering | the two manifests' identity fields diverging across a release (config entry missing or mistyped) | medium | mitigate | `TestPluginManifestIdentityMatches` asserts byte-equality of `name`/`version`/`description` AND the exact `extra-files` entry, under `task`; release-please rewrites both from one config. |
| T-03-14 | Tampering | `/engram-setup`'s generated table carrying an argv that differs from what `--apply` runs (re-typed or stale) | medium | mitigate | The table is rendered from `setup.ClaudeCode.(setup.PluginRuntime).PluginActions` through `Plan.Display()` — never a literal in setupgen (acceptance grep); `surfacesgen --check-setup` is the CI drift gate; `TestRenderPluginTable` pins the rows against the real actions. |
| T-03-15 | Elevation of Privilege | a `hooks` or `mcpServers` key in the codex manifest causing a duplicate-hooks load or a bundled server | low | mitigate | The manifest carries exactly four keys, asserted by `TestPluginManifestIdentityMatches`; `test_plugin_config.py`'s reasoning is cited in the manifest task. |
| T-03-SC | Tampering | npm/pip/cargo installs | low | accept | Not applicable — zero new packages; Task 3 asserts `git diff --exit-code -- go.mod go.sum`; pytest runs through the repo's existing `uv run --with pytest` invocation. |
</threat_model>

<verification>
- `go test ./internal/setupgen/ -count=1 -v` shows `--- PASS` for `TestPluginManifestIdentityMatches`, `TestRenderPluginTable`, `TestRenderRealPlans/bearer+header`, and `TestDriftChecks`.
- `go run ./internal/surfacesgen --check-setup` exits 0; `rg -c -F '| `oauth` |' skill/engram/commands/engram-setup.md` prints `2` (shipped rows intact) and the plugin-delivery heading appears once.
- `uv run --with pytest pytest skill/engram/hooks/tests -q` exits 0.
- `task`, `task license:check`, `git diff --exit-code -- go.mod go.sum`, and `go test ./internal/keylinks/ -count=1` exit 0.
- `git diff --stat FIRST^..LAST` touches only the six files in `files_modified`.
- Not gated here (rule m45p2b4bp7): Codex's loader accepting the minimal manifest — 03-VALIDATION.md Manual-Only, post-release.
</verification>

<success_criteria>
- `skill/engram/.codex-plugin/plugin.json` exists with identity fields equal to `.claude-plugin/plugin.json`'s, release-please-synced, drift-gated by a Go test under `task`, and clean under the shipped-bundle privacy guard (SC5's manifest half).
- `/engram-setup`'s generated region shows the exact claude-code plugin argv per state in the same commit that introduces the renderer change; the existing `setupgen` CI drift gate stays green; the prose explains plugin-first delivery and the never-removed rule (SC5's prose half).
- `task`, `task license:check`, and the key-links gate green; zero new dependencies.
</success_criteria>

<output>
Create `.planning/phases/03-plugin-first-delivery/03-04-SUMMARY.md` when done
</output>