---
phase: 05-apply-time-preserve-gate-documentation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs-site/src/content/docs/guides/install.md
  - docs-site/src/content/docs/guides/plugin.md
  - cmd/engram/install_docs_test.go
  - cmd/engram/plugin_docs_test.go
autonomous: true
requirements:
  - REQ-docs-setup-v2

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`docs-site/src/content/docs/guides/install.md` describes getting the binary AND what the cask installs — the binary, bash/zsh/fish completions, and one man page per command written into the Homebrew `share/man/man1` directory so `man engram` and `man engram-setup` work after a cask install (the cobra `GenManTree` dash separator, verified in 05-RESEARCH.md) — and points at `agent-setup.md` for plugin-first skill delivery via `engram setup --apply`; the archive route states that man pages and completions are not installed by hand (D-07)"
    - "`install.md` carries an `Unreleased as of v0.16.1` notice for the man-page cask hook and plugin-first delivery (neither is in a cut release: the latest tag is v0.16.1 and this milestone's Phases 1-5 are unreleased), placed beside the existing `Setup requires v0.16.0 or later` aside; the existing release-archive instructions for 0.16.0 are left as historical, verified evidence (D-06)"
    - "`docs-site/src/content/docs/guides/plugin.md` describes what the plugin is and does (command, SessionStart hook, PostToolUse hook), states that `engram setup --apply` installs it plugin-first on a Claude Code whose plugin CLI works (the manual `claude plugin marketplace add` / `claude plugin install` commands remain as the by-hand route), points at `agent-setup.md` for installation and runtime support, and cross-links a `preserved` setup result to the agent-setup guide's results section beside its own `claude mcp remove engram --scope user` fallback block (D-07)"
    - "Each of the two guides is gated by an `agent_setup_docs_test.go`-shaped test file — a relative-path constant, a pure `<guide>Violations(doc string) []error` function, a live-file test that skips when the guide is absent and fails when it is empty, and a positive control with a `clean` case plus one injected-violation case per leg — so a later edit that deletes any required sentence fails `go test ./cmd/engram` (D-07); the gates are deterministic pure functions over the guide text"
    - "The Astro site builds (`pnpm --dir docs-site install --frozen-lockfile && pnpm --dir docs-site build` exits 0) and `rumdl check` is clean over both guides; no guide documents the hidden `engram man <dir>` verb as a user command; no SPDX header is added to any `docs-site/**` file; both new Go test files carry the Apache-2.0 SPDX header"
  artifacts:
    - path: "docs-site/src/content/docs/guides/install.md"
      provides: "cask contents incl. man pages, plugin-first pointer, unreleased notice, archive-route note"
      contains: "man engram-setup"
    - path: "docs-site/src/content/docs/guides/plugin.md"
      provides: "plugin-first via engram setup --apply, agent-setup cross-links incl. the preserved remediation"
      contains: "preserved"
    - path: "cmd/engram/install_docs_test.go"
      provides: "installGuideRelPath, installGuideViolations, TestInstallGuideDocumentsSetupV2, TestInstallGuideGateFiresOnInjectedViolation"
      contains: "func TestInstallGuideDocumentsSetupV2("
    - path: "cmd/engram/plugin_docs_test.go"
      provides: "pluginGuideRelPath, pluginGuideViolations, TestPluginGuideDocumentsPluginFirst, TestPluginGuideGateFiresOnInjectedViolation"
      contains: "func TestPluginGuideDocumentsPluginFirst("
  key_links:
    - from: "cmd/engram/install_docs_test.go"
      to: "docs-site/src/content/docs/guides/install.md"
      via: "the install guide is read relative to cmd/engram exactly like agentSetupGuideRelPath"
      pattern: 'installGuideRelPath = "[.][.]/[.][.]/docs-site/src/content/docs/guides/install[.]md"'
    - from: "cmd/engram/plugin_docs_test.go"
      to: "docs-site/src/content/docs/guides/plugin.md"
      via: "the plugin guide is read relative to cmd/engram exactly like agentSetupGuideRelPath"
      pattern: 'pluginGuideRelPath = "[.][.]/[.][.]/docs-site/src/content/docs/guides/plugin[.]md"'
    - from: "cmd/engram/install_docs_test.go"
      to: "cmd/engram/migrate_docs_test.go"
      via: "the four-part docs-gate shape (path constant, violations func, live-file test, positive control) is replicated, never re-invented"
      pattern: 'func installGuideViolations[(]doc string[)] [[][]]error'
    - from: "cmd/engram/plugin_docs_test.go"
      to: "cmd/engram/migrate_docs_test.go"
      via: "same four-part shape"
      pattern: 'func pluginGuideViolations[(]doc string[)] [[][]]error'
  prohibitions:
    - statement: "MUST NOT claim that man pages, plugin-first delivery, `--header`, or the apply gate are available in a cut release: v0.16.1 is the latest tag and none of this milestone's phases has shipped — every such claim sits under an explicit unreleased notice until plan 05-04's post-release handoff replaces it with the observed version (D-06)"
      category: values
      status: resolved
      verification: test
      reason: "acceptance greps require the `Unreleased as of v0.16.1` aside in both guides; 05-POST-RELEASE.md step 4 owns the later replacement — no wired check descriptor by design (spec-less fallback, flagged-unverified)"
    - statement: "MUST NOT document the hidden `engram man <dir>` verb as an operator command or add a release-archive step that runs it: the cask hook is the only sanctioned producer of installed man pages (Phase 1's decision, `cmd/engram/man.go` Hidden: true)"
      category: values
      status: resolved
      verification: test
      reason: "acceptance grep holds install.md to zero occurrences of the hidden verb's invocation form — no wired check descriptor by design (spec-less fallback, flagged-unverified)"
    - statement: "MUST NOT invoke a real `claude`/`codex`/`opencode`/`brew` binary or touch `$HOME` in either new test file — the gates read a Markdown file relative to the package directory and nothing else (rule m45p2b4bp7)"
      category: safety
      status: resolved
      verification: test
      reason: "both files import only fmt/os/strings/testing (plus regexp where a row pattern is needed); acceptance grep forbids os/exec — no wired check descriptor by design (spec-less fallback, flagged-unverified)"
---

<objective>
Bring `guides/install.md` and `guides/plugin.md` current with what this milestone shipped, split
by reader intent per D-07: `install.md` = getting the binary and what the cask installs (binary,
completions, man pages — `man engram-setup`), with a plugin-first pointer to the setup guide;
`plugin.md` = what the plugin is and does, stating that `engram setup --apply` installs it
plugin-first, pointing at `agent-setup.md` for installation, and cross-linking a `preserved`
result to the setup guide's remediation beside its own fallback `claude mcp remove` block. Both
guides carry a truthful `Unreleased as of v0.16.1` notice (D-06). Each guide gets its own
`agent_setup_docs_test.go`-shaped gate — new files `install_docs_test.go` and
`plugin_docs_test.go` — with a live-file test and a positive control (D-07).

Purpose: REQ-docs-setup-v2 names all three guides; this plan delivers the two whose content does
not depend on plan 05-01's code (the man-page cask hook is Phase 1's, plugin-first is Phase 3's),
so it runs in wave 1 beside the apply-gate plan. The requirement itself stays `[ ]` until the
post-release observation (plan 05-04, D-06).

Output: two rewritten guides, two new docs-gate test files.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-apply-time-preserve-gate-documentation/05-CONTEXT.md
@.planning/phases/05-apply-time-preserve-gate-documentation/05-RESEARCH.md
@.planning/phases/05-apply-time-preserve-gate-documentation/05-PATTERNS.md
@docs-site/src/content/docs/guides/install.md
@docs-site/src/content/docs/guides/plugin.md
@cmd/engram/migrate_docs_test.go
@cmd/engram/agent_setup_docs_test.go
@cmd/engram/man.go
@.goreleaser.yaml
</context>

<interfaces>
`cmd/engram/install_docs_test.go` (new, SPDX header, `package main`):
- `const installGuideRelPath = "../../docs-site/src/content/docs/guides/install.md"`
- `func installGuideViolations(doc string) []error` — legs, one `fmt.Errorf` each, never stop at
  the first: 1 — the document contains `man engram-setup`; 2 — one line contains both
  `completions` and `man page` (the cask-contents sentence); 3 — one line contains both
  `plugin` and `/guides/agent-setup/` (the plugin-first pointer); 4 — one line contains
  `Unreleased as of v` (the D-06 notice; version-agnostic so the post-release flip to an
  `available in` notice is a deliberate gate edit, not an accident). Line splitting uses a raw
  literal newline variable (copy `agent_setup_docs_test.go`'s `newline` idiom under a distinct
  name, e.g. `installNewline`, or reuse `newline` since both files share `package main` — reuse
  is preferred; do not redeclare).
- `func TestInstallGuideDocumentsSetupV2(t *testing.T)` — reads the live file; `t.Skipf` when
  absent (trimmed checkout), `t.Fatalf` when empty, `t.Error` per violation.
- `func TestInstallGuideGateFiresOnInjectedViolation(t *testing.T)` — `clean` (false) +
  `man_page_missing`, `cask_contents_missing`, `plugin_pointer_missing`,
  `unreleased_notice_missing` (true), each a one-token `strings.Replace` on the clean fixture.

`cmd/engram/plugin_docs_test.go` (new, SPDX header, `package main`):
- `const pluginGuideRelPath = "../../docs-site/src/content/docs/guides/plugin.md"`
- `func pluginGuideViolations(doc string) []error` — legs: 1 — one line contains
  `/guides/agent-setup/` (cross-link exists); 2 — one line contains `engram setup` and `--apply`
  and `plugin` (plugin-first install statement); 3 — one line contains `preserved` and
  `/guides/agent-setup/` (the remediation cross-link); 4 — one line contains `Unreleased as of v`.
- `func TestPluginGuideDocumentsPluginFirst(t *testing.T)` and
  `func TestPluginGuideGateFiresOnInjectedViolation(t *testing.T)` — `clean` +
  `agent_setup_link_missing`, `plugin_first_missing`, `preserved_crosslink_missing`,
  `unreleased_notice_missing`.
</interfaces>

## Artifacts this phase produces (plan 05-03's share)

| Kind | Artifact | Created by |
|------|----------|------------|
| Docs | `guides/install.md`: cask-contents sentence (completions + man pages, `man engram-setup`), unreleased notice, plugin-first pointer, archive-route note | 05-03 Task 1 |
| Go test file | `cmd/engram/install_docs_test.go` — `installGuideRelPath`, `installGuideViolations`, `TestInstallGuideDocumentsSetupV2`, `TestInstallGuideGateFiresOnInjectedViolation` | 05-03 Task 1 |
| Docs | `guides/plugin.md`: plugin-first via `engram setup --apply`, `preserved` cross-link beside the fallback block, unreleased notice | 05-03 Task 2 |
| Go test file | `cmd/engram/plugin_docs_test.go` — `pluginGuideRelPath`, `pluginGuideViolations`, `TestPluginGuideDocumentsPluginFirst`, `TestPluginGuideGateFiresOnInjectedViolation` | 05-03 Task 2 |

## Flagged Assumptions (edge probe, spec-less fallback)

This plan authors none of the phase's 6 probe rows (plan 05-01 tallies 2 authored + 2 flagged,
05-02 authors 1, 05-04 authors 1 = 6). Recorded for the verifier: the exact roff filenames the
cask writes (`engram.1`, `engram-setup.1`, …) follow from cobra `v1.10.2`'s `GenManTree`
`CommandSeparator: "-"` and `.goreleaser.yaml`'s `engram{,-*}.1` uninstall glob
(05-RESEARCH.md, verified against the pinned module source) — the guide states `man
engram-setup`; whether the installed page renders is a 05-POST-RELEASE.md check, never a test
here (rule m45p2b4bp7).

## Prohibition probe (spec-less fallback, in-prompt recall)

Stage 1 raw candidates: must not claim release availability; must not document the hidden `man`
verb; must not run `brew` or a runtime CLI in a test; must not add SPDX headers to docs; must not
duplicate the setup guide's content into install/plugin (reader-intent split); must not paste a
vendor header name; must not add a release-archive man-page step. Stage 2 keeps three (authored
above, descriptor-less): **truthful-unreleased-notice** (D-06), **hidden-verb-not-documented**
(Phase 1's decision), and **no-real-CLI-in-tests** (m45p2b4bp7). The reader-intent split is
pinned by truths 1 and 3; SPDX-on-docs is a `license:check` scope rule (`.licenserc.yaml`).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `install.md` states what the cask installs — binary, completions, man pages (`man engram-setup`) — plus the plugin-first pointer and the unreleased notice, gated by a new `install_docs_test.go`</name>
  <files>docs-site/src/content/docs/guides/install.md, cmd/engram/install_docs_test.go</files>
  <read_first>
    - `docs-site/src/content/docs/guides/install.md` whole file (137 lines) — lines 10-15 (the availability aside: add the unreleased aside directly after it, same `:::note[...]` syntax), 17-30 (§Homebrew — the cask hooks sentence at 27-28 names completions only; extend it), 88-98 (§Unsigned binary on macOS — the archive route runs no hooks; add the "no completions, no man pages by hand" sentence here or in the archive section), 133-137 (§Next steps — extend the Agent Setup bullet with the plugin-first pointer).
    - `.goreleaser.yaml` lines 200-240 (the cask `post_install` hook: version gate, completions loop, `system_command binary, args: ["man", man1_dir]` writing into `#{HOMEBREW_PREFIX}/share/man/man1`; the uninstall glob `engram{,-*}.1`).
    - `cmd/engram/man.go` lines 130-156 (`manCmd` is `Hidden: true` — the guide must not present it as a user command), 56-63 (what a real `engram man <dir>` generates).
    - `cmd/engram/migrate_docs_test.go` whole file (151 lines — the four-part shape to replicate) and `cmd/engram/agent_setup_docs_test.go` lines 14-38 (`RelPath` idiom, the `newline` var — reuse it, both files are `package main`).
    - `docs-site/src/content/docs/guides/agent-setup.md` lines 165-184 (the current skills section, so the pointer you write matches the vocabulary plan 05-02 is using: "plugin-first", `seanb4t/engram`, `engram@engram`).
    - `05-RESEARCH.md` §"Documentation gap inventory" (`install.md`), §"Man page naming — engram-setup.1"; `05-CONTEXT.md` D-06, D-07; `.planning/milestones/2026-08-23.01-phases/06-install-documentation/06-POST-RELEASE.md` step 4 (notices replaced after the observed release).
  </read_first>
  <behavior>
    Gate FIRST (`cmd/engram/install_docs_test.go` per `<interfaces>`): RED —
    `TestInstallGuideDocumentsSetupV2` fails against the live guide on all four legs (zero
    occurrences of `man engram-setup`, no completions+man page line, no plugin pointer, no
    unreleased notice); the positive control's four violation cases discriminate against the
    in-memory clean fixture from the start. Then edit the guide until the live test is green.
  </behavior>
  <action>
    Guide edits (`docs-site/src/content/docs/guides/install.md`):
    1. After the `Setup requires v0.16.0 or later` aside, add `:::note[Unreleased as of v0.16.1]`
       stating that the man pages the cask installs and plugin-first skill delivery through
       `engram setup --apply` are on the main branch and not yet in a cut release; the notice is
       replaced with the observed version after the next release is verified.
    2. §Homebrew: extend the install-hooks sentence so ONE line states the cask's hooks check the
       installed version, generate bash, zsh, and fish completions, and write one man page per
       command into Homebrew's `share/man/man1` directory (the words `completions` and `man page`
       on the same line — leg 2). Follow with a sentence: after a cask install, `man engram` and
       `man engram-setup` describe the CLI and the setup command (leg 1); `brew uninstall` removes
       them with the completions.
    3. §Release archives / §Unsigned binary: one sentence stating an archive installation runs no
       hooks — completions and man pages are not installed; `engram <command> --help` covers the
       same content. Do NOT mention the hidden verb.
    4. §Next steps: reword the Agent Setup bullet so one line says setup registers the MCP server
       and, on a Claude Code or Codex whose plugin CLI works, delivers the curation skills
       plugin-first through the runtime's plugin system — link text `[Agent Setup](/guides/agent-setup/)`
       on that same line (leg 3: `plugin` and `/guides/agent-setup/` together).
    Gate file per `<interfaces>` (SPDX header; imports `fmt`, `os`, `strings`, `testing`; reuse the
    package-level `newline`). Then `rumdl check docs-site/src/content/docs/guides/install.md` and
    `pnpm --dir docs-site install --frozen-lockfile && pnpm --dir docs-site build` exit 0.
    One commit, explicit pathspec over the two files:
    `docs(install): describe cask man pages and completions with a plugin-first pointer` — body
    cites D-06/D-07 and REQ-docs-setup-v2 (code-gated half; requirement stays open per D-06).
  </action>
  <verify>
    <automated>out=$(go test ./cmd/engram -run '^(TestInstallGuideDocumentsSetupV2|TestInstallGuideGateFiresOnInjectedViolation)$' -count=1 -v 2>&1); rc=$?; printf '%s\n' "$out" | rg -e '^(--- |    --- )(PASS|FAIL|SKIP)' | tail -n 12; test "$rc" -eq 0 && printf '%s\n' "$out" | rg -q -F -e '--- PASS: TestInstallGuideDocumentsSetupV2' && test "$(printf '%s\n' "$out" | rg -o -F -e '--- PASS: TestInstallGuideGateFiresOnInjectedViolation/' | wc -l | tr -d ' ')" -ge 5 && rg -q -F -e 'man engram-setup' docs-site/src/content/docs/guides/install.md && rg -q -F -e 'Unreleased as of v0.16.1' docs-site/src/content/docs/guides/install.md && rumdl check docs-site/src/content/docs/guides/install.md</automated>
    <fails_when>non-zero exit, a `--- FAIL` or `--- SKIP` line, the live-guide PASS line absent, fewer than 5 positive-control subtest PASS lines, `man engram-setup` or the unreleased notice absent from the guide, or a rumdl finding</fails_when>
  </verify>
  <acceptance_criteria>
    - `head -2 cmd/engram/install_docs_test.go | rg -q -F 'SPDX-License-Identifier: Apache-2.0'` exits 0; `head -1 docs-site/src/content/docs/guides/install.md` prints `---` (no header on docs).
    - `rg -n -F 'completions' docs-site/src/content/docs/guides/install.md | rg -q -F 'man page'` exits 0; `rg -n -F 'plugin' docs-site/src/content/docs/guides/install.md | rg -q -F '/guides/agent-setup/'` exits 0.
    - Hidden verb not documented: `rg -n -e 'engram man ' docs-site/src/content/docs/guides/install.md` prints nothing.
    - `rg -n -e 'os/exec|exec[.]Command|UserHomeDir' cmd/engram/install_docs_test.go` prints nothing; `rg -o -F 'installGuideRelPath = "../../docs-site/src/content/docs/guides/install.md"' cmd/engram/install_docs_test.go | wc -l` prints `1`; `rg -n -e '^var newline' cmd/engram/install_docs_test.go` prints nothing (the shared var is reused, not redeclared).
    - `rg -o -e '^[[:space:]]+[{]"[a-z_]+", ' cmd/engram/install_docs_test.go | wc -l` prints `5` (clean + four violation cases).
    - `pnpm --dir docs-site install --frozen-lockfile && pnpm --dir docs-site build` exits 0; `git diff --exit-code -- docs-site/package.json docs-site/pnpm-lock.yaml` exits 0.
    - `git show --format= --name-only HEAD` lists exactly the two files in `<files>`; RED evidence in the SUMMARY: the live-guide test `--- FAIL` listing all four legs before the guide edit.
  </acceptance_criteria>
  <done>`install.md` truthfully states what the cask installs (incl. `man engram-setup`), what the archive route does not, and where plugin-first delivery is documented, under an unreleased notice; a four-leg gate with positive control pins it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `plugin.md` states plugin-first installation via `engram setup --apply`, cross-links a `preserved` result to the setup guide beside its fallback `mcp remove` block, and carries the unreleased notice — gated by a new `plugin_docs_test.go`; plan gate</name>
  <files>docs-site/src/content/docs/guides/plugin.md, cmd/engram/plugin_docs_test.go</files>
  <read_first>
    - `docs-site/src/content/docs/guides/plugin.md` whole file (90 lines) — lines 6-15 (what the plugin ships and the "no bundled MCP server" paragraph — keep), 17-30 (§Install the plugin — the manual `claude plugin marketplace add` / `claude plugin install` commands: keep them as the by-hand route, and add the plugin-first sentence ABOVE them), 44-49 (the delegation availability aside — add the unreleased aside after it), 51-55 (the delegation paragraph already linking `/guides/agent-setup/` — leg 1 is satisfied today; the gate pins it), 73-78 (the standalone-fallback `claude mcp remove engram --scope user` block — the `preserved` cross-link sentence lands directly after it).
    - `cmd/engram/install_docs_test.go` (Task 1's file — replicate its shape one more time; reuse `newline`).
    - `cmd/engram/setup.go` lines 986-1000 (the plugin-first paragraph: marketplace `seanb4t/engram`, plugin `engram@engram`, added/installed/updated/left alone) — the guide paraphrases it.
    - `docs-site/src/content/docs/guides/agent-setup.md` lines 186-208 (the results section the cross-link targets: anchor `/guides/agent-setup/#read-results-and-repeat-safely`).
    - `05-RESEARCH.md` §"Documentation gap inventory" (`plugin.md` — the cross-link recommendation), `05-CONTEXT.md` D-05 (the manual remediation this cross-link makes discoverable from the fallback block), D-06, D-07.
  </read_first>
  <behavior>
    Gate FIRST (`cmd/engram/plugin_docs_test.go` per `<interfaces>`): RED —
    `TestPluginGuideDocumentsPluginFirst` fails against the live guide on legs 2-4 (leg 1 already
    holds; record that honestly); positive control discriminates from the start. Then edit the
    guide until the live test is green.
  </behavior>
  <action>
    Guide edits (`docs-site/src/content/docs/guides/plugin.md`):
    1. §Install the plugin: add, above the manual commands, one line stating that on a Claude Code
       whose plugin CLI works, `engram setup --apply` installs this plugin for you — engram's own
       marketplace and plugin (`seanb4t/engram`, `engram@engram`), added when absent, updated when
       outdated, left alone when current — and that the commands below are the by-hand route
       (leg 2: `engram setup`, `--apply`, and `plugin` on one line). Link `[Agent Setup](/guides/agent-setup/)`
       on that line or the next.
    2. After the delegation availability aside add `:::note[Unreleased as of v0.16.1]` stating
       that plugin-first installation through `engram setup --apply` and the `preserved` result
       described below are on the main branch and not yet in a cut release; replaced with the
       observed version after the next release is verified (leg 4).
    3. After the standalone-fallback `claude mcp remove engram --scope user` block, add one line:
       if `engram setup` reports the registration as `preserved` (it carries something setup did
       not author and cannot reproduce), setup leaves it untouched and never runs this command
       for you — the preserved row's reason names the exact step; see the
       [results section](/guides/agent-setup/#read-results-and-repeat-safely) of the setup guide
       (leg 3: `preserved` and `/guides/agent-setup/` on one line).
    Gate file per `<interfaces>` (SPDX header; imports `fmt`, `os`, `strings`, `testing`; reuse
    `newline`). Then `rumdl check docs-site/src/content/docs/guides/plugin.md` and the plan gate.
    One commit, explicit pathspec over the two files:
    `docs(plugin): plugin-first installation via setup --apply and the preserved cross-link` —
    body cites D-05/D-06/D-07 and REQ-docs-setup-v2 (code-gated half; requirement stays open).
  </action>
  <verify>
    <automated>out=$(go test ./cmd/engram -run '^(TestPluginGuideDocumentsPluginFirst|TestPluginGuideGateFiresOnInjectedViolation|TestInstallGuideDocumentsSetupV2|TestAgentSetupGuideDocumentsDrift)$' -count=1 -v 2>&1); rc=$?; printf '%s\n' "$out" | rg -e '^(--- |    --- )(PASS|FAIL|SKIP)' | tail -n 16; test "$rc" -eq 0 && printf '%s\n' "$out" | rg -q -F -e '--- PASS: TestPluginGuideDocumentsPluginFirst' && test "$(printf '%s\n' "$out" | rg -o -F -e '--- PASS: TestPluginGuideGateFiresOnInjectedViolation/' | wc -l | tr -d ' ')" -ge 5 && rg -n -F -e 'preserved' docs-site/src/content/docs/guides/plugin.md | rg -q -F -e '/guides/agent-setup/' && rg -q -F -e 'Unreleased as of v0.16.1' docs-site/src/content/docs/guides/plugin.md && rumdl check docs-site/src/content/docs/guides/plugin.md docs-site/src/content/docs/guides/install.md && pnpm --dir docs-site install --frozen-lockfile && pnpm --dir docs-site build >/dev/null && task && task license:check && git diff --exit-code -- go.mod go.sum docs-site/package.json docs-site/pnpm-lock.yaml && go test ./internal/keylinks/ -count=1</automated>
    <fails_when>non-zero exit from any command, a `--- FAIL` or `--- SKIP` line, the live-guide PASS line absent, fewer than 5 positive-control subtest PASS lines, the `preserved` cross-link or the unreleased notice absent, a rumdl finding, an Astro build error, or `task`/license/keylinks red</fails_when>
  </verify>
  <acceptance_criteria>
    - `head -2 cmd/engram/plugin_docs_test.go | rg -q -F 'SPDX-License-Identifier: Apache-2.0'` exits 0; `head -1 docs-site/src/content/docs/guides/plugin.md` prints `---`.
    - `rg -n -F 'engram setup' docs-site/src/content/docs/guides/plugin.md | rg -F -e '--apply' | rg -q -F 'plugin'` exits 0; `rg -q -F 'claude mcp remove engram --scope user' docs-site/src/content/docs/guides/plugin.md` exits 0 (the fallback block is kept).
    - `rg -n -e 'os/exec|exec[.]Command|UserHomeDir' cmd/engram/plugin_docs_test.go` prints nothing; `rg -o -F 'pluginGuideRelPath = "../../docs-site/src/content/docs/guides/plugin.md"' cmd/engram/plugin_docs_test.go | wc -l` prints `1`; `rg -n -e '^var newline' cmd/engram/plugin_docs_test.go` prints nothing.
    - `rg -o -e '^[[:space:]]+[{]"[a-z_]+", ' cmd/engram/plugin_docs_test.go | wc -l` prints `5`.
    - `git show --format= --name-only HEAD` lists exactly the two files in `<files>`; `git diff --stat FIRST^..LAST` for this plan touches only the four files in `files_modified`.
    - RED evidence in the SUMMARY: the live-guide test `--- FAIL` on legs 2-4 (leg 1 green from the start, recorded as pinning existing content).
  </acceptance_criteria>
  <done>`plugin.md` says what the plugin is, that setup installs it plugin-first, where installation is documented, and what a `preserved` result means for its own fallback command, under an unreleased notice; a four-leg gate pins it; the plan gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| guide text -> operator | Public documentation; a false availability claim sends an operator to a release that lacks the feature; a missing remediation pointer leaves a `preserved` operator without the manual step. |
| docs-gate tests -> repo filesystem | Read one Markdown file relative to `cmd/engram`; never a CLI, never `$HOME`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-11 | Repudiation (false availability) | `install.md`/`plugin.md` claiming man pages or plugin-first delivery exist in v0.16.1 | medium | mitigate | `Unreleased as of v0.16.1` asides gated by leg 4 of each new gate (`Unreleased as of v` present); 05-POST-RELEASE.md step replaces them after observation. |
| T-05-12 | Tampering (operator misled) | documenting the hidden `engram man <dir>` verb, inviting operators to run an unsupported surface | low | mitigate | Task 1 acceptance grep holds `install.md` to zero `engram man ` occurrences; the cask hook is the only producer. |
| T-05-13 | Information Disclosure | a `preserved` operator copying the fallback `claude mcp remove` command without knowing setup declined on purpose — destroying the unreproducible entry blindly | medium | mitigate | Task 2's cross-link sentence beside the fallback block (leg 3) routes to the results section's remediation, which names what is being preserved first. |
| T-05-14 | Denial of Service (CI) | a docs gate that skips silently on a trimmed checkout or passes on an empty file | low | mitigate | Both live-file tests `t.Skipf` explicitly on absence and `t.Fatalf` on empty content, the migrate/agent-setup precedent. |
| T-05-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages; the docs build uses the committed lockfile (`git diff --exit-code -- docs-site/package.json docs-site/pnpm-lock.yaml`). |
</threat_model>

<verification>
- `go test ./cmd/engram -run '^(TestInstallGuideDocumentsSetupV2|TestInstallGuideGateFiresOnInjectedViolation|TestPluginGuideDocumentsPluginFirst|TestPluginGuideGateFiresOnInjectedViolation)$' -count=1 -v` shows every subtest passing, including both `clean` cases.
- `pnpm --dir docs-site install --frozen-lockfile && pnpm --dir docs-site build && rumdl check docs-site/src/content/docs/guides/install.md docs-site/src/content/docs/guides/plugin.md && task && task license:check && go test ./internal/keylinks/ -count=1` exits 0.
- `git diff --stat FIRST^..LAST` touches only the four files in `files_modified`.
- Red-evidence handoff for the orchestrator (registered after the phase's last plan): suggested patches `05-03-install-drops-man-page.patch` (delete the `man engram-setup` sentence -> `TestInstallGuideDocumentsSetupV2`), `05-03-plugin-drops-preserved-crosslink.patch` (delete the preserved cross-link line -> `TestPluginGuideDocumentsPluginFirst`).
- Verification-record note for `/gsd-verify-work` (D-06): this plan completes the CODE-GATED half of `REQ-docs-setup-v2`'s `install.md` and `plugin.md` legs. The requirement stays `[ ]` and `05-VERIFICATION.md` carries `post_release_status: pending` until a `05-RELEASE-<ver>.md` observation is recorded (plan 05-04). Do not check REQ-docs-setup-v2 off on the strength of this plan.
</verification>

<success_criteria>
- SC4 (code-gated half, two of three guides): `install.md` describes the binary, completions, and man pages the cask installs and points at plugin-first delivery; `plugin.md` describes what the plugin does, that setup installs it plugin-first, and where a `preserved` result is explained — both under truthful unreleased notices, both gated with positive controls.
</success_criteria>

<output>
Create `.planning/phases/05-apply-time-preserve-gate-documentation/05-03-SUMMARY.md` when done
</output>
