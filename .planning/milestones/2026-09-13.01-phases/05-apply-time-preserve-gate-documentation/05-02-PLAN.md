---
phase: 05-apply-time-preserve-gate-documentation
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - cmd/engram/setup.go
  - cmd/engram/setup_test.go
  - cmd/engram/testdata/help.golden
  - cmd/engram/agent_setup_docs_test.go
  - docs-site/src/content/docs/guides/agent-setup.md
autonomous: true
requirements:
  - REQ-apply-preserve-gate
  - REQ-apply-rewrite-consequence
  - REQ-docs-setup-v2

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`engram setup --help` (the `Long` text, pinned by `help.golden`) states that `--apply` makes the same comparison before writing, that an `already-correct` or `preserved` row runs no registration command (Claude Code's `mcp remove` included), that only a `would-write` row is written and then read back, that a `preserved` row's reason names the manual step in the runtime's own tool, and that a Claude Code rewrite of a registration carrying no Authorization header states you will need to log in again — the CLI is correct-by-reading (rule 4aksmneehh); `Short` and `catalog.golden` are byte-unchanged (D-01, D-04, D-05)"
    - "`engram setup --apply --runtime claude-code` on the observed `x-litellm-api-key` shape leaks the literal to neither stdout nor stderr in either output lane and reports `outcome: preserved` in json — the Phase 4 process-boundary redaction proof now covers the apply lane (D-02, REQ-drift-redaction carried into REQ-apply-preserve-gate)"
    - "`docs-site/src/content/docs/guides/agent-setup.md` documents: the apply gate (an `already-correct`/`preserved` row under `--apply` runs no registration command; only `would-write` is written then read back); the CORRECTED `already-correct` semantics for Claude Code and Codex (a pre-write guarantee — the sentence claiming it does not guarantee that no write ran is gone from the whole file); the `preserved` row's manual remediation naming `claude mcp remove engram --scope user` and the `[mcp_servers.engram]` table in `config.toml`; the OAuth re-login consequence on a Claude Code `would-write` row's `notes` in both preview and apply; plugin-first skill delivery per runtime naming `seanb4t/engram` and `engram@engram`; and an `Unreleased as of v0.16.1` notice covering every behavior not yet in a cut release (D-06, D-07)"
    - "`cmd/engram/agent_setup_docs_test.go`'s `agentSetupGuideDriftViolations` gains one leg per new claim plus a zero-occurrence leg over the two stale anchors, and `TestAgentSetupGuideDriftGateFiresOnInjectedViolation` gains one positive-control case per new leg while its `clean` case still yields zero violations — the gate is a deterministic pure function over the guide text, so a second run over unchanged guides reports the same zero violations (edge probe `idempotency` for REQ-docs-setup-v2, resolved explicit)"
    - "`opencode` is still documented as not compared on one line (existing leg 6), the `preserved` row still carries the whole-entry / never-merge / never-shown clauses (existing legs 2-4) now reworded so `--apply` LEAVES a preserved entry untouched rather than 'either overwrites or leaves it', and the `would-write` row still names `facets` (existing leg 5) — no existing leg is loosened to make room for a new one"
    - "The Astro site builds (`pnpm --dir docs-site install --frozen-lockfile && pnpm --dir docs-site build` exits 0) and `rumdl check` is clean after the guide edit; no test in this plan invokes a real `claude`/`codex`/`opencode` binary or touches `$HOME` (rule m45p2b4bp7)"
  artifacts:
    - path: "cmd/engram/setup.go"
      provides: "setupLongDescription's apply-gate sentences appended to the drift-comparison paragraph"
      contains: "before writing"
    - path: "cmd/engram/testdata/help.golden"
      provides: "regenerated `engram setup` section carrying the apply-gate and re-login sentences"
      contains: "log in again"
    - path: "cmd/engram/setup_test.go"
      provides: "TestSetupHelpStatesApplyGate; TestSetupJSONNeverLeaksProbeLiteral/claude-code-observed-shape extended with an --apply mode"
      contains: "func TestSetupHelpStatesApplyGate("
    - path: "cmd/engram/agent_setup_docs_test.go"
      provides: "agentSetupGuideStaleClaimAnchors, alreadyCorrectRowPattern, legs 8-13 of agentSetupGuideDriftViolations, matching positive-control cases"
      contains: "alreadyCorrectRowPattern"
    - path: "docs-site/src/content/docs/guides/agent-setup.md"
      provides: "apply gate, corrected already-correct row, preserved remediation, OAuth re-login consequence, plugin-first delivery, unreleased notice"
      contains: "log in again"
  key_links:
    - from: "cmd/engram/setup_test.go"
      to: "cmd/engram/setup.go"
      via: "the help text is pinned by a test that reads both setupCmd.Long and the help.golden section"
      pattern: 'func TestSetupHelpStatesApplyGate[(]'
    - from: "cmd/engram/agent_setup_docs_test.go"
      to: "docs-site/src/content/docs/guides/agent-setup.md"
      via: "the corrected already-correct row is located by its own table-row pattern, bracket classes only"
      pattern: 'alreadyCorrectRowPattern = regexp[.]MustCompile'
    - from: "cmd/engram/agent_setup_docs_test.go"
      to: "docs-site/src/content/docs/guides/agent-setup.md"
      via: "stale claims are gated on ZERO occurrences, the migrate_docs_test.go idiom"
      pattern: 'agentSetupGuideStaleClaimAnchors'
    - from: "cmd/engram/setup_test.go"
      to: "internal/setup/apply.go"
      via: "the Phase 4 literal-leak proof now runs the apply lane too"
      pattern: 'TestSetupJSONNeverLeaksProbeLiteral'
  prohibitions:
    - statement: "MUST NOT state or imply in `--help` or the guide that `--apply` can overwrite a `preserved` registration, that `already-correct` under `--apply` may still have run a write on Claude Code or Codex, or that a flag exists to force the rewrite (D-01/D-05): the guide's stale second sentence on the `already-correct` row and its 'may perform writes again' sentence are removed, and the preserved row says `--apply` leaves the entry untouched"
      category: values
      status: resolved
      verification: test
      reason: "agentSetupGuideStaleClaimAnchors gates both stale sentences on zero occurrences with a positive control per anchor; TestSetupHelpStatesApplyGate pins the help vocabulary — no wired check descriptor by design (spec-less fallback, flagged-unverified)"
    - statement: "MUST NOT write an unqualified availability claim: every behavior not in a cut release (v0.16.1 is the latest tag) sits under an explicit unreleased notice in the guide, per the D-10 precedent (D-06)"
      category: values
      status: resolved
      verification: test
      reason: "acceptance grep requires the `Unreleased as of v0.16.1` aside; 05-POST-RELEASE.md (plan 05-04) owns replacing it after the observed release — no wired check descriptor by design (spec-less fallback, flagged-unverified)"
    - statement: "MUST NOT regenerate `help.golden` as a side effect of an ordinary test run or touch `catalog.golden`, `skill/`, `internal/setupgen/`, or `release-please-config.json`: the golden is regenerated once by the explicit `-update -count=1` invocation and every other generated surface is asserted byte-unchanged"
      category: safety
      status: resolved
      verification: test
      reason: "the plan gate asserts `git status --porcelain` empty over the generated paths and `go run ./internal/surfacesgen --check-setup` clean — no wired check descriptor by design (spec-less fallback, flagged-unverified)"
---

<objective>
Make the CLI and the setup guide teach what plan 05-01 shipped. `engram setup --help` gains the
apply-gate sentences (D-01), the manual-remediation pointer (D-05), and the OAuth re-login
consequence (D-04), with `help.golden` regenerated through the repo mechanism and pinned by a
new help test; the Phase 4 literal-leak proof at the process boundary
(`TestSetupJSONNeverLeaksProbeLiteral/claude-code-observed-shape`) now also runs `--apply`
(D-02). `guides/agent-setup.md` is brought current per D-07's reader-intent split — running
`engram setup`: plugin-first delivery per runtime, the `--header` shape (already present), the
results table including `preserved`, the apply gate, the corrected `already-correct` semantics
(the sentence that became false under D-01 is REMOVED), the OAuth re-login note, and the manual
remediation — under a truthful `Unreleased as of v0.16.1` notice (D-06), and the existing docs
gate `agent_setup_docs_test.go` is extended with one leg per new claim plus a zero-occurrence
leg over the two stale sentences, each with a positive control.

Purpose: REQ-apply-preserve-gate and REQ-apply-rewrite-consequence are only complete when an
operator can learn the gate and the consequence by reading (rule 4aksmneehh); REQ-docs-setup-v2's
`agent-setup.md` leg is the code-gated half of the D-06 closeout — the requirement itself stays
open until plan 05-04's post-release handoff is observed.

Output: `setup.go` help paragraph + regenerated `help.golden`, two `cmd/engram` tests
(new/extended), the rewritten guide, and six new docs-gate legs with positive controls.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-apply-time-preserve-gate-documentation/05-CONTEXT.md
@.planning/phases/05-apply-time-preserve-gate-documentation/05-RESEARCH.md
@.planning/phases/05-apply-time-preserve-gate-documentation/05-PATTERNS.md
@.planning/phases/05-apply-time-preserve-gate-documentation/05-01-SUMMARY.md
@cmd/engram/setup.go
@cmd/engram/setup_test.go
@cmd/engram/agent_setup_docs_test.go
@cmd/engram/migrate_docs_test.go
@cmd/engram/golden_test.go
@docs-site/src/content/docs/guides/agent-setup.md
@internal/setup/claudecode.go
@internal/setup/codex.go
</context>

<interfaces>
`cmd/engram/setup_test.go`:
- `func TestSetupHelpStatesApplyGate(t *testing.T)` — lowercases `setupCmd.Long` and requires
  each of: `before writing`, `no registration command`, `mcp remove`, `runtime's own tool`,
  `log in again`; then reads `testdata/help.golden`, takes `helpGoldenSection(t, data, "engram
  setup")`, and requires `before writing` and `log in again` in that section (the
  `TestSetupHelpNamesDriftOutcomes` shape at lines 1351-1373).
- `TestSetupJSONNeverLeaksProbeLiteral/claude-code-observed-shape` gains an outer `mode` loop over
  `preview` (no extra arg) and `apply` (`--apply` appended to the argv); the json-lane
  assertions (`Outcome == preserved`, `Facets` contains `header-name`) hold in both modes; in
  `apply` mode additionally `strings.Contains(row.Reason, "claude mcp remove engram --scope
  user")`.

`cmd/engram/agent_setup_docs_test.go`:
- `var agentSetupGuideStaleClaimAnchors = []string{...}` — two anchors, each gated on ZERO
  occurrences over the whole document (the `migrateGuideStaleClaimAnchors` idiom): the
  `already-correct` row's stale second sentence (starts with "does not guarantee that no
  write") and the repeat-safety paragraph's "but it may perform writes again" clause.
- `var alreadyCorrectRowPattern = regexp.MustCompile("(?m)^[|][ ]*` + "`already-correct`" + `[ ]*[|].*[|][ ]*$")`
  — bracket classes only, no backslash (the file's own discipline, D-02 of Phase 1).
- Legs appended to `agentSetupGuideDriftViolations` after leg 7, one `fmt.Errorf` each:
  8 — each stale anchor occurs zero times; 9 — the `already-correct` row exists and contains
  `before writing` and `no registration command`; 10 — the `preserved` row contains `no
  registration command` and `untouched`; 11 — the `preserved` row contains `claude mcp remove
  engram --scope user` and `[mcp_servers.engram]`; 12 — one line contains all of `OAuth`, `log
  in again`, and `notes`; 13 — one line contains both `seanb4t/engram` and `engram@engram`.
- Positive-control cases added to `TestAgentSetupGuideDriftGateFiresOnInjectedViolation`
  (clean fixture extended with an `alreadyCorrectRowLine`, an `oauthLine`, a `pluginFirstLine`,
  and the reworded `preservedRowLine`): `stale_guarantee_sentence_present`,
  `stale_writes_again_sentence_present`, `already_correct_row_deleted`,
  `already_correct_omits_before_writing`, `preserved_omits_untouched`,
  `preserved_omits_claude_remediation`, `preserved_omits_codex_table`, `oauth_note_missing`,
  `plugin_first_missing` — each `expectViolation: true`; `clean` stays `false`.
</interfaces>

## Artifacts this phase produces (plan 05-02's share)

| Kind | Artifact | Created by |
|------|----------|------------|
| Help text | `setupLongDescription` apply-gate + remediation + re-login sentences in `cmd/engram/setup.go` | 05-02 Task 1 |
| Golden | `cmd/engram/testdata/help.golden` regenerated `engram setup` section | 05-02 Task 1 |
| Test func | `TestSetupHelpStatesApplyGate` in `cmd/engram/setup_test.go` | 05-02 Task 1 |
| Test edit | `TestSetupJSONNeverLeaksProbeLiteral/claude-code-observed-shape` gains `preview`/`apply` modes | 05-02 Task 1 |
| Docs | `guides/agent-setup.md`: unreleased notice, apply-gate paragraph, OAuth consequence, plugin-first section, corrected rows, corrected repeat-safety paragraph | 05-02 Task 2 |
| Test edits | `agentSetupGuideStaleClaimAnchors`, `alreadyCorrectRowPattern`, legs 8-13, nine positive-control cases in `cmd/engram/agent_setup_docs_test.go` | 05-02 Task 2 |

## Flagged Assumptions (edge probe, spec-less fallback)

This plan authors **1** of the phase's 6 probe rows (see plan 05-01 for the tally):

| Requirement | Probe category | Disposition |
|---|---|---|
| REQ-docs-setup-v2 | `idempotency` | **Authored (resolved, explicit)** — fourth truth: the docs gates are pure functions over file content; re-running them over unchanged guides yields identical results, and the positive control proves each leg discriminates. |

## Prohibition probe (spec-less fallback, in-prompt recall)

Stage 1 raw candidates: must not claim `--apply` overwrites preserved; must not keep the stale
already-correct sentence; must not document a force flag; must not claim availability in a cut
release; must not regenerate goldens silently; must not touch `catalog.golden`; must not loosen
an existing docs leg; must not document the hidden `engram man` verb as a user command; must
not paste vendor-specific header names into the guide. Stage 2 keeps three (authored above,
descriptor-less): the **no-false-overwrite-or-force claim** values prohibition, the
**truthful-unreleased-notice** values prohibition (D-06), and the **no-silent-golden-regeneration**
safety prohibition. "Do not loosen an existing leg" is pinned by truth 5; vendor neutrality is
Phase 2's `skill/` guard and the guide already uses `x-gateway-api-key`.

<!-- planner-discipline-allow: does not guarantee that no write -->
<!-- planner-discipline-allow: but it may perform writes again -->

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `--help` states the apply gate, the manual remediation, and the re-login consequence; `help.golden` regenerated; the process-boundary literal-leak proof runs `--apply`</name>
  <files>cmd/engram/setup.go, cmd/engram/setup_test.go, cmd/engram/testdata/help.golden</files>
  <read_first>
    - `cmd/engram/setup.go` lines 938-1034 (`setupLongDescription` doc comment and body — the drift-comparison paragraph at 972-982 is the paragraph to extend; the `--apply also delivers` paragraph at 986-1000 stays as-is), 856-871 (`setupApplySummary` — already counts preserved; no change).
    - `cmd/engram/setup_test.go` lines 1351-1373 (`TestSetupHelpNamesDriftOutcomes` — copy its two-part shape: `setupCmd.Long` vocabulary + `helpGoldenSection` check), 1102-1194 (`TestSetupJSONNeverLeaksProbeLiteral`, esp. the `claude-code-observed-shape` subtest at 1159-1194 — wrap its lane loop in a `mode` loop), 197-215 (`claudeGetProbeLiteralText`), 244-260 (`scriptedSetupRun` — every non-`mcp get` call exits 0, so `--apply`'s plugin probes read unavailable and, if the gate ever regressed, the remove/add calls would silently succeed: this test proves the LEAK property; plan 05-01's `TestSetupApplyPreservedRuntimeSkipsRegistrationWrite` proves the zero-write property).
    - `cmd/engram/golden_test.go` lines 18-30 (`-update` flag: regeneration is never a side effect of an ordinary run), 124-140 (`buildHelpGoldenContent`), 294-306 (`TestHelpGolden`); `Taskfile.yaml` `surfaces:gen` (the sanctioned invocation is `go test ./cmd/engram -run '^TestHelpGolden$' -update -count=1`, the 04-04 precedent).
    - `internal/setup/claudecode.go` — `claudeCodeManualRemediation`, `claudeCodeOAuthReLoginNote` (as shipped by 05-01; the help text paraphrases, never re-derives, these).
    - `.planning/phases/04-drift-detection-read-only/04-04-PLAN.md` Task 1 (the help-golden precedent) and `05-01-SUMMARY.md` (exact constant texts as shipped — if 05-01 deviated from its `<interfaces>`, the SUMMARY wins).
    - `.planning/STATE.md` §Accumulated Context "The CLI is correct-by-reading (4aksmneehh)"; `05-CONTEXT.md` D-01, D-04, D-05, "Claude's Discretion" (how `--help` states the gate).
  </read_first>
  <behavior>
    Tests FIRST (RED proven with `-run '^Name$' -v`, recorded in the SUMMARY):
    - `TestSetupHelpStatesApplyGate` per `<interfaces>` — RED: `Long` lacks `before writing`
      and `log in again`; `help.golden`'s section lacks them.
    - `TestSetupJSONNeverLeaksProbeLiteral/claude-code-observed-shape/{preview,apply}/{json,text}`
      per `<interfaces>` — the `apply` mode is GREEN on first run against 05-01's tree (record it
      so — it pins shipped behavior, not a faked RED); it must still be run individually.
  </behavior>
  <action>
    1. `cmd/engram/setup.go`: extend the drift-comparison paragraph of `setupLongDescription`
       with sentences stating, in plain operator prose: `--apply` makes the same comparison before
       writing — an already-correct or preserved row runs no registration command (on claude-code,
       not even its mcp remove), so repeating setup on a converged claude-code or codex
       registration is a no-op; only a would-write row is written and then read back (its
       registered field shows the new registration, redacted); a preserved row's reason names the
       exact step to clear the entry with the runtime's own tool, after which a fresh run reads
       would-write; on claude-code, a would-write row whose existing registration carries no
       Authorization header is treated as OAuth-authenticated and its notes field states, in
       preview and apply alike, that you will need to log in again after the rewrite. Keep the
       paragraph free of Go identifiers and free of any flag that does not exist. Update the
       function's doc comment (add a "Phase 5 (Apply-Time Preserve Gate)" sentence listing the
       additions, citing D-01/D-04/D-05).
    2. Regenerate the golden with exactly `go test ./cmd/engram -run '^TestHelpGolden$' -update
       -count=1`; confirm `catalog.golden` is byte-unchanged (`git status --porcelain --
       cmd/engram/testdata/catalog.golden` empty) — `Short` did not change, so it must be.
    3. Tests per `<behavior>` and `<interfaces>` (both in `cmd/engram/setup_test.go`).
    4. One commit with an explicit pathspec over the three files:
       `feat(cli): setup --help states the apply gate and the OAuth re-login consequence` — body
       cites D-01/D-04/D-05 and notes `help.golden` regenerated via the `-update` invocation.
  </action>
  <verify>
    <automated>out=$(go test ./cmd/engram -run '^(TestSetupHelpStatesApplyGate|TestSetupHelpNamesDriftOutcomes|TestHelpGolden|TestCatalogGolden|TestSetupJSONNeverLeaksProbeLiteral|TestSetupApplyPreservedRuntimeSkipsRegistrationWrite)$' -count=1 -v 2>&1); rc=$?; printf '%s\n' "$out" | rg -e '^(--- |    --- )(PASS|FAIL)' | tail -n 40; test "$rc" -eq 0 && printf '%s\n' "$out" | rg -q -F -e '--- PASS: TestSetupHelpStatesApplyGate' && printf '%s\n' "$out" | rg -q -F -e '--- PASS: TestHelpGolden' && printf '%s\n' "$out" | rg -q -F -e '--- PASS: TestCatalogGolden' && printf '%s\n' "$out" | rg -q -F -e '--- PASS: TestSetupJSONNeverLeaksProbeLiteral/claude-code-observed-shape/apply/json' && printf '%s\n' "$out" | rg -q -F -e '--- PASS: TestSetupJSONNeverLeaksProbeLiteral/claude-code-observed-shape/apply/text' && test -z "$(git status --porcelain -- cmd/engram/testdata/catalog.golden)"</automated>
    <fails_when>non-zero exit, a `--- FAIL` line, any of the five named `--- PASS` lines absent (a filter matching nothing prints no PASS lines), or `git status --porcelain` listing `catalog.golden`</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -q -F 'before writing' cmd/engram/setup.go` exits 0; `rg -q -F 'log in again' cmd/engram/setup.go` exits 0; `rg -q -F 'no registration command' cmd/engram/setup.go` exits 0; `rg -q -F "runtime's own tool" cmd/engram/setup.go` exits 0.
    - `rg -q -F 'before writing' cmd/engram/testdata/help.golden` exits 0 and `rg -q -F 'log in again' cmd/engram/testdata/help.golden` exits 0.
    - No phantom flag in help: `rg -n -e '--replace|--force|--overwrite' cmd/engram/setup.go` prints nothing.
    - `rg -o -F 'func TestSetupHelpStatesApplyGate(' cmd/engram/setup_test.go | wc -l` prints `1`; `rg -n -F '"--apply"' cmd/engram/setup_test.go | rg -q -F 'claude-code-observed-shape' || rg -n -B30 -F 'claude-code-observed-shape' cmd/engram/setup_test.go | rg -q -F 'mode'` — the subtest carries the apply mode (verify by reading the subtest: a `mode` loop with `preview` and `apply` cases).
    - `git show --format= --name-only HEAD` lists exactly `cmd/engram/setup.go`, `cmd/engram/setup_test.go`, `cmd/engram/testdata/help.golden`.
    - RED evidence in the SUMMARY: `TestSetupHelpStatesApplyGate` `--- FAIL` naming the missing vocabulary before the paragraph edit; the `apply` mode recorded as GREEN on first run (pins 05-01's shipped behavior).
  </acceptance_criteria>
  <done>`engram setup --help` teaches the apply gate, the manual remediation, and the re-login consequence; `help.golden` carries them; `catalog.golden` is untouched; the literal-leak proof holds under `--apply` in both output lanes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `agent-setup.md` brought current under a truthful unreleased notice — apply gate, corrected `already-correct`, preserved remediation, OAuth re-login, plugin-first delivery — with six new gate legs and their positive controls</name>
  <files>docs-site/src/content/docs/guides/agent-setup.md, cmd/engram/agent_setup_docs_test.go</files>
  <read_first>
    - `docs-site/src/content/docs/guides/agent-setup.md` whole file (258 lines) — lines 8-11 (the existing `:::note[Requires engram v0.16.0 or later]` aside; add the unreleased aside directly after it), 27-60 ("Preview, review, then apply" — the classification paragraph at 40-45 is where the apply-gate paragraph follows; the "Registration and OAuth login are separate steps" paragraph at 57-60 is where the re-login consequence cross-reference lands), 97-108 (§OAuth — the consequence paragraph lands here), 165-184 ("Registration and curation skills" — rewrite for plugin-first; the "Binary setup does not install the standalone Claude plugin's session hooks" sentence at 183 is now false for a plugin-delivered runtime), 186-208 (results table + the repeat-safety paragraph: the `already-correct` row at 192 carries the stale second sentence; the `preserved` row at 193 says a later `--apply` "either overwrites the entry or leaves it untouched" — false under D-01; the paragraph at 203-208 says repeating "may perform writes again" — false for claude-code/codex), 219-225 (Scripts and JSON — add `notes`).
    - `cmd/engram/agent_setup_docs_test.go` whole file (180 lines) — legs 1-7 (40-115), the `newline` var and the bracket-class-only regex discipline (22-38), the positive-control shape (150-180).
    - `cmd/engram/migrate_docs_test.go` lines 22-60 (`migrateGuideStaleClaimAnchors` and the zero-occurrence leg — copy the idiom and its "gated on ZERO, never a conversion count" comment).
    - `cmd/engram/setup.go` `setupLongDescription` (as shipped by Task 1) and lines 986-1000 (the plugin-first paragraph — the guide's plugin-first section paraphrases it: marketplace `seanb4t/engram`, plugin `engram@engram`, added when absent / installed when absent / updated when outdated / left alone when current; every other runtime gets the native copy).
    - `internal/setup/claudecode.go` (`claudeCodeManualRemediation`, `claudeCodeOAuthReLoginNote`), `internal/setup/codex.go` (`codexManualRemediation`) — the guide states the same steps in prose.
    - `docs-site/src/content/docs/guides/plugin.md` lines 73-78 (the standalone-fallback `claude mcp remove engram --scope user` block — the guide cross-links there for the Claude-only fallback), `install.md` lines 10-15 (the availability aside style to mirror for the unreleased notice).
    - `.planning/milestones/2026-08-23.01-phases/06-install-documentation/06-POST-RELEASE.md` step 4 (notices are updated to the observed release AFTER the human observation — the notice you write here is what that step later replaces).
    - `05-CONTEXT.md` D-06, D-07; `05-RESEARCH.md` §"Documentation gap inventory" (`agent-setup.md`), §"State of the Art" (the deprecated sentence), §"The docs-gate pattern to replicate".
  </read_first>
  <behavior>
    Gate legs FIRST (RED: `TestAgentSetupGuideDocumentsDrift` fails against the live guide on
    legs 8-13; the new positive-control cases fail to discriminate until the clean fixture is
    extended). Then edit the guide until the live-file test is green and the positive control's
    `clean` case still reports zero violations. Legs, anchors, pattern, and case names exactly
    per `<interfaces>`. The clean fixture's `preservedRowLine` is rewritten to the new row text
    (it must keep `whole entry`, `never merge`, `never shown` AND gain `no registration command`,
    `untouched`, `claude mcp remove engram --scope user`, `[mcp_servers.engram]`);
    `alreadyCorrectRowLine` carries `before writing` and `no registration command`; `oauthLine`
    carries `OAuth`, `log in again`, `notes`; `pluginFirstLine` carries `seanb4t/engram` and
    `engram@engram`. Each violation case mutates ONE token out of the clean fixture via
    `strings.Replace` (or appends a stale sentence for the two anchor cases).
  </behavior>
  <action>
    Guide edits (`docs-site/src/content/docs/guides/agent-setup.md`), keeping every existing gated
    sentence (legs 1-7) intact:
    1. After the `Requires engram v0.16.0 or later` aside add `:::note[Unreleased as of v0.16.1]`
       stating that `--header`, plugin-first skill delivery, the `preserved` outcome, the apply
       gate, and the OAuth re-login note described in this guide are on the main branch and not
       yet in a cut release; they ship in the next release, after which this notice is replaced
       with the observed version.
    2. "Preview, review, then apply": after the classification paragraph, add a paragraph stating
       that `--apply` makes the same comparison before writing — an `already-correct` or
       `preserved` row runs no registration command (on Claude Code, not even `claude mcp
       remove`), so repeating setup on a converged Claude Code or Codex installation is a no-op
       that never touches an existing OAuth login; only a `would-write` row is written, then read
       back. Extend the "Registration and OAuth login are separate steps" paragraph with one
       sentence pointing at the OAuth section's re-login note.
    3. §OAuth: add a paragraph — on Claude Code a `would-write` row is rewritten with `claude mcp
       remove` then `claude mcp add`; if the existing registration carries no `Authorization`
       header, setup treats it as OAuth-authenticated and the row's `notes` field states, in both
       preview and apply, that you will need to log in again after `--apply` because replacing
       the entry discards the login; read the preview's `notes` before applying — `--apply` does
       not pause and there is no flag to suppress the rewrite. (One line must contain `OAuth`,
       `log in again`, and `notes` together — leg 12.)
    4. "Registration and curation skills" -> rewrite the intro for plugin-first delivery: a
       Claude Code or Codex whose plugin CLI works receives the skills, session hooks, and the
       `/engram-setup` command through its plugin system — engram's own marketplace and plugin
       only (`seanb4t/engram`, `engram@engram`), added when absent, installed when absent, updated
       when outdated, left alone when current, with the plugin commands shown in the preview;
       every other runtime (opencode, generic, or a Claude Code/Codex without a working plugin
       CLI) receives the native skills copy carried in the binary, per the existing table. Each
       row reports registration, plugin (absent, outdated, current, or unavailable with a
       reason), and skills separately under one aggregated outcome. Replace the "Binary setup
       does not install the standalone Claude plugin's session hooks" sentence with: a
       plugin-delivered runtime receives the hooks through the plugin; a native-copy runtime does
       not — see the plugin guide. (One line must contain `seanb4t/engram` and `engram@engram` —
       leg 13.)
    5. Results table: rewrite the `already-correct` row — the registration read through the
       runtime's own CLI matches the requested URL, auth mode, and header names and references (a
       real comparison, not a guess); for Claude Code and Codex, `--apply` makes the same
       comparison before writing and runs no registration command when the row is
       `already-correct`; opencode is not compared, so its `--apply` writes and then reads back.
       Rewrite the `preserved` row so the whole-entry sentence reads: Claude Code and Codex both
       replace the whole entry on write (no partial merge), so `--apply` leaves a `preserved`
       registration untouched — it runs no registration command, not even Claude Code's `mcp
       remove` — and will never merge into it; then the remediation: to replace it yourself,
       clear it with the runtime's own tool (`claude mcp remove engram --scope user`; for Codex,
       delete the `[mcp_servers.engram]` table from `config.toml`), then run setup again — the
       row then reads `would-write`. Keep `never shown`. Extend the `would-write` row with: on
       Claude Code, apply rewrites with remove-then-add — see the OAuth section for the re-login
       note (keep `facets`). Extend the `wrote` row: `registered` shows the new registration with
       header values redacted.
    6. Repeat-safety paragraph: replace the sentence that says repeating may perform writes again
       with — for Claude Code and Codex a converged registration reads `already-correct` and no
       registration command runs; opencode is written again each time. Keep the interruption /
       not-one-transaction / removal-if-add-fails sentences (still true for a `would-write`
       rewrite on Claude Code).
    7. Scripts and JSON: extend the field sentence so `notes` is named (carries the OAuth
       re-login consequence and tolerant-step records) while the line naming `registered`,
       `facets`, and `drift` stays intact (leg 7).
    Gate edits (`cmd/engram/agent_setup_docs_test.go`) per `<interfaces>`: the anchors slice,
    `alreadyCorrectRowPattern`, legs 8-13 appended inside `agentSetupGuideDriftViolations`
    (update its doc comment's "seven ways" count), the extended clean fixture and nine new cases
    in `TestAgentSetupGuideDriftGateFiresOnInjectedViolation`. Bracket classes only in the new
    regex; the `newline` var for line splitting.
    Then `rumdl check docs-site/src/content/docs/guides/agent-setup.md` and `pnpm --dir docs-site
    build` must both exit 0 (fix any lint finding in the guide, never by disabling a rule).
    One commit, explicit pathspec over the two files:
    `docs(setup): agent-setup guide teaches the apply gate, remediation, and OAuth re-login` —
    body cites D-06/D-07 and REQ-docs-setup-v2 (code-gated half; requirement stays open per D-06).
  </action>
  <verify>
    <automated>out=$(go test ./cmd/engram -run '^(TestAgentSetupGuideDocumentsDrift|TestAgentSetupGuideDriftGateFiresOnInjectedViolation)$' -count=1 -v 2>&1); rc=$?; printf '%s\n' "$out" | rg -e '^(--- |    --- )(PASS|FAIL|SKIP)' | tail -n 30; test "$rc" -eq 0 && printf '%s\n' "$out" | rg -q -F -e '--- PASS: TestAgentSetupGuideDocumentsDrift' && test "$(printf '%s\n' "$out" | rg -o -F -e '--- PASS: TestAgentSetupGuideDriftGateFiresOnInjectedViolation/' | wc -l | tr -d ' ')" -ge 17 && printf '%s\n' "$out" | rg -q -F -e '--- PASS: TestAgentSetupGuideDriftGateFiresOnInjectedViolation/clean' && ! rg -q -F -e 'does not guarantee that no write' docs-site/src/content/docs/guides/agent-setup.md && ! rg -q -F -e 'but it may perform writes again' docs-site/src/content/docs/guides/agent-setup.md && rg -q -F -e 'Unreleased as of v0.16.1' docs-site/src/content/docs/guides/agent-setup.md && rumdl check docs-site/src/content/docs/guides/agent-setup.md && pnpm --dir docs-site install --frozen-lockfile && pnpm --dir docs-site build >/dev/null && task && task license:check && git diff --exit-code -- go.mod go.sum docs-site/package.json docs-site/pnpm-lock.yaml && go test ./internal/keylinks/ -count=1 && go run ./internal/surfacesgen --check-setup && test -z "$(git status --porcelain -- skill/ internal/setupgen/ cmd/engram/testdata/catalog.golden release-please-config.json)"</automated>
    <fails_when>non-zero exit from any command, a `--- FAIL` or `--- SKIP` line for the live-guide test, fewer than 17 positive-control subtest PASS lines, the `clean` case absent, either stale anchor still present in the guide, the unreleased notice absent, a rumdl finding, an Astro build error, `task`/license/keylinks/surfaces red, or `git status --porcelain` listing a generated path</fails_when>
  </verify>
  <acceptance_criteria>
    - Corrected rows: `rg -n -e '^[|][ ]*`already-correct`[ ]*[|]' docs-site/src/content/docs/guides/agent-setup.md | rg -q -F 'before writing'` exits 0; the same row line contains `no registration command`.
    - Preserved remediation on the row: `rg -n -e '^[|][ ]*`preserved`[ ]*[|]' docs-site/src/content/docs/guides/agent-setup.md | rg -q -F 'claude mcp remove engram --scope user'` exits 0 and the same line contains `[mcp_servers.engram]`, `untouched`, `whole entry`, `never merge`, `never shown`.
    - `rg -n -F 'log in again' docs-site/src/content/docs/guides/agent-setup.md | rg -F 'OAuth' | rg -q -F 'notes'` exits 0 (one line carries all three).
    - `rg -n -F 'seanb4t/engram' docs-site/src/content/docs/guides/agent-setup.md | rg -q -F 'engram@engram'` exits 0.
    - Existing legs intact: `rg -n -F 'opencode' docs-site/src/content/docs/guides/agent-setup.md | rg -q -F 'not compared'` exits 0; `rg -n -F 'registered' docs-site/src/content/docs/guides/agent-setup.md | rg -F 'facets' | rg -q -F 'drift'` exits 0.
    - The preserved row no longer claims `--apply` may overwrite: `rg -n -e '^[|][ ]*`preserved`[ ]*[|]' docs-site/src/content/docs/guides/agent-setup.md | rg -q -F 'either overwrites'` exits 1.
    - Gate file: `rg -o -F 'alreadyCorrectRowPattern = regexp.MustCompile(' cmd/engram/agent_setup_docs_test.go | wc -l` prints `1`, and that line is built from `(?m)^[|][ ]*`, a backticked cell name, and `[ ]*[|].*[|][ ]*$` exactly like `preservedRowPattern` — bracket classes only, no backslash escape anywhere in the file (inspect; the file's header comment states the discipline); `rg -o -F 'agentSetupGuideStaleClaimAnchors' cmd/engram/agent_setup_docs_test.go | wc -l` prints at least `2`; `rg -o -e 'expectViolation bool' cmd/engram/agent_setup_docs_test.go | wc -l` prints `1` and the cases slice has at least 17 entries (`rg -o -e '^[[:space:]]+[{]"[a-z_]+", ' cmd/engram/agent_setup_docs_test.go | wc -l` prints at least `17`).
    - `git show --format= --name-only HEAD` lists exactly the two files in `<files>`; no SPDX header was added to the guide (`head -1 docs-site/src/content/docs/guides/agent-setup.md` prints `---`).
    - RED evidence in the SUMMARY: `TestAgentSetupGuideDocumentsDrift` `--- FAIL` listing legs 8-13 against the pre-edit guide.
  </acceptance_criteria>
  <done>The guide states the apply gate, the corrected `already-correct` guarantee, the preserved remediation for both runtimes, the OAuth re-login consequence, and plugin-first delivery under a truthful unreleased notice; the gate discriminates every new claim; the site builds; the plan gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `setupCmd.Long` / `help.golden` -> operator | Engram-authored prose; must describe only behavior that exists (no phantom flag, no false overwrite claim). |
| guide text -> operator | Public documentation; a stale claim about `--apply`'s write behavior misleads an operator into a destructive or a needlessly repeated action. |
| `--apply` json/text output -> operator terminal / scripts | The observed literal must not cross in either lane under `--apply` (T-05-03 re-proved at the process boundary). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03 | Information Disclosure | a literal header value reaching stdout/stderr under `--apply` (post-write re-observe path) | high | mitigate | Task 1 extends `TestSetupJSONNeverLeaksProbeLiteral/claude-code-observed-shape` with an `apply` mode in both lanes; 05-01 Task 2's `TestApplyWroteRegisteredIsRedacted` covers the package level. |
| T-05-08 | Tampering (operator misled) | the guide's stale `already-correct` sentence and 'may perform writes again' clause contradicting D-01, or the preserved row claiming `--apply` may overwrite | medium | mitigate | Task 2: zero-occurrence anchors (`agentSetupGuideStaleClaimAnchors`) + reworded rows gated by legs 9-11, each with a positive control. |
| T-05-09 | Repudiation | the OAuth re-login consequence undocumented — an operator cannot learn it by reading before running `--apply` | medium | mitigate | Task 1 help sentence pinned by `TestSetupHelpStatesApplyGate`; Task 2 leg 12 (`OAuth` + `log in again` + `notes` on one line). |
| T-05-05 | Elevation of Privilege | help or guide documenting an overwrite/force flag that does not exist (inviting a script to try it) | low | mitigate | Task 1 acceptance grep forbids `--replace`/`--force`/`--overwrite` in `setup.go`; the guide states there is no flag to suppress the rewrite. |
| T-05-10 | Tampering | `help.golden`/`catalog.golden` regenerated as a side effect or drifting from the live cobra tree | low | mitigate | Explicit `-update -count=1` invocation only; `TestHelpGolden`/`TestCatalogGolden` green; `git status --porcelain` empty over `catalog.golden` and the other generated paths. |
| T-05-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages; `pnpm --dir docs-site install --frozen-lockfile && pnpm --dir docs-site build` uses the existing lockfile and the gate asserts `git diff --exit-code -- docs-site/package.json docs-site/pnpm-lock.yaml`. |
</threat_model>

<verification>
- `go test ./cmd/engram -run '^(TestSetupHelpStatesApplyGate|TestHelpGolden|TestCatalogGolden|TestSetupJSONNeverLeaksProbeLiteral|TestAgentSetupGuideDocumentsDrift|TestAgentSetupGuideDriftGateFiresOnInjectedViolation)$' -count=1 -v` shows every named subtest passing.
- `task && task license:check && git diff --exit-code -- go.mod go.sum && go test ./internal/keylinks/ -count=1 && go run ./internal/surfacesgen --check-setup && pnpm --dir docs-site install --frozen-lockfile && pnpm --dir docs-site build` exits 0.
- `git diff --stat FIRST^..LAST` touches only the five files in `files_modified`.
- Red-evidence handoff for the orchestrator (registered after the phase's last plan, never by a task here): suggested patches `05-02-help-drops-apply-gate.patch` (delete the apply-gate sentences from `setupLongDescription` -> `TestSetupHelpStatesApplyGate`), `05-02-guide-stale-guarantee.patch` (re-insert the stale second sentence on the `already-correct` row -> `TestAgentSetupGuideDocumentsDrift`), `05-02-guide-preserved-no-remediation.patch` (drop the `claude mcp remove engram --scope user` clause from the preserved row -> `TestAgentSetupGuideDocumentsDrift`).
- Verification-record note for `/gsd-verify-work` (D-06): this plan completes the CODE-GATED half of `REQ-docs-setup-v2`'s `agent-setup.md` leg. The requirement stays `[ ]` and Phase 5's `05-VERIFICATION.md` carries `post_release_status: pending` until a `05-RELEASE-<ver>.md` observation is recorded (plan 05-04 owns the handoff). Do not check REQ-docs-setup-v2 off on the strength of this plan.
</verification>

<success_criteria>
- SC4 (code-gated half): `agent-setup.md` describes plugin-first delivery, the header shape, the `preserved` outcome and apply gate, the OAuth re-login note, and the manual remediation, under a truthful unreleased notice, gated by legs with positive controls.
- `engram setup --help` is correct-by-reading for the gate and the consequence; `help.golden` pinned; `catalog.golden` untouched.
- The observed literal never crosses the process boundary under `--apply` in either lane.
</success_criteria>

<output>
Create `.planning/phases/05-apply-time-preserve-gate-documentation/05-02-SUMMARY.md` when done
</output>
