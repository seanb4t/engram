diff --git a/internal/setup/apply.go b/internal/setup/apply.go
index cbb96aa3..15d6ccbc 100644
--- a/internal/setup/apply.go
+++ b/internal/setup/apply.go
@@ -566,9 +566,7 @@ func execute(ctx context.Context, env Environment, rt Runtime, opts Options, mut
 		res.Outcome = OutcomeWrote
 		res.Registered = ""
 		if probe2Err == nil {
-			if obs2, ok2 := c.dr.Observe(probe2.Stdout+probe2.Stderr, opts); ok2 {
-				res.Registered = boundCapture(renderObservation(obs2))
-			}
+			res.Registered = displayCapture(probe2.Stdout + probe2.Stderr)
 		}
 		return res
 	}
