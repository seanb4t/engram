---
phase: 04-jev-reranker-per-hit-relevance-signal
plan: 06
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-03
files_modified:
  - internal/retrievaleval/rankers.go
  - internal/retrievaleval/rankers_test.go
  - internal/retrievaleval/retrieval_eval_test.go
autonomous: true
requirements:
  - RANK-03

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`evalRankers(nil)` returns the same ten-row roster as today with the `jev` row disabled (`Jev: disabled`); `evalRankers(hook)` with a non-nil hook enables the `jev` row, whose rank function is `store.RankWithHook` with that hook — the exact composition SearchReranked ships — so the eval measures the production reranker, not a copy (D-02)"
    - "The enabled `jev` row is opt-in: it is never a D-05 candidate or winner, even when it has the best paraphrase MRR and passes the #261 guard, and its D-05 eligible cell renders `opt-in`; the D-05 decision over every other row, both D-10 gates and the shipped row are unchanged — there is no eval bar for shipping the opt-in ranker (D-02)"
    - "`TestRetrievalEval` obtains the hook from `server.SearchRankHookFromEnv()`, so the Jev slot runs whenever a decisions provider is configured (independently of ENGRAM_SEARCH_RANKER), and logs one `JEV-EVAL | enabled=... model=... endpoint_host=... rerank_timeout=...` provenance line (host only, never a key)"
    - "Every Jev rank call that returns hits without relevance counts as a fallback; the run logs `JEV-EVAL | fallbacks=N/M` so a timeout- or error-degraded Jev row can never masquerade as measured Jev ranking"
    - "For each no-answer query the run logs the Jev row's per-hit relevance values (three decimals, in returned order), or `none` on a fallback — the per-hit 'nothing answers this' demonstration the CONTEXT specifics ask for"
    - "Without ENGRAM_RETRIEVAL_EVAL the gated test still skips and the package's unit tests need no network, no decisions provider and no Qdrant"
  artifacts:
    - path: "internal/retrievaleval/rankers.go"
      provides: "evalRankers(jevHook), namedRanker.optIn, variantSummary.optIn, opt-in exclusion in decideRanking, opt-in cell in formatVariantTable"
      contains: "func evalRankers(jevHook store.RankHook) []namedRanker"
    - path: "internal/retrievaleval/retrieval_eval_test.go"
      provides: "JEV-EVAL provenance, fallback count and no-answer relevance lines"
      contains: "JEV-EVAL | "
  key_links:
    - from: "internal/retrievaleval/rankers.go"
      to: "internal/store/rerank.go"
      via: "the jev row ranks through the shipped composition"
      pattern: "store[.]RankWithHook[(]"
    - from: "internal/retrievaleval/retrieval_eval_test.go"
      to: "internal/server/decider.go"
      via: "the eval builds the same no-retry, timeout-bounded hook as the server"
      pattern: "server[.]SearchRankHookFromEnv[(][)]"
  prohibitions:
    - statement: "MUST NOT let the Jev row's numbers select, gate or change the default ranking — the opt-in row never enters D-05, and the shipped (default) ranking stays lexical regardless of how Jev scores"
      category: integrity-of-evidence
      status: resolved
      verification: test
      reason: "TestDecideRanking's opt-in case (best MRR, guard passing, never the winner, absent from eligible). No wired check descriptor by design (spec-less fallback, flagged-unverified)"
---

<objective>
Enable the retrieval eval's Jev slot (D-02) so the live run in plan 04-07 records Jev's numbers next to lexical and vector-only without turning them into a ship gate.

- **Task 1 (TDD):** `evalRankers(jevHook)`, the opt-in flag, its exclusion from `decideRanking`, and the `opt-in` table cell.
- **Task 2:** `TestRetrievalEval` wiring — the hook from `server.SearchRankHookFromEnv()`, provenance, fallback count, and no-answer relevance lines.

Decisions implemented: D-02 (slot enabled when a provider is configured; no eval bar; numbers recorded), D-03 (the row measures the shipped composition, including its fallback).
Output: harness changes and unit tests; no live run here.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-jev-reranker-per-hit-relevance-signal/04-CONTEXT.md
@.planning/phases/04-jev-reranker-per-hit-relevance-signal/04-03-SUMMARY.md
@internal/retrievaleval/rankers.go
</context>

<interfaces>
- `internal/retrievaleval/rankers.go`: `jevDisabledReason = "Jev: disabled"`; `rankerFunc func(query string, pool []store.Memory, k int) []store.Memory`; `namedRanker{name, family, simplicity, tuned, rank, disabledReason}`; `evalRankers()` appends the disabled jev stub last; `variantSummary{..., disabled, disabledReason, ...}`; `buildSummaries(roster, ...)`; `formatVariantTable(rows, d)` renders the D-05 eligible column as yes/no, `—` for disabled and shipped rows; `decideRanking(rows)` step 2 excludes disabled and shipped rows.
- `internal/retrievaleval/rankers_test.go`: `TestEvalRankersRoster` (calls `evalRankers()`; ten names; simplicity strictly increasing over enabled rows; jev disabled), `TestDecideRanking` (table), `TestFormatVariantTable`.
- `internal/retrievaleval/retrieval_eval_test.go`: `TestRetrievalEval` — `roster := evalRankers()` (line ~170), per-query pool via `st.Search(..., store.CandidateK(defaultK), store.SearchOptions{Full: true})`, `variantRanked[r.name]`, the no-answer branch logging `no-answer <case>/<query>: <row> top=<label> score=<f>` (lines ~280-298), the final table, D-05 and D-10 log lines.
- From plan 04-01: `store.RankWithHook(ctx, query, hits, k, hook)`, `store.RankHook`, `store.Memory.Relevance *float64`. From plan 04-03: `server.SearchRankHookFromEnv() (store.RankHook, server.SearchRerankInfo, error)` with `SearchRerankInfo{Enabled bool; Model, EndpointHost string; Timeout time.Duration}`.
</interfaces>

## Artifacts this phase produces (plan 04-06's share)

| Kind | Artifact | Task |
|------|----------|------|
| Eval | `evalRankers(jevHook store.RankHook)`, `namedRanker.optIn`, `variantSummary.optIn`, opt-in exclusion in `decideRanking`, `opt-in` cell | 1 |
| Eval | `JEV-EVAL` provenance and fallback lines; no-answer Jev relevance lines | 2 |
| Tests | `TestEvalRankersRoster` (nil hook), `TestEvalRankersJevEnabled`, `TestDecideRanking` opt-in case, `TestFormatVariantTable` opt-in case | 1 |

## Flagged assumptions (spec-less probe)

- The eval uses the production search-path hook (no retry, `ENGRAM_SEARCH_RERANK_TIMEOUT`), so its Jev numbers include production fallbacks; plan 04-07 decides how to present a run with fallbacks.

## Executor notes (every task)

- Execution is sequential on the main tree. NEVER run any git stash subcommand. Commit each task with an explicit pathspec, Conventional Commits, and end the message with the session's attribution trailer lines.
- Keep the roster order, names, simplicity values and every non-jev row exactly as they are.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Opt-in Jev row in the roster, excluded from the D-05 decision</name>
  <files>internal/retrievaleval/rankers.go, internal/retrievaleval/rankers_test.go, internal/retrievaleval/retrieval_eval_test.go</files>
  <read_first>
    - `internal/retrievaleval/rankers.go` (whole file)
    - `internal/retrievaleval/rankers_test.go` (whole file)
  </read_first>
  <behavior>
    - `TestEvalRankersRoster` (updated call to `evalRankers(nil)`, assertions otherwise unchanged): ten names, jev disabled with `jevDisabledReason`, `optIn` false on every row.
    - `TestEvalRankersJevEnabled`: with a scripted hook, the roster has the same ten names in order; the jev row has a non-nil rank, `optIn` true and an empty `disabledReason`; its rank over a fixed pool deep-equals `store.RankWithHook(ctx, q, pool, k, hook)`; every other row is identical (name, family, simplicity, tuned, optIn false) to the nil-hook roster.
    - `TestDecideRanking` new case `opt-in row never wins`: rows where an opt-in jev row passes the guard with the highest paraphrase MRR — the winner, reason and eligible list equal those of the same rows without the jev row, and jev is absent from eligible.
    - `TestFormatVariantTable` new case: an enabled opt-in row renders its numbers and `opt-in` in the D-05 eligible column.
  </behavior>
  <action>
Implements D-02's harness half. Write the new test cases first (they fail to compile against `evalRankers()`). Add `optIn bool` to `namedRanker` and `variantSummary` (doc comments: an opt-in ranker is measured and reported but never competes in D-05 — it ships as an operator opt-in, not as the default, D-02). `buildSummaries` copies it. `decideRanking` step 2 also skips `optIn` rows; update its numbered doc comment accordingly. `formatVariantTable` renders `opt-in` in the eligible column for an enabled opt-in row. Change the signature to `evalRankers(jevHook store.RankHook) []namedRanker`: a nil hook appends today's disabled stub unchanged; a non-nil hook appends `name "jev"`, `family "jev"`, `simplicity 99`, `optIn true`, and a rank closure calling `store.RankWithHook(context.Background(), query, pool, k, jevHook)` (the client's own timeout bounds the call). Update `evalRankers`' and `jevDisabledReason`'s doc comments to describe the enabled form. The package's test files compile together, so change the single existing call site in `retrieval_eval_test.go` to `evalRankers(nil)` in this task; Task 2 replaces it with the configured hook.

Commit as `feat(retrievaleval): add an opt-in Jev row outside the D-05 decision (D-02)` with pathspec `internal/retrievaleval/rankers.go internal/retrievaleval/rankers_test.go internal/retrievaleval/retrieval_eval_test.go` (include the eval test file only if it changed).
  </action>
  <verify>
    <automated>out=$(go test ./internal/retrievaleval/ -run '^(TestEvalRankersRoster|TestEvalRankersJevEnabled|TestDecideRanking|TestFormatVariantTable|TestVariantMetrics)$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e '^--- PASS: (TestEvalRankersRoster|TestEvalRankersJevEnabled|TestDecideRanking|TestFormatVariantTable|TestVariantMetrics) ' | wc -l)" -eq 5 &amp;&amp; printf '%s\n' "$out" | rg -q -e '--- PASS: TestDecideRanking/opt-in_row_never_wins'</automated>
    <fails_when>non-zero exit, fewer than five top-level PASS lines, or the opt-in D-05 case missing or not passing</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -o -e 'store[.]RankWithHook[(]' internal/retrievaleval/rankers.go | wc -l` prints `1`.
    - `golangci-lint run ./internal/retrievaleval/...` reports no findings.
  </acceptance_criteria>
  <done>The roster can carry an enabled, opt-in Jev row measured through the shipped composition, and D-05 provably ignores it; committed.</done>
</task>

<task type="auto">
  <name>Task 2: TestRetrievalEval runs the Jev slot when a provider is configured — provenance, fallbacks, no-answer relevance</name>
  <files>internal/retrievaleval/retrieval_eval_test.go</files>
  <read_first>
    - `internal/retrievaleval/retrieval_eval_test.go` lines 140-440 (TestRetrievalEval)
    - `internal/server/decider.go` (post-04-03: SearchRankHookFromEnv, SearchRerankInfo)
  </read_first>
  <action>
Implements D-02's measurement wiring. In `TestRetrievalEval`, after the embedder is built: call `server.SearchRankHookFromEnv()` (fatal on error), build `roster := evalRankers(jevHook)`, and log `JEV-EVAL | enabled=<bool> model=<model> endpoint_host=<host> rerank_timeout=<duration>`. Where each enabled roster row ranks the pool, for the jev row count one call and, when the ranked slice is non-empty and its first element carries no Relevance, one fallback. In the no-answer branch, after the existing per-row lines, when the jev row is enabled log `no-answer <case>/<query>: jev relevance=[v1 v2 ...]` with each returned hit's relevance to three decimals in returned order, or `relevance=none` when the call fell back. After the final `shipped (SearchReranked) matches` line, when enabled log `JEV-EVAL | fallbacks=<N>/<M>`. Change nothing about the D-05 decision, the D-10 gates, the shipped row or the existing log formats (plans 01-05/01-06 and 04-07 grep them).

Commit as `feat(retrievaleval): run the Jev slot when a decisions provider is configured (D-02)` with pathspec `internal/retrievaleval/retrieval_eval_test.go`.
  </action>
  <verify>
    <automated>go vet ./internal/retrievaleval/ &amp;&amp; out=$(env -u ENGRAM_RETRIEVAL_EVAL go test ./internal/retrievaleval/ -run '^TestRetrievalEval$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; printf '%s\n' "$out" | rg -q -e '^--- SKIP: TestRetrievalEval '</automated>
    <fails_when>a vet error, non-zero exit, or no SKIP line for TestRetrievalEval (the gate no longer skips without ENGRAM_RETRIEVAL_EVAL)</fails_when>
    <automated>test "$(rg -o -e 'JEV-EVAL [|] (enabled|fallbacks)=' internal/retrievaleval/retrieval_eval_test.go | wc -l)" -eq 2 &amp;&amp; test "$(rg -o -F 'jev relevance=' internal/retrievaleval/retrieval_eval_test.go | wc -l)" -ge 1 &amp;&amp; go test ./internal/retrievaleval/ -count=1</automated>
    <fails_when>a provenance or fallback line missing, the no-answer relevance line missing, or a unit test in the package failing</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -o -e 'server[.]SearchRankHookFromEnv[(][)]' internal/retrievaleval/retrieval_eval_test.go | wc -l` prints `1`.
    - `golangci-lint run ./internal/retrievaleval/...` reports no findings.
  </acceptance_criteria>
  <done>A live `task eval:retrieval` with a decisions provider configured will report the Jev row, its provenance, its fallback count and its no-answer relevance values, while every existing gate and log line is unchanged; committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval harness → decisions provider | synthetic, committed corpus text only (no spine content) is sent when the operator runs the gated eval |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-11 | Repudiation / integrity of evidence | D-05 decision | medium | mitigate | the opt-in row cannot win or gate (TestDecideRanking opt-in case); fallbacks are counted and logged so degraded Jev numbers are labeled |
| T-04-12 | Information disclosure | eval provenance log | low | mitigate | host-only endpoint and model; `SearchRankHookFromEnv` never exposes the key |
| T-04-13 | Information disclosure | eval egress | low | accept | the gated eval sends only the committed synthetic paraphrase/#261 corpus, and only when an operator sets ENGRAM_RETRIEVAL_EVAL and a provider |
</threat_model>

<verification>
- `go test ./internal/retrievaleval/ -count=1` exits 0 (unit tests pass; the live eval skips).
- `go vet ./internal/retrievaleval/` exits 0.
</verification>

<success_criteria>
- D-02: the Jev slot is enabled whenever a decisions provider is configured, measured through the shipped composition, and kept out of the ship decision.
</success_criteria>

<output>
Create `.planning/phases/04-jev-reranker-per-hit-relevance-signal/04-06-SUMMARY.md` when done. Record the RED evidence for Task 1.
</output>
