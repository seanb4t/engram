---
phase: 04-jev-reranker-per-hit-relevance-signal
plan: 05
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-03
files_modified:
  - internal/store/store.go
  - internal/store/discovery_rerank_test.go
  - internal/server/store_iface.go
  - internal/server/fakestore_test.go
  - internal/server/tools.go
  - internal/server/discovery_rerank_test.go
  - docs-site/src/content/docs/reference/tools.md
  - docs-site/src/content/docs/reference/memory-record.md
autonomous: true
requirements:
  - RANK-03
  - RANK-04

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "With no rank hook (the lexical default), `search_discovery` on MCP and `SearchDiscoveries` on Connect call `Store.SearchDiscovery` exactly as today — same k, no over-fetch, no rerank, no relevance — and never call the new reranked method (default byte-identical, CONTEXT boundary: lexical stays default)"
    - "With a rank hook, `deps.searchDiscovery` calls `Store.SearchDiscoveryReranked`, which over-fetches `CandidateK(k)` through the existing `SearchDiscovery` filter (discovery category, scope or cross-spine, kind, owner-or-shared, superseded and archived hidden), hands the whole pool to the hook in vector order, stable-sorts by relevance, truncates to k and stamps `Relevance` (D-07, D-04)"
    - "Discovery's fallback is ITS shipped order: on any hook failure `SearchDiscoveryReranked` returns the first k of the over-fetched pool in vector order — the same ids and order `SearchDiscovery` returns at k on the fixture — with no relevance; the D-05 lexical step is NOT applied to discoveries (D-03's 'fallback is exactly today's shipped order')"
    - "Relevance reaches both discovery lanes: MCP `search_discovery` (store.Memory JSON) and Connect `SearchDiscoveries` (memoriesToProto) carry the same per-hit values; neither lane returns another owner's private discovery or hands it to the hook (RANK-04)"
    - "`SearchDiscoveryReranked` rejects k == 0 with ErrInvalidArgument and a k above MaxRecallLimit with the same bound SearchDiscovery enforces"
    - "`search_memory` and `search_discovery` MCP tool descriptions each gain one sentence explaining `relevance` (present only when the operator enabled Jev reranking; the provider's 0-1 probability the record answers the query; all near zero means nothing returned answers it); reference/tools.md documents the field for both tools and memory-record.md lists it as a query-time, never-stored field beside `score`"
    - "FLAGGED ASSUMPTION (D-07 discretion: how search_discovery is threaded): a separate `SearchDiscoveryReranked` gated on the hook, base order = vector order, instead of routing discoveries through SearchReranked's lexical step (research Option B, adjusted so the fallback equals discovery's shipped vector order rather than a new lexical order)"
    - "FLAGGED ASSUMPTION: the over-fetch changes the Qdrant limit from k to CandidateK(k) on the jev path only; on exact (full-scan) collections the top-k are identical, and under approximate HNSW search the first k of a larger request can differ marginally from a size-k request"
  artifacts:
    - path: "internal/store/store.go"
      provides: "SearchDiscoveryReranked"
      contains: "func (s *Store) SearchDiscoveryReranked(ctx context.Context, scope, kind string, subj Subject, query string, vec []float32, k uint64, hook RankHook) ([]Memory, error)"
    - path: "internal/server/tools.go"
      provides: "hook-gated discovery branch and the two description sentences"
      contains: "SearchDiscoveryReranked("
    - path: "docs-site/src/content/docs/reference/memory-record.md"
      provides: "the Relevance field row"
      contains: "| Relevance | `relevance` |"
  key_links:
    - from: "internal/store/store.go"
      to: "internal/store/rerank.go"
      via: "the discovery path reuses the shared hook application and validation"
      pattern: "applyRankHook[(]ctx, query, hits, hook[)]"
    - from: "internal/server/tools.go"
      to: "internal/server/store_iface.go"
      via: "the reranked discovery method is part of the deps store surface"
      pattern: "d[.]st[.]SearchDiscoveryReranked[(]"
    - from: "internal/server/store_iface.go"
      to: "internal/store/store.go"
      via: "the compile-time memStore assertion keeps the carve in sync"
      pattern: "SearchDiscoveryReranked[(]ctx context[.]Context"
  prohibitions:
    - statement: "MUST NOT change search_discovery's ordering, fetch size or response for any caller while no rank hook is configured"
      category: safety
      status: resolved
      verification: test
      reason: "TestSearchDiscoveryDefaultPathUnchanged (spy records SearchDiscovery at the caller's k and never SearchDiscoveryReranked). No wired check descriptor by design (spec-less fallback, flagged-unverified)"
    - statement: "MUST NOT widen visibility on the discovery path — another owner's private discovery never reaches the hook or either lane"
      category: privacy
      status: resolved
      verification: test
      reason: "TestSearchDiscoveryRerankedOwnerIsolation (store, real Qdrant) and the cross-owner subtest of TestSearchDiscoveryRelevanceBothLanes. No wired check descriptor by design (spec-less fallback, flagged-unverified)"
---

<objective>
Give `search_discovery` the opt-in Jev path without touching its default, and document the relevance field on every tool surface.

- **Task 1 (TDD):** `Store.SearchDiscoveryReranked` — over-fetch through the existing discovery filter, hook on the vector-ordered pool, truncate; fallback equals discovery's shipped order.
- **Task 2 (TDD):** `memStore` and `spyStore` gain the method; `deps.searchDiscovery` branches on `d.rankHook`; MCP and Connect discovery lanes carry relevance; the default path is proven unchanged.
- **Task 3:** MCP tool descriptions and the reference docs (tools.md, memory-record.md).

Decisions implemented: D-07 (`search_discovery` in scope, threading at planner discretion), D-03 (fallback equals shipped order), D-05 (relevance on MCP and Connect discovery results), D-06 (per-hit only).
Output: one store method, the server branch, docs, tests.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-jev-reranker-per-hit-relevance-signal/04-CONTEXT.md
@.planning/phases/04-jev-reranker-per-hit-relevance-signal/04-RESEARCH.md
@.planning/phases/04-jev-reranker-per-hit-relevance-signal/04-01-SUMMARY.md
@.planning/phases/04-jev-reranker-per-hit-relevance-signal/04-03-SUMMARY.md
@internal/store/rerank.go
</context>

<interfaces>
- `internal/store/store.go`: `SearchDiscovery(ctx, scope, kind string, subj Subject, vec []float32, k uint64) ([]Memory, error)` (line ~1352) — `rejectOverMaximum("k", k)`, discovery/scope/kind/owner-or-shared/superseded/archived filter, Qdrant query at `Limit: k`, full-view `fetchPayloadsByID`, results in Qdrant score order with Score re-attached; it never calls the rank step. `SearchReranked`'s k == 0 guard message is the precedent for the new guard.
- From plan 04-01: `store.RankHook`, unexported `applyRankHook(ctx, query, ordered, hook) []Memory` (nil hook or empty pool returns ordered unchanged; otherwise validated stable sort with Relevance stamped, or ordered unchanged on any failure), `store.CandidateK`, `store.MaxRecallLimit`.
- `internal/server/store_iface.go`: `memStore` (with `SearchDiscovery` and `SearchReranked`) and `var _ memStore = (*store.Store)(nil)`. `internal/server/fakestore_test.go`: `spyStore.SearchDiscovery` records `"SearchDiscovery"` via `s.record(...)`; wrappers embed `memStore` (upsertFailStore, migrateStatusFailStore) and need no change.
- `internal/server/tools.go`: `deps.searchDiscovery` (line ~2054) ends with `return d.st.SearchDiscovery(ctx, scope, a.Kind, c.Subj, vec, a.K)`; MCP `search_discovery` returns `map[string]any{"discoveries": hits}` (store.Memory JSON); `search_memory` description (line ~2786). `internal/server/connectapi.go` `SearchDiscoveries` (line ~421) defaults k to 20 and maps an empty scope to CrossSpine.
- Store test helpers: `testStore(t)`, `Upsert`, `DeleteAll(ctx, scope, Anonymous())`, `Authenticated`, discovery fixtures in `TestSearchDiscoveryFilters` / `TestSearchDiscoveryOwnerIsolation` (`internal/store/store_test.go` lines ~374-485; discoveries need `Category: "discovery"`, a `Kind`, and at least one citation).
- Docs: `docs-site/src/content/docs/reference/tools.md` `## search_memory` (line ~133) and `## search_discovery` (line ~446); `docs-site/src/content/docs/reference/memory-record.md` field table (the `Score` row, line ~43).
</interfaces>

## Artifacts this phase produces (plan 04-05's share)

| Kind | Artifact | Task |
|------|----------|------|
| Store | `(*Store).SearchDiscoveryReranked` | 1 |
| Server | `memStore.SearchDiscoveryReranked`, `spyStore.SearchDiscoveryReranked`, hook-gated branch in `deps.searchDiscovery`, two MCP description sentences | 2, 3 |
| Docs | tools.md relevance paragraphs for `search_memory` and `search_discovery`; memory-record.md `Relevance` row | 3 |
| Tests | `TestSearchDiscoveryRerankedFallbackIsShippedOrder`, `TestSearchDiscoveryRerankedSortsAndStamps`, `TestSearchDiscoveryRerankedOwnerIsolation`, `TestSearchDiscoveryRerankedRejectsZeroK`, `TestSearchDiscoveryDefaultPathUnchanged`, `TestSearchDiscoveryRelevanceBothLanes` | 1, 2 |

## Flagged assumptions (spec-less probe)

- See the two FLAGGED ASSUMPTION truths above; both are planner discretion under D-07.

## Executor notes (every task)

- Execution is sequential on the main tree. NEVER run any git stash subcommand. Commit each task with an explicit pathspec, Conventional Commits, and end the message with the session's attribution trailer lines.
- New Go files carry the Apache-2.0 SPDX header; docs-site files never do.
- Do not modify `Store.SearchDiscovery`'s body; the reranked method delegates to it.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Store.SearchDiscoveryReranked — over-fetch through the discovery filter, hook on vector order, shipped-order fallback</name>
  <files>internal/store/store.go, internal/store/discovery_rerank_test.go</files>
  <read_first>
    - `internal/store/store.go` lines 1320-1430 (SearchReranked, SearchDiscovery)
    - `internal/store/rerank.go` (post-04-01: applyRankHook, applyRelevance, RankWithHook)
    - `internal/store/store_test.go` lines 374-540 (discovery fixtures, owner isolation, superseded hidden)
  </read_first>
  <behavior>
    - `TestSearchDiscoveryRerankedFallbackIsShippedOrder`: six discoveries in one scope with distinct vectors; for k=3, a nil hook and a hook returning an error each yield exactly the ids and order of `SearchDiscovery(..., k=3)`, with no Relevance.
    - `TestSearchDiscoveryRerankedSortsAndStamps`: a capturing hook is handed all six discoveries (the pool, not k) in `SearchDiscovery`'s vector order; it scores the lowest-vector discovery 0.95; that discovery is first of the three returned and every returned hit carries its value.
    - `TestSearchDiscoveryRerankedOwnerIsolation`: owner B's private discovery in the same scope is never handed to A's hook (even though the hook would score it 1.0) and never returned; B's shared discovery is.
    - `TestSearchDiscoveryRerankedRejectsZeroK` (hermetic): k == 0 returns an error satisfying `errors.Is(err, ErrInvalidArgument)`.
  </behavior>
  <action>
Implements D-07's store half. Write the tests first (they fail to compile). Add `SearchDiscoveryReranked(ctx, scope, kind string, subj Subject, query string, vec []float32, k uint64, hook RankHook) ([]Memory, error)` directly after `SearchDiscovery`: reject k == 0 with ErrInvalidArgument (mirroring SearchReranked's message), apply the same `rejectOverMaximum("k", k)` backstop, call `s.SearchDiscovery(ctx, scope, kind, subj, vec, CandidateK(k))`, pass the result to `applyRankHook(ctx, query, hits, hook)`, and truncate to k. Doc comment: the opt-in Jev path for discoveries; the base order is discovery's shipped vector order (no lexical step), so a hook failure returns exactly what SearchDiscovery would at k; all filtering is SearchDiscovery's own, so reranking never widens visibility; the server calls it only when a rank hook is configured.

Commit as `feat(store): add opt-in reranked discovery search (D-07)` with pathspec `internal/store/store.go internal/store/discovery_rerank_test.go`.
  </action>
  <verify>
    <automated>out=$(go test ./internal/store/ -run '^(TestSearchDiscoveryRerankedFallbackIsShippedOrder|TestSearchDiscoveryRerankedSortsAndStamps|TestSearchDiscoveryRerankedOwnerIsolation|TestSearchDiscoveryRerankedRejectsZeroK|TestSearchDiscoveryFilters|TestSearchDiscoveryOwnerIsolation)$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e '^--- PASS: (TestSearchDiscoveryReranked[A-Za-z]+|TestSearchDiscoveryFilters|TestSearchDiscoveryOwnerIsolation) ' | wc -l)" -eq 6</automated>
    <fails_when>non-zero exit, or fewer than six PASS lines (a Qdrant SKIP counts as missing)</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -o -e 'applyRankHook[(]ctx, query, hits, hook[)]' internal/store/store.go | wc -l` prints `1`.
    - `test "$(go list -deps ./internal/store/ | rg -o -x -e 'github.com/seanb4t/engram/internal/(decide|verdict|relevance|server|embed)' | wc -l)" -eq 0` exits 0.
    - `golangci-lint run ./internal/store/...` reports no findings.
  </acceptance_criteria>
  <done>Reranked discovery search exists, falls back to discovery's shipped order, reranks the whole pool when scored, and keeps SearchDiscovery's visibility; committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: deps.searchDiscovery branches on the hook — default path unchanged, relevance on both discovery lanes</name>
  <files>internal/server/store_iface.go, internal/server/fakestore_test.go, internal/server/tools.go, internal/server/discovery_rerank_test.go</files>
  <read_first>
    - `internal/server/store_iface.go` (whole file)
    - `internal/server/fakestore_test.go` lines 30-60 and 470-540 (spyStore, record, SearchReranked/SearchDiscovery fakes)
    - `internal/server/tools.go` lines 2040-2090 (searchDiscovery) and 2955-2968 (MCP search_discovery closure)
    - `internal/server/connectapi.go` lines 398-440 (SearchDiscoveries)
  </read_first>
  <behavior>
    - `TestSearchDiscoveryDefaultPathUnchanged` (spyStore, no Qdrant): with `d.rankHook` nil, MCP-lane `d.searchDiscovery` (k 0, defaulted to 8) and Connect `SearchDiscoveries` (k 0, defaulted to 20) record exactly one `SearchDiscovery` call each with k 8 and 20 respectively, and zero `SearchDiscoveryReranked` calls.
    - `TestSearchDiscoveryRelevanceBothLanes` (real Qdrant via testDeps with a scripted hook on `d.rankHook`): subtest `both lanes carry the same relevance` — MCP `d.searchDiscovery` and Connect `SearchDiscoveries` return the same ids in the same order, each with the scripted relevance (store.Memory `Relevance` and proto `GetRelevance()` equal and non-nil); subtest `hook error keeps vector order` — both lanes return the no-hook order with no relevance and no error; subtest `another owner's private discovery never appears` — seeded under actor-B, scored 1.0 by the hook, absent from both lanes.
  </behavior>
  <action>
Implements D-07's server half. Write the tests first. Add `SearchDiscoveryReranked` to `memStore` (same signature as the store method, placed beside `SearchDiscovery`); the existing compile-time assertion then covers it. Add `spyStore.SearchDiscoveryReranked`, recording `"SearchDiscoveryReranked"` and otherwise mirroring `spyStore.SearchDiscovery`'s filtering, truncated to k. In `deps.searchDiscovery`, replace the final return with a branch: when `d.rankHook` is nil, return `d.st.SearchDiscovery(ctx, scope, a.Kind, c.Subj, vec, a.K)` exactly as today; otherwise return `d.st.SearchDiscoveryReranked(ctx, scope, a.Kind, c.Subj, a.Query, vec, a.K, d.rankHook)`. Add a short comment on the branch naming the CONTEXT boundary (lexical/vector default untouched; the reranked path runs only when the operator enabled the Jev ranker). New `internal/server/discovery_rerank_test.go` (SPDX) holds both tests; seed discoveries with the store directly (Category `discovery`, a Kind, one citation) and call Connect through `&engramAPI{d: d}`.

Commit as `feat(server): rerank search_discovery by Jev relevance when enabled (D-07)` with pathspec `internal/server/store_iface.go internal/server/fakestore_test.go internal/server/tools.go internal/server/discovery_rerank_test.go`.
  </action>
  <verify>
    <automated>out=$(go test ./internal/server/ -run '^(TestSearchDiscoveryDefaultPathUnchanged|TestSearchDiscoveryRelevanceBothLanes)$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e '^--- PASS: (TestSearchDiscoveryDefaultPathUnchanged|TestSearchDiscoveryRelevanceBothLanes) ' | wc -l)" -eq 2 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e '^ +--- PASS: TestSearchDiscoveryRelevanceBothLanes/' | wc -l)" -eq 3</automated>
    <fails_when>non-zero exit, either parent test not passing (a Qdrant SKIP counts), or fewer than three passing lane subtests</fails_when>
    <automated>go test ./internal/server/ -run 'Discover' -count=1</automated>
    <fails_when>non-zero exit (an existing discovery test regressed after the interface change)</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -o -e 'd[.]st[.]SearchDiscovery[(]ctx, scope, a[.]Kind, c[.]Subj, vec, a[.]K[)]' internal/server/tools.go | wc -l` prints `1` (the default call is unchanged).
    - `rg -o -e 'd[.]st[.]SearchDiscoveryReranked[(]' internal/server/tools.go | wc -l` prints `1`.
    - `golangci-lint run ./internal/server/...` reports no findings; `task license:check` exits 0.
  </acceptance_criteria>
  <done>Discovery search is unchanged without the hook and carries relevance on both lanes with it, with fallback and owner isolation proven; committed.</done>
</task>

<task type="auto">
  <name>Task 3: Describe relevance on the MCP tools and in the reference docs</name>
  <files>internal/server/tools.go, docs-site/src/content/docs/reference/tools.md, docs-site/src/content/docs/reference/memory-record.md</files>
  <read_first>
    - `internal/server/tools.go` line ~2786 (search_memory description) and ~2958 (search_discovery description)
    - `docs-site/src/content/docs/reference/tools.md` lines 133-172 and 446-462
    - `docs-site/src/content/docs/reference/memory-record.md` lines 15-45 (field table)
  </read_first>
  <action>
Documents D-05/D-06 for agents and operators. Append one sentence to each MCP description string: for `search_memory`, after the `score` sentence — "When the operator enables Jev reranking, results are reordered by the provider's probability that each record answers the query, and each result carries `relevance` (0 to 1; values all near zero mean nothing returned answers the query); it is absent when reranking is off or fell back." For `search_discovery`, the same sentence with "discovery" in place of "record". Both sentences must contain the fragment "carries `relevance` (0 to 1". Keep every existing clause and the `scopeRule.Sentence` composition unchanged. In `reference/tools.md`, add a paragraph after `search_memory`'s score paragraph and after `search_discovery`'s `Results carry` line: the opt-in (`ENGRAM_SEARCH_RANKER=jev`, linking the configure guide's Search reranking section), the reorder (lexical order first, then a stable sort by relevance; discoveries keep vector order as their base), the per-hit `relevance` meaning and absence rules, the no-flag rule (callers decide from per-hit values; no hit is filtered), and the fallback (any failure keeps the default order, the call still succeeds). In `memory-record.md`'s field table add a row directly after the Score row, modeled on it cell for cell: field `Relevance`, key `relevance` in backticks like every other key, type `number (optional)`, source `server (query-time)`, and a description that opens with the bold "Not a stored payload key." and says it is the Jev reranker's per-hit probability on `search_memory`/`search_discovery`, present only when reranking ran and succeeded.

Commit as `docs(search): document the per-hit relevance signal (D-05, D-06)` with pathspec `internal/server/tools.go docs-site/src/content/docs/reference/tools.md docs-site/src/content/docs/reference/memory-record.md`.
  </action>
  <verify>
    <automated>go test ./internal/server/ -run 'TestRegisterToolsEnumerable|TestSurfaceConformanceServerSide|TestToolAnnotationsBothDirections|TestSupersedeDocsMatchShippedContract|TestRecallMaximumIsStatedNumerically|TestErrorsDocHintCodesMatchArgErrorConstants' -count=1 &amp;&amp; go test ./internal/surfaces/ -count=1</automated>
    <fails_when>non-zero exit (a description or docs conformance gate rejected the edit)</fails_when>
    <automated>test "$(rg -o -F 'relevance' docs-site/src/content/docs/reference/tools.md | wc -l)" -ge 4 &amp;&amp; test "$(rg -o -F '| Relevance | `relevance` |' docs-site/src/content/docs/reference/memory-record.md | wc -l)" -eq 1 &amp;&amp; task lint:markdown</automated>
    <fails_when>fewer than four relevance mentions in tools.md, the memory-record row missing, or a markdown lint finding</fails_when>
  </verify>
  <acceptance_criteria>
    - `test "$(rg -o -e 'carries .relevance. [(]0 to 1' internal/server/tools.go | wc -l)" -ge 2` exits 0 (both descriptions explain the field).
    - `d1=$(git diff HEAD -- proto/ docs-site/ skill/ gen/ ui/src/lib/gen/ cmd/engram/testdata/) &amp;&amp; task surfaces:gen &amp;&amp; d2=$(git diff HEAD -- proto/ docs-site/ skill/ gen/ ui/src/lib/gen/ cmd/engram/testdata/) &amp;&amp; test "$d1" = "$d2"` exits 0 (regenerating the anchored surfaces changes nothing).
  </acceptance_criteria>
  <done>Agents see what `relevance` means in both tool descriptions; the reference docs describe the field, its opt-in, its absence rules and the fallback; committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engram → decisions provider | discovery content egresses on each discovery search when the ranker is jev |
| caller → Store.SearchDiscovery | owner-or-shared filter is the only visibility gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Information disclosure | discovery reranked path | high | mitigate | runs only when `d.rankHook` is set (ranker jev, plan 04-03); the default path is proven to never call it (TestSearchDiscoveryDefaultPathUnchanged); state bounded by plan 04-01's budget |
| T-04-02 | Elevation of privilege / Information disclosure | `SearchDiscoveryReranked` | high | mitigate | delegates all filtering to `SearchDiscovery`; the hook sees only its result; owner-isolation tests on the store and both lanes |
| T-04-04 | Denial of service | discovery search latency | medium | mitigate | the same one-attempt, timeout-bounded client and lexical/vector fallback as search_memory |
</threat_model>

<verification>
- `go test ./internal/store/ ./internal/server/ -run 'Discovery|Discover' -count=1` exits 0.
- `task lint:markdown` clean; `task surfaces:gen` leaves no diff.
</verification>

<success_criteria>
- D-07: `search_discovery` is reranked and carries relevance when the ranker is jev, on MCP and Connect, and is untouched otherwise.
- RANK-04: the relevance field is documented on every tool surface that can carry it.
</success_criteria>

<output>
Create `.planning/phases/04-jev-reranker-per-hit-relevance-signal/04-05-SUMMARY.md` when done. Record the RED evidence for Tasks 1 and 2.
</output>
