---
phase: 04-jev-reranker-per-hit-relevance-signal
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - internal/relevance/relevance.go
  - internal/relevance/relevance_test.go
autonomous: true
requirements:
  - RANK-05
  - RANK-03

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Budget boundary (spec-less edge RANK-05 boundary): with the estimate E of a default-budget request, a Budget whose TokenBudget equals E returns the identical request (no shrink); TokenBudget E-1 returns a request whose estimate is at most E-1 with a smaller per-candidate length; per exactly equal to MinCandidateChars is accepted, and a budget that would need less returns ErrStateBudget and no request"
    - "At the recall maximum (CandidateK(store.MaxRecallLimit) = 100 candidates), every content shape fits: 100 records of 64 KiB ASCII keep 600 code points each; 100 records of 64 KiB three-byte (CJK) or four-byte (emoji) text shrink to a uniform per-candidate length at or above 100 code points; a 64 KiB query is capped at 2000 code points; EstimateTokens stays at or under 28000 in every case (RANK-05, D-08)"
    - "Precision (spec-less edge RANK-05 precision): EstimateTokens is ceil(UTF-8 bytes / 4) in integer arithmetic — 0 bytes is 0, 1 to 4 bytes is 1, 5 bytes is 2; the shrink step uses int64 floor division and strictly decreases per; provider probabilities are carried into the map verbatim, never rounded, clamped or renormalized"
    - "Empty and single input (spec-less edge RANK-05 empty): zero hits returns ErrNoCandidates, and Hook makes no Decide call; one hit builds exactly one noul question `c00` and the state keys `query` and `c00`"
    - "Adjacency (spec-less edge RANK-05 adjacency): two hits with identical summary and content get two separate keys and two separate questions — nothing is merged or deduplicated"
    - "Ordering (spec-less edge RANK-05 ordering): candidate key i always names hits[i] (the lexical-rank position), and the map FromResponse returns is keyed by hit id, so key order never reorders results"
    - "A 100-question request passes decide.Request.Validate (no question-count cap exists) and uses spike 004's wording verbatim for every question"
    - "FromResponse rejects, as a *decide.Error of kind ErrDecisionMalformedResponse naming the question, a missing answer, a non-noul answer, NaN, +Inf, -Inf, a value below 0 and a value above 1; exact 0 and exact 1 are accepted; answers for unknown keys are ignored"
    - "Hook calls Decide exactly once per invocation (never DecideMany), with one question per hit; on any failure it returns (nil, err) and logs one Warn line whose only attribute is the class word — a sentinel planted in the query and in candidate content never appears in the log output or in err.Error()"
    - "Hook(nil, b) returns a nil RankHook, so an unconfigured decider reaches the store as the default path"
  artifacts:
    - path: "internal/relevance/relevance_test.go"
      provides: "the RANK-05 budget suite and the response/hook edge suite"
      contains: "func TestNewRequestBudgetBoundary("
    - path: "internal/relevance/relevance.go"
      provides: "range- and finiteness-checked FromResponse"
      contains: "math.IsNaN"
  key_links:
    - from: "internal/relevance/relevance_test.go"
      to: "internal/store/rerank.go"
      via: "the recall-maximum case sizes its pool from the store's own over-fetch bound"
      pattern: "store[.]CandidateK[(]store[.]MaxRecallLimit[)]"
    - from: "internal/relevance/relevance.go"
      to: "internal/decide/errors.go"
      via: "malformed answers map to the existing named error, not a new class"
      pattern: "ErrDecisionMalformedResponse"
---

<objective>
Pin the RANK-05 state budget and the response mapping of the search-path relevance request, and close the provider-answer validation gap RED-first.

- **Task 1 (TDD):** the D-08 total-state guard's boundary, floor, multi-byte shrink, query cap, integer estimate, empty/single/duplicate inputs, key ordering, and Validate at 100 questions.
- **Task 2 (TDD, RED first):** FromResponse range and finiteness checks (absent from the tracer), plus Hook's single-call, nil-decider, empty-input and content-free failure logging guarantees.

Decisions implemented: D-08 (budget constants and guard, planner-discretion constants recorded), D-04 (one request per search), D-03 (a malformed answer set is a failure that keeps the lexical order).
Output: `internal/relevance/relevance_test.go` and a hardened `FromResponse`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-jev-reranker-per-hit-relevance-signal/04-CONTEXT.md
@.planning/phases/04-jev-reranker-per-hit-relevance-signal/04-01-SUMMARY.md
@internal/relevance/relevance.go
@internal/decide/errors.go
</context>

<interfaces>
From plan 04-01 (read the post-04-01 file; names below are the plan's contract):

- `internal/relevance`: `StateQuery`, `WhenTrue`, `WhenFalse`, `Instructions(key string) string`, `CandidateKey(i int) string`, `DefaultCandidateChars` (600), `MinCandidateChars` (100), `MaxQueryChars` (2000), `DefaultTokenBudget` (28000), `BytesPerToken` (4), `Budget{CandidateChars, MinCandidateChars, QueryChars, TokenBudget int}`, `DefaultBudget()`, `ErrNoCandidates`, `ErrStateBudget`, `EstimateTokens(decide.Request) int`, `NewRequest(query string, hits []store.Memory, b Budget) (decide.Request, error)`, `FromResponse(resp decide.Response, hits []store.Memory) (map[string]float64, error)`, `Hook(dec decide.Decider, b Budget) store.RankHook`.
- `internal/decide`: `Decider{Decide; DecideMany}`, `Request.Validate()`, `Answer{Type, Probability}`, `QuestionNoul`, `QuestionChoice`, `Error{Kind, Question, ...}`, `ErrDecisionMalformedResponse`, `ErrDecisionUnavailable`, `ErrDecisionTimeout`, `ErrDecisionContextTooLarge`, `ErrDecisionRateLimited`, `ErrDecisionAuth`, `Status(err)`.
- `internal/store`: `Memory{ID, Summary, Content, ...}`, `CandidateK`, `MaxRecallLimit` (1000).
- `internal/verdict.State` truncates by code points; `internal/decide/jev/telemetry_test.go` shows the captured-slog sentinel-scan pattern (`TestJevTelemetryCarriesNoContent`).
</interfaces>

## Artifacts this phase produces (plan 04-02's share)

| Kind | Artifact | Task |
|------|----------|------|
| Tests | `TestNewRequestShape`, `TestNewRequestBudgetASCII`, `TestNewRequestBudgetShrinksMultibyte`, `TestNewRequestQueryCap`, `TestNewRequestBudgetBoundary`, `TestNewRequestBudgetFloor`, `TestNewRequestEmptyAndSingle`, `TestNewRequestDuplicateContentStaysSeparate`, `TestEstimateTokensCeil`, `TestFromResponse`, `TestHookCallsDecideOnce`, `TestHookNilDecider`, `TestHookEmptyHitsNoCall`, `TestHookFailureLogIsContentFree` | 1, 2 |
| Package | `FromResponse` finiteness and range validation | 2 |

## Flagged assumptions (spec-less probe)

- RANK-05 boundary, adjacency, empty, ordering and precision are resolved as explicit truths above (verification: explicit).
- The estimate is a heuristic: bytes/4 can undercount tokens for some scripts. The 4000-token headroom below Jev's 32k context plus the provider's `max_tokens_exceeded` (classified `context_too_large`, degrading to lexical order) are the backstops; no test can prove the provider's tokenizer agrees.

## Executor notes (every task)

- Execution is sequential on the main tree. NEVER run any git stash subcommand. Commit each task with an explicit pathspec, Conventional Commits, and end the message with the session's attribution trailer lines.
- New Go files carry the Apache-2.0 SPDX header.
- Fix `relevance.go` when a test fails; never loosen an expectation stated in the truths.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RANK-05 budget suite — boundary, floor, multi-byte shrink, query cap, integer estimate, empty, single, duplicates, ordering</name>
  <files>internal/relevance/relevance_test.go, internal/relevance/relevance.go</files>
  <read_first>
    - `internal/relevance/relevance.go` (post-04-01, whole file)
    - `internal/verdict/verdict.go` lines 70-110 (State truncation semantics)
    - `internal/decide/validate.go` lines 15-60 (Request.Validate)
  </read_first>
  <behavior>
    - `TestNewRequestShape`: for n = 1 and n = 100 hits, state keys are exactly `query` plus `c00`..`c(n-1)`; each `cNN` state equals `verdict.State(hits[i].Summary, hits[i].Content, 600)` for short inputs; each question is noul with `Instructions(key)`, `WhenTrue`, `WhenFalse` verbatim; `req.Validate()` is nil.
    - `TestNewRequestBudgetASCII`: 100 hits of 64 KiB ASCII content with a 512-byte summary and a 2000-character query: every candidate state is exactly 600 code points and `EstimateTokens(req)` is at most 28000.
    - `TestNewRequestBudgetShrinksMultibyte` (table: 3-byte CJK, 4-byte emoji): 100 hits of 64 KiB such content: estimate at most 28000; every candidate state has the same code-point length, below 600 and at least 100.
    - `TestNewRequestQueryCap`: a 64 KiB emoji query yields a `query` state of exactly 2000 code points; valid UTF-8 throughout.
    - `TestNewRequestBudgetBoundary`: E = estimate of the default-budget request over a fixed fixture that needs no shrink; Budget TokenBudget = E returns a request deep-equal to the default one; TokenBudget = E-1 returns a request with estimate at most E-1 and a shorter candidate state.
    - `TestNewRequestBudgetFloor`: a Budget whose TokenBudget only fits candidates at exactly MinCandidateChars is accepted at that length; one token less returns ErrStateBudget.
    - `TestNewRequestEmptyAndSingle`: nil and empty hits return ErrNoCandidates; one hit gives exactly one question `c00`.
    - `TestNewRequestDuplicateContentStaysSeparate`: two identical hits give `c00` and `c01` with equal state text and two questions.
    - `TestEstimateTokensCeil`: requests whose byte totals are 0, 1, 4, 5 and 8 estimate 0, 1, 1, 2 and 2 (build the requests so the byte totals are exact).
    - `TestNewRequestAtRecallMaximum`: the pool size used is `store.CandidateK(store.MaxRecallLimit)` and equals 100.
  </behavior>
  <action>
Pins D-08's guard as implemented in plan 04-01. Write `internal/relevance/relevance_test.go` (SPDX, package `relevance`) with the tests above, generating large fixtures with `strings.Repeat` (never files). Compute expected lengths with `utf8.RuneCountInString`. If a test fails, fix `NewRequest` or `EstimateTokens` so the truths hold (for example a shrink loop that does not strictly decrease, an estimate that uses float division, or a query that bypasses the cap); record any such fix as a deviation. Keep the constants unchanged unless a truth cannot otherwise hold — and then record the new value and why.

Commit as `test(relevance): pin the rerank state budget at the recall maximum (D-08, RANK-05)` with pathspec `internal/relevance/relevance_test.go internal/relevance/relevance.go` (include `relevance.go` only if it changed).
  </action>
  <verify>
    <automated>out=$(go test ./internal/relevance/ -run '^(TestNewRequestShape|TestNewRequestBudgetASCII|TestNewRequestBudgetShrinksMultibyte|TestNewRequestQueryCap|TestNewRequestBudgetBoundary|TestNewRequestBudgetFloor|TestNewRequestEmptyAndSingle|TestNewRequestDuplicateContentStaysSeparate|TestEstimateTokensCeil|TestNewRequestAtRecallMaximum)$' -count=1 -race -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e '^--- PASS: (TestNewRequest[A-Za-z]+|TestEstimateTokensCeil) ' | wc -l)" -eq 10</automated>
    <fails_when>non-zero exit, a race report, or fewer than ten top-level PASS lines</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -o -e 'func Test(NewRequest[A-Za-z]+|EstimateTokensCeil)[(]' internal/relevance/relevance_test.go | wc -l` prints `10`.
    - `task license:check` exits 0 and `golangci-lint run ./internal/relevance/...` reports no findings.
  </acceptance_criteria>
  <done>The budget guard's boundary, floor, shrink, cap, precision, empty, single, duplicate and ordering edges are named passing tests at the recall maximum; committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Provider-answer validation (RED first) and Hook guarantees — one call, nil decider, empty input, content-free failure log</name>
  <files>internal/relevance/relevance_test.go, internal/relevance/relevance.go</files>
  <read_first>
    - `internal/relevance/relevance.go` and `internal/relevance/relevance_test.go` (post-Task-1)
    - `internal/decide/errors.go` (Error, Status, sentinel list)
    - `internal/decide/jev/telemetry_test.go` (TestJevTelemetryCarriesNoContent: slog capture and sentinel scan)
  </read_first>
  <behavior>
    - `TestFromResponse` (RED first for the range rows): complete noul answers map each hit id to its probability verbatim (0.97, 0.4, 0.02, exact 0, exact 1); an extra answer key is ignored; a missing `c01`, a `c01` of type choice, NaN, +Inf, -Inf, -0.01 and 1.01 each return an error for which `errors.Is(err, decide.ErrDecisionMalformedResponse)` holds and `decide.Status(err)` is `malformed_response`, with a nil map.
    - `TestHookCallsDecideOnce`: a counting fake Decider sees exactly one Decide call per Hook invocation carrying len(hits) questions and zero DecideMany calls; the returned map has one entry per hit.
    - `TestHookNilDecider`: `Hook(nil, DefaultBudget())` is nil.
    - `TestHookEmptyHitsNoCall`: zero hits return (nil, an error satisfying `errors.Is(err, ErrNoCandidates)`) with zero Decide calls.
    - `TestHookFailureLogIsContentFree` (table over fake Decide errors wrapping ErrDecisionUnavailable, ErrDecisionTimeout, ErrDecisionContextTooLarge, ErrDecisionRateLimited, ErrDecisionAuth, and a malformed answer set): the hook returns (nil, non-nil err); captured slog output has exactly one Warn record whose `class` attribute equals `decide.Status(err)` for the provider errors and `malformed_response` for the malformed case; a sentinel planted in the query and in every candidate's content appears in neither the log output nor err.Error().
  </behavior>
  <action>
Closes D-03's malformed-answer path. Add the tests above to `internal/relevance/relevance_test.go` FIRST and run them: the NaN, Inf and out-of-range rows of `TestFromResponse` must fail against plan 04-01's FromResponse — record the failure in the SUMMARY. Then extend `FromResponse` so a probability that is NaN, infinite, below 0 or above 1 returns a `*decide.Error` with Kind `decide.ErrDecisionMalformedResponse` and the question name (use `math.IsNaN` and `math.IsInf`; a bare comparison cannot catch NaN — the same trap Phase 3 hit in `config.ParseProbability`). No new error class. Adjust the Hook only if a Hook test fails, keeping the log attribute set to the class word alone. Fake deciders live in the test file.

Commit as `fix(relevance): reject non-finite or out-of-range relevance answers (D-03)` with pathspec `internal/relevance/relevance.go internal/relevance/relevance_test.go`.
  </action>
  <verify>
    <automated>out=$(go test ./internal/relevance/ -count=1 -race -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e '^--- PASS: (TestFromResponse|TestHookCallsDecideOnce|TestHookNilDecider|TestHookEmptyHitsNoCall|TestHookFailureLogIsContentFree) ' | wc -l)" -eq 5 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e '^--- PASS: ' | wc -l)" -ge 15</automated>
    <fails_when>non-zero exit, a race report, fewer than five PASS lines for the Task 2 tests, or fewer than fifteen top-level PASS lines in the package (a Task 1 test lost)</fails_when>
    <automated>go test ./internal/server/ -run '^TestSearchRerankJevTracer$' -count=1</automated>
    <fails_when>non-zero exit (the stricter FromResponse broke the tracer's canned answers)</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -o -e 'math[.]IsNaN|math[.]IsInf' internal/relevance/relevance.go | wc -l` prints at least `2`.
    - `golangci-lint run ./internal/relevance/...` reports no findings.
  </acceptance_criteria>
  <done>Every malformed-answer shape degrades to a named malformed_response failure; Hook's single-call, nil, empty and content-free-log guarantees are named passing tests; RED recorded; committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engram → decisions provider | the budgeted query and candidate states are the only content that leaves |
| provider → engram | noul probabilities are untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-03 | Tampering | `FromResponse` | medium | mitigate | NaN, Inf, out-of-range and missing or mistyped answers become `malformed_response`; the store keeps the lexical order (TestFromResponse, plan 04-01's applyRelevance) |
| T-04-04 | Denial of service | oversized decision state | medium | mitigate | the total-state guard bounds the request at 28000 estimated tokens for up to 100 candidates of any size (TestNewRequestBudget* at the recall maximum); ErrStateBudget and the provider's context_too_large both degrade to lexical order |
| T-04-05 | Information disclosure | Hook failure log | low | mitigate | one Warn record with only the class word; sentinel scan over log output and err.Error() (TestHookFailureLogIsContentFree) |
</threat_model>

<verification>
- `go test ./internal/relevance/ -count=1 -race` exits 0 with every named test passing.
- The tracer (`TestSearchRerankJevTracer`) still passes with the stricter mapping.
</verification>

<success_criteria>
- RANK-05: the decision state for up to 100 candidates (the pool at the recall maximum) stays within the 28000-token estimate for any content shape, by per-candidate truncation.
- RANK-03's malformed-answer fallback is RED-then-GREEN proven.
</success_criteria>

<output>
Create `.planning/phases/04-jev-reranker-per-hit-relevance-signal/04-02-SUMMARY.md` when done. Record the RED evidence for Task 2 and any constant or algorithm change made to satisfy Task 1.
</output>
