---
phase: 01-eval-foundation-lexical-reranker-fix
plan: 05
type: execute
wave: 3
depends_on:
  - 01-04
files_modified:
  - .planning/phases/01-eval-foundation-lexical-reranker-fix/01-EVAL-BASELINE.log
  - .planning/phases/01-eval-foundation-lexical-reranker-fix/01-RANKING-DECISION.md
autonomous: false
requirements:
  - RANK-01
  - RANK-02

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A live `task eval:retrieval` run against real Qdrant and the production embedder is captured verbatim in `01-EVAL-BASELINE.log`. It contains the per-variant table (`| variant |` header, a `vector-only` row, a `lexical` row, and a `jev` row reading `Jev: disabled`), a `D-05 decision: winner=` line, and a `shipped (SearchReranked) matches:` line (RANK-01, D-09, D-11)"
    - "The baseline run shows no harness failure: no `harness:` line appears, `TestRetrievalEval` did not SKIP, and `TestRetrievalEval_AsymmetryDiffer` did not FAIL. Any `D-10 gate FAILED:` line is recorded as the pre-fix RED evidence, never treated as a harness error"
    - "`01-RANKING-DECISION.md` records the table verbatim, the rule's winner exactly as `decideRanking` emitted it, the eligible set, the deciding clause, both D-10 gate statuses on today's shipped ranking, the embedder model and dimension (never a credential), and which plan 01-06 branch applies (D-05, D-09)"
    - "The human checkpoint confirms the measurement or halts it. It can never select a variant other than the rule's winner. After approval the record carries `Approved winner: <name>` and `Evidence comment authorized: yes|no` (D-05, pre-committed rule)"
    - "No credential appears in any committed artifact: the executor asserts that the value of `ENGRAM_OPENAI_API_KEY` does not occur in the log before committing"
    - "FLAGGED ASSUMPTION (spec-less edge probe: RANK-01/RANK-02 returned unclassified/unresolved): the live-run edges assumed here are the gate-2 RED expected before the fix, a D-05 outcome with no eligible variant (routed to escalation), and a harness failure (routed to halt). The checkpoint options cover each one, but the probe did not classify them"
  artifacts:
    - path: ".planning/phases/01-eval-foundation-lexical-reranker-fix/01-EVAL-BASELINE.log"
      provides: "verbatim live eval output before any ranking change (D-09 evidence, the before side)"
      contains: "D-05 decision: winner="
    - path: ".planning/phases/01-eval-foundation-lexical-reranker-fix/01-RANKING-DECISION.md"
      provides: "the recorded, approved D-05 outcome plan 01-06 implements"
      contains: "Approved winner:"
  key_links: []
  prohibitions:
    - statement: "MUST NOT offer, accept or record a shipped ranking other than the one `decideRanking` emitted. The checkpoint confirms or halts the measurement and never re-decides it"
      category: integrity-of-evidence
      status: resolved
      verification: judgment
      reason: "The checkpoint's options are only approve, halt-harness-suspect and escalate-no-winner. Task 3 copies the winner token from the log line. No wired check descriptor by design (spec-less fallback, flagged-unverified)"
---

<objective>
Run the eval live, before any ranking change, and let the pre-committed D-05 rule pick the shipped ranking:

1. Capture `task eval:retrieval` output verbatim. This is D-09's "before" evidence and RANK-01's first real table.
2. Extract the table and `decideRanking`'s verdict into `01-RANKING-DECISION.md`.
3. Stop at a blocking decision checkpoint. The human confirms the numbers are a sound measurement (not a harness artifact), and says whether plan 01-06 may post the evidence comment on #605 after its green re-run.
4. Record the approved outcome.

The rule has already decided. The checkpoint exists to catch a broken measurement and to authorize an external post, never to re-choose (D-05: "decide before seeing numbers").

Purpose: plan 01-06's implementation branch is gated on these numbers and this approval.
Output: `01-EVAL-BASELINE.log` and `01-RANKING-DECISION.md`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-eval-foundation-lexical-reranker-fix/01-CONTEXT.md
@.planning/phases/01-eval-foundation-lexical-reranker-fix/01-04-SUMMARY.md
@.claude/skills/spike-findings-engram/references/recall-rerank.md
</context>

<interfaces>
- `Taskfile.yaml` `eval:retrieval` runs `ENGRAM_RETRIEVAL_EVAL=1 go test ./internal/retrievaleval/ -run TestRetrievalEval -v`.
- Log markers emitted by plan 01-04 are `harness:`, `D-10 gate FAILED:`, `D-10 gate PASS:`, `D-05 decision: winner=<name|none> reason=… eligible=…`, a Markdown table whose header starts `| variant |`, `shipped (SearchReranked) matches: …`, `no-answer …` and `Jev: disabled`.
- The embedder env the eval builds from (`server.StoreAndEmbedderFromEnvNoEnsure` → `config.Load`) is `ENGRAM_OPENAI_BASE_URL`, `ENGRAM_OPENAI_API_KEY`, `ENGRAM_EMBED_MODEL`, `ENGRAM_EMBED_DIM` and optionally `ENGRAM_EMBED_QUERY_INSTRUCTION`/`ENGRAM_EMBED_DOCUMENT_INSTRUCTION`. Spike 004 used `google/gemini-embedding-2` at 3072.
- Expected before the fix: today's shipped ranking is the lexical reranker, so `shipped (SearchReranked) matches:` should name `lexical`, and D-10 gate 2 may FAIL, which is the RED this phase exists to turn GREEN.
</interfaces>

## Artifacts this phase produces (plan 01-05's share)

| Kind | Artifact | Task |
|------|----------|------|
| Evidence log | `01-EVAL-BASELINE.log` (verbatim `task eval:retrieval` output, before) | 1 |
| Decision record | `01-RANKING-DECISION.md` (table, rule verdict, gate status, branch, approval) | 1, 3 |

## Flagged assumptions (spec-less edge probe)

RANK-01 and RANK-02 came back `unclassified` / `unresolved`. The live-run edges are carried as a `FLAGGED ASSUMPTION` truth above.

## Executor notes (every task)

- NEVER run any git stash subcommand. Commit `.planning` artifacts with an explicit pathspec, using Conventional Commits and the session's attribution trailer.
- Never print, log or commit a credential. Do not echo `ENGRAM_OPENAI_API_KEY`. Refer to it only by name.
- A non-zero exit from `task eval:retrieval` is EXPECTED if the only failures are `D-10 gate FAILED:` lines. It is a real failure only if `harness:` lines appear, the eval SKIPs, or the differ test FAILs.

<tasks>

<task type="tracer">
  <name>Task 1: Live baseline end to end: run the eval against real Qdrant and the production embedder, capture it verbatim, and record the rule's verdict</name>
  <precondition>Docker is reachable (`docker info` exits 0) or ENGRAM_QDRANT_TEST_ADDR names a live Qdrant gRPC endpoint, AND ENGRAM_OPENAI_BASE_URL, ENGRAM_OPENAI_API_KEY, ENGRAM_EMBED_MODEL and ENGRAM_EMBED_DIM are exported with the production embedder's values (check that each is non-empty without printing it).</precondition>
  <files>.planning/phases/01-eval-foundation-lexical-reranker-fix/01-EVAL-BASELINE.log, .planning/phases/01-eval-foundation-lexical-reranker-fix/01-RANKING-DECISION.md</files>
  <read_first>
    - `01-04-SUMMARY.md` (the log markers and roster actually shipped)
    - `01-CONTEXT.md` D-05, D-08, D-09, D-10
    - `internal/retrievaleval/rankers.go` `decideRanking` doc comment (the rule as encoded)
  </read_first>
  <action>
Implements D-09 (before evidence) and applies D-05 through the code, never by hand.

(1) Run `task eval:retrieval 2>&1 | tee .planning/phases/01-eval-foundation-lexical-reranker-fix/01-EVAL-BASELINE.log` once. Keep the exit status, but do not treat gate-only failures as task failure (see Executor notes). If the precondition fails at run time (for example an auth error from the gateway), stop and report it as an authentication gate, and do not fabricate numbers.

(2) Credential check before anything is committed: if `ENGRAM_OPENAI_API_KEY` is non-empty, `rg -qF -- "$ENGRAM_OPENAI_API_KEY" <log>` must find nothing. If it matches, delete the log, stop, and report.

(3) Write `01-RANKING-DECISION.md` (no SPDX header) with these sections:
- `## Measurement`: date, git SHA, embedder model and dim (values of `ENGRAM_EMBED_MODEL`/`ENGRAM_EMBED_DIM` only), and whether the instruction vars were set (yes/no only).
- `## Table`: the table copied verbatim from the log.
- `## Rule verdict (D-05, applied by decideRanking)`: the `D-05 decision:` line verbatim, then `Rule winner: <name>` on its own line with the name copied from the log, plus the eligible set and the deciding clause.
- `## Gates on today's shipped ranking`: each D-10 line verbatim (PASS or FAILED).
- `## Shipped matches`: the diagnostic line verbatim. Expect `lexical`. If it does not name `lexical`, say so plainly, because it means the eval's lexical port diverges from the shipped reranker.
- `## Plan 01-06 branch`: exactly one of `vector-only` (D-08: `SearchReranked` keeps its seam with the vector-order rank step, and the lexical code is deleted from `internal/store`), `tuned` (the winning blend or gate function moves into `internal/store` and becomes the rank step), `lexical` (the rank step stays the lexical reranker), or `none` (no eligible variant, so escalate).
- `## No-answer queries (D-12)`: the `no-answer` lines verbatim.

Commit as `docs(01): record live retrieval-eval baseline and D-05 verdict (#605)` with pathspec for both files.
  </action>
  <verify>
    <automated>l=.planning/phases/01-eval-foundation-lexical-reranker-fix/01-EVAL-BASELINE.log; test -s "$l" &amp;&amp; rg -q 'D-05 decision: winner=' "$l" &amp;&amp; rg -q '[|] vector-only [|]' "$l" &amp;&amp; rg -q '[|] lexical [|]' "$l" &amp;&amp; rg -q 'Jev: disabled' "$l" &amp;&amp; rg -q 'shipped [(]SearchReranked[)] matches:' "$l" &amp;&amp; test "$(rg -o 'harness: ' "$l" | wc -l)" -eq 0 &amp;&amp; test "$(rg -o -e '--- SKIP: TestRetrievalEval ' "$l" | wc -l)" -eq 0 &amp;&amp; test "$(rg -o -e '--- FAIL: TestRetrievalEval_AsymmetryDiffer' "$l" | wc -l)" -eq 0</automated>
    <fails_when>the log is empty or missing the decision line, the table rows or the shipped-matches line; OR any `harness:` line, a SKIP of TestRetrievalEval (the gate or env was not live), or a differ FAIL is present</fails_when>
    <automated>d=.planning/phases/01-eval-foundation-lexical-reranker-fix/01-RANKING-DECISION.md; test "$(rg -o '^Rule winner: [a-z0-9.-]+$' "$d" | wc -l)" -eq 1 &amp;&amp; test "$(rg -o '^## (Measurement|Table|Rule verdict|Gates on today|Shipped matches|Plan 01-06 branch|No-answer queries)' "$d" | wc -l)" -eq 7 &amp;&amp; w=$(rg -o '^Rule winner: ([a-z0-9.-]+)$' -r '$1' "$d") &amp;&amp; rg -q "D-05 decision: winner=$w " .planning/phases/01-eval-foundation-lexical-reranker-fix/01-EVAL-BASELINE.log</automated>
    <fails_when>the decision record lacks a single `Rule winner:` line or any required section, or the recorded winner differs from the log's `D-05 decision: winner=` token</fails_when>
  </verify>
  <acceptance_criteria>
    - If `ENGRAM_OPENAI_API_KEY` is set, `rg -cF -- "$ENGRAM_OPENAI_API_KEY" .planning/phases/01-eval-foundation-lexical-reranker-fix/` finds no file.
    - `01-RANKING-DECISION.md`'s `## Plan 01-06 branch` section names exactly one of `vector-only`, `tuned`, `lexical` or `none`, consistent with the rule winner's family.
  </acceptance_criteria>
  <done>The live eval ran end to end on real infrastructure, and its verbatim output and the rule's verdict are committed. No credential leaked, and no harness failure occurred.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Confirm the measurement and authorize the #605 evidence post (the rule has already chosen)</name>
  <decision>Accept `decideRanking`'s winner as recorded in `01-RANKING-DECISION.md` and let plan 01-06 implement it, or stop because the measurement itself is unsound. Separately, decide whether plan 01-06 may post the evidence comment on GitHub #605 after its green re-run.</decision>
  <context>
    D-05 was pre-committed: the variant chosen is whatever the rule emits over these numbers. That choice is not on the table here. What a human can judge that the code cannot is whether the numbers are a real measurement. Warning signs: `shipped (SearchReranked) matches:` not naming `lexical` (the eval's lexical port diverges from what ships), an implausible table (every variant identical, or MRR near 0 everywhere), or an embedder that is not production's.

    Show the user: the `## Table`, `## Rule verdict`, `## Gates on today's shipped ranking` and `## Shipped matches` sections of `01-RANKING-DECISION.md`, and the `## Plan 01-06 branch`.

    Reversibility of what follows: D-08 is `reversible`. The lexical code is recoverable from git, and the `SearchReranked` seam is unchanged. Posting to #605 is an external, public action, so it needs explicit authorization here, per the user's standing rule against unauthorized external posts.
  </context>
  <options>
    <option id="approve-and-post">
      <name>Approve the rule's winner, and let plan 01-06 post the #605 evidence comment after its green re-run</name>
      <pros>Closes #605 with evidence in the same flow (D-09).</pros>
      <cons>Posts publicly without a second look at the final comment text.</cons>
    </option>
    <option id="approve-draft-only">
      <name>Approve the rule's winner, and have plan 01-06 draft the #605 comment without posting it</name>
      <pros>Leaves a final read of the public text to the user.</pros>
      <cons>A manual `gh issue comment` step remains.</cons>
    </option>
    <option id="halt-harness-suspect">
      <name>Halt: the numbers look like a harness or fixture artifact</name>
      <pros>Stops a wrong measurement from choosing what ships.</pros>
      <cons>Needs a harness fix and a re-run of this plan. Describe the suspect signal.</cons>
    </option>
    <option id="escalate-no-winner">
      <name>Escalate: the rule found no eligible variant (branch `none`)</name>
      <pros>An honest outcome that D-05 did not anticipate, returned to the user rather than improvised.</pros>
      <cons>Blocks RANK-02 until the user decides (for example via `/gsd-discuss-phase`).</cons>
    </option>
  </options>
  <resume-signal>Reply with approve-and-post, approve-draft-only, halt-harness-suspect (plus the signal), or escalate-no-winner</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the approved outcome for plan 01-06</name>
  <files>.planning/phases/01-eval-foundation-lexical-reranker-fix/01-RANKING-DECISION.md</files>
  <read_first>
    - `.planning/phases/01-eval-foundation-lexical-reranker-fix/01-RANKING-DECISION.md` (post-Task-1)
    - The user's checkpoint reply
  </read_first>
  <action>
Implements D-05 (the recorded outcome) and D-09.

Append `## Approval` to `01-RANKING-DECISION.md`, with these lines:
- `Checkpoint reply: <option id>`
- `Approved winner: <name>`, copied exactly from `Rule winner:`. Write this line only for the two approve options.
- `Evidence comment authorized: yes` for approve-and-post, or `no` for approve-draft-only.
- The date.

For halt-harness-suspect or escalate-no-winner, write the reply and the user's stated reason, write NO `Approved winner:` line, commit, and stop. Report the plan as blocked in the SUMMARY, because plan 01-06's precondition will then refuse to run.

Commit as `docs(01): record approved D-05 outcome` with an explicit pathspec.
  </action>
  <verify>
    <automated>d=.planning/phases/01-eval-foundation-lexical-reranker-fix/01-RANKING-DECISION.md; test "$(rg -o '^Approved winner: [a-z0-9.-]+$' "$d" | wc -l)" -eq 1 &amp;&amp; test "$(rg -o '^Evidence comment authorized: (yes|no)$' "$d" | wc -l)" -eq 1 &amp;&amp; test "$(rg -o '^Approved winner: [a-z0-9.-]+$' "$d" | sed 's/Approved winner: //')" = "$(rg -o '^Rule winner: [a-z0-9.-]+$' "$d" | sed 's/Rule winner: //')"</automated>
    <fails_when>no single `Approved winner:` line, no authorization line, or the approved winner differs from the rule winner (someone re-decided)</fails_when>
  </verify>
  <acceptance_criteria>
    - `Approved winner:` equals `Rule winner:` byte for byte.
    - The SUMMARY names the approved winner, the 01-06 branch, and the authorization value.
  </acceptance_criteria>
  <done>Plan 01-06 has a single, approved, rule-derived winner and an explicit posting authorization to act on. If the outcome was a halt or escalation, the halt is recorded and the phase stops here.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator credentials → eval process | the gateway key sits in the environment of a process whose output is committed |
| measurement → shipped ranking | live numbers decide production recall order for MCP and Connect |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-12 | Information disclosure | `01-EVAL-BASELINE.log` committed to a public repo | high | mitigate | The eval never logs env values. Task 1 asserts that `ENGRAM_OPENAI_API_KEY`'s value is absent from the log (`rg -F`) before committing, and deletes the log if it matches. The decision record carries only the model name, the dim and yes/no flags |
| T-01-13 | Tampering (decision integrity) | the checkpoint | medium | mitigate | The checkpoint cannot select a variant. Task 3's verify requires `Approved winner` to equal `Rule winner` byte for byte |
| T-01-14 | Spoofing | a non-production embedder producing the numbers | low | mitigate | The model and dim are recorded, and the checkpoint context tells the user to reject a non-production embedder via halt-harness-suspect |
</threat_model>

<verification>
- `01-EVAL-BASELINE.log` and `01-RANKING-DECISION.md` exist and are committed, and the decision record carries `Approved winner:` (or a recorded halt).
</verification>

<success_criteria>
- ROADMAP success criterion 3 is shown live: the eval reports recall@k and MRR for vector-only, lexical and (disabled) Jev, including the independent paraphrase case.
- The D-05 outcome is recorded from the rule's own output and approved without re-deciding it.
</success_criteria>

<output>
Create `.planning/phases/01-eval-foundation-lexical-reranker-fix/01-05-SUMMARY.md` when done. Include the table and the approved winner (D-09).
</output>
