---
phase: 01-eval-foundation-lexical-reranker-fix
plan: 06
type: execute
wave: 4
depends_on:
  - 01-05
files_modified:
  - internal/store/store.go
  - internal/store/rerank.go
  - internal/store/rerank_test.go
  - internal/retrievaleval/comparison_rankers.go
  - internal/retrievaleval/comparison_rankers_test.go
  - internal/retrievaleval/rankers.go
  - internal/server/connectapi_test.go
  - docs-site/src/content/docs/reference/tools.md
  - .planning/phases/01-eval-foundation-lexical-reranker-fix/01-EVAL-SHIPPED.log
  - .planning/phases/01-eval-foundation-lexical-reranker-fix/01-ISSUE-605-EVIDENCE.md
autonomous: true
requirements:
  - RANK-02

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`store.SearchReranked` keeps its exact signature, its authz-filtered `Search` over-fetch at `CandidateK(k)`, and its forced `Full` view. Its final step is `return rankCandidates(query, hits, int(k)), nil`, a single unexported seam in `rerank.go` whose body is the approved D-05 winner from `01-RANKING-DECISION.md`, and the seam Phase 4's Jev reranker plugs into (D-05, D-08)"
    - "Vector-only branch: `rankCandidates` calls `VectorOrder`, and `RerankHits`, `tokenize`, `lexicalOverlap` and their four `RerankHits` tests are deleted from `internal/store`. `TestCandidateK`, `TestSearchRerankedRejectsZeroK` and the `TestVectorOrder*` tests remain (D-08: delete the lexical code, keep the seam)"
    - "Tuned branch: the winning blend or gate function lives in `internal/store` as an exported function with its parameter as a named constant. Its eval-local copy is deleted, and the eval roster points at the store function. Lexical branch: `rankCandidates` calls `RerankHits`, and the eval's lexical row points at `store.RerankHits`. In every branch the eval still reports vector-only, lexical, blend, gate and Jev rows (ROADMAP Phase 1 success criterion 3)"
    - "MCP `deps.searchMemory` and Connect `SearchMemories` still return identical orders through the one seam, and no cross-owner record leaks. `TestRerankParityMCPAndConnect` asserts the order the shipped rank step deterministically produces on its tied-score fixture, and its comment no longer attributes that order to the lexical reranker unless lexical shipped"
    - "A live re-run (`01-EVAL-SHIPPED.log`) exits 0 with both `D-10 gate PASS:` lines, a `D-05 decision: winner=` token equal to `Approved winner:`, and a `shipped (SearchReranked) matches:` line naming that winner. The paraphrase case does not regress versus vector-only, and #261's target stays at rank 1 (RANK-02, ROADMAP success criterion 4)"
    - "`01-ISSUE-605-EVIDENCE.md` holds the before and after tables, the approved winner and the D-05 rationale (D-09). It is posted to #605 with `gh issue comment` only when `01-RANKING-DECISION.md` records `Evidence comment authorized: yes`. Otherwise it stays a draft"
    - "FLAGGED ASSUMPTION (spec-less edge probe: RANK-02 returned unclassified/unresolved): the shipping edges assumed here are a tied-score candidate set (deterministic ID tie-break), k above the candidate count, the re-run's winner differing from the approved one (halt, never re-ship), and posting without authorization (forbidden). The tests and halting rules in this plan cover each one, but the probe did not classify them"
  artifacts:
    - path: "internal/store/rerank.go"
      provides: "rankCandidates, the single shipped rank step, plus CandidateK and VectorOrder"
      contains: "func rankCandidates("
    - path: "internal/store/rerank_test.go"
      provides: "TestRankCandidatesIsTheD05Winner"
      contains: "func TestRankCandidatesIsTheD05Winner("
    - path: ".planning/phases/01-eval-foundation-lexical-reranker-fix/01-EVAL-SHIPPED.log"
      provides: "verbatim live eval output after the change (D-09 evidence, the after side)"
      contains: "D-10 gate PASS:"
    - path: ".planning/phases/01-eval-foundation-lexical-reranker-fix/01-ISSUE-605-EVIDENCE.md"
      provides: "the #605 evidence comment body"
  key_links:
    - from: "internal/store/store.go"
      to: "internal/store/rerank.go"
      via: "SearchReranked's only rank step is the D-05 winner seam (D-08)"
      pattern: "rankCandidates[(]query, hits, (k|len[(]hits[)])[)]"
    - from: "internal/store/rerank_test.go"
      to: "internal/store/rerank.go"
      via: "a test pins the seam to the approved winner"
      pattern: "rankCandidates[(]"
  prohibitions:
    - statement: "MUST NOT post to GitHub (#605 or anywhere else) unless 01-RANKING-DECISION.md records `Evidence comment authorized: yes` from the user's own checkpoint reply"
      category: consent
      status: resolved
      verification: judgment
      reason: "Task 3 greps the authorization line before any gh call and otherwise leaves a draft. No wired check descriptor by design (spec-less fallback, flagged-unverified)"
    - statement: "MUST NOT ship a ranking other than the approved winner, and MUST NOT re-tune it to make a gate pass. If the re-run disagrees with the approved winner, halt and report, never re-decide"
      category: integrity-of-evidence
      status: resolved
      verification: test
      reason: "TestRankCandidatesIsTheD05Winner plus Task 3's verify comparing the re-run decision token against Approved winner. No wired check descriptor by design (spec-less fallback, flagged-unverified)"
---

<objective>
Ship the approved D-05 winner as the single ranking every recall surface uses, and close #605 with evidence:

- `SearchReranked` keeps its seam and its fetch shape. Its last step becomes `rankCandidates`, whose body is the approved winner. Under D-08 (vector-only wins), the lexical code is deleted from `internal/store`.
- The eval roster keeps every D-06 row by pointing at whichever copy survives, so ROADMAP success criterion 3 keeps holding.
- The MCP/Connect parity test and the docs describe the shipped ranking truthfully.
- A live re-run proves RANK-02: no paraphrase regression versus vector-only, and #261 at rank 1. The before and after evidence is recorded, and it is posted to #605 only if the user authorized it at plan 01-05's checkpoint (D-09).

Design choice (Claude's discretion, recorded here): the `CandidateK` over-fetch and forced `Full` view stay unchanged in every branch. D-08 says "keep `store.SearchReranked` as the single shared seam", and Phase 4's Jev reranker (RANK-03, RANK-05) needs that candidate pool and its content. Today's fetch cost is therefore not regressed, and it is not optimized in this phase.

Output: the rank step shipped in the store, the adjusted eval roster and tests, the docs, and the two evidence artifacts.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-eval-foundation-lexical-reranker-fix/01-CONTEXT.md
@.planning/phases/01-eval-foundation-lexical-reranker-fix/01-RESEARCH.md
@.planning/phases/01-eval-foundation-lexical-reranker-fix/01-RANKING-DECISION.md
@.planning/phases/01-eval-foundation-lexical-reranker-fix/01-05-SUMMARY.md
@internal/store/rerank.go
</context>

<interfaces>
- `internal/store/store.go` `SearchReranked` (after plan 01-02) has a k==0 guard, then `opts.Full = true`, then `hits, err := s.Search(ctx, scope, subj, vec, CandidateK(k), opts)`, then `return RerankHits(query, hits, int(k)), nil`. Its doc comment (about 1304-1321) names "the pure RerankHits lexical-overlap reorder" and must be rewritten in every branch.
- Callers are unchanged: `internal/server/tools.go` `deps.searchMemory` (about 1889) and `internal/server/connectapi.go` `SearchMemories`, via `internal/server/store_iface.go:43`. Neither caller is edited.
- Store tests that use `RerankHits` directly, in `internal/store/rerank_test.go`: `TestRerankHitsPromotesLexicalOverlap`, `TestRerankHitsDeterministic`, `TestRerankHitsTruncatesToK` and `TestRerankHitsIgnoresAccessCount`. Membership and delegation tests that do NOT care which ranker runs, and so need no edit: `TestSearchRerankedMatchesSearchMembership`, the `schemaversion_recallgate_test.go` delegation assertions, `schemaversion_compat_test.go`, and `searchtwophase_oversized_test.go`.
- `internal/server/connectapi_test.go` `TestRerankParityMCPAndConnect` (about 350-515) seeds four records with a FIXED vector, so every raw score ties. Its subtest `identical_reranked_order` asserts `got[0] == recTHigh.ID` ("high-lexical-overlap record ranked first"), and its header comment says the order "is entirely RerankHits's lexical-overlap logic". recTHigh's ID `e3333333-0000-0000-0000-000000000001` is the lowest of the four, so under `VectorOrder`'s ID-ascending tie-break it still sorts first, but for a different reason that the test must state.
- `docs-site/src/content/docs/reference/tools.md` line 143 ("ranked by vector similarity and reranking") and lines 153-154 ("Final order may include reranking; `score` remains first-stage dense similarity and may be non-monotonic after rerank"). This is hand-authored prose with no generator anchor.
- Eval (plan 01-04): `evalRankers()` in `internal/retrievaleval/rankers.go`. The eval-local copies `lexicalRerank`/`cosineBlendRerank`/`overlapGateRerank`/`tokenize`/`lexicalOverlap`/`normalizedOverlap` live in `comparison_rankers.go`.
- `01-RANKING-DECISION.md` lines: `Approved winner: <name>` and `Evidence comment authorized: yes|no`.
</interfaces>

## Artifacts this phase produces (plan 01-06's share)

| Kind | Artifact | Task |
|------|----------|------|
| Func | `rankCandidates(query string, hits []Memory, k int) []Memory` (`internal/store/rerank.go`, unexported; the parameter is named `_` if the winner ignores the query) | 1 |
| Test | `TestRankCandidatesIsTheD05Winner` (`internal/store/rerank_test.go`) | 1 |
| Tuned branch only | exported `CosineBlendRerank` + `const shippedBlendAlpha`, or `OverlapGateRerank` + `const shippedGateTheta`, in `internal/store/rerank.go`, with its tests ported into `rerank_test.go` | 1 |
| Removed (vector-only branch) | `RerankHits`, `tokenize`, `lexicalOverlap` and their four tests, from `internal/store` | 1 |
| Evidence | `01-EVAL-SHIPPED.log`, `01-ISSUE-605-EVIDENCE.md` | 3 |

## Flagged assumptions (spec-less edge probe)

RANK-02 came back `unclassified` / `unresolved`. The shipping edges are carried as a `FLAGGED ASSUMPTION` truth above.

## Executor notes (every task)

- Read `Approved winner:` from `01-RANKING-DECISION.md` FIRST, and follow exactly one branch below: **vector-only**, **tuned** (a name starting `cosine-blend-` or `overlap-gate-`), or **lexical**. Record the branch in the SUMMARY.
- NEVER run any git stash subcommand. Commit with an explicit pathspec, using Conventional Commits, and end the message with the session's attribution trailer lines. Record any use of the signer fallback.
- Real-Qdrant tests run with `ENGRAM_REQUIRE_QDRANT=1`.
- `SearchReranked`'s signature, its k==0 guard, `opts.Full = true` and the `CandidateK(k)` over-fetch are NOT changed in any branch.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: The approved winner ships end to end through SearchReranked's single rank step</name>
  <precondition>`.planning/phases/01-eval-foundation-lexical-reranker-fix/01-RANKING-DECISION.md` contains exactly one `Approved winner: <name>` line naming a roster entry from `evalRankers()` (plan 01-05 approved, not halted or escalated).</precondition>
  <reversibility rating="reversible">D-08 as recorded in CONTEXT.md: the deleted lexical code is recoverable from git and the SearchReranked seam and signature are unchanged, so any branch is undone by one revert.</reversibility>
  <files>internal/store/store.go, internal/store/rerank.go, internal/store/rerank_test.go</files>
  <read_first>
    - `01-RANKING-DECISION.md` (the `Approved winner`, `## Plan 01-06 branch` and `## Table` sections)
    - `internal/store/rerank.go` and `internal/store/rerank_test.go` (whole files), and `internal/store/store.go` lines 1300-1340
    - `internal/retrievaleval/comparison_rankers.go` and its test (the source of a tuned winner's code and tests)
    - `01-CONTEXT.md` D-05, D-08
  </read_first>
  <behavior>
    - `TestRankCandidatesIsTheD05Winner` (hermetic, `t.Parallel()`): over a table of hit sets (mixed scores, tied scores, a #261-shaped set where a lower-score hit has full lexical overlap, k below, equal to and above len), `rankCandidates(query, hits, k)` equals the approved winner's function output exactly. In the vector-only branch it also asserts the #261-shaped set stays in score order, which proves no lexical signal is on the shipped path. The doc comment names D-05, D-08, #605 and `01-RANKING-DECISION.md`.
    - `TestSearchRerankedRejectsZeroK`, `TestCandidateK` and the `TestVectorOrder*` tests stay green.
    - RED first: write the test against the winner while `SearchReranked` still calls `RerankHits`, with `rankCandidates` stubbed to `RerankHits`. It fails unless lexical is the winner. Record the failing lines in the SUMMARY. In the lexical branch, state that no RED is possible because the behavior is unchanged.
  </behavior>
  <action>
Implements D-05 (ship the rule's winner) and D-08.

(1) `internal/store/rerank.go`: add `func rankCandidates(query string, hits []Memory, k int) []Memory`, unexported. Doc comment: this is the shipped rank step `SearchReranked` applies to its already authz-filtered candidate pool. It was chosen by the pre-committed D-05 rule on the 2026-09-22.01 Phase 1 retrieval eval (#605, `01-RANKING-DECISION.md`), and it is the single place Phase 4's Jev reranker (RANK-03) plugs in. Its body per branch:
- **vector-only**: `return VectorOrder(hits, k)`. Name the unused query parameter `_`. DELETE `RerankHits`, `tokenize` and `lexicalOverlap`, and drop `strings` if it is now unused.
- **tuned**: add the exported winner, `CosineBlendRerank(query string, hits []Memory, k int, alpha float64) []Memory` or `OverlapGateRerank(…, theta float64)`, ported verbatim from the eval-local copy, together with the helpers it needs (`tokenize`, `lexicalOverlap`, `normalizedOverlap` over `Memory`). Add a named constant (`shippedBlendAlpha` or `shippedGateTheta`) equal to the approved grid value, with a doc comment citing D-05/D-07 and the eval row name. `rankCandidates` calls the winner with that constant. DELETE `RerankHits`.
- **lexical**: `return RerankHits(query, hits, k)`. Nothing is deleted. Rewrite `RerankHits`' doc comment to say D-05 retained it.

(2) `internal/store/store.go`: replace `SearchReranked`'s final return with exactly `return rankCandidates(query, hits, int(k)), nil`. Rewrite its doc comment and the body comment above `opts.Full = true` so they describe the approved rank step generically, without naming the lexical mechanism unless lexical shipped. The comments must state: over-fetch `CandidateK(k)` via the owner/scope-filtered `Search`, then the D-05-selected rank step via `rankCandidates`, then truncate to k. The forced `Full` view and the over-fetch are the rank step's input contract, retained for Phase 4's content-reading Jev reranker (D-08). The comments keep the existing k==0 and no-embed-import rationale.

(3) `internal/store/rerank_test.go`: add `TestRankCandidatesIsTheD05Winner` per `<behavior>`. In the vector-only and tuned branches, delete the four `RerankHits` tests. The AccessCount invariant stays covered by `TestVectorOrderIgnoresAccessCount`, plus, in the tuned branch, a ported AccessCount test for the winner. In the tuned branch, also port the winner's property tests from `comparison_rankers_test.go` into this file.

Commit as `feat(store): ship the D-05 winner as SearchReranked's rank step (#605)` with pathspec `internal/store/store.go internal/store/rerank.go internal/store/rerank_test.go`.
  </action>
  <verify>
    <automated>out=$(go test ./internal/store/ -run '^(TestRankCandidatesIsTheD05Winner|TestSearchRerankedRejectsZeroK|TestCandidateK|TestVectorOrderScoreThenIDOrder|TestVectorOrderTruncatesAndCopies|TestVectorOrderIgnoresAccessCount)$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e '^--- PASS: ' | wc -l)" -eq 6</automated>
    <fails_when>non-zero exit or fewer than six top-level `--- PASS:` lines</fails_when>
    <automated>out=$(ENGRAM_REQUIRE_QDRANT=1 go test ./internal/store/ -run '^(TestSearchRerankedMatchesSearchMembership|TestSearchTwoPhaseBounded)$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e '^--- PASS: ' | wc -l)" -eq 2 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e '--- SKIP: ' | wc -l)" -eq 0</automated>
    <fails_when>non-zero exit, fewer than two PASS lines, or any SKIP (the real-Qdrant membership and bounded-read contracts were not exercised)</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -o 'return rankCandidates[(]query, hits, int[(]k[)][)], nil' internal/store/store.go | wc -l` prints `1`, and `rg -o 's[.]Search[(]ctx, scope, subj, vec, CandidateK[(]k[)], opts[)]' internal/store/store.go | wc -l` prints `1` (the seam and the fetch shape are unchanged).
    - Vector-only branch: `test "$(rg -o 'func (RerankHits|tokenize|lexicalOverlap)[(]' internal/store --type go | wc -l)" -eq 0`, and `awk '/^\/\/ SearchReranked /,/^func [(]s [*]Store[)] SearchReranked/' internal/store/store.go | rg -o -i 'lexical' | wc -l` prints `0`.
    - Tuned branch: `rg -o 'const shipped(BlendAlpha|GateTheta) +=' internal/store/rerank.go | wc -l` prints `1`, and its value matches the approved row name's numeric suffix.
    - `go build ./...`, `go vet ./internal/store/` and `golangci-lint run ./internal/store/...` are clean.
  </acceptance_criteria>
  <done>SearchReranked ships the approved winner through the single `rankCandidates` seam, with its signature and fetch shape intact. Under D-08 the lexical code is gone from `internal/store`. The seam is pinned by a test. Committed.</done>
</task>

<task type="auto">
  <name>Task 2: Keep every eval row alive, the MCP/Connect parity test truthful, and the docs accurate for the shipped ranking</name>
  <files>internal/retrievaleval/comparison_rankers.go, internal/retrievaleval/comparison_rankers_test.go, internal/retrievaleval/rankers.go, internal/server/connectapi_test.go, docs-site/src/content/docs/reference/tools.md</files>
  <read_first>
    - `01-RANKING-DECISION.md` (`Approved winner`, branch)
    - `internal/retrievaleval/rankers.go`, `comparison_rankers.go` and `comparison_rankers_test.go`
    - `internal/server/connectapi_test.go` lines 350-515 (`TestRerankParityMCPAndConnect`)
    - `docs-site/src/content/docs/reference/tools.md` lines 138-160
  </read_first>
  <action>
Implements D-08 (eval side), D-11 (roster intact) and D-09 (docs truthful).

(1) Eval roster, by branch:
- **vector-only**: nothing moves. The eval's `lexicalRerank`/blend/gate copies stay as comparison-only code. Rewrite `comparison_rankers.go`'s file doc and `lexicalRerank`'s doc comment to say the lexical-overlap reranker `internal/store` shipped until 2026-09-22.01 Phase 1 retired it (D-05/D-08, #605), and that it is kept here only as the eval's comparison baseline (RANK-01). Remove any sentence claiming it matches a function `SearchReranked` ships. Remove every mention of the deleted function's Go name from `internal/retrievaleval` (doc comments in `comparison_rankers.go` and `comparison_rankers_test.go` that plan 01-02 wrote as "port of store.RerankHits" or "mirrors TestRerankHits…"), and describe it as "the retired lexical-overlap reranker" instead.
- **tuned**: delete the eval-local copy of the winning function and its tests from the `comparison_rankers*` files, since they now live in `internal/store`. Point that family's roster rows in `rankers.go` at the exported `store.CosineBlendRerank`/`store.OverlapGateRerank`. Keep the other variants' copies and the helpers they need.
- **lexical**: delete `lexicalRerank` and its test, point the `lexical` roster row at `store.RerankHits`, and keep `tokenize`/`lexicalOverlap`/`normalizedOverlap` for blend and gate.
In every branch, `evalRankers()` still returns the same ten names in the same order.

(2) `internal/server/connectapi_test.go`, `TestRerankParityMCPAndConnect`: rewrite the header comment so the fixed-vector fixture's order is attributed to the shipped rank step. For example: under vector order, every raw score ties, so the order is the deterministic ID-ascending tie-break. Update the `identical_reranked_order` assertion to the order the shipped rank step produces on this fixture. Vector-only: assert that the returned IDs are in ascending order and that `got[0] == recTHigh.ID` because it has the lowest ID, and state that in the message. Tuned or lexical: keep recTHigh-first, with the message naming the shipped step. Keep every parity (`slices.Equal`), `k`, tag, window and no-cross-owner-leak subtest unchanged.

(3) `docs-site/src/content/docs/reference/tools.md`: make lines 143 and 153-154 describe the shipped ranking. Vector-only: results are ordered by `score` (first-stage cosine similarity, descending; ties broken deterministically), so `score` is monotonic in the returned order. Remove "reranking" and "non-monotonic" wording. Tuned or lexical: keep the non-monotonic caveat and name the step generically ("a lexical-overlap adjustment selected by the retrieval eval"). Do not add an SPDX header to docs-site files.

Commit as `test(eval,server): align eval roster, parity test and docs with the shipped ranking (#605)` with pathspec for the five files.
  </action>
  <verify>
    <automated>out=$(ENGRAM_REQUIRE_QDRANT=1 go test ./internal/server/ -run '^TestRerankParityMCPAndConnect$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; printf '%s\n' "$out" | rg -q -e '^--- PASS: TestRerankParityMCPAndConnect' &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e '    --- PASS: TestRerankParityMCPAndConnect/' | wc -l)" -eq 5 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e '--- SKIP: ' | wc -l)" -eq 0</automated>
    <fails_when>non-zero exit, fewer than five subtest PASS lines (a parity subtest dropped), or any SKIP (Qdrant not reached)</fails_when>
    <automated>out=$(env -u ENGRAM_RETRIEVAL_EVAL go test ./internal/retrievaleval/ -count=1 -v 2>&amp;1); rc=$?; test "$rc" -eq 0 &amp;&amp; printf '%s\n' "$out" | rg -q -e '^--- PASS: TestEvalRankersRoster' &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e '^--- FAIL: ' | wc -l)" -eq 0</automated>
    <fails_when>non-zero exit, a FAIL line, or the roster test not passing (a D-06 row lost its implementation)</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -o 'entirely RerankHits' internal/server/connectapi_test.go | wc -l` prints `0` unless the branch is lexical.
    - Vector-only branch: `rg -o -i 'non-monotonic after rerank' docs-site/src/content/docs/reference/tools.md | wc -l` prints `0`, and `test "$(rg -o 'RerankHits' --type go internal | wc -l)" -eq 0`.
    - `task lint` exits 0 (golangci-lint, rumdl on the docs page, license headers).
  </acceptance_criteria>
  <done>The eval still measures all ten roster rows with an implementation behind each. The parity test asserts the real shipped order for a stated reason. The docs describe the ranking that ships. Committed.</done>
</task>

<task type="auto">
  <name>Task 3: Live proof after the change, the D-09 evidence, and the authorized #605 comment</name>
  <precondition>Docker is reachable or ENGRAM_QDRANT_TEST_ADDR names a live Qdrant, AND ENGRAM_OPENAI_BASE_URL, ENGRAM_OPENAI_API_KEY, ENGRAM_EMBED_MODEL and ENGRAM_EMBED_DIM are exported with the same production values plan 01-05 used (compare the model and dim against `01-RANKING-DECISION.md` `## Measurement`).</precondition>
  <files>.planning/phases/01-eval-foundation-lexical-reranker-fix/01-EVAL-SHIPPED.log, .planning/phases/01-eval-foundation-lexical-reranker-fix/01-ISSUE-605-EVIDENCE.md</files>
  <read_first>
    - `01-RANKING-DECISION.md` (the whole file: before table, `Approved winner`, `Evidence comment authorized`)
    - `01-EVAL-BASELINE.log`
    - GitHub #605 body (`gh issue view 605 --json title,body --jq .body | head -60`) for its stated acceptance
  </read_first>
  <action>
Implements D-09 and proves RANK-02 live.

(1) Run `task eval:retrieval 2>&1 | tee .planning/phases/01-eval-foundation-lexical-reranker-fix/01-EVAL-SHIPPED.log`. It must now exit 0. Apply the same credential check as plan 01-05: if `ENGRAM_OPENAI_API_KEY` is set, `rg -qF` for its value must find nothing, otherwise delete the log and stop.

(2) If the log's `D-05 decision: winner=` token differs from `Approved winner:` (embedding noise flipped a close call), do NOT change the shipped ranking and do NOT post. Stop, and report both tables as a blocker in the SUMMARY.

(3) Write `01-ISSUE-605-EVIDENCE.md` (no SPDX). It is a GitHub-comment-ready body in plain Markdown with these parts:
- One sentence naming what shipped and why, citing the pre-committed D-05 rule verbatim from CONTEXT.md.
- The embedder model and dim.
- The before table (from `01-EVAL-BASELINE.log`) and the after table (from `01-EVAL-SHIPPED.log`), both verbatim.
- Both after-run `D-10 gate PASS:` lines.
- The no-answer lines.
- A note that the queries were written blind to targets (D-01), and a pointer to the eval command `task eval:retrieval`.
- No credentials and no hostnames of private gateways.
End it with the AI attribution line the session requires for PR and issue comments.

(4) Post the comment only if `01-RANKING-DECISION.md` contains the exact line `Evidence comment authorized: yes`. In that case run `gh issue comment 605 --body-file .planning/phases/01-eval-foundation-lexical-reranker-fix/01-ISSUE-605-EVIDENCE.md` and record the returned comment URL in the SUMMARY. Otherwise do not call gh, and record "draft only, user to post".

(5) Run the full gate `task` (lint + test). It must exit 0.

Commit as `docs(01): record post-change retrieval eval and #605 evidence (D-09)` with pathspec for both files.
  </action>
  <verify>
    <automated>l=.planning/phases/01-eval-foundation-lexical-reranker-fix/01-EVAL-SHIPPED.log; d=.planning/phases/01-eval-foundation-lexical-reranker-fix/01-RANKING-DECISION.md; w=$(rg -o '^Approved winner: ([a-z0-9.-]+)$' -r '$1' "$d"); test -n "$w" &amp;&amp; rg -q -e '^--- PASS: TestRetrievalEval ' "$l" &amp;&amp; test "$(rg -o 'D-10 gate PASS:' "$l" | wc -l)" -eq 2 &amp;&amp; test "$(rg -o 'D-10 gate FAILED:|harness: ' "$l" | wc -l)" -eq 0 &amp;&amp; rg -q "D-05 decision: winner=$w " "$l" &amp;&amp; rg -q "shipped [(]SearchReranked[)] matches:.*$w" "$l"</automated>
    <fails_when>TestRetrievalEval did not PASS, fewer than two `D-10 gate PASS:` lines, any gate failure or harness line, the re-run winner differs from the approved winner, or the shipped row does not match the approved winner's row</fails_when>
    <automated>task</automated>
    <fails_when>non-zero exit (lint or any test failing anywhere in the repo)</fails_when>
  </verify>
  <acceptance_criteria>
    - `01-ISSUE-605-EVIDENCE.md` contains two `| variant |` table headers (before and after) and the word `blind`.
    - If `Evidence comment authorized: yes`, the SUMMARY carries a `https://github.com/seanb4t/engram/issues/605#issuecomment-` URL. Otherwise the SUMMARY says the comment is a draft and no gh write command was run.
    - If `ENGRAM_OPENAI_API_KEY` is set, no committed file under the phase directory contains its value.
  </acceptance_criteria>
  <done>The shipped ranking is proven live: no paraphrase regression versus vector-only, #261 at rank 1, and the shipped row equal to the approved winner. Before and after evidence is recorded. The #605 comment is posted only with explicit authorization. The full `task` gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authz-filtered `Search` → rank step | the rank step reorders only what the caller is already allowed to see |
| repo → GitHub (#605) | a public comment posted on the user's behalf |
| operator credentials → committed evidence | the live run's environment holds the gateway key |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-15 | Information disclosure | `SearchReranked` ranking change | high | mitigate | The rank step runs strictly after the single owner/scope-filtered `Search`, whose call shape `s.Search(ctx, scope, subj, vec, CandidateK(k), opts)` is asserted unchanged. `TestRerankParityMCPAndConnect/no_cross_owner_leak_through_reranked_path` stays and runs against real Qdrant with `ENGRAM_REQUIRE_QDRANT=1` |
| T-01-16 | Information disclosure | `01-EVAL-SHIPPED.log` and `01-ISSUE-605-EVIDENCE.md` | high | mitigate | An `rg -F` check for the key's value runs before commit and before posting, and the log is deleted if it matches. The evidence body carries the model name and dim only, with no gateway hostnames |
| T-01-17 | Elevation of privilege (acting without consent) | `gh issue comment` | medium | mitigate | Posting requires the literal `Evidence comment authorized: yes` line, which plan 01-05 Task 3 writes only from the user's own checkpoint reply. Otherwise the comment stays a draft |
| T-01-18 | Tampering (decision integrity) | the shipped ranker | medium | mitigate | `TestRankCandidatesIsTheD05Winner` pins the seam. Task 3 halts, never re-ships, if the live winner differs from the approved one |
</threat_model>

<verification>
- `task` exits 0.
- `01-EVAL-SHIPPED.log` shows both D-10 gates PASS and the shipped row matching the approved winner.
- `git grep -n 'rankCandidates' internal/store` shows the definition, the `SearchReranked` call and the test.
</verification>

<success_criteria>
- ROADMAP Phase 1 success criterion 4 holds: on the paraphrase case the shipped ranking does not regress versus vector-only, and #261's target stays at rank 1.
- ROADMAP success criterion 3 still holds after the change: vector-only, lexical, blend, gate and the (disabled) Jev rows are all reported.
- RANK-02 is satisfied. #605 has recorded evidence (D-09), posted only if the user authorized it.
</success_criteria>

<output>
Create `.planning/phases/01-eval-foundation-lexical-reranker-fix/01-06-SUMMARY.md` when done. Include the before and after tables, the shipped winner and its branch, and the #605 comment URL or "draft only".
</output>
