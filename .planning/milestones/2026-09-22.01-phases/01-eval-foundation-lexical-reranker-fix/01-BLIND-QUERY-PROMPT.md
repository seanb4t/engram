Regenerated from the `paraphraseTopics` labels committed alongside this file in `internal/retrievaleval/paraphrase_fixture.go` — labels only, no seed record text, key, or tag is included below (D-01). This is the final, full 24-topic set: 20 answer topics across six domains (tooling, auth, deploy, datamodel, config, testing) plus 4 no-answer topics, interleaved so a topic's position reveals nothing about whether it has an answer.

---8<--- SEND EVERYTHING BELOW THIS LINE VERBATIM AS THE ENTIRE PROMPT ---8<---

Below is a list of topics from a software team's shared notes. You have not seen the notes. For each topic, write ONE question a developer might type into the team's memory-search tool when trying to recall what was decided or noted about it. Write naturally, as someone who half-remembers it, in 6 to 20 words of plain prose, with no quotation marks, backticks or code formatting. Do not use any tools and do not read any files. Answer from this message alone. Reply with exactly one line per topic in the form `- Tnn: <question>`, in the same order, and nothing else.

- T01: the record about what happens to code before it lands
- T02: the record about what order the style tools run in
- T03: the record about where the style checker settings live
- T04: the record about which pipeline step happens first
- T05: the record about how long a login stays authorized
- T06: the record about how automated callers prove who they are
- T07: the record about verifying a token belongs to us
- T08: the record about how the web console keeps you signed in
- T09: the record about how new versions get let out slowly
- T10: the record about undoing a bad release
- T11: the record about what runs before the app comes up
- T12: the record about what a delete actually does to the row
- T13: the record about what shape a thing's unique identifier takes
- T14: the record about how old and new formats coexist
- T15: the record about which prefix every setting name uses
- T16: the record about which config source wins when several are set
- T17: the record about where sensitive values are loaded from
- T18: the record about what backs the database during a test run
- T19: the record about how an unreliable test gets set aside
- T20: the record about how a saved reference catches unintended changes
- T21: the record about the on-call rotation schedule for the week
- T22: the record about which GPU driver revision is pinned for training
- T23: the record about the retention period required for a legal hold
- T24: the record about the push notification quota for the mobile app
