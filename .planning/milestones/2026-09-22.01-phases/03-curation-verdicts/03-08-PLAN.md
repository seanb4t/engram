---
phase: 03-curation-verdicts
plan: 08
type: execute
wave: 5
depends_on:
  - 03-06
  - 03-07
files_modified:
  - .planning/phases/03-curation-verdicts/03-EVAL-RESULTS.md
  - CLAUDE.md
autonomous: true
requirements:
  - CUR-03

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Tracer: `task eval:curation` runs against the live Decisions provider in the orchestrator's environment and its aggregate report is recorded verbatim in `03-EVAL-RESULTS.md` with provenance — date, commit SHA, endpoint host (never a key or userinfo), model snapshot, threshold, corpus size (CUR-03, D-03)"
    - "The results file records the D-03 gate row exactly as the harness printed it (`result=PASS`, `FAIL` or `VACUOUS`); a non-PASS result is recorded unaltered and reported as a blocker in the SUMMARY — the threshold, the corpus and the option criteria are never tuned to make the gate pass"
    - "`03-EVAL-RESULTS.md` contains only `CURATION-EVAL | ` aggregate lines plus provenance — no pair text"
    - "CLAUDE.md's Layout table names `internal/verdict/` and `internal/curationeval/`, and its cmd/engram row says consolidate attaches advisory relation verdicts when a decisions provider is configured"
    - "The phase gate is green on the final tree: `task` (lint plus the full test suite), `task license:check`, `task surfaces:gen` as a no-op, the key-links satisfiability gate, and a clean `git status`"
  artifacts:
    - path: ".planning/phases/03-curation-verdicts/03-EVAL-RESULTS.md"
      provides: "the live measurement of the relation question set: buckets, Brier, gate, confusion"
      contains: "CURATION-EVAL | "
  key_links:
    - from: ".planning/phases/03-curation-verdicts/03-EVAL-RESULTS.md"
      to: "Taskfile.yaml"
      via: "the recorded run came from the registered task target"
      pattern: "task eval:curation"
  prohibitions:
    - statement: "MUST NOT tune the threshold, the pair corpus, the blind labels or the option criteria after seeing live results in order to make the D-03 gate pass"
      category: integrity-of-evidence
      status: resolved
      verification: judgment
      reason: "Task 1 records the first complete run verbatim and escalates a non-PASS as a blocker; plan files for those inputs are outside this plan's files_modified. No wired check descriptor by design (spec-less fallback, flagged-unverified)"
---

<objective>
Run the end-of-phase live measurement and close the phase out.

- **Tracer (Task 1):** `task eval:curation` against the real provider, recorded as an aggregate-only artifact with provenance; the D-03 gate's verdict is recorded, never engineered.
- **Task 2:** CLAUDE.md layout rows for the two new packages, and the full phase gate on the final tree.

Decisions implemented: D-03 (the one hard gate, measured live), D-01 (committed set measured; aggregates only).
Output: `03-EVAL-RESULTS.md`, CLAUDE.md rows, a green phase gate.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-curation-verdicts/03-CONTEXT.md
@.planning/phases/03-curation-verdicts/03-VALIDATION.md
@.planning/phases/03-curation-verdicts/03-07-SUMMARY.md
@CLAUDE.md
</context>

<interfaces>
- `task eval:curation` runs `ENGRAM_CURATION_EVAL=1 go test ./internal/curationeval/ -run '^TestCurationEval$' -count=1 -v`; each report line is logged with the prefix `CURATION-EVAL | ` (inside `go test -v`'s own file:line log prefix); the `committed` subtest fails when the gate row is not `result=PASS`.
- The decider is built by `server.DeciderFromEnv()`: ENGRAM_DECISIONS_PROVIDER=jev, ENGRAM_DECISIONS_BASE_URL, and ENGRAM_DECISIONS_API_KEY or the inherited ENGRAM_OPENAI_API_KEY.
- CLAUDE.md's `## Layout` table (the `internal/decide/` row is the most recent precedent).
- Phase 1's `01-EVAL-SHIPPED.log` / `01-RANKING-DECISION.md` are the precedent for a recorded live run.
</interfaces>

## Artifacts this phase produces (plan 03-08's share)

| Kind | Artifact | Task |
|------|----------|------|
| Doc | `03-EVAL-RESULTS.md` — provenance plus the verbatim aggregate report and gate row | 1 |
| Doc | CLAUDE.md Layout rows for `internal/verdict/`, `internal/curationeval/`; cmd/engram row note | 2 |

## Executor notes (every task)

- Sequential main tree; never any git stash subcommand; explicit pathspec; Conventional Commits; the session's attribution trailer.
- Never print, log or record any API key; the provenance names the endpoint host only.
- `.planning/**` files carry no SPDX header; CLAUDE.md keeps its existing header state (`task license:check` decides).

<tasks>

<task type="tracer">
  <name>Task 1: Live curation eval recorded as an aggregate-only artifact</name>
  <files>.planning/phases/03-curation-verdicts/03-EVAL-RESULTS.md</files>
  <precondition>ENGRAM_DECISIONS_PROVIDER=jev, ENGRAM_DECISIONS_BASE_URL, and ENGRAM_DECISIONS_API_KEY or ENGRAM_OPENAI_API_KEY are set in the executing environment (check with `test -n` on each, never echoing values); if not, halt and return a checkpoint asking the orchestrator to run this task in its credentialed environment.</precondition>
  <read_first>
    - `internal/curationeval/eval_test.go` and `metrics.go` (report line format)
    - `.planning/phases/01-eval-foundation-lexical-reranker-fix/01-RANKING-DECISION.md` (provenance shape)
  </read_first>
  <action>
Implements D-03's live measurement on the committed corpus (D-01).

(1) Run `task eval:curation` once, capturing combined output to a scratch file outside the repo. Do not re-run to obtain a better result; a transport failure (every verdict unavailable, e.g. auth) is not a measurement — fix the environment and run again, recording in the SUMMARY why the earlier run was discarded.
(2) Write `03-EVAL-RESULTS.md` (no SPDX header): a provenance block — date, `git rev-parse HEAD`, the endpoint host (parsed from ENGRAM_DECISIONS_BASE_URL, host only), the model snapshot(s) and threshold as printed by the report, the number of committed pairs, and the command `task eval:curation`; then every `CURATION-EVAL | ` line, extracted in order with the `go test` file:line prefix stripped and the rest verbatim; then a one-paragraph reading comparing the buckets with the spike's (p ≥ 0.9 accuracy 1.00 on 26 real pairs; 0.7-0.9 at 0.71; below 0.7 at 0.20) and noting how `updates` fared.
(3) If the gate row is not `result=PASS`: keep the file exactly as measured, add a "Gate not met" section stating the numbers, commit, and report it as a blocker in the SUMMARY for the orchestrator to raise with the user. Do NOT modify the threshold, `syntheticPairs`, the blind labels or `verdict.RelationCriteria()`.

Commit as `docs(03): record live curation eval results (CUR-03, D-03)` with pathspec `.planning/phases/03-curation-verdicts/03-EVAL-RESULTS.md`.
  </action>
  <verify>
    <automated>f=.planning/phases/03-curation-verdicts/03-EVAL-RESULTS.md; test -f "$f" &amp;&amp; test "$(rg -o -e '^CURATION-EVAL [|] ' "$f" | wc -l)" -ge 8 &amp;&amp; test "$(rg -o -e 'result=(PASS|FAIL|VACUOUS)' "$f" | wc -l)" -eq 1 &amp;&amp; test "$(rg -o -e 'brier=' "$f" | wc -l)" -eq 1 &amp;&amp; test "$(rg -o -i -e 'sk-or-|bearer |api_key=' "$f" | wc -l)" -eq 0</automated>
    <fails_when>the results file is missing, carries fewer than eight report lines, lacks exactly one gate row or one Brier line, or contains anything key-shaped</fails_when>
    <automated>f=.planning/phases/03-curation-verdicts/03-EVAL-RESULTS.md; test "$(rg -o -r '$1' -e 'recordA: +"([^"]{24})' internal/curationeval/pairs.go | wc -l)" -ge 50 &amp;&amp; test "$(rg -o -r '$1' -e 'recordA: +"([^"]{24})' internal/curationeval/pairs.go | while read -r s; do if rg -q -F -- "$s" "$f"; then echo "leak: $s"; fi; done | wc -l)" -eq 0</automated>
    <fails_when>any pair's recordA prefix appears in the results file (pair text leaked into the artifact)</fails_when>
  </verify>
  <acceptance_criteria>
    - The SUMMARY states the gate result verbatim; if it is not PASS, the SUMMARY lists it under blockers.
    - `git show --stat --format= <this task's commit SHA> -- internal/ cmd/` prints nothing (no code changed while recording results).
  </acceptance_criteria>
  <done>The live measurement of the relation question set is recorded with provenance and aggregates only; the gate's outcome is reported honestly; committed.</done>
</task>

<task type="auto">
  <name>Task 2: CLAUDE.md layout rows and the phase gate</name>
  <files>CLAUDE.md</files>
  <read_first>
    - `CLAUDE.md` `## Layout` table
  </read_first>
  <action>
Close-out for the phase's new packages (the project convention Phase 2 followed for `internal/decide`).

(1) CLAUDE.md Layout table: add `internal/verdict/` — the advisory relation-verdict contract shared by `spine-review consolidate` and the curation eval (five-option relation question, same_subject, per-record state, result mapping with needs_review) over `internal/decide`; add `internal/curationeval/` — gated labeled-pair eval of the relation verdicts (`task eval:curation`, `ENGRAM_CURATION_EVAL`, blind-labeled synthetic corpus, optional private local pair file). In the `cmd/engram/` row, note that `spine-review consolidate` attaches advisory relation verdicts when `ENGRAM_DECISIONS_PROVIDER` is set (`--no-verdicts` skips) and requires `--scope` or `--all-scopes`. Edit only those cells and rows.
(2) Run the phase gate on the final tree: `task` (lint plus tests), `task license:check`, `task surfaces:gen` followed by an empty `git status --porcelain`, and `go test ./internal/keylinks/ -count=1`. Fix nothing silently: a failure caused by this phase gets fixed in the owning file with its own commit; a pre-existing failure is reported separately in the SUMMARY with its command and output.
(3) Record in the SUMMARY the known follow-ups: the curating-spine skill does not yet read the new `verdict` object (its judgment workflow was out of scope), and the private real-spine eval mode has not been run.

Commit as `docs(claude): add verdict and curation eval packages to the layout` with pathspec `CLAUDE.md`.
  </action>
  <verify>
    <automated>test "$(rg -o -F '`internal/verdict/`' CLAUDE.md | wc -l)" -ge 1 &amp;&amp; test "$(rg -o -F '`internal/curationeval/`' CLAUDE.md | wc -l)" -ge 1 &amp;&amp; task license:check &amp;&amp; go test ./internal/keylinks/ -count=1</automated>
    <fails_when>either Layout row is missing, the license check fails, or the key-links gate fails</fails_when>
    <automated>task &amp;&amp; task surfaces:gen &amp;&amp; test -z "$(git status --porcelain)"</automated>
    <fails_when>lint or the full test suite fails, or regeneration leaves the tree dirty</fails_when>
  </verify>
  <acceptance_criteria>
    - `git show --numstat --format= <this task's commit SHA> -- CLAUDE.md` shows only the Layout-table lines changed.
  </acceptance_criteria>
  <done>CLAUDE.md names the new packages, and lint, tests, license, generation and key-links gates are green on the final tree; committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orchestrator credentials → eval run | the provider key is used by the run and must not reach the artifact |
| live results → committed artifact | only aggregates cross into the public repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-08 | Repudiation | eval results | low | mitigate | provenance records date, commit SHA, endpoint host, model snapshot, threshold and corpus size |
| T-03-21 | Information disclosure | `03-EVAL-RESULTS.md` | medium | mitigate | aggregate lines only; the verify step greps for key-shaped strings and for any pair text prefix |
| T-03-22 | Tampering (evidence integrity) | gate outcome | medium | mitigate | the first complete run is recorded verbatim; a non-PASS is escalated, never tuned away |
</threat_model>

<verification>
- `03-EVAL-RESULTS.md` exists with the verbatim aggregate report and one gate row.
- `task` is green and `task surfaces:gen` is a no-op on the final tree.
</verification>

<success_criteria>
- CUR-03 / ROADMAP criterion 3: the relation question set is measured live, reporting accuracy by confidence bucket and a Brier score, with no verbatim spine content committed.
- The phase closes with every gate green, or with a clearly reported blocker if the D-03 gate is not met.
</success_criteria>

<output>
Create `.planning/phases/03-curation-verdicts/03-08-SUMMARY.md` when done. State the gate result verbatim.
</output>
