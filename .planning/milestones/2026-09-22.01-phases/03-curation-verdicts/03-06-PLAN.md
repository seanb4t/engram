---
phase: 03-curation-verdicts
plan: 06
type: execute
wave: 4
depends_on:
  - 03-05
files_modified:
  - cmd/engram/operator_view.go
  - cmd/engram/spine_review_consolidate_view.go
  - cmd/engram/spine_review_consolidate_test.go
  - cmd/engram/operator_view_scan_test.go
  - cmd/engram/operator_output_test.go
  - cmd/engram/consolidate_docs_test.go
  - docs-site/src/content/docs/guides/cli.md
autonomous: true
requirements:
  - CUR-01

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Tracer: `consolidate --output text` renders each candidate row with its verdict: a success renders `verdict=<relation> p=<probabilities[relation]>`, then `[needs review]` when flagged, then `same_subject=<p>`, `probabilities=duplicate:<p>,contradicts:<p>,updates:<p>,related:<p>,unrelated:<p>` and `model=<model>`; a failure renders `verdict unavailable (<class>)` (D-07, D-10)"
    - "Every number in the verdict text is the JSON lane's own number text, never re-formatted through float64 (spec-less edge CUR-02 precision on the text lane)"
    - "The verdict text is derived from the same marshaled bytes the JSON lane emits (a renderer registered for the row key `verdict`, fed the field's raw JSON), so text stays a view of the JSON contract; every renderer's output passes through `sanitizeViewValue` inside `viewRow`, so a renderer cannot bypass sanitization"
    - "Any other nested object or array inside a row, or an array element that is itself an array, renders through a generic sanitizing flatten (`key.sub=value`, `key[0]=value`) — `viewScalar` is reached only by scalars, closing WR-02 for every future shape"
    - "`TestOperatorViewFixturesHaveNoUnsanitizedNesting` now proves sanitization instead of forbidding nesting: every string leaf of every operator fixture, at any depth, is replaced with a hostile value (newline, carriage return, tab, ESC, DEL); the rendered output contains no rune below 0x20 except the renderer's own line breaks and no 0x7f, and its line count equals the structural count; a red-control subtest proves the check fails on unsanitized output"
    - "The consolidate fixtures in `spineViewFixtures` include a doc with a successful unflagged verdict and a failed verdict, and a doc with a flagged verdict; the identity gate and the min_score omitempty count subtest still pass"
    - "cli.md's consolidate section documents the advisory verdicts: what is sent and when (ENGRAM_DECISIONS_PROVIDER, ENGRAM_DECISIONS_VERDICT_STATE_CHARS, `--no-verdicts`), the five relations with their definitions and the newer-record-is-record_b rule, `same_subject`, `needs_review` with ENGRAM_DECISIONS_VERDICT_THRESHOLD and `--verdict-threshold`, the JSON success and failure shapes, the text rendering, the failure classes including `state_unavailable`, exit status 0, and that consolidate never merges or mutates; `TestConsolidateGuideStatesVerdictContract` gates it with a red control"
  artifacts:
    - path: "cmd/engram/operator_view.go"
      provides: "registerRowFieldRenderer, the generic sanitizing nested flatten, renderer output sanitized in viewRow"
      contains: "func registerRowFieldRenderer("
    - path: "cmd/engram/spine_review_consolidate_view.go"
      provides: "renderVerdictView and its registration for the verdict row key"
      contains: "func renderVerdictView("
    - path: "cmd/engram/consolidate_docs_test.go"
      provides: "TestConsolidateGuideStatesVerdictContract and its red control"
      contains: "func TestConsolidateGuideStatesVerdictContract("
  key_links:
    - from: "cmd/engram/spine_review_consolidate_view.go"
      to: "cmd/engram/operator_view.go"
      via: "the verdict renderer is registered on the shared operator view, not a second rendering path"
      pattern: "registerRowFieldRenderer[(].verdict., renderVerdictView"
    - from: "cmd/engram/operator_view.go"
      to: "cmd/engram/operator_view.go"
      via: "renderer output is sanitized by viewRow itself"
      pattern: "sanitizeViewValue[(]rendered[)]"
    - from: "cmd/engram/consolidate_docs_test.go"
      to: "docs-site/src/content/docs/guides/cli.md"
      via: "the guide's consolidate section is gated for the verdict contract"
      pattern: "guides/cli[.]md"
  prohibitions:
    - statement: "MUST NOT let any provider-supplied or record-derived string (model, relation, error class) reach the operator's terminal without control-character sanitization"
      category: safety
      status: resolved
      verification: test
      reason: "TestOperatorViewFixturesHaveNoUnsanitizedNesting's hostile-leaf proof plus its red control. Canon terminal-injection hardening is otherwise /gsd-secure-phase territory. No wired check descriptor by design (spec-less fallback, flagged-unverified)"
---

<objective>
Render the verdict in the text view (D-07, D-10's text form) without breaking the operator view's two invariants — text is a view of the JSON bytes, and every value is sanitized (T-06-03, WR-02) — and document the contract in the CLI guide.

- **Tracer (Task 1):** a row-field renderer hook in `viewRow`, the verdict renderer, and one rendered-output test.
- **Task 2:** the verdict fixtures in the shared fixture set, and the WR-02 guard rewritten from "no nesting exists" to "nesting renders sanitized" (RED first: the old guard fails the moment the fixtures land).
- **Task 3:** the CLI guide's consolidate section and its docs gate.

Decisions implemented: D-07 (text shows verdict, its probability, same-subject probability, needs-review marker), D-10 (`verdict unavailable (<class>)`), D-05 (text derived from the JSON contract).
Output: operator view extension, verdict renderer, rewritten guard, fixtures, guide section, docs gate.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-curation-verdicts/03-CONTEXT.md
@.planning/phases/03-curation-verdicts/03-RESEARCH.md
@.planning/phases/03-curation-verdicts/03-05-SUMMARY.md
@cmd/engram/operator_view.go
@cmd/engram/operator_output.go
</context>

<interfaces>
- `cmd/engram/operator_view.go`: `viewFields(doc)` walks the marshaled bytes; array elements that are objects go through `viewRow`, others through `viewScalar`; `viewRow(raw)` emits `key=viewScalar(val)` per field; `viewScalar` sanitizes JSON strings only and returns any other raw text verbatim (numbers keep their exact digits); `sanitizeViewValue` replaces runes below 0x20 and 0x7f with a space; its doc comment's WR-02 paragraph says no report produces nested row shapes today. `renderOperatorView` writes the sanitized headline, a blank line, one line per top-level field, and each row indented by four spaces. The top-level `case '{'` branch in `viewFields` belongs to Phase 5's OPS-02 (#504) — do not remove or restructure it.
- `cmd/engram/operator_output_test.go` lines ~383-472: `TestOperatorViewFixturesHaveNoUnsanitizedNesting`, `assertNoTwoLevelContainerNesting`, `assertRowHasNoContainerFields`; `operatorViewFixtures()` merges every fixture group.
- `cmd/engram/operator_view_scan_test.go`: `spineViewFixtures()` with two consolidate docs and the `min_score omitempty field count` subtest that requires exactly 2 consolidate fixtures and compares `[0]` with `[1]`; `TestSpineViewIdentity`.
- From 03-01/03-05: `consolidateVerdictDoc` (success keys relation, probabilities, same_subject, needs_review, model; failure key error), `consolidateReportDoc.VerdictThreshold`, `attachVerdicts`, `verdict.Verdict`.
- `cmd/engram/migrate_docs_test.go`: the docs-gate shape (relative path constant, skip when the file is absent, an injected-violation red control).
</interfaces>

## Artifacts this phase produces (plan 03-06's share)

| Kind | Artifact | Task |
|------|----------|------|
| Func | `registerRowFieldRenderer(key string, fn func(json.RawMessage) (string, error))`, the generic nested flatten in `operator_view.go` | 1 |
| Func | `renderVerdictView(raw json.RawMessage) (string, error)` + `init` registration (`spine_review_consolidate_view.go`) | 1 |
| Tests | `TestConsolidateTextViewRendersVerdict`, `TestRegisterRowFieldRendererRejectsDuplicates`, rewritten `TestOperatorViewFixturesHaveNoUnsanitizedNesting` (+ red control), verdict fixtures | 1, 2 |
| Docs | cli.md consolidate "advisory verdicts" content; `TestConsolidateGuideStatesVerdictContract` | 3 |

## Flagged assumptions (spec-less probe)

- CUR-01 encoding on the text lane: provider strings are sanitized; East-Asian-wide alignment remains the text lane's documented, accepted limitation.
- The "same_subject high plus related is worth a look" hint (CONTEXT specifics, planner's call) goes into the guide as reader guidance, not into the renderer, so the text view never shows a value absent from the JSON.

## Executor notes (every task)

- Sequential main tree; never any git stash subcommand; explicit pathspec; Conventional Commits; the session's attribution trailer.
- New Go files carry the SPDX header; `docs-site/**` does not.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Verdict text rendering through a sanitized row-field renderer hook</name>
  <files>cmd/engram/operator_view.go, cmd/engram/spine_review_consolidate_view.go, cmd/engram/spine_review_consolidate_test.go</files>
  <read_first>
    - `cmd/engram/operator_view.go` (whole file) and `cmd/engram/operator_output.go`
    - `cmd/engram/spine_review_consolidate.go` (`consolidateVerdictDoc`, `attachVerdicts`)
    - `cmd/engram/spine_review_consolidate_test.go` (`TestConsolidateNeverLabelsPairAsDuplicateOrCluster`)
  </read_first>
  <behavior>
    - `TestConsolidateTextViewRendersVerdict`: a report doc with three candidates (success `duplicate` p 0.93 unflagged, success `related` p 0.62 flagged, failure `timeout`) rendered with `renderOperatorView` has three candidate rows: the first contains `verdict=duplicate p=0.93`, `same_subject=0.97`, `probabilities=duplicate:0.93,contradicts:`, `model=typesafe/jev-1.13-20260917` and no `[needs review]`; the second contains `verdict=related p=0.62 [needs review]`; the third contains `verdict unavailable (timeout)`; a `Verdict threshold` top-level line is present.
    - `TestRegisterRowFieldRendererRejectsDuplicates`: registering an empty key, a nil function, or `verdict` a second time panics.
    - A verdict whose relation has no entry in its probabilities renders `p=` with an empty value and no error.
  </behavior>
  <action>
Implements D-07 and D-10's text form while keeping text a view of the JSON (D-05).

(1) `operator_view.go`: add an unexported `rowFieldRenderers` map and `registerRowFieldRenderer(key string, fn func(json.RawMessage) (string, error))` that panics on an empty key, a nil function, or a duplicate registration (called only from `init` functions). In `viewRow`, when a field's value is a JSON object or array: if a renderer is registered for that key, call it with the raw bytes and append `sanitizeViewValue(rendered)` as one part (no `key=` prefix — the renderer owns its token text); otherwise append the generic flatten. Add the generic flatten: recursively walk the raw value in document order, emitting `path=value` parts where the path is dotted for object keys and indexed for array elements, and every scalar goes through `viewScalar`. In `viewFields`, route an array element that is itself an array through the same generic flatten instead of `viewScalar`. Do not change the top-level `case '{'` branch.
(2) New `cmd/engram/spine_review_consolidate_view.go` (SPDX): `renderVerdictView(raw json.RawMessage) (string, error)` decoding into a small view struct that keeps every number as `json.RawMessage` (verbatim digits) and reads relation, probabilities (five keys), same_subject, needs_review, model and error. A non-empty error renders `verdict unavailable (<error>)`. Otherwise render `verdict=<relation> p=<probabilities[relation]>`, then ` [needs review]` when needs_review is true, then ` same_subject=<n> probabilities=duplicate:<n>,contradicts:<n>,updates:<n>,related:<n>,unrelated:<n> model=<model>`. Return an error only for undecodable JSON. Register it in this file's `init` with exactly `registerRowFieldRenderer("verdict", renderVerdictView)`. Doc comment: the text rendering of the D-05 object, fed the same bytes the JSON lane emits; sanitization happens in `viewRow`.
(3) Tests per `<behavior>`; also update `TestConsolidateNeverLabelsPairAsDuplicateOrCluster`'s doc comment: it covers the structural ranking (a doc with no verdict pass), which still never labels a pair; the advisory verdict may name any relation, including duplicate. Its logic is unchanged.

Commit as `feat(consolidate): render advisory verdicts in the text view (D-07, D-10)` with pathspec `cmd/engram/operator_view.go cmd/engram/spine_review_consolidate_view.go cmd/engram/spine_review_consolidate_test.go`.
  </action>
  <verify>
    <automated>out=$(go test ./cmd/engram/ -run '^(TestConsolidateTextViewRendersVerdict|TestRegisterRowFieldRendererRejectsDuplicates|TestConsolidateNeverLabelsPairAsDuplicateOrCluster|TestOperatorViewIdentity|TestSpineViewIdentity)$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e '^--- PASS: Test' | wc -l)" -eq 5</automated>
    <fails_when>non-zero exit or fewer than five top-level PASS lines</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -o -e 'sanitizeViewValue[(]rendered[)]' cmd/engram/operator_view.go | wc -l` prints `1` (renderer output is sanitized by the shared walker; name the variable `rendered`).
    - `go test ./cmd/engram/ -count=1` exits 0 (the shared fixtures do not yet carry verdicts, so the old guard still passes here).
    - `golangci-lint run ./cmd/engram/...` reports no findings.
  </acceptance_criteria>
  <done>The text view shows each verdict, its probability, the same-subject probability and the needs-review marker, or `verdict unavailable (<class>)`, derived from the JSON bytes and sanitized; committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Verdict fixtures and the WR-02 guard rewritten into a sanitization proof</name>
  <files>cmd/engram/operator_view_scan_test.go, cmd/engram/operator_output_test.go, cmd/engram/operator_view.go</files>
  <read_first>
    - `cmd/engram/operator_output_test.go` lines 160-200 and 375-475
    - `cmd/engram/operator_view_scan_test.go` (whole file)
    - `cmd/engram/operator_view.go` (`sanitizeViewValue` and `viewScalar` doc comments, post-Task-1)
  </read_first>
  <behavior>
    - RED first: after appending the verdict docs to `spineViewFixtures()`, the OLD `TestOperatorViewFixturesHaveNoUnsanitizedNesting` fails on the nested `verdict` object — record that failure in the SUMMARY as the design engaging, not a bug.
    - Rewritten `TestOperatorViewFixturesHaveNoUnsanitizedNesting`: for every fixture in `operatorViewFixtures()`, marshal it, decode it generically, replace every string leaf at any depth with `x` + newline + `y` + carriage return + tab + ESC + DEL + `z`, re-marshal, render with `renderOperatorView(&buf, "headline", json.RawMessage(b))`, and assert the output contains no rune below 0x20 other than newline and no 0x7f, and that its newline count equals 2 plus the number of top-level fields plus the total number of rows `viewFields` reports (every fixture has at least one field).
    - Red-control subtest named `red control`: the same checker applied to a string built by writing a hostile value raw (bypassing the renderer) reports a violation.
    - The `min_score omitempty field count` subtest expects 4 consolidate fixtures and still compares `[0]` with `[1]`.
  </behavior>
  <action>
Closes WR-02 deliberately (RESEARCH Pitfall 1) rather than bypassing it.

(1) `operator_view_scan_test.go`: append two consolidate docs to `spineViewFixtures()` built with `consolidateDoc` plus `attachVerdicts`: one with two candidates (a successful unflagged verdict and a failed `timeout` verdict) and one with a flagged verdict. Keep the existing two docs first; change the count check from 2 to 4.
(2) Run `go test ./cmd/engram/ -run '^TestOperatorViewFixturesHaveNoUnsanitizedNesting$'` and record the RED.
(3) `operator_output_test.go`: replace the test body, its doc comment and the two `assert…Container…` helpers with the sanitization proof in `<behavior>` (keep the test name — 03-VALIDATION.md maps CUR-01's text check to it). The doc comment explains why nesting is now allowed (Task 1's renderer hook and generic flatten) and how it stays sanitized.
(4) `operator_view.go`: rewrite the WR-02 paragraph of `sanitizeViewValue`'s doc comment and `viewScalar`'s doc comment to describe the current guarantee: every string reaching the text lane — top-level scalar, row scalar, generically flattened nested scalar, or registered renderer output — passes through `sanitizeViewValue`.

Commit as `test(operator-view): prove nested values render sanitized instead of forbidding them (WR-02)` with pathspec `cmd/engram/operator_view_scan_test.go cmd/engram/operator_output_test.go cmd/engram/operator_view.go`.
  </action>
  <verify>
    <automated>out=$(go test ./cmd/engram/ -run '^(TestOperatorViewFixturesHaveNoUnsanitizedNesting|TestSpineViewIdentity)$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e '^--- PASS: (TestOperatorViewFixturesHaveNoUnsanitizedNesting|TestSpineViewIdentity) ' | wc -l)" -eq 2 &amp;&amp; printf '%s\n' "$out" | rg -q -i -e '--- PASS: TestOperatorViewFixturesHaveNoUnsanitizedNesting/.*red'</automated>
    <fails_when>non-zero exit, a missing top-level PASS, or no passing red-control subtest</fails_when>
    <automated>test "$(rg -o -e 'func assertNoTwoLevelContainerNesting|func assertRowHasNoContainerFields' cmd/engram/operator_output_test.go | wc -l)" -eq 0 &amp;&amp; go test ./cmd/engram/ -count=1</automated>
    <fails_when>the old no-nesting helpers remain, or any cmd/engram test fails</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -o -e 'len[(]consolidateDocs[)] != 4' cmd/engram/operator_view_scan_test.go | wc -l` prints `1`.
    - `golangci-lint run ./cmd/engram/...` reports no findings.
  </acceptance_criteria>
  <done>The shared fixtures carry verdicts, and the guard now proves every nested value renders sanitized, with a red control; RED recorded; committed.</done>
</task>

<task type="auto">
  <name>Task 3: CLI guide consolidate section and its docs gate</name>
  <files>docs-site/src/content/docs/guides/cli.md, cmd/engram/consolidate_docs_test.go</files>
  <read_first>
    - `docs-site/src/content/docs/guides/cli.md` consolidate section (post-03-03)
    - `cmd/engram/migrate_docs_test.go` (docs-gate shape)
    - `internal/verdict/verdict.go` (`RelationCriteria`, `StateContext`)
    - `.claude/skills/spike-findings-engram/references/curation-verdicts.md` ("What to Avoid")
  </read_first>
  <action>
Documents D-04..D-11 for operators (correct-by-reading).

(1) cli.md, consolidate section: rewrite the bold never-merges paragraph so it says the structural ranking itself never labels a pair and consolidate never merges or mutates, while an optional advisory verdict may name any relation. Add an "Advisory verdicts" part covering: runs by default only when ENGRAM_DECISIONS_PROVIDER is set, `--no-verdicts` skips it; what each request sends (both records' summary plus up to ENGRAM_DECISIONS_VERDICT_STATE_CHARS characters of content) with a link to the configure guide's "What leaves your deployment"; the five relations with their definitions verbatim from `verdict.RelationCriteria()`, and that the more recently created record is sent as record_b so `updates` means the newer record updates the older one; `same_subject`; `needs_review` against ENGRAM_DECISIONS_VERDICT_THRESHOLD (default 0.9) or `--verdict-threshold`; a JSON example of a candidate with a successful verdict and one with `{"error": "timeout"}`; the text rendering (`verdict=… p=… [needs review]`, `verdict unavailable (<class>)`); the failure classes (auth, bad_request, context_too_large, rate_limited, unavailable, timeout, response_too_large, malformed_response, invalid_request, canceled, error, state_unavailable); the stderr disclosure and summary lines; that a failed verdict never changes the exit status; that `--timeout` also bounds the verdict pass; and reader guidance that a `related` verdict with a high `same_subject` is worth a look (refinements and state changes often land there).
(2) New `cmd/engram/consolidate_docs_test.go` (SPDX), mirroring `migrate_docs_test.go`: a relative-path constant for cli.md; a pure function extracting the consolidate section (from its `###` heading to the next `### ` heading) and returning missing anchors; `TestConsolidateGuideStatesVerdictContract` requiring `--no-verdicts`, `--verdict-threshold`, `ENGRAM_DECISIONS_PROVIDER`, `ENGRAM_DECISIONS_VERDICT_THRESHOLD`, `ENGRAM_DECISIONS_VERDICT_STATE_CHARS`, `needs_review`, `same_subject`, `verdict unavailable`, `state_unavailable`, `advisory`, `never merges`, `--scope`, `--all-scopes`, and all five relation names; and requiring that the retired empty-result sentence removed by plan 03-03 is absent from the section (reuse the exact phrase that plan's verify negative-greps); a red-control subtest where a synthetic section missing one anchor is reported.

Commit as `docs(consolidate): document advisory verdicts in the CLI guide with a docs gate` with pathspec `docs-site/src/content/docs/guides/cli.md cmd/engram/consolidate_docs_test.go`.
  </action>
  <verify>
    <automated>out=$(go test ./cmd/engram/ -run '^TestConsolidateGuideStatesVerdictContract$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; printf '%s\n' "$out" | rg -q -e '^--- PASS: TestConsolidateGuideStatesVerdictContract ' &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e 'SKIP' | wc -l)" -eq 0</automated>
    <fails_when>non-zero exit, no PASS line, or a SKIP (the guide was not found and the gate passed vacuously)</fails_when>
  </verify>
  <acceptance_criteria>
    - The docs site builds or lints cleanly for the changed guide: `task lint` reports no new markdown findings.
    - `test "$(rg -o -F 'updates' docs-site/src/content/docs/guides/cli.md | wc -l)" -ge 2` exits 0.
  </acceptance_criteria>
  <done>The CLI guide states the full advisory verdict contract, gated against drift with a red control; committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| provider response → operator terminal | model string, relation and error class are rendered to a TTY |
| stored records → operator terminal | any record-derived value in any operator report |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-03 | Tampering (terminal control injection, T-06-03) | `viewRow`, `renderVerdictView`, generic flatten | medium | mitigate | every renderer output and every nested scalar passes `sanitizeViewValue` inside `viewRow`; the hostile-leaf guard over all operator fixtures plus a red control proves it |
| T-03-18 | Tampering (docs drift) | cli.md consolidate section | low | mitigate | `TestConsolidateGuideStatesVerdictContract` with a red control |
</threat_model>

<verification>
- `go test ./cmd/engram/ -count=1` exits 0.
- `consolidate --output text` renders verdict rows as specified, sanitized.
</verification>

<success_criteria>
- CUR-01's text half and D-07: the text view renders the verdict, its probability, the same-subject probability and a needs-review marker.
- D-10's text form: `verdict unavailable (<class>)`.
</success_criteria>

<output>
Create `.planning/phases/03-curation-verdicts/03-06-SUMMARY.md` when done. Record the RED from Task 2.
</output>
