---
phase: 03-curation-verdicts
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/config/config.go
  - internal/config/registry.go
  - internal/config/validate.go
  - internal/config/decisions_config_test.go
  - internal/config/decisions_docs_test.go
  - docs-site/src/content/docs/guides/configure.md
autonomous: true
requirements:
  - CUR-02

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Tracer: `ENGRAM_DECISIONS_VERDICT_THRESHOLD` is registered once (key `decisions.verdict_threshold`, Default `0.9`, no Legacy, no Flag), reaches `Config.Decisions.VerdictThreshold` through `config.Load(nil)`, is validated by `Config.Validate` through the exported `config.ParseProbability` only when the provider is `jev`, and has a row in configure.md's Typed decisions table that `TestDecisionsVarsDocumented` finds (D-08)"
    - "`ENGRAM_DECISIONS_VERDICT_STATE_CHARS` is registered once (key `decisions.verdict_state_chars`, Default `1500`, no Legacy, no Flag), reaches `Config.Decisions.VerdictStateChars`, is validated through `ParsePositiveIntCap` only when the provider is `jev`, and is documented in the same table (D-09)"
    - "`ParsePositiveIntCap`-style exactness for probabilities: `config.ParseProbability` accepts `0`, `1`, `0.9`, `0.95` and `1.0`; rejects `-0.01`, `1.01`, `NaN`, `nan`, `Inf`, `+Inf`, `-Inf`, `abc` and the empty string, each with an error; it returns the parsed float64 unrounded (spec-less edges CUR-02 boundary and precision)"
    - "With the provider unset, any value in either new var — including an invalid one — validates exactly as before (D-04's inert-when-off rule, the Phase 2 E01 row)"
    - "`TestDecisionsVarsDocumented`'s positive-control count is 11, its red-control synthetic table still omits exactly `ENGRAM_DECISIONS_CONCURRENCY` (the two new rows are added to it), and the gate passes against configure.md"
    - "configure.md no longer says the features that ask the provider questions 'ship separately': it names `engram spine-review consolidate`'s advisory verdicts as a feature that sends record content (each record's summary plus up to `ENGRAM_DECISIONS_VERDICT_STATE_CHARS` characters of content) and keeps every existing disclosure anchor (`TypeSafe`, `retention`, `ENGRAM_OPENAI_API_KEY`, `/alpha/decisions`)"
  artifacts:
    - path: "internal/config/validate.go"
      provides: "ParseProbability and the two gated Validate rules"
      contains: "func ParseProbability(value string) (float64, error)"
    - path: "internal/config/registry.go"
      provides: "decisions.verdict_threshold and decisions.verdict_state_chars rows"
      contains: "ENGRAM_DECISIONS_VERDICT_STATE_CHARS"
    - path: "internal/config/config.go"
      provides: "DecisionsConfig.VerdictThreshold, DecisionsConfig.VerdictStateChars"
      contains: "VerdictThreshold"
    - path: "docs-site/src/content/docs/guides/configure.md"
      provides: "two new Typed decisions table rows and the consolidate disclosure"
      contains: "ENGRAM_DECISIONS_VERDICT_THRESHOLD"
  key_links:
    - from: "internal/config/validate.go"
      to: "internal/config/registry.go"
      via: "the gated rule names the registered var"
      pattern: "ENGRAM_DECISIONS_VERDICT_THRESHOLD %q"
    - from: "internal/config/decisions_docs_test.go"
      to: "docs-site/src/content/docs/guides/configure.md"
      via: "the docs gate requires a table row for every registered decisions var"
      pattern: "len[(]envs[)] != 11"
  prohibitions:
    - statement: "MUST NOT describe the typed-decision feature as sending nothing today once consolidate sends record content — the configuration reference must name the feature that sends it and how much"
      category: transparency
      status: resolved
      verification: judgment
      reason: "Task 2 rewrites the disclosure sentence; reviewed in the plan's acceptance criteria by anchor presence. No wired check descriptor by design (spec-less fallback, flagged-unverified)"
---

<objective>
Register the two knobs the verdict pass needs, with one shared parser each, the provider-gated validation, and the docs rows the completeness gate requires — all in the commits that add them.

- **Tracer (Task 1):** `ENGRAM_DECISIONS_VERDICT_THRESHOLD` through every config layer: registry row, struct field, `ParseProbability`, gated `Validate`, registry/validate tests, the docs-gate count, and the configure.md row (D-08).
- **Expansion (Task 2):** `ENGRAM_DECISIONS_VERDICT_STATE_CHARS` the same way (D-09), and the configure.md disclosure rewrite naming consolidate as the first feature that sends record content.

Decisions implemented: D-08 (registered threshold key, default 0.9), D-09 (registered truncation knob, default 1500), D-04 (both inert when the provider is unset).

Purpose: plan 03-05 resolves these into the verdict pass and the `--verdict-threshold` flag; the eval (03-07) resolves them the same way.
Output: two registry rows, `ParseProbability`, gated validation, tests, docs rows and disclosure.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-curation-verdicts/03-CONTEXT.md
@.planning/phases/03-curation-verdicts/03-RESEARCH.md
@.planning/phases/03-curation-verdicts/03-PATTERNS.md
@internal/config/registry.go
@internal/config/decisions_config_test.go
@internal/config/decisions_docs_test.go
</context>

<interfaces>
Existing code (verified at planning time):

- `internal/config/config.go` lines 216-240: `type DecisionsConfig struct { Provider, BaseURL, APIKey, Model, Timeout, MaxTimeout, DrainBytes, DrainTimeout, Concurrency string }` with `koanf` tags; the struct doc explains "keep as strings, consumer validates".
- `internal/config/registry.go` lines 100-121: the nine `decisions.*` rows with no Legacy and no Flag, preceded by a block comment. `configLoad(nil)` means no flag overlay reaches operator commands, so the new rows stay env-only; plan 03-05 adds `--verdict-threshold` as a command-local override.
- `internal/config/validate.go`: the `if c.Decisions.Provider == "jev"` block (lines ~295-350) appends `fmt.Errorf("ENGRAM_DECISIONS_X %q: %w", value, err)` to `errs`; `ParsePositiveIntCap` / `ParseNonNegativeIntCap` (lines ~420-446) are the exported-shared-parser precedent (WR-01: validated range equals enforced range).
- `internal/config/decisions_config_test.go`: `decisionsFields` (nine rows driving `TestDecisionsRegistryEntries`), `decisionsJevEnabled()` (a full passing DecisionsConfig literal — gotcha `s780vae1vr`: every new required field must be added to every full literal), `TestDecisionsValidate`.
- `internal/config/decisions_docs_test.go` lines 74-127: `len(envs) != 9` positive control; the red-control synthetic table omits only `ENGRAM_DECISIONS_CONCURRENCY`; `extractDecisionsSection`; disclosure anchors `TypeSafe`, `retention`, `ENGRAM_OPENAI_API_KEY`, `/alpha/decisions`.
- `docs-site/src/content/docs/guides/configure.md` lines 152-208: the `## Typed decisions (Jev)` section, the "What leaves your deployment" paragraph, the "ship separately and will name this setting when they do" sentence, and the table (`| Environment variable | Flag | Default | Description |`).
- Helm (`charts/engram/values.yaml`, `_helpers.tpl`) exposes only a subset of `ENGRAM_DECISIONS_*` for the server; these two knobs are read by the operator CLI, so the chart is deliberately unchanged.
</interfaces>

<assumption_delta_decision>
Detector: `chosen` fired on "configurable confidence threshold (default 0.9)".
- Primary noun: the needs-review threshold, now the registered key `ENGRAM_DECISIONS_VERDICT_THRESHOLD` (with the command-local `--verdict-threshold` override in plan 03-05).
- Decision: **promote**. The registry row is the single source of the default; `verdict.DefaultThreshold` (plan 03-01) is only the resolver's fallback, and plan 03-05 adds `TestVerdictSettingsDefaultsMatchVerdictPackage` (mirroring `TestDecisionsModelDefaultMatchesJev`) so the two can never drift. The same holds for `ENGRAM_DECISIONS_VERDICT_STATE_CHARS` and `verdict.DefaultStateChars`.
- Invariant test: plan 03-05 Task 1.
</assumption_delta_decision>

## Artifacts this phase produces (plan 03-02's share)

| Kind | Artifact | Task |
|------|----------|------|
| Config | `DecisionsConfig.VerdictThreshold` (`koanf:"verdict_threshold"`), `DecisionsConfig.VerdictStateChars` (`koanf:"verdict_state_chars"`) | 1, 2 |
| Registry | `decisions.verdict_threshold` / `ENGRAM_DECISIONS_VERDICT_THRESHOLD` (0.9); `decisions.verdict_state_chars` / `ENGRAM_DECISIONS_VERDICT_STATE_CHARS` (1500) | 1, 2 |
| Func | `config.ParseProbability(string) (float64, error)` | 1 |
| Tests | `TestParseProbability`; extended `TestDecisionsRegistryEntries`, `TestDecisionsValidate`, `TestDecisionsVarsDocumented` | 1, 2 |
| Docs | configure.md: two table rows; disclosure naming consolidate | 1, 2 |

## Executor notes (every task)

- Sequential main tree; never any git stash subcommand; explicit pathspec; Conventional Commits; the session's attribution trailer.
- The count bump, the red-control rows, the registry row and the docs row land in the SAME commit, or `TestDecisionsVarsDocumented` goes red between commits (RESEARCH Pitfall 3).
- `docs-site/**` carries no SPDX header.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: ENGRAM_DECISIONS_VERDICT_THRESHOLD end to end — registry, struct, ParseProbability, gated Validate, docs gate, docs row</name>
  <files>internal/config/config.go, internal/config/registry.go, internal/config/validate.go, internal/config/decisions_config_test.go, internal/config/decisions_docs_test.go, docs-site/src/content/docs/guides/configure.md</files>
  <read_first>
    - `internal/config/config.go` lines 205-245
    - `internal/config/registry.go` lines 95-125
    - `internal/config/validate.go` lines 280-350 and 395-450
    - `internal/config/decisions_config_test.go` (whole file) and `internal/config/decisions_docs_test.go` (whole file)
    - `docs-site/src/content/docs/guides/configure.md` lines 150-210
  </read_first>
  <behavior>
    - `TestParseProbability` (table): accepts `0`, `1`, `0.9`, `0.95`, `1.0` returning the exact float64 `strconv.ParseFloat` gives; rejects `-0.01`, `1.01`, `NaN`, `nan`, `Inf`, `+Inf`, `-Inf`, `abc`, and `` with a non-nil error.
    - `TestDecisionsRegistryEntries` covers the new row through `decisionsFields` (registry shape, from-env round trip, empty-env default `0.9`).
    - `TestDecisionsValidate` gains: provider `jev` with threshold `1.5` fails naming `ENGRAM_DECISIONS_VERDICT_THRESHOLD`; `NaN` fails; `0` and `1` pass; provider empty with threshold `garbage` passes.
    - `TestDecisionsVarsDocumented` passes with 10 names (this task) — its literal moves to 11 in Task 2.
  </behavior>
  <action>
Implements D-08's registered key.

(1) `config.go`: add `VerdictThreshold string` with `koanf:"verdict_threshold"` to `DecisionsConfig`; one doc line: the needs-review threshold for consolidate's advisory verdicts (a probability in [0, 1]), overridable per run by `--verdict-threshold`.
(2) `registry.go`: add `{Key: "decisions.verdict_threshold", Env: "ENGRAM_DECISIONS_VERDICT_THRESHOLD", Default: "0.9"}` after `decisions.concurrency`, and extend the block comment: consumed by the operator CLI's verdict pass (not the server), env-only like its siblings because operator commands load config with no flag overlay, and `--verdict-threshold` is a command-local override.
(3) `validate.go`: exported `func ParseProbability(value string) (float64, error)` mirroring `ParsePositiveIntCap`'s doc and shape — `strconv.ParseFloat(value, 64)`, then reject NaN or infinity (`math.IsNaN`, `math.IsInf`) and anything below 0 or above 1 with a plain error ("must be a probability between 0 and 1"); doc comment says `internal/server` and the consolidate flag call this SAME parser so the validated range equals the enforced range (WR-01). Inside the `Provider == "jev"` block add the rule appending `fmt.Errorf("ENGRAM_DECISIONS_VERDICT_THRESHOLD %q: %w", c.Decisions.VerdictThreshold, err)`.
(4) `decisions_config_test.go`: add the row to `decisionsFields` (update the "nine" wording in comments), set `VerdictThreshold: "0.9"` in `decisionsJevEnabled()` and in every other full `DecisionsConfig` literal in the file, add `TestParseProbability` and the Validate cases above.
(5) `decisions_docs_test.go`: change the positive-control literal and message from 9 to 10 and add a `ENGRAM_DECISIONS_VERDICT_THRESHOLD` row to the red-control synthetic table (it must still omit only `ENGRAM_DECISIONS_CONCURRENCY`).
(6) `configure.md`: add a table row after the concurrency row whose first cell is the backticked var name `ENGRAM_DECISIONS_VERDICT_THRESHOLD`, Flag cell `—` and Default cell `0.9` (the pipe, backticked name, pipe prefix the docs gate's `missingDecisionsDocs` searches for). Description: the probability below which a `spine-review consolidate` verdict is marked `needs_review`; a probability between 0 and 1; `--verdict-threshold` overrides it for one run.

Commit as `feat(config): register ENGRAM_DECISIONS_VERDICT_THRESHOLD with a shared probability parser (D-08)` with pathspec `internal/config/config.go internal/config/registry.go internal/config/validate.go internal/config/decisions_config_test.go internal/config/decisions_docs_test.go docs-site/src/content/docs/guides/configure.md`.
  </action>
  <verify>
    <automated>out=$(go test ./internal/config/ -run '^(TestParseProbability|TestDecisionsRegistryEntries|TestDecisionsValidate|TestDecisionsVarsDocumented)$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e '^--- PASS: (TestParseProbability|TestDecisionsRegistryEntries|TestDecisionsValidate|TestDecisionsVarsDocumented) ' | wc -l)" -eq 4</automated>
    <fails_when>non-zero exit, or fewer than four top-level PASS lines</fails_when>
    <automated>go test ./internal/config/ -count=1</automated>
    <fails_when>non-zero exit (a full Config literal elsewhere in the package now fails validation or compilation)</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -o 'len[(]envs[)] != 10' internal/config/decisions_docs_test.go | wc -l` prints `1`.
    - `rg -o -F '| `ENGRAM_DECISIONS_VERDICT_THRESHOLD` |' docs-site/src/content/docs/guides/configure.md | wc -l` prints `1`.
    - `golangci-lint run ./internal/config/...` reports no findings.
  </acceptance_criteria>
  <done>The threshold key flows from the environment through Load and gated Validate, has one shared parser, and is documented with the gate green in the same commit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ENGRAM_DECISIONS_VERDICT_STATE_CHARS and the consolidate data disclosure</name>
  <files>internal/config/config.go, internal/config/registry.go, internal/config/validate.go, internal/config/decisions_config_test.go, internal/config/decisions_docs_test.go, docs-site/src/content/docs/guides/configure.md</files>
  <read_first>
    - the six files as left by Task 1
  </read_first>
  <behavior>
    - `TestDecisionsRegistryEntries` covers `decisions.verdict_state_chars` (default `1500`).
    - `TestDecisionsValidate` gains: provider `jev` with state chars `0`, `-5` and `abc` each fail naming `ENGRAM_DECISIONS_VERDICT_STATE_CHARS`; `1` and `1500` pass; provider empty with `0` passes.
    - `TestDecisionsVarsDocumented` passes at 11 names with the disclosure anchors intact.
  </behavior>
  <action>
Implements D-09's registered knob (name chosen under Claude's discretion) and the transparency half of D-04.

(1) `config.go`: add `VerdictStateChars string` with `koanf:"verdict_state_chars"`; doc line: per-record state length, in Unicode characters, sent to the provider for each side of a consolidate candidate pair.
(2) `registry.go`: add `{Key: "decisions.verdict_state_chars", Env: "ENGRAM_DECISIONS_VERDICT_STATE_CHARS", Default: "1500"}` after the threshold row; extend the comment.
(3) `validate.go`: in the `jev` block, `ParsePositiveIntCap(c.Decisions.VerdictStateChars)` appending `fmt.Errorf("ENGRAM_DECISIONS_VERDICT_STATE_CHARS %q: %w", ...)`.
(4) Tests: `decisionsFields` row; `VerdictStateChars: "1500"` in every full DecisionsConfig literal; the Validate cases above; docs-gate literal and message from 10 to 11 plus the new red-control row.
(5) `configure.md`:
- the table row for `ENGRAM_DECISIONS_VERDICT_STATE_CHARS` (default `1500`): how many characters of each record (its summary, then the head of its content) a consolidate verdict request sends; a positive integer.
- replace the sentence claiming that the features which ask the provider questions will arrive later and name this setting then: the provider is now asked by `engram spine-review consolidate`, which attaches an advisory relation verdict to each candidate pair by default whenever `ENGRAM_DECISIONS_PROVIDER` is set (`--no-verdicts` skips it); link to the CLI guide's consolidate section (`/guides/cli/#spine-review-consolidate`).
- in "What leaves your deployment", state that for consolidate each request carries both records' summary plus up to `ENGRAM_DECISIONS_VERDICT_STATE_CHARS` characters of content. Keep the TypeSafe, retention, zero-data-retention and `/alpha/decisions` text intact.

Commit as `feat(config): register ENGRAM_DECISIONS_VERDICT_STATE_CHARS and disclose what consolidate sends (D-09, D-04)` with the same six-file pathspec.
  </action>
  <verify>
    <automated>out=$(go test ./internal/config/ -run '^(TestDecisionsRegistryEntries|TestDecisionsValidate|TestDecisionsVarsDocumented)$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e '^--- PASS: (TestDecisionsRegistryEntries|TestDecisionsValidate|TestDecisionsVarsDocumented) ' | wc -l)" -eq 3</automated>
    <fails_when>non-zero exit, or fewer than three top-level PASS lines</fails_when>
    <automated>f=docs-site/src/content/docs/guides/configure.md; test "$(rg -o -F 'ship separately' "$f" | wc -l)" -eq 0 &amp;&amp; test "$(rg -o -F 'spine-review consolidate' "$f" | wc -l)" -ge 1 &amp;&amp; test "$(rg -o -F '| `ENGRAM_DECISIONS_VERDICT_STATE_CHARS` |' "$f" | wc -l)" -eq 1</automated>
    <fails_when>the old "ship separately" wording remains, consolidate is not named, or the state-chars row is missing or duplicated</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -o 'len[(]envs[)] != 11' internal/config/decisions_docs_test.go | wc -l` prints `1`.
    - `go test ./internal/config/ -count=1` exits 0 and `golangci-lint run ./internal/config/...` reports no findings.
    - For each of this plan's two commit SHAs, `git show --stat --format= <sha> -- charts/` prints nothing (Helm deliberately unchanged).
  </acceptance_criteria>
  <done>Both knobs are registered, validated only when the provider is set, documented, and the configuration reference names consolidate as a feature that sends record content; committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator env → config | untrusted string values for the threshold and state length |
| docs → operator decision | the operator decides whether to enable the provider from what configure.md discloses |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-12 | Tampering | `ParseProbability` | medium | mitigate | NaN, infinities and out-of-range values rejected (NaN would make every `p < threshold` comparison false, silently disabling needs_review); one exported parser shared by Validate, the resolver and the flag |
| T-03-01 | Information disclosure | configure.md disclosure | high | mitigate | the configuration reference names consolidate as the sending feature and states what each request carries and how much; existing TypeSafe/retention anchors stay gated by `TestDecisionsVarsDocumented` |
| T-03-13 | Denial of service | `VerdictStateChars` | low | accept | a very large value only makes a request exceed Jev's context and fail as `context_too_large` for that pair; the sweep still succeeds (D-10) |
</threat_model>

<verification>
- `go test ./internal/config/ -count=1` exits 0.
- `TestDecisionsVarsDocumented` asserts 11 registered decisions vars, all documented.
</verification>

<success_criteria>
- CUR-02's "configurable threshold (default 0.9)" exists as a registered, validated, documented key.
- D-09's truncation length is a registered, validated, documented knob.
</success_criteria>

<output>
Create `.planning/phases/03-curation-verdicts/03-02-SUMMARY.md` when done.
</output>
