---
phase: 03-curation-verdicts
plan: 05
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
  - 03-03
files_modified:
  - internal/server/decider.go
  - internal/server/decider_test.go
  - cmd/engram/spine_review_consolidate.go
  - cmd/engram/spine_review_consolidate_test.go
  - internal/surfaces/toolclass.go
  - cmd/engram/testdata/help.golden
  - cmd/engram/testdata/catalog.golden
autonomous: true
requirements:
  - CUR-01
  - CUR-02

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Tracer: `ENGRAM_DECISIONS_VERDICT_THRESHOLD` and `ENGRAM_DECISIONS_VERDICT_STATE_CHARS` resolve through `server.verdictSettings` (the shared `config.ParseProbability` / `config.ParsePositiveIntCap`, warn-and-default on an unparseable value) into consolidate's verdict pass, and `--verdict-threshold` overrides the threshold for one run; the resolved threshold is what `needs_review` and the top-level `verdict_threshold` reflect (D-08, D-09)"
    - "With both vars empty, the resolved settings equal `verdict.DefaultThreshold` and `verdict.DefaultStateChars` (TestVerdictSettingsDefaultsMatchVerdictPackage — the assumption-delta invariant that the registry default and the package fallback cannot drift)"
    - "`--verdict-threshold` values `-0.1`, `1.5`, `NaN` and `abc` exit `exitUsage` naming the flag, before any store or decider construction; a valid value with no provider configured changes nothing in the output"
    - "`--no-verdicts` with a provider configured makes zero `RecordStates` and zero decider calls, prints no verdict stderr lines, and produces stdout byte-identical to the no-provider run (D-04)"
    - "Before sending, consolidate prints one stderr disclosure line naming the provider, the model, the endpoint HOST only (never userinfo, path, query or any key), the per-record character bound, and `--no-verdicts`; after the pass it prints one stderr summary line with requested, answered, needs-review and unavailable counts plus per-class failure counts in sorted class order (D-10)"
    - "Mixed failures (an auth error, a missing record state, a timeout, a success) produce per-pair `verdict: {error: <class>}` objects beside the successful verdict, the summary line's counts, and exit status 0; when every request fails with `auth` an additional loud WARNING line names ENGRAM_DECISIONS_API_KEY and the ENGRAM_OPENAI_API_KEY it inherits, and the exit status is still 0 (D-10)"
    - "A `--timeout` deadline reached during the verdict pass leaves unanswered pairs with class `timeout`, keeps every structural candidate, and exits 0; a state-fetch error degrades every pair to `state_unavailable` and exits 0 (D-10, DEC-04)"
    - "250 candidate pairs produce 250 decision requests in one `DecideMany` call — no cap (D-11)"
    - "When the pass ran, the text headline appends a clause stating that verdicts are advisory and never merge or mutate, with answered, needs-review and unavailable counts; with no pass the headline is unchanged"
    - "consolidate's `--help` (Long text and flag usages) states that verdicts run by default only when ENGRAM_DECISIONS_PROVIDER is set, what each request sends, `--no-verdicts`, `--verdict-threshold` and its registered default, that `needs_review` marks a probability below the threshold, and that consolidate never merges or mutates; `--no-verdicts` and `--verdict-threshold` usages name each other"
    - "The `spine-review consolidate` blast-radius row stays ReadOnly, non-destructive, idempotent and OpenWorld false; its comment records that the optional advisory verdict pass sends record content to the configured provider and writes nothing"
  artifacts:
    - path: "internal/server/decider.go"
      provides: "verdictSettings resolving the registered knobs, VerdictSettings{Threshold, StateChars, Provider, Model, EndpointHost}, DeciderFromEnv"
      contains: "func DeciderFromEnv() (decide.Decider, VerdictSettings, error)"
    - path: "cmd/engram/spine_review_consolidate.go"
      provides: "--verdict-threshold, --no-verdicts, disclosure and summary lines, headline clause, Long help"
      contains: "\"no-verdicts\""
  key_links:
    - from: "internal/server/decider.go"
      to: "internal/config/validate.go"
      via: "the resolver uses the same exported parser Config.Validate uses (validated range equals enforced range)"
      pattern: "config[.]ParseProbability[(]cfg[.]Decisions[.]VerdictThreshold[)]"
    - from: "cmd/engram/spine_review_consolidate.go"
      to: "internal/config/validate.go"
      via: "the flag is parsed by the same probability parser"
      pattern: "config[.]ParseProbability[(]"
  prohibitions:
    - statement: "MUST NOT send any record content to a decision provider when the operator passes --no-verdicts, even with ENGRAM_DECISIONS_PROVIDER set"
      category: privacy
      status: resolved
      verification: test
      reason: "TestSpineReviewConsolidateNoVerdictsSuppresses asserts zero RecordStates and decider calls. No wired check descriptor by design (spec-less fallback, flagged-unverified)"
    - statement: "MUST NOT present a verdict as a decision or an action: a below-threshold verdict is marked needs_review, the headline and help state verdicts are advisory and that consolidate never merges or mutates, and no failure of the advisory pass changes the exit status"
      category: transparency
      status: resolved
      verification: judgment
      reason: "Headline clause, Long help and the failure-policy tests; wording reviewed at verify time. No wired check descriptor by design (spec-less fallback, flagged-unverified)"
---

<objective>
Turn plan 03-01's verdict slice into the operator-facing contract: registered knobs and the per-run flag, the opt-out, the failure and cost policy, the disclosure, and the help text.

- **Tracer (Task 1):** registered knob, then `server.verdictSettings`, then `--verdict-threshold` override, then `needs_review` and `verdict_threshold` in the JSON; plus `server.DeciderFromEnv` for the eval.
- **Task 2:** `--no-verdicts`, the stderr disclosure and summary lines, the all-auth warning, deadline and state-fetch degradation, no cap, and the headline clause.
- **Task 3:** `--help` Long text, the command's doc comment, the blast-radius comment, regenerated goldens.

Decisions implemented: D-04 (default-on with a provider, `--no-verdicts`), D-08 (threshold key plus `--verdict-threshold`), D-09 (state length knob), D-10 (failure objects, exit 0, stderr counts, loud all-auth warning), D-11 (no cap).
Output: resolver and eval seam in `internal/server`, consolidate flags and policy, help text, goldens.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-curation-verdicts/03-CONTEXT.md
@.planning/phases/03-curation-verdicts/03-RESEARCH.md
@.planning/phases/03-curation-verdicts/03-01-SUMMARY.md
@.planning/phases/03-curation-verdicts/03-02-SUMMARY.md
@internal/server/decider.go
@cmd/engram/spine_review_consolidate.go
</context>

<interfaces>
- From 03-01: `server.VerdictSettings{Threshold, StateChars}`, `server.verdictSettings(cfg)` (returns the verdict package defaults), `server.StoreAndDeciderFromEnv()`; `verdict.DefaultThreshold`, `verdict.DefaultStateChars`, `verdict.ErrorClass`, `verdict.Verdict.Failed()`; consolidate's `runVerdictPass`, `attachVerdicts`, `spineConsolidateStoreFromEnv` returning `(store, decider, settings, error)`, and the test fakes/helpers.
- From 03-02: `cfg.Decisions.VerdictThreshold`, `cfg.Decisions.VerdictStateChars`, `config.ParseProbability`, `config.ParsePositiveIntCap`.
- From 03-03: `requireSweepScope` is RunE's first statement; flags are validated after it.
- `internal/server/decider.go`: `decisionsConcurrency` (warn-and-default resolver shape) and `logDeciderEnabled` (host-only URL rendering, `api_key_source` naming, never the key).
- `internal/server/decider_test.go`: `TestDecisionsModelDefaultMatchesJev` is the registry-default-equals-package-constant precedent.
- `internal/surfaces/toolclass.go` lines ~349-356: the consolidate row `Class{ReadOnly: true, Destructive: false, Idempotent: true, OpenWorld: false}`.
- `cmd/engram/testdata/help.golden`, `catalog.golden`: regenerated by `task surfaces:gen`.
</interfaces>

## Artifacts this phase produces (plan 03-05's share)

| Kind | Artifact | Task |
|------|----------|------|
| Server | `VerdictSettings` gains `Provider`, `Model`, `EndpointHost`; `verdictSettings` resolves the knobs; `DeciderFromEnv()` | 1 |
| CLI flags | `--verdict-threshold` (string, empty means the registered value), `--no-verdicts` (bool) | 1, 2 |
| CLI | `verdictStats`, disclosure line, summary line, all-auth warning, `verdictHeadlineClause`, consolidate `Long` | 2, 3 |
| Tests | `TestVerdictSettingsDefaultsMatchVerdictPackage`, `TestVerdictSettingsResolvesKnobs`, `TestVerdictSettingsEndpointHostOnly`, `TestDeciderFromEnv`, `TestSpineReviewConsolidateVerdictThresholdFlag`, `TestSpineReviewConsolidateNoVerdictsSuppresses`, `TestSpineReviewConsolidateFailurePolicy`, `TestSpineReviewConsolidateAllAuthFailureWarns`, `TestSpineReviewConsolidateStateFetchErrorDegrades`, `TestSpineReviewConsolidateDeadlineDuringVerdictPass`, `TestSpineReviewConsolidateNoCapOnPairs`, `TestVerdictHeadlineClause` | 1, 2 |
| Comments / goldens | consolidate doc comment, toolclass row comment, `help.golden`, `catalog.golden` | 3 |

## Executor notes (every task)

- Sequential main tree; never any git stash subcommand; explicit pathspec; Conventional Commits; the session's attribution trailer.
- In cmd tests use a small in-file fake `decide.Decider` whose `DecideMany` delegates to `decide.DecideMany` with a per-request function, so ordering and context behavior are the production ones.
- Every stderr line goes through `cmd.PrintErrf`/`cmd.PrintErrln`; stdout stays one parseable document.
- Goldens: `task surfaces:gen`, then review `git status --porcelain` — only the two goldens may change.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Registered knobs and --verdict-threshold reach needs_review end to end; DeciderFromEnv for the eval</name>
  <files>internal/server/decider.go, internal/server/decider_test.go, cmd/engram/spine_review_consolidate.go, cmd/engram/spine_review_consolidate_test.go</files>
  <read_first>
    - `internal/server/decider.go` and `internal/server/decider_test.go` (post-03-01)
    - `internal/config/validate.go` (`ParseProbability`, `ParsePositiveIntCap`)
    - `cmd/engram/spine_review_consolidate.go` and its test file (post-03-03)
  </read_first>
  <behavior>
    - `TestVerdictSettingsDefaultsMatchVerdictPackage`: `config.Load(nil)` with both vars set to empty yields settings equal to `verdict.DefaultThreshold` and `verdict.DefaultStateChars`.
    - `TestVerdictSettingsResolvesKnobs`: `0.75` and `800` resolve as given; `2`, `NaN` and `abc` for the threshold and `0`/`abc` for state chars fall back to the defaults (and log a warning).
    - `TestVerdictSettingsEndpointHostOnly`: a base URL `https://user:secret@decisions.example.com:8443/openrouter?k=v` yields EndpointHost `decisions.example.com:8443`; the settings value contains no `secret`, `openrouter` or `k=v`.
    - `TestDeciderFromEnv`: provider empty returns a nil decider and a nil error with exactly one config load; provider `jev` plus a base URL returns a non-nil decider and settings with Provider `jev` and the configured model. Isolate from ambient `ENGRAM_DECISIONS_*` with `t.Setenv`.
    - `TestSpineReviewConsolidateVerdictThresholdFlag`: a fake decider answering `related` at 0.8 — default settings (0.9) give `needs_review: true` and `verdict_threshold: 0.9`; `--verdict-threshold 0.75` gives `needs_review: false` and `verdict_threshold: 0.75`; `--verdict-threshold 0.8` (exactly p) gives false; `-0.1`, `1.5`, `NaN`, `abc` exit `exitUsage` with the flag named and the constructor never called; `--verdict-threshold 0.5` with no decider produces the byte-identical no-provider stdout.
  </behavior>
  <action>
Implements D-08 (key plus flag) and D-09 (knob) through the resolver.

(1) `internal/server/decider.go`: extend `VerdictSettings` with `Provider`, `Model` and `EndpointHost` string fields. Rewrite `verdictSettings(cfg)` to resolve `Threshold` with `config.ParseProbability(cfg.Decisions.VerdictThreshold)` and `StateChars` with `config.ParsePositiveIntCap(cfg.Decisions.VerdictStateChars)`, each falling back to the verdict package default with a `slog.Warn` naming the var when set but unparseable (the `decisionsConcurrency` shape); `Provider` and `Model` from `cfg.Decisions`; `EndpointHost` as `url.Parse(cfg.Decisions.BaseURL).Host` only (empty on a parse error), the same host-only rule `logDeciderEnabled` follows. Add `func DeciderFromEnv() (decide.Decider, VerdictSettings, error)`: one `loadAndValidate()`, then `deciderFromConfig(cfg)` and `verdictSettings(cfg)` — no store (the curation eval in plan 03-07 needs no Qdrant). Doc comment: an empty provider returns a nil decider and nil error.
(2) Tests above in `internal/server/decider_test.go`.
(3) `cmd/engram/spine_review_consolidate.go`: register `--verdict-threshold` as a STRING flag with empty default (empty means use ENGRAM_DECISIONS_VERDICT_THRESHOLD — the parseMinScore reasoning: a float flag would advertise a default that is not the contract). Usage: the probability between 0 and 1 below which a verdict is marked needs_review; overrides ENGRAM_DECISIONS_VERDICT_THRESHOLD (default 0.9) for this run; no effect without ENGRAM_DECISIONS_PROVIDER or with --no-verdicts. Parse it in RunE after the scope guard, output format and `--min-score`, and before `spineConsolidateStoreFromEnv`, with `config.ParseProbability`; an error returns `usageErrorf` naming `--verdict-threshold` and the value. When set, it replaces `settings.Threshold` for the pass and for `attachVerdicts`.
(4) `TestSpineReviewConsolidateVerdictThresholdFlag` in the consolidate test file.

Commit as `feat(consolidate): resolve verdict knobs and add --verdict-threshold (D-08, D-09)` with pathspec `internal/server/decider.go internal/server/decider_test.go cmd/engram/spine_review_consolidate.go cmd/engram/spine_review_consolidate_test.go`.
  </action>
  <verify>
    <automated>out=$(go test ./internal/server/ -run '^(TestVerdictSettingsDefaultsMatchVerdictPackage|TestVerdictSettingsResolvesKnobs|TestVerdictSettingsEndpointHostOnly|TestDeciderFromEnv|TestStoreAndDeciderFromEnv)$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e '^--- PASS: (TestVerdictSettingsDefaultsMatchVerdictPackage|TestVerdictSettingsResolvesKnobs|TestVerdictSettingsEndpointHostOnly|TestDeciderFromEnv|TestStoreAndDeciderFromEnv) ' | wc -l)" -eq 5</automated>
    <fails_when>non-zero exit, or fewer than five PASS lines (TestStoreAndDeciderFromEnv skipping for lack of Qdrant also fails this)</fails_when>
    <automated>out=$(go test ./cmd/engram/ -run '^(TestSpineReviewConsolidateVerdictThresholdFlag|TestSpineReviewConsolidateVerdictTracer|TestSpineReviewConsolidateNoProviderByteIdentical)$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e '^--- PASS: TestSpineReviewConsolidate' | wc -l)" -eq 3</automated>
    <fails_when>non-zero exit or fewer than three PASS lines</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -o -e 'verdict[.]DefaultThreshold' internal/server/decider.go | wc -l` prints at least `1` (the fallback), and `rg -o -e 'config[.]ParseProbability[(]' internal/server/decider.go cmd/engram/spine_review_consolidate.go | wc -l` prints at least `2`.
    - `golangci-lint run ./internal/server/... ./cmd/engram/...` reports no findings.
  </acceptance_criteria>
  <done>The registered knobs and the flag drive needs_review end to end, the registry default and package fallback are pinned equal, and the eval has a store-free decider seam; committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: --no-verdicts, disclosure and summary lines, failure degradation, no cap, headline clause</name>
  <files>cmd/engram/spine_review_consolidate.go, cmd/engram/spine_review_consolidate_test.go</files>
  <read_first>
    - `cmd/engram/spine_review_consolidate.go` and its test file (post-Task-1)
    - `internal/verdict/verdict.go` (`ErrorClass`, `Failed`)
    - `.planning/phases/03-curation-verdicts/03-CONTEXT.md` D-04, D-10, D-11
  </read_first>
  <behavior>
    - `TestSpineReviewConsolidateNoVerdictsSuppresses`: with a counting fake decider and `--no-verdicts`, stdout equals the no-provider run's stdout byte for byte, RecordStates and decider call counts are 0, and stderr has no line starting `consolidate verdicts:`.
    - `TestSpineReviewConsolidateFailurePolicy`: four pairs — success, an `*decide.Error` with Kind auth, a pair whose second record is missing from the fake's states, and a timeout — give verdicts `{relation…}`, `{"error":"auth"}`, `{"error":"state_unavailable"}`, `{"error":"timeout"}` in pair order; the command returns nil (exit 0); stderr's summary line reports requested 4, answered 1, unavailable 3 and the three classes in sorted order; the disclosure line precedes it and names provider, model, the injected EndpointHost, the state-char bound and `--no-verdicts`.
    - `TestSpineReviewConsolidateAllAuthFailureWarns`: every pair fails with auth — exit 0, stderr contains a `WARNING` line naming `ENGRAM_DECISIONS_API_KEY` and `ENGRAM_OPENAI_API_KEY`; with one success among auth failures there is no WARNING line.
    - `TestSpineReviewConsolidateStateFetchErrorDegrades`: RecordStates returns an error — every pair's verdict is `{"error":"state_unavailable"}`, no decider call, exit 0.
    - `TestSpineReviewConsolidateDeadlineDuringVerdictPass`: a fake decider blocking until its context is done, with `--timeout 200ms` — the structural candidates are all present, each verdict is `{"error":"timeout"}`, exit 0.
    - `TestSpineReviewConsolidateNoCapOnPairs`: 250 fake pairs give exactly 250 requests in a single DecideMany call.
    - `TestVerdictHeadlineClause` (pure): answered/needs-review/unavailable counts render in the clause, which contains "advisory" and "never merges or mutates"; `consolidateSummary`'s existing output is untouched when no pass ran.
  </behavior>
  <action>
Implements D-04's opt-out, D-10's failure and reporting policy, and D-11.

(1) `--no-verdicts` bool flag. Usage: skip the advisory verdict pass even when ENGRAM_DECISIONS_PROVIDER is set — no record content is sent and no verdict objects appear (see --verdict-threshold). When set, RunE behaves exactly as with a nil decider.
(2) A pure `verdictStats(verdicts []verdict.Verdict)` returning requested, answered, needs-review and a per-class failure count map, and a pure `verdictHeadlineClause(stats)` string: verdicts are advisory and consolidate never merges or mutates, then the three counts. RunE appends the clause to `consolidateSummary`'s headline only when the pass ran; `consolidateSummary` itself does not change.
(3) Before `runVerdictPass`, when at least one pair exists, print one stderr disclosure line: `consolidate verdicts:` then requesting advisory verdicts for N candidate pair(s) from decisions provider P (model M) at host H, each request carrying both records' summary and up to C characters of content, and that `--no-verdicts` skips this. After the pass, print one stderr summary line starting `consolidate verdicts:` with requested, answered, needs_review and unavailable counts followed by `class=count` pairs in sorted class order. When requested is at least 1, answered is 0 and every failure class is `auth`, print an extra line starting `WARNING:` saying every verdict request failed authentication, naming ENGRAM_DECISIONS_API_KEY (or the ENGRAM_OPENAI_API_KEY it inherits) and the provider's route grant, and that the candidates are reported without verdicts. None of these lines changes the exit status (D-10).
(4) Confirm `runVerdictPass` (03-01) already degrades a RecordStates error and a missing side to `state_unavailable`, sends every buildable request in one DecideMany call, and that a raw deadline maps to `timeout` via `verdict.ErrorClass`; fix it here if a behavior test shows otherwise.
(5) The tests in `<behavior>`.

Commit as `feat(consolidate): add --no-verdicts and the advisory failure and disclosure policy (D-04, D-10, D-11)` with pathspec `cmd/engram/spine_review_consolidate.go cmd/engram/spine_review_consolidate_test.go`.
  </action>
  <verify>
    <automated>out=$(go test ./cmd/engram/ -run '^(TestSpineReviewConsolidateNoVerdictsSuppresses|TestSpineReviewConsolidateFailurePolicy|TestSpineReviewConsolidateAllAuthFailureWarns|TestSpineReviewConsolidateStateFetchErrorDegrades|TestSpineReviewConsolidateDeadlineDuringVerdictPass|TestSpineReviewConsolidateNoCapOnPairs|TestVerdictHeadlineClause)$' -count=1 -race -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e '^--- PASS: Test' | wc -l)" -eq 7</automated>
    <fails_when>non-zero exit, a data race report, or fewer than seven PASS lines</fails_when>
    <automated>go test ./cmd/engram/ -count=1</automated>
    <fails_when>non-zero exit (an existing consolidate, sweep-scope or operator-output test regressed)</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -o -e '"no-verdicts"' cmd/engram/spine_review_consolidate.go | wc -l` prints `1`.
    - `golangci-lint run ./cmd/engram/...` reports no findings.
  </acceptance_criteria>
  <done>The opt-out, disclosure, summary, all-auth warning, degradation paths and no-cap behavior are each a passing test, all exiting 0; committed.</done>
</task>

<task type="auto">
  <name>Task 3: Help text, doc comments and regenerated goldens</name>
  <files>cmd/engram/spine_review_consolidate.go, internal/surfaces/toolclass.go, cmd/engram/testdata/help.golden, cmd/engram/testdata/catalog.golden</files>
  <read_first>
    - `cmd/engram/spine_review_consolidate.go` (the command var, its doc comment, flag registrations)
    - `internal/surfaces/toolclass.go` lines 340-360
    - `cmd/engram/golden_test.go` and `Taskfile.yaml` `surfaces:gen`
  </read_first>
  <action>
Correct-by-reading deliverable for D-04/D-05/D-08/D-10 (project convention `4aksmneehh`: help text is a deliverable).

(1) Add a `Long` to `spineReviewConsolidateCmd`: consolidate ranks near-duplicate candidate pairs by stored-vector cosine score and never merges or mutates; one of `--scope` or `--all-scopes` is required; when ENGRAM_DECISIONS_PROVIDER is set each pair also gets an advisory relation verdict (duplicate, contradicts, updates, related, unrelated) with probabilities and a same-subject probability, computed by sending both records' summary and content head (ENGRAM_DECISIONS_VERDICT_STATE_CHARS, default 1500) to that provider; verdicts whose probability is below ENGRAM_DECISIONS_VERDICT_THRESHOLD (default 0.9, or `--verdict-threshold`) are marked needs_review; `--no-verdicts` skips the pass; a failed verdict is reported per pair and never changes the exit status; JSON is the stable contract. Rewrite the command var's Go doc comment the same way (the structural ranking itself never labels a pair; the advisory verdict is attached, never acted on).
(2) `internal/surfaces/toolclass.go`: extend the consolidate row's comment — the optional advisory verdict pass reads records and sends their summary and content head to the configured decisions provider, writes nothing, and verdict probabilities may vary run to run without any environment effect, so ReadOnly/non-destructive/idempotent/OpenWorld false still hold. Do not change the `Class` values.
(3) `task surfaces:gen`; review that only the two goldens changed and that the diff covers consolidate's Long and its two new flags.

Commit as `docs(consolidate): document advisory verdicts in --help and the blast-radius row` with pathspec `cmd/engram/spine_review_consolidate.go internal/surfaces/toolclass.go cmd/engram/testdata/help.golden cmd/engram/testdata/catalog.golden`.
  </action>
  <verify>
    <automated>out=$(go test ./cmd/engram/ -run '^(TestHelpGolden|TestCatalogGolden)$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; g=cmd/engram/testdata/help.golden &amp;&amp; for s in 'no-verdicts' 'verdict-threshold' 'ENGRAM_DECISIONS_PROVIDER' 'needs_review' 'never merges or mutates'; do test "$(rg -o -F -- "$s" "$g" | wc -l)" -ge 1 || { echo "help.golden missing: $s"; exit 1; }; done</automated>
    <fails_when>a golden test fails, or the help golden lacks any of the five required phrases</fails_when>
    <automated>go test ./internal/surfaces/... ./internal/server/ -run 'ToolClass|ToolAnnotation|Conform' -count=1</automated>
    <fails_when>non-zero exit (the blast-radius classification or its annotation tests changed)</fails_when>
  </verify>
  <acceptance_criteria>
    - After committing, `task surfaces:gen` leaves `git status --porcelain` empty.
    - `rg -A1 -F 'CLICommand: "spine-review consolidate"' internal/surfaces/toolclass.go | rg -o -F 'Class{ReadOnly: true, Destructive: false, Idempotent: true, OpenWorld: false}' | wc -l` prints `1`.
  </acceptance_criteria>
  <done>`--help`, the catalog and the blast-radius comment all state the advisory verdict contract; goldens regenerated and reviewed; committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engram → decisions provider | record summary and content head leave the deployment per pair |
| config (base URL, key) → stderr | the disclosure line must reveal where content goes without leaking credentials |
| provider errors → exit status | untrusted failures must not flip the sweep's result |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Information disclosure | verdict pass | high | mitigate | default-on only with a configured provider; `--no-verdicts` opt-out proven by a zero-call test; a per-run stderr disclosure names provider, model, host and the character bound before anything is sent |
| T-03-04 | Information disclosure | disclosure line / `VerdictSettings.EndpointHost` | medium | mitigate | host only (`url.URL.Host`), never userinfo, path, query or key; `TestVerdictSettingsEndpointHostOnly` plants all three and asserts their absence |
| T-03-05 | Denial of service | unbounded pair count | low | accept | D-11 declines a cap; in-flight calls are bounded by ENGRAM_DECISIONS_CONCURRENCY, wall-clock by `--timeout`, and cost is about $0.00005 per pair |
| T-03-16 | Tampering | threshold input | medium | mitigate | the flag and the registered var go through `config.ParseProbability` (rejects NaN, infinities and out-of-range) before any construction |
| T-03-17 | Repudiation | silent verdict failure | low | mitigate | per-pair error classes in JSON, a stderr summary with per-class counts, and a loud all-auth WARNING, while the exit status stays 0 (D-10) |
</threat_model>

<verification>
- `go test ./internal/server/ ./cmd/engram/ ./internal/surfaces/... -count=1` exits 0.
- `task surfaces:gen` is a no-op after the final commit.
</verification>

<success_criteria>
- CUR-01/CUR-02: verdicts default-on with a provider, suppressible, threshold configurable by registered key and per-run flag, failures reported per pair without failing the sweep.
- D-04, D-08, D-09, D-10, D-11 each proven by a named test.
</success_criteria>

<output>
Create `.planning/phases/03-curation-verdicts/03-05-SUMMARY.md` when done.
</output>
