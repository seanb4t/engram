---
phase: 03-curation-verdicts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/verdict/verdict.go
  - internal/verdict/verdict_test.go
  - internal/store/boundedread.go
  - internal/store/verdictstate.go
  - internal/store/verdictstate_test.go
  - internal/server/decider.go
  - internal/server/decider_test.go
  - cmd/engram/spine_review_consolidate.go
  - cmd/engram/spine_review_consolidate_test.go
autonomous: true
requirements:
  - CUR-01
  - CUR-02

estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Tracer: with a decider configured, `engram spine-review consolidate --all-scopes --output json` fetches both records' state through `Store.RecordStates`, sends ONE Decisions request per candidate pair carrying the five-option `relation` Choice and the `same_subject` Noul (D-06), and each candidate row gains a nested `verdict` object with exactly the keys `relation`, `probabilities`, `same_subject`, `needs_review`, `model`; `probabilities` has exactly the keys `duplicate`, `contradicts`, `updates`, `related`, `unrelated` in that order (D-05)"
    - "With no decider (ENGRAM_DECISIONS_PROVIDER empty), consolidate's stdout is byte-identical to `json.Marshal(consolidateDoc(...))` plus a newline, contains no `verdict` key, `RecordStates` is never called, and stderr carries only the existing progress lines (D-04)"
    - "A failed pair decision renders `verdict: {\"error\": \"<class>\"}` with exactly one key, where the class comes from `verdict.ErrorClass` (decide.Status's vocabulary, plus `state_unavailable` and a raw deadline mapped to `timeout`); the sweep still exits 0 (D-10, DEC-04)"
    - "The top-level `verdict_threshold` key is present exactly when the verdict pass ran, carrying the threshold that produced every `needs_review` flag; absent otherwise (additive-only operator JSON, D-05)"
    - "`needs_review` is `p` strictly below the threshold, where `p` is `probabilities[relation]` and `relation` is the provider's own choice (never re-derived locally). At `p` exactly equal to the threshold `needs_review` is false; one ulp below it is true; threshold 0 never flags; threshold 1 flags every `p` under 1.0. No probability is rounded, clamped or renormalized (D-05, D-08; spec-less edges CUR-02 boundary and precision)"
    - "Per-record state is the stored summary (when non-empty) followed by the head of the content, truncated to N Unicode code points (default 1500, D-09). Truncation never splits a multi-byte UTF-8 sequence; invalid UTF-8 bytes become U+FFFD and count as one code point (spec-less edge CUR-01 encoding)"
    - "The more recently created record of a pair is always sent as `record_b` (tie: the lexically larger id), so D-06's `updates` criterion ('B is a newer state…') is meaningful regardless of the pair's lexical A/B order; the JSON row keeps its existing `a`/`b` order"
    - "Zero candidate pairs make zero `RecordStates` calls and zero decider calls; the JSON `candidates` array stays `[]` (spec-less edge CUR-01 empty)"
    - "`Store.RecordStates` reuses `fetchPayloadsByID` with a new budgeted `verdictStateView` (content, summary, created_at; ceiling = ContentBytes + summary term + uncappedFieldsAllowance = 82432 bytes at default caps, 25 records per RPC), is Subject-less like `NearDuplicates` (no scope, owner or recall filter), deduplicates ids, omits unknown ids without error, and issues no write RPC"
    - "Every candidate pair with both states available is sent to the decider — no cap on pair count; in-flight calls are bounded only by `DecideMany`'s configured concurrency (D-11)"
    - "FLAGGED ASSUMPTION (planner discretion): the failure class `state_unavailable` (a record in a candidate pair could not be fetched, or the state fetch itself failed) is new vocabulary beside decide.Status's classes; the top-level `verdict_threshold` key and the fixed `context` state sentence are planner choices under CONTEXT's Claude's-discretion clause"
  artifacts:
    - path: "internal/verdict/verdict.go"
      provides: "relation constants, Relations, RelationCriteria, question names, State, Side, PairRequest, NewRequest, Probabilities, Verdict, FromResult, Unavailable, ErrStateUnavailable, ErrorClass, DefaultThreshold, DefaultStateChars"
      contains: "func FromResult(res decide.Result, threshold float64) Verdict"
    - path: "internal/store/verdictstate.go"
      provides: "RecordState, Store.RecordStates"
      contains: "func (s *Store) RecordStates(ctx context.Context, ids []string) (map[string]RecordState, error)"
    - path: "internal/server/decider.go"
      provides: "VerdictSettings, verdictSettings, StoreAndDeciderFromEnv"
      contains: "func StoreAndDeciderFromEnv() (*store.Store, decide.Decider, VerdictSettings, error)"
    - path: "cmd/engram/spine_review_consolidate.go"
      provides: "consolidateVerdictDoc, runVerdictPass, attachVerdicts, verdict wiring in RunE"
      contains: "func runVerdictPass("
  key_links:
    - from: "cmd/engram/spine_review_consolidate.go"
      to: "internal/verdict/verdict.go"
      via: "request building and result mapping go through the shared verdict package, so the eval measures the same question set (D-06)"
      pattern: "verdict[.]PairRequest[(]"
    - from: "cmd/engram/spine_review_consolidate.go"
      to: "internal/decide/decide.go"
      via: "every pair goes through the bounded, order-preserving batch call (D-11)"
      pattern: "[.]DecideMany[(]ctx"
    - from: "internal/store/verdictstate.go"
      to: "internal/store/searchfetch.go"
      via: "the state fetch reuses the budgeted by-id fetch and its batch-of-1 fallback"
      pattern: "fetchPayloadsByID[(]ctx, nil, s[.]verdictStateView[(][)]"
    - from: "internal/server/decider.go"
      to: "internal/server/tools.go"
      via: "one config load feeds both the store and the decider"
      pattern: "loadAndValidate[(][)]"
    - from: "cmd/engram/spine_review_consolidate.go"
      to: "internal/server/decider.go"
      via: "consolidate builds store, decider and settings through the single wiring seam"
      pattern: "server[.]StoreAndDeciderFromEnv[(][)]"
  prohibitions:
    - statement: "MUST NOT mutate, supersede, archive or delete any record because of a verdict, at any confidence, on any path — consolidate's store surface exposes read methods only and the state fetch issues no write RPC"
      category: safety
      status: resolved
      verification: test
      reason: "TestConsolidateStoreSurfaceIsReadOnly (reflection over the interface's method set) and TestRecordStatesDoesNotMutate (before/after point count and payload digest). No wired check descriptor by design (spec-less fallback, flagged-unverified)"
    - statement: "MUST NOT send any record content to a decision provider when ENGRAM_DECISIONS_PROVIDER is unset; with no provider the command makes zero outbound calls and its output is byte-identical to today"
      category: privacy
      status: resolved
      verification: test
      reason: "TestSpineReviewConsolidateNoProviderByteIdentical asserts zero RecordStates calls and a byte-exact stdout. No wired check descriptor by design (spec-less fallback, flagged-unverified)"
---

<objective>
Prove the curation-verdict architecture end to end on one thin path, then harden its two new layers.

- **Tracer (Task 1):** consolidate's RunE, then the single wiring seam (`server.StoreAndDeciderFromEnv`), then a budgeted state fetch (`Store.RecordStates`), then the shared `internal/verdict` package (D-06 question set, D-09 state, pair ordering), then `DecideMany` against a real `jev` client aimed at an httptest Decisions endpoint, then back to a nested `verdict` object on each JSON candidate row (D-05). The no-provider path stays byte-identical (D-04).
- **Store expansion (Task 2):** the state fetch's view, ceiling, scope-span, empty-input and no-mutation guarantees, against the Qdrant testcontainer.
- **Verdict edges (Task 3, TDD):** malformed answers, the threshold boundary, truncation encoding, and error-class mapping.

Decisions implemented: D-04 (nil decider means no pass, byte-identical), D-05 (nested verdict, success and failure shapes, no separate probability field), D-06 (five options plus `same_subject`, one request per pair), D-08 (needs_review against a threshold; the registered knob and flag arrive in plans 03-02 and 03-05 and override the default this plan resolves to), D-09 (summary plus content head, 1500-code-point default), D-10 (failure object, sweep still exits 0), D-11 (every pair, bounded concurrency).

Purpose: catch an architectural dead end (store boundary, wiring seam, shared contract, transport, JSON shape) after one commit, before the controls, text view and eval build on it.
Output: `internal/verdict`, `Store.RecordStates`, `server.StoreAndDeciderFromEnv`, and the consolidate verdict pass with tests.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-curation-verdicts/03-CONTEXT.md
@.planning/phases/03-curation-verdicts/03-RESEARCH.md
@.planning/phases/03-curation-verdicts/03-PATTERNS.md
@.claude/skills/spike-findings-engram/references/curation-verdicts.md
@.claude/skills/spike-findings-engram/references/decision-transport.md
@internal/decide/decide.go
@internal/decide/errors.go
@internal/store/searchfetch.go
@cmd/engram/spine_review_consolidate.go
</context>

<interfaces>
Existing code (verified at planning time):

- `internal/decide/decide.go`: `Decider{Decide; DecideMany(ctx, []Request) []Result}`; `State map[string]any`; `Choice(instructions, options map[string]string)`, `Noul(instructions, whenTrue, whenFalse)`; `Request{State, Questions map[string]Question}`; `Answer{Type, Probability (noul P(true)), Choice, Probabilities map[string]float64, Confidence *float64}`; `Result{Response, Err}`; `Response{Answers, Model (dated snapshot), ID, Provider, Usage}`.
- `internal/decide/errors.go`: `Status(err) string` returns `ok`, `canceled`, `context_too_large`, `auth`, `bad_request`, `rate_limited`, `unavailable`, `timeout`, `response_too_large`, `malformed_response`, `invalid_request`, or `error`. A raw `context.DeadlineExceeded` (what `DecideMany` stores for items it never started once ctx is done) falls through to `error`.
- `internal/decide/jev/jev.go`: `func New(baseURL, apiKey, model string, opts ...Option) *Client`; the endpoint is `{baseURL}/alpha/decisions`. `decodeResponse` already rejects a missing answer or a type mismatch as `malformed_response`, but does NOT check that a choice is one of the requested options or that every option has a probability.
- `internal/store/searchfetch.go`: `func (s *Store) fetchPayloadsByID(ctx, f *qdrant.Filter, view readView, ids []string) (map[string]Memory, error)` — empty ids return an empty map with no RPC; a nil `f` narrows by id only; batches of `perRPCLimit(view.maxRecordBytes)`; batch-of-1 fallback on `ErrResponseTooLarge`; an id absent from the response is absent from the map.
- `internal/store/boundedread.go`: `readView{selector, maxRecordBytes}`, `summaryTerm(c)`, `uncappedFieldsAllowance` (16384), `perRPCLimit`, `citationsView()`/`nearDuplicateIdentityView()` as the sibling pattern. `DefaultRecordCaps()` has ContentBytes 65536, SummaryBytes 512.
- `internal/store/spine.go`: `DuplicatePair{A, B, AShortID, BShortID, AScope, BScope, Score}` (content deliberately absent); `NearDuplicates` enumerates with a scope-only filter (no recall gate), so pairs can include superseded or archived records. `fromPayload` decodes `created_at` into `Memory.CreatedAt`.
- `internal/server/decider.go`: `deciderFromConfig(cfg) (decide.Decider, error)` returns `(nil, nil)` for an empty provider. `internal/server/tools.go`: `loadAndValidate()` (the `configLoad` seam counts loads), `ensureStoreFromConfig(cfg)`, and `StoreAndSummarizerFromEnv` as the combined-constructor precedent (do NOT copy its "model empty ⇒ error" branch).
- `cmd/engram/spine_review_consolidate.go`: `spineConsolidateStore{NearDuplicates}`, `spineConsolidateStoreFromEnv` (package var, test-substitutable), `consolidatePairDoc`, `consolidateReportDoc` (MinScore pointer + omitempty precedent), `consolidateDoc`, `consolidateSummary`, RunE. `cmd/engram/spine_review_consolidate_test.go`: `spineConsolidateFakeStore`, `withFakeConsolidateStore`, `runClient`, `resetClientFlags`.
- `internal/server/decider_test.go`: `tracerNoulResponse` and the httptest Jev pattern; `internal/server/tools_test.go` `TestBuildDepsFromEnvLoadsConfigOnce` is the load-count and ambient-env isolation precedent.
</interfaces>

## Artifacts this phase produces (plan 03-01's share)

| Kind | Artifact | Task |
|------|----------|------|
| Package | `internal/verdict` — `Duplicate`/`Contradicts`/`Updates`/`Related`/`Unrelated`, `Relations()`, `RelationCriteria()`, `QuestionRelation`, `QuestionSameSubject`, `RelationInstructions`, `SameSubjectInstructions`/`SameSubjectTrue`/`SameSubjectFalse`, `StateContext`, `DefaultThreshold` (0.9), `DefaultStateChars` (1500), `State`, `Side`, `PairRequest`, `NewRequest`, `Probabilities` (+`Of`), `Verdict` (+`Failed`), `FromResult`, `Unavailable`, `ErrStateUnavailable`, `ErrorClass` | 1, 3 |
| Store | `verdictStateRecordCeiling`, `(*Store).verdictStateView`, `RecordState`, `(*Store).RecordStates` | 1, 2 |
| Server | `VerdictSettings{Threshold, StateChars}`, `verdictSettings(cfg)`, `StoreAndDeciderFromEnv()` | 1 |
| CLI | `spineConsolidateStore.RecordStates`, `spineConsolidateStoreFromEnv` returning `(store, decider, settings, error)`, `consolidateVerdictDoc`, `consolidateProbabilitiesDoc`, `consolidatePairDoc.Verdict`, `consolidateReportDoc.VerdictThreshold`, `runVerdictPass`, `attachVerdicts` | 1 |
| Tests | `TestSpineReviewConsolidateVerdictTracer`, `TestSpineReviewConsolidateNoProviderByteIdentical`, `TestConsolidateStoreSurfaceIsReadOnly`, `TestRecordStatesFetchesState`, `TestStoreAndDeciderFromEnv`, `TestRecordStatesEmptyIDs`, `TestRecordStatesSpansScopesAndStates`, `TestRecordStatesDoesNotMutate`, `TestVerdictStateViewCeiling`, the `internal/verdict` edge suite | 1-3 |

## Flagged assumptions (spec-less probe)

- CUR-01 empty/encoding and CUR-02 boundary/precision are resolved explicitly as truths above and tested in Tasks 1 and 3.
- `state_unavailable`, the top-level `verdict_threshold`, the fixed `StateContext` sentence, and "newer record is `record_b`" are planner decisions under Claude's discretion (CONTEXT: exact Go names beyond the JSON keys). They add no JSON key inside the locked D-05 verdict shape.
- D-06's option criteria are used verbatim, including the overlap between `duplicate` ("one may be more complete") and `updates` ("a more complete version"). This is a locked decision; the eval (plans 03-04, 03-07, 03-08) measures it rather than this plan re-wording it.

## Executor notes (every task)

- Execution is sequential on the main tree. NEVER run any git stash subcommand. Commit each task with an explicit pathspec, Conventional Commits, and end the message with the session's attribution trailer lines.
- New Go files carry the Apache-2.0 SPDX header (`task license:add` or by hand). `.planning/**` files never do.
- `internal/store` must not import `internal/decide`, `internal/verdict`, `internal/server` or `internal/embed`. `internal/verdict` imports only `internal/decide` and the standard library.
- Every probability comparison is a strict `<` against the threshold; never compare probabilities with exact equality in production logic (±0.03 run-to-run variance, decision-transport.md).

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end verdict on one candidate pair — state fetch, shared question set, DecideMany, nested JSON verdict</name>
  <files>internal/verdict/verdict.go, internal/store/boundedread.go, internal/store/verdictstate.go, internal/store/verdictstate_test.go, internal/server/decider.go, internal/server/decider_test.go, cmd/engram/spine_review_consolidate.go, cmd/engram/spine_review_consolidate_test.go</files>
  <read_first>
    - `cmd/engram/spine_review_consolidate.go` and `cmd/engram/spine_review_consolidate_test.go` (whole files)
    - `internal/decide/decide.go`, `internal/decide/errors.go` (`Status`), `internal/decide/many.go`
    - `internal/store/searchfetch.go` (`fetchPayloadsByID`, `includeIDs` with a nil filter)
    - `internal/store/boundedread.go` lines 195-365 (ceilings, `perRPCLimit`, views)
    - `internal/store/spine.go` lines 430-500 (`NearDuplicateOptions`, `DuplicatePair`) and `internal/store/spine_test.go` lines 25-100 (`newSpineTestStore`, `seedSpineMemoryVector`, `snapshotCollection`)
    - `internal/server/decider.go` (whole file), `internal/server/tools.go` lines 195-270 and 690-715, `internal/server/decider_test.go` lines 1-60, `internal/server/tools_test.go` lines 5163-5200
    - `.claude/skills/spike-findings-engram/references/curation-verdicts.md` (criteria text, request shape)
  </read_first>
  <behavior>
    - `TestSpineReviewConsolidateVerdictTracer` (cmd/engram): a fake store returns one pair (A = `id-a`, B = `id-b`) and states where `id-a` is the NEWER record; the decider is a real `jev.New(srv.URL+"/api", "test-key", "")` whose httptest handler records the request body and replies with a `relation` choice answer (`duplicate`, all five probabilities) and a `same_subject` noul answer (0.97) plus model `typesafe/jev-1.13-20260917`. Running `spine-review consolidate --all-scopes --output json` exits 0; the recorded request has `state.record_b` equal to `id-a`'s state text and `state.record_a` equal to `id-b`'s, a `relation` question whose criteria carry exactly the five option keys, and a `same_subject` noul question; the output's `candidates[0].verdict` decodes to exactly the five keys with `relation` `duplicate`, `needs_review` false, `model` as sent, and top-level `verdict_threshold` 0.9.
    - `TestSpineReviewConsolidateNoProviderByteIdentical`: with a nil decider, stdout equals `json.Marshal(consolidateDoc(pairs, "", true, nil, 5, scanned, queried))` plus a newline byte for byte, stdout and stderr contain no `verdict`, and the fake's RecordStates call count is 0.
    - `TestConsolidateStoreSurfaceIsReadOnly`: the method set of `spineConsolidateStore` (via reflection) is exactly `{NearDuplicates, RecordStates}`.
    - `TestRecordStatesFetchesState` (internal/store, Qdrant): seeds a record with a summary and one without; `RecordStates(ctx, [a, b, a, <unknown uuid>])` returns exactly two entries with the stored Summary, Content and CreatedAt (to the second), the unknown id absent, and a nil error.
    - `TestStoreAndDeciderFromEnv` (internal/server, Qdrant): with ENGRAM_DECISIONS_PROVIDER empty it returns a non-nil store, a nil decider, a nil error, and settings equal to `verdict.DefaultThreshold`/`verdict.DefaultStateChars`; with provider `jev` and a base URL it returns a non-nil decider; each call loads config exactly once through the `configLoad` seam. Isolate from ambient `ENGRAM_DECISIONS_*` with `t.Setenv` exactly as `TestBuildDepsFromEnvLoadsConfigOnce` does.
    - RED first: commit order is free, but record in the SUMMARY that the cmd tracer test failed (compile or assertion) before the RunE wiring existed.
  </behavior>
  <action>
Implements D-04, D-05, D-06, D-09, D-10 and D-11 on one path.

(1) New `internal/verdict/verdict.go` (package `verdict`, SPDX header). Package doc: the advisory relation-verdict contract spine-review consolidate and the curation eval share — the D-06 question set, the D-09 per-record state, pair ordering, and mapping a `decide.Result` to a `Verdict`; no I/O; never acts on a verdict. Contents:
- Relation name constants `Duplicate`, `Contradicts`, `Updates`, `Related`, `Unrelated` (lowercase values) and `func Relations() []string` returning a fresh slice in exactly that order (D-05's key order).
- `func RelationCriteria() map[string]string` returning a fresh map with the spike blueprint's criteria verbatim for duplicate, contradicts, related and unrelated (curation-verdicts.md "How to Build It" step 1) and, for updates, exactly "B is a newer state or a more complete version of the same fact." (D-06).
- Constants `QuestionRelation` = "relation", `QuestionSameSubject` = "same_subject", `RelationInstructions` = "How does record_b relate to record_a?", `SameSubjectInstructions` = "Are both records about the same specific subject?", `SameSubjectTrue` = "both records are about the same specific subject", `SameSubjectFalse` = "the records are about different subjects", and `StateContext`, one fixed sentence carrying no record content, stating that record_a and record_b are two entries from a software team's durable project memory and that record_b was created after record_a.
- `DefaultThreshold` = 0.9 (D-08) and `DefaultStateChars` = 1500 (D-09), each with a doc comment saying plan 03-02 registers the knob whose default must equal it.
- `func State(summary, content string, maxChars int) string`: summary, a blank line, then content when summary is non-empty; content alone otherwise; the result truncated to `maxChars` Unicode code points (iterate runes; never slice bytes); `maxChars` at or below zero uses `DefaultStateChars`.
- `type Side struct { ID string; CreatedAt time.Time; Summary, Content string }` and `func PairRequest(x, y Side, maxChars int) decide.Request`: the side with the later CreatedAt becomes record_b (equal times: the lexically larger ID becomes record_b), then delegates to `NewRequest`.
- `func NewRequest(recordA, recordB string) decide.Request`: State keys `context` (StateContext), `record_a`, `record_b`; Questions `relation` = `decide.Choice(RelationInstructions, RelationCriteria())` and `same_subject` = `decide.Noul(SameSubjectInstructions, SameSubjectTrue, SameSubjectFalse)`. One request per pair (D-06).
- `type Probabilities struct { Duplicate, Contradicts, Updates, Related, Unrelated float64 }` with `func (p Probabilities) Of(relation string) (float64, bool)`.
- `type Verdict struct { Relation string; Probabilities Probabilities; SameSubject float64; NeedsReview bool; Model string; ErrorClass string }` with `func (v Verdict) Failed() bool` (ErrorClass non-empty).
- `var ErrStateUnavailable = errors.New("record state unavailable")`; `func ErrorClass(err error) string`: `ErrStateUnavailable` gives `state_unavailable`; a raw `context.DeadlineExceeded` gives `timeout`; everything else gives `decide.Status(err)`. `func Unavailable(err error) Verdict` returns a Verdict carrying only that class.
- `func FromResult(res decide.Result, threshold float64) Verdict`: a non-nil `res.Err` returns `Unavailable(res.Err)`; otherwise copy the relation answer's Choice and its five probabilities verbatim (no rounding, clamping or renormalizing), `SameSubject` from the same_subject noul's Probability, `Model` from `res.Response.Model`, and `NeedsReview` = `probabilities[relation]` strictly below `threshold` (D-05: `probabilities[relation]` is the single source of truth; D-08). Task 3 adds malformed-answer detection.

(2) `internal/store/boundedread.go`: add `verdictStateRecordCeiling(c RecordCaps) int` = `c.ContentBytes + summaryTerm(c) + uncappedFieldsAllowance`, and `(s *Store) verdictStateView() readView` including exactly `content`, `summary`, `created_at`, placed beside `citationsView`, with a doc comment naming its one caller.

(3) New `internal/store/verdictstate.go` (SPDX): `type RecordState struct { ID, Summary, Content string; CreatedAt time.Time }` with a doc comment in `DuplicatePair`'s style stating what it carries and what it deliberately excludes (no tags, citations, owner, scope — this fetch only feeds the verdict pass's truncated state, it is not a second `Get`). `func (s *Store) RecordStates(ctx context.Context, ids []string) (map[string]RecordState, error)`: deduplicate ids keeping first-seen order, call `s.fetchPayloadsByID(ctx, nil, s.verdictStateView(), deduped)`, and map each returned `Memory` to a `RecordState`. Doc comment: Subject-less operator-tier read exactly like `NearDuplicates` (no scope, owner or recall-gate filter, because pairs can include superseded or archived records), unknown ids are simply absent, and it issues no write RPC.

(4) `internal/server/decider.go`: `type VerdictSettings struct { Threshold float64; StateChars int }`; unexported `verdictSettings(cfg *config.Config) VerdictSettings` returning `verdict.DefaultThreshold` and `verdict.DefaultStateChars` (plan 03-05 extends this same function to read the knobs plan 03-02 registers, falling back to these values); `func StoreAndDeciderFromEnv() (*store.Store, decide.Decider, VerdictSettings, error)` calling `loadAndValidate()` once, then `ensureStoreFromConfig(cfg)`, then `deciderFromConfig(cfg)`. An empty provider returns a nil decider and a nil error — never copy `StoreAndSummarizerFromEnv`'s "feature unset ⇒ error" branch (D-04).

(5) `cmd/engram/spine_review_consolidate.go`:
- `spineConsolidateStore` gains `RecordStates(ctx context.Context, ids []string) (map[string]store.RecordState, error)`; keep the name `spineConsolidateStoreFromEnv` (`migrate_family.go` cites it) but change its signature to return `(spineConsolidateStore, decide.Decider, server.VerdictSettings, error)` via `server.StoreAndDeciderFromEnv()`, returning a nil interface (never a typed nil) for both store and decider on error.
- `consolidateVerdictDoc` whose JSON success form has exactly `relation`, `probabilities`, `same_subject`, `needs_review`, `model` (needs_review false and an empty model string must still be emitted) and whose failure form has exactly `error`; `consolidateProbabilitiesDoc` with fields in D-05 order. Choose the mechanism (pointer fields with omitempty, or a MarshalJSON) and prove the two key sets in the tracer test.
- `consolidatePairDoc` gains `Verdict *consolidateVerdictDoc` tagged `json:"verdict,omitempty"`; rewrite its doc comment: the structural fields are never normalised, bucketed or labelled, and the optional Verdict is an advisory object present only when the verdict pass ran (D-05). `consolidateReportDoc` gains `VerdictThreshold *float64` tagged `json:"verdict_threshold,omitempty"` directly after `min_score`, reusing MinScore's presence-means-applied reasoning.
- `runVerdictPass(ctx context.Context, st spineConsolidateStore, dec decide.Decider, pairs []store.DuplicatePair, s server.VerdictSettings) []verdict.Verdict`: returns an empty slice and makes no call when pairs is empty; otherwise collects both ids of every pair, calls `st.RecordStates` once (on error every pair gets `verdict.Unavailable(fmt.Errorf("...: %w", verdict.ErrStateUnavailable))`), builds one `verdict.PairRequest` per pair whose two states are present (a missing side gives that pair `Unavailable` with `ErrStateUnavailable`), sends every built request in ONE `dec.DecideMany(ctx, reqs)` call, and maps results back by index with `verdict.FromResult(res, s.Threshold)`. Order is the pair order (DecideMany preserves input order).
- `attachVerdicts(doc consolidateReportDoc, verdicts []verdict.Verdict, threshold float64) consolidateReportDoc`: pure; sets `VerdictThreshold` and each candidate's `Verdict` index-aligned.
- RunE: after `NearDuplicates` succeeds, when the decider is non-nil run the pass and attach; when nil, build the doc exactly as today. No mutating store method is reachable from this file.

(6) Tests per `<behavior>`: update `spineConsolidateFakeStore` with a `states` map, a states error and a RecordStates call counter; update `withFakeConsolidateStore` to return `(fake, nil, server.VerdictSettings{}, nil)` so every existing test keeps the no-provider path; add a helper that injects a decider and settings. New `internal/store/verdictstate_test.go` (SPDX) using `newSpineTestStore`/`seedSpineMemoryVector`. Add `TestStoreAndDeciderFromEnv` to `internal/server/decider_test.go`.

Commit as `feat(consolidate): attach advisory relation verdicts to candidate pairs (D-04..D-06, D-09..D-11)` with pathspec `internal/verdict/verdict.go internal/store/boundedread.go internal/store/verdictstate.go internal/store/verdictstate_test.go internal/server/decider.go internal/server/decider_test.go cmd/engram/spine_review_consolidate.go cmd/engram/spine_review_consolidate_test.go`.
  </action>
  <verify>
    <automated>out=$(go test ./cmd/engram/ -run '^(TestSpineReviewConsolidateVerdictTracer|TestSpineReviewConsolidateNoProviderByteIdentical|TestConsolidateStoreSurfaceIsReadOnly)$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e '^--- PASS: (TestSpineReviewConsolidateVerdictTracer|TestSpineReviewConsolidateNoProviderByteIdentical|TestConsolidateStoreSurfaceIsReadOnly) ' | wc -l)" -eq 3</automated>
    <fails_when>non-zero exit, or fewer than three top-level PASS lines (a tracer test missing, skipped or failing)</fails_when>
    <automated>out=$(go test ./internal/store/ -run '^TestRecordStatesFetchesState$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; printf '%s\n' "$out" | rg -q -e '^--- PASS: TestRecordStatesFetchesState '</automated>
    <fails_when>non-zero exit, or no PASS line for TestRecordStatesFetchesState (a SKIP because Qdrant was unavailable also fails this check)</fails_when>
    <automated>out=$(go test ./internal/server/ -run '^TestStoreAndDeciderFromEnv$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; printf '%s\n' "$out" | rg -q -e '^--- PASS: TestStoreAndDeciderFromEnv '</automated>
    <fails_when>non-zero exit, or no PASS line for TestStoreAndDeciderFromEnv</fails_when>
    <automated>go build ./... &amp;&amp; go vet ./internal/verdict/ ./internal/store/ ./internal/server/ ./cmd/engram/ &amp;&amp; go test ./cmd/engram/ -run 'Consolidate' -count=1</automated>
    <fails_when>a build or vet error, or any existing consolidate test failing after the constructor signature change</fails_when>
  </verify>
  <acceptance_criteria>
    - `test "$(go list -deps ./internal/store/ | rg -o -x -e 'github.com/seanb4t/engram/internal/(decide|verdict|server|embed)' | wc -l)" -eq 0` exits 0 (the store boundary holds).
    - `test "$(go list -deps ./internal/verdict/ | rg -o -x -e 'github.com/seanb4t/engram/internal/[a-z/]+' | rg -v -x -e 'github.com/seanb4t/engram/internal/(decide|verdict)' | wc -l)" -eq 0` exits 0 (the verdict package depends on no other engram package than `internal/decide`).
    - `rg -o 'func RecordStates|func [(]s [*]Store[)] RecordStates' internal/store/verdictstate.go | wc -l` prints `1`.
    - `task license:check` exits 0 and `golangci-lint run ./internal/verdict/... ./internal/store/... ./internal/server/... ./cmd/engram/...` reports no findings.
  </acceptance_criteria>
  <done>One candidate pair flows from the CLI through the seam, the budgeted state fetch, the shared question set and a real Jev client to a nested JSON verdict; the no-provider path is byte-identical; RED then GREEN recorded; committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: State fetch guarantees — view ceiling, empty input, scope and state span, no mutation</name>
  <files>internal/store/verdictstate_test.go, internal/store/verdictstate.go</files>
  <read_first>
    - `internal/store/verdictstate.go` and `internal/store/verdictstate_test.go` (post-Task-1)
    - `internal/store/boundedread_test.go` lines 16-64 (`TestRecordCeilingDerivesFromCaps` pattern)
    - `internal/store/spine_test.go` lines 815-850 (`TestNearDuplicatesDoesNotMutate`)
  </read_first>
  <behavior>
    - `TestVerdictStateViewCeiling` (hermetic, no Qdrant): at `DefaultRecordCaps()` the ceiling is 82432 and `perRPCLimit` of it is 25; with SummaryBytes 0 the ceiling uses ContentBytes for the summary term (147456); doubling ContentBytes raises the ceiling by exactly ContentBytes; the view's selector includes exactly `content`, `summary`, `created_at`.
    - `TestRecordStatesEmptyIDs`: nil and empty ids each return a non-nil empty map and a nil error.
    - `TestRecordStatesSpansScopesAndStates`: records in two different scopes, one archived and one superseded, are all returned (Subject-less, no recall gate — the same population `NearDuplicates` can pair).
    - `TestRecordStatesDoesNotMutate`: point count and payload digest are identical before and after a call (`snapshotCollection`).
  </behavior>
  <action>
Hardens Task 1's store layer (D-09's state source, the never-mutates invariant). Add the four tests above to `internal/store/verdictstate_test.go`. The ceiling test is hermetic and belongs beside the others in this file (not in `boundedread_test.go`, which this phase does not own). If any assertion fails, fix `verdictstate.go` or the view in `boundedread.go` rather than the test; if the archived/superseded case fails because a filter was added, remove the filter (consolidate's candidate population is unfiltered by recall state). Seed archived and superseded records through the same helpers `spine_test.go` uses (set `ArchivedAt` / `SupersededBy` on the seeded `Memory`).

Commit as `test(store): pin RecordStates view ceiling, span and no-mutation guarantees` with pathspec `internal/store/verdictstate_test.go internal/store/verdictstate.go internal/store/boundedread.go` (include `boundedread.go` only if it changed).
  </action>
  <verify>
    <automated>out=$(go test ./internal/store/ -run '^(TestVerdictStateViewCeiling|TestRecordStatesEmptyIDs|TestRecordStatesSpansScopesAndStates|TestRecordStatesDoesNotMutate|TestRecordStatesFetchesState)$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e '^--- PASS: (TestVerdictStateViewCeiling|TestRecordStatesEmptyIDs|TestRecordStatesSpansScopesAndStates|TestRecordStatesDoesNotMutate|TestRecordStatesFetchesState) ' | wc -l)" -eq 5</automated>
    <fails_when>non-zero exit, or fewer than five PASS lines (a skipped Qdrant-backed test counts as missing)</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -o 'snapshotCollection[(]' internal/store/verdictstate_test.go | wc -l` prints at least `2` (before and after).
    - `golangci-lint run ./internal/store/...` reports no findings.
  </acceptance_criteria>
  <done>The state fetch's byte budget, span, empty-input and no-write guarantees are pinned against real Qdrant; committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Verdict edges — malformed answers, threshold boundary, truncation encoding, error classes</name>
  <files>internal/verdict/verdict.go, internal/verdict/verdict_test.go</files>
  <read_first>
    - `internal/verdict/verdict.go` (post-Task-1)
    - `internal/decide/errors.go` (`Status`, sentinel list) and `internal/decide/jev/wire.go` lines 95-160 (what the backend already rejects)
  </read_first>
  <behavior>
    - `TestRelationQuestionSet`: `Relations()` is exactly the five names in D-05 order; `RelationCriteria()` has exactly those five keys, `updates` is D-06's sentence verbatim, and mutating a returned slice or map does not change the next call's result; `NewRequest("x","y")` has state keys exactly `context`, `record_a`, `record_b` and questions exactly `relation` (choice, five options) and `same_subject` (noul); `NewRequest(...).Validate()` (decide's structural validation) returns nil.
    - `TestStateTruncation`: summary plus blank line plus content when summary is set; content alone when empty; a 2000-code-point content truncates to exactly 1500 code points by default; a cut landing on a multi-byte rune (é, a 4-byte emoji) keeps the result valid UTF-8 with exactly `maxChars` runes; invalid UTF-8 input yields valid UTF-8 output; `maxChars` 0 and -1 use the default.
    - `TestPairRequestOrdersNewerSecond`: the later CreatedAt is `record_b` whichever argument order it arrives in; equal CreatedAt puts the lexically larger ID second.
    - `TestFromResultThresholdBoundary`: p exactly 0.9 at threshold 0.9 gives needs_review false; `math.Nextafter(0.9, 0)` gives true; threshold 0 never flags (p 0); threshold 1 flags p 0.999999 and not p 1.0.
    - `TestFromResultCarriesValuesVerbatim`: probabilities summing to 0.98 are carried unchanged; `SameSubject` and `Model` copied as sent.
    - `TestFromResultMalformed` (RED first — Task 1 does not detect these): a relation answer whose Choice is not one of the five names, a relation answer missing any of the five probabilities, a missing relation answer, a missing same_subject answer, and a same_subject answer of type choice each yield `ErrorClass` `malformed_response` and no other populated field.
    - `TestErrorClass`: `ErrStateUnavailable` (bare and wrapped) gives `state_unavailable`; raw `context.DeadlineExceeded` gives `timeout`; `context.Canceled` gives `canceled`; each decide sentinel wrapped in `*decide.Error` gives its `decide.Status` word; an unknown error gives `error`.
  </behavior>
  <action>
Closes D-05/D-08's edge contract and spec-less edges CUR-01 encoding, CUR-02 boundary and precision. Write `internal/verdict/verdict_test.go` (SPDX) with the tests above FIRST and run it: `TestFromResultMalformed` must fail against Task 1's code — record the failure in the SUMMARY. Then add the validation to `FromResult`: before mapping, require a `relation` answer of type choice whose Choice is in `Relations()` and whose Probabilities carry all five names, and a `same_subject` answer of type noul; any violation returns `Unavailable` of a `*decide.Error` with Kind `decide.ErrDecisionMalformedResponse` and the offending question name, so the class is `malformed_response` through the existing `decide.Status` mapping (no new class). Extra probability keys are ignored, never an error. Keep all other tests green without changing their expectations.

Commit as `test(verdict): pin relation question set, threshold boundary and malformed-answer handling` with pathspec `internal/verdict/verdict.go internal/verdict/verdict_test.go`.
  </action>
  <verify>
    <automated>out=$(go test ./internal/verdict/ -count=1 -race -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e '^--- PASS: (TestRelationQuestionSet|TestStateTruncation|TestPairRequestOrdersNewerSecond|TestFromResultThresholdBoundary|TestFromResultCarriesValuesVerbatim|TestFromResultMalformed|TestErrorClass) ' | wc -l)" -eq 7</automated>
    <fails_when>non-zero exit, a data race report, or fewer than seven named PASS lines</fails_when>
    <automated>go test ./cmd/engram/ -run 'Consolidate' -count=1</automated>
    <fails_when>non-zero exit (the tighter FromResult broke the consolidate tracer's canned response)</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -o 'ErrDecisionMalformedResponse' internal/verdict/verdict.go | wc -l` prints at least `1`.
    - `golangci-lint run ./internal/verdict/...` reports no findings.
  </acceptance_criteria>
  <done>Every edge of the verdict contract (option set, boundary, precision, encoding, malformed answers, error classes) is a named passing test; RED recorded for the malformed case; committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engram → third-party decisions provider | record summary and content head leave the deployment in the Decisions request |
| provider → engram | the response (choice, probabilities, model string) is untrusted input rendered into operator output |
| operator CLI → Qdrant | Subject-less operator-tier read of every record in the candidate population |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Information disclosure | `runVerdictPass` / `verdict.State` | high | mitigate | The pass runs only when `StoreAndDeciderFromEnv` returns a non-nil decider (ENGRAM_DECISIONS_PROVIDER set); `TestSpineReviewConsolidateNoProviderByteIdentical` proves zero state fetch and zero outbound calls otherwise; state is bounded to N code points per record (D-09). Plans 03-02/03-05 add the per-run disclosure line, `--no-verdicts` and the docs disclosure |
| T-03-02 | Tampering / Elevation | consolidate store surface | high | mitigate | `spineConsolidateStore` exposes only `NearDuplicates` and `RecordStates` (reflection test); `RecordStates` issues no write RPC (snapshot test); no mutating method is reachable from the verdict pass |
| T-03-10 | Denial of service | `RecordStates` on legacy oversized records | medium | mitigate | budgeted `verdictStateView` plus `fetchPayloadsByID`'s batch-of-1 fallback; a fetch failure degrades every pair to `state_unavailable` and the sweep still exits 0 |
| T-03-11 | Tampering | malformed provider answer | medium | mitigate | `FromResult` rejects an unknown choice, missing probability or wrong answer type as `malformed_response` instead of rendering it |
| T-03-SC | Tampering | dependencies | low | accept | no new module dependency (research: every primitive already in-tree) |
</threat_model>

<verification>
- `go build ./...` and `go test ./internal/verdict/ ./internal/store/ ./internal/server/ ./cmd/engram/ -count=1` exit 0.
- The store package still imports none of `internal/decide`, `internal/verdict`, `internal/server`, `internal/embed`.
</verification>

<success_criteria>
- CUR-01's JSON half: each candidate carries a nested advisory verdict (relation, five probabilities, same-subject probability, needs_review, model) when a decider is configured.
- CUR-02's invariant: no verdict can reach a mutating method, and below-threshold verdicts carry `needs_review: true` at the default 0.9.
- D-04: no provider means byte-identical output and zero outbound calls.
</success_criteria>

<output>
Create `.planning/phases/03-curation-verdicts/03-01-SUMMARY.md` when done. Record the RED evidence for Tasks 1 and 3 and any deviation from the identifiers above.
</output>
