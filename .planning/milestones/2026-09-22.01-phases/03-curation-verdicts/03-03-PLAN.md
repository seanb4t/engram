---
phase: 03-curation-verdicts
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - cmd/engram/spine_review_consolidate.go
  - cmd/engram/sweep_scope.go
  - cmd/engram/sweep_scope_test.go
  - cmd/engram/testdata/help.golden
  - cmd/engram/testdata/catalog.golden
  - internal/surfaces/rules.go
  - docs-site/src/content/docs/reference/tools.md
  - docs-site/src/content/docs/guides/cli.md
  - docs-site/src/content/docs/guides/upgrade.md
  - skill/engram/skills/curating-spine/SKILL.md
  - internal/skills/data/curating-spine/SKILL.md
autonomous: true
requirements:
  - CUR-04

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Tracer: `engram spine-review consolidate` with neither `--scope` nor `--all-scopes` exits `exitUsage` (2) with an error whose text equals `surfaces.RuleSweepScopeOrAllScopesRequired`'s registered Sentence exactly — the same result scan, verify and summarize-missing return (D-12, #508)"
    - "A present-but-empty `--scope \"\"` is rejected identically (spec-less edge CUR-04 empty); `--scope x --all-scopes` still fails as mutually exclusive with exitUsage (spec-less edge CUR-04 adjacency)"
    - "The guard is the FIRST statement of consolidate's RunE: with neither flag, output-format validation, `--min-score` parsing, store and decider construction never run and no outbound call is made; `consolidate --output yaml` with neither flag returns the scope rule error (spec-less edge CUR-04 ordering)"
    - "consolidate's `--all-scopes` Usage contains the registered Sentence, and `cmd/engram/testdata/help.golden` and `catalog.golden` each carry the Sentence 4 times (scan, verify, summarize-missing, consolidate) instead of 3"
    - "`spine-review consolidate` is classified in `enforcingSweepLeaves`; `spine-review purge` is the only entry left in `nonEnforcingSweepLeaves`, and every table-driven sweep-scope test in `sweep_scope_test.go` passes"
    - "`RuleSweepScopeOrAllScopesRequired`'s doc comment describes four enforcing leaves and purge as the sole exemption, with the SurfaceFields narrowing re-verified against the post-change tree (the internal/surfaces conformance tests and cmd/engram sweep-scope tests pass unchanged)"
    - "docs-site no longer states that consolidate with neither flag yields an empty sweep: cli.md's consolidate section states the scope-or-all-scopes rule, reference/tools.md's sweep-leaf list names consolidate, and upgrade.md gains a numbered entry plus a 'Do you need to act?' row for the exit-status change"
    - "The curating-spine skill's candidate-pair invocation passes `--scope <scope>` or `--all-scopes`; nothing else in the skill changes, and the vendored copy under internal/skills/data matches the canonical file (TestSkillsEmbedMatchesVendored)"
  artifacts:
    - path: "cmd/engram/spine_review_consolidate.go"
      provides: "the sweep-scope guard as RunE's first statement and the Sentence on --all-scopes"
      contains: "requireSweepScope(spineConsolidateScope, spineConsolidateAllScopes)"
    - path: "cmd/engram/sweep_scope_test.go"
      provides: "consolidate classified as an enforcing sweep leaf"
      contains: "\"spine-review consolidate\": true"
    - path: "docs-site/src/content/docs/guides/upgrade.md"
      provides: "the consolidate scope-requirement upgrade entry"
      contains: "spine-review consolidate"
  key_links:
    - from: "cmd/engram/spine_review_consolidate.go"
      to: "cmd/engram/sweep_scope.go"
      via: "consolidate routes its rejection through the shared registered guard (D-12)"
      pattern: "requireSweepScope[(]spineConsolidateScope, spineConsolidateAllScopes[)]"
    - from: "cmd/engram/spine_review_consolidate.go"
      to: "internal/surfaces/rules.go"
      via: "the --all-scopes Usage publishes the registered Sentence"
      pattern: "sweepScopeRule[(][)][.]Sentence"
  prohibitions:
    - statement: "MUST NOT change the curating-spine skill's consent contract or judgment workflow — only the one invocation line that would otherwise fail with the new rule error"
      category: values
      status: resolved
      verification: test
      reason: "Task 3's acceptance criterion bounds the skill diff to the invocation line (added-plus-removed line count). No wired check descriptor by design (spec-less fallback, flagged-unverified)"
---

<objective>
Close #508: `spine-review consolidate` stops reporting a silent zero-candidate result when neither `--scope` nor `--all-scopes` is given, by reusing the registered sweep-scope rule exactly as its three sibling sweep leaves do (D-12), and bring every surface that described the old behavior current in the same plan.

- **Tracer (Task 1):** RunE guard, published Usage Sentence, classification move, regenerated goldens — one invocation path from flag parse to exit code.
- **Task 2:** the rule's registration comment (four enforcers, purge alone exempt) with the SurfaceFields narrowing re-verified, and reference/tools.md's sweep-leaf sentence.
- **Task 3:** cli.md's scope sentence, the upgrade guide entry, and the curating-spine skill's invocation line (plus its vendored copy).

Decision implemented: D-12. Purpose: CUR-04. Output: the guard, tests, goldens, comment, docs and skill line.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-curation-verdicts/03-CONTEXT.md
@.planning/phases/03-curation-verdicts/03-RESEARCH.md
@cmd/engram/sweep_scope.go
@cmd/engram/sweep_scope_test.go
@cmd/engram/spine_review_scan.go
</context>

<interfaces>
Existing code (verified at planning time):

- `cmd/engram/sweep_scope.go`: `sweepScopeRule()` and `requireSweepScope(scope string, allScopes bool) error` (returns `usageErrorf("%s", Sentence)`); its doc comment lists the three enforcing leaves by name.
- `cmd/engram/spine_review_scan.go` lines 35-38 and 152: the guard as RunE's first statement and the `--all-scopes` Usage ending with `+sweepScopeRule().Sentence`.
- `cmd/engram/sweep_scope_test.go`: `enforcingSweepLeaves` (scan, verify, summarize-missing) and `nonEnforcingSweepLeaves` (consolidate, purge) drive `TestNoHandRolledSweepScopeGuards`, `TestRequireSweepScope`, `TestSweepLeavesRejectMissingScopeIdentically`, `TestSweepLeavesRejectPresentButEmptyScope` and `TestSweepLeavesUsageStatesRegisteredRule`.
- `internal/surfaces/rules.go` lines 164-210: the `RuleSweepScopeOrAllScopesRequired` doc comment; its SurfaceFields paragraph explains consolidate's and purge's exemptions (citing stale spine.go line numbers) and why adding `dry-run` narrows cobra-usage resolution to summarize-missing alone. `Sentence` is "a sweep requires an explicit --scope or --all-scopes: name one scope, or opt into every scope" and does not change.
- `cmd/engram/golden_test.go`: `TestHelpGolden`, `TestCatalogGolden`, regenerated with `-update`. The Sentence currently appears 3 times in each golden.
- `docs-site/src/content/docs/reference/tools.md` line 541: summarize-missing's section says "Like the other sweep-style operator commands (`spine-review scan`, `spine-review verify`)" before the rule anchor; the anchor region itself is generated and must not be hand-edited.
- `docs-site/src/content/docs/guides/cli.md` lines 246-260: consolidate's section, whose first paragraph ends by describing what supplying neither flag does.
- `docs-site/src/content/docs/guides/upgrade.md`: the `## Unreleased — …` section with a "Do you need to act?" table and numbered `### N.` entries (currently through `### 18.`).
- `skill/engram/skills/curating-spine/SKILL.md` line 70 (body, not frontmatter): "Run `engram spine-review consolidate --output json` and consume its" — this exact invocation fails once the guard ships. `task skills:vendor` re-copies `skill/engram/skills` into `internal/skills/data`; `TestSkillsEmbedMatchesVendored` fails if they differ.
- Existing consolidate tests already pass `--all-scopes`; `internal/e2e/spine_review_test.go` and `cmd/engram/operator_output_test.go` invoke consolidate with `--all-scopes` too.
</interfaces>

## Artifacts this phase produces (plan 03-03's share)

| Kind | Artifact | Task |
|------|----------|------|
| CLI | consolidate RunE guard; `--all-scopes` Usage + Sentence | 1 |
| Test data | `enforcingSweepLeaves` / `nonEnforcingSweepLeaves` reclassification; regenerated `help.golden`, `catalog.golden` | 1 |
| Comment | `RuleSweepScopeOrAllScopesRequired` doc (four enforcers, purge exempt); `requireSweepScope` doc | 2, 1 |
| Docs | reference/tools.md sweep-leaf list; cli.md consolidate scope sentence; upgrade.md `### 19.` entry and table row | 2, 3 |
| Skill | curating-spine invocation line (canonical + vendored) | 3 |

## Flagged assumptions (spec-less probe)

- CUR-04 adjacency, empty and ordering are resolved explicitly as truths and covered by the table-driven sweep-scope tests plus one ordering test in Task 1.
- The skill line edit is required by D-12 (the documented invocation would otherwise exit 2); it is not a consent-contract change, which CONTEXT puts out of scope.

## Executor notes (every task)

- Sequential main tree; never any git stash subcommand; explicit pathspec; Conventional Commits; the session's attribution trailer.
- Goldens: regenerate with `task surfaces:gen` (it runs `go run ./internal/surfacesgen`, `proto:gen`, and the `-update` golden pass with `-count=1`), then read `git status --porcelain` and the diff before committing — the only changes must be the two goldens, and inside them only consolidate's `--all-scopes` usage.
- `docs-site/**`, `skill/**/SKILL.md` and `.planning/**` carry no SPDX header.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: consolidate rejects a missing scope with the registered rule, end to end</name>
  <files>cmd/engram/spine_review_consolidate.go, cmd/engram/sweep_scope.go, cmd/engram/sweep_scope_test.go, cmd/engram/testdata/help.golden, cmd/engram/testdata/catalog.golden</files>
  <read_first>
    - `cmd/engram/sweep_scope.go`, `cmd/engram/sweep_scope_test.go` (whole files)
    - `cmd/engram/spine_review_scan.go` lines 30-40 and 145-155
    - `cmd/engram/spine_review_consolidate.go` (RunE and `init`)
  </read_first>
  <behavior>
    - Moving consolidate into `enforcingSweepLeaves` makes `TestSweepLeavesRejectMissingScopeIdentically/spine-review_consolidate`, `TestSweepLeavesRejectPresentButEmptyScope/…` and `TestSweepLeavesUsageStatesRegisteredRule` fail first (RED — record it in the SUMMARY), then pass once the guard and Usage land.
    - New `TestSpineReviewConsolidateScopeGuardPrecedesEverything`: with a store constructor substituted by one that fails the test if called, `spine-review consolidate --output yaml` returns the registered Sentence (not the output-format error) with exitUsage, and the constructor is never called.
  </behavior>
  <action>
Implements D-12.

(1) `cmd/engram/sweep_scope_test.go`: move `"spine-review consolidate": true` from `nonEnforcingSweepLeaves` to `enforcingSweepLeaves`; run the sweep-scope tests and record the RED.
(2) `cmd/engram/spine_review_consolidate.go`: make `if err := requireSweepScope(spineConsolidateScope, spineConsolidateAllScopes); err != nil { return err }` the first statement of RunE, before `operatorOutputFormat`, mirroring scan/verify/summarize-missing exactly. Change `--all-scopes`' Usage to state that it spans every scope, that it is required if `--scope` is omitted, that it is mutually exclusive with `--scope`, that a candidate pair may then cross scopes with each row naming both, and end it with `+sweepScopeRule().Sentence` (scan's concatenation shape). Keep `MarkFlagsMutuallyExclusive("scope", "all-scopes")`.
(3) `cmd/engram/sweep_scope.go`: update `requireSweepScope`'s doc comment to list the four enforcing leaves (spine-review scan, spine-review verify, spine-review consolidate, summarize-missing).
(4) Add `TestSpineReviewConsolidateScopeGuardPrecedesEverything` to `sweep_scope_test.go`, substituting `spineConsolidateStoreFromEnv` with a function that calls `t.Error` and restoring it via `t.Cleanup`.
(5) Regenerate with `task surfaces:gen` and review the diff (goldens only).

Commit as `fix(consolidate): require --scope or --all-scopes like every other sweep leaf (D-12, #508)` with pathspec `cmd/engram/spine_review_consolidate.go cmd/engram/sweep_scope.go cmd/engram/sweep_scope_test.go cmd/engram/testdata/help.golden cmd/engram/testdata/catalog.golden`.
  </action>
  <verify>
    <automated>out=$(go test ./cmd/engram/ -run '^(TestNoHandRolledSweepScopeGuards|TestRequireSweepScope|TestSweepLeavesRejectMissingScopeIdentically|TestSweepLeavesRejectPresentButEmptyScope|TestSweepLeavesUsageStatesRegisteredRule|TestSpineReviewConsolidateScopeGuardPrecedesEverything|TestSpineReviewConsolidateScopeAndAllScopesRejected)$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^\s*--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e '^--- PASS: Test' | wc -l)" -eq 7 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e '--- PASS: TestSweepLeavesRejectMissingScopeIdentically/spine-review_consolidate' | wc -l)" -eq 1</automated>
    <fails_when>non-zero exit, fewer than seven top-level PASS lines, or no consolidate subtest in the missing-scope rejection test (classification not moved)</fails_when>
    <automated>out=$(go test ./cmd/engram/ -run '^(TestHelpGolden|TestCatalogGolden)$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; for g in cmd/engram/testdata/help.golden cmd/engram/testdata/catalog.golden; do test "$(rg -o -F 'a sweep requires an explicit --scope or --all-scopes' "$g" | wc -l)" -eq 4 || exit 1; done</automated>
    <fails_when>a golden test fails (goldens not regenerated), or either golden does not carry the Sentence exactly 4 times</fails_when>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd/engram/ -count=1` exits 0 (every consolidate test already passes `--all-scopes`).
    - `golangci-lint run ./cmd/engram/...` reports no findings.
  </acceptance_criteria>
  <done>consolidate with neither flag exits 2 with the registered Sentence before touching anything else; help and catalog publish the rule; committed.</done>
</task>

<task type="auto">
  <name>Task 2: The rule's registration comment and the reference guide's sweep-leaf list</name>
  <files>internal/surfaces/rules.go, docs-site/src/content/docs/reference/tools.md</files>
  <read_first>
    - `internal/surfaces/rules.go` lines 150-330
    - `internal/surfaces/conformance_test.go` (how SurfaceFields resolves onto the live tree)
    - `docs-site/src/content/docs/reference/tools.md` lines 530-550
  </read_first>
  <action>
Keeps D-12's registry reasoning true (RESEARCH Pitfall 4, Open Question 2).

(1) Rewrite `RuleSweepScopeOrAllScopesRequired`'s doc comment: the rule is enforced at four sweep leaves (spine-review scan, spine-review verify, spine-review consolidate — since #508 — and summarize-missing); spine-review purge is the sole command exposing both flags without enforcing it, with its existing reasoning kept and its spine.go citation re-pointed at the current line of the `!opts.AllScopes && opts.Scope != ""` purge filter (find it with `rg -n` rather than trusting the old number). Remove the stale consolidate exemption sentence and its line citation. Re-derive the SurfaceFields paragraph for four enforcers: recompute the flag-set intersection of the four enforcing leaves from the live tree (list each leaf's flags with a throwaway `go test` or by reading their `init` functions), confirm it is still a subset of purge's flag set, and confirm `dry-run` still narrows cobra-usage resolution to summarize-missing alone; the leaves the narrowing cannot reach (scan, verify, consolidate) are pinned by `TestSweepLeavesUsageStatesRegisteredRule`. State the re-verification in the comment the way the old one cites 08-01-PLAN.md fact 5, citing this plan instead. Do not change `Sentence`, `Fields`, `SurfaceFields` or `Hint`.
(2) `reference/tools.md`: in the sentence just before the sweep-scope rule anchor, list `spine-review consolidate` beside scan and verify. Do not edit inside the `engram:rule:start` / `engram:rule:end` markers.

If the re-derivation shows `dry-run` no longer isolates summarize-missing, STOP and report it rather than changing `SurfaceFields` — that would change generated docs and needs a decision.

Commit as `docs(surfaces): record consolidate as a fourth sweep-scope enforcer` with pathspec `internal/surfaces/rules.go docs-site/src/content/docs/reference/tools.md`.
  </action>
  <verify>
    <automated>go test ./internal/surfaces/... ./cmd/engram/ -count=1</automated>
    <fails_when>non-zero exit (a conformance or surface-resolution test reacts to the reclassification)</fails_when>
    <automated>f=internal/surfaces/rules.go; test "$(rg -o -F 'neither enforces it' "$f" | wc -l)" -eq 0 &amp;&amp; test "$(rg -o -F 'spine-review consolidate' "$f" | wc -l)" -ge 1 &amp;&amp; test "$(rg -o -e 'spine[.]go:384' "$f" | wc -l)" -eq 0</automated>
    <fails_when>the old consolidate-exemption wording or its stale line citation remains, or consolidate is not named</fails_when>
  </verify>
  <acceptance_criteria>
    - After committing, `task surfaces:gen` leaves `git status --porcelain` empty — the comment edit does not alter any generated region or golden.
    - `rg -o -F '`spine-review consolidate`' docs-site/src/content/docs/reference/tools.md | wc -l` prints at least `1`.
  </acceptance_criteria>
  <done>The registry comment and reference guide describe four enforcers and purge's sole exemption, re-verified against the live tree; committed.</done>
</task>

<task type="auto">
  <name>Task 3: CLI guide, upgrade entry, and the curating-spine invocation line</name>
  <files>docs-site/src/content/docs/guides/cli.md, docs-site/src/content/docs/guides/upgrade.md, skill/engram/skills/curating-spine/SKILL.md, internal/skills/data/curating-spine/SKILL.md</files>
  <read_first>
    - `docs-site/src/content/docs/guides/cli.md` lines 240-275
    - `docs-site/src/content/docs/guides/upgrade.md` lines 1-45 and 465-500
    - `skill/engram/skills/curating-spine/SKILL.md` lines 60-80
    - `Taskfile.yaml` `skills:vendor` target
  </read_first>
  <action>
Implements D-12's "publish the rule" on every prose surface.

(1) cli.md, consolidate section: replace the sentence describing what supplying neither flag does with the rule — exactly one of `--scope` or `--all-scopes` is required, and supplying neither is rejected with exit status `2` like scan, verify and summarize-missing (#508). Leave the rest of the section for plan 03-06.
(2) upgrade.md: add a `### 19.` heading after §18 under the Unreleased section, titled with the backticked command `spine-review consolidate` followed by "now requires" and the backticked flags `--scope` or `--all-scopes` — before this release, consolidate with neither flag exited 0 with zero candidates; it now exits 2 with the sweep-scope rule message. Who should act: anyone who scripted consolidate without a scope flag; add `--scope <scope>` or `--all-scopes`. Add a matching row to the "Do you need to act?" table pointing at §19.
(3) curating-spine SKILL.md body line 70 only: change the invocation to pass `--scope <scope>` (or `--all-scopes` to span every scope) before `--output json`, saying one of the two is required. Do not touch the frontmatter, the consent rules, or any judgment step. Then run `task skills:vendor`.

Commit as `docs(consolidate): document the required scope flag and fix the skill invocation (D-12)` with pathspec `docs-site/src/content/docs/guides/cli.md docs-site/src/content/docs/guides/upgrade.md skill/engram/skills/curating-spine/SKILL.md internal/skills/data/curating-spine/SKILL.md`.
  </action>
  <verify>
    <automated>test "$(rg -o -F 'well-defined empty result' docs-site/src/content/docs/guides/cli.md | wc -l)" -eq 0 &amp;&amp; test "$(rg -o -e '^### 19[.] .*spine-review consolidate' docs-site/src/content/docs/guides/upgrade.md | wc -l)" -eq 1 &amp;&amp; test "$(rg -n -o -F 'consolidate --output json' skill/engram/skills/curating-spine/SKILL.md internal/skills/data/curating-spine/SKILL.md | rg -v -e ':3:' | wc -l)" -eq 0</automated>
    <fails_when>the retired empty-result sentence is still in cli.md, the §19 heading is missing, or a scope-less invocation remains in the skill body</fails_when>
    <automated>out=$(go test ./internal/skills/ -run '^TestSkillsEmbedMatchesVendored$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; printf '%s\n' "$out" | rg -q -e '^--- PASS: TestSkillsEmbedMatchesVendored '</automated>
    <fails_when>non-zero exit or no PASS line (vendored copy out of sync)</fails_when>
  </verify>
  <acceptance_criteria>
    - `git show --numstat --format= <this task's commit SHA> -- skill/engram/skills/curating-spine/SKILL.md` reports at most 3 added and 3 removed lines (the invocation sentence only).
    - `task lint` markdown checks (`rumdl`) report nothing new for the two guides; `go test ./internal/server/ -run 'VerbTable' -count=1` exits 0.
  </acceptance_criteria>
  <done>The CLI guide, upgrade guide and skill all state the scope requirement; the vendored skill matches; committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → consolidate flags | a missing scope previously produced a misleading, apparently-complete empty report |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-09 | Repudiation / integrity of report | consolidate with neither scope flag | medium | mitigate | the registered guard rejects the invocation with exitUsage before any store read; the table-driven sweep-scope tests pin identical behavior across all four enforcing leaves |
| T-03-14 | Tampering (docs drift) | rule comment, guides, skill | low | mitigate | goldens pin the published Usage; negative greps remove the retired wording; the vendor drift test pins the skill copy |
</threat_model>

<verification>
- `go test ./cmd/engram/ ./internal/surfaces/... ./internal/skills/ -count=1` exits 0.
- consolidate with neither scope flag exits 2 with the registered Sentence.
</verification>

<success_criteria>
- CUR-04 / ROADMAP criterion 4: consolidate with neither flag gets the scope-or-all-scopes rule error, never a silent zero-candidate report.
</success_criteria>

<output>
Create `.planning/phases/03-curation-verdicts/03-03-SUMMARY.md` when done. Record the RED evidence from Task 1 and the re-derived flag-set intersection from Task 2.
</output>
