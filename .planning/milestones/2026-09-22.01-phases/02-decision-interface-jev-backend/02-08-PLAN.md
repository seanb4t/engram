---
phase: 02-decision-interface-jev-backend
plan: 08
type: execute
wave: 6
depends_on:
  - 02-06
  - 02-07
files_modified:
  - internal/decide/jev/jev.go
  - internal/decide/jev/wire.go
  - internal/decide/jev/wire_test.go
  - internal/decide/jev/fixtures_test.go
  - internal/decide/jev/live_test.go
  - Taskfile.yaml
autonomous: true
requirements:
  - DEC-02
  - DEC-03

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The jev backend sends noul, choice and score questions in the spike-verified wire shape: top-level keys exactly `model`, `state` and `questions`; noul criteria an object with `true`/`false`; choice criteria an object of option to description; score criteria an array in `Scale` order; instructions as strings; state passed through verbatim (DEC-02, DEC-03)"
    - "`TestJevRequestShape` proves both transports: a base of `<srv>/api` posts to `/api/alpha/decisions`, `<srv>/api/` (a trailing slash) posts to the same path with no doubled slash, and `<srv>/openrouter` posts to `/openrouter/alpha/decisions` (DEC-03, spec-less edge E04)"
    - "The backend maps all three answer types into `decide.Answer` verbatim: choice (`Choice`, `Probabilities`, `Confidence`), score (`Score` as float64, `Probabilities` keyed by the wire's level index, `Confidence`, `Legend` as raw JSON) and noul (`Probability`). Probabilities are never renormalized (a set summing to 0.98 decodes to 0.98), and an absent confidence, usage or cost stays nil (DEC-02, spec-less edge E03)"
    - "A 200 that does not decode, lacks an answer for a requested question, or carries an unknown answer type for a requested question returns `ErrDecisionMalformedResponse` naming that question. Answers for questions that were not asked are ignored (DEC-04)"
    - "`task eval:decisions` runs `TestJevLive`, an opt-in live smoke test gated by `ENGRAM_DECISIONS_LIVE`. The gate is resolved like `internal/retrievaleval`'s eval gate: unset or empty skips, and a malformed value fails naming the var and the value, never skipping silently. When enabled, it sends one synthetic noul+choice+score request to the configured base URL and asserts typed answers, usage and a `typesafe/jev-1.13` snapshot"
    - "A human has run `task eval:decisions` against OpenRouter directly (`https://openrouter.ai/api`) and through the LiteLLM pass-through, and both passed (DEC-03, manual-only row in 02-VALIDATION.md)"
  artifacts:
    - path: "internal/decide/jev/wire.go"
      provides: "Request encoding and response decoding for noul, choice and score (both D-06 branches)"
      contains: "func encodeRequest("
    - path: "internal/decide/jev/wire_test.go"
      provides: "TestJevRequestShape, TestJevAnswerMapping"
      contains: "func TestJevRequestShape("
    - path: "internal/decide/jev/live_test.go"
      provides: "TestJevLive opt-in live smoke test"
      contains: "func TestJevLive("
    - path: "Taskfile.yaml"
      provides: "eval:decisions target"
      contains: "eval:decisions:"
  key_links:
    - from: "internal/decide/jev/jev.go"
      to: "internal/decide/jev/wire.go"
      via: "Decide encodes and decodes through the one wire codec"
      pattern: "decodeResponse[(]"
    - from: "Taskfile.yaml"
      to: "internal/decide/jev/live_test.go"
      via: "the eval target enables and runs the live smoke test"
      pattern: "ENGRAM_DECISIONS_LIVE=1"
---

<objective>
Complete the Jev backend's System One coverage and prove it against both real transports.

- **DEC-02:** the backend speaks every question and answer type the contract defines, carrying values verbatim.
- **DEC-03:** both base-URL shapes reach `{base}/alpha/decisions` (automated), and a human confirms one live call through OpenRouter directly and one through the LiteLLM pass-through.
- **DEC-04 (decode half):** a malformed or incomplete 200 is a named error, not a panic or a silent partial.

This is the last plan in the phase. Everything D-01 to D-13 required is in place before it, and this plan fills in the choice/score wire mapping that the tracer and the failure-path work deliberately left for last.

Purpose: Phase 3 asks choice questions (relation verdicts) and Phase 4 asks many noul questions (relevance). Both rely on this mapping, and both run against the LiteLLM pass-through in production.
Output: `wire.go` with tests, answer fixtures, the opt-in `TestJevLive`, and `task eval:decisions`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-decision-interface-jev-backend/02-CONTEXT.md
@.planning/phases/02-decision-interface-jev-backend/02-SDK-EVALUATION.md
@.planning/phases/02-decision-interface-jev-backend/02-VALIDATION.md
@.planning/phases/02-decision-interface-jev-backend/02-07-SUMMARY.md
@.claude/skills/spike-findings-engram/references/decision-transport.md
@internal/decide/decide.go
@internal/decide/jev/jev.go
</context>

<interfaces>
From plans 02-03, 02-04 and 02-07:

- `internal/decide/decide.go`:
  - `Question{Type, Instructions, WhenTrue, WhenFalse, Options map[string]string, Scale []string}`, with the constructors `Noul`, `Choice` and `Score`.
  - `Answer{Type, Probability float64, Choice string, Score float64, Confidence *float64, Probabilities map[string]float64, Legend map[string]json.RawMessage}`.
  - `Response{Answers, Model, ID, Provider, Usage *Usage}` and `Usage{InputTokens, OutputTokens int64; CostUSD *float64}`.
  - `ErrDecisionMalformedResponse` and `*decide.Error{Kind, Question, …}`.
- `internal/decide/jev/jev.go`: `Decide`, which does validate, then budget, then `attempt` (plus at most one retry), then decode, then span. The tracer's noul-only wire structs and decode live inline in `jev.go` on the hand-write branch, or as SDK component mapping on the adopt branch.
- `internal/decide/jev/fixtures_test.go` (plan 02-07): the error fixtures, `fixtureNoulOK` and `fixtureNoulNoUsage`.
- The live-gate precedent is `internal/retrievaleval/gate.go`'s `resolveEvalGate`: a package-local koanf load with `config.Prefix`, a `false` default layer, an empty value keeping the default, `strconv.ParseBool`, and an error on a malformed value.
- `Taskfile.yaml` `eval:retrieval` (about line 81) is the target shape to mirror.

Spike fixture sources (extract, never retype): `jq -r '[.events[] | select(.probe=="happy")][0].body'` and `…=="batch50"…` over `.claude/skills/spike-findings-engram/sources/001-jev-openrouter-transport/results.json`.
</interfaces>

## Artifacts this phase produces

| Artifact | Kind | Plan |
|----------|------|------|
| `02-SDK-EVALUATION.md` and `COVERAGE.md` | phase docs | 02-01, 02-02 |
| `internal/config` and `internal/server/decider.go` | config, wiring | 02-03, 02-05 |
| `internal/decide` contract | contract | 02-03, 02-04 |
| `internal/decide/jev`: `wire.go` (full noul/choice/score codec), `TestJevRequestShape`, `TestJevAnswerMapping`, answer fixtures | backend | 02-03, 02-04, 02-07, **02-08** |
| Helm, chart:validate, docs, `CLAUDE.md` row | chart, docs | 02-06 |
| `task eval:decisions` and `TestJevLive` (opt-in live check) | verification | **02-08** |

## Spec-less fallback ledger (this plan's share)

- **E03** (DEC-02, precision): **explicit** (decode half). Answers decode verbatim: a 0.98 sum is not renormalized, `Score` stays a float, and absent confidence, usage or cost stay nil.
- **E04** (DEC-03, adjacency): **explicit**. A base with or without a trailing slash reaches `/api/alpha/decisions` with no doubled slash, and the LiteLLM shape reaches `/openrouter/alpha/decisions`.

## Executor notes (every task)

- NEVER run any `git stash` subcommand. To inspect a pristine file, use `git show HEAD:<path>` into the scratchpad.
- Commit with an explicit pathspec, using Conventional Commits, and end the message with the session's attribution trailer lines. If the 1Password SSH signer is locked or errors, you MAY retry that ONE commit with `git -c commit.gpgsign=false commit …` (never change persistent git config) and must record it in the SUMMARY.
- Every new Go file carries the two-line SPDX header.
- **D-06 branch:** on `reject-hand-write`, `wire.go` holds unexported JSON wire structs. On `adopt-and-wrap`, it maps to and from the SDK's `components` types per the `## Wrap recipe`. The function names and tests are identical on both branches.
- Never exact-compare probabilities. Never print the API key, even in the live test's logs.
- Live requests use only synthetic state text. Never send store content from this test.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Encode every question type and decode every answer type verbatim; both base-URL shapes reach /alpha/decisions (DEC-02, DEC-03)</name>
  <files>internal/decide/jev/wire.go, internal/decide/jev/wire_test.go, internal/decide/jev/jev.go, internal/decide/jev/fixtures_test.go</files>
  <read_first>
    - `internal/decide/jev/jev.go` (whole file: the tracer's inline wire code, and `Decide`'s structure after plan 02-07)
    - `internal/decide/decide.go` (the Question/Answer fields and their doc comments)
    - `.claude/skills/spike-findings-engram/references/decision-transport.md` ("Request shape", "Response shape")
    - `.planning/phases/02-decision-interface-jev-backend/02-SDK-EVALUATION.md` `### Answer and usage types` and the single recipe section
  </read_first>
  <behavior>
    - `TestJevRequestShape` (httptest server capturing the path and decoded body), subtests:
      - `openrouter base`: `srv.URL+"/api"` posts to `/api/alpha/decisions`.
      - `trailing slash`: `srv.URL+"/api/"` posts to `/api/alpha/decisions`.
      - `litellm base`: `srv.URL+"/openrouter"` posts to `/openrouter/alpha/decisions`.
      - `body shape`: a request with noul `n`, choice `c` (3 options) and score `s` (3 levels), plus state `{"ticket": "synthetic ticket text"}`. The decoded body's top-level keys are exactly `{model, state, questions}`. `questions.n.criteria` is an object with keys exactly `{true, false}`. `questions.c.criteria` equals the Options map. `questions.s.criteria` is an array equal to Scale in order. Every `instructions` is a string, and `state` equals the input.
    - `TestJevAnswerMapping`, subtests:
      - `happy` (`fixtureHappy`): `relation` is a choice answer with `Choice` `duplicate`, `Probabilities["duplicate"]` in [0.87, 0.97] and non-nil `Confidence` in [0.85, 0.95]. `same_subject` is a noul answer with Probability in [0.87, 0.97].
      - `batch50`: all 50 requested noul answers are present.
      - `score` (synthetic `fixtureScore`): `Score` is 1 (a float), the Probabilities keys are exactly `0`, `1` and `2` with their values verbatim, Confidence is non-nil, and Legend has 3 entries whose raw JSON equals the fixture's strings.
      - `no renormalize`: a synthetic choice whose probabilities sum to 0.98 decodes to a sum within 1e-9 of 0.98.
      - `absent optionals`: a missing confidence gives nil. `fixtureNoulNoUsage` gives a nil Usage. A usage without `cost` gives a nil CostUSD.
      - `malformed`: a non-JSON 200, a 200 missing the requested question `c`, and a 200 whose answer for `c` has type `ranking` each give `ErrDecisionMalformedResponse`, and the last two name `c`.
      - `extra ignored`: an answer for an unrequested question is absent from `Response.Answers`.
    - RED first, recorded.
  </behavior>
  <action>
Implements DEC-02 (full vocabulary on the wire) and DEC-03 (both base-URL shapes, automated half).

(1) New `internal/decide/jev/wire.go` (package `jev`, SPDX):
- Move the tracer's wire code out of `jev.go` and extend it.
- `func encodeRequest(model string, req decide.Request) ([]byte, error)` (hand-write branch: JSON bytes; adopt branch: the `components.DecisionsRequest` per the recipe, with the same function name returning what `attempt` sends). The criteria shapes:
  - noul: `{"true": WhenTrue, "false": WhenFalse}`
  - choice: the `Options` map
  - score: `Scale` as an array
- `func decodeResponse(body []byte, req decide.Request) (decide.Response, error)`. For every requested question name: a missing answer, a malformed answer or an unknown answer type returns `&decide.Error{Kind: decide.ErrDecisionMalformedResponse, Question: name}`. An undecodable body gives `&decide.Error{Kind: decide.ErrDecisionMalformedResponse, Err: decodeErr}`. Answers for unrequested names are skipped.
- Map fields verbatim: choice `choice`, `probabilities` and `confidence`. Score `score` (float64), `probabilities` (keys verbatim), `confidence` and `legend` (each value kept as `json.RawMessage`). Noul `noul` into `Probability`. The model, id, provider and usage map with nil for anything absent.
- Never round, clamp or renormalize.

(2) `internal/decide/jev/jev.go`: `Decide` now calls `encodeRequest` before `attempt` and `decodeResponse` on the 200 body that `attempt` bounded. Remove the tracer's unsupported-answer-type error and inline structs. Keep the endpoint join `strings.TrimRight(baseURL, "/") + decisionsPath` (on the adopt branch, the recipe's mapping must produce the same observed paths).

(3) `internal/decide/jev/fixtures_test.go`: add `fixtureHappy` and `fixtureBatch50` (verbatim, via `jq`), `fixtureScore` (synthetic: the score answer `{"type":"score","score":1,"confidence":0.8,"probabilities":{"0":0.1,"1":0.8,"2":0.1},"legend":{"0":"can wait","1":"this week","2":"blocking"}}` with usage, model and id), `fixtureChoiceSum098` (synthetic) and `fixtureUnknownAnswerType` (synthetic).

(4) New `internal/decide/jev/wire_test.go` (SPDX): the two tests in `<behavior>`.

Order: tests (RED), then implementation (GREEN). Commit as `feat(decide/jev): map choice and score questions and answers verbatim; prove both base-URL shapes (DEC-02, DEC-03)` with pathspec `internal/decide/jev/wire.go internal/decide/jev/wire_test.go internal/decide/jev/jev.go internal/decide/jev/fixtures_test.go`.
  </action>
  <verify>
    <automated>out=$(go test ./internal/decide/jev/ -run '^(TestJevRequestShape|TestJevAnswerMapping)$' -count=1 -race -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e '^--- PASS: Test(JevRequestShape|JevAnswerMapping)$' | wc -l)" -eq 2 &amp;&amp; test "$(printf '%s\n' "$out" | rg -o -e '--- PASS: TestJev(RequestShape|AnswerMapping)/' | wc -l)" -ge 11</automated>
    <fails_when>non-zero exit, a missing top-level PASS, or fewer than 11 passing subtests</fails_when>
    <automated>go test ./internal/decide/... ./internal/server/ ./internal/config/ -count=1 -race &amp;&amp; golangci-lint run ./internal/decide/...</automated>
    <fails_when>non-zero exit (a regression in the tracer, classification, telemetry, contract or wiring tests, or a lint finding)</fails_when>
  </verify>
  <acceptance_criteria>
    - The tracer's unsupported-type path is gone: `rg -o -F 'unsupported answer type' internal/decide/jev/jev.go | wc -l` prints `0`.
    - No renormalization or rounding code (comments excluded): `rg -v '^\s*//' internal/decide/jev/wire.go | rg -o -i -e 'normali[sz]e' -e 'math[.]Round' | wc -l` prints `0`.
    - `rg -o 'func (encodeRequest|decodeResponse)[(]' internal/decide/jev/wire.go | wc -l` prints `2`.
  </acceptance_criteria>
  <done>The backend encodes all three question types and decodes all three answer types verbatim, flags malformed or incomplete responses by name, and reaches `/alpha/decisions` for the OpenRouter, trailing-slash and LiteLLM base shapes. RED then GREEN recorded, the whole suite passes, and the change is committed.</done>
</task>

<task type="auto">
  <name>Task 2: An opt-in live smoke test and task eval:decisions, then a human runs it against OpenRouter and the LiteLLM pass-through (DEC-03)</name>
  <files>internal/decide/jev/live_test.go, Taskfile.yaml</files>
  <read_first>
    - `internal/retrievaleval/gate.go` (the `resolveEvalGate` shape to mirror)
    - `Taskfile.yaml` lines 74-86 (`eval:summary`, `eval:retrieval`)
    - `internal/decide/jev/jev.go` (`New` and its options)
    - `.planning/phases/02-decision-interface-jev-backend/02-VALIDATION.md` "Manual-Only Verifications"
  </read_first>
  <action>
Implements DEC-03's live half as a durable, opt-in check (the manual-only row in 02-VALIDATION.md).

(1) New `internal/decide/jev/live_test.go` (package `jev`, SPDX): `func TestJevLive(t *testing.T)`. Its first statement resolves the gate with a small unexported `liveGate(environ func() []string) (bool, error)`. That helper mirrors `resolveEvalGate`: a package-local koanf load of `ENGRAM_DECISIONS_LIVE` with `config.Prefix`, a `false` default, an empty value keeping the default, and `strconv.ParseBool`. On a malformed value the error names the var and the value, and the test calls `t.Fatalf`. When the gate is off, the test calls `t.Skip("set ENGRAM_DECISIONS_LIVE=1 plus ENGRAM_DECISIONS_BASE_URL and a key to run the live Decisions smoke test (task eval:decisions)")`. `ENGRAM_DECISIONS_LIVE` is test-only: never add it to `internal/config`'s registry.

When enabled:
- Load config with `config.Load(nil)`. If `Decisions.BaseURL` is empty, `t.Fatal` naming `ENGRAM_DECISIONS_BASE_URL`.
- Build `New(cfg.Decisions.BaseURL, cmp.Or(cfg.Decisions.APIKey, cfg.OpenAI.APIKey), cfg.Decisions.Model, WithTimeout(30*time.Second))`.
- Send state `{"ticket": "My checkout page shows a blank screen after I click Pay. I have tried two browsers."}` with noul `is_bug`, choice `team` (`account`, `frontend`, `payments`) and score `urgency` (three levels). All text is synthetic.
- Assert: no error; three answers of the right types; the noul probability in [0, 1]; the choice is one of the three options, with every probability in [0, 1]; the score in [0, 2]; a non-nil Usage with `InputTokens > 0`; and `Model` starting with `typesafe/jev-1.13`.
- `t.Logf` the base URL's host (never userinfo), the model snapshot, the token counts, the cost and the latency. Never the key.

(2) `Taskfile.yaml`: add after `eval:retrieval`:
- `eval:decisions` with `desc: Live Decisions smoke test against ENGRAM_DECISIONS_BASE_URL (needs a key; spends a fraction of a cent)`
- one cmd: `ENGRAM_DECISIONS_LIVE=1 go test ./internal/decide/jev/ -run '^TestJevLive$' -count=1 -v`

Run `yamlfmt -lint Taskfile.yaml`.

Commit as `test(decide/jev): add opt-in live Decisions smoke test and task eval:decisions (DEC-03)` with pathspec `internal/decide/jev/live_test.go Taskfile.yaml`.
  </action>
  <verify>
    <automated>out=$(env -u ENGRAM_DECISIONS_LIVE go test ./internal/decide/jev/ -run '^TestJevLive$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; printf '%s\n' "$out" | rg -q -e '^--- SKIP: TestJevLive'</automated>
    <fails_when>non-zero exit, or no `--- SKIP: TestJevLive` line when the gate is unset (the live test ran or failed without being enabled)</fails_when>
    <automated>out=$(ENGRAM_DECISIONS_LIVE=maybe go test ./internal/decide/jev/ -run '^TestJevLive$' -count=1 2>&amp;1); rc=$?; printf '%s\n' "$out" | tail -5; test "$rc" -ne 0 &amp;&amp; printf '%s\n' "$out" | rg -q 'ENGRAM_DECISIONS_LIVE' &amp;&amp; printf '%s\n' "$out" | rg -q 'maybe'</automated>
    <fails_when>exit 0 (a malformed gate value was treated as off), or the output does not name the var and the value</fails_when>
    <automated>list=$(task --list-all 2>&amp;1) || exit 1; printf '%s\n' "$list" | rg -q 'eval:decisions' &amp;&amp; yamlfmt -lint Taskfile.yaml</automated>
    <fails_when>non-zero exit (the target is missing or Taskfile.yaml is not yamlfmt-clean)</fails_when>
    <human-check>
      1. With `ENGRAM_DECISIONS_BASE_URL=https://openrouter.ai/api` and an OpenRouter key in `ENGRAM_DECISIONS_API_KEY` (or `ENGRAM_OPENAI_API_KEY`), run `task eval:decisions`. Expected: `--- PASS: TestJevLive`, logging a `typesafe/jev-1.13-…` snapshot and a nonzero input token count.
      2. With `ENGRAM_DECISIONS_BASE_URL` set to your LiteLLM pass-through (`https://<your-gateway>/openrouter`) and engram's LiteLLM virtual key (which has the `/openrouter/alpha/decisions` pass-through grant), run `task eval:decisions` again. Expected: `--- PASS: TestJevLive`.
      3. Optional negative check: run step 2 with a key lacking the grant. Expected: the test fails with an auth-class error (403), not a decode error.
      Reply with both PASS outputs (the key never appears in them).
    </human-check>
  </verify>
  <acceptance_criteria>
    - The live gate is not registered: `rg -o 'DECISIONS_LIVE' internal/config | wc -l` prints `0`.
    - The gate resolves before anything else: `awk '/^func TestJevLive/,/^}/' internal/decide/jev/live_test.go | sed -n 2p` contains `liveGate`.
    - `rg -o -F 'ENGRAM_DECISIONS_LIVE=1 go test ./internal/decide/jev/' Taskfile.yaml | wc -l` prints `1`.
  </acceptance_criteria>
  <done>A durable opt-in live smoke test and `task eval:decisions` exist. The gate skips when unset and fails loudly when malformed. The live runs against OpenRouter and the LiteLLM pass-through are queued as the phase's human check, and the change is committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Decisions endpoint → engram | untrusted 200 bodies (answer types, missing or extra answers) |
| developer machine → live endpoints | the opt-in live test spends real credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-20 | Tampering / Denial of service | response decode | medium | mitigate | A malformed, missing or unknown answer is a named error per question, and unrequested answers are ignored, so a misbehaving gateway cannot inject answers or crash the decode. `TestJevAnswerMapping` proves it |
| T-02-05 | Information disclosure | API key in live-test logs | high | mitigate | The live test logs the host, snapshot, tokens, cost and latency only. Its `t.Logf` call sites never receive the key |
| T-02-21 | Information disclosure (to a third party) | live-test payload | low | mitigate | The live state is fixed synthetic text, never store content, and the test runs only when explicitly enabled |
| T-02-22 | Repudiation (false-green live check) | the live gate | low | mitigate | A malformed `ENGRAM_DECISIONS_LIVE` fails loudly instead of skipping. The verify proves both the skip and the failure directions |
</threat_model>

<verification>
- `go test ./internal/decide/... ./internal/server/ ./internal/config/ -count=1 -race` passes. `task lint` and `task test` are green for the phase.
- `TestJevLive` skips when the gate is unset and fails loudly when it is malformed. The human check records both live PASS runs.
</verification>

<success_criteria>
- ROADMAP Phase 2 success criterion 3 holds in full: the Jev backend reaches `{base}/alpha/decisions` with its own settings, against OpenRouter directly and through the LiteLLM pass-through (automated path proof, plus the human-run live checks).
- Success criterion 2 holds for every question type on the wire: Choice, Score and Noul round-trip into typed answers.
</success_criteria>

<output>
Create `.planning/phases/02-decision-interface-jev-backend/02-08-SUMMARY.md` when done. Record the RED observations, and note that the live human check is pending end-of-phase UAT (or record its outputs if it was run).
</output>
