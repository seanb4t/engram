---
phase: 02-decision-interface-jev-backend
plan: 06
type: execute
wave: 4
depends_on:
  - 02-03
files_modified:
  - charts/engram/values.yaml
  - charts/engram/templates/_helpers.tpl
  - Taskfile.yaml
  - docs-site/src/content/docs/guides/configure.md
  - docs-site/src/content/docs/guides/deploy.md
  - internal/config/decisions_docs_test.go
  - CLAUDE.md
autonomous: true
requirements:
  - DEC-01
  - DEC-03

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The chart's default render contains no `ENGRAM_DECISIONS_` variable. Every decisions row sits under a `memory.decisions.provider` gate, so the default Deployment env is byte-identical to the pre-phase render. A diff of the default render before and after this plan is empty (DEC-01, D-04, spec-less edge E01)"
    - "With `memory.decisions.provider=jev` and a `baseURL`, the render emits `ENGRAM_DECISIONS_PROVIDER` and `ENGRAM_DECISIONS_BASE_URL`, plus model, timeout and concurrency only when those values are set. `ENGRAM_DECISIONS_API_KEY` appears only as a `secretKeyRef` carrying the configured secret name and key, never as a literal value. A set `apiKeySecret` with an empty provider renders nothing (D-04)"
    - "`task chart:validate` asserts all four chart truths above in both directions and passes with a re-pinned `engram.containerEnv` checksum whose comment records why it moved"
    - "docs-site `guides/configure.md` has a `## Typed decisions (Jev)` section that documents every registered `ENGRAM_DECISIONS_*` var in a table row. The rows are derived from the registry by `TestDecisionsVarsDocumented`, never a hand list, and that gate is proven able to go red (D-04)"
    - "The section tells an operator, before they enable it: the base URL never falls back and gets `/alpha/decisions` appended, with an OpenRouter example and a LiteLLM pass-through example; an empty `ENGRAM_DECISIONS_API_KEY` sends `ENGRAM_OPENAI_API_KEY` to the decisions host; the model is pinned; what data leaves the deployment, to whom, and the provider's data policy; and that a decision failure never fails the operation that asked (D-03, D-12, prohibition P4)"
    - "`guides/deploy.md`'s Helm key-values table lists the `memory.decisions.*` values, and `CLAUDE.md`'s Layout table has an `internal/decide/` row. `engram setup` is unchanged (D-04)"
  artifacts:
    - path: "charts/engram/templates/_helpers.tpl"
      provides: "Provider-gated ENGRAM_DECISIONS_* rows in engram.containerEnv, API key via secretKeyRef"
      contains: "ENGRAM_DECISIONS_PROVIDER"
    - path: "Taskfile.yaml"
      provides: "chart:validate decisions assertions and the re-pinned checksum"
      contains: "ENGRAM_DECISIONS_API_KEY"
    - path: "docs-site/src/content/docs/guides/configure.md"
      provides: "Typed decisions (Jev) operator section"
      contains: "## Typed decisions (Jev)"
    - path: "internal/config/decisions_docs_test.go"
      provides: "TestDecisionsVarsDocumented registry-derived docs gate with a red control"
      contains: "func TestDecisionsVarsDocumented("
  key_links:
    - from: "internal/config/decisions_docs_test.go"
      to: "docs-site/src/content/docs/guides/configure.md"
      via: "the docs gate reads the Typed decisions section and checks it against the registry"
      pattern: "guides/configure[.]md"
    - from: "charts/engram/templates/_helpers.tpl"
      to: "charts/engram/values.yaml"
      via: "every decisions row is gated on memory.decisions.provider (D-01)"
      pattern: "memory[.]decisions[.]provider"
  prohibitions:
    - statement: "MUST NOT let an operator enable third-party decision calls without the docs stating what leaves the deployment (the state and questions a feature sends, which is memory record content for curation and reranking), who receives it (OpenRouter, routing to TypeSafe), the provider's data policy (no training on inputs, standard retention, zero data retention unconfirmed), and that an empty ENGRAM_DECISIONS_API_KEY sends ENGRAM_OPENAI_API_KEY to that host"
      category: transparency
      status: resolved
      verification: test
      reason: "TestDecisionsVarsDocumented asserts the disclosure anchors in the Typed decisions section. No wired check descriptor by design (spec-less fallback, flagged-unverified)"
---

<objective>
Expose the decision provider on the two operator surfaces D-04 names (the Helm chart and the docs-site config reference), keep both off by default, and make the disclosure an operator needs part of the reference.

- **D-04:** Helm values are off by default, the API key comes only from a `secretKeyRef`, and `engram setup` is unchanged. The docs-site config reference and the deploy guide are updated.
- **D-01:** the default Helm render is byte-identical, and a `chart:validate` guard pins that in both directions.
- **D-03:** the docs spell out the no-fallback base URL, the `/alpha/decisions` join, and the key-fallback consequence (an empty decisions key sends the embedder's key to the decisions host).
- **Prohibition P4:** the section says what data leaves, to whom, and under what data policy.

Purpose: an operator can turn the feature on correctly by reading, with no trial and error ("correct-by-reading", rule `4aksmneehh`).
Output: the chart values, template and validation, the configure and deploy docs, the `CLAUDE.md` layout row, and a registry-derived docs gate.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-decision-interface-jev-backend/02-CONTEXT.md
@.planning/phases/02-decision-interface-jev-backend/02-03-SUMMARY.md
@.claude/skills/spike-findings-engram/references/decision-transport.md
@internal/config/registry.go
</context>

<interfaces>
Existing surfaces (verified at planning time):

- `charts/engram/values.yaml`: `memory.openai` (`baseURL`, `apiKeySecret{name,key}`) is at lines 74-90. `memory.summarize` (lines 100-128) is the block to sit beside: its comment style is inline `# ENGRAM_…` after each value. Existing file mentions of the operator's own domain are left alone.
- `charts/engram/templates/_helpers.tpl`: `{{- define "engram.containerEnv" -}}` at line 1. The `with` idiom (omit the var when the value is empty) and the `if …Secret.name` plus `secretKeyRef` idiom are at lines 27-46 (`ENGRAM_SUMMARY_MODEL`, `ENGRAM_OPENAI_CHAT_API_KEY`). The block is the single source of the Deployment and CronJob env.
- `Taskfile.yaml` `chart:validate` (about lines 150-259): bash assertions with a both-directions shape for `ENGRAM_OPENAI_CHAT_API_KEY` and `ENGRAM_SERVICE_AUTH_STATIC_TOKENS`, then the drift pin, `EXPECTED_CHECKSUM="98bda8a99638d33b2a31c11266fd4512e2403a89be369782338fcf27291c9260"`, computed by `awk '/define "engram.containerEnv"/{f=1} f{print} f && /^\{\{- end -\}\}$/{exit}' charts/engram/templates/_helpers.tpl | shasum -a 256`, preceded by a comment that logs every re-pin.
- `docs-site/src/content/docs/guides/configure.md`: `## Auto-summary` (line 66, with a `### Async-on-write summaries` subsection) is followed by `## OIDC / Auth` (line 152). Table header: `| Environment variable | Flag | Default | Description |`.
- `docs-site/src/content/docs/guides/deploy.md` `### Key values` table (`| Helm value | ENGRAM_* variable set | Description |`), followed by the sentence about `ENGRAM_OPENAI_API_KEY` coming from a Secret.
- `CLAUDE.md` Layout table: the row `` | `internal/embed/` | embedder (OpenAI-compatible) | `` at line 18.
- Docs-gate precedent: `cmd/engram/docsync_test.go` and `internal/server/recallmaxdocs_test.go` read docs by a path relative to the package directory (`../../docs-site/...`).
</interfaces>

## Artifacts this phase produces

| Artifact | Kind | Plan |
|----------|------|------|
| `02-SDK-EVALUATION.md` and `COVERAGE.md` | phase docs | 02-01, 02-02 |
| `internal/config`, `internal/decide`, `internal/decide/jev`, `internal/server/decider.go` | code | 02-03, 02-04, 02-05, 02-07, 02-08 |
| Helm `memory.decisions` values, provider-gated `engram.containerEnv` rows, `chart:validate` assertions, re-pinned checksum | chart | **02-06** |
| `guides/configure.md` `## Typed decisions (Jev)`, `guides/deploy.md` rows, `CLAUDE.md` `internal/decide/` row, `TestDecisionsVarsDocumented` | docs | **02-06** |
| `task eval:decisions` and `TestJevLive` | verification | 02-08 |

## Spec-less fallback ledger (this plan's share)

- **E01** (DEC-01, unclassified): **explicit** (chart half). The default render has no `ENGRAM_DECISIONS_` var and is byte-identical. `chart:validate` guards it.
- **Prohibition P4** (transparency, disclosure before enablement): authored above, descriptor-less.

## Executor notes (every task)

- NEVER run any `git stash` subcommand. To capture the pre-change render, write it to the session scratchpad before editing.
- Commit with an explicit pathspec, using Conventional Commits, and end the message with the session's attribution trailer lines. If the 1Password SSH signer is locked or errors, you MAY retry that ONE commit with `git -c commit.gpgsign=false commit …` (never change persistent git config) and must record it in the SUMMARY.
- `docs-site/**` takes no SPDX header. `internal/config/decisions_docs_test.go` does.
- Use `litellm.example.com` or `gateway.example` in every example. Never write the operator's real gateway hostname.
- `task lint` covers yamlfmt, rumdl and golangci-lint, and must stay clean for `values.yaml`, `Taskfile.yaml`, the markdown and the new test.

<tasks>

<task type="auto">
  <name>Task 1: Helm exposes memory.decisions, off by default, with the key only as a secretKeyRef; chart:validate guards both directions (D-01, D-04)</name>
  <files>charts/engram/values.yaml, charts/engram/templates/_helpers.tpl, Taskfile.yaml</files>
  <read_first>
    - `charts/engram/values.yaml` lines 70-130
    - `charts/engram/templates/_helpers.tpl` lines 1-100
    - `Taskfile.yaml` lines 141-259 (`chart:lint`, `chart:validate`)
    - `02-CONTEXT.md` D-01, D-03, D-04. `02-RESEARCH.md` "Code Examples" (the drafted Helm block) and "Security Domain" (V6: key via Secret)
  </read_first>
  <action>
Implements D-04 (Helm, off by default) and D-01 (byte-identical default render).

(0) Before editing, run `helm template charts/engram > <scratchpad>/render-before.yaml`.

(1) `charts/engram/values.yaml`: after `memory.summarize` (and its `cronjob` block), add `memory.decisions`. Precede it with a comment block: typed decisions (Jev over OpenRouter's Decisions API); off by default, because an empty provider renders no `ENGRAM_DECISIONS_*` var and the server constructs nothing; advisory only. Keys, each with an inline `# ENGRAM_…` comment:
- `provider: ""`: `""` (off) or `"jev"`.
- `baseURL: ""`: required when the provider is `jev`, and never falls back to `memory.openai.baseURL`. The comment carries both examples: `https://openrouter.ai/api` and `https://litellm.example.com/openrouter`.
- `apiKeySecret` with `name: ""` and `key: ""`: an empty name inherits `memory.openai.apiKeySecret`'s key, which is then sent to `baseURL`.
- `model: ""`: empty means the binary default, pinned `typesafe/jev-1.13`.
- `timeout: ""`: empty means the binary default of 10s.
- `concurrency: ""`: empty means the binary default of 4.

Run `yamlfmt` on the file.

(2) `charts/engram/templates/_helpers.tpl`: inside `engram.containerEnv`, after the `ENGRAM_SUMMARY_TIMEOUT` row, add one block opened by `{{- if .Values.memory.decisions.provider }}` and closed by `{{- end }}`, with a leading template comment (empty provider omits every decisions var, D-01). Inside it:
- `- { name: ENGRAM_DECISIONS_PROVIDER, value: "{{ .Values.memory.decisions.provider }}" }`
- A `with` row each for `baseURL` (ENGRAM_DECISIONS_BASE_URL), `model` (ENGRAM_DECISIONS_MODEL), `timeout` (ENGRAM_DECISIONS_TIMEOUT) and `concurrency` (ENGRAM_DECISIONS_CONCURRENCY).
- An `if .Values.memory.decisions.apiKeySecret.name` block emitting `ENGRAM_DECISIONS_API_KEY` as `valueFrom.secretKeyRef` with the name and key quoted, exactly like `ENGRAM_OPENAI_CHAT_API_KEY`.

Use `$.Values` inside any `with` scope.

(3) `Taskfile.yaml` `chart:validate`: before the drift pin, add a both-directions block with a comment matching its neighbours:
- (a) The default render must NOT contain `ENGRAM_DECISIONS_`.
- (b) Every decisions value set EXCEPT the provider (`--set memory.decisions.baseURL=https://openrouter.example/api --set memory.decisions.model=m --set memory.decisions.timeout=5s --set memory.decisions.concurrency=2 --set memory.decisions.apiKeySecret.name=cv-dec-secret --set memory.decisions.apiKeySecret.key=cv-dec-key`) must NOT contain `ENGRAM_DECISIONS_`. This is the behavioral proof that every row sits under the provider gate.
- (c) `--set memory.decisions.provider=jev --set memory.decisions.baseURL=https://openrouter.example/api` must emit `name: ENGRAM_DECISIONS_PROVIDER` and `name: ENGRAM_DECISIONS_BASE_URL`, and must NOT emit `ENGRAM_DECISIONS_API_KEY`.
- (d) The (c) flags plus the (b) secret flags must emit `name: ENGRAM_DECISIONS_API_KEY`, followed within 3 lines by `secretKeyRef` and within 5 lines by `name: "cv-dec-secret"` and `key: "cv-dec-key"`.

Each failure echoes a `chart:validate: …` reason and exits 1.

Then recompute the checksum with the task's own `awk | shasum` pipeline, replace `EXPECTED_CHECKSUM`, and append one sentence to the re-pin log comment: re-pinned on today's date (`date +%Y-%m-%d`) for the provider-gated `ENGRAM_DECISIONS_*` rows, with the default render omitting all of them and the rest of the block byte-identical.

(4) Run `helm template charts/engram > <scratchpad>/render-after.yaml` and `diff <scratchpad>/render-before.yaml <scratchpad>/render-after.yaml`. It must print nothing. Record that in the SUMMARY.

Commit as `feat(chart): add memory.decisions values, off by default, key via secretKeyRef (DEC-01, D-04)` with pathspec `charts/engram/values.yaml charts/engram/templates/_helpers.tpl Taskfile.yaml`.
  </action>
  <verify>
    <automated>out=$(task chart:validate 2>&amp;1); rc=$?; printf '%s\n' "$out" | tail -5; test "$rc" -eq 0 &amp;&amp; printf '%s\n' "$out" | rg -q 'chart:validate: OK'</automated>
    <fails_when>non-zero exit, or no `chart:validate: OK` line (a decisions assertion or the checksum pin failed)</fails_when>
    <automated>r=$(helm template charts/engram) || exit 1; test "$(printf '%s\n' "$r" | rg -o 'ENGRAM_DECISIONS_' | wc -l)" -eq 0</automated>
    <fails_when>non-zero exit (the default render emits a decisions variable, breaking DEC-01's byte-identical default)</fails_when>
    <automated>yamlfmt -lint charts/engram/values.yaml Taskfile.yaml</automated>
    <fails_when>non-zero exit (a yamlfmt formatting finding)</fails_when>
  </verify>
  <acceptance_criteria>
    - The API key is never a literal value in the template: `rg -A1 'name: ENGRAM_DECISIONS_API_KEY' charts/engram/templates/_helpers.tpl | rg -o 'valueFrom' | wc -l` prints `1`, and `rg -o '[{] name: ENGRAM_DECISIONS_API_KEY' charts/engram/templates/_helpers.tpl | wc -l` prints `0`.
    - The template names exactly six decisions vars (PROVIDER, BASE_URL, MODEL, TIMEOUT, CONCURRENCY, API_KEY): `rg -o 'ENGRAM_DECISIONS_[A-Z_]+' charts/engram/templates/_helpers.tpl | sort -u | wc -l` prints `6`. `chart:validate` block (b) is the behavioral proof that all six sit under the provider gate.
    - `EXPECTED_CHECKSUM` changed and matches the live block. `task chart:validate` passing proves this.
    - No private gateway hostname: `rg -o 'llm[.]fzymgc' charts docs-site | wc -l` prints `0`.
  </acceptance_criteria>
  <done>The chart exposes decisions off by default, with the key only via `secretKeyRef`. `chart:validate` asserts the gate in both directions and the re-pinned checksum. The default render is byte-identical to before (diff recorded), and the change is committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The config reference documents every ENGRAM_DECISIONS_* var and the disclosure, gated by a registry-derived test; deploy guide and CLAUDE.md follow (D-03, D-04, P4)</name>
  <files>docs-site/src/content/docs/guides/configure.md, docs-site/src/content/docs/guides/deploy.md, internal/config/decisions_docs_test.go, CLAUDE.md</files>
  <read_first>
    - `docs-site/src/content/docs/guides/configure.md` lines 1-12 and 60-160 (the `Auto-summary` section: prose density, the per-lane key paragraph, the table)
    - `docs-site/src/content/docs/guides/deploy.md` lines 18-45
    - `internal/server/recallmaxdocs_test.go` lines 1-60 (the docs-gate shape and relative-path convention)
    - `internal/config/registry.go` (the nine decisions rows from plan 02-03)
    - `.claude/skills/spike-findings-engram/references/decision-transport.md` "Constraints" (the data policy, location, 32k context) and "What to Avoid" (the pass-through route grant)
    - `02-CONTEXT.md` D-03, D-04, D-12
  </read_first>
  <behavior>
    - `TestDecisionsVarsDocumented`:
      - derives the env set from `registry` rows whose `Env` has the prefix `ENGRAM_DECISIONS_`, and asserts it holds 9 names (a positive control, so an empty derivation cannot pass vacuously);
      - reads `../../docs-site/src/content/docs/guides/configure.md` and extracts the section from `## Typed decisions (Jev)` to the next `## ` heading (failing if the heading is absent);
      - asserts `missingDecisionsDocs(section, envs)` returns nothing. That helper reports each env name with no table row starting `` | `<ENV>` | ``;
      - asserts the section contains the disclosure anchors `TypeSafe`, `retention`, `ENGRAM_OPENAI_API_KEY` and `/alpha/decisions`.
    - Red control subtest: `missingDecisionsDocs` over a synthetic section that omits `ENGRAM_DECISIONS_CONCURRENCY` returns exactly `[ENGRAM_DECISIONS_CONCURRENCY]`, which proves the gate can go red.
    - RED first: the test fails before the docs section exists. Record that.
  </behavior>
  <action>
Implements D-04 (the docs-site config reference), D-03 (fallback semantics stated) and prohibition P4 (disclosure).

(1) `docs-site/src/content/docs/guides/configure.md`: insert `## Typed decisions (Jev)` after the Auto-summary section (and its async subsection), before `## OIDC / Auth`. Prose, in this order:
- What it is: an optional typed-decision client (Jev, reached through OpenRouter's Decisions API) that answers yes/no, multiple-choice and scored questions about a piece of state with probabilities. It is off by default: set `ENGRAM_DECISIONS_PROVIDER=jev` to enable it. Answers are advisory, surfaced to you and never acted on automatically. Enabling it today constructs and validates the client at startup. The features that ask it questions ship separately and name this setting when they do.
- Base URL: required when enabled, and it never inherits `ENGRAM_OPENAI_BASE_URL`, because the embeddings/chat gateway does not serve Decisions. engram appends `/alpha/decisions`, so `https://openrouter.ai/api` resolves to `https://openrouter.ai/api/alpha/decisions`, and a LiteLLM pass-through at `https://litellm.example.com/openrouter` resolves to `https://litellm.example.com/openrouter/alpha/decisions`. The LiteLLM key needs the `/openrouter/alpha/decisions` pass-through route granted, or the gateway answers 403. The most likely first failure is a base URL ending in `/v1` (or `/api` on the LiteLLM form), which yields 404.
- Key: an empty `ENGRAM_DECISIONS_API_KEY` inherits `ENGRAM_OPENAI_API_KEY` and **sends it to the decisions host**. Mirror the chat-lane paragraph's warning, and tell operators to set `ENGRAM_DECISIONS_API_KEY` to keep the embedder or gateway key off that host. Startup logs `api_key_source` so the operator can see which key applied. In Helm this is `memory.decisions.apiKeySecret`.
- Model: pinned `typesafe/jev-1.13`. Do not use the floating `~typesafe/jev-latest`, because it moves probability thresholds between releases.
- **What leaves your deployment:** each decision call sends the state and questions a feature builds (for curation and reranking features, that is memory record content) to OpenRouter, which routes Jev to TypeSafe (service on the US West Coast). The provider's policy: no training on inputs, standard retention, and zero data retention not confirmed. Enable it only if that is acceptable for the records in your store.
- Failure behavior: each call is bounded by `ENGRAM_DECISIONS_TIMEOUT` (one retry on 429/5xx inside that budget) and by response-size and drain bounds. Failures are reported as authentication, bad request, context too large (state plus questions over Jev's 32k-token context), rate limited, unavailable, timeout or response too large. A decision failure never fails the operation that asked for it.
- Then the table `| Environment variable | Flag | Default | Description |` with one row per var, all with Flag `—`: PROVIDER `_(empty)_`, BASE_URL `_(empty)_`, API_KEY `_(empty)_`, MODEL `typesafe/jev-1.13`, TIMEOUT `10s`, MAX_TIMEOUT `10m`, DRAIN_BYTES `262144`, DRAIN_TIMEOUT `2s`, CONCURRENCY `4`. The drain and max-timeout descriptions mirror the `ENGRAM_SUMMARY_*` rows' wording. CONCURRENCY caps how many decision calls one batch runs at once.

(2) `docs-site/src/content/docs/guides/deploy.md` `### Key values`: add rows for `memory.decisions.provider`, `memory.decisions.baseURL`, `memory.decisions.model`, `memory.decisions.timeout` and `memory.decisions.concurrency`, each naming its `ENGRAM_DECISIONS_*` var. After the `ENGRAM_OPENAI_API_KEY` Secret sentence, add: `ENGRAM_DECISIONS_API_KEY` comes from `memory.decisions.apiKeySecret`; left empty, the server inherits the `ENGRAM_OPENAI_API_KEY` secret; and every decisions var renders only when `memory.decisions.provider` is set.

(3) `CLAUDE.md` Layout table: after the `internal/embed/` row, add `` | `internal/decide/` | provider-neutral typed-decision contract (`Decider`, System One questions/answers, named errors); `internal/decide/jev` is the Jev backend over OpenRouter's Decisions API, off unless `ENGRAM_DECISIONS_PROVIDER` is set | ``.

(4) New `internal/config/decisions_docs_test.go` (SPDX): the test and helper per `<behavior>`.

Run `rumdl check docs-site/src/content/docs/guides/configure.md docs-site/src/content/docs/guides/deploy.md CLAUDE.md`, and fix any finding.

Order: test (RED), then docs (GREEN). Commit as `docs(config): document ENGRAM_DECISIONS_* with data disclosure; gate the reference against the registry (DEC-01, DEC-03, D-04)` with pathspec `docs-site/src/content/docs/guides/configure.md docs-site/src/content/docs/guides/deploy.md internal/config/decisions_docs_test.go CLAUDE.md`.
  </action>
  <verify>
    <automated>out=$(go test ./internal/config/ -run '^TestDecisionsVarsDocumented$' -count=1 -v 2>&amp;1); rc=$?; printf '%s\n' "$out" | rg -e '^\s*--- |^ok|^FAIL'; test "$rc" -eq 0 &amp;&amp; printf '%s\n' "$out" | rg -q -e '^--- PASS: TestDecisionsVarsDocumented' &amp;&amp; printf '%s\n' "$out" | rg -q -e '--- PASS: TestDecisionsVarsDocumented/'</automated>
    <fails_when>non-zero exit, no top-level PASS, or no PASS for the red-control subtest</fails_when>
    <automated>rumdl check docs-site/src/content/docs/guides/configure.md docs-site/src/content/docs/guides/deploy.md CLAUDE.md</automated>
    <fails_when>non-zero exit (a markdown lint finding)</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -o '^## Typed decisions [(]Jev[)]$' docs-site/src/content/docs/guides/configure.md | wc -l` prints `1`, and it comes after `## Auto-summary` and before `## OIDC / Auth`. Check with `rg -n '^## ' docs-site/src/content/docs/guides/configure.md` order, recorded in the SUMMARY.
    - `rg -o 'memory[.]decisions[.][a-zA-Z]+' docs-site/src/content/docs/guides/deploy.md | sort -u | wc -l` prints at least `6` (the five value rows plus `apiKeySecret`).
    - `rg -o -F '`internal/decide/`' CLAUDE.md | wc -l` prints `1`.
    - `engram setup` is untouched (D-04): neither task's commit pathspec names `internal/setup` or `cmd/engram/setup.go`, and `git status --porcelain -- internal/setup cmd/engram/setup.go` prints nothing.
  </acceptance_criteria>
  <done>The config reference documents all nine vars, the no-fallback base URL, the key-fallback consequence, the pinned model, the data disclosure and the failure contract, and a registry-derived gate with a red control guards it. The deploy guide and `CLAUDE.md` are current, lint is clean, and the change is committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Helm values → pod env | operator values rendered into the Deployment and CronJob env |
| Kubernetes Secret → pod env | the decisions API key |
| docs → operator decision | what an operator believes enabling does |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Information disclosure | `ENGRAM_DECISIONS_API_KEY` in rendered manifests | high | mitigate | Rendered only as a `secretKeyRef`, never as a literal. `chart:validate` (d) asserts `secretKeyRef` plus the name and key, and acceptance greps that no literal-form row exists |
| T-02-13 | Information disclosure | third-party egress on a default install | high | mitigate | Every decisions var sits under the provider gate. `chart:validate` (a)/(b) and the before/after render diff prove the default render is unchanged |
| T-02-08 | Information disclosure | key fallback sends the embedder key to the decisions host | medium | mitigate | configure.md and deploy.md state the consequence and the opt-out (set `ENGRAM_DECISIONS_API_KEY` / `memory.decisions.apiKeySecret`). The docs gate asserts the `ENGRAM_OPENAI_API_KEY` anchor |
| T-02-14 | Information disclosure (to a third party) | record content in decision state | medium | mitigate | The feature is off by default (D-01), and the disclosure paragraph names what leaves, the recipient and the data policy (prohibition P4, docs-gated) |
| T-02-15 | Information disclosure | private gateway hostname in public docs | low | mitigate | Examples use `litellm.example.com`/`gateway.example`, and acceptance greps for the real host |
</threat_model>

<verification>
- `task chart:validate` passes, printing `chart:validate: OK`. The default render has no `ENGRAM_DECISIONS_` var, and the before/after diff is empty.
- `go test ./internal/config/ -run '^TestDecisionsVarsDocumented$'` passes, including its red control.
- `task lint` is clean for the touched YAML, markdown and Go.
</verification>

<success_criteria>
- ROADMAP Phase 2 success criterion 1 holds on the deployment surface: an unset provider leaves the chart's rendered env identical, and setting it turns the feature on.
- D-04 holds: Helm values (off by default, key via Secret) and the docs-site config reference are updated, and `engram setup` is unchanged.
</success_criteria>

<output>
Create `.planning/phases/02-decision-interface-jev-backend/02-06-SUMMARY.md` when done. Record the empty before/after render diff, the new checksum, and the RED observation.
</output>
