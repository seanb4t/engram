<!--
SPDX-License-Identifier: Apache-2.0
-->
---
milestone: v0.9.x
name: Recall Quality
audited: 2026-07-11T00:18:05Z
status: passed
shipped: PR #336 (squash-merge, commit 658795e9) — merged to main 2026-07-10
scores:
  requirements: 6/6
  phases: 4/4
  integration: 11/11 links WIRED
  flows: 4/4 COMPLETE
  security: 3/3 phases 0-open-threats (+P10)
  nyquist: 3/3 planned phases COMPLIANT (P10 n/a — no plans)
gaps:
  requirements: []   # none unsatisfied
  integration: []    # none broken
  flows: []          # none broken
tech_debt:
  - phase: 09-retrieval-eval-harness-ranking-precision
    items:
      - "Human/operational: confirm SC3 (#261 rank bar) on prod-parity qwen3-embedding-8b @4096 w/ ENGRAM_EMBED_QUERY_INSTRUCTION — one live run used a gemini substitute (recall@8=1.00/MRR=1.000) because qwen3-via-OpenRouter was browning out. Rerank MECHANISM proven embedder-independently by TestRerankParityMCPAndConnect. Tracked: GH #334 (blocked by GH #333 embed timeout)."
  - phase: 10-asymmetric-query-document-embeddings
    items:
      - "Optional: eval demonstration of retrieval benefit for an asymmetric config (e.g. Gemini task_type). Rides on GH #334. Capability code-verified + unit-tested; this is proof-of-benefit, not proof-of-capability."
  - phase: 11-async-on-write-summaries
    items:
      - "Code-review residuals GH #335: WR-03 (Wait misuse), IN-01 (duplicate depth gauge registration), IN-02 (test hermeticity). WR-01 (worker context.Background) ACCEPTED — workers reaped at process exit."
  - phase: 12-per-memory-usage-signals
    items:
      - "None functional. Process gap (2 revive findings introduced by 12-04) already resolved inline in commit badd4a5d; task lint:go 0 issues."
  - phase: cross-cutting
    items:
      - "Embed-subsystem debt surfaced during v0.9.x eval work (not v0.9.x scope): GH #333 (configurable ENGRAM_EMBED_TIMEOUT), #332 (base-URL /v1 join brittle for OpenRouter), #331 (Gemini native embeddings)."
      - "Systemic: 331 pre-existing rumdl markdown-lint failures across 37 .planning/ files — .rumdl.toml lacks a .planning exclude. task lint:go + task test pass; only lint:markdown fails, and only on planning docs (never Go deliverables)."
      - "Issue hygiene: GH #320 (Phase 11) and GH #317 (Phase 12) feature issues still OPEN despite the PR #336 merge — should be closed. (#305 already closed for P10.)"
      - "Env restore (from session memory): repo-local commit.gpgsign=false still set (1Password SSH-signing flaky) — restore via `git config --local --unset commit.gpgsign`. Local main not yet synced past the squash merge."
deferred_next_milestone:  # explicitly OUT of v0.9.x scope — not debt
  - "GH #322 — Connect write-lane RPCs + CSRF hardening (v0.10.x)"
  - "GH #323 — Session refresh-token rotation (v0.10.x)"
---

# Milestone v0.9.x — Recall Quality — Audit Report

**Audited:** 2026-07-11T00:18:05Z · **Status:** ✅ **PASSED**
**Shipped:** PR #336 (squash-merge, commit `658795e9`) — merged to `main` 2026-07-10
**Scope:** Phases 9–12 · 6 requirements

> **Theme (DoD):** make recall measurably trustworthy as the store grows, so
> `search-before-store` stops producing duplicates (the #261 failure). All four
> phases achieved their goals; all six requirements are satisfied with evidence;
> cross-phase integration and every end-to-end flow are wired and complete.

---

## 1. Requirements Coverage (3-source cross-reference)

Each requirement is checked against **three independent sources**: the phase
`VERIFICATION.md` status, the plan `SUMMARY.md` `requirements-completed`
frontmatter, and the `REQUIREMENTS.md` traceability checkbox.

| REQ-ID | Phase | VERIFICATION | SUMMARY frontmatter | REQUIREMENTS `[x]` | → Final |
|--------|-------|--------------|---------------------|--------------------|---------|
| REQ-retrieval-eval | 9 | passed · SATISFIED | 09-01 | `[x]` | ✅ **satisfied** |
| REQ-search-similarity-scores | 9 | passed · SATISFIED | 09-01, 09-02 | `[x]` | ✅ **satisfied** |
| REQ-ranking-precision | 9 | passed · SATISFIED (mechanism) | 09-03 | `[x]` | ✅ **satisfied** ¹ |
| REQ-embedder-native-params | 10 | passed · already-shipped | — (no plans) ² | `[x]` | ✅ **satisfied** ² |
| REQ-async-summaries | 11 | passed 9/9 · SATISFIED | 11-01, 11-02, 11-03 | `[x]` | ✅ **satisfied** |
| REQ-usage-signals | 12 | passed 3/3 · SATISFIED | 12-01…12-06 | `[x]` | ✅ **satisfied** |

**Score: 6/6 satisfied · 0 unsatisfied · 0 partial · 0 orphaned.**
No requirement in the traceability table is absent from its phase VERIFICATION
(orphan check clean). FAIL gate not triggered.

¹ **REQ-ranking-precision** carries one *disclosed operational follow-up* (GH
#334): the #261 rank bar was confirmed on a `gemini-embedding` substitute
(recall@8=1.00, MRR=1.000), not the exact prod embedder `qwen3-embedding-8b`
@4096, because qwen3-via-OpenRouter was browning out past the client's 30s
timeout. The rerank **ordering mechanism** is proven embedder-independently by
`TestRerankParityMCPAndConnect` (tied-raw-score corpus). This is a live-gateway
re-confirmation, **not a code gap** — the shipped code, unit tests, and
cross-surface parity test are all present and passing.

² **REQ-embedder-native-params** has no `SUMMARY` frontmatter because Phase 10
was found **already shipped** under Phase 4 (`WithQueryParams`/`WithDocumentParams`/
`WithDocumentInstruction` + `ENGRAM_EMBED_*_PARAMS`/`_DOCUMENT_INSTRUCTION`,
tested + documented) and no plans were built. The "missing frontmatter" is
resolved by the `10-VERIFICATION.md` already-shipped reconciliation (SC1/SC2 MET,
SC3-docs MET), so it is **satisfied**, not partial. GH #305 closed with this evidence.

## 2. Phase Verification Summary

| Phase | Status | Score | Nyquist | Security | Notes |
|-------|--------|-------|---------|----------|-------|
| 9 — Retrieval Eval & Ranking Precision | ✅ passed | 3/3 | ✅ compliant | ✅ 0 open | 1 human-verify follow-up (#334) |
| 10 — Asymmetric Query/Doc Embeddings | ✅ passed | already-shipped | n/a (no plans) | ✅ 0 open | delivered under Phase 4; #305 closed |
| 11 — Async-on-Write Summaries | ✅ passed | 9/9 | ✅ compliant | ✅ 0 open | review-fixed; residuals → #335 |
| 12 — Per-Memory Usage Signals | ✅ passed | 3/3 | ✅ compliant | ✅ 0 open | process gap fixed inline (badd4a5d) |

All four phases have a `VERIFICATION.md` with `status: passed`. No unverified phases.

## 3. Cross-Phase Integration (subagent audit)

**Result: PASS — 11/11 links WIRED, 0 broken.**

| Link | Status | REQ-ID(s) |
|------|--------|-----------|
| `store.SearchReranked` ← MCP `deps.searchMemory` | ✅ WIRED | ranking-precision, similarity-scores |
| `store.SearchReranked` ← Connect `SearchMemories` (parity-tested) | ✅ WIRED | ranking-precision |
| `RerankHits` isolation from usage signals (D-08 invariant) | ✅ WIRED | ranking-precision × usage-signals |
| `summaryQueue`/`usageQueue` on shared `deps`, composed shutdown | ✅ WIRED | async-summaries, usage-signals |
| `serve.go` drains **after** `httpSrv.Shutdown` returns, sequentially | ✅ WIRED | async-summaries, usage-signals |
| `storeMemory`/`scheduleMemory` → `summaryQueue.tryEnqueue` post-Upsert | ✅ WIRED | async-summaries |
| `getMemory`/Connect `GetMemory` → `usageQueue.tryEnqueue` success-only | ✅ WIRED | usage-signals |
| Proto fields 19/20 → `memoryToProto`/`recallView` | ✅ WIRED | usage-signals |
| `gen/` committed, no drift (`git diff --exit-code -- gen/` clean) | ✅ WIRED | usage-signals |
| `ENGRAM_SUMMARY_ON_WRITE` / `ENGRAM_USAGE_SIGNALS` independent + validated | ✅ WIRED | async-summaries, usage-signals |
| Score field `store.Memory.Score` → both surfaces | ✅ WIRED | similarity-scores |

**Integration warnings (non-blocking, cosmetic):**
1. Phase 9 SUMMARY line-number refs drifted from current source (`tools.go:724`→actual `:834`). Docs-only; wiring correct.
2. `storeDiscovery`/`storeRule` intentionally skip the summary queue (per spec; `TestDiscoveryAndRuleNeverEnqueue`); a cross-reference comment already exists.

## 4. End-to-End Flows

| Flow | Status |
|------|--------|
| Store → async summarize → recall (write never blocked by summarizer) | ✅ COMPLETE |
| Search → rerank → score (MCP + Connect parity) | ✅ COMPLETE |
| Get → async usage-increment → visible on next read; update bumps sync | ✅ COMPLETE |
| SIGTERM → `httpSrv.Shutdown` → both queues drain → no panic | ✅ COMPLETE |

## 5. Nyquist & Security

- **Nyquist:** capability active. Phases 9/11/12 all `nyquist_compliant: true`
  (`VALIDATION.md` present). Phase 10 has no `VALIDATION.md` — expected, as an
  already-shipped verification-only phase with no plans/waves to validate. **Not a gap.**
- **Security:** Phases 9/11/12 (+10) each carry a `SECURITY.md` with
  `status: verified`, `threats_open: 0` at the ASVS-L1 block-on gate. Clean.

## 6. Tech Debt & Deferred (non-blocking)

All items below are **already triaged into tracked GitHub issues** or are systemic
pre-existing conditions — none block milestone completion.

| Area | Item | Tracked |
|------|------|---------|
| P9 eval | Prod-parity #261 rank-bar re-confirmation on qwen3-embedding-8b @4096 | GH #334 (blocked by #333) |
| P10 eval | Optional asymmetric-config retrieval-benefit demo | GH #334 |
| P11 review | Residuals WR-03 / IN-01 (dup depth gauge) / IN-02 | GH #335 |
| Embed subsystem | Configurable timeout / base-URL /v1 join / Gemini native | GH #333, #332, #331 |
| Lint (systemic) | 331 rumdl markdown failures in `.planning/`; `.rumdl.toml` lacks `.planning` exclude — Go deliverables clean | (unfiled — recommend `.rumdl.toml` fix) |
| Issue hygiene | Feature issues **#320** (P11) & **#317** (P12) still OPEN post-merge | close on complete |
| Env restore | repo-local `commit.gpgsign=false` still set; local `main` not synced | `git config --local --unset commit.gpgsign` |

**Explicitly deferred to v0.10.x (out of v0.9.x scope — not debt):** GH #322
(Connect write-lane + CSRF), GH #323 (session refresh-token rotation).

## 7. Verdict

Milestone **v0.9.x — Recall Quality** met its definition of done. All 6
requirements satisfied (3-source cross-referenced), all 4 phases verified passed,
cross-phase integration fully WIRED, all E2E flows COMPLETE, security clean, and
Nyquist compliant. Accumulated tech debt is entirely tracked/deferred and
non-blocking. **Ready to complete and archive.**

---

_Audited by `/gsd-audit-milestone` (Claude, gsd orchestrator) — 2026-07-11T00:18:05Z_
