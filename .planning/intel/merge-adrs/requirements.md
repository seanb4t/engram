# Requirements — merge-adrs companion set

All 31 ingest docs are ADRs (architectural decisions), not PRDs. No product requirements
documents were ingested, so no `REQ-*` entries are derived here. Implementation obligations
implied by these locked decisions are captured as constraints (see `constraints.md`) and as
decisions (see `decisions.md`).

Downstream note for gsd-roadmapper: these 31 companion decisions do not introduce new
product scope; they refine the *how* of scopes already governed by the 25 baseline locks in
`.planning/PROJECT.md` and the existing `.planning/REQUIREMENTS.md`. No requirement additions,
deletions, or acceptance-criteria changes are warranted from this set.
