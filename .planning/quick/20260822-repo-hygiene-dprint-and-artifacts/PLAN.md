---
quick_id: 260822-im2
slug: repo-hygiene-dprint-and-artifacts
date: 2026-08-22
description: "Exclude vendored SPA from dprint, fix remaining format drift, track phase-06 review artifacts, gitignore .mcp.json"
isolation: none
---

# Quick Task: Repo hygiene — dprint drift + untracked artifacts

Disposition for two non-blocking items from `.planning/2026-08-12.01-MILESTONE-AUDIT.md`.
Neither is milestone work; both are housekeeping the audit surfaced.

**Isolation note:** run sequentially on the branch, not in a harness worktree.
`workflow.use_worktrees` was `true`; per memory `8vmgdqf83p` harness worktree
isolation denies every external binary in this repo (`git`, `rg`, `ls` all fail
identically), which cost 243K tokens to diagnose once already. Set to `false`
for this task and restored afterward.

## Task 1 — dprint: exclude the vendored SPA, format the rest

`task fmt:check` exits 201 with 4 drifted files. They split into two kinds:

| File | Kind | Action |
|---|---|---|
| `internal/webauth/static/_app/version.json` | vendored build artifact, regenerated by `task ui:build` | add `internal/webauth/static` to `dprint.json` excludes |
| `ui/tsconfig.json` | authored | `dprint fmt` |
| `docs-site/package.json` | authored (Renovate-managed) | `dprint fmt` |
| `.claude/settings.json` | authored | `dprint fmt` |

Formatting the vendored bundle is wrong — the next `task ui:build` reverts it,
so it would re-drift forever. Excluding it is the fix.

**Note:** this drift is NOT milestone-introduced. Last commits to the four files
are 2026-06-02, 2026-06-11, 2026-06-11 and 2026-08-06; the milestone opened
2026-08-12. CI runs neither `fmt:check` nor `dprint` (no hits in
`.github/workflows/`), so this has never gated a PR and does not now.

## Task 2 — track the phase-06 review artifacts

Four untracked files:
`06-REVIEW.iter2.md`, `06-REVIEW.iter3.md`, `06-REVIEW-FIX.iter2.md`, `06-REVIEW-FIX.iter3.md`.

Precedent is decisive: phase 04 carries the identical four-file `iter2`/`iter3`
set and all four are tracked (`git ls-files '.planning/phases/*iter*'`). Both
phase-06 FIX reports read `status: all_fixed` (5/5 and 8/8). Commit them.

## Task 3 — gitignore `.mcp.json`

No secrets, but machine-specific and unshareable: absolute paths under
`/Users/sean/`, a version-pinned homebrew node binary, and a pnpm
content-hash store path. Committing it would break every other contributor and
churn on each node/pnpm upgrade. Add to `.gitignore`.

## Verification

- `task fmt:check` exits 0.
- `git status --porcelain` is empty.
- Control: dprint's exclude actually covers the vendored file — confirm
  `dprint check --list-different` no longer names it while still being able to
  name a deliberately-drifted control file.
