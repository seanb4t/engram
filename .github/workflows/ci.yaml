name: ci

on:
  pull_request: {}
  push:
    branches:
      - main

permissions:
  contents: read

# CI installs each gate via its official action (the runner is bare; the local
# `task lint`/`fmt` suite assumes the tools are already on PATH). `task` remains
# the local-dev entrypoint; these jobs mirror its gates with reliable installs.
#
# Release-please PRs (head branch `release-please--*`) only touch CHANGELOG /
# manifest / Chart.yaml / plugin.json, so the Go/python/license/actionlint jobs
# skip themselves via a job-level `if` — skipped jobs still satisfy required
# status checks (unlike workflow-level path filters, which leave them stuck in
# "Expected"). `chart` and `commit-lint` still run: helm lint validates the
# Chart.yaml bump and commit-lint validates the release PR title.
jobs:
  test:
    name: test
    if: ${{ !startsWith(github.head_ref, 'release-please--') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7
        with:
          go-version-file: go.mod
      - name: Test
        env:
          # Fail-closed real-Qdrant authz/parity gate (round-6 MED, Codex):
          # ubuntu-latest ships Docker, so the existing testcontainers boot
          # path provisions Qdrant; with this flag a boot/pull failure FAILS
          # the job instead of letting TestMain silently skip the integration
          # suite (see internal/server/tools_test.go requireQdrant/TestMain).
          ENGRAM_REQUIRE_QDRANT: "1"
        run: go test ./...
      # Enforce go.mod/go.sum tidiness here, not via a GoReleaser hook: release
      # builds use the exact tagged module files (tag = source of truth).
      - name: go.mod tidy check
        run: |
          go mod tidy
          git diff --exit-code go.mod go.sum
      - name: gofmt
        run: |
          unformatted="$(gofmt -l .)"
          if [ -n "$unformatted" ]; then
            echo "gofmt needed on:"; echo "$unformatted"; exit 1
          fi
      - uses: goreleaser/goreleaser-action@f06c13b6b1a9625abc9e6e439d9c05a8f2190e94 # v7
        with:
          version: "~> v2"
          args: check

  golangci:
    name: golangci-lint
    if: ${{ !startsWith(github.head_ref, 'release-please--') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7
        with:
          go-version-file: go.mod
      - uses: golangci/golangci-lint-action@ba0d7d2ec06a0ea1cb5fa41b2e4a3ab91d21278a # v9

  license:
    name: license headers
    if: ${{ !startsWith(github.head_ref, 'release-please--') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: apache/skywalking-eyes/header@61275cc80d0798a405cb070f7d3a8aaf7cf2c2c1 # v0.8.0

  chart:
    name: helm chart
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7
        with:
          go-version-file: go.mod
      - uses: azure/setup-helm@9bc31f4ebc9c6b171d7bfbaa5d006ae7abdb4310 # v5
      - name: helm lint
        run: helm lint charts/engram
      # Run the D-07/D-08 CronJob invariants + the D-09 engram.containerEnv
      # drift-pin on every PR. Task is installed via `go install` (mirroring the
      # actionlint job) since the runner is bare; without this step the
      # chart:validate guardrail never executes and containerEnv drift stays
      # green.
      - name: chart validate
        run: |
          go install github.com/go-task/task/v3/cmd/task@latest
          "$(go env GOPATH)/bin/task" chart:validate

  workflows:
    name: actionlint
    if: ${{ !startsWith(github.head_ref, 'release-please--') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7
        with:
          go-version-file: go.mod
      - name: actionlint
        run: |-
          go install github.com/rhysd/actionlint/cmd/actionlint@latest
          "$(go env GOPATH)/bin/actionlint"

  python:
    name: python
    if: ${{ !startsWith(github.head_ref, 'release-please--') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0
      - name: ruff
        run: |
          uv run --with ruff==0.16.0 ruff check skill/engram/hooks
          uv run --with ruff==0.16.0 ruff format --check skill/engram/hooks
      - name: pytest
        run: uv run --with pytest pytest skill/engram/hooks/tests -q

  buf:
    name: buf
    runs-on: ubuntu-latest
    if: ${{ !startsWith(github.head_ref, 'release-please--') }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7
        with:
          go-version-file: go.mod
      - name: buf lint
        run: go tool buf lint
      - name: buf breaking (vs main)
        run: go tool buf breaking --against 'https://github.com/seanb4t/engram.git#branch=main'
      - name: generated-code drift
        run: |
          go tool buf generate
          git diff --exit-code -- gen/ || (echo "gen/ is stale; run 'task proto:gen'"; exit 1)
      - name: vendored console gen client drift
        run: |
          rm -rf ui/src/lib/gen/engram ui/src/lib/gen/buf
          cp -R gen/ts/. ui/src/lib/gen/
          git diff --exit-code -- ui/src/lib/gen/ || (echo "ui/src/lib/gen/ is stale; run 'task proto:gen'"; exit 1)
      - name: idempotency ban (no side-effect-free RPC)
        run: |
          if grep -rEn 'idempotency_level[[:space:]]*=[[:space:]]*NO_SIDE_EFFECTS' proto/; then
            echo "::error::NO_SIDE_EFFECTS idempotency_level found in proto/ — see PITFALLS.md Pitfall 2"
            exit 1
          fi

  ui-drift:
    name: ui vendored-asset drift
    runs-on: ubuntu-latest
    if: ${{ !startsWith(github.head_ref, 'release-please--') }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      # `uses:` steps ignore working-directory, so point action-setup at the
      # ui packageManager field (pnpm@11) explicitly — the repo root has no
      # package.json for it to read.
      - uses: pnpm/action-setup@0ebf47130e4866e96fce0953f49152a61190b271 # v6
        with:
          package_json_file: ui/package.json
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: '26'
          cache: pnpm
          cache-dependency-path: ui/pnpm-lock.yaml
      - run: |
          cd ui && pnpm install --frozen-lockfile && pnpm build
          rm -rf ../internal/webauth/static && mkdir -p ../internal/webauth/static
          cp -R build/. ../internal/webauth/static/
      - id: drift
        run: |
          if git diff --exit-code internal/webauth/static/; then
            echo "drifted=false" >> "$GITHUB_OUTPUT"
          else
            echo "drifted=true" >> "$GITHUB_OUTPUT"
          fi
      # Renovate's postUpgradeTasks rule is inert on the homelab runner (#301),
      # so every Renovate ui/ bump lands here with drift. Self-heal instead of
      # failing on a same-repo Renovate PR branch: a GITHUB_TOKEN push would
      # never re-trigger `pull_request: synchronize` (GitHub's anti-recursion
      # guard), leaving the pushed SHA with zero check runs and stalling the
      # protect-main ruleset's required checks on "Expected" forever — see
      # 21-RESEARCH.md. A GitHub App installation token DOES re-trigger it.
      # continue-on-error degrades safely to the fail-with-guidance step below
      # when the App isn't provisioned (missing var/secret) or the guard
      # rejects the branch — both fall through, unchanged from today.
      - uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3
        id: app-token
        if: >-
          steps.drift.outputs.drifted == 'true' && github.event_name == 'pull_request' && startsWith(github.head_ref, 'renovate/')
          && github.actor == 'fzymgc-renovate[bot]' && github.event.pull_request.head.repo.full_name == github.repository
        continue-on-error: true
        with:
          client-id: ${{ secrets.CI_BOT_APP_CLIENT_ID }}
          private-key: ${{ secrets.CI_BOT_APP_PRIVATE_KEY }}
          permission-contents: 'write'
      # Derive the bot's git identity at runtime (app-slug output + bot user
      # id) rather than hardcoding a provisioning-time literal that doesn't
      # exist until a human creates the App.
      - id: get-user-id
        if: steps.app-token.outcome == 'success'
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token }}
          APP_SLUG: ${{ steps.app-token.outputs.app-slug }}
        # Assign on its own line so `set -e` (bash default for run:) aborts the
        # step if `gh api` fails — a nested $() inside echo's argument would let
        # echo exit 0 and emit an empty user-id, degrading the self-heal commit
        # to a malformed committer email. On failure here the self-heal step is
        # skipped (its `if` carries an implicit success()), so no bad commit is
        # pushed; the job goes red and is retryable.
        run: |
          user_id=$(gh api "/users/${APP_SLUG}[bot]" --jq .id)
          echo "user-id=$user_id" >> "$GITHUB_OUTPUT"
      - name: self-heal vendored SPA on renovate PR
        if: steps.app-token.outcome == 'success'
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token }}
          # Pass attacker-influenceable/derived context via env instead of
          # interpolating ${{ }} directly into the shell script — GitHub
          # Actions substitutes expressions as literal text before bash
          # parses the script, so a hostile branch/slug value interpolated
          # inline could break out of quoting and inject commands.
          HEAD_REF: ${{ github.head_ref }}
          REPO: ${{ github.repository }}
          APP_SLUG: ${{ steps.app-token.outputs.app-slug }}
          BOT_USER_ID: ${{ steps.get-user-id.outputs.user-id }}
        run: |
          gh auth setup-git
          # Commit onto the true PR head branch, not this job's checked-out
          # merge ref — a `pull_request` checkout is refs/pull/N/merge, and
          # pushing that HEAD would silently merge main into the Renovate
          # branch. Shallow-clone the actual head branch, copy the already-
          # rebuilt SPA into it, and commit + push from there instead.
          rm -rf /tmp/ui-drift-heal
          git clone --depth 1 --branch "$HEAD_REF" \
            "https://github.com/${REPO}.git" /tmp/ui-drift-heal
          rm -rf /tmp/ui-drift-heal/internal/webauth/static
          cp -R internal/webauth/static /tmp/ui-drift-heal/internal/webauth/
          cd /tmp/ui-drift-heal
          git config user.name "${APP_SLUG}[bot]"
          git config user.email "${BOT_USER_ID}+${APP_SLUG}[bot]@users.noreply.github.com"
          git add internal/webauth/static/
          git commit -m "fix(ui): regenerate vendored spa (renovate drift self-heal)"
          git push origin "HEAD:${HEAD_REF}"
      - name: fail with guidance (non-renovate drift)
        if: steps.drift.outputs.drifted == 'true' && steps.app-token.outcome != 'success'
        run: |
          echo "::error::vendored SPA is stale — run 'task ui:build' and commit"
          exit 1

  ui-test:
    name: ui tests
    runs-on: ubuntu-latest
    if: ${{ !startsWith(github.head_ref, 'release-please--') }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: pnpm/action-setup@0ebf47130e4866e96fce0953f49152a61190b271 # v6
        with:
          package_json_file: ui/package.json
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: '26'
          cache: pnpm
          cache-dependency-path: ui/pnpm-lock.yaml
      - run: cd ui && pnpm install --frozen-lockfile
      # Cache the Playwright browser binaries keyed on the resolved version so
      # warm runs skip the ~190MB Chromium download.
      - name: playwright version
        id: pw
        run: cd ui && echo "version=$(node -p 'require("playwright/package.json").version')" >> "$GITHUB_OUTPUT"
      - uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        id: pw-cache
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ steps.pw.outputs.version }}
      - run: cd ui && pnpm exec playwright install --with-deps chromium
        if: steps.pw-cache.outputs.cache-hit != 'true'
      - run: cd ui && pnpm exec playwright install-deps chromium
        if: steps.pw-cache.outputs.cache-hit == 'true'
      - run: cd ui && pnpm test

  commit-lint:
    name: commit-lint
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    permissions:
      pull-requests: read # action-semantic-pull-request reads PR metadata via GITHUB_TOKEN
    steps:
      - uses: amannn/action-semantic-pull-request@48f256284bd46cdaab1048c3721360e808335d50 # v6.1.1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
